---
milestone: v7.3
milestone_name: LLM Proxy Performance — Claude CLI Worker Pool
audited: 2026-06-21
status: tech_debt
scores:
  requirements: 14/14 satisfied (PERF-03 closed — SC-1 green + SC-2 red both live-proven)
  phases: 5/5 executed (64,65 passed; 62,63 live-discharged via Phase 65 operator runs; 66 gap-closed 66-03/04/05)
  integration: 14/14 wired (overhead chain integration_ok, 0 blockers, 2 carried advisories + 1 new warning)
  flows: 6/6 complete (added: pool-overhead → tile/table graded surface)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 66-dashboard-latency-observability
    items:
      - "WARNING (integration, NEW 2026-06-21): overhead_ms is omitted from the hourly JSON export (token-usage.ts exportToHourFile SELECT) AND from hydrateFromExports() INSERT — a DB wipe + hydrate-from-exports restores historical pool rows with overhead_ms=NULL, depressing/absenting p50_overhead_ms until new pool calls log. Live insert path is correct; only the reset/recovery path loses overhead. Candidate one-line follow-up: add overhead_ms to both the export SELECT and the hydrate INSERT."
      - "WARNING (integration, carried): getRecent caps at 500 rows server-side but the overhead tile requests limit=1000; overhead_ms rows are a SUBSET of those 500 (pool-served only), so under haiku-dominated traffic sonnet/opus overhead sample size can fall to single digits. Degrades gracefully to 'no recent pool calls'. Raising the proxy /recent cap needs no dashboard change."
      - "WARNING (doc, carried): 66-VERIFICATION.md artifact-table rows are stale — they predate BOTH the latency re-point AND the overhead chain (describe the pre-v4 summary?hours=1 fetch). 66-02/03/04/05 SUMMARYs document the current design correctly. Doc-only; regenerate via /gsd-verify-phase 66 if a clean verification artifact is wanted."
      - "No 66-SECURITY.md (security_enforcement=true). Both 66 threat models reached no 'high' (parameterized read-only query; pure render; the 66-05 LLM_PROXY_WORKER_SPAWN_DELAY_MS seam is opt-in test-only, no-op when unset). Run /gsd-secure-phase 66 for formal sign-off."
  - phase: "v7.3 milestone-wide"
    items:
      - "Nyquist: no *-VALIDATION.md exists for any of phases 62–66 (5 MISSING). Discovery-only; behaviour is live-proven by the Phase 65 operator suite (12/12 PASS) + unit suites (65/65 worker-pool, 7/7 token-usage). Run /gsd-validate-phase <N> per phase to fill formal coverage if desired."
      - "~24 open artifact items at audit time (debug_sessions, todos, uat_gaps, verification_gaps, context_questions) — mostly pre-existing cross-milestone debt (Feb–May), to be acknowledged-and-deferred at close."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: [62, 63, 64, 65, 66]
  overall: "all milestone phases lack VALIDATION.md (discovery-only flag; behaviour live-proven by Phase 65 operator suite + unit tests)"
---

# v7.3 Milestone Audit — LLM Proxy Performance (Claude CLI Worker Pool)

**Status: `tech_debt`** — All **14/14** requirements are satisfied, the full value chain (including the new spawn-overhead chain) is wired end-to-end with **zero blockers**, and all six E2E flows complete. The milestone is **archivable**. It carries accumulated non-blocking deferred items worth a review decision at close: one new export/hydrate gap, two carried integration advisories, missing Nyquist validation docs, an optional Phase 66 security sign-off, and pre-existing cross-milestone open artifacts.

> **Re-audit note (2026-06-21, post gap-closure):** The prior audit at 18:02 was `tech_debt` with PERF-03 **partial** — its live confirmation (SC-1 green, SC-2 red) was time-dependent and pending. Phase 66 gap plans **66-03 → 66-04 → 66-05** subsequently closed it: 66-03 added the per-call SPAWN/QUEUE `overhead_ms` metric (the latency the pool actually controls), 66-04 re-pointed both `:3032` surfaces to it, and 66-05 added the opt-in `LLM_PROXY_WORKER_SPAWN_DELAY_MS` test seam that made SC-2's red badge reproducible on any host. Both SC-1 (warm→green) and SC-2 (regression→red) are now live-proven via gsd-browser computed-rgb read-back. A fresh integration check of the **new overhead chain** (not covered by the 18:02 audit) returned `integration_ok`.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | REQUIREMENTS.md | VERIFICATION / Evidence | Final |
|--------|-------|-----------------|--------------------------|-------|
| POOL-01..04 | 62 | `[x]` Complete | live-discharged via Phase 65 operator run | **satisfied** |
| GUARD-01 | 62 | `[x]` Complete | live-discharged (65 escape-hatch SC) | **satisfied** |
| WLIFE-01..04 | 63 | `[x]` Complete | live-proven (63-05 `--live` + 65-01 SC-1..4) | **satisfied** |
| GUARD-02, GUARD-03 | 64 | `[x]` Complete | passed (unit-proven, 58 tests) | **satisfied** |
| PERF-01, PERF-02 | 65 | `[x]` Complete | passed (operator live-run 12/12) | **satisfied** |
| PERF-03 | 66 | `[x]` Complete | **SC-1 green + SC-2 red live-proven** (66-04/05; gsd-browser rgb evidence) | **satisfied** |

**14/14 satisfied. 0 partial, 0 unsatisfied, 0 orphaned.** The FAIL gate is not tripped.

## Integration (cross-phase) — `integration_ok`

The original latency value chain plus the **new overhead value chain** are both fully connected (integration check 2026-06-21, post-66-05):

`worker pool serves claude-code fallback (62/63)` → `worker-pool.mjs stamps overheadMs = firstOutputAt − dispatchedAt, EXCLUDING generation (worker-pool.mjs:706-708)` → `server.mjs logs overhead_ms alongside latency_ms, NULL for non-pool/direct/haiku paths (server.mjs:1680)` → `token-usage.ts overhead_ms nullable column + NULL-safe p50_overhead_ms median (token-usage.ts:381/505/521/894-919)` → `/recent overhead_ms passthrough → LlmLatencyTile "LLM Pool Overhead" (filters Number.isFinite, ≤3s/amber/red)` **and** `getSummary().by_model[].p50_overhead_ms → Token Usage "Spawn Overhead" column` (total-latency Median retained as forensic context).

- **6/6 E2E flows complete** (fallback-served call; GUARD-01 escape hatch; summary→drill-down; recent→latency tile; worker lifecycle; **pool-overhead→graded tile/table**).
- **0 dangling fields** on the live path; field names consistent across the repo boundary (`overheadMs` camelCase in-proxy ↔ `overhead_ms`/`p50_overhead_ms` snake_case in DB/dashboard, single translation at server.mjs:1680).
- **0 blockers.** Advisory #1 from the 18:02 audit (tile blended direct-OAuth + pool calls) is **RESOLVED** — the overhead metric is NULL for non-pool paths, so the median reflects only pool-served calls. Two carried advisories + one new warning remain (see `tech_debt`).

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 62 | missing | — | `/gsd-validate-phase 62` (optional) |
| 63 | missing | — | `/gsd-validate-phase 63` (optional) |
| 64 | missing | — | `/gsd-validate-phase 64` (optional) |
| 65 | missing | — | `/gsd-validate-phase 65` (optional) |
| 66 | missing | — | `/gsd-validate-phase 66` (optional) |

Discovery-only — the audit never auto-runs validation. Behaviour is live-proven by the Phase 65 operator suite + unit tests, so the missing VALIDATION.md docs are a formal-coverage gap rather than a behavioral risk.

## Verdict

v7.3 is **functionally complete, integration-sound, and archivable**. All 14 requirements are satisfied; PERF-03 — the single item that held the prior audit at `partial` — is now fully closed with live SC-1 (green) and SC-2 (red) proof. No critical blockers. The deferred items are all non-blocking and a reasonable accept-at-close set; the one worth a quick decision is the new `overhead_ms` export/hydrate gap (recovery-path only, ~one-line fix). None blocks archiving as `tech_debt` if the deferred items are accepted at close.
