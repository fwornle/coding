# Requirements Archive: v7.6 Experiment Harness

**Archived:** 2026-07-27
**Status:** SHIPPED

Requirements for this project are tracked inline in `ROADMAP.md` (no standalone `REQUIREMENTS.md`). This file captures the v7.6 requirement set at close for traceability.

---

# Milestone v7.6 Requirements — Experiment Harness

**Goal:** Harden the cross-agent `/experiment` harness so every agent launches and is measured under symmetric initial conditions, with a pre-flight gate that records clean skips and probe/ambient suppression that keeps the Runs view clean.

**Builds on (do NOT rebuild):** the v7.5 experiment runner (`experiment-runner.mjs`, `agent-headless.mjs`), v7.4 measurement substrate, and `bin/coding` proxy-routing seam. This milestone corrects harness-invocation correctness — it does not add new experiment features.

---

## v7.6 Requirements

- [x] **ALIGN-01** — Agent-invocation alignment: one source of truth for per-agent routing env + model resolution across cell launches and interactive-shell defaults. _Satisfied — `agent-routing.mjs`; runner + shell launcher both delegate; opencode/copilot model-id mismatches fixed._
- [x] **PREFLIGHT-01** — Per-agent pre-flight validation gate: bounded, fail-soft `/api/complete` round-trip before a cell burns a run; failure lands a clean recorded `skipped:<reason>` (not a mid-run abort, not a required-agent hard failure). _Satisfied — `preflightAgent` wired into `runMatrix`; `writeSkipRun` on failure._
- [x] **SUPPRESS-01** — Probe/ambient suppression: pre-flight and copilot drivability probes create no Runs-table row. _Satisfied — neutral `process:'experiment-preflight'` / no-`task_id` body by construction; leaky `probeCopilot()` removed; dashboard screenshot confirms no probe row._
- [x] **REVERIFY-01** — Real unattended re-run proving execute-or-clean-skip across all cells, confirmed e2e in the dashboard (not DB-query-only). _Satisfied — `88-rv88a.log`, `assert-cells.mjs` exit 0, `88-rv88a-runs.png`, operator APPROVED 2026-07-22._

**Outcome:** 4/4 satisfied and verified (10/10 must-haves in `88-VERIFICATION.md`, operator sign-off). No requirements adjusted or dropped. No orphaned requirements.
