---
phase: 72-syntactic-route-quality
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/experiments/goal-sentence.mjs
  - tests/experiments/goal-sentence.test.mjs
  - tests/fixtures/route/sample-PLAN.md
  - tests/fixtures/route/sample-ROADMAP.md
autonomous: true
requirements: [ROUTE-01]
must_haves:
  truths:
    - "A /gsd run's goal_sentence is derived from the active phase PLAN.md '**Goal**:' line (D-03)"
    - "When no PLAN.md exists, goal_sentence falls back to the ROADMAP phase '**Goal**:' line (D-03)"
    - "Extraction is pure zero-LLM string/regex — no LLM call"
    - "Missing/empty goal source yields an empty string (caller quarantines headless per D-05) — never throws"
  artifacts:
    - path: "lib/experiments/goal-sentence.mjs"
      provides: "deriveGoalSentence({ phase, planPath?, roadmapPath? }) zero-LLM extractor"
      contains: "deriveGoalSentence"
    - path: "tests/experiments/goal-sentence.test.mjs"
      provides: "PLAN.md/ROADMAP extraction + fallback + empty-source tests"
      contains: "node:test"
  key_links:
    - from: "lib/experiments/goal-sentence.mjs"
      to: "PLAN.md / ROADMAP.md '**Goal**:' line"
      via: "regex on **Goal**:"
      pattern: "Goal"
---

<objective>
Author the zero-LLM `goal_sentence` extraction helper for /gsd runs (ROUTE-01, D-03): read the active phase's `PLAN.md` `**Goal**:` line, fall back to the ROADMAP phase `**Goal**:` line when no PLAN.md exists. Pure string/regex extraction, no LLM, no measurement-stop.mjs edit (the wiring lands in Plan 05 to keep single-file ownership).

Purpose: ROUTE-01 needs `goal_sentence` POPULATED (the `SpanRecord.goal_sentence` field already exists). The /gsd auto-derive path is the most run-specific target. Freeform prompt/edit and headless quarantine paths already exist in `measurement-stop.mjs` (`prompt()` :94-103, quarantine :181-185) and are reused as-is by Plan 05 — this plan only supplies the /gsd extractor.

Output: `lib/experiments/goal-sentence.mjs` + a node:test suite with PLAN.md/ROADMAP fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-syntactic-route-quality/72-CONTEXT.md
@.planning/phases/72-syntactic-route-quality/72-RESEARCH.md
@.planning/phases/72-syntactic-route-quality/72-PATTERNS.md

<interfaces>
<!-- The '**Goal**:' line shape this helper extracts. Confirmed live: -->
ROADMAP.md Phase 72 line:  **Goal**: Every run carries a goal sentence and deterministic, zero-LLM route-quality metrics ...
PLAN.md frontmatter/body Goal line uses the same '**Goal**:' marker (per CONTEXT D-03).

<!-- Existing seams in measurement-stop.mjs that Plan 05 will wire this into (DO NOT edit here): -->
measurement-stop.mjs:136-139 — parseStrArg(args,'--goal') / parseStrArg(args,'--phase') arg seam
measurement-stop.mjs:94-103 — prompt() TTY helper (freeform D-04)
measurement-stop.mjs:181-185 — headless quarantine path (empty goal + pending=true, D-05)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: deriveGoalSentence extractor (PLAN.md primary, ROADMAP fallback) — RED→GREEN</name>
  <read_first>
    - tests/experiments/taxonomy.test.mjs (node:test convention for tests/experiments/ — the analog)
    - .planning/ROADMAP.md:115-122 (the real '**Goal**:' line shape for Phase 72)
    - 72-RESEARCH.md §"goal_sentence population (ROUTE-01)" (D-03/D-04/D-05 paths)
    - scripts/measurement-stop.mjs:136-139 (the --goal/--phase arg seam Plan 05 reuses; do NOT edit)
  </read_first>
  <behavior>
    - deriveGoalSentence reading a fixture PLAN.md with '**Goal**: Foo bar.' → 'Foo bar.'
    - PLAN.md absent but ROADMAP fixture has the matching phase '**Goal**: Baz.' → 'Baz.' (fallback)
    - both sources absent/empty → '' (empty string, never throws — caller quarantines headless per D-05)
    - the '**Goal**:' regex tolerates surrounding whitespace and a trailing period; returns the trimmed sentence
    - extraction performs NO network/LLM call (pure fs.readFileSync + regex)
  </behavior>
  <action>
    Create `lib/experiments/goal-sentence.mjs` (pure ESM). Export `deriveGoalSentence({ phase, planPath, roadmapPath })`: (1) if `planPath` exists on disk, read it and regex-match the first `**Goal**:` line, returning the trimmed remainder; (2) else if `roadmapPath` exists, read it, locate the `### Phase {phase}:` heading block and regex-match the `**Goal**:` line within it; (3) else return `''`. Use a tolerant regex (e.g. `/^\s*\*\*Goal\*\*:\s*(.+?)\s*$/m`). Wrap reads in try/catch returning `''` on any failure — never throw (the close must not hard-block, D-05). Import `fs` from `node:fs`, `process` from `node:process`. `process.stderr.write` only for diagnostics — never `console.*` (CLAUDE.md no-console-log). Also create the two fixtures: `tests/fixtures/route/sample-PLAN.md` (with a `**Goal**:` line) and `tests/fixtures/route/sample-ROADMAP.md` (with a `### Phase 72:` block + `**Goal**:` line). Write `tests/experiments/goal-sentence.test.mjs` (node:test + node:assert/strict) covering all behavior cases above. Run RED then GREEN.
  </action>
  <verify>
    <automated>node --test tests/experiments/goal-sentence.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/goal-sentence.test.mjs` exits 0
    - `grep -n "deriveGoalSentence" lib/experiments/goal-sentence.mjs` shows the export
    - `grep -n "Goal" lib/experiments/goal-sentence.mjs` confirms the '**Goal**:' regex marker
    - `grep -c "console\." lib/experiments/goal-sentence.mjs` returns 0
    - `grep -rn "fetch\|http\|llm\|complete" lib/experiments/goal-sentence.mjs | grep -v '^#'` returns nothing (zero-LLM, no network)
    - test asserts the empty-source case returns '' without throwing
  </acceptance_criteria>
  <done>deriveGoalSentence extracts PLAN.md '**Goal**:' with ROADMAP fallback, returns '' on missing source without throwing, zero LLM/network, zero console.*; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → deriveGoalSentence | reads local PLAN.md/ROADMAP.md (project-owned, not attacker-controlled), regex-extracts one line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-PATH | Tampering | planPath/roadmapPath args | accept | Paths are supplied by the caller (the close orchestrator), not from untrusted trace content. Caller passes repo-relative project paths; no `task_id`-derived filename construction here (Pitfall: filename traversal is owned by measurement-span sanitizeTaskId). |
| T-72-02-DOS | Denial of Service | regex over a large PLAN/ROADMAP | mitigate | Linear `/m` line match, no catastrophic backtracking in the anchored pattern; read wrapped in try/catch returning '' on failure so a close never hard-blocks (D-05). |
| T-72-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (stdlib fs/regex only). No checkpoint needed. |
</threat_model>

<verification>
- `node --test tests/experiments/goal-sentence.test.mjs` green.
- Empty-source case returns '' without throwing.
- No LLM/network call, no console.*.
</verification>

<success_criteria>
- /gsd goal_sentence derives from PLAN.md '**Goal**:' with ROADMAP fallback (ROUTE-01, D-03), zero-LLM.
- Missing source yields '' (caller quarantines headless, D-05), never throws.
</success_criteria>

<output>
Create `.planning/phases/72-syntactic-route-quality/72-02-SUMMARY.md` when done
</output>
