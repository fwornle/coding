---
phase: 72-syntactic-route-quality
plan: 05
type: execute
wave: 4
depends_on: ["72-01", "72-02", "72-03", "72-04"]
files_modified:
  - lib/experiments/run-write.mjs
  - scripts/measurement-start.mjs
  - scripts/measurement-stop.mjs
  - scripts/experiments-recompute-route.mjs
  - tests/experiments/run-write.test.mjs
autonomous: false
requirements: [ROUTE-01, ROUTE-02]
must_haves:
  truths:
    - "writeRun writes the six heuristics as flat metrics on the Run.metadata, all null-able (D-09/D-02)"
    - "writeRun creates exactly ONE Route node per Run carrying the same six heuristics + goal_sentence, idempotent on re-close (D-09)"
    - "the Run--tookRoute-->Route edge uses a stable key so N re-closes do NOT create N parallel edges (Pitfall 2)"
    - "measurement-start prompts the operator for the one-sentence goal at span creation when interactive freeform (no --goal, TTY) and passes it as startMeasurement's goal_sentence; headless (no TTY) → empty goal, never blocks (D-04/D-05)"
    - "measurement-stop populates/refines goal_sentence from PLAN.md (/gsd, D-03), prompt (freeform edit-at-close, D-04), or empty+pending (headless, D-05)"
    - "measurement-stop computes heuristics from the normalized trace between token aggregation and writeRun; null trace → all six null (D-02)"
    - "experiments-recompute-route.mjs re-derives heuristics for an existing Run via the idempotent writeRun path (D-14), opening the store via openExperimentStore (ontologyDir)"
  artifacts:
    - path: "lib/experiments/run-write.mjs"
      provides: "writeRun extended with heuristics arg → flat Run metrics + Route node + tookRoute edge"
      contains: "Route"
    - path: "scripts/measurement-start.mjs"
      provides: "freeform start-prompt for goal_sentence (D-04 start side), headless-safe (D-05)"
      contains: "goal_sentence"
    - path: "scripts/measurement-stop.mjs"
      provides: "step 3.5/3.6 trace+heuristics + goal_sentence population wiring"
      contains: "buildNormalizedTrace"
    - path: "scripts/experiments-recompute-route.mjs"
      provides: "on-demand recompute/backfill CLI (idempotent re-close)"
      contains: "openExperimentStore"
  key_links:
    - from: "scripts/measurement-stop.mjs"
      to: "lib/lsl/route/build-trace.mjs + lib/experiments/route-heuristics.mjs"
      via: "buildNormalizedTrace + computeHeuristics between :199 and :205"
      pattern: "computeHeuristics"
    - from: "scripts/measurement-stop.mjs"
      to: "lib/experiments/goal-sentence.mjs"
      via: "deriveGoalSentence (ROUTE-01 /gsd population path)"
      pattern: "deriveGoalSentence"
    - from: "scripts/measurement-start.mjs"
      to: "rapid-llm-proxy measurement-span startMeasurement"
      via: "TTY prompt → startMeasurement({ task_id, goal_sentence }) at span creation (D-04 start side)"
      pattern: "goal_sentence"
    - from: "lib/experiments/run-write.mjs"
      to: "experiment km-core store (Route node)"
      via: "putEntity entityType:'Route' + addRelation tookRoute with stable key"
      pattern: "tookRoute"
    - from: "scripts/experiments-recompute-route.mjs"
      to: "lib/experiments/store.mjs"
      via: "openExperimentStore() (mandatory ontologyDir)"
      pattern: "openExperimentStore"
---

<objective>
Wire the full goal+heuristics path. ROUTE-01 has TWO wiring sides per D-04: (a) the freeform START prompt in `scripts/measurement-start.mjs` that populates `SpanRecord.goal_sentence` at span creation, and (b) the CLOSE-side population/refinement in `scripts/measurement-stop.mjs` (/gsd PLAN.md derive, freeform edit-at-close, headless quarantine). On the close side this plan also extends `writeRun` to persist the six heuristics flat on the Run plus exactly one idempotent Route node per Run (D-09), computes heuristics from the normalized trace at the confirmed insertion point (steps 3.5/3.6), and adds the idempotent recompute/backfill CLI (rides Phase-71 D-14). A blocking human-verify checkpoint proves a real /gsd run close lands the goal + six strict heuristics + a Route node, and a coarser/untraceable run writes `null` (not 0).

Purpose: This is the integration plan that turns the readers + heuristics + goal extractor into a live measurement. It owns both operator span CLIs (`measurement-start.mjs` start prompt, `measurement-stop.mjs` close) and the experiment store write path exclusively (no parallel conflict).

Note on D-04 start-side wiring (revision): the proxy `startMeasurement` (`/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/measurement-span.ts:173-196`) already ACCEPTS an optional `goal_sentence` param and stores it on the span, but it does NOT prompt — the proxy stays generic (Phase-70 principle), so the prompt lives coding-side in `scripts/measurement-start.mjs`. No proxy submodule change is required (ROUTE-01 does not need it); therefore no `npm run build` of the proxy submodule is in scope. The start CLI currently only accepts an explicit `--goal` arg (`scripts/measurement-start.mjs:43,56`); this plan adds the interactive freeform prompt fallback there.

Output: a freeform start-prompt in `measurement-start.mjs`, extended `writeRun` + `measurement-stop.mjs`, a new recompute CLI, extended `run-write.test.mjs`, and a live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-syntactic-route-quality/72-CONTEXT.md
@.planning/phases/72-syntactic-route-quality/72-RESEARCH.md
@.planning/phases/72-syntactic-route-quality/72-PATTERNS.md

<interfaces>
<!-- Plan 01/02/03/04 outputs this plan consumes: -->
import { buildNormalizedTrace } from '../lib/lsl/route/build-trace.mjs'        // Plan 04 — span → RouteEvent[]|null
import { computeHeuristics, ALL_NULL_HEURISTICS } from '../lib/experiments/route-heuristics.mjs'  // Plan 01
import { deriveGoalSentence } from '../lib/experiments/goal-sentence.mjs'      // Plan 02 — PLAN.md/ROADMAP '**Goal**:'

<!-- measurement-start.mjs CURRENT shape to extend (read in full) — the D-04 START side: -->
scripts/measurement-start.mjs:
  :34-38  parseStrArg(argv, flag) — the --goal/--task-id arg parser
  :42-43  const taskId = parseStrArg(args, '--task-id'); const goal = parseStrArg(args, '--goal');
  :51-52  import { startMeasurement, resolveMeasurementPaths } from the LOCAL proxy dist (measurement-span.js)
  :56     span = startMeasurement({ task_id: taskId, ...(goal ? { goal_sentence: goal } : {}) });  // INSERT the prompt fallback so goal_sentence is populated when goal arg absent + TTY

<!-- proxy startMeasurement contract (already accepts goal_sentence — proxy stays generic, no edit): -->
measurement-span.ts:173-196 — startMeasurement({ task_id, goal_sentence?, meta? }) writes goal_sentence onto the span only when truthy (:189). The coding-side CLI is responsible for SOURCING goal_sentence (arg or prompt).

<!-- run-write.mjs CURRENT shape to extend (read in full): -->
export async function writeRun(store, { span, taskClass, pending, tags, totals }) { ... }
  :54-60  idempotent Run lookup by metadata.task_id (mintEntityId, NEVER span.task_id — Pitfall 1)
  :62-69  synthetic provenance object (reuse for the Route node)
  :82-97  Run.metadata object (add six heuristic keys here, all null-able)
  :100-126 Outcome-stub idempotent pattern → REPLICATE for the Route node (lookup by metadata.run_task_id, mint/reuse id)
  :128-138 addRelation with STABLE key `${from}:${type}:${to}` (Pitfall 2 — the tookRoute edge MUST do the same)

<!-- measurement-stop.mjs insertion seams (read in full) — the D-03/D-04 CLOSE side: -->
  :136-138 parseStrArg(args,'--goal') / parseStrArg(args,'--phase')  — /gsd derive seam
  :139     deriveText = [span.goal_sentence, goalArg, phaseArg].filter(Boolean).join(' ')
  :188-189 const { totals, byAgentModel } = aggregateByTaskId(span.task_id); const dominant = byAgentModel[0] ?? {};
  :199     end of token aggregation / tags
  :202-209 openExperimentStore() try { writeRun(...) } finally { store.close() }   — INSERT 3.5/3.6 before :205
  :94-103  prompt() TTY helper (freeform edit-at-close, D-04)
  :181-185 headless quarantine (empty goal + pending=true, D-05)

<!-- store open — MANDATORY ontologyDir pattern (CLAUDE.md), acceptance grep required: -->
import { openExperimentStore } from '../lib/experiments/store.mjs'  // the ONLY constructor passing ontologyDir
const store = await openExperimentStore(); try { ... } finally { await store.close(); }  // single-owner LevelDB

<!-- experiment ontology: Route class declares relationships hasStep:["Step"], properties:{}. Run.relationships.tookRoute:["Route"] EXISTS. -->
<!-- Heuristics ride in metadata — NO ontology edit required (A5; km-core strict validates entityType, not metadata keys). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend writeRun — six flat Run metrics + one idempotent Route node + tookRoute edge</name>
  <read_first>
    - lib/experiments/run-write.mjs (FULL — the Outcome-stub pattern at :100-138 is the exact Route-node analog)
    - tests/experiments/run-write.test.mjs (FULL — node:test isolation harness; EIGHT_TAGS pattern; extend it)
    - .data/ontologies-experiment/experiment-ontology.json (Route class + Run.tookRoute relationship — verify on disk)
    - 72-PATTERNS.md §"lib/experiments/run-write.mjs (EXTEND)" (the exact Route node + edge code shape)
    - 72-RESEARCH.md §"Storage shape (D-09)" + Pitfall 1 (mintEntityId) + Pitfall 2 (stable edge key)
  </read_first>
  <behavior>
    - writeRun({ ..., heuristics }) writes the six heuristic keys (loop_count, edit_revert_count, redundant_read_count, abandoned_tool_count, total_step_count, wallclock_per_step) onto Run.metadata, preserving null values (D-02 — null stays null, not coerced to 0)
    - exactly one Route node exists per Run after writeRun: entityType:'Route', name `${task_id}-route`, description = goal_sentence, metadata carries run_task_id + the six heuristics + goal_sentence
    - re-running writeRun for the same task_id UPDATES the same Route node (idempotent lookup by metadata.run_task_id) — never a duplicate Route
    - the Run--tookRoute-->Route edge uses key `${runId}:tookRoute:${routeId}`; two writeRun calls produce exactly ONE tookRoute edge (no parallel-edge explosion)
    - a writeRun called with heuristics=ALL_NULL_HEURISTICS writes all six as null on BOTH Run and Route (D-02)
  </behavior>
  <action>
    Extend `writeRun(store, { span, taskClass, pending, tags, totals, heuristics })` in `lib/experiments/run-write.mjs`. (1) Add the six heuristic keys to the Run `metadata` object (:82-97), defaulting via `heuristics ?? ALL_NULL_HEURISTICS`-equivalent and preserving `null` (do NOT `?? 0` — null is meaningful per D-02). (2) After the Outcome stub, REPLICATE the Outcome idempotent pattern (:100-126) for a Route node: lookup an existing Route by `metadata.run_task_id === span.task_id` (only when `existingId` is set), then `putEntity({ id: existingRouteId ?? mintEntityId(), name: \`${span.task_id}-route\`, entityType: 'Route', layer:'evidence', description: span.goal_sentence ?? '', metadata: { run_task_id: span.task_id, ...heuristics, goal_sentence: span.goal_sentence ?? '' } }, { provenance })` — NEVER `span.task_id` as the id (Pitfall 1). (3) `addRelation({ type:'tookRoute', from: runId, to: routeId, key: \`${runId}:tookRoute:${routeId}\` })` — the stable key is MANDATORY (Pitfall 2). Reuse the SAME synthetic `provenance` object (:62-69). Do NOT edit the ontology (A5 — heuristics ride in metadata). `process.stderr.write` only — never `console.*`. Extend `tests/experiments/run-write.test.mjs` (node:test) with: six-flat-metrics-on-Run, one-Route-node, idempotent-re-close-no-dup-Route, single-tookRoute-edge-after-two-closes, and the all-null path. Gate any live-only assertion on `EXPERIMENTS_LIVE` (env var, NOT --live argv — MEMORY.md node --test argv gotcha).
  </action>
  <verify>
    <automated>node --test tests/experiments/run-write.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/run-write.test.mjs` exits 0 incl. the new Route + flat-metrics + idempotency blocks
    - `grep -n "entityType: 'Route'\|tookRoute" lib/experiments/run-write.mjs` confirms the Route node + edge
    - `grep -n "mintEntityId" lib/experiments/run-write.mjs` confirms the Route id is minted (never span.task_id)
    - `grep -n "key: \`\${runId}:tookRoute" lib/experiments/run-write.mjs` confirms the stable edge key (Pitfall 2)
    - `grep -c "console\." lib/experiments/run-write.mjs` returns 0
    - test asserts re-close produces exactly ONE Route node and ONE tookRoute edge (idempotency); null heuristics stay null
  </acceptance_criteria>
  <done>writeRun persists six flat Run metrics + one idempotent Route node + a stable-keyed tookRoute edge; null preserved; re-close dedupes; zero console.*; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire goal_sentence — START prompt (measurement-start.mjs, D-04) + CLOSE population/heuristics (measurement-stop.mjs) + recompute CLI</name>
  <read_first>
    - scripts/measurement-start.mjs (FULL — :34-38 parseStrArg, :42-43 task-id/goal args, :51-56 startMeasurement call; the START-side D-04 prompt insertion point is :56)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/measurement-span.ts:173-196 (startMeasurement already accepts goal_sentence; proxy stays generic — coding-side sources the value)
    - scripts/measurement-stop.mjs (FULL — insertion seams at :136-139, :188-189, :199-205; prompt :94-103; quarantine :181-185)
    - lib/experiments/goal-sentence.mjs (Plan 02 — deriveGoalSentence)
    - lib/lsl/route/build-trace.mjs (Plan 04 — buildNormalizedTrace)
    - lib/experiments/route-heuristics.mjs (Plan 01 — computeHeuristics + ALL_NULL_HEURISTICS)
    - scripts/backfill-task-id-by-timestamp.mjs (FULL — the recompute CLI analog: --dry-run, entry-point guard, store open/close)
    - 72-PATTERNS.md §"scripts/measurement-stop.mjs (EXTEND)" + §"scripts/experiments-recompute-route.mjs"
  </read_first>
  <action>
    (A — D-04 START side, in `scripts/measurement-start.mjs`): when `--goal` is ABSENT, populate `goal_sentence` before calling `startMeasurement` (:56). If `process.stdin.isTTY` is true (interactive freeform run), prompt the operator for the one-sentence goal — reuse the same readline/`prompt()`-style TTY helper convention measurement-stop.mjs:94-103 uses (a single-line readline question), trim the answer, and pass it as `goal_sentence` into `startMeasurement({ task_id: taskId, ...(goalSentence ? { goal_sentence: goalSentence } : {}) })`. If `process.stdin.isTTY` is FALSE (headless/cron — D-05) OR the operator submits an empty line, do NOT prompt/do NOT block: call `startMeasurement` with no `goal_sentence` (the span is created with an empty goal; the close-side quarantine path at measurement-stop.mjs:181-185 sets the pending flag at write time). Never hang waiting for input (D-05). Do NOT touch the proxy submodule — `startMeasurement` already accepts `goal_sentence` (measurement-span.ts:189), so no `npm run build` of the proxy is needed. `process.stderr.write`/`process.stdout.write` only — never `console.*`.
    (B — D-03/D-04 CLOSE side, in `scripts/measurement-stop.mjs`): populate/refine `goal_sentence` BEFORE writeRun: for a /gsd run (when `--phase` arg present OR a PLAN.md is locatable), call `deriveGoalSentence({ phase: phaseArg, planPath, roadmapPath })` and assign it onto `span.goal_sentence` if not already set (D-03 — PLAN.md primary, ROADMAP fallback, zero-LLM). Freeform interactive: reuse the existing `prompt()` (:94-103) to confirm/EDIT the goal at close — pre-filled with the start-prompt value the START side captured (D-04 edit-at-close refines the start goal). Headless with no goal: leave `goal_sentence` empty + `pending=true` via the existing quarantine path (:181-185) — never block (D-05).
    (C — heuristics): insert steps 3.5/3.6 between :199 and :205: `import { buildNormalizedTrace } from '../lib/lsl/route/build-trace.mjs'` and `import { computeHeuristics, ALL_NULL_HEURISTICS } from '../lib/experiments/route-heuristics.mjs'`; `const trace = await buildNormalizedTrace(span, { dominantAgent: dominant.agent }); const heuristics = trace ? computeHeuristics(trace) : ALL_NULL_HEURISTICS;` (D-02 null). (D) Pass `heuristics` into the existing `writeRun(store, { span, taskClass, pending, tags, totals, heuristics })` call (:205). Keep all output on `process.stdout.write`/`process.stderr.write` (header :33-35) — never `console.*`.
    (E — recompute CLI): create `scripts/experiments-recompute-route.mjs`: open the store via `openExperimentStore()` ONLY (mandatory `ontologyDir`, CLAUDE.md), in a `try { ... } finally { await store.close(); }` (single-owner LevelDB, Pitfall 5); accept `<task_id>` + `--dry-run`; find the Run by `metadata.task_id` scan (run-write.mjs:54-60), read the archived span from `.data/measurements/<task_id>.json`, rebuild the trace via `buildNormalizedTrace`, recompute heuristics, and call the SAME extended `writeRun` (idempotent — updates the SAME Run + Route, D-14). Add the entry-point guard (`import.meta.url === pathToFileURL(process.argv[1]).href`) so importing for tests does not run `main()`. `process.stderr.write`/`process.stdout.write` only.
  </action>
  <verify>
    <automated>node --check scripts/measurement-start.mjs && node --check scripts/measurement-stop.mjs && node --check scripts/experiments-recompute-route.mjs && node --test tests/experiments/run-write.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/measurement-start.mjs`, `node --check scripts/measurement-stop.mjs`, `node --check scripts/experiments-recompute-route.mjs` all exit 0
    - `grep -n "isTTY" scripts/measurement-start.mjs` confirms the headless guard (D-05 — only prompt when TTY)
    - `grep -n "goal_sentence" scripts/measurement-start.mjs` confirms the prompted value is passed into startMeasurement (D-04 start side)
    - `grep -n "buildNormalizedTrace\|computeHeuristics" scripts/measurement-stop.mjs` confirms steps 3.5/3.6 wired
    - `grep -n "deriveGoalSentence" scripts/measurement-stop.mjs` confirms ROUTE-01 /gsd goal population
    - `grep -n "heuristics" scripts/measurement-stop.mjs` confirms heuristics passed into writeRun
    - `grep -n "openExperimentStore" scripts/experiments-recompute-route.mjs` confirms the mandatory ontologyDir store-open pattern (NEVER `new GraphKMStore` inline)
    - `grep -n "import.meta.url" scripts/experiments-recompute-route.mjs` confirms the entry-point guard
    - `grep -c "console\." scripts/measurement-start.mjs scripts/measurement-stop.mjs scripts/experiments-recompute-route.mjs` returns 0 for all three
  </acceptance_criteria>
  <done>measurement-start prompts for the goal at span creation (TTY freeform) and stays headless-safe (D-04/D-05); measurement-stop derives/refines goal_sentence (PLAN.md/prompt/quarantine) and computes heuristics from the trace before writeRun; recompute CLI opens via openExperimentStore and rides the idempotent re-close; zero console.*; syntax-checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification — real /gsd close lands goal + six heuristics + Route node; freeform start-prompt populates the goal; untraceable run writes null</name>
  <what-built>
    The start CLI now prompts for the one-sentence goal at span creation for interactive freeform runs (headless never blocks). A real /gsd run close now: (1) derives goal_sentence from the active phase PLAN.md '**Goal**:' line (or refines the start-prompt value), (2) builds a normalized cross-agent trace, (3) computes the six strict heuristics, and (4) writes them flat on the Run + a single Route node (Run--tookRoute-->Route) in the experiment store. A coarser/untraceable run writes null (not 0) for the unsupported heuristics.
  </what-built>
  <how-to-verify>
    1. Freeform start-prompt (D-04 start side): run `node scripts/measurement-start.mjs --task-id verify-freeform-$(date +%s)` WITHOUT `--goal` in an interactive terminal — confirm it PROMPTS for the one-sentence goal, then inspect `.data/active-measurement.json` and confirm `goal_sentence` carries the entered text. Then run the same command piped from /dev/null (`node scripts/measurement-start.mjs --task-id verify-headless-$(date +%s) < /dev/null`) — confirm it does NOT block/hang and creates the span with no goal (D-05).
    2. Run a real close on a /gsd run (Claude session present) with a phase that has a PLAN.md:
       `node scripts/measurement-stop.mjs --phase 72 --task-class debug` (or close an active span normally).
       Expect stdout: a close summary line; stderr: `[experiments] writeRun ...`.
    3. Query the experiment store and confirm the new Run + Route:
       `node scripts/experiments-query.mjs` (or the project's experiment query CLI) — confirm the Run.metadata carries a non-empty goal_sentence (matching the PLAN.md '**Goal**:' line) AND six numeric heuristics (total_step_count > 0, wallclock_per_step a number), and exactly ONE Route node exists for that task_id carrying the same six heuristics + goal_sentence.
    4. Re-run the SAME close (`node scripts/experiments-recompute-route.mjs <task_id>`) and re-query — confirm STILL exactly ONE Route node and ONE tookRoute edge (idempotency, no duplicate / no parallel-edge explosion).
    5. D-02 null proof: close a run whose agent trace cannot be located (e.g. a freeform run with no matching session file, or a stubbed dominantAgent) — confirm all six heuristics are written as null (NOT 0) on both the Run and the Route node.
  </how-to-verify>
  <action>Operator-driven verification only — no code changes. Execute the steps in <how-to-verify> against the live experiment store and confirm the start-prompt, Run + Route + idempotency + null-not-zero behavior.</action>
  <resume-signal>Type "approved" once the freeform start-prompt populates goal_sentence (and headless does not block), the live Run shows a PLAN.md-derived goal_sentence + six strict heuristics + one idempotent Route node, and the untraceable run shows null (not 0); or describe what differs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator stdin → start CLI | the prompted goal_sentence is operator-entered free text written onto the span (single-operator, low trust risk) |
| close orchestrator → experiment km-core store | strict-path writes (entityType validated) cross here; provenance is synthetic, not LLM |
| archived span file → recompute CLI | the run time-window + goal_sentence are read from `.data/measurements/<task_id>.json` |
| agent trace files → heuristics | reached transitively via buildNormalizedTrace (gates owned by Plans 03/04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05-HANG | Denial of Service | headless/cron start CLI blocking on a TTY prompt that never gets input | mitigate | Prompt ONLY when `process.stdin.isTTY` (D-05); headless → no prompt, empty goal, span created immediately; never hang waiting for input. Acceptance grep for `isTTY`. |
| T-72-05-ONTO | Tampering | km-core store open without ontologyDir → silent entityType validation no-op | mitigate | `openExperimentStore()` is the ONLY store-open path in measurement-stop.mjs AND the recompute CLI — it passes the mandatory `ontologyDir` (store.mjs:43-50). Acceptance grep for `openExperimentStore` in the recompute CLI; `new GraphKMStore` inline is forbidden (CLAUDE.md). |
| T-72-05-LOCK | Denial of Service | two experiment CLIs holding the single-owner LevelDB | mitigate | open-on-demand + `try/finally` close (measurement-stop.mjs:202-209 pattern reused in the recompute CLI); documented "don't run two experiment CLIs concurrently" (Pitfall 5). |
| T-72-05-PII | Information disclosure | secrets/PII from raw tool-call inputs leaking onto the Route node | mitigate | The Route node stores the six heuristics + goal_sentence + run_task_id only — NOT raw tool inputs. RouteEvents carry inputs_digest (sha256) + target_path, never raw args (research: store inputs_digest, not raw inputs). goal_sentence is a PLAN.md/ROADMAP line or operator-entered sentence (not trace content). |
| T-72-05-ID | Spoofing | task_id used as a km-core entity id | mitigate | Route node id is `mintEntityId()` on first write, reused via metadata.run_task_id lookup on re-close — NEVER span.task_id (parseEntityId requires UUIDv7, Pitfall 1). |
| T-72-05-EDGE | Tampering | parallel-edge explosion on re-close | mitigate | tookRoute edge uses stable key `${runId}:tookRoute:${routeId}` (Pitfall 2). |
| T-72-05-DOS | Denial of Service | malformed trace crashing the close at run-end | mitigate | buildNormalizedTrace returns null on unreadable trace → ALL_NULL_HEURISTICS → close still completes (the readers' per-line try/catch is the line-level gate; D-02 is the run-level fallback). |
| T-72-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (in-repo + stdlib + already-pinned km-core); no proxy submodule build (startMeasurement already accepts goal_sentence). No checkpoint needed. |
</threat_model>

<verification>
- `node --test tests/experiments/run-write.test.mjs` green (Route node + flat metrics + idempotency + null path).
- `node --check` clean on measurement-start.mjs, measurement-stop.mjs, and experiments-recompute-route.mjs.
- measurement-start prompts only when TTY (isTTY grep) and passes the prompted goal_sentence to startMeasurement; headless never blocks.
- Recompute CLI opens via openExperimentStore (ontologyDir grep) and has an entry-point guard.
- No console.* in any touched file.
- Live: a real /gsd close lands a PLAN.md-derived goal + six strict heuristics + one idempotent Route node; the freeform start-prompt populates goal_sentence; untraceable run → null (not 0).
</verification>

<success_criteria>
- ROUTE-01: each run carries a goal_sentence — populated at START (freeform prompt, D-04 / headless quarantine, D-05) and at CLOSE (/gsd PLAN.md derive D-03 / freeform edit-at-close D-04 / headless quarantine D-05) — stored on the Run + Route.
- ROUTE-02: six deterministic heuristics computed per run, stored flat on the Run (queryable alongside tags) + on one Route node (D-09).
- Idempotent re-close + backfill via the recompute CLI (D-14); null (not 0) when trace unavailable (D-02).
</success_criteria>

<output>
Create `.planning/phases/72-syntactic-route-quality/72-05-SUMMARY.md` when done
</output>
