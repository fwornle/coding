---
phase: 72-syntactic-route-quality
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/lsl/route/route-event.mjs
  - lib/experiments/route-heuristics.mjs
  - tests/experiments/route-heuristics.test.mjs
  - tests/fixtures/route/loop.json
  - tests/fixtures/route/edit-revert.json
  - tests/fixtures/route/redundant-read.json
  - tests/fixtures/route/abandoned.json
  - tests/fixtures/route/parallel-same-turn.json
  - tests/fixtures/route/true-negatives.json
autonomous: true
requirements: [ROUTE-02]
must_haves:
  truths:
    - "computeHeuristics(RouteEvent[]) returns the six metrics; each is a real signal under strict calibration (D-06)"
    - "computeHeuristics(null) returns ALL_NULL_HEURISTICS — every metric null, not 0 (D-02)"
    - "errored/denied/abandoned events all count toward total_step_count (D-07)"
    - "parallel same-turn tool calls are counted as separate steps, never collapsed (D-07)"
    - "every heuristic has at least one true-negative fixture proving the strict calibration (D-06/D-08)"
  artifacts:
    - path: "lib/lsl/route/route-event.mjs"
      provides: "RouteEvent jsdoc typedef, outcome enum, inputsDigest(input) sha256 helper"
      contains: "inputsDigest"
    - path: "lib/experiments/route-heuristics.mjs"
      provides: "computeHeuristics(trace) + ALL_NULL_HEURISTICS"
      contains: "ALL_NULL_HEURISTICS"
    - path: "tests/experiments/route-heuristics.test.mjs"
      provides: "one node:test block per heuristic + true-negative cases"
      contains: "node:test"
    - path: "tests/fixtures/route/loop.json"
      provides: "golden RouteEvent[] fixtures"
  key_links:
    - from: "lib/experiments/route-heuristics.mjs"
      to: "lib/lsl/route/route-event.mjs"
      via: "imports inputsDigest + RouteEvent typedef"
      pattern: "from.*route-event"
---

<objective>
Define the ONE cross-agent normalized `RouteEvent` schema (D-01) and the pure, zero-LLM `computeHeuristics()` function that turns an ordered `RouteEvent[]` into the six syntactic route-quality metrics (ROUTE-02), under strict/high-precision calibration (D-06) and per-heuristic null fallback (D-02). Backed by golden-trace fixtures (D-08).

Purpose: This is the deterministic floor under Phase 73's judge. A non-zero count must be unambiguous signal — strict definitions + fixture-tested true-negatives are the load-bearing decisions, not the storage shape. It is also the contract every Wave-2 reader emits against, so it lands first.

Output: `lib/lsl/route/route-event.mjs` (schema + `inputsDigest`), `lib/experiments/route-heuristics.mjs` (`computeHeuristics` + `ALL_NULL_HEURISTICS`), and a fully fixture-backed `node:test` suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-syntactic-route-quality/72-CONTEXT.md
@.planning/phases/72-syntactic-route-quality/72-RESEARCH.md
@.planning/phases/72-syntactic-route-quality/72-PATTERNS.md

<interfaces>
<!-- The RouteEvent schema (RESEARCH §Pattern 1) — this plan AUTHORS it. Every Wave-2 reader emits this shape. -->
RouteEvent fields (one per tool call; D-07 — parallel same-turn calls are SEPARATE events):
  seq: number            // 0-based ordinal; stable sort key, ties broken by tool_call_id
  tool_call_id: string   // Claude toolu_*, Copilot toolCallId, OpenCode callID
  tool_name: string      // 'Read' | 'Edit' | 'Bash' | ... agent-native, NOT normalized away
  inputs_digest: string  // sha256 of canonical JSON of the tool input args
  target_path: string|null   // resolved file_path for file tools; null otherwise
  started_at: string     // ISO-8601
  ended_at: string|null  // ISO-8601; null = abandoned
  outcome: 'success'|'error'|'denied'|'abandoned'
  agent: 'claude'|'copilot'|'opencode'

<!-- The zero-LLM helper style to mirror — pure num()/estimate() functions, no I/O. -->
From lib/lsl/token/claude-token-rows.mjs:56-63:
  export function estimateReasoningTokens(text) { return Math.max(1, Math.ceil((text?.length ?? 0) / 4)); }
  function num(v) { return typeof v === 'number' && Number.isFinite(v) ? v : 0; }
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Author route-event.mjs (RouteEvent schema + inputsDigest + outcome enum)</name>
  <read_first>
    - lib/lsl/token/token-db.mjs (shared-const/helper-module convention — the analog per PATTERNS.md)
    - lib/lsl/token/claude-token-rows.mjs:56-63 (pure zero-LLM helper style: num/estimate)
    - scripts/measurement-stop.mjs:190-192 (existing crypto.createHash('sha256') usage — mirror it)
    - 72-RESEARCH.md §"Pattern 1: Normalized RouteEvent" (the field-by-field schema)
  </read_first>
  <behavior>
    - inputsDigest({a:1}) === inputsDigest({a:1}) (stable for identical input)
    - inputsDigest({a:1}) !== inputsDigest({a:2})
    - inputsDigest(null) and inputsDigest(undefined) return a stable hex string (never throw)
    - exported OUTCOME enum/const set contains exactly: success, error, denied, abandoned
  </behavior>
  <action>
    Create `lib/lsl/route/route-event.mjs` as pure ESM (no build step). Export: (a) a `RouteEvent` jsdoc `@typedef` with the nine fields from the interfaces block above; (b) an `OUTCOMES` frozen const/enum with the four outcome strings; (c) `inputsDigest(input)` that canonicalizes the tool input via `JSON.stringify(input ?? null)` then `crypto.createHash('sha256').update(...).digest('hex')` — NEVER hand-roll a hash (V6). Import `crypto` from `node:crypto`, `process` from `node:process`. Logging via `process.stderr.write` only — never `console.*` (CLAUDE.md no-console-log; do not dodge by switching raw-write APIs). This module imports NOTHING from km-core or fs — it is a pure transform utility.
  </action>
  <verify>
    <automated>node --test tests/experiments/route-heuristics.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "inputsDigest" lib/lsl/route/route-event.mjs` shows the exported function
    - `grep -n "createHash('sha256')" lib/lsl/route/route-event.mjs` confirms stdlib hash (no hand-rolled hash)
    - `grep -c "console\." lib/lsl/route/route-event.mjs` returns 0
    - `node -e "import('./lib/lsl/route/route-event.mjs').then(m=>{if(m.inputsDigest({a:1})!==m.inputsDigest({a:1}))process.exit(1)})"` exits 0
  </acceptance_criteria>
  <done>route-event.mjs exports RouteEvent typedef, OUTCOMES enum, and a deterministic stdlib-sha256 inputsDigest; zero console.* usage.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Golden RouteEvent[] fixtures + computeHeuristics with strict definitions (RED→GREEN)</name>
  <read_first>
    - lib/lsl/route/route-event.mjs (the schema authored in Task 1)
    - 72-RESEARCH.md §"The Six Heuristics" (strict definitions table + notes — the authoritative spec)
    - tests/token-adapters/fixtures/ (per-agent sample-file convention for the fixture dir)
    - tests/experiments/run-write.test.mjs:19-25 (node:test + node:assert/strict import convention — NOT jest)
  </read_first>
  <behavior>
    - total_step_count: fixture of 5 events (1 error, 1 abandoned) → 5 (all outcomes count, D-07)
    - wallclock_per_step: fixture with known ISO timestamps → exact ms/step via (last terminal − first start)/max(1,count); single-event run → ended−started of that event
    - abandoned_tool_count: 1 tool_use with no matching terminal → 1; a matched pair → 0
    - redundant_read_count: Read(a),Read(a) → 1; Read(a),Edit(a),Read(a) → 0 (TRUE-NEGATIVE, state changed); Read(a),Read(b),Read(a) → 1
    - edit_revert_count (v0 = Edit-input A→B→A pattern on same target_path, OQ2/A2 lock): inputs_digest A→B→A on path p → 1; A→B→C → 0 (TRUE-NEGATIVE); null when edit inputs not reconstructable
    - loop_count: Bash(x),Bash(x),Bash(x) → 1 (one maximal cluster, NOT 2); Bash(x),Bash(y),Bash(x) → 0 (TRUE-NEGATIVE, not adjacent)
    - parallel-same-turn fixture: two events same turn, same tool, NOT collapsed → contribute 2 to total_step_count
    - computeHeuristics(null) → ALL_NULL_HEURISTICS (every metric null, NOT 0 — D-02)
  </behavior>
  <action>
    First create the golden fixtures under `tests/fixtures/route/` (one JSON file each: loop.json, edit-revert.json, redundant-read.json, abandoned.json, parallel-same-turn.json, true-negatives.json) — each holds a small ordered `RouteEvent[]` matching the behavior cases above with explicit ISO `started_at`/`ended_at` and the right `outcome`/`target_path`/`inputs_digest` fields. Then write `tests/experiments/route-heuristics.test.mjs` using `node:test` + `node:assert/strict` (the established `tests/experiments/` pattern — NOT jest globals), ONE describe/test block per heuristic, each with at least one TRUE-NEGATIVE case (D-06/D-08), plus a block for the `computeHeuristics(null)` all-null path. Run RED (fail). Then implement `lib/experiments/route-heuristics.mjs`: export `ALL_NULL_HEURISTICS` (all six keys = null) and `computeHeuristics(trace)` returning `{ loop_count, edit_revert_count, redundant_read_count, abandoned_tool_count, total_step_count, wallclock_per_step }`. Lock the v0 `edit_revert_count` as the Edit-input A→B→A pattern (no file I/O; `null` when the agent's edit inputs are not reconstructable — A2/OQ2). Parse every timestamp via `Date.parse` ONLY (Pitfall 6 — readers already normalize to ISO). `null` trace → `ALL_NULL_HEURISTICS`. Reserve `0` for "trace present, genuinely none" (Pitfall 4). Pure ESM, imports `inputsDigest`/typedef from `../lsl/route/route-event.mjs`, no fs/km-core. `process.stderr.write` only. Run GREEN.
  </action>
  <verify>
    <automated>node --test tests/experiments/route-heuristics.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/route-heuristics.test.mjs` exits 0 with every heuristic block + the null block passing
    - `grep -c "console\." lib/experiments/route-heuristics.mjs` returns 0
    - `grep -n "ALL_NULL_HEURISTICS" lib/experiments/route-heuristics.mjs` shows the exported all-null constant with six null keys
    - `grep -rn "Date.parse" lib/experiments/route-heuristics.mjs` confirms timestamps parsed via Date.parse only (Pitfall 6)
    - `ls tests/fixtures/route/*.json | wc -l` returns 6
    - test file contains at least one true-negative assertion per heuristic (grep for the A→B→C / Read(a),Edit(a),Read(a) / Bash(x),Bash(y),Bash(x) cases)
  </acceptance_criteria>
  <done>computeHeuristics matches all six golden fixtures incl. true-negatives; null trace yields all-null; zero console.* usage; six fixture files exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → computeHeuristics | RouteEvent[] crosses here; this plan produces no file reads, only consumes already-normalized events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-DOS | Denial of Service | computeHeuristics over a very large RouteEvent[] | accept | Bounded by the run's tool-call count; linear single-pass per heuristic; no recursion. Pure in-memory transform, no untrusted parse here (readers own the parse gate). |
| T-72-01-CRYPTO | Information disclosure | inputsDigest of tool input args | mitigate | sha256 of canonical JSON via `node:crypto` stdlib (V6, never hand-roll). Digest only — raw inputs are NOT carried forward; the digest is the redundant/loop-detection key. No raw tool inputs persisted by this plan. |
| T-72-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH Package Legitimacy Audit: N/A — all stdlib/in-repo). No checkpoint needed. |
</threat_model>

<verification>
- `node --test tests/experiments/route-heuristics.test.mjs` green (all six heuristics + null path + true-negatives).
- No `console.*` in either new module.
- `inputsDigest` uses `node:crypto` sha256 (no hand-rolled hash).
</verification>

<success_criteria>
- RouteEvent schema authored as the single cross-agent contract (D-01).
- computeHeuristics implements all six strict definitions (D-06/D-07), fixture-backed with true-negatives (D-08).
- null trace → ALL_NULL_HEURISTICS, never 0 (D-02).
</success_criteria>

<output>
Create `.planning/phases/72-syntactic-route-quality/72-01-SUMMARY.md` when done
</output>
