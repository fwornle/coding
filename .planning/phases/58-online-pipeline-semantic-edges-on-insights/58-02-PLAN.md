---
phase: 58-online-pipeline-semantic-edges-on-insights
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - src/live-logging/ObservationConsolidator.js
  - src/live-logging/ObservationWriter.js
  - src/live-logging/ObservationWriter.test.js
autonomous: true
requirements:
  - EDGE-01
  - EDGE-02
must_haves:
  truths:
    - "A new Insight written by ObservationConsolidator emerges in km-core with its mentions edges AND its capturedBy edge AND its has_insight project-anchor edge within the SAME km-core exporter debounce window (~5s) — so the 5s-debounced JSON export tick never serializes an orphan-Insight state (Insight node without its edges). Per-edge findRelations awaits yield the event loop within the window; the EDGE-02 contract is the exporter-debounce envelope, not microtask scheduling."
    - "ObservationConsolidator._pushInsightToKG no longer issues fetch(PUT /api/entities/...) or fetch(POST /api/relations). Per D-06, _pushInsightToKG routes through this._observationWriter.writeInsight(row, {mentionsTargetIds}) — the writer-path is the single km-core-native writer that handles putEntity + mentions edges + capturedBy anchor in one envelope. The has_insight project-anchor edge is emitted via this._kmStore.addRelation after writeInsight returns the minted id."
    - "ObservationWriter.writeInsight accepts an optional pre-computed mentions edge list (options.mentionsTargetIds: string[]) and emits the mentions edges synchronously between putEntity and _anchorEntity in the same try-block — D-06 unification means the consolidator MUST consume this surface, not inline kmStore.putEntity itself."
    - "kmStore.addRelation calls in the writer path are guarded by a findRelations dedup check so re-writes don't multiply edges (D-04 + D-05 idempotency)."
    - "If the mentions classifier throws (LLM proxy down), the Insight is NOT written — the consolidator gates writeInsight behind successful classification per D-04.1."
    - "The pre-existing has_insight project-anchor edge (consolidator.js line 565-580) is preserved — it migrates from fetch(POST /api/relations) to kmStore.addRelation with the same {from, to, type} triple, emitted by the consolidator AFTER writeInsight returns."
    - "ObservationConsolidator gains an _observationWriter handle (constructor-injected or lazily constructed against this._kmStore + this._runId) so _pushInsightToKG can route through it. The single-owner kmStore pattern (D-06) becomes a single-owner writer pattern: one ObservationWriter instance shared by digest/observation/insight code paths."
  artifacts:
    - path: src/live-logging/ObservationConsolidator.js
      provides: "_pushInsightToKG refactored to route through this._observationWriter.writeInsight(row, {mentionsTargetIds}) with mentions classifier integration; has_insight edge emitted post-writeInsight via this._kmStore.addRelation"
      contains: "this._observationWriter.writeInsight"
    - path: src/live-logging/ObservationWriter.js
      provides: "writeInsight extended to accept options.mentionsTargetIds and emit mentions edges in the same atomic block as putEntity + _anchorEntity"
      contains: "mentionsTargetIds"
  key_links:
    - from: src/live-logging/ObservationConsolidator.js
      to: src/live-logging/MentionsClassifier.js
      via: "import { loadMentionCandidates, classifyMentions } from './MentionsClassifier.js'"
      pattern: "from\\s+['\"]\\./MentionsClassifier\\.js['\"]"
    - from: src/live-logging/ObservationConsolidator.js
      to: src/live-logging/ObservationWriter.js
      via: "this._observationWriter.writeInsight(row, {mentionsTargetIds}) — D-06 unification path"
      pattern: "this\\._observationWriter\\.writeInsight"
    - from: src/live-logging/ObservationWriter.js
      to: lib/km-core (kmStore.addRelation)
      via: "for-loop of await kmStore.addRelation per mentionsTargetIds entry, same try-block as putEntity"
      pattern: "for.*mentionsTargetIds.*addRelation"
---

<objective>
Refactor `ObservationConsolidator._pushInsightToKG` (lines 520-585) to route through `ObservationWriter.writeInsight` (per D-06's locked "refactor to write through ObservationWriter.writeInsight" wording) instead of inlining VKB HTTP `PUT /api/entities/...` or inlining `kmStore.putEntity` directly. Extend `ObservationWriter.writeInsight` (lines 1178-1201) to accept a pre-computed `mentions` edge list (`options.mentionsTargetIds`) emitted in the same try-block as the existing `_anchorEntity` call. This realizes D-04 (atomic node+edges write within the km-core exporter debounce window — no orphan-Insight intermediate state visible in JSON exports) and D-06 (writer-path unification — one kmStore-native writer, no drift between the consolidator's path and the writer's path; the consolidator becomes a consumer of writeInsight, not a parallel writer).

Purpose: EDGE-02 acceptance demands atomic node+edges writes (visible as a single exporter tick); EDGE-01 acceptance demands every new Insight carries ≥1 `mentions` edge. Both are wired in this plan via D-06's unification path. Side-effect: the orphan-Insight `capturedBy` fix from commit 955617a1a is preserved automatically — `writeInsight` calls `_anchorEntity` internally, so by routing through it the consolidator inherits the fix instead of re-implementing or regressing it.
Output: Consolidator routes through writer (via a constructor-wired or lazily-resolved `this._observationWriter` handle); writer accepts mentions list; existing `has_insight` project-anchor edge preserved through the kmStore path, emitted by the consolidator AFTER writeInsight returns the minted id.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-CONTEXT.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-PATTERNS.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing code surfaces this plan modifies. Extracted from on-disk source on 2026-06-15. -->

From src/live-logging/ObservationConsolidator.js (lines 520-585) — CURRENT shape to replace:
```
async _pushInsightToKG(entry) {
  if (!entry?.topic) return;
  const vkbUrl = process.env.VKB_API_URL || 'http://localhost:8080';
  const project = entry.project || 'coding';
  const { entityClass, confidence: classConf } = this._classifyInsightByOntology(entry.topic, entry.summary || '');
  const projectName = await this._ensureProjectAnchor(vkbUrl, project);
  const body = { entityType: entityClass, observations: [entry.summary || entry.topic], significance: ..., team: project, source: 'online', metadata: {...} };
  // fetch(PUT /api/entities/${entry.topic}) ← replaced by writeInsight call
  // fetch(POST /api/relations) for has_insight ← replaced by kmStore.addRelation after writeInsight
}
```

The CRITICAL locals that must survive the refactor (used to construct the row payload for writeInsight + the has_insight edge):
- `entityClass` (line ~524) — L2 ontology class string from `_classifyInsightByOntology(...)`. Feeds `row.entityType` / `row.ontologyClass` for writeInsight.
- `classConf` (line ~524) — classification confidence; feeds `row.metadata.ontology.classificationConfidence`.
- `project` (line ~523) — project tag (D-02 from Phase 57). Feeds `row.team` and `row.metadata.project`.
- `projectName` (line ~525) — return of `_ensureProjectAnchor(...)`; the Project entity NAME (not id). Resolved to id post-writeInsight for the has_insight addRelation.

From src/live-logging/ObservationWriter.js (lines 1178-1201) — CURRENT shape, EXTEND:
```
async writeInsight(row) {
  if (!row || typeof row !== 'object') throw ...;
  if (!row.id) throw ...;
  const kmStore = await this._ensureKmStore();
  const ts = row.created_at || new Date().toISOString();
  const entity = legacyInsightToEntity(row, this._runId, ts);
  entity.ontologyClass = 'Detail';
  entity.metadata = { ...entity.metadata, source: 'auto' };
  const mintedId = await kmStore.putEntity(entity, { skipOntologyCheck: true });
  await this._anchorEntity(kmStore, mintedId);   // ← add the mentions loop BEFORE this line
  return row.id;
}
```

The `legacyInsightToEntity` mapper (read its current shape before editing the consolidator) determines which row fields become which Entity fields. The consolidator's row payload MUST be shaped so that legacyInsightToEntity produces an Entity whose `entityType` / `ontologyClass` are the consolidator's `entityClass` local — verify the mapping and adjust the consolidator's row construction (NOT writeInsight's hard-coded `entity.ontologyClass = 'Detail'` — that is the writer's default for cases where the caller didn't pass an entityType; the consolidator's row should carry an explicit entityType that overrides this).

From src/live-logging/ObservationWriter.js (lines 425-443) — `_anchorEntity` is the 1-edge analog for the N-edge mentions loop:
```
async _anchorEntity(kmStore, fromId, relationType = 'capturedBy') {
  if (!fromId) return;
  const anchorId = await this._resolveAnchorId(kmStore);
  if (!anchorId) return;
  try {
    await kmStore.addRelation({ from: fromId, to: anchorId, type: relationType, metadata: {...} });
  } catch (err) {
    process.stderr.write(`[ObservationWriter] anchor edge ${fromId}->${anchorId} failed (non-fatal): ${err.message}\n`);
  }
}
```

From lib/km-core/src/store/GraphKMStore.ts:
- `findRelations(filter: Partial<Relation>): Promise<Relation[]>` (line 863) — used for the (from, to, type) idempotency check before addRelation.
- `addRelation` throws `'Source node not found'` or `'Target node not found'` — treat as benign per PATTERNS Shared Pattern A.

From src/live-logging/MentionsClassifier.js (Plan 58-01):
- `loadMentionCandidates(kmStore): Promise<{id, name, description}[]>`
- `classifyMentions(insightSummary, candidates): Promise<EntityId[]>` (throws on proxy failure per D-04.1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ObservationWriter.writeInsight to accept mentionsTargetIds + emit N edges atomically</name>
  <files>src/live-logging/ObservationWriter.js</files>
  <read_first>
    - src/live-logging/ObservationWriter.js lines 380-450 (_resolveAnchorId + _anchorEntity — the 1-edge atomic pattern to generalize to N edges)
    - src/live-logging/ObservationWriter.js lines 1160-1210 (writeInsight current shape — the target of the extension)
    - src/live-logging/ObservationWriter.js lines 1060-1160 (writeObservation + writeDigest — same kmStore.putEntity + _anchorEntity shape; the mentions extension lands in writeInsight ONLY, not the observation/digest writers, but the patterns inform the extension)
    - src/live-logging/ObservationWriter.js (search for `legacyInsightToEntity` definition or import — confirm which row fields it consumes; the consolidator's row payload in Task 2 must match)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-PATTERNS.md §2 (atomicity contract — the exact for-loop shape with try/catch + dedup-before-write)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-CONTEXT.md D-04, D-04.1, D-04.2, D-06 (atomicity + writer-path unification invariants)
    - lib/km-core/src/store/GraphKMStore.ts lines 837-870 (addRelation + findRelations signatures and error shapes)
  </read_first>
  <action>
    Modify `writeInsight(row, options = {})` in `src/live-logging/ObservationWriter.js` (line 1178 — read the file FIRST to confirm the line number hasn't drifted). Add an optional second parameter `options = {}` with shape `{mentionsTargetIds?: string[]}`. The new ordering inside the existing try-block, in this exact sequence:

    1. `const entity = legacyInsightToEntity(row, this._runId, ts);` — unchanged.
    2. `entity.ontologyClass = 'Detail'; entity.metadata = { ...entity.metadata, source: 'auto' };` — IMPORTANT: change this to NOT clobber an explicit `entity.ontologyClass` set by the row mapper. New shape: `if (!entity.ontologyClass) entity.ontologyClass = 'Detail'; entity.metadata = { ...entity.metadata, source: entity.metadata?.source ?? 'auto' };` — this preserves the consolidator's L2 classification (entityClass local from Task 2) without breaking existing writer callers whose rows didn't set ontologyClass.
    3. `const mintedId = await kmStore.putEntity(entity, { skipOntologyCheck: true });` — unchanged.
    4. **NEW:** `await this._emitMentionsEdges(kmStore, mintedId, Array.isArray(options.mentionsTargetIds) ? options.mentionsTargetIds : []);` — synchronous loop in the same try-block per D-04.
    5. `await this._anchorEntity(kmStore, mintedId);` — unchanged (existing capturedBy edge). This is the line that automatically preserves the orphan-Insight `capturedBy` fix from commit 955617a1a for ALL consumers of writeInsight (including the consolidator post-Task 2 refactor).
    6. `return row.id;` — unchanged.

    Add a new private method `_emitMentionsEdges(kmStore, fromId, targetIds)` on the class (place adjacent to `_anchorEntity` at line 425 for code locality). Implementation:

    Method signature: `async _emitMentionsEdges(kmStore, fromId, targetIds)`. Guards: return early if `!fromId || !Array.isArray(targetIds) || targetIds.length === 0`. For each `toId` in `targetIds`: skip if `!toId || toId === fromId` (self-loop guard). Then dedup-check via `kmStore.findRelations({from: fromId, to: toId, type: 'mentions'})` wrapped in try/catch; if `length > 0` continue. Then `kmStore.addRelation({from: fromId, to: toId, type: 'mentions', metadata: {source: 'observation-writer', classifiedAt: new Date().toISOString(), classifier: 'llm-haiku'}})` wrapped in try/catch; on error, `process.stderr.write('[ObservationWriter] mentions edge ${fromId}->${toId} failed (non-fatal): ${err.message}\n')` (matches the existing `_anchorEntity` non-fatal-error convention — Source/Target-not-found races with putEntity in-flight are benign).

    Update the JSDoc on `writeInsight` to document the new `options.mentionsTargetIds` parameter (Phase 58 D-04 + D-06 reference). NO changes to `writeObservation` or `writeDigest` — the mentions extension is Insight-scoped per CONTEXT scope.

    Per CLAUDE.md, no `console.*` calls — only `process.stderr.write(...)`. Per PATTERNS Shared Pattern A, the `metadata.source` value is `'observation-writer'` for the writer-path emission (Plan 03 script uses `'backfill-insight-mentions'`, Plan 03's bridge extension uses `'consolidator-bridge'`).
  </action>
  <verify>
    <automated>node --check src/live-logging/ObservationWriter.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/live-logging/ObservationWriter.js` exits 0.
    - `grep -n "_emitMentionsEdges" src/live-logging/ObservationWriter.js | wc -l` ≥ 2 (definition + at least one call site in writeInsight).
    - `grep -nE "writeInsight\\s*\\(\\s*row\\s*,\\s*options" src/live-logging/ObservationWriter.js | wc -l` ≥ 1 (signature extended to accept options).
    - `grep -nE "mentionsTargetIds" src/live-logging/ObservationWriter.js | wc -l` ≥ 2 (referenced from both the signature destructure / read AND the loop).
    - `grep -nE "type:\\s*['\"]mentions['\"]" src/live-logging/ObservationWriter.js | wc -l` ≥ 1.
    - `grep -nE "findRelations\\(\\{\\s*from:.*to:.*type:\\s*['\"]mentions['\"]" src/live-logging/ObservationWriter.js | wc -l` ≥ 1 (dedup check present per PATTERNS Shared Pattern A).
    - `grep -nE "source:\\s*['\"]observation-writer['\"]" src/live-logging/ObservationWriter.js | wc -l` ≥ 1 (the new mentions edges carry `metadata.source='observation-writer'`).
    - The ordering `putEntity → _emitMentionsEdges → _anchorEntity` must be preserved in writeInsight: `awk '/async writeInsight\\(/,/^  }$/' src/live-logging/ObservationWriter.js | grep -nE "putEntity|_emitMentionsEdges|_anchorEntity"` must show these three lines in this exact order with putEntity FIRST, _emitMentionsEdges SECOND, _anchorEntity THIRD.
    - The `if (!entity.ontologyClass)` guard is present (caller's ontologyClass not clobbered): `grep -cE "if\\s*\\(\\s*!entity\\.ontologyClass\\s*\\)" src/live-logging/ObservationWriter.js` ≥ 1.
    - `grep -v '^\\s*\\*' src/live-logging/ObservationWriter.js | grep -v '^\\s*//' | grep -E "console\\.(log|error|warn|info)" | wc -l` == 0 (no new console.* calls).
  </acceptance_criteria>
  <done>writeInsight accepts options.mentionsTargetIds, emits N mentions edges in the same try-block via the new _emitMentionsEdges helper (placed between putEntity and _anchorEntity), dedup-checks each edge before write, and no longer clobbers a caller-provided ontologyClass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ObservationConsolidator._pushInsightToKG to route through ObservationWriter.writeInsight (D-06)</name>
  <files>src/live-logging/ObservationConsolidator.js</files>
  <read_first>
    - src/live-logging/ObservationConsolidator.js lines 100-150 (constructor — confirm `this._kmStore = options.kmStore || null;` at line 128. CRITICAL: check whether the consolidator already has `this._observationWriter` or any writer handle. If absent, this task adds it.)
    - src/live-logging/ObservationConsolidator.js lines 520-585 (the _pushInsightToKG method being refactored — read end-to-end before editing; the current HTTP-PUT body has `entityType, observations, significance, team, source, metadata` shape that must map to the writeInsight `row` shape via `legacyInsightToEntity`)
    - src/live-logging/ObservationConsolidator.js lines 604-720 (_ensureProjectAnchor — note signature `_ensureProjectAnchor(vkbUrl, team)` returns the projectName string; leave that method UNTOUCHED for this plan; just consume its return value)
    - src/live-logging/ObservationConsolidator.js lines 1546-1600 (existing _kmStore-native lookup pattern for Insight de-dup by name — informs the existing-Insight merge path inside writeInsight)
    - src/live-logging/ObservationConsolidator.js lines 1685-1700 (call site at line 1691: `try { await this._pushInsightToKG(entry); } catch { /* swallowed */ }` — the call-site swallow MUST be preserved per PATTERNS Landmine 5)
    - src/live-logging/ObservationWriter.js (constructor — confirm signature so the consolidator can construct/inject one: `new ObservationWriter({kmStore, runId, ...})`)
    - src/live-logging/ObservationWriter.js (legacyInsightToEntity definition or import — confirm which row fields are read so the consolidator's row payload includes them all)
    - src/live-logging/MentionsClassifier.js (Plan 58-01 — the import target)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-PATTERNS.md §1 (the full refactor pattern including landmines about entity id vs entity name)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-CONTEXT.md D-04, D-04.1, D-06, D-06.1, D-06.2 (locked behaviour — D-06 wording: "refactor to write through ObservationWriter.writeInsight")
  </read_first>
  <action>
    Refactor `_pushInsightToKG(entry)` in `src/live-logging/ObservationConsolidator.js` (line 520 — re-check the line number; the file is 174KB and edits in adjacent areas may have shifted it). The refactor replaces the `fetch(PUT /api/entities/...)` block AND the `fetch(POST /api/relations)` block with a route-through `this._observationWriter.writeInsight(row, {mentionsTargetIds})` call (D-06) + a post-writeInsight `kmStore.addRelation` for the has_insight project-anchor edge.

    **STEP A — Ensure ObservationConsolidator has an `_observationWriter` handle** (this is the D-06 unification prerequisite; the consolidator must consume the writer, not duplicate its logic):

    In the constructor (~line 128, alongside `this._kmStore = options.kmStore || null;`), add:
    ```
    // D-06 — single-owner writer pattern. The consolidator routes Insight writes through
    // ObservationWriter.writeInsight so the writer-path is the only km-core-native writer
    // for Insights. The capturedBy `_anchorEntity` call inside writeInsight fires for free.
    this._observationWriter = options.observationWriter || null;
    ```

    Add a lazy getter helper (place near other lazy getters; or inline at the top of `_pushInsightToKG`):
    ```
    _ensureObservationWriter() {
      if (this._observationWriter) return this._observationWriter;
      if (!this._kmStore) throw new Error('[ObservationConsolidator] cannot construct ObservationWriter — kmStore not configured');
      // Import lazily to avoid circular deps if ObservationWriter ever imports ObservationConsolidator
      const { ObservationWriter } = require('./ObservationWriter.js');   // or `import` if file is ESM — match module style
      this._observationWriter = new ObservationWriter({
        kmStore: this._kmStore,
        runId: this._runId || `consolidator-${Date.now()}`,
        // Match the writer's constructor option shape — read it FIRST and replicate the required fields.
      });
      return this._observationWriter;
    }
    ```

    Verify the ObservationWriter constructor signature first (read its constructor at the top of `src/live-logging/ObservationWriter.js`). If the constructor accepts more required options (e.g., `dbPath`, `anchorName`), wire them through from `this.*` equivalents on the consolidator. Document any deviation in the SUMMARY.

    **STEP B — Refactor _pushInsightToKG body** (replaces lines 520-585):

    1. At the top of the file (where other imports live), add: `import { loadMentionCandidates, classifyMentions } from './MentionsClassifier.js';`. Re-check the module style — if the file uses `require`, switch to `const { loadMentionCandidates, classifyMentions } = require('./MentionsClassifier.js');` (the MentionsClassifier.js module style must match — coordinated with Plan 58-01 Task 1).

    2. At the top of `_pushInsightToKG`, add the kmStore guard (mirrors lines 722-723 and dozens of similar sites in the file):
       ```
       if (!this._kmStore) throw new Error('[ObservationConsolidator] km-core not configured — pass options.kmStore');
       ```
       Per D-04.1 this is fail-fast — the caller at line 1691 (`try { ... } catch { /* swallowed */ }`) handles it.

    3. **PRESERVE the existing classification + project-anchor locals** (W3 — these locals encode the L2 ontology classification + project tagging from Phase 57; if a future refactor drops them, the writer-path loses ontology data):
       ```
       const project = entry.project || 'coding';
       const { entityClass, confidence: classConf } = this._classifyInsightByOntology(entry.topic, entry.summary || '');
       const projectName = await this._ensureProjectAnchor(/* existing args */, project);
       ```
       CRITICAL: lines 524-528 in the original `_pushInsightToKG` derive `entityClass`, `classConf`, `project`, `projectName`. These FOUR locals feed:
       - `entityClass` → `row.entityType` / `row.ontologyClass` for writeInsight (L2 ontology — ensures the Insight node carries its classified type, not the writer's `'Detail'` default).
       - `classConf` → `row.metadata.ontology.classificationConfidence` (audit trail).
       - `project` → `row.team` and `row.metadata.project` (Phase 57 D-02 project tagging — every entity stamped).
       - `projectName` → resolved post-writeInsight to a Project entity id for the has_insight addRelation (step 6 below).

       The refactor MUST NOT drop these locals or their derivation — they survive verbatim. If `_ensureProjectAnchor` currently takes `vkbUrl` as its first arg AND we're removing the fetch-based flow, the method body of `_ensureProjectAnchor` MAY still need the vkbUrl arg (it could still hit a VKB endpoint internally for unrelated state). Leave the call signature as-is unless that method's own refactor is in scope — out of scope for this plan per D-06.1 ("The VKB HTTP PUT /api/entities/${topic} path is NOT deleted").

    4. Compute the mentions list BEFORE writeInsight (D-04 step 2 — LLM call BEFORE write per D-04.1):
       ```
       let mentionsTargetIds = [];
       try {
         const candidates = await loadMentionCandidates(this._kmStore);
         mentionsTargetIds = await classifyMentions(entry.summary || entry.topic, candidates);
       } catch (err) {
         process.stderr.write(`[Consolidator→KG] mentions classifier failed for ${entry.topic}: ${err.message}\n`);
         return;   // D-04.1: don't write the Insight if classification failed.
       }
       ```

    5. **Build the row payload and route through writeInsight (D-06 — THE CORE OF THIS REFACTOR)**:
       ```
       const writer = this._ensureObservationWriter();
       const row = {
         id: entry.topic,                     // legacyInsightToEntity uses this as the entity name/id key — confirm via the mapper
         topic: entry.topic,                  // explicit topic field for the mapper
         summary: entry.summary || entry.topic,
         entityType: entityClass,             // L2 ontology — overrides writer's 'Detail' default via the Task 1 guard
         ontologyClass: entityClass,          // explicit ontologyClass on the row — Task 1 added the `if (!entity.ontologyClass)` guard so this propagates through the mapper
         team: project,
         source: 'online',
         confidence: entry.confidence,
         created_at: new Date().toISOString(),
         metadata: {
           source: 'online',
           team: project,
           project,                           // Phase 57-03 D-02 — every entity stamped with project
           confidence: entry.confidence,
           digest_ids: entry._digestIds || [],
           ontology: {
             ontologyName: 'development-knowledge-ontology',
             classificationMethod: 'heuristic-token-overlap',
             classificationConfidence: classConf,
           },
         },
       };
       let mintedId;
       try {
         mintedId = await writer.writeInsight(row, { mentionsTargetIds });
       } catch (err) {
         process.stderr.write(`[Consolidator→KG] writeInsight failed for ${entry.topic}: ${err.message}\n`);
         return;
       }
       ```
       Caveat: the executor MUST read `legacyInsightToEntity` first to confirm which row fields it consumes. If it reads `row.id` as the entity id directly, the consolidator's existing topic-as-name convention still works. If the mapper mints a fresh id and ignores `row.id`, the consolidator's downstream lookups (the existing-Insight merge path) need to read `mintedId` from the writeInsight return. The writer DOES handle the "existing Insight, merge" case internally via `kmStore.putEntity` semantics (km-core merges on existing id) — no explicit `findByOntologyClass + mergeAttributes` block needed in the consolidator anymore. Verify and adjust.

    6. Preserve the has_insight project-anchor edge (consolidator.js lines 565-580) — emit it via `this._kmStore.addRelation` AFTER writeInsight returns the minted id. The `projectName` local from step 3 is the entity NAME; resolve to id via `findByOntologyClass('Project')`:
       ```
       if (projectName && mintedId) {
         try {
           const projects = await this._kmStore.findByOntologyClass('Project');
           const projectId = projects.find(p => p.name === projectName)?.id;
           if (projectId) {
             const existingHasInsight = await this._kmStore.findRelations({ from: projectId, to: mintedId, type: 'has_insight' });
             if (existingHasInsight.length === 0) {
               await this._kmStore.addRelation({
                 from: projectId,
                 to: mintedId,
                 type: 'has_insight',
                 metadata: { source: 'observation-consolidator', team: project, confidence: 1.0 },
               });
             }
           }
         } catch (err) {
           process.stderr.write(`[Consolidator→KG] has_insight ${projectName} → ${entry.topic} failed: ${err.message}\n`);
         }
       }
       ```
       This `has_insight` edge lands within the same exporter debounce window as the writeInsight call — the JSON export tick captures both. Per D-06 the has_insight edge is NOT moved INTO writeInsight (writeInsight emits capturedBy via _anchorEntity, not has_insight; has_insight is consolidator-specific and stays on the consolidator side).

    7. DELETE the `fetch(PUT /api/entities/...)` block (lines 547-551 in the original) AND the `fetch(POST /api/relations)` block for has_insight (lines 565-580 in the original). Per D-06.1, OTHER consumers of those VKB endpoints stay — do NOT delete the endpoints themselves. The grep gate `awk '/^  async _pushInsightToKG\\(entry\\) {$/,/^  }$/' src/live-logging/ObservationConsolidator.js | grep -c "fetch("` MUST return 0 (the method body no longer issues HTTP). Other methods like `_relinkOrphanOnlineInsights` still use fetch and Plan 58-03 owns that.

    8. KEEP the existing logging at line 558 (`if (this._kgPushDebug) process.stderr.write(...)`) — it now logs the writer-path mintedId instead of the HTTP success path. Update the log message to reflect the new shape but keep the `_kgPushDebug` gate.

    Preserve the call-site swallow at line 1691 (`try { await this._pushInsightToKG(entry); } catch { /* swallowed */ }`) — PATTERNS Landmine 5. The fail-fast happens INSIDE `_pushInsightToKG` for classifier failures (early return); the swallow handles unexpected exceptions so a single Insight failure doesn't abort the batch.

    **W1 — capturedBy regression check**: Confirm by reading the post-edit code that `_anchorEntity` STILL fires for every Insight. Because step 5 routes through `writer.writeInsight(...)` and the writer's writeInsight body (Task 1) calls `await this._anchorEntity(kmStore, mintedId);` at the end of the try-block, the capturedBy edge is emitted unconditionally — the orphan-fix from commit 955617a1a is preserved by construction, not by re-implementation. Do NOT add a redundant `_anchorEntity` call in the consolidator (would double-emit).
  </action>
  <verify>
    <automated>node --check src/live-logging/ObservationConsolidator.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/live-logging/ObservationConsolidator.js` exits 0.
    - `grep -cE "from\\s+['\"]\\./MentionsClassifier\\.js['\"]|require\\(['\"]\\./MentionsClassifier\\.js['\"]\\)" src/live-logging/ObservationConsolidator.js` ≥ 1 (import wired).
    - The constructor has the `_observationWriter` field: `grep -cE "this\\._observationWriter\\s*=" src/live-logging/ObservationConsolidator.js` ≥ 1.
    - The lazy getter exists: `grep -cE "_ensureObservationWriter" src/live-logging/ObservationConsolidator.js` ≥ 2 (definition + call site).
    - Inside the `_pushInsightToKG` method (sed-extracted between `async _pushInsightToKG(entry) {` and the next matching `^  }$`), the count of `fetch(` calls is 0. Use: `awk '/^  async _pushInsightToKG\\(entry\\) {$/,/^  }$/' src/live-logging/ObservationConsolidator.js | grep -c "fetch("` returns `0`.
    - Inside the same method body, `grep -c "this\\._observationWriter\\.writeInsight\\|writer\\.writeInsight"` returns ≥ 1 (D-06 route-through path wired).
    - Inside the same method body, `grep -c "mentionsTargetIds"` returns ≥ 1 (passed into writeInsight options).
    - Inside the same method body, the LOCALS from step 3 survive: `grep -c "entityClass\\|classConf\\|projectName"` returns ≥ 3 (the four-local derivation step preserved — W3 fix).
    - Inside the same method body, `grep -c "this\\._kmStore\\.addRelation"` returns ≥ 1 (the post-writeInsight has_insight edge — the only kmStore.addRelation that stays in this method; the others moved into writeInsight).
    - Inside the same method body, `grep -c "type:\\s*['\"]has_insight['\"]"` returns ≥ 1 (D-06: existing has_insight edge preserved on the consolidator side).
    - Inside the same method body, `grep -c "classifyMentions"` returns ≥ 1 (classifier wired before write).
    - Inside the same method body, `grep -c "kmStore\\.putEntity\\|this\\._kmStore\\.putEntity"` returns 0 (D-06: no inlined putEntity — writeInsight owns it).
    - The line 1691 caller `try { await this._pushInsightToKG(entry); } catch { /* swallowed */ }` is byte-identical post-edit — verify with `grep -cE "try \\{ await this\\._pushInsightToKG\\(entry\\); \\} catch \\{ /\\* swallowed \\*/ \\}" src/live-logging/ObservationConsolidator.js` returns ≥ 1.
    - `grep -v '^\\s*\\*' src/live-logging/ObservationConsolidator.js | grep -v '^\\s*//' | grep -E "console\\.(log|error|warn|info)" | wc -l` == 0 (no new console.* calls in the file — pre-existing zero-state preserved).
  </acceptance_criteria>
  <done>_pushInsightToKG no longer issues fetch() calls and no longer inlines kmStore.putEntity; routes through this._observationWriter.writeInsight(row, {mentionsTargetIds}) per D-06; emits the has_insight project-anchor edge via this._kmStore.addRelation AFTER writeInsight returns; classifier failure short-circuits the write per D-04.1; the four ontology/project locals (entityClass, classConf, project, projectName) are preserved verbatim; the capturedBy edge from commit 955617a1a is preserved by construction via _anchorEntity inside writeInsight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — atomic putEntity + mentions edges in same microtask</name>
  <files>src/live-logging/ObservationWriter.test.js</files>
  <behavior>
    - Test 1 (EDGE-02 atomicity): Call `writer.writeInsight(row, {mentionsTargetIds: ['e1','e2','e3']})` against an in-memory mock kmStore that records the ORDER of putEntity + addRelation calls. Assert: putEntity is called first, then 3 addRelation calls with `type:'mentions'`, then 1 addRelation call with `type:'capturedBy'`. Assert: between putEntity and the last addRelation, no other awaited operation is performed by the writer (proxied via a counter on the mock that increments only inside putEntity / addRelation / findRelations).
    - Test 2 (EDGE-01 emission): Same call as Test 1; assert kmStore now has 3 mentions edges from `mintedId` to `e1`, `e2`, `e3` respectively, each carrying `metadata.source === 'observation-writer'` and a non-empty `classifiedAt` ISO timestamp.
    - Test 3 (idempotency): Run `writer.writeInsight(row, {mentionsTargetIds: ['e1']})` twice against the same mock kmStore. After the second call, the mock has exactly ONE mentions edge from mintedId to e1, NOT two. The dedup is proved by counting `addRelation({type:'mentions'})` invocations on the second call — should be 0 because findRelations returns the existing edge.
    - Test 4 (empty mentions): `writer.writeInsight(row, {mentionsTargetIds: []})` and `writer.writeInsight(row)` (no options) both succeed; neither calls addRelation with type:'mentions' (only the capturedBy anchor edge).
    - Test 5 (self-loop guard): `writer.writeInsight(row, {mentionsTargetIds: [mintedId]})` (target == fromId) — assert 0 mentions addRelation calls.
    - Test 6 (non-fatal addRelation failure): Mock kmStore where `addRelation` for the SECOND target throws `Error('Target node not found: e2')`. Assert: the writer still completes; the 1st + 3rd mentions edges are written; stderr contains `[ObservationWriter] mentions edge.*->e2 failed (non-fatal)`.
    - Test 7 (consolidator path — refactored _pushInsightToKG, route-through writeInsight): A consolidator test that confirms _pushInsightToKG calls `this._observationWriter.writeInsight` (not fetch, not direct kmStore.putEntity) AND that the resulting kmStore state has ≥1 mentions edge when the classifier returns a non-empty array. Stub `classifyMentions` to return `['e1','e2']`; stub `loadMentionCandidates` to return `[{id:'e1', name:'A', description:''}, {id:'e2', name:'B', description:''}]`. Inject a mock ObservationWriter via the consolidator's `options.observationWriter`. Assert: the mock writer's writeInsight was called with `{mentionsTargetIds: ['e1','e2']}` AND the mock kmStore receives the post-writeInsight has_insight addRelation call.
    - Test 8 (consolidator path — fail-fast on classifier error per D-04.1): Stub `classifyMentions` to throw. Assert mock ObservationWriter.writeInsight was NEVER called (Insight not written).
  </behavior>
  <read_first>
    - src/live-logging/ObservationWriter.test.js (CURRENT state — read entire file to understand existing test scaffolding, mock kmStore conventions, and how Phase 44/57 writer tests are structured. The new tests must match the existing test framework and helper patterns.)
    - src/live-logging/ObservationWriter.js post-Task 1 state (the file under test)
    - src/live-logging/ObservationConsolidator.js post-Task 2 state (the consolidator path tests target this refactored method)
    - src/live-logging/MentionsClassifier.test.js (Plan 58-01 — same mock framework, same stub conventions)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-PATTERNS.md §2 (atomicity contract — the test must lock the writeInsight try-block ordering)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-CONTEXT.md success criteria SC#1 (≥18/20 sampled Insights carry mentions edges) and SC#2 (atomic node+edges write — no orphan-Insight visible in JSON exports)
  </read_first>
  <action>
    Extend `src/live-logging/ObservationWriter.test.js` (or create it if absent) with 8 new tests realizing the behaviour above. Use a mock kmStore with the following shape:
    ```
    function createMockKmStore() {
      const callLog = [];
      const entities = new Map();
      const relations = [];
      return {
        callLog,
        async putEntity(entity, opts) {
          const id = entity.id || `mock-${entities.size + 1}`;
          callLog.push({ op: 'putEntity', id, entity });
          entities.set(id, { ...entity, id });
          return id;
        },
        async addRelation(rel) {
          callLog.push({ op: 'addRelation', from: rel.from, to: rel.to, type: rel.type, metadata: rel.metadata });
          relations.push(rel);
        },
        async findRelations(filter) {
          callLog.push({ op: 'findRelations', filter });
          return relations.filter(r =>
            (filter.from === undefined || r.from === filter.from) &&
            (filter.to === undefined || r.to === filter.to) &&
            (filter.type === undefined || r.type === filter.type)
          );
        },
        async findByOntologyClass(klass) {
          callLog.push({ op: 'findByOntologyClass', klass });
          return Array.from(entities.values()).filter(e => e.ontologyClass === klass);
        },
        async mergeAttributes(id, patch) {
          callLog.push({ op: 'mergeAttributes', id, patch });
          if (entities.has(id)) entities.set(id, { ...entities.get(id), ...patch });
        },
        // For the writer's _resolveAnchorId path
        emit() {},
      };
    }
    ```

    Inject the mock via the ObservationWriter constructor's `options.kmStore` path (read the constructor signature to confirm — based on Phase 44/57 conventions, the writer accepts `{kmStore}` directly OR via `_ensureKmStore`; stub `_ensureKmStore` if needed). For the consolidator tests (Test 7 + Test 8), inject a mock `ObservationWriter` via `options.observationWriter` on the consolidator constructor (the surface Task 2 added). Stub `classifyMentions` / `loadMentionCandidates` via module-mocking (`{esModuleInterop:true}` / `--experimental-test-module-mocks` if needed; OR by re-exporting the functions through a thin wrapper the test can override). Read existing `src/live-logging/*.test.js` files to determine the mocking convention the codebase uses — match it.

    Run via `node --test src/live-logging/ObservationWriter.test.js`. If a `package.json` test script invokes the whole `src/live-logging/` test suite, ensure it picks the new file up.

    Per CLAUDE.md, no `console.*` — use `process.stderr.write` for any forensic output (but tests should mostly use `assert.*` and not write to stderr at all unless intentionally capturing the writer's stderr stream).
  </action>
  <verify>
    <automated>node --test src/live-logging/ObservationWriter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/live-logging/ObservationWriter.test.js` exits 0.
    - stdout contains `# pass 8` (or framework-equivalent: ≥ 8 new passing tests; pre-existing tests in the file must also pass).
    - stdout contains `# fail 0`.
    - The atomicity ordering test (Test 1) explicitly asserts via `callLog` indices: `putEntity` index < every `addRelation({type:'mentions'})` index < the `addRelation({type:'capturedBy'})` index. Grep gate: `grep -c "callLog" src/live-logging/ObservationWriter.test.js` ≥ 1.
    - `grep -c "EDGE-02\\|atomicity\\|exporter.*debounce\\|same.*tick\\|try.block" src/live-logging/ObservationWriter.test.js` ≥ 1 (an EDGE-02 narrative comment is present in the test file so reviewers can trace SC#2 acceptance to the writer's try-block envelope).
    - `grep -c "classifyMentions" src/live-logging/ObservationWriter.test.js` ≥ 1 (Test 7 and Test 8 stub the classifier).
    - `grep -c "fail-fast\\|D-04.1" src/live-logging/ObservationWriter.test.js` ≥ 1 (Test 8 narrative comment present).
    - `grep -c "observationWriter\\|_observationWriter" src/live-logging/ObservationWriter.test.js` ≥ 1 (Test 7 + Test 8 inject the mock writer via the consolidator surface Task 2 added).
  </acceptance_criteria>
  <done>The 8 tests pass; EDGE-02 atomicity is locked at the unit level (putEntity → mentions×N → capturedBy ordering within the writer's try-block, with dedup); EDGE-01 emission is exercised; D-04.1 fail-fast is exercised; the D-06 route-through path (consolidator → writeInsight) is exercised in Tests 7-8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Insight summary → classifier (Plan 58-01) → mentionsTargetIds → writer | Untrusted Insight content traverses the classifier into the kmStore-write path. Plan 58-01's closed-set guard is the structural mitigation; this plan inherits it. |
| Concurrent /api/v1/entities reader ↔ writer | A reader may hit the JSON exporter mid-flush during an Insight write — EDGE-02 demands no orphan-Insight state visible in any exporter tick. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-01 | Tampering | classifier output → addRelation | mitigate | Plan 58-01's `extractMentionsFromLLMResponse` already filters by closed-set membership and caps at 20 ids. Plan 58-02 adds defense-in-depth: the `_emitMentionsEdges` loop guards against `targetId === fromId` (self-loop) and treats `Source/Target-not-found` as benign per `_anchorEntity` precedent. |
| T-58-02-02 | Tampering | concurrent reader sees orphan Insight | mitigate | D-04 atomic write envelope: writeInsight's try-block performs `putEntity → _emitMentionsEdges(N edges) → _anchorEntity(capturedBy)`. Per-edge findRelations awaits yield the event loop, but km-core's JSON exporter is 5s-debounced — every addRelation in the loop lands in the same exporter tick as putEntity (the EDGE-02 contract is the exporter-debounce envelope, NOT JavaScript microtask scheduling). A concurrent `/api/v1/entities` reader either sees the prior state (no Insight) or the new state (Insight + mentions + capturedBy + has_insight). EDGE-02 acceptance is locked by Test 1 in Task 3 (callLog ordering assertion within the writer's try-block). Defense-in-depth: when km-core adds transactional `addRelations` (deferred per D-04.2), promote this writer to use it — the exporter-debounce envelope is the current ship-floor, the transactional primitive is the future ceiling. |
| T-58-02-03 | Denial of Service | writer fails on every Insight if LLM proxy down | accept | D-04.1 trade-off: when the LLM is down, no Insight is written until the proxy recovers. Acceptable because (a) Insights are derived state — no data loss (digests + observations remain), (b) writer-path failures surface in operator workflows quickly. The alternative (write-then-edge-later) re-introduces the orphan-bleed Phase 58 closes. |
| T-58-02-04 | Repudiation | metadata.source ambiguity between three writers | mitigate | Three distinct `metadata.source` strings: `'observation-writer'` (this plan), `'observation-consolidator'` (this plan's _pushInsightToKG post-writeInsight has_insight edge), `'backfill-insight-mentions'` (Plan 58-03 script), `'consolidator-bridge'` (Plan 58-03 bridge extension). Every emitted edge is traceable to its writer. |
| T-58-02-SC | Tampering | npm package installs | mitigate | No new package installs (Node built-in `node:test` / `node:assert/strict`). Grep gate: `git diff --stat HEAD package.json` shows 0 net additions to dependencies. |
</threat_model>

<verification>
- `node --check src/live-logging/ObservationConsolidator.js` exits 0.
- `node --check src/live-logging/ObservationWriter.js` exits 0.
- `node --test src/live-logging/ObservationWriter.test.js` exits 0 with ≥ 8 new passing tests + zero regressions on the existing tests.
- Inside the `_pushInsightToKG` method body, the `fetch(` count is 0 (HTTP-PUT path retired) AND the `kmStore.putEntity` count is 0 (writeInsight owns putEntity per D-06).
- Inside the `_pushInsightToKG` method body, `this._observationWriter.writeInsight(...)` is invoked AND `this._kmStore.addRelation({type:'has_insight'})` is invoked post-writeInsight.
- EDGE-01 acceptance signal partially locked at unit level via Test 7 (consolidator path emits mentions edges via writeInsight); full live verification deferred to Plan 58-04.
- EDGE-02 acceptance signal locked at unit level via Test 1 (atomicity ordering inside writeInsight's try-block); live concurrent-reader verification deferred to Plan 58-04.
</verification>

<success_criteria>
- ObservationConsolidator._pushInsightToKG routes through ObservationWriter.writeInsight per D-06 — no fetch() calls, no inlined kmStore.putEntity.
- ObservationWriter.writeInsight accepts `options.mentionsTargetIds` and emits N mentions edges in the same try-block between putEntity and _anchorEntity.
- The existing `has_insight` project-anchor edge (D-06: preserve) is preserved via a consolidator-side post-writeInsight kmStore.addRelation; it lands within the same exporter debounce window as writeInsight's emissions.
- Unit-level atomicity contract (EDGE-02 SC#2 prerequisite) is proven by the callLog ordering test against the writer's try-block.
- Classifier failure (proxy down, hallucinated all names → empty array) does NOT write a half-Insight (D-04.1).
- The capturedBy edge fix from commit 955617a1a is preserved by construction: _anchorEntity fires inside writeInsight for every consumer including the consolidator.
- The four ontology/project locals (entityClass, classConf, project, projectName) are preserved verbatim — L2 ontology classification and project tagging do NOT regress.
</success_criteria>

<output>
Create `.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-02-SUMMARY.md` when done.
</output>
</output>
