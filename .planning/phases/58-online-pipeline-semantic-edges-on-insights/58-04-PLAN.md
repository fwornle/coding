---
phase: 58-online-pipeline-semantic-edges-on-insights
plan: 04
type: execute
wave: 4
depends_on:
  - 58-01
  - 58-02
  - 58-03
files_modified:
  - src/live-logging/MentionsAtomicity.integration.test.js
  - scripts/check-insight-mentions-coverage.mjs
autonomous: true
requirements:
  - EDGE-01
  - EDGE-02
must_haves:
  truths:
    - "An end-to-end integration test exercises ObservationConsolidator._pushInsightToKG with a stubbed LLM classifier and verifies (a) the new Insight node lands in the kmStore with ≥1 mentions edge within the km-core exporter debounce window (~5s) as the node, (b) the edge order is putEntity → mentions×N → capturedBy → has_insight inside the writer's try-block, (c) a concurrent reader probing the kmStore during the writer's execution never observes the Insight node without its mentions edges in any callLog snapshot — proving the writer's try-block is the atomic unit the 5s-debounced JSON exporter serializes."
    - "A coverage-check script `scripts/check-insight-mentions-coverage.mjs` reads the live JSON export, samples 20 random recent Insights, and exits 0 IFF ≥18 of them carry ≥1 mentions edge (locks SC#1 acceptance signal)."
    - "A re-run idempotency check confirms the writer + backfill + bridge do not multiply edges on re-emission — the dedup gates from Plans 58-02 and 58-03 are exercised live."
    - "Test 5 imports the exported `processInsight` helper from `scripts/backfill-insight-mentions.mjs` directly (per W5 — Plan 58-03 Task 1 exports it). The test does NOT skip — it exercises the backfill script's per-Insight path against the same mock kmStore as the consolidator tests, proving the dedup logic is byte-identical to the consolidator-side dedup."
    - "The coverage script honors --sample N --min M --source PATH CLI flags following the Phase 57-04 check-l2-emission-rate.mjs convention."
  artifacts:
    - path: src/live-logging/MentionsAtomicity.integration.test.js
      provides: "End-to-end atomicity + emission integration test (EDGE-01 + EDGE-02 acceptance at integration level)"
    - path: scripts/check-insight-mentions-coverage.mjs
      provides: "Operator-runnable coverage check codifying SC#1 acceptance signal"
  key_links:
    - from: scripts/check-insight-mentions-coverage.mjs
      to: .data/knowledge-graph/exports/general.json
      via: "JSON.parse + jq-like sampling logic"
      pattern: "general\\.json"
    - from: src/live-logging/MentionsAtomicity.integration.test.js
      to: src/live-logging/ObservationConsolidator.js (kmStore-native _pushInsightToKG from Plan 58-02)
      via: "consolidator instantiation with injected kmStore + stubbed classifier"
      pattern: "_pushInsightToKG\\|ObservationConsolidator"
    - from: src/live-logging/MentionsAtomicity.integration.test.js
      to: scripts/backfill-insight-mentions.mjs (exported processInsight from Plan 58-03 Task 1)
      via: "ESM import { processInsight } — Test 5 exercise path"
      pattern: "import\\s*\\{\\s*processInsight\\s*\\}"
---

<objective>
Lock the EDGE-01 + EDGE-02 acceptance signals at the integration level. Two surfaces:

1. **An integration test** (`MentionsAtomicity.integration.test.js`) that runs the kmStore-native consolidator writer end-to-end against an in-memory km-core-compatible store with a stubbed classifier, and assertively verifies (a) ≥1 mentions edge emerges per Insight (EDGE-01) and (b) the edge ordering is atomic relative to the node write within the writer's try-block (EDGE-02 — no observable orphan-Insight state in any callLog snapshot, which models how the 5s-debounced JSON exporter serializes the writer's emissions).

2. **An operator-runnable coverage script** (`scripts/check-insight-mentions-coverage.mjs`) that reads the live JSON export, samples 20 random Insights (per SC#1 acceptance language), and exits 0 IFF ≥18 carry ≥1 mentions edge. This codifies SC#1 as a CI-or-operator gate — the same shape as Phase 57-04's `scripts/check-l2-emission-rate.mjs`.

Purpose: Move EDGE-01 + EDGE-02 from "claimed in plan SUMMARYs" to "verified via reproducible script", and provide the operator with a one-line acceptance gate they can run after Plan 58-03's Task 3 live backfill closes.
Output: One integration test file + one CLI script. Together they answer the question "does Phase 58 ship?" with a binary pass/fail at the system level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-CONTEXT.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-PATTERNS.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-01-SUMMARY.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-02-SUMMARY.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-03-SUMMARY.md
@.planning/phases/57-lower-ontology-project-tagging-foundation/57-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing test + script analogs. Extracted from on-disk source on 2026-06-15. -->

From integrations/mcp-server-semantic-analysis/scripts/check-l2-emission-rate.mjs (Phase 57-04 analog, 229 LoC):
- CLI flags: --sample N (default 20), --min M (default 18), --source PATH (default the live export)
- Exits 0 PASS / exit 1 FAIL with a one-line summary on stderr/stdout
- Reads the live JSON export, filters to recent entities, samples N at random, counts how many meet the SC criterion
- Codifies one specific Success Criterion as a reproducible binary gate

From .planning/ROADMAP.md Phase 58 success criteria:
- SC#1: Sampling 20 random recent online-learned Insights, ≥18 carry at least one semantic-content relation type beyond capturedBy.
- SC#2: A concurrent /api/v1/entities reader during writer execution never observes an orphan-Insight intermediate state.
- SC#3: Unified viewer with Online filter shows online Insights connected to domain entities (NOT a Phase 58 verification surface — viewer rendering is Phase 60 LOWERONTO-03 work).

From src/live-logging/MentionsClassifier.js (Plan 58-01) + ObservationConsolidator.js (Plan 58-02) + scripts/backfill-insight-mentions.mjs (Plan 58-03):
- The full integration surface this plan tests.
- Plan 58-03 Task 1 exports `async function processInsight(insightNode, kmStore, options)` — Test 5 imports this directly per W5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end integration test — atomic putEntity + mentions edges via the refactored consolidator</name>
  <files>src/live-logging/MentionsAtomicity.integration.test.js</files>
  <behavior>
    - Test 1 (EDGE-01 — emission): Instantiate ObservationConsolidator with `{kmStore: mockKmStore}`. Pre-seed the mock with 3 Component entities (`e1`, `e2`, `e3`) and 1 Project entity (`p-coding`). Stub `classifyMentions` to return `['e1','e2']`. Call `await consolidator._pushInsightToKG({topic:'Test Insight', summary:'discusses e1 and e2', project:'coding', confidence:0.7, _digestIds:[]})`. Assert: mockKmStore now has 1 Insight entity AND 2 mentions edges (from Insight-id to e1, e2) AND 1 capturedBy edge (from Insight-id to LiveLoggingSystem) AND 1 has_insight edge (from p-coding to Insight-id). Total edge count: 4.
    - Test 2 (EDGE-02 — atomicity ordering via callLog): Same setup as Test 1. After the call, walk the mockKmStore.callLog array and assert: index of `op:'putEntity'` < every index of `op:'addRelation', type:'mentions'` < index of `op:'addRelation', type:'capturedBy'`. The has_insight edge can land before OR after the capturedBy edge — both are post-putEntity. NO `findRelations` call interleaves between putEntity and the addRelation calls in a way that would let a reader observe an orphan-Insight.
    - Test 3 (EDGE-02 — exporter-debounce envelope, concurrent reader cannot see orphan): Use a Promise-based barrier where a "reader" Promise calls `mockKmStore.findByOntologyClass('Insight')` (the queries that /api/v1/entities would issue) at 5 evenly-spaced points during the writer's execution (use a mock that yields the event loop between putEntity and each addRelation via `setImmediate`). Assert: at any point where the reader observes the Insight entity, the same callLog snapshot contains ALL the mentions addRelation calls for that Insight. This test asserts the writer's try-block is the atomic emission envelope: km-core's actual exporter is 5s-debounced (outside the unit-test horizon), and because every emission in the try-block lands within that single debounce window, the JSON-export tick the exporter eventually flushes contains either {pre-write state} or {post-write state, all edges present} — never {post-putEntity, pre-mentions}. The test models this by treating "callLog snapshot at reader-probe time" as the surrogate for "JSON export contents at exporter-flush time".
    - Test 4 (re-emission idempotency — writer): Run the same Test 1 setup twice (two consecutive `_pushInsightToKG` calls with the same entry). After the second call, mockKmStore has exactly 2 mentions edges + 1 capturedBy + 1 has_insight (NOT 4 mentions + 2 capturedBy + 2 has_insight). Idempotency is achieved via mergeAttributes on the second writer call + findRelations dedup before each addRelation.
    - Test 5 (re-emission idempotency — backfill via imported processInsight): Import the exported `processInsight` helper directly: `import { processInsight } from '../../scripts/backfill-insight-mentions.mjs'`. Per W5 + Plan 58-03 Task 1, this helper IS exported as a top-level async function — NO skip path. Build a mock kmStore pre-seeded with an Insight node AND a single existing mentions edge (Insight→e1). Build a stub classifier that returns `['e1', 'e2']`. Call `await processInsight(insightNode, mockKmStore, {classifier: stubClassifier, candidates: [{id:'e1',...}, {id:'e2',...}], source: 'backfill-insight-mentions'})`. Assert: the returned per-Insight record has `mentionsAdded: 1` (e2 only — e1 was dedup-skipped); the mockKmStore now has exactly 2 mentions edges (the pre-existing one + the new e2 edge); the new edge carries `metadata.source === 'backfill-insight-mentions'`. Re-run processInsight a second time with the same inputs — assert `mentionsAdded: 0` (both e1 and e2 are now dedup-skipped). This locks Plan 58-03's idempotency contract from the consumer side.
    - Test 6 (D-04.1 fail-fast — classifier throws): Stub `classifyMentions` to `throw new Error('Proxy down')`. Call _pushInsightToKG. Assert: mockKmStore has ZERO entities and ZERO relations after the call (the Insight was NOT written — D-04.1 fail-fast).
    - Test 7 (classifier returns empty array): Stub `classifyMentions` to return `[]`. Call _pushInsightToKG. Assert: mockKmStore HAS the Insight entity (writer proceeds even with zero mentions) AND has the capturedBy + has_insight edges, but ZERO mentions edges. This is the corner case where SC#1 acceptance still allows up to 2 of 20 Insights to lack mentions edges (≥18 of 20 — not 100%).
    - Test 8 (bridge extension — D-06.2): Pre-seed mockKmStore with an Insight that has capturedBy edge but NO mentions edges. Call `await consolidator._relinkOrphanOnlineInsights()`. Assert: the bridge detected the missing-mentions state, called the classifier, and emitted ≥1 mentions edge. Then call the bridge a SECOND time — assert NO new mentions edges added (idempotency gate from Plan 58-03 Task 2 — `existing.length > 0 → continue`).
  </behavior>
  <read_first>
    - src/live-logging/ObservationConsolidator.js post-Plan-58-02 + post-Plan-58-03 state (the refactored _pushInsightToKG + the extended _relinkOrphanOnlineInsights)
    - src/live-logging/ObservationWriter.test.js (Plan 58-02 mock kmStore template — reuse the createMockKmStore helper or factor it into a shared `test-utils/mockKmStore.js` if not already done)
    - src/live-logging/MentionsClassifier.test.js (Plan 58-01 — stubbing convention for classifyMentions)
    - scripts/backfill-insight-mentions.mjs (Plan 58-03 Task 1 — verify `export async function processInsight` exists at the top level; Test 5 imports this symbol directly)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-PATTERNS.md §1, §2, §5 (all three integration surfaces this test exercises)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-CONTEXT.md success criteria SC#1, SC#2 (what's being acceptance-tested)
  </read_first>
  <action>
    Create `src/live-logging/MentionsAtomicity.integration.test.js` (file name encodes intent — atomicity is the EDGE-02 acceptance signal). Use the same test framework as the rest of Phase 58 (`node --test` + `node:assert/strict` per Plans 58-01 / 58-02 / 58-03). Implement the 8 tests above.

    **Mock infrastructure:**
    - Reuse `createMockKmStore` from Plan 58-02 Task 3 — if it lives in `ObservationWriter.test.js` only, factor it into a shared module `src/live-logging/__test_helpers__/mockKmStore.js` (executor's discretion — but document the choice and grep-prove no copy-paste duplication: `grep -lr "function createMockKmStore" src/` should return ≤ 2 files).
    - Module-mock `MentionsClassifier.js` via Node's `--experimental-test-module-mocks` if available, OR via injecting `classifyMentionsOverride` through the consolidator constructor (executor adds a test-only option `options.__classifyMentionsOverride` IF the existing Plan 58-02 design didn't already provide an injection hook; this is the LEAST-INVASIVE option — add the hook in Plan 58-02 Task 2 as a non-controversial Rule-3 deviation if needed).
    - For Test 5, import `processInsight` directly from `scripts/backfill-insight-mentions.mjs` (Plan 58-03 exports it). NO module-mock needed for that path — pass the stub classifier via `options.classifier` per the exported helper's signature.

    **Atomicity assertion (Test 2):**
    ```
    const log = mockKmStore.callLog;
    const putEntityIdx = log.findIndex(c => c.op === 'putEntity');
    const mentionsIndices = log.map((c, i) => (c.op === 'addRelation' && c.type === 'mentions') ? i : -1).filter(i => i >= 0);
    const capturedByIdx = log.findIndex(c => c.op === 'addRelation' && c.type === 'capturedBy');
    assert.ok(putEntityIdx >= 0, 'putEntity called');
    assert.ok(mentionsIndices.length >= 1, 'at least one mentions edge');
    assert.ok(capturedByIdx >= 0, 'capturedBy edge present');
    for (const mi of mentionsIndices) {
      assert.ok(mi > putEntityIdx, `mentions edge at ${mi} must come after putEntity at ${putEntityIdx}`);
      assert.ok(mi < capturedByIdx, `mentions edge at ${mi} must come before capturedBy at ${capturedByIdx}`);
    }
    ```

    **Concurrent-reader simulation (Test 3 — exporter-debounce envelope):** Override mockKmStore.putEntity to `await new Promise(setImmediate)` between the entity-storage and the return — this yields the event loop, simulating a reader interleave point within the writer's try-block. Schedule a parallel `mockKmStore.findByOntologyClass('Insight')` Promise BEFORE calling _pushInsightToKG; await both; assert the reader's snapshot is one of {pre-write, post-write-with-all-edges} — never {post-putEntity, pre-mentions}. The narrative comment in the test MUST reference the 5s exporter-debounce envelope (NOT "microtask burst") — the contract is "every emission within the writer's try-block lands in the same exporter-tick window", not "every emission is in the same JavaScript microtask".

    **Test 5 — processInsight import path (no skip):** Per W5, the test directly imports the exported helper. Sample skeleton:
    ```
    import { processInsight } from '../../scripts/backfill-insight-mentions.mjs';
    // ... in the test body ...
    const mockStore = createMockKmStore();
    // Pre-seed: one Insight node + one existing mentions edge to e1
    const insightNode = { id: 'i1', name: 'Test', entityType: 'Insight', description: 'discusses e1 and e2' };
    await mockStore.putEntity(insightNode, { skipOntologyCheck: true });
    await mockStore.addRelation({ from: 'i1', to: 'e1', type: 'mentions', metadata: { source: 'pre-existing' } });
    const stubClassifier = async () => ['e1', 'e2'];
    const result = await processInsight(insightNode, mockStore, {
      classifier: stubClassifier,
      candidates: [{ id: 'e1', name: 'A', description: '' }, { id: 'e2', name: 'B', description: '' }],
      source: 'backfill-insight-mentions',
    });
    assert.strictEqual(result.mentionsAdded, 1, 'only e2 should be added; e1 was already there');
    // Re-run for idempotency assertion
    const result2 = await processInsight(insightNode, mockStore, { classifier: stubClassifier, candidates: ..., source: ... });
    assert.strictEqual(result2.mentionsAdded, 0, 'both e1 and e2 are now dedup-skipped');
    ```
    NO skip path — if `import { processInsight }` throws ENOENT or the symbol is `undefined`, that is a Plan 58-03 contract regression and the test FAILS (as intended — the import wires Phase 58 plans together). The acceptance criterion below requires exactly 8 passing tests.

    Test 8 may need the consolidator to expose internal helpers as exports. If not currently exported, treat this as a Rule-3 deviation — add the export in this plan (small, non-controversial) and grep-prove the addition: `grep -c "export.*_relinkOrphanOnlineInsights" src/live-logging/ObservationConsolidator.js` ≥ 1 if needed.

    Per CLAUDE.md, no `console.*` — use `process.stderr.write` for diagnostic output.
  </action>
  <verify>
    <automated>node --test src/live-logging/MentionsAtomicity.integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/live-logging/MentionsAtomicity.integration.test.js` exits 0.
    - stdout contains `# pass 8` and `# fail 0` (W5 — Test 5 imports processInsight directly and contributes a passing test; no `# skipped` for any of the 8 tests).
    - Test names visible in stdout include strings matching: `EDGE-01`, `EDGE-02`, `atomicity`, `idempot`, `fail-fast`, `bridge`, `processInsight` (one identifier per test — proves the test set is mapped to the acceptance criteria, not just an opaque suite, AND Test 5's import-path is verifiable).
    - `grep -c "putEntityIdx\\|callLog\\|exporter.*debounce\\|setImmediate" src/live-logging/MentionsAtomicity.integration.test.js` ≥ 2 (atomicity-ordering assertion AND concurrent-reader simulation are both present; the narrative references exporter-debounce, not microtask).
    - `grep -c "classifyMentionsOverride\\|__test_helpers__\\|module-mock" src/live-logging/MentionsAtomicity.integration.test.js` ≥ 1 (classifier is stubbed, not hitting the live LLM).
    - `grep -cE "import\\s*\\{\\s*processInsight\\s*\\}" src/live-logging/MentionsAtomicity.integration.test.js` ≥ 1 (Test 5 imports the exported helper from Plan 58-03 — W5 contract).
    - The test does NOT contain a skip path for Test 5: `grep -cE "skip|t\\.skip|test\\.skip" src/live-logging/MentionsAtomicity.integration.test.js` == 0 (zero skip directives — every test exercises its assertion path).
    - `grep -v '^\\s*\\*' src/live-logging/MentionsAtomicity.integration.test.js | grep -v '^\\s*//' | grep -E "console\\.(log|error|warn|info)" | wc -l` == 0.
    - The whole `src/live-logging/` test suite (across all 4 plans) still passes — run `node --test src/live-logging/*.test.js src/live-logging/*.integration.test.js` and confirm exit 0 with zero failures.
  </acceptance_criteria>
  <done>All 8 integration tests pass — none skip. EDGE-01 + EDGE-02 acceptance signals are locked at the integration level. The atomicity ordering is provable via callLog index assertions within the writer's try-block (the exporter-debounce envelope the JSON exporter eventually serializes), not just narrative comments. Test 5 imports `processInsight` from `scripts/backfill-insight-mentions.mjs` directly per the W5 export-symbol contract.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage check script — scripts/check-insight-mentions-coverage.mjs (SC#1 gate)</name>
  <files>scripts/check-insight-mentions-coverage.mjs</files>
  <read_first>
    - integrations/mcp-server-semantic-analysis/scripts/check-l2-emission-rate.mjs (Phase 57-04 analog, 229 LoC — full structural template: CLI flags, random sampling, exit codes, summary output shape)
    - scripts/backfill-insight-mentions.mjs (Plan 58-03 Task 1 — read the JSON export shape probe; reuse the same node/edge field-access patterns)
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-04-SUMMARY.md § "Operator Runbook" (how Phase 57-04 wrote its check script's invocation contract — Phase 58 mirrors)
    - .planning/ROADMAP.md Phase 58 SC#1 (the exact acceptance language: "Sampling 20 random recent online-learned Insights, at least 18 carry at least one semantic-content relation type beyond capturedBy.")
  </read_first>
  <action>
    Create `scripts/check-insight-mentions-coverage.mjs` modeled on `integrations/mcp-server-semantic-analysis/scripts/check-l2-emission-rate.mjs`. The script MUST:

    1. Be a Node ES-module with `#!/usr/bin/env node` shebang and executable permission.
    2. Implement `parseArgs(argv)` with flags:
       - `--sample N` (default 20) — number of Insights to sample
       - `--min M` (default 18) — threshold: at least M of the N must carry ≥1 mentions edge
       - `--source PATH` (default `.data/knowledge-graph/exports/general.json`)
       - `--recent-only` (default true; pass `--no-recent-only` to disable) — when true, sort Insights by `metadata.createdAt` descending and sample from the first 2×N before random-picking N (SC#1 says "recent" Insights)
       - `--help`
    3. Read the JSON export. Identify Insight nodes via `entityType==='Insight'`. Build a `Set<insightId>` of those carrying ≥1 mentions edge by scanning `.edges[]` for `attributes.type === 'mentions'`.
    4. Sample N Insights at random from the eligible pool (post-`--recent-only` filter if enabled). Use a deterministic seed if `--seed=` flag is passed for reproducibility; otherwise `Math.random()`.
    5. Count how many of the N have ≥1 mentions edge (i.e., are in the Set).
    6. Print a one-line summary to stdout: `[check-58] sample=N covered=K threshold=M result=PASS|FAIL` (single line, parseable).
    7. Exit 0 if `K >= M`, exit 1 otherwise.
    8. Per CONTEXT SC#1, the script is the binary acceptance gate. It is the executable codification of "EDGE-01 ships".

    The script is OPERATOR-RUNNABLE — no obs-api downtime needed (it reads the JSON export, not the LevelDB). It can be run anytime AFTER Plan 58-03 Task 3 closes (the live backfill).

    Per CLAUDE.md, no `console.*` — use `process.stdout.write` for the result line and `process.stderr.write` for diagnostics.
  </action>
  <verify>
    <automated>node --check scripts/check-insight-mentions-coverage.mjs && node scripts/check-insight-mentions-coverage.mjs --help</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/check-insight-mentions-coverage.mjs` exits 0.
    - `head -1 scripts/check-insight-mentions-coverage.mjs` equals `#!/usr/bin/env node`.
    - `test -x scripts/check-insight-mentions-coverage.mjs`.
    - `node scripts/check-insight-mentions-coverage.mjs --help` exits 0 and stdout includes documentation for `--sample`, `--min`, `--source`, `--recent-only`.
    - `node scripts/check-insight-mentions-coverage.mjs --sample 20 --min 18` exits with code 0 OR 1 (NEVER any other code) AND stdout contains a single line matching `^\\[check-58\\] sample=\\d+ covered=\\d+ threshold=\\d+ result=(PASS|FAIL)$`.
    - Running against a synthetic test fixture WHERE 0 of N Insights carry mentions edges, the script exits 1 with `result=FAIL`. Running against a synthetic fixture where 19 of 20 carry mentions edges, the script exits 0 with `result=PASS`. Create these fixtures inline in the verify step OR document the manual smoke procedure in the SUMMARY.
    - `grep -c "mentions" scripts/check-insight-mentions-coverage.mjs` ≥ 3 (edge-type identifier appears in the filter logic, the count logic, and the result line).
    - `grep -v '^\\s*\\*' scripts/check-insight-mentions-coverage.mjs | grep -v '^\\s*//' | grep -E "console\\.(log|error|warn|info)" | wc -l` == 0.
  </acceptance_criteria>
  <done>The coverage script lands, parses, runs with --help, and exits with the documented contract (0=PASS / 1=FAIL on the threshold check). The single-line stdout summary is parseable so CI/operators can capture it without parsing prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test ↔ in-memory mock kmStore | No external surfaces; all assertions are deterministic against the mock callLog. |
| Coverage script ↔ JSON export | Read-only operation; the script never writes to the graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Tampering | test stub for classifyMentions | accept | The stub is test-internal. If a future plan changes the production classifier shape, the stub must be updated; the test will fail loudly. Acceptance: a failing test is a feature here, not a bug. |
| T-58-04-02 | Information Disclosure | coverage script reveals graph statistics | accept | The graph statistics are already public via `/api/v1/entities` reader. The script reads the same data via a different path. |
| T-58-04-03 | Denial of Service | sampling 20 of 96 with a deterministic seed = stable test | mitigate | When `--seed` is passed, the sampling is reproducible. Default unseeded random sampling matches SC#1 acceptance language ("20 random recent Insights") — operator can re-run for variance, but the threshold ≥ 18 of 20 has a generous margin for sampling noise. |
| T-58-04-SC | Tampering | npm package installs | mitigate | No new package installs. Node built-ins only (`node:fs/promises`, `node:test`). |
</threat_model>

<verification>
- `node --test src/live-logging/MentionsAtomicity.integration.test.js` exits 0 with 8 passing tests (none skipped — W5 contract).
- `node --check scripts/check-insight-mentions-coverage.mjs` exits 0.
- `node scripts/check-insight-mentions-coverage.mjs --help` exits 0 and documents the four CLI flags.
- The full `src/live-logging/` test suite (all 4 phase-58 plans combined) passes without regression.
- After Plan 58-03 Task 3's live backfill closes, `node scripts/check-insight-mentions-coverage.mjs --sample 20 --min 18` exits 0 — locking SC#1.
- Phase 58 ROADMAP success criteria mapping:
  - SC#1 → `check-insight-mentions-coverage.mjs` (binary gate)
  - SC#2 → `MentionsAtomicity.integration.test.js` Test 2 + Test 3 (atomicity ordering within the writer's try-block + concurrent reader cannot see orphan — the unit-level surrogate for the exporter-debounce envelope contract)
  - SC#3 → out of scope for this plan (viewer rendering is Phase 60 LOWERONTO-03 work — operator-visual check at milestone close)
</verification>

<success_criteria>
- Integration test landed and all 8 tests pass (none skipped).
- Coverage script landed, executable, and contract-compliant (exit 0 or 1 with parseable single-line summary).
- EDGE-01 + EDGE-02 acceptance is now traceable to specific test names and a specific CLI invocation — no plan SUMMARY needs to "claim" the acceptance; the test/script provides it on demand.
- Test 5 imports `processInsight` from `scripts/backfill-insight-mentions.mjs` directly — the Plan 58-03 export contract is exercised, not skipped.
- Operator has a one-line command (`node scripts/check-insight-mentions-coverage.mjs --sample 20 --min 18`) that returns PASS/FAIL for SC#1 after the live backfill.
</success_criteria>

<output>
Create `.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-04-SUMMARY.md` when done. Include in the SUMMARY (a) the live output of `node scripts/check-insight-mentions-coverage.mjs --sample 20 --min 18` if the operator has already executed Plan 58-03 Task 3, OR (b) a verification-debt note flagging that the script will produce its PASS/FAIL signal after the operator-executed backfill, mirroring Phase 57-04's Verification Debt pattern.
</output>
