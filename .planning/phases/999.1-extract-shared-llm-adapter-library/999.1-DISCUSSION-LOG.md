# Phase 999.1: Extract Shared LLM Adapter Library - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-14
**Phase:** 999.1-extract-shared-llm-adapter-library
**Areas discussed:** Phase intent (single question, closure decision)

---

## Phase intent

Pre-discussion scout findings surfaced that the ROADMAP entry for Phase 999.1 predated work that had already landed:

- The "direct HTTP path for Claude Max" goal shipped in `~/Agentic/_work/rapid-llm-proxy` commit `c043362` (2026-05-19), implemented as `completeClaudeCodeDirect()` in `proxy-bridge/server.mjs:873`. Probed end-to-end at ~0.9s and 9 input + 4 output tokens for "say OK", vs ~7.5s and 14.9K tokens via the CLI path.
- The "shared LLM library" goal was delivered as the standalone npm package `@rapid/llm-proxy`, consumed by `coding` (`src/llm-proxy/llm-proxy.mjs` thin wrapper + `lib/km-core/src/dedup/LLMSemanticMatcher.ts` + four `integrations/mcp-server-semantic-analysis/src/agents/*.ts` callers) and `rapid-automations/OKM` (`src/index.ts`, `src/ingestion/{extractor,deduplicator}.ts`, `src/api/server.ts`, plus a Phase 43 D-G5.1 test-only shim at `src/llm/{index,llm-service}.ts`).
- The `lib/llm/` directory referenced by the original phase title was **never created** in the `coding` repo. Phase 50's discussion log (`.planning/phases/50-…/50-DISCUSSION-LOG.md:71`) flagged this explicitly: *"`lib/llm/` referenced in CLAUDE.md is the Phase 999.1 target, not yet created."*

The user was asked how to frame the phase in light of those findings.

| Option | Description | Selected |
|--------|-------------|----------|
| Close as already-done | Mark Phase 999.1 done — the two listed goals were absorbed by the rapid-llm-proxy submodule + Phase 42.2 work. No further phase needed; write a CONTEXT.md that records this as the closure rationale and update ROADMAP/STATE. | ✓ |
| Reframe — make `@rapid/llm-proxy` a git submodule of `coding` | Currently `@rapid/llm-proxy` is a standalone repo consumed via npm/tgz. Reframe phase to vendor it as a git submodule under `coding/lib/llm/` (and likewise under rapid-automations) for single-tree visibility + atomic cross-repo PRs. | |
| Reframe — coding-side adapter/config layer | Keep `@rapid/llm-proxy` upstream as-is, but build a coding-specific adapter/config layer (e.g., `lib/llm/` with `processOverrides`, route-by-process helpers, retry policies, `taskType` routing) that today is scattered across scripts/integrations. Centralize without forking the proxy. | |
| Something else / let me describe | The actual goal is different — operator would explain what they really want. | |

**User's choice:** Close as already-done.
**Notes:** No follow-up scope was requested. The four "deferred ideas" captured in CONTEXT.md (typed `/api/complete` client for coding scripts, OKM test-shim cleanup, npm-registry publication of `@rapid/llm-proxy`, embeddings via the proxy) are recorded for future consideration but not promoted to any phase.

---

## Claude's Discretion

- Form of the closure record (CONTEXT.md content, ROADMAP marking, STATE.md update wording) was left to Claude per the close-as-already-done choice.

## Deferred Ideas

Captured in `999.1-CONTEXT.md` `<deferred>` section. None require immediate roadmap promotion.
