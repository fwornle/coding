---
phase: 21-mastracode-agent-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/combined-status-line.js
  - scripts/statusline-health-monitor.js
  - scripts/global-process-supervisor.js
  - scripts/health-remediation-actions.js
autonomous: true
requirements: [MSTR-02]

must_haves:
  truths:
    - "Mastracode session appears in tmux statusline with unique color and icon"
    - "Health monitor detects mastracode process and reports status"
    - "If the mastracode session crashes, the process supervisor detects and attempts restart (same behavior as other agents)"
  artifacts:
    - path: "scripts/combined-status-line.js"
      provides: "Mastra agent display in combined statusline"
      contains: "mastra"
    - path: "scripts/statusline-health-monitor.js"
      provides: "Mastra agent detection and health reporting"
      contains: "mastra"
    - path: "scripts/global-process-supervisor.js"
      provides: "Mastra process type in supervision"
      contains: "mastra"
    - path: "scripts/health-remediation-actions.js"
      provides: "Mastra health remediation checks"
      contains: "mastra"
  key_links:
    - from: "scripts/statusline-health-monitor.js"
      to: "mastra process"
      via: "pgrep or ps detection for mastra agent type"
      pattern: "mastra"
    - from: "scripts/combined-status-line.js"
      to: "scripts/statusline-health-monitor.js"
      via: "reads agent status data"
      pattern: "mastra"
---

<objective>
Register mastracode in the statusline, health monitor, process supervisor, and remediation systems so that running mastracode sessions are visible and managed like Claude/Copilot/OpenCode sessions.

Purpose: MSTR-02 requires mastracode sessions to appear in tmux statusline with LSL indicator and health monitoring.
Output: Updated statusline, health monitor, process supervisor, and remediation scripts with mastra agent support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-mastracode-agent-integration/21-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mastra agent to statusline and health monitor</name>
  <files>scripts/combined-status-line.js, scripts/statusline-health-monitor.js</files>
  <read_first>
    scripts/combined-status-line.js
    scripts/statusline-health-monitor.js
  </read_first>
  <action>
Per D-12: Same combined statusline pattern as all other agents.
Per D-13: Unique color + icon to distinguish mastracode.
Per D-14: Same recovery infrastructure as other agents.

In scripts/statusline-health-monitor.js:
- Find where agent types are enumerated/detected (look for patterns like `opencode|copilot` or agent type arrays)
- Add `mastra` to all agent detection logic alongside existing agents
- In the capture file regex (around line 80), add mastra: change `(opencode|copilot)` to `(opencode|copilot|mastra)` in any capture file patterns
- In the tmux session pattern, add `coding-mastra-` alongside `coding-opencode-`
- In getRunningAgentSessions (around line 295), add mastra process detection:
  - Detect via ps for mastracode processes, OR
  - Detect via tmux session name `coding-mastra-*`

In scripts/combined-status-line.js:
- Find where agent-specific colors/icons are defined
- Add mastra entry with a unique color and icon. Use:
  - Icon: a purple/magenta diamond or star (something visually distinct from claude/copilot/opencode)
  - Color: magenta/purple tmux color (e.g., `#colour13` or `#b48ead` or similar)
- Register "mastra" in any agent type lists/maps/switch statements
- Ensure the statusline segment for mastra shows: icon + "Mastra" + LSL indicator (same format as other agents)

IMPORTANT: Read both files FULLY before editing. These are complex scripts -- find all locations where agent names are listed and add mastra to each one. Missing even one location means the agent won't appear correctly.
  </action>
  <verify>
    <automated>grep -c 'mastra' /Users/Q284340/Agentic/coding/scripts/combined-status-line.js && grep -c 'mastra' /Users/Q284340/Agentic/coding/scripts/statusline-health-monitor.js</automated>
  </verify>
  <acceptance_criteria>
    - scripts/combined-status-line.js contains "mastra" in agent type lists/maps
    - scripts/combined-status-line.js contains a unique color for mastra (distinct from claude/copilot/opencode)
    - scripts/combined-status-line.js contains an icon for mastra
    - scripts/statusline-health-monitor.js contains "mastra" in agent detection regex/patterns
    - scripts/statusline-health-monitor.js contains "mastra" in capture file pattern (if applicable)
    - `node -c scripts/combined-status-line.js` exits 0 (valid syntax -- use `node --check` equivalent)
    - `node -c scripts/statusline-health-monitor.js` exits 0
  </acceptance_criteria>
  <done>Mastracode appears in tmux statusline with unique color/icon. Health monitor detects running mastra sessions and reports status. Same display pattern as claude/copilot/opencode.</done>
</task>

<task type="auto">
  <name>Task 2: Add mastra to process supervisor and health remediation</name>
  <files>scripts/global-process-supervisor.js, scripts/health-remediation-actions.js</files>
  <read_first>
    scripts/global-process-supervisor.js
    scripts/health-remediation-actions.js
  </read_first>
  <action>
Per D-14: Same recovery infrastructure as other agents -- no special handling.

In scripts/global-process-supervisor.js:
- Find where agent types or process types are listed (look for claude/opencode/copilot patterns)
- Add "mastra" to those lists
- Ensure the supervisor can detect mastra processes (tmux session pattern: `coding-mastra-*`)
- If there's a process health check function, add mastra alongside existing agents

In scripts/health-remediation-actions.js:
- Find where agent-specific remediation checks are defined
- Add mastra remediation following the same pattern as existing agents
- The remediation should handle: process restart, health endpoint check, LLM proxy connectivity
- Per D-15: LLM proxy unreachable at startup should warn, not block -- so remediation should log a warning for proxy issues but not mark mastra as "unhealthy" just because proxy is down

IMPORTANT: Read both files FULLY before editing. Find ALL locations where agent names appear and add mastra to each one.
  </action>
  <verify>
    <automated>grep -c 'mastra' /Users/Q284340/Agentic/coding/scripts/global-process-supervisor.js && grep -c 'mastra' /Users/Q284340/Agentic/coding/scripts/health-remediation-actions.js</automated>
  </verify>
  <acceptance_criteria>
    - scripts/global-process-supervisor.js contains "mastra" in agent/process type lists
    - scripts/health-remediation-actions.js contains "mastra" in remediation checks
    - `node -c scripts/global-process-supervisor.js` exits 0
    - `node -c scripts/health-remediation-actions.js` exits 0
    - Mastra remediation does NOT treat LLM proxy unavailability as a blocking failure
  </acceptance_criteria>
  <done>Process supervisor manages mastra agent type with crash detection and restart attempts (same behavior as other agents). Health remediation includes mastra checks with non-blocking LLM proxy warning. Both follow identical patterns to existing agents.</done>
</task>

</tasks>

<verification>
- All four JS files pass syntax check (`node -c <file>`)
- grep confirms "mastra" appears in all four files
- No other agent references (claude/copilot/opencode) are missing from any list where mastra was added
</verification>

<success_criteria>
- Mastracode sessions visible in tmux statusline with unique color/icon per D-13
- Health monitor detects and reports mastra session status
- Process supervisor manages mastra processes with crash detection and restart
- Health remediation handles mastra with non-blocking LLM proxy per D-15
- All changes follow identical patterns to existing agent integrations per D-14
</success_criteria>

<output>
After completion, create `.planning/phases/21-mastracode-agent-integration/21-02-SUMMARY.md`
</output>
