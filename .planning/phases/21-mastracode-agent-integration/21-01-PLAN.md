---
phase: 21-mastracode-agent-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/agents/mastra.sh
  - scripts/launch-mastra.sh
  - bin/coding
autonomous: true
requirements: [MSTR-01]

must_haves:
  truths:
    - "User can run 'coding --mastra' and a tmux session launches with mastracode TUI"
    - "Agent adapter follows identical structure to config/agents/opencode.sh"
    - "Launch wrapper follows identical structure to scripts/launch-claude.sh"
  artifacts:
    - path: "config/agents/mastra.sh"
      provides: "Mastra agent adapter with AGENT_NAME, AGENT_COMMAND, hooks"
      contains: "AGENT_NAME=\"mastra\""
    - path: "scripts/launch-mastra.sh"
      provides: "Thin launch wrapper sourcing launch-agent-common.sh"
      contains: "launch_agent"
    - path: "bin/coding"
      provides: "Main launcher with --mastra flag"
      contains: "--mastra"
  key_links:
    - from: "bin/coding"
      to: "scripts/launch-mastra.sh"
      via: "exec launch script when --mastra flag passed"
      pattern: "FORCE_AGENT=\"mastra\""
    - from: "scripts/launch-mastra.sh"
      to: "config/agents/mastra.sh"
      via: "launch_agent function argument"
      pattern: "config/agents/mastra.sh"
    - from: "config/agents/mastra.sh"
      to: "src/llm-proxy/llm-proxy.mjs"
      via: "agent_pre_launch validates LLM proxy reachability"
      pattern: "llm-proxy"
---

<objective>
Create the mastracode agent adapter, launch wrapper, and main launcher flag so that `coding --mastra` starts a mastracode session inside the standard tmux layout.

Purpose: MSTR-01 requires users to start mastracode via `coding --mastra` with proper tmux session setup.
Output: config/agents/mastra.sh, scripts/launch-mastra.sh, updated bin/coding
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mastracode-agent-integration/21-CONTEXT.md
@.planning/phases/20-foundation-opencode-om/20-01-SUMMARY.md

<interfaces>
<!-- Existing agent adapter pattern (from config/agents/opencode.sh): -->
AGENT_NAME="opencode"
AGENT_DISPLAY_NAME="OpenCode"
AGENT_COMMAND="opencode"
AGENT_SESSION_PREFIX="opencode"
AGENT_SESSION_VAR="OPENCODE_SESSION_ID"
AGENT_TRANSCRIPT_FMT="opencode"
AGENT_ENABLE_PIPE_CAPTURE=true
AGENT_PROMPT_REGEX='>\s+([^\n\r]+)[\n\r]'
AGENT_REQUIRES_COMMANDS="opencode"
agent_check_requirements() { ... }
agent_pre_launch() { ... }

<!-- Existing launch wrapper pattern (from scripts/launch-claude.sh): -->
source "$SCRIPT_DIR/agent-common-setup.sh"
source "$SCRIPT_DIR/launch-agent-common.sh"
launch_agent "$CODING_REPO/config/agents/claude.sh" "$@"

<!-- Main launcher flag pattern (from bin/coding lines 174-191): -->
    --claude)
      FORCE_AGENT="claude"
      shift
      ;;
    --opencode)
      FORCE_AGENT="opencode"
      shift
      ;;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mastra agent adapter and launch wrapper</name>
  <files>config/agents/mastra.sh, scripts/launch-mastra.sh</files>
  <read_first>
    config/agents/opencode.sh
    config/agents/claude.sh
    scripts/launch-claude.sh
    scripts/launch-agent-common.sh (lines 1-30 for hook function interface)
    src/llm-proxy/llm-proxy.mjs (lines 1-20 for health endpoint path)
    .observations/config.json
  </read_first>
  <action>
Create config/agents/mastra.sh following the exact structure of opencode.sh:

```bash
#!/bin/bash
# Agent definition: Mastracode
# Sourced by launch-agent-common.sh
#
# Mastracode uses @mastra/opencode with observational memory plugin.
# All LLM calls route through the coding LLM proxy (per D-07).

AGENT_NAME="mastra"
AGENT_DISPLAY_NAME="Mastracode"
AGENT_COMMAND="opencode"
AGENT_SESSION_PREFIX="mastra"
AGENT_SESSION_VAR="MASTRA_SESSION_ID"
AGENT_TRANSCRIPT_FMT="mastra"
AGENT_ENABLE_PIPE_CAPTURE=false
AGENT_REQUIRES_COMMANDS="opencode"
```

For agent_check_requirements():
- Check `command -v opencode` exists (same as opencode.sh)
- Check that `.opencode/mastra.json` plugin config exists (Phase 20 artifact)
- If missing, print error with guidance: "Run install.sh to set up @mastra/opencode"

For agent_pre_launch():
- Per D-07: All LLM calls route through Phase 20's llm-proxy.mjs only
- Per D-06: Detect missing config/auth and guide user through inline setup
- Per D-15: Check LLM proxy reachability via `curl -sf http://localhost:3033/health` (warn and continue if unreachable, do not block)
- Network-adaptive model selection following opencode.sh INSIDE_CN pattern:
  - VPN: `export OPENCODE_CONFIG_CONTENT='{"model":"github-copilot-enterprise/claude-opus-4.6","disabled_providers":["anthropic"]}'`
  - Outside VPN: `export OPENCODE_CONFIG_CONTENT='{"model":"claude-opus-4-6","disabled_providers":["copilot"]}'`
- Set `MASTRA_PLUGIN_ENABLED=true` environment variable
- Call `validate_agent_connectivity "$AGENT_NAME" || true` (warn only, per D-15)

AGENT_ENABLE_PIPE_CAPTURE must be false per D-08 (lifecycle hooks, not pipe-pane).

Create scripts/launch-mastra.sh following the exact structure of scripts/launch-claude.sh:

```bash
#!/bin/bash
# Launch Mastracode with observational memory
# Thin wrapper -- all shared logic lives in launch-agent-common.sh

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CODING_REPO="$(dirname "$SCRIPT_DIR")"
export CODING_REPO

source "$SCRIPT_DIR/agent-common-setup.sh"
source "$SCRIPT_DIR/launch-agent-common.sh"
launch_agent "$CODING_REPO/config/agents/mastra.sh" "$@"
```

Make launch-mastra.sh executable: `chmod +x scripts/launch-mastra.sh`
  </action>
  <verify>
    <automated>bash -c 'source /Users/Q284340/Agentic/coding/config/agents/mastra.sh 2>/dev/null; echo "AGENT_NAME=$AGENT_NAME AGENT_DISPLAY_NAME=$AGENT_DISPLAY_NAME AGENT_COMMAND=$AGENT_COMMAND AGENT_ENABLE_PIPE_CAPTURE=$AGENT_ENABLE_PIPE_CAPTURE"' && test -x /Users/Q284340/Agentic/coding/scripts/launch-mastra.sh && grep -q 'launch_agent.*config/agents/mastra.sh' /Users/Q284340/Agentic/coding/scripts/launch-mastra.sh</automated>
  </verify>
  <acceptance_criteria>
    - config/agents/mastra.sh contains `AGENT_NAME="mastra"`
    - config/agents/mastra.sh contains `AGENT_DISPLAY_NAME="Mastracode"`
    - config/agents/mastra.sh contains `AGENT_COMMAND="opencode"`
    - config/agents/mastra.sh contains `AGENT_ENABLE_PIPE_CAPTURE=false`
    - config/agents/mastra.sh contains `agent_check_requirements()` function
    - config/agents/mastra.sh contains `agent_pre_launch()` function
    - config/agents/mastra.sh contains `validate_agent_connectivity` call
    - config/agents/mastra.sh contains curl health check with "warn" (not exit/abort)
    - scripts/launch-mastra.sh is executable (chmod +x)
    - scripts/launch-mastra.sh contains `launch_agent "$CODING_REPO/config/agents/mastra.sh"`
    - scripts/launch-mastra.sh contains `source "$SCRIPT_DIR/agent-common-setup.sh"`
    - scripts/launch-mastra.sh contains `source "$SCRIPT_DIR/launch-agent-common.sh"`
  </acceptance_criteria>
  <done>Agent adapter defines mastra agent with all required variables and hooks. Launch wrapper sources common framework and delegates to adapter. Both files follow the exact pattern of existing agents.</done>
</task>

<task type="auto">
  <name>Task 2: Add --mastra flag to main launcher and help text</name>
  <files>bin/coding</files>
  <read_first>
    bin/coding
  </read_first>
  <action>
Add `--mastra` flag to bin/coding in three locations:

1. In the argument parser (after the `--opencode)` case around line 191), add:
```bash
    --mastra)
      FORCE_AGENT="mastra"
      shift
      ;;
```

2. In the show_help() function (after `--opencode` line around line 125), add:
```
  --mastra           Force Mastracode (same as --agent mastra)
```

3. In the AGENTS section of help text (after the opencode line around line 149), add:
```
  mastra     Mastracode with observational memory (mastra.ai)
```

4. In the EXAMPLES section, add:
```
  coding-agent --mastra           # Force Mastracode
```

Do NOT change any other part of bin/coding. This is a surgical addition of 4 lines.
  </action>
  <verify>
    <automated>grep -c '\-\-mastra' /Users/Q284340/Agentic/coding/bin/coding | grep -q '[2-9]' && grep -q 'FORCE_AGENT="mastra"' /Users/Q284340/Agentic/coding/bin/coding && grep -q 'Mastracode' /Users/Q284340/Agentic/coding/bin/coding</automated>
  </verify>
  <acceptance_criteria>
    - bin/coding contains `--mastra)` case in argument parser
    - bin/coding contains `FORCE_AGENT="mastra"` inside that case
    - bin/coding show_help contains `--mastra` with description "Force Mastracode"
    - bin/coding AGENTS section contains `mastra` entry
    - `coding --help` output includes --mastra flag
    - No other lines in bin/coding are modified
  </acceptance_criteria>
  <done>`coding --mastra` sets FORCE_AGENT=mastra and the help text documents the flag. The existing launcher logic resolves config/agents/mastra.sh and calls scripts/launch-mastra.sh automatically.</done>
</task>

</tasks>

<verification>
- `bash -n config/agents/mastra.sh` passes (valid bash syntax)
- `bash -n scripts/launch-mastra.sh` passes (valid bash syntax)
- `grep 'FORCE_AGENT="mastra"' bin/coding` matches
- `coding --help 2>&1 | grep -q mastra` succeeds
- Agent config file exists at config/agents/mastra.sh (launcher will find it)
</verification>

<success_criteria>
- `coding --mastra` resolves to launch-mastra.sh which delegates to launch-agent-common.sh with mastra adapter
- Agent adapter has correct AGENT_NAME, AGENT_COMMAND, hooks following opencode.sh pattern
- AGENT_ENABLE_PIPE_CAPTURE=false per D-08 (lifecycle hooks, not pipe-pane)
- LLM proxy health check warns but does not block per D-15
- Help text shows --mastra as a valid option
</success_criteria>

<output>
After completion, create `.planning/phases/21-mastracode-agent-integration/21-01-SUMMARY.md`
</output>
