---
phase: 52-dashboard-llm-routing-label-process-tag-observability-fix
plan: 03
type: execute
wave: 3
depends_on: [52-01, 52-02]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts
  - integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx
  - integrations/mcp-server-semantic-analysis (outer-repo pointer bump)
autonomous: false
requirements: [D-12, D-13, D-14, D-15]
tags: [progress-emission, dashboard-ux, wave-controller, throttled-write]

must_haves:
  truths:
    - "While a wave is running, the dashboard wave row shows a live {n}/{N} counter that updates within 5 seconds of the next item completion"
    - "When the next item lands, dashboard updates within 5 seconds (or 5 items completed, whichever is later)"
    - "When itemsTotal is missing from workflow-progress.json (older runs OR runs before this plan), the dashboard falls back to the legacy arrow render"
    - "All four waves (wave1, wave2, wave3, wave4) emit per-item progress through the wave-controller-owned writer path that bypasses coordinator.preserveFromExisting"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts"
      provides: "maybeEmitItemProgress throttle helper + per-wave instrumentation in runWithConcurrency onTaskComplete callbacks"
      contains: "maybeEmitItemProgress"
    - path: "integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx"
      provides: "ItemProgressBadge component + wave row render integration"
      contains: "ItemProgressBadge"
  key_links:
    - from: "wave-controller.ts runWithConcurrency onTaskComplete (wave1/2/3)"
      to: "this.updateStepOutputs(stepName, { itemsCompleted, itemsTotal })"
      via: "throttled call inside the existing onTaskComplete callback"
      pattern: "maybeEmitItemProgress"
    - from: "wave-controller.ts insightTasks loop (wave4)"
      to: "this.updateStepOutputs('wave4_insights', { ..., itemsCompleted: generated + failed, itemsTotal: planned })"
      via: "field expansion of the existing emission at line 2851"
      pattern: "itemsCompleted: generated"
    - from: "trace-modal.tsx wave row"
      to: "<ItemProgressBadge completed={wg.itemsCompleted} total={wg.itemsTotal} />"
      via: "conditional render guard wg.itemsTotal != null && wg.itemsTotal > 0"
      pattern: "ItemProgressBadge"
---

<objective>
Emit throttled per-item progress updates from inside wave1/2/3/4 loops in `wave-controller.ts` so operators see live `{n}/{N}` counters and a progress bar on each wave row in the dashboard's workflow trace modal — replacing today's `0 → N` jumps with continuous feedback. Honors Phase 42.2-02 single-writer architecture by routing through `wave-controller`'s OWN writer path (`updateStepOutputs` → `touchProgress` → `fs.writeFileSync(this.progressFile, …)`), NOT through `coordinator.writeProgressPreservingDetails`.

Purpose: A 68m 42s wave with no intermediate feedback is operationally indistinguishable from a hung process. NOTES.md captured this as Issue 3. D-12 bundles the fix into Phase 52 so the dashboard-UX story (live badges + live progress) ships as one coherent PR.

Output:
- `wave-controller.ts`: new private `maybeEmitItemProgress(stepName, completed, total)` helper with K=5 items / 2000ms throttle. Wired into the `onTaskComplete` callback of `runWithConcurrency` for wave1/2/3 component-classify loops; wave4 emission at line ~2851 expanded with `itemsCompleted: generated + failed, itemsTotal: planned`.
- `trace-modal.tsx`: new `ItemProgressBadge` memoized component (inline `{n}/{N}` + thin progress bar). Wave row render integrates it conditionally; legacy arrow display unchanged when `itemsTotal` is missing (D-15 backward compat).
- `WaveGroup` reducer (already in trace-modal.tsx around lines 404-482) extended to surface `itemsCompleted/itemsTotal` from `step.outputs` to the wave-level aggregate.
- Submodule + outer-repo pointer-bump commit pair for the wave-controller change.
- Dashboard rebuilt + frontend supervisorctl-restarted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/52-CONTEXT.md
@.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/52-PATTERNS.md
@.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/NOTES.md
@.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/52-01-PLAN.md
@.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/52-02-PLAN.md

<interfaces>
<!-- Existing wave-controller.ts surfaces (verified — DO NOT modify) -->

From wave-controller.ts:257-262 — updateStepOutputs (modify the call sites, NOT the helper):
  private updateStepOutputs(stepName: string, outputs: Record<string, unknown>): void {
    const existing = this.stepMetrics.get(stepName) ?? {};
    const mergedOutputs = { ...((existing as any).outputs ?? {}), ...outputs };
    this.stepMetrics.set(stepName, { ...existing, outputs: mergedOutputs });
    this.touchProgress();
  }

From wave-controller.ts:144 — touchProgress (read-merge-write against this.progressFile, bypasses coordinator.preserveFromExisting per the Phase 42.2-02 single-writer split — wave-controller OWNS the wave*.outputs namespace).

From wave-controller.ts:2851 (wave4 existing emission — extend this exact line):
  this.updateStepOutputs('wave4_insights', { planned, generated, failed, skippedDiagrams });

From wave-controller.ts:2943 — runWithConcurrency(tasks, concurrency, onTaskComplete?). The third arg is the per-task-completion callback Phase 52 D-13 hooks into. Example call site at wave-controller.ts:1762:
  await this.runWithConcurrency(agentTasks, this.maxAgentsPerWave, (idx, output) => { … });

<!-- New throttle helper (planner adds to wave-controller.ts class body) -->
  private lastProgressEmitTs = 0;
  private progressItemsSinceEmit = 0;
  private readonly PROGRESS_EMIT_EVERY_N = 5;
  private readonly PROGRESS_EMIT_MIN_MS = 2000;

  private maybeEmitItemProgress(stepName: string, completed: number, total: number): void {
    this.progressItemsSinceEmit += 1;
    const now = Date.now();
    const dueByCount = this.progressItemsSinceEmit >= this.PROGRESS_EMIT_EVERY_N;
    const dueByTime  = now - this.lastProgressEmitTs >= this.PROGRESS_EMIT_MIN_MS;
    if (dueByCount || dueByTime || completed === total) {
      this.updateStepOutputs(stepName, { itemsCompleted: completed, itemsTotal: total });
      this.progressItemsSinceEmit = 0;
      this.lastProgressEmitTs = now;
    }
  }

  // Reset between waves so per-wave throttle bookkeeping does not leak:
  private resetItemProgressThrottle(): void {
    this.lastProgressEmitTs = 0;
    this.progressItemsSinceEmit = 0;
  }

<!-- New dashboard component (planner adds to trace-modal.tsx) -->
  const ItemProgressBadge = React.memo(function ItemProgressBadge({
    completed, total
  }: { completed: number; total: number }) {
    const pct = total > 0 ? Math.min(100, Math.round((completed / total) * 100)) : 0
    return (
      <span className="inline-flex items-center gap-1.5 text-[10px] font-mono">
        <span className="text-zinc-400 tabular-nums">{completed}/{total}</span>
        <span className="inline-block w-12 h-1 bg-zinc-700 rounded-full overflow-hidden">
          <span
            className="block h-full bg-blue-400 transition-all duration-300"
            style={{ width: `${pct}%` }}
          />
        </span>
      </span>
    )
  })
</interfaces>

<critical_constraints>
1. **CONTEXT.md D-13 wording referenced `coordinator.writeProgressPreservingDetails`. PATTERNS.md supersedes this**: wave-controller has its OWN writer (`touchProgress` → `fs.writeFileSync(this.progressFile, …)`) that intentionally bypasses `coordinator.preserveFromExisting`. This is the Phase 42.2-02 architecturally-correct path. Emission MUST route through `wave-controller.updateStepOutputs` (which calls `touchProgress`), NOT through any coordinator method. **Verify** with `grep -n "preserveFromExisting" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` — expect 0 hits both before AND after this plan.

2. The fields written (`outputs.itemsCompleted`, `outputs.itemsTotal`) live in the wave-owned namespace (`stepsDetail.{wave}.outputs.*`). This matches the Phase 42.2-02 split — wave-controller owns `wave*` keys, coordinator owns everything else.

3. Throttle constants: K=5 items, 2000 ms min between emits, OR final-item override (always emit when `completed === total`). Tuneable; planner may adjust based on inner-loop call rates.

4. Reset throttle bookkeeping BETWEEN waves so wave2's first emit doesn't suppress because wave1 just emitted. Call `this.resetItemProgressThrottle()` at the start of each wave's instrumented loop.

5. `wave-controller.ts` is in `src/agents/` REAL submodule subdir → submodule + outer-repo pointer-bump commit pair.

6. Dashboard files are bind-mounted → `npm run build` + frontend supervisorctl restart (NO Docker container rebuild).

7. `no-console-log` constraint — wave-controller already follows it; the new throttle helper and trace-modal additions must too.

8. Backward compat (D-15 hard contract): when `itemsTotal` is missing or zero, the legacy `EntityFlowBadge` + arrow render at trace-modal.tsx:786-791 stays in place unchanged. The new `ItemProgressBadge` renders ONLY when `wg.itemsTotal != null && wg.itemsTotal > 0`. This must work for older `workflow-progress.json` files that don't carry the new fields.

9. The wave1/2/3 loops that need instrumentation are the `runWithConcurrency(componentTasks, …)` calls (PATTERNS.md cites wave-controller line 1762 example). The wave4 loop is the `insightTasks` block where line 2851 already emits — that one just expands the existing emission object, no new loop instrumentation. PATTERNS.md §5 has the per-wave map.

10. The dashboard `WaveGroup` reducer at trace-modal.tsx lines 404-482 (the `waveGroups` useMemo) aggregates step-level data into wave-level. Planner extends the aggregation to surface `step.outputs?.itemsCompleted` / `step.outputs?.itemsTotal` to the wave's top-level fields. For wave4 the data is on `wave4_insights` step. For wave1/2/3 the data lives on whatever step name the planner picks (e.g. `wave1_analyze`, `wave2_classify`, `wave3_classify`) — must match exactly what `updateStepOutputs(stepName, …)` writes.

11. THIS PLAN'S `depends_on: [52-01, 52-02]` reflects two file-level dependencies: (a) registry-tagged callers must be LIVE in the container so the Task 4 visual verification sees real telemetry traffic (52-01), and (b) Plan 52-02 already modifies `trace-modal.tsx` to add the live-badge wiring — Plan 52-03 also modifies the SAME file (adding `ItemProgressBadge` + WaveGroup reducer extension), so it must land AFTER 52-02 to avoid merge conflicts. Wave 3 placement enforces this serialization. Plan 52-03's wave-controller.ts changes are independent of Plan 52-02 entirely; the cross-plan coupling is purely the shared `trace-modal.tsx` file.
</critical_constraints>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add maybeEmitItemProgress helper + reset hook + instrument wave1/2/3/4 emit points</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts</files>
  <read_first>
    @integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts (lines 130-205 fields + touchProgress; lines 250-340 updateStepOutputs + captureAgentMetrics; lines 1150-1230 wave1/2/3 onTaskComplete entry; lines 1750-1780 existing runWithConcurrency call with onTaskComplete callback example; lines 2840-2900 wave4 insightTasks emission)
    @integrations/mcp-server-semantic-analysis/src/agents/workflow-runner.ts (lines 469-530 — Phase 10 race-condition zone; wave-controller's bypass is the WHY)
    @.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/52-PATTERNS.md (§ 5 wave-controller pattern + CRITICAL Phase 10 zone note)
  </read_first>
  <action>
    Phase A — add class members + helper (private). In the WaveController class (file `wave-controller.ts`), add the four private fields + two methods exactly as documented in `<interfaces>` above: `lastProgressEmitTs`, `progressItemsSinceEmit`, `PROGRESS_EMIT_EVERY_N = 5`, `PROGRESS_EMIT_MIN_MS = 2000`, `maybeEmitItemProgress(stepName, completed, total)`, `resetItemProgressThrottle()`. Place adjacent to the existing `updateStepOutputs` method (~line 257) so the relationship is visually clear. JSDoc on `maybeEmitItemProgress` references D-13 throttle policy + cites the Phase 10 race-condition zone (workflow-runner.ts:469-530) + reiterates that wave-controller owns the `wave*.outputs.*` namespace.

    Phase B — wave1/2/3 instrumentation. For each wave's component-classify loop, locate the `await this.runWithConcurrency(<tasks>, <concurrency>, (idx, output) => { … });` call site. (PATTERNS.md cites wave-controller.ts:1762 as an example of the wave-level pattern; planner inventories the three wave-specific loops — they fire one `runWithConcurrency` call per wave for the LLM-bearing inner work.) Inside each `onTaskComplete` callback:
    1. Call `this.maybeEmitItemProgress(<wave-step-name>, idx + 1, <total-tasks>)` where:
       - `<wave-step-name>` matches the step name passed to `updateStepOutputs` elsewhere in the wave (e.g. `'wave1_analyze'`, `'wave2_classify'`, `'wave3_classify'` — planner reads each wave's existing `captureAgentMetrics(stepName, …)` or `updateStepOutputs(stepName, …)` invocation to pick the right name. The same step name must be the one the dashboard's reducer pulls `outputs.itemsCompleted` from.)
       - `<total-tasks>` is the `tasks.length` from the outer `runWithConcurrency` call (already in scope in the same closure).
       - `idx + 1` is the completed-so-far count (callback gives 0-indexed task index).
    2. At the start of each wave's instrumented loop, call `this.resetItemProgressThrottle()` BEFORE the `runWithConcurrency` invocation so per-wave throttle bookkeeping does not leak across waves.

    Phase C — wave4 instrumentation. At wave-controller.ts:2851 the existing `this.updateStepOutputs('wave4_insights', { planned, generated, failed, skippedDiagrams });` call is INSIDE the per-entity insightTasks loop. Expand the emitted object to include `itemsCompleted: generated + failed` and `itemsTotal: planned`. No structural change to the loop. PATTERNS.md §5 step 3 — "Zero structural change — just expand the second arg to updateStepOutputs(…)". Also call `this.resetItemProgressThrottle()` immediately before the wave4 loop begins.

    Note for the wave4 path: wave4 already emits AT EVERY ENTITY (the loop body already fires updateStepOutputs each iteration per the existing line 2851 contract). Phase 52 D-13 throttle is therefore wave1/2/3-only — wave4 emission stays per-item because that's the existing pattern. The new `itemsCompleted/itemsTotal` fields don't change wave4's emission cadence. Document this distinction in a JSDoc comment block above line 2851.

    Constraint: no `console.*` anywhere. Use `log(...)` from the existing logging module if any logging is needed (probably none — the helper is straight-line and short).
  </action>
  <acceptance_criteria>
    - Helper exists: `grep -c "private maybeEmitItemProgress" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 1
    - Helper resets between waves: `grep -c "resetItemProgressThrottle" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` >= 4 (one declaration + at least one call per wave 1/2/3 instrumented loop; wave4 also resets)
    - Throttle constants: `grep -c "PROGRESS_EMIT_EVERY_N\s*=\s*5" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 1; `grep -c "PROGRESS_EMIT_MIN_MS\s*=\s*2000" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 1
    - Wave1/2/3 callbacks invoke the helper: `grep -c "this\.maybeEmitItemProgress" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` >= 3
    - Wave4 emission expanded: `grep -c "itemsCompleted: generated" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 1; `grep -c "itemsTotal: planned" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 1
    - **CRITICAL** — wave-controller still does NOT call coordinator.preserveFromExisting (Phase 42.2-02 invariant preserved): `grep -c "preserveFromExisting" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 0
    - No console.*: `grep -cE "\bconsole\.(log|error|warn|info)" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 0 (or pre-existing baseline preserved — measure before/after if pre-existing hits exist).
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Wave-controller emits throttled per-item progress for waves 1/2/3 + expanded fields for wave4; emissions route through the wave-owned writer; coordinator.preserveFromExisting is NOT touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build submodule + Docker rebuild + outer-repo pointer-bump commit</name>
  <files>
    integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js,
    integrations/mcp-server-semantic-analysis (outer-repo submodule pointer)
  </files>
  <read_first>
    @CLAUDE.md (section "Rebuilding After Code Changes")
  </read_first>
  <action>
    Phase A — submodule commit. Inside `integrations/mcp-server-semantic-analysis/`, commit the source change to `src/agents/wave-controller.ts`. Commit message: `phase52: throttled per-item progress emission (D-13/D-14)`.

    Phase B — submodule build: `cd integrations/mcp-server-semantic-analysis && npm run build`. Verify `dist/agents/wave-controller.js` regenerated.

    Phase C — Docker rebuild: `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services`.

    Phase D — outer-repo pointer-bump commit (single commit; this plan does NOT touch any other outer-repo file in this task — the dashboard work in Task 3 commits separately): stage `integrations/mcp-server-semantic-analysis` (submodule pointer only). Commit message: `bump submodule: phase52 per-item progress emission`.
  </action>
  <acceptance_criteria>
    - Dist updated: `grep -c "maybeEmitItemProgress" integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js` >= 1
    - Container has the new dist: `docker exec coding-services grep -c "maybeEmitItemProgress" /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js` >= 1
    - Submodule commit landed: `cd integrations/mcp-server-semantic-analysis && git log -1 --format=%s | grep -c "^phase52:"` == 1
    - Pointer-bump landed: `git -C /Users/Q284340/Agentic/coding log -1 --format=%s | grep -c "^bump submodule: phase52"` == 1
  </acceptance_criteria>
  <verify>
    <automated>docker exec coding-services grep -c "maybeEmitItemProgress" /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js</automated>
  </verify>
  <done>Submodule rebuilt + container hot-swapped with new dist; outer-repo pointer-bump commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add ItemProgressBadge to trace-modal.tsx + extend WaveGroup reducer + build + frontend restart + commit</name>
  <files>integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx</files>
  <read_first>
    @integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx (lines 125-141 EntityFlowBadge memoized pattern; lines 404-482 waveGroups useMemo reducer; lines 759-790 wave row render where ItemProgressBadge inserts; lines 33-58 StepInfo type already extended in Plan 52-02 with itemsCompleted/itemsTotal)
    @.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/52-PATTERNS.md (§ 9 — ItemProgressBadge implementation)
  </read_first>
  <action>
    Phase A — add `ItemProgressBadge` memoized component next to `EntityFlowBadge` (around line 131-141 of `trace-modal.tsx`). Implementation: see `<interfaces>` block above — copy the JSX shape verbatim (memoized inline-flex span with monospace `{n}/{N}` text + 12-unit wide thin progress bar using inline `style={{ width: \`${pct}%\` }}`). Tailwind classes match the EntityFlowBadge precedent for styling consistency.

    Phase B — extend the `waveGroups` useMemo reducer (around lines 404-482). The reducer aggregates per-step data into a `WaveGroup` shape. Find the step iteration that populates `wg.totalDuration / wg.totalLLMCalls / wg.totalTokens / wg.entityFlow`. Surface `step.outputs?.itemsCompleted` and `step.outputs?.itemsTotal` to `wg.itemsCompleted` and `wg.itemsTotal`:
    - For waves with multiple instrumented steps, define behavior: take the values from the LAST step with a non-null `itemsTotal` (the "currently active" step). This is a simple heuristic; planner may pick differently if multiple wave-steps overlap. Document the chosen rule in a comment.
    - When NO step has `itemsTotal`, leave `wg.itemsCompleted` and `wg.itemsTotal` undefined. The render path's guard handles this case.
    - The `WaveGroup` type (planner finds in `@/store/slices/ukbSlice` per PATTERNS.md §9 step 3) needs `itemsCompleted?: number` + `itemsTotal?: number`. If the store-side type is in a separate file, add the optional fields there too. Otherwise, if `WaveGroup` is declared inline in trace-modal.tsx, modify it locally.

    Phase C — wave row render (around lines 759-790). Insert the new badge BETWEEN `totalLLMCalls` and `EntityFlowBadge` (existing render order: chevron → name → duration → llm-calls badge → token count → entity-flow). The exact spot is the empty line just before `{(wg.entityFlow.produced > 0 || wg.entityFlow.persisted > 0) && (<EntityFlowBadge … />)}`. Insert:
    ```
    {wg.itemsTotal != null && wg.itemsTotal > 0 && wg.itemsCompleted != null && (
      <ItemProgressBadge completed={wg.itemsCompleted} total={wg.itemsTotal} />
    )}
    ```
    D-15 backward-compat guard: the conditional ensures legacy progress files (no itemsTotal) render exactly as before — the new badge slot is empty, no layout shift.

    Phase D — build + restart:
    `cd /Users/Q284340/Agentic/coding/integrations/system-health-dashboard && npm run build`
    `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`

    Phase E — outer-repo commit: stage `integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx`. Commit message: `phase52(dashboard): live {n}/{N} progress badge on wave rows (D-15)`.
  </action>
  <acceptance_criteria>
    - ItemProgressBadge defined: `grep -c "const ItemProgressBadge = React.memo" integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx` == 1
    - Conditional guard renders ItemProgressBadge: `grep -E "wg\.itemsTotal\s*!=\s*null\s*&&\s*wg\.itemsTotal\s*>\s*0" integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx` returns a hit
    - WaveGroup reducer surfaces itemsCompleted/itemsTotal: `grep -c "outputs\?.itemsCompleted\|outputs\?.itemsTotal" integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx` >= 1
    - Render placed inside wave row between llm-calls badge and EntityFlowBadge — manual verification at Task 4 checkpoint (no easy grep gate for JSX ordering; instead the visual check is the gate).
    - Dashboard reachable post-restart: `curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3032` == 200
    - Commit landed: `git -C /Users/Q284340/Agentic/coding log -1 --format=%s | grep -c "phase52(dashboard): live {n}/{N}"` == 1
    - No console.*: `grep -cE "\bconsole\.(log|error|warn|info)" integrations/system-health-dashboard/src/components/workflow/trace-modal.tsx` == 0
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit 2>&1 | tail -20 && curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3032</automated>
  </verify>
  <done>ItemProgressBadge component lands; wave row render uses it conditionally; WaveGroup reducer surfaces the wave-level itemsCompleted/itemsTotal; dashboard built and frontend restarted; commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification — live {n}/{N} counter updates during a running wave-analysis</name>
  <what-built>
    Throttled per-item progress emission inside wave-controller for waves 1/2/3 (K=5 / 2s throttle) + per-entity emission for wave4 (existing cadence + new itemsCompleted/itemsTotal fields); ItemProgressBadge component in trace-modal.tsx renders {n}/{N} + thin progress bar; legacy arrow render preserved when itemsTotal is missing.
  </what-built>
  <how-to-verify>
    1. Reset workflow sticky state (per MEMORY.md):
       `curl -s -X POST http://localhost:3033/api/ukb/mock-llm -H "Content-Type: application/json" -d '{"enabled": false}'`
       `curl -s -X POST http://localhost:3033/api/ukb/single-step-mode -H "Content-Type: application/json" -d '{"enabled": false}'`
       `curl -s -X POST http://localhost:3033/api/ukb/llm-mode/global -H "Content-Type: application/json" -d '{"mode": "public"}'`
    2. Kick off a fresh PRODUCTION wave-analysis via MCP:
       ```
       mcp__semantic-analysis__execute_workflow
         workflow_name: "wave-analysis"
         async_mode: true
         parameters: { team: "coding" }
       ```
    3. Open http://localhost:3032 → Workflow Trace modal IMMEDIATELY (while wave1 is still ramping up).
    4. Observe each wave row. Expected state machine:
       - Initial state: legacy display only (no progress badge yet — total unknown).
       - After ~5 items OR ~2 seconds in wave1: the `{5}/{N}` badge appears with a thin blue progress bar at ~5/N%.
       - Counter increments every 5 items OR every 2 seconds, whichever lands later.
       - When wave1 completes: `{N}/{N}` with full progress bar; transition to wave2.
       - Wave2/wave3 show the same shape.
       - Wave4 emits per-entity (more granular). Confirm the badge increments more frequently than waves 1/2/3.
    5. Verify backward-compat path: open `.data/workflow-progress.json` from a PRIOR run (anything from before Phase 52 ships). Load it into the modal (planner picks the mechanism — restart with a stale JSON, or manually edit the live one to remove itemsTotal). Confirm the legacy arrow display (`8 → 8 → 8`) renders unchanged.
    6. Verify no Phase 42.2-02 regression: `grep -c "preserveFromExisting" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 0 (wave-controller's writer bypass is intact).
    7. Verify the wave-controller writer is not racing with coordinator: tail Docker logs during the wave-analysis run: `docker exec coding-services tail -f /coding/logs/coding-services.log 2>/dev/null | grep -E "(Race condition|preserve)"`. Expect ZERO "Race condition detected" or progress-overwrite warnings.
    8. Optional: capture a screenshot of the live progress badge via `/playwright-cli` or `/gsd-browser`.
  </how-to-verify>
  <resume-signal>Type "approved" once you observe {n}/{N} live updates on all four waves AND the legacy fallback renders correctly when itemsTotal is missing. Paste log lines if a race-condition warning surfaces — planner diagnoses.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wave-controller → workflow-progress.json | Wave-controller writes the file directly via `fs.writeFileSync` (Phase 42.2-02 single-writer pattern; wave-owned namespace). |
| workflow-progress.json → SSE stream → dashboard | Read-merge-push pipeline (existing infra; no new surface). |
| dashboard render → operator screen | UI surface visible to whoever has access to the dashboard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03-01 | Tampering | Throttled writes from wave-controller could race with coordinator writes on the same file | accept | Phase 42.2-02 EXPLICITLY accepted this split: wave-controller owns `wave*.outputs.*`, coordinator owns `stepPaused/llmState/...` (the PROGRESS_PRESERVE_KEYS allowlist). The current code's `touchProgress()` already does a read-merge-write, so a coordinator write happening between the read and the write would be overwritten — that's the pre-existing race surface, not new. Mitigation lives in Phase 42.2-02; Plan 52-03 does not widen it (no new files written, no new keys outside the wave namespace). Acceptance criterion in Task 1 grep-asserts `preserveFromExisting` is STILL zero in wave-controller.ts. |
| T-52-03-02 | Denial of Service | Un-throttled per-item writes could saturate disk I/O during wave3 (200+ entities per wave) | mitigate | K=5 / 2000ms throttle bounds emission to at most ~200 writes per wave (worst case at wave3's largest cohort). The actual rate is bounded by the slower of LLM-call latency (~1s+ per item) and the 2s throttle. Empirical floor: 2 writes/sec on the wave-progress file. No new write hot-spot vs. baseline (wave-controller already writes via `touchProgress` after every `updateStepOutputs`). |
| T-52-03-03 | Information Disclosure | `workflow-progress.json` may contain agent-emitted error strings | accept | Pre-existing surface — not new in Phase 52. The new fields (`itemsCompleted`, `itemsTotal`) are pure numerics; no PII or prompts. |
| T-52-03-04 | Repudiation | A wave that crashes mid-loop leaves stale `{n}/{N}` showing on the dashboard | accept | Existing terminal-state behavior in workflow-progress.json applies — the dashboard already handles process-exit / orphan states via the `status` field (Phase 42.2-02 Plan 02 SC#4 known residual; tracked in STATE.md "Plan 02 follow-up for Plan 7"). Plan 52-03 does not widen this surface. |
| T-52-03-05 | Tampering | Dashboard could be served a manipulated workflow-progress.json (e.g. an operator hand-edits) | accept | Already operator-trusted. Workflow-progress is a working-file, not an audit log. |
| T-52-03-SC | Tampering | npm/pip/cargo installs | accept | NO new package installs in this plan. Uses existing React + the existing wave-controller writer. No supply-chain delta. |
</threat_model>

<verification>
- Submodule TS compiles: `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit` exit 0.
- Dashboard TS compiles: `cd integrations/system-health-dashboard && npx tsc --noEmit` exit 0.
- All grep acceptance criteria above hit.
- Phase 42.2-02 invariant preserved: `grep -c "preserveFromExisting" integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` == 0.
- Live `{n}/{N}` counter updates during a real wave-analysis run (Task 4 checkpoint, step 4).
- Backward-compat path: legacy arrow render unchanged when itemsTotal missing (Task 4 step 5).
- No race-condition warnings in container logs during the verification run (Task 4 step 7).
- Container running new wave-controller dist: `docker exec coding-services grep -c "maybeEmitItemProgress" /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js` >= 1.
</verification>

<success_criteria>
- D-12 satisfied: per-item progress emission ships in the same phase as live badges (single coherent UI story).
- D-13 satisfied: throttled emission via `maybeEmitItemProgress` (K=5 / 2000ms) — verified by code grep + visual cadence at Task 4.
- D-14 satisfied: all four waves (wave1/2/3/4) emit progress; verified by helper invocation count >= 3 (waves 1/2/3) + wave4 field expansion present.
- D-15 satisfied: dashboard renders `{n}/{N}` + thin progress bar inline on each wave row; legacy arrow display preserved when itemsTotal is absent (D-15 backward compat).
</success_criteria>

<output>
Create `.planning/phases/52-dashboard-llm-routing-label-process-tag-observability-fix/52-03-SUMMARY.md` when done. Record: D-12..D-15 coverage map, the step names chosen for each wave's instrumented emission (so future plans can find them), measured emission cadence (count of progress writes per minute during a real wave-analysis run — sample from `.data/workflow-progress.json` mtimes or container logs), and any unexpected interaction with coordinator's writer (if a race-condition warning surfaced, diagnose).
</output>
