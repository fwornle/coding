---
phase: 87-interactive-spans-and-branch-avenues
plan: 06
type: execute
wave: 4
depends_on: [87-03, 87-04, 87-05]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/avenue-panel.tsx
  - integrations/system-health-dashboard/src/components/performance/merge-status-badge.tsx
  - integrations/system-health-dashboard/src/components/performance/performance.tsx
  - integrations/system-health-dashboard/tests/e2e/performance/performance-compare.spec.ts
autonomous: true
requirements: [AVN-07, AVN-08, AVN-09]
must_haves:
  truths:
    - "An origin-grouped panel lists all avenues for a span as ranked rows, default-sorted by outcome score (best first), with tokens/cost, route quality, wall-clock as secondary sortable columns"
    - "Each row carries a merge-status badge computed from git (merged/unmerged/conflicts; unknown → no badge)"
    - "Selecting any two rows opens the EXISTING 86-04 DifferenceViewer via setCompareA/setCompareB (do not rebuild trajectory diffing)"
    - "Per-row Promote (conflict-blocked, confirm-gated) and Prune (destructive confirm; 'measurement data stays') actions call the coordinator seam"
  artifacts:
    - path: "avenue-panel.tsx"
      provides: "origin-grouped N-way ranked table + 2-of-N compare selection + Promote/Prune actions"
      contains: "Compare selected"
    - path: "merge-status-badge.tsx"
      provides: "git-computed merge-state Badge (status tokens + lucide icon; null → no badge)"
      contains: "conflicts"
    - path: "performanceSlice.ts"
      provides: "origin-grouping + ranking selectors + fetchMergeStatus/promoteAvenue/pruneAvenue thunks"
      contains: "selectAvenuesByOrigin"
  key_links:
    - from: "avenue-panel rows"
      to: "selectAvenuesByOrigin (grouped by origin_span_id)"
      via: "ranking selector, default sort outcome score"
      pattern: "selectAvenuesByOrigin|origin_span_id"
    - from: "2-of-N select"
      to: "setCompareA/setCompareB → DifferenceViewer"
      via: "existing 86-05 compare wiring (Compare tab)"
      pattern: "setCompareA|setCompareB"
    - from: "Promote/Prune"
      to: "/api/experiments/avenue-promote | avenue-prune"
      via: "thunks → vkb-server → coordinator"
      pattern: "promoteAvenue|pruneAvenue"
    - from: "merge badge"
      to: "/api/experiments/avenue-merge-status"
      via: "fetchMergeStatus thunk, rendered verbatim"
      pattern: "fetchMergeStatus"
---

<objective>
Deliver the origin-grouped N-way ranked comparison panel — the primary screen of this phase — plus the merge-status badge and per-row Promote/Prune actions, all reusing the existing 86-04 DifferenceViewer + 86-05 compare wiring and the Plan 04 coordinator routes. Extend `performanceSlice.ts` with grouping/ranking selectors + merge-status/promote/prune thunks; do not rebuild trajectory diffing (D-06) and do not fork the slice (D-05).

This plan runs in Wave 4 (after Plan 05) because it shares `performanceSlice.ts` + `performance.tsx` with Plan 05 — sequential to avoid a file conflict.

Purpose: AVN-07 (origin-grouped N-way ranked panel; select 2 → existing DifferenceViewer), AVN-08 (merge-status badge + promote-a-winner), AVN-09 (prune action, measurement data survives). No new packages.
Output: `avenue-panel.tsx`, `merge-status-badge.tsx`, slice selectors/thunks, extended Playwright e2e; verified via gsd-browser in both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-CONTEXT.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md

# Backend routes + origin_span_id + Plan 05 slice extension this builds on:
@.planning/phases/87-interactive-spans-and-branch-avenues/87-03-PLAN.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-04-PLAN.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-05-PLAN.md

<interfaces>
<!-- Confirmed frontend seams (read this session). -->

performanceSlice.ts (already extended by Plan 05 with fork prefill + origin_span_id-bearing LauncherPrefill):
  Run interface: score (RunScore: goal_achieved, corrected_goal_achieved), outcome (RunOutcome: totalTokens), route (loop_count, wallclock_per_step), canonical_model/canonical_agent, origin_span_id (Plan 03)
  :1026 setCompareA / :1029 setCompareB / :1316 selectCompareA / :1317 selectCompareB  — the 2-of-N compare wiring
  :484 fetchRuns / :1310 selectRuns / selectFilteredRuns  — the run source to group
  :872 launchExperiment (bridge idiom to mirror for new thunks)

DifferenceViewer (REUSE UNCHANGED, D-06): components/performance/difference-viewer.tsx — self-reads selectCompareA/selectCompareB, aligns via pure run-align.ts. Mount as-is in the Compare tab (the 86-05 wiring: runs-table "Compare selected (2)" → setCompareA/B → switch to Compare tab).

reconciliation-badge.tsx — the badge idiom to CLONE for merge-status (Badge variant="outline" + lucide icon + status-* token + tooltip; null → NO badge — honesty).

UI-SPEC (mandatory): merge-state mapping merged(Check, status-success)/unmerged(GitBranch, muted)/conflicts(AlertTriangle, status-warning)/unknown(no badge); ranking default = outcome score best-first, secondary sortable tokens/cost, route quality, wall-clock (font-mono, right-aligned); Prune = destructive confirm bar (bg-destructive/10, role=alertdialog) with the "Measurement data stays in .data" reassurance (D-05); Promote = confirm step (variant=default), conflict → text-status-warning block; empty states copy.

Plan 04 routes: /api/experiments/avenue-merge-status | avenue-promote | avenue-prune (POST {task_id}).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: slice — origin-grouping + ranking selectors + merge-status/promote/prune thunks</name>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (Run interface incl. origin_span_id, score/outcome/route fields, :484 fetchRuns, :872 launchExperiment thunk idiom, :1026 setCompareA/B; the Plan-05 fork-prefill extension)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md §6 (grouping key + ranking selector) + §7 (routes)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md (Interaction Contract 5 — default sort outcome score, secondary columns)
  </read_first>
  <action>
    EXTEND `performanceSlice.ts` (no new slice — D-05 / 86-02 discipline): (1) `selectAvenuesByOrigin` — groups `Run`s by `origin_span_id` (Plan 03) into origin→avenues[] map. (2) A ranking selector that sorts an origin's avenues by outcome score default (`corrected_goal_achieved ?? goal_achieved`, Phase 73 corrected-wins), with secondary comparators for tokens/cost (`outcome.totalTokens`), route quality (`loop_count`/wallclock heuristic), and wall-clock — sort direction/column parameterized. (3) Three thunks mirroring the `launchExperiment` bridge idiom: `fetchMergeStatus(taskId)` → `POST /api/experiments/avenue-merge-status`, `promoteAvenue(taskId)` → `/api/experiments/avenue-promote`, `pruneAvenue(taskId)` → `/api/experiments/avenue-prune`; store merge-status per task_id in slice state, served VERBATIM (never client-recomputed — honesty). Null/absent merge-status stays null (→ no badge). Reuse the existing setCompareA/B for the 2-of-N selection.
  </action>
  <acceptance_criteria>
    - `grep -nE "selectAvenuesByOrigin|origin_span_id" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` matches (grouping key).
    - `grep -nE "fetchMergeStatus|promoteAvenue|pruneAvenue" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` shows all three thunks hitting the `/api/experiments/avenue-*` routes.
    - `grep -nE "corrected_goal_achieved|goal_achieved" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` used in the ranking selector (Phase 73 corrected-wins default sort).
    - No new slice: `grep -rc "createSlice" integrations/system-health-dashboard/src/store/slices/` unchanged from baseline.
    - Dashboard builds: `cd integrations/system-health-dashboard && npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <done>Slice extended with origin grouping + outcome-score ranking + merge-status/promote/prune thunks (verbatim status); no new slice; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: merge-status badge + origin-grouped N-way ranked panel with Promote/Prune + compare-of-2</name>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/reconciliation-badge.tsx (the badge idiom to clone; null → no badge)
    - integrations/system-health-dashboard/src/components/performance/runs-table.tsx (:367 "Compare selected (N)" CTA + :395 bg-destructive/10 confirm bar idiom to reuse for Prune)
    - integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx (REUSE unchanged; the Compare-tab mount)
    - integrations/system-health-dashboard/src/components/performance/performance.tsx (tab host; where to mount the panel + Compare tab wiring from 86-05)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md (full Color/Typography/Spacing/Copywriting: merge badge mapping, ranked table focal point, Promote/Prune confirm copy, empty states, font-mono numerics, status-* tokens only)
  </read_first>
  <action>
    Create `merge-status-badge.tsx` cloning the `reconciliation-badge.tsx` idiom: `Badge variant="outline"` + lucide icon + `status-*` token class + tooltip carrying the verbatim git detail (`branch: avenue/{task_id} · ahead {n} · behind {m} · {conflicts} conflicting files`); mapping merged(`Check`,`status-success`) / unmerged(`GitBranch`, muted) / conflicts(`AlertTriangle`,`status-warning`); `null/unknown` → render NOTHING (honesty). Create `avenue-panel.tsx`: a `Card` per origin span containing a sortable shadcn `Table` — the ranked table is the primary focal point (UI-SPEC). Columns: avenue label (agent/model/framework/injection, font-mono where machine), outcome score (default sort, best first), tokens/cost, route quality, wall-clock (all font-mono, right-aligned, sortable), merge-status badge, and row actions. Row selection is 2-of-N enabling a "Compare selected (2)" CTA that dispatches `setCompareA/setCompareB` and switches to the Compare tab where the EXISTING `DifferenceViewer` renders (reuse the 86-05 wiring — do NOT fork the diff). Per-row **Promote** = `Button` + a confirm step (variant=default) calling `promoteAvenue`; if the merge-status is `conflicts`, disable/block with the `text-status-warning` copy "avenue/{task_id} has conflicts with main — resolve them before promoting". Per-row **Prune** = `Button variant="destructive" size="sm"` → the inline `bg-destructive/10` confirm bar (`role="alertdialog"`, cloned from runs-table:395) with the mandatory D-05 reassurance "Prune branch avenue/{task_id}? Measurement data stays in .data — only the branch is removed." calling `pruneAvenue`. Empty states per UI-SPEC ("No avenues yet" / "Avenues are running… no results to rank yet."). Mount the panel in `performance.tsx`. Honesty: null/unmeasured → em-dash or italic "unmeasured", never 0. Status-* tokens + `--primary` only for the selected-for-compare emphasis; no raw hex; no new package.
  </action>
  <acceptance_criteria>
    - `grep -nE "conflicts|merged|unmerged" integrations/system-health-dashboard/src/components/performance/merge-status-badge.tsx` matches; the component returns null when status is null/unknown (`grep -n "return null" merge-status-badge.tsx`).
    - `grep -nE "Compare selected|setCompareA|setCompareB" integrations/system-health-dashboard/src/components/performance/avenue-panel.tsx` matches (2-of-N → existing DifferenceViewer, not a new diff).
    - `grep -n "Measurement data stays" integrations/system-health-dashboard/src/components/performance/avenue-panel.tsx` matches (D-05 prune reassurance).
    - `grep -nE "has conflicts with main|resolve them before promoting" integrations/system-health-dashboard/src/components/performance/avenue-panel.tsx` matches (conflict-blocked promote copy).
    - No second diff viewer created: `grep -rc "alignRuns\|run-align" integrations/system-health-dashboard/src/components/performance/avenue-panel.tsx` is 0 (reuses DifferenceViewer, doesn't re-implement alignment).
    - No raw hex added: `grep -nE "#[0-9a-fA-F]{3,6}" integrations/system-health-dashboard/src/components/performance/avenue-panel.tsx integrations/system-health-dashboard/src/components/performance/merge-status-badge.tsx` shows no NEW hex.
    - Dashboard builds: `cd integrations/system-health-dashboard && npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <done>Origin-grouped ranked panel (outcome-score default) with merge badge + conflict-blocked Promote + destructive-confirm Prune; 2-of-N reuses DifferenceViewer; honesty + tokens honored; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: extend performance-compare.spec.ts e2e + gsd-browser visual verify (both themes)</name>
  <read_first>
    - integrations/system-health-dashboard/tests/e2e/performance/performance-compare.spec.ts (existing compare-of-2 → DifferenceViewer flow + data-presence skip guards)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md (Interaction Contract 5/6/7 — ranked panel, honesty, both themes)
    - CLAUDE.md (gsd-browser mandate; dashboard rebuild+restart recipe; never claim UI works from DB alone)
  </read_first>
  <action>
    Extend `performance-compare.spec.ts` with the avenue-panel flow: (a) origin-grouped panel renders avenues ranked by outcome score (best first), (b) the merge-status badge appears per row (and is ABSENT when status is null — honesty), (c) selecting two rows enables "Compare selected (2)" → switches to Compare tab → the existing DifferenceViewer aligns, (d) the Prune confirm bar shows the "Measurement data stays" copy, (e) Promote is blocked with the conflict copy when status is conflicts. Use data-presence skip guards (86-05 idiom) when no avenue data exists. Then run the mandatory dashboard rebuild+restart and gsd-browser visual verify: `cd integrations/system-health-dashboard && npm run build && docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`, then `gsd-browser navigate http://localhost:3032` → Performance tab → screenshot the ranked panel + merge badge + prune/promote confirm bars in BOTH light and `.dark`. Record observations in the SUMMARY (UI-truth gate — do not claim from DB alone).
  </action>
  <acceptance_criteria>
    - `grep -nE "avenue|merge|Compare selected|Prune|Promote" integrations/system-health-dashboard/tests/e2e/performance/performance-compare.spec.ts` shows the new panel assertions.
    - `npx playwright test performance-compare.spec.ts` passes (or skips cleanly with the data-presence guard — documented in SUMMARY).
    - Dashboard rebuilt + frontend restarted (`npm run build` clean; `supervisorctl restart web-services:health-dashboard-frontend` run) — a recorded criterion.
    - gsd-browser screenshots on :3032 in BOTH light and dark: ranked panel + merge badge + prune/promote confirm visible + legible; observation captured in SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx playwright test performance-compare.spec.ts</automated>
  </verify>
  <done>e2e covers ranked panel + merge badge + compare-of-2 + prune/promote confirms; dashboard rebuilt/restarted; gsd-browser visual verify in both themes recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → vkb-server → coordinator | Promote/Prune requests traverse the container→host git bridge |
| git-computed merge status → UI | status rendered in the badge/tooltip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-06-01 | Elevation | Promote merging into main from the UI | mitigate | promote routes host-side (Plan 04 coordinator); UI confirm step; conflict-blocked both server-side (promoteAvenue rejects) AND UI (disabled + warning copy when status=conflicts) |
| T-87-06-02 | Tampering | Prune from the UI deleting a branch | mitigate | prune routes host-side (Plan 04); destructive confirm bar (role=alertdialog); server-side sanitizeTaskId + fixed-argv; D-05 reassurance that measurement data survives |
| T-87-06-03 | Spoofing | fabricated merge/ranking state | mitigate | merge status + scores rendered VERBATIM from server (never client-recomputed); unknown → no badge; null → em-dash/italic "unmeasured", never 0 (honesty, Phases 82/83/86) |
| T-87-06-04 | — (defense-in-depth) | client-side re-implementation of the diff | mitigate | reuse DifferenceViewer/run-align unchanged (D-06); grep gate forbids re-implementing alignRuns in avenue-panel |
</threat_model>

<verification>
- `cd integrations/system-health-dashboard && npm run build` clean (both component tasks).
- `npx playwright test performance-compare.spec.ts` green (or documented data-presence skip).
- gsd-browser visual verify of the ranked panel + merge badge + prune/promote confirms in light + dark, recorded in SUMMARY.
</verification>

<success_criteria>
- Origin-grouped N-way ranked panel, outcome-score default; select 2 → existing DifferenceViewer (AVN-07).
- Merge-status badge git-computed; promote conflict-blocked + host-side (AVN-08).
- Prune with measurement-data-survives reassurance, host-side (AVN-09).
- UI-SPEC tokens/copy/honesty honored; verified in both themes.
</success_criteria>

<output>
Create `.planning/phases/87-interactive-spans-and-branch-avenues/87-06-SUMMARY.md` when done.
</output>
