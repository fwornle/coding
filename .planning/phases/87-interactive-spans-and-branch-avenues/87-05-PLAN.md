---
phase: 87-interactive-spans-and-branch-avenues
plan: 05
type: execute
wave: 3
depends_on: [87-03, 87-04]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/runs-table.tsx
  - integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx
  - integrations/system-health-dashboard/tests/e2e/performance/performance.spec.ts
autonomous: true
requirements: [AVN-02, AVN-03]
must_haves:
  truths:
    - "A 'Fork into avenues' ghost button appears ONLY on completed span rows (mirrors the Re-run guard) and pre-fills the variant picker with the ring-2 ring-primary highlight"
    - "The variant picker renders four axes: Agent, Model, SDD framework (disambiguated), Knowledge injection (prominent on/off) — curated-by-default with a Sweep toggle"
    - "A sweep shows a SERVER-resolved count + cost/token preview BEFORE launch; the launch button is disabled until the preview has rendered"
    - "Fork/launch flows through the existing launchExperiment → vkb-server → coordinator bridge (no UI-only path — D-01)"
  artifacts:
    - path: "runs-table.tsx"
      provides: "Fork into avenues ghost button on completed rows"
      contains: "Fork into avenues"
    - path: "experiment-launcher.tsx"
      provides: "4-axis picker + sweep toggle + guardrail count/cost preview"
      contains: "Knowledge injection"
    - path: "performanceSlice.ts"
      provides: "buildForkPrefill + avenue-fork launch wiring (extends launcherPrefill)"
      contains: "origin_span_id"
  key_links:
    - from: "runs-table Fork button"
      to: "setLauncherPrefill(buildForkPrefill(run))"
      via: "dispatch (mirrors buildRerunPrefill idiom)"
      pattern: "buildForkPrefill|Fork into avenues"
    - from: "sweep preview"
      to: "server-resolved cellCount"
      via: "matrix-preview box (never client recompute)"
      pattern: "matrix-cell-count|cellCount"
    - from: "launch"
      to: "launchExperiment thunk → /api/experiments/run"
      via: "existing bridge"
      pattern: "launchExperiment"
---

<objective>
Deliver the fork trigger + four-axis variant picker + mandatory sweep guardrail on the existing dashboard, all as a thin wrapper over the existing launch bridge (D-01 dashboard-first, CLI-backed — AVN-02/AVN-03). Extend `runs-table.tsx` (Fork button, mirroring Re-run), `experiment-launcher.tsx` (4-axis picker + sweep toggle + count/cost preview), and `performanceSlice.ts` (fork prefill), reusing every existing token/component per the UI-SPEC. No new packages.

Purpose: AVN-02 (dashboard Fork = thin wrapper over experiment-run via the vkb→coordinator seam) + AVN-03 (curated-default 4-axis picker + sweep with server-resolved count+cost preview before launch).
Output: Fork button, extended launcher, fork prefill wiring, extended Playwright e2e; verified visually via gsd-browser in both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-CONTEXT.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md

# Backend routes/spec this consumes:
@.planning/phases/87-interactive-spans-and-branch-avenues/87-03-PLAN.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-04-PLAN.md

<interfaces>
<!-- Confirmed frontend seams (read this session). Executor reuses these directly. -->

runs-table.tsx:
  :55 isCompletedExperimentRun(run) — the Fork guard (identical to Re-run's completed-only guard)
  :81 buildRerunPrefill(run, specs) — the prefill builder idiom to mirror for buildForkPrefill
  :530 dispatch(setLauncherPrefill(buildRerunPrefill(run, specList))) — the Fork dispatch mirrors this
  :517-542 the Re-run ghost Button block — the Fork button clones this idiom

experiment-launcher.tsx:
  :176 data-testid="experiment-launcher"; :181 ring-2 ring-primary prefill highlight
  :211 data-testid="spec-select"; :227 data-testid="matrix-preview" (bg-muted/40); :255 data-testid="matrix-cell-count" (font-mono, SERVER cellCount)
  :291 data-testid="variant-subset"; :311/:318 per-variant model/agent Input overrides
  :340 data-testid="launch-experiment" (disable until preview rendered); :351 data-testid="launch-error" role="alert" dismissible

performanceSlice.ts:
  :408 LauncherPrefill interface (extend for fork: axes + sweep + origin_span_id)
  :853 fetchSpecList; :872 launchExperiment; :898 fetchRunStatus; :917 cancelRun; :1040 setLauncherPrefill
  Run record now carries origin_span_id (Plan 03)

UI-SPEC copy (mandatory): framework label "SDD framework" + helper caption "…not a code framework"; "Knowledge injection" toggle + caption; sweep guardrail "{V} variants × {R} repeats = {N} avenues" + "Est. {tokens} tokens · ~{cost}" + over-threshold text-status-warning; confirm CTA "Launch {N} avenues"; launch DISABLED until preview rendered.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork prefill wiring in slice + Fork button in runs-table (AVN-02)</name>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (:408 LauncherPrefill, :872 launchExperiment, :1040 setLauncherPrefill, Run.origin_span_id)
    - integrations/system-health-dashboard/src/components/performance/runs-table.tsx (:55 guard, :81 buildRerunPrefill, :517-542 Re-run button block)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md (Fork trigger location + copy + ghost button idiom)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md §8 (Fork = capture origin snapshot + synthesize spec + POST through launchExperiment)
  </read_first>
  <action>
    Extend `LauncherPrefill` in `performanceSlice.ts` with fork fields (origin span/task id, origin snapshot id, the four selectable axes, sweep flag) — EXTEND the existing interface, do not fork the slice (Phase 86-02 frozen-contract discipline). Add a `buildForkPrefill(run)` helper (mirror `buildRerunPrefill`) that seeds the picker from a completed span and carries `origin_span_id`. In `runs-table.tsx`, add a "Fork into avenues" `Button variant="ghost" size="sm"` gated by the SAME `isCompletedExperimentRun(run)` guard as Re-run (:55), dispatching `setLauncherPrefill(buildForkPrefill(run))` (mirror :530) and scrolling the launcher into view. The fork does NOT introduce a new API path — it reuses `launchExperiment` (D-01 thin wrapper); the avenue-spec synthesis is Plan 03's `synthesizeAvenueSpec` invoked server-side via the existing run bridge. Use existing tokens only (ghost button, lucide `GitBranch` icon per UI-SPEC). No raw hex, no new package.
  </action>
  <acceptance_criteria>
    - `grep -n "Fork into avenues" integrations/system-health-dashboard/src/components/performance/runs-table.tsx` matches; the button sits inside the `isCompletedExperimentRun(run) &&` guard (same guard as Re-run).
    - `grep -n "buildForkPrefill" integrations/system-health-dashboard/src/components/performance/runs-table.tsx integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` matches.
    - `grep -n "origin_span_id" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` matches (prefill carries the origin link).
    - Dashboard builds: `cd integrations/system-health-dashboard && npm run build` succeeds (typecheck gate).
    - No new slice created (`grep -rc "createSlice" integrations/system-health-dashboard/src/store/slices/` unchanged from baseline).
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <done>Fork button on completed rows pre-fills the launcher via buildForkPrefill; launch reuses launchExperiment; slice extended not forked; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: 4-axis picker + sweep toggle + mandatory count/cost guardrail in launcher (AVN-03)</name>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx (:227 matrix-preview, :255 matrix-cell-count SERVER cellCount, :291 variant-subset, :340 launch-experiment disable, :351 launch-error, :181 ring)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md (Copywriting Contract: SDD framework caption, Knowledge injection caption, sweep guardrail copy, "Launch {N} avenues"; Interaction Contract 3/4; Color: accent reserved list; Typography: font-mono numerics)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md §2/§3/§6 (axes; D-09 server-resolved count, never client recompute)
  </read_first>
  <action>
    Extend `experiment-launcher.tsx` to render the four D-03 axes as labelled `Checkbox` groups when a fork prefill is active: **Agent** (claude/copilot/opencode/mastra — map the mastra label to the `mastracode` literal Plan 02 accepts), **Model** (opus/sonnet/gpt-5/haiku/…), **SDD framework** (gsd/spec-workflow/none) with the mandatory disambiguation caption "The spec-driven-development harness … not a code framework", and **Knowledge injection** as a PROMINENT on/off toggle (its caption per UI-SPEC; encodes to the `env` kb-on/kb-off axis from Plan 02). Add a **Sweep** toggle ("Sweep (cross-product of chosen axes)") that expands the chosen axes. REUSE the existing `data-testid="matrix-preview"` box for the guardrail: show `{V} variants × {R} repeats = {N} avenues` (font-mono) plus an `Est. {tokens} tokens · ~{cost}` line, and a `text-status-warning` caution line when N exceeds a threshold. The count MUST be the SERVER-resolved `cellCount` (reuse the existing `previewCellCount`/`fetchSpecList` D-09 path — NEVER a client-side axes recompute). Keep the launch button (`data-testid="launch-experiment"`) DISABLED until the preview has rendered for the current selection, relabelled "Launch {N} avenues". Reuse the dismissible `launch-error` holder for failures ("Could not launch avenues: {reason}…"). Tokens/typography per UI-SPEC (four sizes, two weights, font-mono numerics, status-* tokens only, `--primary` only for the launch CTA + prefill ring). No new package.
  </action>
  <acceptance_criteria>
    - `grep -nE "SDD framework|Knowledge injection|Sweep" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` matches all three axis/toggle labels.
    - `grep -n "not a code framework" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` matches (mandatory disambiguation caption).
    - `grep -nE "Launch \{|Launch {N}|avenues" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows the count-reflecting CTA; launch stays disabled until preview (assert the disabled-until-preview condition references previewCellCount).
    - `grep -n "matrix-cell-count" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` still uses the SERVER cellCount (no new client axes-multiplication introduced — `grep -cE "\.length \* .*\.length \* " …` shows no client cross-product count feeding the preview).
    - No raw hex: `grep -nE "#[0-9a-fA-F]{3,6}" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows no NEW hex added (status-*/token classes only).
    - Dashboard builds: `cd integrations/system-health-dashboard && npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <done>4-axis picker + sweep + server-resolved count/cost guardrail render; launch disabled until preview; framework disambiguated; injection prominent; tokens-only styling; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: extend performance.spec.ts e2e for fork launcher + gsd-browser visual verify (both themes)</name>
  <read_first>
    - integrations/system-health-dashboard/tests/e2e/performance/performance.spec.ts (existing launcher flow specs + data-presence skip guards)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-UI-SPEC.md (Interaction Contract 4 + 7 — guardrail-before-launch + both-themes)
    - CLAUDE.md (gsd-browser mandate; dashboard rebuild+restart recipe)
  </read_first>
  <action>
    Extend `performance.spec.ts` with a fork-launcher flow: (a) Fork button visible only on a completed span row, (b) clicking it opens the launcher with the ring highlight + four axes, (c) selecting a sweep renders the server-resolved count/cost preview and the launch button is DISABLED until it renders then reflects "Launch {N} avenues", (d) a launch-error dismiss works. Use data-presence skip guards (mirror the 86-05 idiom) when no completed run exists. Then perform the mandatory dashboard rebuild+restart and a gsd-browser visual verify: `cd integrations/system-health-dashboard && npm run build && docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`, then `gsd-browser navigate http://localhost:3032` → Performance tab → screenshot the fork picker + guardrail in BOTH light and `.dark` themes (never claim "works" from a DB query — standing feedback). Record the screenshots' observations in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -nE "Fork into avenues|Launch |Sweep" integrations/system-health-dashboard/tests/e2e/performance/performance.spec.ts` shows the new fork flow assertions.
    - `npx playwright test performance.spec.ts` passes (or skips cleanly with the data-presence guard when no completed run exists — documented in SUMMARY).
    - Dashboard rebuilt + frontend restarted (`npm run build` clean; `supervisorctl restart web-services:health-dashboard-frontend` run) — recorded as a criterion, not skipped.
    - gsd-browser screenshots taken on :3032 in BOTH light and dark; the guardrail preview + 4-axis picker are visible and legible; observation captured in SUMMARY (this is the human-facing UI-truth gate).
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx playwright test performance.spec.ts</automated>
  </verify>
  <done>e2e covers the fork launcher + guardrail-before-launch; dashboard rebuilt/restarted; gsd-browser visual verify in both themes recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user picker input → launch request | axis selections + sweep flag cross from the browser into launchExperiment |
| browser → vkb-server → coordinator | fork launch traverses the container→host bridge (Phase 85 seam) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-05-01 | Tampering | axis values (agent/model/framework) from the picker | mitigate | server-side validateCells (Plan 02, fail-fast on unknown agent + SHELL_META_RE on test_command) is the authority; the UI cannot smuggle an illegal agent past the enum |
| T-87-05-02 | DoS (matrix explosion) | sweep cross-product | mitigate | mandatory server-resolved count+cost preview BEFORE launch; launch disabled until rendered; over-threshold caution line; "Launch {N} avenues" confirm (UI-SPEC guardrail, D-02) |
| T-87-05-03 | Tampering (count spoof) | client-side count recompute | mitigate | count is the SERVER `cellCount` (D-09); grep gate forbids a new client cross-product multiply feeding the preview |
| T-87-05-04 | Info disclosure | launch failure message | accept | error copy is the reason string in the existing dismissible holder; no secrets surfaced (server controls the message) |
</threat_model>

<verification>
- `cd integrations/system-health-dashboard && npm run build` clean (both frontend tasks).
- `npx playwright test performance.spec.ts` green (or documented data-presence skip).
- gsd-browser visual verify of the fork picker + guardrail in light + dark, recorded in SUMMARY.
</verification>

<success_criteria>
- Fork button on completed rows → thin wrapper over launchExperiment (AVN-02).
- 4-axis picker + sweep + server-resolved count/cost guardrail, launch-disabled-until-preview (AVN-03).
- UI-SPEC tokens/copy honored; verified in both themes.
</success_criteria>

<output>
Create `.planning/phases/87-interactive-spans-and-branch-avenues/87-05-SUMMARY.md` when done.
</output>
