---
phase: 87-interactive-spans-and-branch-avenues
plan: 07
type: execute
wave: 1
depends_on: [87-03, 87-05]
files_modified:
  - lib/experiments/experiment-runner.mjs
  - scripts/experiment-run.mjs
  - lib/experiments/run-launch.mjs
  - lib/vkb-server/api-routes.js
  - scripts/health-coordinator.js
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx
  - tests/experiments/experiment-runner.test.mjs
  - tests/experiments/run-launch.test.mjs
autonomous: true
requirements: [AVN-01, AVN-02, AVN-03, AVN-07]
must_haves:
  truths:
    - "A dashboard 'Fork into avenues' launch (choosing a non-default agent/model axis) reaches the runner with origin_span_id AND the chosen fork axes, so the runner launches an AVENUE matrix (not the origin spec's unmodified matrix)"
    - "runMatrix forwards avenue/originSpanId/commitAvenue from its opts into the runCell({...}) call site, so runCell's already-correct avenue-mode logic is reachable via the only production caller"
    - "scripts/experiment-run.mjs accepts --avenue and --origin-span-id CLI flags and threads them into the runMatrix opts; run-launch.mjs's buildRunArgv emits both when present"
    - "handleExperimentRun reads origin_span_id + forkAxes from the request body, calls synthesizeAvenueSpec + synthesizeToYamlFile to build+persist the forked spec, and forwards origin_span_id/avenue through the coordinator seam to the runner"
    - "A completed avenue Run carries a non-null origin_span_id, so selectAvenuesByOrigin groups it and the AvenuePanel renders a real ranked row (AVN-07 gets real data)"
    - "experiment-launcher.tsx includes forkAxes/sweep in the launchExperiment dispatch payload, and previewCellCount reflects a server-computed axes-aware count (not the origin spec's static YAML metadata)"
  artifacts:
    - path: "lib/experiments/experiment-runner.mjs"
      provides: "runMatrix threads avenue/originSpanId/commitAvenue from opts into the runCell call"
      contains: "avenue"
    - path: "scripts/experiment-run.mjs"
      provides: "--avenue / --origin-span-id CLI flags parsed into runMatrix opts"
      contains: "origin-span-id"
    - path: "lib/vkb-server/api-routes.js"
      provides: "handleExperimentRun reads origin_span_id/forkAxes, calls synthesizeAvenueSpec, forwards avenue/origin_span_id to coordinator"
      contains: "synthesizeAvenueSpec"
    - path: "integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx"
      provides: "forkAxes/sweep in launch payload; axes-aware server-resolved previewCellCount"
      contains: "forkAxes"
  key_links:
    - from: "runMatrix opts"
      to: "runCell({ ..., avenue, originSpanId, commitAvenue })"
      via: "opts destructure + call-site passthrough (experiment-runner.mjs ~676-685 / ~769-774)"
      pattern: "avenue.*originSpanId|originSpanId.*avenue"
    - from: "experiment-run.mjs CLI"
      to: "runMatrix opts.avenue / opts.originSpanId"
      via: "--avenue / --origin-span-id flag parse"
      pattern: "origin-span-id"
    - from: "run-launch buildRunArgv"
      to: "--avenue / --origin-span-id argv"
      via: "overrides.avenue / overrides.origin_span_id passthrough"
      pattern: "--origin-span-id"
    - from: "handleExperimentRun body"
      to: "synthesizeAvenueSpec(originRun, forkAxes) → synthesizeToYamlFile → coordinator /experiments/run"
      via: "origin_span_id + forkAxes read + spec synthesis"
      pattern: "synthesizeAvenueSpec"
    - from: "coordinator /experiments/run"
      to: "runExperiment overrides.origin_span_id / overrides.avenue"
      via: "request body destructure (health-coordinator.js ~2735-2766)"
      pattern: "origin_span_id"
    - from: "experiment-launcher launch payload"
      to: "launchExperiment({ ..., forkAxes, sweep })"
      via: "buildOverrides / dispatch"
      pattern: "forkAxes"
    - from: "avenue Run"
      to: "selectAvenuesByOrigin grouping"
      via: "non-null origin_span_id on the Run record"
      pattern: "origin_span_id"
---

<objective>
Close the CR-01/CR-02/CR-03 causal chain that broke the phase's headline capability: a real dashboard fork of a completed span is silently dropped at the seam between the launch request and the runner, so no production Run ever carries a non-null origin_span_id and the (correctly-built) origin-grouped ranked panel has no data. This is a THREADING job — the primitives (runCell avenue mode, synthesizeAvenueSpec, synthesizeToYamlFile, run-write origin_span_id persistence, selectAvenuesByOrigin/rankAvenues) are already implemented and unit-tested; they are simply never reached by the real launch path. Wire origin_span_id + fork axes end-to-end: runner → CLI → run-launch argv → vkb-server → coordinator → client payload + axes-aware preview.

Purpose: re-establish AVN-01 (Run carries origin_span_id via the REAL launch path), AVN-02 (dashboard Fork actually forks, not a plain rerun), AVN-03 (axis picker + preview are honest, not decorative), AVN-07 (the ranked panel gets real grouped data). AVN-04/05/06/08/09 stay intact (their primitives are unchanged).
Output: threaded runner/CLI/run-launch, an axes-aware server fork path, an honest client preview + payload, and regression tests proving a fork launch produces an origin_span_id-bearing avenue matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-CONTEXT.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-VERIFICATION.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md

# The verified-correct-but-orphaned primitives + the plans that built them:
@.planning/phases/87-interactive-spans-and-branch-avenues/87-03-PLAN.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-05-PLAN.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-06-PLAN.md

<interfaces>
<!-- Confirmed seams (read this session). Both ends of each threading gap are named here so the
     executor threads values through — it does NOT re-implement the already-correct primitives. -->

runCell ALREADY accepts these (experiment-runner.mjs :461 destructure, :484-486): `avenue = false`,
`originSpanId`, `commitAvenue = commitAvenueWorktree`. When `avenue` is true it restores onto
`avenue/${sanitizeTaskId(taskId)}`, threads `--origin-span-id <originSpanId>` into measurement-start
(:530-533), and commits the worktree on close (:592-596). runCell is CORRECT — do not touch its body.

THE CR-01 GAP — runMatrix (experiment-runner.mjs):
  :676-685 opts destructure — has NO avenue/originSpanId/commitAvenue entry.
  :769-774 runCell({...}) call — passes NONE of the three. Add the three fields to BOTH.

THE CR-01 CLI GAP — scripts/experiment-run.mjs:
  :150-170 arg parse (parseStrArg / args.includes idiom) — no --avenue / --origin-span-id.
  :287-302 opts object handed to runMatrix — no avenue/originSpanId.

THE CR-01 argv GAP — lib/experiments/run-launch.mjs buildRunArgv:
  :72-79 valueFlags list — no origin_span_id entry; :99-102 boolean-flag block — no avenue flag.

synthesizeAvenueSpec (lib/experiments/avenue-spec.mjs :105): `({ originRun, variants, repeats, test_command })`
  → experiment-spec object (goal_sentence=origin prompt, snapshot_id=origin snapshot, one cell per variant).
synthesizeToYamlFile (avenue-spec.mjs :144): `(spec, { dir, repoRoot })` → writes
  config/experiments/avenue-<origin_task_id>.yaml, returns the outPath.

THE CR-02 SERVER GAP — lib/vkb-server/api-routes.js handleExperimentRun (:952):
  :957 destructures only `{ spec, overrides, rerun_of }` — add origin_span_id + forkAxes.
  :1009-1022 spec-membership V5 gate requires `spec` to be a server-LISTED config/experiments basename.
    → For a fork: synthesizeAvenueSpec + synthesizeToYamlFile FIRST (writes the avenue-<origin>.yaml
      basename into config/experiments), THEN that basename passes the same V5 listing gate.
  :1073-1075 `_coordinatorPost('/experiments/run', { spec, run_id, run_dir, overrides })` — fold
    origin_span_id + an `avenue:true` flag into `ov` (the forwarded overrides) so run-launch emits them.
  To read the origin Run's prompt/snapshot for synthesizeAvenueSpec, resolve origin_span_id against the
  Run store the server already reads (mirror how a Run is read elsewhere in api-routes.js — do NOT open
  the experiment LevelDB during a live run, Pitfall 6; read the persisted Run JSON/aggregate the server
  already uses for the runs table).

THE CR-02 COORDINATOR GAP — scripts/health-coordinator.js /experiments/run (:2735-2766):
  :2740 destructures `{ spec, run_id, run_dir, overrides }` — overrides already forwarded WHOLE to
  runExperiment (:2762), so origin_span_id/avenue folded into `overrides` reach run-launch WITHOUT a
  coordinator change IF they live inside overrides. Confirm the overrides object is passed through
  untouched; add a passthrough only if the coordinator filters override keys.

THE CR-02/CR-03 CLIENT GAP — experiment-launcher.tsx:
  :146-154 previewCellCount derives from selectedSpec.variantCount/cellCount/repeats (origin spec YAML) —
    make it axes-aware (reflect the chosen forkAxes count via a server round-trip, per Task 4).
  :206-218 buildOverrides() — never includes forkAxes/sweep.
  :230-235 launchExperiment({ spec, overrides, rerun_of, origin_span_id }) — add forkAxes + sweep.
  performanceSlice.ts :964-967 launchExperiment thunk type + :980-985 POST body — extend to carry
    forkAxes + sweep (already carries origin_span_id).
  ForkAxes interface (performanceSlice.ts :427-439): { agents?, models?, frameworks?, kbOn?, kbOff? }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread avenue/originSpanId/commitAvenue through runMatrix → runCell + CLI + run-launch argv (CR-01)</name>
  <read_first>
    - lib/experiments/experiment-runner.mjs (runMatrix :675-792 — the :676-685 opts destructure + the :769-774 runCell call; runCell :461-611 to CONFIRM the param names avenue/originSpanId/commitAvenue — do NOT modify runCell's body)
    - scripts/experiment-run.mjs (:150-170 arg parse idioms parseStrArg/args.includes; :287-302 the opts object; :107-149 usage text to extend)
    - lib/experiments/run-launch.mjs (buildRunArgv :62-105 — the valueFlags table + the boolean-flag block)
    - lib/experiments/avenue-branch.mjs (commitAvenueWorktree — the default seam runCell already imports; runMatrix just forwards it or lets runCell default it)
    - tests/experiments/experiment-runner.test.mjs + tests/experiments/run-launch.test.mjs (existing suites — extend, do not regress the non-avenue byte-identical contract)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-03-PLAN.md (the runCell avenue seam this threading feeds)
  </read_first>
  <action>
    In `lib/experiments/experiment-runner.mjs`: add `avenue = false`, `originSpanId`, and `commitAvenue` to the `runMatrix` opts destructure (:676-685), and forward all three into the `runCell({...})` call site (:769-774) so they read `runCell({ ..., avenue, originSpanId, commitAvenue })`. Keep the default behavior byte-identical when absent (`avenue` defaults false → non-avenue cells unchanged; omit `commitAvenue` so runCell keeps its own `commitAvenueWorktree` default when not injected).
    In `scripts/experiment-run.mjs`: parse an `--avenue` PRESENCE flag (mirror the `--capture-raw-bodies` `args.includes('--avenue')` idiom at :170) and an `--origin-span-id <id>` value flag (mirror `parseStrArg(args, '--rerun-of')` at :160). Add both to the `opts` object handed to `runMatrix` (:287-302) as `avenue` and `originSpanId`. Extend the usage text (:107-149) to document both flags — this is the D-01 scriptable-CLI path.
    In `lib/experiments/run-launch.mjs` `buildRunArgv` (:62-105): add `['origin_span_id', '--origin-span-id']` to the valueFlags table (:72-79) so an override carrying `origin_span_id` emits the flag; add an `overrides.avenue === true` boolean-flag push (mirror the `capture_raw_bodies` block at :99-102) emitting `--avenue`.
    Extend `tests/experiments/experiment-runner.test.mjs` (avenue opts reach the runCell seam via the injectable seams) and `tests/experiments/run-launch.test.mjs` (buildRunArgv emits `--origin-span-id <id>` and `--avenue` when the overrides carry them; emits NEITHER when absent — the non-avenue argv stays byte-identical).
  </action>
  <acceptance_criteria>
    - `grep -nE "runCell\(\{[^}]*avenue[^}]*originSpanId|avenue,\s*originSpanId,\s*commitAvenue" lib/experiments/experiment-runner.mjs` matches at the runMatrix call site (all three forwarded).
    - `grep -c "avenue" lib/experiments/experiment-runner.mjs` shows avenue in the runMatrix destructure (a NEW occurrence in the :676-685 block, not only in runCell).
    - `grep -n "origin-span-id" scripts/experiment-run.mjs` matches (flag parsed) AND `grep -nE "avenue|originSpanId" scripts/experiment-run.mjs` shows both in the opts object handed to runMatrix.
    - `grep -n "\-\-origin-span-id" lib/experiments/run-launch.mjs` matches in buildRunArgv AND `grep -n "\-\-avenue" lib/experiments/run-launch.mjs` matches the boolean-flag push.
    - `node --test tests/experiments/experiment-runner.test.mjs tests/experiments/run-launch.test.mjs` passes: buildRunArgv emits `--origin-span-id <id>` and `--avenue` when overrides carry them, emits NEITHER when absent (non-avenue argv byte-identical).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/experiment-runner.test.mjs tests/experiments/run-launch.test.mjs</automated>
  </verify>
  <done>runMatrix forwards avenue/originSpanId/commitAvenue into runCell; experiment-run.mjs parses --avenue/--origin-span-id into runMatrix opts; buildRunArgv emits both; non-avenue path byte-unchanged; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Server fork synthesis — handleExperimentRun reads origin_span_id + forkAxes, synthesizes+persists the avenue spec, forwards through the coordinator (CR-02)</name>
  <read_first>
    - lib/vkb-server/api-routes.js (handleExperimentRun :952-1098 — the :957 body destructure, the :1009-1022 V5 spec-listing gate, the :1040-1055 overrides fold, the :1073-1075 _coordinatorPost; and wherever a persisted Run is read for the runs table, to resolve origin_span_id → origin Run prompt/snapshot WITHOUT opening the live experiment LevelDB, Pitfall 6)
    - lib/experiments/avenue-spec.mjs (synthesizeAvenueSpec :105 signature + synthesizeToYamlFile :144 — the exact args: { originRun, variants, repeats } and { dir, repoRoot }; the output basename convention avenue-<origin_task_id>.yaml)
    - scripts/health-coordinator.js (/experiments/run :2735-2766 — confirm `overrides` is forwarded WHOLE to runExperiment at :2762 so folded keys reach run-launch; add a passthrough ONLY if it filters keys)
    - lib/experiments/run-launch.mjs (buildRunArgv — Task 1's --avenue/--origin-span-id emission; the overrides keys it now reads)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-03-PLAN.md (synthesizeAvenueSpec contract) + 87-RESEARCH.md §8 (Fork = capture origin snapshot + synthesize spec + POST)
  </read_first>
  <action>
    In `lib/vkb-server/api-routes.js` `handleExperimentRun`: extend the :957 body destructure to `{ spec, overrides, rerun_of, origin_span_id, forkAxes }`. When `origin_span_id` is present (fork mode): (1) resolve the origin Run record by origin_span_id via the SAME persisted-Run read the runs table uses (never the live experiment LevelDB — Pitfall 6), extracting its goal_sentence/prompt + snapshot_id as `originRun`; (2) map `forkAxes` (agents/models/frameworks/kbOn/kbOff — the ForkAxes shape) into the `variants` array synthesizeAvenueSpec expects (one cell per chosen agent×model×framework×env, env = kb-on/kb-off from the kbOn/kbOff flags); (3) call `synthesizeAvenueSpec({ originRun, variants, repeats })` then `synthesizeToYamlFile(synthSpec, { repoRoot: this._repoRoot() })` to persist `config/experiments/avenue-<origin_task_id>.yaml`; (4) set the local `spec` variable to that avenue basename so it PASSES the existing V5 spec-listing gate (:1009-1022) unchanged; (5) fold `origin_span_id` and `avenue: true` into the forwarded `ov` overrides object (alongside the existing rerun_of fold at :1046-1051) so run-launch's buildRunArgv (Task 1) emits `--origin-span-id` + `--avenue`. Validate `origin_span_id` shape defensively (reject an ill-shaped id with 400, mirroring the rerun_of validation) and 404 if the origin Run cannot be resolved — never forward a raw/unknown id. Non-fork launches (origin_span_id absent) stay byte-identical.
    In `scripts/health-coordinator.js` /experiments/run (:2735-2766): confirm `overrides` reaches `runExperiment` unfiltered (it already does at :2762); add an explicit origin_span_id/avenue passthrough ONLY if the coordinator strips unknown override keys. Keep the V4 origin gate + run_id/run_dir sanitization intact.
  </action>
  <acceptance_criteria>
    - `grep -nE "origin_span_id|forkAxes" lib/vkb-server/api-routes.js` matches in handleExperimentRun's body destructure (both fields read server-side — CR-02 closed).
    - `grep -n "synthesizeAvenueSpec" lib/vkb-server/api-routes.js` matches (imported + called) AND `grep -n "synthesizeToYamlFile" lib/vkb-server/api-routes.js` matches (the avenue spec is persisted so it passes the V5 listing gate).
    - `grep -nE "ov\.origin_span_id|ov\.avenue|avenue: true|origin_span_id:" lib/vkb-server/api-routes.js` shows origin_span_id + avenue folded into the forwarded overrides for the coordinator POST.
    - The coordinator forwards overrides whole: `grep -nE "overrides" scripts/health-coordinator.js` at the /experiments/run handler confirms `overrides` is passed to runExperiment unfiltered (or an explicit origin_span_id passthrough was added).
    - vkb-server unit/route tests still pass: `node --test tests/vkb-server/*.test.*js 2>/dev/null || node --test $(grep -rl handleExperimentRun tests 2>/dev/null | head -1)` — the non-fork rerun path is byte-unchanged (rerun_of still folded, V5 gate still rejects unlisted specs).
  </acceptance_criteria>
  <verify>
    <automated>node --test $(grep -rl "handleExperimentRun\|experiments/run" tests 2>/dev/null | tr '\n' ' ')</automated>
  </verify>
  <done>handleExperimentRun reads origin_span_id + forkAxes, synthesizes+persists the avenue spec, sets spec to that basename, and forwards origin_span_id/avenue through the coordinator; non-fork path unchanged; coordinator forwards overrides whole.</done>
</task>

<task type="auto">
  <name>Task 3: Client payload + axes-aware preview — forkAxes/sweep in launchExperiment, honest previewCellCount (CR-02/CR-03 client)</name>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx (:146-154 previewCellCount, :156-169 avenueCount/estTokens/overThreshold, :206-218 buildOverrides, :220-251 onLaunch/launchExperiment dispatch, :171-180 toggleAxisMember/forkAxes state)
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (:427-439 ForkAxes, :447-460 LauncherPrefill, :964-994 launchExperiment thunk type + POST body, :1875-1890 selectAvenuesByOrigin so the grouping is understood — do NOT modify it, it is already correct)
    - lib/vkb-server/api-routes.js (Task 2's fork synthesis — the server now returns/knows the axes-aware cell count; confirm what the launch/preview round-trip can read back)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-05-PLAN.md (the launcher axis picker + guardrail contract) + 87-UI-SPEC.md (server-resolved count, launch-disabled-until-preview)
  </read_first>
  <action>
    In `performanceSlice.ts`: extend the `launchExperiment` thunk's argument type (:964-967) with `forkAxes?: ForkAxes` and `sweep?: boolean`, and include both in the POST body (:980-985) alongside the existing origin_span_id. Do NOT create a new slice (Phase 86 frozen-contract discipline) — extend in place.
    In `experiment-launcher.tsx`: (1) `buildOverrides()` (:206-218) OR the `onLaunch` dispatch (:230-235) must now pass `forkAxes` + `sweep` into `launchExperiment(...)` so the chosen axes actually reach the server (the field is currently never sent — the picker is decorative). (2) Make `previewCellCount` (:146-154) AXES-AWARE in fork mode: when a fork prefill is active, the count must reflect the CHOSEN forkAxes (agents×models×frameworks×injection cells, ×repeats), sourced from a SERVER round-trip that resolves the actual fork request — NOT `selectedSpec.variantCount/cellCount` (the origin spec's static YAML metadata, which is the wrong source per CR-02). Reuse the existing server-resolved-count seam pattern (the same D-09 discipline: the SERVER is authoritative for the count, the client does not smuggle a spoofed cross-product); if the server preview seam does not yet return an axes-aware count, add a minimal server preview call that synthesizes-and-counts without launching, and consume its cellCount here. Keep the launch button DISABLED until the axes-aware preview has resolved (the existing `avenueCount == null` gate at :225), and keep the "Launch {N} avenues" CTA reflecting that count. Non-fork (plain rerun) preview stays exactly as-is.
    Build the dashboard so the typecheck gate runs: `cd integrations/system-health-dashboard && npm run build`.
  </action>
  <acceptance_criteria>
    - `grep -nE "forkAxes|sweep" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` shows both in the launchExperiment thunk type AND in the POST body (the axes now reach the server).
    - `grep -nE "forkAxes|sweep" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows both passed into the launchExperiment dispatch (the picker is no longer decorative).
    - previewCellCount in fork mode no longer derives solely from origin-spec static metadata: `grep -n "previewCellCount" experiment-launcher.tsx` and the fork-mode branch references the chosen axes / a server axes-aware count (not `selectedSpec.variantCount` as the fork source) — assert the fork branch does NOT read selectedSpec.variantCount for its count.
    - No client cross-product spoof feeding the launch gate: `grep -cE "\.length \* .*\.length" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows no NEW client-side axes multiplication used as the authoritative launch-gating count (server count stays authoritative).
    - No new slice: `grep -rc "createSlice" integrations/system-health-dashboard/src/store/slices/` unchanged from baseline.
    - Dashboard builds: `cd integrations/system-health-dashboard && npm run build` succeeds (typecheck gate).
  </acceptance_criteria>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <done>launchExperiment carries forkAxes + sweep; the fork preview count is server-resolved + axes-aware (not the origin spec's static metadata); launch disabled until the axes-aware preview renders; no new slice; build clean.</done>
</task>

<task type="auto">
  <name>Task 4: End-to-end thread assertion — a fork launch yields an origin_span_id-bearing avenue Run (AVN-01/07 flip)</name>
  <read_first>
    - lib/experiments/run-write.mjs (:139 origin_span_id persistence — CONFIRM it reads t.origin_span_id ?? null; do NOT modify, it is correct)
    - scripts/measurement-start.mjs (:113-116/:199 --origin-span-id parse) + scripts/measurement-stop.mjs (:180 origin_span_id passthrough) — the persistence path Task 1's threading now REACHES
    - lib/experiments/experiment-runner.mjs (Task 1's threaded runCell call) + scripts/experiment-run.mjs (Task 1's CLI flags)
    - tests/experiments/avenue-spec.test.mjs + tests/experiments/experiment-runner.test.mjs (the injectable-seam idiom to drive runMatrix with fake spawnAgent/runMeasurement so no real agent spawns)
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (selectAvenuesByOrigin ~:1881 — the grouping predicate the produced Run must satisfy)
  </read_first>
  <action>
    Add an integration test `tests/experiments/avenue-fork-thread.test.mjs` (repo convention — tests/experiments/, never lib/**) proving the CR-01/CR-02/CR-03 thread is now CONTINUOUS end-to-end, using the REAL runMatrix/runCell/run-write code with injectable seams (fake spawnAgent + fake runMeasurement so no real agent launches): (a) drive `runMatrix(avenueSpec, { avenue:true, originSpanId:'origin-xyz', ...fakeSeams })` and assert the fake measurement-start seam RECEIVED `--origin-span-id origin-xyz` (or the equivalent originSpanId param) for each avenue cell — proving Task 1's threading reaches the persistence boundary; (b) assert a Run written through run-write in that flow reads back with `origin_span_id === 'origin-xyz'` (non-null), and a non-avenue run reads null (honesty, no fabrication); (c) a documentation-level assertion that this non-null origin_span_id is exactly the predicate selectAvenuesByOrigin groups on (cite AVN-07 in the test name so re-verification traces the flip). Reference AVN-01/AVN-02/AVN-07 in test-name strings for traceability. Then run the full phase-87 experiments suite to confirm no regression: `node --test tests/experiments/`.
  </action>
  <acceptance_criteria>
    - `node --test tests/experiments/avenue-fork-thread.test.mjs` passes all assertions against the real runMatrix/runCell/run-write code (injectable seams, no real spawn).
    - The test proves originSpanId reaches the measurement-start boundary: `grep -nE "origin-span-id|originSpanId" tests/experiments/avenue-fork-thread.test.mjs` matches, and the assertion is on the SEAM receiving it (not a mock of run-write).
    - The Run round-trip assertion is present: `grep -n "origin_span_id" tests/experiments/avenue-fork-thread.test.mjs` matches with a non-null AND a null case.
    - Traceability: `grep -cE "AVN-01|AVN-07" tests/experiments/avenue-fork-thread.test.mjs` ≥ 1.
    - Full experiments suite green (no regression from the threading): `node --test tests/experiments/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/avenue-fork-thread.test.mjs && node --test tests/experiments/</automated>
  </verify>
  <done>An integration test proves a fork launch (avenue:true + originSpanId) threads through the REAL runner to run-write, producing a Run with a non-null origin_span_id that selectAvenuesByOrigin groups — AVN-01/AVN-07 have a live data path; full experiments suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser fork request → vkb-server | origin_span_id + forkAxes cross from the untrusted client into spec synthesis |
| synthesized avenue spec → config/experiments file | server writes a client-influenced spec basename onto disk, then re-reads it through the V5 listing gate |
| server overrides → coordinator → host runner | origin_span_id/avenue folded into overrides cross the container→host bridge into a detached spawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-07-01 | Tampering (path/spec injection) | synthesizeToYamlFile output basename | mitigate | basename derived from sanitizeTaskId(origin_task_id) (avenue-spec.mjs convention); the written file must still PASS the existing V5 server-listing gate (:1009-1022) before it launches — a synthesized name that escapes config/experiments is rejected by that gate unchanged |
| T-87-07-02 | Spoofing (unknown origin) | origin_span_id from the client | mitigate | validate origin_span_id shape (mirror rerun_of validation, 400 on ill-shaped); 404 if the origin Run cannot be resolved from the persisted Run store — never forward a raw/unknown id to the runner |
| T-87-07-03 | Tampering (count spoof) | client axes-aware preview count | mitigate | the launch-gating count stays SERVER-resolved (D-09); grep gate forbids a NEW client cross-product multiply feeding the preview; the server re-synthesizes+counts the authoritative matrix |
| T-87-07-04 | Repudiation / mis-attribution | avenue Run origin linkage | mitigate | Task 4 asserts the REAL thread stamps the exact origin_span_id (no fabrication; null when absent); disjoint per-task_id aggregation already proven by 87-03's avenue-crossbranch test |
| T-87-07-05 | Info disclosure (Pitfall 6) | origin Run resolution during a live run | mitigate | resolve origin_span_id via the persisted Run read the runs table already uses — NEVER open the experiment LevelDB during a live run (lock contention) |
| T-87-07-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO installs (threading + tests only); `git diff --stat package.json integrations/system-health-dashboard/package.json` is empty |
</threat_model>

<verification>
- `node --test tests/experiments/experiment-runner.test.mjs tests/experiments/run-launch.test.mjs` green (CR-01 threading + argv).
- `node --test $(grep -rl "handleExperimentRun\|experiments/run" tests | tr '\n' ' ')` green (CR-02 server fork synthesis; non-fork unchanged).
- `cd integrations/system-health-dashboard && npm run build` clean (client payload + axes-aware preview typecheck).
- `node --test tests/experiments/avenue-fork-thread.test.mjs && node --test tests/experiments/` green (end-to-end thread; no regression).
- `git diff --stat package.json integrations/system-health-dashboard/package.json` empty (no installs).
</verification>

<success_criteria>
- runMatrix forwards avenue/originSpanId/commitAvenue into runCell; CLI + run-launch emit the flags (CR-01 closed).
- handleExperimentRun reads origin_span_id + forkAxes, synthesizes+persists the avenue spec, forwards through the coordinator (CR-02 closed).
- The client sends forkAxes/sweep and shows an axes-aware server-resolved preview (AVN-03 honest).
- A fork launch produces a Run with a non-null origin_span_id that selectAvenuesByOrigin groups (AVN-01/AVN-07 have real data; CR-03 resolves automatically).
</success_criteria>

<output>
Create `.planning/phases/87-interactive-spans-and-branch-avenues/87-07-SUMMARY.md` when done.
</output>
