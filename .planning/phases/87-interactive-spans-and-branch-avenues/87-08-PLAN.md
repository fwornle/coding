---
phase: 87-interactive-spans-and-branch-avenues
plan: 08
type: execute
wave: 2
depends_on: [87-07]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [AVN-01, AVN-02, AVN-03, AVN-07]
must_haves:
  truths:
    - "The two Blocker-severity misleading code comments (performanceSlice.ts:975-979 + experiment-launcher.tsx:156-161) are corrected to describe the REAL post-87-07 wiring — no comment asserts behavior the code does not implement"
    - "REQUIREMENTS.md contains an AVN-01..09 requirements section + a traceability table mapping each to its phase-87 plan and status, closing the ledger gap"
    - "A live main-tree dashboard fork of a completed span (non-default agent/model axis) produces a ranked, origin-grouped avenue row with an outcome score and a git-computed merge badge in the Avenues panel, verified visually in both light and dark themes"
  artifacts:
    - path: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      provides: "corrected launchExperiment comment matching the real server thread"
      contains: "forkAxes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AVN-01..09 requirements section + traceability rows"
      contains: "AVN-01"
  key_links:
    - from: "performanceSlice.ts launchExperiment comment"
      to: "the real server thread (Plan 87-07 CR-02 fix)"
      via: "comment text correction"
      pattern: "AVN-02|origin_span_id"
    - from: "experiment-launcher.tsx axis-count comment"
      to: "the real axes-aware forkAxes send + server count (Plan 87-07)"
      via: "comment text correction"
      pattern: "forkAxes"
    - from: "REQUIREMENTS.md AVN section"
      to: "ROADMAP AVN-01..09 + phase-87 plan frontmatter"
      via: "requirements ledger entry + traceability table"
      pattern: "AVN-0"
---

<objective>
Close the two documentation-hygiene gaps (G4, G5) that the verifier flagged and perform the live end-to-end human verification the phase goal demands. G4: two Blocker-severity code comments assert cross-layer wiring that did not exist pre-87-07 (performanceSlice.ts:975-979 falsely claims the server threads origin_span_id to the runner; experiment-launcher.tsx:156-161 falsely claims "the SERVER resolves HOW MANY" when forkAxes was never sent) — now that 87-07 makes both statements TRUE, correct the comments to match the shipped wiring so a future maintainer trusts them. G5: REQUIREMENTS.md has zero AVN-01..09 entries despite these IDs being used throughout ROADMAP.md and all phase-87 plans — add the section + traceability. Then rebuild the bind-mounted main-tree dashboard and visually verify (gsd-browser) that a real fork now populates the origin-grouped ranked Avenues panel — the human-verification block from 87-VERIFICATION.md.

Purpose: flip the AVN-01/AVN-02/AVN-03/AVN-07 verification truths from FAILED/PARTIAL to VERIFIED by (a) removing the misleading-comment anti-patterns, (b) closing the traceability ledger gap, and (c) proving the end-to-end fork live against real data.
Output: two corrected comments, a REQUIREMENTS.md AVN section + traceability rows, a rebuilt+restarted live dashboard, and a recorded gsd-browser visual verification in both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-VERIFICATION.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-07-PLAN.md

<interfaces>
<!-- Confirmed seams. -->

G4 comment #1 — performanceSlice.ts :975-979: currently claims "The server threads it to the runner's
  --origin-span-id (Plan 87-03) so avenue Runs group by origin" and describes the fork as already-wired.
  Post-87-07 this IS true (handleExperimentRun reads origin_span_id + forkAxes, synthesizes the avenue
  spec, folds origin_span_id/avenue into the coordinator overrides). Correct the comment to also note
  forkAxes/sweep are now sent (87-07), not just origin_span_id.
G4 comment #2 — experiment-launcher.tsx :156-161: currently claims "The axis selections shape WHAT is
  forked; the SERVER resolves HOW MANY cells that becomes." Pre-87-07 forkAxes was never sent (aspirational).
  Post-87-07 forkAxes IS sent and previewCellCount is axes-aware/server-resolved. Correct the comment to
  match: the axes now reach the server, and the count reflects the actual fork request.

G5 — .planning/REQUIREMENTS.md: has a `## v7.5 Requirements` section (:21) with sub-sections
  (SPEC/RUN/CMP/ORCH, :31-53) and a `## v7.5 Traceability` table (:56-73). AVN-01..09 appear NOWHERE.
  ROADMAP.md :424 lists Requirements: AVN-01..09 for Phase 87; each phase-87 PLAN.md frontmatter uses
  a subset. Requirement descriptions are recoverable from the ROADMAP goal (:422) + 87-CONTEXT decisions
  D-01..D-08 + the 87-VERIFICATION "Requirements Coverage" table (:157-167, which already names each
  AVN-01..09 with a one-line description + source plan).

Requirement-entry format (mirror the existing ledger, e.g. REQUIREMENTS.md :33/:38/:45):
  `- [ ] **AVN-01:** <description>.`  and traceability rows: `| AVN-01 | Phase 87 (87-03/87-07) | Complete |`.

Dashboard rebuild recipe (CLAUDE.md — bind-mounted, VirtioFS caches):
  cd integrations/system-health-dashboard && npm run build
  docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend
Visual verify: gsd-browser navigate http://localhost:3032 → Performance → Avenues; NEVER a hand-rolled
  node playwright script (feedback_dashboard_screenshots_gsd_browser); NEVER claim "works" from a DB
  query (feedback_e2e_verify).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two misleading Blocker comments + add the AVN-01..09 REQUIREMENTS section (G4 + G5)</name>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (:975-979 — the launchExperiment comment; and :980-985 the POST body AFTER 87-07 added forkAxes/sweep, so the correction matches the shipped body)
    - integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx (:156-169 — the axis-count comment; and :146-154 previewCellCount AFTER 87-07 made it axes-aware)
    - .planning/REQUIREMENTS.md (:21 v7.5 Requirements header, :31-53 the SPEC/RUN/CMP/ORCH sub-section format, :56-73 the v7.5 Traceability table, :75-96 the per-phase requirement narrative — mirror ALL for the AVN section)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-VERIFICATION.md (:157-167 Requirements Coverage — the authoritative one-line description + source plan per AVN-01..09)
    - .planning/ROADMAP.md (:420-445 Phase 87 goal + requirements + per-plan mapping)
  </read_first>
  <action>
    (G4) In `performanceSlice.ts` (:975-979): edit the `launchExperiment` comment so it describes the REAL post-87-07 wiring — the fork POSTs `origin_span_id` AND `forkAxes`/`sweep`; the server (handleExperimentRun, 87-07) reads them, synthesizes the avenue spec via synthesizeAvenueSpec, and threads origin_span_id + `--avenue` through the coordinator to the runner so avenue Runs carry origin_span_id and group by origin. Remove any wording implying origin_span_id alone is sent or that the server "already" threads it when it did not. Do NOT alter code — comment text only.
    (G4) In `experiment-launcher.tsx` (:156-161): edit the axis-count comment so it matches the shipped behavior — the axis selections are now SENT to the server (forkAxes in the launch payload, 87-07), and the launch-gating count is the SERVER-resolved axes-aware previewCellCount reflecting the actual fork request (not the origin spec's static metadata). Remove the aspirational "shape WHAT is forked; the SERVER resolves HOW MANY" framing that described unimplemented behavior. Comment text only.
    (G5) In `.planning/REQUIREMENTS.md`: add a `### Interactive Spans & Branch Avenues (AVN)` sub-section under `## v7.5 Requirements` with one `- [ ] **AVN-0N:** <description>.` bullet per AVN-01..09 (descriptions sourced from the 87-VERIFICATION Requirements Coverage table + ROADMAP goal), and add nine rows to the `## v7.5 Traceability` table mapping each AVN to `Phase 87` (with the source plan(s), e.g. `Phase 87 (87-03/87-07)`) and its verified status (AVN-04/05/06/08/09 Complete; AVN-01/02/03/07 Complete after 87-07). Follow the documentation-style skill (invoke `/documentation-style` before editing the markdown ledger — CLAUDE.md mandate).
  </action>
  <acceptance_criteria>
    - `grep -nE "forkAxes|synthesizeAvenueSpec|--avenue" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` shows the corrected comment references the real fork thread (not origin_span_id alone); no residual "The server threads it to the runner's --origin-span-id (Plan 87-03)" claiming an unwired state.
    - `grep -n "shape WHAT is forked" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` returns NOTHING (the aspirational framing is gone); `grep -nE "forkAxes|axes-aware|server-resolved" experiment-launcher.tsx` shows the corrected comment.
    - `grep -cE "AVN-0[1-9]" .planning/REQUIREMENTS.md` shows all nine IDs present (≥ 18 matches: 9 bullets + 9 traceability rows).
    - `grep -n "AVN-01" .planning/REQUIREMENTS.md` matches in BOTH a requirement bullet and a traceability row.
    - Dashboard still builds (comment-only edits do not break the typecheck): `cd integrations/system-health-dashboard && npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "AVN-0[1-9]" .planning/REQUIREMENTS.md && ! grep -q "shape WHAT is forked" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx && cd integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <done>Both misleading Blocker comments describe the real post-87-07 wiring; REQUIREMENTS.md has an AVN-01..09 section + traceability rows; dashboard build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live main-tree fork → origin-grouped ranked Avenues panel, both themes (the 87-VERIFICATION human-verification gate)</name>
  <what-built>
    The full 87-07 origin_span_id + forkAxes thread (runner→CLI→run-launch→vkb-server→coordinator→client)
    plus the corrected comments. The dashboard is BIND-MOUNTED from the main tree, so after a `npm run build`
    + frontend restart the live :3032 dashboard reflects the fix (the prior worktree-authored e2e spec was
    only typecheck/collection-gated, never run against live data). A real fork of a completed span now
    synthesizes an avenue spec, launches an avenue matrix on an `avenue/<task_id>` branch, and the resulting
    Run carries a non-null origin_span_id that selectAvenuesByOrigin groups + rankAvenues ranks.
  </what-built>
  <how-to-verify>
    Claude performs these automated steps FIRST, then pauses for the operator to confirm the visual result:
    1. Rebuild + restart the bind-mounted dashboard (CLAUDE.md recipe):
       `cd integrations/system-health-dashboard && npm run build`
       `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`
       (verify host vs container file sizes match if the backend server.js was touched — it was not here, frontend-only).
    2. `gsd-browser navigate http://localhost:3032` → Performance tab. Confirm a COMPLETED span row exists
       (if none, note the data-presence gap and, per D-01 scriptable path, launch a small fork via the
       documented CLI `experiment-run.mjs --spec config/experiments/avenue-<origin>.yaml --avenue --origin-span-id <id>`
       to seed one — never fabricate the result).
    3. Click "Fork into avenues" on the completed row; in the launcher pick a NON-DEFAULT agent/model axis
       (e.g. a different agent than the origin); confirm the server-resolved count/cost preview renders and
       reflects the CHOSEN axes (not the origin spec's static count); Launch {N} avenues.
    4. Wait for the avenue run to complete; open the Avenues sub-tab.
    Operator confirms: a ranked avenue row appears UNDER the origin span's card, sorted best-first, with a
    REAL (non-em-dash) outcome score, a git-computed merge-status badge, and working Promote/Prune actions —
    verified in BOTH light and `.dark` themes via gsd-browser screenshots (record the screenshot observations
    in the SUMMARY; do NOT claim "works" from a DB query — feedback_e2e_verify / feedback_dashboard_screenshots).
  </how-to-verify>
  <resume-signal>Type "approved" once the ranked origin-grouped avenue row (with score + merge badge) is visible in both themes, or describe what the panel actually showed (e.g. still empty-state) so the gap can be re-planned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live fork request → runner | the human-verify step exercises the full untrusted-client→host-runner thread with real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-08-01 | Repudiation | claiming the panel works without live evidence | mitigate | the checkpoint is a BLOCKING human-verify gated on real gsd-browser screenshots in both themes; a DB-query claim is explicitly forbidden (feedback_e2e_verify) |
| T-87-08-02 | Tampering | misleading comment reintroduced | mitigate | grep gates assert the aspirational framing is GONE and the corrected comment references the real forkAxes/synthesizeAvenueSpec thread |
| T-87-08-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO installs (docs + comments + a rebuild of existing deps); no package.json change |
</threat_model>

<verification>
- `grep -cE "AVN-0[1-9]" .planning/REQUIREMENTS.md` ≥ 18 (9 bullets + 9 traceability rows).
- `! grep -q "shape WHAT is forked" experiment-launcher.tsx` (the aspirational comment is gone).
- `cd integrations/system-health-dashboard && npm run build` clean.
- Live gsd-browser visual verify (both themes) recorded in SUMMARY — the human-verification gate from 87-VERIFICATION.md.
</verification>

<success_criteria>
- Both Blocker misleading comments corrected to the real post-87-07 wiring (G4).
- REQUIREMENTS.md AVN-01..09 section + traceability added (G5).
- A live main-tree fork produces a ranked origin-grouped avenue row (score + merge badge) in both themes (the 87-VERIFICATION human gate) — AVN-01/02/03/07 flip to VERIFIED on re-verification.
</success_criteria>

<output>
Create `.planning/phases/87-interactive-spans-and-branch-avenues/87-08-SUMMARY.md` when done.
</output>
