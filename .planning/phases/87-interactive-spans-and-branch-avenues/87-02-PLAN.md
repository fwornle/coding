---
phase: 87-interactive-spans-and-branch-avenues
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/experiments/experiment-spec.mjs
  - tests/experiments/experiment-spec.test.mjs
  - src/hooks/knowledge-injection-hook.js
  - scripts/launch-agent-common.sh
  - tests/experiments/injection-env.test.mjs
autonomous: true
requirements: [AVN-03, AVN-04]
must_haves:
  truths:
    - "`mastracode` is a legal agent — a mastra avenue cell no longer HARD-BLOCKS in validateCells"
    - "Knowledge-injection is an avenue axis encoded in the existing `env` field (kb-on / kb-off) — no silent 5th-key drop"
    - "An injection-OFF avenue threads `CODING_KNOWLEDGE_INJECTION=0` into the agent's spawned env; both the Claude hook and the bash injector early-return when it is off"
    - "Toggling injection off for an avenue does NOT disable injection for the operator's interactive session"
  artifacts:
    - path: "lib/experiments/experiment-spec.mjs"
      provides: "mastracode in KNOWN_AGENTS + knowledge-injection env-axis vocabulary"
      contains: "mastracode"
    - path: "src/hooks/knowledge-injection-hook.js"
      provides: "CODING_KNOWLEDGE_INJECTION off → early return before retrieval (Claude)"
      contains: "CODING_KNOWLEDGE_INJECTION"
    - path: "scripts/launch-agent-common.sh"
      provides: "CODING_KNOWLEDGE_INJECTION off → early return in _inject_knowledge_context (opencode/copilot/mastra)"
      contains: "CODING_KNOWLEDGE_INJECTION"
  key_links:
    - from: "avenue env (kb-off)"
      to: "CODING_KNOWLEDGE_INJECTION=0 in agent child env"
      via: "spec env-axis → runner agentBaseEnv (consumed in Plan 03)"
      pattern: "CODING_KNOWLEDGE_INJECTION"
    - from: "src/hooks/knowledge-injection-hook.js main()"
      to: "early return (no callRetrieval)"
      via: "env-var guard before retrieval"
      pattern: "CODING_KNOWLEDGE_INJECTION"
---

<objective>
Make the spec model and the injection seam ready for the D-03 axes: (1) add `mastracode` to the agent enum so a mastra avenue is not hard-blocked, (2) encode the knowledge-injection on/off axis in the EXISTING `env` cell field (avoiding the Pitfall-3 5th-key contract break), and (3) add the per-avenue `CODING_KNOWLEDGE_INJECTION` on/off guard at both injection seams (Claude hook + bash injector) scoped to the spawned agent env only.

Purpose: AVN-04 (knowledge-injection toggle wired per-agent) + the spec-axis half of AVN-03 (variant axes agent × model × framework × knowledge-injection). This is a backend seam that Plan 03 (runner) threads into agent launch and Plan 04 (picker) surfaces.
Output: extended `experiment-spec.mjs` (enum + injection env vocabulary), guarded injection hook + bash injector, and a unit test proving the env-threading + early-return.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-CONTEXT.md
@.planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md

<interfaces>
<!-- Confirmed seams (read this session). -->

experiment-spec.mjs:39  export const KNOWN_AGENTS = Object.freeze(['claude', 'copilot', 'opencode']);  // mastracode MISSING
experiment-spec.mjs:80  function makeCell({ agent, model, framework, env, test_command }) { ... }  // EXACTLY these 5 keys; any other key silently dropped
experiment-spec.mjs:103 export function expandAxes(axes = {}, { test_command } = {})  // cartesian over agent[]/model[]/framework[]/env[]
experiment-spec.mjs:143 validateCells: HARD-BLOCK if !KNOWN_AGENTS.includes(cell.agent)

Injection seams (RESEARCH §3):
- Claude: src/hooks/knowledge-injection-hook.js main() → callRetrieval(...) → hookSpecificOutput.additionalContext. Global ~/.claude/settings.json:117 UserPromptSubmit registration (fires for interactive session too → Pitfall 4).
- Non-Claude: scripts/launch-agent-common.sh _inject_knowledge_context() (lines ~337-365), called at :570. Runs src/hooks/knowledge-injection-${agent}.js (opencode/copilot/mastra).

Encoding decision (RESEARCH Open Q2 recommendation): reuse the `env` axis value `kb-on`/`kb-off` — do NOT add a 5th cell key (Pitfall 3). The runner (Plan 03) maps `env === 'kb-off'` → `CODING_KNOWLEDGE_INJECTION=0` in the agent child env.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mastracode agent enum + kb-on/kb-off env-axis vocabulary in experiment-spec</name>
  <read_first>
    - lib/experiments/experiment-spec.mjs (KNOWN_AGENTS:39, makeCell:80, expandAxes:103, validateCells:143, SHELL_META_RE reject:162)
    - tests/experiments/experiment-spec.test.mjs (existing enum/validate tests to extend — repo convention: tests live under tests/experiments/, never lib/**)
    - lib/experiments/agent-headless.mjs (argvForAgent — confirms `mastracode` is the supported literal, matching RESEARCH A1)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md §2 (spec gap) + Assumptions A1/A2 + Pitfall 3
  </read_first>
  <behavior>
    - Test: a cell with `agent:'mastracode'` PASSES validateCells (no longer hard-blocked); an unknown agent still hard-blocks with the fail-fast error listing legal agents.
    - Test: `expandAxes({agent:['claude'], model:['sonnet'], framework:['gsd'], env:['kb-on','kb-off']})` yields 2 cells differing only in `env`, each carrying exactly the 5 canonical keys.
    - Test: a `kb-off` cell survives makeCell (env is a first-class existing key, not dropped) — asserts against Pitfall 3.
  </behavior>
  <action>
    In `lib/experiments/experiment-spec.mjs`, add `'mastracode'` to the frozen `KNOWN_AGENTS` array (per RESEARCH A1 — the literal matching `argvForAgent`/`AGENT_CONFIG_FILE`; leave a comment that the variant picker emits `mastra`→`mastracode` mapping is Plan 04's job). Encode the knowledge-injection axis as the reserved `env` values `kb-on` / `kb-off` — document these as the canonical injection-axis vocabulary in the module header comment (the same header that enumerates the 5 keys), so downstream readers (runner in Plan 03) map `env==='kb-off'` → injection disabled. Do NOT add a 5th cell key (Pitfall 3): `env` already exists and passes through makeCell. Extend `experiment-spec.test.mjs` with the three behaviors above.
  </action>
  <acceptance_criteria>
    - `grep -n "mastracode" lib/experiments/experiment-spec.mjs` matches inside KNOWN_AGENTS.
    - `node --test tests/experiments/experiment-spec.test.mjs` passes (mastracode legal, kb-on/kb-off expand to 2 cells, unknown agent still blocks).
    - `grep -nE "kb-on|kb-off" lib/experiments/experiment-spec.mjs` matches (injection vocabulary documented).
    - `grep -c "function makeCell" lib/experiments/experiment-spec.mjs` shows makeCell still destructures exactly `agent, model, framework, env, test_command` (no 5th key added — Pitfall 3 held).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/experiment-spec.test.mjs</automated>
  </verify>
  <done>mastracode is a legal agent; kb-on/kb-off is the documented injection env-axis; makeCell 5-key contract unchanged; spec suite green.</done>
</task>

<task type="auto">
  <name>Task 2: CODING_KNOWLEDGE_INJECTION off-guard at both injection seams + env-threading test (AVN-04)</name>
  <read_first>
    - src/hooks/knowledge-injection-hook.js (main() entry, before callRetrieval; MAX_CONTEXT_CHARS)
    - scripts/launch-agent-common.sh (_inject_knowledge_context, lines ~337-365; call site ~:570)
    - src/hooks/retrieval-client.js (callRetrieval signature — confirm the guard sits BEFORE this call)
    - .planning/phases/87-interactive-spans-and-branch-avenues/87-RESEARCH.md §3 (toggle wiring + Pitfall 4 scoping caveat + A5 env-inheritance)
  </read_first>
  <action>
    Add a single env-var guard `CODING_KNOWLEDGE_INJECTION` (default ON; only the literal `0`/`false`/`off` disables) at BOTH seams. (1) In `src/hooks/knowledge-injection-hook.js`, at the very top of `main()` — BEFORE `callRetrieval` — read `process.env.CODING_KNOWLEDGE_INJECTION`; when disabled, emit an empty/absent `hookSpecificOutput.additionalContext` and return early (no retrieval call, no working-memory prefix). (2) In `scripts/launch-agent-common.sh` `_inject_knowledge_context()`, add an early `return 0` at the function top when `$CODING_KNOWLEDGE_INJECTION` is disabled — one guard covers opencode/copilot/mastra. Do NOT log a secret; use the project logger / existing echo idiom in the bash function (no raw console outside the logger — CLAUDE.md constraint applies to the JS hook: use the hook's existing stderr-logger, not a new console.log). CRITICAL (Pitfall 4): the guard reads the env var, so it only disables injection for a process whose env has it set — the operator's interactive session (env unset) keeps injection. Write `tests/experiments/injection-env.test.mjs` that (a) invokes the Claude hook with `CODING_KNOWLEDGE_INJECTION=0` and asserts NO retrieval call / empty additionalContext (mock or stub callRetrieval), and (b) asserts the default (env unset) path still calls retrieval — proving the guard is scoped, not global.
  </action>
  <acceptance_criteria>
    - `grep -n "CODING_KNOWLEDGE_INJECTION" src/hooks/knowledge-injection-hook.js` matches BEFORE the callRetrieval line (`grep -n "callRetrieval" src/hooks/knowledge-injection-hook.js` line number is greater).
    - `grep -n "CODING_KNOWLEDGE_INJECTION" scripts/launch-agent-common.sh` matches inside `_inject_knowledge_context`.
    - `node --test tests/experiments/injection-env.test.mjs` passes: off → no retrieval / empty prefix; unset → retrieval invoked (scoping proven).
    - `grep -nE "console\.(log|info|warn|error)" src/hooks/knowledge-injection-hook.js` shows no NEW raw console added by this change (logger/stderr idiom preserved — constraint-dodging forbidden).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/injection-env.test.mjs</automated>
  </verify>
  <done>Both injection seams early-return when CODING_KNOWLEDGE_INJECTION is off; guard is per-process (interactive session unaffected); env-threading test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| avenue axis value → agent child env | `env: kb-off` maps to an env var consumed by the spawned agent; must not leak to the operator session |
| agent literal (mastracode) → argvForAgent | agent field feeds fixed-argv launch (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02-01 | Tampering | agent field → validateCells → argvForAgent | mitigate | mastracode added to the frozen KNOWN_AGENTS allowlist; validateCells still fail-fast-rejects any agent not in the enum (no arbitrary agent literal reaches argv) |
| T-87-02-02 | Information Disclosure / scope leak | global Claude injection hook | mitigate | the off-guard is env-var-scoped to the spawned avenue child only; unit test proves the default (unset) path still injects, so the operator's interactive session is never disabled (Pitfall 4) |
| T-87-02-03 | Tampering (5th-key drop) | makeCell contract | mitigate | injection encoded in the existing `env` key (kb-on/kb-off), not a new cell key — no silent drop across measurement-stop/evidence-harness readers (Pitfall 3); grep asserts the 5-key destructure is unchanged |
</threat_model>

<verification>
- `node --test tests/experiments/experiment-spec.test.mjs` green (mastracode + kb-on/kb-off).
- `node --test tests/experiments/injection-env.test.mjs` green (per-process guard, scoped).
- No new raw console in the JS hook (logger idiom preserved).
</verification>

<success_criteria>
- A mastra avenue cell validates (AVN-03 axis completeness).
- Knowledge-injection is an env-axis toggle wired per-agent, scoped to the avenue child (AVN-04).
- No 5th-key contract break; no global injection disable.
</success_criteria>

<output>
Create `.planning/phases/87-interactive-spans-and-branch-avenues/87-02-SUMMARY.md` when done.
</output>
