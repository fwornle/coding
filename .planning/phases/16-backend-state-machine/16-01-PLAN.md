---
phase: 16-backend-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/mcp-server-semantic-analysis/src/workflow-state-machine.ts
  - integrations/mcp-server-semantic-analysis/src/workflow-state-machine.test.ts
  - integrations/mcp-server-semantic-analysis/src/tools.ts
  - integrations/mcp-server-semantic-analysis/src/workflow-runner.ts
autonomous: true
requirements: [BE-02, BE-03, BE-04]

must_haves:
  truths:
    - "dispatch(start) while status is 'running' throws InvalidTransitionError"
    - "dispatch(cancel) from running state changes getState().status to 'cancelled'"
    - "getState() returns current WorkflowState without reading progress file from disk"
    - "Progress file JSON parses as WorkflowState after each dispatch (subscriber writes it)"
    - "tools.ts get_workflow_status calls getState(), not readFileSync on progress file"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/workflow-state-machine.ts"
      provides: "State machine singleton with getState, dispatch, subscribe, reset"
      exports: ["getState", "dispatch", "subscribe", "reset"]
    - path: "integrations/mcp-server-semantic-analysis/src/workflow-state-machine.test.ts"
      provides: "Tests for singleton lifecycle, subscriber notification, progress file writes"
      min_lines: 40
  key_links:
    - from: "src/workflow-state-machine.ts"
      to: "shared/workflow-types/transitions.ts"
      via: "import { transition } from shared types"
      pattern: "import.*transition.*from.*workflow-types"
    - from: "src/tools.ts"
      to: "src/workflow-state-machine.ts"
      via: "import { getState, dispatch } for status queries and cancel"
      pattern: "import.*getState.*dispatch.*from.*workflow-state-machine"
    - from: "src/workflow-state-machine.ts"
      to: ".data/workflow-progress.json"
      via: "subscriber writes WorkflowState on every transition"
      pattern: "writeFileSync.*workflow-progress"
---

<objective>
Create the state machine singleton and integrate it with health API and progress file writing.

Purpose: Establishes the central state management module that all other components will use. The singleton wraps the pure transition function from Phase 15 with in-memory state, subscriber notification, and single-workflow enforcement. The health API switches from reading the progress file to querying getState(). Cancel becomes a typed transition. The progress file becomes a subscriber-written artifact.

Output: workflow-state-machine.ts singleton, updated tools.ts (status + cancel), cleaned workflow-runner.ts progress writes
</objective>

<execution_context>
@/Users/Q284340/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Q284340/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-backend-state-machine/16-CONTEXT.md
@.planning/phases/15-type-definitions/15-01-SUMMARY.md
@.planning/phases/15-type-definitions/15-02-SUMMARY.md

<interfaces>
<!-- Phase 15 shared types that this plan imports and builds upon -->

From shared/workflow-types/index.ts:
```typescript
export { RunConfigSchema, RunProgressSchema, type RunConfig, type RunProgress } from './config';
export { WorkflowStateSchema, type WorkflowState, type IdleState, type RunningState,
         type PausedState, type CompletedState, type FailedState, type CancelledState } from './state';
export { WorkflowTransitionEventSchema, InvalidTransitionError, transition,
         type WorkflowTransitionEvent, type TransitionMap } from './transitions';
export { deriveStepStatuses, deriveSubstepStatuses } from './derived';
```

From shared/workflow-types/transitions.ts:
```typescript
// Pure function: applies event to state, returns new state, throws InvalidTransitionError
export function transition(state: WorkflowState, event: WorkflowTransitionEvent): WorkflowState;
```

From shared/workflow-types/state.ts:
```typescript
// 6 variants discriminated on 'status': idle | running | paused | completed | failed | cancelled
export type WorkflowState = IdleState | RunningState | PausedState | CompletedState | FailedState | CancelledState;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create state machine singleton with progress file subscriber</name>
  <files>
    integrations/mcp-server-semantic-analysis/src/workflow-state-machine.ts,
    integrations/mcp-server-semantic-analysis/src/workflow-state-machine.test.ts
  </files>
  <behavior>
    - Test 1: getState() returns idle initially
    - Test 2: dispatch(start event) moves state to running, getState() returns running state
    - Test 3: dispatch(start) while already running throws InvalidTransitionError
    - Test 4: subscribe() callback is invoked on every dispatch with (newState, event)
    - Test 5: Multiple subscribers all receive notifications
    - Test 6: reset() returns state to idle (for tests and between workflow runs)
    - Test 7: Progress file subscriber writes WorkflowState JSON to disk on each transition
    - Test 8: dispatch(cancel) from running state moves to cancelled
  </behavior>
  <action>
    Create `integrations/mcp-server-semantic-analysis/src/workflow-state-machine.ts` as a singleton module:

    Module-level state:
    - `let currentState: WorkflowState = { status: 'idle' }`
    - `const subscribers: Array<(state: WorkflowState, event: WorkflowTransitionEvent) => void> = []`

    Exported functions:
    - `getState(): WorkflowState` — returns currentState
    - `dispatch(event: WorkflowTransitionEvent): WorkflowState` — calls `transition(currentState, event)` from shared types, updates currentState, notifies all subscribers, returns new state
    - `subscribe(fn: (state: WorkflowState, event: WorkflowTransitionEvent) => void): () => void` — adds subscriber, returns unsubscribe function
    - `reset(): void` — sets currentState back to `{ status: 'idle' }`, used for testing and cleanup

    Progress file subscriber factory:
    - `createProgressFileSubscriber(progressFilePath: string): (state: WorkflowState, event: WorkflowTransitionEvent) => void`
    - On each call: `writeFileSync(progressFilePath, JSON.stringify(state, null, 2))`
    - Wrap in try/catch — log errors via `process.stderr.write()` but don't throw (subscriber errors must not break state machine)
    - Export this so workflow-runner can register it at workflow start

    Error handling in dispatch:
    - Catch subscriber errors individually — one failing subscriber doesn't block others
    - Log subscriber errors to stderr
    - Let InvalidTransitionError from transition() propagate to caller

    Import path: `import { transition } from '../../shared/workflow-types'` (adjust relative path based on actual project structure — shared/ is at repo root, src/ is inside integrations/mcp-server-semantic-analysis/)

    For tests: use the test file to verify singleton behavior. Import reset() to clean up between tests. Use a temp directory for progress file subscriber tests.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npx tsx --test src/workflow-state-machine.test.ts</automated>
  </verify>
  <done>Singleton module exports getState/dispatch/subscribe/reset/createProgressFileSubscriber. All 8 tests pass. dispatch() enforces valid transitions, notifies subscribers, and the progress file subscriber writes state to disk.</done>
</task>

<task type="auto">
  <name>Task 2: Update health API and workflow-runner to use state machine</name>
  <files>
    integrations/mcp-server-semantic-analysis/src/tools.ts,
    integrations/mcp-server-semantic-analysis/src/workflow-runner.ts
  </files>
  <action>
    **tools.ts changes:**

    1. Import `{ getState, dispatch }` from `./workflow-state-machine`

    2. Update `get_workflow_status` handler (around line 734): Instead of reading `.data/workflow-progress.json` and parsing it, call `getState()` and return the WorkflowState directly. If status is 'idle', return a message indicating no workflow is running. For backward compatibility with callers expecting the old format, map the WorkflowState fields to the expected response shape (status, currentStep, workflowId, etc.).

    3. Update cancel logic (around line 160-168 in `cleanupExistingWorkflows`): Instead of writing `{ status: 'cancelled' }` directly to the progress file, call `dispatch({ type: 'cancel', reason: 'Cancelled by new workflow start' })`. Wrap in try/catch — if state is already idle/completed/cancelled, the transition will throw InvalidTransitionError, which is fine (already not running).

    4. Update workflow start in `executeWorkflowTool` (around line 1043-1140): After resolving debug settings, call `dispatch({ type: 'start', config: { singleStepMode, mockLLM, llmMode, stepIntoSubsteps }, workflowName, firstStep })` to move state machine to running. Remove the manual `writeFileSync` of status: 'starting' / status: 'running' to progress file — the progress file subscriber handles this now.

    5. For single-step mode toggles (singleStepMode, mockLLM endpoints): These are RunConfig which is immutable per run. Keep the existing dashboard API endpoints for now (they write to progress file for the old system). Phase 19 migration will clean these up. Add a comment: `// TODO(phase-19): Remove — RunConfig is immutable on WorkflowState`.

    **workflow-runner.ts changes:**

    1. Import `{ dispatch, subscribe, createProgressFileSubscriber }` from `./workflow-state-machine`

    2. In the `runWaveAnalysis` function (line ~540-670): Register the progress file subscriber at workflow start: `const unsubscribe = subscribe(createProgressFileSubscriber(progressFile))`. Remove `writeProgress()` and `writeProgressPreservingDetails()` calls — the subscriber handles all progress file writes now.

    3. In the `executeWorkflow` function (line ~700-830): Same pattern — register subscriber, remove direct `writeProgress/writeProgressPreservingDetails` calls. Replace the error-path writes with `dispatch({ type: 'fail', error: errorMessage, step: currentStep })`. Replace success-path writes with `dispatch({ type: 'complete', summary: { ... } })`.

    4. Remove or deprecate the `writeProgress()` and `writeProgressPreservingDetails()` functions. Add `// @deprecated — remove after Phase 16 wave-controller migration (Plan 02)` comment if any call sites remain (wave-controller's updateProgress still calls through here temporarily until Plan 02).

    5. Remove heartbeat intervals — the subscriber writes on every transition, and the wave-controller's frequent transitions (~30 per run) serve as natural heartbeats. If no transition occurs for >60s, that's a real hang, not a heartbeat issue.

    6. Call `unsubscribe()` in the finally block to clean up the subscriber.

    **Do NOT touch wave-controller.ts** — that's Plan 02's scope. The old `updateProgress()` method on wave-controller still writes to the progress file directly for now. Plan 02 replaces those calls.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <done>tools.ts queries getState() for workflow status, uses dispatch() for cancel and workflow start. workflow-runner.ts registers progress file subscriber and uses dispatch() for fail/complete transitions. Old writeProgress/writeProgressPreservingDetails functions deprecated. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `cd integrations/mcp-server-semantic-analysis && npx tsx --test src/workflow-state-machine.test.ts` — all singleton tests pass
2. `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit --skipLibCheck` — no type errors
3. `grep -r "writeFileSync.*workflow-progress" src/tools.ts` — zero direct progress file writes from tools.ts (subscriber handles it)
4. `grep "getState\|dispatch" src/tools.ts` — confirms state machine usage in health API
</verification>

<success_criteria>
- State machine singleton exists with getState/dispatch/subscribe/reset exports
- Progress file is written exclusively by subscriber (not directly by tools.ts or workflow-runner.ts)
- Health API reads state via getState(), not by parsing progress file
- Cancel triggers a typed transition to cancelled state
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/16-backend-state-machine/16-01-SUMMARY.md`
</output>
