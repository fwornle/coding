---
phase: 20-foundation-opencode-om
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm-proxy/llm-proxy.mjs
  - config/llm-providers.yaml
autonomous: true
requirements: [OCOM-03]

must_haves:
  truths:
    - "LLM proxy server starts on port 8089 and responds to /health"
    - "POST /api/complete routes to claude-code or copilot SDK provider"
    - "Provider config YAML specifies model tiers for observation use case"
  artifacts:
    - path: "src/llm-proxy/llm-proxy.mjs"
      provides: "HTTP bridge server for subscription-based LLM access"
      min_lines: 300
    - path: "config/llm-providers.yaml"
      provides: "Provider configuration with model tiers and priority"
      contains: "providerPriority"
  key_links:
    - from: "src/llm-proxy/llm-proxy.mjs"
      to: "@anthropic-ai/claude-agent-sdk"
      via: "dynamic import"
      pattern: "import.*claude-agent-sdk"
    - from: "src/llm-proxy/llm-proxy.mjs"
      to: "@github/copilot-sdk"
      via: "dynamic import"
      pattern: "import.*copilot-sdk"
---

<objective>
Port the OKM LLM proxy bridge server into the coding project so Docker containers can access subscription-based LLM completions (Claude Max, Copilot Enterprise) without API keys.

Purpose: Establishes the LLM proxy infrastructure required by OCOM-03 (observer/reflector agents use coding LLM proxy). The proxy runs on the host and Docker containers connect via `LLM_CLI_PROXY_URL=http://host.docker.internal:8089`.
Output: Working `src/llm-proxy/llm-proxy.mjs` server and `config/llm-providers.yaml` configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation-opencode-om/20-CONTEXT.md
@.planning/phases/20-foundation-opencode-om/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port LLM proxy bridge server from OKM</name>
  <files>src/llm-proxy/llm-proxy.mjs</files>
  <read_first>
    /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docker/llm-proxy.mjs
    /Users/Q284340/Agentic/coding/config/agents/opencode.sh
  </read_first>
  <action>
    Copy `/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docker/llm-proxy.mjs` to `src/llm-proxy/llm-proxy.mjs` per D-03 (port into this repo, not shared package).

    Adaptations required (do NOT just blindly copy):
    1. Update the file header comment to reference the coding project, not OKM
    2. Add network-adaptive provider selection per D-02: read `INSIDE_CN` env var. If set to "true", prefer copilot provider; otherwise prefer claude-code. Add a `getPreferredProvider()` function that returns the provider name based on this env var.
    3. Add `LLM_CONFIG_PATH` env var support to load config from `config/llm-providers.yaml` (default: `../config/llm-providers.yaml` relative to script, or absolute path via env). Use `fs.readFileSync` + a simple YAML parser or import the `yaml` package.
    4. Keep the existing `/health` and `/api/complete` endpoints unchanged in their request/response format
    5. Ensure logging uses `process.stderr.write` (not console.log) per project conventions — the source already uses a `log()` wrapper, keep that pattern but route to stderr
    6. The script must remain standalone (no build step) — it runs directly with `node src/llm-proxy/llm-proxy.mjs`

    Also install the two SDK packages on the HOST (not in Docker):
    ```bash
    npm install @anthropic-ai/claude-agent-sdk@^0.2.86 @github/copilot-sdk@^0.2.0
    ```

    Do NOT port: circuit-breaker.ts, cache.ts, provider-registry.ts, llm-service.ts (per Research Pitfall 6 — only the bridge server is needed for Phase 20).
  </action>
  <verify>
    <automated>node -c src/llm-proxy/llm-proxy.mjs && grep -q "api/complete" src/llm-proxy/llm-proxy.mjs && grep -q "INSIDE_CN" src/llm-proxy/llm-proxy.mjs && grep -q "health" src/llm-proxy/llm-proxy.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/llm-proxy/llm-proxy.mjs` exists and passes `node -c` syntax check
    - File contains `/api/complete` endpoint handler
    - File contains `/health` endpoint handler
    - File contains `INSIDE_CN` env var check for network-adaptive routing (per D-02)
    - File contains `process.stderr.write` or equivalent (not bare console.log)
    - File header references coding project (not OKM)
    - `node_modules/@anthropic-ai/claude-agent-sdk` exists after npm install
    - `node_modules/@github/copilot-sdk` exists after npm install (or package.json has the dependency)
    - File is standalone (no TypeScript, no build step required)
  </acceptance_criteria>
  <done>LLM proxy bridge server ported with network-adaptive routing, SDK packages installed, server starts and responds to /health</done>
</task>

<task type="auto">
  <name>Task 2: Create LLM provider configuration YAML</name>
  <files>config/llm-providers.yaml</files>
  <read_first>
    /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/config/llm-providers.yaml
    /Users/Q284340/Agentic/coding/.planning/phases/20-foundation-opencode-om/20-RESEARCH.md
  </read_first>
  <action>
    Copy the OKM `config/llm-providers.yaml` to `config/llm-providers.yaml` and adapt for the coding project's observation use case per D-11 (fast/cheap models for background observation work).

    Changes from OKM source:
    1. Keep all provider definitions (claude-code, copilot, groq, gemini, anthropic, openai, github-models) — they're all potentially useful
    2. Update `providerPriority` to reflect observation use case:
       - `fast`: groq -> copilot -> claude-code -> github-models (groq first for cheap background work per D-11)
       - `standard`: copilot -> claude-code -> groq -> gemini (subscription providers before paid APIs)
       - `premium`: copilot -> claude-code -> anthropic -> gemini
    3. Add an `observation` task tier under `taskTiers`:
       ```yaml
       taskTiers:
         observation: fast
         reflection: fast
         entity_extraction: fast
         deduplication_matching: fast
       ```
    4. Keep `circuitBreaker` settings as-is (failureThreshold: 10, resetTimeoutMs: 60000)

    This file is used by the proxy server to determine which provider/model to use when no explicit model is specified in the request.
  </action>
  <verify>
    <automated>test -f config/llm-providers.yaml && grep -q "observation" config/llm-providers.yaml && grep -q "providerPriority" config/llm-providers.yaml && grep -q "groq" config/llm-providers.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `config/llm-providers.yaml` exists
    - Contains `providerPriority` section with fast/standard/premium tiers
    - `fast` tier lists `groq` before `copilot` (cheap models first per D-11)
    - Contains `taskTiers` with `observation: fast` and `reflection: fast`
    - Contains provider definitions for at least: claude-code, copilot, groq, gemini
    - Contains `circuitBreaker` section
  </acceptance_criteria>
  <done>Provider config YAML created with observation-optimized priority (fast/cheap models first per D-11)</done>
</task>

</tasks>

<verification>
1. `node -c src/llm-proxy/llm-proxy.mjs` passes (valid JavaScript)
2. `test -f config/llm-providers.yaml` passes
3. `grep -q "INSIDE_CN" src/llm-proxy/llm-proxy.mjs` confirms network-adaptive routing
4. `grep -q "observation" config/llm-providers.yaml` confirms observation task tier
</verification>

<success_criteria>
- LLM proxy bridge server exists at src/llm-proxy/llm-proxy.mjs, is syntactically valid, and can be started with `node src/llm-proxy/llm-proxy.mjs`
- Provider configuration at config/llm-providers.yaml has observation-optimized model tiers
- SDK packages (@anthropic-ai/claude-agent-sdk, @github/copilot-sdk) are in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-opencode-om/20-01-SUMMARY.md`
</output>
