---
phase: 20-foundation-opencode-om
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - uninstall.sh
  - .observations/config.json
  - .opencode/mastra.json
  - .gitignore
autonomous: true
requirements: [OCOM-01, OCOM-02, OCOM-04]

must_haves:
  truths:
    - "Running install.sh installs @mastra/opencode and creates .observations/ directory"
    - "Running uninstall.sh removes the mastra plugin and cleans up"
    - "Token budget config exists at .observations/config.json with per-agent limits"
    - "Plugin config at .opencode/mastra.json points storage to .observations/observations.db"
  artifacts:
    - path: "install.sh"
      provides: "install_mastra_opencode() function"
      contains: "install_mastra_opencode"
    - path: "uninstall.sh"
      provides: "Mastra plugin removal logic"
      contains: "mastra"
    - path: ".observations/config.json"
      provides: "Token budget configuration per agent"
      contains: "maxDailyTokens"
    - path: ".opencode/mastra.json"
      provides: "Mastra OpenCode plugin configuration"
      contains: "storagePath"
  key_links:
    - from: "install.sh"
      to: "@mastra/opencode"
      via: "npm install"
      pattern: "npm install.*@mastra/opencode"
    - from: ".opencode/mastra.json"
      to: ".observations/observations.db"
      via: "storagePath config"
      pattern: "storagePath.*observations"
---

<objective>
Add mastra/opencode plugin installation to install.sh/uninstall.sh, create LibSQL observation storage directory and config, and set up token budget controls.

Purpose: Addresses OCOM-01 (plugin installation lifecycle), OCOM-02 (LibSQL storage with configurable path), and OCOM-04 (token budget limits per agent).
Output: Updated install.sh and uninstall.sh, new .observations/config.json, new .opencode/mastra.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation-opencode-om/20-CONTEXT.md
@.planning/phases/20-foundation-opencode-om/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install_mastra_opencode() and uninstall logic</name>
  <files>install.sh, uninstall.sh, .gitignore</files>
  <read_first>
    /Users/Q284340/Agentic/coding/install.sh
    /Users/Q284340/Agentic/coding/uninstall.sh
    /Users/Q284340/Agentic/coding/.gitignore
  </read_first>
  <action>
    **install.sh changes:**
    1. Add `install_mastra_opencode()` function (place it after the existing `install_code_graph_rag()` function, around line ~1100). Follow the pattern from `install_semantic_analysis()` (line 678). The function must:
       - Check Node.js version >= 22 (per Research Pitfall 5): `local node_major=$(node -v | sed 's/v\([0-9]*\).*/\1/')` then check `$node_major -lt 22`
       - Run `npm install @mastra/opencode@latest` per D-07 (try npm first). On failure, print warning about monorepo build fallback but do NOT implement the fallback (the package is confirmed published on npm)
       - Create `.observations/` directory: `mkdir -p .observations`
       - Create default `.observations/config.json` if not exists (see Task 2 for exact content — use a heredoc)
       - Create `.opencode/` directory: `mkdir -p .opencode`
       - Create default `.opencode/mastra.json` if not exists (see Task 2 for exact content — use a heredoc)
       - Use project color variables (`$CYAN`, `$GREEN`, `$NC`) and helper functions (`info`, `success`, `warning`) matching existing install.sh style
    2. Call `install_mastra_opencode` from the main installation flow (find where `install_semantic_analysis` and `install_code_graph_rag` are called, around line ~2860, add the call after them)

    **uninstall.sh changes:**
    1. Add a section that removes mastra-related components (after existing removal sections):
       - `npm uninstall @mastra/opencode 2>/dev/null || true`
       - Remove `.opencode/mastra.json` (but preserve `.opencode/` dir if it has other files)
       - Print info about `.observations/` being preserved (same pattern as knowledge data preservation): "Observation data (.observations/) preserved"
       - Do NOT delete `.observations/` — user data, same policy as `.data/knowledge-graph/`

    **.gitignore changes:**
    1. Add `.observations/observations.db` (the LibSQL database file — generated, not tracked)
    2. Add `.observations/test.db` (test database from smoke tests)
    3. Do NOT gitignore `.observations/config.json` — this IS tracked (project config)
    4. Do NOT gitignore `.opencode/mastra.json` — this IS tracked (plugin config)
  </action>
  <verify>
    <automated>grep -q "install_mastra_opencode" install.sh && grep -q "mastra" uninstall.sh && grep -q "observations" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `install.sh` contains function `install_mastra_opencode()` with `npm install @mastra/opencode`
    - `install.sh` checks Node.js version >= 22 inside the function
    - `install.sh` creates `.observations/` and `.opencode/` directories
    - `install.sh` creates default config files if they don't exist
    - `install.sh` calls `install_mastra_opencode` in the main flow (grep confirms the call outside the function definition)
    - `uninstall.sh` contains `npm uninstall @mastra/opencode`
    - `uninstall.sh` removes `.opencode/mastra.json` but preserves `.observations/` data
    - `.gitignore` contains `.observations/observations.db`
    - `.gitignore` contains `.observations/test.db`
    - `.gitignore` does NOT contain `.observations/config.json`
  </acceptance_criteria>
  <done>install.sh installs mastra plugin with Node.js version check, uninstall.sh cleans up plugin without deleting observation data, .gitignore excludes generated DB files</done>
</task>

<task type="auto">
  <name>Task 2: Create observation storage config and plugin config files</name>
  <files>.observations/config.json, .opencode/mastra.json</files>
  <read_first>
    /Users/Q284340/Agentic/coding/.planning/phases/20-foundation-opencode-om/20-RESEARCH.md
  </read_first>
  <action>
    Create two configuration files with the exact content specified below.

    **`.observations/config.json`** — Token budget configuration per D-10, D-11, D-12:
    ```json
    {
      "version": 1,
      "defaults": {
        "model": "google/gemini-2.5-flash",
        "observation": {
          "messageTokens": 20000,
          "bufferTokens": 0.2
        },
        "reflection": {
          "observationTokens": 90000,
          "bufferTokens": 0.2
        }
      },
      "agents": {
        "opencode": {
          "model": "google/gemini-2.5-flash",
          "maxDailyTokens": 500000
        },
        "mastra": {
          "model": "google/gemini-2.5-flash",
          "maxDailyTokens": 500000
        },
        "claude": {
          "model": "groq/llama-3.3-70b-versatile",
          "maxDailyTokens": 1000000
        }
      }
    }
    ```

    **`.opencode/mastra.json`** — Plugin config per D-04 (storagePath) and Research Pattern 3:
    ```json
    {
      "model": "google/gemini-2.5-flash",
      "storagePath": ".observations/observations.db",
      "observation": {
        "messageTokens": 20000
      },
      "reflection": {
        "observationTokens": 90000
      }
    }
    ```

    Key decisions reflected:
    - D-04: `storagePath` points to `.observations/observations.db` (NOT the plugin default of `.opencode/memory/observations.db`)
    - D-05: Single shared DB path — all agents configured to use the same `.observations/observations.db`
    - D-11: Default model is `google/gemini-2.5-flash` (fast/cheap for background work)
    - D-12: Per-agent budgets with `maxDailyTokens` limits
  </action>
  <verify>
    <automated>test -f .observations/config.json && test -f .opencode/mastra.json && node -e "const c=JSON.parse(require('fs').readFileSync('.observations/config.json','utf8')); if(c.agents.opencode.maxDailyTokens!==500000) process.exit(1); console.log('ok')" && node -e "const c=JSON.parse(require('fs').readFileSync('.opencode/mastra.json','utf8')); if(!c.storagePath.includes('observations')) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.observations/config.json` exists and is valid JSON
    - `.observations/config.json` has `version: 1`
    - `.observations/config.json` has `defaults.model` set to `"google/gemini-2.5-flash"` (per D-11)
    - `.observations/config.json` has `agents.opencode.maxDailyTokens` set to `500000`
    - `.observations/config.json` has `agents.claude.model` set to `"groq/llama-3.3-70b-versatile"`
    - `.opencode/mastra.json` exists and is valid JSON
    - `.opencode/mastra.json` has `storagePath` set to `".observations/observations.db"` (per D-04)
    - `.opencode/mastra.json` has `model` set to `"google/gemini-2.5-flash"`
    - `.opencode/mastra.json` has `observation.messageTokens` set to `20000`
    - `.opencode/mastra.json` has `reflection.observationTokens` set to `90000`
  </acceptance_criteria>
  <done>Both config files created with correct storage paths (D-04), fast/cheap model defaults (D-11), and per-agent token budgets (D-12)</done>
</task>

<task type="auto">
  <name>Task 3: Add mastra plugin smoke test to test-coding.sh</name>
  <files>scripts/test-coding.sh</files>
  <read_first>
    /Users/Q284340/Agentic/coding/scripts/test-coding.sh
  </read_first>
  <action>
    Add a `test_mastra_opencode()` function to `scripts/test-coding.sh` following the pattern of `test_enhanced_lsl()` (line 2453). Place it after the last test function.

    The function must run 5 tests per D-09 (full smoke test, not just import validation):

    1. **Package installed**: `node -e "require.resolve('@mastra/opencode')"` — verifies npm package is available
    2. **Observations directory**: Check `[[ -d "$CODING_REPO/.observations" ]]`
    3. **Configuration files**: Check both `.observations/config.json` and `.opencode/mastra.json` exist
    4. **Plugin exports**: `timeout 10s node -e "import('@mastra/opencode').then(m => { if (m.MastraPlugin) console.log('ok'); else process.exit(1); }).catch(() => process.exit(1));"` — verifies MastraPlugin is exported
    5. **LibSQL DB creation**: Create a test DB at `.observations/test.db`, verify it initializes, then remove it:
       ```bash
       timeout 10s node -e "
         import('@mastra/libsql').then(async ({ LibSQLStore }) => {
           const store = new LibSQLStore({ url: 'file:$CODING_REPO/.observations/test.db' });
           await store.init();
           console.log('ok');
         }).catch(e => { console.error(e); process.exit(1); });
       "
       rm -f "$CODING_REPO/.observations/test.db"
       ```

    Use existing helper functions: `print_section`, `print_test`, `print_pass`, `print_fail`, `print_summary`. Use `tests_passed` / `tests_total` counter pattern.

    Also add a call to `test_mastra_opencode` in the main test execution section (find where other test functions are called, add it there).
  </action>
  <verify>
    <automated>grep -q "test_mastra_opencode" scripts/test-coding.sh && grep -q "MastraPlugin" scripts/test-coding.sh && grep -q "LibSQLStore" scripts/test-coding.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-coding.sh` contains function `test_mastra_opencode()`
    - Function tests 5 things: package installed, .observations dir, config files, MastraPlugin export, LibSQL DB creation
    - Function uses `print_section`, `print_test`, `print_pass`, `print_fail`, `print_summary` helpers
    - Function cleans up test.db after the LibSQL test
    - `test_mastra_opencode` is called in the main test execution flow
    - grep for "tests_total=5" inside the function confirms 5 tests
  </acceptance_criteria>
  <done>Smoke test validates full mastra plugin installation: package, directories, config, exports, and DB creation (per D-09)</done>
</task>

</tasks>

<verification>
1. `grep -c "install_mastra_opencode" install.sh` returns >= 2 (definition + call)
2. `grep -q "mastra" uninstall.sh` passes
3. `node -e "JSON.parse(require('fs').readFileSync('.observations/config.json','utf8'))"` passes (valid JSON)
4. `node -e "JSON.parse(require('fs').readFileSync('.opencode/mastra.json','utf8'))"` passes (valid JSON)
5. `grep -q "test_mastra_opencode" scripts/test-coding.sh` passes
6. `grep -q "observations" .gitignore` passes
</verification>

<success_criteria>
- install.sh has install_mastra_opencode() that installs @mastra/opencode, creates directories, creates default configs
- uninstall.sh removes the plugin but preserves observation data
- .observations/config.json has per-agent token budgets with fast/cheap model defaults
- .opencode/mastra.json points storagePath to .observations/observations.db
- test-coding.sh has 5-test smoke test for the complete mastra plugin installation
- .gitignore excludes generated DB files but tracks config files
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-opencode-om/20-02-SUMMARY.md`
</output>
