---
phase: 20-foundation-opencode-om
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - uninstall.sh
  - scripts/test-coding.sh
autonomous: true
requirements: [OCOM-01, OCOM-02]

must_haves:
  truths:
    - "Running install.sh installs @mastra/opencode npm package"
    - "Running install.sh creates .observations/ directory and default configs"
    - "Running uninstall.sh removes the mastra plugin and cleans up"
    - "Running test-coding.sh validates plugin installation, config files, and LibSQL DB creation"
    - "Observation storage uses LibSQL at .observations/observations.db"
  artifacts:
    - path: "install.sh"
      provides: "install_mastra_opencode() function"
      contains: "install_mastra_opencode"
    - path: "uninstall.sh"
      provides: "Mastra plugin cleanup section"
      contains: "mastra"
    - path: "scripts/test-coding.sh"
      provides: "test_mastra_opencode() smoke test function"
      contains: "test_mastra_opencode"
  key_links:
    - from: "install.sh"
      to: "@mastra/opencode"
      via: "npm install"
      pattern: "npm install.*@mastra/opencode"
    - from: "scripts/test-coding.sh"
      to: ".observations/"
      via: "directory and DB existence checks"
      pattern: "observations"
    - from: "install.sh"
      to: ".observations/config.json"
      via: "default config creation"
      pattern: "config.json"
---

<objective>
Add mastra OpenCode plugin installation, uninstallation, and smoke test functions to the project's lifecycle scripts.

Purpose: Implements OCOM-01 (plugin installation via install.sh with uninstall and test validation) and OCOM-02 (LibSQL storage setup at .observations/ with configurable path). After this plan, running `install.sh` sets up the complete mastra observation infrastructure, and `test-coding.sh` validates it works end-to-end.

Output: Updated install.sh, uninstall.sh, and scripts/test-coding.sh with mastra-specific functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation-opencode-om/20-CONTEXT.md
@.planning/phases/20-foundation-opencode-om/20-RESEARCH.md

@install.sh
@uninstall.sh
@scripts/test-coding.sh
@config/agents/opencode.sh

<interfaces>
<!-- Existing patterns to follow in install.sh -->
<!-- From install.sh line 678: install_semantic_analysis() pattern -->
<!-- Function signature: install_<name>() with echo/success/warning helpers -->
<!-- Uses: CODING_REPO, CYAN, NC, success(), warning() shell functions -->

<!-- From scripts/test-coding.sh line 2453: test_enhanced_lsl() pattern -->
<!-- Uses: print_section, print_test, print_pass, print_fail, print_summary -->
<!-- Format: tests_passed counter, tests_total counter, increment on pass -->

<!-- From uninstall.sh: section-based removal with echo headers -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install_mastra_opencode() to install.sh</name>
  <files>install.sh</files>
  <read_first>
    /Users/Q284340/Agentic/coding/install.sh
  </read_first>
  <action>
    Add `install_mastra_opencode()` function to install.sh following the `install_semantic_analysis()` pattern (line 678). Place it after the existing install functions.

    The function must (per D-07, D-04, D-10):

    1. Check Node.js >= 22.13.0 (per Pitfall 5 from RESEARCH.md). Extract major version from `node -v`, fail with warning if < 22.

    2. Install @mastra/opencode via npm:
       ```bash
       npm install @mastra/opencode@latest
       ```
       If npm install fails, log a warning about monorepo build fallback (per D-07) but do NOT implement the monorepo fallback -- just return 1.

    3. Create `.observations/` directory: `mkdir -p .observations`

    4. Create `.observations/config.json` if not exists (per D-10, D-11, D-12):
       Default config with version 1, google/gemini-2.5-flash model, messageTokens 20000, observationTokens 90000, per-agent budgets (opencode 500K, mastra 500K, claude 1M daily tokens).

    5. Create `.opencode/` directory and `.opencode/mastra.json` if not exists:
       Plugin config with model, storagePath pointing to `.observations/observations.db` (per D-04), observation/reflection token thresholds.

    6. Add the function call to the main installation flow (find where other install_* functions are called and add `install_mastra_opencode` in the appropriate section).

    Follow the existing coding style: use `echo -e` with color codes, `success()` and `warning()` helpers, `cd "$CODING_REPO"` at start and end.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && grep -q "install_mastra_opencode" install.sh && bash -n install.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "install_mastra_opencode" install.sh
    - grep -q "@mastra/opencode" install.sh
    - grep -q "observations" install.sh
    - grep -q "mastra.json" install.sh
    - grep -q "node_major" install.sh
    - bash -n install.sh (syntax valid)
  </acceptance_criteria>
  <done>
    install.sh contains install_mastra_opencode() that installs @mastra/opencode via npm, creates .observations/ directory with default config.json, creates .opencode/mastra.json with storage path override, validates Node.js version. Function is called from main install flow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add uninstall section and test_mastra_opencode() smoke test</name>
  <files>uninstall.sh, scripts/test-coding.sh</files>
  <read_first>
    /Users/Q284340/Agentic/coding/uninstall.sh
    /Users/Q284340/Agentic/coding/scripts/test-coding.sh
  </read_first>
  <action>
    **uninstall.sh** (per D-08):
    Add a "Mastra OpenCode" section that:
    1. Removes @mastra/opencode from node_modules: `npm uninstall @mastra/opencode 2>/dev/null`
    2. Removes `.opencode/mastra.json` (plugin config)
    3. Does NOT remove `.observations/` (preserve user data, same as .data/ preservation pattern)
    4. Prints what was removed and what was preserved

    Follow the existing section pattern in uninstall.sh (echo headers with color, conditional removal).

    **scripts/test-coding.sh** (per D-09):
    Add `test_mastra_opencode()` function following the `test_enhanced_lsl()` pattern (line 2453). The smoke test must run 5 checks:

    1. **Package installed**: `node -e "require.resolve('@mastra/opencode')"` succeeds
    2. **Observations directory**: `.observations/` directory exists
    3. **Config files**: `.observations/config.json` AND `.opencode/mastra.json` both exist
    4. **Plugin exports**: `import('@mastra/opencode').then(m => m.MastraPlugin ? ... : fail)` with 10s timeout
    5. **LibSQL DB creation**: Create a temp DB via `LibSQLStore` init, verify it works, clean up. Use `import { LibSQLStore } from '@mastra/libsql'` -- this comes as a transitive dep of @mastra/opencode (do NOT install separately per RESEARCH anti-pattern).

    Use `print_section`, `print_test`, `print_pass`, `print_fail`, `print_summary` helpers. Counter: tests_passed / tests_total.

    Add call to `test_mastra_opencode` in the main test runner section of test-coding.sh.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && grep -q "test_mastra_opencode" scripts/test-coding.sh && bash -n scripts/test-coding.sh && grep -q "mastra" uninstall.sh && bash -n uninstall.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "test_mastra_opencode" scripts/test-coding.sh
    - grep -q "MastraPlugin" scripts/test-coding.sh
    - grep -q "LibSQLStore\|libsql" scripts/test-coding.sh
    - grep -q "observations" scripts/test-coding.sh
    - grep -q "@mastra/opencode" uninstall.sh
    - grep -q "mastra.json" uninstall.sh
    - bash -n scripts/test-coding.sh (syntax valid)
    - bash -n uninstall.sh (syntax valid)
  </acceptance_criteria>
  <done>
    uninstall.sh removes @mastra/opencode package and plugin config while preserving observation data. test-coding.sh has test_mastra_opencode() running 5 smoke tests: package installed, directory exists, configs present, plugin exports MastraPlugin, LibSQL DB creation works. Both scripts pass syntax validation.
  </done>
</task>

</tasks>

<verification>
1. `bash -n install.sh` passes (no syntax errors)
2. `bash -n uninstall.sh` passes
3. `bash -n scripts/test-coding.sh` passes
4. `grep -c "install_mastra_opencode\|test_mastra_opencode" install.sh scripts/test-coding.sh` shows functions exist in both files
5. install.sh creates .observations/ and config files when run
6. test-coding.sh smoke test validates all 5 checks when @mastra/opencode is installed
</verification>

<success_criteria>
- install.sh has install_mastra_opencode() that installs npm package, creates directories and config files
- uninstall.sh has mastra cleanup section that removes package and plugin config but preserves observation data
- test-coding.sh has test_mastra_opencode() with 5 smoke tests covering package, directory, configs, exports, LibSQL
- All three scripts pass bash -n syntax validation
- Functions follow existing patterns (install_semantic_analysis, test_enhanced_lsl, uninstall sections)
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-opencode-om/20-02-SUMMARY.md`
</output>
