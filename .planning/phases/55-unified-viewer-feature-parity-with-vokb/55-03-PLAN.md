---
phase: 55-unified-viewer-feature-parity-with-vokb
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/unified-viewer/src/graph/vokb-palette.ts
  - integrations/unified-viewer/src/graph/vokb-palette.test.ts
  - integrations/unified-viewer/src/lib-domain/evidence-types.ts
  - integrations/unified-viewer/src/lib-domain/evidence-types.test.ts
autonomous: true
requirements: [UI-02]
requirements_addressed: [UI-02]
must_haves:
  truths:
    - "`vokb-palette.ts` exports `FAILURE_MODEL_CLASSES`, `BUSINESS_CLASSES`, `nodeFill`, `nodeStroke`, `nodeStrokeWidth`, `nodeStrokeDasharray`, `EDGE_STYLES`, `LAYER_BADGE_CLASS`, `CONFIDENCE_COLOR`, `RUN_COLORS` — single source of truth for VOKB semantic palette (UI-SPEC §4)"
    - "`evidence-types.ts` exports `EVIDENCE_TYPE_ICONS`, `EVIDENCE_TYPE_LABELS`, `evidenceAgeBadge` — shared between EntityDetailPanel + IssueTriageView (no duplication, per D-55-02a verbatim VOKB port)"
    - "Snapshot test: `nodeFill('evidence', 'FailurePattern')` returns `'#93c5fd'` matching VOKB GraphVisualization.tsx:79 exactly"
    - "Snapshot test: `EVIDENCE_TYPE_ICONS.argo_workflow === '⚙'` matching VOKB NodeDetails.tsx:243"
    - "Logger discipline: ZERO raw `console.*` calls in either module (per `feedback_logger_class.md`)"
  artifacts:
    - path: "integrations/unified-viewer/src/graph/vokb-palette.ts"
      provides: "VOKB semantic palette constants for renderer + Legend + EntityDetail badges"
      contains: "FAILURE_MODEL_CLASSES"
    - path: "integrations/unified-viewer/src/lib-domain/evidence-types.ts"
      provides: "Evidence type icons + labels + age-badge helper"
      contains: "EVIDENCE_TYPE_ICONS"
  key_links:
    - from: "graph/vokb-palette.ts"
      to: "_work/.../viewer/src/components/KnowledgeGraph/GraphVisualization.tsx:31-176"
      via: "verbatim port"
      pattern: "FAILURE_MODEL_CLASSES"
---

<objective>
Land two shared constant modules that downstream plans (55-05 renderer, 55-07 LegendPanel + StatsBar, 55-09 EntityDetailPanel, 55-10 IssueTriageView + TrendingPanel) consume. Both modules are verbatim ports from VOKB source per D-55-02a exact-parity rule.

Purpose: Without these as the single-source-of-truth, the renderer / Legend / EntityDetailPanel / IssueTriageView each invent their own color/icon constants and drift. VOKB has the same `EVIDENCE_TYPE_ICONS` duplicated in `NodeDetails.tsx:243` AND `IssueTriage.tsx:21` — Phase 55 extracts once.

Output: Two new files with snapshot tests asserting verbatim parity with the VOKB source line numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-CONTEXT.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/GraphVisualization.tsx
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/NodeDetails.tsx
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/IssueTriage.tsx
@integrations/unified-viewer/src/lib/logging/Logger.ts

<interfaces>
Required exports — see 55-PATTERNS.md § `graph/vokb-palette.ts (NEW)` and § `lib-domain/evidence-types.ts (NEW)` for full signatures.

`vokb-palette.ts` minimum surface:
- `FAILURE_MODEL_CLASSES: ReadonlySet<string>` (5 entries — VOKB GraphVisualization.tsx:60-62)
- `BUSINESS_CLASSES: ReadonlySet<string>` (5 entries — :66-68)
- `nodeFill(layer, ontologyClass?): string` (:70-85)
- `nodeStroke(layer, ontologyClass?, sourceAuthority?): string` (:87-110)
- `nodeStrokeWidth(sourceAuthority?): number`
- `nodeStrokeDasharray(sourceAuthority?): string`
- `EDGE_STYLES: Record<string, {color: string; dasharray: string}>` (:135-173)
- `LAYER_BADGE_CLASS: Record<string, string>` (NodeDetails.tsx:71-74)
- `CONFIDENCE_COLOR: Record<'High'|'Moderate'|'Low', {class: string; dot: string}>` (NodeDetails.tsx:219-228)
- `RUN_COLORS: Array<{bg: string; border: string; dot: string}>` (NodeDetails.tsx:232-241)

`evidence-types.ts` minimum surface:
- `type EvidenceLinkType` (14 literals — NodeDetails.tsx:243)
- `EVIDENCE_TYPE_ICONS: Record<EvidenceLinkType, string>` (:243-258)
- `EVIDENCE_TYPE_LABELS: Record<EvidenceLinkType, string>` (:260-278)
- `evidenceAgeBadge(addedAt: string): { label: string; className: string } | null` (:280-296)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port vokb-palette.ts (verbatim from VOKB GraphVisualization.tsx + NodeDetails.tsx)</name>
  <read_first>
    - _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/GraphVisualization.tsx (lines 31-176 — semantic palette block)
    - _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/NodeDetails.tsx (lines 71-74, 94-128, 219-228, 232-241, 649-672 — badge classes + RUN_COLORS + RELATION_COLORS)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ vokb-palette.ts — direct extract block)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§4 — semantic palette table; the table values MUST equal the VOKB source)
  </read_first>
  <behavior>
    - Test: `FAILURE_MODEL_CLASSES.has('FailurePattern')` is true; size === 5
    - Test: `BUSINESS_CLASSES.has('Decision')` is true; size === 5
    - Test: `nodeFill('evidence', 'FailurePattern')` === `'#93c5fd'` (light-blue failure-mode evidence; VOKB :79)
    - Test: `nodeFill('evidence', 'Component')` === `'#3b82f6'` (standard evidence; VOKB :79)
    - Test: `nodeFill('pattern', 'FailurePattern')` === `'#fdba74'` (VOKB :81)
    - Test: `nodeFill('evidence', 'Decision')` === `'#10b981'` (business override; VOKB :72-75)
    - Test: `nodeFill('unknown', undefined)` === `'#6b7280'` (default)
    - Test: `EDGE_STYLES['CAUSED_BY'].color` matches VOKB causal-red family (`'#ef4444'` or close — read VOKB :135-173 literal)
    - Test: `EDGE_STYLES['CORRELATED_WITH'].dasharray === '5,5'` (VOKB dashed convention)
    - Test: `LAYER_BADGE_CLASS.evidence === 'bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-200'`
    - Test: `RUN_COLORS.length === 8`
    - Test: `CONFIDENCE_COLOR.High.dot === 'bg-green-500'`
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/graph/vokb-palette.ts` as a verbatim port of the VOKB semantic-palette block. Copy `FAILURE_MODEL_CLASSES`, `BUSINESS_CLASSES`, `nodeFill`, `nodeStroke`, `nodeStrokeWidth`, `nodeStrokeDasharray`, the full `EDGE_STYLES` map, `LAYER_BADGE_CLASS`, `CONFIDENCE_COLOR`, `RUN_COLORS` directly from the VOKB source files at the line ranges named in `<read_first>` and `<interfaces>`. Inline hex values and dasharray strings VERBATIM — DO NOT round, DO NOT translate to Tailwind classes (the renderer needs hex). The Tailwind-class side (`LAYER_BADGE_CLASS`, `CONFIDENCE_COLOR`, `RUN_COLORS`) preserves the dark-mode `dark:` modifier strings verbatim.
    Add a header comment at the top of the file noting `// PORT-SPEC: VOKB GraphVisualization.tsx:31-176 + NodeDetails.tsx:{71-74,94-128,219-228,232-241,649-672}` per 55-PATTERNS.md.
    Create `vokb-palette.test.ts` with the cases in `<behavior>`. Snapshot the file content using vitest's `toMatchInlineSnapshot` for at least one of the larger maps (EDGE_STYLES) so future drift fails loudly.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/graph/vokb-palette.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All vokb-palette.test.ts cases pass. `grep -c "FAILURE_MODEL_CLASSES\|BUSINESS_CLASSES\|EDGE_STYLES\|LAYER_BADGE_CLASS\|CONFIDENCE_COLOR\|RUN_COLORS\|nodeFill" integrations/unified-viewer/src/graph/vokb-palette.ts` returns ≥7. Side-by-side hex-diff against VOKB GraphVisualization.tsx:31-176 shows zero drift.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port evidence-types.ts (verbatim from VOKB NodeDetails.tsx; replaces VOKB IssueTriage.tsx duplicate)</name>
  <read_first>
    - _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/NodeDetails.tsx (lines 243-296 — EVIDENCE_TYPE_ICONS / EVIDENCE_TYPE_LABELS / evidenceAgeBadge)
    - _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/IssueTriage.tsx (lines 21-53 — the duplicate)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ evidence-types.ts (NEW) — direct extract)
    - integrations/unified-viewer/src/lib-domain/ (verify sibling files for module conventions)
  </read_first>
  <behavior>
    - Test: `EVIDENCE_TYPE_ICONS.argo_workflow === '⚙'`
    - Test: `EVIDENCE_TYPE_ICONS.github === '⟨⟩'`
    - Test: `EVIDENCE_TYPE_LABELS.cloudwatch === 'CloudWatch'`
    - Test: `EVIDENCE_TYPE_LABELS.other === 'Other'` (Object.keys length === 14)
    - Test: `EvidenceLinkType` union accepts all 14 keys at compile time (negative `@ts-expect-error` on `'unknown_link'`)
    - Test: `evidenceAgeBadge(180-days-ago-ISO)` returns `null` (boundary just under 180d)
    - Test: `evidenceAgeBadge(181-days-ago-ISO)` returns `{label: '181d', className contains 'bg-red-100'}` (>180d red)
    - Test: `evidenceAgeBadge(91-days-ago-ISO)` returns `{label: '91d', className contains 'bg-amber-100'}` (>90d amber)
    - Test: `evidenceAgeBadge(now)` returns `null`
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/lib-domain/evidence-types.ts` as a verbatim port of NodeDetails.tsx:243-296. Inline the 14-key `EvidenceLinkType` union, both `Record<EvidenceLinkType, string>` maps (icons + labels), and the `evidenceAgeBadge` helper function. Preserve the date math exactly: `ageDays = (Date.now() - new Date(addedAt).getTime()) / 86_400_000`; boundaries at `> 180` and `> 90` (strict inequality, per NodeDetails.tsx :280-296).
    Header comment: `// PORT-SPEC: VOKB NodeDetails.tsx:243-296 (and IssueTriage.tsx:21-53 duplicate — extracted to shared module per UI-SPEC §7 row 10)`.
    Create `evidence-types.test.ts` with the cases in `<behavior>`.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/lib-domain/evidence-types.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All evidence-types.test.ts cases pass. `grep -c "EVIDENCE_TYPE_ICONS\|EVIDENCE_TYPE_LABELS\|evidenceAgeBadge\|EvidenceLinkType" integrations/unified-viewer/src/lib-domain/evidence-types.ts` returns ≥4. ZERO raw `console.*` (`grep -nE "console\.(log|warn|error|info|debug)" integrations/unified-viewer/src/lib-domain/evidence-types.ts` returns 0).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| port consumer (`EntityDetailPanel`, renderer, Legend) → shared module | constants are read-only; no input from user/network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03-01 | Tampering | drift between Phase 55 port and VOKB source over time | mitigate | snapshot test asserts at least one full map literally matches VOKB hex values; D-55-02a port-spec source citations in header comments |
| T-55-03-02 | XSS | icon strings rendered in panel | mitigate | React text-node rendering escapes by default; no `dangerouslySetInnerHTML` |

Package legitimacy: no new packages introduced.
</threat_model>

<verification>
- `cd integrations/unified-viewer && npx vitest run src/graph/vokb-palette.test.ts src/lib-domain/evidence-types.test.ts` → all green
- `cd integrations/unified-viewer && npx tsc --noEmit` → exit 0
- `grep -rnE "console\.(log|warn|error|info|debug)" integrations/unified-viewer/src/graph/vokb-palette.ts integrations/unified-viewer/src/lib-domain/evidence-types.ts` → 0 matches
- Side-by-side hex diff: `nodeFill('evidence','Component')` in port === VOKB GraphVisualization.tsx line :79 value
</verification>

<success_criteria>
- vokb-palette.ts exports ≥7 named constants/functions per `<interfaces>`
- evidence-types.ts exports 3 named constants (+ type) per `<interfaces>`
- Snapshot tests guard against drift from VOKB source
- ZERO raw `console.*` in either module
- Phase 45 vitest suite still green
</success_criteria>

<output>
Create `.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-03-SUMMARY.md` when done.
</output>
