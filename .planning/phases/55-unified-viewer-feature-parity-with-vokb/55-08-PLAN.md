---
phase: 55-unified-viewer-feature-parity-with-vokb
plan: 08
type: execute
wave: 3
depends_on: [55-04]
files_modified:
  - integrations/unified-viewer/src/panels/filters/LayerFilter.tsx
  - integrations/unified-viewer/src/panels/filters/LayerFilter.test.tsx
  - integrations/unified-viewer/src/panels/filters/DomainFilter.tsx
  - integrations/unified-viewer/src/panels/filters/DomainFilter.test.tsx
  - integrations/unified-viewer/src/panels/filters/OntologyFilter.tsx
  - integrations/unified-viewer/src/panels/filters/OntologyFilter.test.tsx
  - integrations/unified-viewer/src/panels/filters/GraphToggles.tsx
  - integrations/unified-viewer/src/panels/filters/GraphToggles.test.tsx
  - integrations/unified-viewer/src/panels/FilterRail.tsx
  - integrations/unified-viewer/src/panels/FilterRail.test.tsx
autonomous: true
requirements: [UI-02]
requirements_addressed: [UI-02]
must_haves:
  truths:
    - "FilterRail mounts Layer / Domain / Ontology (grouped tree) / GraphToggles in the order specified by UI-SPEC §6 — REPLACES Phase 45's flat ClassList"
    - "FilterRail OWNS the lazy mount lines for TrendingPanel (always) and HierarchyNavigator (coding only). Downstream plans 55-10 / 55-11 ONLY create the lazy-imported component files — they DO NOT edit FilterRail.tsx."
    - "LayerFilter exposes Evidence + Pattern with per-layer count badges (VOKB convention; empty selection = all visible)"
    - "DomainFilter on coding tab renders italic 'Domain filter not applicable for this system' if no entity has a `domain` field (graceful degradation per UI-SPEC §7 row 3)"
    - "OntologyFilter accepts a `groupingSchema` prop; VOKB_SCHEMA (Upper/Lower) on okb, CODING_SCHEMA (Hierarchy/Typed Views) on coding"
    - "OntologyFilter per-class count badge uses `text-[10px]`; group `all` / `none` toggles use `text-[9px]` — micro-type exceptions per UI-SPEC §3 preserved verbatim from VOKB"
    - "GraphToggles renders 4 toggles + nested 'Labels' sub-toggle when 'Show All Relations' is on; italic hint text uses `text-[10px] ml-6 leading-tight` (verbatim VOKB)"
    - "Filter composition rules per UI-SPEC §10: cross-dimension AND, within-dimension multi-select OR, empty set = 'all visible', `Hide Documentation` + `Merged Only` applied post-filter"
    - "Logger discipline: ZERO raw `console.*` in any new filter file; usage of `Logger.info(Logger.Categories.FILTERS, ...)` per VOKB analog (LayerFilter.tsx:155)"
  artifacts:
    - path: "integrations/unified-viewer/src/panels/filters/LayerFilter.tsx"
      provides: "Surface #2 (UI-SPEC §7) — VOKB-shape Layer filter"
      contains: "selectedLayers"
    - path: "integrations/unified-viewer/src/panels/filters/OntologyFilter.tsx"
      provides: "Surface #4 — grouped Ontology Class tree with per-class counts"
      contains: "VOKB_SCHEMA"
  key_links:
    - from: "FilterRail.tsx"
      to: "panels/filters/{Layer,Domain,Ontology}Filter.tsx + GraphToggles.tsx"
      via: "child component composition"
      pattern: "<LayerFilter"
    - from: "FilterRail.tsx"
      to: "panels/TrendingPanel.tsx (55-10) + panels/coding/HierarchyNavigator.tsx (55-11)"
      via: "lazy(() => import(...)) — file owned here, populated by downstream plans"
      pattern: "lazy.*TrendingPanel"
---

<objective>
Land 4 ported VOKB filter components (Layer, Domain, OntologyFilter grouped tree, GraphToggles) and rewire FilterRail to mount them in the order specified by UI-SPEC §6, replacing Phase 45's flat ClassList. This is the SC-5 "Filter parity with VOKB" deliverable.

This plan ALSO claims sole ownership of `FilterRail.tsx` for the remainder of Phase 55. It mounts the lazy-loaded TrendingPanel + HierarchyNavigator slots NOW (closes B-2 + B-3 from the plan-checker), so the downstream plans 55-10 and 55-11 only need to create the component files. No other plan edits FilterRail.tsx.

Purpose: Phase 45 ships a flat class checklist; VOKB has Layer / Domain / Ontology tree with counts + 4 toggles. SC-5 requires the full VOKB filter rail (D-55-02a exact-parity). Pinning Filter Rail mount lines here also eliminates the "TrendingPanel ships but is never imported" failure mode flagged by the plan-checker.

Output: 4 new filter components + extended FilterRail wiring with lazy mounts for the two downstream surfaces; Zustand store from 55-04 provides the state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-04-PLAN.md
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/Filters/LayerFilter.tsx
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/Filters/DomainFilter.tsx
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/Filters/OntologyFilter.tsx
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/Filters/LegendPanel.tsx
@integrations/unified-viewer/src/panels/FilterRail.tsx
@integrations/unified-viewer/src/lib/logging/Logger.ts

<interfaces>
Per 55-PATTERNS.md sections § LayerFilter/DomainFilter/OntologyFilter/GraphToggles.

OntologyFilter `groupingSchema` prop type:

```typescript
interface OntologyGroupSchema {
  upper: { name: string; subgroups: Record<string, ReadonlySet<string>> }
  lower: { name: string; subgroups: Record<string, ReadonlySet<string>> }
}
export const VOKB_SCHEMA: OntologyGroupSchema = { upper: { name: 'Upper Ontology', subgroups: {...} }, lower: { name: 'Lower Ontology', subgroups: {...} } }
export const CODING_SCHEMA: OntologyGroupSchema = { upper: { name: 'Hierarchy', subgroups: {'L0 — Project':new Set(['Project']), ...} }, lower: { name: 'Typed Views', subgroups: {'LSL Pipeline':new Set(['Observation','Digest','Insight','LearningArtifact']), ...} } }
```

Per 55-PATTERNS.md §OntologyFilter for the full VOKB and coding subgroup contents.

GraphToggles per 55-PATTERNS.md § GraphToggles: 4 toggles (`Show All Relations`, `Show Clusters`, `Merged Only`, `Hide Documentation`) + nested `Labels` sub-toggle when "Show All Relations" is on, with verbatim italic hint copy per UI-SPEC §5 + VOKB LegendPanel.tsx:14-80.

Click semantics + filter composition rules in UI-SPEC §10. Within-dimension multi-select OR, cross-dimension AND, empty = all-visible.

FilterRail lazy-mount contract for downstream plans (this plan OWNS these lines; 55-10 / 55-11 only create the imported files):

```typescript
// In FilterRail.tsx top-of-file (added by Task 3):
import { lazy, Suspense } from 'react'
const TrendingPanel = lazy(() => import('./TrendingPanel'))                      // populated by 55-10
const HierarchyNavigator = lazy(() => import('./coding/HierarchyNavigator'))    // populated by 55-11

// In FilterRail render (added by Task 3, between GraphToggles and LegendPanel):
<Suspense fallback={<div data-testid="trending-panel-fallback" className="text-xs text-muted-foreground px-3 py-2">Loading trending…</div>}>
  <TrendingPanel apiClient={apiClient} />
</Suspense>
{system === 'coding' && (
  <Suspense fallback={<div data-testid="hierarchy-navigator-fallback" className="text-xs text-muted-foreground px-3 py-2">Loading hierarchy…</div>}>
    <HierarchyNavigator system={system} />
  </Suspense>
)}
```

Until 55-10 lands, `./TrendingPanel` does not exist; Task 3 ships a minimal placeholder component at that exact path so the lazy import resolves at build time (see Task 3 action). 55-10 Task 1 OVERWRITES that placeholder with the real port. Same pattern for `./coding/HierarchyNavigator` (overwritten by 55-11 Task 1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port LayerFilter + DomainFilter</name>
  <read_first>
    - _work/.../viewer/src/components/Filters/LayerFilter.tsx (74 lines — full file)
    - _work/.../viewer/src/components/Filters/DomainFilter.tsx (74 lines — full file)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ LayerFilter + § DomainFilter)
    - integrations/unified-viewer/src/store/viewer-store.ts (Zustand selectors `selectedLayers`, `toggleLayer`, `selectedDomains`, `toggleDomain` — from 55-04)
    - integrations/unified-viewer/src/components/ui/checkbox.tsx (shadcn Checkbox)
  </read_first>
  <behavior>
    - Test (LayerFilter): renders 2 checkboxes (Evidence, Pattern) with `text-xs` labels and `text-[10px]` count badges
    - Test (LayerFilter): clicking Evidence calls `toggleLayer('evidence')` (verify via spied Zustand store)
    - Test (LayerFilter): when `selectedLayers === []`, `isSelected('evidence')` is true (empty = all visible per VOKB semantic)
    - Test (LayerFilter): count badges derive from entity array (`{evidence: N, pattern: M}` aggregation)
    - Test (DomainFilter): on okb tab where `entity.domain` is set, renders DOMAINS (`raas`, `kpifw`, `general`) with counts
    - Test (DomainFilter): on coding tab where all entities lack `domain`, renders italic muted "Domain filter not applicable for this system" (NOT a hard error)
    - Test (Both): Logger calls use `Logger.info(Logger.Categories.FILTERS, ...)`; no raw `console.*`
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/panels/filters/LayerFilter.tsx` as a verbatim port of VOKB LayerFilter.tsx. Translate Redux `dispatch(toggleLayer(...))` → Zustand `useViewerStore(s => s.toggleLayer)(...)`. Use shadcn `<Checkbox>` (Phase 45 already has it under `src/components/ui/`). Tailwind classes preserved verbatim per 55-PATTERNS.md.
    Create `integrations/unified-viewer/src/panels/filters/DomainFilter.tsx` mirroring LayerFilter structurally. Add the graceful-degradation branch for coding tab (UI-SPEC §7 row 3 — italic muted "Domain filter not applicable for this system" if no entity in the visible set has a `domain` field).
    Use `Logger.info(Logger.Categories.FILTERS, 'LayerFilter toggle: evidence')` style log lines (matches VOKB LayerFilter.tsx:155 convention).
    Tests: cases in `<behavior>`.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/panels/filters/LayerFilter.test.tsx src/panels/filters/DomainFilter.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All test cases pass. `grep -c "useViewerStore\|toggleLayer\|toggleDomain" integrations/unified-viewer/src/panels/filters/LayerFilter.tsx integrations/unified-viewer/src/panels/filters/DomainFilter.tsx` returns ≥3 per file. `grep -nE "console\.(log|warn|error|info|debug)" integrations/unified-viewer/src/panels/filters/LayerFilter.tsx integrations/unified-viewer/src/panels/filters/DomainFilter.tsx` returns 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port OntologyFilter (grouped tree) with VOKB_SCHEMA + CODING_SCHEMA</name>
  <read_first>
    - _work/.../viewer/src/components/Filters/OntologyFilter.tsx (265 lines — full file)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ OntologyFilter — Imports + grouping constants + Group-select pattern + Per-class checkbox row + Group-header pattern + Executor guidance)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§3 micro-type exceptions — `text-[10px]`, `text-[9px]`, `text-[8px]`)
    - integrations/unified-viewer/src/store/viewer-store.ts (Zustand `selectedOntologyClasses`, `toggleOntologyClass`, `setSelectedOntologyClasses` from 55-04)
  </read_first>
  <behavior>
    - Test: renders Upper Ontology and Lower Ontology sections with sub-groups per `VOKB_SCHEMA` default
    - Test: when `groupingSchema={CODING_SCHEMA}` prop is passed, renders Hierarchy (L0–L3) + Typed Views (LSL Pipeline / Patterns / Other) sub-groups
    - Test: per-class checkbox row uses `text-xs` label + `text-[10px]` count badge (UI-SPEC §3 micro-type exception)
    - Test: group-level `all` / `none` toggles use `text-[9px]` and operate as union-add / filter-remove (NOT replace)
    - Test: unknown classes from the entity payload that don't match any subgroup land in a catch-all "Other" group (verbatim VOKB rule per OntologyFilter.tsx:42)
    - Test: clicking `all` for a group adds all that group's classes to `selectedOntologyClasses` (union); does NOT clear other groups' selections
    - Test: clicking individual class checkbox calls `toggleOntologyClass(cls)`
    - Test: `text-[8px]` only used on disclosure triangles (NOT on text content) per UI-SPEC §3
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/panels/filters/OntologyFilter.tsx` as a verbatim port of VOKB OntologyFilter.tsx. Add the `groupingSchema` prop per UI-SPEC §7 row 4. Export both `VOKB_SCHEMA` and `CODING_SCHEMA` constants from this file (or a sibling `ontology-schemas.ts` — prefer same-file for now; refactor only if file exceeds 300 lines).
    Translate Redux `useDispatch` + selectors → Zustand `useViewerStore(s => s.selectedOntologyClasses)` + `(s => s.toggleOntologyClass)` + `(s => s.setSelectedOntologyClasses)`. Preserve `selectGroup`/`deselectGroup` callbacks per 55-PATTERNS.md (union-add, filter-remove — NOT replace).
    Preserve micro-type exceptions verbatim. Log filter changes via `Logger.info(Logger.Categories.FILTERS, ...)`.
    Tests: cases in `<behavior>`.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/panels/filters/OntologyFilter.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All test cases pass. `grep -c "VOKB_SCHEMA\|CODING_SCHEMA\|groupingSchema\|setSelectedOntologyClasses\|toggleOntologyClass" integrations/unified-viewer/src/panels/filters/OntologyFilter.tsx` returns ≥5. `grep -c "text-\\[10px\\]\|text-\\[9px\\]" integrations/unified-viewer/src/panels/filters/OntologyFilter.tsx` returns ≥2 (micro-type exceptions preserved).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port GraphToggles + wire FilterRail (incl. unconditional lazy mounts for TrendingPanel + HierarchyNavigator slots)</name>
  <read_first>
    - _work/.../viewer/src/components/Filters/LegendPanel.tsx (lines 14-80 — toggle block ONLY, NOT the legend below)
    - integrations/unified-viewer/src/panels/FilterRail.tsx (current Phase 45 full file)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ GraphToggles + § OntologyFilter executor guidance — Phase 45 flat ClassList delete)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§5 copywriting for all 4 toggle labels + nested italic hints; §6 layout map for FilterRail order)
    - integrations/unified-viewer/src/store/viewer-store.ts (Zustand `showEdges`, `showRelationLabels`, `showClusters`, `showMergedOnly`, `hideDocNodes` + their toggle actions from 55-04)
  </read_first>
  <behavior>
    - Test (GraphToggles): renders 4 top-level checkboxes (`Show All Relations`, `Show Clusters`, `Merged Only`, `Hide Documentation`) — copy verbatim per UI-SPEC §5
    - Test (GraphToggles): "Labels" nested sub-checkbox appears ONLY when `showEdges` is true; disappears when false
    - Test (GraphToggles): each toggle calls the appropriate Zustand action setter
    - Test (GraphToggles): italic hint text under `Show Clusters` matches "Halos = connectivity clusters (densely linked groups)" verbatim (UI-SPEC §5)
    - Test (FilterRail): mounts in this order top-to-bottom: Search (existing) → LayerFilter → DomainFilter → OntologyFilter → GraphToggles → TrendingPanel (lazy, always) → HierarchyNavigator (lazy, coding only) → LegendPanel (from 55-07, at bottom)
    - Test (FilterRail, lazy mounts present): `await screen.findByTestId('trending-panel-fallback')` resolves OR the lazy module loads (Suspense fallback rendered). After `await act` flush, the TrendingPanel placeholder content (from this task's stub or, later, the real 55-10 component) is visible
    - Test (FilterRail, coding gate): on `system === 'coding'`, HierarchyNavigator slot renders (Suspense fallback + then real/placeholder content); on `system === 'okb'`, the slot is absent (negative assertion — `queryByTestId('hierarchy-navigator-fallback')` is null AND no `<HierarchyNavigator>` element in the DOM)
    - Test (FilterRail): Phase 45 flat ClassList is REMOVED (negative assertion — no longer renders a plain checkbox list of classes)
    - Test (FilterRail): on coding tab, OntologyFilter receives `groupingSchema={CODING_SCHEMA}`; on okb tab, default VOKB_SCHEMA used
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/panels/filters/GraphToggles.tsx` per 55-PATTERNS.md § GraphToggles. Each `<Checkbox>` row: `<label className="flex items-center gap-2 text-xs">`. Nested italic hint: `<p className="text-[10px] ml-6 leading-tight">`. Wire to Zustand actions per 55-04.

    Edit `integrations/unified-viewer/src/panels/FilterRail.tsx`:
    (1) Import `LayerFilter`, `DomainFilter`, `OntologyFilter`, `CODING_SCHEMA`, `GraphToggles` from new files;
    (2) DELETE the Phase 45 flat `ClassList` (or whatever the flat class-checkbox block is called) per UI-SPEC §6 mandate;
    (3) Mount the 4 new components in UI-SPEC §6 order. Use `system === 'coding' ? CODING_SCHEMA : undefined` to pick the OntologyFilter schema (undefined defaults to VOKB_SCHEMA).
    (4) Add the two lazy mounts per the `<interfaces>` block:
        - `const TrendingPanel = lazy(() => import('./TrendingPanel'))` at the top of the file. ALWAYS rendered (no gate). Wrap in `<Suspense fallback={<div data-testid="trending-panel-fallback" ...>Loading trending…</div>}>`.
        - `const HierarchyNavigator = lazy(() => import('./coding/HierarchyNavigator'))` at the top of the file. Gated by `system === 'coding'`. Wrap in `<Suspense fallback={<div data-testid="hierarchy-navigator-fallback" ...>Loading hierarchy…</div>}>`.
    (5) Because `./TrendingPanel` and `./coding/HierarchyNavigator` do not yet exist at this point in the wave order (55-10 / 55-11 ship in later waves), THIS TASK also ships minimal placeholder modules at those exact paths so the lazy imports resolve at build time and the wave-3 vitest suite passes:
        - `integrations/unified-viewer/src/panels/TrendingPanel.tsx`: `export default function TrendingPanelPlaceholder() { return <div data-testid="trending-panel-placeholder" className="text-xs text-muted-foreground px-3 py-2 italic">Trending Patterns — loading from 55-10…</div> }`. 55-10 Task 1 OVERWRITES this file with the real port.
        - `integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx`: same shape, returning `<div data-testid="hierarchy-navigator-placeholder">Hierarchy — loading from 55-11…</div>`. 55-11 Task 1 OVERWRITES this file with the real component.
        These placeholders are deliberately minimal (single-div, no logic) — they exist solely so the build resolves between 55-08 ship and 55-10/55-11 ship.
    Tests per `<behavior>`. Add `Logger.info(Logger.Categories.PANELS, 'FilterRail mounted')` once at mount.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/panels/filters/GraphToggles.test.tsx src/panels/FilterRail.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All test cases pass. `grep -c "LayerFilter\|DomainFilter\|OntologyFilter\|GraphToggles" integrations/unified-viewer/src/panels/FilterRail.tsx` returns ≥4. The Phase 45 flat class checklist is GONE — verify via test or `grep -c "Phase 45 ClassList\|flat class checklist" integrations/unified-viewer/src/panels/FilterRail.tsx` returns 0 (anti-marker comment removed in deletion).
    Lazy mounts pinned: `grep -c "lazy(() => import('./TrendingPanel'))" integrations/unified-viewer/src/panels/FilterRail.tsx` ≥ 1 AND `grep -c "lazy(() => import('./coding/HierarchyNavigator'))" integrations/unified-viewer/src/panels/FilterRail.tsx` ≥ 1.
    Placeholder modules exist: `test -f integrations/unified-viewer/src/panels/TrendingPanel.tsx && test -f integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx` exit 0.
    Manual smoke: `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-08-filter-rail.png` shows full filter rail with Layer / Domain / Ontology grouped tree / 4 toggles / placeholder strips for Trending + Hierarchy.
    Side-by-side: `gsd-browser navigate http://localhost:3002 && gsd-browser screenshot /tmp/55-08-vokb-filter-rail.png` — manual operator review confirms VOKB-parity layout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand store ← user clicks → filter state | client-side only; user controls their own filter state |
| OntologyFilter groupingSchema prop ← FilterRail | typed prop; either VOKB_SCHEMA or CODING_SCHEMA literal — no user input |
| Lazy-loaded TrendingPanel / HierarchyNavigator modules ← network bundle | same-origin static assets; React.lazy import path is a string literal (not user-controlled) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-08-01 | XSS | filter labels render entity class names from backend | mitigate | React text-node escaping; class names are validated by the OntologyRegistry on the backend (Phase 38) before they reach the frontend |
| T-55-08-02 | Information disclosure | Domain filter exposes domain values from entities | accept | domains are descriptive metadata; same trust class as entity names |
| T-55-08-03 | Tampering | lazy-loaded module path could be altered to load attacker code | accept | `lazy(() => import('./TrendingPanel'))` uses a string-literal relative path; Vite resolves at build time; no user input flows here |

Package legitimacy: no new packages.
</threat_model>

<verification>
- `cd integrations/unified-viewer && npx vitest run src/panels/filters/ src/panels/FilterRail.test.tsx --reporter=verbose` → all green
- `cd integrations/unified-viewer && npx tsc --noEmit && npx vite build` → exit 0
- `grep -rnE "console\.(log|warn|error|info|debug)" integrations/unified-viewer/src/panels/filters/` → 0 matches
- `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-08-coding-filters.png` shows Layer + Domain + Ontology tree (coding L0-L3 + Typed Views grouping) + 4 toggles + Trending placeholder strip + Hierarchy placeholder strip
- `gsd-browser navigate http://localhost:5173/viewer/okb && gsd-browser screenshot /tmp/55-08-okb-filters.png` shows Layer + Domain + Ontology tree (VOKB Upper/Lower grouping) + 4 toggles + Trending placeholder strip (NO Hierarchy strip — coding gate)
- Operator side-by-side review against `gsd-browser screenshot http://localhost:3002` (VOKB) confirms VOKB-parity filter rail layout
</verification>

<success_criteria>
- SC-5 (Filter parity with VOKB) DELIVERED end-to-end on both /viewer/coding and /viewer/okb
- Phase 45 flat ClassList REPLACED by VOKB grouped tree (UI-SPEC §6 mandate)
- `groupingSchema` prop allows the same OntologyFilter to host both VOKB (Upper/Lower) and coding (Hierarchy / Typed Views) layouts
- Micro-type exceptions (`text-[10px]`/`text-[9px]`/`text-[8px]`) preserved verbatim
- Filter composition rules from UI-SPEC §10 honored
- TrendingPanel + HierarchyNavigator lazy mounts pinned in FilterRail.tsx by this plan (closes plan-checker B-2/B-3); downstream 55-10 / 55-11 only overwrite the placeholder files
- ZERO raw `console.*` introduced
</success_criteria>

<output>
Create `.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-08-SUMMARY.md` when done.
</output>
