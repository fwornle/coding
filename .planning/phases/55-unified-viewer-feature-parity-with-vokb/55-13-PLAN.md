---
phase: 55-unified-viewer-feature-parity-with-vokb
plan: 13
type: execute
wave: 7
depends_on: [55-01, 55-05, 55-07, 55-08, 55-09, 55-10, 55-11, 55-12]
files_modified:
  - tests/e2e/unified-viewer/55-cap-removal.spec.ts
  - tests/e2e/unified-viewer/55-okb-routing.spec.ts
  - tests/e2e/unified-viewer/55-stats-bar.spec.ts
  - tests/e2e/unified-viewer/55-filters-parity.spec.ts
  - tests/e2e/unified-viewer/55-entity-sub-tabs.spec.ts
  - tests/e2e/unified-viewer/55-issue-triage.spec.ts
  - tests/e2e/unified-viewer/55-coding-surfaces.spec.ts
  - tests/e2e/unified-viewer/55-renderer-encoding.spec.ts
  - tests/e2e/unified-viewer/55-side-by-side-screenshots.spec.ts
  - tests/e2e/unified-viewer/55-fixtures/expected-vokb-screenshots/
autonomous: false
requirements: [UI-02]
requirements_addressed: [UI-02]
must_haves:
  truths:
    - "9 new E2E spec files under `tests/e2e/unified-viewer/` covering all 10 SC + 4 coding-additions surfaces; runnable via `npx playwright test tests/e2e/unified-viewer/55-*.spec.ts`"
    - "Forbidden-string assertion: every spec asserts `body` does NOT contain `cc.bmwgroup.net` per D-55-01c"
    - "Phase 45 e2e baseline tests still green (BC); Phase 55 specs ADDED, not replaced"
    - "Side-by-side screenshot spec captures `localhost:5173/viewer/{coding,okb}` vs `localhost:3002` (VOKB) per surface; golden VOKB references committed under `tests/e2e/unified-viewer/55-fixtures/expected-vokb-screenshots/`"
    - "All specs are structured `*.spec.ts` files runnable via `npx playwright test` — NOT hand-rolled `node /tmp/foo.mjs` scripts (CLAUDE.md prefer-gsd-browser does NOT block structured tests)"
    - "Final acceptance gate (checkpoint:human-verify): operator approves the side-by-side screenshot pairs as visually equivalent for ported surfaces"
  artifacts:
    - path: "tests/e2e/unified-viewer/55-*.spec.ts"
      provides: "9 E2E spec files covering all SCs + coding additions + CAP removal + OKB routing + side-by-side parity"
      contains: "describe"
    - path: "tests/e2e/unified-viewer/55-fixtures/expected-vokb-screenshots/"
      provides: "Golden VOKB reference screenshots for parity diffing"
      contains: "vokb"
  key_links:
    - from: "55-*.spec.ts"
      to: "running services at localhost:5173 (unified-viewer dev) + localhost:3002 (VOKB) + localhost:12436 (obs-api)"
      via: "Playwright test runner"
      pattern: "page.goto"
---

<objective>
Land the Phase 55 E2E verification harness — one spec per surface group covering all 10 SC + 4 coding-additions + CAP removal + OKB routing. Includes a side-by-side screenshot diff spec that captures both Phase 55 unified-viewer AND VOKB at `localhost:3002`, then asserts visual parity per surface (Phase 45 retrospective requirement + UI-SPEC §17).

Purpose: Closes the verification gap that caused Phase 45's premature sign-off. 55-CONTEXT.md explicitly mandates a side-by-side screenshot comparison; this plan delivers it.

Output: 9 spec files + golden screenshot fixtures + a checkpoint where the operator approves the parity diffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-CONTEXT.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md
@.planning/phases/45-unified-web-viewer/45-VERIFICATION.md
@tests/e2e/unified-viewer/system-routing.spec.ts
@tests/e2e/unified-viewer/entity-detail.spec.ts
@tests/e2e/unified-viewer/filter-search.spec.ts
@CLAUDE.md

<interfaces>
Playwright structured test format (extends Phase 45 convention under `tests/e2e/unified-viewer/`). Each Phase 55 spec named `55-{topic}.spec.ts` for chronological grouping.

Coverage map:

| Spec | SCs covered |
|------|-------------|
| `55-cap-removal.spec.ts` | D-55-01b/c — `/viewer/cap` 404; no `cc.bmwgroup.net`; no `cap` tab UI |
| `55-okb-routing.spec.ts` | SC-1 — OKB tab targets `:8090`; entity names look like OKM not coding |
| `55-stats-bar.spec.ts` | SC-6 — 7 metrics visible; LIVE indicator; tabular-nums |
| `55-filters-parity.spec.ts` | SC-5 — Layer / Domain / Ontology grouped tree / 4 toggles |
| `55-entity-sub-tabs.spec.ts` | SC-7 / SC-8 — sub-tabs visible per predicate; Markdown identity header; harmonized widths; keyboard `1/2/3/4` |
| `55-issue-triage.spec.ts` | SC-10 — mode switch; two-pane layout; RCA chain BFS visible |
| `55-coding-surfaces.spec.ts` | D-55-02b — Hierarchy / LSL strip / ETM sheet / Workflow status; gating on `system === 'coding'` |
| `55-renderer-encoding.spec.ts` | SC-3 / SC-4 — Legend visible; distinct node shapes; dashed orphan; pulse halo on Observation |
| `55-side-by-side-screenshots.spec.ts` | UI-SPEC §17 + Phase 45 retrospective — golden VOKB screenshot diff per surface |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specs for routing fix + stats bar + filter parity + renderer encoding</name>
  <read_first>
    - tests/e2e/unified-viewer/system-routing.spec.ts (Phase 45 — convention reference)
    - tests/e2e/unified-viewer/filter-search.spec.ts (Phase 45)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-CONTEXT.md (success criteria #1–6)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§6/§14 layout + encoding)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-01-PLAN.md (CAP removal expectations)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-05-PLAN.md (renderer behavior)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-07-PLAN.md (StatsBar expectations)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-08-PLAN.md (filter rail layout)
  </read_first>
  <acceptance_criteria>
    - `55-cap-removal.spec.ts`: navigates to `/viewer/cap`, asserts NO redirect to `cc.bmwgroup.net`, asserts page body does not contain `cc.bmwgroup.net` or `(CORS)`, asserts no `'cap'` tab option in any NavBar/Tabs element
    - `55-okb-routing.spec.ts`: navigates to `/viewer/okb`, intercepts network requests, asserts at least one fetch goes to `localhost:8090/api/v1/*` (NOT `:3848`); entity names do NOT match coding-typical names like `PersistenceAgent`/`CodeAnalyzer`
    - `55-stats-bar.spec.ts`: on both `/viewer/coding` and `/viewer/okb`, asserts StatsBar has 7 numeric metric slots + a LIVE chip; numbers use the `tabular-nums` class; LIVE dot is `bg-emerald-500` when up
    - `55-filters-parity.spec.ts`: on `/viewer/coding`, asserts FilterRail contains Layer / Domain / Ontology (Hierarchy + Typed Views groups) / 4 toggles; OntologyFilter has Upper/Lower groups when on `/viewer/okb`; per-class count badges visible; clicking group `all`/`none` mutates the URL state (or visible chip count) accordingly
    - `55-renderer-encoding.spec.ts`: on `/viewer/coding`, asserts Legend `<details>` is present and contains 4 sections; asserts at least 3 distinct shape elements rendered in the Sigma canvas (use Playwright canvas inspection or count distinct `data-shape` attributes if Sigma exposes them; otherwise rely on the `gsd-browser screenshot` + visual diff in 55-13-side-by-side spec); at least one orphan node has visible dashed border
  </acceptance_criteria>
  <action>
    Create 5 spec files under `tests/e2e/unified-viewer/`. Each spec uses Playwright structured `describe`/`test` blocks + `page.goto` + assertions. For interactive flows, use `page.click` + `page.waitForRequest`. Network interception (OKB routing spec) uses `page.route('**/api/v1/**', ...)` or `page.on('request', ...)` to record actual URL targets.
    Each spec MUST assert the forbidden-string rule: `expect(page.locator('body')).not.toContainText('cc.bmwgroup.net')` (D-55-01c).
    DO NOT write hand-rolled `node /tmp/foo.mjs` Playwright scripts (CLAUDE.md `prefer-gsd-browser`) — structured `.spec.ts` files runnable via `npx playwright test` are the right pattern; CLAUDE.md explicitly allows this convention.
    All 5 specs added to existing `tests/e2e/unified-viewer/` directory; they inherit the Playwright config from Phase 45.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; npx playwright test tests/e2e/unified-viewer/55-cap-removal.spec.ts tests/e2e/unified-viewer/55-okb-routing.spec.ts tests/e2e/unified-viewer/55-stats-bar.spec.ts tests/e2e/unified-viewer/55-filters-parity.spec.ts tests/e2e/unified-viewer/55-renderer-encoding.spec.ts --reporter=line 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    All 5 specs pass. `grep -l "cc.bmwgroup.net" tests/e2e/unified-viewer/55-*.spec.ts | xargs grep -c "not.toContainText.*cc.bmwgroup.net"` returns ≥5 (every spec has the forbidden-string assertion). Phase 45 e2e baseline still green: `npx playwright test tests/e2e/unified-viewer/system-routing.spec.ts tests/e2e/unified-viewer/filter-search.spec.ts tests/e2e/unified-viewer/entity-detail.spec.ts --reporter=line` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Specs for sub-tabs + Triage + coding-only surfaces</name>
  <read_first>
    - tests/e2e/unified-viewer/entity-detail.spec.ts (Phase 45 — convention reference)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-09-PLAN.md (sub-tab visibility predicate + width logic)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-10-PLAN.md (Issue Triage BFS + RCA_EDGE_TYPES + SECTION_ORDER)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-11-PLAN.md (Hierarchy + LSL Strip gating)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-12-PLAN.md (ETM sheet + Workflow panel gating; keyboard `t`)
  </read_first>
  <acceptance_criteria>
    - `55-entity-sub-tabs.spec.ts`: navigates to `/viewer/coding`, clicks a node with descriptionSegments, asserts at least 2 pills (Default + Evolution) visible in `[role="tablist"]`. Pressing keyboard `2` activates Evolution. Pressing `1` reverts to Default. Selecting a different node resets to Default. Asserts MarkdownViewerPanel on `/viewer/okb` renders an EntityIdentityHeader element. Side-panel width transitions to `w-[30rem]` when sub-tab is `evolution` or `timeline`.
    - `55-issue-triage.spec.ts`: only runs on `/viewer/coding` if at least one entity has ontologyClass `Incident` or `FailureIncident`; navigates with `?mode=triage`, asserts two-pane layout (left w-[380px], right flex-1) renders, asserts SECTION_ORDER section labels visible ("Symptoms", "Failure Patterns", "Root Causes", "Resolutions", "Risks", "Decisions") when an incident is selected, asserts "View in Graph" CTA returns to KG mode preserving selectedNodeId. If coding lacks incidents, spec is skipped with `test.skip` + a console log message via `test.info().annotations`.
    - `55-coding-surfaces.spec.ts`: on `/viewer/coding`, asserts HierarchyNavigator (FilterRail Accordion with `role="tree"`) is visible; LslTimelineStrip rendered between content and Footer; clicking 📡 NavBar button opens a right-side Sheet (`aria-label="Close observation stream"` after click); WorkflowStatusPanel rendered below Footer. On `/viewer/okb`, NONE of these are visible (gate verification — assert `document.querySelector('[role="tree"]')` is null).
  </acceptance_criteria>
  <action>
    Create 3 spec files using the same structure as Task 1. Use `page.evaluate(() => Array.from(document.querySelectorAll('[role="tab"]')).map(e => e.textContent))` for sub-tab assertions. For the keyboard tests, use `page.keyboard.press('2')` after focusing the SidePanel.
    Triage spec uses `test.skip` conditionally based on whether the entity payload contains incidents (probe via fetch in beforeAll).
    Coding-surfaces spec asserts gating by navigating to BOTH systems and contrasting presence.
    Forbidden-string rule assertion in every spec.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; npx playwright test tests/e2e/unified-viewer/55-entity-sub-tabs.spec.ts tests/e2e/unified-viewer/55-issue-triage.spec.ts tests/e2e/unified-viewer/55-coding-surfaces.spec.ts --reporter=line 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    All 3 specs pass (Triage spec may auto-skip if no incidents). Coverage check: `grep -c "test(" tests/e2e/unified-viewer/55-entity-sub-tabs.spec.ts tests/e2e/unified-viewer/55-issue-triage.spec.ts tests/e2e/unified-viewer/55-coding-surfaces.spec.ts` returns ≥10 total test cases.
  </done>
</task>

<task type="auto">
  <name>Task 3: Side-by-side screenshot diff harness (gold fixtures)</name>
  <read_first>
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-CONTEXT.md (Process Amendments — side-by-side screenshot comparison MANDATORY)
    - .planning/phases/45-unified-web-viewer/45-VERIFICATION.md (the 15%-shortfall finding that this harness prevents)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§17 layout map — list of ported surfaces)
    - tests/e2e/unified-viewer/ (Phase 45 playwright config — verify there's a baseScreenshotPath and snapshot expectations)
  </read_first>
  <acceptance_criteria>
    - `55-side-by-side-screenshots.spec.ts` captures screenshots from both `localhost:5173/viewer/coding` AND `localhost:3002` (VOKB) for: FilterRail / EntityDetailPanel / StatsBar / TrendingPanel / IssueTriageView / LegendPanel
    - Per-surface screenshots saved to `tests/e2e/unified-viewer/55-fixtures/expected-vokb-screenshots/{surface}.png` (first run produces; subsequent runs diff)
    - VOKB-side screenshots committed as golden references; unified-viewer side compared via Playwright `expect(page).toHaveScreenshot({maxDiffPixelRatio: 0.15})` — 15% tolerance reflects layout differences (Sigma vs D3) while still catching surface-omission regressions
    - Spec includes a `test.fixme` or `test.skip` branch for the Sigma-vs-D3 canvas comparison (renderer-level pixel diff is structurally impossible; the spec asserts the SURFACES are present, NOT pixel-perfect parity)
    - When `localhost:3002` is unreachable, the spec skips with `test.skip` + an annotation logging the absence (avoids false negatives in CI without VOKB running)
  </acceptance_criteria>
  <action>
    Create `tests/e2e/unified-viewer/55-side-by-side-screenshots.spec.ts`. The spec uses Playwright's snapshot testing: on first run, baseline screenshots of VOKB at `:3002` are captured and committed under `55-fixtures/expected-vokb-screenshots/`. Subsequent runs compare current `:3002` output vs the baseline (catches VOKB drift) AND compare `:5173` unified-viewer screenshots vs structural assertions (layout selectors present, not pixel-equal).
    For layout-level assertions across the two viewers: write a `compareSurfacePresence(page, surface)` helper that asserts a CSS selector (e.g., `[data-testid="filter-rail-legend"]`) is present in both viewers. This delivers the operator's "are the surfaces actually there?" check from Phase 45's retrospective without requiring pixel-perfect parity.
    Pre-flight: `test.beforeAll` probes `localhost:3002` via `fetch` — if it returns non-200, skip the spec and annotate "VOKB not running; skipping side-by-side comparison".
    Forbidden-string rule applies even in fixtures.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; npx playwright test tests/e2e/unified-viewer/55-side-by-side-screenshots.spec.ts --reporter=line 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    Spec passes when VOKB is running; auto-skips with annotation when not. `ls tests/e2e/unified-viewer/55-fixtures/expected-vokb-screenshots/` returns non-empty after first successful run. The spec file `grep -c "compareSurfacePresence\|toHaveScreenshot\|test.skip" returns ≥3` (correct usage).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator side-by-side parity review (UI-SPEC §17 + Phase 45 retrospective gate)</name>
  <what-built>
    9 E2E spec files (`55-*.spec.ts`) + golden VOKB reference screenshots covering all 10 SC + 4 coding-additions + CAP removal + OKB routing + side-by-side parity. Specs include the forbidden-string assertion (`cc.bmwgroup.net`).
  </what-built>
  <how-to-verify>
    1. Start all required services:
       - `bin/coding --claude` (or confirm via `launchctl list | grep com.coding`)
       - Unified viewer dev: `cd integrations/unified-viewer && npm run dev` → opens on `:5173`
       - VOKB: ensure `_work/.../operational-knowledge-management/viewer/` is serving on `localhost:3002` (run `npm run dev` in that directory in a separate terminal)
    2. Run the full Phase 55 e2e suite: `cd /Users/Q284340/Agentic/coding && npx playwright test tests/e2e/unified-viewer/55-*.spec.ts --reporter=line`. Expected: 0 failures (possibly some skips for Triage if no incidents OR side-by-side if VOKB not running).
    3. Open the Playwright report to inspect screenshots: `npx playwright show-report`.
    4. Open the parity-fixture pairs in `tests/e2e/unified-viewer/55-fixtures/expected-vokb-screenshots/` alongside the current unified-viewer screenshots from the report.
    5. Manually run `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-final-coding.png` and `gsd-browser navigate http://localhost:3002 && gsd-browser screenshot /tmp/55-final-vokb.png`. Open both. Compare per surface from UI-SPEC §17:
       - StatsBar present in unified-viewer ✓
       - FilterRail Layer / Domain / OntologyFilter grouped tree / 4 toggles / Trending / Legend visible ✓
       - Right panel sub-tabs visible per visibility predicate ✓
       - Issue Triage mode reachable (if entities have Incident/FailureIncident) ✓
       - Hierarchy / LSL Strip / ETM Sheet / Workflow Panel visible on coding tab ONLY ✓
    6. Verify forbidden strings purged: `grep -rIn "cc.bmwgroup.net" integrations/unified-viewer/ tests/e2e/unified-viewer/` returns no matches.
    7. Approve only if every SC + addition surface visibly delivers per UI-SPEC; flag missing or regressed surfaces inline.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 55, or describe issues per surface (e.g., "FilterRail Domain filter missing on coding tab — open 55-08 follow-up").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright e2e tests → local dev services | localhost-only; no external network |
| Golden screenshot fixtures committed to repo | static images; no executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-13-01 | Tampering | committed golden screenshot replaced by a subtly different one to hide regression | mitigate | side-by-side test compares against LIVE VOKB at `:3002` when available, not just the golden — golden only used as fallback when VOKB unreachable |
| T-55-13-02 | Information disclosure | screenshots may reveal entity names from the local KG | accept | screenshots are committed under a developer-controlled tree; entity names are NOT credentials |
| T-55-13-03 | False negative | CI without VOKB running silently skips side-by-side | mitigate | spec emits a Playwright annotation when skipped; operator gate explicitly verifies VOKB was running during the run |

Package legitimacy: no new packages introduced (Playwright already a Phase 45 dep per existing `tests/e2e/unified-viewer/` structure).
</threat_model>

<verification>
- `cd /Users/Q284340/Agentic/coding && npx playwright test tests/e2e/unified-viewer/55-*.spec.ts --reporter=line` → all specs pass (or auto-skip with annotations)
- Phase 45 baseline still green: `npx playwright test tests/e2e/unified-viewer/ --grep -v "^55-" --reporter=line` → exit 0
- `grep -l "not.toContainText.*cc.bmwgroup.net" tests/e2e/unified-viewer/55-*.spec.ts | wc -l` returns 9 (every spec has the forbidden-string assertion)
- `ls tests/e2e/unified-viewer/55-fixtures/expected-vokb-screenshots/ | wc -l` returns >0 (golden fixtures committed)
- Operator gate cleared with "approved" resolution
</verification>

<success_criteria>
- 9 spec files covering all SCs + coding additions + forbidden-string gate
- Side-by-side fixtures committed under `55-fixtures/`
- All specs runnable via `npx playwright test`
- Phase 45 baseline e2e tests still green (BC preserved)
- Operator approval recorded — gate cleared
</success_criteria>

<output>
Create `.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-13-SUMMARY.md` when done.
</output>
