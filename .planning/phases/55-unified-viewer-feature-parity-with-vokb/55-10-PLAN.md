---
phase: 55-unified-viewer-feature-parity-with-vokb
plan: 10
type: execute
wave: 4
depends_on: [55-03, 55-04, 55-06, 55-07, 55-08]
files_modified:
  - integrations/unified-viewer/src/panels/TrendingPanel.tsx
  - integrations/unified-viewer/src/panels/TrendingPanel.test.tsx
  - integrations/unified-viewer/src/routes/IssueTriageView.tsx
  - integrations/unified-viewer/src/routes/IssueTriageView.test.tsx
  - integrations/unified-viewer/src/routes/UnifiedViewer.tsx
autonomous: true
requirements: [UI-02]
requirements_addressed: [UI-02]
must_haves:
  truths:
    - "TrendingPanel renders sparkline rows for the top patterns from `GET /api/v1/trends?top=20`; each row: score-coded dot + name + score badge + 7d/30d/90d trend counts (tabular-nums)"
    - "TrendingPanel empty state copy matches UI-SPEC §5 verbatim (`No recurring patterns detected yet.` + italic sub-text)"
    - "TrendingPanel click → setSelectedNodeId(pattern.nodeId); if currently in Triage mode, switch back to KG mode first (UI-SPEC §10 click semantics)"
    - "TrendingPanel collapsible inside FilterRail (matches Phase 55 layout map UI-SPEC §6); polls every 60s per UI-SPEC §18"
    - "TrendingPanel.tsx OVERWRITES the placeholder module shipped by 55-08 Task 3; 55-08 already pinned the lazy mount in FilterRail.tsx so this plan does NOT edit FilterRail.tsx"
    - "IssueTriageView matches VOKB IssueTriage.tsx structure: left pane w-[380px] incident list + search + trending strip; right pane RCA chain (6 sections per SECTION_ORDER) + Sources & Evidence section"
    - "RCA chain BFS implementation matches VOKB IssueTriage.tsx:97-182 verbatim — 2-hop walk filtered by RCA_EDGE_TYPES set (12 edge types locked)"
    - "Click chain item → setSelectedNodeId + 'View in Graph' CTA returns to KG mode (UI-SPEC §10 row 'Click on chain item in Triage right panel')"
    - "Issue Triage mode-switch HIDDEN entirely when entity dataset lacks Incident/FailureIncident types (predicate per UI-SPEC §9 + NavBar from 55-07)"
    - "Sources & Evidence section reuses `lib-domain/evidence-types.ts` (55-03) — NO duplicate icons/labels"
    - "UnifiedViewer.tsx: swap the TriagePlaceholder import (shipped by 55-07 Task 2) for `lazy(() => import('@/routes/IssueTriageView'))`; mode==='triage' renders the real view"
    - "Logger discipline: ZERO raw `console.*`"
  artifacts:
    - path: "integrations/unified-viewer/src/panels/TrendingPanel.tsx"
      provides: "Surface #6 — Trending Patterns sidebar (overwrites 55-08 placeholder)"
      contains: "useTrendsQuery"
    - path: "integrations/unified-viewer/src/routes/IssueTriageView.tsx"
      provides: "Surface #7 — Issue Triage mode view (Mode B canvas)"
      contains: "RCA_EDGE_TYPES"
  key_links:
    - from: "TrendingPanel.tsx"
      to: "GET /api/v1/trends"
      via: "TanStack Query polling (60s)"
      pattern: "/api/v1/trends"
    - from: "IssueTriageView.tsx"
      to: "lib-domain/evidence-types.ts"
      via: "shared EVIDENCE_TYPE_ICONS"
      pattern: "EVIDENCE_TYPE_ICONS"
    - from: "UnifiedViewer.tsx"
      to: "@/routes/IssueTriageView"
      via: "lazy import swap of TriagePlaceholder (55-07 → 55-10)"
      pattern: "lazy.*IssueTriageView"
---

<objective>
Land SC-9 (Trending Patterns sparklines) and SC-10 (Issue Triage mode). TrendingPanel.tsx OVERWRITES the placeholder file 55-08 Task 3 shipped (so the lazy import already pinned in FilterRail.tsx now resolves to the real component). IssueTriageView is the Mode B canvas; UnifiedViewer.tsx swaps the `TriagePlaceholder` lazy import (shipped by 55-07 Task 2) for the real `IssueTriageView` lazy import.

Purpose: Both surfaces are direct VOKB ports per D-55-02a exact-parity. IssueTriageView is the largest VOKB component (530 lines) — port it as a single plan rather than mixing with other surfaces. By ONLY overwriting placeholder files (55-08 TrendingPanel + 55-07 TriagePlaceholder), this plan avoids the FilterRail / UnifiedViewer co-edit conflicts the plan-checker flagged as B-2 / B-4.

Output: Trending sidebar + full Issue Triage mode end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-03-PLAN.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-06-PLAN.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-07-PLAN.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-08-PLAN.md
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/TrendingPanel.tsx
@_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/components/KnowledgeGraph/IssueTriage.tsx
@integrations/unified-viewer/src/lib-domain/evidence-types.ts
@integrations/unified-viewer/src/graph/vokb-palette.ts
@integrations/unified-viewer/src/api/ApiClient.ts

<interfaces>
TrendingPattern shape (matches okbClient.ts:68-78 — used by 55-06 endpoint):

```typescript
interface TrendingPattern {
  nodeId: string
  entity: { id: string; name: string; entityType: string; description?: string }
  trendScore: number
  trends: { last7Days: number; last30Days: number; last90Days: number }
}
```

IssueTriageView RCA edge types (LOCKED set per IssueTriage.tsx:64-68):

```typescript
const RCA_EDGE_TYPES = new Set([
  'HAS_SYMPTOM', 'HAS_ROOT_CAUSE', 'CAUSED', 'CAUSED_BY',
  'INDICATES', 'MITIGATED_BY', 'MITIGATES', 'ADDRESSES',
  'MATCHES', 'DERIVED_FROM', 'AFFECTS', 'APPLIED_TO',
])
```

Section metadata (IssueTriage.tsx:71-81 verbatim):

```typescript
const SECTION_ORDER = ['Symptom','FailurePattern','RootCause','Resolution','Risk','Decision']
const SECTION_META = {
  Symptom:        { label: 'Symptoms',         color: '#ef4444', icon: '🔴' },
  FailurePattern: { label: 'Failure Patterns', color: '#f59e0b', icon: '🟡' },
  RootCause:      { label: 'Root Causes',      color: '#8b5cf6', icon: '🟣' },
  Resolution:     { label: 'Resolutions',      color: '#10b981', icon: '🟢' },
  Risk:           { label: 'Risks',            color: '#f97316', icon: '🟠' },
  Decision:       { label: 'Decisions',        color: '#3b82f6', icon: '🔵' },
}
```

BFS implementation per IssueTriage.tsx:97-182 (55-PATTERNS.md § IssueTriageView.tsx — BFS pattern, full block).

UnifiedViewer.tsx swap (Task 2 of this plan):

```typescript
// Before this plan (shipped by 55-07 Task 2):
const TriagePlaceholder = lazy(() => import('./TriagePlaceholder'))

// After this plan (55-10 Task 2):
const IssueTriageView = lazy(() => import('@/routes/IssueTriageView'))
// Replace `<TriagePlaceholder />` jsx with `<IssueTriageView />` inside the `mode === 'triage'` branch.
```

Mode switching wired in 55-07 NavBar. This plan ONLY implements the route content for `mode === 'triage'` plus the import swap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overwrite the 55-08 placeholder with real TrendingPanel</name>
  <read_first>
    - _work/.../viewer/src/components/KnowledgeGraph/TrendingPanel.tsx (103 lines — full file)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ TrendingPanel.tsx)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§5 empty-state copy, §10 click semantics row 7, §18 row 5 endpoint)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-08-PLAN.md (§ Task 3 — note the placeholder TrendingPanel.tsx this plan overwrites + the FilterRail lazy mount already pinned)
    - integrations/unified-viewer/src/panels/TrendingPanel.tsx (the placeholder shipped by 55-08 Task 3 — overwrite, don't append)
    - integrations/unified-viewer/src/api/ApiClient.ts (existing GET helper)
    - integrations/unified-viewer/src/store/viewer-store.ts (setSelectedNodeId, mode, setMode from 55-04)
  </read_first>
  <behavior>
    - Test: when API returns 3 patterns, renders 3 rows; each row has score-coded dot (scoreColor helper from TrendingPanel.tsx:14-24) + name + score badge (scoreBadgeClass) + 7d/30d/90d trend counts
    - Test: all numeric values use `tabular-nums` className
    - Test: empty state copy matches UI-SPEC §5 verbatim (`No recurring patterns detected yet.` + italic sub-text `Patterns appear here when the same failure modes are ingested multiple times. Run more analyses to build occurrence data.`)
    - Test: click on a row calls `setSelectedNodeId(pattern.nodeId)` AND if `useViewerStore.getState().mode === 'triage'`, also calls `setMode('kg')` first
    - Test: error state — `Could not load trends — retry` chip per UI-SPEC §16 row 5
    - Test: polls every 60s (use `vi.useFakeTimers` + TanStack Query `refetchInterval`)
    - Test: Logger.info(Logger.Categories.PANELS, ...) on row click; no raw console.*
    - Test (integration with 55-08 lazy mount): the FilterRail lazy import `lazy(() => import('./TrendingPanel'))` resolves the default export to this real component (verify via a small import smoke test in the test file — `await import('./TrendingPanel')` returns an object whose `.default` is a function returning a sparkline row container)
  </behavior>
  <action>
    OVERWRITE `integrations/unified-viewer/src/panels/TrendingPanel.tsx` (currently the minimal placeholder shipped by 55-08 Task 3) with a verbatim port of VOKB TrendingPanel.tsx. The export shape MUST be `export default function TrendingPanel(...) { ... }` so the lazy import in FilterRail.tsx (pinned by 55-08) resolves correctly.
    Use TanStack Query (`useQuery` per Phase 45 convention — see `MarkdownViewerPanel.tsx:27`) with `refetchInterval: 60_000`. Hook endpoint to `apiClient.get<{patterns: TrendingPattern[]}>('/api/v1/trends?top=20')` (envelope already unwrapped by ApiClient per Phase 45 convention).
    Port `scoreColor` + `scoreBadgeClass` helpers verbatim per 55-PATTERNS.md § TrendingPanel.tsx (lines 14-24). Sparkline rendering: inline SVG `<line>` segments (no chart library — VOKB doesn't use one either per 55-PATTERNS.md).
    Wrap in shadcn `<Collapsible>` per UI-SPEC §6.

    **No FilterRail.tsx edit.** 55-08 Task 3 already added `const TrendingPanel = lazy(() => import('./TrendingPanel'))` plus the `<Suspense>` wrapper. This plan simply REPLACES the file contents at that import path. DO NOT touch `FilterRail.tsx`.

    Tests per `<behavior>`.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/panels/TrendingPanel.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All TrendingPanel test cases pass. `grep -c "scoreColor\|scoreBadgeClass\|setSelectedNodeId\|refetchInterval\|tabular-nums" integrations/unified-viewer/src/panels/TrendingPanel.tsx` returns ≥5. ZERO raw `console.*`.
    Placeholder fully replaced: `grep -c "TrendingPanelPlaceholder\|trending-panel-placeholder" integrations/unified-viewer/src/panels/TrendingPanel.tsx` returns 0 (verifies the 55-08 placeholder is gone).
    Default export present: `grep -c "export default" integrations/unified-viewer/src/panels/TrendingPanel.tsx` returns 1.
    Lazy mount intact in FilterRail: `grep -c "lazy(() => import('./TrendingPanel'))" integrations/unified-viewer/src/panels/FilterRail.tsx` returns 1 (verifies 55-08's mount is still in place; this plan did not regress it).
    Manual smoke: `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-10-trending.png` shows real Trending Patterns sparkline rows in FilterRail (not the loading placeholder).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port IssueTriageView (Mode B canvas) + swap UnifiedViewer's TriagePlaceholder import</name>
  <read_first>
    - _work/.../viewer/src/components/KnowledgeGraph/IssueTriage.tsx (530 lines — full file, especially: 21-53 imports + EVIDENCE constants, 64-68 RCA_EDGE_TYPES, 71-81 SECTION_META, 97-182 BFS, 319-322 empty-state, the click-handler block for "View in Graph" CTA)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ IssueTriageView.tsx — full block)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§5 copywriting for triage states, §6 layout map, §9 mode switching, §10 click semantics rows for Triage)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-07-PLAN.md (§ Task 2 — verify the TriagePlaceholder import line + JSX usage are exactly what this task swaps)
    - integrations/unified-viewer/src/routes/TriagePlaceholder.tsx (the small component shipped by 55-07 Task 2 — to be replaced via import swap, NOT deleted; keep the file as a fallback for future tooling)
    - integrations/unified-viewer/src/lib-domain/evidence-types.ts (55-03 — share evidence stuff)
    - integrations/unified-viewer/src/store/viewer-store.ts (setSelectedNodeId, mode, setMode, entities, relations from 55-04 / Phase 45)
    - integrations/unified-viewer/src/routes/UnifiedViewer.tsx (55-07 — the route shell that mounts the TriagePlaceholder when mode === 'triage')
  </read_first>
  <behavior>
    - Test: layout has left pane w-[380px] (incident list + search + trending strip) and right pane flex-1 (RCA chain + Sources & Evidence)
    - Test: incident list seeded from entities filtered by `ontologyClass in {Incident, FailureIncident}`
    - Test: search input filters incident list by name (case-insensitive)
    - Test: clicking incident in left pane sets `incidentKey` state; RCA chain renders for that key
    - Test (BFS): RCA chain implementation matches VOKB:97-182 verbatim — 2-hop walk, RCA_EDGE_TYPES filter at depth > 0, depth-1 RootCause/Symptom/FailurePattern get extended to depth 2
    - Test (SECTION_ORDER): chain renders 6 sections in order Symptom / FailurePattern / RootCause / Resolution / Risk / Decision with SECTION_META icons + colors
    - Test (chain item): click → `setSelectedNodeId(item.entity.key)` + 300ms flash highlight (animation class added then removed)
    - Test ("View in Graph" CTA): click → calls `setMode('kg')` and preserves `selectedNodeId`
    - Test (Sources & Evidence): renders for the selected incident's entity using EVIDENCE_TYPE_ICONS / EVIDENCE_TYPE_LABELS from 55-03 (verify import, not local re-declare)
    - Test (empty left): renders UI-SPEC §5 verbatim `No incidents in knowledge base. Ingest data to populate.`
    - Test (empty right): renders verbatim `Select an incident to investigate.` + sub-text
    - Test (mode-switch hidden): when entity dataset lacks incidents, the Issue Triage option is hidden in NavBar (this is enforced by 55-07; verify IssueTriageView never receives navigation unless incidents exist by reading the route guard)
    - Test (UnifiedViewer swap): the previously-imported `TriagePlaceholder` from 55-07 is replaced by a `lazy(() => import('@/routes/IssueTriageView'))`; pressing the Triage toggle now renders the real two-pane view (Testing-Library `findByTestId('issue-triage-view')` succeeds; `findByTestId('triage-mode-placeholder')` returns null)
    - Test (logger): `Logger.info(Logger.Categories.PANELS, ...)` on incident selection + chain item click; no raw console.*
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/routes/IssueTriageView.tsx` as a verbatim port of VOKB IssueTriage.tsx. Translate Redux → Zustand for `entities`/`relations`/`setSelectedNodeId`/`mode`/`setMode`. Use local `useState` for `selectedKey` (incidentKey) and `searchTerm` per 55-PATTERNS.md Executor guidance. The root JSX element MUST have `data-testid="issue-triage-view"` so the integration test in Task 2 can assert the swap.
    BFS implementation verbatim per 55-PATTERNS.md § IssueTriageView.tsx BFS pattern — DO NOT paraphrase; copy the adjacency build + 2-hop walk exactly.
    SECTION_ORDER + SECTION_META copied verbatim.
    Sources & Evidence imports from `@/lib-domain/evidence-types` (55-03). DO NOT redeclare EVIDENCE_TYPE_ICONS / EVIDENCE_TYPE_LABELS locally — that was the VOKB duplicate Phase 55 is consolidating.
    "View in Graph" CTA: button uses primary accent (UI-SPEC §4 accent reserved-for list item 4); copy `View in Graph` verbatim; click handler `() => { setMode('kg'); /* selectedNodeId already set on chain item click */ }`.

    Edit `integrations/unified-viewer/src/routes/UnifiedViewer.tsx` (one targeted swap):
    (a) Remove the `const TriagePlaceholder = lazy(() => import('./TriagePlaceholder'))` line shipped by 55-07 Task 2.
    (b) Add `const IssueTriageView = lazy(() => import('@/routes/IssueTriageView'))` in its place.
    (c) Inside the `mode === 'triage'` JSX branch, replace `<TriagePlaceholder />` with `<IssueTriageView />`.
    DO NOT modify any other part of UnifiedViewer.tsx (no NavBar / StatsBar / FilterRail wiring changes; those are owned by 55-07).
    Leave `integrations/unified-viewer/src/routes/TriagePlaceholder.tsx` on disk (it stays as a small typed component, unreferenced after this edit — removal is out of scope to avoid churn; can be cleaned up in a future tidy-pass).

    Tests per `<behavior>` (≥10 cases).
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/routes/IssueTriageView.test.tsx src/routes/UnifiedViewer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All IssueTriageView + UnifiedViewer test cases pass. `grep -c "RCA_EDGE_TYPES\|SECTION_ORDER\|SECTION_META\|EVIDENCE_TYPE_ICONS\|setMode\|setSelectedNodeId" integrations/unified-viewer/src/routes/IssueTriageView.tsx` returns ≥6. ZERO raw `console.*`.
    Import swap verified: `grep -c "lazy(() => import('@/routes/IssueTriageView'))" integrations/unified-viewer/src/routes/UnifiedViewer.tsx` returns 1 AND `grep -c "TriagePlaceholder" integrations/unified-viewer/src/routes/UnifiedViewer.tsx` returns 0 (placeholder fully removed from the route shell).
    `grep -c "EVIDENCE_TYPE_ICONS" integrations/unified-viewer/src/routes/IssueTriageView.tsx` reflects an IMPORT (not a local re-declaration); verify with `head -30 integrations/unified-viewer/src/routes/IssueTriageView.tsx | grep -c "from '@/lib-domain/evidence-types'"` returns 1.
    Manual smoke: `gsd-browser navigate http://localhost:5173/viewer/coding?mode=triage` (if coding has Incident entities) → screenshot shows two-pane layout with incident list and RCA chain sections. `gsd-browser click "[data-testid='view-in-graph']"` returns to KG mode preserving selection.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/api/v1/trends` → frontend | trusted; envelope-wrapped per Phase 44; entity payloads sanitized via React text-node rendering |
| RCA chain BFS over relations | client-side computation; no untrusted input |
| Lazy import path `@/routes/IssueTriageView` ← UnifiedViewer | static string literal; Vite resolves at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-10-01 | XSS | incident names + RCA chain item descriptions rendered | mitigate | React text-node escaping; no `dangerouslySetInnerHTML` |
| T-55-10-02 | Denial of service | TanStack Query polling at 60s under load | accept | 60s cadence is light; users browse small datasets in viewer |
| T-55-10-03 | Logic | BFS deviates from VOKB → triage results differ from VOKB | mitigate | port verbatim per 55-PATTERNS.md; snapshot test asserts adjacency-build behavior matches a fixture |

Package legitimacy: no new packages.
</threat_model>

<verification>
- `cd integrations/unified-viewer && npx vitest run src/panels/TrendingPanel.test.tsx src/routes/IssueTriageView.test.tsx src/routes/UnifiedViewer.test.tsx --reporter=verbose` → all green
- `cd integrations/unified-viewer && npx tsc --noEmit && npx vite build` → exit 0
- `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-10-trending-on-coding.png` → real Trending Patterns sparklines visible in FilterRail (not the placeholder)
- If coding has Incidents: `gsd-browser navigate http://localhost:5173/viewer/coding?mode=triage && gsd-browser screenshot /tmp/55-10-triage.png` shows two-pane Triage UI
- Operator side-by-side: `gsd-browser screenshot http://localhost:3002` (VOKB Triage) vs Phase 55 → BFS chain + SECTION_ORDER + 6 sections visually match
- `grep -rnE "console\.(log|warn|error|info|debug)" integrations/unified-viewer/src/panels/TrendingPanel.tsx integrations/unified-viewer/src/routes/IssueTriageView.tsx` → 0 matches
</verification>

<success_criteria>
- SC-9 (Trending Patterns sidebar) DELIVERED (real component overwriting the 55-08 placeholder; lazy mount in FilterRail unchanged)
- SC-10 (Issue Triage mode) DELIVERED (UnifiedViewer swap from TriagePlaceholder to IssueTriageView is a one-line edit; no co-ownership of FilterRail / NavBar / StatsBar)
- BFS matches VOKB IssueTriage.tsx:97-182 verbatim (RCA_EDGE_TYPES + SECTION_ORDER + SECTION_META locked)
- Evidence constants imported from shared 55-03 module (no duplicate declarations)
- Mode switch wired end-to-end with KG ↔ Triage state preservation per UI-SPEC §9
- ZERO raw `console.*`
</success_criteria>

<output>
Create `.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-10-SUMMARY.md` when done.
</output>
