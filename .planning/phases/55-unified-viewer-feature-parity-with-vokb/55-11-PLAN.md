---
phase: 55-unified-viewer-feature-parity-with-vokb
plan: 11
type: execute
wave: 5
depends_on: [55-04, 55-06, 55-07, 55-08]
files_modified:
  - integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx
  - integrations/unified-viewer/src/panels/coding/HierarchyNavigator.test.tsx
  - integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx
  - integrations/unified-viewer/src/panels/coding/LslTimelineStrip.test.tsx
  - integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts
  - integrations/unified-viewer/src/hooks/useKeyboardShortcuts.test.tsx
  - integrations/unified-viewer/src/routes/UnifiedViewer.tsx
autonomous: true
requirements: [UI-02]
requirements_addressed: [UI-02]
must_haves:
  truths:
    - "HierarchyNavigator renders only when `system === 'coding'` — gated at component level (defense-in-depth: 55-08 already gates at FilterRail mount level)"
    - "HierarchyNavigator builds a tree from `entities` filtered by ontologyClass in {Project, Component, SubComponent, Detail}, ordered by metadata.parent / metadata.hierarchyPath"
    - "HierarchyNavigator uses shadcn `<Accordion type='multiple'>` with `role='tree'` parent + `role='treeitem'` rows + `aria-level` + `aria-expanded`"
    - "Click L1 row sets transient filter via `setHierarchySubtreeFilter(id)`; transient filter chip rendered above canvas (UI-SPEC §13.1)"
    - "HierarchyNavigator.tsx OVERWRITES the placeholder module shipped by 55-08 Task 3; 55-08 already pinned the gated lazy mount in FilterRail.tsx so this plan does NOT edit FilterRail.tsx"
    - "`useKeyboardShortcuts.ts` is EXTENDED in this plan to support two-key sequences with input-focus guard; the `g h` shortcut is registered via this extension, NOT inlined inside HierarchyNavigator (closes plan-checker W-6)"
    - "`Cmd/Ctrl+F` while navigator is focused opens text input above tree; keyboard shortcut `g h` focuses the search input (UI-SPEC §10)"
    - "LslTimelineStrip renders only when `system === 'coding'` — gated; absent on /viewer/okb"
    - "LslTimelineStrip fetches `GET /api/coding/lsl/sessions?since=<computed>` per selected window (24h/7d/30d) via shadcn ToggleGroup; updates on window change"
    - "Click a session tick adds/replaces transient filter via `setLslSessionFilter([id])`; Cmd/Ctrl+click multi-selects via `addLslSessionFilter`"
    - "Currently-running session (endAt === null) has ring-2 ring-primary outline"
    - "Keyboard nav per UI-SPEC §13.2: ← / → between ticks; Enter to select; Esc to clear filter"
    - "Logger discipline: ZERO raw `console.*`"
  artifacts:
    - path: "integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx"
      provides: "Surface #13 — coding-only hierarchy tree navigator (overwrites 55-08 placeholder)"
      contains: "role=\"tree\""
    - path: "integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx"
      provides: "Surface #14 — coding-only horizontal session timeline"
      contains: "/api/coding/lsl/sessions"
    - path: "integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts"
      provides: "Extended hook supporting two-key sequences with input-focus guard"
      contains: "registerSequence"
  key_links:
    - from: "HierarchyNavigator (file)"
      to: "FilterRail.tsx lazy mount (already pinned by 55-08)"
      via: "overwrite placeholder file at the exact import path"
      pattern: "panels/coding/HierarchyNavigator.tsx"
    - from: "LslTimelineStrip → UnifiedViewer mount"
      to: "below main content row, above Footer (UI-SPEC §6)"
      via: "conditional mount on system === 'coding'"
      pattern: "LslTimelineStrip"
    - from: "HierarchyNavigator search-focus + 'g h' shortcut"
      to: "useKeyboardShortcuts.ts sequence registration"
      via: "hook extension"
      pattern: "registerSequence"
---

<objective>
Land 2 of the 4 coding-specific surfaces (D-55-02b): HierarchyNavigator (#13) and LslTimelineStrip (#14). HierarchyNavigator.tsx OVERWRITES the placeholder shipped by 55-08 Task 3 (so the gated lazy import already pinned in FilterRail.tsx now resolves to the real component). LslTimelineStrip mounts in UnifiedViewer.tsx below the main content row.

This plan ALSO extends `useKeyboardShortcuts.ts` to support two-key sequences with input-focus guard (closes plan-checker W-6 — `g h` is registered via the hook extension, NOT inlined inside HierarchyNavigator).

Purpose: These two surfaces are coding-specific (no VOKB analog) and provide the project-hierarchy filter and session-timeline filter that make the coding KG navigable. Together with 55-12, they deliver D-55-02b's full superset. The hook extension is the canonical home for two-key sequences across Phase 55+ (avoids ad-hoc per-component state machines).

Output: Two new components + UnifiedViewer mount for LSL strip + extended useKeyboardShortcuts hook. FilterRail.tsx is NOT touched (mount line owned by 55-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-04-PLAN.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-06-PLAN.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-07-PLAN.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-08-PLAN.md
@integrations/unified-viewer/src/components/ui/accordion.tsx
@integrations/unified-viewer/src/components/ui/toggle-group.tsx
@integrations/unified-viewer/src/components/ui/tooltip.tsx
@integrations/unified-viewer/src/api/ApiClient.ts
@integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts

<interfaces>
HierarchyNavigator props/state:

```typescript
interface Props { system: System }  // gated; renders null when system !== 'coding'
// Internal: useViewerStore for entities + setHierarchySubtreeFilter (from 55-04)
// Local state: searchQuery (Cmd/Ctrl+F text)
// Default export shape required (FilterRail's lazy import expects default export)
export default function HierarchyNavigator(props: Props): JSX.Element | null
```

LslTimelineStrip props/state:

```typescript
interface Props { system: System; apiClient: ApiClient }
// Internal: useViewerStore for lslSessionFilter + setLslSessionFilter + addLslSessionFilter + clearLslSessionFilter
// Local state: window: '24h'|'7d'|'30d'
// Fetch: GET /api/coding/lsl/sessions?since=<iso>&limit=200
```

useKeyboardShortcuts extension (Task 1):

```typescript
// Add to KeyboardShortcutHandle:
export interface KeyboardShortcutHandle {
  registerSearchInputRef: (input: HTMLInputElement | null) => void
  /**
   * Register a two-key sequence (e.g., 'g h'). The handler fires when the
   * user presses key1 then key2 within `windowMs` (default 800ms). The
   * sequence is SKIPPED when an <input> or <textarea> has focus, so typing
   * 'github' or 'graph' into the search box does NOT trigger `g h`.
   * Returns an unregister function suitable for useEffect cleanup.
   */
  registerSequence: (
    key1: string,
    key2: string,
    handler: () => void,
    opts?: { windowMs?: number }
  ) => () => void
}
```

Both surfaces gate on `system === 'coding'` at component level. The FilterRail mount line for HierarchyNavigator already exists (pinned by 55-08 with a coding gate) — this plan only overwrites the placeholder file. The UnifiedViewer mount for LslTimelineStrip IS owned by this plan (no prior plan pinned it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useKeyboardShortcuts.ts with two-key sequence support + overwrite 55-08 placeholder with real HierarchyNavigator</name>
  <read_first>
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ HierarchyNavigator.tsx — tree-build + render pattern + executor guidance)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§13.1 Hierarchy Navigator full UX, §10 keyboard `g h` shortcut)
    - integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts (existing Phase 45 hook — extend, don't rewrite)
    - integrations/unified-viewer/src/hooks/useKeyboardShortcuts.test.tsx (existing tests — preserve all)
    - integrations/unified-viewer/src/components/ui/accordion.tsx (shadcn Accordion exports)
    - integrations/unified-viewer/src/store/viewer-store.ts (55-04 — `hierarchySubtreeFilter`, `setHierarchySubtreeFilter`)
    - integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx (placeholder shipped by 55-08 Task 3 — overwrite)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-08-PLAN.md (§ Task 3 — confirm the gated lazy mount in FilterRail.tsx is `{system === 'coding' && <Suspense>...<HierarchyNavigator ... /></Suspense>}`)
  </read_first>
  <behavior>
    - Test (hook extension): `registerSequence('g', 'h', handler)` fires `handler` when user presses `g` then `h` within 800ms; does NOT fire when interval > 800ms; does NOT fire when third key intercedes
    - Test (hook extension, input-focus guard): when an `<input>` is focused, pressing `g` then `h` does NOT fire `handler` (so typing "github" in search input is safe)
    - Test (hook extension, cleanup): the return value of `registerSequence` removes the binding when called (verify with vi.useFakeTimers + spy)
    - Test (hook extension, BC): all existing Phase 45 shortcut tests still pass (`/`, `Esc`, `?`, `f` continue to behave correctly)
    - Test (HierarchyNavigator): when `system !== 'coding'`, returns null (renders nothing)
    - Test (HierarchyNavigator, default export): `(await import('./HierarchyNavigator')).default` is a React component (confirms FilterRail's lazy import resolves)
    - Test (HierarchyNavigator, tree build): given entities with ontologyClass `Project`/`Component`/`SubComponent`/`Detail` and `metadata.parent` linking them, builds a 4-level tree
    - Test (HierarchyNavigator, render): each L1 row is a `<button aria-label="Filter to {className}: {name} ({childCount} descendants)">` inside an AccordionItem
    - Test (HierarchyNavigator, a11y): tree uses `role="tree"` on parent + `role="treeitem"` rows + `aria-level={n}` + `aria-expanded`
    - Test (HierarchyNavigator, click L1): `setHierarchySubtreeFilter(l1.id)` called; verify via store state change
    - Test (HierarchyNavigator, search Cmd/Ctrl+F): pressing Cmd+F (or Ctrl+F) while focus is in the navigator opens a small text input above the tree
    - Test (HierarchyNavigator, `g h` shortcut via hook): registers via `registerSequence('g', 'h', focusSearchInput)`; pressing `g` then `h` (no input focus) calls the focus callback; typing `gh` into another input does NOT trigger (input-focus guard from hook extension)
    - Test (HierarchyNavigator, empty state): "No hierarchy data yet." + "Run wave-analysis to populate." sub-text per UI-SPEC §13.1
    - Test (Logger): `Logger.info(Logger.Categories.PANELS, 'Hierarchy filter set: ${id}')` on L1 click; no raw console.*
  </behavior>
  <action>
    EXTEND `integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts` to support two-key sequences:
    (a) Add `registerSequence: (key1, key2, handler, opts?) => () => void` to the `KeyboardShortcutHandle` interface.
    (b) Inside the hook, maintain a `sequenceRegistry: Map<string, { key2: string; handler: () => void; windowMs: number }[]>` keyed by `key1`. On keydown:
        - If input is focused, do nothing for sequence keys (preserves Phase 45 input-focus discipline).
        - Else: if pressed key matches any registered `key1`, set `pendingSequence = { key1, timestamp: Date.now() }`; clear after `windowMs`.
        - If `pendingSequence` set and next key matches the registered `key2` within the window, fire `handler()`, clear `pendingSequence`.
        - Any third key clears `pendingSequence`.
    (c) Return an unregister function that removes the handler from the registry.
    (d) Preserve ALL existing Phase 45 shortcut behavior (`/`, `Esc`, `?`, `f`) verbatim.

    OVERWRITE `integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx` (currently the minimal placeholder from 55-08 Task 3) per 55-PATTERNS.md § HierarchyNavigator.tsx. The export shape MUST be `export default function HierarchyNavigator(...) { ... }` so the lazy import in FilterRail.tsx (pinned by 55-08) resolves correctly.

    Tree-build per the verbatim block: filter `entities` by ontologyClass in `{Project, Component, SubComponent, Detail}`, index by id, walk parent pointers. Render using shadcn `<Accordion type="multiple">`.

    Register `Cmd/Ctrl+F` for opening the in-navigator search input (when focus is inside the navigator). Register the `g h` sequence via `useKeyboardShortcuts(...).registerSequence('g', 'h', () => searchInputRef.current?.focus())` — DO NOT inline a per-component two-key state machine; the hook extension is the single source of truth (per plan-checker W-6 resolution).

    **No FilterRail.tsx edit.** 55-08 Task 3 already added `const HierarchyNavigator = lazy(() => import('./coding/HierarchyNavigator'))` plus the `{system === 'coding' && <Suspense>...</Suspense>}` wrapper. This plan REPLACES the file contents at that import path. DO NOT touch `FilterRail.tsx`.

    Tests per `<behavior>` (split into `useKeyboardShortcuts.test.tsx` for hook extension + `HierarchyNavigator.test.tsx` for component).
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/hooks/useKeyboardShortcuts.test.tsx src/panels/coding/HierarchyNavigator.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All hook + HierarchyNavigator test cases pass. `grep -c "registerSequence" integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts` returns ≥2 (interface + impl). `grep -c "system === 'coding'" integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx` returns ≥1. `grep -c "role=\"tree\"\|role=\"treeitem\"\|aria-level\|aria-expanded" integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx` returns ≥4.
    Placeholder fully replaced: `grep -c "hierarchy-navigator-placeholder" integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx` returns 0.
    Default export present: `grep -c "export default" integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx` returns 1.
    No ad-hoc inline sequence handling: `grep -nE "lastKey|pendingKey|gKeyPressed" integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx` returns 0 (the sequence logic lives in the hook now).
    Lazy mount intact in FilterRail: `grep -c "lazy(() => import('./coding/HierarchyNavigator'))" integrations/unified-viewer/src/panels/FilterRail.tsx` returns 1 (55-08's mount unchanged).
    ZERO raw `console.*`.
    Manual smoke (coding only): `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-11-hierarchy.png` shows real expandable tree in FilterRail (not the placeholder strip). `gsd-browser navigate http://localhost:5173/viewer/okb` shows NO HierarchyNavigator (gate working at both 55-08 mount layer and 55-11 component layer — defense in depth).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LslTimelineStrip (coding-only horizontal session timeline) + UnifiedViewer mount</name>
  <read_first>
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ LslTimelineStrip.tsx — data shape + render layout + executor guidance)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§13.2 LSL Timeline Strip full UX, §10 click semantics for LSL ticks)
    - integrations/unified-viewer/src/components/ui/toggle-group.tsx (added by 55-07)
    - integrations/unified-viewer/src/components/ui/tooltip.tsx (Phase 45 default; verify)
    - integrations/unified-viewer/src/store/viewer-store.ts (55-04 — `lslSessionFilter`, `setLslSessionFilter`, `addLslSessionFilter`, `clearLslSessionFilter`)
    - integrations/unified-viewer/src/api/ApiClient.ts (`apiClient.get` typed signature)
    - integrations/unified-viewer/src/routes/UnifiedViewer.tsx (current state after 55-07 + 55-10 edits — this plan adds ONE conditional mount line below the main content row)
  </read_first>
  <behavior>
    - Test: when `system !== 'coding'`, returns null
    - Test (window toggle): default `7d`; clicking `24h` re-fetches with `since = now - 24h`; clicking `30d` re-fetches with `since = now - 30d`
    - Test (render): each session is a tick (w-2 h-6 inside h-8 strip) positioned by `pctOfWindow(startAt)`; ticks sorted by startAt; max-200 per fetch
    - Test (tooltip): hover shows `{id.slice(0,8)} · {startAt} → {endAt ?? 'running'} · {observationCount} obs · {entityIds.length} entities` (UI-SPEC §13.2)
    - Test (active session): session with `endAt === null` has `ring-2 ring-primary` class
    - Test (click single): clicking a tick calls `setLslSessionFilter([id])` (replaces selection)
    - Test (Cmd/Ctrl+click): calls `addLslSessionFilter(id)` (additive multi-select)
    - Test (keyboard): ← / → arrows move focus between ticks; Enter calls setLslSessionFilter; Esc calls clearLslSessionFilter
    - Test (empty): "No sessions recorded in this time window." with the time-window selector still active (UI-SPEC §13.2)
    - Test (UnifiedViewer mount): on `system === 'coding'`, the strip is mounted between the main content row and the Footer; on `system === 'okb'`, no LSL strip is present
    - Test (Logger): logs window change + tick click via `Logger.info(Logger.Categories.PANELS, ...)`; no raw console.*
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx` per 55-PATTERNS.md § LslTimelineStrip.tsx. Use TanStack Query for the fetch — `useQuery(['lsl-sessions', window], () => apiClient.get<{sessions: LslSession[]}>('/api/coding/lsl/sessions?since=...&limit=200'))`. Window state local (useState). Toggle group from `@/components/ui/toggle-group`.
    Position calc: `pctOfWindow(iso) = ((Date.now() - new Date(iso).getTime()) / windowMs) * 100` flipped to `100 - pct` so newest is on the right.
    Keyboard handler: add tabindex=0 on each tick `<button>`; arrow-key listener on the strip wrapper moves focus; `Enter` / `Esc` call store actions.

    Edit `integrations/unified-viewer/src/routes/UnifiedViewer.tsx` (incremental edit on top of 55-07 + 55-10 changes) to import + mount `{system === 'coding' && <LslTimelineStrip system={system} apiClient={apiClient} />}` BETWEEN the main content row and Footer per UI-SPEC §6. This is the only UnifiedViewer.tsx edit in this plan — Task 1 does not touch UnifiedViewer.tsx (HierarchyNavigator mounts inside FilterRail, owned by 55-08).

    Tests per `<behavior>`.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/panels/coding/LslTimelineStrip.test.tsx src/routes/UnifiedViewer.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All LslTimelineStrip + UnifiedViewer (BC + new mount) test cases pass. `grep -c "system === 'coding'\|/api/coding/lsl/sessions\|ToggleGroup\|setLslSessionFilter" integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx` returns ≥4. ZERO raw `console.*`.
    Manual smoke: `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-11-lsl.png` shows horizontal strip below main canvas with ticks. `gsd-browser eval "Array.from(document.querySelectorAll('[role=\"button\"]')).length"` returns >0 for the tick buttons. `gsd-browser navigate http://localhost:5173/viewer/okb` → no LSL strip visible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/api/coding/lsl/sessions` → frontend | trusted; envelope-wrapped; session IDs are filename hashes (not credentials) |
| Hierarchy tree click → Zustand mutation | client-side, user-controlled action |
| Coding-only gate | `system === 'coding'` predicate enforced in component AND in route mount; defense in depth |
| Keyboard sequence registry | global document keydown listener; sequence handlers only invoked when input is NOT focused (W-6 input-focus guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-11-01 | Cross-system data leak | OKB tab renders coding-only surfaces | mitigate | gate on `system === 'coding'` at BOTH render time (component) AND mount time (FilterRail / UnifiedViewer) |
| T-55-11-02 | XSS | hierarchy names / session IDs rendered | mitigate | React text-node escaping; entity names already validated server-side |
| T-55-11-03 | Information disclosure | LSL session metadata (start/end time, observation count) shown in tooltip | accept | localhost-only; same trust class as `/api/coding/observations` already exposed |
| T-55-11-04 | Performance | rapid window changes trigger many fetches | mitigate | TanStack Query caches per window key; debounce only if observed in production smoke |
| T-55-11-05 | Logic | `g h` sequence fires while user is typing 'github' in search input → unintended search focus | mitigate | input-focus guard in `useKeyboardShortcuts.registerSequence` skips sequence keys when `<input>`/`<textarea>` is focused (verified by test in Task 1) |

Package legitimacy: no new packages.
</threat_model>

<verification>
- `cd integrations/unified-viewer && npx vitest run src/panels/coding/ src/hooks/useKeyboardShortcuts.test.tsx src/routes/UnifiedViewer.test.tsx --reporter=verbose` → all green
- `cd integrations/unified-viewer && npx tsc --noEmit && npx vite build` → exit 0
- `gsd-browser navigate http://localhost:5173/viewer/coding && gsd-browser screenshot /tmp/55-11-coding-surfaces.png` shows real Hierarchy navigator in FilterRail (not placeholder) + LSL strip below canvas
- `gsd-browser navigate http://localhost:5173/viewer/okb && gsd-browser screenshot /tmp/55-11-okb-no-coding-surfaces.png` shows NEITHER surface (gate verification)
- `grep -rnE "console\.(log|warn|error|info|debug)" integrations/unified-viewer/src/panels/coding/HierarchyNavigator.tsx integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts` → 0 matches
</verification>

<success_criteria>
- 2 of 4 D-55-02b coding-only surfaces delivered (Hierarchy + LSL Strip)
- Both gated on `system === 'coding'` at component + mount levels
- `g h` keyboard shortcut wired through the canonical `useKeyboardShortcuts.registerSequence` extension (no ad-hoc per-component sequence state — closes plan-checker W-6)
- Accessibility (`role="tree"`, `aria-level`, keyboard nav on LSL ticks) honored
- FilterRail.tsx NOT modified by this plan (placeholder overwrite only — closes plan-checker B-2 for hierarchy mount)
- ZERO raw `console.*` introduced
</success_criteria>

<output>
Create `.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-11-SUMMARY.md` when done.
</output>
