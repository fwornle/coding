---
phase: 55-unified-viewer-feature-parity-with-vokb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/unified-viewer/src/config/system-endpoints.ts
  - integrations/unified-viewer/src/config/system-endpoints.test.ts
  - integrations/unified-viewer/src/App.tsx
  - integrations/unified-viewer/src/panels/SidePanel.tsx
  - integrations/unified-viewer/src/panels/SidePanel.test.tsx
  - integrations/unified-viewer/src/panels/RcaOpsPanel.tsx
  - integrations/unified-viewer/src/panels/RcaOpsPanel.test.tsx
  - integrations/unified-viewer/tests/e2e/unified-viewer/rca-ingestion.spec.ts
  - integrations/unified-viewer/.env.example
autonomous: true
requirements: [UI-02]
requirements_addressed: [UI-02]
must_haves:
  truths:
    - "`/viewer/cap` route is not matched anywhere; navigating to it falls through to App's not-found path"
    - "OKB tab fetches from `http://localhost:8090` (OKM Express) by default, NOT `localhost:3848` (semantic-analysis)"
    - "Repo contains zero `cc.bmwgroup.net` references in code, tests, fixtures, comments, env examples"
    - "Repo contains zero literal `'cap'` string tied to the System union; `cap` no longer a member of `VALID_SYSTEMS` or `SYSTEM_ENDPOINTS` or `SYSTEM_LABELS` or `SidePanel.TabValue`"
    - "Build (`tsc --noEmit` + `vite build` in integrations/unified-viewer) passes after CAP removal"
    - "Logger discipline: zero raw `console.*` calls introduced in any file this plan touches (per feedback_logger_class.md)"
  artifacts:
    - path: "integrations/unified-viewer/src/config/system-endpoints.ts"
      provides: "System union and endpoint map without `cap`; `okb` defaults to `http://localhost:8090`"
      contains: "type System = 'coding' | 'okb'"
    - path: "integrations/unified-viewer/src/panels/SidePanel.tsx"
      provides: "SidePanel tab map without `rca` tab"
      contains: "type TabValue = 'entity' | 'markdown'"
  key_links:
    - from: "integrations/unified-viewer/src/config/system-endpoints.ts"
      to: "VITE_BACKEND_OKB_URL env var"
      via: "import.meta.env"
      pattern: "VITE_BACKEND_OKB_URL.*8090"
    - from: "integrations/unified-viewer/src/App.tsx"
      to: "isValidSystem guard"
      via: "route param validation"
      pattern: "isValidSystem"
---

<objective>
Land D-55-01a (generalized km-core routing — OKB → localhost:8090), D-55-01b (drop CAP tab entirely — no placeholder), and D-55-01c (purge `cc.bmwgroup.net` from the repo). This plan also removes the RcaOpsPanel + `rca` tab from SidePanel since it was the CAP tab's only consumer.

Purpose: Without this fix, the OKB tab silently shows the coding KG (same backend, mirrored data), and the viewer ships with a hallucinated `cc.bmwgroup.net` URL that produces a misleading "(CORS)" error on every CAP click. This is the SC-1 + SC-2 prerequisite for all downstream UI work.

Output: A 2-system viewer (`coding`, `okb`), `cap` literal eliminated everywhere, `cc.bmwgroup.net` grep returns zero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-CONTEXT.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md
@.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md
@integrations/unified-viewer/src/config/system-endpoints.ts
@integrations/unified-viewer/src/App.tsx
@integrations/unified-viewer/src/panels/SidePanel.tsx
@integrations/unified-viewer/src/panels/RcaOpsPanel.tsx

<interfaces>
Existing Phase 45 System union (before this plan):

```typescript
// src/config/system-endpoints.ts
export type System = 'coding' | 'okb' | 'cap'
export const VALID_SYSTEMS: readonly System[] = ['coding', 'okb', 'cap'] as const
export const SYSTEM_ENDPOINTS: Record<System, string> = {
  coding: import.meta.env.VITE_BACKEND_CODING_URL ?? 'http://localhost:12436',
  okb:    import.meta.env.VITE_BACKEND_OKB_URL    ?? 'http://localhost:3848',
  cap:    import.meta.env.VITE_BACKEND_CAP_URL    ?? 'https://okm.cc.bmwgroup.net',
}
export const SYSTEM_LABELS: Record<System, string> = { coding: 'Coding', okb: 'OKB', cap: 'CAP' }
export function isValidSystem(s: string | undefined): s is System { ... }

// src/panels/SidePanel.tsx (Phase 45)
type TabValue = 'entity' | 'markdown' | 'rca'
```

After this plan:

```typescript
export type System = 'coding' | 'okb'
export const VALID_SYSTEMS: readonly System[] = ['coding', 'okb'] as const
export const SYSTEM_ENDPOINTS: Record<System, string> = {
  coding: import.meta.env.VITE_BACKEND_CODING_URL ?? 'http://localhost:12436',
  okb:    import.meta.env.VITE_BACKEND_OKB_URL    ?? 'http://localhost:8090',
}
export const SYSTEM_LABELS: Record<System, string> = { coding: 'Coding', okb: 'OKB' }

type TabValue = 'entity' | 'markdown'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop `cap` from system-endpoints + retarget okb to OKM Express</name>
  <read_first>
    - integrations/unified-viewer/src/config/system-endpoints.ts (current file)
    - integrations/unified-viewer/src/config/system-endpoints.test.ts (current tests)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ system-endpoints.ts edit pattern)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-CONTEXT.md (D-55-01a + D-55-01b + D-55-01c)
    - integrations/unified-viewer/.env.example
  </read_first>
  <behavior>
    - Test: `VALID_SYSTEMS` length === 2 and contains exactly `'coding'` and `'okb'`
    - Test: `SYSTEM_ENDPOINTS.okb` falls back to `'http://localhost:8090'` when `VITE_BACKEND_OKB_URL` unset
    - Test: `SYSTEM_LABELS` has exactly 2 keys (no `cap`)
    - Test: `isValidSystem('cap')` returns `false`
    - Test: TypeScript compilation does NOT accept a string literal `'cap'` as a `System` (negative type-test via `@ts-expect-error`)
  </behavior>
  <action>
    Edit `integrations/unified-viewer/src/config/system-endpoints.ts` per D-55-01a/b. Specifically:
    (1) narrow the `System` union to `'coding' | 'okb'`;
    (2) remove `cap` from `VALID_SYSTEMS`, `SYSTEM_ENDPOINTS`, `SYSTEM_LABELS`;
    (3) change `okb` endpoint fallback from `http://localhost:3848` to `http://localhost:8090` (OKM Express, per `_work/.../operational-knowledge-management/src/index.ts:17` `OKB_PORT ?? 8090`);
    (4) remove `VITE_BACKEND_CAP_URL` reference;
    (5) update `isValidSystem` body to drop the `'cap'` branch.
    Edit `integrations/unified-viewer/src/config/system-endpoints.test.ts` to rewrite test cases per the new shape per `<behavior>` block — replace any test that asserts `cap` membership with the negative-case assertion that `cap` is rejected. Edit `integrations/unified-viewer/.env.example` to remove the `VITE_BACKEND_CAP_URL` line and to retarget `VITE_BACKEND_OKB_URL` default comment to `http://localhost:8090`.
    Do NOT modify any other file in this task.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/config/system-endpoints.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All system-endpoints.test.ts cases pass. `grep -n "cap" integrations/unified-viewer/src/config/system-endpoints.ts | grep -v '^[0-9]*:.*//\|capability\|capture' | grep -c .` returns 0 (no remaining `'cap'` System literals). `grep -c "8090" integrations/unified-viewer/src/config/system-endpoints.ts` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop `rca` tab from SidePanel; delete RcaOpsPanel</name>
  <read_first>
    - integrations/unified-viewer/src/panels/SidePanel.tsx
    - integrations/unified-viewer/src/panels/SidePanel.test.tsx
    - integrations/unified-viewer/src/panels/RcaOpsPanel.tsx
    - integrations/unified-viewer/src/panels/RcaOpsPanel.test.tsx
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§17 row "SidePanel.tsx (existing)" + §17 row "RcaOpsPanel.tsx (existing)")
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-PATTERNS.md (§ SidePanel.tsx (EDIT — drop RCA tab))
  </read_first>
  <behavior>
    - Test: rendering `<SidePanel selectedTab="entity" />` does NOT render a TabsTrigger with text matching `/rca/i`
    - Test: `TabValue` type accepts `'entity'` and `'markdown'` only (negative `@ts-expect-error` for `'rca'`)
    - Test: SidePanel renders without throwing when an unknown tab value is passed (defensive default)
  </behavior>
  <action>
    Edit `integrations/unified-viewer/src/panels/SidePanel.tsx`: change `type TabValue = 'entity' | 'markdown' | 'rca'` to `type TabValue = 'entity' | 'markdown'`; remove the `<TabsTrigger value="rca">` element, the `<TabsContent value="rca">` block, and the `import RcaOpsPanel` line. Delete `integrations/unified-viewer/src/panels/RcaOpsPanel.tsx` and `integrations/unified-viewer/src/panels/RcaOpsPanel.test.tsx` (use `git rm`).
    Rewrite `SidePanel.test.tsx` cases that previously exercised the `rca` tab — replace with explicit "rca tab is gone" assertions per `<behavior>`. Use the existing `Logger` from `@/lib/logging` if any logging is added (zero raw `console.*` allowed).
    Width-harmonization logic from UI-SPEC §11 is OUT OF SCOPE for this plan — that lands in 55-09 once `EntityIdentityHeader` exists.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/panels/SidePanel.test.tsx --reporter=verbose &amp;&amp; ls src/panels/RcaOpsPanel.tsx 2>/dev/null; test $? -ne 0</automated>
  </verify>
  <done>
    SidePanel.test.tsx all green. `RcaOpsPanel.tsx` file does not exist on disk. `grep -rn "RcaOpsPanel\|TabValue.*rca\|'rca'" integrations/unified-viewer/src/ | grep -v -E '(\.test\.|//)' | grep -c .` returns 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Purge `cc.bmwgroup.net` + obsolete CAP fixtures from tests + env</name>
  <read_first>
    - integrations/unified-viewer/tests/e2e/unified-viewer/rca-ingestion.spec.ts
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-CONTEXT.md (D-55-01c)
    - .planning/phases/55-unified-viewer-feature-parity-with-vokb/55-UI-SPEC.md (§5 forbidden strings)
    - feedback_bmw_ghe_https.md (memory) — bmw.ghe.com not cc.bmwgroup.net
  </read_first>
  <behavior>
    - Test (E2E): a spec asserting `/viewer/cap` route resolves to App's existing not-found / fallback path (NO redirect to `cc.bmwgroup.net`, NO "(CORS)" banner)
    - Test (shell-grep gate): `grep -rIn "cc.bmwgroup.net" integrations/unified-viewer/` returns nothing
    - Test (App.tsx): `App.tsx` is updated so the route map no longer references `cap` — uses `isValidSystem` per existing Phase 45 pattern
  </behavior>
  <action>
    Edit `integrations/unified-viewer/src/App.tsx` to remove any hardcoded `cap` route literal; rely on `isValidSystem` from `system-endpoints.ts` (which now rejects `cap`). Delete or rewrite `integrations/unified-viewer/tests/e2e/unified-viewer/rca-ingestion.spec.ts` — if the spec exists and references CAP-tab assertions, replace with a "55-cap-removal" smoke test that navigates to `/viewer/cap` and asserts the not-found fallback path renders AND the page body does NOT contain `cc.bmwgroup.net` or `(CORS)`. Use `gsd-browser` semantics — DO NOT hand-roll a `node /tmp/foo.mjs` Playwright script (CLAUDE.md `prefer-gsd-browser`). Test uses the existing `tests/e2e/unified-viewer/` Phase 45 harness.
    Final repo-wide grep gate: `grep -rIln "cc.bmwgroup.net" .` (run from repo root; expected exit 1 / no matches in `integrations/unified-viewer/` subtree; if hits elsewhere, decide per file — most likely safe to also purge but record in SUMMARY).
  </action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx playwright test tests/e2e/unified-viewer/rca-ingestion.spec.ts --reporter=line 2>&amp;1 | tail -20 &amp;&amp; ! grep -rIn "cc.bmwgroup.net" integrations/unified-viewer/</automated>
  </verify>
  <done>
    E2E `/viewer/cap` route fall-through passes. `grep -rIn "cc.bmwgroup.net" integrations/unified-viewer/` returns 0 lines. `grep -rIn "VITE_BACKEND_CAP_URL" integrations/unified-viewer/` returns 0 lines. Build green: `cd integrations/unified-viewer &amp;&amp; npx tsc --noEmit`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time env → frontend bundle | `VITE_BACKEND_OKB_URL` env var is baked into the bundle at `vite build`. A malicious env override could redirect OKB tab fetches to an arbitrary URL. Bounded by local-dev trust model (the developer owns their shell env). |
| `/viewer/:system` route → ApiClient base URL | route param validates via `isValidSystem`. Removing `cap` shrinks the attack surface to the 2 known systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01-01 | Information disclosure | OKB tab routing | mitigate | retarget `okb` default to `localhost:8090` (OKM Express, OKM data) — eliminates the Phase 45 bug where OKB silently served coding KG entities (D-55-01a) |
| T-55-01-02 | Tampering | hallucinated `cc.bmwgroup.net` URL → user trains on misleading "(CORS)" error | mitigate | purge `cc.bmwgroup.net` from code/comments/tests/env-examples (D-55-01c); grep gate enforces zero occurrences |
| T-55-01-03 | Spoofing | open redirect via `VITE_BACKEND_OKB_URL` env override | accept | local-dev trust model; env override is documented in `.env.example`. No corporate proxy / production-server use in this phase |

Package legitimacy: no new packages installed in this plan. No `[ASSUMED]`/`[SUS]` packages.
</threat_model>

<verification>
- `cd integrations/unified-viewer && npx vitest run src/config/system-endpoints.test.ts src/panels/SidePanel.test.tsx --reporter=verbose` → all green
- `cd integrations/unified-viewer && npx tsc --noEmit` → exit 0
- `cd integrations/unified-viewer && npx vite build` → exit 0
- `grep -rIn "cc.bmwgroup.net" integrations/unified-viewer/` → no matches
- `grep -rIn "VITE_BACKEND_CAP_URL\|RcaOpsPanel" integrations/unified-viewer/src/` → no matches
- `grep -c "8090" integrations/unified-viewer/src/config/system-endpoints.ts` → 1
- `gsd-browser navigate http://localhost:5173/viewer/cap` → renders App's not-found fallback (NO `(CORS)` banner, NO `cc.bmwgroup.net` string)
- `gsd-browser navigate http://localhost:5173/viewer/okb` → ApiClient base URL is `http://localhost:8090` (verify via DevTools Network tab or `localStorage` inspection)
</verification>

<success_criteria>
- D-55-01a delivered: OKB tab targets OKM Express at `:8090` by default
- D-55-01b delivered: `cap` literal removed from `System` union, `VALID_SYSTEMS`, `SYSTEM_ENDPOINTS`, `SYSTEM_LABELS`, `isValidSystem`, `App.tsx` route map, SidePanel `TabValue`, all test fixtures, `.env.example`
- D-55-01c delivered: `cc.bmwgroup.net` grep returns zero in `integrations/unified-viewer/` subtree
- RcaOpsPanel.tsx + .test.tsx files deleted from disk
- TypeScript strict mode + Vite build still green after changes
- ZERO raw `console.*` introduced (per `feedback_logger_class.md`)
</success_criteria>

<output>
Create `.planning/phases/55-unified-viewer-feature-parity-with-vokb/55-01-SUMMARY.md` when done.
</output>
