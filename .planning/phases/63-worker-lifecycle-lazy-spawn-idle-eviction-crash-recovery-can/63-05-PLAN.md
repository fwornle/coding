---
phase: 63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can
plan: 05
type: execute
wave: 5
depends_on: ["63-01", "63-02", "63-03", "63-04"]
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs
autonomous: false
requirements: [WLIFE-01, WLIFE-02, WLIFE-03, WLIFE-04]
must_haves:
  truths:
    - "SC-1 (WLIFE-01): immediately after proxy start, `ps` shows ZERO `claude -p` worker subprocesses; after the first sonnet claude-code fallback request, EXACTLY ONE exists — proving lazy spawn (verify-only; spawn already implemented in Phase 62)."
    - "SC-2 (WLIFE-02): with a tiny LLM_PROXY_WORKER_IDLE_MS, a spawned worker disappears from `ps` after the idle window, and a subsequent same-key request respawns a fresh worker (new pid)."
    - "SC-3 (WLIFE-03): killing a worker subprocess mid-request surfaces the in-flight request as RETRYABLE, and the logs/`ps` show NO respawn-storm (no tight spawn->crash loop); the next request lazily respawns one fresh worker."
    - "SC-4 (WLIFE-04): aborting an in-flight request SIGTERMs+disposes the worker (gone from `ps`); the next same-key request spawns a NEW pid — the Phase-62 cancel test that HUNG (62-HUMAN-UAT test 6) now PASSES instead of hanging."
    - "Every --live case is gated on `process.argv.includes('--live') || process.env.LLM_PROXY_LIVE === '1'` (NEVER `node --test <file> --live` argv forwarding), and an afterEach/teardown disposes the pool and asserts no orphaned `claude -p` workers remain."
  artifacts:
    - path: /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs
      provides: "Four --live lifecycle cases (cold-start/idle/crash/cancel) proving ROADMAP SC-1..SC-4 against a real `claude -p` subprocess, plus a `ps`-based worker-count helper and a teardown that reaps orphaned workers"
      contains: "claude -p"
  key_links:
    - from: "live test cold-start case"
      to: "ps count of `claude -p` worker subprocesses"
      via: "countClaudeWorkers() helper (pgrep/ps grep for `claude -p`)"
      pattern: "claude -p"
    - from: "live cancel case"
      to: "worker SIGTERMed + new pid on respawn"
      via: "controller.abort() -> WorkerPool _disposeAndDrop -> next complete spawns fresh pid"
      pattern: "abort\\("
---

<objective>
Land the `--live` lifecycle verification suite that proves all four ROADMAP Phase-63 success criteria against a real `claude -p` worker subprocess, and close the one explicitly-missing assertion (WLIFE-01 / SC-1 cold-start) that Phase-62 left only PARTIAL. This is the LIVE proof layer on top of the unit-level mechanisms built in Plans 63-01..63-04: idle eviction (01), EPIPE + generation guard (02), crash cooldown (03), and SIGTERM-on-abort cancellation (04).

Purpose: Plans 63-01..04 prove each mechanism at the UNIT level (claiming WLIFE-02/03/04). NO plan yet covers WLIFE-01, and NO plan proves the four ROADMAP success criteria against a real subprocess via `ps`. This plan adds the dedicated `ps`-based cold-start assertion (D-09, the gap) plus the idle/crash/cancel live cases — most importantly the cancel case, which is the live counterpart to the Phase-62 test that HUNG indefinitely (62-HUMAN-UAT test 6) and must now PASS under the D-01 SIGTERM+dispose behavior from Plan 04.

Output: four new `--live` cases in `tests/integration/worker-pool-live.test.mjs`, a `ps`-based `countClaudeWorkers()` helper, and an `afterEach` teardown that disposes the pool and asserts zero orphaned workers.

Scope note: this plan is VERIFY-ONLY. It re-implements NO lifecycle logic — D-09 is explicit that lazy spawn already exists (Phase 62) and that idle/crash/cancel mechanisms ship in Plans 63-01..04. This plan only OBSERVES them against a live subprocess.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can/63-CONTEXT.md
@.planning/phases/63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can/63-PATTERNS.md
@.planning/phases/63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can/63-01-SUMMARY.md
@.planning/phases/63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can/63-02-SUMMARY.md
@.planning/phases/63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can/63-03-SUMMARY.md
@.planning/phases/63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can/63-04-SUMMARY.md
@.planning/phases/62-worker-pool-core-stream-json-transport/62-HUMAN-UAT.md

<interfaces>
<!-- From proxy-bridge/worker-pool.mjs — the live surface these cases drive. -->
ClaudeWorker:
  new ClaudeWorker(model, systemPrompt, deps={})  // deps.idleMs injectable (Plan 01 added it); default LLM_PROXY_WORKER_IDLE_MS
  .pid                                            // the real `claude -p` subprocess pid (worker-pool.mjs:231)
  async write(content, timeoutMs)                 // returns { content, model, tokens }; spawns the subprocess on first write
  dispose()                                       // stdin.end() + SIGTERM; clears idle timer (Plan 01)
  cancel()                                        // DEAD protocol-interrupt seam — Phase-62-proved to HANG; do NOT use it to cancel
WorkerPool:
  new WorkerPool(opts={})                         // opts.idleMs / opts.crashThreshold / opts.crashWindowMs injectable
  keyFor(body) -> key
  workersByKey: Map<key, ClaudeWorker[]>
  async complete(body, abortSignal, overflowFn)  // CLI-fallback path; lazily spawns; onAbort -> _disposeAndDrop (Plan 04)
  _disposeAndDrop(key, worker)                    // Plan 01: synchronous dispose+splice+prune
  disposeAll()                                    // dispose every worker + clear the map (teardown uses this)

The real worker subprocess is launched as:
  `claude -p --input-format stream-json --output-format stream-json --verbose [strip flags] --model sonnet`
so a `ps`/`pgrep` match on the literal substring `claude -p` counts live worker subprocesses.
</interfaces>

<test_seams>
<!-- From tests/integration/worker-pool-live.test.mjs (Phase 62). -->
- The runner-robust `--live` gate is ALREADY at lines 39-50:
  `const LIVE = process.argv.includes('--live') || process.env.LLM_PROXY_LIVE === '1';`
  then `if (MODE !== 'live') { describe(skipped) } else { describe(live) }`.
  REUSE this exact envelope — append the new cases INSIDE the existing live describe block; do NOT add a second gate.
- Existing live cases (POOL-01 stable-PID, POOL-04 no-spawn-on-direct, GUARD-01 escape hatch, the OLD cancel-seam case, warm sanity) are at lines ~62-135. The OLD cancel-seam case (lines ~107-124) asserts the SAME pid survives an interrupt — that expectation is now INVERTED by D-01 (SIGTERM+respawn => NEW pid). This plan REPLACES/UPDATES that case (Task 3), it does not leave both.
- There is NO `ps`-based helper today — this plan introduces `countClaudeWorkers()` (Task 1) and all four new cases consume it.
- Live prerequisites (header comment lines 16-21): Max OAuth via keychain, network access, installed `claude` CLI. These cases CANNOT run unattended in CI — hence `autonomous: false`.
</test_seams>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ps-based worker-count helper + teardown + SC-1 cold-start (WLIFE-01, verify-only)</name>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs (the `--live` gate lines 39-50; the live describe block; POOL-04 no-spawn-on-direct lines ~73-82 for the `keyFor`/`workersByKey` idiom; the GUARD-01 case lines ~84-105 for how `complete(body, undefined, overflowFn)` drives the CLI-fallback path)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs (`ClaudeWorker.pid` line ~231; the `claude -p --input-format stream-json` spawn argv; `WorkerPool.complete` lazy-spawn; `disposeAll` line ~760)
    - .planning/phases/63-.../63-CONTEXT.md D-09 (cold-start is the EXPLICITLY-MISSING assertion; verify-only — do NOT re-implement spawn) and D-discretion (the `--live` gate must use LLM_PROXY_LIVE=1 / direct `--live`, NEVER `node --test` argv)
    - .planning/phases/62-.../62-HUMAN-UAT.md test 2 (SC-2 was only PARTIAL — "no dedicated cold-start probe assertion"; this task adds it)
  </read_first>
  <action>
    Add a module-scope helper `countClaudeWorkers()` near the top of the live describe block that shells out (via `node:child_process` `execFileSync`/`spawnSync` of `pgrep -f 'claude -p'` with a `ps -ax`+grep fallback for portability) and returns the integer count of live `claude -p` worker subprocesses. Filter the grep so the matcher line itself is not counted (e.g. drop the helper's own pgrep/grep pid). Add an `afterEach(async () => { await pool?.disposeAll(); worker?.dispose(); })`-style teardown that disposes any pool/worker the test created AND asserts (with a short settle wait) that `countClaudeWorkers()` returns 0 — no orphaned subprocess survives a case (the T-63-11 resource-exhaustion mitigation). Then add the SC-1 cold-start case named exactly "SC-1 WLIFE-01 cold-start: zero `claude -p` workers at start, exactly one after first sonnet fallback": (1) assert `countClaudeWorkers() === 0` BEFORE any fallback request (cold pool); (2) issue ONE sonnet claude-code fallback request through `pool.complete({ model: 'sonnet', messages: [{role:'user', content:'say OK'}] }, undefined, overflowFn)` — the same CLI-fallback entry the GUARD-01 case uses, but WITHOUT the disable flag so the real worker spawns; (3) after it returns, assert `countClaudeWorkers() === 1` AND `(pool.workersByKey.get(pool.keyFor(body)) || []).length === 1`. This is VERIFY-ONLY — it observes the Phase-62 lazy-spawn already proven indirectly; it does NOT add or change any spawn logic (D-09). Keep all of this INSIDE the existing `MODE === 'live'` describe block; reuse the gate at lines 39-50 — do not introduce a new gate or a new file.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --test tests/integration/worker-pool-live.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claude -p" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` >= 1 (the `ps`/`pgrep` matcher targets the literal worker argv).
    - `grep -n "countClaudeWorkers" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` shows the helper defined AND invoked in both the SC-1 case and the teardown.
    - `grep -n "afterEach" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` shows a teardown that disposes the pool/worker.
    - `grep -n "SC-1" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` shows the cold-start case.
    - DEFAULT (mock) run is green: `node --test tests/integration/worker-pool-live.test.mjs` exits 0 with the live block skipped (no Max OAuth in CI).
    - LIVE run (operator, manual — `autonomous: false`): `cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && LLM_PROXY_LIVE=1 node --test tests/integration/worker-pool-live.test.mjs` exits 0 with the SC-1 case PASS (0 workers before, exactly 1 after).
  </acceptance_criteria>
  <done>A `countClaudeWorkers()` `ps`-based helper exists, a teardown disposes the pool and asserts zero orphaned `claude -p` workers, and the SC-1 cold-start case asserts 0 workers before the first sonnet fallback and exactly 1 after — closing the Phase-62 PARTIAL (WLIFE-01). Mock mode green; live mode SC-1 PASS.</done>
</task>

<task type="auto">
  <name>Task 2: SC-2 idle-evict (WLIFE-02) + SC-3 crash (WLIFE-03) live cases</name>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs (Task-1's `countClaudeWorkers()` helper + teardown; POOL-01 stable-PID case lines ~62-72 for the `worker.write` + `.pid` idiom)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs (`ClaudeWorker` constructor `deps.idleMs` injection — Plan 01; `.pid`; `dispose()`; the `'exit'` -> `_dropWorker` reap; `WorkerPool` opts `idleMs` injection)
    - .planning/phases/63-.../63-01-SUMMARY.md (the exact `idleMs` injection seam name/shape Plan 01 landed) and 63-02-SUMMARY.md / 63-03-SUMMARY.md (EPIPE-as-crash + cooldown; the RETRYABLE error shape `err.retryable===true`, `err.code==='WORKER_RETRYABLE'`)
    - .planning/phases/63-.../63-CONTEXT.md D-04 (idle default 30 min, knob `LLM_PROXY_WORKER_IDLE_MS`), D-06 (no auto-restart; cooldown is the respawn-storm guard), SC-3 (kill mid-request -> RETRYABLE, no respawn-storm)
  </read_first>
  <action>
    Add two `--live` cases inside the existing live describe block, each using Task-1's `countClaudeWorkers()` and the shared teardown.

    SC-2 idle-evict, named exactly "SC-2 WLIFE-02 idle-evict: worker disappears from ps after idle window, respawns fresh on next request": construct a worker (or pool) with a TINY injected idle timeout (`new ClaudeWorker('sonnet', sys, { idleMs: <small, e.g. 1500> })` — the Plan-01 injection seam) so the test runs in seconds, not 30 min. Drive one `worker.write('say OK')` to spawn it; capture `pidBefore = worker.pid` and assert `countClaudeWorkers() === 1`. Wait past the idle window (a real `setTimeout` slightly longer than `idleMs`). Assert the subprocess is GONE: `countClaudeWorkers() === 0` (the idle timer disposed it -> `'exit'` -> drop). Then issue a fresh request for the SAME (model x prompt) and assert a NEW worker spawned with a DIFFERENT pid (`worker2.pid !== pidBefore`) and `countClaudeWorkers() === 1`. This exercises Plan 63-01's idle timer end-to-end.

    SC-3 crash, named exactly "SC-3 WLIFE-03 crash: kill worker mid-request -> RETRYABLE, no respawn-storm, lazy respawn on next request": spawn a worker and start a longer in-flight request (`const inflight = worker.write('count slowly to one hundred, one per line')`). After a short delay so the request is genuinely in-flight, kill the worker subprocess EXTERNALLY by its pid (`process.kill(worker.pid, 'SIGKILL')`). Assert the in-flight promise rejects RETRYABLE (`await assert.rejects(inflight, (e) => e.retryable === true && e.code === 'WORKER_RETRYABLE')`). Then prove NO respawn-storm: sample `countClaudeWorkers()` a few times over a short window (e.g. every 250ms for ~1.5s) WITHOUT issuing a new request and assert it never exceeds 1 and settles to 0 (the killed worker is reaped and is NOT auto-restarted in a tight loop — D-06: respawn is lazy-on-next-request only). Finally issue ONE new same-key request and assert exactly one fresh worker spawns (`countClaudeWorkers() === 1`, new pid) — lazy respawn. This exercises Plans 63-02/63-03 end-to-end.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --test tests/integration/worker-pool-live.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SC-2 WLIFE-02 idle-evict" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` and `grep -n "SC-3 WLIFE-03 crash" ...` both return the two case names.
    - `grep -n "process.kill" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` shows the SC-3 case kills the worker by pid (mid-request).
    - `grep -n "WORKER_RETRYABLE" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` shows SC-3 asserts the RETRYABLE shape.
    - `grep -n "idleMs" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` shows SC-2 injects a tiny idle window (no 30-min wait).
    - Mock run green: `node --test tests/integration/worker-pool-live.test.mjs` exits 0 (live block skipped).
    - LIVE run (operator, manual): `LLM_PROXY_LIVE=1 node --test tests/integration/worker-pool-live.test.mjs` exits 0 with SC-2 + SC-3 PASS; teardown leaves zero orphaned workers.
  </acceptance_criteria>
  <done>Two `--live` cases prove WLIFE-02 (idle-evict: worker gone from `ps` after a tiny idle window, fresh respawn on next request) and WLIFE-03 (kill mid-request -> RETRYABLE, no respawn-storm sampled via `ps`, lazy respawn). Mock mode green; live mode both PASS.</done>
</task>

<task type="auto">
  <name>Task 3: SC-4 cancel (WLIFE-04) live case — the inverse of the Phase-62 HANG</name>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs (the OLD cancel-seam case lines ~107-124 — it asserts SAME pid survives an interrupt; this task REPLACES that expectation; Task-1 `countClaudeWorkers()` + teardown)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs (`WorkerPool.complete` onAbort -> `_disposeAndDrop` from Plan 04; `ClaudeWorker.cancel()` — the DEAD protocol interrupt that hangs; `.pid`)
    - .planning/phases/63-.../63-04-SUMMARY.md (the D-01 SIGTERM+dispose-on-abort behavior + how `complete`'s abortSignal drives `_disposeAndDrop`)
    - .planning/phases/62-.../62-HUMAN-UAT.md test 6 (the cancel-seam HANG — the protocol interrupt does NOT terminate the real claude stream; THIS is the test that must now PASS via SIGTERM)
    - .planning/phases/63-.../63-CONTEXT.md D-01 (cancel = SIGTERM + respawn; the next same-key request gets a fresh cold worker; the disconnect path NEVER uses `worker.cancel()`)
  </read_first>
  <action>
    REPLACE the Phase-62 cancel-seam case (lines ~107-124, which asserted a surviving SAME pid via `worker.cancel()`) with a new case named exactly "SC-4 WLIFE-04 cancel: abort in-flight -> worker SIGTERMed+gone from ps, next request spawns NEW pid (Phase-62 hang now passes)". Drive the cancel through the REAL abort path, NOT `worker.cancel()`: build an `AbortController`, start an in-flight request via `pool.complete(body, controller.signal, overflowFn)` (sonnet CLI-fallback) WITHOUT awaiting it, wait a short delay so the request is genuinely in-flight against the real stream, capture `pidBefore` from `pool.workersByKey.get(pool.keyFor(body))[0].pid` and assert `countClaudeWorkers() === 1`, then call `controller.abort()`. Assert (a) the aborted `complete` promise settles promptly — it does NOT HANG (wrap in a bounded race against a timeout so a regression to the Phase-62 hang FAILS the test instead of hanging the suite); (b) the worker is SIGTERMed and GONE: after a short settle, `countClaudeWorkers() === 0` AND the key's worker array no longer contains `pidBefore` (D-01 dispose+drop); (c) `worker.cancel()` was NOT the mechanism — the disconnect path uses SIGTERM+dispose (assert no zombie remains, which the protocol-interrupt path provably could not deliver). Then issue a fresh same-key request and assert a NEW worker spawns with a DIFFERENT pid (`!== pidBefore`) and `countClaudeWorkers() === 1` — the cold respawn D-01 guarantees. This case is the live proof that the Phase-62 cancel HANG (62-HUMAN-UAT test 6) is closed: it must PASS, not hang. This is VERIFY-ONLY — it observes Plan 04's behavior; it does NOT change the abort wiring.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --test tests/integration/worker-pool-live.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SC-4 WLIFE-04 cancel" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` returns the case name.
    - `grep -n "controller.abort\|AbortController" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` shows the cancel is driven via the abort signal (NOT `worker.cancel()`).
    - The OLD "worker must survive interrupt — no respawn" assertion is GONE (the SAME-pid expectation is replaced by the NEW-pid expectation): `grep -c "must survive interrupt" /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs` == 0.
    - The case bounds the abort against a timeout so a regression hangs the CASE (FAIL) not the suite: `grep -n "setTimeout\|Promise.race\|signal:" ...` shows a bounded wait around the aborted promise.
    - Mock run green: `node --test tests/integration/worker-pool-live.test.mjs` exits 0 (live block skipped).
    - LIVE run (operator, manual): `LLM_PROXY_LIVE=1 node --test tests/integration/worker-pool-live.test.mjs` exits 0 with SC-4 PASS (worker gone from `ps`, NEW pid on respawn, no hang); teardown leaves zero orphaned workers.
  </acceptance_criteria>
  <done>The Phase-62 cancel-seam case is replaced by a SC-4 case that drives a real client-abort, asserts the worker is SIGTERMed+gone from `ps`, the aborted request settles without hanging, and the next same-key request cold-respawns a NEW pid — proving the Phase-62 cancel HANG (62-HUMAN-UAT test 6) is closed. Mock mode green; live mode SC-4 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live test process ↔ spawned `claude -p` workers | the suite spawns and (on failure) may leave real subprocesses; an aborted/failed case is untrusted to clean up after itself |
| test SIGKILL/abort ↔ host process table | the SC-3/SC-4 cases kill/SIGTERM real subprocesses by pid; a stale/reused pid must not target an unrelated process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-11 | Denial of Service | a `--live` case fails mid-flight and leaves orphaned `claude -p` worker subprocesses (resource/RAM exhaustion across repeated runs) | mitigate | an `afterEach` teardown disposes the pool/worker AND asserts `countClaudeWorkers() === 0` after each case, so a leak fails loudly rather than silently accumulating. Proven by the teardown assertion in Task 1. Residual LOW. |
| T-63-12 | Tampering | SC-3/SC-4 kill/abort targets a wrong pid (pid reuse) and signals an unrelated host process | mitigate | the cases only `process.kill`/abort a pid read DIRECTLY from the just-spawned `worker.pid` (live handle), within the same case, immediately after spawn — no stored/stale pid is signaled. Residual LOW (pid reuse within a multi-millisecond window against a self-spawned child is negligible). |
| T-63-13 | Information Disclosure | `countClaudeWorkers()` greps the host process table and could surface unrelated `claude -p` invocations from a developer's parallel session | accept | the count is a coarse liveness signal for a dedicated live-test run; the operator runbook notes the suite must run without a competing `claude -p` session. No secrets are read; only process-name counts. Residual LOW — rationale: this is a test-only observability helper, not a production path. |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | No new packages — Node builtins only (`node:child_process`, `node:test`, `node:assert`). No install task in this plan; package legitimacy gate N/A. |
</threat_model>

<verification>
- Mock (CI) gate: `cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --test tests/integration/worker-pool-live.test.mjs` exits 0 with the live block SKIPPED (no Max OAuth / network required).
- Live gate (operator, manual — `autonomous: false`): `cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && LLM_PROXY_LIVE=1 node --test tests/integration/worker-pool-live.test.mjs` exits 0 with SC-1, SC-2, SC-3, SC-4 all PASS and the teardown reporting zero orphaned `claude -p` workers.
- `grep -n "SC-1\|SC-2 WLIFE-02\|SC-3 WLIFE-03\|SC-4 WLIFE-04\|countClaudeWorkers\|afterEach" proxy-bridge/../tests/integration/worker-pool-live.test.mjs` confirms all four cases + the helper + the teardown are present.
- The OLD cancel-seam SAME-pid assertion is removed (`grep -c "must survive interrupt" tests/integration/worker-pool-live.test.mjs` == 0).
- The `--live` gate is the runner-robust env/argv form (lines 39-50), NEVER relying on `node --test <file> --live` argv forwarding.
</verification>

<success_criteria>
- WLIFE-01 / SC-1: live `ps`-based cold-start case asserts zero `claude -p` workers at start, exactly one after the first sonnet fallback (closes the Phase-62 PARTIAL; verify-only).
- WLIFE-02 / SC-2: live idle-evict case (tiny idle window) shows the worker gone from `ps` after idle and a fresh respawn on the next request.
- WLIFE-03 / SC-3: live crash case kills a worker mid-request, asserts RETRYABLE, samples `ps` to prove no respawn-storm, then a single lazy respawn.
- WLIFE-04 / SC-4: live cancel case drives a real abort, asserts the worker is SIGTERMed+gone and the next request gets a NEW pid — the Phase-62 cancel HANG now PASSES.
- Teardown asserts zero orphaned workers after every case; mock mode is green in CI; the suite uses the runner-robust `--live` gate.
</success_criteria>

<output>
Create `.planning/phases/63-worker-lifecycle-lazy-spawn-idle-eviction-crash-recovery-can/63-05-SUMMARY.md` when done. Because this plan is `autonomous: false`, the SUMMARY MUST record the operator live-run result (`LLM_PROXY_LIVE=1 node --test tests/integration/worker-pool-live.test.mjs`) for SC-1..SC-4 — the live PASS is the WLIFE-01..04 ROADMAP discharge.
Commit CODE (the test file) to the rapid-llm-proxy repo: `git -C /Users/Q284340/Agentic/_work/rapid-llm-proxy commit ...`. Planning artifacts (SUMMARY) stay in the coding repo.
</output>
</content>
</invoke>
