---
phase: 40-ingest-pipeline-layered-dedup
plan: 07
type: execute
wave: 5
depends_on: ["40-01", "40-02", "40-03", "40-04", "40-05", "40-06"]
files_modified:
  - ~/Agentic/km-core/src/dedup/types.ts
  - ~/Agentic/km-core/src/pipeline/index.ts
  - ~/Agentic/km-core/src/dedup/index.ts
  - ~/Agentic/km-core/src/index.ts
  - ~/Agentic/km-core/package.json
autonomous: true
requirements: [PIPE-01, DEDUP-01]
must_haves:
  truths:
    - "Consumers can import { IngestPipeline, LayeredDeduplicator, JaccardNameMatcher, CosineEmbeddingMatcher, LLMSemanticMatcher } from '@fwornle/km-core' and get the full Phase 40 runtime surface from the root barrel."
    - "Consumers can import types (IngestPipelineOpts, IngestOpts, IngestResult, PhaseCallback, StageName, Extractor, Synthesizer, ExactNameLayer, EmbeddingLayer, LLMSemanticLayer, MatchResult, DedupResult, EmbeddingClient, LLMClient) from '@fwornle/km-core' via the root barrel."
    - "Sub-path imports work: import { IngestPipeline } from '@fwornle/km-core/pipeline' AND import { LayeredDeduplicator } from '@fwornle/km-core/dedup' (per Phase 38 package.json exports-map precedent)."
    - "src/dedup/types.ts re-exports EmbeddingClient + LLMClient from the matcher files so consumers reaching the dedup sub-barrel get a complete type surface."
    - "npm run build is clean (zero TS errors); npm test is green (full suite)."
    - "Phase 38 ./ontology sub-path export still resolves — root barrel + sub-paths coexist."
  artifacts:
    - path: "~/Agentic/km-core/src/dedup/types.ts"
      provides: "Plan 07 amendment — adds EmbeddingClient + LLMClient re-exports (now that Plans 03 + 04 created the source files)"
      contains: "export type { EmbeddingClient } from './CosineEmbeddingMatcher.js'"
    - path: "~/Agentic/km-core/src/pipeline/index.ts"
      provides: "Pipeline sub-barrel (~15 lines per RESEARCH size estimate)"
      contains: "export { IngestPipeline } from './IngestPipeline.js'"
    - path: "~/Agentic/km-core/src/dedup/index.ts"
      provides: "Dedup sub-barrel (~15 lines)"
      contains: "export { LayeredDeduplicator, JaccardNameMatcher, CosineEmbeddingMatcher, LLMSemanticMatcher }"
    - path: "~/Agentic/km-core/src/index.ts"
      provides: "Root barrel — append-only Phase 40 block (mirrors Phase 39 append block at end of file)"
      contains: "export { IngestPipeline } from './pipeline/IngestPipeline.js'"
    - path: "~/Agentic/km-core/package.json"
      provides: "Extended exports map with ./pipeline + ./dedup sub-paths"
      contains: "./pipeline"
  key_links:
    - from: "src/index.ts"
      to: "src/pipeline/IngestPipeline.js + src/dedup/*.js"
      via: "append-only export block"
      pattern: "export.*from './(pipeline|dedup)/"
    - from: "package.json exports map"
      to: "dist/(pipeline|dedup)/index.{js,d.ts}"
      via: "sub-path entries"
      pattern: "\\./(pipeline|dedup)"
---

<objective>
Land the public-API barrel for Phase 40: amend `src/dedup/types.ts` to re-export `EmbeddingClient` + `LLMClient` (created by Plans 03 + 04 — couldn't be re-exported in Plan 01 because the source files didn't exist yet); create the two sub-barrels `src/pipeline/index.ts` + `src/dedup/index.ts` (mirrors the Phase 38 `src/ontology/index.ts` + Phase 39 `src/segments/index.ts` template); append the Phase 40 block to the root `src/index.ts` (after Phase 39's backfill block); and extend `package.json`'s `exports` map with `./pipeline` + `./dedup` sub-path entries (mirrors Phase 38 Plan 06's `./ontology` precedent).

Final green gate: `npm run build` + `npm test` + external-consumer smoke compile (one-line import probe via tmpdir + tsc — mirrors Phase 38 Plan 03 FLAG-1 verification). When this plan completes, Phase 40 is shippable: Phase 41/42/43 consumers can `import { IngestPipeline, LayeredDeduplicator } from '@fwornle/km-core'` and consume the full surface.

Output: 4 modified files + 1 new package.json `exports` entry pair + green build + green test + green external smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-CONTEXT.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-06-SUMMARY.md
@~/Agentic/km-core/src/index.ts
@~/Agentic/km-core/src/ontology/index.ts
@~/Agentic/km-core/src/segments/index.ts
@~/Agentic/km-core/package.json

<interfaces>
Current ~/Agentic/km-core/src/index.ts ends at the Phase 39 backfill block:
  export { backfillEntityDataModel } from './backfill/index.js';
  export type { BackfillOptions, BackfillResolver, BackfillResult } from './backfill/index.js';
  // ← Phase 40 append block goes HERE (after this)

Current ~/Agentic/km-core/package.json exports map (verified at planning time):
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./ontology": { "types": "./dist/ontology/index.d.ts", "import": "./dist/ontology/index.js" }
  }
  // ← Phase 40 adds "./pipeline" + "./dedup" entries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend src/dedup/types.ts — re-export EmbeddingClient + LLMClient</name>
  <files>~/Agentic/km-core/src/dedup/types.ts</files>
  <read_first>
    - ~/Agentic/km-core/src/dedup/types.ts (current state from Plan 01 — find the TODO comment placed at the bottom of the file)
    - ~/Agentic/km-core/src/dedup/CosineEmbeddingMatcher.ts (Plan 03 — confirm `export interface EmbeddingClient` exists)
    - ~/Agentic/km-core/src/dedup/LLMSemanticMatcher.ts (Plan 04 — confirm `export interface LLMClient` exists)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md offset 511-516 ("Client interfaces re-exported here" spec)
  </read_first>
  <action>
    Edit `~/Agentic/km-core/src/dedup/types.ts`:
    - Locate the TODO comment from Plan 01 (line near bottom: `// Phase 40 Plan 07 will add re-exports: ...`).
    - Replace the TODO with two active type re-export lines using the patterns: `export type {{ EmbeddingClient }} from './CosineEmbeddingMatcher.js';` and `export type {{ LLMClient }} from './LLMSemanticMatcher.js';` (replace double braces with single braces — escaping for plan-text only).
    - Per PATTERNS.md offset 511-516: these are TYPE-ONLY re-exports (the runtime classes stay in their own files; only the interface types come through this barrel).
    - Update the file header comment to remove the "deferred to Plan 07" note.

    Verify the file still compiles (`npx tsc --noEmit`) and the EmbeddingClient + LLMClient types can be imported from `./types.js`.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && grep -cE "export type \\{ EmbeddingClient \\} from ['\"]\\./CosineEmbeddingMatcher\\.js['\"]" src/dedup/types.ts | grep -q "^1$" &amp;&amp; grep -cE "export type \\{ LLMClient \\} from ['\"]\\./LLMSemanticMatcher\\.js['\"]" src/dedup/types.ts | grep -q "^1$" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -qE "error TS" &amp;&amp; echo "TYPES FAIL" || echo "TYPES OK"</automated>
  </verify>
  <acceptance_criteria>
    - Re-export line for EmbeddingClient present (one match per grep above).
    - Re-export line for LLMClient present.
    - TODO comment removed: `grep -cE "(TODO\\(40-07\\)|Plan 07 will add)" ~/Agentic/km-core/src/dedup/types.ts` returns `0`.
    - `npx tsc --noEmit` from km-core returns exit 0.
  </acceptance_criteria>
  <done>EmbeddingClient + LLMClient now re-export-accessible via src/dedup/types.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/pipeline/index.ts + src/dedup/index.ts sub-barrels</name>
  <files>
    ~/Agentic/km-core/src/pipeline/index.ts
    ~/Agentic/km-core/src/dedup/index.ts
  </files>
  <read_first>
    - ~/Agentic/km-core/src/ontology/index.ts (Phase 38 sub-barrel — exact template; ~20 lines with header comment + classes + types)
    - ~/Agentic/km-core/src/segments/index.ts (Phase 39 sub-barrel — alternative example)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md offset 166-194 ("src/pipeline/index.ts" section — verbatim shape)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md offset 520-546 ("src/dedup/index.ts" section — verbatim shape)
  </read_first>
  <action>
    File 1 — `~/Agentic/km-core/src/pipeline/index.ts` per PATTERNS.md offset 170-193: header comment describes the sub-path import contract (`@fwornle/km-core/pipeline`) and that root-barrel + sub-path imports resolve to the same module. Body: one runtime re-export of `IngestPipeline` from `./IngestPipeline.js` plus a `export type { ... }` block covering all 7 public type names from `./types.js`: IngestPipelineOpts, IngestOpts, IngestResult, PhaseCallback, StageName, Extractor, Synthesizer.

    File 2 — `~/Agentic/km-core/src/dedup/index.ts` per PATTERNS.md offset 524-546: header comment describes `@fwornle/km-core/dedup` sub-path. Body: 4 runtime re-exports (LayeredDeduplicator from `./LayeredDeduplicator.js`; JaccardNameMatcher from `./JaccardNameMatcher.js`; CosineEmbeddingMatcher from `./CosineEmbeddingMatcher.js`; LLMSemanticMatcher from `./LLMSemanticMatcher.js`) plus a `export type { ... }` block covering all 7 public type names from `./types.js`: ExactNameLayer, EmbeddingLayer, LLMSemanticLayer, MatchResult, DedupResult, EmbeddingClient, LLMClient.

    Both files: relative imports use `.js` suffix per CF-D06. No console.*. No runtime logic beyond the re-exports.
  </action>
  <verify>
    <automated>test -f ~/Agentic/km-core/src/pipeline/index.ts &amp;&amp; test -f ~/Agentic/km-core/src/dedup/index.ts &amp;&amp; grep -cE "export \\{ IngestPipeline \\} from ['\"]\\./IngestPipeline\\.js['\"]" ~/Agentic/km-core/src/pipeline/index.ts | grep -q "^1$" &amp;&amp; grep -cE "export \\{ LayeredDeduplicator \\} from ['\"]\\./LayeredDeduplicator\\.js['\"]" ~/Agentic/km-core/src/dedup/index.ts | grep -q "^1$" &amp;&amp; cd ~/Agentic/km-core &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -qE "error TS" &amp;&amp; echo "BARREL FAIL" || echo "BARREL OK"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist.
    - `src/pipeline/index.ts`: re-exports `IngestPipeline` (1) + 7 type re-exports (IngestPipelineOpts/IngestOpts/IngestResult/PhaseCallback/StageName/Extractor/Synthesizer all grep-verifiable).
    - `src/dedup/index.ts`: re-exports 4 classes (LayeredDeduplicator/JaccardNameMatcher/CosineEmbeddingMatcher/LLMSemanticMatcher) + 7 type re-exports (ExactNameLayer/EmbeddingLayer/LLMSemanticLayer/MatchResult/DedupResult/EmbeddingClient/LLMClient).
    - Both files compile clean under `npx tsc --noEmit`.
    - All relative imports use `.js` suffix: `grep -E "^export.*from '\\.\\." ~/Agentic/km-core/src/pipeline/index.ts | grep -vE "\\.js';\\s*$" | wc -l` returns 0; same for dedup/index.ts.
    - Zero console.* in either file.
  </acceptance_criteria>
  <done>Sub-barrels in place; sub-path consumer imports will work once package.json is updated (Task 4).</done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 40 block to root src/index.ts</name>
  <files>~/Agentic/km-core/src/index.ts</files>
  <read_first>
    - ~/Agentic/km-core/src/index.ts (current state — Phase 39 block ends near end of file with backfillEntityDataModel exports)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md offset 551-589 ("src/index.ts (modified — append-only root barrel)" section — verbatim append block)
  </read_first>
  <action>
    Append the Phase 40 block AFTER the existing Phase 39 `backfillEntityDataModel` exports (current last lines of the file). DO NOT modify any prior lines.

    Append block per PATTERNS.md offset 556-589 (verbatim text — copy from PATTERNS.md):

    Block contents (high-level):
    1. Phase 40 header comment (8 lines) describing PIPE-01 + DEDUP-01 — Pipeline threads ProvenanceStamp, supersession via Phase 39 putEntity, layers caller-pluggable, short-circuit per D-44, CR-01 legacy-id widening preserved.
    2. `export {{ IngestPipeline }} from './pipeline/IngestPipeline.js';` (single braces in the final file)
    3. `export type {{ IngestPipelineOpts, IngestOpts, IngestResult, PhaseCallback, StageName, Extractor, Synthesizer }} from './pipeline/types.js';`
    4. 4 runtime re-exports: LayeredDeduplicator, JaccardNameMatcher, CosineEmbeddingMatcher, LLMSemanticMatcher (each from `./dedup/<name>.js`).
    5. `export type {{ ExactNameLayer, EmbeddingLayer, LLMSemanticLayer, MatchResult, DedupResult, EmbeddingClient, LLMClient }} from './dedup/types.js';`

    Verify nothing else in the file was touched — Phase 39 block must be byte-identical before/after this task. Use a snapshot check: `grep -c "backfillEntityDataModel" src/index.ts` must remain 1 before AND after.

    Compile: `npx tsc --noEmit` clean.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core &amp;&amp; grep -cE "export \\{ IngestPipeline \\} from ['\"]\\./pipeline/IngestPipeline\\.js['\"]" src/index.ts | grep -q "^1$" &amp;&amp; grep -cE "export \\{ LayeredDeduplicator \\} from ['\"]\\./dedup/LayeredDeduplicator\\.js['\"]" src/index.ts | grep -q "^1$" &amp;&amp; grep -cE "export \\{ JaccardNameMatcher \\} from ['\"]\\./dedup/JaccardNameMatcher\\.js['\"]" src/index.ts | grep -q "^1$" &amp;&amp; grep -cE "export \\{ CosineEmbeddingMatcher \\} from ['\"]\\./dedup/CosineEmbeddingMatcher\\.js['\"]" src/index.ts | grep -q "^1$" &amp;&amp; grep -cE "export \\{ LLMSemanticMatcher \\} from ['\"]\\./dedup/LLMSemanticMatcher\\.js['\"]" src/index.ts | grep -q "^1$" &amp;&amp; grep -c "backfillEntityDataModel" src/index.ts | grep -q "^1$" &amp;&amp; grep -c "OntologyRegistry" src/index.ts | grep -qE "^[1-9]" &amp;&amp; grep -c "mintEntityId" src/index.ts | grep -q "^1$" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -qE "error TS" &amp;&amp; echo "ROOT FAIL" || echo "ROOT OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 Phase 40 runtime classes are re-exported from the root barrel.
    - All 14 Phase 40 public type names are re-exported.
    - Phase 39's `backfillEntityDataModel` export is STILL present.
    - Phase 38's `OntologyRegistry` export is STILL present.
    - Phase 37's `mintEntityId` export is STILL present.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Root barrel append-only block in place; all prior phase exports preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Extend package.json exports map with ./pipeline + ./dedup sub-paths</name>
  <files>~/Agentic/km-core/package.json</files>
  <read_first>
    - ~/Agentic/km-core/package.json (current exports map — has `.` and `./ontology` entries)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md offset 990-997 ("Pattern G: Sub-Barrel + Sub-Path Export" — Phase 38 precedent)
  </read_first>
  <action>
    Edit `~/Agentic/km-core/package.json`. In the `"exports"` object, add two new entries AFTER the existing `"./ontology"` entry. The new entries follow the exact same shape as `./ontology`:

    - `./pipeline` entry: `types` → `./dist/pipeline/index.d.ts`; `import` → `./dist/pipeline/index.js`.
    - `./dedup` entry: `types` → `./dist/dedup/index.d.ts`; `import` → `./dist/dedup/index.js`.

    Final `exports` shape (4 entries total): `.`, `./ontology`, `./pipeline`, `./dedup`.

    Do NOT modify any other field in package.json (name, version, dependencies, etc.). Validate JSON parses cleanly via a node one-liner that reads the file and asserts all 4 export entries exist + the root + ontology entries are unchanged.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core &amp;&amp; node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); if (!p.exports['./pipeline']) throw new Error('missing pipeline'); if (!p.exports['./dedup']) throw new Error('missing dedup'); if (!p.exports['./ontology']) throw new Error('lost ontology'); if (!p.exports['.']) throw new Error('lost root'); if (p.exports['./pipeline'].types !== './dist/pipeline/index.d.ts') throw new Error('bad types path'); console.log('EXPORTS OK')"</automated>
  </verify>
  <acceptance_criteria>
    - JSON parses cleanly.
    - `package.json` `exports` map has all 4 entries: `.`, `./ontology`, `./pipeline`, `./dedup`.
    - `./pipeline` entry has both `types` (`./dist/pipeline/index.d.ts`) and `import` (`./dist/pipeline/index.js`) fields.
    - `./dedup` entry has both `types` (`./dist/dedup/index.d.ts`) and `import` (`./dist/dedup/index.js`) fields.
    - `./ontology` entry is byte-identical to its pre-edit state.
    - Root `.` entry is byte-identical to its pre-edit state.
  </acceptance_criteria>
  <done>package.json exports map extended; consumers can resolve all 4 sub-paths.</done>
</task>

<task type="auto">
  <name>Task 5: Build + full-suite test + external smoke compile (Phase 40 final gate)</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md (Submodule build pipeline — `npm run build` is MANDATORY after src/ changes per RESEARCH Pitfall 6)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md (Plan 38-03 FLAG-1 external smoke pattern — tmpdir + npm install + tsc compile of import probe)
  </read_first>
  <action>
    Step 1 — Build km-core: `cd ~/Agentic/km-core && npm run build`. Expect zero `error TS` lines. Confirm new dist artifacts exist:
    - `~/Agentic/km-core/dist/pipeline/index.js` + `.d.ts`
    - `~/Agentic/km-core/dist/pipeline/IngestPipeline.js` + `.d.ts`
    - `~/Agentic/km-core/dist/dedup/index.js` + `.d.ts`
    - `~/Agentic/km-core/dist/dedup/LayeredDeduplicator.js` + `.d.ts`
    - `~/Agentic/km-core/dist/dedup/JaccardNameMatcher.js` + `.d.ts`
    - `~/Agentic/km-core/dist/dedup/CosineEmbeddingMatcher.js` + `.d.ts`
    - `~/Agentic/km-core/dist/dedup/LLMSemanticMatcher.js` + `.d.ts`

    Step 2 — Full test suite: `cd ~/Agentic/km-core && npm test`. Expect all tests green (estimated ~95-100 tests = 56 Phase 38 baseline + Phase 39 adds + Phase 40 adds across Plans 02-06).

    Step 3 — External smoke compile (mirrors Phase 38 Plan 38-03 FLAG-1 verification):
      a. Create a tmpdir: `TMPDIR=$(mktemp -d)`.
      b. Inside, create a `package.json` with `"type": "module"` + `"dependencies": { "@fwornle/km-core": "file:/Users/Q284340/Agentic/km-core" }` + dev deps `@types/node` + `typescript`.
      c. Create a `tsconfig.json` with NodeNext + ES2022 + `strict: true`.
      d. Create 3 probe files:
         - `probe-root.ts`: imports both runtime classes and types from `'@fwornle/km-core'` root barrel; instantiates `new JaccardNameMatcher()` and asserts threshold.
         - `probe-pipeline.ts`: imports from `'@fwornle/km-core/pipeline'` sub-path; instantiates `new IngestPipeline(/*store stub*/, { extractor, deduplicator, synthesizer })`.
         - `probe-dedup.ts`: imports from `'@fwornle/km-core/dedup'` sub-path; instantiates each of the 4 classes; asserts each class is a constructor.
      e. Run `npm install` in tmpdir (installs km-core via file:// link + downloads ts/types).
      f. Run `npx tsc --noEmit` — expect exit 0 (all 3 probes compile clean).
      g. Cleanup: `rm -rf $TMPDIR`.

    Step 4 — Final phase verification: confirm all 4 ROADMAP Phase 40 success criteria are satisfied by enumerating the test names that prove each one (see Output spec for the SUMMARY format).
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core &amp;&amp; npm run build 2>&amp;1 | grep -qE "error TS" &amp;&amp; echo "BUILD FAILED" || echo "BUILD OK" &amp;&amp; test -f dist/pipeline/index.js &amp;&amp; test -f dist/pipeline/IngestPipeline.js &amp;&amp; test -f dist/dedup/index.js &amp;&amp; test -f dist/dedup/LayeredDeduplicator.js &amp;&amp; test -f dist/dedup/JaccardNameMatcher.js &amp;&amp; test -f dist/dedup/CosineEmbeddingMatcher.js &amp;&amp; test -f dist/dedup/LLMSemanticMatcher.js &amp;&amp; echo "DIST OK" &amp;&amp; cd ~/Agentic/km-core &amp;&amp; npm test 2>&amp;1 | tee /tmp/40-07-final-test.log | grep -E "Test Files|Tests.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 with zero `error TS` lines.
    - All 7 new dist artifacts exist (5 runtime modules + 2 sub-barrel index files for pipeline + dedup).
    - `npm test` exit 0 — full suite green. Capture `Test Files / Tests` totals for the SUMMARY.
    - External smoke compile: all 3 probe files in tmpdir compile clean under `tsc --noEmit`.
    - Probe via root barrel works: `node --input-type=module -e "import { IngestPipeline, LayeredDeduplicator, JaccardNameMatcher } from '@fwornle/km-core/dist/index.js'; new JaccardNameMatcher(); console.log('ROOT IMPORT OK')"` (executed from km-core directory; the explicit dist path bypasses the exports map for this sanity check).
    - Probe via pipeline sub-path works: `cd ~/Agentic/km-core && node --input-type=module -e "import { IngestPipeline } from './dist/pipeline/index.js'; console.log('PIPELINE SUBPATH OK')"`.
    - Probe via dedup sub-path works: `cd ~/Agentic/km-core && node --input-type=module -e "import { LayeredDeduplicator, JaccardNameMatcher } from './dist/dedup/index.js'; new JaccardNameMatcher(); console.log('DEDUP SUBPATH OK')"`.
  </acceptance_criteria>
  <done>Phase 40 is shippable: build green, tests green, external consumers can import via root barrel OR sub-paths; all 4 ROADMAP SCs traced to passing tests.</done>
</task>

</tasks>

<verification>
- Plan goal: Land the public-API barrel so Phase 41/42/43 can begin consuming Phase 40 surface. After this plan: `import { IngestPipeline, LayeredDeduplicator, JaccardNameMatcher, CosineEmbeddingMatcher, LLMSemanticMatcher } from '@fwornle/km-core'` resolves; all 14 public types resolve; sub-paths `./pipeline` + `./dedup` resolve.
- Phase 40 verification: ROADMAP SC#1 (4-stage framework) — Plan 06 pipeline.test.ts. SC#2 (synthetic collision catch) — Plan 06 layered-dedup-collision-catch.test.ts. SC#3 (skipStages cadence) — Plan 06 pipeline.test.ts skipStages tests. SC#4 (no duplicated dedup algorithm) — fully discharged at end of Phase 42, partially proven by Plan 06's use of all 3 km-core layers in a single integration test.
</verification>

<success_criteria>
- 4 modified files + 1 package.json exports extension landed.
- `npm run build` green; all 7 new dist artifacts exist.
- `npm test` green; full Phase 40 test suite of ~40 added tests passes.
- External smoke compile green: root barrel + both sub-paths work for external consumers.
- All 4 ROADMAP Phase 40 SCs traced to test names in the SUMMARY.
- Phase 39 baseline preserved: backfillEntityDataModel + OntologyRegistry + mintEntityId all still exported from the root barrel.
</success_criteria>

<output>
Create `.planning/phases/40-ingest-pipeline-layered-dedup/40-07-SUMMARY.md` when done. Summary must:
  - Confirm Tasks 1-5 all green.
  - Show the final `package.json` exports map (4 entries: `.`, `./ontology`, `./pipeline`, `./dedup`).
  - Cite the external smoke compile result for all 3 probe files.
  - Map each of the 4 ROADMAP SCs to the test name(s) that verify it:
    - SC#1 — `tests/unit/pipeline.test.ts` 'stage order', 'IngestResult shape' + Plan 01 type contracts (compile-time verification).
    - SC#2 — `tests/integration/layered-dedup-collision-catch.test.ts` 'SC#2: synthetic 3-collision batch'.
    - SC#3 — `tests/unit/pipeline.test.ts` 'skipStages synthesize', 'skipStages extract contract', 'runStage synthesize'.
    - SC#4 — partially via Plan 06's composition; full discharge at end of Phase 42 when B's local Jaccard copy is deleted.
  - State the final test count (e.g., "Phase 40 complete: NN tests passing, +M added relative to Phase 39 baseline").
  - Note any deviations from PATTERNS.md (e.g., if any sub-barrel re-export had to be omitted because the target type wasn't actually exported).
  - State explicitly: "Phase 40 ready for `/gsd:verify-phase 40`."
</output>
