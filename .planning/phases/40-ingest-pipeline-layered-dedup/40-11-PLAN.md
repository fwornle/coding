---
phase: 40-ingest-pipeline-layered-dedup
plan: 11
type: execute
wave: 7
depends_on: ["40-07"]
files_modified:
  - ~/Agentic/km-core/examples/custom-adapter.ts
  - ~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts
autonomous: true
gap_closure: true
requirements: [PIPE-01, DEDUP-01]
must_haves:
  truths:
    - "SC#1 closed in code: ~/Agentic/km-core/examples/custom-adapter.ts exists, compiles against the public-API barrel (NO relative imports into internal src/ files), and runs to completion. SC#1 success criterion ('a developer can wire a new ingest adapter into KM-Core by implementing the four named stage interfaces and registering it — no fork of the pipeline code required') is now provable by inspection + execution."
    - "Header comment marker present: 'Reference adapter for SC#1 — demonstrates Phase 40 pluggable stage interfaces with no fork required.' (gap_source line directly cited)."
    - "Implements ALL 4 stage interfaces: Extractor + Synthesizer + the 3 dedup layers (ExactNameLayer or LayeredDeduplicator) — proves the full PIPE-01 + DEDUP-01 surface compiles + runs end-to-end."
    - "Pipeline constructed via the OPTIONS-OBJECT shape per D-42 + invoked via `pipeline.ingest(text, { provenance, domain })` per D-43."
    - "Companion test (custom-adapter-example.test.ts) confirms the example compiles, the IngestPipeline can be instantiated with the example's stage impls, and `await pipeline.ingest(...)` returns an IngestResult with extractedCount > 0."
    - "Imports come ONLY from `@fwornle/km-core` (root barrel) and/or `@fwornle/km-core/pipeline` + `@fwornle/km-core/dedup` (sub-paths) — proves the SC#1 'no fork of the pipeline code required' clause via the package.json exports map (Plan 40-07's surface)."
  artifacts:
    - path: "~/Agentic/km-core/examples/custom-adapter.ts"
      provides: "SC#1 reference adapter — a working ~80-100 line consumer demonstrating the 4-stage interface implementation against the public API"
      contains: "Extractor; Synthesizer; IngestPipeline; pipeline.ingest"
    - path: "~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts"
      provides: "Test that the example file compiles + runs end-to-end and the pipeline produces a non-empty IngestResult"
      contains: "custom-adapter example"
  key_links:
    - from: "~/Agentic/km-core/examples/custom-adapter.ts"
      to: "@fwornle/km-core (public-API barrel — Plan 40-07)"
      via: "import { IngestPipeline, LayeredDeduplicator, JaccardNameMatcher, ... } from '@fwornle/km-core'"
      pattern: "from '@fwornle/km-core"
    - from: "~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts"
      to: "~/Agentic/km-core/examples/custom-adapter.ts"
      via: "imports the example's exported factory + runs it"
      pattern: "from '../../examples/custom-adapter"
---

<objective>
Close the SC#1 human-verification gap surfaced by `40-VERIFICATION.md` (line 49-51): write the 30-80 line example adapter at `~/Agentic/km-core/examples/custom-adapter.ts` that the verifier requested. Per VERIFICATION.md line 49: *"Build a 30-line example app at `~/Agentic/km-core/examples/custom-adapter.ts` that implements all 4 stage interfaces against an in-memory store and runs successfully."* Per the gap_source: the example should:

1. Implement `Extractor` + `Synthesizer` (minimal stubs prove the contracts compile).
2. Wire `EmbeddingClient` + `LLMClient` (minimal mocks; show the 3-layer dedup wiring with thresholds — Jaccard 0.85, Cosine 0.90, LLM 0.70).
3. Construct an `IngestPipeline` via the D-42 options-object ctor + invoke `pipeline.ingest(text, { provenance, domain })`.
4. Include the header comment EXACTLY as the gap_source specified: *"Reference adapter for SC#1 — demonstrates Phase 40's pluggable stage interfaces with no fork required."*

A companion integration test (`tests/integration/custom-adapter-example.test.ts`) exercises the example end-to-end — proves it compiles, the IngestPipeline can be constructed with the example's stage impls, and `pipeline.ingest()` returns an `IngestResult` with `extractedCount > 0`. The companion test is the GREEN-only proof that the file works; there's no useful RED state here (a missing file is just a missing file, not a behavioral assertion).

**Crucial — import discipline.** The example MUST import ONLY from `@fwornle/km-core` (root barrel) and/or its sub-paths `@fwornle/km-core/pipeline` + `@fwornle/km-core/dedup`. NO `from '../src/...'` paths anywhere — that would defeat the SC#1 "no fork of the pipeline code required" clause (a downstream consumer never reaches into KM-Core's source tree). The package's `exports` map (Plan 40-07's surface) is the only legal entry point. The companion test, however, may import from `../../examples/custom-adapter.ts` (relative within the same repo) because it's part of the km-core monorepo, not a downstream consumer.

The example pipes through a real `GraphKMStore` instance (use the same Ctx + makeFixture pattern as the existing integration tests). The minimal stubs are: an `Extractor` that returns a hardcoded 2-entity array (so the test can assert extractedCount = 2); a `Synthesizer` that just records the survivor ids it received (a `vi.fn()` is fine but since this is a runtime example, use a regular function that pushes to a local array); minimal `EmbeddingClient` returning a fixed unit vector; minimal `LLMClient` returning the canonical empty-matches response. The dedup layers are wired in the canonical D-44 declared order.

Cross-repo workflow: code edits in `~/Agentic/km-core/` with `feat(40-11): ...` + `test(40-11): ...`. SUMMARY in coding/ worktree with `docs(40-11): ...`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-CONTEXT.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-VERIFICATION.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-RESEARCH.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-VALIDATION.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-07-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Public surface this example wires against — all importable from the root barrel
     OR the two sub-paths @fwornle/km-core/pipeline + /dedup. Plan 40-07 landed
     these as the package exports map. -->

From '@fwornle/km-core' (or '@fwornle/km-core/pipeline'):
  export class IngestPipeline {
    constructor(store: GraphKMStore, opts: IngestPipelineOpts);
    ingest(text: string, opts: IngestOpts): Promise<IngestResult>;
    runStage(name: 'extract', input: string, opts?: { domain?: string }): Promise<Entity[]>;  // post-CR-04
    // ... 3 other overloads
  }
  export interface Extractor { extract(text: string, domain?: string): Promise<Entity[]>; }
  export interface Synthesizer { synthesize(survivorIds: EntityId[], opts: { provenance: ProvenanceStamp }): Promise<void>; }
  export interface IngestPipelineOpts { extractor: Extractor; deduplicator: Deduplicator; synthesizer: Synthesizer; onPhase?: PhaseCallback; }
  export interface IngestOpts { provenance: ProvenanceStamp; skipStages?: StageName[]; domain?: string; }
  export interface IngestResult { extractedCount; mergedCount; storedCount; skippedCount; droppedCount; durations: {...}; skippedStages: StageName[]; }

From '@fwornle/km-core' (or '@fwornle/km-core/dedup'):
  export class LayeredDeduplicator {
    constructor(opts: { exactName?: ExactNameLayer; embedding?: EmbeddingLayer; llmSemantic?: LLMSemanticLayer; shortCircuit?: boolean });
    dedup(entity: Entity, candidates: Entity[]): Promise<DedupResult>;
  }
  export class JaccardNameMatcher { constructor(opts?: { threshold?: number }); }
  export class CosineEmbeddingMatcher { constructor(opts: { client: EmbeddingClient; threshold?: number; textOf?: (e: Entity) => string }); }
  export class LLMSemanticMatcher { constructor(opts: { client: LLMClient; threshold?: number; onError?: 'skip' | 'throw'; ... }); }
  export interface EmbeddingClient { embed(text: string): Promise<Float32Array | number[]>; }
  export interface LLMClient { complete(req: { messages; taskType?; responseFormat?; timeout?; }): Promise<{ content: string }>; }

From '@fwornle/km-core' (Phase 37 + 39 core types):
  export class GraphKMStore { ... }
  export interface Entity { id: EntityId; name: string; ontologyClass?: string; entityType: string; ... }
  export interface ProvenanceStamp { provider; model; runId; timestamp; }
  export function mintEntityId(): EntityId;

Example file structure target (~80-100 lines including JSDoc):

  /**
   * Reference adapter for SC#1 — demonstrates Phase 40's pluggable stage
   * interfaces with no fork required.
   *
   * Implements all four stage interfaces (Extractor + Synthesizer + 3 dedup
   * layers via minimal mock clients) and wires them into an IngestPipeline.
   * Run as a smoke test — no Docker, no external services required.
   *
   * Import-discipline: this file imports ONLY from `@fwornle/km-core` (the
   * public-API barrel landed by Plan 40-07). A downstream consumer in a
   * different repo would do the same `import { ... } from '@fwornle/km-core'`
   * — no relative paths into the km-core source tree, no fork of pipeline.ts.
   */
  import { IngestPipeline, LayeredDeduplicator, JaccardNameMatcher,
           CosineEmbeddingMatcher, LLMSemanticMatcher,
           GraphKMStore, mintEntityId } from '@fwornle/km-core';
  import type { Extractor, Synthesizer, EmbeddingClient, LLMClient,
                Entity, EntityId, ProvenanceStamp, IngestResult } from '@fwornle/km-core';

  // Minimal Extractor stub — returns 2 hardcoded entities.
  const exampleExtractor: Extractor = {
    async extract(text: string, domain?: string): Promise<Entity[]> { ... },
  };

  // Minimal Synthesizer stub — records the survivor ids it was given.
  export function createExampleSynthesizer(): { synthesizer: Synthesizer; receivedIds: EntityId[] } { ... }

  // Minimal EmbeddingClient stub — returns a fixed unit vector.
  const exampleEmbeddingClient: EmbeddingClient = {
    async embed(text: string): Promise<number[]> { return [1, 0, 0]; },
  };

  // Minimal LLMClient stub — returns the canonical empty-matches response.
  const exampleLLMClient: LLMClient = {
    async complete(req): Promise<{ content: string }> { return { content: '{"matches":[]}' }; },
  };

  // Build the layered deduplicator with D-44 declared order + canonical thresholds.
  const deduplicator = new LayeredDeduplicator({
    exactName: new JaccardNameMatcher({ threshold: 0.85 }),
    embedding: new CosineEmbeddingMatcher({ client: exampleEmbeddingClient, threshold: 0.90 }),
    llmSemantic: new LLMSemanticMatcher({ client: exampleLLMClient, threshold: 0.70 }),
  });

  // Public factory — the companion test calls this.
  export async function runExampleAdapter(store: GraphKMStore): Promise<IngestResult> {
    const { synthesizer } = createExampleSynthesizer();
    const pipeline = new IngestPipeline(store, {
      extractor: exampleExtractor,
      deduplicator,
      synthesizer,
    });
    const provenance: ProvenanceStamp = {
      provider: 'example',
      model: 'sc1-reference-adapter',
      runId: 'sc1-' + Date.now(),
      timestamp: new Date().toISOString(),
    };
    return await pipeline.ingest('some sample text — extractor ignores this', {
      provenance,
      domain: 'coding',
    });
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ~/Agentic/km-core/examples/custom-adapter.ts (the SC#1 reference adapter)</name>
  <files>
    ~/Agentic/km-core/examples/custom-adapter.ts
  </files>
  <read_first>
    - ~/Agentic/km-core/package.json (especially the `exports` map at lines 16-23 — confirms the importable surface: '.' + './pipeline' + './dedup' + './ontology')
    - ~/Agentic/km-core/src/index.ts (Plan 40-07 root barrel — confirms which classes + types are re-exported)
    - ~/Agentic/km-core/src/pipeline/index.ts + ~/Agentic/km-core/src/dedup/index.ts (Plan 40-07 sub-barrels — confirms sub-path surface)
    - ~/Agentic/km-core/tests/integration/pipeline-supersession.test.ts (Ctx + makeFixture pattern for the companion test in Task 2)
    - ~/Agentic/km-core/tests/integration/pipeline-candidate-pool.test.ts (Ctx + makeFixture pattern reference)
    - ~/Agentic/km-core/src/store/GraphKMStore.ts lines 80-180 (GraphKMStore ctor + constructor opts shape — to confirm how the companion test instantiates a store)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-VERIFICATION.md lines 49-51 (gap human_verification item — the exact ask)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-VALIDATION.md line 93 (Manual-Only SC#1 row — references this same example)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-07-SUMMARY.md (Plan 40-07 confirmed the package.json exports map AND the smoke compile — re-use the import surface that smoke compile validated)
  </read_first>
  <action>
    Step A: cd into km-core:
      cd /Users/Q284340/Agentic/km-core

    Step B: Create the `examples/` directory if it doesn't exist:
      mkdir -p examples

    Step C: Create `examples/custom-adapter.ts` per the `<interfaces>` skeleton above. Concrete content per the gap_source bullet list:

    1. **JSDoc header (REQUIRED first line of file)**:
       ```
       /**
        * Reference adapter for SC#1 — demonstrates Phase 40's pluggable stage
        * interfaces with no fork required.
        * ...
        */
       ```
       The exact phrase `Reference adapter for SC#1 — demonstrates Phase 40's pluggable stage interfaces with no fork required` (or VERY close — punctuation flex on the em-dash) MUST appear verbatim per the gap_source instruction.

    2. **Imports — ONLY from `@fwornle/km-core` (or its sub-paths).** Verify by `grep "from '" examples/custom-adapter.ts | grep -vE "from '@fwornle/km-core"` returning zero matches.

    3. **Implements all 4 stage interfaces** (per gap_source bullet "Implement Extractor, Synthesizer (minimal stubs are fine — point is to prove the contracts compile)"):
       - `Extractor` — `exampleExtractor: Extractor`. The `extract()` method returns a 2-entity array (named `'UserAuthService'` and `'PaymentProcessor'`, ontologyClass `'Component'`, entityType `'Component'`, id minted via `mintEntityId()`, plus the rest of the required Entity fields — name/createdAt/updatedAt/validFrom). The signature MUST accept the optional `domain` arg (post-CR-04 contract).
       - `Synthesizer` via `createExampleSynthesizer()` factory — returns `{ synthesizer, receivedIds }` so the companion test can assert which ids were passed.
       - `EmbeddingClient` — `exampleEmbeddingClient` returns `[1, 0, 0]` from `embed()`.
       - `LLMClient` — `exampleLLMClient` returns `{ content: '{"matches":[]}' }` from `complete()` (canonical empty-matches per Plan 04 contract).

    4. **Wire the 3-layer dedup pipeline with thresholds** (per gap_source bullet "show the 3-layer dedup wiring with thresholds"):

       ```typescript
       const deduplicator = new LayeredDeduplicator({
         exactName: new JaccardNameMatcher({ threshold: 0.85 }),
         embedding: new CosineEmbeddingMatcher({ client: exampleEmbeddingClient, threshold: 0.90 }),
         llmSemantic: new LLMSemanticMatcher({ client: exampleLLMClient, threshold: 0.70 }),
       });
       ```

    5. **Construct + invoke IngestPipeline** (per gap_source bullet "Construct an IngestPipeline + invoke `pipeline.ingest(text, { provenance, domain })`"):

       Export `async function runExampleAdapter(store: GraphKMStore): Promise<IngestResult>` that builds the pipeline + calls `pipeline.ingest(text, { provenance, domain: 'coding' })` + returns the IngestResult. The companion test in Task 2 calls this function with a real GraphKMStore instance.

    6. **No console.\* anywhere.** Per the project-wide no-console-log gate.

    7. **All relative imports use .js suffix** — but per the import-discipline rule, there should be ZERO relative imports in this file (only `@fwornle/km-core` package imports).

    Step D: Smoke-compile the file:
      npx tsc --noEmit

    Step E: Commit in km-core/:
      git add examples/custom-adapter.ts
      git commit -m "feat(40-11): SC#1 reference adapter at examples/custom-adapter.ts"

    Step F: cd back to coding/:
      cd -
  </action>
  <verify>
    <automated>test -f /Users/Q284340/Agentic/km-core/examples/custom-adapter.ts && cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit 2>&1 | tee /tmp/40-11-task-1-tsc.log | grep -qE "error TS" && echo "TSC FAIL" || echo "TSC OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `~/Agentic/km-core/examples/custom-adapter.ts` exists.
    - Header marker present verbatim: `grep -c "Reference adapter for SC#1" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"`.
    - Header marker mentions "no fork required": `grep -c "no fork required" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"`.
    - All imports come ONLY from @fwornle/km-core or its sub-paths: `grep "^import" ~/Agentic/km-core/examples/custom-adapter.ts | grep -vE "from '@fwornle/km-core" | wc -l | grep -q "^0$"`.
    - Wires all 4 dedup layer classes by name: `grep -cE "(JaccardNameMatcher|CosineEmbeddingMatcher|LLMSemanticMatcher|LayeredDeduplicator)" ~/Agentic/km-core/examples/custom-adapter.ts` returns at least `4`.
    - Implements Extractor + Synthesizer + EmbeddingClient + LLMClient: `grep -cE ": (Extractor|Synthesizer|EmbeddingClient|LLMClient)\\s*=" ~/Agentic/km-core/examples/custom-adapter.ts` returns at least `2` (Extractor + at least one client) AND `grep -c "Synthesizer" ~/Agentic/km-core/examples/custom-adapter.ts` returns at least `2` (type import + at least one factory/usage).
    - Constructs IngestPipeline via options-object ctor (D-42): `grep -c "new IngestPipeline(store" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"`.
    - Calls pipeline.ingest with provenance + domain: `grep -c "pipeline.ingest" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"` AND `grep -c "domain: 'coding'" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"`.
    - Threshold values present (0.85 Jaccard, 0.90 Cosine, 0.70 LLM): `grep -c "threshold: 0.85" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"`, `grep -c "threshold: 0.90" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"`, `grep -c "threshold: 0.70" ~/Agentic/km-core/examples/custom-adapter.ts | grep -q "^1$"`.
    - Zero console.*: `grep -v '^//' ~/Agentic/km-core/examples/custom-adapter.ts | grep -v '^ \\* ' | grep -cE 'console\\.(log|info|warn|error|debug)' | grep -q "^0$"`.
    - tsc strict clean: `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exit 0.
    - File size in the gap_source target range — between 30 and ~150 lines: `wc -l < ~/Agentic/km-core/examples/custom-adapter.ts | awk '{print ($1>=30 && $1<=200) ? "OK" : "BAD"}' | grep -q "OK"`.
    - km-core commit `feat(40-11): SC#1 reference adapter at examples/custom-adapter.ts` exists.
  </acceptance_criteria>
  <done>Reference adapter on disk; tsc strict clean; imports come only from the public-API barrel; all 4 stage interfaces implemented + wired; ready for the companion test in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the companion test tests/integration/custom-adapter-example.test.ts (GREEN)</name>
  <files>
    ~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts
  </files>
  <read_first>
    - ~/Agentic/km-core/examples/custom-adapter.ts (just-created in Task 1 — the test invokes `runExampleAdapter` from this file)
    - ~/Agentic/km-core/tests/integration/pipeline-supersession.test.ts (Ctx + makeFixture pattern — clone the GraphKMStore lifecycle: beforeEach create temp dir + new store; afterEach close + cleanup)
    - ~/Agentic/km-core/tests/integration/pipeline-candidate-pool.test.ts (same pattern, alternative reference)
    - ~/Agentic/km-core/src/store/GraphKMStore.ts (ctor opts — needs `persistDir` or whatever the existing integration tests pass)
  </read_first>
  <behavior>
    Test cases (1 file, 2-3 tests):

    Test 1: "SC#1: custom-adapter example compiles + runs end-to-end against the public-API barrel"
    - Build a real GraphKMStore in a temp dir (mirror existing integration test setup).
    - Call `await runExampleAdapter(store)`.
    - Assert the IngestResult shape: `result.extractedCount === 2` (the example's extractor returns 2 entities), `result.storedCount === 2`, `result.mergedCount === 0` (no dedup hits because the store starts empty), `result.skippedStages` is empty.
    - Assert no throws.

    Test 2: "SC#1: example uses only public-API barrel imports (no relative paths into src/)"
    - Read the file `examples/custom-adapter.ts` with `fs.readFileSync`.
    - Use a regex `^import .* from ['"](\\..*|.*src/.*)['"]` to find any forbidden imports.
    - Assert zero matches (no relative imports, no '../src/' imports).

    Test 3 (optional): "SC#1: example demonstrates the D-44 declared order — Jaccard then Cosine then LLM"
    - Read the file; grep that JaccardNameMatcher appears in declaration order BEFORE CosineEmbeddingMatcher BEFORE LLMSemanticMatcher (so the source documentation visibly aligns with D-44).
    - This is a textual ordering assertion; not strictly necessary but reinforces the SC#1 documentation value.
  </behavior>
  <action>
    Step A: cd into km-core:
      cd /Users/Q284340/Agentic/km-core

    Step B: Write `tests/integration/custom-adapter-example.test.ts`. Imports:

      import { describe, test, expect, beforeEach, afterEach } from 'vitest';
      import { GraphKMStore } from '../../src/index.js';  // OK — test file is part of km-core monorepo
      import { runExampleAdapter } from '../../examples/custom-adapter.js';
      import * as fs from 'node:fs';
      import * as path from 'node:path';
      import * as os from 'node:os';

    Use the Ctx + makeFixture pattern from `pipeline-supersession.test.ts`. Test bodies follow the `<behavior>` cases above.

    For Test 2 (import-discipline), do something like:

      const exampleSrc = fs.readFileSync(path.join(__dirname, '../../examples/custom-adapter.ts'), 'utf8');
      const importLines = exampleSrc.split('\n').filter((l) => /^import\s/.test(l));
      const forbidden = importLines.filter((l) =>
        /from\s+['"](\.\.?\/|.*\/src\/)/.test(l)
      );
      expect(forbidden).toEqual([]);

    Step C: Run the test file:
      npx vitest run tests/integration/custom-adapter-example.test.ts

    All tests must PASS.

    Step D: Run the full suite:
      npm test
      npm run build

    Step E: Commit in km-core/:
      git add tests/integration/custom-adapter-example.test.ts
      git commit -m "test(40-11): SC#1 custom-adapter example compiles + runs end-to-end"

    Step F: cd back to coding/:
      cd -
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx vitest run tests/integration/custom-adapter-example.test.ts 2>&1 | tee /tmp/40-11-task-2-test.log | grep -qE "Tests +(2|3) passed" && echo "EXAMPLE OK" && cd /Users/Q284340/Agentic/km-core && npm test 2>&1 | tee /tmp/40-11-task-2-full.log | grep -E "Test Files|Tests.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts` exists.
    - Imports `runExampleAdapter` from the example file: `grep -c "from '../../examples/custom-adapter" ~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts | grep -q "^1$"`.
    - Has at least 2 test declarations: `grep -c "^  test(" ~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts | grep -qE "^[2-9]"`.
    - SC#1 marker present in test names: `grep -c "SC#1" ~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts | grep -qE "^[2-9]"`.
    - Import-discipline test present: `grep -c "from\\\\s+\\\\['\\\"](\\\\\\\\.\\\\\\\\.?\\\\/|.\\*\\\\/src\\\\/)" ~/Agentic/km-core/tests/integration/custom-adapter-example.test.ts | grep -qE "^[1-9]"`.
    - The companion test PASSES: `npx vitest run tests/integration/custom-adapter-example.test.ts` exits 0.
    - Full suite green: `npm test` exit 0.
    - `npm run build` clean.
    - km-core commit `test(40-11): SC#1 custom-adapter example compiles + runs end-to-end` exists.
  </acceptance_criteria>
  <done>Companion test green; SC#1 is now PROVABLE by execution — both the example compiles and runs through the public-API barrel AND the test asserts the import-discipline gate.</done>
</task>

<task type="auto">
  <name>Task 3: Write SUMMARY in coding/ worktree</name>
  <files>
    .planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/40-ingest-pipeline-layered-dedup/40-07-SUMMARY.md (format reference for barrel/surface-level SUMMARYs)
    - .planning/phases/40-ingest-pipeline-layered-dedup/40-08-SUMMARY.md (gap-closure format reference)
    - .planning/phases/40-ingest-pipeline-layered-dedup/40-VERIFICATION.md lines 49-51 (the gap this plan closes)
  </read_first>
  <action>
    Write `.planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md` capturing:

    - Tasks 1 + 2 outcome (the 2 km-core commit hashes — feat + test).
    - Paste the verbatim header comment from `examples/custom-adapter.ts` (proves SC#1 marker landed).
    - Paste the full `import` block from the example (proves import-discipline — only @fwornle/km-core).
    - Paste the `LayeredDeduplicator` construction block from the example (proves 3 layers + thresholds).
    - Paste the `pipeline.ingest(...)` call site (proves the gap_source's "invoke pipeline.ingest(text, { provenance, domain })" requirement).
    - State the test count delta (e.g. "+2 new tests in custom-adapter-example.test.ts; full suite now N").
    - State explicitly: "SC#1 CLOSED — the reference adapter is on disk, compiles against the public-API barrel, and runs end-to-end. With Plans 40-08 (CR-01 + CR-04), 40-09 (CR-02), 40-10 (CR-03 + WR-08) and 40-11 (SC#1) all landed, Phase 40 close gate is satisfied — ready for `/gsd:verify-phase 40` re-run."

    Commit in coding/:
      git add .planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md
      git commit -m "docs(40-11): summary — SC#1 closed via examples/custom-adapter.ts"
  </action>
  <verify>
    <automated>test -f /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md && grep -c "SC#1" /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md | grep -qE "^[3-9]" && grep -c "Reference adapter" /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md | grep -qE "^[1-9]" && echo "SUMMARY OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md` exists.
    - SUMMARY cites the 2 km-core commit hashes (feat + test).
    - SUMMARY pastes the verbatim header block from custom-adapter.ts.
    - SUMMARY pastes the import block.
    - SUMMARY pastes the LayeredDeduplicator + IngestPipeline + ingest() construction blocks.
    - SUMMARY states "SC#1 CLOSED" plus the gap-closure plan inventory.
    - coding/ commit `docs(40-11): summary — SC#1 closed via examples/custom-adapter.ts` exists.
  </acceptance_criteria>
  <done>SUMMARY written and committed; SC#1 closure documented; phase close gate ready for /gsd:verify-phase 40 re-run.</done>
</task>

</tasks>

<verification>
- Plan goal: close the SC#1 human-verification gap by writing the reference adapter the verifier asked for.
- Verify the file's import discipline: `grep "^import" ~/Agentic/km-core/examples/custom-adapter.ts | grep -vE "from '@fwornle/km-core" | wc -l` must print `0`.
- Verify the file compiles and runs: `cd /Users/Q284340/Agentic/km-core && npx vitest run tests/integration/custom-adapter-example.test.ts` exits 0 with all tests passing.
- Verify full suite green: `cd /Users/Q284340/Agentic/km-core && npm test` exits 0.
</verification>

<success_criteria>
- 1 new source file (~/Agentic/km-core/examples/custom-adapter.ts, 30-150 lines).
- 1 new test file (tests/integration/custom-adapter-example.test.ts, 2-3 tests).
- 2 km-core commits: `feat(40-11): ...` + `test(40-11): ...`.
- 1 coding/ commit: `docs(40-11): summary — SC#1 closed via examples/custom-adapter.ts`.
- SC#1 ROADMAP success criterion ("a developer can wire a new ingest adapter into KM-Core by implementing the four named stage interfaces and registering it — no fork of the pipeline code required") is now PROVABLE by execution.
- Header marker present verbatim per gap_source instruction.
- Import discipline: ONLY @fwornle/km-core imports in the example.
- All 4 stage interfaces implemented + wired + invoked.
- Full vitest suite green after the example + test land.
</success_criteria>

<output>
Create `.planning/phases/40-ingest-pipeline-layered-dedup/40-11-SUMMARY.md` when done (see Task 3).
</output>
</content>
