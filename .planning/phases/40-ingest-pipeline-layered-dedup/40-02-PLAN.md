---
phase: 40-ingest-pipeline-layered-dedup
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - ~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts
  - ~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts
autonomous: true
requirements: [DEDUP-01]
must_haves:
  truths:
    - "JaccardNameMatcher.match(entity, candidates) returns the best-matching candidate by Jaccard word-set similarity above its threshold."
    - "Threshold defaults to 0.85 (B's production similarityConfig.similarityThreshold value)."
    - "JaccardNameMatcher implements the ExactNameLayer interface from src/dedup/types.ts."
    - "The algorithm matches B's calculateStringSimilarity verbatim: lowercase + split on \\s+ + Jaccard index over word sets."
  artifacts:
    - path: "~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts"
      provides: "Exact-name dedup layer impl (DEDUP-01 layer 1 of 3)"
      contains: "export class JaccardNameMatcher implements ExactNameLayer"
    - path: "~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts"
      provides: "Unit-test coverage for JaccardNameMatcher (PATTERNS.md offset 704-715)"
      contains: "describe('JaccardNameMatcher'"
  key_links:
    - from: "src/dedup/JaccardNameMatcher.ts"
      to: "src/dedup/types.js"
      via: "implements ExactNameLayer"
      pattern: "implements ExactNameLayer"
    - from: "tests/unit/jaccard-matcher.test.ts"
      to: "tests/unit/_helpers/fakes.js"
      via: "imports mkEntity"
      pattern: "from '\\./_helpers/fakes\\.js'"
---

<objective>
Port B's pure 9-line Jaccard similarity function (`integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts:436-445`) into `JaccardNameMatcher` — a class implementing the `ExactNameLayer` interface from Plan 01. Default threshold 0.85 (matches B's production value per RESEARCH.md A1). Pure function, zero I/O, zero dependencies beyond the type imports. Unit tests cover the 7 contract cases enumerated in PATTERNS.md offset 704-715.

Purpose: Layer 1 of the 3-layer dedup pipeline. Cheapest layer — runs first per D-44 declared order, short-circuits when matched per default. Phase 42 (INT-02) later deletes B's local copy of this algorithm once B is migrated to consume `JaccardNameMatcher` from km-core.

Output: 1 source file (~40 lines per RESEARCH.md size estimate) + 1 test file (~7 test cases) + green vitest run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-CONTEXT.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-RESEARCH.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-01-SUMMARY.md

<interfaces>
<!-- Interfaces this plan implements against — extracted from src/dedup/types.ts (created in Plan 01). -->

From src/dedup/types.ts:
  export interface MatchResult { matched: boolean; survivor?: Entity; confidence: number; }
  export interface ExactNameLayer { readonly threshold: number; match(entity: Entity, candidates: Entity[]): Promise<MatchResult>; }

From src/types/entity.ts:
  export interface Entity { id: EntityId; name: string; ... }

From tests/unit/_helpers/fakes.ts:
  export function mkEntity(overrides?: Partial<Entity>): Entity
  // mkEntity defaults: id '0192a000-0000-7000-8000-000000000000', name 'TestEntity', ontologyClass 'Component'.

Source algorithm (port verbatim):
  // From integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts lines 436-445:
  function calculateStringSimilarity(str1: string, str2: string): number {
    const words1 = new Set(str1.toLowerCase().split(/\s+/));
    const words2 = new Set(str2.toLowerCase().split(/\s+/));
    const intersection = new Set([...words1].filter(w => words2.has(w)));
    const union = new Set([...words1, ...words2]);
    return union.size > 0 ? intersection.size / union.size : 0;
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test — tests/unit/jaccard-matcher.test.ts (RED)</name>
  <files>~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts</files>
  <read_first>
    - ~/Agentic/km-core/tests/unit/segments-merge.test.ts (pure-function test pattern, no store needed; mkEntity usage; describe/test/expect from vitest)
    - ~/Agentic/km-core/tests/unit/_helpers/fakes.ts (Plan 01 — `mkEntity` builder)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md offset 704-715 ("tests/unit/jaccard-matcher.test.ts" section — exact test names)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-RESEARCH.md offset 919-942 (Validation Architecture — test name `npx vitest run tests/unit/jaccard-matcher.test.ts -t "score"`)
  </read_first>
  <behavior>
    - `'exact match returns confidence 1.0'` — same name → Jaccard 1.0 → matched: true.
    - `'no shared words returns confidence 0'` — disjoint names → 0 → matched: false.
    - `'mixed case is normalized (case-insensitive)'` — 'UserAuth' vs 'userauth' both lowercase first.
    - `'threshold default 0.85 — confidence 0.84 returns matched: false'` — boundary check.
    - `'threshold opt overrides default'` — ctor `{ threshold: 0.5 }` accepts lower-confidence matches.
    - `'never matches against entity.id === candidate.id (self-match)'` — self-skip per RESEARCH.md Example 2 line 353.
    - `'picks best candidate when multiple exceed threshold'` — highest-score wins.
  </behavior>
  <action>
    Create `~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts` containing the 7 tests above. Import `JaccardNameMatcher` from `'../../src/dedup/JaccardNameMatcher.js'` (forward reference — file is created in Task 2). Import `mkEntity` from `'./_helpers/fakes.js'`. Import vitest primitives from `'vitest'`.

    Test pattern (mirror `tests/unit/segments-merge.test.ts` structure):
    ```
    describe('JaccardNameMatcher', () => {
      test('exact match returns confidence 1.0', async () => {
        const matcher = new JaccardNameMatcher();
        const entity = mkEntity({ name: 'PaymentService' });
        const candidate = mkEntity({ id: '...' as EntityId, name: 'PaymentService' });
        const result = await matcher.match(entity, [candidate]);
        expect(result.matched).toBe(true);
        expect(result.survivor?.id).toBe(candidate.id);
        expect(result.confidence).toBe(1);
      });
      // ... etc
    });
    ```

    For the "picks best candidate" test, provide 2 candidates with scores 0.86 and 0.92 — expect survivor with score 0.92.
    For the "no shared words" test, use names 'Alpha Beta' and 'Gamma Delta' (Jaccard 0).
    For the "mixed case" test, use names 'User Auth Service' and 'USER AUTH SERVICE' (Jaccard 1.0 after lowercase).
    For the "0.84 boundary" test, construct names with deterministic word-set sizes; e.g., 'one two three four five six' vs 'one two three four five seven' → 5/7 ≈ 0.714 (below 0.85); pick names that yield ~0.84.
    For the self-match test, use the SAME id for entity and candidate; expect `matched: false`.

    Each test's `id` for distinct candidates uses a UUIDv7-shaped EntityId differing in the last hex digit (e.g., `'0192a000-0000-7000-8000-000000000001'` as EntityId).

    Execute the test — it MUST fail (the source file doesn't exist yet). Capture: `cd ~/Agentic/km-core && npx vitest run tests/unit/jaccard-matcher.test.ts 2>&1 | tail -20` — expect "Failed | ENOENT" or "Cannot find module" or similar error referencing `JaccardNameMatcher.js`. Commit AFTER confirming RED.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && npx vitest run tests/unit/jaccard-matcher.test.ts 2>&1 | tee /tmp/40-02-task-1-red.log | grep -qE "(Cannot find module|ENOENT|Failed)" && echo "RED OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts` exists.
    - All 7 tests from `<behavior>` are present: `grep -c "^  test(" ~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts` returns `7`.
    - Test file imports from forward-referenced source file: `grep -c "from '../../src/dedup/JaccardNameMatcher.js'" ~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts` returns `1`.
    - Test file imports mkEntity from fakes: `grep -c "from './_helpers/fakes.js'" ~/Agentic/km-core/tests/unit/jaccard-matcher.test.ts` returns `1`.
    - Running the test file FAILS (RED state): exit code non-zero from `npx vitest run tests/unit/jaccard-matcher.test.ts`.
  </acceptance_criteria>
  <done>RED state confirmed — 7 tests exist and fail because JaccardNameMatcher.ts has not been created yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/dedup/JaccardNameMatcher.ts (GREEN)</name>
  <files>~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts</files>
  <read_first>
    - ~/Agentic/km-core/src/segments/merge.ts (lines 1-31 — PATTERNS.md Pattern F SOURCE-comment header style; lines 134-137 stderr-warn idiom is NOT needed here since this matcher emits no diagnostics)
    - ~/Agentic/km-core/src/dedup/types.ts (Plan 01 — ExactNameLayer + MatchResult definitions)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts (lines 436-445 — the verbatim 9-line algorithm to port)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-RESEARCH.md offset 331-373 (Example 2 — complete reference impl)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-PATTERNS.md offset 266-329 ("src/dedup/JaccardNameMatcher.ts" section — class shape + verbatim algo + header)
  </read_first>
  <behavior>
    - All 7 tests from Task 1 PASS after this file lands.
  </behavior>
  <action>
    Create `~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts` per RESEARCH.md Example 2 (offset 331-373) + PATTERNS.md offset 266-329.

    File structure:
    1. SOURCE-comment header (PATTERNS.md Pattern F template) citing: SOURCE = `integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts:436-445` (B's `calculateStringSimilarity`); DELTAS = (1) implements ExactNameLayer interface (returns MatchResult, not bare number); (2) threshold becomes ctor opt with default 0.85 per D-44 + RESEARCH A1; (3) no-console-log — matcher emits zero diagnostics.
    2. Imports: `import type { Entity } from '../types/entity.js';` + `import type { ExactNameLayer, MatchResult } from './types.js';` ONLY. CF-D06 .js suffix.
    3. Export `interface JaccardNameMatcherOpts { threshold?: number; }`.
    4. Export `class JaccardNameMatcher implements ExactNameLayer` with `readonly threshold: number` field + ctor `(opts: JaccardNameMatcherOpts = {})` setting `this.threshold = opts.threshold ?? 0.85`.
    5. `async match(entity: Entity, candidates: Entity[]): Promise<MatchResult>` — iterates candidates, calls private `jaccard()` helper, picks best score above threshold, skips self-match via `if (candidate.id === entity.id) continue;`, returns `{ matched: true, survivor, confidence }` or `{ matched: false, confidence: 0 }`.
    6. Private `function jaccard(a: string, b: string): number` — VERBATIM port from B's algorithm:
       ```
       const words1 = new Set(a.toLowerCase().split(/\s+/));
       const words2 = new Set(b.toLowerCase().split(/\s+/));
       const intersection = new Set([...words1].filter((w) => words2.has(w)));
       const union = new Set([...words1, ...words2]);
       return union.size > 0 ? intersection.size / union.size : 0;
       ```

    Threshold default: `0.85` (NOT 0.9 from CONTEXT.md "starting point" — per RESEARCH.md A1, B's production value is 0.85 in `deduplication.ts:61` similarityConfig.similarityThreshold; the researcher recommends matching production to avoid Phase 42 drift).

    Constraints:
    - NO `console.*` anywhere — matcher emits zero diagnostics per PATTERNS.md offset 286-287.
    - NO mutation of `entity` or `candidates` (purity contract — mirror `merge.ts`).
    - NO async beyond the `match` signature (the algorithm is sync; `async` only because the interface requires it).
    - All relative imports use `.js` suffix.

    Run tests after writing — all 7 MUST pass: `cd ~/Agentic/km-core && npx vitest run tests/unit/jaccard-matcher.test.ts`.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && npx vitest run tests/unit/jaccard-matcher.test.ts 2>&1 | tee /tmp/40-02-task-2-green.log | grep -E "Tests.*7 passed" && echo "GREEN OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts` exists.
    - `grep -c "implements ExactNameLayer" ~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts` returns `1`.
    - `grep -c "this.threshold = opts.threshold ?? 0.85" ~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts` returns `1`.
    - The verbatim algorithm is present: `grep -c "split(/\\\\s+/)" ~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts` returns at least `2` (one for each operand).
    - Self-match guard present: `grep -cE "candidate\\.id\\s*===\\s*entity\\.id" ~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts` returns `1`.
    - All 7 tests pass: `npx vitest run tests/unit/jaccard-matcher.test.ts -t "score"` from the Validation Architecture mapping is green.
    - `npx vitest run tests/unit/jaccard-matcher.test.ts` exits 0.
    - Zero console.* in source: `grep -v '^//' ~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts | grep -v '^ \\* ' | grep -cE 'console\\.(log|info|warn|error|debug)' | grep -q "^0$"`.
    - All relative imports use .js: `grep -E "^import.*from '\\.\\./" ~/Agentic/km-core/src/dedup/JaccardNameMatcher.ts | grep -vE "\\.js';\\s*$" | wc -l | grep -q "^0$"`.
  </acceptance_criteria>
  <done>All 7 unit tests pass; file under ~50 LoC matching RESEARCH size estimate; zero console.*; purity preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Build + full-suite regression check</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md (Submodule build pipeline — `cd ~/Agentic/km-core && npm run build` MUST run after any src/ change per RESEARCH.md Pitfall 6)
    - /Users/Q284340/Agentic/coding/.planning/phases/40-ingest-pipeline-layered-dedup/40-RESEARCH.md offset 252-256 (Pitfall 6 — stale dist consequences)
  </read_first>
  <action>
    Run the km-core build to compile the new TS file into dist/ (RESEARCH.md Pitfall 6 — without this, Phase 41/42/43 consumers will see a stale dist when they later import `JaccardNameMatcher`):
    ```
    cd ~/Agentic/km-core && npm run build
    ```
    Expect zero `tsc` errors.

    Run the full vitest suite to confirm zero regression on the 56 existing tests + 7 new Jaccard tests = 63 expected:
    ```
    cd ~/Agentic/km-core && npm test
    ```

    Confirm the file is present in dist/: `ls ~/Agentic/km-core/dist/dedup/JaccardNameMatcher.js`. (The first time, the `dist/dedup/` directory is also new — created by `npm run build`.)
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && npm run build 2>&1 | tee /tmp/40-02-task-3-build.log | grep -qE "(error TS|error TS[0-9]+:)" && echo "BUILD FAILED" || echo "BUILD OK" && test -f ~/Agentic/km-core/dist/dedup/JaccardNameMatcher.js && echo "DIST OK" && cd ~/Agentic/km-core && npm test 2>&1 | tee /tmp/40-02-task-3-test.log | grep -E "Tests.*passed" | grep -vE "0 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 (no `error TS` in output).
    - `~/Agentic/km-core/dist/dedup/JaccardNameMatcher.js` exists.
    - `~/Agentic/km-core/dist/dedup/JaccardNameMatcher.d.ts` exists.
    - `npm test` exit 0.
    - Pre-existing 56 tests + 7 new Jaccard tests = 63 tests passing (or higher if other Wave 2 plans landed first — verify NO REGRESSION specifically: zero failing tests). Capture the `Test Files / Tests` line from output.
  </acceptance_criteria>
  <done>dist/ is fresh, full suite green, ready for Wave 2 orchestrator plans to consume JaccardNameMatcher.</done>
</task>

</tasks>

<verification>
- Plan goal: Wave 1 layer 1/3 (exact-name) lands as a tested, built module that LayeredDeduplicator (Plan 05) can compose.
- Verify the matcher can be imported from a hypothetical Plan 05 consumer: `cd ~/Agentic/km-core && node -e "import('./dist/dedup/JaccardNameMatcher.js').then(m => { const j = new m.JaccardNameMatcher(); process.stdout.write('threshold=' + j.threshold + '\n'); })"` — expect `threshold=0.85`.
</verification>

<success_criteria>
- 1 source file + 1 test file created.
- 7 unit tests pass; pre-existing suite still passes (zero regression).
- `dist/dedup/JaccardNameMatcher.js` + `.d.ts` exist after `npm run build`.
- Algorithm is verbatim-portable from B (line-by-line match of the 5-line core).
- Threshold default 0.85 (matches B production per RESEARCH A1, NOT the 0.9 CONTEXT starting point).
</success_criteria>

<output>
Create `.planning/phases/40-ingest-pipeline-layered-dedup/40-02-SUMMARY.md` when done. Summary must:
  - Confirm RED→GREEN cycle: cite the RED test output + the GREEN test output (7 passed).
  - State the threshold default (0.85) and cite the RESEARCH.md justification (A1).
  - Confirm `dist/dedup/JaccardNameMatcher.js` exists.
  - State the total test count after this plan (e.g., "63 tests passing").
  - Note any deviation from RESEARCH.md Example 2 (none expected).
</output>
