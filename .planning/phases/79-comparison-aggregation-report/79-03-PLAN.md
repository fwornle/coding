---
phase: 79-comparison-aggregation-report
plan: 03
type: execute
wave: 2
depends_on: [79-02]
files_modified:
  - scripts/experiments-compare.mjs
  - tests/experiments/experiments-compare.test.mjs
autonomous: true
requirements: [CMP-03]
must_haves:
  truths:
    - "An operator can run `node scripts/experiments-compare.mjs --task-hash <h>` and get a ranked human-readable CLI table on stdout"
    - "A machine-readable JSON report is written to .data/experiments/reports/<task_hash>.json with a stable, documented schema"
    - "The JSON schema includes per-variant success-gate outcome, the full {mean,stddev,median,min,max,n} block per metric, the rank, and the failed/ungated/unscored groupings"
    - "--csv additionally writes a CSV; --rank-by overrides the default composite ranking"
    - "task_hash is validated/sanitized before use as a filename (no path traversal)"
  artifacts:
    - path: "scripts/experiments-compare.mjs"
      provides: "operator CLI: store open/close in main, ranked table, JSON+CSV export, entry-point guard, named exports"
      exports: ["renderTable", "writeReportJson", "writeReportCsv", "sanitizeTaskHash"]
    - path: "tests/experiments/experiments-compare.test.mjs"
      provides: "integration coverage of table render, JSON export path+schema, CSV, task_hash sanitization"
      contains: "writeReportJson"
  key_links:
    - from: "scripts/experiments-compare.mjs main"
      to: "lib/experiments/compare.mjs buildComparison"
      via: "readRuns(store) → buildComparison(rows, {taskHash, rankBy})"
      pattern: "buildComparison"
    - from: "scripts/experiments-compare.mjs writeReportJson"
      to: ".data/experiments/reports/<task_hash>.json"
      via: "fs.mkdirSync(reportsDir,{recursive:true}) + writeFileSync of sanitized path"
      pattern: "experiments/reports"
    - from: "scripts/experiments-compare.mjs"
      to: "openExperimentStore()"
      via: "store open in main, try/finally close (caller-owns-store)"
      pattern: "openExperimentStore"
---

<objective>
Create the operator CLI `scripts/experiments-compare.mjs` — the CMP-03 surface. It opens the experiment store, calls `readRuns` → `buildComparison` (Plan 02), renders a ranked human-readable table to stdout, and writes the canonical machine-readable JSON export to `.data/experiments/reports/<task_hash>.json` (the stable contract Phase 80 consumes). `--csv` additionally writes CSV; `--rank-by` overrides the default composite. task_hash is sanitized before being used as a filename.

Purpose: This is the operator-facing entry point AND the 79→80 seam. Schema stability is a hard requirement — Phase 80's dashboard variant columns render this JSON without re-running the experiment.
Output: the CLI, its JSON/CSV writers, the sanitizeTaskHash guard, and an integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-comparison-aggregation-report/79-CONTEXT.md
@.planning/phases/79-comparison-aggregation-report/79-PATTERNS.md
@.planning/phases/79-comparison-aggregation-report/79-02-SUMMARY.md

<interfaces>
<!-- Mirror scripts/experiments-query.mjs EXACTLY: shebang, arg parser, store-open-in-main, -->
<!-- try/finally close, entry-point guard, named exports for pure helpers. -->

From scripts/experiments-query.mjs (the exact-shape analog):
```
#!/usr/bin/env node
import process from 'node:process';
import { pathToFileURL } from 'node:url';
import { openExperimentStore } from '../lib/experiments/store.mjs'; // ontologyDir set inside — NEVER `new GraphKMStore`

function parseStrArg(argv, flag) { const i = argv.indexOf(flag); if (i < 0) return null; return argv[i + 1] ?? null; }

async function main() {
  const args = process.argv.slice(2);
  const store = await openExperimentStore();     // caller owns lifecycle
  try { /* ...read + build + render + write... */ } finally { await store.close(); }
}

const isMain = (() => { try { return import.meta.url === pathToFileURL(process.argv[1]).href; } catch { return false; } })();
if (isMain) { main().catch((err) => { process.stderr.write(`FATAL: ${err.stack || err.message}\n`); process.exit(1); }); }
export { /* pure helpers */ };
```

From lib/experiments/compare.mjs (Plan 02 — the aggregator this CLI drives):
```
export function buildComparison(rows, { taskHash, rankBy = 'composite' })  // → report object (ranked/failed/ungated/unscored + per-metric variance)
```
From lib/experiments/query.mjs: `export async function readRuns(store, { includePending })`

Report dir: `.data/experiments/` exists (holds exports/, runs/, snapshots/); `reports/` does NOT yet exist — writeReportJson must `fs.mkdirSync(reportsDir, { recursive: true })` first.

Output discipline (CLAUDE.md no-console-log): table + status → process.stdout.write; diagnostics → process.stderr.write. No console.*, no process.stderr.write dodge for user output.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: task_hash sanitizer + JSON/CSV report writers</name>
  <read_first>
    - scripts/experiments-query.mjs (lines 1-39 header/arg-parser; lines 105-145 main + entry-point guard + named exports)
    - lib/experiments/compare.mjs (the report object shape from Plan 02, per 79-02-SUMMARY.md)
    - .planning/phases/79-comparison-aggregation-report/79-PATTERNS.md (CLI section, lines 70-127; report dir note line 124)
    - .planning/phases/79-comparison-aggregation-report/79-CONTEXT.md (D-11/D-12/D-13)
  </read_first>
  <behavior>
    - sanitizeTaskHash('abc123') → 'abc123'
    - sanitizeTaskHash('../../etc/passwd') → throws OR returns a rejected value (no path separators, no ..) — a traversal attempt NEVER produces a write outside .data/experiments/reports/
    - sanitizeTaskHash('a/b') and sanitizeTaskHash('a\\0b') → rejected/sanitized to a safe basename
    - writeReportJson(report, taskHash) → creates .data/experiments/reports/ if absent and writes <sanitized>.json containing per-variant gate outcome, {mean,stddev,median,min,max,n} per metric, rank, and failed/ungated/unscored groupings, keyed by task_hash
    - writeReportCsv(report, taskHash) → writes <sanitized>.csv alongside the JSON (one row per variant with the ranked metric + n + gate outcome)
    - the written JSON round-trips (JSON.parse) with a documented, stable field set
  </behavior>
  <action>
    Create scripts/experiments-compare.mjs (net-new). Copy the experiments-query.mjs shebang, imports, parseStrArg, entry-point guard, and FATAL wrapper verbatim. Add `sanitizeTaskHash(h)`: reject null/empty; strip to a safe basename — allow only `[A-Za-z0-9._-]`, reject any value containing `/`, `\`, `..`, or a null byte (throw a clear error routed to stderr). Use the sanitized value as the ONLY filename component; build the path via `path.join(REPO_ROOT, '.data/experiments/reports', sanitized + '.json')` and assert the resolved path stays under the reports dir. Add `writeReportJson(report, taskHash)`: mkdirSync the reports dir recursive, write the report object as pretty JSON keyed by task_hash — include per-variant gate outcome, the full {mean,stddev,median,min,max,n} block per metric, the rank, and the failed/ungated/unscored groupings (D-13). Add `writeReportCsv(report, taskHash)`: emit one CSV row per variant (variant, rank, n, gate outcome, the ranked metric value + key variance columns) alongside the JSON. Export sanitizeTaskHash, writeReportJson, writeReportCsv (and renderTable in Task 2) so the test imports them without running main. Document the JSON schema field names in a header comment (stable — Phase 80 consumes them). No console.*.
  </action>
  <verify>
    <automated>node --test tests/experiments/experiments-compare.test.mjs 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - experiments-compare.mjs exports sanitizeTaskHash, writeReportJson, writeReportCsv
    - sanitizeTaskHash('../../etc/passwd') throws or rejects — never yields a path outside .data/experiments/reports/ (asserted by the traversal test in Task 2)
    - sanitizeTaskHash('abc123') === 'abc123'
    - writeReportJson creates .data/experiments/reports/<hash>.json and the file JSON.parses with per-variant gate outcome + {mean,stddev,median,min,max,n} + rank + failed/ungated/unscored keys
    - grep -v '^\s*//' scripts/experiments-compare.mjs | grep -c "console\." == 0
  </acceptance_criteria>
  <done>The CLI's writers + sanitizer exist; JSON export path-safe and schema-complete; CSV opt-in writer present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire main() + ranked table renderer + integration test</name>
  <read_first>
    - scripts/experiments-query.mjs (lines 90-145: main store-open/close + entry-point guard + exports)
    - lib/experiments/compare.mjs (buildComparison signature + report shape)
    - lib/experiments/query.mjs (readRuns signature)
    - tests/experiments/experiments-compare.test.mjs (the file created in Task 1 — extend it)
    - tests/experiments/run-endpoint.test.mjs (store-backed synthetic Run+Score fixture idiom for an integration test)
  </read_first>
  <action>
    In scripts/experiments-compare.mjs: implement `renderTable(report)` returning a human-readable ranked table string (columns at Claude's discretion — include variant, rank, n, gate outcome, the ranked metric + its stddev, and a distinct section for the failed/ungated/unscored groups so a "no successful runs" variant is visibly NOT a winner). Implement `main()` mirroring experiments-query.mjs: parse `--task-hash <h>` (required — error to stderr + exit 2 if absent), `--rank-by <metric>` (default 'composite'), and boolean `--csv` (`args.includes('--csv')`); open the store via openExperimentStore(); in try, `readRuns(store)` → `buildComparison(rows, {taskHash, rankBy})` → `process.stdout.write(renderTable(report))` → `writeReportJson(report, taskHash)` → conditionally `writeReportCsv(report, taskHash)`; finally `await store.close()`. Add renderTable to the named exports. Extend tests/experiments/experiments-compare.test.mjs with a store-backed integration test: write synthetic Run+Score entities (some complete+gate_passed=true, some gate_passed=false, one timeout) sharing a task_hash, run the read→build→render→write pipeline, and assert (a) the JSON export lands at the sanitized reports path, (b) the failed/timeout variants appear in the non-ranked section of both the JSON and the rendered table, (c) --rank-by tokens changes the ordering, (d) --csv writes the CSV file, (e) a traversal task_hash (e.g. '../../etc/passwd') is rejected by sanitizeTaskHash. Caller-owns-store try/finally close in the test.
  </action>
  <verify>
    <automated>node --test tests/experiments/experiments-compare.test.mjs 2>&1 | grep -E "# fail 0" && grep -c "openExperimentStore" scripts/experiments-compare.mjs && grep -c "await store.close" scripts/experiments-compare.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/experiments/experiments-compare.test.mjs exits 0 (# fail 0)
    - experiments-compare.mjs main opens the store via openExperimentStore and closes it in finally (grep openExperimentStore >=1 AND store.close >=1)
    - experiments-compare.mjs NEVER constructs a store inline (grep -c "new GraphKMStore" == 0)
    - a test asserts running the CLI pipeline for --task-hash X writes .data/experiments/reports/X.json
    - a test asserts a gate_passed=false / timeout variant renders in the "no successful runs" section, not the ranking, in both JSON and table
    - a test asserts --rank-by tokens reorders vs the default composite
    - a test asserts sanitizeTaskHash rejects '../../etc/passwd'
  </acceptance_criteria>
  <done>The CLI runs end-to-end against synthetic store fixtures: ranked table to stdout, schema-stable JSON export, opt-in CSV, task_hash-safe paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv (--task-hash, --csv) → filesystem | Operator-supplied task_hash is used to derive a report filename under .data/experiments/reports/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Tampering | task_hash → report path | mitigate | sanitizeTaskHash rejects `/`, `\`, `..`, null bytes; allows only `[A-Za-z0-9._-]`; the resolved path is asserted to stay under .data/experiments/reports/ — no path traversal on the JSON/CSV write. Asserted by the traversal test. |
| T-79-03-02 | Elevation of Privilege | shell execution via CLI | accept | No shell/exec introduced; the gate is NOT re-run (reuses the persisted gate_passed field). CLI does read + pure aggregate + file write only. |
| T-79-03-03 | Repudiation | schema drift 79→80 | mitigate | JSON schema field names documented in the CLI header + the SUMMARY; the integration test asserts the presence of gate outcome, the variance block, rank, and the groupings — a schema change breaks the test. |
| T-79-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Node builtins fs/path/url + node:test only). |
</threat_model>

<verification>
- `node --test tests/experiments/experiments-compare.test.mjs` exits 0.
- `node scripts/experiments-compare.mjs --task-hash <existing-hash>` (a real hash from `.data/experiments/runs/`) prints a table and writes `.data/experiments/reports/<hash>.json`.
- sanitizeTaskHash rejects a traversal attempt.
- CLI opens/closes the store correctly and never constructs one inline.
</verification>

<success_criteria>
- CMP-03: a side-by-side ranked report (CLI table + JSON export) keyed by task_hash, showing variance and each variant's gate outcome, with failed/ungated/unscored groupings.
- The JSON export schema is stable and documented (Phase 80 seam).
- task_hash is sanitized — no path traversal on the report write.
- `--csv` and `--rank-by` behave per D-12/D-06.
</success_criteria>

<output>
Create `.planning/phases/79-comparison-aggregation-report/79-03-SUMMARY.md` when done. DOCUMENT the final JSON export schema (field names + nesting) as the stable Phase-80 contract, and note the sanitizeTaskHash allowlist.
</output>
