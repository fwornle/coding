---
phase: 61-lsl-timeline-okb-routing-honesty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/observations-api-server.mjs
  - tests/integration/obs-api.coding-lsl-sessions.test.js
autonomous: true
requirements: [LSLTIME-01, LSLTIME-03]
must_haves:
  truths:
    - "GET /api/coding/lsl/sessions returns a numeric `total` field equal to the full pre-slice session count (M), even when the array is capped at `limit` (N)"
    - "Each session object carries a `source` field that is either 'online' or 'batch'"
    - "A session whose [startAt,endAt) window contains ANY km-core entity with metadata.source==='manual' has source==='batch'; otherwise source==='online'"
    - "The integration test asserts both new fields and the any-batch->batch rule with a mixed-source fixture"
  artifacts:
    - path: "scripts/observations-api-server.mjs"
      provides: "lsl/sessions handler emitting total + per-session source"
      contains: "total: sessions.length"
    - path: "tests/integration/obs-api.coding-lsl-sessions.test.js"
      provides: "regression coverage for total + source + any-batch->batch"
      contains: "source"
  key_links:
    - from: "scripts/observations-api-server.mjs allEnts scan (line ~2401)"
      to: "aggregateForRange source derivation (line ~2423)"
      via: "matched-entity metadata.source any-manual->batch rule"
      pattern: "source.*manual.*batch"
---

<objective>
Add the two additive backend fields the LSL timeline strip needs to stop lying: the true total session count `total` (M) for the "N of M" honesty badge, and a per-session `source` discriminator ('online' | 'batch') for bi-source tick coloring.

Purpose: The strip's badge (LSLTIME-01) and two-color ticks (LSLTIME-03) both require backend truth — the count is already computed-then-discarded, and source must be derived server-side from km-core entity metadata (client-side id-heuristic was rejected per D-07 as fragile). This is the only backend touch in the phase and is the dependency Wave 2's frontend plan builds on.

Output: obs-api `/api/coding/lsl/sessions` emits `total` + per-session `source`; the integration test locks both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-lsl-timeline-okb-routing-honesty/61-CONTEXT.md
@.planning/phases/61-lsl-timeline-okb-routing-honesty/61-RESEARCH.md
@.planning/phases/61-lsl-timeline-okb-routing-honesty/61-PATTERNS.md

<interfaces>
<!-- Handler: scripts/observations-api-server.mjs:2345-2468. Extracted live 2026-06-20. -->
<!-- Constants: LSL_DEFAULT_LIMIT=200 (L2291), LSL_MAX_LIMIT=500 (L2292). -->

allEnts scan push (L2401-2407) currently captures:
  { id, type: attrs.entityType, hidden: ..., createdMs }
  -- source NOT captured. Source location varies (Pitfall 3): attrs.metadata?.source ?? attrs.source

aggregateForRange (L2423-2432) returns:
  { entityIds: matches.map(x => x.id), totalCount: matches.length }
  -- destructured at L2447 as { entityIds, totalCount }; non-finite branch L2450 returns { entityIds: [], totalCount: 0 }

per-session push (L2453-2459):
  { id, startAt, endAt, observationCount: totalCount, entityIds }

envelope (L2463):
  res.json({ success: true, data: { sessions: sliced } })

Logging idiom (L2411, L2465): process.stderr.write(...) -- NEVER console.log (CLAUDE.md no-console-log).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend obs-api lsl/sessions handler with total + per-session source</name>
  <files>scripts/observations-api-server.mjs</files>
  <read_first>
    - scripts/observations-api-server.mjs lines 2276-2468 (the full handler, the allEnts scan, aggregateForRange, the per-session push, the res.json envelope)
    - scripts/observations-api-server.mjs lines 2291-2293 (LSL_DEFAULT_LIMIT / LSL_MAX_LIMIT constants)
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-PATTERNS.md § "scripts/observations-api-server.mjs" (the exact additive diff for total + source)
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-RESEARCH.md § "Unknown 1" (total) and § "Unknown 2" (source derivation + the live metadata.source distribution table + Pitfall 3 source-location note)
  </read_first>
  <behavior>
    - GET /api/coding/lsl/sessions?since=...&limit=2 against 4 seeded sessions returns data.sessions.length===2 AND data.total===4
    - Each session object has a `source` property in {'online','batch'}
    - A session window containing an entity with metadata.source==='manual' yields source==='batch'
    - A session window with only auto/online/null-source entities (or zero entities) yields source==='online'
  </behavior>
  <action>
    In the allEnts scan push (around L2401-2407) add a `source` property read as `(attrs.metadata && attrs.metadata.source) ?? attrs.source` — covers both attribute locations per Pitfall 3. Do NOT change the existing id/type/hidden/createdMs fields.

    In `aggregateForRange` (around L2423-2432) add a third returned property `source`, applying the PINNED D-09 rule: `source: matches.some((m) => m.source === 'manual') ? 'batch' : 'online'`. This is the any-`manual`->`batch`, else `'online'` rule (order-independent, no tie-break). Keep `entityIds` and `totalCount` unchanged.

    At the destructure (around L2447) add `source` to `const { entityIds, totalCount, source }`; on the non-finite fallback branch (around L2450) default `source: 'online'`. Add `source` to the per-session push object (around L2453-2459).

    At the envelope (around L2463) change to `res.json({ success: true, data: { sessions: sliced, total: sessions.length, limit } })` — `total` is `sessions.length` BEFORE the slice (the already-computed M), `limit` echoes the applied cap so the client need not hard-code it. Do NOT add a second query or count endpoint.

    Leave LSL_MAX_LIMIT at 500 (the client cap-raise to 500 is the Wave-2 frontend change; server max already accommodates it). For any new diagnostics use `process.stderr.write(...)` only — NEVER console.log.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node --check scripts/observations-api-server.mjs && grep -c "total: sessions.length" scripts/observations-api-server.mjs | grep -qv '^0$' && grep -q "source === 'manual'" scripts/observations-api-server.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/observations-api-server.mjs` exits 0 (valid syntax)
    - `grep "total: sessions.length" scripts/observations-api-server.mjs` matches (envelope carries total)
    - `grep "source === 'manual'" scripts/observations-api-server.mjs` matches (any-batch rule present)
    - Zero `console.log` added: `grep -nE 'console\.(log|error|warn)' scripts/observations-api-server.mjs` returns no NEW lines beyond pre-existing (handler uses process.stderr.write)
    - The allEnts push object includes a `source` key reading `attrs.metadata?.source ?? attrs.source`
  </acceptance_criteria>
  <done>Handler emits `total` on the envelope and `source` ('online'|'batch') on every session per the any-manual->batch rule; file passes node --check; no console.log introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the obs-api integration test for total + source + any-batch->batch</name>
  <files>tests/integration/obs-api.coding-lsl-sessions.test.js</files>
  <read_first>
    - tests/integration/obs-api.coding-lsl-sessions.test.js (full file — the beforeAll km-core seeding at L35-92, the httpGet helper L94-98, the existing shape/cap tests L100-145, the header comment L1-17)
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-PATTERNS.md § "tests/integration/obs-api.coding-lsl-sessions.test.js" (the exact additive test sketches)
    - scripts/observations-api-server.mjs lines 2390-2463 (to mirror how createdAt range-matching selects entities into a session, so the seeded entity createdAt lands inside the target session window)
  </read_first>
  <action>
    Add three assertions/tests, extending (not restructuring) the existing suite:

    (a) total field: add a test using `?since=2026-05-01T00:00:00Z&limit=2` asserting `body.data.sessions.length === 2` AND `body.data.total === 4` (the 4 seeded sessions). This is the N&lt;M case driving the badge.

    (b) source shape: in the existing shape loop (around L105-111) add `expect(['online','batch']).toContain(s.source)`.

    (c) any-batch->batch rule: in beforeAll, seed km-core entities via the existing `kmStore` (same handle used at L68-77) with `createdAt` inside the `2026-06-09_1600-1700` session window — one carrying `metadata.source: 'manual'` (must yield that session's `source==='batch'`), and ensure another session window (e.g. `2026-06-08_0900-1000`) gets only `auto`/`online`-source entities or none (must yield `source==='online'`). Add a test asserting the `2026-06-09` session has `source==='batch'` and the online-only one has `source==='online'`. Use `kmStore.putEntity` (or the same seeding primitive the file/km-core already uses) with createdAt timestamps inside the respective `[startAt,endAt)` windows.

    Update the header comment wire-shape line (around L7) to include `source` and `total`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && npx jest tests/integration/obs-api.coding-lsl-sessions.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest tests/integration/obs-api.coding-lsl-sessions.test.js` passes all tests (existing + 3 new)
    - A test asserts `body.data.total === 4` while `body.data.sessions.length === 2`
    - A test asserts the `2026-06-09` session `source==='batch'` (manual-tagged entity in window) and an online-only session `source==='online'`
    - The shape loop asserts `['online','batch']` contains `s.source`
  </acceptance_criteria>
  <done>Integration test green with new total + source + any-batch->batch coverage; header comment updated to document the new wire fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unified-viewer (browser) -> obs-api :12436 | viewer fetches sessions over localhost HTTP; obs-api reads filesystem (LSL dir) + km-core graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | Information Disclosure | new `total`/`source` envelope fields | accept | Both are aggregate scalars derived from data already exposed (session count, coarse source bucket); no path, secret, or PII leaks. `source` is a 2-value enum, not the raw metadata.source string. |
| T-61-01-02 | Tampering | `?since` / `?limit` query params | accept | Already mitigated pre-existing (T-55-06-03): `?since` parsed via `new Date`, never used as a path; `?limit` clamped to [1, LSL_MAX_LIMIT]. No new input introduced by this plan. |
| T-61-01-03 | Denial of Service | full-history filesystem walk for `total` | accept | `total = sessions.length` reuses the EXISTING walk — no extra I/O. The walk was already O(files); counting is free. No second query added. |
| T-61-01-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs zero packages (RESEARCH Package Legitimacy Audit: not applicable). No install task -> no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `node --check scripts/observations-api-server.mjs` exits 0
- `npx jest tests/integration/obs-api.coding-lsl-sessions.test.js` green
- `grep -nE 'console\.(log|error|warn)' scripts/observations-api-server.mjs` shows no NEW console calls in the handler region
- To deploy for downstream manual verification: `launchctl kickstart -k gui/$(id -u)/com.coding.obs-api` then `curl -s 'http://localhost:12436/api/coding/lsl/sessions?since=2026-05-01T00:00:00Z&limit=2' | python3 -m json.tool` shows `total` and per-session `source`
</verification>

<success_criteria>
- obs-api `/api/coding/lsl/sessions` returns `total` (full pre-slice M) and per-session `source` ('online'|'batch')
- Source follows the deterministic any-`manual`->`batch`, else `'online'` rule (D-09)
- Integration test locks total, source shape, and the any-batch->batch rule
- No console.log introduced; handler keeps the process.stderr.write idiom
</success_criteria>

<output>
Create `.planning/phases/61-lsl-timeline-okb-routing-honesty/61-01-SUMMARY.md` when done
</output>
