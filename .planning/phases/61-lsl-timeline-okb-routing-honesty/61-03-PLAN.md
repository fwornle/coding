---
phase: 61-lsl-timeline-okb-routing-honesty
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - integrations/unified-viewer/src/panels/coding/useLslSessions.ts
  - integrations/unified-viewer/src/graph/useNodeToBucketsIndex.ts
  - integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx
  - integrations/unified-viewer/src/panels/coding/LslTimelineStrip.test.tsx
autonomous: false
requirements: [LSLTIME-01, LSLTIME-02, LSLTIME-03]
must_haves:
  truths:
    - "The strip surfaces a visible 'showing N of M' badge whenever the rendered tick count is below the underlying total returned by the backend"
    - "The window ladder reads 24h / 7d / 30d / 1y — no 'all' label and no 'all' LslWindow literal remains"
    - "Manual/batch sessions (source==='batch') render in a distinct hue (amber) and online/auto sessions (source==='online') keep pink — visually distinguishable at a glance"
    - "The client raises its fetch cap to limit=500 (the backend max)"
  artifacts:
    - path: "integrations/unified-viewer/src/panels/coding/useLslSessions.ts"
      provides: "1y window key, total-M plumbing, limit=500, source field on LslSession"
      contains: "'1y'"
    - path: "integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx"
      provides: "N-of-M badge, bi-source fillClass, 1y toggle + consumer renames"
      contains: "lsl-nofm-badge"
  key_links:
    - from: "useLslSessions fetchSessions"
      to: "strip N-of-M badge"
      via: "{ sessions, total } return surfaced through the hook"
      pattern: "total"
    - from: "LslSession.source ('online'|'batch')"
      to: "strip fillClass branch"
      via: "s.source === 'batch' -> amber, else pink"
      pattern: "source === 'batch'"
---

<objective>
Make the LSL timeline strip tell the truth on all three axes: a visible "showing N of M" badge so the bounded fetch cap can never silently fool the operator (LSLTIME-01), an honest `1y` window label replacing the misnamed `'all'` (which is a silent 365-day cap) (LSLTIME-02), and two distinct tick colors so manual/batch and online/auto sessions are distinguishable at a glance (LSLTIME-03).

Purpose: This is the operator-facing payoff of the phase. It consumes Plan 01's backend `total` + `source` fields. All three changes touch the same two strip files (`useLslSessions.ts`, `LslTimelineStrip.tsx`), so they are bundled to avoid file-conflict serialization. A human-verify checkpoint confirms the visual result since tick color + badge are visual properties.

Output: honest badge, `1y` ladder, amber/pink bi-source ticks, raised cap, and a Playwright/gsd-browser visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-lsl-timeline-okb-routing-honesty/61-CONTEXT.md
@.planning/phases/61-lsl-timeline-okb-routing-honesty/61-RESEARCH.md
@.planning/phases/61-lsl-timeline-okb-routing-honesty/61-PATTERNS.md
@.planning/phases/61-lsl-timeline-okb-routing-honesty/61-01-PLAN.md

<interfaces>
<!-- useLslSessions.ts (extracted live 2026-06-20): -->
<!--   LslWindow = '24h'|'7d'|'30d'|'all'                 // L23  -> rename 'all' to '1y' -->
<!--   WINDOW_MS Record key 'all': 365*24*3600_000        // L33  -> rename key '1y', value unchanged -->
<!--   fetchSessions(...): Promise<LslSession[]>           // L44-63; url has &limit=200 at L49 -->
<!--     body type: { sessions: LslSession[] } | LslSession[]  // L54-56 -->
<!--   useLslSessions(apiClient, windowKey): useQuery -> data is LslSession[]  // L76-82 -->
<!--   LslSession interface  // L36-42 (NO source field today) -->
<!-- useNodeToBucketsIndex.ts (graph/, SECOND consumer of the hook): -->
<!--   useNodeToBucketsIndex.ts:71 -- const { data: sessions } = useLslSessions(apiClient) -->
<!--     treats `sessions` (the hook `data`) as the LslSession[] array TODAY -->
<!--     -> after the {sessions,total} widen it must read `data.sessions` -->
<!-- LslTimelineStrip.tsx (extracted live 2026-06-20): -->
<!--   const { data } = useLslSessions(apiClient, windowKey)   // L423 (data is LslSession[] today) -->
<!--   auto-slide ladder fall-through ': 'all''                // L405 -->
<!--   allOriginMs guard: if (windowKey !== 'all') return undefined  // L452 -->
<!--   const isAll = windowKey === 'all'                       // L477 -->
<!--   WINDOW_MS['all'] read                                   // L479 -->
<!--   onWindowChange guard: next === ... || next === 'all'    // L497 -->
<!--   ToggleGroupItem value="all" aria-label="All time"       // L835-837 -->
<!--   isHaloBucket fillClass branch                           // L919-921 (bg-pink-300 at L921) -->
<!--   tick render: className=`... ${fillClass} ... ${ringClass} ${disabledClass}`  // L945 -->
<!-- Note: WINDOW_MS[windowKey] indexed reads at L401, L488, L868 are key-safe after rename. -->
<!-- Note: LslTimelineStrip.test.tsx references 'all' only in PROSE comments (L1630-1797) -- no functional 'all'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename 'all'->'1y', plumb total-M, add source to LslSession, raise cap to 500 in the hook</name>
  <files>integrations/unified-viewer/src/panels/coding/useLslSessions.ts, integrations/unified-viewer/src/graph/useNodeToBucketsIndex.ts</files>
  <read_first>
    - integrations/unified-viewer/src/panels/coding/useLslSessions.ts (full file — LslWindow L23, WINDOW_MS L29-34, fetchSessions L44-63, LslSession interface L36-42, useLslSessions hook L76-82, header comment L1-18)
    - integrations/unified-viewer/src/graph/useNodeToBucketsIndex.ts line 71 (the SECOND consumer: `const { data: sessions } = useLslSessions(apiClient)` — destructures `data` AS the session array today; will break when `data` becomes `{ sessions, total }`)
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-PATTERNS.md § "useLslSessions.ts" (the exact rename + body-widen + limit=500 + header-comment diff)
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-RESEARCH.md § "Unknown 1" (the total plumbing) and § "'all'-key consumer inventory" (the rename sites)
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-01-PLAN.md (the backend now returns `{ sessions, total, limit }`)
  </read_first>
  <action>
    (a) Rename the window key per D-04/D-06: `LslWindow` (L23) `'24h' | '7d' | '30d' | 'all'` -> `... | '1y'`; `WINDOW_MS` record key (L33) `'all'` -> `'1y'` with the 365-day VALUE UNCHANGED. The rename is label-honesty only; no backend window change.

    (b) Add `source?: 'online' | 'batch'` to the `LslSession` interface (L36-42) — matches Plan 01's backend field; consumed by the strip's fillClass branch.

    (c) Plumb total-M (D-02): widen the body object arm (L54-56) to `{ sessions: LslSession[]; total?: number; limit?: number } | LslSession[]`. Change `fetchSessions` return type from `Promise<LslSession[]>` to `Promise<{ sessions: LslSession[]; total?: number }>`; return `{ sessions: data, total: data.length }` on the array arm and `{ sessions: data?.sessions ?? [], total: data?.total }` on the object arm. The `useLslSessions` hook's `data` now resolves to `{ sessions, total }` — downstream consumers (the strip, useNodeToBucketsIndex) read `data.sessions` / `data.total`.

    (d) Raise the cap (D-03): change `&limit=200` (L49) to `&limit=500` (the backend LSL_MAX_LIMIT). Typical windows now fit without the badge; the badge still fires honestly when exceeded.

    (e) Header comment (D-03): the header (L1-18) documents the 56.1 extraction contract, not the cap. Add a line documenting the bounded 500 cap + the meaning of the "N of M" badge (operator can never be silently truncated).

    (f) CONDITIONAL CONSUMER UPDATE — useNodeToBucketsIndex.ts:71 (second consumer of this hook): it currently does `const { data: sessions } = useLslSessions(apiClient)` — destructuring the hook `data` directly AS the `LslSession[]` array. After widening `data` to `{ sessions, total }`, that destructure makes `sessions` the WHOLE object, not the array, and every downstream `sessions.map`/`.forEach`/`.length` either throws or type-errors. Update this consumer to read `data.sessions` (e.g. `const { data } = useLslSessions(apiClient); const sessions = data?.sessions ?? []`). Run the grep pre-check in `<verify>` FIRST to confirm the exact access pattern before relying on tsc to surface the mismatch — do NOT loosen types to dodge (TypeScript strict).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/unified-viewer && echo "--- useNodeToBucketsIndex hook access pre-check ---" && grep -n "useLslSessions" src/graph/useNodeToBucketsIndex.ts && echo "--- tsc gate ---" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'1y'" src/panels/coding/useLslSessions.ts` matches; `grep -n "'all'" src/panels/coding/useLslSessions.ts` returns NOTHING (no 'all' literal remains)
    - `grep -n "limit=500" src/panels/coding/useLslSessions.ts` matches
    - `grep -n "source" src/panels/coding/useLslSessions.ts` shows `source?: 'online' | 'batch'` on LslSession
    - The pre-check grep confirms how useNodeToBucketsIndex.ts:71 destructures the hook (so the executor updates `data.sessions` rather than only discovering it via a type error)
    - `grep -n "data.sessions\|data?.sessions" src/graph/useNodeToBucketsIndex.ts` shows the consumer reads `.sessions` after the widen (no bare-array destructure left)
    - `npx tsc --noEmit` exits 0 (no type errors — fetchSessions return widening propagates cleanly to BOTH consumers: the strip and useNodeToBucketsIndex)
  </acceptance_criteria>
  <done>Hook renames 'all'->'1y' (value unchanged), adds source to LslSession, returns { sessions, total }, requests limit=500, documents the cap, and the second consumer (useNodeToBucketsIndex.ts) is updated to read data.sessions; tsc clean across both consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Strip — N-of-M badge, bi-source fillClass, 1y consumer/toggle renames</name>
  <files>integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx, integrations/unified-viewer/src/panels/coding/LslTimelineStrip.test.tsx</files>
  <read_first>
    - integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx (the 'all' consumer sites L405/L452/L477/L479/L497, the ToggleGroupItem block L826-837, the fillClass branch L919-921, the tick className L945, the toggle row L819-846, the useLslSessions consumption L423)
    - integrations/unified-viewer/src/panels/coding/LslTimelineStrip.test.tsx (the existing vitest idiom + the 'all' prose comments at L1630-1797 — non-functional, but the auto-slide tests around them may assert the renamed fall-through value)
    - integrations/unified-viewer/src/graph/color-fallback.ts lines 44-73 (the existing source==='auto'?ONLINE:BATCH mental model to mirror)
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-PATTERNS.md § "LslTimelineStrip.tsx" (the fillClass split sketch, the ToggleGroupItem verbatim idiom, the badge sketch with data-testid="lsl-nofm-badge")
    - .planning/phases/61-lsl-timeline-okb-routing-honesty/61-RESEARCH.md § "Unknown 2" (amber recommendation, avoid slate) and § "'all'-key consumer inventory"
  </read_first>
  <behavior>
    - Strip renders a `data-testid="lsl-nofm-badge"` element reading "showing N of M" only when total > rendered tick count
    - Window toggle renders items 24h / 7d / 30d / 1y; no "all"/"All time" item
    - A tick whose session.source==='batch' gets the amber fillClass; source==='online' (or absent) gets pink; halo/selection (blue) and disabled (opacity-40) classes still compose on top unchanged
  </behavior>
  <action>
    (a) Consume the widened hook (Task 1): `const { data } = useLslSessions(...)` at L423 now yields `{ sessions, total }`. Update the strip to read `data?.sessions` for the tick list and `data?.total` for the badge `M`. Wherever the strip previously treated `data` as the session array, switch to `data?.sessions ?? []`.

    (b) Bi-source tick color (D-08): split the non-halo arm of `fillClass` (L919-921) on `s.source`. Keep `bg-pink-300 hover:bg-pink-400` for online/auto (the existing convention); add `s.source === 'batch' ? 'bg-amber-300 hover:bg-amber-400' : 'bg-pink-300 hover:bg-pink-400'`. Use AMBER (not slate — slate reads too close to the opacity-40 disabled dim per RESEARCH). Do NOT touch the disabled (`opacity-40`) or ring (`ring-blue-*`) classes — they compose on top at L945 unchanged.

    (c) 'all'->'1y' consumer renames (D-06): auto-slide ladder fall-through (L405) `: 'all'` -> `: '1y'`; allOriginMs guard (L452) `windowKey !== 'all'` -> `'1y'`; isAll (L477) `windowKey === 'all'` -> `'1y'`; `WINDOW_MS['all']` (L479) -> `WINDOW_MS['1y']`; onWindowChange guard (L497) `next === 'all'` -> `next === '1y'`. The `WINDOW_MS[windowKey]` indexed reads (L401, L488, L868) are key-safe — no literal change. Rename the ToggleGroupItem (L835-837): `value="all"` -> `value="1y"`, `aria-label="All time"` -> `aria-label="1 year"`, visible label `all`/`All` -> `1y`. Copy the exact `className="text-[10px] h-6 px-1.5"` from the sibling 24h/7d/30d items (L826-834). Update the prose comments referencing `'all'` (L96/98/211/335/336/387/446/447/469/471/474) for clarity — non-functional.

    (d) N-of-M badge (D-01/D-02): render near the toggle row (L819-846) a `<span data-testid="lsl-nofm-badge" className="text-[10px] text-muted-foreground px-1.5">showing {N} of {M}</span>` ONLY when `typeof total === 'number' && total > N`, where `N` is the rendered tick count (the deduped `sessions.length` actually drawn as ticks) and `M` is `data.total`. Use the existing `text-[10px] text-muted-foreground` chrome styling (matches the scale-row at L846). The honest comparison is M-total vs ticks-rendered.

    (e) Update LslTimelineStrip.test.tsx: the prose `'all'` comments (L1630-1797) are non-functional; if any auto-slide test asserts the fall-through window value equals `'all'`, update that assertion to `'1y'`. Add (or extend) a test asserting the toggle renders a `1y` item and no `all` item, and a test asserting a `source: 'batch'` session tick gets the amber class while a `source: 'online'` tick gets pink (mock the hook to return both). Add a test that the `lsl-nofm-badge` renders when `total > sessions.length` and is absent when not.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/unified-viewer && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run src/panels/coding/LslTimelineStrip.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lsl-nofm-badge" src/panels/coding/LslTimelineStrip.tsx` matches (badge present)
    - `grep -n "source === 'batch'" src/panels/coding/LslTimelineStrip.tsx` matches and `grep -n "bg-amber-300" src/panels/coding/LslTimelineStrip.tsx` matches (bi-source color)
    - `grep -n "value=\"1y\"" src/panels/coding/LslTimelineStrip.tsx` matches; `grep -nE "'all'|value=\"all\"" src/panels/coding/LslTimelineStrip.tsx` returns NOTHING in functional code (prose-only comments may be updated away too)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/panels/coding/LslTimelineStrip.test.tsx` passes including new badge + bi-color + 1y-toggle tests
  </acceptance_criteria>
  <done>Strip renders the N-of-M badge, amber/pink bi-source ticks, and a 1y toggle with no 'all' literal; halo/disabled classes untouched; vitest + tsc green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of badge, 1y ladder, and bi-source tick color</name>
  <what-built>
    The LSL timeline strip now shows: a "showing N of M" badge when the window's session count exceeds the rendered ticks, a window ladder reading 24h/7d/30d/1y (no "all"), and two tick colors — amber for manual/batch sessions, pink for online/auto. Backend (Plan 01) supplies the total + per-session source.
  </what-built>
  <how-to-verify>
    Prereq: ensure Plan 01's obs-api change is deployed — `launchctl kickstart -k gui/$(id -u)/com.coding.obs-api`. The live viewer surface is the Vite dev server on :5173 (source edits are live; no rebuild needed). If verifying the production bind-mount instead, run `cd integrations/unified-viewer && npm run build && docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend` first.

    Per CLAUDE.md, use the `gsd-browser` CLI (NOT a hand-rolled Playwright script) for the visual smoke:
    1. `gsd-browser navigate http://localhost:5173/viewer/coding`
    2. `gsd-browser screenshot` — confirm the window toggle row reads 24h / 7d / 30d / 1y (NO "all" / "All time").
    3. Select the `1y` window. Confirm a "showing N of M" badge appears near the toggle IF the year holds more sessions than the rendered ticks (it should, given 12 months of history vs the 500 cap). Confirm ticks are visible back toward the window edge — not a silent cliff at "no data prior to May 27".
    4. Confirm two distinct tick colors are visible: amber ticks (manual/batch — wave-analysis runs) and pink ticks (online/auto). Hover is NOT required to tell them apart.
    5. Confirm the blue selection/halo rings and the greyed-out 0-obs disabled ticks still render correctly (not overwritten by the new color).
  </how-to-verify>
  <resume-signal>Type "approved" if the badge, 1y ladder, and amber/pink bi-source ticks all render correctly; otherwise describe what's wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unified-viewer (browser) -> obs-api :12436 | strip fetches sessions (incl. new total/source) over localhost HTTP and renders counts + colors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | Tampering (XSS) | N-of-M badge text | mitigate | `N` and `M` are numbers rendered as React text children (default escaping); no string interpolation from untrusted source into markup, no `dangerouslySetInnerHTML`. |
| T-61-03-02 | Information Disclosure | `source` enum on ticks | accept | `source` is a coarse 2-value bucket ('online'|'batch') already exposed by Plan 01; rendering it as a tick color leaks nothing beyond session provenance class. |
| T-61-03-03 | Spoofing (visual deception) | misleading "all" label hiding a 365-day cap | mitigate | This plan REMOVES the deceptive surface: '1y' label is honest, and the N-of-M badge prevents silent truncation. The threat being closed IS the phase goal. |
| T-61-03-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed (RESEARCH Package Legitimacy Audit: not applicable). No install task -> no legitimacy checkpoint. |
</threat_model>

<verification>
- `cd integrations/unified-viewer && npx tsc --noEmit` exits 0 (the { sessions, total } widen propagates to BOTH the strip and useNodeToBucketsIndex)
- `npx vitest run src/panels/coding/LslTimelineStrip.test.tsx` green (badge + bi-color + 1y-toggle tests)
- `grep -nE "'all'|value=\"all\"" src/panels/coding/useLslSessions.ts src/panels/coding/LslTimelineStrip.tsx` returns nothing in functional code
- Human-verify checkpoint (Task 3) confirms the visual result via gsd-browser
</verification>

<success_criteria>
- Visible "showing N of M" badge when rendered ticks < total (LSLTIME-01)
- Window ladder reads 24h/7d/30d/1y, no 'all' literal anywhere (LSLTIME-02)
- Amber (batch) vs pink (online) ticks distinguishable at a glance (LSLTIME-03)
- Client cap raised to 500; type-checks clean; human-verify approved
</success_criteria>

<output>
Create `.planning/phases/61-lsl-timeline-okb-routing-honesty/61-03-SUMMARY.md` when done
</output>
