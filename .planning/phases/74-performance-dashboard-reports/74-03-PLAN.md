---
phase: 74-performance-dashboard-reports
plan: 03
type: execute
wave: 3
depends_on: ["74-01", "74-02"]
files_modified:
  - .data/ontologies-experiment/experiment-ontology.json
  - lib/experiments/report-write.mjs
  - lib/experiments/report-read.mjs
autonomous: true
requirements: [KB-04, DASH-03]
user_setup: []

must_haves:
  truths:
    - "The Report ontology class declares scalar properties (reportId, title, createdBy, createdAt, snapshotFrozenAt)"
    - "writeReport mints a UUIDv7 once and is idempotent on metadata.report_id"
    - "refreshReport re-runs the saved query and overwrites the snapshot while reusing the same entity id"
    - "A saved Report's rendered snapshot is stable until an explicit refresh"
  artifacts:
    - path: ".data/ontologies-experiment/experiment-ontology.json"
      provides: "Filled-in Report class (KB-04)"
      contains: "\"Report\""
    - path: "lib/experiments/report-write.mjs"
      provides: "writeReport / refreshReport (idempotent strict-path entity write)"
      exports: ["writeReport", "refreshReport"]
    - path: "lib/experiments/report-read.mjs"
      provides: "readReports / readReport (parse serialized snapshot + facet_state)"
      exports: ["readReports", "readReport"]
  key_links:
    - from: "lib/experiments/report-write.mjs"
      to: "experiment LevelDB (Report entity)"
      via: "store.putEntity strict path, mintEntityId once, idempotency on metadata.report_id"
      pattern: "mintEntityId"
    - from: "lib/experiments/report-write.mjs"
      to: "lib/experiments/query.mjs"
      via: "refreshReport calls readRuns to recompute the snapshot"
      pattern: "readRuns"
---

<objective>
Fill in the `Report` ontology class (currently a schema-only stub) and build the Report writer/reader that realize KB-04 (saved query + stable snapshot) and DASH-03 (render from the frozen snapshot, manual Refresh re-runs the query). Resolve RESEARCH Open Question 1: Reports are stored as `Report` entities in the dedicated experiment store (the KB-04 intended home, per D-05 discretion) — single source of truth, idempotent like writeScore. Turn the Plan 01 `report-write.test.mjs` and `report-snapshot.test.mjs` GREEN.

Purpose: The Reports sub-view (Plan 06) and the report endpoints (Plan 04) depend on these. The snapshot-stability property is the load-bearing DASH-03 correctness guarantee.
Output: filled `Report` class + `lib/experiments/report-write.mjs` + `lib/experiments/report-read.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-performance-dashboard-reports/74-PATTERNS.md
@.planning/phases/74-performance-dashboard-reports/74-RESEARCH.md
@lib/experiments/score-write.mjs
@lib/experiments/store.mjs

<interfaces>
<!-- From the live codebase. -->

Report stub today — experiment-ontology.json:78-83 (extends Feature, empty properties/relationships).
Sibling Score class — experiment-ontology.json:65-77 (declares scalar props; variable/serialized payloads ride in metadata).

writeReport analog — score-write.mjs:72-157 (writeScore idempotent strict-path write). Mirror exactly:
- caller passes an ALREADY-OPEN store
- idempotency: `for await (const e of store.iterate({ entityType:'Report' })) if (e.metadata?.report_id === reportId) { existingId = e.id; break; }`
- `id: existingId ?? mintEntityId()` — mint UUIDv7 ONCE; NEVER use reportId as the entity id (parseEntityId requires UUIDv7)
- synthetic provenance: `{ provider:'coding-dashboard', model:'n/a', runId: reportId, timestamp: ISO }` (store never invents one, D-30)
- `entityType:'Report'`, `layer:'evidence'`, metadata.domain:'experiment' (exporter buckets → experiment.json)
- serialize objects/arrays into metadata via JSON.stringify (Score serializes event_labels this way, score-write.mjs:134); parse on read
- metadata fields: report_id, facet_state (JSON), snapshot (JSON), snapshot_frozen_at (ISO), title, created_by

refreshReport: re-run the saved facet_state query (call readRuns from query.mjs), overwrite snapshot + snapshot_frozen_at, REUSE the same id via the iterate-find lookup (D-04 same idempotent path).

readReports/readReport: iterate Report entities; JSON.parse(metadata.snapshot) and metadata.facet_state on read; readReport(reportId) finds by metadata.report_id.

import: `import { mintEntityId } from '@fwornle/km-core';`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill in the Report ontology class</name>
  <read_first>
    - .data/ontologies-experiment/experiment-ontology.json (lines 65-83 — the Score sibling pattern + the Report stub to replace)
    - .planning/phases/74-performance-dashboard-reports/74-PATTERNS.md (§experiment-ontology.json Report fill-in — the exact shape)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Report Schema Design — declared scalar props; serialized payloads ride in metadata)
  </read_first>
  <files>.data/ontologies-experiment/experiment-ontology.json</files>
  <action>Replace the empty `Report` stub (lines 78-83) with the filled class per KB-04: keep `extends: "Feature"`; set description to "Saved query (facet-state) + frozen results snapshot (KB-04 / DASH-03)."; set `relationships: { "references": ["Run"] }`; declare scalar `properties`: `reportId`, `title`, `createdBy`, `createdAt`, `snapshotFrozenAt` (each `{ "type": "string" }` with a one-line description). Do NOT declare `facet_state`/`snapshot` as properties — they ride in metadata as JSON strings (mirroring how Score keeps event_labels/corrected_* in metadata). Preserve the rest of the file byte-for-byte (only the Report block changes). Keep valid JSON (no trailing comma).</action>
  <verify>
    <automated>node -e "const o=require('./.data/ontologies-experiment/experiment-ontology.json');const r=o.classes.Report;if(r.extends!=='Feature'||!r.properties.reportId||!r.relationships.references)process.exit(1);console.error('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `.data/ontologies-experiment/experiment-ontology.json` parses as valid JSON (the node -e load succeeds).
    - Source assertion: `classes.Report.properties` contains reportId, title, createdBy, createdAt, snapshotFrozenAt.
    - Source assertion: `classes.Report.relationships.references` includes "Run".
    - Source assertion: `classes.Score` block is unchanged (`git diff --stat` shows only the Report region modified; reviewer confirms no other class touched).
  </acceptance_criteria>
  <done>Report class declares its scalar properties + Run reference; rest of the ontology unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: writeReport + refreshReport (report-write.mjs)</name>
  <read_first>
    - lib/experiments/score-write.mjs (lines 72-157 — the idempotent mint-once + iterate-find + strict putEntity + synthetic provenance pattern to mirror exactly)
    - lib/experiments/query.mjs (readRuns — refreshReport calls it to recompute the snapshot) [created in Plan 02]
    - tests/experiments/report-write.test.mjs + tests/experiments/report-snapshot.test.mjs (the RED tests this turns GREEN — idempotency + stability contracts)
    - .planning/phases/74-performance-dashboard-reports/74-PATTERNS.md (§report-write.mjs — writeReport/refreshReport block)
  </read_first>
  <files>lib/experiments/report-write.mjs</files>
  <action>Create `lib/experiments/report-write.mjs` exporting `async function writeReport(store, { reportId, title, createdBy, facetState, snapshotRows })` and `async function refreshReport(store, reportId)`. Both receive an ALREADY-OPEN store. `writeReport`: iterate `{ entityType:'Report' }` to find an existing entity by `metadata.report_id === reportId`; `putEntity` strict-path with `id: existingId ?? mintEntityId()` (mint UUIDv7 once — NEVER reportId), `name: \`${reportId}-report\``, `entityType:'Report'`, `layer:'evidence'`, synthetic provenance, metadata `{ domain:'experiment', report_id, facet_state: JSON.stringify(facetState), snapshot: JSON.stringify(snapshotRows), snapshot_frozen_at: new Date().toISOString(), title, created_by: createdBy }`. `refreshReport(store, reportId)`: find the existing Report (throw a clear Error if absent), parse its `facet_state`, re-run the query by calling `readRuns(store, parsedFacetState)` (apply the saved facet filters — at minimum includePending and any saved filters; filtering may be applied in-memory over readRuns output), then putEntity on the SAME id overwriting `snapshot` + `snapshot_frozen_at` only (preserve title/created_by/facet_state). Return the entity id from both. Log via `process.stderr.write`. Never construct a store inline (no `new GraphKMStore`).</action>
  <verify>
    <automated>node --test tests/experiments/report-write.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `node --test tests/experiments/report-write.test.mjs` passes (GREEN) — idempotency on report_id + refresh reuses id + updates snapshot_frozen_at.
    - Source assertion: `grep -c "mintEntityId" lib/experiments/report-write.mjs` ≥ 1 and reportId is NOT used as the entity id (reviewer confirms `id: existingId ?? mintEntityId()`).
    - Source assertion: `grep -n "domain: 'experiment'" lib/experiments/report-write.mjs` matches (exporter bucketing).
    - Source assertion: `grep -n "readRuns" lib/experiments/report-write.mjs` matches (refresh re-runs the query).
    - Source assertion: `grep -c "new GraphKMStore" lib/experiments/report-write.mjs` is 0; no `console.` (comment-filtered).
  </acceptance_criteria>
  <done>writeReport is idempotent on report_id (UUIDv7 minted once); refreshReport recomputes the snapshot on the same node; report-write.test.mjs green.</done>
</task>

<task type="auto">
  <name>Task 3: readReports + readReport (report-read.mjs) and snapshot-stability proof</name>
  <read_first>
    - lib/experiments/report-write.mjs (the metadata shape readReports must parse) [created in Task 2]
    - lib/experiments/score-write.mjs (iterate-by-entityType read idiom)
    - tests/experiments/report-snapshot.test.mjs (the load-bearing DASH-03 stability test this turns GREEN)
    - .planning/phases/74-performance-dashboard-reports/74-VALIDATION.md (§Load-bearing correctness properties — snapshot stability)
  </read_first>
  <files>lib/experiments/report-read.mjs</files>
  <action>Create `lib/experiments/report-read.mjs` exporting `async function readReports(store)` and `async function readReport(store, reportId)`. `readReports`: iterate `{ entityType:'Report' }`, map each to `{ reportId: m.report_id, title: m.title, createdBy: m.created_by, snapshotFrozenAt: m.snapshot_frozen_at, facetState: JSON.parse(m.facet_state ?? '{}'), snapshot: JSON.parse(m.snapshot ?? '[]') }`. `readReport(store, reportId)`: same mapping for the single entity whose `metadata.report_id === reportId`; return null if absent. Both receive an open store; no store construction. The DASH-03 stability guarantee is structural: readReport returns the STORED snapshot, never re-queries (only refreshReport re-queries) — ensure readReport does NOT call readRuns. Log via `process.stderr.write`.</action>
  <verify>
    <automated>node --test tests/experiments/report-snapshot.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `node --test tests/experiments/report-snapshot.test.mjs` passes (GREEN) — snapshot UNCHANGED after an underlying Run/Score mutation, CHANGED only after refreshReport.
    - Source assertion: `readReport`/`readReports` do NOT import or call `readRuns` (`grep -c "readRuns" lib/experiments/report-read.mjs` is 0 — render-from-snapshot, never re-query on view).
    - Source assertion: `grep -n "JSON.parse" lib/experiments/report-read.mjs` matches (deserializes snapshot + facet_state).
    - Source assertion: no `new GraphKMStore`, no `console.` (comment-filtered).
  </acceptance_criteria>
  <done>readReports/readReport return the frozen snapshot without re-querying; the snapshot-stability property is proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer → experiment LevelDB | Report entities written strict-path to the dedicated store only |
| report title/facet strings → stored metadata | Operator-supplied strings persisted as serialized JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03-01 | Tampering | path/store confusion writing Reports to the shared KG | mitigate | Reports go ONLY through the open experiment store (caller uses openExperimentStore); writer never imports UKBDatabaseWriter or the shared KG |
| T-74-03-02 | Tampering | non-UUIDv7 id throwing in parseEntityId | mitigate | mint UUIDv7 once via mintEntityId; idempotency keyed on metadata.report_id, never the id |
| T-74-03-03 | Tampering (XSS substrate) | report title/facet strings | accept→mitigate downstream | Stored as plain serialized JSON; React escapes on render (Plan 06); no dangerouslySetInnerHTML |
| T-74-03-SC | Tampering | npm installs | mitigate | No new packages (km-core pre-existing, Approved). No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `node --test tests/experiments/report-write.test.mjs tests/experiments/report-snapshot.test.mjs` — both green.
- `node --test tests/experiments/` — full suite stays green.
- Report ontology JSON parses; only the Report block changed.
</verification>

<success_criteria>
- Report class filled in (KB-04 schema home).
- writeReport idempotent (UUIDv7 once); refreshReport recomputes on same node.
- readReport renders the frozen snapshot, stable until refresh (DASH-03).
</success_criteria>

<output>
Create `.planning/phases/74-performance-dashboard-reports/74-03-SUMMARY.md` when done.
</output>
