---
phase: 74-performance-dashboard-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/experiments/_fixtures/seed-experiment-store.mjs
  - tests/experiments/run-read.test.mjs
  - tests/experiments/runs-endpoint.test.mjs
  - tests/experiments/timeline-read.test.mjs
  - tests/experiments/report-write.test.mjs
  - tests/experiments/report-snapshot.test.mjs
  - tests/e2e/dashboard/performance.spec.ts
autonomous: true
requirements: [DASH-01, DASH-02, KB-04, DASH-03]
user_setup: []

must_haves:
  truths:
    - "Every Wave 0 test file exists and runs (RED — fails because the production module it targets does not exist yet)"
    - "A shared seedIsolatedStore fixture is importable by all new experiment tests"
    - "The Playwright performance spec exists and is collected by the runner"
  artifacts:
    - path: "tests/experiments/_fixtures/seed-experiment-store.mjs"
      provides: "Reusable isolated-store seeding helper (tmp repoRoot + verbatim ontology copy + writeRun/writeScore seed)"
      exports: ["seedIsolatedStore"]
    - path: "tests/experiments/run-read.test.mjs"
      provides: "DASH-01 readRuns join/pending-exclude RED test"
    - path: "tests/experiments/runs-endpoint.test.mjs"
      provides: "DASH-01 GET /api/experiments/runs endpoint RED test"
    - path: "tests/experiments/timeline-read.test.mjs"
      provides: "DASH-02 readTimeline grouping/readonly/graceful-empty RED test"
    - path: "tests/experiments/report-write.test.mjs"
      provides: "KB-04 writeReport/refreshReport idempotency RED test"
    - path: "tests/experiments/report-snapshot.test.mjs"
      provides: "DASH-03 snapshot-stability RED test"
    - path: "tests/e2e/dashboard/performance.spec.ts"
      provides: "DASH-01/02/03 Playwright UI-flow skeleton"
  key_links:
    - from: "tests/experiments/run-read.test.mjs"
      to: "tests/experiments/_fixtures/seed-experiment-store.mjs"
      via: "import { seedIsolatedStore }"
      pattern: "seedIsolatedStore"
---

<objective>
Lay the Nyquist validation scaffold for Phase 74 BEFORE any production code. Factor the inline `seedIsolatedStore` (currently embedded in `score-override-endpoint.test.mjs:40`) into a shared fixture, then author every Wave 0 test file in a RED state — each one fails only because the production module it exercises (`query.mjs`, `timeline-read.mjs`, `report-write.mjs`, the new endpoints, the Performance page) does not exist yet.

Purpose: Satisfies 74-VALIDATION.md Wave 0 (Dimension 8) so downstream plans implement against a failing test, not into a vacuum. Each later plan turns its target RED test GREEN.
Output: 1 shared fixture + 5 node:test RED files + 1 Playwright skeleton spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-performance-dashboard-reports/74-VALIDATION.md
@.planning/phases/74-performance-dashboard-reports/74-PATTERNS.md

<interfaces>
<!-- Extracted from the live codebase — executor uses these directly, no exploration needed. -->

From tests/experiments/score-override-endpoint.test.mjs:40-52 (the inline seed to factor out):
- `async function seedIsolatedStore(taskId='t1')` → `{ repoRoot, cleanup }`
- mkdtemp tmp; copy EVERY file from `<REPO>/.data/ontologies-experiment` into `<tmp>/.data/ontologies-experiment` verbatim (`fs.copyFileSync`)
- `openExperimentStore({ repoRoot: tmp })` then `writeRun(...)` + `writeScore(...)`, close in finally
- REPO_ROOT resolves via `path.resolve(__dirname,'..','..')`; SRC_ONTOLOGY_DIR = `<REPO_ROOT>/.data/ontologies-experiment`

From lib/experiments/store.mjs:
- `export async function openExperimentStore(opts?: { repoRoot?: string }): Promise<GraphKMStore>` — caller owns close()

From lib/experiments/run-write.mjs (seed shape — see score-override-endpoint.test.mjs:54-70):
- `writeRun(store, { span:{task_id,started_at,ended_at,goal_sentence}, taskClass, pending, tags:{task_hash,agent,model,framework,trace_id}, totals:{input_tokens,output_tokens,total_tokens,reasoning_tokens,calls} })`

From lib/experiments/score-write.mjs:
- `writeScore(store, { span:{task_id}, judgment:{goal_aligned_ratio, event_labels, rubric:{goal_achieved,code_quality,test_coverage,regressions,spec_drift}, ...} })`
- `applyOverride(store, { taskId, dimension, value, by })` — dimensions: goal_achieved|code_quality|test_coverage|regressions|spec_drift

From lib/experiments/token-aggregate.mjs (timeline DB analog):
- `new Database(dbPath, { readonly:true, fileMustExist:true })`; missing file → graceful empty (NOT throw)
- token_usage columns: timestamp, granularity_tier, tool_call_id, parent_call_id, reasoning_tokens, input_tokens, output_tokens, total_tokens, tokens_estimated, model, task_id, agent
- per-reasoning-step tool_call_id form: `${baseToolCallId}:reason:${N}`

Test runner: `node --test tests/experiments/<file>.test.mjs` (NOT jest). node:test + node:assert/strict.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Factor the shared seedIsolatedStore fixture</name>
  <read_first>
    - tests/experiments/score-override-endpoint.test.mjs (lines 1-95 — the inline seedIsolatedStore + Run/Score seed shape to extract)
    - lib/experiments/store.mjs (openExperimentStore signature + repoRoot override)
    - .planning/phases/74-performance-dashboard-reports/74-PATTERNS.md (§tests/experiments/*.test.mjs — the seeding-helper analog block)
  </read_first>
  <files>tests/experiments/_fixtures/seed-experiment-store.mjs</files>
  <action>Create `tests/experiments/_fixtures/seed-experiment-store.mjs` exporting `seedIsolatedStore(taskId='t1', overrides={})`. Lift the body from score-override-endpoint.test.mjs:40-75: mkdtemp under os.tmpdir(); copy EVERY file from `<REPO_ROOT>/.data/ontologies-experiment` into `<tmp>/.data/ontologies-experiment` verbatim via fs.copyFileSync; resolve REPO_ROOT via `path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..','..')` (fixture is one dir deeper than the test). Open via `openExperimentStore({ repoRoot: tmp })`, seed a Run + Score with the same shape the existing test uses (allow `overrides` to tweak taskClass/pending/tags/totals/judgment), close in finally. Return `{ repoRoot: tmp, cleanup: () => fs.rmSync(tmp,{recursive:true,force:true}) }`. Also export a `seedTokenDb(rows)` helper that writes a throwaway SQLite token-usage.db (better-sqlite3, writable in the FIXTURE only) with the token_usage columns listed in interfaces and returns its path — for timeline-read tests to point `dbPathOverride` at. Use `process.stderr.write` for any logging (no-console-log).</action>
  <verify>
    <automated>node -e "import('./tests/experiments/_fixtures/seed-experiment-store.mjs').then(m=>{if(!m.seedIsolatedStore||!m.seedTokenDb)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file exports both `seedIsolatedStore` and `seedTokenDb`.
    - Behavior assertion: `node -e "import('./tests/experiments/_fixtures/seed-experiment-store.mjs').then(async m=>{const s=await m.seedIsolatedStore('t1');s.cleanup()})"` exits 0 (seeds + cleans an isolated store).
    - Source assertion: `grep -n "openExperimentStore" tests/experiments/_fixtures/seed-experiment-store.mjs` matches (uses the factory, never `new GraphKMStore`).
    - Source assertion: no `console.` token: `grep -v '^//' tests/experiments/_fixtures/seed-experiment-store.mjs | grep -c "console\." | grep -qx 0`.
  </acceptance_criteria>
  <done>Shared fixture exists, seeds an isolated experiment store + a throwaway token-usage.db, and cleans up.</done>
</task>

<task type="auto">
  <name>Task 2: Author the four backend RED test files</name>
  <read_first>
    - tests/experiments/_fixtures/seed-experiment-store.mjs (the fixture from Task 1)
    - tests/experiments/score-override-endpoint.test.mjs (Object.create(ApiRoutes.prototype) + experimentRepoRoot handler-invocation idiom for runs-endpoint test)
    - tests/experiments/token-aggregate.test.mjs (readonly-DB + graceful-missing-file assertion idiom for timeline-read test)
    - .planning/phases/74-performance-dashboard-reports/74-VALIDATION.md (§Per-Task Verification Map + §Load-bearing correctness properties)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Code Examples — readRuns join shape; §Timeline Data Source — :reason:N grouping)
  </read_first>
  <files>tests/experiments/run-read.test.mjs, tests/experiments/runs-endpoint.test.mjs, tests/experiments/timeline-read.test.mjs, tests/experiments/report-write.test.mjs, tests/experiments/report-snapshot.test.mjs</files>
  <action>Write five node:test files, each importing the Task-1 fixture and asserting the documented behavior; each MUST currently fail at import (the production module is absent) — that is the RED state. (1) `run-read.test.mjs`: imports `../../lib/experiments/query.mjs`#`readRuns`; assert it returns Runs joined to Score+Outcome by `metadata.run_task_id`, and that `pending` Runs are excluded unless `includePending:true` (D-06). (2) `runs-endpoint.test.mjs`: invoke `handleRunsQuery` via `Object.create(ApiRoutes.prototype)` with `experimentRepoRoot` set to the seeded tmp root (mirror score-override-endpoint.test.mjs), assert 200 + rows array. (3) `timeline-read.test.mjs`: imports `../../lib/experiments/timeline-read.mjs`#`readTimeline`; seed a token-usage.db via `seedTokenDb` with one per-turn parent (`tool_call_id='turn1'`) + two per-reasoning-step children (`tool_call_id='turn1:reason:0'`,`'turn1:reason:1'`), assert children nest under the parent; assert a missing DB path returns graceful empty (NOT a throw); assert task_id is bound as `?` (seed a second task_id's rows and confirm they are excluded). (4) `report-write.test.mjs`: imports `../../lib/experiments/report-write.mjs`#`writeReport`/`refreshReport`; assert writeReport is idempotent on `report_id` (two calls → same entity id, one Report node), and refreshReport reuses the same id while updating `snapshot_frozen_at`. (5) `report-snapshot.test.mjs` (the load-bearing DASH-03 property): writeReport a snapshot, mutate an underlying Run/Score, re-read the Report → snapshot UNCHANGED; then refreshReport → snapshot now reflects the mutation. Each test calls `cleanup()` in an afterEach/finally.</action>
  <verify>
    <automated>for f in run-read runs-endpoint timeline-read report-write report-snapshot; do node --test tests/experiments/$f.test.mjs 2>&1 | grep -qE "fail|error|Cannot find|not a function" || { echo "EXPECTED-RED-MISSING:$f"; exit 1; }; done; echo ALL-RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: each of the five files, when run via `node --test`, fails (RED) due to the absent production module — the verify loop prints `ALL-RED-AS-EXPECTED`.
    - Source assertion: every test file imports `_fixtures/seed-experiment-store.mjs` (`grep -l seedIsolatedStore tests/experiments/{run-read,runs-endpoint,report-write,report-snapshot}.test.mjs` lists all four; timeline-read imports `seedTokenDb`).
    - Source assertion: `runs-endpoint.test.mjs` contains `experimentRepoRoot` and `Object.create`.
    - Source assertion: `report-snapshot.test.mjs` asserts snapshot equality BEFORE refresh and inequality AFTER (the DASH-03 stability contract).
    - Source assertion: no `console.` in any of the five files.
  </acceptance_criteria>
  <done>Five RED node:test files exist, import the shared fixture, and each fails only because its target production module is unwritten.</done>
</task>

<task type="auto">
  <name>Task 3: Author the Playwright performance spec skeleton</name>
  <read_first>
    - tests/e2e/dashboard/workflow-graph-colors.spec.ts (the only existing dashboard spec — copy its setup/baseURL/test-structure)
    - tests/e2e/dashboard/playwright.config.ts (baseURL + projects)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Interaction Contracts — the flows to cover: facet narrow, drawer open, timeline expand, report save)
  </read_first>
  <files>tests/e2e/dashboard/performance.spec.ts</files>
  <action>Create `tests/e2e/dashboard/performance.spec.ts` mirroring workflow-graph-colors.spec.ts setup. Author the UI-flow assertions as `test.fixme(...)` (or `test.skip` with a TODO referencing the plan that delivers each): (a) navigates to `/performance`, page `h1` reads `Performance`; (b) the faceted sidebar renders and selecting a facet narrows the runs table; (c) clicking a run row opens the right-side detail drawer (Sheet); (d) a timeline parent turn expands to reveal per-reasoning-step sub-bands and shows a granularity_tier badge; (e) `Save report` freezes a snapshot that appears in the Reports sub-view. Marking them fixme keeps the suite green-collectible now and Plan 06 flips them live. Add a single ACTIVE smoke test that just asserts the spec file is collected (a trivial `expect(true).toBe(true)` placeholder is acceptable as the collection witness).</action>
  <verify>
    <automated>npx playwright test tests/e2e/dashboard/performance.spec.ts --list 2>&1 | grep -qi "performance" && echo SPEC-COLLECTED</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `npx playwright test tests/e2e/dashboard/performance.spec.ts --list` collects the spec (prints `SPEC-COLLECTED`).
    - Source assertion: the file contains `test.fixme` (or `test.skip`) markers for the five UI flows a-e, each with a TODO naming the delivering plan.
    - Source assertion: navigation target is `/performance` and the title assertion text is `Performance`.
  </acceptance_criteria>
  <done>Playwright spec exists, is collected by the runner, and stubs the five DASH UI flows as fixme tests for Plan 06 to activate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → experiment LevelDB | Tests open isolated tmp stores only — never the real single-owner store |
| test fixture → token-usage.db | The `seedTokenDb` helper is the ONLY writable open of a token-usage.db in this phase, and it writes a throwaway temp DB, never the proxy-owned `.data/llm-proxy/token-usage.db` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01-01 | Tampering | test seed touching the real single-owner store | mitigate | `seedIsolatedStore` always opens via `openExperimentStore({ repoRoot: tmp })` into a mkdtemp dir; cleanup removes it |
| T-74-01-02 | Tampering | test writing the proxy-owned token-usage.db | mitigate | `seedTokenDb` writes ONLY a throwaway temp SQLite file; the real `.data/llm-proxy/token-usage.db` is never opened writable |
| T-74-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (tests only); RESEARCH §Package Legitimacy Audit confirms zero new backend deps. No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `node --test tests/experiments/run-read.test.mjs tests/experiments/runs-endpoint.test.mjs tests/experiments/timeline-read.test.mjs tests/experiments/report-write.test.mjs tests/experiments/report-snapshot.test.mjs` — all RED (target modules unwritten).
- `npx playwright test tests/e2e/dashboard/performance.spec.ts --list` — spec collected.
- `grep -rL "console\." tests/experiments/_fixtures/seed-experiment-store.mjs` — no console usage.
</verification>

<success_criteria>
- Shared `seedIsolatedStore` + `seedTokenDb` fixture importable and self-cleaning.
- All five backend RED tests fail ONLY because their production module is absent.
- Playwright spec collected with five fixme UI flows.
</success_criteria>

<output>
Create `.planning/phases/74-performance-dashboard-reports/74-01-SUMMARY.md` when done.
</output>
