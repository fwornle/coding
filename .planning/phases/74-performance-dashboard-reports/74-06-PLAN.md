---
phase: 74-performance-dashboard-reports
plan: 06
type: execute
wave: 6
depends_on: ["74-05"]
files_modified:
  - integrations/system-health-dashboard/src/components/performance/score-drawer.tsx
  - integrations/system-health-dashboard/src/components/performance/reports-subview.tsx
  - integrations/system-health-dashboard/src/pages/performance.tsx
  - tests/e2e/dashboard/performance.spec.ts
autonomous: false
requirements: [KB-04, DASH-03, SCORE-02]
user_setup: []

must_haves:
  truths:
    - "Clicking a run row opens a right-side detail drawer showing all 5 rubric dimensions with judged values, editable corrected_* inputs, and the rationale"
    - "Saving the drawer issues PATCH /api/experiments/scores/:taskId and the corrected value appears with an 'edited' marker in the table"
    - "Drawer inputs respect the same value ranges the server enforces (regressions 0|1, others 0-1)"
    - "Save report freezes the current facet-state + results snapshot; the saved report renders the frozen snapshot stably"
    - "Refresh snapshot re-runs the saved query and updates the snapshot with a frozen-at relative time"
  artifacts:
    - path: "integrations/system-health-dashboard/src/components/performance/score-drawer.tsx"
      provides: "Sheet drawer with 5 rubric rows + corrected_* inputs → PATCH (D-02/SCORE-02)"
    - path: "integrations/system-health-dashboard/src/components/performance/reports-subview.tsx"
      provides: "Saved Reports sub-view: save/list/render-snapshot/refresh (D-04/D-05/DASH-03)"
    - path: "tests/e2e/dashboard/performance.spec.ts"
      provides: "Activated Playwright UI flows (fixme → live)"
  key_links:
    - from: "integrations/system-health-dashboard/src/components/performance/score-drawer.tsx"
      to: "/api/experiments/scores/:taskId"
      via: "PATCH per edited dimension, same-origin"
      pattern: "method:\\s*['\"]PATCH['\"]"
    - from: "integrations/system-health-dashboard/src/components/performance/reports-subview.tsx"
      to: "/api/experiments/reports"
      via: "POST save + POST :id/refresh + GET list, same-origin"
      pattern: "/api/experiments/reports"
---

<objective>
Complete the Performance tab interactive surface: the score-override drawer (D-02 / SCORE-02 — closes ROADMAP SC#5, the Phase 73 D-07 deferral) and the Saved Reports sub-view (D-04/D-05/DASH-03/KB-04). Then activate the Plan 01 Playwright flows from fixme to live. The drawer reuses the existing Phase 73 PATCH endpoint (no new write logic); the reports sub-view drives the Plan 04 report endpoints.

Purpose: This is the last interactive layer — operators correct judged scores and freeze shareable, bit-rot-proof report snapshots. After this, all four Phase 74 requirements (DASH-01/02/03, KB-04) plus SC#5 are operator-usable.
Output: score-drawer + reports-subview components, page wiring, activated E2E spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md
@.planning/phases/74-performance-dashboard-reports/74-PATTERNS.md
@lib/experiments/score-write.mjs

<interfaces>
<!-- From the live codebase + UI-SPEC. -->

Existing PATCH (Phase 73, api-routes.js:403-477; do NOT re-implement): `PATCH /api/experiments/scores/:taskId` body `{ dimension, value, overridden_by }`. Server validation (api-routes.js:411-455): dimension ∈ {goal_achieved,code_quality,test_coverage,regressions,spec_drift}; regressions ∈ {0,1}; the other four ∈ [0,1]; overridden_by non-empty ≤256. 400 = validation; 404 = score changed/missing. The drawer MUST mirror these ranges client-side but the server is authoritative.

applyOverride contract (score-write.mjs:197-249): one corrected_<dimension> slot + overridden_by + overridden_at; judged fields never clobbered. corrected_* stored separately from judged (D-06).

Plan 04 report endpoints: GET /api/experiments/reports ({reports:[{reportId,title,createdBy,snapshotFrozenAt,facetState,snapshot}]}); POST /api/experiments/reports ({reportId} on 201, body {title, facetState, snapshotRows, reportId?}); POST /api/experiments/reports/:id/refresh ({reportId, refreshedAt}).

sheet primitive (Plan 05): `src/components/ui/sheet.tsx` (Sheet, SheetContent, SheetHeader, SheetTitle, SheetFooter). Drawer is right-side `sm:max-w-md` (UI-SPEC). Fallback (if Plan 05 took the dialog fallback): right-aligned dialog.

Corrected-wins helper (Plan 05): effective()/isEdited() — reuse the same definition.

UI-SPEC locks: drawer 5 rubric rows each = judged value (read-only `text-muted-foreground`) + editable corrected_* `Input` + rationale; footer primary `Save override` (`Button` default) + `Discard changes` (`variant="ghost"`). Reports sub-view = second `Tabs` value INSIDE Performance (not a top-level tab). Copy: "Save override", "Save report", "Refresh snapshot", "Refreshing…", drawer save error "Couldn't save the override. The score may have changed — reopen the run and try again.", success "Override saved", staleness "Snapshot frozen {relative-time}. Refresh snapshot to re-run the saved query.", empty "No saved reports yet" / "Build a query in the sidebar, then Save report to freeze a snapshot you can return to."

Rebuild (FRONTEND): `cd integrations/system-health-dashboard && npm run build` + `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`.
E2E: `npx playwright test tests/e2e/dashboard/performance.spec.ts`; `gsd-browser` visual pass.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score-override drawer (Sheet) wired to the existing PATCH</name>
  <read_first>
    - integrations/system-health-dashboard/src/components/ui/sheet.tsx (the primitive from Plan 05)
    - integrations/system-health-dashboard/src/pages/performance.tsx (the onSelectRun hook + corrected-wins helper to reuse) [Plan 05]
    - lib/experiments/score-write.mjs (applyOverride dimension allowlist + value ranges — mirror client-side)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Interaction Contracts item 3 — drawer contract; §Copywriting — drawer copy + Discard changes rationale)
    - .planning/phases/74-performance-dashboard-reports/74-PATTERNS.md (§sheet.tsx + score-drawer.tsx — PATCH save idiom + ranges)
  </read_first>
  <files>integrations/system-health-dashboard/src/components/performance/score-drawer.tsx, integrations/system-health-dashboard/src/pages/performance.tsx</files>
  <behavior>
    - Selecting a run row opens the right-side drawer showing 5 rubric rows: judged value (read-only) + editable corrected_* input + rationale.
    - Editing regressions to a value other than 0 or 1 is rejected client-side before save (server is authoritative).
    - "Save override" issues one PATCH per edited dimension; on success the drawer closes, the table reflects corrected-wins, and "Override saved" appears.
    - A 404 from PATCH surfaces "Couldn't save the override. The score may have changed — reopen the run and try again."
    - "Discard changes" closes the drawer abandoning unsaved edits without any PATCH.
  </behavior>
  <action>Create `components/performance/score-drawer.tsx`: a right-side `Sheet` (`SheetContent` `sm:max-w-md`) opened by the page's selected-run state. Render the 5 rubric rows (goal_achieved, code_quality, test_coverage, regressions, spec_drift): each shows the judged value read-only (`text-muted-foreground`), an editable `corrected_*` `Input`, and the dimension's rationale text (`Separator` between rows). Mirror the server ranges client-side: regressions accepts only 0|1; the other four accept [0,1]; block save + show inline validation otherwise (server stays authoritative). Footer: primary `Save override` (`Button` default) + `Discard changes` (`Button variant="ghost"`). On Save, issue one `PATCH /api/experiments/scores/:taskId` (same-origin) per EDITED dimension with `{ dimension, value, overridden_by }` (operator id — use an existing operator/identity source if present, else a sensible default constant documented in the SUMMARY); on all-success close the drawer + surface transient "Override saved" + refetch runs so corrected-wins shows; on 404 show the reopen copy; on 400 show inline validation. Wire the page's `onSelectRun` (added in Plan 05) to open this drawer. Build + restart frontend.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "score-drawer|performance" && exit 1 || echo TYPES-OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: drawer issues a PATCH — `grep -E "method:\\s*['\\\"]PATCH['\\\"]" integrations/system-health-dashboard/src/components/performance/score-drawer.tsx` matches; URL is same-origin `/api/experiments/scores/`.
    - Source assertion: client-side ranges present — regressions restricted to 0|1 and others to [0,1] (reviewer confirms the validation guard).
    - Source assertion: footer has `Save override` (default Button) + `Discard changes` (`variant="ghost"`); 404 copy and "Override saved" copy present verbatim.
    - Source assertion: NO new write endpoint or applyOverride re-implementation in the frontend (reuses the existing PATCH only).
    - Behavior assertion (build): frontend `npm run build` + frontend supervisor restart documented in SUMMARY.
  </acceptance_criteria>
  <done>The drawer opens on row click, edits corrected_* with mirrored ranges, saves via the existing PATCH, and reflects corrected-wins in the table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Saved Reports sub-view (save / list / render-snapshot / refresh)</name>
  <read_first>
    - integrations/system-health-dashboard/src/pages/performance.tsx (the Tabs shell + facet state to capture) [Plan 05]
    - lib/experiments/report-write.mjs + report-read.mjs (the snapshot/facet_state shape the sub-view round-trips) [Plan 03]
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Interaction Contracts item 5 — reports sub-view; §Copywriting — save/refresh/staleness/empty copy)
    - .planning/phases/74-performance-dashboard-reports/74-PATTERNS.md (§reports-subview.tsx)
  </read_first>
  <files>integrations/system-health-dashboard/src/components/performance/reports-subview.tsx, integrations/system-health-dashboard/src/pages/performance.tsx</files>
  <behavior>
    - A second Tabs value "Reports" INSIDE the Performance tab lists saved reports (not a top-level nav tab).
    - "Save report" captures the current facet-state + the currently-filtered result rows and POSTs them; the new report appears in the list.
    - Opening a saved report renders its FROZEN snapshot (stable — does not re-query on view).
    - "Refresh snapshot" POSTs to :id/refresh, updates the snapshot, and shows "Snapshot frozen {relative-time}".
    - With no saved reports, the empty state "No saved reports yet" + body copy shows.
  </behavior>
  <action>Create `components/performance/reports-subview.tsx`: a "Reports" `TabsContent` (second value of the page's `Tabs`, INSIDE Performance — D-05, never a top-level nav tab). List saved reports from `GET /api/experiments/reports` as a `Card` list (title + frozen-at relative time). A `Save report` `Button` (default/primary) captures the page's current facet-state + the currently-filtered run rows and `POST`s `{ title, facetState, snapshotRows }` to `/api/experiments/reports`, then refetches the list. Selecting a report renders its FROZEN `snapshot` in a read-only table (DASH-03 — render from snapshot, NEVER re-query on view). Each report has a `Refresh snapshot` `Button variant="outline"` that `POST`s `/api/experiments/reports/:id/refresh`, shows "Refreshing…" while pending, then refetches + shows the staleness note "Snapshot frozen {relative-time}. Refresh snapshot to re-run the saved query." Empty state "No saved reports yet" + body copy. Add the "Reports" tab trigger to the page's `Tabs`. No delete affordance (out of scope). Build + restart frontend.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "reports-subview|performance" && exit 1 || echo TYPES-OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: sub-view hits all three report endpoints same-origin — `grep -c "/api/experiments/reports" integrations/system-health-dashboard/src/components/performance/reports-subview.tsx` ≥ 2 (list/save + refresh).
    - Source assertion: report view renders the stored `snapshot` and does NOT call the runs endpoint on view (reviewer confirms no re-query — DASH-03 render-from-snapshot).
    - Source assertion: it is a `TabsContent` inside Performance (NOT a new entry in nav-bar.tsx `tabs[]`).
    - Source assertion: copy present verbatim — "Save report", "Refresh snapshot", "Refreshing…", "No saved reports yet", staleness note.
    - Behavior assertion (build): frontend `npm run build` + frontend supervisor restart documented in SUMMARY.
  </acceptance_criteria>
  <done>Reports sub-view saves a frozen snapshot, renders it stably, and refreshes on demand — all inside the Performance tab.</done>
</task>

<task type="auto">
  <name>Task 3: Activate the Playwright performance flows</name>
  <read_first>
    - tests/e2e/dashboard/performance.spec.ts (the fixme skeleton from Plan 01)
    - tests/e2e/dashboard/workflow-graph-colors.spec.ts (selectors/baseURL/wait idioms)
    - .planning/phases/74-performance-dashboard-reports/74-VALIDATION.md (§Per-Task Verification Map — the E2E flow row)
  </read_first>
  <files>tests/e2e/dashboard/performance.spec.ts</files>
  <action>Convert the five fixme flows in `performance.spec.ts` to live tests against `http://localhost:3032/performance`: (a) title "Performance" + tab after Token Usage; (b) facet selection narrows the runs table; (c) row click opens the drawer (Sheet visible); (d) timeline parent expand reveals sub-bands + tier badge stays visible; (e) Save report → the report appears in the Reports sub-view and renders its snapshot. Use stable selectors (text/role); add reasonable waits for the same-origin fetches. Keep the spec runnable headless via the existing playwright.config. If a flow depends on seeded data, gate it on data presence (skip with a clear message when the store is empty) rather than hard-failing on a fresh checkout.</action>
  <verify>
    <automated>npx playwright test tests/e2e/dashboard/performance.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `npx playwright test tests/e2e/dashboard/performance.spec.ts` passes (flows green, or cleanly skipped with a stated reason when the store has no runs).
    - Source assertion: no remaining `test.fixme` for the five flows (`grep -c "test.fixme" tests/e2e/dashboard/performance.spec.ts` is 0 for the activated flows).
    - Source assertion: navigation target is `/performance`; assertions cover facet-narrow, drawer-open, timeline-expand, report-save.
  </acceptance_criteria>
  <done>The performance E2E spec runs live and exercises facet/drawer/timeline/report flows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full interactive Performance tab: score-override drawer (corrects judged dimensions via the Phase 73 PATCH — closes SC#5) and the Saved Reports sub-view (freeze/render/refresh stable snapshots — KB-04/DASH-03), plus a live Playwright suite.</what-built>
  <how-to-verify>
    1. Ensure frontend rebuilt: `cd integrations/system-health-dashboard && npm run build && docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`.
    2. `gsd-browser navigate http://localhost:3032/performance` then `gsd-browser screenshot`.
    3. Click a run row → the right-side drawer opens with 5 rubric rows (judged read-only + editable corrected inputs + rationale). Edit a dimension within range, click "Save override" → drawer closes, the table shows the corrected value with an "edited" marker. Try regressions = 0.5 → it is rejected.
    4. Switch to the "Reports" sub-tab (inside Performance, NOT a new top-level nav tab). Build a facet query, click "Save report" → it appears in the list. Open it → it renders the frozen snapshot. Click "Refresh snapshot" → the frozen-at time updates.
    5. `npx playwright test tests/e2e/dashboard/performance.spec.ts` is green (or cleanly skipped on empty data).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator drawer input → PATCH endpoint | corrected_* values + overridden_by cross to the server |
| operator report title/facet → POST reports | operator strings persisted and later rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-06-01 | Tampering | unvalidated override values corrupting Score | mitigate | client mirrors the server ranges (regressions 0|1, others [0,1], overridden_by ≤256) BUT the existing PATCH is authoritative — server re-validates (api-routes.js:411-455) |
| T-74-06-02 | Tampering (XSS) | report title / rationale / snapshot strings in the DOM | mitigate | React escapes by default; no `dangerouslySetInnerHTML`; snapshot/title treated as text |
| T-74-06-03 | Information disclosure | drawer/report fetches leaking via wrong origin | mitigate | all calls same-origin `/api/experiments/...` through the Plan 04 proxy; no host:port |
| T-74-06-04 | Tampering | re-implementing override write client-side (bypass validation) | mitigate | drawer calls ONLY the existing PATCH; no applyOverride/write logic duplicated in the frontend |
| T-74-06-SC | Tampering | npm installs | mitigate | No new packages (sheet vendored in Plan 05 from official registry). No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the new components.
- `npm run build` succeeds; frontend supervisor restarted.
- `npx playwright test tests/e2e/dashboard/performance.spec.ts` green (or cleanly skipped on empty data).
- `gsd-browser` visual pass (human checkpoint): drawer save + reports freeze/refresh.
- `node --test tests/experiments/` — full experiment suite still green (no backend regression).
</verification>

<success_criteria>
- Score-override drawer closes SC#5 via the existing PATCH (corrected stored separately).
- Reports sub-view freezes + renders + refreshes stable snapshots (KB-04/DASH-03).
- E2E spec live and green.
</success_criteria>

<output>
Create `.planning/phases/74-performance-dashboard-reports/74-06-SUMMARY.md` when done.
</output>
