---
phase: 74-performance-dashboard-reports
plan: 05
type: execute
wave: 5
depends_on: ["74-04"]
files_modified:
  - integrations/system-health-dashboard/src/components/ui/sheet.tsx
  - integrations/system-health-dashboard/src/App.tsx
  - integrations/system-health-dashboard/src/components/nav-bar.tsx
  - integrations/system-health-dashboard/src/pages/performance.tsx
  - integrations/system-health-dashboard/src/components/performance/faceted-sidebar.tsx
  - integrations/system-health-dashboard/src/components/performance/runs-table.tsx
  - integrations/system-health-dashboard/src/components/performance/timeline.tsx
autonomous: false
requirements: [DASH-01, DASH-02]
user_setup: []

must_haves:
  truths:
    - "A Performance tab appears after Token Usage and routes to /performance"
    - "The page renders summary cards, a 260px faceted sidebar, and a runs table fetched same-origin from /api/experiments/runs"
    - "Selecting a facet progressively narrows the runs table and live counts update"
    - "A run row shows corrected-wins effective values with an 'edited' marker; null renders as — never 0"
    - "The timeline renders per-turn parents collapsed-by-default with an always-visible granularity_tier badge; expanding reveals per-reasoning-step sub-bands"
  artifacts:
    - path: "integrations/system-health-dashboard/src/pages/performance.tsx"
      provides: "Performance page shell (mirror token-usage.tsx) with sidebar+table+timeline+Tabs"
      min_lines: 60
    - path: "integrations/system-health-dashboard/src/components/performance/faceted-sidebar.tsx"
      provides: "260px faceted filter rail with live counts (D-01)"
    - path: "integrations/system-health-dashboard/src/components/performance/runs-table.tsx"
      provides: "Runs table with corrected-wins cells (D-03)"
    - path: "integrations/system-health-dashboard/src/components/performance/timeline.tsx"
      provides: "Collapsible per-turn timeline with tier badge + reasoning sub-bands (D-06)"
    - path: "integrations/system-health-dashboard/src/components/ui/sheet.tsx"
      provides: "shadcn sheet primitive (for Plan 06 drawer)"
  key_links:
    - from: "integrations/system-health-dashboard/src/pages/performance.tsx"
      to: "/api/experiments/runs"
      via: "same-origin fetch (NOT host:port)"
      pattern: "fetch\\(['\"`]/api/experiments"
    - from: "integrations/system-health-dashboard/src/App.tsx"
      to: "pages/performance.tsx"
      via: "Route path=/performance after /token-usage"
      pattern: "/performance"
---

<objective>
Build the operator-facing Performance tab read surface: register the route + nav tab, add the shadcn `sheet` primitive (consumed by Plan 06), and build the page shell with a faceted sidebar (D-01), a corrected-wins runs table (D-03), and a collapsible timeline with tier badges (D-06). All data fetched same-origin via the Plan 04 proxy. Activates the navigation + facet-narrow + timeline-expand Playwright flows from Plan 01.

Purpose: This is the DASH-01 query-builder + DASH-02 honest timeline surface. The score drawer + reports sub-view land in Plan 06 (which consumes the `sheet` primitive added here).
Output: route/nav wiring + sheet primitive + page + 3 performance sub-components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md
@.planning/phases/74-performance-dashboard-reports/74-PATTERNS.md
@integrations/system-health-dashboard/src/pages/token-usage.tsx
@integrations/system-health-dashboard/src/App.tsx
@integrations/system-health-dashboard/src/components/nav-bar.tsx

<interfaces>
<!-- From the live codebase + UI-SPEC. -->

App.tsx: import line ~9 (TokenUsagePage); route line 31 (`<Route path="/token-usage" .../>`). Add `import { PerformancePage } from './pages/performance'` + `<Route path="/performance" element={<PerformancePage />} />` after line 31.
nav-bar.tsx: tabs[] line 29-36. Add `{ label: 'Performance', path: '/performance' }` after the Token Usage entry (line 35). Active style is `border-b-2 border-primary` (existing).

token-usage.tsx primitive imports (reuse exact paths): button, card{Card,CardContent,CardHeader,CardTitle,CardDescription}, badge, table{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}, tabs{Tabs,TabsContent,TabsList,TabsTrigger}, alert{Alert,AlertDescription}. Plus checkbox, collapsible, scroll-area, separator, tooltip from ui/.

Fetch base: SAME-ORIGIN `/api/experiments/...` (NOT token-usage's PROXY_BASE host:port). The latency-tile idiom for container-served pages. Endpoints (Plan 04): GET /api/experiments/runs ({runs:[...]}); GET /api/experiments/runs/:taskId/timeline ({timeline:[...]}).

Run row fields (from readRuns): task_id, task_class, agent, model, framework, pending, started_at, ended_at, route heuristics, score:{goal_aligned_ratio, goal_achieved, code_quality, test_coverage, regressions, spec_drift, corrected_*, overridden_by, overridden_at, pending, not_scored}, outcome:{totalTokens,...}.

Corrected-wins (D-03): effective(dim,score) = score[`corrected_${dim}`] != null ? score[`corrected_${dim}`] : (score[dim] ?? null). isEdited = score[`corrected_${dim}`] != null. null → render `—`, NEVER 0.

Timeline row (from readTimeline): { ...parent, tier, estimated, children:[...] }. tier ∈ {per-turn, per-reasoning-step, per-session-aggregate}.

UI-SPEC locks: layout `grid grid-cols-[260px_1fr] gap-6`; page padding `p-6`; summary Card grid is the focal point; 4 type sizes (text-3xl/2xl/base/sm) + 2 weights; de-emphasis via text-muted-foreground not smaller size; tier badge = `Badge variant="secondary"` muted (honesty signal, never pass/fail color); edited marker = amber `outline` badge or 14px Pencil; threshold triad green/amber/red bg-*-50 text-*-700 border-*-200. Copy: title "Performance"; subtitle "Task-anchored query over experiment runs — cost, route quality, and outcome scores"; empty states + "Clear filters" per UI-SPEC Copywriting.

shadcn sheet: `npx shadcn@latest add sheet` (run inside integrations/system-health-dashboard). Fallback: right-aligned dialog.

Rebuild (FRONTEND): `cd integrations/system-health-dashboard && npm run build` then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend` (no Docker rebuild).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sheet primitive + register route and nav tab</name>
  <read_first>
    - integrations/system-health-dashboard/src/App.tsx (full — route table)
    - integrations/system-health-dashboard/src/components/nav-bar.tsx (lines 29-55 — tabs[] + active style)
    - integrations/system-health-dashboard/components.json (confirm new-york/neutral preset for the sheet add)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Component inventory — nav registration row; §One new primitive)
  </read_first>
  <files>integrations/system-health-dashboard/src/components/ui/sheet.tsx, integrations/system-health-dashboard/src/App.tsx, integrations/system-health-dashboard/src/components/nav-bar.tsx</files>
  <action>Run `npx shadcn@latest add sheet` inside `integrations/system-health-dashboard` to vendor `src/components/ui/sheet.tsx` from the shadcn official registry (no third-party registry; components.json declares `"registries": {}`). If the registry fetch fails, fall back to the UI-SPEC-sanctioned right-aligned `dialog` and note it in the SUMMARY. In `App.tsx` add `import { PerformancePage } from './pages/performance'` and a `<Route path="/performance" element={<PerformancePage />} />` immediately after the `/token-usage` route (line 31). In `nav-bar.tsx` add `{ label: 'Performance', path: '/performance' }` to `tabs[]` immediately after the Token Usage entry. (PerformancePage is created in Task 2 — author the import now; the build in Task 2 will resolve it.)</action>
  <verify>
    <automated>test -f integrations/system-health-dashboard/src/components/ui/sheet.tsx && grep -q "/performance" integrations/system-health-dashboard/src/App.tsx && grep -q "Performance" integrations/system-health-dashboard/src/components/nav-bar.tsx && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/components/ui/sheet.tsx` exists (or, if fallback taken, the SUMMARY documents the dialog fallback and the drawer plan adjusts).
    - Source assertion: App.tsx contains `<Route path="/performance"` AFTER the `/token-usage` route (reviewer confirms ordering).
    - Source assertion: nav-bar.tsx tabs[] contains `{ label: 'Performance', path: '/performance' }` after Token Usage.
  </acceptance_criteria>
  <done>sheet primitive vendored; /performance route + Performance nav tab registered after Token Usage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Performance page shell + faceted sidebar + runs table</name>
  <read_first>
    - integrations/system-health-dashboard/src/pages/token-usage.tsx (full — page shell: header, summary Card grid, fetch/state/loading/error, threshold-badge triad at lines ~800-817)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Visual Hierarchy, §Spacing, §Typography, §Color, §Copywriting, §Interaction Contracts items 1-2)
    - .planning/phases/74-performance-dashboard-reports/74-PATTERNS.md (§performance.tsx — imports + same-origin fetch + corrected-wins + layout; §faceted-sidebar / runs-table)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Code Examples — corrected-wins effective() / isEdited())
  </read_first>
  <files>integrations/system-health-dashboard/src/pages/performance.tsx, integrations/system-health-dashboard/src/components/performance/faceted-sidebar.tsx, integrations/system-health-dashboard/src/components/performance/runs-table.tsx</files>
  <behavior>
    - Page fetches GET /api/experiments/runs same-origin on mount; loading spinner, then summary cards + sidebar + table.
    - Selecting a facet value narrows the visible runs; facet counts reflect the post-filter set; "Clear filters" resets.
    - A run row with a corrected dimension shows the corrected value + an "edited" marker; judged value on hover (Tooltip).
    - A null score/heuristic renders as `—`, never 0.
    - Empty filtered set shows "No runs match these filters"; empty store shows "No runs recorded yet".
  </behavior>
  <action>Create `pages/performance.tsx` modelled on token-usage.tsx: header (`h1` "Performance" `text-2xl` + muted subtitle), a summary `Card` grid focal point (e.g. total runs, scored runs, total tokens, median wallclock/step — `text-3xl` numbers), then a `grid grid-cols-[260px_1fr] gap-6` body with `<FacetedSidebar>` left and `<RunsTable>` right, wrapped in `Tabs` whose first value is "Runs" (the Reports sub-view value is added in Plan 06). Fetch `/api/experiments/runs` SAME-ORIGIN (never host:port). Hold facet state in the page; pass filtered rows down. Create `faceted-sidebar.tsx`: a `Card` rail (260px) with `Collapsible` facet groups (expanded by default) for task_id, date window, task_class/route-class, score state (scored/pending/not_scored), and score-dimension threshold facets; each value = `Checkbox` + label + live count `Badge variant="secondary"` (`text-sm text-muted-foreground`); a "Clear filters" `Button variant="outline"`. Justify any facet beyond the locked set in a comment (Claude's-discretion clause). Counts recompute from the current filtered set. Create `runs-table.tsx`: a `Table` of runs; for each of the 5 score dims render the corrected-wins effective value via a shared `effective()/isEdited()` helper (define once, export from a small util or inline in the page and import) — `—` for null, amber `outline` "edited" badge or 14px `Pencil` when edited, judged value in a `Tooltip` on hover; row click calls an `onSelectRun(taskId)` prop (wired to the drawer in Plan 06 — accept the prop now, no-op if unused). Use only the 4 type sizes + muted-for-de-emphasis. All copy from the UI-SPEC Copywriting table. Build + restart frontend per acceptance.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "performance|faceted|runs-table" && exit 1 || echo TYPES-OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: page fetches same-origin — `grep -E "fetch\(['\\\"\`]/api/experiments" integrations/system-health-dashboard/src/pages/performance.tsx` matches; there is NO `http://localhost:8080` or `PROXY_BASE.*8080` in the page.
    - Source assertion: layout uses `grid-cols-[260px_1fr]` (UI-SPEC).
    - Source assertion: corrected-wins helper present and null→`—` (no `?? 0` on a score dim): `grep -n "?? 0" integrations/system-health-dashboard/src/components/performance/runs-table.tsx` returns nothing.
    - Behavior assertion (build): `cd integrations/system-health-dashboard && npm run build` succeeds, then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend` — documented in SUMMARY (FRONTEND rebuild path).
    - Source assertion: tier badge uses `variant="secondary"`; edited marker is amber outline/Pencil, never destructive red or success green.
  </acceptance_criteria>
  <done>Performance page renders summary cards + 260px faceted sidebar + corrected-wins runs table from same-origin data; types compile; frontend rebuilt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collapsible timeline with tier badge + reasoning sub-bands</name>
  <read_first>
    - integrations/system-health-dashboard/src/components/ui/collapsible.tsx + badge.tsx (the primitives to compose)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Interaction Contracts item 4 — collapsed-by-default, tier badge always visible; §Color — tier badge is informational not status)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Timeline Data Source — parent/child shape, estimated honesty)
    - lib/experiments/timeline-read.mjs (the row shape the component renders) [Plan 02]
  </read_first>
  <files>integrations/system-health-dashboard/src/components/performance/timeline.tsx</files>
  <behavior>
    - A per-turn parent renders collapsed by default with an always-visible granularity_tier badge.
    - Expanding a parent reveals its per-reasoning-step sub-bands stacked beneath it.
    - The tier badge stays visible whether collapsed or expanded (honesty signal).
    - per-reasoning-step rows show an "estimated" honesty hint (tokens_estimated === 1).
    - A copilot per-session-aggregate row renders a single band with no expand affordance.
  </behavior>
  <action>Create `components/performance/timeline.tsx` taking a `timeline` array (from GET /api/experiments/runs/:taskId/timeline). Render each parent as a `Collapsible` row, `defaultOpen={false}` (collapsed by default, D-06), with an always-visible `Badge variant="secondary"` (`text-sm text-muted-foreground`) showing the `granularity_tier` — visible in both collapsed and expanded states (never hidden behind the collapse). Inside `CollapsibleContent`, stack the `children` per-reasoning-step sub-bands; on any `estimated` row show a small muted "estimated" hint next to the tokens. A `per-session-aggregate` parent (copilot) renders as a single band with NO expand trigger. Numbers use `font-mono` tabular-nums; null → `—`. The page fetches the timeline lazily when a run is selected (or shows the selected run's timeline in a panel) — accept a `taskId` prop and fetch same-origin. Wire this into the page (a timeline panel under the table or in the Runs tab). Build + restart frontend.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "timeline" && exit 1 || echo TYPES-OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tier badge is `variant="secondary"` and rendered OUTSIDE `CollapsibleContent` (always visible) — reviewer confirms badge is a sibling of the trigger, not inside the collapsible body.
    - Source assertion: `defaultOpen={false}` (collapsed by default).
    - Source assertion: per-session-aggregate path renders no expand trigger (reviewer confirms the conditional).
    - Source assertion: no `?? 0` on token fields; null → `—`.
    - Behavior assertion (build): frontend `npm run build` + `supervisorctl restart web-services:health-dashboard-frontend` documented in SUMMARY.
  </acceptance_criteria>
  <done>Timeline renders collapsed per-turn parents with an always-visible tier badge and expandable reasoning sub-bands; copilot aggregate degrades gracefully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Performance tab read surface: nav tab + /performance route, summary cards, 260px faceted sidebar with live counts, corrected-wins runs table, and a collapsible timeline with always-visible granularity_tier badges. All data fetched same-origin via the Plan 04 proxy.</what-built>
  <how-to-verify>
    1. Ensure the frontend is rebuilt: `cd integrations/system-health-dashboard && npm run build && docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`. (If server.js from Plan 04 was just changed, also `cd docker && docker-compose restart coding-services`.)
    2. `gsd-browser navigate http://localhost:3032/performance` then `gsd-browser screenshot`.
    3. Confirm: the "Performance" tab sits AFTER "Token Usage" in the nav; the page title reads "Performance"; summary cards are the visual focal point; the 260px faceted sidebar shows facet groups with live count badges.
    4. Select a facet value — the runs table narrows and counts update. Click "Clear filters" — the full set returns.
    5. Hover a run row's edited dimension (if any) — the judged value shows in a tooltip; null dimensions render as "—", never 0.
    6. Expand a timeline parent turn — per-reasoning-step sub-bands appear; the granularity_tier badge stays visible whether collapsed or expanded.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| experiment API JSON → React render | Run/Score/Report strings (titles, agent, model) rendered in the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05-01 | Tampering (XSS) | rendering run/score/report strings | mitigate | React escapes by default; no `dangerouslySetInnerHTML` anywhere in the new components |
| T-74-05-02 | Information disclosure | page reaching :8080 directly bypassing the proxy | mitigate | page fetches same-origin `/api/experiments/...` only (grep-gated); no host:port fetch |
| T-74-05-03 | Tampering (data honesty) | null coerced to 0 in the UI | mitigate | corrected-wins helper renders null as `—`; grep-gate forbids `?? 0` on score/token fields |
| T-74-05-SC | Tampering | npm installs (shadcn sheet) | mitigate | `npx shadcn add sheet` vendors SOURCE from the shadcn OFFICIAL registry (not a package install); components.json forbids third-party registries; RESEARCH Package Legitimacy Audit — Approved. No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the new files.
- `npm run build` succeeds; frontend supervisor restarted.
- `gsd-browser` visual pass against `localhost:3032/performance` (human checkpoint).
- Same-origin fetch grep-gate passes; no `?? 0` on score/token fields.
</verification>

<success_criteria>
- Performance tab + route registered after Token Usage.
- Faceted sidebar narrows the runs table with live counts.
- Corrected-wins table cells + null-as-— honesty.
- Collapsible timeline with always-visible tier badge.
</success_criteria>

<output>
Create `.planning/phases/74-performance-dashboard-reports/74-05-SUMMARY.md` when done.
</output>
