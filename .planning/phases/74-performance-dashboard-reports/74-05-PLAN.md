---
phase: 74-performance-dashboard-reports
plan: 05
type: execute
wave: 5
depends_on: ["74-04"]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/store/index.ts
  - integrations/system-health-dashboard/src/components/ui/sheet.tsx
  - integrations/system-health-dashboard/src/App.tsx
  - integrations/system-health-dashboard/src/components/nav-bar.tsx
  - integrations/system-health-dashboard/src/pages/performance.tsx
  - integrations/system-health-dashboard/src/components/performance/faceted-sidebar.tsx
  - integrations/system-health-dashboard/src/components/performance/runs-table.tsx
  - integrations/system-health-dashboard/src/components/performance/timeline.tsx
autonomous: false
requirements: [DASH-01, DASH-02]
user_setup: []

must_haves:
  truths:
    - "A Performance tab appears after Token Usage and routes to /performance"
    - "Shared Performance state (facets, runs, selected run, timeline) lives in a Redux slice — NOT page-local useState"
    - "The page renders summary cards, a 260px faceted sidebar, and a runs table fetched same-origin from /api/experiments/runs via the fetchRuns thunk"
    - "Selecting a facet (a plain reducer action) progressively narrows the runs table and live counts update via selectors"
    - "A run row shows corrected-wins effective values with an 'edited' marker; null renders as — never 0"
    - "The timeline renders per-turn parents collapsed-by-default with an always-visible granularity_tier badge; expanding reveals per-reasoning-step sub-bands"
  artifacts:
    - path: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      provides: "Redux slice (createSlice) for shared Performance state + createAsyncThunk fetchRuns/fetchTimeline + facet reducers + selectors (DASH-01/DASH-02 state mgmt)"
      min_lines: 80
    - path: "integrations/system-health-dashboard/src/pages/performance.tsx"
      provides: "Performance page shell (layout mirrors token-usage.tsx) consuming the slice via useAppSelector/useAppDispatch"
      min_lines: 60
    - path: "integrations/system-health-dashboard/src/components/performance/faceted-sidebar.tsx"
      provides: "260px faceted filter rail with live counts from selectors (D-01)"
    - path: "integrations/system-health-dashboard/src/components/performance/runs-table.tsx"
      provides: "Runs table with corrected-wins cells (D-03); row click dispatches setSelectedTaskId"
    - path: "integrations/system-health-dashboard/src/components/performance/timeline.tsx"
      provides: "Collapsible per-turn timeline with tier badge + reasoning sub-bands (D-06); fetches via fetchTimeline thunk"
    - path: "integrations/system-health-dashboard/src/components/ui/sheet.tsx"
      provides: "shadcn sheet primitive (for Plan 06 drawer)"
  key_links:
    - from: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      to: "/api/experiments/runs"
      via: "fetchRuns createAsyncThunk (same-origin fetch INSIDE the thunk, NOT host:port)"
      pattern: "fetch\\(['\"`]/api/experiments"
    - from: "integrations/system-health-dashboard/src/store/index.ts"
      to: "performanceSlice reducer"
      via: "combineReducers registers `performance:`"
      pattern: "performance:\\s*performanceReducer"
    - from: "integrations/system-health-dashboard/src/pages/performance.tsx"
      to: "performanceSlice"
      via: "useAppSelector/useAppDispatch consume slice state + dispatch thunks"
      pattern: "useAppSelector|useAppDispatch"
    - from: "integrations/system-health-dashboard/src/App.tsx"
      to: "pages/performance.tsx"
      via: "Route path=/performance after /token-usage"
      pattern: "/performance"
---

<objective>
Build the operator-facing Performance tab read surface. The SHARED Performance state (facets, runs, selected run, timeline) is managed by a NEW Redux Toolkit slice — the dashboard's established state-management convention — NOT page-local useState. Register the route + nav tab, add the shadcn `sheet` primitive (consumed by Plan 06), then build the page shell with a faceted sidebar (D-01), a corrected-wins runs table (D-03), and a collapsible timeline with tier badges (D-06). All data fetched same-origin via `createAsyncThunk` thunks that wrap the Plan 04 proxy. Activates the navigation + facet-narrow + timeline-expand Playwright flows from Plan 01.

Purpose: This is the DASH-01 query-builder + DASH-02 honest timeline surface. The score drawer + reports sub-view land in Plan 06 (which consumes the `sheet` primitive added here and the same `performance` slice).
Output: Redux slice + reducer registration + route/nav wiring + sheet primitive + page + 3 performance sub-components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md
@.planning/phases/74-performance-dashboard-reports/74-PATTERNS.md
@integrations/system-health-dashboard/src/store/index.ts
@integrations/system-health-dashboard/src/store/slices/workflowConfigSlice.ts
@integrations/system-health-dashboard/src/store/slices/healthStatusSlice.ts
@integrations/system-health-dashboard/src/pages/token-usage.tsx
@integrations/system-health-dashboard/src/App.tsx
@integrations/system-health-dashboard/src/components/nav-bar.tsx

<interfaces>
<!-- From the live codebase + UI-SPEC. -->

STATE MANAGEMENT — Redux Toolkit (the dashboard convention). The page and components consume SHARED state from a new `performance` slice; they do NOT hold runs/facets/selectedRun in page-local useState.
- Slice pattern to MIRROR EXACTLY (see workflowConfigSlice.ts / healthStatusSlice.ts):
  - `createSlice({ name, initialState, reducers, extraReducers })` from `@reduxjs/toolkit`.
  - Async: `createAsyncThunk('performance/fetchRuns', async (_, { rejectWithValue }) => { ... })` — the same-origin `fetch` lives INSIDE the thunk; handle pending/fulfilled/rejected in `extraReducers` (set runsLoading/runsError). Use `rejectWithValue` on failure exactly like `initializeWorkflowConfig`.
  - Selectors live in the slice file as `(state: RootState) => state.performance.X` (import `RootState` from `../index`).
  - Register reducer: in `store/index.ts` add `import performanceReducer from './slices/performanceSlice'` and `performance: performanceReducer,` inside `combineReducers`.
  - Consume in components via the EXPORTED typed hooks: `import { useAppSelector, useAppDispatch } from '../../store'` (i.e. store/index.ts). Dispatch thunks/actions: `dispatch(fetchRuns())`, `dispatch(setFacet(...))`, `dispatch(setSelectedTaskId(taskId))`, `dispatch(fetchTimeline(taskId))`.
- DO NOT introduce RTK Query, Zustand, or Context. The `<Provider store={store}>` already wraps the app (store/provider.tsx) — no provider wiring needed.

SLICE STATE SHAPE (minimum): `{ filters/facetState, runs: Run[], runsLoading, runsError, selectedTaskId: string|null, timelineByTaskId: Record<string, TimelineRow[]>, timelineLoading }`. Facet selection + clear-filters are plain reducers (`setFacet`, `clearFilters`, `setSelectedTaskId`). Derive the FILTERED set + live counts via selectors (memoized selector or compute in `useAppSelector`), NOT page-local arrays.

App.tsx: import line ~9 (TokenUsagePage); route line 31 (`<Route path="/token-usage" .../>`). Add `import { PerformancePage } from './pages/performance'` + `<Route path="/performance" element={<PerformancePage />} />` after line 31.
nav-bar.tsx: tabs[] line 29-36. Add `{ label: 'Performance', path: '/performance' }` after the Token Usage entry (line 35). Active style is `border-b-2 border-primary` (existing).

token-usage.tsx is a LAYOUT analog ONLY (header, summary Card grid, threshold-badge triad, primitive import paths). It is NOT the state-management analog — token-usage uses plain useState, which is the WRONG pattern for shared Performance state. Use Redux (above) for state; reuse token-usage solely for visual structure.

token-usage.tsx primitive imports (reuse exact paths): button, card{Card,CardContent,CardHeader,CardTitle,CardDescription}, badge, table{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}, tabs{Tabs,TabsContent,TabsList,TabsTrigger}, alert{Alert,AlertDescription}. Plus checkbox, collapsible, scroll-area, separator, tooltip from ui/.

Fetch base: SAME-ORIGIN `/api/experiments/...` (NOT token-usage's PROXY_BASE host:port), and the fetch call lives INSIDE the thunks. Endpoints (Plan 04): GET /api/experiments/runs ({runs:[...]}); GET /api/experiments/runs/:taskId/timeline ({timeline:[...]}).

Run row fields (from readRuns): task_id, task_class, agent, model, framework, pending, started_at, ended_at, route heuristics, score:{goal_aligned_ratio, goal_achieved, code_quality, test_coverage, regressions, spec_drift, corrected_*, overridden_by, overridden_at, pending, not_scored}, outcome:{totalTokens,...}.

Corrected-wins (D-03): effective(dim,score) = score[`corrected_${dim}`] != null ? score[`corrected_${dim}`] : (score[dim] ?? null). isEdited = score[`corrected_${dim}`] != null. null → render `—`, NEVER 0. Define `effective()/isEdited()` once (small util or page-level export) and import in runs-table + Plan 06's drawer.

Timeline row (from readTimeline): { ...parent, tier, estimated, children:[...] }. tier ∈ {per-turn, per-reasoning-step, per-session-aggregate}.

UI-SPEC locks: layout `grid grid-cols-[260px_1fr] gap-6`; page padding `p-6`; summary Card grid is the focal point; 4 type sizes (text-3xl/2xl/base/sm) + 2 weights; de-emphasis via text-muted-foreground not smaller size; tier badge = `Badge variant="secondary"` muted (honesty signal, never pass/fail color); edited marker = amber `outline` badge or 14px Pencil; threshold triad green/amber/red bg-*-50 text-*-700 border-*-200. Copy: title "Performance"; subtitle "Task-anchored query over experiment runs — cost, route quality, and outcome scores"; empty states + "Clear filters" per UI-SPEC Copywriting.

shadcn sheet: `npx shadcn@latest add sheet` (run inside integrations/system-health-dashboard). Fallback: right-aligned dialog.

Rebuild (FRONTEND): `cd integrations/system-health-dashboard && npm run build` then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend` (no Docker rebuild).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Performance Redux slice + reducer registration</name>
  <read_first>
    - integrations/system-health-dashboard/src/store/index.ts (full — combineReducers, configureStore, exported useAppSelector/useAppDispatch typed hooks)
    - integrations/system-health-dashboard/src/store/slices/workflowConfigSlice.ts (full — the createSlice + createAsyncThunk + rejectWithValue + extraReducers + in-file selector convention to MIRROR)
    - integrations/system-health-dashboard/src/store/slices/healthStatusSlice.ts (full — the simpler createSlice + plain reducer convention)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Interaction Contracts items 1-2 + 4 — what state the page/timeline need)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Code Examples — corrected-wins effective() / isEdited(); §Timeline Data Source — row shape)
  </read_first>
  <files>integrations/system-health-dashboard/src/store/slices/performanceSlice.ts, integrations/system-health-dashboard/src/store/index.ts</files>
  <behavior>
    - `fetchRuns` thunk fetches GET /api/experiments/runs same-origin; pending sets runsLoading=true; fulfilled stores runs[] + clears loading; rejected stores runsError via rejectWithValue.
    - `fetchTimeline(taskId)` thunk fetches GET /api/experiments/runs/:taskId/timeline same-origin and stores it under timelineByTaskId[taskId].
    - `setFacet`/`clearFilters` plain reducers mutate facetState; a `selectFilteredRuns` selector derives the narrowed set; a counts selector derives live per-facet counts from the filtered set.
    - `setSelectedTaskId` plain reducer sets selectedTaskId (drives the Plan 06 drawer); selecting null clears it.
    - The `performance` reducer is registered in store/index.ts combineReducers and RootState includes `performance`.
  </behavior>
  <action>Create `src/store/slices/performanceSlice.ts` MIRRORING the existing slice conventions (createSlice + createAsyncThunk + in-file selectors + RootState import from `../index`). State shape (minimum): `{ facetState, runs: Run[], runsLoading: boolean, runsError: string|null, selectedTaskId: string|null, timelineByTaskId: Record<string, TimelineRow[]>, timelineLoading: boolean }`. Define `createAsyncThunk('performance/fetchRuns', ...)` and `createAsyncThunk('performance/fetchTimeline', ...)` with the SAME-ORIGIN `fetch('/api/experiments/...')` INSIDE the thunk (never host:port), `rejectWithValue` on non-ok exactly like `initializeWorkflowConfig`; handle pending/fulfilled/rejected in `extraReducers` (runsLoading/runsError, timelineLoading, store into timelineByTaskId keyed by taskId). Plain reducers: `setFacet`, `clearFilters`, `setSelectedTaskId`. Export actions. Export in-file selectors: `selectRuns`, `selectRunsLoading`, `selectRunsError`, `selectFacetState`, `selectSelectedTaskId`, `selectTimelineFor(taskId)`, plus a derived `selectFilteredRuns` (applies facetState to runs) and a `selectFacetCounts` (live counts over the filtered set). `export default performanceSlice.reducer`. In `store/index.ts`: add `import performanceReducer from './slices/performanceSlice'` and `performance: performanceReducer,` inside `combineReducers`. Do NOT add RTK Query/Zustand/Context. Type-check; no frontend build needed yet (no UI consumers until Task 2).</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && grep -q "performance: performanceReducer" src/store/index.ts && grep -q "createAsyncThunk" src/store/slices/performanceSlice.ts && grep -E "fetch\(['\"\`]/api/experiments" src/store/slices/performanceSlice.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "performanceSlice|store/index" && exit 1 || echo SLICE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `performanceSlice.ts` uses `createSlice` + `createAsyncThunk` (NOT RTK Query `createApi`, NOT Zustand, NOT Context).
    - Source assertion: the same-origin fetch lives INSIDE the thunk — `grep -E "fetch\(['\"\`]/api/experiments" src/store/slices/performanceSlice.ts` matches; there is NO `localhost:8080`/`PROXY_BASE` in the slice.
    - Source assertion: reducer registered — `grep -q "performance: performanceReducer" src/store/index.ts`.
    - Source assertion: plain reducers `setFacet`, `clearFilters`, `setSelectedTaskId` exported; `rejectWithValue` used in the thunks (mirrors initializeWorkflowConfig).
    - Source assertion: a derived filtered-runs selector + counts selector exist (no page-local filtering planned).
  </acceptance_criteria>
  <done>The `performance` slice exists (createSlice + fetchRuns/fetchTimeline thunks + facet/selected reducers + selectors), is registered in combineReducers, and types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Add sheet primitive + register route and nav tab</name>
  <read_first>
    - integrations/system-health-dashboard/src/App.tsx (full — route table)
    - integrations/system-health-dashboard/src/components/nav-bar.tsx (lines 29-55 — tabs[] + active style)
    - integrations/system-health-dashboard/components.json (confirm new-york/neutral preset for the sheet add)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Component inventory — nav registration row; §One new primitive)
  </read_first>
  <files>integrations/system-health-dashboard/src/components/ui/sheet.tsx, integrations/system-health-dashboard/src/App.tsx, integrations/system-health-dashboard/src/components/nav-bar.tsx</files>
  <action>Run `npx shadcn@latest add sheet` inside `integrations/system-health-dashboard` to vendor `src/components/ui/sheet.tsx` from the shadcn official registry (no third-party registry; components.json declares `"registries": {}`). If the registry fetch fails, fall back to the UI-SPEC-sanctioned right-aligned `dialog` and note it in the SUMMARY. In `App.tsx` add `import { PerformancePage } from './pages/performance'` and a `<Route path="/performance" element={<PerformancePage />} />` immediately after the `/token-usage` route (line 31). In `nav-bar.tsx` add `{ label: 'Performance', path: '/performance' }` to `tabs[]` immediately after the Token Usage entry. (PerformancePage is created in Task 3 — author the import now; the build in Task 3 will resolve it.)</action>
  <verify>
    <automated>test -f integrations/system-health-dashboard/src/components/ui/sheet.tsx && grep -q "/performance" integrations/system-health-dashboard/src/App.tsx && grep -q "Performance" integrations/system-health-dashboard/src/components/nav-bar.tsx && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/components/ui/sheet.tsx` exists (or, if fallback taken, the SUMMARY documents the dialog fallback and the drawer plan adjusts).
    - Source assertion: App.tsx contains `<Route path="/performance"` AFTER the `/token-usage` route (reviewer confirms ordering).
    - Source assertion: nav-bar.tsx tabs[] contains `{ label: 'Performance', path: '/performance' }` after Token Usage.
  </acceptance_criteria>
  <done>sheet primitive vendored; /performance route + Performance nav tab registered after Token Usage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Performance page shell + faceted sidebar + runs table (Redux-backed)</name>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (Task 1 — the slice state, thunks, actions, and selectors to consume)
    - integrations/system-health-dashboard/src/store/index.ts (the exported useAppSelector/useAppDispatch typed hooks to import)
    - integrations/system-health-dashboard/src/pages/token-usage.tsx (full — LAYOUT analog ONLY: header, summary Card grid, threshold-badge triad at lines ~800-817; do NOT copy its useState state-management)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Visual Hierarchy, §Spacing, §Typography, §Color, §Copywriting, §Interaction Contracts items 1-2)
    - .planning/phases/74-performance-dashboard-reports/74-PATTERNS.md (§performance.tsx — imports + layout + corrected-wins; §faceted-sidebar / runs-table)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Code Examples — corrected-wins effective() / isEdited())
  </read_first>
  <files>integrations/system-health-dashboard/src/pages/performance.tsx, integrations/system-health-dashboard/src/components/performance/faceted-sidebar.tsx, integrations/system-health-dashboard/src/components/performance/runs-table.tsx</files>
  <behavior>
    - On mount the page dispatches `fetchRuns()`; runsLoading shows a spinner, then summary cards + sidebar + table render from slice state via useAppSelector.
    - Selecting a facet dispatches `setFacet(...)`; the visible runs narrow via the `selectFilteredRuns` selector; facet counts reflect the post-filter set; "Clear filters" dispatches `clearFilters`.
    - A run row with a corrected dimension shows the corrected value + an "edited" marker; judged value on hover (Tooltip).
    - A null score/heuristic renders as `—`, never 0.
    - Row click dispatches `setSelectedTaskId(taskId)` (drives the Plan 06 drawer).
    - Empty filtered set shows "No runs match these filters"; empty store shows "No runs recorded yet".
    - NO page-level useState holds runs/facetState/selectedRun — those are all in the slice (in-component UI-only state like a hover tooltip is fine).
  </behavior>
  <action>Create `pages/performance.tsx` with the LAYOUT modelled on token-usage.tsx but the STATE driven by Redux: import `useAppSelector, useAppDispatch` from `../store`; on mount `useEffect(() => { dispatch(fetchRuns()) }, [])`; read `runs/runsLoading/runsError/facetState/selectedTaskId` and the derived `selectFilteredRuns`/`selectFacetCounts` via `useAppSelector`. Header (`h1` "Performance" `text-2xl` + muted subtitle), a summary `Card` grid focal point (total runs, scored runs, total tokens, median wallclock/step — `text-3xl` numbers) computed from the slice runs, then a `grid grid-cols-[260px_1fr] gap-6` body with `<FacetedSidebar>` left and `<RunsTable>` right, wrapped in `Tabs` whose first value is "Runs" (the Reports sub-view value is added in Plan 06). Do NOT fetch in the component or hold facet/runs/selectedRun in page-local useState — all shared state comes from the slice. Create `faceted-sidebar.tsx`: a `Card` rail (260px) with `Collapsible` facet groups (expanded by default) for task_id, date window, task_class/route-class, score state (scored/pending/not_scored), and score-dimension threshold facets; it reads facetState + counts via `useAppSelector` and dispatches `setFacet`/`clearFilters` via `useAppDispatch`; each value = `Checkbox` + label + live count `Badge variant="secondary"` (`text-sm text-muted-foreground`); a "Clear filters" `Button variant="outline"`. Justify any facet beyond the locked set in a comment (Claude's-discretion clause). Create `runs-table.tsx`: a `Table` of the filtered runs (from `selectFilteredRuns` via `useAppSelector`); for each of the 5 score dims render the corrected-wins effective value via a shared `effective()/isEdited()` helper (define once, export from a small util or page-level, import here and reuse in Plan 06) — `—` for null, amber `outline` "edited" badge or 14px `Pencil` when edited, judged value in a `Tooltip` on hover; row click dispatches `setSelectedTaskId(taskId)` (no-op consumer until Plan 06's drawer reads selectedTaskId). Use only the 4 type sizes + muted-for-de-emphasis. All copy from the UI-SPEC Copywriting table. Build + restart frontend per acceptance.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "performance|faceted|runs-table" && exit 1 || echo TYPES-OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (Redux): `grep -E "useAppSelector|useAppDispatch" integrations/system-health-dashboard/src/pages/performance.tsx` matches; the page imports them from `../store`.
    - Source assertion (no shared local state): the page does NOT hold runs/facetState/selectedRun in top-level `useState` — reviewer confirms those come from the slice (a UI-only useState such as a tooltip toggle is allowed). There is no `fetch(` call directly in performance.tsx (the fetch lives in the slice thunk).
    - Source assertion: row click dispatches `setSelectedTaskId` (not page-local selected state).
    - Source assertion: page fetches via the slice thunk same-origin — the slice (Task 1) matches `fetch\(['\"\`]/api/experiments`; there is NO `http://localhost:8080`/`PROXY_BASE.*8080` anywhere in the page or the slice.
    - Source assertion: layout uses `grid-cols-[260px_1fr]` (UI-SPEC).
    - Source assertion: corrected-wins helper present and null→`—` (no `?? 0` on a score dim): `grep -n "?? 0" integrations/system-health-dashboard/src/components/performance/runs-table.tsx` returns nothing.
    - Behavior assertion (build): `cd integrations/system-health-dashboard && npm run build` succeeds, then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend` — documented in SUMMARY (FRONTEND rebuild path).
    - Source assertion: tier/edited markers — edited marker is amber outline/Pencil, never destructive red or success green.
  </acceptance_criteria>
  <done>Performance page renders summary cards + 260px faceted sidebar + corrected-wins runs table from the Redux slice (selectors + thunks, no page-local shared state); types compile; frontend rebuilt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Collapsible timeline with tier badge + reasoning sub-bands (Redux-backed fetch)</name>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (Task 1 — fetchTimeline thunk + selectTimelineFor + timelineLoading)
    - integrations/system-health-dashboard/src/store/index.ts (useAppSelector/useAppDispatch)
    - integrations/system-health-dashboard/src/components/ui/collapsible.tsx + badge.tsx (the primitives to compose)
    - .planning/phases/74-performance-dashboard-reports/74-UI-SPEC.md (§Interaction Contracts item 4 — collapsed-by-default, tier badge always visible; §Color — tier badge is informational not status)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Timeline Data Source — parent/child shape, estimated honesty)
    - lib/experiments/timeline-read.mjs (the row shape the component renders) [Plan 02]
  </read_first>
  <files>integrations/system-health-dashboard/src/components/performance/timeline.tsx</files>
  <behavior>
    - When a run is selected (selectedTaskId in the slice), the page/timeline dispatches `fetchTimeline(taskId)` and renders the result read from `selectTimelineFor(taskId)`.
    - A per-turn parent renders collapsed by default with an always-visible granularity_tier badge.
    - Expanding a parent reveals its per-reasoning-step sub-bands stacked beneath it.
    - The tier badge stays visible whether collapsed or expanded (honesty signal).
    - per-reasoning-step rows show an "estimated" honesty hint (tokens_estimated === 1).
    - A copilot per-session-aggregate row renders a single band with no expand affordance.
  </behavior>
  <action>Create `components/performance/timeline.tsx` taking a `taskId` prop. On taskId change `dispatch(fetchTimeline(taskId))` (the fetch lives in the slice thunk, not here) and read the rows via `useAppSelector(selectTimelineFor(taskId))` + `timelineLoading`. Render each parent as a `Collapsible` row, `defaultOpen={false}` (collapsed by default, D-06), with an always-visible `Badge variant="secondary"` (`text-sm text-muted-foreground`) showing the `granularity_tier` — visible in both collapsed and expanded states (never hidden behind the collapse). Inside `CollapsibleContent`, stack the `children` per-reasoning-step sub-bands; on any `estimated` row show a small muted "estimated" hint next to the tokens. A `per-session-aggregate` parent (copilot) renders as a single band with NO expand trigger. Numbers use `font-mono` tabular-nums; null → `—`. Wire this into the page (a timeline panel under the table or in the Runs tab) driven by the slice's selectedTaskId. Build + restart frontend.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "timeline" && exit 1 || echo TYPES-OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (Redux): the timeline reads rows via `useAppSelector` (selectTimelineFor) and dispatches `fetchTimeline` — `grep -E "useAppSelector|useAppDispatch|fetchTimeline" integrations/system-health-dashboard/src/components/performance/timeline.tsx` matches; no direct `fetch(` in the component.
    - Source assertion: tier badge is `variant="secondary"` and rendered OUTSIDE `CollapsibleContent` (always visible) — reviewer confirms badge is a sibling of the trigger, not inside the collapsible body.
    - Source assertion: `defaultOpen={false}` (collapsed by default).
    - Source assertion: per-session-aggregate path renders no expand trigger (reviewer confirms the conditional).
    - Source assertion: no `?? 0` on token fields; null → `—`.
    - Behavior assertion (build): frontend `npm run build` + `supervisorctl restart web-services:health-dashboard-frontend` documented in SUMMARY.
  </acceptance_criteria>
  <done>Timeline renders collapsed per-turn parents with an always-visible tier badge and expandable reasoning sub-bands, fetched via the fetchTimeline thunk and read from the slice; copilot aggregate degrades gracefully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Performance tab read surface backed by a new Redux slice: nav tab + /performance route, summary cards, 260px faceted sidebar with live counts (selectors), corrected-wins runs table, and a collapsible timeline with always-visible granularity_tier badges. All shared state is in the `performance` slice; data fetched same-origin via createAsyncThunk thunks.</what-built>
  <how-to-verify>
    1. Ensure the frontend is rebuilt: `cd integrations/system-health-dashboard && npm run build && docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`. (If server.js from Plan 04 was just changed, also `cd docker && docker-compose restart coding-services`.)
    2. `gsd-browser navigate http://localhost:3032/performance` then `gsd-browser screenshot`.
    3. Confirm: the "Performance" tab sits AFTER "Token Usage" in the nav; the page title reads "Performance"; summary cards are the visual focal point; the 260px faceted sidebar shows facet groups with live count badges.
    4. Select a facet value — the runs table narrows and counts update (driven by the slice selectors). Click "Clear filters" — the full set returns.
    5. Hover a run row's edited dimension (if any) — the judged value shows in a tooltip; null dimensions render as "—", never 0.
    6. Expand a timeline parent turn — per-reasoning-step sub-bands appear; the granularity_tier badge stays visible whether collapsed or expanded.
    7. (Optional, Redux sanity) In dev, `window.__REDUX_STORE__.getState().performance` reflects runs/facetState/selectedTaskId.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| experiment API JSON → React render | Run/Score/Report strings (titles, agent, model) rendered in the DOM (now via the Redux slice) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05-01 | Tampering (XSS) | rendering run/score/report strings | mitigate | React escapes by default; no `dangerouslySetInnerHTML` anywhere in the new components |
| T-74-05-02 | Information disclosure | thunk reaching :8080 directly bypassing the proxy | mitigate | fetchRuns/fetchTimeline thunks fetch same-origin `/api/experiments/...` only (grep-gated); no host:port fetch in slice or components |
| T-74-05-03 | Tampering (data honesty) | null coerced to 0 in the UI | mitigate | corrected-wins helper renders null as `—`; grep-gate forbids `?? 0` on score/token fields |
| T-74-05-SC | Tampering | npm installs (shadcn sheet) | mitigate | `npx shadcn add sheet` vendors SOURCE from the shadcn OFFICIAL registry (not a package install); components.json forbids third-party registries; RESEARCH Package Legitimacy Audit — Approved. No [ASSUMED]/[SUS]. (No new state-management package — @reduxjs/toolkit + react-redux already present in store/.) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the new files (slice + page + components).
- `performance` reducer registered in store/index.ts combineReducers.
- Page/components use Redux (`useAppSelector`/`useAppDispatch`); no page-local shared state; no in-component fetch.
- `npm run build` succeeds; frontend supervisor restarted.
- `gsd-browser` visual pass against `localhost:3032/performance` (human checkpoint).
- Same-origin fetch grep-gate passes (in the slice); no `?? 0` on score/token fields.
</verification>

<success_criteria>
- Performance tab + route registered after Token Usage.
- A `performance` Redux slice (createSlice + fetchRuns/fetchTimeline thunks + facet/selected reducers + selectors) registered in the store; page + components consume it via the typed hooks.
- Faceted sidebar narrows the runs table with live counts via selectors (no page-local filtered arrays).
- Corrected-wins table cells + null-as-— honesty.
- Collapsible timeline with always-visible tier badge, fetched via the fetchTimeline thunk.
</success_criteria>

<output>
Create `.planning/phases/74-performance-dashboard-reports/74-05-SUMMARY.md` when done.
</output>
</parameter>
</invoke>
