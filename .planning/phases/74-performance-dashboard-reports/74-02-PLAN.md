---
phase: 74-performance-dashboard-reports
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - lib/experiments/query.mjs
  - lib/experiments/timeline-read.mjs
autonomous: true
requirements: [DASH-01, DASH-02]
user_setup: []

must_haves:
  truths:
    - "readRuns returns each Run joined to its Score and Outcome by metadata.run_task_id"
    - "Pending Runs are excluded by default and included only when includePending is set"
    - "A score/heuristic dimension that is null survives as null (never coerced to 0)"
    - "readTimeline groups per-reasoning-step rows under their per-turn parent and never opens token-usage.db writable"
    - "A missing token-usage.db yields a graceful empty timeline, not a throw"
  artifacts:
    - path: "lib/experiments/query.mjs"
      provides: "readRuns(store, opts) — iterate Runs + join Score/Outcome maps"
      exports: ["readRuns"]
    - path: "lib/experiments/timeline-read.mjs"
      provides: "readTimeline(taskId, dbPathOverride) — readonly token-usage.db, parent/child grouped"
      exports: ["readTimeline"]
  key_links:
    - from: "lib/experiments/timeline-read.mjs"
      to: ".data/llm-proxy/token-usage.db"
      via: "better-sqlite3 readonly open, task_id bound as ?"
      pattern: "readonly:\\s*true"
    - from: "lib/experiments/query.mjs"
      to: "store.iterate"
      via: "iterate by entityType then join on run_task_id"
      pattern: "iterate\\(\\{\\s*entityType"
---

<objective>
Build the two net-new read services the Performance tab consumes: `readRuns` (joins Runs to Scores+Outcomes from the experiment LevelDB) and `readTimeline` (reads per-turn / per-reasoning-step rows read-only from the proxy-owned `token-usage.db` and nests sub-bands under their parent turn). These are the DASH-01 and DASH-02 data layers. Turn the Plan 01 `run-read.test.mjs` and `timeline-read.test.mjs` GREEN.

Purpose: The page and endpoints (Plans 04/05) call these readers; getting the two-store boundary and the null-not-zero rule right here is load-bearing for honest UI numbers.
Output: `lib/experiments/query.mjs` + `lib/experiments/timeline-read.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-performance-dashboard-reports/74-PATTERNS.md
@.planning/phases/74-performance-dashboard-reports/74-RESEARCH.md
@lib/experiments/score-write.mjs
@lib/experiments/token-aggregate.mjs

<interfaces>
<!-- From the live codebase. -->

readRuns analog — score-write.mjs:85-104 (iterate-by-entityType + metadata-key join):
- caller passes an ALREADY-OPEN store; readRuns does NOT open/close (mirrors writeScore/applyOverride)
- `for await (const e of store.iterate({ entityType: 'Score' })) { ... e.metadata.run_task_id ... }`
- Join keys: Score.metadata.run_task_id == Run.metadata.task_id; Outcome.metadata.run_task_id == Run.metadata.task_id
- Run facet fields (run-write.mjs / experiment.json): task_id, task_class, agent, model, framework, pending, started_at, ended_at, loop_count, edit_revert_count, redundant_read_count, abandoned_tool_count, total_step_count, wallclock_per_step
- Score dims (5): goal_achieved, code_quality, test_coverage, regressions, spec_drift; plus goal_aligned_ratio, corrected_*, overridden_by, overridden_at, pending, not_scored
- null-not-zero: NEVER `?? 0` a dimension/heuristic — null means "no evidence / could not compute"

readTimeline analog — token-aggregate.mjs:62-100 (readonly DB + bound param + graceful missing-file):
- `if (!fs.existsSync(dbPath)) return [];`  (graceful empty, NOT throw)
- `new Database(dbPath, { readonly: true, fileMustExist: true })` — readonly is load-bearing (sole-writer guardrail)
- SELECT timestamp, granularity_tier, tool_call_id, parent_call_id, reasoning_tokens, input_tokens, output_tokens, total_tokens, tokens_estimated, model FROM token_usage WHERE task_id = ? ORDER BY timestamp ASC, tool_call_id ASC
- task_id ALWAYS bound as `?` — NEVER string-interpolated (V5 / T-71-03-01)
- granularity_tier ∈ {'per-turn','per-reasoning-step','per-session-aggregate'}
- parent linkage: a per-turn row's tool_call_id is the base; its sub-bands are per-reasoning-step rows whose tool_call_id.split(':reason:')[0] === parent tool_call_id
- copilot per-session-aggregate = single band, no children
- accept `dbPathOverride` param (tests point at a fixture DB); default resolves like token-aggregate.mjs:resolveDbPath
- close() in finally
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: readRuns join service (query.mjs)</name>
  <read_first>
    - lib/experiments/score-write.mjs (lines 85-104 — iterate-by-entityType + run_task_id join idiom to mirror)
    - lib/experiments/run-write.mjs (Run/Outcome metadata field list + null-not-zero rule)
    - tests/experiments/run-read.test.mjs (the RED test this turns GREEN — its exact assertions are the contract)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Code Examples — readRuns reference implementation; §Experiment Data Model — join keys)
  </read_first>
  <files>lib/experiments/query.mjs</files>
  <action>Create `lib/experiments/query.mjs` exporting `async function readRuns(store, { includePending = false } = {})`. It receives an ALREADY-OPEN store (caller owns open/close — same contract as writeScore). In one `iterate({ entityType:'Score' })` pass build `Map(run_task_id → Score.metadata)`; in one `iterate({ entityType:'Outcome' })` pass build `Map(run_task_id → Outcome.metadata)`; then `iterate({ entityType:'Run' })` and for each Run skip it when `metadata.pending && !includePending` (D-06 quarantine), else push `{ ...run.metadata, score: scoreMap.get(task_id) ?? null, outcome: outcomeMap.get(task_id) ?? null }`. Preserve null dimension/heuristic values exactly — do NOT apply `?? 0` anywhere (null = "no evidence", RESEARCH Pitfall 4). Return the rows array. Add a `effectiveDimension(score, dim)` helper exported alongside: `corrected_<dim>` if non-null else judged `<dim>` else null (the corrected-wins rule, D-03 — shared so endpoints/aggregates use one definition). Log via `process.stderr.write` only (no-console-log). Do NOT import or construct a store — the store comes in as an argument.</action>
  <verify>
    <automated>node --test tests/experiments/run-read.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `node --test tests/experiments/run-read.test.mjs` passes (GREEN).
    - Source assertion: `grep -c "iterate({ entityType" lib/experiments/query.mjs` ≥ 3 (Score, Outcome, Run passes).
    - Source assertion: no `?? 0` on a score/heuristic field: `grep -n "?? 0" lib/experiments/query.mjs` returns nothing (null-not-zero).
    - Source assertion: file does NOT call `openExperimentStore` or `new GraphKMStore` (it takes an open store) — `grep -c "new GraphKMStore" lib/experiments/query.mjs` is 0.
    - Source assertion: no `console.` token (filter comment lines): `grep -v '^//' lib/experiments/query.mjs | grep -c "console\." | grep -qx 0`.
  </acceptance_criteria>
  <done>readRuns returns Runs joined to Score+Outcome, excludes pending by default, preserves nulls; run-read.test.mjs green.</done>
</task>

<task type="auto">
  <name>Task 2: readTimeline read-only service (timeline-read.mjs)</name>
  <read_first>
    - lib/experiments/token-aggregate.mjs (lines 55-100 — readonly open, bound param, graceful-missing-file, dbPathOverride, resolveDbPath)
    - tests/experiments/timeline-read.test.mjs (the RED test this turns GREEN — grouping + readonly + graceful-empty assertions)
    - lib/lsl/token/claude-token-rows.mjs (lines ~189,217-224 — granularity_tier values + `${base}:reason:${N}` tool_call_id form)
    - .planning/phases/74-performance-dashboard-reports/74-RESEARCH.md (§Timeline Data Source — parent/child grouping + estimated-token honesty)
  </read_first>
  <files>lib/experiments/timeline-read.mjs</files>
  <action>Create `lib/experiments/timeline-read.mjs` exporting `function readTimeline(taskId, dbPathOverride)`. Mirror token-aggregate.mjs exactly for DB access: resolve dbPath (reuse the same default-resolution logic; accept `dbPathOverride` for tests); if the file is missing return `[]` (graceful empty, NOT a throw); open `new Database(dbPath, { readonly: true, fileMustExist: true })` and `db.close()` in finally. Run a single prepared SELECT of the columns in interfaces WHERE `task_id = ?` (taskId bound as `?` — NEVER interpolated) ORDER BY timestamp ASC, tool_call_id ASC. Group rows into parents+children: a `per-turn` (or `per-session-aggregate`) row is a parent; `per-reasoning-step` rows attach to the parent whose `tool_call_id` equals `child.tool_call_id.split(':reason:')[0]`; orphan reasoning rows (no matching parent) become their own parent so nothing is dropped. Return an array of `{ ...parentRow, tier: granularity_tier, estimated: tokens_estimated === 1, children: [...] }`. Copilot `per-session-aggregate` rows have no children. Log via `process.stderr.write` only.</action>
  <verify>
    <automated>node --test tests/experiments/timeline-read.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `node --test tests/experiments/timeline-read.test.mjs` passes (GREEN) — including the per-reasoning-step-nests-under-parent, missing-file-graceful-empty, and task_id-scoping cases.
    - Source assertion: `grep -c "readonly: true" lib/experiments/timeline-read.mjs` ≥ 1 (sole-writer guardrail).
    - Source assertion: the SELECT binds task_id as a parameter — `grep -n "WHERE task_id = ?" lib/experiments/timeline-read.mjs` matches; `grep -n "task_id = '" lib/experiments/timeline-read.mjs` returns nothing (no interpolation).
    - Source assertion: `grep -n ":reason:" lib/experiments/timeline-read.mjs` matches (parent/child split logic present).
    - Source assertion: no `console.` token (comment-filtered).
  </acceptance_criteria>
  <done>readTimeline reads token-usage.db read-only, nests reasoning sub-bands under their turn, graceful-empty on missing DB; timeline-read.test.mjs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → token-usage.db | Untrusted `task_id` reaches a SQL query against the proxy-owned DB |
| reader → experiment LevelDB | Reads only; never writes; store opened by caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02-01 | Tampering | SQL injection via task_id into token-usage.db | mitigate | task_id ALWAYS bound as `?` in the prepared statement; never string-interpolated (token-aggregate.mjs precedent, T-71-03-01) |
| T-74-02-02 | Tampering/availability | becoming a second writer to the proxy-owned DB | mitigate | open `token-usage.db` `{ readonly: true }` only — grep-gated in acceptance |
| T-74-02-03 | Information disclosure | over-serializing internal fields from Runs | mitigate | readRuns returns only Run/Score/Outcome metadata fields; no provenance/internal-only fields emitted |
| T-74-02-SC | Tampering | npm installs | mitigate | No new packages; `better-sqlite3` + km-core pre-existing (RESEARCH Package Legitimacy Audit — Approved). No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `node --test tests/experiments/run-read.test.mjs tests/experiments/timeline-read.test.mjs` — both green.
- `node --test tests/experiments/` — full experiment suite stays green (no regression).
- Grep gates above all pass.
</verification>

<success_criteria>
- readRuns joins + excludes pending + preserves nulls.
- readTimeline reads read-only, bound param, nests sub-bands, graceful-empty.
- Two-store boundary honored (LevelDB for Runs, SQLite for timeline).
</success_criteria>

<output>
Create `.planning/phases/74-performance-dashboard-reports/74-02-SUMMARY.md` when done.
</output>
