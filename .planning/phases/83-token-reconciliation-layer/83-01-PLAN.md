---
phase: 83-token-reconciliation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs
autonomous: true
requirements: [D-07, D-08, D-10]
must_haves:
  truths:
    - "A malformed x-task-id header on the /v1/messages tap never crashes the proxy daemon"
    - "A header-less (interactive) tap request does not inherit a concurrent measured cell's task_id"
    - "The same logical task_id keys identically at the DB task_id seam, the _ctxMaxByTask in-memory seam, and the breakdown-filename seam"
  artifacts:
    - path: /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs
      provides: "Guarded + unified task-id binding on the tap/shim handlers"
      contains: "sanitizeTaskId"
  key_links:
    - from: "tap header path (~server.mjs:1999)"
      to: "sanitizeTaskId"
      via: "try/catch → resolveLiveTaskId() fallback on throw"
      pattern: "sanitizeTaskId"
    - from: "_ctxMaxByTask key + DB task_id + perRunBreakdownPath"
      to: "one sanitized task_id form"
      via: "sanitizeTaskId wrap at every seam"
      pattern: "sanitizeTaskId"
---

<objective>
Harden the proxy's task-id binding on the `/v1/messages` tap and OpenAI-shim handlers so the reconciliation matcher has stable, non-crashing, non-leaking row identity. Three Phase-82-review fold-ins, all surgical in-place patches to `proxy-bridge/server.mjs`:
- **D-07 (CR-01):** guard the unguarded `sanitizeTaskId` call site so a malformed `x-task-id` header cannot crash the daemon.
- **D-10 (WR-06):** unify task-id sanitization across the three binding seams so the same logical id keys identically everywhere (restores the DB↔breakdown-file join reconciliation depends on).
- **D-08 (WR-01):** kill the ambient-span leak on the tap's empty-header branch so an interactive row no longer inherits a concurrent measured cell's task_id.

Purpose: A meaningful golden comparison (Plan 07) under concurrency is impossible while interactive rows steal cell task_ids and while the same id keys three different ways. This plan is a prerequisite for the reconcile matcher and the acceptance run.
Output: A patched `server.mjs` (not yet deployed — Plan 02 owns the single build+kickstart that also picks up these edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-token-reconciliation-layer/83-CONTEXT.md
@.planning/phases/83-token-reconciliation-layer/83-PATTERNS.md
@.planning/phases/82-wire-measurement-foundation/82-REVIEW.md
@.planning/research/proxy-infra-report.md

<interfaces>
<!-- Existing seams in proxy-bridge/server.mjs (line refs approximate — read the file). -->
<!-- sanitizeTaskId is imported at server.mjs:49 from '../dist/measurement-span.js' — the canonical validator. -->
<!-- D-07/D-10 WRAP it, never replace it. -->

Tap header path (~:1988-1999):
  const taskId = hdrTaskId ? sanitizeTaskId(hdrTaskId) : resolveLiveTaskId();
  // The :1999 branch is UNGUARDED — a sanitizeTaskId throw crashes the daemon (D-07).
  // The empty-header fallback to resolveLiveTaskId() inherits the ambient live span (D-08).

Shim path segment (~:2243-2244) — ALREADY guarded (copy its shape onto the tap site):
  try { shimPathTaskId = sanitizeTaskId(decodeURIComponent(m[2])); }
  catch { shimPathTaskId = sanitizeTaskId(m[2]); }

The three WR-06 seams that must key on ONE sanitized form (D-10):
  - raw DB task_id           : tap logTokenCall task_id (~:2174), shim internalBody.task_id (~:2315)
  - raw in-memory key        : _ctxMaxByTask (~:2031-2032 uses taskId; ~:2293-2294 uses effTaskId = taskId || resolveLiveTaskId())
  - sanitized filename       : perRunBreakdownPath (~:1592 = sanitizeTaskId(taskId))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the tap sanitizeTaskId site (D-07) and unify the three task-id seams (D-10)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (read the tap block ~:1957-2182, the shim block ~:2226-2338, perRunBreakdownPath ~:1592, and the _ctxMaxByTask writes ~:2031-2032 and ~:2293-2294 — do NOT edit blind)
    - .planning/phases/82-wire-measurement-foundation/82-REVIEW.md (CR-01 + WR-06 exact repro and suggested fixes)
    - .planning/phases/83-token-reconciliation-layer/83-PATTERNS.md (§ server.mjs — CR-01/WR-06 pattern assignments)
  </read_first>
  <action>
    D-07 (CR-01): wrap the tap header sanitizeTaskId call (~:1999) in a try/catch that falls back to `resolveLiveTaskId()` on throw, copying the exact shape the shim site already uses at ~:2243-2244 (`try { sanitizeTaskId(x) } catch { safe-default }`). A malformed `x-task-id` header must resolve to the live-span fallback (which D-08's Task 2 then further constrains), never propagate an exception out of the request handler. Do NOT replace `sanitizeTaskId` — it stays the single validator imported at :49.
    D-10 (WR-06): define ONE canonical sanitized form of the task id (the result of `sanitizeTaskId(rawId)`) at the point the id is first resolved in each handler, and key ALL THREE seams on that single value: the DB `task_id` written by `logTokenCall` (tap ~:2174 and shim `internalBody.task_id` ~:2315), the in-memory `_ctxMaxByTask` map key (~:2031-2032 and the `effTaskId` computation ~:2293-2294), and the breakdown filename via `perRunBreakdownPath` (~:1592). The raw id must never be used as a map/DB key while the filename uses the sanitized form — that mismatch is exactly what breaks the DB↔breakdown-file join. Preserve every existing fallback branch (header-less → resolveLiveTaskId) but sanitize its output before it becomes a key.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --check proxy-bridge/server.mjs && grep -c "sanitizeTaskId" proxy-bridge/server.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check proxy-bridge/server.mjs` exits 0 (no syntax error introduced).
    - The tap header resolution at ~:1999 is inside a try/catch whose catch resolves to `resolveLiveTaskId()` (grep: a try-block wrapping the tap `sanitizeTaskId(hdrTaskId)` with a `resolveLiveTaskId` catch-fallback appears).
    - The `_ctxMaxByTask` write sites and the `logTokenCall` task_id argument key on the sanitized id, not the raw header value — verified by reading the edited region: no path binds `hdrTaskId` (raw) directly into `_ctxMaxByTask[...]` or the DB row.
    - `perRunBreakdownPath` continues to receive the same sanitized id used as the DB/map key.
  </acceptance_criteria>
  <done>Malformed x-task-id cannot throw out of the tap handler; the DB task_id, the _ctxMaxByTask key, and the breakdown filename all derive from one sanitizeTaskId(rawId) value.</done>
</task>

<task type="auto">
  <name>Task 2: Kill the ambient-span leak on the tap's empty-header branch (D-08)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (the tap task-id resolution ~:1988-1999 and the WIRE-03 comment block ~:1988-1993 documenting the intent)
    - .planning/phases/82-wire-measurement-foundation/82-CONTEXT.md (header-binding + ambient-span decisions from Phase 82)
    - .planning/phases/82-wire-measurement-foundation/82-REVIEW.md (WR-01 exact repro)
  </read_first>
  <action>
    The tap's empty-header branch currently falls back to `resolveLiveTaskId()` (~:1999), so a header-less interactive `/v1/messages` request inherits whatever measured cell span is currently live — the exact contamination the 82-06 decision fixed for the `/api/complete` path but NOT for the tap. Tighten the empty-header branch so a header-less tap request does NOT inherit a concurrent measured cell's task_id: when no `x-task-id` header is present, the tap row must be stamped as interactive (empty/neutral task_id) rather than adopting the ambient live span. Only an explicit `x-task-id` header (or the shim path-segment task id) binds a row to a measured cell. Preserve the existing header-precedence logic (explicit header always wins) and keep the change behind the same WIRE-03 comment intent already documented at ~:1988-1993; extend that comment to note the empty-header no-inherit rule. Mark the empty-header neutral-stamp site with a distinctive short sentinel token `WR-01-no-inherit` on the neutral-stamp line/block (this is the greppable behavioral anchor — the tap task-id resolution is inline in the request handler, not a pure `dist/` helper like `isBackgroundProcess`, so there is no cheap unit seam; the sentinel + a negative `resolveLiveTaskId(` grep over its branch is the strengthened gate). This closes the WR-01 leak so Plan 07's concurrent golden comparison shows interactive rows with no cell task_id.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --check proxy-bridge/server.mjs && grep -qF 'WR-01-no-inherit' proxy-bridge/server.mjs && ! grep -A4 -F 'WR-01-no-inherit' proxy-bridge/server.mjs | grep -qF 'resolveLiveTaskId('</automated>
  </verify>
  <acceptance_criteria>
    - `node --check proxy-bridge/server.mjs` exits 0.
    - The empty-header neutral-stamp site carries a `WR-01-no-inherit` sentinel AND has no `resolveLiveTaskId(` call within its branch (the 4 lines following the sentinel) — a wrong edit that reverts the header-less path to the ambient fallback trips this grep gate instead of only failing at the live golden run.
    - The empty-header branch of the tap task-id resolution no longer returns `resolveLiveTaskId()` as the row's task_id for interactive traffic — verified by reading the edited ~:1988-1999 region: the header-less path stamps a neutral/empty task_id, and only an explicit header binds a measured cell.
    - An explicit `x-task-id` header still binds the row (header-precedence preserved).
    - The WIRE-03 comment block documents the no-inherit rule.
  </acceptance_criteria>
  <done>Header-less tap rows carry no measured-cell task_id; explicit-header rows still bind correctly. Live confirmation of no-inheritance under concurrency happens in Plan 07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coding-agent CLI → proxy `/v1/messages` tap | Untrusted `x-task-id` request header crosses here |
| proxy → SQLite token_usage + breakdown files | task_id becomes a DB key AND a filename component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01-01 | Denial of Service | tap header sanitizeTaskId (~:1999) | mitigate | D-07 try/catch → resolveLiveTaskId fallback; malformed header can never throw out of the handler and crash the daemon |
| T-83-01-02 | Tampering | task_id used as _ctxMaxByTask key / DB task_id | mitigate | All seams key on `sanitizeTaskId(raw)` (`[A-Za-z0-9._-]`, ≤80) — no raw header value reaches a map/DB key; unifies join key |
| T-83-01-03 | Spoofing | header-less request adopting a live cell span | mitigate | D-08 empty-header no-inherit rule; interactive traffic cannot attribute itself to a measured cell |
| T-83-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — edits to existing server.mjs only |
</threat_model>

<verification>
- `node --check proxy-bridge/server.mjs` exits 0 after both tasks.
- `sanitizeTaskId` remains imported once (server.mjs:49) and is never redefined.
- No live deploy in this plan — the single build + `launchctl kickstart` that activates these edits lives in Plan 02 (which also lands D-09/D-11), and Plan 02's smoke test verifies the combined daemon.
</verification>

<success_criteria>
- Malformed `x-task-id` cannot crash the tap handler (D-07).
- One sanitized task_id form keys the DB, the in-memory map, and the breakdown filename (D-10).
- Header-less tap rows do not inherit a measured cell's task_id (D-08).
</success_criteria>

<output>
Create `.planning/phases/83-token-reconciliation-layer/83-01-SUMMARY.md` when done.
</output>
