---
phase: 83-token-reconciliation-layer
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [D-04, D-05, D-08, D-12]
files_modified:
  - lib/lsl/token/reconcile.mjs
  - lib/lsl/token/token-db.mjs
  - lib/lsl/token/stop-adapter-registry.mjs
  - scripts/measurement-stop.mjs
  - scripts/launch-agent-common.sh
  - tests/token-adapters/reconcile-matcher.test.js
  - tests/token-adapters/reconcile-mode.test.js

must_haves:
  truths:
    - "reconciliation.json summary.aggregateDeltas contains the per-field SUM of every perRequest[].deltas[field].delta (not {} when non-zero deltas exist)"
    - "A measured claude span with a genuine orphan proxy wire row (user_hash='cladpt', no transcript counterpart) reports unmatched_wire >= 1 — the metric is meaningful, not vacuously 0"
    - "An interactive-launch measured span whose proxy wire rows carry task_id='' gets those rows stamped with the span task_id at reconcile time, so aggregateByTaskId(span.task_id) recovers the foreground tokens (no attribution loss)"
  artifacts:
    - path: lib/lsl/token/reconcile.mjs
      provides: "aggregatePerRequestDeltas(perRequest) pure roll-up helper + reconcileRow returns the matched wire row id + task_id gap-fill bind"
      contains: "export function aggregatePerRequestDeltas"
    - path: lib/lsl/token/token-db.mjs
      provides: "RECONCILE_GAP_FILL_SQL backfills task_id on task_id='' wire rows"
      contains: "task_id = CASE WHEN task_id"
    - path: scripts/measurement-stop.mjs
      provides: "sink aggregateDeltas computed via aggregatePerRequestDeltas (dead typeof-number guard removed)"
    - path: lib/lsl/token/stop-adapter-registry.mjs
      provides: "pre-loop wire-row PK snapshot + rowid-based unmatched_wire count (no user_hash!=adapter exclusion)"
    - path: scripts/launch-agent-common.sh
      provides: "corrected claude header comment stating the D-08 no-inherit rule"
    - path: tests/token-adapters/reconcile-matcher.test.js
      provides: "unit test for aggregatePerRequestDeltas summing a non-zero delta"
    - path: tests/token-adapters/reconcile-mode.test.js
      provides: "production-shape (user_hash='cladpt') orphan-wire test + task_id='' gap-fill stamping test"
  key_links:
    - from: scripts/measurement-stop.mjs
      to: lib/lsl/token/reconcile.mjs
      via: "import { aggregatePerRequestDeltas }"
      pattern: "aggregatePerRequestDeltas"
    - from: lib/lsl/token/reconcile.mjs reconcileRow
      to: lib/lsl/token/token-db.mjs reconcileGapFill
      via: "task_id passed in the gap-fill fields object"
      pattern: "task_id"
    - from: lib/lsl/token/stop-adapter-registry.mjs reconcileBatches
      to: token_usage wire rows
      via: "pre-loop rowid snapshot minus matched wire-row ids"
      pattern: "matchedWireRowIds"
---

<objective>
Close the three verified BLOCKERs from 83-VERIFICATION.md so the three FAILED must-have truths flip to VERIFIED on re-verification. All three are surgical host-side fixes to the reconciliation output/wiring stage; the core reconcile machinery (matcher, enrich SQL, fallback, API route) is already correct and fully tested.

- **CR-01** — `summary.aggregateDeltas` in every `reconciliation.json` is always `{}` because the roll-up in `measurement-stop.mjs` guards on `typeof val === 'number'` while `computeDeltas` emits `{wire,transcript,delta,flagged}` objects. Repairs D-05 / D-12.
- **CR-02** — `unmatched_wire` is structurally 0 for claude because `countUnmatchedWireRows` excludes rows where `user_hash = 'cladpt'`, which is exactly the user_hash the proxy tap stamps on claude wire rows. Repairs the Plan-04 metric intent under D-04.
- **CR-03** — Interactive-launch spans (agents launched without `TASK_ID`) send a blank `x-task-id` for their lifetime; post-D-08 the tap stamps `task_id=''`, and `RECONCILE_GAP_FILL_SQL` never backfills it, so `aggregateByTaskId(span.task_id)` finds 0 foreground tokens. Repairs the D-08 interaction without reintroducing ambient inheritance (the stamp is span-scoped, applied only to matched transcript rows at reconcile time).

Purpose: The Phase-86 reconciliation badge and any future flagged/interactive-launch run must read real aggregate deltas, a meaningful unmatched_wire, and non-zero foreground totals. The Plan-07 golden run masked all three because it used experiment cells (launched WITH TASK_ID, perfectly-matched pairs) and abstracted the production data shape away (user_hash='wire01', not 'cladpt').

Output: 5 modified source files + 2 test files with 3 new/changed test cases (one per BLOCKER). No proxy/docker rebuild (all files are host-side .mjs/.sh — none touch the rapid-llm-proxy repo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/83-token-reconciliation-layer/83-VERIFICATION.md
@.planning/phases/83-token-reconciliation-layer/83-CONTEXT.md
@.planning/phases/83-token-reconciliation-layer/83-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly; no exploration needed. -->

lib/lsl/token/reconcile.mjs (existing exports):
- `matchWireRow(db, transcriptRow, span, opts) -> { method: 'request-id'|'fuzzy'|null, wireRow: {id, tool_call_id, model, timestamp, input_tokens, output_tokens, reasoning_tokens, cache_read_tokens, cache_write_tokens, parent_call_id, granularity_tier} | null }`
- `computeDeltas(wireRow, transcriptRow) -> Record<field, {wire:number, transcript:number, delta:number, flagged:boolean}>`  (only NON-zero-delta fields are present)
- `reconcileRow(db, transcriptRow, span, opts) -> { method, matched:boolean, enriched:boolean, fallback:boolean, deltas:object, flagged:boolean, alwaysInsert?:boolean }`  ← this return shape is extended by Tasks 2 & 3

lib/lsl/token/token-db.mjs (existing):
- `RECONCILE_GAP_FILL_SQL` — UPDATE token_usage SET reasoning_tokens=MAX(...), granularity_tier=COALESCE(NULLIF(...)), parent_call_id=COALESCE(NULLIF(...)), cache_read_tokens=CASE..., cache_write_tokens=CASE... WHERE tool_call_id = ?
- `reconcileGapFill(db, toolCallId, fields)` — binds (num(reasoning), text(granularity), text(parent), num(cache_read), num(cache_write), toolCallId) in that positional order; `num()`/`text()` coalescers exist in-module
- `insertTokenRowDeduped(db, row)`

lib/lsl/token/stop-adapter-registry.mjs (existing):
- `reconcileBatches({db, batches, span, agent, adapter, taskId, opts})` — per-transcript-row loop; builds `report` and `matchedRequestIds`; calls `countUnmatchedWireRows(db, span, taskId, adapter.userHash, matchedRequestIds)` post-loop
- `countUnmatchedWireRows(db, span, taskId, adapterUserHash, matchedRequestIds)` — CURRENTLY `SELECT ... WHERE task_id = ? AND user_hash != ?` (the CR-02 defect)
- `withinSpanWindow(row, span)` — module-private window clamp; reuse verbatim
- `adapter.userHash` is `'cladpt'` for claude, `'copadt'` for copilot

scripts/measurement-stop.mjs (existing, ~lines 440-486):
- builds `aggregateDeltas` inline via `for (const [field, val] of Object.entries(deltas)) { if (typeof val === 'number' ...) }` — the CR-01 dead guard; `reconcileReport.perRequest[]` each carry `.deltas` (object of `{wire,transcript,delta,flagged}`)

PROOF that wire and transcript rows share user_hash for claude (root of CR-02): proxy tap writes `user_hash: adapterUserHash('claude') === 'cladpt'` and `process: 'token-adapter-claude'` (rapid-llm-proxy proxy-bridge/server.mjs ~:2047-2051, :2217) — IDENTICAL to the transcript adapter's own stamp. Wire vs transcript rows CANNOT be distinguished by user_hash OR process for claude; only by row identity (PK) captured before transcript inserts happen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (CR-01): aggregateDeltas roll-up sums perRequest deltas</name>
  <files>lib/lsl/token/reconcile.mjs, scripts/measurement-stop.mjs, tests/token-adapters/reconcile-matcher.test.js</files>
  <read_first>
    - lib/lsl/token/reconcile.mjs (see computeDeltas at :181-192 — the per-field `{wire,transcript,delta,flagged}` shape whose `.delta` must be summed)
    - scripts/measurement-stop.mjs lines 440-486 (the dead `typeof val === 'number'` roll-up at :447-454 and the reconciliation object it feeds)
    - tests/token-adapters/reconcile-matcher.test.js lines 27-45 (import style: `const { matchWireRow, computeDeltas, reconcileRow } = await import('../../lib/lsl/token/reconcile.mjs')`)
    - .planning/phases/83-token-reconciliation-layer/83-VERIFICATION.md gaps[0] (CR-01 reason + missing items)
  </read_first>
  <behavior>
    - aggregatePerRequestDeltas([]) returns {}
    - aggregatePerRequestDeltas([{deltas:{cache_read_tokens:{wire:100,transcript:150,delta:50,flagged:false}}}]) returns {cache_read_tokens: 50}
    - Given two perRequest entries each with a non-zero cache_read_tokens delta (50 and 30), the field sums to 80
    - Multiple distinct fields (e.g. cache_read_tokens and reasoning_tokens) each accumulate independently
    - A perRequest entry with `deltas: {}` contributes nothing; a null/missing deltas is skipped without throwing
  </behavior>
  <action>
    Add an exported pure helper `aggregatePerRequestDeltas(perRequest)` to lib/lsl/token/reconcile.mjs, placed adjacent to `computeDeltas`. It iterates the array; for each entry's `deltas` object it reads each field value's `.delta` number (the entry is `{wire,transcript,delta,flagged}`), and accumulates `out[field] = (out[field] ?? 0) + delta` only when `.delta` is a finite number. It must never throw on null/undefined/non-object entries or non-object deltas (return the partial map). This is the exact inverse of the CR-01 defect: the OLD inline loop tested `typeof val === 'number'` against the whole delta OBJECT (always false) — the helper must unwrap `.delta` first.
    Then in scripts/measurement-stop.mjs, import `aggregatePerRequestDeltas` from `../lib/lsl/token/reconcile.mjs` (match the existing relative-path import convention in that file) and REPLACE the inline `aggregateDeltas` accumulation loop (~:446-454) with `const aggregateDeltas = aggregatePerRequestDeltas(perRequest);`. Leave the surrounding best-effort try/catch, the `reconciliation` object literal, and the sink write untouched.
    Add a unit test to tests/token-adapters/reconcile-matcher.test.js covering the behaviors above (import `aggregatePerRequestDeltas` alongside the existing reconcile.mjs imports).
  </action>
  <verify>
    <automated>node --test tests/token-adapters/reconcile-matcher.test.js 2>&1 | tail -5</automated>
    <automated>node -e "import('./lib/lsl/token/reconcile.mjs').then(m => { const r = m.aggregatePerRequestDeltas([{deltas:{cache_read_tokens:{wire:100,transcript:150,delta:50,flagged:false}}},{deltas:{cache_read_tokens:{wire:0,transcript:30,delta:30,flagged:false}}}]); if (r.cache_read_tokens !== 80) { console.error('FAIL', r); process.exit(1);} console.log('OK', r); })"</automated>
    <automated>grep -q "aggregatePerRequestDeltas" scripts/measurement-stop.mjs && grep -vq "typeof val === 'number'" scripts/measurement-stop.mjs && echo GUARD-REMOVED</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lsl/token/reconcile.mjs` exports `aggregatePerRequestDeltas` (grep `export function aggregatePerRequestDeltas`)
    - The node -e assertion above prints `OK { cache_read_tokens: 80 }` and exits 0
    - `scripts/measurement-stop.mjs` calls `aggregatePerRequestDeltas(perRequest)` and no longer contains the string `typeof val === 'number'`
    - `node --test tests/token-adapters/reconcile-matcher.test.js` reports all tests passing (13 existing + the new aggregate case)
  </acceptance_criteria>
  <done>reconciliation.json summary.aggregateDeltas is the per-field SUM of perRequest deltas; a non-zero delta appears in the aggregate instead of `{}`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (CR-03): reconcile gap-fill backfills span task_id onto task_id='' wire rows</name>
  <files>lib/lsl/token/token-db.mjs, lib/lsl/token/reconcile.mjs, scripts/launch-agent-common.sh, tests/token-adapters/reconcile-mode.test.js</files>
  <read_first>
    - lib/lsl/token/token-db.mjs lines 240-317 (RECONCILE_GAP_FILL_SQL at :253-260 and reconcileGapFill's positional binds at :299-317)
    - lib/lsl/token/reconcile.mjs lines 208-247 (reconcileRow — the `reconcileGapFill(db, wireRow.tool_call_id, {...})` call at :235-241 whose fields object gains task_id)
    - scripts/launch-agent-common.sh lines 405-420 (the claude case; stale comment at :414-416 and `ANTHROPIC_CUSTOM_HEADERS="x-task-id: ${TASK_ID:-}"` at :418)
    - tests/token-adapters/reconcile-mode.test.js lines 96-135 (seedWireRow fixture; note task_id default at :126)
    - .planning/phases/83-token-reconciliation-layer/83-VERIFICATION.md gaps[2] (CR-03 reason + missing items) and the D-08 constraint note in the plan header
  </read_first>
  <behavior>
    - A matched wire row seeded with task_id='' is UPDATEd to the span's task_id after reconcileRow runs (the gap-fill stamps it)
    - A matched wire row that ALREADY carries a non-empty task_id is left untouched (CASE WHEN task_id='' guard — never overwrite an existing binding)
    - After reconcile, a direct `SELECT SUM(...) FROM token_usage WHERE task_id = <spanTaskId>` recovers the previously-task_id='' wire row's tokens (attribution recovered)
    - Wire counts (input/output/cache) are NOT altered by the task_id backfill (only task_id changes)
  </behavior>
  <action>
    In lib/lsl/token/token-db.mjs, extend RECONCILE_GAP_FILL_SQL's SET clause with `task_id = CASE WHEN task_id = '' THEN ? ELSE task_id END`. Add the corresponding bind in `reconcileGapFill` — insert `text(fields.task_id)` into the positional `.run(...)` list at the SAME position the new `?` occupies in the SQL (the `?` order and the bind order MUST stay aligned; the WHERE-clause `toolCallId` bind stays last). Update the RECONCILE_GAP_FILL_SQL doc comment (:240-252) to state that a task_id='' wire row (interactive-launch, blank x-task-id per D-08) is stamped with the span task_id at reconcile time — a span-scoped stamp on an already-matched transcript row, NOT ambient inheritance.
    In lib/lsl/token/reconcile.mjs, add `task_id: transcriptRow.task_id` to the fields object passed to `reconcileGapFill` in reconcileRow (:235-241). `transcriptRow.task_id` is already the span taskId (set by reconcileBatches at stop-adapter-registry.mjs:505).
    In scripts/launch-agent-common.sh, correct the stale comment at :414-416: replace the claim that an empty TASK_ID lets "the tap fall back to its ambient resolveLiveTaskId() (safety valve)" with an accurate D-08 statement — an empty TASK_ID sends a blank x-task-id and, per D-08 (no-inherit), the tap stamps task_id='' on those wire rows (NO ambient inheritance); the span binding is recovered later at reconcile time by the gap-fill task_id backfill. Do NOT change the `export ANTHROPIC_CUSTOM_HEADERS` line itself.
    Add a test to tests/token-adapters/reconcile-mode.test.js: seed a wire row (production shape — see Task 3's seedWireRow change; for this task seed with task_id='' explicitly and user_hash='cladpt') whose tool_call_id matches a transcript fixture row; run captureForegroundTokens with reconcile:true; assert the wire row's task_id is now the span task_id (SELECT task_id FROM token_usage WHERE tool_call_id=...), and that a second wire row pre-seeded with a non-empty task_id is unchanged.
  </action>
  <verify>
    <automated>node --test tests/token-adapters/reconcile-mode.test.js 2>&1 | tail -6</automated>
    <automated>node --test tests/token-adapters/reconcile-matcher.test.js 2>&1 | tail -3</automated>
    <automated>node --test tests/token-adapters/token-db-dedup-merge.test.js 2>&1 | tail -3</automated>
    <automated>grep -q "task_id = CASE WHEN task_id" lib/lsl/token/token-db.mjs && echo GAPFILL-TASKID-PRESENT</automated>
    <automated>grep -Eq "no-inherit|D-08|task_id=''|blank x-task-id" scripts/launch-agent-common.sh && echo COMMENT-CORRECTED</automated>
  </verify>
  <acceptance_criteria>
    - `RECONCILE_GAP_FILL_SQL` contains `task_id = CASE WHEN task_id = '' THEN ? ELSE task_id END` and `reconcileGapFill` binds `text(fields.task_id)` at the matching position (bind count matches `?` count)
    - `reconcileRow` passes `task_id: transcriptRow.task_id` in the gap-fill fields object
    - New reconcile-mode test: a task_id='' matched wire row ends with the span task_id; a pre-bound wire row keeps its task_id; wire token counts unchanged
    - `scripts/launch-agent-common.sh` claude-case comment no longer claims ambient `resolveLiveTaskId()` fallback; it states the D-08 no-inherit rule
    - reconcile-mode, reconcile-matcher, and token-db-dedup-merge suites all pass
  </acceptance_criteria>
  <done>Interactive-launch measured spans recover foreground attribution: matched wire rows with task_id='' are stamped the span task_id at reconcile time, so aggregateByTaskId(span.task_id) is non-zero. No ambient inheritance reintroduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (CR-02): unmatched_wire counts real cladpt orphan wire rows</name>
  <files>lib/lsl/token/stop-adapter-registry.mjs, lib/lsl/token/reconcile.mjs, tests/token-adapters/reconcile-mode.test.js</files>
  <read_first>
    - lib/lsl/token/stop-adapter-registry.mjs lines 486-610 (reconcileBatches loop, the matchedRequestIds tracking, and countUnmatchedWireRows with the defective `user_hash != ?` filter at :593)
    - lib/lsl/token/stop-adapter-registry.mjs line 342 (withinSpanWindow — reuse verbatim for the snapshot filter)
    - lib/lsl/token/reconcile.mjs lines 208-247 (reconcileRow return object — extend with the matched wire row id) and lines 21-24 (the misleading "DIFFERENT user_hash by design" comment)
    - tests/token-adapters/reconcile-mode.test.js lines 96-135 (seedWireRow — the `user_hash ?? 'wire01'` default at :105 that masks CR-02) and the existing unmatched_wire test (behavior #7)
    - .planning/phases/83-token-reconciliation-layer/83-VERIFICATION.md gaps[1] (CR-02 reason + missing items) and the interfaces PROOF block in this plan's context (wire and claude transcript rows share user_hash AND process — only row identity distinguishes them)
  </read_first>
  <behavior>
    - Two production-shape wire rows (user_hash='cladpt') in the span window: one whose tool_call_id matches a transcript fixture row, one orphan with no transcript counterpart → report.unmatched_wire === 1
    - When every span-window wire row is matched → report.unmatched_wire === 0 (healthy span)
    - A cladpt fallback row INSERTED during the loop (process='token-adapter-claude-fallback') is NOT counted as an unmatched wire row (it post-dates the pre-loop snapshot)
    - The count keys on row identity (PK/rowid), not on user_hash exclusion or process
  </behavior>
  <action>
    Rework unmatched_wire in lib/lsl/token/stop-adapter-registry.mjs to a pre-loop wire-row PK snapshot approach (the interfaces PROOF shows wire and claude transcript rows are indistinguishable by user_hash AND process — only PK identity works). BEFORE the batch loop in reconcileBatches, snapshot the candidate wire rows: `SELECT id, tool_call_id, timestamp, task_id FROM token_usage WHERE user_hash = ? AND (task_id = ? OR task_id = '')` bound with (adapter.userHash, taskId), then filter each through `withinSpanWindow(row, span)`; collect their `id` values into a `wireRowIds` Set. These are all wire rows because transcript-adapter rows are not inserted until the loop runs. Extend reconcileRow (reconcile.mjs) to also return the matched wire row's id (e.g. add `wireRowId: wireRow.id` on the matched-return path and `wireRowId: null` on non-match paths) so the loop can record it without re-probing. In the reconcileBatches loop, when `result.matched`, add `result.wireRowId` to a `matchedWireRowIds` Set (drop the fuzzy re-probe at the current :529-535 since reconcileRow now returns the id directly). Replace the countUnmatchedWireRows call/body: unmatched_wire = number of ids in `wireRowIds` NOT present in `matchedWireRowIds`. Preserve the best-effort/never-throw discipline (wrap the snapshot query in try/catch → stderr line + treat as empty set on failure; a query failure yields 0, never blocks the close). Keep parameterized `?` binds only.
    Also fix the misleading comment at reconcile.mjs:21-24: the cross-key join is on `tool_call_id` ALONE because retry/fuzzy needs it, but note that for CLAUDE the wire and transcript rows carry the SAME user_hash ('cladpt') — the "different user_hash by design" framing is false for claude and was the root of the CR-02 mis-design.
    Update tests/token-adapters/reconcile-mode.test.js: change the seedWireRow default user_hash from 'wire01' to 'cladpt' (production shape) so the existing behavior-#7 unmatched_wire test exercises the real path, and add the two-cladpt-wire-row orphan test from <behavior> (one matched, one orphan → unmatched_wire===1). Ensure all existing reconcile-mode assertions still hold under the cladpt shape.
  </action>
  <verify>
    <automated>node --test tests/token-adapters/reconcile-mode.test.js 2>&1 | tail -8</automated>
    <automated>node --test tests/token-adapters/reconcile-matcher.test.js 2>&1 | tail -3</automated>
    <automated>node --test tests/token-adapters/token-db-dedup-merge.test.js 2>&1 | tail -3</automated>
    <automated>node --test tests/vkb-server/reconciliation-route.test.js 2>&1 | tail -3</automated>
    <automated>grep -Eq "user_hash != " lib/lsl/token/stop-adapter-registry.mjs && echo STILL-EXCLUDING-FAIL || echo EXCLUSION-REMOVED</automated>
    <automated>grep -q "matchedWireRowIds" lib/lsl/token/stop-adapter-registry.mjs && echo SNAPSHOT-PRESENT</automated>
    <automated>grep -c "cladpt" tests/token-adapters/reconcile-mode.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lsl/token/stop-adapter-registry.mjs` no longer contains a `user_hash != ` exclusion for the unmatched_wire count; it computes unmatched_wire from a pre-loop `wireRowIds` snapshot minus `matchedWireRowIds` (grep both `matchedWireRowIds` and absence of `user_hash != `)
    - `reconcileRow` returns a `wireRowId` field (matched → the wire row id; non-match → null)
    - New reconcile-mode orphan test: two cladpt wire rows (one matched, one orphan) → `report.unmatched_wire === 1`; all-matched → 0
    - `seedWireRow` default user_hash is 'cladpt'; the behavior-#7 test passes against the new counting logic (it would FAIL against the old `user_hash != 'cladpt'` query — proving the de-masking)
    - reconcile-mode, reconcile-matcher, token-db-dedup-merge, and reconciliation-route suites all pass
  </acceptance_criteria>
  <done>unmatched_wire is meaningful for claude spans: a genuine cladpt orphan wire row is counted (>=1), a healthy fully-matched span reports 0. The metric no longer passes vacuously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript/session file → reconcile matcher | adapter-parsed transcript rows (attacker-influenced only via local session logs) cross into DB reads/writes |
| reconcile matcher → SQLite token_usage | UPDATE/SELECT with task_id, tool_call_id, model, token values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-08-01 | Tampering (SQL injection) | RECONCILE_GAP_FILL_SQL task_id backfill + pre-loop wire-row snapshot query | mitigate | All new SQL uses `?` parameter binds only; task_id/tool_call_id/user_hash never string-interpolated; values coalesced via existing `text()`/`num()` helpers |
| T-83-08-02 | Denial of Service | reconcile roll-up + snapshot query at span close | mitigate | Every new path preserves the best-effort/never-throw contract (D-06/D-08): a failure writes a `[...] (non-fatal)` stderr line and returns a safe default (empty aggregate / empty snapshot / 0 unmatched); a reconciliation failure never blocks the measurement-stop close |
| T-83-08-03 | Elevation/Attribution tamper | task_id backfill on wire rows | accept (guarded) | The CASE-guard only stamps task_id when it is currently '' (never overwrites an existing binding) and only on rows already matched by tool_call_id to a span transcript row — span-scoped, not ambient (honors D-08); no cross-span leakage path introduced |
| T-83-08-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan; no dependency changes |
</threat_model>

<verification>
Full regression + gap-close proof (all host-side, no proxy/docker rebuild):

- `node --test tests/token-adapters/reconcile-matcher.test.js` — 13 existing + aggregate roll-up case pass
- `node --test tests/token-adapters/reconcile-mode.test.js` — 8 existing + orphan-wire + task_id-stamp cases pass (fixtures now production-shape 'cladpt')
- `node --test tests/token-adapters/token-db-dedup-merge.test.js` — 4 regression pass
- `node --test tests/vkb-server/reconciliation-route.test.js` — 5 regression pass
- CR-01: `scripts/measurement-stop.mjs` calls `aggregatePerRequestDeltas`; string `typeof val === 'number'` absent
- CR-02: `stop-adapter-registry.mjs` has no `user_hash != ` unmatched-wire exclusion; uses `matchedWireRowIds`
- CR-03: `token-db.mjs` RECONCILE_GAP_FILL_SQL contains `task_id = CASE WHEN task_id`; launcher comment states D-08 no-inherit
</verification>

<success_criteria>
- The three FAILED truths in 83-VERIFICATION.md flip to VERIFIED on re-verification:
  1. aggregateDeltas is the per-field SUM of perRequest deltas (not `{}` when deltas exist)
  2. unmatched_wire counts a genuine cladpt orphan wire row (meaningful, not vacuous 0)
  3. interactive-launch spans (task_id='' wire rows) recover foreground attribution via reconcile-time task_id stamping
- All four existing test suites stay green (reconcile-matcher 13, reconcile-mode ≥8, token-db-dedup-merge 4, reconciliation-route 5)
- No changes to the rapid-llm-proxy repo; no docker/proxy rebuild required
- No ambient inheritance reintroduced (D-08 preserved: task_id stamp is span-scoped, applied only to already-matched transcript rows)
</success_criteria>

<output>
Create `.planning/phases/83-token-reconciliation-layer/83-08-SUMMARY.md` when done.
</output>
