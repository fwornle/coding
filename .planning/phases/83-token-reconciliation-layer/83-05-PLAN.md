---
phase: 83-token-reconciliation-layer
plan: 05
type: execute
wave: 3
depends_on: [83-04]
files_modified:
  - scripts/measurement-stop.mjs
  - lib/vkb-server/api-routes.js
  - tests/vkb-server/reconciliation-route.test.js
autonomous: true
requirements: [D-01, D-06, D-12, D-13]
must_haves:
  truths:
    - "measurement-stop invokes captureForegroundTokens with reconcile:true and writes reconciliation.json for the span"
    - "reconciliation.json contains a top-level summary plus a per-request array (self-contained for debugging)"
    - "A flagged discrepancy never fails or invalidates the run (advisory only)"
    - "GET /api/experiments/runs/:taskId/reconciliation serves the file verbatim, 400 on invalid taskId, graceful-empty on ENOENT"
  artifacts:
    - path: scripts/measurement-stop.mjs
      provides: "reconcile invocation + reconciliation.json sink under .data/measurements/<span>/"
      contains: "reconciliation"
    - path: lib/vkb-server/api-routes.js
      provides: "handleReconciliation read-only GET route"
      contains: "handleReconciliation"
    - path: tests/vkb-server/reconciliation-route.test.js
      provides: "route: valid/invalid taskId + ENOENT + verbatim pass-through"
      contains: "reconciliation"
  key_links:
    - from: "measurement-stop capture call"
      to: "captureForegroundTokens({ reconcile: true })"
      via: "measured-span path only (D-01)"
      pattern: "reconcile"
    - from: "handleReconciliation"
      to: ".data/measurements/<sanitized taskId>/reconciliation.json"
      via: "_validTaskId gate + fs.readFile verbatim"
      pattern: "handleReconciliation|_validTaskId"
---

<objective>
Close the reconcile loop at span-close and expose it read-only:
- **D-01 / D-12:** `measurement-stop` invokes the adapters with `reconcile: true` (measured-span path only), receives the reconciliation report, and writes it verbatim to `.data/measurements/<span>/reconciliation.json` — a self-contained artifact: top-level span summary (matched / unmatched_wire / unmatched_transcript / fallback-inserted counts, per-field aggregate deltas, flagged count) PLUS a per-request array (match method, per-field deltas, flags).
- **D-06:** a flagged discrepancy never fails or invalidates the run — the sink write is best-effort and advisory; no run-taint marker.
- **D-13:** `GET /api/experiments/runs/:taskId/reconciliation` in `lib/vkb-server/api-routes.js` serves the file verbatim so Phase 86's badge needs zero backend work.

Purpose: The reconcile machinery from Plans 03/04 is inert until measurement-stop calls it and persists the result where the dashboard can read it.
Output: A reconcile-invoking span-close path, the per-span sink, and a read-only route + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-token-reconciliation-layer/83-CONTEXT.md
@.planning/phases/83-token-reconciliation-layer/83-PATTERNS.md
@scripts/measurement-stop.mjs
@lib/vkb-server/api-routes.js

<interfaces>
<!-- measurement-stop.mjs capture seam (~:402-417) — thread reconcile + capture the report: -->
  const foregroundAgent = span.agent ?? span.meta?.agent ?? 'claude';
  try {
    await captureForegroundTokens(span, { agent: foregroundAgent });   // add { reconcile: true }, capture return
  } catch (err) {
    process.stderr.write(`[measurement-stop] foreground capture failed (non-fatal): ${err.message}\n`);
  }
<!-- Per-span artifact-write pattern (~:479-507): sanitizeTaskId(span.task_id) dir name,
     fs.writeFileSync, whole block best-effort try/catch + stderr (never hard-blocks close). -->
<!-- sanitizeTaskId imported at measurement-stop.mjs:82. Sink dir: .data/measurements/<span>/ -->

<!-- api-routes.js — copy handleTimeline (~:564-591), register like ~:89: -->
  app.get('/api/experiments/runs/:taskId/timeline', (req,res)=>this.handleTimeline(req,res));
  // ADD: app.get('/api/experiments/runs/:taskId/reconciliation', (req,res)=>this.handleReconciliation(req,res));
  // _validTaskId (~:735-737): [A-Za-z0-9._-], ≤80. _dataDir() (~:729-732): container-safe root.
  // Reconciliation reads a FILE (like handleTimeline) — NEVER open the experiment LevelDB.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke reconcile at span close + write reconciliation.json (D-01/D-12/D-06)</name>
  <files>scripts/measurement-stop.mjs</files>
  <read_first>
    - scripts/measurement-stop.mjs (the capture-invocation seam ~:402-417, the fixture-archive/per-span write block ~:479-507 with its best-effort try/catch contract ~:505-507, the sanitizeTaskId import ~:82)
    - lib/lsl/token/stop-adapter-registry.mjs (captureForegroundTokens report shape returned when reconcile:true — from Plan 04)
    - .planning/phases/83-token-reconciliation-layer/83-CONTEXT.md (D-12 shape; D-06 advisory-only)
  </read_first>
  <action>
    At the capture seam (~:402-417), pass `{ agent: foregroundAgent, reconcile: true }` to `captureForegroundTokens` and capture its returned reconciliation report (Plan 04 returns a FLAT report object — `{ matched, unmatched_wire, unmatched_transcript, fallback, perRequest, flaggedCount }` — when reconcile is requested; it does NOT carry a `span` wrapper or per-field aggregate deltas). This is the measured-span path (D-01) — do NOT touch any interactive Stop/sweep caller. Then ASSEMBLE the D-12 shape from that flat report and write it to `.data/measurements/<sanitizeTaskId(span.task_id)>/reconciliation.json` (do NOT write Plan 04's flat return verbatim — 83-05 owns the span-summary wrapper): (a) add a top-level `span` metadata header (`{task_id, agent, ...}` sourced from the span); (b) compute `summary.aggregateDeltas` by summing `perRequest[].deltas` per field across all matched requests (Plan 04 emits per-request deltas only — the per-field aggregate roll-up is 83-05's to build); (c) carry `matched / unmatched_wire / unmatched_transcript / fallback / flaggedCount` from the flat report into `summary`; (d) pass `perRequest` through unchanged. Copy the per-span artifact-write mechanics from the fixture-archive block (~:479-507): resolve the dir under REPO_ROOT/.data/measurements, `fs.mkdir` recursive, `fs.writeFileSync(JSON.stringify(report, null, 2))`, ALL wrapped in a best-effort try/catch that writes a `[measurement-stop] reconciliation sink failed (non-fatal): <msg>` stderr line and NEVER hard-blocks the close (mirrors ~:505-507). The assembled JSON is the D-12 shape: `{ span: {task_id, agent, ...}, summary: {matched, unmatched_wire, unmatched_transcript, fallback, aggregateDeltas, flaggedCount}, perRequest: [...] }` (exact field names + a `schemaVersion` are Claude's Discretion). Per D-06: write the flagged count into the summary but do NOT set any run-taint/invalidation marker — a flagged discrepancy is advisory only.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node --check scripts/measurement-stop.mjs && grep -c "reconcile" scripts/measurement-stop.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/measurement-stop.mjs` exits 0.
    - The capture call passes `reconcile: true` and captures the return (grep: `captureForegroundTokens` invocation includes `reconcile`).
    - The sink writes to `.data/measurements/<sanitized task_id>/reconciliation.json` using `sanitizeTaskId(span.task_id)` (grep: the path build uses `sanitizeTaskId` and the `measurements` dir).
    - The written `summary.aggregateDeltas` is the per-field SUM of `perRequest[].deltas` (assembled in 83-05), not passed through from Plan 04's flat report (which emits per-request deltas only).
    - The written JSON has a top-level `span` header plus `summary` and `perRequest` (the D-12 wrapper 83-05 assembles around Plan 04's flat return).
    - The write block is inside a best-effort try/catch with a `[measurement-stop]` non-fatal stderr line (a sink failure cannot throw out of span close).
    - No run-taint/invalidation flag is set on a flagged discrepancy (grep confirms no new taint marker written to run artifacts).
  </acceptance_criteria>
  <done>Measured span close invokes reconcile and persists a self-contained reconciliation.json; failures are non-fatal; flagged discrepancies are advisory only.</done>
</task>

<task type="auto">
  <name>Task 2: GET .../reconciliation read-only route (D-13)</name>
  <files>lib/vkb-server/api-routes.js, tests/vkb-server/reconciliation-route.test.js</files>
  <read_first>
    - lib/vkb-server/api-routes.js (route registration ~:89, handleTimeline ~:564-591 — the exact template: validate taskId, read on-disk source, graceful-empty on ENOENT; _validTaskId ~:735-737; _dataDir ~:729-732; the two-store boundary note ~:84-86)
    - .planning/phases/83-token-reconciliation-layer/83-PATTERNS.md (§ api-routes.js pattern assignment)
  </read_first>
  <action>
    Add `handleReconciliation(req, res)` modeled EXACTLY on `handleTimeline` (~:564-591) and register `app.get('/api/experiments/runs/:taskId/reconciliation', (req,res)=>this.handleReconciliation(req,res))` next to the timeline route (~:89). The handler: (1) validate `req.params.taskId` via `_validTaskId` (~:735-737, `[A-Za-z0-9._-]`, ≤80) — on invalid, `res.status(400).json({ error: 'Invalid taskId', message: 'taskId is required' })`; (2) build the path `<_dataDir()>/measurements/<taskId>/reconciliation.json` (sanitized taskId keeps the read inside `.data/measurements/`); (3) `fs.readFile` and `res.status(200).json(<parsed>)`; (4) on ENOENT, return `res.status(200)` with an empty/absent shape (graceful — a run without reconciliation data is not an error, matching handleTimeline). Serve the file VERBATIM (parse-and-return; do NOT re-shape) so Phase 86's badge needs zero backend work. Do NOT open the experiment LevelDB — this reads a file only (two-store boundary ~:84-86). Write `tests/vkb-server/reconciliation-route.test.js`: valid taskId returns the file JSON; invalid/traversal taskId returns 400; ENOENT returns 200 empty; a `../` taskId is rejected by _validTaskId (no path escape).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node --test tests/vkb-server/reconciliation-route.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/vkb-server/reconciliation-route.test.js` exits 0.
    - api-routes.js registers `/api/experiments/runs/:taskId/reconciliation` and defines `handleReconciliation` (grep both present).
    - Invalid taskId (empty, or containing `../` / `/`) yields HTTP 400 (test asserts a `../etc` taskId is rejected — no path traversal).
    - ENOENT yields HTTP 200 with an empty/absent shape, not 500.
    - A present reconciliation.json is returned verbatim (parsed JSON equals the on-disk content).
    - The handler never opens the experiment LevelDB (grep: no store-open call in handleReconciliation).
  </acceptance_criteria>
  <done>The read-only reconciliation route serves the per-span file verbatim with taskId validation and graceful-empty semantics — ready for Phase 86's badge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → GET .../:taskId/reconciliation | Untrusted `taskId` path parameter becomes a filesystem path component |
| span close → .data/measurements sink | task_id becomes a directory name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-05-01 | Information Disclosure | path traversal via `taskId` (`../`) in the read route | mitigate | `_validTaskId` (`[A-Za-z0-9._-]`, ≤80) rejects `/` and `..` before path build; read stays inside `.data/measurements/`; unit-asserted with a `../etc` case |
| T-83-05-02 | Tampering | sink task_id escaping the measurements dir | mitigate | `sanitizeTaskId(span.task_id)` sanitizes the dir name at write time (same validator family) |
| T-83-05-03 | Denial of Service | sink write or read throwing | mitigate | Sink write best-effort try/catch (never blocks close); read ENOENT → 200 empty, never 500 |
| T-83-05-04 | Elevation of Privilege | run invalidated by advisory flag | accept | D-06: flagged discrepancy is advisory only; no run-taint marker written — by design |
| T-83-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — edits to existing files + new test |
</threat_model>

<verification>
- reconciliation-route suite exits 0 (incl. the traversal-rejection case).
- `node --check scripts/measurement-stop.mjs` exits 0.
- No interactive Stop/sweep caller was modified (grep: the only `reconcile: true` invocation is in measurement-stop's measured-span path).
</verification>

<success_criteria>
- measurement-stop writes a self-contained reconciliation.json for measured spans (D-01/D-12).
- Flagged discrepancies are advisory only — no run invalidation (D-06).
- GET .../reconciliation serves the file verbatim with taskId validation + graceful-empty (D-13).
</success_criteria>

<output>
Create `.planning/phases/83-token-reconciliation-layer/83-05-SUMMARY.md` when done.
</output>
