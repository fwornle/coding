---
phase: 83-token-reconciliation-layer
plan: 07
type: execute
wave: 4
depends_on: [83-02, 83-05, 83-06]
files_modified:
  - config/experiments/wire-verify-83-reconcile.yaml
autonomous: false
requirements: [D-01, D-02, D-04, D-05, D-12]
must_haves:
  truths:
    - "A routed claude cell's reconciled totals equal the pre-change transcript-only totals (no double-count, no loss)"
    - "A proxy-down cell falls back to full transcript capture, tagged as fallback in reconciliation.json"
    - "A healthy span shows unmatched_wire=0"
    - "The reconciliation.json summary + per-request array is well-formed and readable via the GET route"
  artifacts:
    - path: config/experiments/wire-verify-83-reconcile.yaml
      provides: "golden-comparison experiment spec (claude routed + proxy-down cell)"
      contains: "experiment_id"
  key_links:
    - from: "measured cell close"
      to: ".data/measurements/<span>/reconciliation.json"
      via: "reconcile invocation (Plan 05)"
      pattern: "reconciliation"
    - from: "GET /api/experiments/runs/:taskId/reconciliation"
      to: "the per-span file"
      via: "verbatim read (Plan 05)"
      pattern: "reconciliation"
---

<objective>
Prove the reconciliation layer end-to-end with the golden comparison (dossier, verbatim), Phase-82 house style (live-gate + human checkpoint):
1. **Routed claude cell totals == pre-change transcript-only totals** — reconcile neither double-counts nor drops tokens relative to the old transcript-only path.
2. **Proxy-down cell → full transcript fallback** — with the proxy stopped, the cell's tokens are captured entirely via transcript fallback, tagged as fallback in reconciliation.json.
3. **Healthy span shows unmatched_wire=0** — every wire row in a healthy measured span finds its transcript match.

Purpose: This is the phase's acceptance gate. It exercises the full stack: proxy hardening (Plan 02), the matcher (Plan 03), reconcile mode (Plan 04), the sink + read API (Plan 05), and copilot gating (Plan 06).
Output: A golden-comparison experiment spec + a human-verified acceptance run with the three properties confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-token-reconciliation-layer/83-CONTEXT.md
@.planning/phases/83-token-reconciliation-layer/83-PATTERNS.md
@config/experiments/wire-verify-82-06-v2.yaml

<interfaces>
<!-- Model the spec on wire-verify-82-06-v2.yaml (2-cell, ATTENDED-by-design reproducer). -->
<!-- Golden comparison properties (dossier): -->
<!--   (1) routed claude cell totals == pre-change transcript-only totals -->
<!--   (2) proxy-down cell → full transcript fallback -->
<!--   (3) healthy span shows unmatched_wire=0 -->
<!-- Calibration reference: 82-06 v2 matched claude cache_read wire-vs-cladpt within 47946–72264. -->
<!-- Run standalone/UNATTENDED per the compare-fizzbuzz caveat (one global active-measurement.json span slot). -->
<!-- Sink: .data/measurements/<span>/reconciliation.json ; read via GET /api/experiments/runs/:taskId/reconciliation -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the golden-comparison experiment spec</name>
  <files>config/experiments/wire-verify-83-reconcile.yaml</files>
  <read_first>
    - config/experiments/wire-verify-82-06-v2.yaml (2-cell structure, goal_sentence, variants, snapshot_id, test_command — the template)
    - config/experiments/compare-fizzbuzz.yaml (header notes on UNATTENDED runs + one global span slot)
    - .planning/phases/83-token-reconciliation-layer/83-CONTEXT.md (§ specifics — golden comparison exact wording)
  </read_first>
  <action>
    Create `config/experiments/wire-verify-83-reconcile.yaml` modeled on `wire-verify-82-06-v2.yaml` with a fresh `experiment_id` (e.g. `wire-verify-83-reconcile`) so no prior resume state is touched. Use the same non-trivial fizzbuzz `goal_sentence` + `test_command` (two file writes → ≥2 consequential events, so the run is scored, not quarantined). Define TWO claude cells to exercise the golden comparison: (a) a HEALTHY routed claude cell (env: default, proxy up) for properties (1) and (3); (b) a PROXY-DOWN claude cell for property (2) — document in the spec header that the operator stops the proxy (`launchctl stop com.coding.llm-cli-proxy`) for this cell so all its tokens land via transcript fallback, then restarts it. Add a header comment block stating the three golden-comparison properties, the UNATTENDED run instruction (`node scripts/experiment-run.mjs --spec config/experiments/wire-verify-83-reconcile.yaml`), and the calibration reference (47946–72264 matched-pair cache_read range from 82-06). `repeats: 1`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node -e "const y=require('fs').readFileSync('config/experiments/wire-verify-83-reconcile.yaml','utf8'); if(!/experiment_id/.test(y)||!/goal_sentence/.test(y)||!/variants:/.test(y)) throw new Error('spec malformed'); console.log('spec ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `config/experiments/wire-verify-83-reconcile.yaml` exists with `experiment_id`, `goal_sentence`, `test_command`, and `variants:` (grep all present).
    - The spec header documents the three golden-comparison properties + the proxy-down operator step + the UNATTENDED run instruction.
    - The experiment_id is fresh (not reused from any wire-verify-82-* / compare-fizzbuzz id).
  </acceptance_criteria>
  <done>A runnable golden-comparison spec exists that exercises healthy-routed and proxy-down claude cells.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live golden-comparison acceptance run (human checkpoint)</name>
  <files>config/experiments/wire-verify-83-reconcile.yaml, .data/measurements/</files>
  <action>
    OPERATOR-DRIVEN verification (no code change). The executor prepares the run and presents the results; the operator confirms the three golden-comparison properties. Steps are enumerated in <how-to-verify>. This is a blocking checkpoint — the phase is not complete until the operator approves. Run UNATTENDED: no concurrent main-session LLM traffic in this repo while a cell is open (one global active-measurement.json span slot).
  </action>
  <what-built>
    The full reconciliation stack: proxy task-id hardening + OpenAI cache parse + dup-id constraint (Plans 01/02), the cross-user_hash request-id matcher with fuzzy fallback and tolerance flagging (Plan 03), reconcile mode in captureForegroundTokens with provenance-tagged fallback (Plan 04), the reconciliation.json sink + read API (Plan 05), and copilot BYOK gating (Plan 06). This checkpoint runs the golden comparison to confirm zero double-count, correct proxy-down fallback, and unmatched_wire=0 on a healthy span.
  </what-built>
  <how-to-verify>
    Run UNATTENDED (no concurrent main-session LLM traffic in this repo — one global span slot):

    1. Confirm the proxy is deployed + healthy (Plan 02 done): `launchctl list | grep com.coding.llm-cli-proxy` shows a live PID, and coordinator :3034 reports `location=open`.
    2. Kick off the healthy portion: `node scripts/experiment-run.mjs --spec config/experiments/wire-verify-83-reconcile.yaml` and let the healthy claude cell complete.
    3. PROPERTY 1 (no double-count / no loss): compare the routed claude cell's reconciled token totals against the pre-change transcript-only totals for the same goal (the 82-06 baseline). Reconciled totals must EQUAL the transcript-only totals — not roughly-double (double-count) and not near-zero (loss). claude cache_read on matched pairs should sit in the calibrated 47946–72264 range.
    4. PROPERTY 3 (unmatched_wire=0): open the healthy cell's sink — `cat .data/measurements/<span>/reconciliation.json` OR `curl -s localhost:12436/api/experiments/runs/<taskId>/reconciliation` (vkb-server) — and confirm `summary.unmatched_wire === 0` and the per-request array is well-formed (each entry has method, deltas, flags).
    5. PROPERTY 2 (proxy-down fallback): stop the proxy (`launchctl stop com.coding.llm-cli-proxy`), run the proxy-down cell, confirm all its tokens are captured via transcript fallback (reconciliation.json shows `summary.fallback > 0` and the rows carry the fallback provenance marker), then restart the proxy (`launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy` after confirming :3034 location=open).
    6. Confirm no interactive/daemon rows were mis-stamped with the cell task_id (Plan 01 D-08 ambient-leak fix): the cell task_ids in token_usage contain only genuine cell traffic.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms: (1) reconciled claude totals == transcript-only totals; (2) proxy-down cell fully fell back with fallback provenance; (3) healthy span unmatched_wire=0 and the sink is readable via the GET route; (4) no daemon/interactive contamination of cell task_ids.</human-check>
  </verify>
  <resume-signal>Type "approved" if all three golden-comparison properties hold, or describe the discrepancy (e.g. double-count factor, unmatched_wire count, fallback miss).</resume-signal>
  <done>Operator has approved: all three golden-comparison properties confirmed on a live run, sink readable via the GET route, no daemon/interactive contamination of cell task_ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| experiment cell agents → proxy + token_usage | measured cells generate the wire/transcript rows under test |
| operator proxy stop/start → measurement | proxy-down window is deliberately induced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-07-01 | Repudiation | double-count masking a matcher bug | mitigate | Property 1 golden comparison against the transcript-only baseline surfaces any double-count/loss numerically |
| T-83-07-02 | Denial of Service | proxy-down window losing tokens silently | mitigate | Property 2 asserts full transcript fallback with provenance — a fallback miss shows as a low fallback count, not silent loss |
| T-83-07-03 | Spoofing | daemon/interactive rows contaminating cell task_ids | mitigate | Property 6 confirms the Plan-01 D-08 ambient-leak fix under live concurrency |
| T-83-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — a YAML spec + a live run only |
</threat_model>

<verification>
- Spec parses and has the required fields.
- Human checkpoint confirms all three golden-comparison properties.
- reconciliation.json is readable via both the file and the GET route.
</verification>

<success_criteria>
- Routed claude reconciled totals == pre-change transcript-only totals (no double-count, no loss).
- Proxy-down cell fully falls back with fallback provenance.
- Healthy span shows unmatched_wire=0; sink readable via the Phase-83 read API.
</success_criteria>

<output>
Create `.planning/phases/83-token-reconciliation-layer/83-07-SUMMARY.md` when done.
</output>
