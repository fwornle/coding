---
phase: 83-token-reconciliation-layer
plan: 02
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/openai-cache-parse.test.mjs
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-dupid-constraint.test.mjs
autonomous: true
requirements: [D-09, D-11]
must_haves:
  truths:
    - "Copilot/opencode wire rows carry real cache_read_tokens parsed from OpenAI prompt_tokens_details"
    - "Concurrent tap+adapter writes to the same user_hash can no longer collide on id (or a collision no longer throws)"
    - "The deployed proxy daemon is alive after build+kickstart and writes a sentinel row"
  artifacts:
    - path: /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts
      provides: "parseOpenAICache pure helper (mirrors parseUsageCache contract)"
      contains: "cached_tokens"
    - path: /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts
      provides: "duplicate-id constraint / coordinated id allocation"
      contains: "token_usage"
  key_links:
    - from: "completeCopilot usage parse (server.mjs ~:796-800, ~:905-907, ~:1084-1086)"
      to: "cache_read_tokens on the wire row"
      via: "parseOpenAICache(usage)"
      pattern: "parseOpenAICache|cached_tokens"
    - from: "src/token-usage.ts migration"
      to: "coding-side ensureCacheColumns idempotency"
      via: "PRAGMA-guarded additive, mutually idempotent"
      pattern: "PRAGMA table_info"
---

<objective>
Complete the proxy-side reconciliation prerequisites and deploy all Phase-83 proxy edits (this plan + Plan 01) with a single build+kickstart:
- **D-09 (IN-05):** parse OpenAI-wire cache fields (`prompt_tokens_details.cached_tokens`) on the shim/copilot completion path so copilot/opencode wire rows carry real cache splits instead of dropping cache. The copadt session-state merge (Plan 04) then covers only what the wire genuinely lacks.
- **D-11 (duplicate-id fix):** add the missing PK/unique constraint (or coordinated id allocation) in `token-usage.ts` so concurrent tap-writer + adapter-writer INSERTs to the same `user_hash` cannot collide/throw. The matcher makes row identity load-bearing.

Purpose: The reconcile matcher (Plan 03) joins on `tool_call_id` and reads per-field cache/reasoning deltas — it needs wire rows that carry cache and a `token_usage` table where `id` is genuinely unique.
Output: Rebuilt `dist/` + a live-verified proxy daemon carrying both Plan 01 and Plan 02 edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-token-reconciliation-layer/83-CONTEXT.md
@.planning/phases/83-token-reconciliation-layer/83-PATTERNS.md
@.planning/phases/82-wire-measurement-foundation/82-REVIEW.md

<interfaces>
<!-- src/usage-cache.ts already exports parseUsageCache for the Anthropic tap (pure, never-throws, coalesces to 0). -->
<!-- Model the new OpenAI helper on it. -->

Existing shim usage parse (server.mjs ~:796-800, and sibling sites ~:905-907, ~:1084-1086):
  tokens: {
    input: usage.prompt_tokens || 0,
    output: usage.completion_tokens || 0,
    total: usage.total_tokens || 0,
  }
  // cache DROPPED — OpenAI carries it in usage.prompt_tokens_details.cached_tokens

token-usage.ts current PK is (user_hash, id) (~:423). Both the tap writer
(server.mjs ~:2154 logTokenCall) and adapter writers allocate id via per-user_hash
MAX(id)+1 from SEPARATE connections → concurrent same-user_hash writes can collide.
Migration idioms to copy: composite-PK rebuild (~:458-507) and PRAGMA-guarded
additive ALTER blocks (~:520-623). Coding-side counterpart: token-db.mjs
ensureCacheColumns (~:70-78) — the two migrations MUST stay mutually idempotent
(byte-identical column names/types).

CLAUDE.md deploy loop: edit → `npm run build` in rapid-llm-proxy →
`launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`.
Memory reference_llm_proxy_override_fallback: confirm coordinator :3034 location=open
BEFORE kickstart.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse OpenAI cache fields on the shim path (D-09 / IN-05)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts, /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs, /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/openai-cache-parse.test.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts (parseUsageCache — the pure/never-throw/coalesce-to-0 contract to mirror)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (the completeCopilot return ~:796-800 and sibling parse sites ~:905-907, ~:1084-1086)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/messages-tap-cache-parse.test.mjs (test structure + LLM_PROXY_LIVE env-gate convention — NOT argv, per memory reference_node_test_argv_live_gate)
  </read_first>
  <behavior>
    - Test 1: parseOpenAICache({ prompt_tokens: 100, completion_tokens: 20, prompt_tokens_details: { cached_tokens: 40 } }) returns cache_read_tokens=40 (and cache_write_tokens=0 — OpenAI has no cache-write field).
    - Test 2: parseOpenAICache with no prompt_tokens_details returns cache_read_tokens=0 (coalesce, never undefined/NaN).
    - Test 3: parseOpenAICache(null) and parseOpenAICache({}) return zeros without throwing.
    - Test 4: a non-numeric cached_tokens coalesces to 0.
  </behavior>
  <action>
    Add `parseOpenAICache(usage)` to `src/usage-cache.ts` as a pure helper next to `parseUsageCache`, following that function's contract exactly: never throws, coalesces every field to 0, no side effects. It maps `usage.prompt_tokens_details.cached_tokens` → `cache_read_tokens` and sets `cache_write_tokens = 0` (OpenAI exposes no cache-write). Then wire it into the three shim/copilot completion parse sites in `server.mjs` (~:796-800, ~:905-907, ~:1084-1086) so the emitted `tokens`/row object carries `cache_read_tokens` alongside input/output/total — copilot/opencode wire rows must no longer drop cache. Write the unit test at `tests/integration/openai-cache-parse.test.mjs` covering the four behaviors above against the compiled `dist/` helper, gated on the `LLM_PROXY_LIVE` convention only where a live daemon is required (the pure-helper tests need no live gate).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build && node --test tests/integration/openai-cache-parse.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/openai-cache-parse.test.mjs` exits 0 with 4 passing cases.
    - `src/usage-cache.ts` exports `parseOpenAICache` and references `cached_tokens`.
    - All three server.mjs shim parse sites include `cache_read_tokens` in the emitted row/tokens object (grep: `cache_read_tokens` count on server.mjs increases relative to pre-edit).
    - The helper never throws on null/{}/non-numeric input (Test 3/4 pass).
  </acceptance_criteria>
  <done>Copilot/opencode wire rows carry a real cache_read split derived from OpenAI's prompt_tokens_details.cached_tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the duplicate-id constraint / coordinated allocation (D-11)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts, /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-dupid-constraint.test.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts (composite-PK rebuild ~:458-507, PRAGMA-guarded additive ALTER blocks ~:520-623, insertStmt ~:630-638, hydrate INSERT OR IGNORE ~:710-717)
    - lib/lsl/token/token-db.mjs (ensureCacheColumns ~:70-78 — the coding-side migration this must stay mutually idempotent with)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-cache-migration.test.mjs and token-usage-schema-migration.test.mjs (idempotency test structure to copy)
  </read_first>
  <behavior>
    - Test 1: running the migration twice on the same DB is a no-op the second time (idempotent) — no throw, same schema.
    - Test 2: after migration, two INSERTs that would previously collide on (user_hash, id) for the same user_hash both survive (either via a coordinated id or via a unique-key strategy that does not silently drop a distinct row).
    - Test 3: a genuine duplicate (same tool_call_id, same user_hash) is still deduped/ignored — the fix must not double-count real duplicates.
  </behavior>
  <action>
    Fix the duplicate-`id` collision between the tap writer (server.mjs ~:2154) and the adapter writers, which each allocate `id` via per-user_hash `MAX(id)+1` from separate connections. Choose the mechanics (D-11 is Claude's Discretion): either (a) add a surrogate autoincrement rowid / widen the PK to include `tool_call_id` so distinct rows never collide, or (b) coordinate id allocation. Whatever is chosen MUST be an additive, PRAGMA-guarded, idempotent, non-fatal migration modeled on the existing blocks (~:520-623 / ~:458-507) — wrapped in try/catch that logs a `[token-usage]` stderr line and never aborts startup. It MUST remain mutually idempotent with the coding-side `ensureCacheColumns` (token-db.mjs ~:70-78): column names/types stay byte-identical, "whichever migration runs first wins" contract preserved. If existing rows already contain colliding ids, include a best-effort in-place repair. Do NOT remove the existing `(user_hash, tool_call_id)` dedup semantics — a genuine duplicate must still be dropped, only distinct rows must stop colliding. Write the idempotency + no-collision + still-dedups test at `tests/integration/token-usage-dupid-constraint.test.mjs`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build && node --test tests/integration/token-usage-dupid-constraint.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/token-usage-dupid-constraint.test.mjs` exits 0 (3 behaviors pass, including double-run idempotency).
    - The migration is PRAGMA-guarded and wrapped in try/catch (grep: `PRAGMA table_info` present; the new block sits inside a try/catch with a `[token-usage]` stderr line).
    - Two distinct concurrent-shaped rows for the same user_hash both persist; a genuine same-tool_call_id duplicate is still deduped.
    - Column names/types added here match token-db.mjs ensureCacheColumns exactly (no divergence that would break mutual idempotency).
  </acceptance_criteria>
  <done>Concurrent tap+adapter writes to the same user_hash no longer collide on id; the migration is idempotent and mutually compatible with the coding-side schema.</done>
</task>

<task type="auto">
  <name>Task 3: Build + deploy the proxy (activates Plan 01 + Plan 02 edits) + smoke</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - CLAUDE.md (§ km-core LLM proxy endpoint — port 12435 contract; § launchd daemons — kickstart recipe)
    - .planning/phases/83-token-reconciliation-layer/83-CONTEXT.md (canonical_refs — coordinator :3034 location=open BEFORE kickstart)
  </read_first>
  <action>
    Deploy the combined proxy edits from Plan 01 (server.mjs task-id hardening) and Plan 02 (usage-cache + token-usage + server.mjs shim). This is the single build+deploy for all Phase-83 proxy changes. Steps: (1) `cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build` (compiles src/usage-cache.ts + src/token-usage.ts → dist/); (2) confirm the coordinator reports `location=open` on :3034 BEFORE restarting (per memory reference_llm_proxy_override_fallback — a corporate mis-detect during restart mis-routes providers); (3) `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`; (4) smoke: confirm the daemon is alive on port 12435 (`POST /api/complete` with a trivial `{process, messages}` body returns a `{content, provider, model}` shape, NOT `Cannot POST` HTML — never use port 3033), and confirm a fresh token_usage row is written with the new schema (cache columns present, no id-collision throw in the daemon log).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build && node --check proxy-bridge/server.mjs && launchctl list | grep com.coding.llm-cli-proxy</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `dist/usage-cache.js` and `dist/token-usage.js` reflect the new code.
    - `launchctl list | grep com.coding.llm-cli-proxy` shows the daemon registered with a live PID (not `-`) after kickstart.
    - A smoke `POST http://localhost:12435/api/complete` returns a JSON `{content, provider, model}` shape (not `Cannot POST /api/complete` HTML).
    - The daemon log shows no unhandled exception from the id-allocation path after a token-writing request.
    - Coordinator :3034 confirmed `location=open` before the kickstart (recorded in SUMMARY).
  </acceptance_criteria>
  <done>The proxy daemon runs the combined Phase-83 edits: guarded/unified task-id binding, OpenAI cache parse, and the dup-id constraint — verified live on port 12435.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream provider → shim usage parse | Untrusted `usage` JSON (possibly missing/malformed cache fields) |
| concurrent writers → SQLite token_usage | Two independent connections allocate id for the same user_hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-02-01 | Denial of Service | parseOpenAICache on malformed usage | mitigate | Pure/never-throw/coalesce-to-0 contract mirrored from parseUsageCache; null/{}/non-numeric all return zeros |
| T-83-02-02 | Tampering | id collision silently dropping a distinct row | mitigate | D-11 constraint keeps distinct rows; genuine dedup preserved; migration is non-fatal + idempotent |
| T-83-02-03 | Denial of Service | migration throwing on startup | mitigate | PRAGMA-guarded, try/catch → stderr, never aborts daemon start |
| T-83-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — edits to existing proxy source only; `npm run build` uses the pinned toolchain |
</threat_model>

<verification>
- Both unit tests exit 0.
- The deployed daemon is alive on port 12435 and serves `/api/complete`.
- Combined server.mjs (Plan 01 + Plan 02) passes `node --check`.
</verification>

<success_criteria>
- Copilot/opencode wire rows carry real cache splits (D-09).
- token_usage id collisions between tap and adapter writers are eliminated, migration idempotent (D-11).
- All Phase-83 proxy edits are live on port 12435.
</success_criteria>

<output>
Create `.planning/phases/83-token-reconciliation-layer/83-02-SUMMARY.md` when done.
</output>
