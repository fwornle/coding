---
phase: 83-token-reconciliation-layer
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - config/agents/copilot.sh
  - scripts/launch-agent-common.sh
  - lib/experiments/experiment-runner.mjs
autonomous: true
requirements: [D-03]
must_haves:
  truths:
    - "Interactive copilot launch does NOT export COPILOT_PROVIDER_* (returns to copadt-only capture)"
    - "Measured copilot launches (experiment cells / active span) still receive the BYOK env"
    - "The unhealthy / no-span branch never leaves a stale COPILOT_PROVIDER_BASE_URL set"
  artifacts:
    - path: config/agents/copilot.sh
      provides: "BYOK exports removed/gated out of the unconditional agent_pre_launch"
      contains: "COPILOT_PROVIDER"
    - path: scripts/launch-agent-common.sh
      provides: "BYOK exports live only in the health-gated measured branch (or explicit unset)"
      contains: "COPILOT_PROVIDER"
  key_links:
    - from: "config/agents/copilot.sh agent_pre_launch"
      to: "no unconditional COPILOT_PROVIDER_* export"
      via: "gated to measured launch only"
      pattern: "COPILOT_PROVIDER"
    - from: "lib/experiments/experiment-runner.mjs configureProxyRoutingEnv"
      to: "per-cell COPILOT_PROVIDER_* env (unchanged)"
      via: "measured-by-definition cell wiring"
      pattern: "COPILOT_PROVIDER_BASE_URL"
---

<objective>
Gate the copilot BYOK env to measured launches only (D-03), which simultaneously kills the WR-02 interactive double-writer and fixes WR-05's dead-URL fail-soft break:
- **D-03:** move the `COPILOT_PROVIDER_*` exports OUT of the unconditional `config/agents/copilot.sh agent_pre_launch()` path and into measured-launch wiring only. Interactive copilot returns to copadt-only capture (no wire double-write). Measured launches (experiment cells via `experiment-runner.mjs`, and the health-gated `configure_proxy_routing` copilot branch when a span is active) keep the BYOK env.

Purpose: While every interactive copilot session exports BYOK, it both double-writes (proxy wire + copadt transcript) and — per WR-05 — breaks fail-soft when the proxy URL is dead. Reconcile mode (Plan 04) assumes interactive copilot is copadt-only; this plan makes that true.
Output: BYOK exports gated to measured launches; interactive copilot clean; cell-side wiring verified unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-token-reconciliation-layer/83-CONTEXT.md
@.planning/phases/83-token-reconciliation-layer/83-PATTERNS.md
@.planning/phases/82-wire-measurement-foundation/82-REVIEW.md
@config/agents/copilot.sh
@scripts/launch-agent-common.sh
@lib/experiments/experiment-runner.mjs

<interfaces>
<!-- config/agents/copilot.sh agent_pre_launch (~:64-82) — the UNCONDITIONAL exports to remove/gate: -->
  if [ -n "${TASK_ID:-}" ]; then
    export COPILOT_PROVIDER_BASE_URL="http://127.0.0.1:${_copilot_proxy_port}/v1/copilot/t/${TASK_ID}"
  else
    export COPILOT_PROVIDER_BASE_URL="http://127.0.0.1:${_copilot_proxy_port}/v1/copilot"
  fi
  export COPILOT_PROVIDER_TYPE="openai"
  export COPILOT_PROVIDER_API_KEY="rapid-proxy-no-auth-placeholder"

<!-- scripts/launch-agent-common.sh — the ALREADY-health-gated home: -->
  configure_proxy_routing() {  # opt-out (CODING_PROXY_ROUTE=0) + curl health gate preamble ~:388-404
    ...
    # copilot branch ~:441-459 — measured/health-gated; if [ -n "${TASK_ID:-}" ] structure to copy
  }

<!-- lib/experiments/experiment-runner.mjs configureProxyRoutingEnv copilot case (~:179-192) — KEEPS BYOK
     (cells are measured by definition); env built PER-CELL, passed to launchCell (~:246-250), never exported
     into an interactive shell. Health-gated ~:157-162. This is the reference for "measured BYOK done right". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove/gate the unconditional BYOK exports from copilot.sh (D-03, WR-02)</name>
  <files>config/agents/copilot.sh</files>
  <read_first>
    - config/agents/copilot.sh (agent_pre_launch ~:64-82 — the unconditional COPILOT_PROVIDER_* block; the `_copilot_proxy_port` resolution just above it)
    - .planning/phases/82-wire-measurement-foundation/82-REVIEW.md (WR-05 fix shape + WR-02 double-writer)
    - .planning/phases/83-token-reconciliation-layer/83-PATTERNS.md (§ launch-agent-common.sh + copilot.sh gating)
  </read_first>
  <action>
    Remove the unconditional `COPILOT_PROVIDER_BASE_URL` / `COPILOT_PROVIDER_TYPE` / `COPILOT_PROVIDER_API_KEY` exports from `agent_pre_launch()` (~:64-82) so an interactive copilot launch (no measured span) does NOT route through the proxy — it returns to copadt-only transcript capture, killing the WR-02 double-writer. Gate the exports on a measured-launch signal only: export them ONLY when a measured span is active (e.g. `TASK_ID` set AND the proxy health gate passed) OR delegate entirely to `launch-agent-common.sh configure_proxy_routing` (Task 2's home). Per WR-05, ensure the non-measured branch does NOT leave a stale `COPILOT_PROVIDER_BASE_URL` — if the value could have been inherited from the environment, explicitly `unset COPILOT_PROVIDER_BASE_URL COPILOT_PROVIDER_TYPE COPILOT_PROVIDER_API_KEY` on the interactive/unhealthy path so a dead proxy URL never breaks copilot's fail-soft. Preserve the `_copilot_proxy_port` resolution and all non-BYOK pre-launch behavior.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && bash -n config/agents/copilot.sh && grep -n "COPILOT_PROVIDER" config/agents/copilot.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n config/agents/copilot.sh` exits 0 (no shell syntax error).
    - `agent_pre_launch` no longer exports `COPILOT_PROVIDER_*` unconditionally — any remaining export is guarded by a measured-launch condition (grep: no `export COPILOT_PROVIDER_BASE_URL` outside a `TASK_ID`/health guard or delegation).
    - The interactive/unhealthy path explicitly unsets `COPILOT_PROVIDER_*` (or never sets it) so a stale/dead URL cannot break fail-soft (WR-05).
    - `_copilot_proxy_port` resolution and other pre-launch steps are unchanged.
  </acceptance_criteria>
  <done>Interactive copilot no longer exports BYOK — it captures via copadt only; a dead proxy URL cannot break its fail-soft.</done>
</task>

<task type="auto">
  <name>Task 2: Keep measured BYOK in the health-gated launcher + verify cell-side wiring unchanged (D-03)</name>
  <files>scripts/launch-agent-common.sh, lib/experiments/experiment-runner.mjs</files>
  <read_first>
    - scripts/launch-agent-common.sh (configure_proxy_routing opt-out + health-gate preamble ~:388-404; copilot branch ~:441-459)
    - lib/experiments/experiment-runner.mjs (configureProxyRoutingEnv copilot case ~:179-192; launchCell env pass ~:246-250; health gate ~:157-162)
    - .planning/phases/83-token-reconciliation-layer/83-CONTEXT.md (D-03 — gating may live in launch-agent-common.sh, experiment-runner.mjs, or both)
  </read_first>
  <action>
    Ensure the copilot BYOK exports live ONLY inside the health-gated `configure_proxy_routing` copilot branch (~:441-459) for measured spans — copy its existing `if [ -n "${TASK_ID:-}" ]` structure so the BYOK env is set only when routing is active and a task is measured, matching the CODING_PROXY_ROUTE=0 opt-out + curl health-gate preamble (~:388-404). This is the single measured home for launcher-driven copilot BYOK (copilot.sh delegated to it in Task 1). Then VERIFY (read-only, no change needed) that `lib/experiments/experiment-runner.mjs configureProxyRoutingEnv` copilot case (~:179-192) still builds the per-cell `COPILOT_PROVIDER_*` env and passes it to `launchCell` (~:246-250) — cells are measured by definition and their BYOK wiring STAYS. If a code change is required to keep the two homes consistent (e.g. the launcher branch was setting env the cell path duplicates), reconcile so a measured launch gets BYOK from exactly one path, never double-set. Do not alter the interactive behavior fixed in Task 1.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && bash -n scripts/launch-agent-common.sh && node --check lib/experiments/experiment-runner.mjs && grep -n "COPILOT_PROVIDER_BASE_URL" scripts/launch-agent-common.sh lib/experiments/experiment-runner.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/launch-agent-common.sh` exits 0 and `node --check lib/experiments/experiment-runner.mjs` exits 0.
    - The launcher's `COPILOT_PROVIDER_*` exports sit inside the health-gated `configure_proxy_routing` copilot branch behind a `TASK_ID`/health condition (grep: no unconditional launcher export).
    - experiment-runner.mjs `configureProxyRoutingEnv` copilot case still sets `COPILOT_PROVIDER_BASE_URL` per-cell (grep confirms it is present and unchanged in intent).
    - A measured copilot launch receives BYOK from exactly one home (launcher branch for launcher-driven; per-cell env for experiment cells) — no double-set.
  </acceptance_criteria>
  <done>Measured copilot launches keep BYOK via the health-gated launcher branch and per-cell experiment env; the two homes do not double-set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell env → copilot process | COPILOT_PROVIDER_* exports steer copilot's provider endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-06-01 | Tampering | interactive copilot routed through a stale/dead proxy URL | mitigate | WR-05: interactive/unhealthy path unsets COPILOT_PROVIDER_* so a dead URL never breaks fail-soft |
| T-83-06-02 | Information Disclosure | COPILOT_PROVIDER_API_KEY value | accept | Value is the literal `rapid-proxy-no-auth-placeholder` — not a real secret; local loopback (127.0.0.1) only |
| T-83-06-03 | Repudiation | interactive copilot double-writing wire+transcript | mitigate | BYOK gated off the interactive path → copadt-only capture (WR-02 discharged) |
| T-83-06-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — shell + JS env-wiring edits only |
</threat_model>

<verification>
- `bash -n` clean on both shell files; `node --check` clean on experiment-runner.mjs.
- Interactive copilot exports no BYOK; measured launches export it from exactly one home.
- experiment-runner.mjs cell-side BYOK verified unchanged.
</verification>

<success_criteria>
- Interactive copilot returns to copadt-only capture; no wire double-write (D-03 / WR-02).
- A dead proxy URL cannot break interactive copilot's fail-soft (WR-05).
- Measured copilot launches keep BYOK env (experiment cells + health-gated launcher) with no double-set (D-03).
</success_criteria>

<output>
Create `.planning/phases/83-token-reconciliation-layer/83-06-SUMMARY.md` when done.
</output>
