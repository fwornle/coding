---
phase: 83-token-reconciliation-layer
plan: 04
type: execute
wave: 2
depends_on: [83-03]
files_modified:
  - lib/lsl/token/stop-adapter-registry.mjs
  - tests/token-adapters/reconcile-mode.test.js
autonomous: true
requirements: [D-01, D-02, D-04]
must_haves:
  truths:
    - "captureForegroundTokens in reconcile mode matches transcript rows against wire rows instead of blindly inserting"
    - "A transcript row with no wire match inserts as fallback tagged with fallback provenance"
    - "A :reason:N row always inserts (wire never carries reasoning-step splits)"
    - "The interactive Stop/sweep path (reconcile not requested) keeps today's transcript+dedup-merge behavior unchanged"
    - "captureForegroundTokens returns a reconciliation report object (not just an insert count) when reconcile is requested"
  artifacts:
    - path: lib/lsl/token/stop-adapter-registry.mjs
      provides: "reconcile branch in captureForegroundTokens + copadt session-state cache merge"
      contains: "reconcile"
    - path: tests/token-adapters/reconcile-mode.test.js
      provides: "reconcile-vs-interactive dispatch + fallback provenance + report shape"
      contains: "reconcile"
  key_links:
    - from: "captureForegroundTokens reconcile branch"
      to: "reconcile.mjs reconcileRow"
      via: "per-row match-then-enrich-or-fallback"
      pattern: "reconcileRow"
    - from: "fallback-inserted rows"
      to: "distinct fallback provenance marker"
      via: "process/marker stamp"
      pattern: "fallback"
---

<objective>
Wire the Plan-03 matcher into `captureForegroundTokens` as a new reconcile path, applied to measured spans only (D-01):
- **D-01:** thread a `reconcile: true` opt (passed by measurement-stop, Plan 05) that switches claude/copilot onto match-then-enrich-or-fallback; the interactive Stop/sweep path (no `reconcile`) keeps today's `mode: 'transcript'` + `insertTokenRowDeduped` behavior untouched.
- **D-02:** any transcript row with no wire match (request-id, then fuzzy) inserts as fallback, tagged with distinct fallback provenance and counted; a `:reason:N` row always inserts (wire never carries reasoning-step splits). No health-infrastructure gating — a matching bug shows up as a high fallback count, never a silent double-count.
- **D-04:** matched rows enrich wire-empty gaps only (via the Plan-03 gap-fill); copilot's cache split merges from copadt session-state into wire rows only where the wire genuinely lacks it.

The function must now return a reconciliation report object (matched / unmatched_wire / unmatched_transcript / fallback / per-request deltas + flags) so Plan 05 can write the sink.
Purpose: This is the no-double-count contract in action — reconcile replaces the blind insert loop only on the measured path.
Output: A reconcile branch in `stop-adapter-registry.mjs` + a dispatch/fallback/report unit suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-token-reconciliation-layer/83-CONTEXT.md
@.planning/phases/83-token-reconciliation-layer/83-PATTERNS.md
@lib/lsl/token/stop-adapter-registry.mjs
@lib/lsl/token/reconcile.mjs

<interfaces>
<!-- stop-adapter-registry.mjs current surface: -->
  export const STOP_ADAPTERS = {
    claude:  { mode: 'transcript', userHash: 'cladpt', build(sp){...} },  // ~:86-96
    copilot: { mode: 'transcript', userHash: 'copadt', build(sp){...} },  // ~:97-102
    opencode:{ mode: 'stamp-only' }, mastra:{ mode: 'stamp-only' },       // ~:103-104
  };
  export const SUBAGENT_PROCESS = 'token-adapter-claude-subagent';         // ~:64
  function withinSpanWindow(row, span)                                     // ~:319-328
  export async function captureForegroundTokens(span = {}, opts = {})      // ~:330

<!-- Current insert loop (~:388-409) — reconcile REPLACES the blind insertTokenRowDeduped call: -->
  for (const row of batch.rows) {
    if (!withinSpanWindow(row, span)) continue;   // reuse verbatim
    const ok = insertTokenRowDeduped(db, { ...row, user_hash: adapter.userHash, task_id: taskId });
    if (ok) inserted += 1;
  }

<!-- From Plan 03 (reconcile.mjs): -->
  reconcileRow(db, transcriptRow, span, opts) -> { method, matched, enriched, fallback, deltas, flagged }
  matchWireRow / computeDeltas
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the reconcile branch to captureForegroundTokens + report object (D-01/D-02/D-04)</name>
  <files>lib/lsl/token/stop-adapter-registry.mjs, tests/token-adapters/reconcile-mode.test.js</files>
  <read_first>
    - lib/lsl/token/stop-adapter-registry.mjs (the dispatch guard ~:330-339, the insert loop ~:388-409, withinSpanWindow ~:319-328, SUBAGENT_PROCESS provenance convention ~:64, the no-double-count docblock ~:80-85 which is the contract to preserve)
    - lib/lsl/token/reconcile.mjs (reconcileRow signature + return shape from Plan 03)
    - tests/token-adapters/token-db-dedup-merge.test.js (temp-DB harness to copy for the reconcile-mode suite)
  </read_first>
  <behavior>
    - Test 1 (dispatch): captureForegroundTokens(span, { agent, reconcile: true }) routes claude/copilot through reconcileRow; without reconcile it uses the existing insertTokenRowDeduped loop (interactive path unchanged).
    - Test 2 (match no double-count): a transcript row whose tool_call_id matches an existing wire row does NOT insert a second row — it enriches in place; total row count is unchanged.
    - Test 3 (fallback): a transcript row with no wire match inserts exactly one fallback row tagged with a distinct fallback provenance marker, and increments unmatched_transcript + fallback counts.
    - Test 4 (:reason: always-insert): a :reason:N row inserts unconditionally regardless of wire state.
    - Test 5 (report): the reconcile call returns { matched, unmatched_wire, unmatched_transcript, fallback, perRequest:[{tool_call_id, method, deltas, flagged}], flaggedCount }.
    - Test 6 (stamp-only untouched): opencode/mastra still return 0 (guard preserved).
  </behavior>
  <action>
    Extend `captureForegroundTokens` so that when `opts.reconcile === true` AND the agent is claude or copilot, the per-row loop calls `reconcileRow(db, { ...row, user_hash: adapter.userHash, task_id: taskId }, span, opts)` INSTEAD of the blind `insertTokenRowDeduped`. Preserve the `withinSpanWindow` filter verbatim (a `:reason:N` row bypasses the window/match per D-02 and always inserts). For a `fallback` result, insert the transcript row via `insertTokenRow`/`insertTokenRowDeduped` but stamp a distinct fallback provenance marker on `process` (a `token-adapter-<agent>-fallback` style tag, mirroring the SUBAGENT_PROCESS convention ~:64) so canonical selection and the sink can distinguish proxy-down fallbacks. Accumulate a report object — `{ matched, unmatched_wire, unmatched_transcript, fallback, perRequest:[...], flaggedCount }` — and RETURN it from `captureForegroundTokens` when reconcile was requested (the interactive path keeps returning the insert count; do not break existing callers — return the count when `!opts.reconcile`). For copilot specifically, before finalizing, merge the copadt session-state cache split into any matched wire row that still has 0 cache (D-04 gap-fill via the Plan-03 primitive) — session-state read mechanics (time-window width, provenance shape) are Claude's Discretion; keep it best-effort/never-throw. The `mode: 'transcript'` dispatch and the stamp-only ZERO-work guard (~:330-339, ~:80-85 docblock) stay intact for the interactive path. Create `tests/token-adapters/reconcile-mode.test.js` covering the six behaviors with a temp DB pre-seeded with wire rows.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node --test tests/token-adapters/reconcile-mode.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/token-adapters/reconcile-mode.test.js` exits 0 with all six behaviors passing.
    - With `reconcile: true`, a matched transcript row adds ZERO net rows (Test 2) — the no-double-count invariant holds.
    - Fallback rows carry a distinct provenance marker (grep: a `fallback` process tag distinct from the normal `cladpt`/`copadt` process) and are counted (Test 3).
    - `:reason:N` rows always insert (Test 4).
    - Without `reconcile`, captureForegroundTokens behaves byte-identically to today (Test 1 asserts the interactive path still uses insertTokenRowDeduped and returns a count) — run `node --test tests/token-adapters/token-db-dedup-merge.test.js` to confirm zero regression in the interactive path.
    - The returned report has matched/unmatched_wire/unmatched_transcript/fallback/perRequest/flaggedCount fields (Test 5).
  </acceptance_criteria>
  <done>Reconcile mode replaces the blind insert with match-then-enrich-or-fallback on measured spans only; the interactive Stop/sweep path is unchanged; a structured report is returned for the sink.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript batch (adapter build) → token_usage DB | transcript rows drive match/enrich/fallback inserts on the measured path |
| copadt session-state file → wire row cache merge | session-state JSON supplies cache split |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-04-01 | Elevation of Privilege | reconcile path double-inserting a matched row | mitigate | Matched rows enrich in place (Plan-03 gap-fill); unit-asserted zero net rows on match (Test 2) |
| T-83-04-02 | Repudiation | fallback rows indistinguishable from wire rows | mitigate | Distinct fallback provenance marker on `process` so the sink counts + surfaces them (D-02) |
| T-83-04-03 | Denial of Service | reconcile throwing during span close | mitigate | Never-throw: per-row catch → count as unmatched + `[token-adapter]` stderr; a failure never blocks the close (D-06) |
| T-83-04-04 | Tampering | interactive path silently changed | mitigate | Reconcile gated on `opts.reconcile`; interactive path uses the unchanged insertTokenRowDeduped loop; regression suite asserts parity |
| T-83-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — edits to existing module + new test |
</threat_model>

<verification>
- reconcile-mode suite exits 0.
- Interactive dedup-merge suite (token-db-dedup-merge.test.js) still exits 0 — no regression to the non-measured path.
- reconcile.mjs matcher suite (Plan 03) still exits 0.
</verification>

<success_criteria>
- Reconcile mode applies to measured spans only; interactive path unchanged (D-01).
- Unmatched transcript rows insert as counted, provenance-tagged fallbacks; :reason: always inserts (D-02).
- Matched rows enrich wire-empty gaps only, including copilot session-state cache merge (D-04).
- captureForegroundTokens returns a reconciliation report for the sink.
</success_criteria>

<output>
Create `.planning/phases/83-token-reconciliation-layer/83-04-SUMMARY.md` when done.
</output>
