---
phase: 45-unified-web-viewer
plan: 06
type: execute
wave: 6
depends_on:
  - 45-02
  - 45-03
  - 45-04
  - 45-05
files_modified:
  - tests/e2e/unified-viewer/system-routing.spec.ts
  - tests/e2e/unified-viewer/entity-detail.spec.ts
  - tests/e2e/unified-viewer/expand-neighbors.spec.ts
  - tests/e2e/unified-viewer/filter-search.spec.ts
  - tests/e2e/unified-viewer/state-reset.spec.ts
  - tests/e2e/unified-viewer/markdown-viewer.spec.ts
  - tests/e2e/unified-viewer/rca-ingestion.spec.ts
  - tests/e2e/unified-viewer/error-states.spec.ts
  - tests/e2e/unified-viewer/webgl-context.spec.ts
  - playwright.config.ts
  - .planning/phases/45-unified-web-viewer/45-06-OPERATOR-PROBES.md
autonomous: false
requirements:
  - UI-01
user_setup:
  - service: bmw-corp-network
    why: "Wave 0 operator probes #1 + #2 (CORS + SSE reachability) require the operator's BMW corporate laptop on-network to verify okm.cc.bmwgroup.net. Probes #3 + #4 are local-only (lucide-react icon completeness + display-overlay query param)."
    env_vars: []
    dashboard_config:
      - task: "Operator runs the 4 Wave 0 probe commands recorded in 45-06-OPERATOR-PROBES.md and signs off Task 1 per probe outcome (RED → fallback per RESEARCH § Open Question #5; GREEN → continue)."
        location: "Plan 06 Task 1 operator checkpoint"

must_haves:
  truths:
    - "Four Wave 0 operator probes from `45-VALIDATION.md` are EXECUTED and outcomes recorded in `.planning/phases/45-unified-web-viewer/45-06-OPERATOR-PROBES.md`: (1) C-system CORS via curl from BMW laptop, (2) C-system SSE via curl from BMW laptop, (3) lucide-react icon completeness via node REPL, (4) display-overlay query-param strategy operator decision (gated ?withDisplay=true vs always-rich)."
    - "Playwright E2E suite at `tests/e2e/unified-viewer/` covers the 9 scenarios from 45-VALIDATION.md Per-Task Verification Map row-by-row: system-routing, entity-detail, expand-neighbors, filter-search (combines the 3 FilterRail rows), state-reset, markdown-viewer, rca-ingestion (mocked SSE if C unreachable), error-states (CORS + unreachable + ontology-fetch), webgl-context (20 system-switch loop per Pitfall 8 + T-45-02-01)."
    - "`playwright.config.ts` has a `unified-viewer` project that launches Vite (`cd integrations/unified-viewer && npm run dev`) before tests, points at `http://localhost:5173`, and uses `gsd-browser`-compatible chromium config per CLAUDE.md mandatory rule."
    - "20-cycle system switch (`/viewer/coding ↔ /viewer/okb` via React-Router programmatic navigation) does NOT leak WebGL contexts — browser console shows zero `Too many active WebGL contexts` warnings; canvas still renders after the 20th cycle (Pitfall 8 + T-45-02-01 closure)."
    - "Cross-system smoke: against live A (:12436) AND B (:3848) backends, the viewer renders ≥10 entities, side panel populates on click, search filters work, and the camelCase wire-shape lock holds (`digest.observationIds.length` accessible; `digest.observation_ids === undefined`)."
    - "Final cutover gate: operator confirms the viewer at /viewer/coding feels at least feature-equivalent for daily browsing (NOT yet retiring VKB or VOKB — that is Phase 45.1 per CONTEXT.md Deferred Ideas). VKB at memory-visualizer and VOKB at rapid-automations stay operational as fallback."
    - "All `npm run build` outputs are clean; no TS strict-mode errors; bundle size reported and compared against RESEARCH § Standard Stack A4 budget (~500KB gzipped target)."
  artifacts:
    - path: tests/e2e/unified-viewer/system-routing.spec.ts
      provides: "Playwright spec asserting /viewer/coding → 200, /viewer/okb → 200, /viewer/cap → 200 (or error banner if Tier 1 CORS fails), /viewer/foo → UnknownSystem page, / → redirect /viewer/coding."
      contains: "page.goto"
      min_lines: 50
    - path: tests/e2e/unified-viewer/webgl-context.spec.ts
      provides: "20-cycle /viewer/coding ↔ /viewer/okb switch loop; asserts canvas remains visible AND console has no WebGL-context-limit warnings."
      contains: "for (let i"
      min_lines: 30
    - path: tests/e2e/unified-viewer/error-states.spec.ts
      provides: "Stops obs-api (or uses a port that's not listening) → verifies ErrorUnreachableState banner renders with the exact UI-SPEC copy + interpolated baseUrl. Mocks a CORS response → ErrorCorsState. Mocks 500 on /api/v1/ontology/classes → ErrorOntologyFetchState amber notice."
      contains: "Cannot reach"
    - path: tests/e2e/unified-viewer/markdown-viewer.spec.ts
      provides: "Loads /viewer/okb, selects an entity with description, asserts: (1) headings have anchor IDs, (2) Mermaid block renders the placeholder div NOT a Mermaid SVG, (3) theme toggle swaps highlight.js stylesheet, (4) clicking a .md link triggers history push (back/forward IconButtons enable/disable correctly)."
      contains: "Mermaid"
    - path: tests/e2e/unified-viewer/rca-ingestion.spec.ts
      provides: "If Tier 1 (CAP reachable): triggers an Ingest on a small RaaS dir, asserts the 5 stage pills cycle, progress bar advances, success Card appears. If Tier 1 unreachable (Wave 0 probe 1+2 RED): mocks /api/okm/rca/* + SSE; verifies the same flow in mock mode."
      contains: "rcaIngest"
    - path: .planning/phases/45-unified-web-viewer/45-06-OPERATOR-PROBES.md
      provides: "Operator-filled probe outcome record — 4 probe results + decisions + fallback path activations."
      contains: "Probe 1"
      min_lines: 40
    - path: playwright.config.ts
      provides: "Adds the `unified-viewer` project entry that boots Vite + targets :5173 via baseURL."
      contains: "unified-viewer"
  key_links:
    - from: tests/e2e/unified-viewer/system-routing.spec.ts
      to: integrations/unified-viewer/src/routes/UnifiedViewer.tsx
      via: "Playwright navigates to /viewer/{coding,okb,cap,foo} and asserts the mounted component"
      pattern: "/viewer/"
    - from: tests/e2e/unified-viewer/webgl-context.spec.ts
      to: integrations/unified-viewer/src/graph/SigmaCanvas.tsx
      via: "Loop drives Router-level state changes; asserts canvas element is still present after 20 cycles"
      pattern: "canvas"
    - from: .planning/phases/45-unified-web-viewer/45-06-OPERATOR-PROBES.md
      to: .planning/phases/45-unified-web-viewer/45-05-SUMMARY.md
      via: "If probe 1 or 2 is RED, Plan 05's deliverables remain functional (UI error states cover the gap) — record the fallback Tier per RESEARCH"
      pattern: "Tier"

threat_model:
  - id: T-45-06-01
    threat: "E2E suite passes against a sufficiently-recent build that masks a real regression in production (e.g., a new browser version disables a CSS feature the viewer relies on)."
    category: Repudiation
    severity: low
    disposition: accept
    mitigation: "Playwright pins chromium version per `gsd-browser` convention; rerun the suite before each production deploy. Document in 45-06-SUMMARY: 'tested against chromium <X>'."
  - id: T-45-06-02
    threat: "RcaOpsPanel mocked-SSE path masks the real C-system bug — viewer ships, operator tries it for real, it doesn't work."
    category: Repudiation
    severity: medium
    disposition: mitigate
    mitigation: "Probe 1 + 2 in Wave 0 confirm real C reachability BEFORE Phase 45 close-out. If RED, the panel is verified in mock mode + the production failure is FLAGGED as Phase 45.1 work — NOT shipped silently broken."
  - id: T-45-06-03
    threat: "20-cycle WebGL test passes locally but the leak is browser-version-specific (Chrome canary vs production stable)."
    category: Denial of Service
    severity: low
    disposition: mitigate
    mitigation: "Run the webgl-context test in BOTH chromium AND firefox (Playwright supports both); if either fails, Phase 45 close-out gates on the leak fix."
  - id: T-45-06-04
    threat: "Cutover gate is approved while VKB or VOKB users have a workflow that the unified viewer doesn't support yet — silent regression for some operators."
    category: Repudiation
    severity: medium
    disposition: accept
    mitigation: "D-45-04's MVP-worthy decision rule is documented: 'does a VKB or VOKB user lose access to a task they regularly do?' Phase 45 ships read-only browse + filter + entity-detail + Markdown (B) + RCA-ops (C). Per CONTEXT.md, VKB + VOKB stay operational as fallback during MVP rollout; v2 (Phase 45.1) closes the parity union. Document the v2 deferred list in 45-06-SUMMARY (TeamSelector, MermaidDiagram, ConfirmDialog/UndoToast, cluster overlays)."
  - id: T-45-06-SC
    threat: "New e2e tests + Playwright project pull untrusted browser code."
    category: Tampering
    severity: low
    disposition: accept
    mitigation: "Playwright is a transitive dependency of the existing project — already vetted in `.planning/phases/44/*` and earlier. No new package install in this plan."
---

<objective>
Close out Phase 45 with the cross-system Playwright smoke + Wave 0 operator probes. After this plan, Phase 45's MVP slate is verified end-to-end against live backends (Coding + OKB) and against either live (Tier 1) or mocked (Tier 2/3 fallback) CAP backend. The operator decides whether to retire VKB + VOKB (NOT this phase — D-45-04 says Phase 45.1 owns retirement) or to keep them as fallback while the unified viewer accumulates trust.

Purpose: Discharge the SC#1 "single web viewer renders any KM-Core graph parameterized by ontology config; both VKB (B) and VOKB (C) users migrate to it without functional regression" via E2E evidence + the operator-facing cutover gate.

Output: A 9-spec Playwright suite, a probe-outcome record, and the final go/no-go signal for Phase 45 close-out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-unified-web-viewer/45-CONTEXT.md
@.planning/phases/45-unified-web-viewer/45-RESEARCH.md
@.planning/phases/45-unified-web-viewer/45-UI-SPEC.md
@.planning/phases/45-unified-web-viewer/45-VALIDATION.md
@.planning/phases/45-unified-web-viewer/45-PATTERNS.md
@.planning/phases/45-unified-web-viewer/45-02-SUMMARY.md
@.planning/phases/45-unified-web-viewer/45-03-SUMMARY.md
@.planning/phases/45-unified-web-viewer/45-04-SUMMARY.md
@.planning/phases/45-unified-web-viewer/45-05-SUMMARY.md
@CLAUDE.md
@playwright.config.ts

<interfaces>
<!-- Wave 0 operator probes (45-VALIDATION.md § Wave 0 — Operator Probes table verbatim): -->

| # | Probe | Command | Fallback if RED |
| 1 | C CORS | `curl -i -H "Origin: http://localhost:5173" https://okm.cc.bmwgroup.net/api/okm/health` (from BMW corporate laptop) | Tier 2 (backend proxy via A) or Tier 3 (via B) per RESEARCH § Open Question #5 — adds ~2 days Phase 45.1 work |
| 2 | C SSE | `curl -N https://okm.cc.bmwgroup.net/api/okm/ingest/progress` (from BMW corporate laptop) | Plan 05 RcaOpsPanel runs in mock-SSE mode (test pattern only); real-stream verification deferred to Phase 45.1 |
| 3 | lucide-react icons | `node -e "const l=require('lucide-react'); ['ZoomIn','ZoomOut','Maximize','Search','Filter','ChevronLeft','ChevronRight','HelpCircle','Sun','Moon','Loader2','Network','MousePointer','ArrowRight','Info'].forEach(i=>console.log(i, typeof l[i]))"` | Substitute icon names with available alternates; update UI-SPEC § Icon-only controls table |
| 4 | display-overlay query param | Operator decision: `?withDisplay=true` GATED (researcher rec) vs always-rich shape | If always-rich preferred → cross-repo OKM `rest-contract.test.ts:257` update — adds 1 day Phase 45.1 work |

Plan 04 Task 1 ALREADY chose `?withDisplay=true` GATED (per RESEARCH recommendation). Probe 4 confirms operator endorsement.

<!-- Playwright project entry skeleton for playwright.config.ts: -->
```typescript
// projects: [
{
  name: 'unified-viewer',
  use: {
    ...devices['Desktop Chrome'],
    baseURL: 'http://localhost:5173',
  },
  testDir: 'tests/e2e/unified-viewer',
  webServer: {
    command: 'cd integrations/unified-viewer && npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: true,
    timeout: 60_000,
  },
},
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    No automation yet — this checkpoint runs the 4 Wave 0 operator probes from 45-VALIDATION.md against the operator's environment. The probes are LOW-confidence research items the researcher could not verify autonomously.
  </what-built>
  <how-to-verify>
    1. Operator opens `.planning/phases/45-unified-web-viewer/45-06-OPERATOR-PROBES.md` (Claude will create the empty template before this checkpoint via the action below).
    2. **Probe 3 (LOCAL — run first, fastest):** in any terminal, run the lucide-react availability check:
       ```
       cd integrations/unified-viewer
       node -e "const l=require('lucide-react'); ['ZoomIn','ZoomOut','Maximize','Search','Filter','ChevronLeft','ChevronRight','HelpCircle','Sun','Moon','Loader2','Network','MousePointer','ArrowRight','Info'].forEach(i=>console.log(i, typeof l[i]))"
       ```
       Expected: every icon prints `<name> function`. Any `<name> undefined` is a RED — record the missing icons in 45-06-OPERATOR-PROBES.md § Probe 3 and substitute available alternates in UI-SPEC + viewer code.
    3. **Probe 4 (LOCAL):** Plan 04 already ships `?withDisplay=true` gated. Operator confirms whether to keep this gated extension or to lift it to "always-rich" (which would require OKM cross-repo work). Default: KEEP gated (RESEARCH § Open Question #4 recommendation + Plan 04 implementation).
    4. **Probe 1 (REQUIRES BMW corporate laptop on-network):** run the CORS probe.
       ```
       curl -i -H "Origin: http://localhost:5173" https://okm.cc.bmwgroup.net/api/okm/health
       ```
       Expected GREEN: HTTP 200 + `Access-Control-Allow-Origin: http://localhost:5173` (or `*`) in headers. RED: any CORS failure, redirect to SSO login HTML, or network unreachable → activate Tier 2/3 fallback per RESEARCH § Open Question #5 (defer to Phase 45.1; RcaOpsPanel in MVP shows the ErrorCorsState/ErrorUnreachableState banner from Plan 03).
    5. **Probe 2 (REQUIRES BMW corporate laptop on-network):** run the SSE probe.
       ```
       curl -N https://okm.cc.bmwgroup.net/api/okm/ingest/progress
       ```
       Expected GREEN: stream stays open, emits `event:` lines. Same RED handling as Probe 1.
    6. Operator fills in 45-06-OPERATOR-PROBES.md with the verbatim curl outputs (redact any session/auth headers) and the GREEN/RED outcome per probe.
    7. If Probe 1 + 2 are RED, operator must decide: ship MVP without live CAP RCA functionality (Plan 05 already covers the error UI gracefully — the operator just sees the banner), or block close-out on Phase 45.1 backend-proxy work. Per RESEARCH § Open Question #5: the planner's recommended default is SHIP — the UI failure modes are correct, Tier 2/3 is additive in 45.1.
  </how-to-verify>
  <resume-signal>Type "probe-results approved: 3+4 GREEN; 1+2 &lt;GREEN|RED&gt;; ship-decision &lt;ship|block&gt;". The Plan 04 + Plan 05 implementations were authored against the GREEN default; RED outcomes are gracefully handled by the State Contract from Plan 03.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire `unified-viewer` Playwright project + write the 9 E2E specs</name>
  <files>playwright.config.ts, tests/e2e/unified-viewer/system-routing.spec.ts, tests/e2e/unified-viewer/entity-detail.spec.ts, tests/e2e/unified-viewer/expand-neighbors.spec.ts, tests/e2e/unified-viewer/filter-search.spec.ts, tests/e2e/unified-viewer/state-reset.spec.ts, tests/e2e/unified-viewer/markdown-viewer.spec.ts, tests/e2e/unified-viewer/rca-ingestion.spec.ts, tests/e2e/unified-viewer/error-states.spec.ts, tests/e2e/unified-viewer/webgl-context.spec.ts, .planning/phases/45-unified-web-viewer/45-06-OPERATOR-PROBES.md</files>
  <action>Create the empty `45-06-OPERATOR-PROBES.md` template at the path above with 4 sections (one per probe) — each section has placeholders for "Command run", "Output (redacted)", "Outcome: GREEN/RED", and "Decision". The Wave 0 operator checkpoint in Task 1 fills this in. Update `playwright.config.ts` (root of repo) to add the `unified-viewer` project entry per the interfaces block. The `webServer.command` boots Vite; `reuseExistingServer: true` allows local-dev parallelism. Write the 9 spec files in `tests/e2e/unified-viewer/`. All specs MUST use `gsd-browser` patterns per CLAUDE.md mandatory rule — NO hand-rolled `chromium.launch()`; use Playwright's `test` import + page fixture from `@playwright/test`. Each spec follows the Per-Task Verification Map in 45-VALIDATION.md. Spec details: (1) `system-routing.spec.ts` — visits `/` and asserts redirect to `/viewer/coding`; visits each `/viewer/{coding,okb,cap}` and asserts `document.querySelector('canvas')` is present (NB: `/viewer/cap` may show ErrorUnreachableState if Probe 1 RED — accept either canvas OR the destructive banner with `Cannot reach cap API at https://okm.cc.bmwgroup.net`); visits `/viewer/foo` and asserts the heading text "Unknown system" + three `<a>` links labelled Coding/OKB/CAP. (2) `entity-detail.spec.ts` — load `/viewer/coding`, click a node (use page.evaluate to invoke sigma's `camera.gotoNode(...)` THEN coordinate click — or use the data-testid we'll surface on the SigmaContainer wrapper), assert side panel renders the 5 sections per UI-SPEC § Entity Detail Panel; assert `digest.observationIds.length` is accessible via `page.evaluate` against the loaded state (camelCase wire-shape verification). (3) `expand-neighbors.spec.ts` — load `/viewer/coding`, count `graph.order` via `page.evaluate(() => window.__viewerSigma?.getGraph()?.order)`, double-click a node, assert graph order increased by ≥1 (or stayed if no new neighbors), assert no console errors. (4) `filter-search.spec.ts` — load /viewer/coding, type 'observation' in search input, assert footer 'Showing X of Y' shows X < Y; uncheck L1 checkbox, assert footer count changes again; select one ontology class in the multi-select, assert further filtering. (5) `state-reset.spec.ts` — load `/viewer/coding`, click a node (verify selectedNodeId via page.evaluate against the Zustand store), navigate to `/viewer/okb`, assert NO selectedNodeId leaks (Pitfall 2 + T-45-01-03 closure). (6) `markdown-viewer.spec.ts` — load `/viewer/okb`, click an entity with description containing a Mermaid block, assert the placeholder div text "Mermaid diagrams are not rendered in MVP" is present; assert a heading anchor (e.g., `#hello` exists in the rendered DOM); assert that toggling the theme via NavBar IconButton (`Toggle theme` aria-label) flips the `<html class>` between '' and 'dark' AND the highlight.js stylesheet swaps. (7) `rca-ingestion.spec.ts` — If Probe 1 + 2 GREEN: load `/viewer/cap`, wait for dir list, click a small RaaS dir's Ingest button, await SSE progression: stage pills cycle, progress bar advances 0→100, success Card appears. If Probe 1+2 RED OR Wave 0 not yet run: use Playwright's `page.route('**/api/okm/rca/dirs', route => route.fulfill({json: <fixture>}))` to mock, and use a TestEventSource init script to feed SSE messages. Spec auto-detects mode by trying live first; on connection error or CORS, falls through to mock. (8) `error-states.spec.ts` — use `page.route('**/api/v1/entities', route => route.abort('failed'))` to simulate unreachable backend; load `/viewer/coding`; assert the ErrorUnreachableState banner with VERBATIM copy `Cannot reach coding API at http://localhost:12436`. Test the CORS variant: `page.route('**/api/v1/entities', route => route.fulfill({status: 0}))` to mimic CORS preflight failure → ErrorCorsState banner. Test 500 on ontology endpoint → ErrorOntologyFetchState amber notice; graph still renders. (9) `webgl-context.spec.ts` — load `/viewer/coding`; in a loop 20 times: `page.goto('/viewer/okb')`, wait for canvas; `page.goto('/viewer/coding')`, wait for canvas. After the loop, assert `await page.evaluate(() => document.querySelectorAll('canvas').length)` ≥ 1 AND check `page.consoleMessages` for zero matches of `/WebGL.*context/i` (Pitfall 8 + T-45-02-01). Across all specs, the SigmaCanvas component should expose `window.__viewerSigma` in dev or test mode for inspection helpers (add a small `useEffect` in `SigmaCanvas.tsx` gated on `import.meta.env.MODE !== 'production'` — write this as part of Task 2, modify Plan 02's `SigmaCanvas.tsx` minimally to surface the test hook). Run the full suite from the repo root: `npx playwright test --project=unified-viewer`. Live verification against A on :12436 and B on :3848 (assume both are launchd-managed and running per STATE.md).</action>
  <verify>
    <automated>npx playwright test --project=unified-viewer --reporter=line</automated>
  </verify>
  <done>Playwright reports all 9 specs PASS (rca-ingestion may run in mock mode if Probe 1+2 RED — counts as PASS); console captures show 0 WebGL warnings across the 20-cycle test; all spec files exist under `tests/e2e/unified-viewer/`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 45 MVP complete:
    - Plan 01: scaffold + ApiClient + Zod wire-shape lock + Router + Zustand
    - Plan 02: WebGL graph renderer + UI-SPEC color contract
    - Plan 03: search/filter/entity-detail + state surfaces + keyboard model + a11y IconButton
    - Plan 04: MarkdownViewer (OKB) + km-core ?withDisplay=true extension + display.json seed
    - Plan 05: RcaOpsPanel (CAP) + OkmRcaClient SSE wrapper
    - Plan 06: 9-spec Playwright suite + Wave 0 operator probes (this task — final gate)
  </what-built>
  <how-to-verify>
    1. Run the full Playwright suite once more: `npx playwright test --project=unified-viewer`. Confirm all 9 specs PASS.
    2. Browse each route manually via `gsd-browser`:
       - `gsd-browser navigate http://localhost:5173/viewer/coding` + `screenshot` — confirm graph renders with seeded display.json colors (Observation blue, Digest green, etc.) per Plan 04.
       - `gsd-browser navigate http://localhost:5173/viewer/okb` + `screenshot` — confirm graph + Markdown tab available; select an entity with markdown and confirm the panel renders formatted output per Plan 04.
       - `gsd-browser navigate http://localhost:5173/viewer/cap` + `screenshot` — either dir list (Tier 1 GREEN per Probe 1+2) or the destructive banner with UI-SPEC copy (Tier 1 RED — degraded gracefully per Plan 03 error states).
    3. Confirm the camelCase wire-shape contract holds in production: `curl -s http://localhost:12436/api/coding/digests?limit=1 | python3 -c "import sys,json; d=json.load(sys.stdin)['data'][0]; assert 'observationIds' in d and 'observation_ids' not in d, 'wire shape broken'"`.
    4. Bundle-size check per RESEARCH § Assumption A4: `cd integrations/unified-viewer && npm run build && du -sh dist/`. Compare against the ~500KB gzipped target; if substantially over, record as a Phase 45.1 follow-up (chunk splitting per `vite.config.ts manualChunks` is already configured).
    5. Confirm `45-06-OPERATOR-PROBES.md` is complete + signed off per Task 1.
    6. Operator decides: APPROVE → Phase 45 closes; VKB + VOKB stay alive as fallback (per D-45-04 + CONTEXT.md Deferred Ideas — retirement is Phase 45.1 work). REJECT → record gap; spawn `/gsd-plan-phase 45 --gaps` for remediation.
    7. If APPROVED: tracking ROADMAP marks Phase 45 ✓ at the next /gsd-verify-phase 45 run.
  </how-to-verify>
  <resume-signal>Type "approved — Phase 45 MVP ships" OR "issues: &lt;list&gt;" to spawn gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → live A/B/C backends | Tests against real services for fidelity; mocks only when unavoidable (CAP CORS RED). |
| 20-cycle WebGL test | Stress-tests the cleanup discipline of Plan 02. |
| Operator probe outputs | Untrusted (typed by operator) but recorded for traceability; redacted of auth headers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06-01 | Repudiation | E2E vs production divergence | accept | Pin Playwright chromium; re-run before each prod deploy. |
| T-45-06-02 | Repudiation | mocked SSE masks real bug | mitigate | Probe 1+2 confirm live C BEFORE close-out; RED defers Phase 45.1. |
| T-45-06-03 | Denial of Service | browser-specific WebGL leak | mitigate | Optional firefox project parallel test. |
| T-45-06-04 | Repudiation | unintended VKB/VOKB regression | accept | VKB + VOKB stay live as fallback; v2 retires both per CONTEXT.md Deferred. |
| T-45-06-SC | Tampering | Playwright transitive supply chain | accept | Existing project dep — vetted in earlier phases. |
</threat_model>

<verification>
- `npx playwright test --project=unified-viewer` reports all 9 specs PASS (or mock-mode PASS for rca-ingestion per Probe outcome)
- `.planning/phases/45-unified-web-viewer/45-06-OPERATOR-PROBES.md` exists, all 4 probe sections filled, with explicit GREEN/RED outcomes
- `cd integrations/unified-viewer &amp;&amp; npm run build` succeeds; bundle-size recorded
- `curl -s http://localhost:12436/api/coding/digests?limit=1` returns camelCase keys (Plan 44-16 lock still GREEN end-to-end)
- 20-cycle WebGL test reports 0 console warnings
- Both operator checkpoints (Task 1 Wave 0 probes + Task 3 final cutover) cleared with "approved"
</verification>

<success_criteria>
- All 4 Wave 0 operator probes EXECUTED with recorded outcomes. Probe 3 + 4 GREEN (local). Probe 1 + 2 either GREEN (full Tier 1) or RED+documented (fall through to Plan 03 error states for /viewer/cap; Phase 45.1 backlog).
- 9 Playwright specs exist and PASS against live A + B (mock fallback for C if Probe 1+2 RED).
- 20-cycle system switch leaks no WebGL contexts.
- Cross-system smoke confirms the camelCase wire-shape contract end-to-end.
- Phase 45 MVP feature set per D-45-04 — graph + filter + search + entity panel + MarkdownViewer (OKB) + RcaOpsPanel (CAP) — is reachable in the live viewer; v2 deferreds (TeamSelector / MermaidDiagram / ConfirmDialog / cluster overlays) explicitly out of scope per CONTEXT.md Deferred Ideas.
- Both operator checkpoints cleared with "approved".
</success_criteria>

<rollback>
If Plan 06 reveals end-to-end breakage:
1. The 9 specs identify the failing surface area; each spec maps to its source plan (system-routing → 01, expand-neighbors → 02, filter-search → 03, markdown → 04, rca → 05).
2. Spawn `/gsd-plan-phase 45 --gaps` against the failed specs.
3. VKB + VOKB stay operational as fallback per CONTEXT.md Deferred Ideas; users can continue working while the gap closure runs.

If the bundle size substantially exceeds the ~500KB gzipped target:
1. Inspect `vite.config.ts` manualChunks — the four vendor chunks (vendor-react, vendor-icons, vendor-graph, vendor-markdown) should already split the heavy deps.
2. Add `vendor-radix` chunk for the 11 Radix primitives.
3. Defer further optimization to Phase 45.1 if MVP can still ship.

If the 20-cycle WebGL test fails:
1. The Pitfall 8 mitigation in Plan 02 (the `useEffect` cleanup calling `sigma.kill()` if needed) was insufficient.
2. Gap-closure plan adds explicit `sigmaRef.current?.kill()` in the `<SigmaContainer>` cleanup; rerun the test.

No production code is modified; all changes confined to `integrations/unified-viewer/` + `tests/e2e/unified-viewer/`.
</rollback>

<output>
Create `.planning/phases/45-unified-web-viewer/45-06-SUMMARY.md` when done.
</output>
