---
phase: 45-unified-web-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Hand-authored files (22). The 15 shadcn primitives under
  # `integrations/unified-viewer/src/components/ui/{button,card,badge,input,dialog,tooltip,scroll-area,separator,tabs,select,checkbox,collapsible,progress,accordion,alert}.tsx`
  # are tool-generated via `npx shadcn add ...` (Task 1 action step 4) using the
  # `new-york` / `neutral` preset inherited from `integrations/system-health-dashboard/components.json`.
  # They are NOT hand-authored, so they are intentionally omitted from this list per
  # plan-checker Dimension 5 scope discipline. The shadcn-add step IS verified by
  # the Task 1 `<done>` acceptance: `ls integrations/unified-viewer/src/components/ui/ | wc -l` returns 15.
  - integrations/unified-viewer/package.json
  - integrations/unified-viewer/tsconfig.json
  - integrations/unified-viewer/vite.config.ts
  - integrations/unified-viewer/vitest.config.ts
  - integrations/unified-viewer/tailwind.config.ts
  - integrations/unified-viewer/postcss.config.mjs
  - integrations/unified-viewer/components.json
  - integrations/unified-viewer/index.html
  - integrations/unified-viewer/.env.example
  - integrations/unified-viewer/.gitignore
  - integrations/unified-viewer/src/main.tsx
  - integrations/unified-viewer/src/App.tsx
  - integrations/unified-viewer/src/index.css
  - integrations/unified-viewer/src/lib/utils.ts
  - integrations/unified-viewer/src/config/system-endpoints.ts
  - integrations/unified-viewer/src/config/theme.ts
  - integrations/unified-viewer/src/api/ApiClient.ts
  - integrations/unified-viewer/src/api/schemas.ts
  - integrations/unified-viewer/src/api/shape-lock.test.ts
  - integrations/unified-viewer/src/store/viewer-store.ts
  - integrations/unified-viewer/src/routes/UnifiedViewer.tsx
  - integrations/unified-viewer/src/routes/UnknownSystem.tsx
  - integrations/unified-viewer/src/test-setup.ts
autonomous: false
requirements:
  - UI-01
user_setup: []

must_haves:
  truths:
    - "A new package exists at `integrations/unified-viewer/` with React 18.3.1 + Vite 5.3.1 + TypeScript 5.2 + Tailwind 3.4.4 + lucide-react ^0.544.0 — versions pinned VERBATIM from `integrations/system-health-dashboard/package.json` per Pitfall 6 (no auto-bump to npm latest)."
    - "`npm install` succeeds in the new package; `npm run build` exits 0; `npm run dev` starts Vite on :5173."
    - "`/viewer/coding`, `/viewer/okb`, `/viewer/cap` each mount `<UnifiedViewer key={system}/>` (per D-45-02 + Pitfall 2 anti-state-leak), with `ApiClient` constructed against the per-system base URL from `SYSTEM_ENDPOINTS`."
    - "Visiting `/` redirects to `/viewer/coding` (per UI-SPEC § Routing default landing)."
    - "Visiting `/viewer/unknown` renders `<UnknownSystem/>` 404 page listing the three valid system slugs as links (per UI-SPEC § Routing)."
    - "Wire-shape lock test asserts `digest.observationIds` exists AND `digest.observation_ids` is rejected (mirror of `tests/integration/typed-views.test.js` Plan 44-16 lock); same for `insight.digestIds`/`lastUpdated`. Observations preserve snake_case `session_id` only (per 44-CONTEXT-amendment-4 § Future-planner guidance)."
    - "The 15 shadcn primitives from UI-SPEC § Design System (button, card, badge, input, dialog, tooltip, scroll-area, separator, tabs, select, checkbox, collapsible, progress, accordion, alert) are present under `src/components/ui/` and import cleanly."
    - "The Zustand `useViewerStore` exposes the slice shape from PATTERNS.md (`selectedNodeId, selectedEdgeId, searchQuery, visibleLevels: Set<0|1|2|3>, selectedClasses: Set<string>, theme, filterRailCollapsed`) + actions (`setSelectedNode, setSearch, toggleLevel, toggleClass, reset`)."
  artifacts:
    - path: integrations/unified-viewer/package.json
      provides: "Pinned dependencies — react@^18.3.1, vite@^5.3.1, tailwindcss@^3.4.4, lucide-react@^0.544.0, react-router-dom@^7.14.0, zustand@^5.0.14, @tanstack/react-query@^5.101.0, zod@^4.3.6, and the same 11 @radix-ui/* primitives the dashboard uses (per PATTERNS.md § package.json table)."
      contains: "lucide-react"
    - path: integrations/unified-viewer/src/config/system-endpoints.ts
      provides: "SYSTEM_ENDPOINTS map for coding (:12436), okb (:3848), cap (https://okm.cc.bmwgroup.net) with VITE_BACKEND_*_URL overrides; SYSTEM_LABELS map; isValidSystem() type-guard."
      contains: "SYSTEM_ENDPOINTS"
    - path: integrations/unified-viewer/src/api/ApiClient.ts
      provides: "km-core /api/v1 REST client class — listEntities / listRelations / listOntologyClasses (calling ?withDisplay=true per Plan 04 contract) / listOntologyClassesNoDisplay (BC fallback) / getNeighbors(id, depth) / listDigests / listInsights against the camelCase typed-views surface."
      contains: "class ApiClient"
    - path: integrations/unified-viewer/src/api/schemas.ts
      provides: "Zod schemas DigestSchema + InsightSchema (camelCase) and ObservationSchema (session_id stays snake_case) — mirror of REQUIRED_DIGEST_KEYS + REQUIRED_INSIGHT_KEYS from tests/integration/typed-views.test.js."
      contains: "observationIds"
    - path: integrations/unified-viewer/src/api/shape-lock.test.ts
      provides: "Vitest mirror of Plan 44-16 wire-shape lock — fails if any reader regresses to snake_case for digests/insights."
      min_lines: 40
      contains: "observationIds"
    - path: integrations/unified-viewer/src/routes/UnifiedViewer.tsx
      provides: "Top-level system router page — reads :system param, validates, returns <ViewerCore key={system}/> per RESEARCH Pattern 1."
      contains: "key={system}"
    - path: integrations/unified-viewer/src/store/viewer-store.ts
      provides: "Zustand store with the slice shape from PATTERNS.md § viewer-store.ts; flushed implicitly when the parent <UnifiedViewer key={system}/> remounts."
      contains: "create<ViewerState>"
  key_links:
    - from: integrations/unified-viewer/src/App.tsx
      to: integrations/unified-viewer/src/routes/UnifiedViewer.tsx
      via: "React Router v7 <Route path='/viewer/:system' element={<UnifiedViewer/>}/> + Navigate redirect from / to /viewer/coding"
      pattern: "viewer/:system"
    - from: integrations/unified-viewer/src/routes/UnifiedViewer.tsx
      to: integrations/unified-viewer/src/config/system-endpoints.ts
      via: "isValidSystem(params.system) + SYSTEM_ENDPOINTS[system] -> ApiClient(baseUrl)"
      pattern: "SYSTEM_ENDPOINTS\\["
    - from: integrations/unified-viewer/src/api/ApiClient.ts
      to: lib/km-core/src/api/handlers/ontology.ts
      via: "GET /api/v1/ontology/classes (no display block until Plan 04 adds the ?withDisplay=true branch — the client targets the future shape, BC falls back to listOntologyClassesNoDisplay)"
      pattern: "/api/v1/ontology/classes"

threat_model:
  - id: T-45-01-01
    threat: "npm install pulls latest major versions (vite@8, react@19, tailwind@4, lucide-react@1) instead of the pinned versions, breaking UI-SPEC verbatim-inherit promise + shadcn baseline (Pitfall 6)."
    category: Tampering
    severity: high
    disposition: mitigate
    mitigation: "Use `npm install <name>@<exact-version>` for the 5 pin-sensitive packages (react / react-dom / vite / tailwindcss / lucide-react). Wave 0 of this plan COPIES the version-pin block from `integrations/system-health-dashboard/package.json` verbatim before running `npm install`. Acceptance grep: `node -p \"require('./integrations/unified-viewer/package.json').dependencies['lucide-react']\"` must equal `^0.544.0` exactly."
  - id: T-45-01-02
    threat: "Wire-shape regression to snake_case (Pitfall 1) — viewer's Zod schemas use observation_ids / digest_ids / last_updated, side panel shows undefined fields silently."
    category: Information Disclosure
    severity: high
    disposition: mitigate
    mitigation: "Mirror REQUIRED_DIGEST_KEYS + REQUIRED_INSIGHT_KEYS from `tests/integration/typed-views.test.js` (Plan 44-16) verbatim into `src/api/schemas.ts` Zod definitions. `shape-lock.test.ts` asserts the snake_case shape THROWS — three independent ratification sites (test, schema, 44-CONTEXT-amendment-4) keep the contract pinned."
  - id: T-45-01-03
    threat: "System-switch state leak (Pitfall 2) — selectedNodeId or filter Set survives /viewer/coding -> /viewer/okb navigation; OKB shows Coding selection."
    category: Information Disclosure
    severity: medium
    disposition: mitigate
    mitigation: "`<UnifiedViewer key={system}/>` pattern forces full unmount on system switch per RESEARCH Pattern 1. Zustand store is module-scoped but the entire React subtree unmounts; integration test in Plan 06 exercises 20 round-trip switches and asserts canvas + store cleanliness."
  - id: T-45-01-SC
    threat: "npm package install — supply-chain risk on the 20 packages slopcheck-verified in RESEARCH § Package Legitimacy Audit."
    category: Tampering
    severity: high
    disposition: mitigate
    mitigation: "RESEARCH.md § Package Legitimacy Audit ran slopcheck 2026-06-07; all 20 returned [OK]. No [ASSUMED]/[SUS] packages — no blocking-human checkpoint required per the planner's package-legitimacy gate. Re-run slopcheck inside `integrations/unified-viewer/` after `npm install` to confirm; commit slopcheck output to `.planning/phases/45-unified-web-viewer/45-01-SLOPCHECK.txt`."
  - id: T-45-01-04
    threat: "CSP / mixed-content — viewer served HTTPS in production but obs-api/semantic-analysis only serve HTTP on localhost. Browser blocks the requests."
    category: Information Disclosure
    severity: low
    disposition: accept
    mitigation: "MVP scope is dev-host Vite (http://localhost:5173) talking to http://localhost:12436 + http://localhost:3848 — same scheme, no CSP problem. Production deployment is out of MVP scope; record as Phase 45.1 follow-up if HTTPS deployment is requested."
---

<objective>
Land the greenfield React + Vite scaffold for the unified viewer at `integrations/unified-viewer/` — package.json with VERBATIM-inherited dashboard pins, the 15 shadcn primitives copied across, ApiClient/Zod schemas wired to Phase 44's camelCase wire shape, React-Router-driven `/viewer/{system}` routing with `key={system}` remount, and the Zustand store skeleton — so subsequent plans (graph renderer, panels, RCA, smoke) can plug in without re-doing foundation work.

Purpose: Discharge D-45-01 (greenfield package), D-45-02 (URL-path routing), and the wire-shape lock prerequisite of D-45-03. No graph rendering yet — `<SigmaCanvas/>` is a placeholder div in this plan.

Output: A self-contained Vite package that builds, serves three valid routes plus a 404, asserts the wire-shape contract at runtime via Zod, and exposes a typed ApiClient for the next plan's graph fetches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-unified-web-viewer/45-CONTEXT.md
@.planning/phases/45-unified-web-viewer/45-RESEARCH.md
@.planning/phases/45-unified-web-viewer/45-UI-SPEC.md
@.planning/phases/45-unified-web-viewer/45-PATTERNS.md
@.planning/phases/45-unified-web-viewer/45-VALIDATION.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-4.md
@CLAUDE.md
@integrations/system-health-dashboard/package.json
@integrations/system-health-dashboard/tsconfig.json
@integrations/system-health-dashboard/vite.config.ts
@integrations/system-health-dashboard/tailwind.config.ts
@integrations/system-health-dashboard/components.json
@integrations/system-health-dashboard/src/lib/utils.ts
@integrations/system-health-dashboard/src/main.tsx
@integrations/system-health-dashboard/src/App.tsx
@integrations/system-health-dashboard/src/lib/config.ts
@lib/km-core/src/api/handlers/ontology.ts
@lib/km-core/src/adapters/observation-view.ts
@tests/integration/typed-views.test.js

<interfaces>
<!-- Wire-shape contract from Plan 44-16 (camelCase ratified 2026-06-07).
     The viewer's Zod schemas MUST mirror these key lists verbatim. -->

From tests/integration/typed-views.test.js (the canonical lock):
```
REQUIRED_OBS_KEYS     = ['id','agent','project','content','artifacts','timestamp']     // session_id snake_case is optional/exception
REQUIRED_DIGEST_KEYS  = ['id','date','theme','summary','observationIds','agents','filesTouched','project']
REQUIRED_INSIGHT_KEYS = ['id','topic','summary','confidence','digestIds','lastUpdated','project']
```

From lib/km-core/src/adapters/observation-view.ts (the LegacyDigest/LegacyInsight TS types — load-bearing LOCKED-contract comments above digestToLegacy + insightToLegacy):
```typescript
interface LegacyDigest  { id: string; date: string; theme: string; summary: string; observationIds: string[]; agents: string[]; filesTouched: string[]; project?: string | null; createdAt?: string; }
interface LegacyInsight { id: string; topic: string; summary: string; confidence: number; digestIds: string[]; lastUpdated: string; project?: string | null; }
```

From lib/km-core/src/api/handlers/ontology.ts (current shape; Plan 04 EXTENDS):
```
GET /api/v1/ontology/classes -> ApiSuccessEnvelope(z.array(z.string()))   // current — array of NAMES
GET /api/v1/ontology/classes?withDisplay=true -> ApiSuccessEnvelope(z.array(z.object({name, level?, parent?, display?})))   // Phase 45 Plan 04 extension
```

The ApiClient in this plan calls `?withDisplay=true` BY DEFAULT, with `listOntologyClassesNoDisplay()` as a back-compat method. Plan 04 lands the server side; until then the with-display call falls through to the existing handler (gets the string-array response, decoded with a defensive map) — graph renderer in Plan 02 uses hash-fallback color exclusively until Plan 04 lands.

From integrations/system-health-dashboard/package.json (the PIN TABLE — DO NOT BUMP):
```
"react":              "^18.3.1"   // NOT 19.x
"react-dom":          "^18.3.1"
"react-router-dom":   "^7.14.0"
"vite":               "^5.3.1"    // NOT 8.x (Pitfall 4)
"tailwindcss":        "^3.4.4"    // NOT 4.x (Pitfall 4)
"lucide-react":       "^0.544.0"  // NOT 1.x (Pitfall 3)
"clsx":               "^2.1.1"
"tailwind-merge":     "^3.3.1"
"class-variance-authority": "^0.7.1"
```
</interfaces>

<!-- Pattern excerpts moved to read_first sources above; tasks reference them by line ranges. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap the package — config files + version-pinned package.json + shadcn primitives + Zod schemas + shape-lock test</name>
  <files>integrations/unified-viewer/package.json, integrations/unified-viewer/tsconfig.json, integrations/unified-viewer/vite.config.ts, integrations/unified-viewer/vitest.config.ts, integrations/unified-viewer/tailwind.config.ts, integrations/unified-viewer/postcss.config.mjs, integrations/unified-viewer/components.json, integrations/unified-viewer/index.html, integrations/unified-viewer/.env.example, integrations/unified-viewer/.gitignore, integrations/unified-viewer/src/index.css, integrations/unified-viewer/src/lib/utils.ts, integrations/unified-viewer/src/test-setup.ts, integrations/unified-viewer/src/components/ui/*.tsx (15 files), integrations/unified-viewer/src/api/schemas.ts, integrations/unified-viewer/src/api/shape-lock.test.ts</files>
  <behavior>
    - Test 1: DigestSchema.parse({id,date,theme,summary,observationIds:['x'],agents:['claude'],filesTouched:['f'],project:'coding'}) → ok.
    - Test 2: DigestSchema.parse({...valid, observation_ids:['x'], observationIds: undefined}) → throws.
    - Test 3: InsightSchema.parse({id,topic,summary,confidence:0.9,digestIds:['a'],lastUpdated:'2026-06-07',project:null}) → ok; snake_case variant throws.
    - Test 4: ObservationSchema.parse({id,agent,project,content,artifacts:[],timestamp,session_id:'sid'}) → ok (session_id exception per amendment-4).
    - Test 5: `node -p "require('./integrations/unified-viewer/package.json').dependencies['lucide-react']"` returns exactly `"^0.544.0"`.
  </behavior>
  <action>Create the `integrations/unified-viewer/` directory tree per PATTERNS.md § File Classification. Copy `tsconfig.json` / `tailwind.config.ts` / `postcss.config.mjs` / `components.json` VERBATIM from `integrations/system-health-dashboard/` (PATTERNS.md sections "tsconfig.json", "tailwind.config.ts + postcss.config.mjs + components.json + src/index.css"). Adjust `tsconfig.json` `include` to also pick up `tests/**/*.ts`. Write `vite.config.ts` per PATTERNS.md § vite.config.ts: `vendor-graph` + `vendor-markdown` manualChunks added, `server.port: 5173`, `strictPort: true`. Write `vitest.config.ts` with `environment: 'jsdom'`, `setupFiles: ['./src/test-setup.ts']`, `globals: true`. Write `package.json` with the VERBATIM pin table from PATTERNS.md § "package.json" (dependencies block + devDependencies block) — pins copied from `integrations/system-health-dashboard/package.json` lines 10-50 EXCEPT no @reduxjs/toolkit / react-redux / recharts / cors / express / yaml (server-only deps). Add scripts `dev`/`build`/`preview`/`test`/`test:watch`/`test:e2e` per PATTERNS.md § package.json scripts. Write `index.html` (Vite-standard root mount), `.env.example` listing `VITE_BACKEND_CODING_URL` / `VITE_BACKEND_OKB_URL` / `VITE_BACKEND_CAP_URL` per D-45-02, `.gitignore` with `node_modules/`, `dist/`, `*.local`, `.env.local`. Copy `src/lib/utils.ts` VERBATIM from dashboard (`cn()` helper). Copy `src/index.css` from dashboard preserving the `:root` + `.dark` CSS-variable blocks (lines 46-91 of dashboard `src/index.css`). Run `npm install` to materialize the lockfile; then `npx shadcn add button card badge input dialog tooltip scroll-area separator tabs select checkbox collapsible progress accordion alert` per UI-SPEC § Design System; verify 15 files land under `src/components/ui/`. Write `src/api/schemas.ts` per PATTERNS.md § schemas.ts — DigestSchema/InsightSchema use camelCase keys verbatim from `tests/integration/typed-views.test.js` REQUIRED_*_KEYS; ObservationSchema includes optional `session_id: z.string()` per 44-CONTEXT-amendment-4. Write `src/api/shape-lock.test.ts` per PATTERNS.md § shape-lock.test.ts — Vitest mirror of typed-views.test.js (the JS test uses Jest; this one uses Vitest). Write `src/test-setup.ts` importing `@testing-library/jest-dom/vitest`. Re-run slopcheck inside `integrations/unified-viewer/` (RESEARCH § Package Legitimacy Audit note about install side-effect) and write output to `.planning/phases/45-unified-web-viewer/45-01-SLOPCHECK.txt`. Per Pitfall 6, after `npm install` runs, GREP `package.json` and confirm the 5 pin-sensitive packages still match exactly the dashboard's values (this script catches an accidental `^` cross of v0→v1).</action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npm run build &amp;&amp; npx vitest run src/api/shape-lock.test.ts --reporter=verbose</automated>
  </verify>
  <done>Package builds (tsc + vite produce `dist/`); shape-lock Vitest passes 5/5; `npm view` confirms no auto-bumped majors; `.planning/phases/45-unified-web-viewer/45-01-SLOPCHECK.txt` exists with all-[OK] results; `ls integrations/unified-viewer/src/components/ui/ | wc -l` returns 15.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the SYSTEM_ENDPOINTS config, ApiClient, Zustand store, and React Router routing — the runtime spine</name>
  <files>integrations/unified-viewer/src/config/system-endpoints.ts, integrations/unified-viewer/src/config/theme.ts, integrations/unified-viewer/src/api/ApiClient.ts, integrations/unified-viewer/src/store/viewer-store.ts, integrations/unified-viewer/src/routes/UnifiedViewer.tsx, integrations/unified-viewer/src/routes/UnknownSystem.tsx, integrations/unified-viewer/src/main.tsx, integrations/unified-viewer/src/App.tsx</files>
  <behavior>
    - Test 1: `isValidSystem('coding')` returns true; `isValidSystem('foo')` returns false (type-narrowing via `s is System`).
    - Test 2: `new ApiClient('http://localhost:12436').listEntities()` issues GET to `http://localhost:12436/api/v1/entities` with `Accept: application/json` and throws on non-2xx. (Tested via msw fetch interceptor in jsdom env.)
    - Test 3: `useViewerStore.getState().reset()` clears `selectedNodeId`, `selectedEdgeId`, `searchQuery`, and `selectedClasses` Set; `visibleLevels` is intentionally NOT reset (filter defaults persist across system switches by design — reset is for in-system clear button only; cross-system reset is the remount).
    - Test 4: Visiting `/viewer/coding` renders the ViewerCore placeholder with the heading text "Coding"; visiting `/viewer/unknown` renders UnknownSystem with three links labelled "Coding", "OKB", "CAP".
    - Test 5: Rendering `<UnifiedViewer/>` twice under MemoryRouter with different `:system` params produces TWO distinct `useViewerStore.getState()` snapshots after remount (key prop forces unmount; this proves Pitfall 2 mitigation).
  </behavior>
  <action>Write `src/config/system-endpoints.ts` per PATTERNS.md § system-endpoints.ts — `SYSTEM_ENDPOINTS` Record per D-45-02 (Coding :12436, OKB :3848, CAP `https://okm.cc.bmwgroup.net`), `SYSTEM_LABELS`, `isValidSystem()` type-guard. Use `import.meta.env.VITE_BACKEND_*_URL` (Vite-canonical) NOT `process.env.*` (Pitfall 4 / PATTERNS.md note). Write `src/config/theme.ts` exposing a small `useTheme()` hook that reads/writes `localStorage('viewer-theme')` and toggles `.dark` on `<html>` per UI-SPEC § Design System theming. Write `src/api/ApiClient.ts` per PATTERNS.md § ApiClient.ts — class with private `get<T>(path)` envelope-unwrap helper; methods `listEntities()`, `listRelations()`, `listOntologyClasses()` (calls `?withDisplay=true`), `listOntologyClassesNoDisplay()` (back-compat), `getNeighbors(id, depth=1)`, `listDigests(limit=50)`, `listInsights(limit=50)`. The `?withDisplay=true` branch handles the BC fallback: if the response body is `string[]` (current pre-Plan-04 shape), map to `[{name: s}]` objects so the graph renderer never sees the mismatch — this avoids hard-coupling Plan 02 to Plan 04 landing first. Write `src/store/viewer-store.ts` per PATTERNS.md § viewer-store.ts — Zustand 5 `create<ViewerState>` with the slice shape declared in must_haves; `reset()` clears only the in-system fields (NOT `visibleLevels` or `theme`); document the per-system-remount-as-reset discipline in a top-of-file comment quoting RESEARCH Pattern 1. Write `src/routes/UnifiedViewer.tsx` per PATTERNS.md § UnifiedViewer.tsx — `useParams<{system}>()` + `isValidSystem(system)` guard + return `<ViewerCore key={system} system={system}/>`. ViewerCore in this plan is a STUB layout (NavBar wordmark + 3 placeholder regions for FilterRail/GraphCanvas/SidePanel + Footer) — actual implementations land in Plans 02-05. Write `src/routes/UnknownSystem.tsx` per UI-SPEC § Routing 404 spec: heading "Unknown system", body lists 3 links to `/viewer/coding`, `/viewer/okb`, `/viewer/cap` with `SYSTEM_LABELS` text. Write `src/main.tsx` per PATTERNS.md § main.tsx — React.StrictMode + QueryClientProvider (staleTime 30s, refetchOnWindowFocus true) + `<App/>`. Write `src/App.tsx` per PATTERNS.md § App.tsx — BrowserRouter + Routes with `/` → Navigate(/viewer/coding), `/viewer/:system` → UnifiedViewer, `*` → UnknownSystem. NO Provider wrap (Zustand is provider-less per PATTERNS.md note).</action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run --reporter=verbose &amp;&amp; npm run build</automated>
  </verify>
  <done>All Vitest tests in this task GREEN; `npm run build` succeeds; routing manually exercised once via `npm run dev` → browser shows the ViewerCore stub at `/viewer/coding`, UnknownSystem at `/viewer/foo`, and `/` redirects to `/viewer/coding`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Scaffolded greenfield package at `integrations/unified-viewer/` with:
    - VERBATIM dashboard-pinned package.json (react@18.3.1 / vite@5.3.1 / tailwind@3.4.4 / lucide@0.544.0)
    - 15 shadcn primitives copied across, same design-tokens via shared CSS vars
    - ApiClient with camelCase Zod schemas (Plan 44-16 lock) + shape-lock Vitest
    - Zustand store + React Router DOM 7 routing with `/viewer/{system}` + `key={system}` remount
    - `/` redirects to `/viewer/coding`; `/viewer/{unknown}` renders 404 page
    - Slopcheck output captured in `45-01-SLOPCHECK.txt`
  </what-built>
  <how-to-verify>
    1. Open a terminal, `cd integrations/unified-viewer`, run `npm run dev` — Vite serves on http://localhost:5173.
    2. Visit `http://localhost:5173/` — browser URL should change to `/viewer/coding` and the page should render the ViewerCore stub with a "Coding" wordmark and three labelled placeholder regions.
    3. Visit `http://localhost:5173/viewer/okb` — wordmark changes to "OKB"; visit `/viewer/cap` — wordmark changes to "CAP".
    4. Visit `http://localhost:5173/viewer/foo` — UnknownSystem page renders with three links visible labelled "Coding" / "OKB" / "CAP".
    5. Confirm dashboard-style look-and-feel: same neutral background, same Inter font, same shadcn button styling on the 3 valid-system links in the 404 page.
    6. Run `cat integrations/unified-viewer/package.json | grep -E '\"(react|vite|tailwindcss|lucide-react)\":'` — confirm the 4 pins read `^18.3.1`, `^5.3.1`, `^3.4.4`, `^0.544.0` respectively (NOT 19/8/4/1).
    7. Run `cd integrations/unified-viewer &amp;&amp; npx vitest run` — all tests should be GREEN.
    8. Run `cat .planning/phases/45-unified-web-viewer/45-01-SLOPCHECK.txt` — confirm all 20 packages returned `[OK]`.
  </how-to-verify>
  <resume-signal>Type "approved" if scaffold is correct, or describe regressions (e.g., "wrong port", "missing primitive", "shape-lock test failed").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → A/B/C REST APIs | Untrusted: viewer client speaks to obs-api (:12436), semantic-analysis (:3848), OKM (https). All response payloads decoded via Zod before reaching React state. |
| npm install → local file system | Untrusted: every package crosses the supply-chain boundary; slopcheck-gated per RESEARCH § Package Legitimacy Audit. |
| import.meta.env.VITE_BACKEND_*_URL → bundle | Inlined at build time; PUBLIC by Vite design — do NOT place secrets here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | Tampering | npm install version resolution | mitigate | Pin react/vite/tailwind/lucide-react to dashboard's exact versions; acceptance grep on `package.json` post-install. |
| T-45-01-02 | Information Disclosure | `src/api/schemas.ts` | mitigate | Zod schemas mirror REQUIRED_DIGEST_KEYS / REQUIRED_INSIGHT_KEYS from `tests/integration/typed-views.test.js` (Plan 44-16 lock) verbatim; `shape-lock.test.ts` fails on snake_case regression. |
| T-45-01-03 | Information Disclosure | `<UnifiedViewer key={system}/>` | mitigate | React Router remount on `:system` change forces unmount of entire subtree, including Zustand store; Plan 06 E2E asserts via 20-round-trip switch + canvas integrity check. |
| T-45-01-04 | Information Disclosure | dev-mode HTTP backends | accept | Localhost-only HTTP for MVP; production HTTPS deployment is a Phase 45.1 concern. |
| T-45-01-SC | Tampering | npm package supply chain | mitigate | slopcheck output captured to `45-01-SLOPCHECK.txt`; all 20 packages [OK] per RESEARCH § Package Legitimacy Audit (no [ASSUMED]/[SUS] — no blocking-human checkpoint required). Re-run inside `integrations/unified-viewer/` post-install to verify the package's actual install (slopcheck install side-effect noted in RESEARCH). |
</threat_model>

<verification>
- `cd integrations/unified-viewer &amp;&amp; npm run build` exits 0
- `cd integrations/unified-viewer &amp;&amp; npx vitest run` reports GREEN with at least 5 tests (shape-lock + routing/store/config tests from Task 2)
- `cd integrations/unified-viewer &amp;&amp; ls src/components/ui/*.tsx | wc -l` returns 15
- `node -p "require('./integrations/unified-viewer/package.json').dependencies['lucide-react']"` returns `"^0.544.0"`
- `node -p "require('./integrations/unified-viewer/package.json').dependencies['react']"` returns `"^18.3.1"`
- `grep -c 'key={system}' integrations/unified-viewer/src/routes/UnifiedViewer.tsx` returns at least 1
- `grep -c 'observationIds' integrations/unified-viewer/src/api/schemas.ts` returns at least 1
- `grep -c 'observation_ids' integrations/unified-viewer/src/api/schemas.ts` returns 0
- `.planning/phases/45-unified-web-viewer/45-01-SLOPCHECK.txt` exists and ends with all-[OK]
</verification>

<success_criteria>
- Greenfield package `integrations/unified-viewer/` builds and serves on :5173 via `npm run dev`.
- The three valid routes mount with correct base URLs per `SYSTEM_ENDPOINTS`; unknown route renders the 404 page; `/` redirects to `/viewer/coding`.
- Wire-shape lock test enforces camelCase for digests + insights, allows snake_case `session_id` on observations — three ratification sites pin the contract (`shape-lock.test.ts` + `schemas.ts` + the upstream `44-CONTEXT-amendment-4.md`).
- The 15 shadcn primitives match the dashboard preset (`new-york` / `neutral` / `lucide` per UI-SPEC § Design System).
- All 5 anti-auto-bump pins (react / react-dom / vite / tailwindcss / lucide-react) survive post-install grep.
- Operator checkpoint cleared with "approved".
</success_criteria>

<rollback>
If scaffold fails to build:
1. `rm -rf integrations/unified-viewer/` to discard.
2. Re-run Task 1 step-by-step; the dashboard analog files are stable upstream and copy verbatim — failures usually mean an env-specific Node version issue, not a plan defect.

If `npm install` resolves to wrong majors (Pitfall 6 manifest):
1. `rm -rf integrations/unified-viewer/node_modules integrations/unified-viewer/package-lock.json`
2. Edit `package.json` to use exact-version pins (e.g., `"react": "18.3.1"` no caret) — re-install.
3. Document the resolver behaviour in Task 1 acceptance notes.

No production code is touched by this plan — no rollback risk to live services.
</rollback>

<output>
Create `.planning/phases/45-unified-web-viewer/45-01-SUMMARY.md` when done.
</output>
