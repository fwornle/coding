---
phase: 45-unified-web-viewer
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - integrations/unified-viewer/src/graph/SigmaCanvas.tsx
  - integrations/unified-viewer/src/graph/useGraphData.ts
  - integrations/unified-viewer/src/graph/color-fallback.ts
  - integrations/unified-viewer/src/graph/color-fallback.test.ts
  - integrations/unified-viewer/src/graph/node-renderer.ts
  - integrations/unified-viewer/src/graph/node-renderer.test.ts
  - integrations/unified-viewer/src/graph/events.ts
  - integrations/unified-viewer/src/graph/types.ts
  - integrations/unified-viewer/src/routes/UnifiedViewer.tsx
  - integrations/unified-viewer/package.json
autonomous: false
requirements:
  - UI-01
user_setup: []

must_haves:
  truths:
    - "`integrations/unified-viewer/src/graph/SigmaCanvas.tsx` mounts an `@react-sigma/core` `<SigmaContainer/>` populated by `useLoadGraph()` from a `graphology.Graph` instance built off TanStack-Query-fetched entities + relations."
    - "Force layout runs via `graphology-layout-forceatlas2` (web-worker mode); >2k nodes lay out in <3s on this machine."
    - "Per-node fill resolves from `ontologyClass.display.color` when present, else from the FNV-1a HSL fallback formula in UI-SPEC § Color (lines 122-137). The fallback theme-conditions S/L: dark = `hsl(h, 65%, 60%)`, light = `hsl(h, 55%, 45%)` — bit-identical to UI-SPEC."
    - "Per-state stroke renders via sigma's `nodeReducer` callback per UI-SPEC § Color State color overlays table (default 1px border / hover 2px ring / selected 3px primary / search-match 2px amber `hsl(45, 100%, 50%)` / filter-dimmed 1px border opacity 0.25 / filter-hidden = not rendered). State is read from the Zustand store + filter predicates — NO force re-simulation when filters or selection change."
    - "Edge state follows UI-SPEC § Color Edges: default `hsl(var(--border))` opacity 0.5; incident-on-selected stroke `hsl(var(--primary)/0.6)` opacity 1.0; incident-only-on-dimmed opacity 0.1."
    - "Click on a node calls `useViewerStore.setState({selectedNodeId: id})`; double-click invokes `apiClient.getNeighbors(id, 1)` and merges new nodes/edges into the existing `graphology.Graph` via `graph.mergeNodeAttributes` + `graph.mergeEdge` (NO full re-render; no force-layout re-run on existing nodes — only the newly added subset is positioned)."
    - "Pan via mouse drag on background; scroll-wheel zoom centered on cursor; pinch-zoom via touchpad — all delivered by sigma's default camera controller (no custom code)."
    - "Hover shows cursor:pointer + 2px ring stroke + 400ms-delayed Tooltip with `{node.name}` + `{node.ontologyClass}` per UI-SPEC § Pointer Hover row."
    - "The ViewerCore layout from Plan 01 is updated so the GraphCanvas region renders `<SigmaCanvas/>` instead of the placeholder; FilterRail and SidePanel remain stubs until Plan 03."
    - "Sigma canvas survives 20 round-trip system switches (/viewer/coding ↔ /viewer/okb in a Playwright loop) without leaking WebGL contexts (Pitfall 8 mitigation — `<UnifiedViewer key={system}/>` from Plan 01 must clean up the SigmaContainer)."
  artifacts:
    - path: integrations/unified-viewer/src/graph/SigmaCanvas.tsx
      provides: "Top-level renderer component — wraps `<SigmaContainer>` with `<GraphSetup/>` (loads graphology + sigma per RESEARCH Example 1 + PATTERNS.md § SigmaCanvas.tsx) + zoom-control cluster at bottom-right (lucide ZoomIn / ZoomOut / Maximize, each with aria-label + Radix Tooltip per UI-SPEC § Icon-only controls)."
      contains: "SigmaContainer"
      min_lines: 80
    - path: integrations/unified-viewer/src/graph/useGraphData.ts
      provides: "TanStack Query hook — useEntities + useRelations + useOntologyClasses, all keyed by `[system, kind]` per RESEARCH Pattern 2 + Pitfall 2; staleTime 30s; refetchOnWindowFocus true."
      contains: "queryKey"
    - path: integrations/unified-viewer/src/graph/color-fallback.ts
      provides: "Pure `classColor(className, theme)` per UI-SPEC § Color FNV-1a HSL formula (lines 122-137)."
      contains: "2166136261"
    - path: integrations/unified-viewer/src/graph/color-fallback.test.ts
      provides: "Vitest pinning the FNV-1a output for 5 known class names across light + dark; if formula drifts, this fails."
      min_lines: 30
    - path: integrations/unified-viewer/src/graph/node-renderer.ts
      provides: "Per-state stroke + opacity table verbatim from UI-SPEC § Color State; exposes `nodeStrokeForState(state) → {width,color,opacity}` and `edgeStateForRelation(edge, selectedNodeId) → {color,opacity}`."
      contains: "hsl(var(--primary))"
    - path: integrations/unified-viewer/src/graph/events.ts
      provides: "useRegisterEvents wrapper — clickNode → setSelectedNode; doubleClickNode → apiClient.getNeighbors + mergeIntoGraph; hover-with-tooltip-delay handled here too."
      contains: "useRegisterEvents"
  key_links:
    - from: integrations/unified-viewer/src/graph/SigmaCanvas.tsx
      to: integrations/unified-viewer/src/api/ApiClient.ts
      via: "TanStack Query hooks in useGraphData -> apiClient.listEntities + apiClient.listRelations + apiClient.listOntologyClasses"
      pattern: "useEntities|useRelations|useOntologyClasses"
    - from: integrations/unified-viewer/src/graph/SigmaCanvas.tsx
      to: integrations/unified-viewer/src/store/viewer-store.ts
      via: "click/double-click handlers in events.ts read+write Zustand store; nodeReducer reads selectedNodeId + filter sets"
      pattern: "useViewerStore"
    - from: integrations/unified-viewer/src/graph/SigmaCanvas.tsx
      to: "@react-sigma/core + sigma + graphology"
      via: "SigmaContainer + useLoadGraph + useRegisterEvents from @react-sigma/core; graphology Graph as the data model"
      pattern: "@react-sigma/core"

threat_model:
  - id: T-45-02-01
    threat: "WebGL context leak across system switches (Pitfall 8) — browser caps WebGL contexts at 8-16/tab; rapid /viewer/coding ↔ /viewer/okb switching exhausts the budget; canvas turns black."
    category: Denial of Service
    severity: high
    disposition: mitigate
    mitigation: "`<UnifiedViewer key={system}/>` from Plan 01 forces full unmount; verify sigma's container cleans up its WebGL context on unmount by adding an integration test in Plan 06 that switches systems 20 times and asserts canvas still renders. In dev, React StrictMode double-invoke can mask the leak — test in production-mode build."
  - id: T-45-02-02
    threat: "Force-layout simulation blocks the main thread on >2k nodes; UI freezes during initial layout."
    category: Denial of Service
    severity: medium
    disposition: mitigate
    mitigation: "Use `@react-sigma/layout-forceatlas2` web-worker mode (RESEARCH § Standard Stack note) — the `iterations: 100` config offloads to a Worker. Add a runtime perf gate: if entities.length > 2000, log a warning and consider raising iterations cap. In MVP scope, ≤2k typical."
  - id: T-45-02-03
    threat: "Hash-fallback color collisions — two distinct class names hash to the same hue; user can't visually distinguish them."
    category: Information Disclosure
    severity: low
    disposition: accept
    mitigation: "FNV-1a + mod-360 gives uniform distribution; collisions occur but the ontology JSON's `display.color` (Plan 04) overrides — operators with parity-critical class sets author display.json entries. Document in 45-01-SUMMARY follow-ups."
  - id: T-45-02-04
    threat: "Double-click expand-neighbors fetch loops infinitely if the API returns the same node as its own neighbor (data integrity bug)."
    category: Denial of Service
    severity: low
    disposition: mitigate
    mitigation: "`mergeIntoGraph` uses `graph.mergeNodeAttributes(id, attrs)` (idempotent) — re-adding the same node id is a no-op. Add a unit test that double-clicks the same node twice and asserts `graph.order` doesn't change after the 2nd call."
  - id: T-45-02-05
    threat: "Pre-Plan-04 ontology endpoint returns string[] not object[]; SigmaCanvas color resolver crashes on `.find(c => c.name === ...)` against strings."
    category: Tampering
    severity: medium
    disposition: mitigate
    mitigation: "Plan 01's ApiClient.listOntologyClasses defensively maps a string[] response to `[{name: s}]` objects (back-compat for pre-Plan-04). The display-block branch is undefined-safe; hash-fallback covers every node until display lands."
threat_model_legend:
  none: ""
---

<objective>
Land the WebGL graph renderer at `integrations/unified-viewer/src/graph/` — `<SigmaCanvas/>` driven by `@react-sigma/core` + `sigma` + `graphology`, fed by TanStack-Query-fetched entities/relations/ontology, with UI-SPEC § Color contract (FNV-1a HSL fallback + per-state strokes + filter-dim opacity) honored via sigma's `nodeReducer`. Click selects, double-click expands neighbors, pan/zoom via the default camera, hover shows a tooltip.

Purpose: Discharge the central deliverable of D-45-04 ("force-directed graph render") + the rendering side of D-45-03 (live ontology fetch with hash fallback). After this plan, the user can navigate to `/viewer/coding`, see entities laid out on a force-directed graph, click + double-click + zoom — but search/filter/detail-panel are still stubs until Plan 03.

Output: A working graph canvas component pluggable into the ViewerCore shell. Per-state visual contract is testable via `node-renderer.test.ts`; the FNV-1a fallback is testable via `color-fallback.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-unified-web-viewer/45-CONTEXT.md
@.planning/phases/45-unified-web-viewer/45-RESEARCH.md
@.planning/phases/45-unified-web-viewer/45-UI-SPEC.md
@.planning/phases/45-unified-web-viewer/45-PATTERNS.md
@.planning/phases/45-unified-web-viewer/45-VALIDATION.md
@.planning/phases/45-unified-web-viewer/45-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Public surface from Plan 01 — Plan 02 builds against these without modifying them. -->

From integrations/unified-viewer/src/api/ApiClient.ts (Plan 01):
```typescript
class ApiClient {
  listEntities(): Promise<Entity[]>;
  listRelations(): Promise<Relation[]>;
  listOntologyClasses(): Promise<OntologyClass[]>;   // ?withDisplay=true; BC-falls-back to [{name}] objects
  getNeighbors(id: string, depth?: number): Promise<NeighborhoodPayload>;
}
```

From integrations/unified-viewer/src/store/viewer-store.ts (Plan 01):
```typescript
interface ViewerState {
  selectedNodeId: string | null;
  searchQuery: string;
  visibleLevels: Set<0|1|2|3>;
  selectedClasses: Set<string>;
  theme: 'light' | 'dark';
  setSelectedNode: (id: string | null) => void;
  // ...
}
const useViewerStore: UseBoundStore<...>;
```

<!-- New types this plan introduces (in integrations/unified-viewer/src/graph/types.ts): -->
```typescript
export interface Entity {
  id: string;
  name: string;
  ontologyClass: string;
  level?: 0 | 1 | 2 | 3;
  description?: string;
  metadata?: Record<string, unknown>;
}
export interface Relation { from: string; to: string; type: string; }
export interface OntologyClass {
  name: string;
  level?: number;
  parent?: string;
  display?: { color?: string; icon?: string; shape?: 'circle' | 'square' | 'diamond' };
}
export interface NeighborhoodPayload { entities: Entity[]; relations: Relation[]; }
export type NodeState = 'default' | 'hover' | 'selected' | 'search-match' | 'filter-dimmed' | 'filter-hidden';
```

<!-- UI-SPEC § Color State color overlays table (lines 142-152) — verbatim: -->
```
default        per-class fill, 1px stroke = hsl(var(--border)),       opacity 1.0
hovered        per-class fill, 2px stroke = hsl(var(--ring)),         opacity 1.0
selected       per-class fill, 3px stroke = hsl(var(--primary)),      opacity 1.0, outer-glow 4px hsl(var(--primary)/0.3)
search-match   per-class fill, 2px stroke = hsl(45, 100%, 50%) AMBER, opacity 1.0
filter-dimmed  per-class fill, 1px stroke = hsl(var(--border)),       opacity 0.25
filter-hidden  not rendered
```

<!-- UI-SPEC § Color Edges (line 152): -->
```
default                       hsl(var(--border)), opacity 0.5
incident on selected node     hsl(var(--primary)/0.6), opacity 1.0
incident only on dimmed nodes hsl(var(--border)), opacity 0.1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure renderers + types — FNV-1a color, per-state stroke, edge state, graph types. Zero React, zero sigma — testable in isolation.</name>
  <files>integrations/unified-viewer/src/graph/types.ts, integrations/unified-viewer/src/graph/color-fallback.ts, integrations/unified-viewer/src/graph/color-fallback.test.ts, integrations/unified-viewer/src/graph/node-renderer.ts, integrations/unified-viewer/src/graph/node-renderer.test.ts</files>
  <behavior>
    - color-fallback Test 1: `classColor('Observation', 'dark')` returns the literal string computed from FNV-1a-32(2166136261, 'Observation') % 360 → `hsl(<H>, 65%, 60%)`. Pin the actual H value once (e.g. via a snapshot or by computing it in the test setup), and assert byte-equality on subsequent calls.
    - color-fallback Test 2: Same input on `'light'` returns `hsl(<H>, 55%, 45%)` (different S/L, same H).
    - color-fallback Test 3: Two different class names produce different hues (rejecting a constant-output bug).
    - color-fallback Test 4: `classColor('', 'dark')` does not throw, returns a valid `hsl(0, 65%, 60%)` (FNV-1a seed mod 360 for empty input).
    - color-fallback Test 5: Output format matches `/^hsl\(\d{1,3}, \d{1,3}%, \d{1,3}%\)$/`.
    - node-renderer Test 1: `nodeStrokeForState('default')` returns `{width:1, color:'hsl(var(--border))', opacity:1.0}`.
    - node-renderer Test 2: `nodeStrokeForState('selected')` returns `{width:3, color:'hsl(var(--primary))', opacity:1.0}` AND exposes `glow: { color: 'hsl(var(--primary)/0.3)', size: 4 }` per UI-SPEC outer-glow row.
    - node-renderer Test 3: `nodeStrokeForState('search-match')` returns the literal amber `hsl(45, 100%, 50%)` (the one hardcoded non-token color per UI-SPEC justification).
    - node-renderer Test 4: `nodeStrokeForState('filter-dimmed')` returns opacity 0.25.
    - node-renderer Test 5: `nodeStrokeForState('filter-hidden')` returns null (caller decodes "do not render").
    - node-renderer Test 6: `edgeStateForRelation({from:'a',to:'b'}, selectedNodeId='a')` returns the primary-incident style; with `selectedNodeId='c'` (non-incident) returns the default style; with both endpoints dimmed returns the dim-incident style.
  </behavior>
  <action>Write `src/graph/types.ts` exporting `Entity`, `Relation`, `OntologyClass`, `NeighborhoodPayload`, `NodeState` per the interfaces block above. Write `src/graph/color-fallback.ts` per PATTERNS.md § color-fallback.ts (verbatim port of UI-SPEC § Color formula lines 122-137); use `>>> 0` to keep `h` unsigned per UI-SPEC pseudocode (NOT `>>>=` which is a parse error in some configs). Write `src/graph/color-fallback.test.ts` — 5 tests per the behavior block. To pin the FNV-1a output deterministically, the test FIRST computes the expected hue inline (re-running the formula), then asserts the function returns the same value — this catches accidental formula drift while not hardcoding magic numbers (which would re-derive the test from the impl). Write `src/graph/node-renderer.ts` per PATTERNS.md § node-renderer.ts: `nodeStrokeForState(state) → {width, color, opacity, glow?}` and `edgeStateForRelation(edge, selectedNodeId, dimmedNodeIds) → {color, opacity}`. The glow field is new vs PATTERNS.md (which omitted it) — UI-SPEC line 147 specifies the selected-state outer glow at 4px `hsl(var(--primary)/0.3)`; include it in the selected case. Write `src/graph/node-renderer.test.ts` — 6 tests per the behavior block. Do NOT touch any React or sigma code in this task — keep these modules dependency-free for fast test runs.</action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/graph/color-fallback.test.ts src/graph/node-renderer.test.ts --reporter=verbose</automated>
  </verify>
  <done>11 tests across the two files all GREEN; the two source files compile under `tsc --noEmit`; grep `grep -c '2166136261' src/graph/color-fallback.ts` returns 1; grep `grep -c 'hsl(45, 100%, 50%)' src/graph/node-renderer.ts` returns 1 (the search-match amber per UI-SPEC).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SigmaCanvas wired end-to-end — useGraphData hooks, GraphSetup with useLoadGraph + nodeReducer + ForceAtlas2, events handler, zoom controls. Replace ViewerCore placeholder.</name>
  <files>integrations/unified-viewer/src/graph/useGraphData.ts, integrations/unified-viewer/src/graph/SigmaCanvas.tsx, integrations/unified-viewer/src/graph/events.ts, integrations/unified-viewer/src/routes/UnifiedViewer.tsx, integrations/unified-viewer/package.json</files>
  <behavior>
    - Test 1: `useGraphData(apiClient)` issues exactly three queries with keys `['entities', system]`, `['relations', system]`, `['ontology', system]` (mocked via MSW fetch handlers).
    - Test 2: After both queries resolve, `<GraphSetup>` calls `loadGraph(graph)` exactly once with a graph that has `entities.length` nodes and `relations.length` edges.
    - Test 3: A node with ontologyClass mapped to `display.color='#ff00ff'` in ontology data renders with that color attribute; a node whose class has no display block renders with the FNV-1a fallback (assert via `graph.getNodeAttribute(id, 'color')` against the value returned by `classColor(className, theme)`).
    - Test 4: `events.handleClick(node)` calls `useViewerStore.setState({selectedNodeId: node})`; `events.handleDoubleClick(node)` calls `apiClient.getNeighbors(node, 1)` exactly once and `graph.mergeNodeAttributes` for each returned entity. A second double-click on the same already-expanded node does NOT increase `graph.order` (idempotency — Pitfall mitigation T-45-02-04).
    - Test 5: Clicking the same node twice keeps `selectedNodeId` set; clicking the background (null node) sets it to null.
    - Test 6: The component's nodeReducer returns the correct `{stroke, opacity}` shape derived from `nodeStrokeForState(state)` for each of: default, hover, selected, search-match, filter-dimmed cases (filter-hidden returns `{hidden: true}` which sigma honors as "do not render").
  </behavior>
  <action>Append `@react-sigma/layout-forceatlas2` to `integrations/unified-viewer/package.json` dependencies (latest compatible with `@react-sigma/core@^5.0.6`; verify with `npm view @react-sigma/layout-forceatlas2 versions`). Re-run `npm install`. Write `src/graph/useGraphData.ts` per RESEARCH Pattern 2 + PATTERNS.md § useGraphData.ts — three `useQuery` hooks (`['entities', system]`, `['relations', system]`, `['ontology', system]`), all with `staleTime: 30_000`. Combine into a single hook return `{entities, relations, ontology, isLoading, error}`. Write `src/graph/events.ts` exporting `useGraphEvents(apiClient, graph)` — wraps `useRegisterEvents` per RESEARCH Example 1 + PATTERNS.md § events.ts; handles `clickNode` / `clickStage` / `doubleClickNode` / `enterNode` / `leaveNode`. The doubleClickNode handler awaits `apiClient.getNeighbors(node, 1)` and merges with `graph.mergeNodeAttributes(id, attrs)` + `graph.mergeEdge(from, to, attrs)` — both idempotent per Graphology semantics. Hover handling: on `enterNode`, set a local `hoveredNode` ref and after a 400ms `setTimeout` show a Radix `<Tooltip>` with `{node.name}` + `{node.ontologyClass}` per UI-SPEC § Pointer Hover row; clear on `leaveNode`. Write `src/graph/SigmaCanvas.tsx` per RESEARCH Example 1 + PATTERNS.md § SigmaCanvas.tsx: outer `<SigmaContainer style={{height:'100%', width:'100%', background:'transparent'}}>` wrapping `<GraphSetup/>` + `<ZoomControls/>`. `<GraphSetup/>` uses `useLoadGraph` to mount a fresh `graphology.Graph` from the useGraphData payload (entities → addNode with `{x:Math.random(), y:Math.random(), size:8, label:e.name, color: ontology.find(c => c.name === e.ontologyClass)?.display?.color ?? classColor(e.ontologyClass, theme)}`; relations → addEdge with `{size:1, color:'hsl(var(--border))', opacity:0.5}`). After `loadGraph(graph)`, call `useLayoutForceAtlas2({iterations: 100, settings: {...}}).assign()` to run the force layout (web-worker mode per RESEARCH § Standard Stack note). Configure `sigmaSettings.nodeReducer = (node, attrs) => {const state = computeNodeState(node, useViewerStore.getState()); const stroke = nodeStrokeForState(state); if (!stroke) return {...attrs, hidden: true}; return {...attrs, borderColor: stroke.color, borderSize: stroke.width, opacity: stroke.opacity};}`. `computeNodeState` is local: selected if `id === selectedNodeId`; search-match if `searchQuery && (node.name.toLowerCase().includes(q.toLowerCase()) || node.description?.toLowerCase().includes(q.toLowerCase()))`; filter-hidden if `!visibleLevels.has(node.level) || (selectedClasses.size > 0 && !selectedClasses.has(node.ontologyClass))` AND search not active; filter-dimmed if level/class filter excludes but search matches (graceful overlap); else default. Similarly configure `sigmaSettings.edgeReducer` per UI-SPEC § Color Edges. Build `<ZoomControls/>` — three icon buttons (lucide `ZoomIn` / `ZoomOut` / `Maximize`) wired to sigma's `camera.animatedZoom({duration:300, factor:1.5})` / `animatedUnzoom` / `animatedReset`; each button has `aria-label` + `<Tooltip>` per UI-SPEC § Icon-only controls (rows 1-3). Update `src/routes/UnifiedViewer.tsx` (ViewerCore section) to swap the GraphCanvas placeholder for `<SigmaCanvas apiClient={apiClient} system={system}/>`. Empty/error/loading states wire to PATTERNS.md § states.tsx components (those land in Plan 03; until then inline simple `<div>Loading...</div>` / `<div>No entities yet</div>` per UI-SPEC § State Contract copy table). Run integration check: `npm run dev`, navigate to `/viewer/coding`, confirm the canvas renders the live obs-api entities (~500 nodes at recent count per STATE.md, well under 2k).</action>
  <verify>
    <automated>cd integrations/unified-viewer &amp;&amp; npx vitest run src/graph/ --reporter=verbose &amp;&amp; npm run build</automated>
  </verify>
  <done>All Vitest tests for `src/graph/` GREEN; `npm run build` succeeds; the dev server at /viewer/coding renders a force-directed graph with ≥10 visible nodes against live obs-api on :12436; click changes the selected-state stroke (verified by `gsd-browser navigate` + screenshot); zoom controls work; hover tooltip appears after 400ms.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Live force-directed WebGL graph at /viewer/coding (and /viewer/okb if B is running). Per-class node colors via FNV-1a hash fallback (Plan 04 lands the display.color override later). Click → 3px primary stroke around selected node + 4px outer glow. Double-click → expand neighbors via /api/v1/entities/{id}/neighbors. Pan/zoom via mouse drag + scroll. Hover → 400ms tooltip with name + class.
  </what-built>
  <how-to-verify>
    1. `cd integrations/unified-viewer &amp;&amp; npm run dev` (Vite on :5173).
    2. `gsd-browser navigate http://localhost:5173/viewer/coding` — graph should render with visible nodes after ~1-2s force layout.
    3. `gsd-browser screenshot` — confirm node-color diversity (multiple distinct hues from FNV-1a fallback).
    4. `gsd-browser click <a-node-coordinate>` — selected node should grow a 3px primary-color stroke + 4px outer glow.
    5. `gsd-browser eval "document.querySelector('canvas').width"` — should return a non-zero pixel width (canvas is live).
    6. Hover over a node for 400ms — Tooltip should pop with `{name}` and `{ontologyClass}` lines.
    7. Double-click a node — observe neighbor nodes fade in (force layout adjusts only the new positions, existing nodes unchanged).
    8. Click the empty background — selection clears, no node has the 3px stroke.
    9. Use the three zoom controls (bottom-right) — verify zoom in/out/reset all animate smoothly.
    10. Switch to `/viewer/okb` (if B is up) — confirm canvas reloads with B's entities, no Coding nodes leak through, no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" if graph + interactions work as specified, or "issues: &lt;description&gt;" with screenshots/regressions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → A/B REST APIs | Same trust boundary as Plan 01; entity/relation payloads decoded via Zod-typed `Entity[]` / `Relation[]` from ApiClient. |
| graphology Graph → sigma WebGL | In-process; sigma reads node attributes directly. Trust is implicit but the data already crossed the Zod gate. |
| user pointer events → camera controller | Sigma's default; no custom code that could be tricked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Denial of Service | `<SigmaContainer>` WebGL context | mitigate | `key={system}` from Plan 01 forces full unmount; Plan 06 E2E asserts 20-cycle switch leaves canvas live. |
| T-45-02-02 | Denial of Service | force-layout main thread | mitigate | web-worker mode via `useLayoutForceAtlas2` per RESEARCH § Standard Stack. |
| T-45-02-03 | Information Disclosure | hash-color collision | accept | Document; users with parity-critical classes author display.json (Plan 04). |
| T-45-02-04 | Denial of Service | double-click expand loop | mitigate | Graphology merge ops are idempotent; test asserts `graph.order` unchanged on re-click. |
| T-45-02-05 | Tampering | pre-Plan-04 ontology BC | mitigate | ApiClient defensive-maps `string[]` → `[{name}]`; fallback color path covers every node. |
</threat_model>

<verification>
- `cd integrations/unified-viewer &amp;&amp; npx vitest run src/graph/` reports GREEN with ≥17 tests (11 from Task 1 + ≥6 from Task 2)
- `grep -c '@react-sigma/core' integrations/unified-viewer/src/graph/SigmaCanvas.tsx` returns at least 1
- `grep -c 'useLoadGraph' integrations/unified-viewer/src/graph/SigmaCanvas.tsx` returns at least 1
- `grep -c 'nodeReducer' integrations/unified-viewer/src/graph/SigmaCanvas.tsx` returns at least 1
- `grep -c 'classColor' integrations/unified-viewer/src/graph/SigmaCanvas.tsx` returns at least 1
- `grep -c 'aria-label' integrations/unified-viewer/src/graph/SigmaCanvas.tsx` returns at least 3 (the 3 zoom buttons per UI-SPEC § Icon-only controls)
- `grep -c 'placeholder' integrations/unified-viewer/src/routes/UnifiedViewer.tsx` returns 0 in the GraphCanvas region (placeholder replaced by `<SigmaCanvas/>`)
- Operator checkpoint passes ("approved")
</verification>

<success_criteria>
- Force-directed graph renders at `/viewer/coding` and `/viewer/okb` against live A and B backends, using either `display.color` (when Plan 04 has landed in same wave-batch — graceful fallback otherwise) or the FNV-1a HSL fallback.
- The 6 per-state visual treatments from UI-SPEC § Color State color overlays table are reachable on the canvas (default, hover, selected, search-match, filter-dimmed, filter-hidden) — though search and filters wire in Plan 03; this plan validates the renderer machinery, not the UI controls that trigger states.
- Click selects, double-click expands, pan + zoom work, hover tooltip appears after 400ms.
- Zoom-control icons carry `aria-label` + `<Tooltip>` per UI-SPEC § Icon-only controls table (non-optional per UI-SPEC reviewer remediation).
- Canvas survives 20 system-switch round trips (deferred to Plan 06 E2E for full validation; this plan's checkpoint exercises a single switch manually).
</success_criteria>

<rollback>
If sigma/graphology integration breaks:
1. `git revert HEAD` on the Task 2 commits — Task 1 (pure modules) is safe and reusable.
2. Inspect the actual sigma version installed: `npm ls sigma @react-sigma/core graphology` should show `^3.0.3` / `^5.0.6` / `^0.26.0`. Version mismatch is the most common failure mode.
3. The dashboard at :3032 is unaffected — Phase 45 is greenfield isolated.

If WebGL context leak surfaces (Pitfall 8):
1. Verify React StrictMode in `src/main.tsx` is enabled (intentional double-invoke catches cleanup bugs in dev).
2. Run a 20-cycle Playwright switch test with `gsd-browser` — if the canvas turns black, the SigmaContainer's `componentWillUnmount` (effectively the cleanup of `useLoadGraph`'s underlying effect) is not running.
3. Fallback: wrap the SigmaContainer in a manual `useEffect(() => () => { sigmaRef.current?.kill(); }, [])` — sigma exposes `kill()` to force WebGL teardown.

No production code is modified; all changes confined to `integrations/unified-viewer/`.
</rollback>

<output>
Create `.planning/phases/45-unified-web-viewer/45-02-SUMMARY.md` when done.
</output>
