---
phase: 19-migration-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/coordinator.ts
  - integrations/system-health-dashboard/src/store/slices/ukbSlice.ts
  - integrations/system-health-dashboard/src/components/workflow/constants.ts
  - shared/workflow-types/schemas.ts
  - shared/workflow-types/index.ts
  - integrations/mcp-server-semantic-analysis/src/shared/workflow-types/schemas.ts
  - integrations/mcp-server-semantic-analysis/src/shared/workflow-types/index.ts
autonomous: true
requirements: [MIG-03]

must_haves:
  truths:
    - "Zero writeProgressFile calls remain in coordinator.ts"
    - "Zero LEGACY-tagged fields remain in ukbSlice.ts"
    - "Zero @deprecated fallback mappings remain in constants.ts"
    - "Zero old-format migration code remains in schemas.ts"
    - "The codebase has exactly one state management path (typed state machine)"
    - "Both backend and dashboard compile cleanly after all deletions"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/coordinator.ts"
      provides: "Coordinator with no writeProgressFile method or calls"
      contains: "!writeProgressFile"
    - path: "integrations/system-health-dashboard/src/store/slices/ukbSlice.ts"
      provides: "Clean UKBState with only workflowState, lastTransition, preferences"
      contains: "!LEGACY"
    - path: "shared/workflow-types/schemas.ts"
      provides: "Clean schemas with schemaVersion: 2 and no migration preprocess"
      contains: "schemaVersion"
  key_links:
    - from: "coordinator.ts"
      to: "state machine dispatch()"
      via: "Only state management path remaining"
      pattern: "dispatch\\("
    - from: "ukbSlice.ts setWorkflowState"
      to: "workflowState"
      via: "Direct assignment, no backward-compat sync"
      pattern: "state\\.workflowState"
---

<objective>
Remove all legacy state management code in 2 staged tasks (4 logical commit groups per user decisions). Each deletion must leave the codebase compilable.

Purpose: MIG-03 requires removing all old inference/fallback code after validation proves the new state machine is correct. The 3 validation runs from Plan 01 have confirmed equivalence.
Output: Clean codebase with exactly one state management path.
</objective>

<execution_context>
@/Users/Q284340/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Q284340/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-migration-cleanup/19-CONTEXT.md
@.planning/phases/19-migration-cleanup/19-RESEARCH.md
@.planning/phases/19-migration-cleanup/19-01-SUMMARY.md

<interfaces>
From coordinator.ts:
```typescript
// writeProgressFile method (lines 155-340): ~185 lines to delete entirely
// ~55 call sites across lines 1847-3744 to remove
// Abort signal checking (lines 160-176): VERIFY cancel still works via state machine cooperative checks
// Debug state preservation (lines 180-227): VERIFY RunConfig handles this via start transition
```

From ukbSlice.ts - fields to REMOVE:
```typescript
execution: WorkflowExecutionState      // LEGACY
singleStepMode: boolean                // LEGACY
singleStepModeExplicit: boolean        // LEGACY
stepPaused: boolean                    // LEGACY
pausedAtStep: string | null            // LEGACY
mockLLM: boolean                       // LEGACY
mockLLMExplicit: boolean               // LEGACY
mockLLMDelay: number                   // LEGACY
// Plus: backward-compat sync block in setWorkflowState (lines 898-946)
// Plus: all actions/reducers that only write to legacy fields
```

From constants.ts - blocks to REMOVE:
```typescript
// @deprecated STEP_TO_AGENT mapping (line 367+)
// @deprecated STEP_TO_SUBSTEP mapping (line 444+)
```

From schemas.ts - functions to REMOVE:
```typescript
function isOldFormat(val: unknown): val is Record<string, unknown>
function migrateOldFormat(old: Record<string, unknown>): Record<string, unknown>
export const WorkflowStateWithMigrationSchema = z.preprocess(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove coordinator writeProgressFile and all call sites</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/coordinator.ts</files>
  <action>
**Strategy:** Delete the method FIRST, then fix all compile errors. TypeScript catches every reference.

1. Delete the entire `writeProgressFile` method (lines 155-~340, approximately 185 lines). This includes:
   - The abort signal checking (lines 160-176) -- redundant with state machine cooperative cancel checks (6 `getState().status === 'cancelled'` checks in wave-controller)
   - The debug state preservation (lines 180-227) -- redundant with RunConfig set immutably at workflow start (SM-03)
   - The stepsDetail builder and JSON write logic

2. Delete ALL call sites. Search for `this.writeProgressFile(` and remove each call. There are ~55 occurrences spread across:
   - `executeStep` / `runStepWithAgent` (step lifecycle writes)
   - `executeWaveWorkflow` (wave progress writes)
   - `finalizeBatchWorkflow` / `finalizeWaveWorkflow` (completion writes)
   - Error handlers (failure writes)
   - Cancel handlers (cancellation writes)

   For each call site: remove the entire `this.writeProgressFile(...)` statement. These are standalone statements (not part of expressions), so removing them is safe. The state machine dispatch() calls that were added in Phase 16 handle all state transitions.

3. Also remove the comparison utility call added in Plan 01 Task 1 (the post-completion comparison) -- it's no longer needed since both paths are being eliminated.

4. Remove the `comparison-util.ts` import if it was added to coordinator.ts.

5. Clean up any now-unused imports (e.g., if `fs` was only used for writeProgressFile, though it's likely used elsewhere).

6. Run `npm run build` to verify compilation. Fix any remaining references the compiler flags.

**CRITICAL:** Do NOT remove the `dispatch()` calls or `getState()` checks -- those are the NEW path that stays. Only remove `writeProgressFile` and its call sites.

**Abort signal note:** The `.data/workflow-abort.json` mechanism was only checked inside writeProgressFile. After removal, cancellation works via: (1) health API dispatches `cancel` transition to state machine, (2) wave-controller checks `getState().status === 'cancelled'` at 6 cooperative points. If the abort file mechanism is still used by the health API cancel endpoint (check tools.ts), leave that code in tools.ts -- it's the cross-process fallback for the detached runner process.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build && grep -c "writeProgressFile" src/agents/coordinator.ts; test $? -ne 0 || echo "FAIL: writeProgressFile references remain"</automated>
  </verify>
  <done>Zero occurrences of writeProgressFile in coordinator.ts. Backend compiles cleanly. State machine dispatch() calls are the sole state management path.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dashboard legacy fields, deprecated constants, and migration schemas</name>
  <files>
    integrations/system-health-dashboard/src/store/slices/ukbSlice.ts,
    integrations/system-health-dashboard/src/components/workflow/constants.ts,
    shared/workflow-types/schemas.ts,
    shared/workflow-types/index.ts,
    integrations/mcp-server-semantic-analysis/src/shared/workflow-types/schemas.ts,
    integrations/mcp-server-semantic-analysis/src/shared/workflow-types/index.ts
  </files>
  <action>
**Staged removal in 3 logical groups (user decision: ordered commits). Delete aggressively -- remove ALL dead code.**

**Group A: Dashboard legacy cleanup (ukbSlice.ts)**

1. Remove LEGACY-tagged fields from UKBState interface:
   - `execution: WorkflowExecutionState`
   - `singleStepMode`, `singleStepModeExplicit`, `stepPaused`, `pausedAtStep`
   - `mockLLM`, `mockLLMExplicit`, `mockLLMDelay`

2. Remove corresponding initial state values from `initialState`.

3. Remove the backward-compat sync block in `setWorkflowState` reducer (lines 898-946 approx). Keep the `state.workflowState = ws` and `state.lastTransition` assignments.

4. Remove all actions/reducers that ONLY write to legacy fields:
   - `setSingleStepMode`, `syncStepPauseFromServer`, `syncSingleStepFromServer`, `resetSingleStepExplicit`
   - `setMockLLM`, `syncMockLLMFromServer`, `resetMockLLMExplicit`
   - Any selectors that only read legacy fields

5. Remove `WorkflowExecutionState` interface and `initialExecutionState` if they become unused.

6. Remove the re-export of `STEP_TO_AGENT` from constants if it's only used for backward compatibility.

7. Remove the `DivergenceBanner` import/usage and divergence fetch from `ukb-workflow-graph.tsx` (temporary component from Plan 01, no longer needed).

8. Delete `DivergenceBanner.tsx` file entirely.

9. Run `npm run build` on dashboard to verify. Fix any component compilation errors by updating components that still reference removed fields/actions -- they should already be using `workflowState` from Phase 18, but verify.

**Group B: Constants deprecated mappings**

1. In `integrations/system-health-dashboard/src/components/workflow/constants.ts`:
   - Delete the `@deprecated` STEP_TO_AGENT mapping block (around line 367)
   - Delete the `@deprecated` STEP_TO_SUBSTEP mapping block (around line 444)
   - Per user decision: YAML is sole source, no fallback. If YAML isn't loaded, graph doesn't render.

2. Search for any imports of `STEP_TO_AGENT` or `STEP_TO_SUBSTEP` from constants.ts in other files. Remove those imports and any code that used them as fallbacks.

3. Run `npm run build` on dashboard.

**Group C: Schema migration code removal + schemaVersion**

1. In `shared/workflow-types/schemas.ts`:
   - Delete `isOldFormat()` function
   - Delete `migrateOldFormat()` function
   - Delete `WorkflowStateWithMigrationSchema` export (replace all usages with `WorkflowStateSchema` directly)
   - Add `schemaVersion: z.literal(2).default(2)` field to the base WorkflowState schema (or as a top-level field in the progress file format)

2. Update `shared/workflow-types/index.ts` to remove the `WorkflowStateWithMigrationSchema` export.

3. Search for any imports of `WorkflowStateWithMigrationSchema` across the codebase. Replace with `WorkflowStateSchema`. Key locations: progress file reader in the state machine subscriber, SSE event parsing.

4. Copy updated shared types to backend: sync `shared/workflow-types/` to `integrations/mcp-server-semantic-analysis/src/shared/workflow-types/` (same file-copy pattern used throughout v3.0).

5. Add `schemaVersion: 2` to the progress file writer (state machine subscriber) so new progress files are stamped.

6. Per user decision: if a stale old-format progress file is found after cutover, delete it and start with idle state. Add this logic to the progress file reader: if parsed data lacks `schemaVersion` or has version < 2, delete the file and return idle state.

7. Run `npm run build` on BOTH backend and dashboard submodules.

**After all groups:** Also remove the temporary `comparison-util.ts` file from Plan 01 if not already removed in Task 1. Delete `DivergenceBanner.tsx` and its health API endpoint for migration divergences.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build && cd /Users/Q284340/Agentic/coding/integrations/system-health-dashboard && npm run build && echo "Both submodules compile" && grep -rn "LEGACY\|@deprecated\|writeProgressFile\|WorkflowStateWithMigration\|isOldFormat\|migrateOldFormat" /Users/Q284340/Agentic/coding/shared/workflow-types/ /Users/Q284340/Agentic/coding/integrations/system-health-dashboard/src/store/slices/ukbSlice.ts /Users/Q284340/Agentic/coding/integrations/system-health-dashboard/src/components/workflow/constants.ts 2>/dev/null; test $? -ne 0 && echo "CLEAN: No legacy references found"</automated>
  </verify>
  <done>
- Zero LEGACY-tagged fields in ukbSlice.ts
- Zero @deprecated mappings in constants.ts
- Zero migration preprocess in schemas.ts
- schemaVersion: 2 present in progress file schema
- Both backend and dashboard compile cleanly
- Codebase has exactly one state management path (typed state machine)
  </done>
</task>

</tasks>

<verification>
- `grep -rn "writeProgressFile" integrations/mcp-server-semantic-analysis/src/` returns zero hits
- `grep -rn "LEGACY" integrations/system-health-dashboard/src/store/slices/ukbSlice.ts` returns zero hits
- `grep -rn "@deprecated" integrations/system-health-dashboard/src/components/workflow/constants.ts` returns zero hits
- `grep -rn "WorkflowStateWithMigration\|isOldFormat\|migrateOldFormat" shared/workflow-types/` returns zero hits
- `npm run build` passes in both submodules
- `grep "schemaVersion" shared/workflow-types/schemas.ts` confirms version field exists
</verification>

<success_criteria>
- All legacy state management code removed (MIG-03 complete)
- Both submodules compile with zero errors
- grep confirms zero references to writeProgressFile, LEGACY fields, @deprecated mappings, migration schemas
- schemaVersion: 2 added to progress file format
- Single state management path: dispatch() -> subscriber -> progress file
</success_criteria>

<output>
After completion, create `.planning/phases/19-migration-cleanup/19-02-SUMMARY.md`
</output>
