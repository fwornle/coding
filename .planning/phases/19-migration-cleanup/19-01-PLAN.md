---
phase: 19-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/coordinator.ts
  - integrations/mcp-server-semantic-analysis/src/utils/comparison-util.ts
  - integrations/system-health-dashboard/src/components/workflow/DivergenceBanner.tsx
  - integrations/system-health-dashboard/src/components/ukb-workflow-graph.tsx
autonomous: false
requirements: [MIG-01, MIG-02]

must_haves:
  truths:
    - "Old writeProgressFile writes to workflow-progress-legacy.json while state machine subscriber writes to workflow-progress.json"
    - "After a workflow run completes, a comparison utility detects divergences between legacy and new progress files"
    - "Dashboard shows a warning banner when divergences are detected"
    - "3 validation runs (production, debug, cancel) pass with no divergence before cutover"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/utils/comparison-util.ts"
      provides: "Post-run snapshot comparison between legacy and new progress files"
      exports: ["compareSnapshots", "Divergence"]
    - path: "integrations/system-health-dashboard/src/components/workflow/DivergenceBanner.tsx"
      provides: "Visual warning when state divergence detected"
      exports: ["DivergenceBanner"]
  key_links:
    - from: "coordinator.ts writeProgressFile"
      to: ".data/workflow-progress-legacy.json"
      via: "Changed output path"
      pattern: "workflow-progress-legacy\\.json"
    - from: "DivergenceBanner.tsx"
      to: "comparison-log.json"
      via: "Reads divergence data from API or file"
      pattern: "divergence|comparison"
---

<objective>
Wire up parallel validation: redirect coordinator's writeProgressFile to a legacy output file while the state machine subscriber continues writing the main progress file. Create a comparison utility for post-run divergence detection and a dashboard warning banner for visibility.

Purpose: MIG-01 requires parallel operation with divergence detection before legacy code can be removed. MIG-02 requires proving the backward-compatible reader works (already implemented in WorkflowStateWithMigrationSchema) before removing it.
Output: Parallel validation infrastructure ready for 3 test runs.
</objective>

<execution_context>
@/Users/Q284340/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Q284340/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-migration-cleanup/19-CONTEXT.md
@.planning/phases/19-migration-cleanup/19-RESEARCH.md

@integrations/mcp-server-semantic-analysis/src/agents/coordinator.ts (lines 155-235: writeProgressFile method)
@shared/workflow-types/schemas.ts (WorkflowStateWithMigrationSchema, isOldFormat, migrateOldFormat)

<interfaces>
From shared/workflow-types/schemas.ts:
```typescript
export const WorkflowStateWithMigrationSchema = z.preprocess((val) => {
  if (isOldFormat(val)) { return migrateOldFormat(val as Record<string, unknown>); }
  return val;
}, WorkflowStateSchema);
```

From coordinator.ts writeProgressFile (line 155):
```typescript
private writeProgressFile(execution: WorkflowExecution, workflow: WorkflowDefinition, currentStep?: string, runningSteps?: string[], batchProgress?: { currentBatch: number; totalBatches: number; batchId?: string }): void
```
The method at line 157 writes to: `${this.repositoryPath}/.data/workflow-progress.json`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect writeProgressFile to legacy file + create comparison utility</name>
  <files>
    integrations/mcp-server-semantic-analysis/src/agents/coordinator.ts,
    integrations/mcp-server-semantic-analysis/src/utils/comparison-util.ts
  </files>
  <action>
1. In coordinator.ts, change line 157 inside writeProgressFile from:
   `const progressPath = \`${this.repositoryPath}/.data/workflow-progress.json\`;`
   to:
   `const progressPath = \`${this.repositoryPath}/.data/workflow-progress-legacy.json\`;`
   This is the ONLY change needed in coordinator.ts -- the abort signal checking and debug state preservation continue to work against the legacy file. The state machine subscriber writes to the main workflow-progress.json unchanged.

2. Create `integrations/mcp-server-semantic-analysis/src/utils/comparison-util.ts` with:
   - `interface Divergence { field: string; legacy: unknown; current: unknown; }`
   - Field mapping array that maps legacy flat format keys to new structured format keys:
     - `status` -> `status` (transform: 'starting' -> 'running')
     - `currentStep` -> `progress.currentStepName`
     - `workflowName` -> `workflowName`
     - `stepsCompleted` -> computed from `progress.currentStepIndex`
     - `totalSteps` -> computed from `progress.totalSteps`
     - `batchProgress` -> `progress.batchProgress` (if present)
   - `compareSnapshots(legacyPath: string, newPath: string): Divergence[]` function
   - `writeComparisonLog(divergences: Divergence[], outputPath: string): void` function that appends timestamped results to `.data/comparison-log.json`
   - Helper `getNestedValue(obj, dotPath)` for accessing `progress.currentStepName` style paths
   - If either file doesn't exist, return empty array (no divergence = not an error)

3. Wire the comparison into the workflow completion path. In coordinator.ts, after the workflow execution completes (near the existing `dispatch({ type: 'complete', ... })` call), add a call to `compareSnapshots` and `writeComparisonLog`. Import the utility at the top. The comparison runs once at workflow end, not on every progress write.

Use `process.stderr.write()` for any logging (no console.log per project rules).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build</automated>
  </verify>
  <done>writeProgressFile writes to workflow-progress-legacy.json, comparison utility exists and compiles, comparison runs at workflow completion</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard divergence warning banner</name>
  <files>
    integrations/system-health-dashboard/src/components/workflow/DivergenceBanner.tsx,
    integrations/system-health-dashboard/src/components/ukb-workflow-graph.tsx
  </files>
  <action>
1. Create `DivergenceBanner.tsx`:
   - Simple React component that checks for divergence data
   - Props: `divergences: Array<{ field: string; legacy: unknown; current: unknown }>`
   - Renders a yellow warning bar (background: #fff3cd, border: 1px solid #ffc107) at the top of the workflow view
   - Shows: "Migration Warning: {N} state divergence(s) detected. Check .data/comparison-log.json for details."
   - Returns null when divergences array is empty
   - TypeScript strict, no `any` types

2. Wire into `ukb-workflow-graph.tsx`:
   - Import DivergenceBanner
   - Add state for divergences (initially empty array)
   - Fetch divergence data from the health API. Add a lightweight endpoint check: `GET /api/ukb/migration-divergences` that reads `.data/comparison-log.json` and returns the latest entry. If file doesn't exist, return `{ divergences: [] }`.
   - Render `<DivergenceBanner divergences={divergences} />` at the top of the workflow graph container
   - Only fetch on component mount and after workflow completion (when workflowState transitions to 'completed' or 'failed')

Note: The health API endpoint for divergence data requires a small addition to the backend tools.ts. Add a GET handler that reads comparison-log.json and returns the last entry. Use `process.stderr.write()` for logging.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <done>DivergenceBanner component renders warning when divergences exist, shows nothing when clean. Banner visible in workflow graph view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate parallel operation with 3 workflow runs</name>
  <action>
User runs 3 validation workflows to confirm old and new state management paths produce equivalent output. This is the gate before legacy code removal in Plan 02.
  </action>
  <what-built>
Parallel validation infrastructure: coordinator writes to legacy file, state machine subscriber writes to main file, comparison utility runs at workflow end, dashboard shows warning on divergence.
  </what-built>
  <how-to-verify>
1. Build and deploy:
   ```
   cd integrations/mcp-server-semantic-analysis && npm run build
   cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services
   cd /Users/Q284340/Agentic/coding/integrations/system-health-dashboard && npm run build
   ```

2. Run 3 validation workflows (MIG-01 requirement):
   a. **Production run**: Start a full UKB workflow (production mode). After completion, check `.data/workflow-progress-legacy.json` exists alongside `.data/workflow-progress.json`. Check `.data/comparison-log.json` for results. Dashboard should show NO warning banner (no divergence).
   b. **Debug/mock run**: Start a debug UKB workflow with mockLLM. Same checks.
   c. **Cancel run**: Start a workflow, cancel mid-run. Verify both files reflect cancelled state. Same checks.

3. Verify MIG-02: The state machine subscriber writes new-format progress that can be read by WorkflowStateWithMigrationSchema. The dashboard continues to work correctly throughout all 3 runs.

4. If any divergences appear in comparison-log.json, report them here. If no divergences across all 3 runs, type "approved" to proceed to Plan 02 (legacy deletion).
  </how-to-verify>
  <verify>User confirms 3 successful runs with zero divergences</verify>
  <done>3 validation runs (production, debug, cancel) complete with no divergence between legacy and new progress files</done>
  <resume-signal>Type "approved" after 3 successful runs with no divergence, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes in both backend and dashboard submodules
- `.data/workflow-progress-legacy.json` is created during workflow runs
- `.data/comparison-log.json` records comparison results
- Dashboard shows no warning banner when divergences are empty
- All 3 validation runs complete with no divergence
</verification>

<success_criteria>
- writeProgressFile output redirected to legacy file (grep confirms `workflow-progress-legacy.json` in coordinator.ts)
- Comparison utility exists and runs at workflow completion
- Dashboard warning banner component exists and is wired into workflow graph
- 3 validation runs pass (production, debug, cancel) with zero divergences
- MIG-02 proven: backward-compatible reader handles both formats
</success_criteria>

<output>
After completion, create `.planning/phases/19-migration-cleanup/19-01-SUMMARY.md`
</output>
