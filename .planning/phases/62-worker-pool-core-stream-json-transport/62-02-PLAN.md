---
phase: 62-worker-pool-core-stream-json-transport
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs
autonomous: true
requirements: [POOL-01]
user_setup: []

must_haves:
  truths:
    - "A persistent claude -p stream-JSON subprocess boots once and serves multiple sequential requests without respawning (stable PID)."
    - "A request written to a worker's stdin as {type:'user',...} resolves to {content,model,tokens} parsed from the {type:'result'} event."
    - "stdout is buffered and split on newline so a JSON line spanning chunk boundaries parses correctly; stderr is drained continuously."
    - "A worker exposes a cancel() seam that emits control_request{subtype:'interrupt'} and survives to serve the next request."
    - "A worker recycles itself (drain + respawn) after a bounded request/context threshold so one caller's content never persists into the next caller's context."
  artifacts:
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs"
      provides: "ClaudeWorker class: persistent spawn, JSON-Lines framing, write()->Promise, cancel(), recycle, reap-on-exit"
      min_lines: 120
      exports: ["ClaudeWorker"]
  key_links:
    - from: "proxy-bridge/worker-pool.mjs"
      to: "claude -p --input-format stream-json"
      via: "spawn(CLAUDE_CLI, bootArgs, {env: buildClaudeEnv(), cwd:'/tmp'})"
      pattern: "spawn\\("
    - from: "proxy-bridge/worker-pool.mjs"
      to: "{type:'result'} stdout event"
      via: "newline-split JSON-Lines parse; resolve pending promise on result"
      pattern: "type.*result"
    - from: "proxy-bridge/worker-pool.mjs"
      to: "control_request interrupt"
      via: "cancel() writes interrupt envelope to stdin"
      pattern: "interrupt"
---

<objective>
Implement the `ClaudeWorker` class in a new `proxy-bridge/worker-pool.mjs`: a persistent `claude -p --input-format stream-json --output-format stream-json` subprocess that boots once with the verbatim context-stripping flag set, frames JSON-Lines stdio (one-promise-per-request under concurrency-1), extracts `{content,model,tokens}` from the `{type:"result"}` event using the existing summation logic, exposes a `cancel()` seam via `control_request{interrupt}`, recycles to bound cross-call context leakage, and reaps cleanly on exit. This is the POOL-01 core; the pool router + dispatcher wiring land in Plan 03.

Purpose: Eliminate the ~2.3s per-call spawn/boot cost (RESEARCH Q4: cold ~3.4s → warm ~991ms) while replicating the execFile path's flags/env/cwd/token-extraction so the worker is drop-in interchangeable with `completeClaudeCodeViaCLI`.
Output: `worker-pool.mjs` exporting `ClaudeWorker`; the unit tests from Plan 01 that target `ClaudeWorker` (parsing) turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-worker-pool-core-stream-json-transport/62-RESEARCH.md
@.planning/phases/62-worker-pool-core-stream-json-transport/62-PATTERNS.md
@/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs

<interfaces>
<!-- Reuse from server.mjs (Plan 03 wires these in; for unit-testability, ClaudeWorker accepts them as injectable deps with these as defaults). -->

From server.mjs:395 — const CLAUDE_CLI = process.env.CLAUDE_CLI_PATH || '/opt/homebrew/bin/claude';
From server.mjs:401-406 — function buildClaudeEnv(): clones process.env, deletes ANTHROPIC_API_KEY + ANTHROPIC_AUTH_TOKEN (forces Max OAuth via keychain).
From server.mjs:531-533 — function resolveClaudeModel(model): maps body.model -> 'haiku'|'sonnet'|'opus' via CLAUDE_MODEL_MAP.
From server.mjs:1106-1122 — the context-stripping arg set (the exact flags to translate to session-start args; swap --output-format json -> --input-format stream-json --output-format stream-json --verbose; DROP the trailing `--`/prompt — prompt arrives over stdin).
From server.mjs:1147-1156 — QUOTA_EXHAUSTED / AUTH_ERROR / generic error string mapping (reuse for result.is_error===true).
From server.mjs:1158-1178 — token+content extraction (modelUsage summation: inputTokens + cacheRead + cacheCreation; the {content,model,tokens:{input,output,total}} return contract).
From server.mjs:1185-1187 — client-disconnect/abort semantics (map result.subtype==='error_during_execution' to this).

Return contract (MUST match completeClaudeCodeViaCLI exactly so it is interchangeable as the D-06 overflow target in Plan 03):
{ content: string, model: string, tokens: { input: number, output: number, total: number } }

Test mock shape: ClaudeWorker must accept a constructor dependency to substitute spawn/stdio (the Plan 01 mock helper at tests/helpers/mock-claude-stdio.mjs) so unit tests run without a real CLI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClaudeWorker boot, JSON-Lines framing, write()->Promise, token/content extraction</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (lines 1071-1201 — completeClaudeCodeViaCLI: the load-bearing analog for flags/env/cwd/error-mapping/token-extraction; lines 395, 401-406, 531-533 — CLAUDE_CLI, buildClaudeEnv, resolveClaudeModel)
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-RESEARCH.md § "Session-start args", § "Request envelope", § "Response event sequence", § "Token + content extraction", § "Boot args helper"
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-PATTERNS.md § "proxy-bridge/worker-pool.mjs" (Boot-args / Spawn env / Error mapping / Token extraction / Pattern 1 excerpts)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/helpers/mock-claude-stdio.mjs (Plan 01 — the injected stdio mock the unit tests drive)
  </read_first>
  <behavior>
    - Test: feeding the Plan-01 mock's system/init -> assistant -> result sequence to a ClaudeWorker resolves `.write(content)` to `{content,model,tokens:{input,output,total}}`.
    - Test: `tokens.input === inputTokens + cacheReadInputTokens + cacheCreationInputTokens` (server.mjs:1165 summation); `tokens.total === tokens.input + tokens.output`; `model === Object.keys(result.modelUsage)[0]`.
    - Test: a JSON `result` line delivered split across two stdout chunks still parses (Pitfall 3 partial-line buffering).
    - Test: a `{type:"result",is_error:true,result:"... rate limit ..."}` rejects with a `QUOTA_EXHAUSTED:`-prefixed error; an auth-string error rejects `AUTH_ERROR:` (reuse server.mjs:1147-1156 mapping).
  </behavior>
  <action>
    Create `worker-pool.mjs` (ESM, `node:child_process` `spawn` + `node:crypto` `createHash`). Implement `class ClaudeWorker` whose constructor takes `(model, systemPrompt, deps={})` where `deps` may inject `{ spawn, claudeCli, buildEnv }` for testability (defaults: real `spawn`, `CLAUDE_CLI`, `buildClaudeEnv` — import or receive these from the parent; for unit isolation, accept them as deps so Plan 01's mock-stdio drives the worker without a real process). Build session-start argv from a `workerBootArgs(model, systemPrompt)` helper that mirrors server.mjs:1106-1122 VERBATIM except: `--output-format json` becomes `--input-format stream-json --output-format stream-json --verbose`, and the trailing `--`/prompt line is OMITTED (prompt goes over stdin). Spawn with `{ env: buildClaudeEnv(), cwd: '/tmp', stdio: ['pipe','pipe','pipe'] }`. Implement the stdout reader as a continuous buffer that splits on `\n` and `JSON.parse`es only complete lines (never `JSON.parse` a raw chunk — Pitfall 3). Implement `.write(content)` returning a Promise stored as a single `_pending` slot (concurrency-1, Pattern 1): write `{type:"user",message:{role:"user",content}}\n` to stdin; on the `{type:"result"}` event, resolve with the extracted `{content,model,tokens}` (port server.mjs:1158-1178; `content = ev.result`) or reject via the server.mjs:1147-1156 QUOTA/AUTH mapping when `ev.is_error`, then clear `_pending` and mark the worker free. Treat `{type:"system","subtype":"init"}` and `assistant` events as non-terminal (only `result` ends a response — anti-pattern: do NOT treat init as the boundary). Continuously drain stderr (read-and-discard now; throttled logging is GUARD-03/Phase 64) so the pipe never blocks a long-lived worker. Expose `.pid` (the subprocess pid) and a `.model`/`.systemPrompt` getter. No fenced code in this action — follow the cited line-range excerpts.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --check proxy-bridge/worker-pool.mjs && node --test tests/unit/worker-pool.test.mjs 2>&1 | grep -E "pass|fail"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check proxy-bridge/worker-pool.mjs` exits 0.
    - Source assertion: `export class ClaudeWorker` present; boot args grep shows `--input-format`, `stream-json`, `--verbose`, `--model`, and the full strip set (`--tools`, `--no-session-persistence`, `--disable-slash-commands`, `--strict-mcp-config`, `--setting-sources`, `--system-prompt`); grep confirms NO `args.push('--', ` (prompt-over-stdin, not argv). Filter comments: `grep -v '^\\s*//' proxy-bridge/worker-pool.mjs | grep -c "args.push('--',"` returns 0.
    - Source assertion: stdout framing splits on `\n` (grep finds a newline-split buffer) and never calls `JSON.parse` on a raw `data` chunk without buffering.
    - Behavior assertion: the Plan-01 unit tests for POOL-01 result-parsing and the QUOTA/AUTH mapping PASS (`node --test tests/unit/worker-pool.test.mjs` reports those cases green).
    - Behavior assertion: `tokens.input` uses the cacheRead+cacheCreation summation (the parsing test asserts the exact summed value).
  </acceptance_criteria>
  <done>worker-pool.mjs exports ClaudeWorker; it boots with the verbatim strip flags over stream-JSON, frames JSON-Lines with partial-line safety, drains stderr, and resolves `.write()` to the interchangeable return contract; Plan-01 parsing/error-mapping unit tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cancel() interrupt seam, context-leak recycle, reap-on-exit</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/worker-pool.mjs (Task 1 output — extend the same class)
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-RESEARCH.md § "Q3 — Stream-JSON cancellation" (interrupt VERDICT), § "Cancellation envelope", § "Pitfall 1: Conversation context accumulates", § "Runtime State Inventory" (reap-on-exit), § "Security Domain" (V3 data-isolation)
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-PATTERNS.md § "Cancel-hook seam"
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (lines 1185-1187 — client-disconnect mapping to reuse for error_during_execution)
  </read_first>
  <behavior>
    - Test: calling `.cancel()` while a request is in-flight writes a `control_request` with `request.subtype:'interrupt'` (and a `request_id`) to stdin; the in-flight `.write` promise settles via the `error_during_execution` -> client-disconnect mapping; the worker does NOT exit (`.pid` unchanged) and the NEXT `.write` resolves normally (Q3: worker survives).
    - Test: after a configurable max-requests threshold (default from a knob, e.g. 50) OR when `result.usage.input_tokens` exceeds a configurable ceiling, the worker flags itself for recycle (a `needsRecycle`/`isStale` signal the pool reads) so the pool drains+respawns it — bounding cross-call context leakage (Pitfall 1).
    - Test: on subprocess `exit`, the worker emits an `'exit'` event and rejects any outstanding `_pending` as RETRYABLE-shaped (not a silent hang).
  </behavior>
  <action>
    Extend `ClaudeWorker` with: (1) `cancel()` — write `{type:"control_request","request_id":<uuid>,"request":{"subtype":"interrupt"}}\n` to stdin (the seam the Phase-63 client-disconnect wiring will call; full propagation is Phase 63/WLIFE-04 — expose the seam now per D-03/Claude's Discretion). When the subsequent `{type:"result",is_error:true,subtype:"error_during_execution"}` arrives, settle the in-flight promise using the server.mjs:1185-1187 `client disconnected` mapping. Do NOT SIGTERM or respawn on cancel (Q3: interrupt keeps the worker alive — anti-pattern to avoid: SIGTERM+respawn as the primary cancel path). (2) Context-leak recycle (mandatory correctness control, NOT optional hardening — security hazard from RESEARCH § Pitfall 1 / Security Domain V3): track a per-worker request count and the last `result.usage.input_tokens`; when either crosses a configurable threshold (request-count default ~50, names planner/executor discretion — read the threshold from an injectable option with a documented default), set a `needsRecycle`/`isStale` flag the pool (Plan 03) checks before reuse, so callers' content never persists indefinitely into a later caller's context. Document the non-statelessness in a comment. (3) Reap-on-exit: register the subprocess `'exit'`/`'close'` handler to emit a worker `'exit'` event and reject any outstanding `_pending` with a RETRYABLE-shaped error (full crash-recovery is Phase 63; basic reap belongs here per Runtime State Inventory). Add an explicit `.dispose()`/`.drain()` that ends stdin and lets the subprocess exit so proxy shutdown / pool eviction can reap workers without orphans. Add a `.cancel()`-uses-`crypto.randomUUID()` for the request_id.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --check proxy-bridge/worker-pool.mjs && node --test tests/unit/worker-pool.test.mjs 2>&1 | grep -E "pass|fail"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `cancel()` emits an interrupt envelope — `grep -v '^\\s*//' proxy-bridge/worker-pool.mjs | grep -c "interrupt"` returns ≥1; grep finds `control_request` and `request_id`.
    - Source assertion: cancel path does NOT SIGTERM/kill the worker — grep confirms no `kill(`/`SIGTERM` inside the `cancel()` body (kill may exist only in `dispose()`/reap).
    - Source assertion: a recycle/staleness signal exists (grep finds `needsRecycle` or `isStale`) gated on a request-count/context-token threshold (grep finds `input_tokens` or a count threshold).
    - Source assertion: an `'exit'` handler emits a worker exit event and settles outstanding `_pending` (grep finds an exit/close listener and a `_pending` rejection path).
    - Behavior assertion: Plan-01 unit cases that target cancel-seam shape and recycle-flag behavior (mock-driven) PASS.
  </acceptance_criteria>
  <done>ClaudeWorker exposes a non-destructive cancel() interrupt seam (worker survives), a threshold-driven recycle/staleness flag bounding context leakage, and reap-on-exit that settles in-flight requests as RETRYABLE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy → claude CLI subprocess | Untrusted user prompt crosses into the subprocess as JSON over stdin (never argv). |
| request A ↔ request B on a shared persistent worker | A correctness/data-isolation boundary: a long-lived worker accumulates conversation context across requests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02-01 | Information Disclosure | Persistent worker conversation context (cross-call leakage) | mitigate | MANDATORY threshold-driven recycle (Task 2) bounds how long one caller's content stays in a worker's context; `session_id` is NOT relied upon (ignored per Q4). Worker non-statelessness is documented so callers never assume isolation within a worker's life. (RESEARCH § Pitfall 1 / Security Domain V3.) |
| T-62-02-02 | Tampering | Prompt content interpreted as CLI flags | mitigate | Prompt travels as `JSON.stringify`-ed `{type:"user"}` over stdin — never as argv. Strictly safer than the execFile `--` path. (RESEARCH § Security Domain V5.) |
| T-62-02-03 | Denial of Service (RAM) | Orphaned worker subprocesses on proxy crash/restart | mitigate | Reap-on-exit handler + `.dispose()`/`.drain()` end stdin and settle in-flight requests; the parent `com.coding.llm-cli-proxy` launchd job manages the proxy lifecycle. Full idle-evict is Phase 63. |
| T-62-02-04 | Denial of Service | Undrained stderr pipe stalls a long-lived worker | mitigate | stderr is drained continuously (read-and-discard); throttled logging is GUARD-03/Phase 64. (RESEARCH § Pitfall 3.) |
| T-62-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed — all Node builtins (`node:child_process`, `node:crypto`). No install task; no legitimacy checkpoint required. (RESEARCH § Package Legitimacy Audit: N/A.) |
</threat_model>

<verification>
- `node --check proxy-bridge/worker-pool.mjs` passes.
- Plan-01 unit tests targeting ClaudeWorker (parsing, error mapping, cancel-seam shape, recycle flag) are GREEN with dependency-injected stdio (no real CLI).
- Boot-args grep confirms the verbatim strip set translated to stream-JSON with prompt-over-stdin (no argv prompt).
- cancel() is non-destructive (no kill in the cancel path); reap-on-exit settles in-flight requests.
</verification>

<success_criteria>
- POOL-01 is implemented at the worker level: a persistent stream-JSON subprocess boots once, serves sequential requests, frames JSON-Lines safely, and returns the interchangeable `{content,model,tokens}` contract.
- The cross-call context-leakage hazard is mitigated by a mandatory threshold-driven recycle signal (correctness requirement, not optional).
- The cancel-hook seam (interrupt, worker survives) is exposed for Phase 63 to extend without refactoring.
</success_criteria>

<output>
Create `.planning/phases/62-worker-pool-core-stream-json-transport/62-02-SUMMARY.md` when done.
</output>
