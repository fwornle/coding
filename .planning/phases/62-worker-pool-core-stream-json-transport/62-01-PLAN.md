---
phase: 62-worker-pool-core-stream-json-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/unit/worker-pool.test.mjs
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/helpers/mock-claude-stdio.mjs
autonomous: true
requirements: [POOL-01, POOL-02, POOL-03, POOL-04, GUARD-01]
user_setup: []

must_haves:
  truths:
    - "A unit test file exists that asserts a {type:'result'} stream-JSON line is parsed into {content,model,tokens}."
    - "A unit test file exists that asserts the router never hands a sonnet request to a haiku-booted worker (keying by model)."
    - "A unit test file exists that asserts concurrency-1 and all-workers-busy overflow to a completeClaudeCodeViaCLI spy (D-06)."
    - "An integration test file exists, argv-gated (--live), covering stable-PID reuse, no-spawn-on-direct, GUARD-01 escape hatch, and the cancel seam."
    - "A shared mock-stdio helper emits the system/init -> assistant -> result line sequence used by the unit tests."
  artifacts:
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/helpers/mock-claude-stdio.mjs"
      provides: "Reusable mock claude worker stdio stream (system/init -> assistant -> result JSON-Lines)"
      min_lines: 30
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/unit/worker-pool.test.mjs"
      provides: "RED unit suite for POOL-01 parsing, POOL-02 keying, POOL-03 concurrency-1 + D-06 overflow"
      min_lines: 60
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs"
      provides: "RED integration suite (--live gated) for POOL-01/POOL-04/GUARD-01/cancel-seam"
      min_lines: 50
  key_links:
    - from: "tests/unit/worker-pool.test.mjs"
      to: "proxy-bridge/worker-pool.mjs"
      via: "import { ClaudeWorker, WorkerPool }"
      pattern: "import.*worker-pool"
    - from: "tests/unit/worker-pool.test.mjs"
      to: "tests/helpers/mock-claude-stdio.mjs"
      via: "import mock helper"
      pattern: "import.*mock-claude-stdio"
---

<objective>
Create the Wave-0 test scaffolding for the worker pool: a shared mock-stdio helper, a deterministic unit suite (mock streams, no real CLI), and an argv-gated live integration suite. These tests encode the behavioral contracts (POOL-01..04, GUARD-01, cancel seam) that Plans 02 and 03 must satisfy — they are RED until the worker module and dispatcher wiring land.

Purpose: Nyquist validation coverage (Dimension 8) — every requirement in this phase has a named automated test BEFORE implementation, derived from RESEARCH.md § Validation Architecture.
Output: Three test/helper files committed in RED state (importing a not-yet-existing `worker-pool.mjs`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-worker-pool-core-stream-json-transport/62-RESEARCH.md
@.planning/phases/62-worker-pool-core-stream-json-transport/62-PATTERNS.md

<interfaces>
<!-- Contracts the tests pin. The worker module (Plan 02) and pool/dispatcher (Plan 03) implement against these. -->

Worker module public surface (to be created in proxy-bridge/worker-pool.mjs):
- class ClaudeWorker — constructor(model, systemPrompt, deps?); .write(content): Promise<{content,model,tokens:{input,output,total}}>; .cancel(): void; .pid; event 'exit'
- class WorkerPool — .complete(body, abortSignal, overflowFn): Promise<{content,model,tokens}>; keyed Map<key, ClaudeWorker[]>

Return contract (interchangeable with completeClaudeCodeViaCLI — server.mjs:1170-1178):
{ content: string, model: string, tokens: { input: number, output: number, total: number } }

stream-JSON result event the mock helper must emit (RESEARCH § Response event sequence):
{"type":"result","subtype":"success","is_error":false,"result":"<text>","usage":{...},"modelUsage":{"claude-sonnet-4-6":{"inputTokens":..,"outputTokens":..,"cacheReadInputTokens":..,"cacheCreationInputTokens":..}}}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared mock-claude-stdio helper</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/helpers/mock-claude-stdio.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration-bridge.test.mjs (lines 30-40 — house test stack: node:test + assert/strict, ESM, argv mode switch)
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-RESEARCH.md § "Response event sequence (stdout, JSON-Lines)" and § "Token + content extraction"
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-PATTERNS.md § "tests/unit/worker-pool.test.mjs" (Wave 0 third bullet — shared helper)
  </read_first>
  <action>
    Export a factory (e.g. `makeMockWorkerStdio({ model, resultText, inputTokens, outputTokens, cacheRead })`) that returns an object exposing a readable-like stdout source and a way to drive it. It must emit, as separate `\n`-terminated JSON lines in order: a `{type:"system","subtype":"init",...}` line, one `{type:"assistant","message":{content:[{type:"text","text":resultText}],usage:{...}}}` line, then the terminal `{type:"result","subtype":"success","is_error":false,"result":resultText,"usage":{...},"modelUsage":{[model]:{inputTokens,outputTokens,cacheReadInputTokens:cacheRead,cacheCreationInputTokens:0}}}` line. Also export a helper to emit an error result (`is_error:true, subtype:"error_during_execution", result:""`) for the cancel/interrupt and QUOTA/AUTH mapping tests. Keep the helper transport-agnostic: it should let the worker code do its own `\n`-split buffering (do NOT pre-parse). Provide a way to emit a line split across two chunks so Plan 02's Pitfall-3 partial-line buffering can be exercised. Use `node:stream` PassThrough or an EventEmitter the worker can read; document the chosen shape in a top comment so Plan 02 wires the worker's stdout reader to it.
  </action>
  <verify>
    <automated>node --check /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/helpers/mock-claude-stdio.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` on the helper exits 0 (valid ESM).
    - Source assertion: file exports a factory and an error-result emitter (grep finds `export` for both names).
    - Source assertion: the terminal line carries `"type":"result"` and a `modelUsage` keyed by the passed model (grep finds both tokens in the emitted template).
    - Behavior assertion: the helper can emit a single logical JSON line split across ≥2 chunks (a split-line emitter is present).
  </acceptance_criteria>
  <done>Helper file exists, is valid ESM, emits the system/init -> assistant -> result sequence plus an error variant and a split-line variant.</done>
</task>

<task type="auto">
  <name>Task 2: RED unit suite (POOL-01 parsing, POOL-02 keying, POOL-03 concurrency-1 + D-06 overflow)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/unit/worker-pool.test.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/helpers/mock-claude-stdio.mjs (Task 1 output — the mock the unit tests drive)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration-bridge.test.mjs (lines 30-37 — import style, MODE switch)
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-RESEARCH.md § "Phase Requirements -> Test Map" (the exact behaviors) and § "Token + content extraction"
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-PATTERNS.md § Pattern 1 / Pattern 2 (worker key, one-promise-per-request)
  </read_first>
  <action>
    Write a `node:test` + `assert/strict` suite importing `{ ClaudeWorker, WorkerPool }` from `../../proxy-bridge/worker-pool.mjs` and the Task 1 mock helper. Cover: (a) POOL-01 — feeding a mock `{type:"result"}` line through a ClaudeWorker (with injected mock stdio) resolves `.write(content)` to `{content,model,tokens:{input,output,total}}` with `tokens.input === inputTokens + cacheReadInputTokens + cacheCreationInputTokens` (the server.mjs:1165 summation) and `tokens.total === input+output`; (b) POOL-02 — a WorkerPool computing a key for `body.model:'sonnet'` vs `'haiku'` produces distinct keys and never returns a haiku-booted worker for a sonnet request (assert on the `model::prompthash` key shape from RESEARCH Pattern 2); (c) POOL-03 — with pool size 1, a second `.write` while the first is in-flight does not interleave on the same worker's stdin (the second resolves only after the first's `result`, or routes to a sibling when size>1); (d) POOL-03/D-06 — when all workers for a key are busy, `pool.complete` invokes the passed `overflowFn` spy (stand-in for `completeClaudeCodeViaCLI`) exactly once and returns its value. Inject the spawn/stdio via a constructor dependency so NO real `claude` process is started (deterministic). Tests run in default (mock) mode — gate nothing behind `--live`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --test tests/unit/worker-pool.test.mjs; test $? -ne 0 && echo "RED-AS-EXPECTED (worker-pool.mjs not yet implemented)" || echo "GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: suite imports from `../../proxy-bridge/worker-pool.mjs` and from the Task 1 mock helper (grep both import paths).
    - Source assertion: suite contains test cases named for POOL-01 parsing, POOL-02 keying, POOL-03 concurrency-1, and D-06 overflow (grep finds the four behaviors).
    - Behavior assertion: running the suite FAILS with a module-not-found or assertion error referencing `worker-pool.mjs` (RED is expected — implementation lands in Plans 02/03). The verify command treats non-zero exit as the expected RED state.
    - The suite uses dependency-injected stdio (no `spawn` of a real binary) — grep confirms no direct `'/opt/homebrew/bin/claude'` or live spawn in the unit file.
  </acceptance_criteria>
  <done>Unit suite exists, imports the (not-yet-existing) worker module + mock helper, encodes the four POOL/D-06 behaviors, and runs RED.</done>
</task>

<task type="auto">
  <name>Task 3: RED live integration suite (POOL-01 PID reuse, POOL-04 no-spawn-on-direct, GUARD-01, cancel seam)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/worker-pool-live.test.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration-bridge.test.mjs (lines 19-40 — the --live/--smoke/--mock argv gating and run-mode header to mirror)
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-RESEARCH.md § "Phase Requirements -> Test Map" + § "Cancellation envelope" + Pitfall 4 (warm-up first)
    - /Users/Q284340/Agentic/coding/.planning/phases/62-worker-pool-core-stream-json-transport/62-PATTERNS.md § "tests/integration/worker-pool-live.test.mjs"
  </read_first>
  <action>
    Write a `node:test` suite that runs its live describe-block ONLY when `process.argv.includes('--live')` (mirror integration-bridge.test.mjs:35 MODE switch); in default mode it should be a no-op / skip so CI without Max OAuth passes. Cover, against a real `ClaudeWorker`/`WorkerPool`: (a) POOL-01 — spawn a sonnet worker, send two sequential `say OK`-class requests, assert the worker `.pid` is identical across both (stable PID = no respawn) and both resolve to non-empty `content`; (b) POOL-04 — assert that a haiku request that would take the direct OAuth path never causes a `claude -p` worker subprocess to be spawned (assert pool has zero workers for the haiku key after such a request — exercised via the dispatcher with a forced-direct stub or by asserting the pool is only engaged on the fallback branch); (c) GUARD-01 — with `LLM_PROXY_DISABLE_WORKER_POOL=1` set, assert no worker is spawned and the path returns the same `{content,model,tokens}` shape as the execFile baseline; (d) cancel seam — start a long request, call `worker.cancel()`, assert the in-flight promise rejects/resolves with the interrupt/`error_during_execution` mapping AND a subsequent `say PONG` on the SAME worker (same `.pid`) succeeds (worker survives — Q3); (e) warm-perf sanity — send a warm-up request first, then assert the 2nd request wall time < 3s (informal; formal gate is PERF-01/Phase 65 — Pitfall 4). Document at the top that this is RED until Plans 02/03 land and that `--live` requires Max OAuth + network.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --test tests/integration/worker-pool-live.test.mjs; echo "exit=$? (mock/default mode must be 0 — live block skipped without --live)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: suite contains an argv `--live` gate mirroring integration-bridge.test.mjs (grep finds `--live`).
    - Source assertion: suite covers POOL-01 stable-PID, POOL-04 no-spawn-on-direct, GUARD-01 escape hatch, cancel seam (interrupt + survives), and a warm <3s sanity check (grep finds all five).
    - Behavior assertion: in DEFAULT (non-`--live`) mode the suite exits 0 (live work skipped) so CI without OAuth is green; the `--live` block is RED until Plans 02/03 implement the module.
    - Source assertion: GUARD-01 case reads/sets `LLM_PROXY_DISABLE_WORKER_POOL` (grep finds the env var name).
  </acceptance_criteria>
  <done>Integration suite exists, argv-gated to `--live`, encodes POOL-01/POOL-04/GUARD-01/cancel/warm-sanity, and is a no-op (exit 0) in default mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → proxy | Untrusted request body (messages, model) crosses into the proxy; tests assert JSON-encoding of prompt (no shell interpolation). |
| proxy → claude CLI subprocess | Prompt content crosses into a subprocess over stdin as JSON (not argv). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01-01 | Information Disclosure | Test fixtures / mock helper | accept | Mock helper uses synthetic prompts only; no real secrets or PII in test data. |
| T-62-01-02 | Tampering | Live test env-var handling (`LLM_PROXY_DISABLE_WORKER_POOL`) | mitigate | GUARD-01 test reads the env var via `process.env === '1'` exact match (no eval, no shell); test restores env after run. |
| T-62-01-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 62 installs ZERO packages (RESEARCH § Package Legitimacy Audit: N/A — all Node builtins). No install task exists in this plan; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `node --check` passes on the mock helper.
- Unit suite runs RED (references not-yet-existing `worker-pool.mjs`), proving the contracts are encoded ahead of implementation.
- Integration suite exits 0 in default mode (live block skipped) and is RED only under `--live` until Plans 02/03 land.
- No real `claude` subprocess is spawned by the unit suite (dependency-injected stdio).
</verification>

<success_criteria>
- Three files exist: the mock-stdio helper, the unit suite, the live integration suite.
- Every Phase-62 requirement (POOL-01..04, GUARD-01) and the cancel seam maps to at least one named test case across the two suites (RESEARCH § Test Map fully covered).
- Default-mode test run is green (CI-safe); implementation-dependent assertions are RED pending Plans 02/03.
</success_criteria>

<output>
Create `.planning/phases/62-worker-pool-core-stream-json-transport/62-01-SUMMARY.md` when done.
</output>
