---
phase: 41-online-learning-adapter-post-hoc-resolution
plan: 04
type: execute
wave: 2
depends_on:
  - 41-02
files_modified:
  - /Users/Q284340/Agentic/km-core/src/adapters/online/reprojectFromOnlineStore.ts
  - /Users/Q284340/Agentic/km-core/src/adapters/online/checkpoint.ts
  - /Users/Q284340/Agentic/km-core/src/adapters/online/index.ts
  - /Users/Q284340/Agentic/km-core/tests/unit/adapters/reproject.test.ts
autonomous: true
requirements:
  - INT-01
user_setup: []

must_haves:
  truths:
    - "`reprojectFromOnlineStore(store, options): Promise<ReprojectResult>` is a top-level library function in `@fwornle/km-core/adapters/online` (sub-barrel) AND re-exported via the root barrel (Plan 06)."
    - "Reprojection is idempotent: running twice on the same source data produces the same final entity count (legacyId resolver de-duplicates per CF-D37). Idempotency scan reads `entity.legacyId.id` at the TOP LEVEL (per entity.ts:147 + backfill/index.ts:238 canonical placement)."
    - "Reprojection is checkpoint-resumable: crash mid-run + restart resumes from the last-processed source-row id (CF-D38 atomic temp-rename pattern)."
    - "Reprojection is read-only against A's data: never opens A's `observations.db` for writing; reads `.data/observation-export/*.json` via the `jsonExports` source path."
    - "Every reprojected entity has TOP-LEVEL `entity.legacyId = { system: 'A', id: <sqlite-id> }` per CF-D37, `entity.metadata.subsystem = 'online'` as a SEPARATE discriminator, and pre-stamped `metadata.provenance: EntityProvenance` (operator-supplied `legacyProvenance: ProvenanceStamp` becomes both `createdBy` and `lastConfirmedBy`)."
    - "Aggregation graph edges are written: Digest→Observation via `addRelation({ type: 'aggregates', from: digestId, to: observationId })`; Insight→Digest analogously."
    - "ReprojectResult includes a `warnings: string[]` field accumulating non-fatal events (missing-source-file, orphan edge references, etc.) so operator misconfig is visible — distinct from `errors[]` which is reserved for actual operation failures."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/adapters/online/reprojectFromOnlineStore.ts"
      provides: "Top-level library function `reprojectFromOnlineStore`"
      exports: ["reprojectFromOnlineStore", "ReprojectOptions", "ReprojectResult"]
    - path: "/Users/Q284340/Agentic/km-core/src/adapters/online/checkpoint.ts"
      provides: "Atomic checkpoint read/write for resumable reprojection"
      exports: ["readReprojectCheckpoint", "writeReprojectCheckpointAtomic", "ReprojectCheckpoint"]
    - path: "/Users/Q284340/Agentic/km-core/src/adapters/online/index.ts"
      provides: "Sub-barrel for adapter module"
  key_links:
    - from: "reprojectFromOnlineStore"
      to: "Plan 02 mappers (mapObservationRow / mapDigestRow / mapInsightRow)"
      via: "named import from './mapper.js'"
      pattern: "import \\{ mapObservationRow"
    - from: "reprojectFromOnlineStore"
      to: "GraphKMStore.putEntity({ skipOntologyCheck: true }) + addRelation"
      via: "trusted-path writes with pre-stamped provenance"
      pattern: "skipOntologyCheck: true"
    - from: "reprojectFromOnlineStore"
      to: "atomic checkpoint file"
      via: "writeReprojectCheckpointAtomic after every successful row write"
      pattern: "writeReprojectCheckpointAtomic"
---

<objective>
Land the library function `reprojectFromOnlineStore(store, opts)` that reads A's `.data/observation-export/*.json` files, runs Plan 02's mappers, and writes the resulting `Entity` + `Relation` records into a caller-supplied `GraphKMStore` — idempotent (legacyId resolver), checkpoint-resumable (atomic temp-rename), and read-only against A's writer.

Purpose: INT-01 SC#1 (typed-ontology-class query API over A's data) and SC#4 (the resolveEntities maintenance op is callable on A's adapter-fronted graph) — both require A's data to be materialized as KM-Core entities in a GraphKMStore. This is the reprojection step.

Output: 3 new source files + 1 test file under `/Users/Q284340/Agentic/km-core/src/adapters/online/`. Pure library function — no script entry point (Plan 07 owns that).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-02-PLAN.md
@.planning/phases/39-entity-data-model/39-CONTEXT.md
@.planning/phases/37-km-core-foundation/37-CONTEXT.md

<interfaces>
<!-- Key types and contracts from km-core that this plan imports. -->

From /Users/Q284340/Agentic/km-core/src/backfill/index.ts (THE primary analog — D-36 library-function pattern, canonical CF-D37 placement verified at lines 230-245):

  export interface BackfillResolver {
    (entity: Entity): {
      validFrom: string;
      legacyId?: { system: 'A' | 'B' | 'C'; id: string };
    };
  }
  export interface BackfillOptions {
    resolver: BackfillResolver;
    legacyProvenance: ProvenanceStamp;
    checkpointPath?: string;
    dryRun?: boolean;
  }
  export interface BackfillResult {
    scanned: number;
    stamped: number;
    skipped: number;
    runId: string;
  }
  export async function backfillEntityDataModel(
    store: GraphKMStore,
    options: BackfillOptions,
  ): Promise<BackfillResult>;

  CANONICAL legacyId stamping pattern (backfill/index.ts:235-243 — VERIFIED):
    const stampedEntity: Entity = {
      ...entity,
      validFrom: resolved.validFrom,
      legacyId: resolved.legacyId ?? entity.legacyId,   // ← TOP-LEVEL, not metadata
      metadata: {
        ...(entity.metadata ?? {}),
        provenance: syntheticProv,
      },
    };
    await store.putEntity(stampedEntity, { skipOntologyCheck: true });

From /Users/Q284340/Agentic/km-core/src/backfill/checkpoint.ts (THE checkpoint analog — lift VERBATIM with field renames):

  export interface Checkpoint {
    version: 1;
    runId: string;
    lastStampedId: string;
    scanned: number; stamped: number; skipped: number;
    updatedAt: string;
  }
  export async function writeCheckpointAtomic(path: string, cp: Checkpoint): Promise<void>;
  export async function readCheckpoint(path: string): Promise<Checkpoint | null>;
  // - ENOENT returns null (fresh start)
  // - Throws on JSON.parse SyntaxError (surfaces directly, NOT mis-coded as I/O error per WR-03)
  // - Throws on version mismatch with actionable message including path
  // - Atomic temp-file + rename in SAME directory (POSIX-atomic)

From /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts (Plan 02 output):

  export interface ObservationRow { id: string; summary: string; agent: string; project: string; quality: string; createdAt: string; digestedAt?: string | null; llm: string | null; modifiedFiles: string[] | null }
  export interface DigestRow { id: string; date: string; theme: string; summary: string; observationIds: string[]; agents: string[]; filesTouched: string[]; quality?: string; project?: string; createdAt?: string }
  export interface InsightRow { id: string; topic: string; summary: string; confidence: number; digestIds: string[]; project?: string; createdAt?: string }
  export function mapObservationRow(row: ObservationRow): Entity;  // stamps `entity.legacyId = { system: 'A', id: row.id }` TOP-LEVEL + `entity.metadata.subsystem = 'online'`
  export function mapDigestRow(row: DigestRow): Entity;            // same TOP-LEVEL legacyId placement
  export function mapInsightRow(row: InsightRow): Entity;          // same

From /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (public surface used by this plan):

  async putEntity(entity: Partial<Entity> & { name: string; entityType: string }, opts?: PutEntityOpts): Promise<Entity>;
  async addRelation(r: Relation): Promise<void>;
  async findRelations(filter: Partial<Relation>): Promise<Relation[]>;
  async findByOntologyClass(name: string, opts?: { includeSuperseded?: boolean }): Promise<Entity[]>;
  iterate(filter: FilterObject, opts?: { includeSuperseded?: boolean }): AsyncIterable<Entity>;
  async batch(ops: BatchOp[]): Promise<void>;

From /Users/Q284340/Agentic/km-core/src/ids/branded.ts (deterministic UUID derivation):

  // Plan 41-04 chooses: fresh mintEntityId() + idempotency via legacyId-based store lookup
  // (Option (b) per CONTEXT.md "Claude's Discretion") — no new npm dependency, no v5 helper.
  // The in-memory `Map<sqliteId, EntityId>` populated at startup hashes on
  // `entity.legacyId.id` (TOP LEVEL — NOT `entity.metadata.legacyId.id`).
  // Filter: `entity.legacyId?.system === 'A' && entity.metadata?.subsystem === 'online'`
  // catches A's online subsystem entities without false-positives from B/C entities
  // or non-online A entities that might land in the same store in future phases.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create checkpoint utility for reprojection (atomic temp-rename, ENOENT-as-fresh-start, version-1 schema)</name>
  <files>/Users/Q284340/Agentic/km-core/src/adapters/online/checkpoint.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/backfill/checkpoint.ts (the VERBATIM analog — read entire file)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on `checkpoint.ts` — "lift VERBATIM with rename")
    - .planning/phases/39-entity-data-model/39-REVIEW-FIX.md (WR-03 — JSON.parse outside the I/O catch so SyntaxError surfaces directly)
  </read_first>
  <behavior>
    - `writeReprojectCheckpointAtomic(path, cp)` creates parent dirs (recursive: true), writes to `${path}.tmp.${pid}.${ts}` in the same directory, then `fs.promises.rename(tmp, path)` — atomic.
    - `readReprojectCheckpoint(path)` returns `null` on ENOENT (fresh start), throws on JSON.parse SyntaxError (NOT swallowed), throws on `cp.version !== 1` with a message including the path (so operator can delete and restart).
    - Schema `ReprojectCheckpoint`: `{ version: 1; runId: string; lastProcessedSourceId: string; lastProcessedTable: 'observations' | 'digests' | 'insights'; scanned: { observations: number; digests: number; insights: number }; written: { observations: number; digests: number; insights: number; relations: number }; skipped: { observations: number; digests: number; insights: number }; updatedAt: string }`.
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/src/adapters/online/checkpoint.ts`. Lift the patterns from `src/backfill/checkpoint.ts` (do NOT import from there — copy the patterns with rename so the adapter module has zero coupling to the backfill module). Rename:
      - `Checkpoint` → `ReprojectCheckpoint`
      - `writeCheckpointAtomic` → `writeReprojectCheckpointAtomic`
      - `readCheckpoint` → `readReprojectCheckpoint`
      - `lastStampedId` → `lastProcessedSourceId`
      - Add `lastProcessedTable: 'observations' | 'digests' | 'insights'` so resume knows which source table the cursor was last in (per CONTEXT.md `<specifics>` processing order).
      - Replace `scanned/stamped/skipped: number` with the structured shape from the behavior block (split by source-table: observations/digests/insights/relations).
    JSON.parse stays OUTSIDE the I/O try/catch per WR-03 fix from Phase 39. Version-mismatch throw includes `path` in message.
    All relative imports use `.js` suffix. No `console.*` calls.
    NO tests for this file in this task — the consumer tests in Task 3 below exercise checkpoint behavior end-to-end (write → kill → resume).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && grep -c "export " src/adapters/online/checkpoint.ts | grep -qE "^[3-9]|^[1-9][0-9]" && grep -c "version: 1\\|version === 1\\|version !== 1" src/adapters/online/checkpoint.ts | grep -qvE "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/src/adapters/online/checkpoint.ts` exists.
    - `grep -nE "export interface ReprojectCheckpoint|export async function writeReprojectCheckpointAtomic|export async function readReprojectCheckpoint" src/adapters/online/checkpoint.ts` returns exactly 3 lines.
    - `grep -c "lastProcessedSourceId" src/adapters/online/checkpoint.ts` ≥ 1.
    - `grep -c "lastProcessedTable" src/adapters/online/checkpoint.ts` ≥ 1.
    - `grep -cE "console\\." src/adapters/online/checkpoint.ts` returns `0`.
    - `npx tsc --noEmit` exits 0.
    - The temp-rename idiom `${path}.tmp.` is present and `fs.promises.rename` is called (grep both).
  </acceptance_criteria>
  <done>Checkpoint utility lands with atomic write + ENOENT-fresh-start + version-mismatch-throws contract; no console.* calls; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reprojectFromOnlineStore library function</name>
  <files>/Users/Q284340/Agentic/km-core/src/adapters/online/reprojectFromOnlineStore.ts, /Users/Q284340/Agentic/km-core/src/adapters/online/index.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/backfill/index.ts (lines 41-260 — full library-function shape; especially path-traversal guard 130-138, per-row write + atomic checkpoint loop 187-258, pre-stamp + skipOntologyCheck pattern 226-245 — note `legacyId: resolved.legacyId ?? entity.legacyId` at TOP LEVEL on line 238)
    - /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts (Plan 02 output — mappers + row interfaces; stamps top-level legacyId already)
    - /Users/Q284340/Agentic/km-core/src/adapters/online/checkpoint.ts (Task 1 output above)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (lines 200, 325, 583, 696 — open, putEntity, addRelation, batch signatures)
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (Entity / EntityProvenance / ProvenanceStamp shape; line 147 — TOP-LEVEL legacyId field)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on `reprojectFromOnlineStore.ts` — verbatim deltas listed under "What to keep" + "What to adapt")
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md (D-47 / CF-D37 / CF-D38)
  </read_first>
  <behavior>
    - Signature: `export async function reprojectFromOnlineStore(store: GraphKMStore, options: ReprojectOptions): Promise<ReprojectResult>`.
    - `ReprojectOptions`: `{ sources: { sqlite?: string; jsonExports?: string }; legacyProvenance: ProvenanceStamp; dryRun?: boolean; checkpointPath?: string; chunkSize?: number; onProgress?: (e: ProgressEvent) => void }`.
    - **`sources` shape matches CONTEXT.md `<specifics>` verbatim** — both `sqlite` and `jsonExports` keys are optional. Plan 41-04 implements ONLY the `jsonExports` branch:
      * If `sources.sqlite` is set, throw `'reprojectFromOnlineStore: sources.sqlite is not yet supported in Phase 41; use sources.jsonExports'` immediately (before any other work). Phase 41 ships the JSON-exports path only; SQLite is reserved for a future phase.
      * If `sources.jsonExports` is omitted (or both keys are absent), throw `'reprojectFromOnlineStore: sources.jsonExports is required'`.
      * If only `sources.jsonExports` is set, proceed with the JSON-exports path.
    - `sources.jsonExports` is the directory containing `observations.json`, `digests.json`, `insights.json` (matches A's actual layout under `.data/observation-export/`).
    - `ReprojectResult`: `{ runId: string; scanned: { observations: number; digests: number; insights: number }; written: { observations: number; digests: number; insights: number; relations: number }; skipped: { observations: number; digests: number; insights: number }; warnings: string[]; dryRun: boolean }`.
    - **`warnings: string[]`** accumulates non-fatal events that operators should see but that don't indicate a failure of the operation itself: missing source file (`'missing-source-file: <path>'`), orphan edge reference (`'orphan-edge-ref: digest <id> references unknown observation <id>'`), version-mismatch fallback. Distinct from `errors[]` (Plan 06 uses errors[] for actual operation failures); `reprojectFromOnlineStore` does not have an `errors[]` because per-row errors that aren't `JSON.parse` failures are not currently tolerated — but warnings give operators early visibility into misconfig.
    - `legacyProvenance` is required (operator supplies — reprojected entities have no native provenance per 41-CONTEXT.md `<specifics>`).
    - Default `checkpointPath` is `.data/reproject-online-checkpoint.json` (resolved against `process.cwd()`).
    - Path-traversal guard: `checkpointPath` must not contain `..` segments — throws `'reprojectFromOnlineStore: checkpointPath must not contain \\'..\\' segments'` per T-39-04-01 / WR-style mitigation.
    - Processing order: observations FIRST, then digests, then insights (so digest-→observation edges and insight-→digest edges have their endpoints present in the store).
    - **Idempotency scan** at startup (CF-D37 canonical pattern):
      * Build `Map<sqliteId, EntityId>` by iterating `store.iterate({}, { includeSuperseded: true })`.
      * For each iterated entity, INCLUDE in map iff `entity.legacyId?.system === 'A' && entity.metadata?.subsystem === 'online'` (filters out B/C entities and non-online A entities that may share the store in future phases).
      * Key the map by `entity.legacyId.id` (TOP-LEVEL `.id`, NOT `.metadata.legacyId.id` — the metadata bag does NOT carry legacyId per Plan 02).
      * Value is the EntityId of the existing entity.
      * On second+ runs, if a source row's `id` is already in the map, skip (idempotent re-run).
    - Trusted-path write: every `putEntity` call passes `{ skipOntologyCheck: true }` because (a) the entity may have a legacy non-v7 id (mintEntityId is used for fresh entities, so most ids will be v7, but the trusted path is the only one that lets the caller pre-stamp `metadata.provenance` without the strict-path D-30 throw firing); (b) consistency with the backfill pattern.
    - Pre-stamp follow the backfill/index.ts:230-243 verbatim shape — stamp `entity.legacyId` at TOP LEVEL (per the mapper output it's already there, but if a caller-supplied resolver overrides, mirror `legacyId: resolved.legacyId ?? entity.legacyId`). Pre-stamp `metadata.provenance: EntityProvenance` before write: `{ createdBy: options.legacyProvenance, lastConfirmedBy: options.legacyProvenance, confirmationCount: 1 }`.
    - For each Digest row: after writing the Digest entity, iterate `row.observationIds`; for each observationId, look up the survivor EntityId via the in-memory map; if found, call `store.addRelation({ type: 'aggregates', from: digestEntityId, to: observationEntityId })`. If the observationId is unknown (orphan reference), push `'orphan-edge-ref: digest <id> references unknown observation <obsId>'` onto `result.warnings` AND log via `process.stderr.write('[km-core/adapters/online] digest <id> references unknown observation <id>; skipping edge\n')` and continue.
    - For each Insight row: write the Insight entity, then for each `row.digestIds` emit `addRelation({ type: 'aggregates', from: insightEntityId, to: digestEntityId })`. Orphan handling identical to Digest case.
    - Relation already-exists check: before adding a relation, call `store.findRelations({ from, to, type: 'aggregates' })` and skip if any match (idempotent re-run).
    - Per-row atomic checkpoint write after EACH successful entity write (memory-bound; matches T-39-04-04 mitigation in backfill).
    - Resume cursor: at start, call `readReprojectCheckpoint(checkpointPath)`; if non-null AND `cp.runId` matches `options.runId ?? defaultRunId`, skip rows in `cp.lastProcessedTable` whose `id` precedes `cp.lastProcessedSourceId`, then resume from there. Tables that come after `cp.lastProcessedTable` in the processing order are processed from scratch.
    - `dryRun: true`: load + map + log via `onProgress` but write NEITHER entities NOR relations NOR checkpoint.
    - All diagnostics via `process.stderr.write('[km-core/adapters/online] ...')`. NO `console.*`.
    - `onProgress` callback receives `{ phase: 'observations' | 'digests' | 'insights' | 'relations'; processed: number; total: number }` events. Optional — no-op if undefined.
    - When the source files do not exist (`jsonExports` dir missing or specific file ENOENT), the function pushes `'missing-source-file: <path>'` onto `result.warnings` AND logs via stderr `'[km-core/adapters/online] source file <path> not found; treating as empty'` and continues — does NOT throw. Operators may have only observations.json available on a fresh machine.
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/src/adapters/online/reprojectFromOnlineStore.ts` implementing the behavior above. Follow the backfill/index.ts shape (CF-D14 options-object, top-level free function — NO class). Use `node:fs` (sync read for small JSON files is fine; the export files are typically <50MB) for source-file reads; use `node:path` for path resolution and traversal guard. Use `randomUUID` from `node:crypto` (already used in the backfill module) for `runId` generation.
    For the legacyId resolver scan at startup, use `for await (const e of store.iterate({}, { includeSuperseded: true }))` (already async-iterable per D-18). Build the Map<sqliteId, EntityId> incrementally, filtering on `e.legacyId?.system === 'A' && e.metadata?.subsystem === 'online'`. Key by `e.legacyId.id` (TOP-LEVEL).
    For mintEntityId, import from `'../../ids/branded.js'` (or wherever the existing mint helper lives — read the file to confirm path).
    Define module-level constant `DEFAULT_CHECKPOINT_PATH = '.data/reproject-online-checkpoint.json'`.
    Path-traversal guard helper resolveCheckpointPath VERBATIM-ported from backfill/index.ts lines 130-138 (rename function + error prefix).
    Type-only imports for `Entity`, `EntityProvenance`, `ProvenanceStamp`, `Relation` per CF-D06.
    Create `/Users/Q284340/Agentic/km-core/src/adapters/online/index.ts` (sub-barrel) re-exporting:
      - `reprojectFromOnlineStore` runtime function
      - `mapObservationRow`, `mapDigestRow`, `mapInsightRow` (re-exports from `./mapper.js` — Plan 02 module)
      - `ObservationRow`, `DigestRow`, `InsightRow` types
      - `ReprojectOptions`, `ReprojectResult` types
      - `ReprojectCheckpoint` type
    Sub-barrel style matches `src/dedup/index.ts` (block comment + runtime exports + type exports separated).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit 2>&1 | tail -20 && grep -c "export async function reprojectFromOnlineStore" src/adapters/online/reprojectFromOnlineStore.ts && grep -c "skipOntologyCheck: true" src/adapters/online/reprojectFromOnlineStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/src/adapters/online/reprojectFromOnlineStore.ts` exists.
    - File `/Users/Q284340/Agentic/km-core/src/adapters/online/index.ts` exists.
    - `grep -cE "export async function reprojectFromOnlineStore" src/adapters/online/reprojectFromOnlineStore.ts` returns `1`.
    - `grep -cE "console\\." src/adapters/online/reprojectFromOnlineStore.ts src/adapters/online/index.ts` returns `0`.
    - `grep -c "skipOntologyCheck: true" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `1` (trusted-path writes use the BC-2 widening).
    - `grep -c "checkpointPath must not contain" src/adapters/online/reprojectFromOnlineStore.ts` returns `1` (path-traversal guard message present).
    - `grep -c "type: 'aggregates'" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `2` (aggregation edges for both Digest→Observation and Insight→Digest).
    - `grep -c "addRelation" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `1`.
    - `grep -c "process.stderr.write" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `1`.
    - `grep -E "from '\\.\\./" src/adapters/online/reprojectFromOnlineStore.ts | grep -vE "\\.js'" | grep -c .` returns `0` (CF-D06 .js suffix).
    - **Idempotency scan reads TOP-LEVEL legacyId:** `grep -cE "e\\.legacyId\\?\\.system\\s*===\\s*'A'|\\.legacyId\\.id" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `2` (the in-memory map filter+key reads top-level `.legacyId.{system,id}`, NOT `metadata.legacyId.*`).
    - **Idempotency filter includes subsystem check:** `grep -cE "metadata\\?\\.subsystem\\s*===\\s*'online'|metadata\\.subsystem === 'online'" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `1`.
    - **No metadata.legacyId reads:** `grep -cE "metadata\\.legacyId|metadata\\?.legacyId" src/adapters/online/reprojectFromOnlineStore.ts` returns `0` (legacyId is read at TOP LEVEL only per CF-D37).
    - **Warnings field present:** `grep -c "warnings" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `2` (declared in ReprojectResult + pushed-to in missing-source-file and orphan-edge branches).
    - **Sources discrimination:** `grep -c "sources.sqlite\\|sources\\.jsonExports" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `2` (both branches present — sqlite throws, jsonExports proceeds).
    - **Sqlite-not-supported guard:** `grep -c "sources.sqlite is not yet supported" src/adapters/online/reprojectFromOnlineStore.ts` ≥ `1`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>reprojectFromOnlineStore + sub-barrel + checkpoint module land; tsc clean; aggregation edge writes use 'aggregates' predicate; idempotency map built from store.iterate with TOP-LEVEL `legacyId` scan + subsystem filter; warnings[] field accumulates missing-file + orphan-ref events; sources.sqlite throws "not yet supported"; no console.* calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for reprojectFromOnlineStore — happy path + idempotency + dryRun + resume + path-traversal guard + warnings + sources.sqlite throw</name>
  <files>/Users/Q284340/Agentic/km-core/tests/unit/adapters/reproject.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/tests/unit/backfill.test.ts (analog — real GraphKMStore + tmpdir + makeStoreCtx + per-test cleanup; cumulative-vs-per-run counter semantics from CR-02 fix)
    - /Users/Q284340/Agentic/km-core/src/adapters/online/reprojectFromOnlineStore.ts (Task 2 output)
    - /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/{observations,digests,insights}.json (Plan 02 fixtures)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on reproject tests — same store-backed shape)
  </read_first>
  <behavior>
    - Test A — Happy path: fresh tmpdir store, jsonExports → Plan 02 fixtures. After `await reprojectFromOnlineStore(store, { sources: { jsonExports: fixtureDir }, legacyProvenance })`: `result.written.observations === fixture-observation-count`, `result.written.digests === fixture-digest-count`, `result.written.insights === fixture-insight-count`, `result.written.relations >= 2` (at least one Digest→Observation + one Insight→Digest edge), `result.warnings.length === 0`.
    - Test B — Idempotency: run reproject twice with the same args; second run's `result.written.observations === 0` AND `result.skipped.observations === fixture-observation-count`. Total store entity count after second run equals after first run. (Tests the TOP-LEVEL legacyId scan correctness.)
    - Test C — DryRun: call with `dryRun: true`; `result.written.observations === 0`; assert `await store.findByOntologyClass('Observation')` returns `[]` (nothing was written) AND no checkpoint file was created on disk.
    - Test D — Resume: simulate a partial run via a manually-written checkpoint with `lastProcessedSourceId = fixture[1].id` (second observation), `lastProcessedTable: 'observations'`; run reproject; assert observations 0+1 were skipped and observations 2..N written.
    - Test E — Path traversal guard: call with `checkpointPath: '../etc/passwd'`; expect throw with message containing `checkpointPath must not contain`.
    - Test F — Missing jsonExports dir: pass a path to a nonexistent directory; expect NO throw; `result.written.observations === 0`; `result.warnings` contains a string matching `/missing-source-file/`; stderr emits `'source file ... not found; treating as empty'` (use vi.spyOn on `process.stderr.write` to capture).
    - Test G — Provenance + canonical top-level legacyId stamping: after happy-path run, fetch one observation entity from the store and assert `entity.metadata.provenance.createdBy.runId === legacyProvenance.runId` AND `entity.legacyId.system === 'A'` AND `entity.legacyId.id === <observation row id>` (TOP-LEVEL) AND `entity.metadata.subsystem === 'online'`.
    - Test H — Aggregation edges: after happy-path run, call `await store.findRelations({ type: 'aggregates' })`; assert at least one edge has `from === digest-entity-id` and `to === observation-entity-id` matching the fixture's `observationIds` cross-reference.
    - Test I — Orphan edge reference: hand-author a digest fixture observationId that does NOT exist in observations.json; assert reproject does NOT throw; `result.warnings` contains a string matching `/orphan-edge-ref/`; no relation is written for the orphan reference.
    - Test J — sources.sqlite throws: call with `sources: { sqlite: '/tmp/anything.db' }`; assert throw with message containing `sources.sqlite is not yet supported`.
    - Test K — sources.jsonExports required: call with `sources: {}` (empty object); assert throw with message containing `sources.jsonExports is required`.
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/tests/unit/adapters/reproject.test.ts`. Use the existing `makeStoreCtx` / `cleanup` helper pattern from `backfill.test.ts` (copy verbatim — do not extract). Path to fixtures: `path.resolve(__dirname, '..', '..', 'fixtures', 'online-export')`.
    Use `vi.spyOn(process.stderr, 'write')` for Test F. Use `vi.restoreAllMocks()` in `afterEach`.
    For Test D's manual checkpoint: import `writeReprojectCheckpointAtomic` from the checkpoint module (Task 1 output) and write the partial checkpoint before invoking reproject. Verify by reading the fixture observations.json, taking the SECOND row's id, and writing a checkpoint with `lastProcessedSourceId = secondRowId`, `lastProcessedTable: 'observations'`, `runId = 'test-resume-runId'` (also pass the same `runId` to options if the signature supports it; otherwise omit and let the checkpoint be loaded by path-equality).
    For Test I: do not modify the shared fixture file (which other tests depend on); instead use a per-test tmpdir for the fixture set, copy observations.json minus one of the rows referenced by digests.json — the dangling reference triggers the orphan-warning path.
    `legacyProvenance` test fixture: `{ provider: 'reproject-test', model: 'phase-41-plan-04', runId: 'test-run-' + Date.now(), timestamp: new Date().toISOString() }`.
    All assertions use `expect(...).toBe(...)` / `toEqual(...)` / `toMatch(...)` for value equality; `toHaveBeenCalledWith` for spy assertions.
    Test G's legacyId assertions read `entity.legacyId.system` and `entity.legacyId.id` at TOP LEVEL (NOT via `entity.metadata.legacyId.*`).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx vitest run tests/unit/adapters/reproject.test.ts 2>&1 | grep -E "(passed|failed|Tests )" | tail -5 && npx vitest run 2>&1 | grep "Test Files" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/tests/unit/adapters/reproject.test.ts` exists.
    - `npx vitest run tests/unit/adapters/reproject.test.ts` exits 0 with ≥11 passing tests (A through K).
    - `grep -c "describe\\|test(" tests/unit/adapters/reproject.test.ts` ≥ 12 (1 describe + 11 tests).
    - `grep -c "checkpointPath must not contain" tests/unit/adapters/reproject.test.ts` ≥ 1 (Test E asserts the message).
    - **Top-level legacyId assertion present:** `grep -cE "entity\\.legacyId\\.system|\\.legacyId\\.system === 'A'" tests/unit/adapters/reproject.test.ts` ≥ 1 (Test G).
    - **No metadata.legacyId reads in test assertions:** `grep -cE "metadata\\.legacyId\\.|metadata\\?.legacyId\\." tests/unit/adapters/reproject.test.ts` returns `0`.
    - `grep -c "warnings" tests/unit/adapters/reproject.test.ts` ≥ 2 (Tests F, I).
    - `grep -c "orphan-edge-ref\\|orphan" tests/unit/adapters/reproject.test.ts` ≥ 1 (Test I).
    - `grep -c "sources.sqlite\\|sources: { sqlite" tests/unit/adapters/reproject.test.ts` ≥ 1 (Test J).
    - Full vitest suite still green: `npx vitest run` exits 0; total tests increase by ≥11 from pre-plan-04 baseline.
    - `grep -cE "console\\." tests/unit/adapters/reproject.test.ts` returns `0`.
  </acceptance_criteria>
  <done>11 tests pass; idempotency (via TOP-LEVEL legacyId scan) + dryRun + resume + path-traversal + missing-dir warning + provenance + canonical top-level legacyId + aggregation-edges + orphan-edge warning + sources.sqlite throw + sources.jsonExports required throw all green; zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reproject reads ← A's `.data/observation-export/*.json` | Source files are produced by A's `ObservationSanitizer`; data is already filtered for project-internal use. Operator-controlled path passed via options. |
| Reproject writes → caller-supplied GraphKMStore | Trusted-caller pattern; reproject is the writer for the projection. Idempotency enforced via top-level `entity.legacyId` resolver scan + `metadata.subsystem === 'online'` filter (CF-D37 canonical pattern). |
| Checkpoint file ← operator | Operator supplies `checkpointPath`; path-traversal guard rejects `..` segments to prevent escape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Tampering | path traversal via `checkpointPath: '../etc/passwd'` | mitigate | Path-traversal guard verbatim-ported from backfill/index.ts:130-138; rejects any `..` segment with a clear error. Test E pins. |
| T-41-04-02 | Information Disclosure | reprojection reads A's data which is already project-internal | accept | A's `ObservationSanitizer` upstream filters PII; per CONTEXT.md `<deferred>` PII hooks deferred to a future phase. Reproject inherits the filtering. |
| T-41-04-03 | Tampering | concurrent reproject runs corrupt the checkpoint | mitigate | Atomic temp-rename pattern (POSIX-atomic) prevents partial-write race. Operators are responsible for not running two reprojects simultaneously — out of scope to lock-file. |
| T-41-04-04 | Tampering | malformed source JSON crashes the reproject | mitigate | `JSON.parse` errors propagate (do NOT silently skip — operator wants to know); per-row validation via the mapper functions which tolerate null fields. Missing files emit stderr warn AND push to `result.warnings` (Test F). Orphan edge references emit stderr warn AND push to `result.warnings` (Test I) but do not fail the run. |
| T-41-04-05 | Denial of Service | huge source files exhaust memory | accept | Per-row write + atomic checkpoint after EACH write (matches T-39-04-04 backfill mitigation). Memory bound is the in-memory legacyId Map + one row at a time. Operators with >1M rows would split via per-table reprojection options in a future phase. |
| T-41-04-06 | Elevation of Privilege | reprojection opens A's SQLite for writing | mitigate | Reproject reads ONLY `.data/observation-export/*.json`. `sources.sqlite` is rejected with `'sources.sqlite is not yet supported in Phase 41'` (Test J). SC#2 (zero hot-path impact on A) is met by construction. |
| T-41-04-07 | Tampering | npm installs | mitigate | No npm installs — pure source addition. mintEntityId reuses existing km-core helper; no `uuid` v5 dependency added (Option (b) chosen in `<interfaces>`). |
</threat_model>

<verification>
- `reprojectFromOnlineStore` is callable; happy-path writes entities + aggregation edges.
- Idempotent on re-run (TOP-LEVEL legacyId scan + subsystem filter); resume cursor on partial-run checkpoint.
- DryRun is non-mutating.
- Path-traversal guard rejects `..` segments.
- Missing source files emit warnings AND do not throw; orphan edge references emit warnings AND do not throw.
- `sources.sqlite` throws "not yet supported"; `sources.jsonExports` required.
- TypeScript strict build clean.
- Full vitest suite green.
</verification>

<success_criteria>
- INT-01 SC#1 prerequisite met: A's observations/digests/insights materialize as KM-Core entities with `ontologyClass` ∈ {Observation, Digest, Insight}.
- INT-01 SC#2 met by construction: reproject is read-only against A's SQLite (only reads JSON exports — `sources.sqlite` is explicitly rejected).
- PIPE-02 prerequisite met: a GraphKMStore populated by reproject is the input fixture for `resolveEntities` integration tests (Plan 07).
- 11 unit tests pin happy-path + idempotency (via canonical CF-D37 top-level legacyId scan) + dryRun + resume + path-traversal + missing-dir-warning + provenance + aggregation-edges + orphan-edge-warning + sources.sqlite-throw + sources.jsonExports-required-throw.
</success_criteria>

<output>
Create `.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-04-SUMMARY.md` when done.
</output>
