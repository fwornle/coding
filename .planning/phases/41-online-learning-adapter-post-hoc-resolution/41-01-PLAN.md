---
phase: 41-online-learning-adapter-post-hoc-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/km-core/ontology/upper.json
  - /Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json
  - /Users/Q284340/Agentic/km-core/tests/unit/ontology-learning-artifacts.test.ts
autonomous: true
requirements:
  - INT-01
  - PIPE-02
user_setup: []

must_haves:
  truths:
    - "Files /Users/Q284340/Agentic/km-core/ontology/upper.json (live upper containing LearningArtifact class) and /Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json (lower with Observation/Digest/Insight) exist and are valid JSON."
    - "A consumer constructing `new OntologyRegistry({ ontologyDir: '<km-core>/ontology' })` finds class names 'LearningArtifact', 'Observation', 'Digest', 'Insight'."
    - "Observation, Digest, Insight each declare `extends: 'LearningArtifact'` and inherit the upper's shared properties via Phase 38 ONTO-02 property merging."
    - "Digest declares `aggregates: ['Observation']` and Insight declares `aggregates: ['Digest']` as graph-edge relationship predicates."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/ontology/upper.json"
      provides: "Live upper ontology (registry hardcodes this filename) containing the LearningArtifact upper class"
      contains: "\"LearningArtifact\""
    - path: "/Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json"
      provides: "Observation / Digest / Insight lower classes extending LearningArtifact"
      contains: "\"extends\": \"LearningArtifact\""
    - path: "/Users/Q284340/Agentic/km-core/tests/unit/ontology-learning-artifacts.test.ts"
      provides: "Unit tests proving registry discovery + extends chain"
  key_links:
    - from: "OntologyRegistry({ ontologyDir: '<km-core>/ontology' })"
      to: "upper.json + learning-artifacts.json"
      via: "readdirSync(ontologyDir) auto-discovery (Phase 38 ONTO-01)"
      pattern: "registry\\.isValidClass\\('Observation'\\) === true"
    - from: "Phase 38 ResolvedClass.extends chain"
      to: "Observation / Digest / Insight"
      via: "meta.extends + per-class extends resolution"
      pattern: "registry\\.parentChainOf\\('Observation'\\).*LearningArtifact"
---

<objective>
Land the `upper.json` (containing the LearningArtifact upper class) and `learning-artifacts.json` (lowers) files in the new (currently non-existent) `/Users/Q284340/Agentic/km-core/ontology/` directory and prove they are auto-discovered by `OntologyRegistry` with proper `extends`-chain resolution (Phase 38 ONTO-02). This is the orthogonal LearningArtifact axis (Observation/Digest/Insight) that the Phase 41 adapter populates and that PIPE-02 will scan against.

Purpose: PIPE-02's `resolveEntities(store, { classes: ['LearningArtifact'] })` resolves to all subclasses via the registry's class-walk; the adapter stamps `ontologyClass: 'Observation' | 'Digest' | 'Insight'` on every reprojected entity. Both depend on these classes existing in the registry. **Plan 01 ships the LearningArtifact ontology that PIPE-02's default-class resolution scans against** — without these files registered Plan 06's default branch fails. This is why this plan's `requirements` includes BOTH INT-01 (the adapter axis the ontology powers) AND PIPE-02 (the scan-by-class API whose default branch depends on these classes).

Output: 2 new ontology JSON files + 1 unit test file in km-core. /Users/Q284340/Agentic/km-core/ontology/ directory created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md
@.planning/phases/38-ontology-registry/38-CONTEXT.md

<interfaces>
<!-- Key types and contracts from km-core that this plan touches. Extracted from src/. -->
<!-- Executor must use these directly — no codebase exploration needed. -->

From /Users/Q284340/Agentic/km-core/src/types/ontology.ts (the ontology file shape):

  interface OntologyFile {
    meta: { name: string; version: string; extends?: string; description?: string };
    classes: Record<string, OntologyClassDef>;
  }
  interface OntologyClassDef {
    extends?: string;
    defaultLayer?: 'evidence' | 'pattern';  // optional
    description?: string;
    relationships?: Record<string, string[]>;
    properties?: Record<string, { type: string; required?: boolean; description?: string }>;
  }

From /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (public surface — verified at line ~220):

  class OntologyRegistry {
    constructor(opts: { ontologyDir: string; strict?: boolean });
    isValidClass(name: string): boolean;
    getClass(name: string): ResolvedClass | undefined;
    getAllClassNames(): string[];
    parentChainOf(className: string): ResolvedClass[];  // returns CHAIN of resolved-parent objects (NOT strings); does NOT include the class itself; stops on missing parent (registry.ts:220)
    provenanceOf(name: string): string | undefined;
    // upper.json is mandatory in the configured directory
  }

From /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/raas.json (analog — verbatim shape to follow):

  {
    "meta": { "name": "raas", "version": "1.0.0", "extends": "upper", "description": "..." },
    "classes": {
      "ArgoWorkflow": { "extends": "Pipeline", "defaultLayer": "evidence", "description": "...", "relationships": { ... }, "properties": { ... } }
    }
  }

KEY ANOMALY (per 41-PATTERNS Cross-Cutting note "Existing `ontology/` directory does not yet exist"):
The registry's `loadFromDisk()` (registry.ts:65) requires `upper.json` in the configured `ontologyDir`. The default fixture directory is `tests/fixtures/ontology/`. The live `ontology/` directory at the km-core package root DOES NOT YET EXIST. This plan's first file MUST be `ontology/upper.json` (NOT `ontology/learning-artifacts-upper.json`) because the registry hardcodes `'upper.json'` as the file name for the upper. The LearningArtifact upper class lives INSIDE `ontology/upper.json` as the sole class entry — the file's filename must be `upper.json`. The separate `ontology/learning-artifacts.json` lower extends `'upper'` and contains Observation/Digest/Insight — matches the `raas.json` precedent exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /Users/Q284340/Agentic/km-core/ontology/ directory + learning-artifacts upper + lower JSON files</name>
  <files>/Users/Q284340/Agentic/km-core/ontology/upper.json, /Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (lines 60-90 — loadFromDisk + upper.json hardcoding)
    - /Users/Q284340/Agentic/km-core/src/types/ontology.ts (full file — OntologyFile + ResolvedClass shape)
    - /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/upper.json (analog for `meta` + `classes` shape)
    - /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/raas.json (analog for `extends` semantics on meta + per-class)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md (D-48 — abstract LearningArtifact upper + 3 lowers)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (sections on `learning-artifacts-upper.json` + `learning-artifacts-lower.json`)
  </read_first>
  <action>
    Create directory `/Users/Q284340/Agentic/km-core/ontology/` (mkdir -p).
    Create file `/Users/Q284340/Agentic/km-core/ontology/upper.json` (this is the LIVE upper, distinct from the tests/fixtures/ontology/upper.json). The file MUST be named exactly `upper.json` because registry.ts:65 hardcodes that filename via `join(this.ontologyDir, 'upper.json')`. Shape:
      `meta: { name: 'upper', version: '1.0.0', description: 'KM-Core live upper ontology — LearningArtifact axis (Phase 41).' }`
      `classes: { LearningArtifact: { defaultLayer: 'evidence', description: 'Abstract upper class for online-learning artifacts (Observation/Digest/Insight tiers).', relationships: {}, properties: { id: { type: 'string', required: true }, project: { type: 'string' }, createdAt: { type: 'string', required: true }, validFrom: { type: 'string' }, validUntil: { type: 'string' }, quality: { type: 'string' } } } }`
    NB the `id`, `project`, `createdAt`, `validFrom`, `validUntil`, `quality` properties are the shared LearningArtifact axis per D-48; provenance is NOT declared as a property here (it lives on `Entity.metadata.provenance` per Phase 39 D-30, NOT the ontology schema).
    Create file `/Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json` with:
      `meta: { name: 'learning-artifacts', version: '1.0.0', extends: 'upper', description: 'Online-learning lower ontology — Observation/Digest/Insight tiers extending LearningArtifact (Phase 41 D-48).' }`
      `classes:` containing THREE entries:
        Observation: `extends: 'LearningArtifact'`, `defaultLayer: 'evidence'`, `description: 'A single ETM-captured intent/approach/artifacts/result observation from a coding session.'`, `relationships: {}`, `properties: { agent: { type: 'string' }, summary: { type: 'string', required: true }, modifiedFiles: { type: 'array' }, digestedAt: { type: 'string' }, llm: { type: 'string' } }`
        Digest: `extends: 'LearningArtifact'`, `defaultLayer: 'evidence'`, `description: 'A daily roll-up of observations summarising one project/session-window theme.'`, `relationships: { aggregates: ['Observation'] }`, `properties: { date: { type: 'string', required: true }, theme: { type: 'string' }, summary: { type: 'string' }, observationIds: { type: 'array' }, agents: { type: 'array' }, filesTouched: { type: 'array' } }`
        Insight: `extends: 'LearningArtifact'`, `defaultLayer: 'pattern'`, `description: 'A weekly synthesised insight derived from digests; long-term retained for prompt injection.'`, `relationships: { aggregates: ['Digest'] }`, `properties: { topic: { type: 'string', required: true }, summary: { type: 'string' }, confidence: { type: 'number' }, digestIds: { type: 'array' } }`
    Aggregation predicate name choice (per D-48 "Claude's discretion"): use `aggregates` — single-direction predicate from parent (Digest/Insight) to child (Observation/Digest). Verb matches the existing OKM upper-ontology style (`CONTAINS`/`PRODUCED_BY`) and is the simplest of the candidates listed in CONTEXT.md `<specifics>`.
    Files are valid JSON (no trailing commas, no comments). Both files have a final newline.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && node -e "const r=require('fs').readFileSync; JSON.parse(r('ontology/upper.json','utf8')); JSON.parse(r('ontology/learning-artifacts.json','utf8')); console.log('OK');" && grep -l '"extends": "LearningArtifact"' ontology/learning-artifacts.json && grep -l '"name": "learning-artifacts"' ontology/learning-artifacts.json</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/Q284340/Agentic/km-core/ontology/upper.json` exists and parses as JSON.
    - `/Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json` exists and parses as JSON.
    - `grep -c '"extends": "LearningArtifact"' /Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json` returns exactly `3` (Observation, Digest, Insight).
    - `grep -c '"aggregates"' /Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json` returns at least `2` (Digest and Insight declare aggregation).
    - `jq '.meta.name' /Users/Q284340/Agentic/km-core/ontology/upper.json` returns `"upper"` (registry hardcodes this filename + name).
    - `jq '.meta.extends' /Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json` returns `"upper"`.
    - `jq -r '.classes | keys | .[]' /Users/Q284340/Agentic/km-core/ontology/learning-artifacts.json | sort | tr '\n' ' '` returns `Digest Insight Observation ` (alphabetical).
  </acceptance_criteria>
  <done>Both ontology files exist, parse as JSON, declare the LearningArtifact axis with extends chain. No code changes outside `/Users/Q284340/Agentic/km-core/ontology/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests proving OntologyRegistry auto-discovers the new files + extends chain resolves</name>
  <files>/Users/Q284340/Agentic/km-core/tests/unit/ontology-learning-artifacts.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts (analog — the existing Phase 38 registry test suite; pattern for isolated-tmpdir tests against the live `ontology/` dir vs fixtures)
    - /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (registry public surface — isValidClass, getClass, parentChainOf, provenanceOf, getAllClassNames; parentChainOf returns ResolvedClass[], not string[] — verified at line 220)
    - /Users/Q284340/Agentic/km-core/src/types/ontology.ts (ResolvedClass shape)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on `ontology-learning-artifacts.test.ts` — there is no explicit analog row but follow the existing `tests/unit/ontology-registry.test.ts` style)
  </read_first>
  <behavior>
    - Test A: `new OntologyRegistry({ ontologyDir: '<package-root>/ontology' })` does NOT throw and the registry is non-empty.
    - Test B: `registry.isValidClass('LearningArtifact')` returns `true`.
    - Test C: `registry.isValidClass('Observation')`, `registry.isValidClass('Digest')`, `registry.isValidClass('Insight')` all return `true`.
    - Test D: `registry.parentChainOf('Observation').some(rc => rc.name === 'LearningArtifact')` is `true` (parentChainOf returns ResolvedClass[] — match by `.name`, NOT `.includes('LearningArtifact')`).
    - Test E: `registry.getClass('Digest')!.relationships` contains an `aggregates` key whose value includes `'Observation'`.
    - Test F: `registry.getClass('Insight')!.relationships.aggregates` includes `'Digest'`.
    - Test G: Inherited property merging — `registry.getClass('Observation')!.properties` includes BOTH `createdAt` (from LearningArtifact upper) AND `summary` (declared on Observation lower).
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/tests/unit/ontology-learning-artifacts.test.ts`. Use vitest `describe` / `test` / `expect` matching existing test style. Resolve the live ontology dir as `path.resolve(__dirname, '..', '..', 'ontology')` (test file is at `tests/unit/` so two levels up reaches the package root). Use `import.meta.url` + `fileURLToPath` if running under ESM (matches Phase 38 ontology-registry.test.ts pattern). Construct `new OntologyRegistry({ ontologyDir })` once at `describe` top level (cheap — registry is sync-load). Write the 7 tests A-G above with `.js` suffix on all relative imports per CF-D06. No mocks, no stubs — real registry against real files.

    CRITICAL: `parentChainOf` returns `ResolvedClass[]` (objects), NOT `string[]`. Test D MUST assert `chain.some(rc => rc.name === 'LearningArtifact')`, NOT `chain.includes('LearningArtifact')`. The same correction applies to any consumer code checking this chain (see Plan 06 Task 1 for the canonical pattern).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx vitest run tests/unit/ontology-learning-artifacts.test.ts 2>&1 | grep -E "(passed|failed|✓|✗)" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/tests/unit/ontology-learning-artifacts.test.ts` exists.
    - `cd /Users/Q284340/Agentic/km-core && npx vitest run tests/unit/ontology-learning-artifacts.test.ts` exits 0 with at least 7 passing tests.
    - `cd /Users/Q284340/Agentic/km-core && npx vitest run` exits 0 (no Phase 37/38/39/40 regressions — total test count goes UP by ≥7 from the pre-Plan-01 baseline).
    - `grep -c "isValidClass\\|parentChainOf\\|getClass" /Users/Q284340/Agentic/km-core/tests/unit/ontology-learning-artifacts.test.ts` returns ≥ 5 (assertions exercising the registry public surface).
    - `grep -vE '^[[:space:]]*//|^[[:space:]]*\\*' /Users/Q284340/Agentic/km-core/tests/unit/ontology-learning-artifacts.test.ts | grep -c "console\\." | grep -q "^0$"` (no `console.*` calls outside comments per no-console-log).
  </acceptance_criteria>
  <done>Vitest reports ≥7 passing tests; full vitest suite still green; new tests assert all 7 behaviors above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry ← ontology files | Untrusted JSON could carry malformed/missing fields; registry already validates shape via loadOntologyFile and emits stderr warn + skip (or strict throw). |
| New ontology directory ← filesystem | Files live alongside committed source; no external write surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | `ontology/upper.json` filename | mitigate | Filename `upper.json` is hardcoded in registry.ts:65 — operators cannot rename without breaking registry discovery (catches accidental rename in code review + test). |
| T-41-01-02 | Information Disclosure | new ontology files contain no PII | accept | Ontology schemas are domain abstractions, no user/project secrets. |
| T-41-01-03 | Denial of Service | malformed lower file → registry warn+skip | accept | Phase 38 D-29 atomicity already handles this: bad file is skipped with stderr warn, registry stays usable. Plan 01's tests prove both files are well-formed. |
| T-41-01-04 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — only adds JSON + test file. No npm install touched. |
</threat_model>

<verification>
- All files exist and parse as JSON.
- Live `/Users/Q284340/Agentic/km-core/ontology/` directory is created (distinct from `tests/fixtures/ontology/`).
- Full vitest suite is green (no regressions in Phase 37/38/39/40 tests).
- Phase 38 ONTO-02 `extends` resolution is exercised end-to-end.
</verification>

<success_criteria>
- Both ontology JSON files committed to km-core.
- 7-test vitest spec passes against the LIVE ontology directory.
- Phase 41 SC#3 prerequisite met: PIPE-02 can later scan `classes: ['LearningArtifact']` and find Observation/Digest/Insight subclasses via registry's class-walk.
- Requirements both INT-01 (the adapter axis) and PIPE-02 (the default-class scan branch) reachable from this plan's output.
</success_criteria>

<output>
Create `.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-01-SUMMARY.md` when done.
</output>
