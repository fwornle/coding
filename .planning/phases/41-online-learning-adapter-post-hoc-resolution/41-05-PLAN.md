---
phase: 41-online-learning-adapter-post-hoc-resolution
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts
  - /Users/Q284340/Agentic/km-core/tests/unit/maintenance/mergeEntities.test.ts
autonomous: true
requirements:
  - PIPE-02
user_setup: []

must_haves:
  truths:
    - "`mergeEntities(store, survivorId, duplicateIds, opts): Promise<MergeResult>` is a top-level library function in `@fwornle/km-core/maintenance` (sub-barrel landed by Plan 06)."
    - "All merge operations land atomically in a single `store.batch([...])` call (CF-D17): closes each duplicate's `validUntil` + adds SUPERSEDED_BY edges + rewires each duplicate's in/out edges to the survivor + folds duplicate's descriptionSegments + bumps survivor.confirmationCount."
    - "WR-02 single-successor invariant enforced: if any of `duplicateIds` already has a SUPERSEDED_BY successor edge, throw before mutating state."
    - "Self-loop guard: when an edge rewire would produce `from === to` (edge between duplicate and survivor, or duplicate self-loop), the edge is removed without adding a self-loop replacement."
    - "Edge type + metadata preserved verbatim on rewire (Relation.type unchanged; Relation.metadata copied)."
    - "Edges enumerated for rewire are de-duplicated by identity key (`(from, to, type, createdAt)`) before BatchOp construction so self-loops on a single duplicate are not counted twice."
    - "`mergeEntities` is reusable by Phase 42 (B migration) and Phase 43 (C migration) — both phases will delete their local mergeEntityGroup implementations in favor of this primitive (D-50a)."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts"
      provides: "Atomic merge primitive"
      exports: ["mergeEntities", "MergeOptions", "MergeResult"]
    - path: "/Users/Q284340/Agentic/km-core/tests/unit/maintenance/mergeEntities.test.ts"
      provides: "Unit tests for atomic merge + WR-02 + self-loop + edge-type preservation + idempotency"
  key_links:
    - from: "mergeEntities"
      to: "GraphKMStore.batch ([putEntity + addRelation + removeRelation])"
      via: "single atomic batch call covering all merge ops"
      pattern: "store\\.batch\\(\\["
    - from: "mergeEntities"
      to: "Phase 39 mergeDescriptionSegment helper"
      via: "import from '../segments/merge.js' — per-segment fold for D-39"
      pattern: "mergeDescriptionSegment"
    - from: "mergeEntities"
      to: "Phase 37 BatchOp.skipOntologyCheck per-op widening (CR-01)"
      via: "every BatchOp.putEntity carries `skipOntologyCheck: true`"
      pattern: "skipOntologyCheck: true"
---

<objective>
Land the atomic `mergeEntities(store, survivorId, duplicateIds, opts)` primitive that collapses N duplicate entities into a survivor with a single atomic write (CF-D17). The primitive is the canonical "merge two entities + their graph context" op for ALL THREE systems (A in Phase 41, B in Phase 42, C in Phase 43 — D-50a). Phase 41 lands the primitive; Phase 42 + 43 delete their local merge logic.

This plan ONLY ships `mergeEntities`. Plan 06 wraps it with `resolveEntities` (the per-class LLM-scan orchestrator) + the maintenance barrel + package.json exports. Splitting prevents one mega-plan; the two functions have orthogonal failure modes (atomicity vs. orchestration).

**Wave 1, no dependencies:** `mergeEntities` only uses already-shipped Phase 37/39 primitives (`store.batch`, `findRelations`, supersession path, `mergeDescriptionSegment` helper). It has no Phase 41 prerequisites; the consumer relationship (Plan 06 calls `mergeEntities`) is one-directional and resolved at Wave 3 via Plan 06's `depends_on` (not Plan 05's).

Purpose: PIPE-02 — without this primitive, `resolveEntities` (Plan 06) cannot turn LLM matches into actual graph mutations; surfaced matches would produce dangling edges or violate Phase 39 D-33 supersession atomicity.

Output: 1 source file (~150-200 LoC) + 1 test file. No barrel changes — Plan 06 owns the barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md
@.planning/phases/37-km-core-foundation/37-CONTEXT.md
@.planning/phases/39-entity-data-model/39-CONTEXT.md
@.planning/phases/39-entity-data-model/39-REVIEW-FIX.md

<interfaces>
<!-- Key types and contracts the merge primitive consumes. -->

From /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (D-33 supersession closure analog — lines 422-484):

  // The atomic two-write pattern from putEntity strict path:
  await this.batch([
    { type: 'putEntity', entity: closedOld, skipOntologyCheck: true },
    { type: 'putEntity', entity, skipOntologyCheck: true },
  ]);
  // Plus SUPERSEDED_BY edge: addRelation({ type: 'SUPERSEDED_BY', from: oldId, to: id });

  // WR-02 enforcement (lines 440-450): "if any SUPERSEDED_BY out-edge already
  // exists on oldId, throw 'Entity X already has a successor — cannot
  // supersede twice (WR-02 single-successor invariant)'"

From /Users/Q284340/Agentic/km-core/src/store/types.ts:

  type BatchOp =
    | { type: 'putEntity'; entity: Partial<Entity> & { name: string; entityType: string }; skipOntologyCheck?: boolean }
    | { type: 'deleteEntity'; id: EntityId }
    | { type: 'addRelation'; relation: Relation }
    | { type: 'removeRelation'; relation: Relation };

From /Users/Q284340/Agentic/km-core/src/segments/merge.ts (Phase 39 D-39 helper):

  export function mergeDescriptionSegment(entity: Entity, newSegment: DescriptionSegment): Entity;
  // Pure — returns a NEW Entity. Whitespace-normalized identical-text dedup (D-40).
  // WR-04 mitigation: drops caller-supplied confirmations on D-40 miss.

From /Users/Q284340/Agentic/km-core/src/types/entity.ts:

  interface Entity {
    id: EntityId; name: string; entityType: string; ontologyClass?: string;
    description?: string; metadata?: Record<string, unknown>;
    createdAt?: string; updatedAt?: string;
    validFrom?: string; validUntil?: string; supersedes?: EntityId;
    legacyId?: { system: 'A' | 'B' | 'C'; id: string };  // TOP-LEVEL per CF-D37
  }
  interface Relation { type: string; from: EntityId; to: EntityId; metadata?: Record<string, unknown>; createdAt?: string; validFrom?: string; validUntil?: string }
  interface ProvenanceStamp { provider: string; model: string; runId: string; timestamp: string }
  interface EntityProvenance { createdBy: ProvenanceStamp; lastConfirmedBy: ProvenanceStamp; confirmationCount: number }
  interface ResolutionRecord { mergedEntityId: EntityId; mergedAt: string; mergedBy: ProvenanceStamp; mergedReason?: string }

From Graphology (private graph access not available — must use store public API):

  // mergeEntities does NOT have access to this.graph; must use:
  //   - store.findRelations({ from: dupId }) / store.findRelations({ to: dupId })
  //     to enumerate edges
  //   - store.batch([...]) with addRelation + removeRelation ops to mutate
  // Phase 39 batch() supports addRelation + removeRelation BatchOps.

From OKM analog (`_work/rapid-automations/.../deduplicator.ts:910-933 migrateEdges`):

  // Algorithm: enumerate all edges where source=duplicate or target=duplicate;
  // for each, replace the duplicate endpoint with survivor; skip self-loops.
  // KM-Core variant: enumerate via two findRelations queries (from-side + to-side),
  // BUT: dedupe enumerated edges by identity key (from, to, type, createdAt) before
  // building BatchOps — otherwise a self-loop edge on a duplicate (already from=to)
  // would be returned by BOTH the from-side and to-side queries and counted twice.
  // Include all unique enumerated edges in the single mergeEntities batch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement mergeEntities atomic primitive</name>
  <files>/Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (lines 380-500 — putEntity strict path with D-33 supersession closure; especially the batch() call at 474-477 and the WR-02 guard around 440-450; lines 583-606 addRelation; lines 696-755 batch implementation; lines 609-625 findRelations)
    - /Users/Q284340/Agentic/km-core/src/store/types.ts (BatchOp shape; PutEntityOpts)
    - /Users/Q284340/Agentic/km-core/src/segments/merge.ts (mergeDescriptionSegment — pure helper signature + WR-04 behavior)
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (Entity / EntityProvenance / ProvenanceStamp / ResolutionRecord / DescriptionSegment)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (full section on `mergeEntities.ts` — composes 3 analogs: D-33 supersession closure + mergeDescriptionSegment + OKM migrateEdges; "What to keep" + "What to adapt" lists)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md (D-50 — 4-step primitive; D-50a — Phase 42/43 reuse)
    - .planning/phases/39-entity-data-model/39-REVIEW-FIX.md (CR-01 BatchOp.skipOntologyCheck per-op widening — every BatchOp must carry skipOntologyCheck:true; WR-02 single-successor enforcement)
  </read_first>
  <behavior>
    - Signature: `export async function mergeEntities(store: GraphKMStore, survivorId: EntityId, duplicateIds: EntityId[], opts: MergeOptions): Promise<MergeResult>`.
    - `MergeOptions`: `{ provenance: ProvenanceStamp; mergeSegments?: boolean (default true); reason?: string }`.
    - `MergeResult`: `{ survivorId: EntityId; duplicateIds: EntityId[]; edgesRewired: number; segmentsMerged: number; oldValidUntil: string; durationMs: number }`.
    - Pre-flight (BEFORE building the batch — these checks throw fast if violated):
      1. `survivor = await store.getEntity(survivorId)` — throw if undefined: `'mergeEntities: survivor <id> not found'`.
      2. `duplicates = await Promise.all(duplicateIds.map(id => store.getEntity(id)))` — throw on first undefined: `'mergeEntities: duplicate <id> not found'`.
      3. `if (duplicateIds.includes(survivorId)) throw 'mergeEntities: survivor cannot be in duplicateIds'`.
      4. `if (duplicateIds.length === 0) throw 'mergeEntities: duplicateIds must be non-empty'`.
      5. For each duplicate: check existing SUPERSEDED_BY out-edges via `store.findRelations({ from: dupId, type: 'SUPERSEDED_BY' })`. If non-empty → throw `'mergeEntities: duplicate <dupId> already has a successor — cannot supersede twice (WR-02 single-successor invariant)'`.
    - Build the batch (single `store.batch([...])` call):
      Common values: `oldValidUntil = new Date().toISOString()`.
      For each duplicate `dup`:
        a. Closed-duplicate write: `{ type: 'putEntity', entity: { ...dup, validUntil: oldValidUntil, updatedAt: oldValidUntil }, skipOntologyCheck: true }`.
        b. SUPERSEDED_BY edge: `{ type: 'addRelation', relation: { type: 'SUPERSEDED_BY', from: dup.id, to: survivor.id, createdAt: oldValidUntil, validFrom: oldValidUntil } }`.
        c. Edge rewires: enumerate edges via `store.findRelations({ from: dup.id })` and `store.findRelations({ to: dup.id })`. **DEDUPE the enumerated edges** by identity key `(from, to, type, createdAt)` before building BatchOps — otherwise a self-loop edge `dup → dup` is returned by both the from-side and to-side queries and would be removed twice / counted twice in `edgesRewired`. Use a `Set<string>` of identity keys to deduplicate.
           For each unique edge (excluding SUPERSEDED_BY edges added in step b — filter by type and timestamp to avoid rewiring the edge we just added):
           - newFrom = (edge.from === dup.id ? survivor.id : edge.from)
           - newTo = (edge.to === dup.id ? survivor.id : edge.to)
           - If newFrom === newTo (self-loop after rewire): emit ONLY `{ type: 'removeRelation', relation: <original edge> }` — drop without re-adding.
           - Otherwise emit BOTH `{ type: 'removeRelation', relation: <original edge> }` AND `{ type: 'addRelation', relation: { ...edge, from: newFrom, to: newTo } }`. Preserve edge.type AND edge.metadata verbatim.
           NB: enumerate BEFORE building the batch (queries are not part of the batch); the batch only contains add/remove ops, no further reads.
        d. Segment fold (if `opts.mergeSegments !== false`): if `dup.metadata?.descriptionSegments` is a non-empty array, iterate each segment and apply `mergeDescriptionSegment(survivor, segment)` accumulating onto the survivor object (track the running survivor state across all duplicate folds — call this `mergedSurvivor`).
      After processing all duplicates:
        e. Build the final survivor write: bump `confirmationCount`, update `lastConfirmedBy`, append `resolutionHistory` records (one per duplicate). The final `mergedSurvivor` has:
           `metadata.provenance = { createdBy: survivor.metadata.provenance.createdBy, lastConfirmedBy: opts.provenance, confirmationCount: (survivor.metadata?.provenance?.confirmationCount ?? 1) + duplicateIds.length }`.
           `metadata.resolutionHistory = [...(survivor.metadata.resolutionHistory ?? []), ...duplicateIds.map(dupId => ({ mergedEntityId: dupId, mergedAt: oldValidUntil, mergedBy: opts.provenance, ...(opts.reason ? { mergedReason: opts.reason } : {}) }))]`.
           Append `{ type: 'putEntity', entity: mergedSurvivor, skipOntologyCheck: true }` to the batch.
    - Execute: `await store.batch(allOps)`. The batch is all-or-nothing per D-17 (validates all ops first, mutates only if all pass).
    - Return `{ survivorId, duplicateIds, edgesRewired, segmentsMerged, oldValidUntil, durationMs: Date.now() - startTs }`.
    - All diagnostics via `process.stderr.write('[km-core/maintenance] ...')`. NO `console.*`.
    - All relative imports use `.js` suffix per CF-D06.
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/src/maintenance/` directory (it does not yet exist). Create file `mergeEntities.ts` implementing the behavior above. Top-level free function (CF-D14 options-object pattern); NO class. Type-only imports for `Entity`, `Relation`, `EntityProvenance`, `ProvenanceStamp`, `ResolutionRecord`, `DescriptionSegment`. Named import for `mergeDescriptionSegment` from `'../segments/merge.js'`. Named import for `BatchOp` from `'../store/types.js'`. Type-only import for `GraphKMStore` from `'../store/GraphKMStore.js'`, `EntityId` from `'../ids/branded.js'`.

    JSDoc on the function MUST reference:
    - D-50 (4-step primitive list).
    - D-50a (Phase 42/43 reuse — this primitive replaces B's `mergeEntityGroup` and C's local merge).
    - WR-02 single-successor enforcement (pre-flight check).
    - CR-01 BatchOp.skipOntologyCheck per-op widening usage.
    - CF-D17 atomicity (single batch covers all merge ops).
    - Edge-enumeration deduplication note: from-side + to-side queries can return the same self-loop edge twice; the enumeration MUST hash on `(from, to, type, createdAt)` and dedupe before counting/emitting BatchOps.

    The function MUST be a single function — NO helper exports. Internal helpers (e.g. `enumerateDuplicateEdges`, `dedupeEdgeIdentities`, `buildRewireOps`, `buildFinalSurvivor`) may exist but stay module-private.

    Edge-enumeration filter: when calling `store.findRelations({ from: dup.id })`, the result includes any pre-existing SUPERSEDED_BY edges. The pre-flight check (WR-02) already rejected duplicates with an existing successor, so by the time we enumerate, the duplicate has NO SUPERSEDED_BY out-edge yet. Therefore rewire-enumeration can blindly take all results without a type filter. However, edges TO the duplicate via SUPERSEDED_BY (i.e. the duplicate is itself a successor of an older predecessor) MUST be preserved: the rewire moves them to the survivor as-is (so the older predecessor now points at the survivor, preserving the supersession chain). This is correct behavior.

    Pitfall: the SUPERSEDED_BY edge ADDED in step (b) of the batch is a NEW edge being added in the same batch; it's not yet visible to `store.findRelations` at enumeration time (enumeration runs BEFORE batch). Therefore no special filter is required for the newly-added edges — they cannot be double-rewired.

    Edge dedupe identity key: `${edge.from}|${edge.to}|${edge.type}|${edge.createdAt ?? ''}`. Build a `Set<string>` of seen keys; skip duplicates. Document in JSDoc that this catches the from-side+to-side double-enumeration of self-loops.

    Verify the WR-02 enforcement check uses the same error message text as `GraphKMStore.ts` lines 440-450 (load-bearing per Phase 39 WR-02 fix): `'Entity ' + dupId + ' already has a successor — cannot supersede twice (WR-02 single-successor invariant)'`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit 2>&1 | tail -10 && grep -c "export async function mergeEntities" src/maintenance/mergeEntities.ts && grep -c "store.batch(" src/maintenance/mergeEntities.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts` exists.
    - `grep -c "export async function mergeEntities" src/maintenance/mergeEntities.ts` returns exactly `1`.
    - `grep -cE "store\\.batch\\(" src/maintenance/mergeEntities.ts` returns exactly `1` (single atomic batch call).
    - `grep -c "skipOntologyCheck: true" src/maintenance/mergeEntities.ts` ≥ `2` (every putEntity BatchOp carries the per-op flag).
    - `grep -c "WR-02 single-successor invariant" src/maintenance/mergeEntities.ts` ≥ `1` (error message matches GraphKMStore for consistency).
    - `grep -c "mergeDescriptionSegment" src/maintenance/mergeEntities.ts` ≥ `1` (segment fold uses Phase 39 helper).
    - `grep -c "resolutionHistory" src/maintenance/mergeEntities.ts` ≥ `1` (records appended per duplicate).
    - `grep -c "SUPERSEDED_BY" src/maintenance/mergeEntities.ts` ≥ `1` (forward index added per duplicate).
    - **Edge dedupe present:** `grep -cE "Set<string>|seenEdges|edgeIdentity|dedupeEdge" src/maintenance/mergeEntities.ts` ≥ `1` (identity-key dedupe for from-side + to-side queries).
    - `grep -cE "console\\." src/maintenance/mergeEntities.ts` returns `0`.
    - `grep -E "from '\\.\\./" src/maintenance/mergeEntities.ts | grep -vE "\\.js'" | grep -c .` returns `0` (CF-D06).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>mergeEntities lands as a single-batch atomic primitive; WR-02 enforced pre-flight; edges deduplicated by (from,to,type,createdAt) identity key before rewire-BatchOp build; rewires preserve type+metadata; segment fold via Phase 39 helper; resolutionHistory appended; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for mergeEntities — atomic merge + self-loop + edge-type + WR-02 + segment fold + resolutionHistory + edge-dedupe</name>
  <files>/Users/Q284340/Agentic/km-core/tests/unit/maintenance/mergeEntities.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/tests/unit/backfill.test.ts (analog — real GraphKMStore + tmpdir fixture; lines 30-95 makeStoreCtx pattern)
    - /Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts (Task 1 output)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (lines 422-484 D-33 supersession behavior — test expectations of validUntil + SUPERSEDED_BY)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on `mergeEntities.spec.ts` — 5 test cases listed)
  </read_first>
  <behavior>
    - Test A — Happy path: seed survivor S + 2 duplicates D1, D2. Seed edges: A→D1 (incoming), D1→B (outgoing), D2→C (outgoing). Call `mergeEntities(store, S.id, [D1.id, D2.id], { provenance })`. Assertions:
      - `D1.validUntil` and `D2.validUntil` are non-empty after the call.
      - SUPERSEDED_BY edges from D1→S AND from D2→S exist.
      - Survivor S now has edges A→S (rewired from A→D1), S→B (rewired from D1→B), S→C (rewired from D2→C).
      - D1 and D2 have zero non-SUPERSEDED_BY edges.
      - `result.edgesRewired === 3`, `result.duplicateIds.length === 2`.
      - `survivor.metadata.resolutionHistory.length === 2` with mergedEntityId values matching D1.id and D2.id.
      - `survivor.metadata.provenance.confirmationCount === <initial> + 2`.
    - Test B — Self-loop guard: seed survivor S + duplicate D1; seed an edge D1→S (already pointing at survivor). Call mergeEntities. Assertion: after merge, NO edge S→S exists (would be a self-loop). The edge is removed only.
    - Test C — Edge-type preservation: seed an edge D1→X with `type: 'custom-edge-type'` AND `metadata: { weight: 0.85, label: 'test' }`. After merge, the rewired edge S→X must have `type: 'custom-edge-type'` AND `metadata.weight === 0.85` AND `metadata.label === 'test'`.
    - Test D — WR-02 single-successor: seed duplicate D1 that ALREADY has a SUPERSEDED_BY edge to some other entity Z. Call mergeEntities. Assert it throws with message containing `'WR-02 single-successor invariant'` AND no state is mutated (S, D1 still in pre-call shape — verify by re-fetching after the throw and checking `validUntil` is undefined).
    - Test E — Survivor not found: call mergeEntities with a survivorId that does not exist. Assert throw with message containing `'mergeEntities: survivor'` AND `'not found'`.
    - Test F — Survivor in duplicateIds: call mergeEntities with `duplicateIds: [survivorId]`. Assert throw with message containing `'survivor cannot be in duplicateIds'`.
    - Test G — Empty duplicateIds: call mergeEntities with `duplicateIds: []`. Assert throw with message containing `'must be non-empty'`.
    - Test H — Segment fold: seed survivor S with one descriptionSegment ("text-A"); seed duplicate D1 with descriptionSegments [{text: "text-B"}, {text: "text-C"}]. After merge, survivor's descriptionSegments contain "text-A", "text-B", "text-C" (3 distinct entries). Use mergeDescriptionSegment's identity contract (D-40 whitespace-normalized identical-text test) — if all texts are distinct, all 3 appear.
    - Test I — mergeSegments: false: same setup as H but pass `mergeSegments: false`. After merge, survivor's descriptionSegments still contain ONLY "text-A" (D1's segments not folded).
    - Test J — Idempotent re-call throws (second call after first succeeds): call mergeEntities successfully, then call AGAIN with the same args. Assert the second call throws (D1 now has a SUPERSEDED_BY edge to S, WR-02 fires).
    - Test K — Self-loop edge on duplicate (dedupe coverage): seed duplicate D1 with a self-loop edge D1→D1 (type: 'self-ref'). Call mergeEntities; assertion: after merge, NO edge S→S exists (self-loop after rewire dropped); `result.edgesRewired === 0` (the self-loop is removed without a replacement, so it doesn't count as rewired). This pins that the from-side + to-side enumeration dedupe (Task 1 identity-key Set) prevents double-counting / double-removal of the same self-loop.
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/tests/unit/maintenance/` directory. Create file `mergeEntities.test.ts`. Use the `makeStoreCtx` / `cleanup` pattern from backfill.test.ts. Seed entities via `store.putEntity({ ...e, id: mintEntityId(...) }, { provenance })` (strict path, not trusted — provenance is provided so D-30 is satisfied; mintEntityId imports from `'../../../src/ids/branded.js'` or its alias). Add edges via `store.addRelation({ ... })`.
    For each assertion, use `expect(result.edgesRewired).toBe(3)` etc.; for "edge exists" queries, use `await store.findRelations({ from: X, to: Y, type: 'SUPERSEDED_BY' })` and `expect(rel.length).toBeGreaterThan(0)`.
    Tests run against a real GraphKMStore (no mocks). Each test uses its own `makeStoreCtx()` to isolate.
    `afterEach` performs `await ctx.store.close()` + `fs.rmSync(ctx.tmpdir, { recursive: true, force: true })`.
    `provenance` fixture: `{ provider: 'mergeEntities-test', model: 'phase-41-plan-05', runId: 'test-run-' + Date.now(), timestamp: new Date().toISOString() }`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx vitest run tests/unit/maintenance/mergeEntities.test.ts 2>&1 | grep -E "(passed|failed|Tests )" | tail -5 && npx vitest run 2>&1 | grep "Test Files" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/tests/unit/maintenance/mergeEntities.test.ts` exists.
    - `npx vitest run tests/unit/maintenance/mergeEntities.test.ts` exits 0 with ≥11 passing tests (A through K).
    - `grep -c "test(" tests/unit/maintenance/mergeEntities.test.ts` ≥ 11.
    - `grep -c "WR-02 single-successor" tests/unit/maintenance/mergeEntities.test.ts` ≥ 1 (Test D pins the error message).
    - `grep -c "resolutionHistory" tests/unit/maintenance/mergeEntities.test.ts` ≥ 1 (Test A asserts).
    - `grep -c "edgesRewired" tests/unit/maintenance/mergeEntities.test.ts` ≥ 1 (Test A asserts the count).
    - `grep -cE "self-loop|self-ref|D1→D1|D1.id.*D1.id" tests/unit/maintenance/mergeEntities.test.ts` ≥ 1 (Test K duplicate self-loop dedupe coverage).
    - `grep -cE "console\\." tests/unit/maintenance/mergeEntities.test.ts` returns `0`.
    - Full vitest suite still green: `npx vitest run` exits 0; total tests increase by ≥11 from pre-plan-05 baseline.
  </acceptance_criteria>
  <done>11 tests pass; happy-path + self-loop + edge-type + WR-02 + survivor-not-found + empty-args + segment-fold + mergeSegments:false + idempotent-throws + duplicate-self-loop-dedupe all green; zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mergeEntities ← caller-supplied EntityIds | Caller may pass malformed/missing ids; pre-flight check throws fast before any mutation. |
| mergeEntities → store.batch | Single atomic batch ensures all-or-nothing per CF-D17; partial state impossible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05-01 | Tampering | non-atomic merge leaves dangling edges | mitigate | Single `store.batch([...])` covers all merge ops (CF-D17). Phase 37 D-17 guarantees all-or-nothing on validation failure (Phase 1 validates ALL ops first; mutations only happen if all pass). Test A asserts atomicity by checking all expected post-conditions in one assertion block. |
| T-41-05-02 | Tampering | concurrent merges produce inconsistent state | accept | Single-threaded Node.js + single GraphKMStore writer (Phase 37 D-04 / D-17). Concurrent calls would serialize on the underlying LevelDB batch — last writer wins, no torn state. Operators are responsible for not invoking resolveEntities concurrently against the same store. |
| T-41-05-03 | Spoofing | survivor's resolutionHistory carries attacker-controlled ProvenanceStamp | mitigate | `opts.provenance` is the trust source; caller must supply it. mergeEntities does NOT mint a ProvenanceStamp; it propagates the caller's. resolveEntities (Plan 06) and the per-system scripts are the trust source for this stamp. |
| T-41-05-04 | Information Disclosure | dropped duplicate's descriptionSegments leak through to survivor | accept | This IS the intended behavior of segment fold (D-39). Phase 39 D-39 `mergeDescriptionSegment` already filters caller-supplied confirmations[] (WR-04 fix), so the merged provenance trail stays auditable. |
| T-41-05-05 | Repudiation | merge history is auditable | mitigate | `resolutionHistory` array on the survivor records each merge: `mergedEntityId`, `mergedAt`, `mergedBy: ProvenanceStamp`, `mergedReason?`. Test A asserts presence and count. |
| T-41-05-06 | Denial of Service | merging an entity with millions of edges exhausts memory | accept | Edge enumeration happens before batch (in-memory); a duplicate with >100k edges would be unusual and would emit a stderr warn via the consumer (Plan 06 `resolveEntities`). Out-of-scope to chunk; defer to a future operator-tooling phase if encountered. |
| T-41-05-07 | Tampering | WR-02 single-successor violation slips through | mitigate | Pre-flight check enumerates SUPERSEDED_BY out-edges via `store.findRelations` and throws BEFORE building the batch. Test D pins the error message. |
| T-41-05-08 | Tampering | self-loop edge double-removed corrupts batch | mitigate | Edge enumeration dedupes by `(from, to, type, createdAt)` identity key — from-side + to-side queries returning the same self-loop are collapsed to one BatchOp. Test K pins the self-loop dedupe path. |
| T-41-05-09 | Tampering | npm installs | mitigate | No npm installs — pure source addition. No new dependencies. |
</threat_model>

<verification>
- `mergeEntities` lands as a single-function module.
- All 11 unit tests pass.
- TypeScript strict build clean.
- Full vitest suite green.
- No console.* calls.
- All relative imports use .js suffix.
- Edge enumeration deduplication tested directly via Test K (duplicate self-loop).
</verification>

<success_criteria>
- PIPE-02 atomicity primitive shipped; Plan 06's resolveEntities can call it per surfaced match.
- D-50a future-reuse contract preserved (Phase 42 + 43 will delete their local mergeEntityGroup in favor of this primitive).
- WR-02 single-successor invariant enforced at the merge boundary (matches existing GraphKMStore D-33 closure behavior).
- Edge rewiring preserves edge.type and edge.metadata verbatim (Test C pins).
- Edge enumeration is dedupe-safe under from-side + to-side query overlap (Test K pins).
</success_criteria>

<output>
Create `.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-05-SUMMARY.md` when done.
</output>
