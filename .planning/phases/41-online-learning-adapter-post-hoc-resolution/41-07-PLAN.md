---
phase: 41-online-learning-adapter-post-hoc-resolution
plan: 07
type: execute
wave: 4
depends_on:
  - 41-06
files_modified:
  - /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs
  - /Users/Q284340/Agentic/km-core/tests/integration/reproject-resolve-merge.test.ts
autonomous: false
requirements:
  - INT-01
  - PIPE-02
user_setup: []

must_haves:
  truths:
    - "A new operator-facing CLI lives at `coding/scripts/reproject-online.mjs` that wraps `reprojectFromOnlineStore` + optionally chains `resolveEntities` from the Phase 41 km-core surface."
    - "CLI supports `--dry-run` (no writes), `--resolve` (chain resolveEntities after reproject), `--resolve-dry-run` (reproject for real then resolveEntities in dryRun mode), default flags (full reproject, no resolve)."
    - "Integration test (`tests/integration/reproject-resolve-merge.test.ts` in km-core) seeds a fixture export directory, runs `reproject → resolveEntities(dryRun:true) → resolveEntities(dryRun:false) → mergeEntities under the hood`, asserts all four ROADMAP success criteria are met end-to-end."
    - "Reproject is verified idempotent in the integration test (running twice produces same entity count)."
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/scripts/reproject-online.mjs"
      provides: "Per-system migration script with --dry-run / --resolve flags"
      contains: "reprojectFromOnlineStore"
    - path: "/Users/Q284340/Agentic/km-core/tests/integration/reproject-resolve-merge.test.ts"
      provides: "End-to-end integration test exercising all 4 SCs"
  key_links:
    - from: "coding/scripts/reproject-online.mjs"
      to: "@fwornle/km-core (root barrel)"
      via: "import { GraphKMStore, reprojectFromOnlineStore, resolveEntities } from '@fwornle/km-core'"
      pattern: "from '@fwornle/km-core'"
    - from: "integration test"
      to: "reprojectFromOnlineStore + resolveEntities + mergeEntities (indirectly)"
      via: "library function chain against real GraphKMStore + fixture export dir + mock LLMSemanticLayer"
      pattern: "reprojectFromOnlineStore.*resolveEntities"
---

<objective>
Land the operator-facing CLI (`coding/scripts/reproject-online.mjs`) and the end-to-end integration test in km-core. This plan is the user-visible deliverable: an operator can `node scripts/reproject-online.mjs --resolve-dry-run` to see what duplicates would be merged, then `--resolve` to actually apply them.

Plan 07 has a human-verify checkpoint AT THE END — the operator runs the script against the actual `.data/observation-export/*.json` in dry-run mode and confirms (a) reproject populates a tmpdir GraphKMStore without errors, (b) resolveEntities dry-run surfaces a sensible set of duplicates. This is the SC#1 + SC#4 functional gate.

Purpose: complete Phase 41 — both INT-01 and PIPE-02 are end-to-end exercised against real km-core code + fixtures + (in human-verify) real A data.

Output: 1 new script in coding/ + 1 integration test in km-core + 1 human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-04-PLAN.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-06-PLAN.md

<interfaces>
<!-- Key types and contracts this plan composes. -->

From /Users/Q284340/Agentic/coding/scripts/backfill-raw-observations.mjs (analog — script shape):

  #!/usr/bin/env node
  /**
   * <header JSDoc with Usage + Env>
   */
  import { createRequire } from 'node:module';
  import path from 'node:path';
  import process from 'node:process';

  const args = process.argv.slice(2);
  const DRY_RUN = args.includes('--dry-run');
  // ...
  // process.stderr.write(...) for diagnostics
  // process.stdout.write(JSON.stringify(result, null, 2)) for result

From @fwornle/km-core (root barrel after Plan 06):

  export { GraphKMStore } from '...';                            // Phase 37
  export { reprojectFromOnlineStore } from '...';                // Plan 04
  export type { ReprojectOptions, ReprojectResult } from '...';  // Plan 04
  export { resolveEntities, mergeEntities } from '...';          // Plan 05+06
  export type { ResolveOptions, ResolveResult, MergeOptions, MergeResult } from '...';
  export { LLMSemanticMatcher } from '...';                      // Phase 40 — for building the matcher
  export type { LLMClient } from '...';                          // Phase 40

From /Users/Q284340/Agentic/coding/scripts/backfill-raw-observations.mjs:41 (the LLM proxy endpoint convention):

  // Local LLM proxy on port 12435 — used by Plan 07 when --resolve is set
  const LLM_PROXY_URL = process.env.LLM_PROXY_URL || 'http://localhost:12435/v1/chat/completions';

From Plan 04's reprojectFromOnlineStore behavior:

  await reprojectFromOnlineStore(store, {
    sources: { jsonExports: <dir-path> },
    legacyProvenance: { provider, model, runId, timestamp },
    dryRun?, checkpointPath?, chunkSize?, onProgress?,
  });

From Plan 06's resolveEntities behavior:

  await resolveEntities(store, {
    llmMatcher: <LLMSemanticMatcher instance>,
    provenance: ProvenanceStamp,
    classes?, dryRun?, concurrency?, batchSize?, threshold?,
  });

KEY POINT: For the human-verify checkpoint, the operator runs `node scripts/reproject-online.mjs --dry-run` or `--resolve-dry-run` against the REAL `/Users/Q284340/Agentic/coding/.data/observation-export/` directory. NO writes touch A's SQLite (reproject reads JSON exports only, per Plan 04 SC#2). The output GraphKMStore lives in a TEMPORARY directory (`/tmp/km-core-reproject-<runId>/`) so the operator can inspect it without polluting `.data/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement reproject-online.mjs CLI script</name>
  <files>/Users/Q284340/Agentic/coding/scripts/reproject-online.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/scripts/backfill-raw-observations.mjs (analog — shebang, JSDoc header, arg parsing, env vars, stdout/stderr split, LLM proxy URL)
    - /Users/Q284340/Agentic/coding/.data/observation-export/observations.json (verify this file exists by `ls` — the script reads from this directory in default mode)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on `reproject-online.mjs` — full template)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md (`<specifics>` section — reprojection script entry point, opts shape)
    - /Users/Q284340/Agentic/km-core/dist/index.js (verify the root barrel built from Plan 06 actually exports the symbols the script imports — `node -e "import('/Users/Q284340/Agentic/km-core/dist/index.js').then(m => console.log(Object.keys(m).sort().join('\n')))"`)
  </read_first>
  <action>
    Create `/Users/Q284340/Agentic/coding/scripts/reproject-online.mjs`. Shebang `#!/usr/bin/env node`. JSDoc header block matching the template in 41-PATTERNS Section "reproject-online.mjs":
      ```
      Usage:
        node scripts/reproject-online.mjs                  # full reproject, no resolve
        node scripts/reproject-online.mjs --dry-run        # plan only, no writes (reproject dry-run)
        node scripts/reproject-online.mjs --resolve        # reproject + chain resolveEntities (writes)
        node scripts/reproject-online.mjs --resolve-dry-run # reproject (writes) + resolveEntities (dry-run only)

      Env:
        OBSERVATION_EXPORT_DIR  default ./.data/observation-export
        KM_GRAPH_DIR            default /tmp/km-core-reproject-<runId>
        LLM_PROXY_URL           default http://localhost:12435/v1/chat/completions
      ```

    Imports (use createRequire ONLY if needed for non-ESM modules; km-core is ESM so direct ESM import works):
      ```
      import path from 'node:path';
      import process from 'node:process';
      import { randomUUID } from 'node:crypto';
      import { GraphKMStore, reprojectFromOnlineStore, resolveEntities, LLMSemanticMatcher } from '@fwornle/km-core';
      ```
    NB: the script depends on a working symlink or npm install of `@fwornle/km-core` in coding/. Verify via `ls /Users/Q284340/Agentic/coding/node_modules/@fwornle/km-core` exists (it should from Phase 37 Plan 05 — cross-repo TS smoke setup, per STATE.md decision row).

    Arg parsing:
      `const args = new Set(process.argv.slice(2));`
      `const DRY_RUN = args.has('--dry-run');`
      `const RESOLVE = args.has('--resolve');`
      `const RESOLVE_DRY_RUN = args.has('--resolve-dry-run');`
      Validate: if BOTH `--dry-run` and `--resolve` are set, throw with usage message (mutually exclusive — use `--resolve-dry-run` instead).

    Resolve paths:
      ```
      const obsExportDir = process.env.OBSERVATION_EXPORT_DIR || path.resolve(process.cwd(), '.data/observation-export');
      const runId = randomUUID();
      const kmGraphDir = process.env.KM_GRAPH_DIR || path.join('/tmp', 'km-core-reproject-' + runId);
      const llmProxyUrl = process.env.LLM_PROXY_URL || 'http://localhost:12435/v1/chat/completions';
      ```

    Build ProvenanceStamp:
      ```
      const provenance = {
        provider: 'reproject-online',
        model: 'phase-41',
        runId,
        timestamp: new Date().toISOString(),
      };
      ```

    Construct store:
      ```
      const store = new GraphKMStore({
        dbPath: path.join(kmGraphDir, 'leveldb'),
        exportDir: path.join(kmGraphDir, 'exports'),
        debounceMs: 100,
      });
      await store.open();
      ```

    Run reproject:
      ```
      const reprojectResult = await reprojectFromOnlineStore(store, {
        sources: { jsonExports: obsExportDir },
        legacyProvenance: provenance,
        dryRun: DRY_RUN,
        checkpointPath: path.join(kmGraphDir, 'reproject-checkpoint.json'),
        onProgress: (e) => process.stderr.write('[reproject-online] ' + JSON.stringify(e) + '\n'),
      });
      ```

    If RESOLVE or RESOLVE_DRY_RUN (and not just DRY_RUN):
      Build LLMSemanticMatcher with an LLMClient that wraps `fetch(llmProxyUrl, { method: 'POST', ... })`. The LLMClient shape is per Plan 40-04: `{ complete: async (prompt: string) => ({ content: string }) }`. Wire the existing OpenAI-compatible chat-completions JSON payload (use backfill-raw-observations.mjs:60-120 as the analog for the request shape). Add a 60s timeout via `AbortController`.

      Then call:
      ```
      const resolveResult = await resolveEntities(store, {
        llmMatcher: new LLMSemanticMatcher({ client: llmClient }),
        provenance,
        dryRun: RESOLVE_DRY_RUN,
      });
      ```

    Output:
      ```
      const result = { reprojectResult, ...(RESOLVE || RESOLVE_DRY_RUN ? { resolveResult } : {}) };
      process.stdout.write(JSON.stringify(result, null, 2) + '\n');
      ```

    Cleanup:
      ```
      await store.close();
      process.stderr.write('[reproject-online] done; kmGraphDir=' + kmGraphDir + '\n');
      ```

    All script diagnostics use `process.stderr.write`. The RESULT is the only thing on `process.stdout`. This follows the `coding/scripts/*.mjs` convention.

    Error handling: top-level try/catch around the whole script body; on error, write `JSON.stringify({ error: err.message, stack: err.stack }, null, 2)` to stderr, exit code 1.
  </action>
  <verify>
    <automated>node -e "process.argv[2]='--help'; require('child_process').spawnSync('node', ['--check', '/Users/Q284340/Agentic/coding/scripts/reproject-online.mjs'], { stdio: 'inherit' });" 2>&1; node --check /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs && echo "syntax OK"; grep -c "reprojectFromOnlineStore\\|resolveEntities\\|GraphKMStore" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` exists.
    - First line is `#!/usr/bin/env node`.
    - `node --check /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` exits 0 (parse syntax clean).
    - `grep -c "reprojectFromOnlineStore" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 1.
    - `grep -c "resolveEntities" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 1.
    - `grep -c "LLMSemanticMatcher" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 1.
    - `grep -c "GraphKMStore" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 1.
    - `grep -c "process.stderr.write" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 1.
    - `grep -c "process.stdout.write" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 1.
    - `grep -cE "console\\." /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` returns `0`.
    - `grep -c "from '@fwornle/km-core'" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 1 (single root-barrel import).
    - `grep -c "--dry-run\\|--resolve\\|--resolve-dry-run" /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs` ≥ 3 (all three flag patterns present).
  </acceptance_criteria>
  <done>Script lands with syntax-clean parse; root-barrel imports; flag parsing; stderr/stdout split; process.stderr.write for diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end integration test in km-core — reproject → resolveEntities(dryRun) → resolveEntities(real)</name>
  <files>/Users/Q284340/Agentic/km-core/tests/integration/reproject-resolve-merge.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/tests/integration/pipeline-supersession.test.ts (analog — real GraphKMStore + tmpdir + supersession assertions; per-test makeFixture pattern)
    - /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/*.json (Plan 02 fixtures — input data for the integration test)
    - /Users/Q284340/Agentic/km-core/src/adapters/online/reprojectFromOnlineStore.ts (Plan 04 output)
    - /Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts (Plan 06 output)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on `reproject-resolve-merge.test.ts` — end-to-end shape)
    - .planning/ROADMAP.md (Phase 41 SC#1-4 — the integration test is the goal-backward verification)
  </read_first>
  <behavior>
    - Test 1 (SC#1 verification — typed ontology classes): seed fixture export dir; build a store with `ontologyDir = path.resolve(__dirname, '..', '..', 'ontology')` so the LearningArtifact subclass registry is available; run `reprojectFromOnlineStore`; assert `await store.findByOntologyClass('Observation')`, `await store.findByOntologyClass('Digest')`, `await store.findByOntologyClass('Insight')` each return ≥1 entity AND each entity has `ontologyClass === '<expected-class>'` AND **`entity.legacyId.system === 'A'` (TOP-LEVEL per CF-D37)** AND `entity.metadata.subsystem === 'online'`.
    - Test 2 (SC#2 verification — read-only against A): reprojectFromOnlineStore was given a `sources.jsonExports` path. The fixture path is `tests/fixtures/online-export/` which contains ONLY JSON files (no SQLite). Assert via `fs.readdirSync(fixtureDir)` returns only `.json` files (no `.db`). This is a structural assertion that reproject CANNOT have opened a SQLite writer — the path doesn't contain one. SC#2 is met by construction (Plan 04 only implements the `jsonExports` source per CONTEXT.md `<specifics>`; `sources.sqlite` is rejected).
    - Test 3 (SC#3 verification — cross-batch duplicates collapse): the fixture has at least one deliberate duplicate pair in observations.json (per Plan 02 Task 1 behavior). After reproject, `await store.findByOntologyClass('Observation')` returns N entities. Build a mock LLMSemanticLayer that returns `{ matched: true, confidence: 0.95, matchedTo: { name: <the-duplicate.name>, description: <the-duplicate.description> } }` for the deliberate pair. Call `resolveEntities(store, { llmMatcher, provenance, classes: ['Observation'], dryRun: false })`. Assert: `result.merges.length === 1`, after the call `await store.findByOntologyClass('Observation')` returns N-1 entities (duplicate superseded; active-only default excludes), `await store.getSupersessionChain(duplicate.id)` includes the survivor.
    - Test 4 (SC#4 verification — API callable against adapter-fronted graph): the GraphKMStore IS the adapter-fronted graph (it was populated by reproject in Test 1). Same call sequence as Test 3 IS Test 4 verification — `resolveEntities` runs against the adapter-populated store identically to how it would run against a B/C-populated store. Assert this explicitly by adding a comment-block test that re-affirms the merge in Test 3 worked against an adapter-populated store (no separate fixture).
    - Test 5 (Idempotency): run `reprojectFromOnlineStore` twice with the same args; second run's `result.written.observations === 0` AND `await store.findByOntologyClass('Observation')` returns the same count both times.
    - Test 6 (DryRun resolveEntities returns plan without writes): seed reproject result; call `resolveEntities(store, { llmMatcher, provenance, dryRun: true })`. Assert: `result.dryRun === true`, `result.merges.length >= 1`, then `await store.findByOntologyClass('Observation')` is UNCHANGED from before the call.
    - Test 7 (Aggregation edges via reproject): after reproject, `await store.findRelations({ type: 'aggregates' })` returns ≥ 1 relation (Digest→Observation or Insight→Digest from the fixture cross-references).
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/tests/integration/reproject-resolve-merge.test.ts`. Use the integration-test patterns from `tests/integration/pipeline-supersession.test.ts` (per-test makeFixture, real GraphKMStore, tmpdir per test, beforeEach/afterEach).
    Construct the store WITH `ontologyDir = path.resolve(__dirname, '..', '..', 'ontology')` so the live Plan 01 ontology directory is loaded; the `ontology` getter returns the registry; resolveEntities can default `classes` to LearningArtifact subclasses.
    Mock LLMSemanticLayer: `const llmMatcher = { match: vi.fn().mockImplementation(async (subject, candidates) => { /* return matched true for known deliberate-duplicate pair, false otherwise */ }) }`. Use the fixture's specific duplicate pair (read from fixture JSON to derive the deliberate pair's `summary` field — both summaries differ only in trivial whitespace/punctuation per Plan 02 Task 1 spec). The mock's matchedTo MUST be `{ name: <name-of-duplicate-in-pool>, description: <description-of-duplicate-in-pool> }` so the Plan 06 reverse lookup succeeds.
    Test 1's legacyId assertion reads `entity.legacyId.system` at TOP LEVEL (NOT `entity.metadata.legacyId.*`).
    `provenance` fixture: `{ provider: 'reproject-resolve-merge-test', model: 'phase-41-plan-07', runId: 'test-run-' + Date.now(), timestamp: new Date().toISOString() }`.
    Each test is self-contained — fresh tmpdir, fresh store per `beforeEach`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx vitest run tests/integration/reproject-resolve-merge.test.ts 2>&1 | grep -E "(passed|failed|Tests )" | tail -5 && npx vitest run 2>&1 | grep "Test Files" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/tests/integration/reproject-resolve-merge.test.ts` exists.
    - `npx vitest run tests/integration/reproject-resolve-merge.test.ts` exits 0 with ≥7 passing tests.
    - `grep -c "test(" tests/integration/reproject-resolve-merge.test.ts` ≥ 7.
    - `grep -c "SC#1\\|SC#2\\|SC#3\\|SC#4" tests/integration/reproject-resolve-merge.test.ts` ≥ 4 (each SC referenced by comment in test header for goal-backward traceability).
    - `grep -c "reprojectFromOnlineStore" tests/integration/reproject-resolve-merge.test.ts` ≥ 1.
    - `grep -c "resolveEntities" tests/integration/reproject-resolve-merge.test.ts` ≥ 2 (dryRun + real).
    - `grep -c "findByOntologyClass" tests/integration/reproject-resolve-merge.test.ts` ≥ 3 (the three LearningArtifact subclasses tested).
    - `grep -c "type: 'aggregates'" tests/integration/reproject-resolve-merge.test.ts` ≥ 1 (Test 7).
    - **Top-level legacyId assertion:** `grep -cE "entity\\.legacyId\\.system|\\.legacyId\\.system === 'A'" tests/integration/reproject-resolve-merge.test.ts` ≥ 1 (Test 1).
    - Full vitest suite still green: `npx vitest run` exits 0; total tests increase by ≥7 from pre-plan-07 baseline.
  </acceptance_criteria>
  <done>7 integration tests pass; each ROADMAP SC has a dedicated assertion block; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify reproject-online.mjs against live A data (--dry-run + --resolve-dry-run)</name>
  <what-built>
    A CLI script at `coding/scripts/reproject-online.mjs` that reads A's `.data/observation-export/*.json` files into a temporary GraphKMStore, optionally chaining `resolveEntities` against a local LLM proxy. Plus an integration test in km-core that exercises the full reproject → resolveEntities → mergeEntities chain end-to-end against synthetic fixtures.

    All write operations target a fresh `/tmp/km-core-reproject-<runId>/` directory; A's SQLite and `.data/observation-export/` files are read-only inputs.
  </what-built>
  <how-to-verify>
    1. **Sanity — km-core build is fresh:** **REQUIRED:** `cd /Users/Q284340/Agentic/km-core && npx tsc` MUST exit 0 BEFORE proceeding to steps 2-7. If `dist/` is stale, the script may run old code and produce confusing results. Confirm `ls /Users/Q284340/Agentic/km-core/dist/index.js /Users/Q284340/Agentic/km-core/dist/maintenance/index.js /Users/Q284340/Agentic/km-core/dist/adapters/online/index.js` all resolve to existing files. Do NOT skip this step — operator-side stale `dist/` is the most common cause of human-verify confusion (per checker W9).
    2. **Confirm node_modules link:** `ls /Users/Q284340/Agentic/coding/node_modules/@fwornle/km-core` should resolve (Phase 37 Plan 05 set up the symlink). If missing, link via `(cd /Users/Q284340/Agentic/coding && npm link @fwornle/km-core)` or whatever your existing convention is — DO NOT install from npm (km-core is unpublished).
    3. **Confirm live ontology dir:** `ls /Users/Q284340/Agentic/km-core/ontology/{upper,learning-artifacts}.json` should both resolve (Plan 01 output).
    4. **Reproject dry-run:** `cd /Users/Q284340/Agentic/coding && node scripts/reproject-online.mjs --dry-run 2>/tmp/reproject-stderr.log` — wait for completion. Inspect stdout (the JSON result) and stderr (the [reproject-online] progress events).
       Expected:
       - Stdout JSON contains `reprojectResult.written.observations === 0` (dry-run), `reprojectResult.scanned.observations === <actual N from .data/observation-export/observations.json>` (positive number).
       - Stderr shows `[reproject-online] {"phase":"observations",...}` events.
       - No `[reproject-online] ERROR` lines.
       - Exit code 0.
    5. **Reproject for real + resolve dry-run:** `cd /Users/Q284340/Agentic/coding && node scripts/reproject-online.mjs --resolve-dry-run 2>/tmp/reproject-resolve-stderr.log` — wait for completion (this DOES actually write to a /tmp store but does NOT actually merge anything; it just plans).
       Expected:
       - Stdout JSON contains `reprojectResult.written.observations > 0` AND `resolveResult.dryRun === true`.
       - `resolveResult.merges[]` may be empty (if no LLM-flagged duplicates) or a small list of high-confidence matches; either is acceptable.
       - `resolveResult.errors[]` should be empty OR contain only timeouts/transient LLM errors (NOT structural errors).
       - The tmpdir `/tmp/km-core-reproject-<runId>/` contains `leveldb/` and `exports/` directories with non-empty contents (verify via `ls -la`).
    6. **No mutation of A's data:** `git status` in `/Users/Q284340/Agentic/coding/` shows ZERO changes to `.data/observation-export/` (SC#2 sanity).
    7. **Cleanup:** `rm -rf /tmp/km-core-reproject-*` once verified.

    Report which of (1), (4), (5) succeeded; paste relevant stderr/stdout excerpts if any failure. Also confirm step (6).
  </how-to-verify>
  <resume-signal>Type "approved" if all steps succeed, or describe the failure (e.g. "step 4 failed: ENOENT on .data/observation-export/observations.json")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script ← user CLI args | `--dry-run` / `--resolve` flag combinations; mutually exclusive flags throw with usage message. |
| Script ← env vars | `OBSERVATION_EXPORT_DIR` / `KM_GRAPH_DIR` / `LLM_PROXY_URL`; operator controls these. |
| Script → tmpdir GraphKMStore | Writes to `/tmp/km-core-reproject-<runId>/` only; never `.data/`. |
| Script ← A's `.data/observation-export/*.json` | Read-only access; reproject opens no writer on observations.db (Plan 04 only implements `jsonExports` source; `sources.sqlite` rejected). |
| Script → local LLM proxy (port 12435) | Outbound HTTPS in `--resolve` mode only; existing proxy setup per coding/CLAUDE.md is the trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-01 | Tampering | accidental write to .data/observation-export/ | mitigate | Script's KM_GRAPH_DIR default is `/tmp/km-core-reproject-<runId>/`, NEVER overlapping with `.data/`. Reproject reads only the JSON exports (Plan 04 SC#2). Human-verify step 6 confirms zero git diff. |
| T-41-07-02 | Tampering | concurrent script invocations clobber each other | accept | Each run uses a fresh runId-suffixed tmpdir; no shared mutable state. Operators may run multiple --dry-run invocations safely. |
| T-41-07-03 | Information Disclosure | LLM proxy receives A's project-internal data when --resolve is set | accept | A's `ObservationSanitizer` upstream filters PII; LLM proxy is the same trust boundary as the existing UKB pipeline. Per CONTEXT.md `<deferred>`, PII hooks are deferred. |
| T-41-07-04 | Tampering | malicious LLM output causes mass-merge | mitigate | Plan 06 confidence threshold (0.70) gates accept; `--resolve-dry-run` lets operator inspect proposed merges before applying. |
| T-41-07-05 | Denial of Service | huge `.data/observation-export/observations.json` exhausts memory | accept | Plan 04 reproject is per-row; memory bound is the in-memory legacyId Map. For >100k observations, operators can use `chunkSize` option (Plan 04). Default operation is fine for the current `.data/` size (verified <50MB). |
| T-41-07-06 | Repudiation | merge decisions are auditable | mitigate | `resolveResult.merges[]` records every merge; `resolveResult.errors[]` records every failure (including LLM unmatchable hallucinations from Plan 06). Stdout is JSON; operators can persist for audit. Provenance stamp threaded through ensures each survivor's `resolutionHistory` contains an auditable trail (Plan 05). |
| T-41-07-07 | Tampering | stale km-core dist/ runs old code | mitigate | Human-verify step 1 (REQUIRED) runs `npx tsc` exit-0 BEFORE the script runs — ensures the script under test exercises the latest Plan 06 surface (resolveEntities + reprojectFromOnlineStore + maintenance sub-barrel). Skipping step 1 was the W9 risk path. |
| T-41-07-08 | Tampering | npm installs | mitigate | No new npm dependencies. Script depends on existing `@fwornle/km-core` link/install (Phase 37 Plan 05 set up). Integration test uses existing vitest + fixtures only. |
</threat_model>

<verification>
- CLI script syntax-checks clean.
- Integration test in km-core passes with ≥7 tests covering each ROADMAP SC.
- Human-verify confirms script runs end-to-end against live A data without mutating `.data/`.
- Human-verify step 1 (npx tsc exit 0) pinned BEFORE script runs.
- Full vitest suite green.
</verification>

<success_criteria>
- INT-01 SC#1 end-to-end verified: typed-ontology-class query API works against reprojected entities (Test 1; canonical TOP-LEVEL `legacyId` pinned in assertion).
- INT-01 SC#2 verified by construction: reproject reads JSON exports only; SQLite untouched (Test 2 + human-verify step 6).
- PIPE-02 SC#3 verified: synthetic-duplicate-pair collapses through resolveEntities (Test 3).
- PIPE-02 SC#4 verified: resolveEntities runs against adapter-fronted graph (Test 4 — implicit in Test 3).
- Operator workflow proven: `--dry-run` and `--resolve-dry-run` give safe inspection paths before applying merges.
</success_criteria>

<output>
Create `.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-07-SUMMARY.md` when done.
</output>
