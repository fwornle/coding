---
phase: 41-online-learning-adapter-post-hoc-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts
  - /Users/Q284340/Agentic/km-core/tests/unit/adapters/online-mapper.test.ts
  - /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/observations.json
  - /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/digests.json
  - /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/insights.json
autonomous: true
requirements:
  - INT-01
user_setup: []

must_haves:
  truths:
    - "A pure function `mapObservationRow(row: ObservationRow): Entity` exists and returns a KM-Core Entity with `ontologyClass: 'Observation'`."
    - "A pure function `mapDigestRow(row: DigestRow): Entity` exists with `ontologyClass: 'Digest'`."
    - "A pure function `mapInsightRow(row: InsightRow): Entity` exists with `ontologyClass: 'Insight'`."
    - "Every mapped entity carries `entity.legacyId = { system: 'A', id: <source-row-id> }` at the canonical TOP-LEVEL `Entity.legacyId` field (per Phase 39 CF-D37 — see backfill/index.ts:238 `legacyId: resolved.legacyId ?? entity.legacyId`). The subsystem discriminator lives at `entity.metadata.subsystem = 'online'`, kept SEPARATE from legacyId."
    - "Mappers never perform I/O — no fs, no store, no http; pure (input row) → Entity transform."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts"
      provides: "Pure row-to-Entity mappers + exported row interfaces"
      exports: ["mapObservationRow", "mapDigestRow", "mapInsightRow", "ObservationRow", "DigestRow", "InsightRow"]
    - path: "/Users/Q284340/Agentic/km-core/tests/unit/adapters/online-mapper.test.ts"
      provides: "Unit tests covering happy-path + null-handling + top-level legacyId stamping for each mapper"
    - path: "/Users/Q284340/Agentic/km-core/tests/fixtures/online-export/{observations,digests,insights}.json"
      provides: "Minimal fixture rows mirroring A's actual export shape"
  key_links:
    - from: "mapper.ts"
      to: "src/types/entity.ts (Entity, ProvenanceStamp, DescriptionSegment)"
      via: "type-only imports with .js suffix"
      pattern: "import type \\{ Entity"
---

<objective>
Land the read-only mapper module under `/Users/Q284340/Agentic/km-core/src/adapters/online/` that converts A's SQLite/JSON-export row shapes into KM-Core `Entity` instances. The mappers are pure functions — no I/O, no store coupling — mirroring `src/segments/merge.ts` shape. Plan 04 (the reproject library function) consumes these mappers; this plan lands the mappers + fixtures + unit tests.

Purpose: INT-01 — Observation / Digest / Insight rows from `.data/observation-export/*.json` must come out the other side as KM-Core entities with `ontologyClass`, top-level canonical `legacyId`, and provenance ready for the store. SC#1 (typed ontology classes) and SC#2 (zero hot-path impact) both depend on the mapping being pure and read-only.

Output: 1 source file (mapper.ts), 1 test file, 3 fixture JSON files. ~85-130 LoC across module + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md
@.planning/phases/39-entity-data-model/39-CONTEXT.md

<interfaces>
<!-- Key types and contracts the mapper produces. Extracted from km-core src/. -->

From /Users/Q284340/Agentic/km-core/src/types/entity.ts (the canonical Entity shape — verified at lines 113-148):

  type Layer = 'evidence' | 'pattern';
  interface DescriptionSegment {
    text: string;
    confirmations: SegmentConfirmation[];
  }
  interface EntityProvenance {
    createdBy: ProvenanceStamp;
    lastConfirmedBy: ProvenanceStamp;
    confirmationCount: number;
  }
  interface Entity {
    id: EntityId;                       // branded UUIDv7 (line 116)
    name: string;                       // line 117
    entityType: string;                 // line 118
    ontologyClass?: string;             // line 119
    layer: Layer;                       // line 120 (REQUIRED, not optional)
    description: string;                // line 121 (REQUIRED, not optional)
    createdAt: string;                  // line 122 (REQUIRED, not optional)
    updatedAt: string;                  // line 123 (REQUIRED, not optional)
    metadata: Record<string, unknown>;  // line 124 (REQUIRED, not optional)
    validFrom?: string;                 // line 132
    validUntil?: string;                // line 139
    supersedes?: EntityId;              // line 145
    legacyId?: { system: 'A' | 'B' | 'C'; id: string };  // line 147 — CANONICAL TOP-LEVEL FIELD
  }

  CRITICAL — CANONICAL `legacyId` PLACEMENT (per Phase 39 CF-D37 and verified at entity.ts:147):
  `legacyId` is a TYPED TOP-LEVEL field on Entity, NOT a metadata bag entry. Phase 39's backfill stamps it
  at the top level (`src/backfill/index.ts:238`: `legacyId: resolved.legacyId ?? entity.legacyId`).
  The Phase 39 D-37 contract says: per-system migrations stamp `entity.legacyId.system = 'A'|'B'|'C'`
  using the SYSTEM letter, NOT a subsystem discriminator.

  For A's online learning subsystem, this means:
    - `entity.legacyId = { system: 'A', id: <row.id> }`  ← TOP-LEVEL legacyId
    - `entity.metadata.subsystem = 'online'`              ← SEPARATE metadata bag key (NOT inside legacyId)

  The 41-PATTERNS.md Cross-Cutting note "Option B (system + subsystem on metadata)" is SUPERSEDED by this
  canonical Phase 39 placement. The system stays narrow ('A'); the subsystem discriminator is its OWN
  metadata field, NOT nested inside legacyId.

From /Users/Q284340/Agentic/coding/.data/observation-export/observations.json (actual A source row shape — from 41-PATTERNS section "Source row shape (A's JSON exports)"):

  ObservationRow:
    id: string                  // sqlite-side UUID
    summary: string             // "Intent: ...\nApproach: ...\nArtifacts: ...\nResult: ..."
    agent: string               // e.g. 'opencode', 'claude-code'
    project: string             // e.g. 'coding'
    quality: string             // 'high' | 'medium' | 'low'
    createdAt: string           // ISO 8601
    digestedAt?: string | null
    llm: string | null          // may be null
    modifiedFiles: string[] | null

  DigestRow:
    id: string
    date: string
    theme: string
    summary: string
    observationIds: string[]
    agents: string[]
    filesTouched: string[]
    quality?: string
    project?: string
    createdAt?: string

  InsightRow:
    id: string
    topic: string
    summary: string
    confidence: number
    digestIds: string[]
    project?: string
    createdAt?: string

From /Users/Q284340/Agentic/km-core/src/segments/merge.ts (analog for pure-function shape — see 41-PATTERNS section on mapper module):

  export function mergeDescriptionSegment(entity: Entity, newSegment: DescriptionSegment): Entity {
    // pure — returns a NEW Entity; no I/O; .js relative imports
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture minimal fixture rows under tests/fixtures/online-export/</name>
  <files>/Users/Q284340/Agentic/km-core/tests/fixtures/online-export/observations.json, /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/digests.json, /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/insights.json</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.data/observation-export/observations.json (read first 3-5 entries via `head -200` or jq for the actual on-disk shape)
    - /Users/Q284340/Agentic/coding/.data/observation-export/digests.json (same — 2 entries are enough)
    - /Users/Q284340/Agentic/coding/.data/observation-export/insights.json (same — 1 entry is enough)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (the "Source row shape" excerpt)
  </read_first>
  <behavior>
    - observations.json fixture: 3-5 rows; at least one row has `modifiedFiles: null` AND `llm: null` (null-handling coverage); at least 2 rows have near-duplicate `summary` text (Plan 07 integration test seeds a deliberate duplicate pair).
    - digests.json fixture: 2 rows, each referencing observation ids that exist in observations.json fixture.
    - insights.json fixture: 1 row referencing both digest ids.
    - Each row carries the same field names and types as the actual A export files (use the live data shape — do not invent fields).
  </behavior>
  <action>
    Read 3 sample rows from each of the live `.data/observation-export/*.json` files (use `jq '.[0:3]'` or `head` to inspect; do NOT copy the files verbatim — capture a minimal subset). Hand-author small fixture files containing 3-5 observations, 2 digests, 1 insight per the behavior block above. The files MUST be in JSON-array shape (not JSONL). Trim long summaries to ≤200 chars for test readability. Strip any project-internal sensitive content — fixtures are committed to km-core (per CF-D04 km-core is a separate repo). Use synthetic project name `coding-test` and synthetic agent name `test-agent` in fixtures to avoid coupling to live data.
    Place the deliberate duplicate-pair so two observation rows have summary fields that differ only in trivial whitespace/punctuation (Plan 07's integration test relies on this for `resolveEntities` to find a match).
    Files end with a final newline.
  </action>
  <verify>
    <automated>for f in /Users/Q284340/Agentic/km-core/tests/fixtures/online-export/{observations,digests,insights}.json; do node -e "const a=JSON.parse(require('fs').readFileSync('$f','utf8')); if(!Array.isArray(a)){throw new Error('$f not array')}; console.log('$f', a.length, 'rows');" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All 3 fixture files exist and parse as JSON arrays.
    - `jq 'length' tests/fixtures/online-export/observations.json` returns a value between 3 and 10.
    - `jq 'length' tests/fixtures/online-export/digests.json` returns a value ≥ 2.
    - `jq 'length' tests/fixtures/online-export/insights.json` returns a value ≥ 1.
    - At least one observation row has `modifiedFiles: null` (verify via `jq '[.[] | select(.modifiedFiles == null)] | length'` returns ≥ 1).
    - At least one observation row has `llm: null` (verify analogously).
    - Each digest row's `observationIds` array contains at least one id that appears in `observations.json` (cross-reference holds — Plan 07 integration test depends on this).
  </acceptance_criteria>
  <done>3 fixture files committed; cross-references between observations/digests/insights are intact; null-handling sentinels present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure mapper functions in src/adapters/online/mapper.ts</name>
  <files>/Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts, /Users/Q284340/Agentic/km-core/tests/unit/adapters/online-mapper.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (Entity + EntityId + DescriptionSegment shape — note `legacyId` at line 147 is TOP-LEVEL, not metadata)
    - /Users/Q284340/Agentic/km-core/src/backfill/index.ts (lines 226-245 — canonical CF-D37 pattern: `entity.legacyId = resolved.legacyId ?? entity.legacyId` at TOP LEVEL)
    - /Users/Q284340/Agentic/km-core/src/segments/merge.ts (analog — pure-function shape, `.js` relative imports, type-only imports for Entity)
    - /Users/Q284340/Agentic/km-core/src/ids/branded.ts (or wherever EntityId brand + mintEntityId live — mappers should NOT mint a v7 id here; they leave `id` to be assigned by the caller via the legacyId resolver in Plan 04)
    - /Users/Q284340/Agentic/km-core/tests/unit/segments-merge.test.ts (analog — pure-function unit test style)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (sections on "online-mapper.spec.ts" + "mapper / pure function")
    - tests/fixtures/online-export/observations.json (created Task 1 — the row shape under test)
  </read_first>
  <behavior>
    - Test A: `mapObservationRow(fixture[0])` returns an Entity with `entityType === 'Observation'` AND `ontologyClass === 'Observation'`.
    - Test B: Mapped Observation has `description === row.summary` AND `createdAt === row.createdAt`.
    - Test C: Mapped Observation has `entity.legacyId.system === 'A'` AND `entity.legacyId.id === row.id` (top-level `Entity.legacyId` per CF-D37 / entity.ts:147).
    - Test D: Mapped Observation has `entity.metadata.subsystem === 'online'` (subsystem discriminator on metadata, SEPARATE from legacyId).
    - Test E: When `row.modifiedFiles === null`, mapped entity's `metadata.modifiedFiles` is `[]` (empty array, not null).
    - Test F: When `row.llm === null`, mapped entity's `metadata.llm` is `undefined` (key absent from metadata).
    - Test G: `mapDigestRow(fixture[0])` returns an Entity with `entityType === 'Digest'`, `ontologyClass === 'Digest'`, `metadata.observationIds: string[]` populated, AND `entity.legacyId.system === 'A'` AND `entity.legacyId.id === row.id` AND `entity.metadata.subsystem === 'online'`.
    - Test H: `mapInsightRow(fixture[0])` returns an Entity with `entityType === 'Insight'`, `ontologyClass === 'Insight'`, `metadata.digestIds: string[]` populated, AND same top-level `legacyId` + metadata.subsystem invariants as G.
    - Test I: `name` derivation — for Observation, `name` is the first non-empty line of `row.summary` (truncated to ≤120 chars). For Digest, `name` is `row.theme`. For Insight, `name` is `row.topic`.
    - Test J: Mappers are pure — calling twice with the same input returns equal-by-value entities (no side effects, no Date.now / Math.random).
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/src/adapters/online/` directory. Create file `mapper.ts` exporting:
      - Interface `ObservationRow` with the field shape from the `<interfaces>` block above.
      - Interface `DigestRow` analogously.
      - Interface `InsightRow` analogously.
      - Function `mapObservationRow(row: ObservationRow): Entity` returning an Entity where:
        * `name = deriveName(row.summary)` — extract first non-empty line of `summary`, then `.slice(0, 120)`.
        * `entityType = 'Observation'`, `ontologyClass = 'Observation'`.
        * `description = row.summary`.
        * `createdAt = row.createdAt`, `validFrom = row.createdAt` (so reprojection preserves source ordering for D-34 active-only queries).
        * **`legacyId = { system: 'A', id: row.id }`** at the TOP LEVEL of the returned Entity (matches Phase 39 backfill/index.ts:238 canonical placement; line 147 of entity.ts declares this field).
        * `metadata = { subsystem: 'online', project: row.project, agent: row.agent, quality: row.quality, modifiedFiles: row.modifiedFiles ?? [], ...(row.llm ? { llm: row.llm } : {}) }`. The `subsystem: 'online'` key is SEPARATE from legacyId — it's the discriminator that says "this is A's online subsystem" while legacyId.system='A' identifies which of the 3 systems.
        * `metadata.digestedAt = row.digestedAt` if present.
        * `id` field is INTENTIONALLY OMITTED — the reproject library function in Plan 04 mints/resolves the EntityId via the legacyId resolver pattern (CF-D37). The mapper returns a "partial entity that needs id-stamping"; document this contract in JSDoc on the function. The return type stays `Entity` even though `id` is missing because the strict `parseEntityId` runs only on the strict path; trusted-path `putEntity({ skipOntologyCheck: true })` accepts a not-yet-v7 id, and the legacyId-resolver lookup in Plan 04 will assign the canonical id before put.
      - Function `mapDigestRow(row: DigestRow): Entity` analogous with `entityType/ontologyClass = 'Digest'`; `name = row.theme`; `legacyId = { system: 'A', id: row.id }` TOP-LEVEL; `metadata = { subsystem: 'online', observationIds: row.observationIds, agents: row.agents, filesTouched: row.filesTouched, date: row.date, ... }`.
      - Function `mapInsightRow(row: InsightRow): Entity` analogous with `entityType/ontologyClass = 'Insight'`; `name = row.topic`; `legacyId = { system: 'A', id: row.id }` TOP-LEVEL; `metadata = { subsystem: 'online', digestIds: row.digestIds, confidence: row.confidence, ... }`.
      - Helper `deriveName(summary: string): string` — splits on `\n`, picks first trimmed non-empty line, slices to 120 chars; returns `'(empty)'` if no non-empty line. NOT exported (module-private).
    Use type-only imports for `Entity` (`import type { Entity } from '../../types/entity.js'`). All relative imports use `.js` suffix per CF-D06. No `console.*` calls — pure functions emit no diagnostics. No `Date.now()` calls — `createdAt` comes from the row.
    Create `/Users/Q284340/Agentic/km-core/tests/unit/adapters/online-mapper.test.ts` with the 10 tests A-J from the `<behavior>` block. Load fixtures via `readFileSync(path.resolve(__dirname, '..', '..', 'fixtures', 'online-export', '<file>.json'), 'utf8')` then `JSON.parse`. Use vitest `describe` / `test` / `expect`. NO real GraphKMStore — these are pure-function tests.

    CRITICAL — Tests C/D/G/H assertions on legacyId MUST read `entity.legacyId.system` / `entity.legacyId.id` at the TOP LEVEL of the entity, NOT `entity.metadata.legacyId.*`. Any grep-based acceptance check that hunts for `metadata.legacyId.` is invalid — the canonical path is `entity.legacyId.` at top level.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && npx vitest run tests/unit/adapters/online-mapper.test.ts 2>&1 | grep -E "(passed|failed|Tests )" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` exists.
    - File `/Users/Q284340/Agentic/km-core/tests/unit/adapters/online-mapper.test.ts` exists.
    - `npx tsc --noEmit` in km-core exits 0.
    - `npx vitest run tests/unit/adapters/online-mapper.test.ts` exits 0 with ≥10 passing tests.
    - `grep -c "export function map" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` returns exactly `3` (mapObservationRow, mapDigestRow, mapInsightRow).
    - `grep -c "export interface" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` returns exactly `3` (ObservationRow, DigestRow, InsightRow).
    - `grep -cE "console\\." /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` returns `0` (no-console-log).
    - `grep -cE "from '\\.\\./" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts | head -1` → all relative imports use `.js` suffix (verify with `grep -E "from '\\.\\./" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts | grep -vE "\\.js'" | grep -c .` returning `0`).
    - **Top-level legacyId placement:** `grep -cE "legacyId:\\s*\\{\\s*system:\\s*'A'" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` returns ≥ `3` (one per mapper — Observation, Digest, Insight) at TOP-LEVEL of the returned Entity object literal (NOT inside a `metadata: {...}` nesting).
    - **Subsystem metadata placement:** `grep -cE "subsystem:\\s*'online'" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` returns ≥ `3` (one per mapper — appears in the `metadata: {...}` bag, NOT inside `legacyId`).
    - **No legacyId in metadata:** `grep -cE "metadata\\.legacyId|metadata:\\s*\\{[^}]*legacyId" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` returns `0` (legacyId is NEVER placed under metadata — it lives at the top level of Entity per CF-D37).
    - `grep -c "fs\\.\\|readFileSync\\|writeFileSync\\|http\\.\\|fetch(" /Users/Q284340/Agentic/km-core/src/adapters/online/mapper.ts` returns `0` (mapper is pure — no I/O).
    - Tests assert top-level legacyId: `grep -cE "entity\\.legacyId\\.system|\\.legacyId\\.system === 'A'" /Users/Q284340/Agentic/km-core/tests/unit/adapters/online-mapper.test.ts` returns ≥ `3` (Tests C, G, H read `legacyId.system` at top level).
    - Tests assert separate subsystem on metadata: `grep -cE "metadata\\.subsystem === 'online'|metadata\\.subsystem.*'online'" /Users/Q284340/Agentic/km-core/tests/unit/adapters/online-mapper.test.ts` returns ≥ `1` (Test D).
    - Full vitest suite still green: `npx vitest run` exit 0.
  </acceptance_criteria>
  <done>Three pure mapper functions land with 10 passing unit tests. Mappers are I/O-free. `entity.legacyId = { system: 'A', id }` at the canonical TOP LEVEL (CF-D37); `entity.metadata.subsystem = 'online'` SEPARATE discriminator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mapper input ← A's JSON exports | Source rows may carry malformed/null fields; mapper must tolerate nulls per ColdStoreReader invariant (skip-and-continue on row level). |
| Mapper output → Entity consumer | Mapper does NOT mint EntityId — the reproject function in Plan 04 owns id stamping via legacyId resolver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Information Disclosure | A's project-internal data in fixtures | mitigate | Use synthetic project name `coding-test` and synthetic agent name `test-agent`; do NOT verbatim-copy live `.data/observation-export/` content. |
| T-41-02-02 | Tampering | malformed source row → mapper crash | mitigate | Null-tolerant field unpacking; helper `deriveName` returns `'(empty)'` for missing summary; unit tests E/F pin null-handling behavior. |
| T-41-02-03 | Spoofing | source row claims a `legacyId` that already exists | accept | Mapper does NOT lookup the store; idempotency is enforced in Plan 04's reproject via `findRelations({ from: legacyId })` or `entity.legacyId.id` filter scan. Out of scope for the mapper layer. |
| T-41-02-04 | Tampering | npm installs | mitigate | No npm installs in this plan. |
</threat_model>

<verification>
- Mappers are pure functions (no I/O, no globals, no side effects).
- 10 unit tests cover happy-path + null-handling + name-derivation + top-level legacyId stamping + purity.
- `entity.legacyId` is canonical TOP-LEVEL per CF-D37 (entity.ts:147 + backfill/index.ts:238 are the reference points).
- `metadata.subsystem` is SEPARATE from legacyId.
- TypeScript strict build clean.
- Full vitest suite green.
</verification>

<success_criteria>
- mapper.ts + 3 fixtures + test file committed to km-core.
- `Entity.legacyId.system` stays narrow (`'A' | 'B' | 'C'`) — Phase 39 CF-D37 canonical pattern carried forward without widening the type. `'online'` discriminator lives on `entity.metadata.subsystem`, separate from legacyId.
- Plan 04 (reproject) unblocked — has typed mappers + fixture data + interfaces to consume, with canonical top-level legacyId placement that Plan 04's idempotency scan can hash on directly.
</success_criteria>

<output>
Create `.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-02-SUMMARY.md` when done.
</output>
