---
phase: 41-online-learning-adapter-post-hoc-resolution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts
  - /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts
autonomous: true
requirements:
  - PIPE-02
user_setup: []

must_haves:
  truths:
    - "`GraphKMStore.getDegree(id: EntityId): Promise<number>` is a public async method that returns the Graphology in+out degree for the entity's node."
    - "Calling `getDegree` on a missing id returns `0` (never throws)."
    - "`resolveEntities` (Plan 06) can call `await store.getDegree(survivorId)` to break ties between merge candidates per the OKM survivor-selection heuristic ported in `deduplicator.ts:711-719`."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts"
      provides: "Public `getDegree` method"
      exports: ["GraphKMStore.prototype.getDegree"]
  key_links:
    - from: "GraphKMStore.getDegree"
      to: "graphology MultiDirectedGraph.degree(id)"
      via: "this.graph.degree(id) wrapper"
      pattern: "this\\.graph\\.degree\\("
---

<objective>
Add a minimal `getDegree(id: EntityId): Promise<number>` public method to `GraphKMStore`. This is the only modification required to the store contract for Phase 41 — it unblocks `resolveEntities` (Plan 06) to perform the OKM-pattern survivor-selection ("prefer the higher-degree node when merging duplicates") without exposing the private Graphology instance or routing through callbacks.

Per 41-PATTERNS Cross-Cutting note "No `getDegree(id)` in km-core GraphKMStore" and CONTEXT.md "Claude's Discretion: Survivor-selection heuristic — OKM uses 'prefer node with more graph edges' (getDegree); planner may stick with that or tune". Picked option: stick with `getDegree` and add it to the store API (cheapest of the three options listed in PATTERNS).

Purpose: PIPE-02 — `resolveEntities` calls `getDegree` for each candidate pair from the LLM matcher to decide survivor. Without this, the maintenance op cannot port OKM's tested heuristic verbatim and would need a more complex caller-callback shape.

Output: ~10 LoC public method add to GraphKMStore.ts + 3 unit tests appended to graph-store.test.ts. Public API addition only — no behavior change for existing callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md
@.planning/phases/37-km-core-foundation/37-CONTEXT.md
@.planning/phases/39-entity-data-model/39-CONTEXT.md

<interfaces>
<!-- Key types and contracts the new method binds to. -->

From /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (class declaration, line 136):

  export class GraphKMStore extends EventEmitter {
    private graph: MultiDirectedGraph;  // Graphology instance
    // ... existing methods at lines 200, 267, 325, 514, 523, 556, 583, 609, 631, 696, 807, 818, 840
    // Public methods: open, restore, putEntity, getEntity, deleteEntity,
    //                 findByOntologyClass, addRelation, findRelations,
    //                 getSupersessionChain, batch, iterate, exportJson,
    //                 mergeAttributes, close
  }

From /Users/Q284340/Agentic/km-core/src/ids/branded.ts:

  type EntityId = string & { __brand: 'EntityId' };

From graphology@0.26 (already a km-core dependency — directed multi-graph semantics):

  class MultiDirectedGraph {
    degree(node: string): number;        // total in+out (counts each edge once for the endpoint).
                                         // For a directed multi-graph with a SINGLE edge A→B:
                                         //   degree(A) === 1 (outgoing edge counted once)
                                         //   degree(B) === 1 (incoming edge counted once)
                                         // For 3 edges A→B, A→C, A→D:
                                         //   degree(A) === 3 (three outgoing edges).
                                         // These values are pinned by Plan 03 tests A and B.
    inDegree(node: string): number;
    outDegree(node: string): number;
    hasNode(node: string): boolean;
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public getDegree method to GraphKMStore + 3 unit tests</name>
  <files>/Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts, /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (lines 136-160 — class declaration; lines 514-528 — `getEntity` / `deleteEntity` for analog "guard-with-hasNode" pattern; lines 583-606 — `addRelation` for relation-write pattern)
    - /Users/Q284340/Agentic/km-core/src/ids/branded.ts (EntityId brand)
    - /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts (analog — existing test style, makeStoreCtx pattern, beforeEach/afterEach)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (Cross-Cutting "No getDegree" — picks Option 1: add to store)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md ("Claude's Discretion: Survivor-selection heuristic — OKM uses 'prefer node with more graph edges'")
  </read_first>
  <behavior>
    - Test A: After putting two entities A,B and addRelation({from:A, to:B}), `await store.getDegree(A.id) === 1` AND `await store.getDegree(B.id) === 1`. Graphology's `MultiDirectedGraph.degree(node)` returns the sum of inDegree+outDegree; a SINGLE directed edge A→B gives A one outgoing and B one incoming, so both endpoints' degree count is exactly 1. Pin to these literal values directly — do not defer to library-doc verification at execution time.
    - Test B: After adding 3 outgoing edges from A (to B, C, D), `await store.getDegree(A.id) === 3` (three outgoing edges; inDegree=0, outDegree=3, total=3).
    - Test C: `await store.getDegree('nonexistent-id' as EntityId) === 0` and does NOT throw.
  </behavior>
  <action>
    In `/Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts`, insert a new public async method `getDegree` immediately after `findRelations` (so it groups with the other read-only query methods). Signature: `async getDegree(id: EntityId): Promise<number>`. Body: if `!this.graph.hasNode(id)` return `0`; else return `this.graph.degree(id)`. JSDoc must reference: (a) Phase 41 Plan 03 — survivor-selection support for PIPE-02 `resolveEntities`; (b) the "missing node returns 0" contract for caller convenience; (c) the in+out total semantics (Graphology's `degree` returns the sum, distinct from `inDegree` / `outDegree`); (d) explicit pin to "single A→B edge → both endpoints' degree === 1" so Plan 06 Test I (degree-based survivor selection) has a deterministic reference.
    Method is async (`Promise<number>`) for consistency with the rest of the public surface even though the underlying call is synchronous — matches Pattern S4 from Phase 38 registry.ts which also uses `async` for sync underlying ops.
    Do NOT touch any other method. Do NOT add `inDegree` / `outDegree` separately — YAGNI; consumers can be added later if needed.
    In `/Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts`, append a new `describe('getDegree (Phase 41 Plan 03)', ...)` block containing 3 tests A, B, C from the `<behavior>` section. Use the existing `makeStoreCtx` / `cleanup` helpers in the file (do not duplicate them). Seed entities via the existing trusted-path pattern (`store.putEntity(e, { skipOntologyCheck: true })`). Use `mintEntityId()` or `as EntityId` for ids per the existing test style.

    Test A asserts the literal `=== 1` for both endpoints AFTER a single directed edge. Test B asserts `=== 3` after three outgoing edges. Test C asserts `=== 0` and `does not throw` for an absent node id. These are deterministic in directed multi-graph semantics — pin the test to the value directly, no conditional language about "if non-obvious".
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && npx vitest run tests/unit/graph-store.test.ts 2>&1 | grep -E "(passed|failed|getDegree)" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "async getDegree\\(" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` returns exactly one match.
    - `grep -nE "this\\.graph\\.degree\\(" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` returns exactly one match (the new method body — no other call site exists).
    - `grep -n "getDegree" /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts` returns ≥ 4 matches (1 describe + 3 tests).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/unit/graph-store.test.ts` exits 0 with ≥3 NEW passing tests (total count goes up by ≥3 from pre-plan baseline).
    - Full vitest suite still green: `npx vitest run` exits 0; ZERO regressions in Phase 37/38/39/40 tests.
    - `grep -cE "console\\." /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` is unchanged from baseline (no new `console.*` calls — diff should show pure addition of one method).
  </acceptance_criteria>
  <done>`getDegree` public method exists with 3 passing tests pinning literal degree values (1/1, 3, 0); missing-node returns 0; type compile clean; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API addition | A new method becomes part of the public surface; future removal would be a breaking change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Information Disclosure | `getDegree` exposes graph structure metadata | accept | km-core is a library consumed by trusted callers; degree count is non-sensitive metadata. |
| T-41-03-02 | Tampering | adding a method could shadow a future Graphology API change | mitigate | `getDegree` is a thin wrapper; any future Graphology API change to `degree()` requires an explicit km-core version bump. Pinned to graphology@^0.26.0 in package.json. |
| T-41-03-03 | Denial of Service | `getDegree` called in a tight loop on a huge graph | accept | Graphology's `degree` is O(1) for a node lookup; not a DoS surface. resolveEntities (Plan 06) calls it at most twice per LLM-surfaced match — bounded by match count. |
| T-41-03-04 | Tampering | npm installs | mitigate | No npm installs — pure source addition. |
</threat_model>

<verification>
- `getDegree` method exists, returns a Promise<number>, returns 0 for missing nodes.
- 3 new unit tests pass, pinning literal degree values (Test A: 1/1; Test B: 3; Test C: 0).
- Zero regression in Phase 37/38/39/40 tests.
- No other code changes — diff is bounded to one method addition + one test block append.
</verification>

<success_criteria>
- `GraphKMStore.getDegree` is callable by Plan 06's `resolveEntities`.
- Plan 06 can implement OKM's survivor-selection ("higher-degree wins") via two `await store.getDegree(...)` calls.
- No widening of `legacyId.system` union — Plan 02's canonical CF-D37 top-level placement (`entity.legacyId = { system: 'A', id }` + `entity.metadata.subsystem = 'online'`) holds; this plan touches only the store, not entity types.
</success_criteria>

<output>
Create `.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-03-SUMMARY.md` when done.
</output>
