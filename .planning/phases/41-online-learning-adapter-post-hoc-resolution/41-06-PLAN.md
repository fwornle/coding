---
phase: 41-online-learning-adapter-post-hoc-resolution
plan: 06
type: execute
wave: 3
depends_on:
  - 41-03
  - 41-04
  - 41-05
files_modified:
  - /Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts
  - /Users/Q284340/Agentic/km-core/src/maintenance/index.ts
  - /Users/Q284340/Agentic/km-core/src/index.ts
  - /Users/Q284340/Agentic/km-core/package.json
  - /Users/Q284340/Agentic/km-core/tests/unit/maintenance/resolveEntities.test.ts
autonomous: true
requirements:
  - PIPE-02
  - INT-01
user_setup: []

must_haves:
  truths:
    - "`resolveEntities(store, opts): Promise<ResolveResult>` is a top-level library function in `@fwornle/km-core/maintenance` (sub-barrel)."
    - "`resolveEntities` defaults to scanning ALL subclasses of `LearningArtifact` (resolved via Phase 38 OntologyRegistry's `parentChainOf` chain-of-ResolvedClass walk — match by `.name === 'LearningArtifact'`) when `opts.classes` is omitted."
    - "Per-class iteration: for each class in opts.classes, fetch active entities via `store.findByOntologyClass(class)` (Phase 39 D-34 active-only default), build entity-name+description pairs, send batches of `BATCH_SIZE` (default 30) to the caller-supplied `LLMSemanticLayer` matcher with `concurrency` (default 3) classes in flight."
    - "Per surfaced match (entityA, entityB, confidence ≥ threshold): pick survivor by `await store.getDegree()` (higher wins; tie → entityA wins), invoke `mergeEntities(store, survivor.id, [duplicate.id], { provenance, reason })`."
    - "MatchResult.matchedTo → EntityId reverse lookup is deterministic: filter candidates by `name === matchedTo.name && description === matchedTo.description`; if multiple candidates match, pick the LOWEST lexicographic id (deterministic tie-break)."
    - "`dryRun: true` returns the planned merges without invoking `mergeEntities`; the returned `merges[]` array shape is identical regardless of dryRun."
    - "Per-batch LLM failures are caught into `errors[]` (never throw); `mergeEntities` failures bubble up (these ARE fatal — atomicity violations)."
    - "The maintenance sub-path is exported via package.json `exports` map as `'./maintenance'`."
    - "The root barrel `src/index.ts` re-exports `resolveEntities`, `mergeEntities`, `reprojectFromOnlineStore` and their option/result types — consumers can take a single import from `@fwornle/km-core` for the Phase 41 surface."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts"
      provides: "Per-class LLM-scan orchestrator"
      exports: ["resolveEntities", "ResolveOptions", "ResolveResult"]
    - path: "/Users/Q284340/Agentic/km-core/src/maintenance/index.ts"
      provides: "Maintenance sub-barrel"
      exports: ["resolveEntities", "mergeEntities", "ResolveOptions", "ResolveResult", "MergeOptions", "MergeResult"]
    - path: "/Users/Q284340/Agentic/km-core/src/index.ts"
      provides: "Phase 41 block appended after Phase 40 block (lines 91-122)"
    - path: "/Users/Q284340/Agentic/km-core/package.json"
      provides: "exports map extended with './maintenance' and './adapters/online'"
  key_links:
    - from: "resolveEntities"
      to: "Plan 02 mapper-stamped LearningArtifact subclasses in store"
      via: "store.findByOntologyClass(class)"
      pattern: "findByOntologyClass"
    - from: "resolveEntities"
      to: "Plan 03 GraphKMStore.getDegree"
      via: "survivor selection (higher degree wins)"
      pattern: "store\\.getDegree"
    - from: "resolveEntities"
      to: "Plan 05 mergeEntities primitive"
      via: "per-match merge invocation when dryRun=false"
      pattern: "mergeEntities\\(store,"
    - from: "resolveEntities"
      to: "Plan 01 OntologyRegistry LearningArtifact subclasses"
      via: "default `classes` resolution via parentChainOf().some(rc => rc.name === 'LearningArtifact')"
      pattern: "parentChainOf"
    - from: "package.json exports"
      to: "dist/maintenance/index.js + dist/adapters/online/index.js"
      via: "subPath entry per Plan 40-07 precedent"
      pattern: "./maintenance"
---

<objective>
Wrap Plans 05 (`mergeEntities`) + 03 (`getDegree`) + 02 (mappers) + 01 (ontology classes) + 04 (reprojection module surface) into the user-facing PIPE-02 surface: `resolveEntities(store, opts)` library function, plus the `/maintenance` sub-barrel, plus the root barrel append, plus the `package.json` exports map extension.

Purpose: PIPE-02 SC#3 + SC#4 — operators can call `resolveEntities(store, { llmMatcher, dryRun, classes? })` from any consumer (per-system script, future Phase 44 REST route) to scan-and-merge cross-batch duplicates within a class. The whole Phase 41 deliverable is reachable through one consistent import surface.

Output: 2 new source files (resolveEntities.ts + maintenance/index.ts), 2 modifications (root barrel + package.json), 1 test file. ~250-350 LoC across module + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-05-PLAN.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-03-PLAN.md
@.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-04-PLAN.md
@.planning/phases/40-ingest-pipeline-layered-dedup/40-CONTEXT.md
@.planning/phases/38-ontology-registry/38-CONTEXT.md

<interfaces>
<!-- Key types and contracts this plan composes. -->

From OKM `_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts:620-720` (THE reference algorithm — port verbatim with KM-Core deltas listed in 41-PATTERNS):

  // OKM signature
  async resolveEntities(graphStore, dryRun = false, classes?: string[]): Promise<{
    merges: Array<{ survivorId, survivorName, duplicateId, duplicateName, ontologyClass }>;
    synthesizedCount: number;
    errors: string[];
  }>;
  // Constants: RESOLUTION_CONCURRENCY=3, BATCH_SIZE=30 (line 651-654)
  // Algorithm: groupByClass → per-class wave of CONCURRENCY classes → per-class
  //   BATCH_SIZE batches sent to batchLLMResolution → matches accumulated →
  //   for each match, pick survivor by graphStore.getDegree, merge.

From /Users/Q284340/Agentic/km-core/src/dedup/types.ts (Plan 40-04 — caller-supplied LLM matcher):

  interface LLMSemanticLayer {
    match(entity: { name: string; description?: string }, candidates: Array<{ name: string; description?: string }>): Promise<MatchResult>;
  }
  interface MatchResult {
    matched: boolean;
    confidence: number;
    matchedTo?: { name: string; description?: string };   // NO id field — name+description only
    reason?: string;
  }
  // LLMSemanticMatcher (Plan 40-04) implements this with caller-supplied LLMClient.

  CRITICAL — MatchResult.matchedTo → EntityId reverse lookup contract (this plan owns the mapping):
  When `result.matched && result.confidence >= threshold && result.matchedTo` is populated,
  resolveEntities must map `result.matchedTo.{name, description}` back to a concrete EntityId
  from the batch's candidate pool. The contract:

    const matches = candidates.filter(
      c => c.name === result.matchedTo.name && c.description === result.matchedTo.description,
    );
    if (matches.length === 0) {
      // Unmatchable — LLM hallucinated a name+description that's not in the candidate pool.
      // Push to errors[] and skip (do not merge).
      errors.push(`unmatchable: LLM returned matchedTo name="${result.matchedTo.name}" not in candidate pool for class ${ontologyClass}`);
      continue;
    }
    // Tie-break on name+description collisions: pick the candidate with the LOWEST
    // lexicographic id (deterministic across runs; matches.length === 1 means no
    // collision, and the sort is a no-op).
    const target = matches.sort((a, b) => a.id.localeCompare(b.id))[0];
    // target.id is the EntityId that gets passed to mergeEntities as the duplicate.

  Document this lookup in JSDoc on `resolveEntities`. Test G+I+(new K) pin the contract.

From /Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts (Plan 05 output):

  export async function mergeEntities(
    store: GraphKMStore,
    survivorId: EntityId,
    duplicateIds: EntityId[],
    opts: MergeOptions,
  ): Promise<MergeResult>;
  export interface MergeOptions { provenance: ProvenanceStamp; mergeSegments?: boolean; reason?: string }
  export interface MergeResult { survivorId; duplicateIds; edgesRewired; segmentsMerged; oldValidUntil; durationMs }

From /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (public surface used by this plan):

  async findByOntologyClass(name: string, opts?: { includeSuperseded?: boolean }): Promise<Entity[]>;
  async getDegree(id: EntityId): Promise<number>;  // Plan 03 output
  get ontology(): OntologyRegistry | undefined;     // Phase 38 D-26+
  // findByOntologyClass defaults to includeSuperseded:false per Phase 39 D-34

From /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (Phase 38 — VERIFIED at registry.ts:220):

  class OntologyRegistry {
    parentChainOf(className: string): ResolvedClass[];   // returns CHAIN of resolved-parent OBJECTS (NOT strings); does NOT include the class itself; stops at first missing parent (line 220-230).
    getAllClassNames(): string[];
    getClass(name: string): ResolvedClass | undefined;
  }
  interface ResolvedClass {
    name: string;
    source: string;
    extends?: string;
    defaultLayer?: 'evidence' | 'pattern';
    description?: string;
    relationships: Record<string, string[]>;
    properties: Record<string, { type: string; required?: boolean; description?: string }>;
  }
  // No `subclassesOf` method exists. Compute via:
  //   const subclasses = registry.getAllClassNames().filter(c =>
  //     registry.parentChainOf(c).some(rc => rc.name === 'LearningArtifact')
  //   );
  // This:
  //   - hashes on .name (ResolvedClass objects, NOT bare strings) — `parentChainOf(c).includes('LearningArtifact')` IS WRONG (object !== string).
  //   - EXCLUDES 'LearningArtifact' itself from the result (parentChainOf does not include the class itself).
  //   - includes Observation/Digest/Insight because each has `extends: 'LearningArtifact'` so the chain
  //     starts with LearningArtifact (matched by .name).

From /Users/Q284340/Agentic/km-core/src/types/entity.ts:

  interface ProvenanceStamp { provider: string; model: string; runId: string; timestamp: string }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement resolveEntities library function + maintenance sub-barrel</name>
  <files>/Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts, /Users/Q284340/Agentic/km-core/src/maintenance/index.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts (lines 620-720 — full resolveEntities body; especially line 666 200-char description truncation; lines 711-719 degree-based survivor selection)
    - /Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts (Plan 05 output — invoked per surfaced match)
    - /Users/Q284340/Agentic/km-core/src/dedup/LLMSemanticMatcher.ts (the caller-supplied LLMSemanticLayer implementation; understand the match() return shape and onError defaults)
    - /Users/Q284340/Agentic/km-core/src/dedup/types.ts (LLMSemanticLayer + MatchResult interfaces; **note MatchResult.matchedTo has NO id field — only name+description**)
    - /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (parentChainOf signature at line 220 — returns ResolvedClass[], NOT string[]; does NOT include the class itself)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (findByOntologyClass + getDegree + ontology getter signatures)
    - /Users/Q284340/Agentic/km-core/src/dedup/index.ts (sub-barrel analog — block comment + runtime exports + type exports)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on `resolveEntities.ts` — "What to keep" + "What to adapt" lists)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md (D-49 — top-level fn in /maintenance sub-path; D-49a — package.json exports map; CF-D34 — active-only default; "Claude's Discretion" — survivor heuristic via getDegree)
  </read_first>
  <behavior>
    - Signature: `export async function resolveEntities(store: GraphKMStore, options: ResolveOptions): Promise<ResolveResult>`.
    - `ResolveOptions`: `{ llmMatcher: LLMSemanticLayer; provenance: ProvenanceStamp; classes?: string[]; dryRun?: boolean; concurrency?: number; batchSize?: number; threshold?: number; log?: (e: ResolveEvent) => void }`.
    - `ResolveEvent`: `{ phase: 'startClass' | 'batchDone' | 'classDone' | 'match' | 'merge' | 'error'; ontologyClass?: string; entitiesScanned?: number; matchesFound?: number; merge?: { survivorId; duplicateId }; error?: string }`.
    - `ResolveResult`: `{ runId: string; merges: Array<{ survivorId: EntityId; survivorName: string; duplicateId: EntityId; duplicateName: string; ontologyClass: string; confidence: number }>; errors: string[]; dryRun: boolean; classesScanned: string[]; durationMs: number }`.
    - Defaults: `concurrency = 3`, `batchSize = 30`, `threshold = 0.70` (matches Plan 40 LLMSemanticMatcher default).
    - **Class resolution (CORRECTED — uses real registry.parentChainOf semantics):**
      * If `opts.classes` is provided: use it verbatim.
      * If `opts.classes` is omitted: default to all subclasses of `'LearningArtifact'` resolved via `store.ontology` getter. Implementation:
        - If `store.ontology === undefined`: throw `'resolveEntities: opts.classes omitted but store has no ontology registry (set ontologyDir in GraphKMStore constructor)'`.
        - Else: compute subclasses via:
          ```
          const registry = store.ontology;
          const subclasses = registry.getAllClassNames().filter(c =>
            registry.parentChainOf(c).some(rc => rc.name === 'LearningArtifact')
          );
          ```
          CRITICAL — `parentChainOf` returns `ResolvedClass[]` (objects with `.name`, `.source`, etc.), NOT `string[]`. Matching via `.some(rc => rc.name === 'LearningArtifact')` is correct; `.includes('LearningArtifact')` would always be false (object !== string) and produce empty subclass list → silent SC#3/SC#4 failure.
          NB `parentChainOf` does NOT include the class itself in the chain (verified at registry.ts:220-230 — the loop walks `cur.extends` parents until missing). Since Observation/Digest/Insight each declare `extends: 'LearningArtifact'` (Plan 01), the chain starts with LearningArtifact for each subclass — the filter matches correctly.
          'LearningArtifact' itself is NOT in the result (its parent chain is empty). This is the intended behavior — we scan the leaf classes, not the abstract upper.
        - If the resulting list is empty: `process.stderr.write('[km-core/maintenance] resolveEntities: no LearningArtifact subclasses registered; nothing to scan\n')` and return early with empty merges/errors.
    - Per-class iteration (CF-D34 active-only default):
      1. Fetch active entities: `const entities = await store.findByOntologyClass(ontologyClass)` (default `includeSuperseded:false`).
      2. If `entities.length < 2`: skip class (log `phase: 'classDone'` with `matchesFound: 0`).
      3. Build entity summaries: `entities.map(e => ({ id: e.id, name: e.name, description: (e.description ?? '').slice(0, 200) }))` — 200-char truncation per OKM line 666 (load-bearing for prompt size).
    - Concurrency wave: process `concurrency` classes in flight via `Promise.allSettled`. For each class:
      1. For each batch of `batchSize` summaries:
         - If batch.length < 2: skip.
         - For each entity in batch, call `llmMatcher.match({ name: entity.name, description: entity.description }, candidates_excluding_self_by_id)` and collect MatchResults where `result.matched && result.confidence >= threshold`. Per-call errors caught into `errors[]` with message `'LLM resolution error for class <class> entity <entityId>: <message>'` (matches OKM line 678 style).
         - NB: `match()` takes ONE entity + N candidates and returns at most ONE match. To find ALL pairs in a class, iterate each entity in the batch as the "subject" entity with the rest of the batch as candidates. This is O(batch²) LLM calls per batch — accept the cost.
         - Track `matchedAway: Set<EntityId>` per class to avoid double-merging an entity that was already merged into another survivor in the same class.
      2. After all batches: emit `phase: 'classDone'`.
    - **Per surfaced match (subject entity + MatchResult.matchedTo):**
      * If `result.matched && result.confidence >= threshold && result.matchedTo`:
        - Find target candidates in batch: `const candidates = batchSummaries.filter(c => c.id !== subject.id && c.name === result.matchedTo.name && c.description === result.matchedTo.description);`
        - If `candidates.length === 0`: push `'unmatchable: LLM returned matchedTo name="${result.matchedTo.name}" not in candidate pool for class ${ontologyClass}'` to `errors[]` and continue. NEVER merge an entity that doesn't have a deterministic id mapping.
        - **Tie-break on name+description collisions:** sort `candidates` by `(a, b) => a.id.localeCompare(b.id)` and pick the FIRST (lowest lexicographic id). Single match → sort is no-op. Multiple matches (name+description collision in the candidate pool) → deterministic across runs.
          ```
          const target = candidates.slice().sort((a, b) => a.id.localeCompare(b.id))[0];
          ```
        - Skip if subject.id or target.id is in `matchedAway`.
        - Pick survivor: `const [degA, degB] = await Promise.all([store.getDegree(subject.id), store.getDegree(target.id)])`. Survivor is the higher-degree node; tie → subject wins (matches OKM line 712 `degreeA >= degreeB ? [entityA, entityB, ...] : [entityB, entityA, ...]`; `entityA` here is `subject`).
        - Append match to `merges` with `{ survivorId, survivorName, duplicateId, duplicateName, ontologyClass, confidence }` populated.
        - If `dryRun: true`: continue (no merge invocation; matchedAway is NOT updated because nothing was merged).
        - Else: call `await mergeEntities(store, survivor.id, [duplicate.id], { provenance: opts.provenance, reason: 'resolveEntities ' + ontologyClass + ' confidence=' + confidence })`. On success: `matchedAway.add(duplicate.id)`. On throw: catch into `errors[]` with message `'mergeEntities failed for <survivorId> <- <duplicateId>: <message>'`. Do NOT re-throw — let the scan continue to next match. (WR-02 invariant violations covered by this catch.)
    - Final result: `runId = options.provenance.runId` (use the caller's stamp's runId — single source of truth across the run); `durationMs = Date.now() - startTs`.
    - All diagnostics via `process.stderr.write('[km-core/maintenance] ...')`. NO `console.*`.
    - Create `/Users/Q284340/Agentic/km-core/src/maintenance/index.ts` sub-barrel re-exporting:
      ```
      export { resolveEntities } from './resolveEntities.js';
      export { mergeEntities } from './mergeEntities.js';
      export type { ResolveOptions, ResolveResult, ResolveEvent } from './resolveEntities.js';
      export type { MergeOptions, MergeResult } from './mergeEntities.js';
      ```
      Block comment lists consumer import path `@fwornle/km-core/maintenance`. Match `src/dedup/index.ts` style.
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts` implementing the behavior above. Top-level free function (CF-D14); NO class.
    Type-only imports for `Entity`, `EntityId`, `ProvenanceStamp` from `'../types/entity.js'` / `'../ids/branded.js'`. Type-only import for `LLMSemanticLayer`, `MatchResult` from `'../dedup/types.js'`. Type-only import for `GraphKMStore` from `'../store/GraphKMStore.js'`. Runtime import for `mergeEntities` from `'./mergeEntities.js'`.
    Concurrency implementation: chunk `classes` into waves of size `concurrency`; per wave run `Promise.allSettled(wave.map(processClass))`.
    The per-entity LLM call inside the batch loop: pass `{ name: e.name, description: e.description }` as the subject and `batch.filter(c => c.id !== e.id)` (excluding self by id, not by name — names may collide in the batch) as candidates.
    Class-default resolution MUST use `registry.parentChainOf(c).some(rc => rc.name === 'LearningArtifact')` — `.some(rc => rc.name === ...)`, NOT `.includes(...)`. Add a JSDoc note explaining this is because `parentChainOf` returns `ResolvedClass[]`, not `string[]`.
    `matchedTo → EntityId` reverse lookup MUST filter by `name === matchedTo.name && description === matchedTo.description` AND tie-break via `.sort((a, b) => a.id.localeCompare(b.id))[0]`. Unmatchable case (zero candidates) pushes to errors[] and skips.
    Emit `process.stderr.write` for: class start (`scanning class <name> N entities`), batch error (`LLM resolution error for class <name> entity <id>: <message>`), unmatchable warning (`unmatchable: LLM returned matchedTo name=... not in candidate pool`), merge invocation (`merging duplicate <id> into survivor <id> (confidence X)`), merge failure (`mergeEntities failed ...`), final summary (`scanned C classes, M merges, E errors, durationMs=...`).
    `log?` optional callback is called when provided; never throws on log errors (wrap in try/catch).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit 2>&1 | tail -10 && grep -c "export async function resolveEntities" src/maintenance/resolveEntities.ts && grep -c "mergeEntities" src/maintenance/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts` exists.
    - File `/Users/Q284340/Agentic/km-core/src/maintenance/index.ts` exists.
    - `grep -c "export async function resolveEntities" src/maintenance/resolveEntities.ts` returns `1`.
    - `grep -c "store\\.getDegree" src/maintenance/resolveEntities.ts` ≥ 1 (Plan 03 dependency invoked).
    - `grep -c "mergeEntities(store" src/maintenance/resolveEntities.ts` ≥ 1 (Plan 05 dependency invoked).
    - `grep -c "store\\.findByOntologyClass" src/maintenance/resolveEntities.ts` ≥ 1 (active-only class scan).
    - `grep -c "store\\.ontology" src/maintenance/resolveEntities.ts` ≥ 1 (default-classes resolution via registry).
    - **Correct parentChainOf usage (matches by .name on ResolvedClass objects):** `grep -cE "parentChainOf\\(.*\\)\\.some\\(\\s*(rc|r|cls)\\s*=>\\s*\\1\\.name\\s*===\\s*['\"]LearningArtifact['\"]" src/maintenance/resolveEntities.ts` ≥ `1`. The check must be `.some(rc => rc.name === 'LearningArtifact')`, NOT `.includes('LearningArtifact')`.
    - **No string-includes fallback:** `grep -cE "parentChainOf\\([^)]+\\)\\.includes\\(\\s*['\"]LearningArtifact" src/maintenance/resolveEntities.ts` returns `0` (would be wrong because parentChainOf returns objects, not strings).
    - **MatchResult → EntityId reverse lookup present:** `grep -cE "matchedTo\\.name|c\\.name === result\\.matchedTo|result\\.matchedTo\\.name" src/maintenance/resolveEntities.ts` ≥ `2` (filter by name+description AND tie-break sort).
    - **Tie-break sort by lexicographic id:** `grep -cE "localeCompare|\\.sort\\(.*=>.*\\.id" src/maintenance/resolveEntities.ts` ≥ `1`.
    - **Unmatchable error path:** `grep -c "unmatchable" src/maintenance/resolveEntities.ts` ≥ `1`.
    - **Concurrency default uses `grep -F` for the `??` alternates** (the BRE form `??` is literal): `grep -cE "concurrency\\s*=\\s*3|CONCURRENCY\\s*=\\s*3" src/maintenance/resolveEntities.ts` ≥ `1` OR `grep -cF "?? 3" src/maintenance/resolveEntities.ts` ≥ `1`.
    - **batchSize default present:** `grep -cE "batchSize\\s*=\\s*30|BATCH_SIZE\\s*=\\s*30" src/maintenance/resolveEntities.ts` ≥ `1` OR `grep -cF "?? 30" src/maintenance/resolveEntities.ts` ≥ `1`.
    - `grep -c "slice(0, 200)" src/maintenance/resolveEntities.ts` ≥ 1 (OKM line 666 truncation preserved).
    - `grep -c "dryRun" src/maintenance/resolveEntities.ts` ≥ 1 (dryRun branch).
    - `grep -cE "console\\." src/maintenance/resolveEntities.ts src/maintenance/index.ts` returns `0`.
    - `grep -E "from '\\.\\./" src/maintenance/resolveEntities.ts | grep -vE "\\.js'" | grep -c .` returns `0` (CF-D06).
    - `grep -E "from '\\.\\./" src/maintenance/index.ts | grep -vE "\\.js'" | grep -c .` returns `0`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>resolveEntities + sub-barrel land; tsc clean; OKM algorithm ported with KM-Core deltas (top-level fn, LLMSemanticLayer caller-supplied, getDegree for survivor selection, ResolvedClass-aware parentChainOf walk for default classes, deterministic matchedTo→EntityId reverse lookup with lex-id tie-break); dryRun honored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for resolveEntities — happy path + dryRun + classes filter + active-only + error accumulation + ontology default-resolution + matchedTo lookup + tie-break</name>
  <files>/Users/Q284340/Agentic/km-core/tests/unit/maintenance/resolveEntities.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/tests/unit/llm-matcher.test.ts (analog — mock LLMClient pattern)
    - /Users/Q284340/Agentic/km-core/tests/unit/maintenance/mergeEntities.test.ts (Plan 05 output — makeStoreCtx pattern)
    - /Users/Q284340/Agentic/km-core/tests/unit/_helpers/fakes.ts (mkEntity helper)
    - /Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts (Task 1 output)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (section on resolveEntities.spec.ts — 5 test cases listed)
  </read_first>
  <behavior>
    - Test A — Happy path: seed 4 Observation entities in a tmpdir store (use Plan 01's live ontology dir for registry); 2 of them have near-duplicate names AND descriptions. Construct a mock LLMSemanticLayer whose `match` returns `{ matched: true, confidence: 0.95, matchedTo: { name: <duplicate.name>, description: <duplicate.description> } }` when called with the pair, else `{ matched: false, confidence: 0 }`. Call `resolveEntities(store, { llmMatcher: mock, provenance, classes: ['Observation'] })`. Assertions: `result.merges.length === 1`, `result.merges[0].ontologyClass === 'Observation'`, `result.merges[0].confidence === 0.95`, after the call `await store.findByOntologyClass('Observation')` returns 3 entities (duplicate was superseded; active-only default excludes it).
    - Test B — DryRun: same setup as A; call with `dryRun: true`. Assertions: `result.merges.length === 1` (plan is computed), `result.dryRun === true`, `await store.findByOntologyClass('Observation')` still returns 4 entities (no actual merge).
    - Test C — Class filter: seed 3 Observation + 3 Digest entities with a deliberate duplicate pair in each class. Mock LLMSemanticLayer matches both pairs. Call with `classes: ['Digest']`. Assert: `result.classesScanned === ['Digest']`, the Observation duplicate pair is UNTOUCHED (still 3 active observations), the Digest duplicate is merged (2 active digests).
    - Test D — Active-only: seed 2 active Observation entities + 1 already-superseded Observation (write with `validUntil` in the past via the trusted path). Mock matcher would match the superseded one with an active one if called. Call resolveEntities. Assert: matcher was called only with active entities; `result.merges.length === 0` (the active pair isn't a match per the mock); the superseded entity remains superseded.
    - Test E — Error accumulation (LLM throw): mock LLMSemanticLayer whose `match` throws on the third call. Seed 4 entities. Call resolveEntities. Assert: NO throw bubbles out; `result.errors.length >= 1`; `result.errors[0]` contains `'LLM resolution error'`.
    - Test F — mergeEntities failure caught: mock LLMSemanticLayer returns match; pre-seed a SUPERSEDED_BY edge on one of the entities (forcing WR-02 violation when mergeEntities is invoked). Call resolveEntities. Assert: `result.errors.length >= 1` with message containing `'mergeEntities failed'`; the OTHER pairs (if any) continue to be scanned. No throw bubbles out.
    - Test G — Ontology default-resolution (parentChainOf .name match): open a store with `ontologyDir = <km-core>/ontology` (Plan 01's live directory). Seed 2 Observation entities (near-duplicate) + 2 Digest entities (near-duplicate). Call resolveEntities with `classes` OMITTED. Assert: `result.classesScanned.sort()` includes both `'Observation'` and `'Digest'` (and possibly `'Insight'` if Plan 01 registered it — but no insight entities means no merge for that class). Both pairs are merged. This pins that the default-class resolution actually finds Plan 01's LearningArtifact subclasses (the B1 fix from revision 1 — the `.includes('LearningArtifact')` bug would silently return zero subclasses and produce zero merges).
    - Test H — Ontology missing: open a store WITHOUT `ontologyDir` set (so `store.ontology === undefined`). Call resolveEntities with `classes` omitted. Assert throw with message containing `'opts.classes omitted but store has no ontology registry'`.
    - Test I — getDegree survivor selection: seed entities A (degree 5) and B (degree 1) that the matcher says are duplicates. Verify after merge that survivor is A (the higher-degree node). Implement by adding extra dummy entities + edges to bump A's degree before merge.
    - Test J — Unmatchable matchedTo: mock LLMSemanticLayer returns `{ matched: true, confidence: 0.95, matchedTo: { name: 'GHOST', description: 'not-in-pool' } }` — a name+description that does NOT exist in the candidate pool. Call resolveEntities. Assert: `result.merges.length === 0`, `result.errors.length >= 1`, `result.errors[0]` contains `'unmatchable'` (no merge invoked; LLM hallucination caught).
    - Test K — Tie-break on duplicate name+description (NEW per checker B4): seed three Observation entities where Entity1 and Entity2 share EXACTLY the same name + description (Entity1.id lex-smaller than Entity2.id; both differ from Entity3). Mock matcher's `match(subject, candidates)` returns `{ matched: true, confidence: 0.95, matchedTo: { name: Entity1.name, description: Entity1.description } }` when subject is Entity3. Call resolveEntities. Assert: `result.merges.length === 1` AND `result.merges[0].duplicateId === Entity1.id` (lex-smallest of the two name+description-equal candidates wins — deterministic tie-break per Task 1 contract). Re-run the same call (in a fresh store with identical seeding) and assert the SAME duplicateId is picked (deterministic across runs).
  </behavior>
  <action>
    Create `/Users/Q284340/Agentic/km-core/tests/unit/maintenance/resolveEntities.test.ts`. Reuse `makeStoreCtx` from `mergeEntities.test.ts` (copy verbatim into this file's helper block — DO NOT add cross-test-file imports per existing test conventions).
    For tests A-F and J-K, construct the store WITHOUT `ontologyDir` and call resolveEntities with `classes: ['Observation']` or `classes: ['Digest']` — bypasses the registry requirement. For test G, construct the store WITH `ontologyDir = path.resolve(__dirname, '..', '..', '..', 'ontology')` (the live Plan 01 directory).
    Mock LLMSemanticLayer: simple object with a `match: vi.fn()` method. Use `vi.fn().mockImplementation(...)` to return per-call values. For test E (throw on 3rd call): `let calls = 0; mock.match = vi.fn(async () => { calls++; if (calls === 3) throw new Error('mock LLM throws'); return { matched: false, confidence: 0 }; });`.
    For test I (degree-based survivor): seed entities A, B, X1, X2, X3, X4. Add edges A→X1, A→X2, A→X3, A→X4, A→B → A has degree 5. Add edge B→X1 → B has degree 2. Confirm via `await store.getDegree(A.id) === 5` BEFORE calling resolveEntities. Have the mock match A and B; assert `result.merges[0].survivorId === A.id`.
    For test K (tie-break): seed Entity1 with `id` like `'00000000-0000-0000-0000-000000000001'` and Entity2 with `'00000000-0000-0000-0000-000000000002'` so lex-comparison is unambiguous; both with SAME name + description. Entity3 is the subject the matcher matches against. After resolveEntities, assert `merges[0].duplicateId === Entity1.id`.
    `provenance` fixture: `{ provider: 'resolveEntities-test', model: 'phase-41-plan-06', runId: 'test-run-' + Date.now(), timestamp: new Date().toISOString() }`.
    `afterEach`: close store + rm tmpdir.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx vitest run tests/unit/maintenance/resolveEntities.test.ts 2>&1 | grep -E "(passed|failed|Tests )" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/Q284340/Agentic/km-core/tests/unit/maintenance/resolveEntities.test.ts` exists.
    - `npx vitest run tests/unit/maintenance/resolveEntities.test.ts` exits 0 with ≥11 passing tests (A through K).
    - `grep -c "test(" tests/unit/maintenance/resolveEntities.test.ts` ≥ 11.
    - `grep -c "dryRun" tests/unit/maintenance/resolveEntities.test.ts` ≥ 2 (Test B + others).
    - `grep -c "store.getDegree\\|getDegree(" tests/unit/maintenance/resolveEntities.test.ts` ≥ 1 (Test I).
    - **Unmatchable test present:** `grep -c "unmatchable\\|GHOST\\|not-in-pool" tests/unit/maintenance/resolveEntities.test.ts` ≥ 1 (Test J).
    - **Tie-break test present:** `grep -cE "duplicateId.*Entity1|tie-break|localeCompare|00000000-0000-0000-0000-000000000001" tests/unit/maintenance/resolveEntities.test.ts` ≥ 1 (Test K).
    - **parentChainOf-via-ontology coverage:** `grep -c "ontologyDir\\|classesScanned" tests/unit/maintenance/resolveEntities.test.ts` ≥ 1 (Test G).
    - Full vitest suite still green: `npx vitest run` exits 0.
  </acceptance_criteria>
  <done>11 tests pass; happy-path + dryRun + class-filter + active-only + LLM-error + merge-failure + ontology-default-via-parentChainOf-by-name + ontology-missing + degree-survivor + unmatchable-matchedTo + tie-break-by-lex-id all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend package.json exports map + append Phase 41 block to root barrel</name>
  <files>/Users/Q284340/Agentic/km-core/package.json, /Users/Q284340/Agentic/km-core/src/index.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/package.json (lines 7-24 — existing exports map; `./ontology`, `./pipeline`, `./dedup` precedents)
    - /Users/Q284340/Agentic/km-core/src/index.ts (lines 91-122 Phase 40 block — pattern to emulate)
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-PATTERNS.md (sections on `src/index.ts` (MODIFY) and `package.json` (MODIFY))
    - .planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md (D-49a — exports map gains `./maintenance` entry)
  </read_first>
  <behavior>
    - package.json `exports` map MUST gain two new entries (alphabetical order with existing entries):
      ```
      "./adapters/online": { "types": "./dist/adapters/online/index.d.ts", "import": "./dist/adapters/online/index.js" },
      "./maintenance": { "types": "./dist/maintenance/index.d.ts", "import": "./dist/maintenance/index.js" }
      ```
    - `src/index.ts` root barrel MUST append a Phase 41 block AFTER the Phase 40 block. Format:
      ```
      // Phase 41 (INT-01 + PIPE-02): online-learning adapter + post-hoc resolveEntities.
      // ...
      export { reprojectFromOnlineStore } from './adapters/online/index.js';
      export type { ReprojectOptions, ReprojectResult, ReprojectCheckpoint } from './adapters/online/index.js';
      export { mapObservationRow, mapDigestRow, mapInsightRow } from './adapters/online/index.js';
      export type { ObservationRow, DigestRow, InsightRow } from './adapters/online/index.js';
      export { resolveEntities, mergeEntities } from './maintenance/index.js';
      export type { ResolveOptions, ResolveResult, ResolveEvent, MergeOptions, MergeResult } from './maintenance/index.js';
      ```
    - Both root barrel and sub-paths MUST resolve cleanly from an external tmpdir consumer (smoke test below).
  </behavior>
  <action>
    Edit `/Users/Q284340/Agentic/km-core/package.json`: insert the two new exports map entries. Keep JSON formatting consistent with existing entries (2-space indent, no trailing commas, key order matches existing entries: `"types"` then `"import"`).
    Edit `/Users/Q284340/Agentic/km-core/src/index.ts`: append the Phase 41 block at the end of the file, mirroring the Phase 40 block at lines 91-122 (block comment then runtime exports then type exports). Do NOT modify the Phase 40 block or any earlier block.
    Run `npx tsc` (full build, not just `--noEmit`) to confirm dist/maintenance/index.{js,d.ts} and dist/adapters/online/index.{js,d.ts} are emitted.
    Smoke-compile from external tmpdir: create a temporary directory; `npm init -y`; `npm install <km-core-local-path>`; write a `smoke.ts` that imports both root-barrel symbols AND sub-path symbols (`@fwornle/km-core` for root, `@fwornle/km-core/maintenance` for sub-path, `@fwornle/km-core/adapters/online` for adapter sub-path); compile with NodeNext + ES2022. The compile must succeed for all three import surfaces.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc 2>&1 | tail -5 && ls dist/maintenance/index.{js,d.ts} && ls dist/adapters/online/index.{js,d.ts} && jq -r '.exports | keys | .[]' package.json | sort | tr '\n' ' '</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.exports | keys | .[]' /Users/Q284340/Agentic/km-core/package.json | sort | tr '\n' ' '` returns `. ./adapters/online ./dedup ./maintenance ./ontology ./pipeline ` (all 6 entries present).
    - `jq -r '.exports."./maintenance".types' /Users/Q284340/Agentic/km-core/package.json` returns `./dist/maintenance/index.d.ts`.
    - `jq -r '.exports."./maintenance".import' /Users/Q284340/Agentic/km-core/package.json` returns `./dist/maintenance/index.js`.
    - `jq -r '.exports."./adapters/online".import' /Users/Q284340/Agentic/km-core/package.json` returns `./dist/adapters/online/index.js`.
    - `npx tsc` exits 0 (full build, not just --noEmit).
    - Files `/Users/Q284340/Agentic/km-core/dist/maintenance/index.js` and `/Users/Q284340/Agentic/km-core/dist/maintenance/index.d.ts` exist.
    - Files `/Users/Q284340/Agentic/km-core/dist/adapters/online/index.js` and `/Users/Q284340/Agentic/km-core/dist/adapters/online/index.d.ts` exist.
    - `grep -c "Phase 41" src/index.ts` ≥ 1 (block-comment landed).
    - `grep -c "export.*from './maintenance/index.js'" src/index.ts` ≥ 1.
    - `grep -c "export.*from './adapters/online/index.js'" src/index.ts` ≥ 1.
    - External tmpdir smoke-compile succeeds for all three import surfaces (root barrel + `./maintenance` + `./adapters/online`).
    - Full vitest suite still green: `npx vitest run` exits 0.
  </acceptance_criteria>
  <done>package.json exports map extended; root barrel appends Phase 41 block; dist contains both new sub-paths; external smoke-compile clean across root + 2 sub-paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolveEntities ← caller-supplied LLMSemanticLayer | Caller injects the LLM matcher; matcher is responsible for its own client + parse-error handling (Plan 40 LLMSemanticMatcher). |
| resolveEntities → mergeEntities | Trusted-caller pattern; mergeEntities validates inputs and atomically commits. |
| Public API addition (resolveEntities, sub-path exports) | New methods become part of the public surface; future removal would be a breaking change. |
| LLM prompt content ← entity name + description (200-char truncation) | Prompt size bound by truncation per OKM line 666; entity descriptions may contain project-internal text but are already filtered by upstream sources. |
| LLM-returned matchedTo → EntityId mapping | LLM returns name+description, not id; resolveEntities does the reverse lookup. Unmatchable LLM outputs (hallucinated names) are caught as `errors[]` entries — NOT propagated as merges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06-01 | Tampering | malicious LLM output causes unrelated entity merge | mitigate | Confidence threshold (default 0.70) gates match acceptance. dryRun returns the plan for human inspection before applying. mergeEntities throws on WR-02 violation (atomicity preserved even on bad LLM input). MatchResult.matchedTo → EntityId reverse lookup requires name AND description equality with a candidate; LLM-hallucinated `matchedTo` values (no candidate matches) push to `errors[]` and skip — NEVER merge unmatchable. |
| T-41-06-02 | Information Disclosure | LLM prompt contains project-internal data | accept | Per CONTEXT.md `<deferred>` PII hooks deferred to a future phase. A's `ObservationSanitizer` upstream filters. resolveEntities is library code; caller (per-system script) is the trust boundary for the LLM endpoint. |
| T-41-06-03 | Tampering | concurrent resolveEntities calls corrupt state | accept | Plan 05 `mergeEntities` is the atomicity boundary (single batch). Two concurrent resolveEntities calls would serialize merges through the batch — last writer wins on a given entity. Out-of-scope to lock-file. |
| T-41-06-04 | Denial of Service | per-batch O(batch²) LLM calls explode cost on huge classes | accept | OKM-pattern BATCH_SIZE=30 bounds per-batch cost to ≤900 LLM calls per batch (less in practice — `match()` returns no-match quickly). Operators control which classes are scanned via `opts.classes`. A future phase may add a `batchMatch` method to LLMSemanticLayer to reduce O(n²) to O(n). |
| T-41-06-05 | Repudiation | merge decisions are auditable | mitigate | `result.merges[]` records every merge with confidence + survivor + duplicate. `result.errors[]` records every LLM failure or merge failure or unmatchable matchedTo. The provenance stamp threaded into mergeEntities lands in survivor's resolutionHistory (Plan 05 Test A). |
| T-41-06-06 | Spoofing | caller injects malicious LLMSemanticLayer | accept | Caller controls the matcher; library cannot defend against caller injecting a malicious matcher. The trust boundary is the calling script (Plan 07's reproject-online.mjs) which controls LLM endpoint selection. |
| T-41-06-07 | Tampering | LLM returns matchedTo for an entity not in candidate pool | mitigate | The `matchedTo → EntityId` reverse lookup filters by name+description; zero matches → unmatchable error (Test J pins). Deterministic tie-break (`localeCompare` on id) handles legitimate name+description collisions across distinct entities (Test K pins). |
| T-41-06-08 | Tampering | npm installs | mitigate | No new npm dependencies. package.json changes are confined to the `exports` map (no `dependencies` / `devDependencies` modifications). |
</threat_model>

<verification>
- `resolveEntities` is callable; OKM algorithm ported with KM-Core deltas (LLMSemanticLayer caller-supplied, getDegree survivor selection, ontology-default class resolution via parentChainOf-by-name, deterministic matchedTo→EntityId reverse lookup with lex-id tie-break).
- `dryRun` returns plan without mutating.
- Class filter, active-only default, error accumulation, ontology default-resolution, ontology-missing throw, unmatchable matchedTo, name+description tie-break — all pinned by tests.
- package.json exports map gains `./maintenance` + `./adapters/online`.
- Root barrel re-exports Phase 41 surface.
- External tmpdir smoke-compile succeeds.
- TypeScript strict build clean; full vitest suite green.
</verification>

<success_criteria>
- PIPE-02 SC#3 reachable: an operator calling `resolveEntities(store, { llmMatcher, provenance, classes: ['Observation'] })` against a graph with known cross-batch duplicates collapses them into one canonical entity with merged provenance (Test A pins).
- PIPE-02 SC#4 reachable: the same `resolveEntities` API is callable against an A-adapter-fronted graph (Plan 04 reproject populates the store; Plan 07 integration test exercises the full pipeline).
- Default-class resolution actually finds LearningArtifact subclasses (Test G pins via the live ontology dir + parentChainOf-by-name walk — the B1 revision bug fix).
- LLM hallucinations CANNOT cause unintended merges (Test J pins unmatchable error path; Test K pins deterministic tie-break).
- INT-01 surface complete: `@fwornle/km-core/adapters/online` exposes `reprojectFromOnlineStore`; `@fwornle/km-core/maintenance` exposes `resolveEntities` + `mergeEntities`; root barrel re-exports all three.
- D-49 + D-49a + D-50 + D-50a + CF-D34 + CF-D14 all encoded in plan structure.
</success_criteria>

<output>
Create `.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-06-SUMMARY.md` when done.
</output>
