---
phase: 11-content-quality-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts
autonomous: true
requirements: [QUAL-09]

must_haves:
  truths:
    - "L3 detail nodes are capped at 5 per L2 parent (down from 8)"
    - "Code-evidence filtering actively rejects L3 entities without file/class/function references in observations"
    - "All three wave agent prompts include hardened anti-hallucination instructions"
    - "Wave 1 and Wave 2 prompts include code-evidence requirements matching Wave 3 quality"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts"
      provides: "Tightened L3 cap (MAX_L3_PER_AGENT = 5), strengthened code-evidence filter"
      contains: "MAX_L3_PER_AGENT = 5"
    - path: "integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts"
      provides: "Hardened anti-hallucination prompt, tightened L3 suggestion cap"
      contains: "DO NOT hallucinate"
    - path: "integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts"
      provides: "Anti-hallucination prompt hardening"
      contains: "DO NOT hallucinate"
  key_links:
    - from: "wave3-detail-agent.ts discoverL3Details"
      to: "code-evidence filter"
      via: "hasCodeEvidence check on parsed LLM response"
      pattern: "hasCodeEvidence"
    - from: "wave2-component-agent.ts"
      to: "L3 suggestion cap"
      via: "MAX_L3_PER_L2 constant"
      pattern: "MAX_L3_PER_L2"
---

<objective>
Harden anti-hallucination guards across all wave agents: tighten L3 caps, strengthen code-evidence filtering, and add explicit anti-hallucination instructions to Wave 1 and Wave 2 prompts.

Purpose: Wave 3 already has basic anti-hallucination guards (code evidence filter, observation validation). But Wave 1 and 2 lack these guards, and the L3 caps are too generous (8 per agent, 6 per L2 suggestion). This plan tightens all guards uniformly.

Output: All three wave agents with hardened prompts and tighter caps.
</objective>

<execution_context>
@/Users/Q284340/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Q284340/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts
@integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts
@integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts

<interfaces>
<!-- From wave3-detail-agent.ts -->
```typescript
// Line 344: MAX_L3_PER_AGENT = 8 (too high, reduce to 5)
// Line 329-336: hasCodeEvidence filter checks observations for file paths, class names
// Line 201-256: LLM prompt with anti-hallucination rules (good template for Wave 1/2)
// Line 423-436: ensureMinimumObservations checks for >= 3 specific observations
```

<!-- From wave2-component-agent.ts -->
```typescript
// Line 356: MAX_L3_PER_L2 = 6 (reduce to 4)
// Line 240: Only anti-hallucination instruction: "BAD observations (DO NOT write these)"
// Line 250: "Only suggest L3 entities that have clear code evidence"
```

<!-- From wave1-project-agent.ts -->
```typescript
// Line 296: Only anti-hallucination instruction: "BAD observations (DO NOT write these)"
// No code-evidence requirement in prompt
```

<!-- wave-controller.ts caps -->
```typescript
// Line 814: MAX_TOTAL_L3_AGENTS = 80 (reduce to 50)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten L3 caps and strengthen code-evidence filtering in Wave 3</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts</files>
  <action>
In wave3-detail-agent.ts:

1. **Reduce `MAX_L3_PER_AGENT`** from 8 to 5 (line ~344). This caps L3 entities per Wave 3 agent invocation. The prompt already says "2-8" for entities with files -- change to "2-5" in the prompt (line ~212).

2. **Strengthen the code-evidence filter** (lines ~329-336). Currently it checks if `detail.observations.some(obs => ...)` has a code reference. Make it stricter:
   - Require at least 2 out of N observations to contain code evidence (not just 1)
   - Add a `hasSpecificCodeReference()` helper that checks for: file paths (containing `/` or `.ts`/`.js`/`.py`), class/function names (PascalCase or camelCase identifiers), line number references (`:NN`)
   - Log filtered entities: `[Wave3Agent] Filtered L3 entity '${name}': insufficient code evidence (${evidenceCount}/${total} observations)`

3. **Harden the LLM prompt** (lines ~201-256):
   - Add after the existing "CRITICAL RULE" block:
     ```
     ANTI-HALLUCINATION RULES:
     - DO NOT invent file paths, class names, or function names that are not in the Source Files above
     - DO NOT reference modules, packages, or libraries not visible in the source code
     - If you are uncertain whether something exists, DO NOT include it
     - Every observation MUST quote a specific artifact from the Source Files section
     - Prefer returning fewer high-quality entities over many speculative ones
     ```
   - Change the "Identify 2-8 Detail-level" to "Identify 2-5 Detail-level" (with files) and "1-3" to "1-2" (without files)

4. **Strengthen `isSpecificObservation()`** if it exists -- ensure it checks for code artifact references, not just length.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MAX_L3_PER_AGENT = 5. Code evidence filter requires 2+ observations with references. Prompt includes ANTI-HALLUCINATION RULES block. Entity counts in prompt reduced.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Wave 1 and Wave 2 prompts, tighten Wave 2 L3 suggestion cap, reduce global L3 cap</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts, integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts, integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts</files>
  <action>
**In wave1-project-agent.ts**, find the LLM prompt (around line 296 near "BAD observations"):

1. Add an ANTI-HALLUCINATION block after the existing "BAD observations" section:
   ```
   ANTI-HALLUCINATION RULES:
   - Every component you identify MUST correspond to actual directories, modules, or systems visible in the repository
   - DO NOT invent components that you cannot point to in the codebase
   - Each observation MUST reference specific files, directories, or configuration artifacts
   - If you are uncertain whether a component exists, DO NOT include it
   - It is better to return fewer accurate components than many speculative ones
   ```

2. Ensure each observation requirement in the prompt explicitly demands a code artifact reference (file path, directory, config file).

**In wave2-component-agent.ts**, find the LLM prompt (around line 240 near "BAD observations"):

1. Add the same ANTI-HALLUCINATION block as Wave 1 (adapted for sub-components):
   ```
   ANTI-HALLUCINATION RULES:
   - Every sub-component you identify MUST correspond to actual code in the Source Files above
   - DO NOT invent sub-components, classes, or modules not visible in the source code
   - Each observation MUST reference specific files, classes, or functions FROM the Source Files section
   - If source files are empty or insufficient, return fewer sub-components rather than speculating
   - DO NOT reference file paths or code artifacts not shown in the Source Files section
   ```

2. **Reduce `MAX_L3_PER_L2`** from 6 to 4 (line ~356). Also update the prompt if it mentions a number of L3 suggestions to suggest.

**In wave-controller.ts**, reduce `MAX_TOTAL_L3_AGENTS` from 80 to 50 (line ~814). This global cap limits total L3 agent spawns across all L2 entities.

All three files must compile cleanly.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All three wave agent prompts include ANTI-HALLUCINATION RULES. MAX_L3_PER_L2 = 4. MAX_TOTAL_L3_AGENTS = 50. Full build succeeds.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit`
2. Build succeeds: `cd integrations/mcp-server-semantic-analysis && npm run build`
3. L3 cap reduced: `grep "MAX_L3_PER_AGENT\|MAX_L3_PER_L2\|MAX_TOTAL_L3_AGENTS" integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts`
4. Anti-hallucination in all agents: `grep -l "ANTI-HALLUCINATION" integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts`
5. Code-evidence filter strengthened: `grep "hasSpecificCodeReference\|evidenceCount" integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts`
</verification>

<success_criteria>
- MAX_L3_PER_AGENT = 5 (was 8), MAX_L3_PER_L2 = 4 (was 6), MAX_TOTAL_L3_AGENTS = 50 (was 80)
- All three wave agent LLM prompts include ANTI-HALLUCINATION RULES block
- Wave 3 code-evidence filter requires 2+ observations with code references (was 1+)
- TypeScript compiles and builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/11-content-quality-gate/11-03-SUMMARY.md`
</output>
