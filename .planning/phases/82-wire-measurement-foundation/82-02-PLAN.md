---
phase: 82-wire-measurement-foundation
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/messages-tap-cache-parse.test.mjs
autonomous: true
requirements: [WIRE-02, WIRE-03]
must_haves:
  truths:
    - "/v1/messages tap parses cache_read_input_tokens + cache_creation_input_tokens (and the cache_creation object form) from SSE message_start usage AND the non-streaming JSON usage"
    - "total_tokens still equals uIn + uOut — cache is surfaced separately, never folded into total"
    - "The tap honors x-task-id and x-agent headers with precedence over ambient resolveLiveTaskId(); empty header falls back to ambient"
    - "When x-agent != claude, the tap stamps agent, process='token-adapter-<agent>', and a matching non-cladpt user_hash satisfying /^[a-z][a-z0-9]{5}$/"
  artifacts:
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts"
      provides: "pure parseUsageCache(usage) helper — flat + cache_creation-object forms"
      exports: ["parseUsageCache"]
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs"
      provides: "tap cache capture + x-task-id/x-agent binding + non-cladpt stamping"
      contains: "cache_read_tokens"
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/messages-tap-cache-parse.test.mjs"
      provides: "fixture-based parseUsageCache assertions (both wire forms + total exclusion)"
  key_links:
    - from: "SSE message_start.message.usage"
      to: "logTokenCall row.cache_read_tokens / cache_write_tokens"
      via: "parseUsageCache extraction"
      pattern: "cache_read_tokens: "
    - from: "req.headers['x-task-id'] / ['x-agent']"
      to: "tap taskId / agent / user_hash"
      via: "header precedence over resolveLiveTaskId()"
      pattern: "x-task-id"
---

<objective>
Upgrade the /v1/messages tap in the proxy so it (a) parses cache usage off the Anthropic wire (SSE + non-streaming) and writes it via the Plan-01 columns, and (b) binds each captured row to the correct task+agent via `x-task-id`/`x-agent` request headers, killing the ambient-span singleton that leaked concurrent traffic (the v9 opencode rows contaminated with observation-writer/health-coordinator calls).

Purpose: The dossier's claim "proxy routing corrupts claude cache accounting" is FALSE — the wire always carried cache usage, the tap simply never parsed it. This plan fixes that root cause AND replaces the hardcoded `resolveLiveTaskId()` + `agent:'claude'` + `user_hash:'cladpt'` with per-request header binding so concurrent spans (interactive + N experiment cells) no longer cross-contaminate.
Output: A new pure helper `src/usage-cache.ts` (unit-testable, compiles to dist/), tap edits in `proxy-bridge/server.mjs` (runtime, no build), and a fixture-based integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-wire-measurement-foundation/82-CONTEXT.md
@.planning/phases/82-wire-measurement-foundation/82-PATTERNS.md
@.planning/phases/82-wire-measurement-foundation/82-01-SUMMARY.md

<interfaces>
From /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (extracted line map):
- Tap handler spans :1898-2096. Ambient task binding today: taskId = resolveLiveTaskId() (:1930); row hardcodes user_hash:'cladpt' (:2085), agent:'claude', process:proc (:2084-2085).
- SSE parse loop reads input_tokens/output_tokens off message_start.message.usage + message_delta.usage at :2029-2044. Non-streaming JSON path at :2057-2064 (j.usage). logTokenCall row build at :2071-2089. logErr non-fatal guards at :2091-2093.
- Forward-headers copied verbatim at :1974-1979 (so ANTHROPIC_CUSTOM_HEADERS reaches the tap; tap reads req.headers directly — no forwarding change needed).
- Shim precedence block to MIRROR verbatim: :2144-2153 — hdrTaskId = req.headers['x-task-id']?.trim(); hdrAgent = req.headers['x-agent']?.trim(); taskId = hdrTaskId || resolveLiveTaskId(); agent = hdrAgent || 'claude'.
- resolveLiveTaskId + sanitizeTaskId imported from ../dist/measurement-span.js (:49).
- Adapter user_hash contract: must match /^[a-z][a-z0-9]{5}$/ (token-db.mjs:38-43; cladpt/copadt precedent). Derive from agent, e.g. copilot→'copadt', opencode→'opcadt', mastra→'mstadt' (6-char lowercase, first alpha).
Wire usage fields: usage.cache_read_input_tokens (flat), usage.cache_creation_input_tokens (flat), usage.cache_creation (newer OBJECT with numeric sub-fields to sum defensively).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseUsageCache helper + tap cache capture</name>
  <files>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs
  </files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (read :1898-2096 — the full tap handler)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ /v1/messages cache tap)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-schema-migration.test.mjs (dist-import harness style)
  </read_first>
  <behavior>
    - parseUsageCache({cache_read_input_tokens:5, cache_creation_input_tokens:8}) → {cacheRead:5, cacheWrite:8}.
    - parseUsageCache({cache_read_input_tokens:5, cache_creation:{ephemeral_5m_input_tokens:3, ephemeral_1h_input_tokens:4}}) → {cacheRead:5, cacheWrite:7} (sums numeric sub-fields of the object form).
    - parseUsageCache(undefined) or {} → {cacheRead:0, cacheWrite:0}; non-numeric fields coalesce to 0.
    - Tap: after an SSE stream carrying cache usage, the persisted row's total_tokens == uIn+uOut (cache excluded), and cache_read_tokens/cache_write_tokens equal the parsed values.
  </behavior>
  <action>
    Create /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/usage-cache.ts exporting a pure `parseUsageCache(usage)` returning `{cacheRead, cacheWrite}`: cacheRead = numeric usage.cache_read_input_tokens ?? 0; cacheWrite = numeric usage.cache_creation_input_tokens, else if usage.cache_creation is an object sum its numeric values, else 0. No I/O, no throw, all numerics coalesced to 0. In server.mjs, import parseUsageCache from ../dist/usage-cache.js and, in BOTH the SSE parse loop (:2029-2044, off message_start.message.usage — the delta path carries no cache) and the non-streaming JSON path (:2057-2064, off j.usage), accumulate cacheRead/cacheWrite alongside uIn/uOut. Add `cache_read_tokens: cacheRead, cache_write_tokens: cacheWrite` to the logTokenCall row object (:2071-2089). Preserve the existing contract total_tokens = uIn + uOut — do NOT fold cache into total. Keep all writes inside the existing logErr non-fatal guard.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build && node -e "const {parseUsageCache}=require('./dist/usage-cache.js'); const a=parseUsageCache({cache_read_input_tokens:5,cache_creation_input_tokens:8}); const b=parseUsageCache({cache_read_input_tokens:5,cache_creation:{x:3,y:4}}); if(a.cacheRead!==5||a.cacheWrite!==8||b.cacheWrite!==7){process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` probe above prints `ok` (flat + object cache_creation forms both parse correctly).
    - `grep -n "cache_read_tokens: " proxy-bridge/server.mjs` returns a match inside the logTokenCall row build (both SSE + non-streaming paths feed it).
    - `grep -n "import.*parseUsageCache.*dist/usage-cache" proxy-bridge/server.mjs` returns a match.
    - `npm run build` exits 0.
    - No fold-in: the line computing total_tokens still reads uIn + uOut (grep confirms cache vars are not added into total).
  </acceptance_criteria>
  <done>parseUsageCache exists and is unit-callable; the tap writes cache_read/cache_write from both wire forms; total unchanged; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Per-request x-task-id/x-agent binding + non-cladpt stamping</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (read :1920-1960 for the taskId=resolveLiveTaskId() site, :2071-2096 for the row build, :2144-2153 for the shim precedence block to mirror)
    - /Users/Q284340/Agentic/coding/lib/lsl/token/token-db.mjs (read :36-45 for the user_hash /^[a-z][a-z0-9]{5}$/ contract + cladpt/copadt precedent)
    - .planning/phases/82-wire-measurement-foundation/82-CONTEXT.md (§ Per-request task binding)
  </read_first>
  <action>
    Replace the ambient-only binding in the tap. At the taskId resolution site (:1930), compute hdrTaskId from req.headers['x-task-id'] (string, trimmed, else '') and hdrAgent from req.headers['x-agent'] (string, trimmed, else '') exactly like the shim block at :2144-2153, then taskId = hdrTaskId || resolveLiveTaskId() and agent = hdrAgent || 'claude'. In the row build (:2084-2085), when agent === 'claude' keep the current user_hash 'cladpt' and process; when agent !== 'claude' stamp agent=<agent>, process = 'token-adapter-' + agent, and user_hash = a deterministic 6-char lowercase code derived from the agent that matches /^[a-z][a-z0-9]{5}$/ (copilot→'copadt', opencode→'opcadt', mastra→'mstadt'; fall back to first-6-of-agent lowercased+padded for any other). Leave captureBelongsToRun (:1567, used :1956/:2163) intact as the secondary guard — header-bound task_ids simply bypass the ambient file. Sanitize the header task_id via the already-imported sanitizeTaskId before use.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && grep -nE "req.headers\['x-task-id'\]|req.headers\['x-agent'\]" proxy-bridge/server.mjs | grep -v 2144 | grep -v 2151 | head; grep -c "token-adapter-" proxy-bridge/server.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "x-task-id"` and `grep -n "x-agent"` in server.mjs each return a NEW match inside the /v1/messages tap region (:1898-2096), in addition to the pre-existing shim occurrences.
    - `grep -n "token-adapter-' + agent"` (or equivalent template) returns a match in the tap row build.
    - The claude path still stamps user_hash 'cladpt' (grep confirms the literal survives on the claude branch).
    - A code-review assertion: the derived non-cladpt user_hash values (copadt/opcadt/mstadt) all match `/^[a-z][a-z0-9]{5}$/` (state each literal in the SUMMARY).
    - `sanitizeTaskId` is applied to the header-supplied task id before it reaches the row.
  </acceptance_criteria>
  <done>Header binding wins over ambient; non-claude agents stamp agent/process/user_hash correctly; claude path unchanged; secondary guard intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SSE cache-parse integration test</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/messages-tap-cache-parse.test.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-schema-migration.test.mjs (harness: node:test + node:assert/strict, assert against ../../dist/)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/fixtures/mock-llm-responses.ts (fixture precedent)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ Test Patterns → SSE cache-parse)
  </read_first>
  <behavior>
    - Fixture A (flat form: cache_read_input_tokens + cache_creation_input_tokens) → parseUsageCache yields the exact read/write.
    - Fixture B (cache_creation object form) → cacheWrite equals the sum of the object's numeric sub-fields.
    - Assert total = uIn + uOut is independent of cache (compute uIn/uOut from message_start/message_delta and confirm cache is not summed in).
  </behavior>
  <action>
    Author the integration test importing parseUsageCache from ../../dist/usage-cache.js (after npm run build). Record two SSE message_start usage fixtures (flat cache_read_input_tokens/cache_creation_input_tokens variant and the cache_creation-object variant) plus their message_delta output. Assert parseUsageCache returns the expected cacheRead/cacheWrite for each, and assert an explicit check that total (uIn+uOut derived from the fixture) excludes cache tokens. Gate any live-proxy assertion on LLM_PROXY_LIVE=1 (env, not argv) per memory reference_node_test_argv_live_gate; the default run must pass purely against the pure helper + fixtures.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build && node --test tests/integration/messages-tap-cache-parse.test.mjs 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/messages-tap-cache-parse.test.mjs` exits 0.
    - The test asserts both wire forms (flat + cache_creation object) and the total-excludes-cache invariant.
    - The test imports from ../../dist/usage-cache.js (proves the shipped build, not the TS source).
  </acceptance_criteria>
  <done>Fixture-based test proves cache parsing for both wire forms and total exclusion; green after build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /v1/messages tap | x-task-id / x-agent request headers (untrusted) drive row attribution |
| Anthropic upstream → tap | SSE/JSON usage object parsed into DB counters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02-01 | Spoofing | x-agent header can claim any agent | mitigate | user_hash is derived to a constrained /^[a-z][a-z0-9]{5}$/ code; process forced to 'token-adapter-<agent>'; proxy is localhost-bound (127.0.0.1/host.docker.internal) so the header source is already a trusted local process — accepted residual documented |
| T-82-02-02 | Tampering | x-task-id path/injection into DB or filenames | mitigate | header task id passed through sanitizeTaskId (charset whitelist + path.basename) before use, per Plan 68-02 contract |
| T-82-02-03 | Information disclosure | wrong-task attribution leaks one span's tokens into another | mitigate | header precedence + captureBelongsToRun secondary guard together kill the ambient-singleton leak that this plan targets; live regression covered in Plan 06 (2-cell concurrent test) |
| T-82-02-04 | Denial of Service | malformed usage object crashes the tap | mitigate | parseUsageCache never throws (numeric coalesce); writes stay inside the existing logErr non-fatal guard |
| T-82-02-SC | Tampering | npm install of dependencies | accept | no new packages; extends existing seam |
</threat_model>

<verification>
- `npm run build` green; both node --test files pass.
- Manual grep confirms header binding present in the tap region distinct from the shim block.
- Live per-task binding correctness is deferred to Plan 06's 2-cell concurrent gate.
</verification>

<success_criteria>
Tap parses cache usage (both wire forms) and writes it via Plan-01 columns; total excludes cache; x-task-id/x-agent bind rows to the right task+agent with non-cladpt stamping; fixture test green.
</success_criteria>

<output>
Create `.planning/phases/82-wire-measurement-foundation/82-02-SUMMARY.md` when done.
</output>
