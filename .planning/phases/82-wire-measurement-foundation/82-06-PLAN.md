---
phase: 82-wire-measurement-foundation
plan: 06
type: execute
wave: 4
depends_on: [82-01, 82-02, 82-03, 82-04, 82-05]
files_modified:
  - /Users/Q284340/Agentic/coding/.planning/phases/82-wire-measurement-foundation/82-06-SUMMARY.md
autonomous: false
requirements: [WIRE-08]
must_haves:
  truths:
    - "The proxy is rebuilt (npm run build) and restarted (launchctl kickstart com.coding.llm-cli-proxy) with the Plan 01-03 code live"
    - "ANTHROPIC_CUSTOM_HEADERS forwards x-task-id to the tap in the newline-separated Name: value form (verified live BEFORE the full run)"
    - "A 2-cell experiment (claude + opencode) run CONCURRENTLY with an interactive session produces per-task rows with ZERO cross-contamination"
    - "Claude cache_read from the tap matches the cladpt transcript row within tolerance"
    - "GET /api/token-usage/recent returns cache_read_tokens + cache_write_tokens fields"
    - "A copilot BYOK file-creation task AND an opencode file-creation task each produce a REAL file on disk (tool passthrough behavioral acceptance)"
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/.planning/phases/82-wire-measurement-foundation/82-06-SUMMARY.md"
      provides: "live verification evidence (row queries, header trace, file-on-disk proof)"
  key_links:
    - from: "launched agent ANTHROPIC_CUSTOM_HEADERS"
      to: "tap x-task-id binding → token_usage.task_id"
      via: "live claude launch"
      pattern: "x-task-id"
    - from: "copilot BYOK /v1/copilot file-creation task"
      to: "actual file on disk"
      via: "tool-call passthrough"
      pattern: "tool_calls"
---

<objective>
Live-verify the whole wired substrate end to end: deploy the rebuilt proxy, confirm the ANTHROPIC_CUSTOM_HEADERS format EARLY (the header binding depends on it), run a concurrent 2-cell + interactive experiment to prove zero cross-contamination and cache fidelity, and prove the HARD tool-passthrough requirement — a copilot BYOK and an opencode file-creation task each write a real file on disk.

Purpose: Plans 01-05 prove the wiring in isolation; only a live run proves the ambient-singleton leak is dead, claude cache accounting matches cladpt, and agentic loops are real (not text hallucinations). This is the phase's acceptance gate.
Output: Deployment + a SUMMARY capturing row queries, the header trace, and the on-disk file proof. Human checkpoints gate the interactive/visual confirmations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-wire-measurement-foundation/82-CONTEXT.md
@.planning/phases/82-wire-measurement-foundation/82-PATTERNS.md
@.planning/phases/82-wire-measurement-foundation/82-01-SUMMARY.md
@.planning/phases/82-wire-measurement-foundation/82-02-SUMMARY.md
@.planning/phases/82-wire-measurement-foundation/82-03-SUMMARY.md
@.planning/phases/82-wire-measurement-foundation/82-04-SUMMARY.md
@.planning/phases/82-wire-measurement-foundation/82-05-SUMMARY.md

<interfaces>
Deploy (CLAUDE.md + CONTEXT §specifics): proxy src edits (token-usage.ts, usage-cache.ts, copilot-provider.ts) require `cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build`; server.mjs is runtime (no build). Restart: `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`. Restart can mis-detect corporate network — confirm coordinator :3034 location=open BEFORE kickstart (memory reference_llm_proxy_override_fallback). LLM proxy host port = 12435 (NOT 3033).
Query surfaces: GET http://127.0.0.1:12435/api/token-usage/recent returns rows including cache_read_tokens/cache_write_tokens; the token DB is at the LLM_PROXY_DATA_DIR/token-usage.db (query directly with sqlite3 for task_id/agent/user_hash/cache columns).
Spike BYOK invocation (adapt base to /v1/copilot): COPILOT_PROVIDER_BASE_URL=http://127.0.0.1:12435/v1/copilot COPILOT_PROVIDER_TYPE=openai COPILOT_PROVIDER_API_KEY=rapid-proxy-no-auth-placeholder COPILOT_MODEL=claude-haiku-4-5 COPILOT_AUTO_UPDATE=false copilot -p "..." --allow-all.
Leakage regression baseline: v9 opencode task rows contained observation-writer/health-coordinator calls stamped with the cell task_id — the exact failure this gate must NOT reproduce.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the rebuilt proxy + EARLY ANTHROPIC_CUSTOM_HEADERS format verification</name>
  <files>/Users/Q284340/Agentic/coding/.planning/phases/82-wire-measurement-foundation/82-06-SUMMARY.md</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md (§ km-core LLM proxy endpoint port 12435, § launchd daemons kickstart com.coding.llm-cli-proxy)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ Deploy note)
    - /Users/Q284340/.claude/projects/-Users-Q284340-Agentic-coding/memory/reference_llm_proxy_override_fallback.md
  </read_first>
  <action>
    Build and deploy: cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build (compiles token-usage.ts, usage-cache.ts, copilot-provider.ts to dist/). Confirm the coordinator at :3034 reports location=open BEFORE restarting (avoid the corporate-network mis-detect flap). Restart the proxy: launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy. Then EARLY-verify the header format: launch a minimal claude invocation with ANTHROPIC_CUSTOM_HEADERS set to a known sentinel task id (x-task-id: verify-82-06-<ts>), make one trivial LLM call, and confirm via a direct token DB query (or GET /api/token-usage/recent) that the resulting row's task_id equals the sentinel — proving the newline-separated Name: value format actually forwards to the tap. If the format does NOT forward, STOP and record the exact env var shape that DOES work (this de-risks the whole binding path). Capture the build output, coordinator location, and the header-trace result in the SUMMARY.
  </action>
  <verify>
    <automated>curl -s http://127.0.0.1:12435/api/token-usage/recent 2>/dev/null | head -c 400; echo; cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && ls -la dist/usage-cache.js dist/token-usage.js 2>&1 | head</automated>
  </verify>
  <acceptance_criteria>
    - dist/usage-cache.js and dist/token-usage.js exist and are newer than the src edits (npm run build ran).
    - The coordinator :3034 location was confirmed open before kickstart (record the value).
    - GET /api/token-usage/recent responds and its row objects include cache_read_tokens + cache_write_tokens keys.
    - A sentinel claude call with ANTHROPIC_CUSTOM_HEADERS produced a token_usage row whose task_id == the sentinel (header format verified live). The working env var shape is recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <done>Proxy rebuilt + restarted with Plan 01-03 live; /recent exposes cache fields; the ANTHROPIC_CUSTOM_HEADERS format is proven to bind the tap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live 2-cell concurrent experiment — zero cross-contamination + cache fidelity</name>
  <what-built>
    A concurrent measured run: a 2-cell experiment (one claude cell + one opencode cell) executed AT THE SAME TIME as an active interactive session, so three spans share the window. Plan 02's header binding + Plan 04's dedup-merge should keep every proxy row bound to its own task_id with correct cache accounting.
  </what-built>
  <how-to-verify>
    1. Start (or keep running) an interactive claude session in another pane so background daemon traffic (observation-writer, health-coordinator) is live during the run.
    2. Launch a 2-cell experiment with a claude cell and an opencode cell (each with a distinct cell.task_id) via the experiment runner.
    3. After completion, query the token DB grouped by task_id:
       - Each cell's rows carry ONLY that cell's task_id (no observation-writer/health-coordinator rows leaked in — the v9 regression must NOT reproduce).
       - The claude cell's tap cache_read_tokens matches the cladpt transcript row for the same (user_hash, tool_call_id) within tolerance (dedup-merge enriched, did not shadow).
       - The opencode cell rows are stamped agent=opencode with a non-cladpt user_hash (not the ambient cladpt).
    4. Confirm GET /api/token-usage/recent shows cache fields populated for the claude cell.
    Record the per-task_id row counts + a sample cache comparison in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" if per-task rows are clean with zero cross-contamination and claude cache matches cladpt; else describe the leakage/mismatch.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tool-passthrough behavioral acceptance — real files on disk</name>
  <what-built>
    The shim tool-call passthrough (Plan 03): a copilot BYOK run and an opencode run, each given a file-creation task, routed through the proxy shim with native tool_calls forwarded and mapped back — so the agent actually invokes its file-write tool instead of hallucinating success.
  </what-built>
  <how-to-verify>
    1. Copilot BYOK: run copilot with the spike BYOK env pointed at /v1/copilot (COPILOT_PROVIDER_BASE_URL=http://127.0.0.1:12435/v1/copilot, TYPE=openai, API_KEY=rapid-proxy-no-auth-placeholder, MODEL=claude-haiku-4-5, COPILOT_AUTO_UPDATE=false, copilot -p "create a file named /tmp/byok-proof-<ts>.txt containing OK" --allow-all). Confirm the file EXISTS on disk with the expected content (this is the exact spike gap — previously it claimed success with no file).
    2. Opencode: run an equivalent file-creation task through the opencode proxy path and confirm the file exists on disk.
    3. Confirm the proxy did NOT silently strip tools onto the claude-code CLI provider (capability gating held): the request landed on a function-calling-capable provider, or failed loudly — never a silent no-op.
    Record both file paths + contents + the provider each request landed on in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" if BOTH the copilot BYOK file and the opencode file exist on disk with correct content; else describe which run failed to produce a file.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator deploy → live proxy | build + launchctl kickstart swaps running proxy code |
| BYOK agent → /v1/copilot | tool schemas forwarded; agent executes file-write tools with --allow-all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-06-01 | Denial of Service | proxy restart mis-detects corporate network and flaps | mitigate | confirm coordinator :3034 location=open BEFORE kickstart (memory reference_llm_proxy_override_fallback); restart is launchd-managed (auto-respawn) |
| T-82-06-02 | Elevation of privilege | --allow-all file-creation task writes outside the intended path | mitigate | scope the acceptance task to a /tmp sentinel path with a timestamped name; review the created path before approving |
| T-82-06-03 | Information disclosure | concurrent run cross-contaminates spans (the very defect under test) | mitigate | this gate's PASS condition IS zero cross-contamination; a leak fails the checkpoint and blocks phase close |
| T-82-06-SC | Tampering | npm run build pulls new/changed dependencies | mitigate | build uses the already-locked rapid-llm-proxy dependency set (no new packages added across Plans 01-05); legitimacy table not required — verify `git status` shows no package.json/lockfile change before build |
</threat_model>

<verification>
- Deploy succeeded; /api/token-usage/recent exposes cache fields.
- Header format verified live (Task 1); concurrent run clean (Task 2); real files on disk (Task 3).
- All three human checkpoints approved.
</verification>

<success_criteria>
The rebuilt proxy is live; ANTHROPIC_CUSTOM_HEADERS binds the tap; a concurrent 2-cell + interactive run shows zero cross-contamination with claude cache matching cladpt; /recent returns cache fields; and copilot BYOK + opencode file-creation tasks each produce a real file on disk.
</success_criteria>

<output>
Create `.planning/phases/82-wire-measurement-foundation/82-06-SUMMARY.md` when done, capturing the deploy log, header trace, per-task row queries, cache comparison, and both on-disk file proofs.
</output>
