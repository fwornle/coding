---
phase: 82-wire-measurement-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-cache-migration.test.mjs
autonomous: true
requirements: [WIRE-01]
must_haves:
  truths:
    - "token_usage table has cache_read_tokens and cache_write_tokens INTEGER NOT NULL DEFAULT 0 columns after migration"
    - "logCall persists cache_read_tokens/cache_write_tokens when a TokenUsageRow carries them"
    - "getSummary returns cache_read/cache_write SUM aggregates; export/hydrate round-trip preserves them"
    - "The proxy migration is mutually idempotent with the coding-side ensureCacheColumns (identical names+types)"
  artifacts:
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts"
      provides: "cache-token schema migration + logCall bind + getSummary aggregates + export/hydrate round-trip"
      contains: "cache_read_tokens"
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-cache-migration.test.mjs"
      provides: "migration idempotency + logCall persistence + summary/export round-trip assertions"
  key_links:
    - from: "TokenUsageRow"
      to: "insertStmt positional bind"
      via: "row.cache_read_tokens ?? 0"
      pattern: "cache_read_tokens \\?\\? 0"
    - from: "PRAGMA table_info(token_usage)"
      to: "ALTER TABLE ... ADD COLUMN cache_read_tokens"
      via: "existence-guarded standalone ALTER"
      pattern: "cache_read_tokens INTEGER NOT NULL DEFAULT 0"
---

<objective>
Add the two cache-token columns to the proxy `token_usage` schema and thread them through every persistence surface (migration, `TokenUsageRow`, `logCall` bind, `getSummary` aggregates, Phase-36 export/hydrate round-trip). This is the storage foundation every later plan writes into.

Purpose: Without these columns the /v1/messages tap (Plan 02) has nowhere to store the cache usage it parses, and the coding-side dedup-merge (Plan 04) cannot enrich a cache-less row. The column names+types MUST be byte-identical to the coding-side `ensureCacheColumns` so the two independent migrations are mutually idempotent (whichever runs first wins, the other is a no-op).
Output: Additive schema migration + persistence wiring in `src/token-usage.ts` (proxy repo, compiles to `dist/` via `npm run build`), plus an integration test proving migration idempotency and round-trip persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-wire-measurement-foundation/82-CONTEXT.md
@.planning/phases/82-wire-measurement-foundation/82-PATTERNS.md

<interfaces>
Existing self-precedents in /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts (extracted):
- TokenUsageRow interface at :83; reasoning_tokens?: number at :127 (add the two cache fields adjacent, both optional number).
- Standalone PRAGMA-guarded ALTER precedents: model_raw at :512-518, attribution block (reasoning_tokens) at :559-577 (uses `PRAGMA table_info(token_usage)` → Set of existing names → conditional `db.exec(ddl)`; wrapped in try/catch with `[token-usage] ... migration failed (non-fatal)` stderr).
- insertStmt column list at :584-591; logCall .run() positional args, reasoning_tokens ?? 0 at :830.
- getSummary aggregates SUM(input_tokens)/SUM(total_tokens) at :898-932; return-type interface at :865-884.
- exportToHourFile SELECT at :347; deprecated exportToJson SELECT at :741-747; hydrateFromExports INSERT OR IGNORE at :663-697 with model_raw fallback coalesce at :678-681.
- Coding-side contract to match (do NOT edit this file, just match names/types): lib/lsl/token/token-db.mjs:70-78 adds both columns as `INTEGER NOT NULL DEFAULT 0`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache-column migration + TokenUsageRow + logCall bind</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts (read :83-130 for TokenUsageRow, :390-600 for CREATE TABLE + migration blocks, :580-840 for insertStmt + logCall)
    - /Users/Q284340/Agentic/coding/lib/lsl/token/token-db.mjs (read :44-115 for the coding-side ensureCacheColumns contract this must match)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ src/token-usage.ts pattern assignment)
  </read_first>
  <behavior>
    - After opening a legacy DB lacking the columns, both cache_read_tokens and cache_write_tokens exist as INTEGER NOT NULL DEFAULT 0.
    - Running the migration twice against the same DB is a no-op (no throw, no duplicate-column error).
    - A TokenUsageRow with cache_read_tokens=12, cache_write_tokens=34 persists those exact values via logCall; a row omitting them persists 0/0.
    - total_tokens is unchanged by cache values (cache is a SEPARATE counter, never folded into total).
  </behavior>
  <action>
    Add a NEW standalone PRAGMA-guarded ALTER block (or extend the existing attributionCols array at :559-577) that adds `cache_read_tokens` and `cache_write_tokens`, each with DDL `ALTER TABLE token_usage ADD COLUMN <name> INTEGER NOT NULL DEFAULT 0`, guarded by `PRAGMA table_info(token_usage)` existence check, wrapped in try/catch emitting `[token-usage] cache column migration failed (non-fatal)` to process.stderr. Place it as a standalone post-rebuild ALTER — do NOT add these columns to the composite-PK rebuild INSERT...SELECT (:474-484) nor to the fresh-install CREATE TABLE (:395-411); the ALTER path is authoritative, matching model_raw/reasoning_tokens. Add `cache_read_tokens?: number` and `cache_write_tokens?: number` to the TokenUsageRow interface adjacent to reasoning_tokens (:127). Extend the insertStmt column list (:584-591) to include the two columns AND add matching positional args `row.cache_read_tokens ?? 0` and `row.cache_write_tokens ?? 0` to the logCall .run() call (:798-837), coalesced exactly like reasoning_tokens ?? 0 at :830. Preserve total_tokens semantics — do not add cache into it.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build && node -e "const t=require('./dist/token-usage.js'); const fs=require('fs'); const p=require('os').tmpdir()+'/tu-'+Date.now()+'.db'; process.env.LLM_PROXY_TOKEN_DB_PATH=p; const db=t.openTokenDb?t.openTokenDb():null;" 2>&1 | head -5; cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && grep -c 'cache_read_tokens' src/token-usage.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cache_read_tokens INTEGER NOT NULL DEFAULT 0" src/token-usage.ts` returns a match (both column DDLs present).
    - `grep -n "row.cache_read_tokens ?? 0" src/token-usage.ts` and `grep -n "row.cache_write_tokens ?? 0" src/token-usage.ts` each return a match inside the logCall .run() args.
    - `cache_read_tokens?: number` and `cache_write_tokens?: number` present in the TokenUsageRow interface.
    - `npm run build` exits 0 (compiles clean to dist/).
    - The two new column DDLs are NOT present inside the CREATE TABLE statement (:395-411) nor the composite-PK rebuild INSERT...SELECT (grep confirms they live only in the standalone ALTER block).
  </acceptance_criteria>
  <done>Both cache columns migrate idempotently, TokenUsageRow carries the fields, logCall binds them; build is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getSummary aggregates + export/hydrate round-trip + integration test</name>
  <files>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-cache-migration.test.mjs
  </files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts (read :319-360 exportToHourFile, :660-770 hydrate/exportToJson, :860-935 getSummary)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/tests/integration/token-usage-schema-migration.test.mjs (harness precedent: node:test + node:assert/strict, isolated tmpdir via LLM_PROXY_TOKEN_DB_PATH, assert against ../../dist/token-usage.js after build)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ Test Patterns → SSE cache-parse harness note + dedup harness)
  </read_first>
  <behavior>
    - getSummary totals include cache_read_tokens and cache_write_tokens as COALESCE(SUM(...),0), present alongside the existing input/total aggregates.
    - An exported hour-file JSON carries both cache columns; hydrateFromExports re-imports them; a peer file lacking the columns coalesces to 0 (no crash).
    - Round-trip: logCall a row with cache_read=7/cache_write=9 → exportToHourFile → fresh DB → hydrateFromExports → getRecent surfaces cache_read_tokens=7, cache_write_tokens=9.
  </behavior>
  <action>
    Add `COALESCE(SUM(cache_read_tokens),0)` and `COALESCE(SUM(cache_write_tokens),0)` to the getSummary totals query (:898-905) and to by_process/by_provider/by_model buckets as needed (:906-932), mirroring the existing SUM(input_tokens) shape; extend the getSummary return-type interface (:865-884) with the two keys. Add `cache_read_tokens, cache_write_tokens` to the exportToHourFile SELECT (:347) and the deprecated exportToJson SELECT (:741-747). Add the two columns to the hydrateFromExports INSERT OR IGNORE column list + values (:663-697), coalescing older peer files that lack them via `(r.cache_read_tokens as number) ?? 0`, exactly like the model_raw fallback at :678-681. getRecent needs NO change (SELECT * surfaces the columns automatically). Then author the integration test asserting: (a) migration idempotency (open twice, both columns exist, no throw), (b) logCall persistence of nonzero cache values via getRecent, (c) the export→fresh-DB→hydrate round-trip preserves both values, (d) a hydrate source object missing the columns coalesces to 0. Gate any live-only assertion on an env var (LLM_PROXY_LIVE=1) not trailing argv, per memory reference_node_test_argv_live_gate.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build && node --test tests/integration/token-usage-cache-migration.test.mjs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/token-usage-cache-migration.test.mjs` exits 0 with all assertions passing (migration idempotency, logCall persistence, export/hydrate round-trip, missing-column coalesce).
    - `grep -n "COALESCE(SUM(cache_read_tokens)" src/token-usage.ts` returns a match in getSummary.
    - `grep -n "cache_read_tokens" src/token-usage.ts | wc -l` shows matches across migration, logCall, getSummary, export SELECT, and hydrate INSERT (>= 5 distinct sites).
    - The round-trip test asserts getRecent returns cache_read_tokens=7 and cache_write_tokens=9 for the seeded row.
  </acceptance_criteria>
  <done>Summary/export/hydrate all carry the cache columns; the integration test proves idempotency + round-trip; build+test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → token_usage DB | TokenUsageRow values (including cache counts) written to SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Tampering | migration ALTER on legacy DBs | mitigate | PRAGMA table_info existence guard + try/catch non-fatal stderr; never re-adds a present column; DEFAULT 0 keeps existing rows valid |
| T-82-01-02 | Denial of Service | logCall on malformed row | mitigate | numeric coalesce `?? 0` on both cache fields; write stays inside existing best-effort logCall try/catch — a bad value can never throw on the LLM hot path |
| T-82-01-03 | Information disclosure | export JSON gains cache columns | accept | cache counts are integers, no PII; exports are already git-tracked per Phase 36 convention |
| T-82-01-SC | Tampering | npm install of dependencies | accept | no new packages added (extends existing better-sqlite3 seam); no package-manager install task in this plan |
</threat_model>

<verification>
- `npm run build` in the proxy repo is green (TypeScript compiles).
- `node --test tests/integration/token-usage-cache-migration.test.mjs` passes.
- Column names+types are byte-identical to lib/lsl/token/token-db.mjs:71-77.
</verification>

<success_criteria>
Both cache columns exist and migrate idempotently; logCall/getSummary/export/hydrate all carry them; getRecent surfaces them for free; integration test green; no new npm dependency.
</success_criteria>

<output>
Create `.planning/phases/82-wire-measurement-foundation/82-01-SUMMARY.md` when done.
</output>
