---
phase: 82-wire-measurement-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/coding/lib/experiments/experiment-runner.mjs
  - /Users/Q284340/Agentic/coding/scripts/launch-agent-common.sh
  - /Users/Q284340/Agentic/coding/config/agents/copilot.sh
  - /Users/Q284340/Agentic/coding/config/agents/opencode.sh
autonomous: true
requirements: [WIRE-07]
must_haves:
  truths:
    - "The claude unroute special-case (delete env.ANTHROPIC_BASE_URL) is removed so claude experiment cells route through the proxy again"
    - "Claude cells and the interactive claude launcher set ANTHROPIC_CUSTOM_HEADERS with x-task-id (newline-separated Name: value form)"
    - "Copilot BYOK env (COPILOT_PROVIDER_BASE_URL=.../v1/copilot, COPILOT_PROVIDER_TYPE=openai, COPILOT_PROVIDER_API_KEY=<placeholder>, COPILOT_MODEL) is set for measured launches; the 'copilot unmeasured' warning is dropped"
    - "The CODING_PROXY_ROUTE opt-out and the health gate stay intact as the safety valve"
    - "The opencode anthropic-native provider entry is flag-gated OPT-IN only this phase (default behavior unchanged)"
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/lib/experiments/experiment-runner.mjs"
      provides: "claude re-route + ANTHROPIC_CUSTOM_HEADERS + copilot BYOK case in configureProxyRoutingEnv"
      contains: "COPILOT_PROVIDER_BASE_URL"
    - path: "/Users/Q284340/Agentic/coding/scripts/launch-agent-common.sh"
      provides: "interactive claude x-task-id header + copilot BYOK env (warning dropped)"
      contains: "ANTHROPIC_CUSTOM_HEADERS"
  key_links:
    - from: "cell.task_id"
      to: "ANTHROPIC_CUSTOM_HEADERS='x-task-id: <id>'"
      via: "experiment-runner claude branch"
      pattern: "ANTHROPIC_CUSTOM_HEADERS"
    - from: "COPILOT_PROVIDER_BASE_URL /v1/copilot"
      to: "proxy /v1/copilot/chat/completions"
      via: "copilot BYOK env"
      pattern: "/v1/copilot"
---

<objective>
Wire the coding-side launchers so claude experiment cells route back through the proxy (removing the deliberate unroute workaround), claude launches carry an x-task-id header for the Plan-02 tap binding, and copilot joins measurement via the Phase-81 BYOK seam pointed at the new /v1/copilot path. Also land the flag-gated (opt-in) opencode anthropic-native provider entry.

Purpose: The claude cells were unrouted precisely because the tap dropped their cache accounting and first-writer-wins shadowed the richer row — both fixed in Plans 02+04. With those fixed, re-routing claude cells is safe and necessary for uniform measurement. Copilot BYOK (verified live in Phase 81) needs its env pointed at /v1/copilot so BYOK requests stamp agent=copilot (Plan 03). The header format (ANTHROPIC_CUSTOM_HEADERS newline-separated Name: value) is a documented risk — it is verified live in Plan 06's EARLY gate before the full run.
Output: Edits to experiment-runner.mjs (cells), launch-agent-common.sh (interactive), copilot.sh (BYOK exports), opencode.sh (flag-gated provider entry). No proxy-repo edits — this plan is coding-repo only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-wire-measurement-foundation/82-CONTEXT.md
@.planning/phases/82-wire-measurement-foundation/82-PATTERNS.md
@.planning/phases/81-copilot-byok-verification-spike/81-SPIKE-VERDICT.md

<interfaces>
From /Users/Q284340/Agentic/coding/lib/experiments/experiment-runner.mjs (extracted):
- configureProxyRoutingEnv(agent, baseEnv, opts) at :135; returns a COPY of baseEnv, NEVER mutates in place (:132).
- CODING_PROXY_ROUTE opt-out at :141-145; health gate at :150-153.
- switch(agent) at :156; opencode case sets env.ANTHROPIC_BASE_URL = base (:157-158); the claude special-case default runs delete env.ANTHROPIC_BASE_URL at ~:157-170 (the block to REMOVE). base is the proxy base URL (host:port).
From /Users/Q284340/Agentic/coding/scripts/launch-agent-common.sh (extracted):
- configure_proxy_routing() case block at :388-441; opt-out :382-388; health gate :398-403.
- claude case :405-412 sets ANTHROPIC_BASE_URL + unsets API-key envs (TASK_ID is the launcher span id).
- copilot case :434-436 currently prints the "NOT yet proxy-measured (follow-up)" warning (:435).
Spike-verified BYOK invocation (adapt base to /v1/copilot): COPILOT_PROVIDER_BASE_URL=http://127.0.0.1:12435/v1/copilot, COPILOT_PROVIDER_TYPE=openai, COPILOT_PROVIDER_API_KEY=rapid-proxy-no-auth-placeholder, COPILOT_MODEL=<model>, plus COPILOT_PROVIDER_WIRE_MODEL when the wire name differs, COPILOT_AUTO_UPDATE=false.
From config/agents: opencode.sh:35,39 sets OPENCODE_CONFIG_CONTENT JSON blob ({"model":..,"disabled_providers":[..]}); copilot.sh:41,58 runs the HTTP adapter (COPILOT_HTTP_ADAPTER_PID).
CLAUDE.md port contract: LLM proxy host port is 12435 (NOT 3033 which is the Health API).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: experiment-runner — remove claude unroute, add x-task-id header + copilot BYOK case</name>
  <files>/Users/Q284340/Agentic/coding/lib/experiments/experiment-runner.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/lib/experiments/experiment-runner.mjs (read :100-175 — configureProxyRoutingEnv, opt-out, health gate, switch)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ experiment-runner.mjs)
    - .planning/phases/82-wire-measurement-foundation/82-CONTEXT.md (§ Routing changes)
  </read_first>
  <action>
    Delete the claude special-case that runs delete env.ANTHROPIC_BASE_URL (~:157-170) so claude cells fall into the same env.ANTHROPIC_BASE_URL = base treatment as opencode, routing through the proxy. Keep the CODING_PROXY_ROUTE opt-out (:141-145) and the health gate (:150-153) byte-untouched as the safety valve. In the claude branch, additionally set env.ANTHROPIC_CUSTOM_HEADERS to the string 'x-task-id: ' concatenated with cell.task_id (newline-separated Name: value form; append a newline plus 'x-agent: claude' only if the tap binding requires it — the Plan-02 tap treats absent x-agent as claude, so x-agent is optional). Add a new case 'copilot': setting COPILOT_PROVIDER_BASE_URL to 'http://127.0.0.1:' + port + '/v1/copilot' (derive port from the same base the opencode case uses), COPILOT_PROVIDER_TYPE='openai', COPILOT_PROVIDER_API_KEY='rapid-proxy-no-auth-placeholder', COPILOT_MODEL from the cell model, COPILOT_PROVIDER_WIRE_MODEL when the wire name differs, COPILOT_AUTO_UPDATE='false'. Preserve the COPY-of-baseEnv discipline (never mutate baseEnv or LLM_PROXY_DATA_DIR).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && ! grep -q "delete env.ANTHROPIC_BASE_URL" lib/experiments/experiment-runner.mjs && grep -nE "ANTHROPIC_CUSTOM_HEADERS|COPILOT_PROVIDER_BASE_URL|v1/copilot" lib/experiments/experiment-runner.mjs && node --check lib/experiments/experiment-runner.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep for "delete env.ANTHROPIC_BASE_URL" in experiment-runner.mjs finds NOTHING (unroute special-case gone).
    - grep "ANTHROPIC_CUSTOM_HEADERS" shows the claude branch sets it to 'x-task-id: ' + the cell task id.
    - grep "COPILOT_PROVIDER_BASE_URL" shows the copilot case points at /v1/copilot with type openai + placeholder key + model.
    - node --check exits 0 (syntax valid).
    - The CODING_PROXY_ROUTE opt-out and health gate lines are byte-unchanged (state in SUMMARY).
  </acceptance_criteria>
  <done>Claude cells re-route with x-task-id header; copilot BYOK case added; opt-out/health gate intact; syntax valid.</done>
</task>

<task type="auto">
  <name>Task 2: launch-agent-common.sh + copilot.sh — interactive claude header + copilot BYOK env (drop warning)</name>
  <files>
    - /Users/Q284340/Agentic/coding/scripts/launch-agent-common.sh
    - /Users/Q284340/Agentic/coding/config/agents/copilot.sh
  </files>
  <read_first>
    - /Users/Q284340/Agentic/coding/scripts/launch-agent-common.sh (read :380-441 — configure_proxy_routing case block, opt-out, health gate, claude case, copilot case + warning)
    - /Users/Q284340/Agentic/coding/config/agents/copilot.sh (read full — HTTP adapter exports at :41,58)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ launch-agent-common.sh + § agent-config touch)
  </read_first>
  <action>
    In configure_proxy_routing() claude case (:405-412), add an export of ANTHROPIC_CUSTOM_HEADERS set to the string x-task-id: followed by the TASK_ID launcher span id, alongside the existing ANTHROPIC_BASE_URL export. In the copilot case (:434-436), replace the "NOT yet proxy-measured (follow-up)" warning (:435) with BYOK env exports: COPILOT_PROVIDER_BASE_URL set to http://127.0.0.1: plus ${LLM_CLI_PROXY_PORT:-12435} plus /v1/copilot; COPILOT_PROVIDER_TYPE=openai; COPILOT_PROVIDER_API_KEY=rapid-proxy-no-auth-placeholder; COPILOT_MODEL defaulting to ${COPILOT_MODEL:-claude-haiku-4-5}; COPILOT_AUTO_UPDATE=false; plus COPILOT_PROVIDER_WIRE_MODEL when the wire name differs — all gated behind the same health gate + Phase-81 verification already surrounding the case. Keep the opt-out (:382-388) and health gate (:398-403) intact. In config/agents/copilot.sh, land the same BYOK env exports alongside the existing HTTP-adapter exports so the interactive copilot launcher inherits them. Use the CLAUDE.md port contract (host port 12435 for the LLM proxy — NOT 3033).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && bash -n scripts/launch-agent-common.sh && bash -n config/agents/copilot.sh && grep -n "ANTHROPIC_CUSTOM_HEADERS" scripts/launch-agent-common.sh && grep -n "COPILOT_PROVIDER_BASE_URL" scripts/launch-agent-common.sh config/agents/copilot.sh && ! grep -q "NOT yet proxy-measured" scripts/launch-agent-common.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash -n on both files exits 0 (valid shell syntax).
    - grep "ANTHROPIC_CUSTOM_HEADERS" in launch-agent-common.sh shows the claude case exports x-task-id: ${TASK_ID}.
    - grep "COPILOT_PROVIDER_BASE_URL" shows /v1/copilot in BOTH launch-agent-common.sh and config/agents/copilot.sh, using port 12435 (or ${LLM_CLI_PROXY_PORT:-12435}).
    - grep "NOT yet proxy-measured" in launch-agent-common.sh finds NOTHING (warning dropped).
    - The opt-out and health-gate lines are byte-unchanged (state in SUMMARY).
  </acceptance_criteria>
  <done>Interactive claude launch carries x-task-id; copilot BYOK env exported in launcher + config; warning gone; shell syntax valid.</done>
</task>

<task type="auto">
  <name>Task 3: opencode.sh — flag-gated (opt-in) anthropic-native provider entry</name>
  <files>/Users/Q284340/Agentic/coding/config/agents/opencode.sh</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/config/agents/opencode.sh (read full — OPENCODE_CONFIG_CONTENT JSON blob at :35,39)
    - .planning/phases/82-wire-measurement-foundation/82-CONTEXT.md (§ Flag-gated extra + § Deferred Ideas — MUST stay opt-in this phase)
    - .planning/phases/82-wire-measurement-foundation/82-PATTERNS.md (§ opencode/copilot agent-config touch)
  </read_first>
  <action>
    Add an OPT-IN flag-gated anthropic-native provider entry to the OPENCODE_CONFIG_CONTENT JSON blob (an @ai-sdk/anthropic provider pointed at the proxy /v1/messages with per-launch options.headers carrying x-task-id and x-agent: opencode). Gate the entire addition behind an env flag (e.g. OPENCODE_ANTHROPIC_NATIVE=1) that DEFAULTS OFF, so the default OPENCODE_CONFIG_CONTENT is byte-identical to today when the flag is unset. Do NOT make this the default — CONTEXT §Deferred requires opt-in only this phase; live verification that opencode actually emits cache_control breakpoints on this path is a prerequisite for a future default and is out of scope here. Claude's Discretion (CONTEXT): env interpolation vs config rewrite for templating per-launch headers — pick the lower-risk approach and document it.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && bash -n config/agents/opencode.sh && grep -n "OPENCODE_ANTHROPIC_NATIVE\|v1/messages" config/agents/opencode.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash -n config/agents/opencode.sh exits 0.
    - The anthropic-native provider entry is present ONLY inside a conditional gated on an OFF-by-default flag (grep shows the flag name; state the default in SUMMARY).
    - With the flag unset, OPENCODE_CONFIG_CONTENT is byte-identical to the pre-change blob (assert by diffing the rendered default in SUMMARY).
    - The provider entry targets the proxy /v1/messages and sets options.headers x-task-id + x-agent: opencode.
  </acceptance_criteria>
  <done>Opt-in anthropic-native provider entry added, default-off, default config unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launcher env → agent process | ANTHROPIC_CUSTOM_HEADERS / COPILOT_PROVIDER_* env values drive proxy routing + task binding |
| placeholder API key | COPILOT_PROVIDER_API_KEY is a non-secret placeholder against a localhost no-auth proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-05-01 | Information disclosure | placeholder API key in launcher env/logs | accept | value is a literal non-secret ('rapid-proxy-no-auth-placeholder') against a localhost-bound no-auth proxy; no real credential is exposed |
| T-82-05-02 | Spoofing | x-task-id header value could be attacker-chosen | mitigate | value is the launcher-owned span/cell task id, not user input; the Plan-02 tap sanitizes it via sanitizeTaskId before use |
| T-82-05-03 | Tampering | re-routing claude cells could regress cache accounting | mitigate | routing depends on Plans 02 (cache tap) + 04 (dedup merge) landing first; CODING_PROXY_ROUTE opt-out + health gate remain the reversible safety valve |
| T-82-05-04 | Elevation of privilege | flag-gated opencode provider becomes default prematurely | mitigate | entry is OFF-by-default behind an explicit env flag; default config byte-identical; making it default is explicitly deferred pending cache_control live-verification |
| T-82-05-SC | Tampering | npm install of dependencies | accept | no new packages; pure launcher/config env edits |
</threat_model>

<verification>
- node --check experiment-runner.mjs and bash -n on all three shell files pass.
- The unroute special-case is gone; opt-out + health gate byte-unchanged; opencode default config unchanged when the flag is unset.
- Live end-to-end (header actually forwarded, copilot BYOK produces a file) is Plan 06's gate.
</verification>

<success_criteria>
Claude cells + interactive launcher route through the proxy with an x-task-id header; copilot BYOK env points at /v1/copilot for measured launches; the unmeasured warning is dropped; opencode anthropic-native entry is opt-in only; safety valve intact.
</success_criteria>

<output>
Create `.planning/phases/82-wire-measurement-foundation/82-05-SUMMARY.md` when done.
</output>
