---
phase: 59-digest-insight-writer-edge-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/live-logging/ObservationConsolidator.js
  - src/live-logging/ObservationConsolidator.test.js
autonomous: true
requirements:
  - ORPHAN-DIG-01
must_haves:
  truths:
    - "Every Digest written by the plain-insert path in `consolidateDay` (OC.js:1271-1296) is followed in the SAME try-block by `kmStore.addRelation` calls — one `Digest -[derivedFrom]-> Observation` edge per id in `row.observation_ids`. The km-core JSON exporter's 5s debounce window captures putEntity + every addRelation in one export tick (Phase 58 D-04 atomicity envelope, applied verbatim to Digests per D-02)."
    - "The mintedId returned by `await kmStore.putEntity(entity, {skipOntologyCheck: true})` at OC.js:1294 is captured into a local (`const digestMintedId = await kmStore.putEntity(...)`) and used as the `from` argument of every addRelation call. The pre-edit code at OC.js:1294 discards the return value; this plan captures it."
    - "Observation id resolution per D-02.2: for each obsId in `row.observation_ids`, the writer calls `kmStore.findByLegacyId({system:'A', id: obsId})` to recover the minted Observation km-core id. If the Observation is not yet persisted (findByLegacyId returns null), the writer logs a `[Consolidator] derivedFrom: observation <id> not yet persisted, skipping edge` line to stderr and continues the loop — the Digest still lands with its remaining edges. The missing edge is picked up by the next repair-script run (Plan 59-04)."
    - "Per-edge addRelation failure is non-fatal — wrapped in its own try/catch with `[Consolidator] derivedFrom edge ... failed (non-fatal)` stderr logging (mirrors the OW.js:514-520 _emitMentionsEdges precedent)."
    - "The writer path does NOT probe-before-write (D-02.1): the upstream `_buildDigestMergePlan` dedup at OC.js:1198-1245 returns `{action: 'merge', targetId: ...}` for already-seen digests, so the plain-insert branch only runs for genuinely new Digests. Probe-before-write would add a per-edge findRelations cost that buys nothing on the normal path. The repair script (Plan 59-04) DOES probe before write because it re-runs by definition."
    - "Edge metadata payload mirrors the has_insight shape at OC.js:694-698: `{source: 'observation-consolidator', confidence: 1.0, addedAt: new Date().toISOString()}`. The `addedAt` ISO timestamp is the audit trail for which run added the edge. The `source` tag distinguishes writer-path edges from `'repair-orphan-digest-insight-edges'` (Plan 59-04 script's tag) so every edge is traceable to its emitter."
    - "Two new unit tests cover: (1) writer emits N derivedFrom edges for N observation_ids when all are resolvable via findByLegacyId; (2) writer skips edges for unresolved observation_ids (findByLegacyId returns null) but completes the Digest insert with the remaining edges. Tests assert ordering: putEntity precedes every addRelation; addRelation type is `'derivedFrom'`; metadata payload shape matches."
  artifacts:
    - path: src/live-logging/ObservationConsolidator.js
      provides: "Digest plain-insert path emits N derivedFrom edges in the same try-block as putEntity per D-02"
      contains: "type: 'derivedFrom'"
    - path: src/live-logging/ObservationConsolidator.test.js
      provides: "Tests covering the derivedFrom loop emission + unresolved-obs skip behaviour"
      contains: "derivedFrom"
  key_links:
    - from: src/live-logging/ObservationConsolidator.js
      to: lib/km-core (kmStore.addRelation)
      via: "for-loop of await kmStore.addRelation per row.observation_ids entry, same try-block as putEntity, with findByLegacyId resolution"
      pattern: "for\\s*\\([^)]*observation_ids[^)]*\\)\\s*\\{[\\s\\S]*?addRelation"
    - from: src/live-logging/ObservationConsolidator.js
      to: lib/km-core (kmStore.findByLegacyId)
      via: "obsId → minted observation entity id resolution per D-02.2"
      pattern: "findByLegacyId\\(\\{\\s*system:\\s*['\"]A['\"],\\s*id:\\s*obsId"
---

<objective>
Fix the writer-path bug at the plain-insert branch of `consolidateDay` in `src/live-logging/ObservationConsolidator.js` (lines 1271-1296). The current code calls `await kmStore.putEntity(entity, {skipOntologyCheck: true})` at OC.js:1294 but never follows with the `kmStore.addRelation('derivedFrom')` calls that `metadata.observation_ids` references — every newly-inserted Digest lands as a zero-degree node in the live km-core graph. The fix per D-02 (which mirrors Phase 58 D-04 verbatim, applied to Digests): capture the mintedId from putEntity, then iterate `row.observation_ids` and emit one `derivedFrom` edge per id in the same try-block, resolving each obsId via `findByLegacyId({system:'A', id})` before calling addRelation.

Purpose: ORPHAN-DIG-01 acceptance — "New Digest inserts after this phase ships are NEVER zero-degree". The atomicity envelope is the km-core JSON exporter's 5s debounce, exactly as Phase 58 D-04 established for Insights. Per-edge awaits inside the loop are fine — the debounce window is wall-clock seconds while the loop runs in microseconds.

Output: writer-side emission of N `derivedFrom` edges per Digest insert; per-edge guards for self-loops and not-yet-persisted observations; unit tests proving the loop fires + the unresolved-obs skip is non-fatal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-digest-insight-writer-edge-repair/59-CONTEXT.md
@.planning/phases/59-digest-insight-writer-edge-repair/59-PATTERNS.md
@.planning/phases/58-online-pipeline-semantic-edges-on-insights/58-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified line numbers on disk 2026-06-16 — PATTERNS.md "Drift from CONTEXT.md" table. -->
<!-- There is NO method named _executeDigestStage. The digest plain-insert lives INLINE in consolidateDay. -->

From src/live-logging/ObservationConsolidator.js (lines 1271-1296) — CURRENT shape, EDIT:
```
// Plain insert: build a LegacyDigestRow and persist via the shared
// legacy-ingest adapter (single source of truth for entityType /
// ontologyClass / legacyId / Phase 39 D-30 provenance shape).
const row = {
  id: d.id,
  date: d.date,
  theme: this._redact(d.theme),
  summary: this._redact(d.summary),
  observation_ids: d.observationIds,
  agents: d.agents,
  files_touched: (() => { /* ... */ })(),
  quality: d.quality,
  created_at: now,
  metadata: (() => { /* ... */ })(),
  project: d.project || 'unknown',
};
const entity = legacyDigestToEntity(row, this._runId, now);
await kmStore.putEntity(entity, { skipOntologyCheck: true });           // ← discards return — CHANGE: capture
createdCount++;
for (const obsId of d.observationIds) digestedObsIds.add(obsId);
// ← NEW derivedFrom loop lands HERE, BEFORE createdCount++ ideally, OR between putEntity and digestedObsIds.add
```

From src/live-logging/ObservationConsolidator.js (lines 679-705) — IN-FILE ANALOG (has_insight follower in `_pushInsightToKG`). Same probe-then-write idiom, same error-logging convention. Phase 59 D-02 emits derivedFrom edges WITHOUT the probe (D-02.1 — writer path dedups upstream); the metadata payload shape is borrowed from this analog:
```
await this._kmStore.addRelation({
  from: projectId,
  to: mintedId,
  type: 'has_insight',
  metadata: {
    source: 'observation-consolidator',
    team: project,
    confidence: 1.0,
  },
});
```

From src/live-logging/ObservationConsolidator.js (line 1306) — IN-FILE ANALOG (findByLegacyId resolution for obsId, used in the mark-digested loop at :1304-1313). Same shape needed in the new derivedFrom loop:
```
const obsEntity = await kmStore.findByLegacyId({ system: 'A', id: obsId });
if (!obsEntity) continue;     // not yet persisted — skip; never throw.
```

From src/live-logging/ObservationWriter.js (lines 478-522) — SECONDARY ANALOG (_emitMentionsEdges; per-edge try/catch + non-fatal error logging). The new derivedFrom loop mirrors this control flow (minus the dedup probe):
```
for (const toId of targetIds) {
  if (!toId || toId === fromId) continue;
  try {
    await kmStore.addRelation({
      from: fromId,
      to: toId,
      type: 'mentions',
      metadata: { source: 'observation-writer', classifiedAt: ..., classifier: 'llm-haiku' },
    });
  } catch (err) {
    process.stderr.write(`[ObservationWriter] mentions edge ${fromId}->${toId} failed (non-fatal): ${err.message}\n`);
  }
}
```

From integrations/mcp-server-semantic-analysis/src/agents/observation-generation-agent.ts:1396-1397 — VOCABULARY SOURCE. The string literal `'derivedFrom'` is already in the km-core edge-type vocabulary; Phase 59 D-01 reuses it verbatim:
```
relationType: 'derivedFrom',
type: 'derivedFrom',
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add derivedFrom loop after putEntity in the Digest plain-insert branch</name>
  <files>src/live-logging/ObservationConsolidator.js</files>
  <behavior>
    - At OC.js:1294, the line `await kmStore.putEntity(entity, { skipOntologyCheck: true });` becomes `const digestMintedId = await kmStore.putEntity(entity, { skipOntologyCheck: true });` — captures the mintedId into a const.
    - Immediately after the captured-putEntity line, BEFORE `createdCount++` at OC.js:1295, a new for-loop emits N `derivedFrom` edges, one per `obsId` in `d.observationIds` (the same source array already consumed at OC.js:1296). The loop iterates a const-local `const obsIds = Array.isArray(d.observationIds) ? d.observationIds : []` to be defensive against malformed inputs.
    - For each obsId: (a) self-loop guard `if (!obsId) continue;`. (b) resolve via `await kmStore.findByLegacyId({ system: 'A', id: obsId })`; on null, log `[Consolidator] derivedFrom: observation ${obsId.slice(0,8)} not yet persisted, skipping edge` to stderr and `continue`. (c) call `await kmStore.addRelation({from: digestMintedId, to: obsEntity.id, type: 'derivedFrom', metadata: {source: 'observation-consolidator', confidence: 1.0, addedAt: new Date().toISOString()}})` wrapped in its own try/catch; on error log `[Consolidator] derivedFrom edge ${digestMintedId}->${obsEntity.id} failed (non-fatal): ${err.message}` and continue the loop.
    - The findByLegacyId call is NOT pre-existing in this branch — the existing mark-digested loop at OC.js:1304-1313 has the same shape but runs AFTER the for-loop over digestEntries closes. The new derivedFrom loop must run INSIDE the for-loop over digestEntries, not after.
    - NO probe-before-write (D-02.1). NO dedup. The upstream `_buildDigestMergePlan` at OC.js:1198-1245 guarantees the plain-insert branch only runs for new Digests.
    - The existing `createdCount++` and `for (const obsId of d.observationIds) digestedObsIds.add(obsId);` lines at OC.js:1295-1296 stay in place (same line order, same semantics — they accumulate counters used elsewhere in `consolidateDay`).
    - The merge branch at OC.js:1219-1267 is NOT modified by this plan. The merge branch already updates `metadata.observation_ids` via `mergeAttributes` at OC.js:1247-1262 but does NOT emit derivedFrom edges. That is an acknowledged limitation: when a digest is merged into a pre-existing target, NEW observation_ids may have been added to the metadata array without corresponding graph edges. **The repair script (Plan 59-04 Layer 1) covers this gap** — it walks orphan + low-degree Digests and emits any missing derivedFrom edges based on `metadata.observation_ids`. Phase 59 D-02 narrowly closes the plain-insert path; the merge branch is left for Plan 59-04 to backstop.
    - NO `console.*` calls — all logging via `process.stderr.write` per CLAUDE.md `no-console-log` constraint and PATTERNS.md Shared Pattern D.
  </behavior>
  <read_first>
    - src/live-logging/ObservationConsolidator.js lines 1093-1100 (entry of `consolidateDay` — confirm `kmStore` is bound as a local at the top of the method; the new code reads it without re-deriving)
    - src/live-logging/ObservationConsolidator.js lines 1198-1245 (_buildDigestMergePlan call site — confirms the dedup-upstream invariant that justifies D-02.1's "no probe-before-write" decision)
    - src/live-logging/ObservationConsolidator.js lines 1215-1297 (the for-loop over digestEntries — both merge branch and plain-insert branch; this is the file the edit lands in)
    - src/live-logging/ObservationConsolidator.js lines 1271-1296 (the plain-insert branch — the exact edit site)
    - src/live-logging/ObservationConsolidator.js lines 1304-1314 (the post-loop mark-digested code that uses `kmStore.findByLegacyId({system:'A', id: obsId})` — same call shape needed in the new derivedFrom loop)
    - src/live-logging/ObservationConsolidator.js lines 679-705 (has_insight follower — metadata payload shape borrowed)
    - src/live-logging/ObservationWriter.js lines 478-522 (_emitMentionsEdges — secondary analog for the N-edge loop control flow)
    - .planning/phases/59-digest-insight-writer-edge-repair/59-PATTERNS.md "Drift from CONTEXT.md" table (the verified line-number corrections — the executor uses THESE numbers, not the drifted CONTEXT.md numbers)
    - .planning/phases/59-digest-insight-writer-edge-repair/59-PATTERNS.md "ObservationConsolidator.js — D-02 Digest derivedFrom loop" section (the synthesized implementation reference — NOT for verbatim paste, but for shape verification)
    - .planning/phases/59-digest-insight-writer-edge-repair/59-CONTEXT.md D-01, D-02, D-02.1, D-02.2 (the locked semantics)
    - integrations/mcp-server-semantic-analysis/src/agents/observation-generation-agent.ts lines 1393-1400 (the existing `'derivedFrom'` string literal — D-01 reuses this vocabulary verbatim)
    - CLAUDE.md (`no-console-log` constraint — use `process.stderr.write`; submodule rebuild rule — Phase 59 touches `src/live-logging/` host-side only, NO docker rebuild needed for this plan; constraint-dodging prohibition — do not introduce alternative raw-write APIs)
  </read_first>
  <action>
    Edit `src/live-logging/ObservationConsolidator.js` at lines 1294-1296 inside the `consolidateDay` method's plain-insert branch (the `for (let i = 0; i < digestEntries.length; i++)` loop body, after the `if (decision.action === 'merge' && decision.targetId)` block, in the trailing plain-insert section). Five surgical changes:

    1. **Capture the mintedId from putEntity.** Change OC.js:1294 from:
       ```
       await kmStore.putEntity(entity, { skipOntologyCheck: true });
       ```
       to:
       ```
       const digestMintedId = await kmStore.putEntity(entity, { skipOntologyCheck: true });
       ```
       The `digestMintedId` local is scoped to the current iteration of the `for (let i = 0; ...)` loop, so each digest has its own.

    2. **Insert the derivedFrom emission loop BETWEEN the captured-putEntity line (now :1294) and the `createdCount++` line (still :1295 after the edit since the change at step 1 doesn't add a line).** The new code:
       ```
       // ORPHAN-DIG-01 (D-02) — emit one derivedFrom edge per observation_id
       // referenced by this Digest. Same try-block as putEntity so the km-core
       // JSON exporter's 5s debounce captures putEntity + every addRelation in
       // one export tick (Phase 58 D-04 atomicity envelope applied to Digests).
       // No probe-before-write per D-02.1 — _buildDigestMergePlan dedupes
       // upstream, so the plain-insert branch only runs for genuinely new
       // Digests. The repair script (Plan 59-04 Layer 1) is the idempotent
       // re-run path that does probe.
       const obsIds = Array.isArray(d.observationIds) ? d.observationIds : [];
       for (const obsId of obsIds) {
         if (!obsId) continue;
         let obsEntity;
         try {
           obsEntity = await kmStore.findByLegacyId({ system: 'A', id: obsId });
         } catch (err) {
           process.stderr.write(`[Consolidator] derivedFrom: findByLegacyId ${obsId.slice(0,8)} failed (non-fatal): ${err.message}\n`);
           continue;
         }
         if (!obsEntity) {
           // D-02.2 — observation not yet persisted at consolidation time.
           // Skip this edge; the next repair-script run (Plan 59-04) catches it.
           process.stderr.write(`[Consolidator] derivedFrom: observation ${obsId.slice(0,8)} not yet persisted, skipping edge\n`);
           continue;
         }
         try {
           await kmStore.addRelation({
             from: digestMintedId,
             to: obsEntity.id,
             type: 'derivedFrom',
             metadata: {
               source: 'observation-consolidator',
               confidence: 1.0,
               addedAt: new Date().toISOString(),
             },
           });
         } catch (err) {
           process.stderr.write(`[Consolidator] derivedFrom edge ${digestMintedId}->${obsEntity.id} failed (non-fatal): ${err.message}\n`);
         }
       }
       ```

    3. **DO NOT touch** the merge branch at OC.js:1219-1267. The merge case is acknowledged-deferred to the repair script (Plan 59-04 Layer 1) per the must_haves narrative — it would require a separate dedup-against-existing-edges path that doesn't belong in the plain-insert branch's atomicity envelope.

    4. **DO NOT touch** the `createdCount++` line OR the `for (const obsId of d.observationIds) digestedObsIds.add(obsId);` line at OC.js:1295-1296. They remain byte-identical post-edit; the new derivedFrom loop lands BEFORE them, so post-edit the order is:
       - `const digestMintedId = await kmStore.putEntity(...)`  ← was OC.js:1294
       - `[NEW derivedFrom loop block, ~25 lines]`
       - `createdCount++;`                                       ← was OC.js:1295, now shifted by ~25 lines
       - `for (const obsId of d.observationIds) digestedObsIds.add(obsId);`  ← was OC.js:1296, now shifted

    5. **DO NOT touch** any other branch, method, or file. Specifically: writeInsight (in OW.js) is Plan 59-01's scope, and _pushInsightToKG (in OC.js) is Plan 59-03's scope. The Digest writer fix lives entirely in this one editing site.

    Per CLAUDE.md `no-console-log` constraint: all new logging uses `process.stderr.write`. Per CLAUDE.md "constraint dodging is forbidden": do not introduce alternative raw-write APIs to dodge the lint rule.

    Per CLAUDE.md submodule-rebuild rule: `src/live-logging/` is at the top level of the `coding` repo (not in any submodule), so NO `npm run build` + docker rebuild step is required for this plan. The file is consumed in-process by the obs-api daemon via `com.coding.obs-api` launchd label; a `launchctl kickstart -k gui/$(id -u)/com.coding.obs-api` may be required for the change to take effect at runtime, but that is a /gsd:execute-phase concern, not a planning-time concern.
  </action>
  <verify>
    <automated>node --check src/live-logging/ObservationConsolidator.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/live-logging/ObservationConsolidator.js` exits 0.
    - The mintedId is captured: `grep -cE "const\s+digestMintedId\s*=\s*await\s+kmStore\.putEntity" src/live-logging/ObservationConsolidator.js` returns ≥ 1.
    - The derivedFrom loop is present: `grep -cE "type:\s*['\"]derivedFrom['\"]" src/live-logging/ObservationConsolidator.js` returns ≥ 1.
    - The new loop is INSIDE the for-loop over digestEntries (so it runs per-digest), not after it. Verify by extracting the for-loop body and confirming the derivedFrom edge type appears inside it: `awk '/for \(let i = 0; i < digestEntries\.length; i\+\+\)/,/^    \}$/' src/live-logging/ObservationConsolidator.js | grep -cE "type:\s*['\"]derivedFrom['\"]"` returns ≥ 1.
    - The findByLegacyId resolution is wired with system:'A': `grep -cE "findByLegacyId\(\s*\{\s*system:\s*['\"]A['\"],\s*id:\s*obsId" src/live-logging/ObservationConsolidator.js` returns ≥ 2 (the new derivedFrom loop + the pre-existing mark-digested loop at :1306 — both should be present).
    - The metadata payload includes the three required keys: `grep -cE "source:\s*['\"]observation-consolidator['\"]" src/live-logging/ObservationConsolidator.js` returns ≥ 2 (the new derivedFrom + pre-existing has_insight); `grep -cE "addedAt:\s*new Date\(\)\.toISOString\(\)" src/live-logging/ObservationConsolidator.js` returns ≥ 1.
    - The merge branch at OC.js:1219-1267 is byte-identical post-edit. Verify by checking the merge-branch's `kmStore.mergeAttributes` call is still present and unmodified: `grep -cE "await kmStore\.mergeAttributes\(targetEntity\.id" src/live-logging/ObservationConsolidator.js` returns ≥ 1.
    - The `createdCount++` and `for (const obsId of d.observationIds) digestedObsIds.add(obsId);` lines survive (still in the plain-insert branch): `grep -cE "createdCount\+\+" src/live-logging/ObservationConsolidator.js` ≥ 1; `grep -cE "digestedObsIds\.add\(obsId\)" src/live-logging/ObservationConsolidator.js` ≥ 1.
    - No new `console.*` calls: `grep -v '^\s*\*' src/live-logging/ObservationConsolidator.js | grep -v '^\s*//' | grep -cE "console\.(log|error|warn|info)"` returns 0.
    - Process.stderr.write logging present for the two new failure modes: `grep -cE "derivedFrom.*not yet persisted, skipping edge" src/live-logging/ObservationConsolidator.js` returns ≥ 1; `grep -cE "derivedFrom edge .*failed \(non-fatal\)" src/live-logging/ObservationConsolidator.js` returns ≥ 1.
    - writeInsight at OW.js + Plan 59-01's edits are NOT touched by this plan: `git diff src/live-logging/ObservationWriter.js` shows zero modifications (Plan 59-02 lands BEFORE Plan 59-01 if executed in parallel; but the two plans touch DIFFERENT files so no merge conflict is possible).
  </acceptance_criteria>
  <done>The plain-insert branch of consolidateDay captures the mintedId from putEntity, iterates d.observationIds, resolves each via findByLegacyId, and emits a `derivedFrom` edge per resolvable observation in the same try-block; per-edge errors are non-fatal stderr logs; unresolved observations are skipped (the repair script catches them later); the merge branch is untouched; createdCount++ and digestedObsIds.add are byte-identical post-edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for the derivedFrom emission loop</name>
  <files>src/live-logging/ObservationConsolidator.test.js</files>
  <behavior>
    - Test 1 (happy path — N edges emitted): Invoke the plain-insert branch via `consolidator.consolidateDay(...)` (or whatever public method exercises it; read the test file to find the convention) with a mock kmStore that resolves `findByLegacyId({system:'A', id})` to a non-null entity for all 3 observation ids. Assert kmStore.addRelation was called 3 times with `type: 'derivedFrom'`, each with `from: <digestMintedId>` and `to: <resolved obsEntity.id>`. Assert metadata payload has exactly the three keys `source: 'observation-consolidator'`, `confidence: 1.0`, `addedAt: <ISO-8601 string>`.
    - Test 2 (unresolved-obs skip is non-fatal): Same setup but the mock kmStore returns null for the SECOND observation id. Assert: (a) the writer still completes (the Digest is inserted via putEntity); (b) addRelation is called exactly TWICE (for the first and third obs); (c) stderr captures `derivedFrom: observation .* not yet persisted, skipping edge`.
    - Test 3 (atomicity ordering — putEntity precedes all addRelation calls within the same iteration): Use a callLog recording trick (record each kmStore op in order). Assert: for a single Digest insert, the call order is `putEntity → findByLegacyId → addRelation` repeated per-obs. The putEntity index is strictly less than every addRelation index for that iteration.
    - Test 4 (per-edge addRelation failure is non-fatal): Mock kmStore where the SECOND addRelation throws. Assert: putEntity still succeeds, the third addRelation still fires (loop continues), createdCount still increments. stderr captures `derivedFrom edge .* failed (non-fatal)`.
    - Pre-existing tests in the file must continue to pass.
  </behavior>
  <read_first>
    - src/live-logging/ObservationConsolidator.test.js (CURRENT state — read entire file to understand the existing test framework, mock kmStore conventions used by Phase 58 / earlier consolidator tests, and how `consolidateDay` or its plain-insert branch is exercised in existing tests. The new tests must match the existing convention.)
    - src/live-logging/ObservationConsolidator.js post-Task-1 state (the surface under test)
    - .planning/phases/58-online-pipeline-semantic-edges-on-insights/58-02-PLAN.md Task 3 (the established mock-kmStore + callLog pattern for ObservationWriter tests — same pattern applies here; the writer-side tests in Phase 58 are the closest analog)
    - .planning/phases/59-digest-insight-writer-edge-repair/59-CONTEXT.md D-02, D-02.1, D-02.2 (the locked semantics being tested)
    - .planning/phases/59-digest-insight-writer-edge-repair/59-PATTERNS.md "Shared Pattern A" (the probe-then-write pattern — Phase 59 D-02.1 EXCLUDES the probe on the writer path; the test must NOT assert findRelations is called; it WOULD be called by the Plan 59-04 repair script's loop, which is tested separately)
  </read_first>
  <action>
    Extend `src/live-logging/ObservationConsolidator.test.js` with four new tests realizing the behaviour above. Match the existing test framework + mock kmStore convention already in the file — if absent, use `node --test` (`node:test` + `node:assert/strict`) matching Phase 58 Plan 02 Task 3's convention.

    The mock kmStore must support at minimum:
    - `putEntity(entity, opts)` — records call, returns a generated string id (e.g. `'mock-digest-' + (++counter)`)
    - `findByLegacyId({system, id})` — returns either `null` or a stub entity `{id: 'mock-obs-' + id, ...}`, configurable per-test
    - `addRelation(rel)` — records call to callLog, can be configured to throw on a specific iteration
    - `mergeAttributes(id, patch)` — records call (used by the pre-existing mark-digested loop at OC.js:1304-1313)
    - `findRelations(filter)` — returns `[]` (the writer path does NOT probe per D-02.1, but the test must not crash if the consolidator's other code paths call it)
    - `findByOntologyClass(klass)` — returns `[]` (the consolidator's has_insight follower in `_pushInsightToKG` calls this; not exercised by the Digest plain-insert test, but a safety stub avoids test breakage)

    For Test 3's callLog ordering assertion, the mock records each op as `{op, ...rest}` on a shared array; the test then asserts the indices match the expected ordering.

    For Test 2's stderr capture, redirect `process.stderr.write` to a recording function for the duration of the test, then restore. Match the existing convention used by Phase 58 tests if any — otherwise use a simple wrap-and-restore pattern.

    All new tests get explanatory comments referencing D-02 / D-02.1 / D-02.2 so a future reader can trace each assertion back to a locked decision.

    Per CLAUDE.md `no-console-log`: no `console.*` in the test file; use `process.stderr.write` if forensic output is needed.

    The new tests do NOT need to drive the full `consolidateDay` happy path — exercising the plain-insert branch may require constructing a digest entry array + a mergePlan array with `{action: 'insert'}`. Read the existing consolidator tests to see how this is done; if the existing tests do not exercise consolidateDay directly, prefer a narrower harness: extract the for-loop's body into a private method (out of scope for Phase 59 unless absolutely necessary), OR construct a minimal-but-realistic digestEntries fixture + mergePlan + call consolidateDay end-to-end. The latter is preferred even if it requires a richer mock — it tests the actual integration site.
  </action>
  <verify>
    <automated>node --test src/live-logging/ObservationConsolidator.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/live-logging/ObservationConsolidator.test.js` exits 0.
    - stdout reports zero failures.
    - At least 4 new test cases are present (executor records pre-edit count; current count >= pre-edit + 4): `grep -cE "(it|test)\(" src/live-logging/ObservationConsolidator.test.js` is sufficient relative to baseline.
    - The atomicity ordering test (Test 3) uses a callLog: `grep -c "callLog" src/live-logging/ObservationConsolidator.test.js` ≥ 1.
    - The derivedFrom edge type is asserted: `grep -cE "['\"]derivedFrom['\"]" src/live-logging/ObservationConsolidator.test.js` ≥ 1.
    - The findByLegacyId mock is wired: `grep -cE "findByLegacyId" src/live-logging/ObservationConsolidator.test.js` ≥ 1.
    - The D-02 / D-02.1 / D-02.2 narrative comments are present (at least one of the three referenced per test): `grep -cE "D-02" src/live-logging/ObservationConsolidator.test.js` ≥ 3.
    - Pre-existing tests pass: the test runner's overall exit code is 0 AND no previously-named tests are missing from the new output.
    - No new console.* in the test file: `grep -v '^\s*\*' src/live-logging/ObservationConsolidator.test.js | grep -v '^\s*//' | grep -cE "console\.(log|error|warn|info)"` returns 0.
  </acceptance_criteria>
  <done>Four new tests prove the derivedFrom loop emits N edges for N resolvable observation_ids; skipped edges for unresolved ids do not crash the insert; per-edge addRelation failures are non-fatal; the atomicity ordering putEntity → findByLegacyId → addRelation is locked at the unit level.</done>
</task>

</tasks>

<threat_model>
asvs_level: 1
scope: "km-core graph mutations from local writer process (ObservationConsolidator host-side via launchctl com.coding.obs-api)"
threats:
  - name: "Log injection via metadata.observation_ids"
    severity: low
    mitigation: "observation_ids are uuids generated by ObservationWriter from internal counters, not user input. The slice(0,8) in stderr logging is a length-bounded display only, not a security boundary."
  - name: "addRelation race with km-core JSON exporter mid-flush"
    severity: low
    mitigation: "Phase 58 D-04 atomicity envelope applies verbatim: the 5s debounce window swallows putEntity + every addRelation in one export tick. Concurrent readers either see the prior state (no Digest) or the new state (Digest + N derivedFrom edges). This is the same guarantee Insights inherited from Phase 58."
  - name: "obsEntity.id resolved via findByLegacyId is stale (race with km-core in-memory hydration)"
    severity: low
    mitigation: "If findByLegacyId returns null (the race window), the edge is skipped and stderr-logged. The repair script (Plan 59-04 Layer 1) catches the missed edge on its next run. No data loss — the Digest still lands with its remaining edges."
  - name: "Per-edge addRelation failure cascades and corrupts createdCount"
    severity: low
    mitigation: "Per-edge try/catch wraps each addRelation call individually. A failure on one edge does not affect createdCount, digestedObsIds, or subsequent iterations in either the inner derivedFrom loop or the outer digestEntries loop. The createdCount increment fires unconditionally after the loop closes — the Digest IS created regardless of how many edges succeeded."
residual_risks: "None high-severity for ASVS L1. The merge branch (OC.js:1219-1267) does NOT emit derivedFrom edges; this is an acknowledged limitation backstopped by Plan 59-04 Layer 1's repair script. Operator awareness: between Phase 59 deployment and the first repair-script run, merge-branch Digests may have stale metadata.observation_ids without corresponding edges; this is the existing baseline and does not regress."
</threat_model>

<verification>
- `node --check src/live-logging/ObservationConsolidator.js` exits 0.
- `node --test src/live-logging/ObservationConsolidator.test.js` exits 0 with ≥ 4 new passing tests and zero regressions.
- The Digest plain-insert branch emits N derivedFrom edges per Digest (verified by Test 1 — happy path 3-edge case).
- Unresolved observation_ids are skipped non-fatally (verified by Test 2).
- Per-edge addRelation failures are non-fatal (verified by Test 4).
- ORPHAN-DIG-01 acceptance signal locked at the unit level: every Digest written by the plain-insert path of `consolidateDay` is followed in the same try-block by `kmStore.addRelation` calls for each entry in `row.observation_ids`.
- Live verification (newly-inserted Digests are never zero-degree) deferred to Plan 59-05's 24h soak harness.
</verification>

<success_criteria>
- The plain-insert branch of consolidateDay captures the digestMintedId and emits derivedFrom edges per d.observationIds in the same try-block as putEntity.
- Each edge carries metadata `{source: 'observation-consolidator', confidence: 1.0, addedAt: <ISO-8601>}`.
- Unresolved observation_ids (findByLegacyId returns null) are skipped with a stderr log; the Digest still lands with remaining edges.
- Per-edge addRelation failures are non-fatal (per-edge try/catch + stderr log).
- The merge branch (OC.js:1219-1267) is byte-identical post-edit; its derivedFrom-edge backfill is acknowledged-deferred to Plan 59-04 Layer 1.
- writeInsight (in OW.js) is NOT modified by this plan — that is Plan 59-01's scope.
- _pushInsightToKG (in OC.js, lines 654-665 + 679-705) is NOT modified by this plan — that is Plan 59-03's scope.
</success_criteria>

<output>
Create `.planning/phases/59-digest-insight-writer-edge-repair/59-02-SUMMARY.md` when done.
</output>
