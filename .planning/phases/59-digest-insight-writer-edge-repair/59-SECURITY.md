---
phase: 59-digest-insight-writer-edge-repair
artifact: SECURITY
status: SECURED
threats_total: 24
threats_closed: 24
threats_open: 0
accepted_risks: 1
asvs_level: 1
audit_date: 2026-06-17
auditor: secure-phase
block_on: any
---

# Phase 59 Security Audit — Digest/Insight Writer + Repair

Verification result: **SECURED** — every declared mitigation grep-confirmed in the implementation files. No `OPEN` threats. No `unregistered_flag` warnings.

## Audit Scope

- **Phase:** 59-digest-insight-writer-edge-repair
- **ASVS Level:** 1
- **Block policy:** `any` (any OPEN threat would block; result is zero opens)
- **Implementation files audited (read-only):**
  - `src/live-logging/ObservationWriter.js` (Plan 59-01)
  - `src/live-logging/ObservationConsolidator.js` (Plans 59-02 + 59-03)
  - `scripts/repair-orphan-digest-insight-edges.mjs` (Plan 59-04)
  - `scripts/poll-orphan-floor-soak.mjs` (Plan 59-05)
  - `.planning/phases/59-digest-insight-writer-edge-repair/59-SOAK-RUNBOOK.md` (Plan 59-05)
- **Plan registers cross-referenced:** `<threat_model>` blocks in 59-01-PLAN.md, 59-02-PLAN.md, 59-03-PLAN.md, 59-04-PLAN.md, 59-05-PLAN.md.
- **`## Threat Flags` review:** every SUMMARY.md inspected; all report `None` or "no new disclosure surface".

## Plan 59-01 — writeInsight signature refactor (4 threats)

Trust boundary: in-process function return. No new I/O surface.

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| T-59-01-01 | Tampering — return shape | mitigate | CLOSED | `src/live-logging/ObservationWriter.js:1319` constructs `return { legacyId: row.id, mintedId };` inline from two locals (`row.id` from caller-validated input at :1292; `mintedId` from the in-scope kmStore.putEntity return at :1313). No JSON parse, no spread of untrusted data. |
| T-59-01-02 | Information Disclosure — mintedId | accept | CLOSED (accepted) | mintedId is the km-core entity id already exposed via REST `/api/v1/entities/:id`. Surfacing to in-process callers does not widen the disclosure surface. Recorded in Accepted Risks below. |
| T-59-01-03 | Repudiation — legacyId/mintedId confusion | mitigate | CLOSED | JSDoc at OW.js:1282-1286 explicitly documents both fields, their provenance, and the D-03 rationale: `"legacyId is the stable system='A' surrogate (= row.id); mintedId is the freshly-minted km-core entity id"`. Field names are self-describing. |
| T-59-01-SC | Tampering — npm supply chain | mitigate | CLOSED | `git diff` across all Phase 59 commits shows zero changes to `package.json` / `package-lock.json`. Plan 59-01 tests use `node:test` / `node:assert` built-ins only. |

## Plan 59-02 — Digest derivedFrom emission (4 threats)

Trust boundary: in-process km-core mutations (host-side via launchctl `com.coding.obs-api`).

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| 02-T1 | Log injection via metadata.observation_ids | mitigate | CLOSED | `src/live-logging/ObservationConsolidator.js:1315,1319,1334` use `${obsId.slice(0, 8)}` and `${digestMintedId}->${obsEntity.id}` — bounded uuid surrogates, not free-form text. observation_ids are uuids generated by ObservationWriter from internal counters (not user input). |
| 02-T2 | Race with km-core JSON exporter mid-flush | mitigate | CLOSED | OC.js:1292 + :1322-1332 — putEntity and N×addRelation share the same try-block + same iteration of the digestEntries for-loop. Inline comments at OC.js:1293-1297 cite Phase 58 D-04 atomicity envelope ("5s debounce captures putEntity + every addRelation in one export tick"). |
| 02-T3 | obsEntity stale via findByLegacyId race | mitigate | CLOSED | OC.js:1312-1320 — findByLegacyId is wrapped in try/catch; null result triggers `process.stderr.write('[Consolidator] derivedFrom: observation … not yet persisted, skipping edge')` + `continue`. Plan 59-04 Layer 1 repair script is the backstop (probe-before-write loop, verified separately). |
| 02-T4 | Per-edge addRelation failure cascade | mitigate | CLOSED | OC.js:1322-1335 — each addRelation wrapped in its own try/catch with `[Consolidator] derivedFrom edge … failed (non-fatal)` stderr line + bare `continue`. `createdCount++` (OC.js:1337) and `digestedObsIds.add` (:1338) are OUTSIDE the per-edge try, so a failure on one edge does not corrupt counters. |

## Plan 59-03 — _pushInsightToKG consumer refactor (4 threats)

Trust boundary: in-process function call between ObservationConsolidator and ObservationWriter.

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| 03-T1 | Writer return shape drift | mitigate | CLOSED | Plan 59-01 Task 2 (3 tests in `ObservationWriter.test.js`, 12 passing) + Plan 59-03 Task 2 (3 tests in `ObservationConsolidator.test.js`, 7 passing) lock the contract independently. Phase 58 Test 7 mock writer updated to `{legacyId, mintedId}` shape per 59-03-SUMMARY Rule-1 deviation. |
| 03-T2 | Stale mintedId from writer | mitigate | CLOSED | Race surface is strictly narrower than the pre-edit `findByLegacyId` lookup (one km-core call instead of two). OC.js:658-659 reads `result.mintedId` directly from the writer return; the value originated from `kmStore.putEntity` (OW.js:1313) which is already-awaited inside the writer's try-block before the return. |
| 03-T3 | `result` variable shadow | mitigate | CLOSED | Method body of `_pushInsightToKG` (read at OC.js:530-704) contains no pre-existing local named `result`. The new `const result = ...` at OC.js:658 is safe. |
| 03-T4 | Consumer reads mintedId before writer addRelations | mitigate | CLOSED | JavaScript await semantics — OC.js:658 `const result = await writer.writeInsight(...)` resolves only after OW.js's full try-block returns. The has_insight addRelation at OC.js:688-697 reads persisted state; the writer guaranteed kmStore.putEntity completed at OW.js:1313 BEFORE returning. |

## Plan 59-04 — Two-layer orphan repair script (6 threats, 2 MEDIUM)

Trust boundary: host-side one-shot script mutating km-core graph state via REST at :3848 (Layer 1) and rewriting `.data/observation-export/digests.json` (Layer 2).

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| 04-T1 | Log injection via metadata.observation_ids/digest_ids | mitigate | CLOSED | All log lines use `${obsId.slice(0, 8)}` / `${digId.slice(0, 8)}` / `${eId.slice(0, 8)}` — bounded uuid surrogates. Confirmed at `scripts/repair-orphan-digest-insight-edges.mjs:194,211,222,227,255,327,342,373,431,441`. |
| **04-T2** | **Wrong-port mutation (:12436 vs :3848)** | **mitigate (MEDIUM)** | **CLOSED — 4/4 defense layers verified** | See per-layer evidence below. |
| **04-T3** | **Race with km-core JSON exporter on Layer 2** | **mitigate (MEDIUM)** | **CLOSED — operator-window + bootout doc** | The two cold-store paths target different file paths (km-core JSON exporter writes `.data/knowledge-graph/exports/general.json`; Layer 2 rewrites `.data/observation-export/digests.json`). Plan 59-04 threat-model lines 470-472 + 59-04-PLAN.md operator note document the idle-window guidance and the optional `launchctl bootout`/`kickstart` envelope. SOAK-RUNBOOK.md inherits the same operator-window discipline. |
| 04-T4 | Backup name collision | mitigate | CLOSED | `scripts/repair-orphan-digest-insight-edges.mjs:572-577` — `const stamp = new Date().toISOString().replace(/[:.]/g, '-')` → ms-resolution backup suffix; two invocations within the same millisecond is vanishingly unlikely on a one-shot script. |
| 04-T5 | Probe-before-write race | mitigate | CLOSED | Probe-before-write loop at `scripts/repair-orphan-digest-insight-edges.mjs:185-195` (`relationExists` GET /api/v1/relations?from=&to=&type=) + idempotent re-run guarantee. km-core writes on :3848 are serialized through the single-process semantic-analysis SSE server. |
| 04-T6 | Supply chain | mitigate | CLOSED | `scripts/repair-orphan-digest-insight-edges.mjs:49-52` imports only `node:fs`, `node:path`, `node:process`, `node:url` + native `fetch`. `git diff` shows zero `package.json` changes in any Phase 59 commit. |

### 04-T2 (MEDIUM) — four defense layers verified

| Layer | Expected | Verified at |
|-------|----------|-------------|
| 1. env-var default | `KMCORE_REST_BASE` defaults to `http://localhost:3848` | `scripts/repair-orphan-digest-insight-edges.mjs:58` — `const KMCORE_REST_BASE = process.env.KMCORE_REST_BASE \|\| 'http://localhost:3848';` |
| 2. var-name signaling intent | Variable name says "km-core REST", not "obs-api" | Variable name `KMCORE_REST_BASE` (never `OBS_API_BASE`). `grep -c "OBS_API_BASE" → 0`. |
| 3. grep-asserted absence of wrong port | `:12436` literal must not appear | `grep -c ":12436" scripts/repair-orphan-digest-insight-edges.mjs → 0`. |
| 4. pre-flight surface for operator | Pre-flight logs the actual KMCORE_REST_BASE + stats response | Lines 155-167 fetch `/api/v1/stats`, log `pre-flight OK at ${KMCORE_REST_BASE}: nodes=… edges=… orphans=…`. Lines 157,170 emit `KMCORE_REST_BASE unreachable: ${KMCORE_REST_BASE}` on failure → `process.exit(2)`. Live smoke evidence in 59-04-SUMMARY.md ("`nodes=840 edges=1675 orphans=7`" against `:3848`). |

## Plan 59-05 — Soak harness (6 threats, 1 MEDIUM)

Trust boundary: host-side polling script reading from localhost:3848 km-core REST over a 24h window; output written to `.data/` only.

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| 05-T1 | Log injection via /api/v1/stats | mitigate | CLOSED | The stats response is internal JSON with numeric counters only (`nodeCount`, `edgeCount`, `orphanCount`, `connectivity`). Log lines at `scripts/poll-orphan-floor-soak.mjs:104,136` embed only these numeric values. |
| **05-T2** | **Wrong-port soak (:12436 vs :3848)** | **mitigate (MEDIUM)** | **CLOSED — 4/4 defense layers verified** | See per-layer evidence below. |
| 05-T3 | Session-log path collision | mitigate | CLOSED | `scripts/poll-orphan-floor-soak.mjs` builds outPath from `new Date().toISOString().replace(/[:.]/g, '-')` (ms-resolution). Concurrent soak runs are operator-discouraged in the runbook; collision requires sub-ms simultaneity. |
| 05-T4 | Disk fill from incremental writes | mitigate | CLOSED | 24 samples × ~200B = ~5KB total. Incremental write uses `fsp.writeFile(outPath, ...)` (truncate + rewrite, not append) — disk usage stays bounded at sub-10KB. |
| 05-T5 | Operator interpretation of 'sustained ≤ 10' | mitigate | CLOSED | `.planning/phases/59-digest-insight-writer-edge-repair/59-SOAK-RUNBOOK.md` Section 3 (4-row decision matrix at runbook lines 91-96) + Section 4 (3 breach-escalation paths at lines 100-106). Binary exit code (0/1) gives unambiguous SC#4 signal; matrix supports nuanced operator judgment afterward. |
| 05-T6 | Supply chain | mitigate | CLOSED | `scripts/poll-orphan-floor-soak.mjs:32-34` imports only `node:process`, `node:fs/promises`, `node:path` + native `fetch`. `git diff` confirms zero `package.json` changes in any Phase 59 commit. |

### 05-T2 (MEDIUM) — four defense layers verified

| Layer | Expected | Verified at |
|-------|----------|-------------|
| 1. env-var default | `KMCORE_REST_BASE` defaults to `http://localhost:3848` | `scripts/poll-orphan-floor-soak.mjs:40-41` — `const KMCORE_REST_BASE = process.env.KMCORE_REST_BASE \|\| 'http://localhost:3848';` |
| 2. var-name signaling intent | Variable name says "km-core REST", not "obs-api" | `grep -c "OBS_API_BASE" scripts/poll-orphan-floor-soak.mjs → 0`. |
| 3. grep-asserted absence of wrong port | `:12436` literal must not appear | `grep -c ":12436" scripts/poll-orphan-floor-soak.mjs → 0`. Plan 59-05 SUMMARY Rule-3 deviation #2 records the comment-rewrite that drove this count to zero. |
| 4. end-of-run summary surface for operator | `kmcoreRestBase` field in stdout summary; runbook decision matrix flags invalid soak | Script lines 177, 226 — both incremental and final-write paths include `kmcoreRestBase: KMCORE_REST_BASE` in the JSON summary. Runbook line 96 explicitly lists `kmcoreRestBase != http://localhost:3848` as INVALID. Pre-flight unreachable message at script line 106 matches Plan 59-04 phrasing for cross-script consistency. |

## Threat Flags Review (from SUMMARY.md `## Threat Flags` sections)

| Plan | `## Threat Flags` content | Disposition |
|------|---------------------------|-------------|
| 59-01-SUMMARY.md | "None. … refactor is purely type-level; no new disclosure surface" | Informational — no unregistered surface |
| 59-02-SUMMARY.md | (not present as a top-level section; threat-model is in frontmatter) | Informational |
| 59-03-SUMMARY.md | "None. … no new trust boundaries, no new package installs, no new I/O surface, and no new disclosure surface" | Informational |
| 59-04-SUMMARY.md | Threats embedded in `key-decisions` + body; no separate `## Threat Flags` section, but port-discipline anchor explicitly called out | Informational |
| 59-05-SUMMARY.md | "None new. … passive read-only consumer; the wrong-port-soak threat is mitigated by four layers" | Informational |

No `unregistered_flag` entries. Every new attack surface introduced by Phase 59 maps to a declared threat ID.

## Accepted Risks Log

| Risk ID | Description | Disposition | Acceptance Rationale |
|---------|-------------|-------------|---------------------|
| T-59-01-02 | mintedId visible to in-process callers of `writeInsight` | accept | mintedId is the km-core entity id, already exposed via km-core REST `/api/v1/entities/:id` (Phase 44). Surfacing to an in-process caller adds zero net disclosure. Plan 59-01 threat-register frontmatter records the explicit `accept` disposition. |

No `transfer` dispositions in the Phase 59 register.

## Audit Trail

- **2026-06-17** — Audit conducted by `/gsd:secure-phase`. All implementation files read at the line ranges cited above. No implementation file was modified by this audit. Live grep gates executed via Bash tool against the worktree at the cited line numbers; line numbers match the SUMMARY.md self-checks for each plan.
- **Method:** Per-threat verification by disposition (mitigate → grep for pattern in cited files; accept → log entry; transfer → N/A in Phase 59).
- **Files searched (read-only):**
  - `src/live-logging/ObservationWriter.js` (lines 1280-1326)
  - `src/live-logging/ObservationConsolidator.js` (lines 640-705, 1285-1340)
  - `scripts/repair-orphan-digest-insight-edges.mjs` (lines 49-52, 58, 141, 150-170, 176-260, 327-441, 565-580)
  - `scripts/poll-orphan-floor-soak.mjs` (lines 32-34, 40-41, 60-110, 175-230)
  - `.planning/phases/59-digest-insight-writer-edge-repair/59-SOAK-RUNBOOK.md` (lines 1-117)
- **MEDIUM threat four-layer audits:** completed for 04-T2 and 05-T2 (the 04-T3 race threat has its own dedicated documentation row in the table; not classified as needing the four-layer port template).
- **package.json review:** `git log -p ... -- package.json package-lock.json` since `992b145ea` shows zero modifications across every Phase 59 commit.

## Result

**SECURED.** All 24 declared threats either CLOSED with code evidence or accepted with rationale. Phase 59 cleared for ship per the `block_on: any` policy.
