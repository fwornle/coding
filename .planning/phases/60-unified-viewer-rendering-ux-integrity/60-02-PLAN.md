---
phase: 60-unified-viewer-rendering-ux-integrity
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - integrations/unified-viewer/src/panels/LegendPanel.tsx
  - integrations/unified-viewer/src/panels/LegendPanel.test.tsx
  - integrations/unified-viewer/src/panels/FilterRail.tsx
autonomous: true
requirements: [VKBUI-02]
must_haves:
  truths:
    - "Sidebar Legend DOMAINS section lists only ontologyClass values present in rendered entities"
    - "Sidebar Legend LAYERS section lists only deriveLayer() values present in rendered entities"
    - "Sidebar Legend SOURCE section lists only metadata.source values present in rendered entities"
    - "Sidebar Legend RELATIONSHIPS section lists only relation.type values present in rendered relations"
    - "Static SHAPE_SWATCHES / LAYER_BADGE_CLASS-derived legend / SOURCE_SAMPLES / RELATIONSHIP_SAMPLES arrays no longer drive Legend rendering"
    - "Empty section (zero entries) is hidden entirely; no '(none)' placeholder rows (D-07)"
    - "Section ordering preserved: DOMAINS → LAYERS → SOURCE → RELATIONSHIPS (D-08)"
    - "No OKB-only content (RuntimeDiagnostics, Official doc, Automated RCA, etc.) appears in VKB tab when those tags are absent from rendered set"
  artifacts:
    - path: "integrations/unified-viewer/src/panels/LegendPanel.tsx"
      provides: "Dynamic LegendPanel that receives entities + relations props and derives all sections"
      contains: "entities: readonly Entity[]"
    - path: "integrations/unified-viewer/src/panels/LegendPanel.test.tsx"
      provides: "Tests covering all 4 sections with multiple entity-set fixtures incl. empty sections"
    - path: "integrations/unified-viewer/src/panels/FilterRail.tsx"
      provides: "LegendPanel mounted with entities + relations props from FilterRail"
      contains: "<LegendPanel"
  key_links:
    - from: "integrations/unified-viewer/src/panels/LegendPanel.tsx"
      to: "integrations/unified-viewer/src/graph/layer.ts"
      via: "import { deriveLayer }"
      pattern: "from '[@/.]+/graph/layer'"
    - from: "integrations/unified-viewer/src/panels/LegendPanel.tsx"
      to: "integrations/unified-viewer/src/graph/vokb-palette.ts"
      via: "EDGE_STYLES + LAYER_BADGE_CLASS lookup (palette stays the source for color/shape values)"
      pattern: "from '[@/.]+/graph/vokb-palette'"
    - from: "integrations/unified-viewer/src/panels/FilterRail.tsx"
      to: "integrations/unified-viewer/src/panels/LegendPanel.tsx"
      via: "<LegendPanel entities={...} relations={...}/>"
      pattern: "<LegendPanel[^/]*entities="
---

<objective>
G2 (D-05..D-08) — Rewrite `LegendPanel.tsx` so every section is derived from the currently-rendered
(post-filter) entities + relations. Drop the four static arrays that ship OKB content into the VKB tab.
Closes VKBUI-02.

Purpose: Today the Legend ships `RuntimeDiagnostics` / `Automated RCA` / `Official doc` / etc. regardless
of what's on screen. Operators are misled about what the canvas actually contains.

Output: LegendPanel becomes a thin filter over the existing palette tables, keyed on the rendered set.
Color/shape/edge lookups stay in `vokb-palette`; the Legend just selects from them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/60-unified-viewer-rendering-ux-integrity/60-CONTEXT.md
@integrations/unified-viewer/src/panels/LegendPanel.tsx
@integrations/unified-viewer/src/panels/FilterRail.tsx
@integrations/unified-viewer/src/graph/vokb-palette.ts
@integrations/unified-viewer/src/graph/layer.ts
@integrations/unified-viewer/src/api/ApiClient.ts

<interfaces>
From integrations/unified-viewer/src/graph/layer.ts (Plan 60-01):
```typescript
export type Layer = 'evidence' | 'pattern';
export function deriveLayer(
  entity: { ontologyClass?: string; metadata?: { layer?: string }; layer?: string },
  registry: readonly { name: string; parent?: string | null }[] | undefined
): Layer;
```

From integrations/unified-viewer/src/api/ApiClient.ts:
```typescript
export interface Entity {
  id: string
  name: string
  ontologyClass: string
  description?: string | null
  level?: number
  [k: string]: unknown
}
export interface Relation { from: string; to: string; type?: string; [k: string]: unknown }
```

From integrations/unified-viewer/src/graph/vokb-palette.ts (existing exports, do NOT change):
```typescript
export const LAYER_BADGE_CLASS: Record<string, string>;  // {evidence: '...', pattern: '...'}
export const EDGE_STYLES: Record<string, { color: string; dasharray: string }>;
export const SHAPE_PALETTE: Record<string, { color: string; shape: string }>;
```

From integrations/unified-viewer/src/graph/color-fallback.ts:
```typescript
// shape/color lookup with gray fallback for unknown classes
```

From FilterRail.tsx today (line ~217 mount points):
```tsx
<LayerFilter entities={entities} />
<OntologyFilter entities={entities} groupingSchema={ontologySchema} />
<GraphToggles />
<LegendPanel />                    // ← receives no props today
```

The `entities` array FilterRail already has in scope IS the post-filter set passed to LayerFilter and
OntologyFilter — so Legend just needs to consume the same prop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite LegendPanel.tsx to derive all sections from entities + relations props</name>
  <files>
    integrations/unified-viewer/src/panels/LegendPanel.tsx
    integrations/unified-viewer/src/panels/LegendPanel.test.tsx
  </files>
  <read_first>
    integrations/unified-viewer/src/panels/LegendPanel.tsx (full current file)
    integrations/unified-viewer/src/panels/LegendPanel.test.tsx (existing test conventions)
    integrations/unified-viewer/src/graph/vokb-palette.ts (LAYER_BADGE_CLASS + EDGE_STYLES + SHAPE_PALETTE)
    integrations/unified-viewer/src/graph/color-fallback.ts (SHAPE_PALETTE + fallback logic)
    integrations/unified-viewer/src/graph/layer.ts (Plan 60-01 deriveLayer signature)
    integrations/unified-viewer/src/api/ApiClient.ts (Entity + Relation shapes)
    .planning/phases/60-unified-viewer-rendering-ux-integrity/60-CONTEXT.md (D-05..D-08)
  </read_first>
  <behavior>
    - Test 1 (DOMAINS derivation): entities = 3 of class 'Component', 2 of 'Service', 1 of 'Pattern' → DOMAINS section renders exactly 3 rows (Component / Service / Pattern), each with the SHAPE swatch from SHAPE_PALETTE or color-fallback. No 'RuntimeDiagnostics' row.
    - Test 2 (DOMAINS unknown-class fallback): entity with ontologyClass='NovelClass2099' not in SHAPE_PALETTE → row renders with fallback circle + gray + tooltip text "class without registered shape"
    - Test 3 (LAYERS derivation): entities = 2 'Component' + 1 'Pattern' (with deriveLayer treating Pattern as 'pattern') → LAYERS section renders both 'evidence' and 'pattern' badges
    - Test 4 (LAYERS single-layer): entities all class 'Component' → LAYERS section renders only 'evidence' badge (no 'pattern' row)
    - Test 5 (SOURCE derivation): entities with metadata.source values ['manual', 'auto', 'auto', 'online'] → SOURCE section renders 3 distinct rows (manual / auto / online). No 'Official doc' / 'Automated RCA' / 'User input' / 'Team knowledge' rows.
    - Test 6 (SOURCE missing): entities with no metadata.source → SOURCE section hidden entirely (D-07)
    - Test 7 (RELATIONSHIPS derivation): relations with type values ['PART_OF', 'PART_OF', 'CAUSED_BY'] → RELATIONSHIPS section renders 2 rows (PART_OF / CAUSED_BY). No 'RELATES_TO' or other unrelated rows.
    - Test 8 (RELATIONSHIPS unknown-type fallback): relation with type='NOVEL_REL' not in EDGE_STYLES → row renders with `#d1d5db` gray line (defensive existing behavior preserved)
    - Test 9 (D-07 empty sections hidden): empty entities + empty relations → no Section components rendered inside the open <details>; only the summary 'Legend' visible
    - Test 10 (D-08 section order): when all four sections have entries, the DOM order is DOMAINS → LAYERS → SOURCE → RELATIONSHIPS
    - Test 11 (no static OKB bleed): grep the rendered output for the strings 'RuntimeDiagnostics', 'Official doc', 'Automated RCA', 'Team knowledge', 'User input', 'CORRELATED_WITH', 'RELATES_TO' — with entities=[{ontologyClass:'Component'}] and relations=[] none of those strings appear in the rendered text content.
  </behavior>
  <action>
    Replace `integrations/unified-viewer/src/panels/LegendPanel.tsx` end-to-end:

    1. Remove the constants `SHAPE_SWATCHES`, `SOURCE_SAMPLES`, `RELATIONSHIP_SAMPLES`, and the
       `interface ShapeSwatch` and `interface SourceSample` declarations entirely.
    2. Add new exports:
       - `export interface LegendPanelProps { entities: readonly Entity[]; relations: readonly Relation[]; ontologyRegistry?: readonly { name: string; parent?: string | null }[]; className?: string }`
       - The default export `LegendPanel({ entities, relations, ontologyRegistry, className })` reads these props.
    3. Imports: keep `EDGE_STYLES` from `@/graph/vokb-palette`. Add `SHAPE_PALETTE` (or whatever the shape-lookup export is named — confirm by reading `vokb-palette.ts` and `color-fallback.ts`) and the fallback resolver. Add `import { deriveLayer } from '@/graph/layer'`. Add `import type { Entity, Relation } from '@/api/ApiClient'`.
    4. Derive 4 memoized arrays:
       - `domains: { className: string; shape: string; color: string; isFallback: boolean }[]` — distinct `entity.ontologyClass` values present in `entities`, mapped via SHAPE_PALETTE/color-fallback; unknown classes carry `isFallback: true` so the row renders a gray circle with tooltip `"class without registered shape"`.
       - `layers: Layer[]` — distinct `deriveLayer(e, ontologyRegistry)` values across `entities`; preserve insertion order ('evidence' before 'pattern' if both present, by walking entities in order).
       - `sources: string[]` — distinct truthy `entity.metadata?.source` values present.
       - `relTypes: string[]` — distinct truthy `relation.type` values present.
    5. Reuse the existing `Section` component (per D-22 — keep it co-located in `LegendPanel.tsx` since no
       other file imports it; verify with `grep -rn "from.*LegendPanel.*Section"` — if no consumers, keep
       it private). Reuse the existing `ShapeIcon` SVG component — but extend the `shape` union to accept
       any string and dispatch to the appropriate primitive (circle as final fallback).
    6. Render sections in order DOMAINS → LAYERS → SOURCE → RELATIONSHIPS (D-08). Skip rendering a
       `<Section>` when its derived array is empty (D-07).
    7. DOMAINS section per-row: render `<ShapeIcon shape={d.shape} color={d.color}/> <span>{d.className}</span> <span className="text-muted-foreground">({d.shape})</span>`. For fallback rows, wrap in `<span title="class without registered shape">`.
    8. LAYERS section per-row: render the existing `LAYER_BADGE_CLASS[layer]`-styled chip (verbatim — palette lookup unchanged). Skip rows for layers absent from the derived set.
    9. SOURCE section per-row: render `<span>{sourceValue}</span>` (plain text label of whatever tag is in the data — D-06). No stroke/dasharray styling — that was OKB-VOKB encoding and doesn't translate. Keep `data-testid="legend-source-{sourceValue}"`.
    10. RELATIONSHIPS section per-row: keep the existing SVG line rendering with `EDGE_STYLES[type]?.color ?? '#d1d5db'` fallback (already defensive). Label = the relation type literal. Skip if type is absent from EDGE_STYLES AND no entries.
    11. Preserve the `<details>` wrapper with `<summary>Legend</summary>` — collapsible visible even when empty (D-07 explicit).
    12. Keep all UI-SPEC §3 micro-type classes verbatim: `text-[10px]`, `text-[11px]` row text, etc. No new design tokens.

    Then create / rewrite `integrations/unified-viewer/src/panels/LegendPanel.test.tsx` with the 11
    behaviors above. Use vitest + @testing-library/react. Build entity/relation fixtures inline. For
    Test 11, use `screen.queryByText` to assert NEGATIVE absence of static OKB strings.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer && npx vitest run src/panels/LegendPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SHAPE_SWATCHES" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns 0
    - `grep -c "SOURCE_SAMPLES" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns 0
    - `grep -c "RELATIONSHIP_SAMPLES" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns 0
    - `grep -c "RuntimeDiagnostics" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns 0
    - `grep -c "Automated RCA\|Official doc\|Team knowledge" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns 0
    - `grep -n "entities: readonly Entity\[\]" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns at least one match
    - `grep -n "relations: readonly Relation\[\]" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns at least one match
    - `grep -n "deriveLayer" integrations/unified-viewer/src/panels/LegendPanel.tsx` returns at least one match
    - `cd integrations/unified-viewer && npx vitest run src/panels/LegendPanel.test.tsx` exits 0 with 11 tests passing
  </acceptance_criteria>
  <done>
    LegendPanel is fully prop-driven; every section reflects only what is in the rendered set; OKB-only content removed from VKB tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire LegendPanel props from FilterRail (entities + relations from current visible set)</name>
  <files>
    integrations/unified-viewer/src/panels/FilterRail.tsx
  </files>
  <read_first>
    integrations/unified-viewer/src/panels/FilterRail.tsx (full current file, especially the LegendPanel mount around line 226 and the `entities` / `relations` props the rail already receives)
    integrations/unified-viewer/src/panels/LegendPanel.tsx (Task 1 — new Props shape)
    integrations/unified-viewer/src/graph/D3GraphCanvas.tsx lines 250-340 (the visibleEntities memo — confirms FilterRail already has the post-filter set)
  </read_first>
  <action>
    Edit `integrations/unified-viewer/src/panels/FilterRail.tsx`:

    1. Confirm `entities: readonly Entity[]` and any `relations: readonly Relation[]` are already in the
       `FilterRailProps` interface. If `relations` is NOT yet plumbed through, add it to the
       `FilterRailProps` interface as `relations: readonly Relation[]` (Relation imported from
       `@/api/ApiClient`).
    2. Update the `<LegendPanel />` mount site (~line 226) to:
       `<LegendPanel entities={entities} relations={relations} />`
       Do not pass `ontologyRegistry` yet — the optional prop in LegendPanel handles undefined gracefully.
    3. If `relations` was not in props, propagate the addition up to the FilterRail consumer (typically `App.tsx` / `panels/coding/CodingTab` / similar) — locate via `grep -rn "<FilterRail" integrations/unified-viewer/src/` and add `relations={relations}` to each call site, where the parent already has `relations` in scope (D3GraphCanvas / useGraphData already produce it). If a call site doesn't have `relations` in scope, pass `relations={[]}` and add a comment `// TODO Phase 60-06: thread real relations` rather than refactoring the data flow in this plan.
    4. Run `npm run build` inside `integrations/unified-viewer/` to confirm no TypeScript errors.
  </action>
  <verify>
    <automated>cd integrations/unified-viewer && npx tsc --noEmit && npx vitest run src/panels/LegendPanel.test.tsx src/panels/FilterRail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "<LegendPanel" integrations/unified-viewer/src/panels/FilterRail.tsx` shows the mount carries `entities={entities}` AND `relations={relations}` (or `relations={[]}` with TODO comment)
    - `cd integrations/unified-viewer && npx tsc --noEmit` exits 0
    - `cd integrations/unified-viewer && npx vitest run src/panels/LegendPanel.test.tsx src/panels/FilterRail.test.tsx` exits 0
    - `cd integrations/unified-viewer && npm run build` exits 0 (bind-mounted submodule build per CLAUDE.md)
  </acceptance_criteria>
  <done>
    LegendPanel receives the same post-filter entities array that LayerFilter and OntologyFilter receive; relations are threaded; submodule builds clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → render | entities/relations props consumed without validation (trusted upstream — same predicate as canvas) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | Information Disclosure | LegendPanel SOURCE section | accept | metadata.source labels are non-PII tags ('manual'/'auto'/'online') |
| T-60-02-02 | Denial of Service | LegendPanel derivation | mitigate | useMemo memoization keyed on entities/relations references (no recompute on identical-content writes per Phase 56 contract #3) |
</threat_model>

<verification>
- Visual smoke (handled in Plan 60-06): with VKB tab loaded, Legend shows the real entities + relations on screen
- Phase 56 viewport stability preserved: useMemo dep arrays don't include unstable references
- Tests cover both populated and empty section paths (D-07)
</verification>

<success_criteria>
- VKBUI-02 SC#2: no static OKB-domain entries (RuntimeDiagnostics et al.) render when zero such nodes are on screen
- DOMAINS / LAYERS / SOURCE / RELATIONSHIPS sections all derive from rendered set
- Empty sections hidden; section order preserved
</success_criteria>

<output>
Create `.planning/phases/60-unified-viewer-rendering-ux-integrity/60-02-SUMMARY.md` when done
</output>
