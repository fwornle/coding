---
phase: 60-unified-viewer-rendering-ux-integrity
plan: 07
type: execute
wave: 4
depends_on: [60-04, 60-05, 60-06]
gap_closure: true
resolves_gaps:
  - "SC#5 PARTIAL — OntologyFilter L1→L2 hierarchy not rendering because GET /api/v1/ontology/classes?withDisplay=true returns 145 entries with all level=null, no parent (Phase 60 60-VERIFICATION.md § SC#5)"
files_modified:
  - .data/ontologies/coding-ontology.json
  - .data/ontologies/coding.lower.json
  - lib/km-core/src/api/handlers/ontology.ts
  - lib/km-core/src/api/__tests__/ontology-handler.withDisplay-level-parent.test.ts
  - lib/km-core/src/ontology/registry.ts
autonomous: false
requirements: [LOWERONTO-03]
must_haves:
  truths:
    - "GET /api/v1/ontology/classes?withDisplay=true for the coding system returns `level` and `parent` for every L1 and L2 class (no more level=null on Component/SubComponent/Detail/Phase-57-lowers)"
    - "L0 anchors (System, Project) surface in the enriched response with `level: 0` — either authored as classes in coding-ontology.json OR synthesized in the handler from @fwornle/km-core HIERARCHY_ROOTS / HIERARCHY_ROOT_CLASS"
    - "OntologyFilter at /viewer/coding renders ungrouped L0 rows (System, Project) at the top of the section, L1 Component as a collapsible group containing the Phase-57 L2 children (LiveLoggingSystem, ConstraintMonitor, KnowledgeManagement, BatchSemanticAnalysis, RapidLlmProxy, DockerizedServices), L1 Detail as a collapsible group containing L2 children (OnlineObservation, OnlineDigest, OnlineInsight), L1 SubComponent as a group containing EtmDaemon"
    - "Phase 60 SC#5 visual smoke screenshots in `screenshots/sc5-ontology-filter.png` show the L1→L2 hierarchy with at least one L1 group expanded; `[all]` / `[none]` link-buttons present per group; NO `Typed Views` group"
    - "OKB tab regression — visiting /viewer/okb still renders the VOKB_SCHEMA hardcoded path (Upper Ontology + Failure Model groups) intact; the API changes do NOT degrade okb rendering (Plan 60-05 dual-mode contract preserved)"
    - "Backward compatibility — the BC string-array path (GET /api/v1/ontology/classes WITHOUT ?withDisplay=true) still returns the same set of class-name strings byte-identically to pre-60.07 (T-45-04-03 mitigation preserved)"
    - "Re-run of the visual-smoke script in Plan 60-06 § Task 2 updates 60-VERIFICATION.md: SC#5 → PASS (was PARTIAL); status frontmatter flips `gaps_found` → `passed`"
  artifacts:
    - path: ".data/ontologies/coding-ontology.json"
      provides: "Component/SubComponent/Detail get explicit `level: 1` at the class level (not nested in properties); System and Project synthesized via NEW top-level entries with `level: 0` (or this is moved to handler-side synthesis — planner picks during research)"
      contains: "\"level\": 1"
    - path: ".data/ontologies/coding.lower.json"
      provides: "Every L2 class gets `level: 2` and `parent: <L1>` derived literally from its `extends` field (10 classes total per Phase 57 D-09)"
      contains: "\"level\": 2"
    - path: "lib/km-core/src/api/handlers/ontology.ts"
      provides: "Enriched response derives `parent` from `extends` when explicit `parent` absent; surfaces L0 anchors from HIERARCHY_ROOTS when configured; keeps BC string-array path byte-identical"
      contains: "HIERARCHY_ROOTS"
    - path: "lib/km-core/src/api/__tests__/ontology-handler.withDisplay-level-parent.test.ts"
      provides: "Unit test fixture for the coding system asserting every Phase-57 L2 class returns level=2 + parent=<L1>; every L1 returns level=1; L0 anchors (System, Project) returned with level=0"
  key_links:
    - from: "lib/km-core/src/api/handlers/ontology.ts"
      to: "lib/km-core/src/types/hierarchy-roots.ts"
      via: "import { HIERARCHY_ROOTS, HIERARCHY_ROOT_CLASS } from '../../types/hierarchy-roots.js'"
      pattern: "hierarchy-roots"
    - from: ".data/ontologies/coding.lower.json"
      to: "GET /api/v1/ontology/classes?withDisplay=true response"
      via: "level + parent fields flow through registry resolution to enriched handler path"
      pattern: "level.*parent"
    - from: ".planning/phases/60-unified-viewer-rendering-ux-integrity/60-VERIFICATION.md"
      to: "screenshots/sc5-ontology-filter.png"
      via: "re-run smoke flips status:passed and updates sc_passed:5 sc_partial:0"
      pattern: "screenshots/sc5-"
---

<objective>
Close the SC#5 gap surfaced by Phase 60 verification: `OntologyFilter.tsx` (post-60-05) drops the
hardcoded `CODING_SCHEMA` stopgap and correctly calls
`GET /api/v1/ontology/classes?withDisplay=true`, but the API returns 145 entries with all
`level: null` and no `parent` — so the viewer falls back to flat rows for whatever's in the rendered
set (currently just `Digest 34` + `Insight 49`). L0 anchors (`System`, `Project`) and the Phase 57
L2 classes (`LiveLoggingSystem`, `ConstraintMonitor`, `KnowledgeManagement`, `BatchSemanticAnalysis`,
`RapidLlmProxy`, `DockerizedServices`, `EtmDaemon`, `OnlineObservation`, `OnlineDigest`,
`OnlineInsight`) never reach the filter because no `{level, parent}` metadata flows through.

Two-side fix:
(a) Data side — annotate `coding-ontology.json` L1 classes with `level: 1` and
`coding.lower.json` L2 classes with `level: 2` + explicit `parent: <L1>` (derived from `extends`).
(b) Code side — patch `lib/km-core/src/api/handlers/ontology.ts` enriched-path to:
- derive `parent` from `extends` when explicit `parent` is absent (defensive),
- synthesize L0 anchors (`System`, `Project`) from the existing
  `HIERARCHY_ROOTS` / `HIERARCHY_ROOT_CLASS` constants (Plan 60-04 deliverable —
  single source of truth, no new constant)

Output: API now returns `{name, level, parent, display}` for every coding-system class. Viewer
OntologyFilter renders L0 rows at top, L1 collapsible groups with L2 children. SC#5 flips to PASS;
60-VERIFICATION.md re-runs cleanly; phase 60 closes without an open gap.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/60-unified-viewer-rendering-ux-integrity/60-CONTEXT.md
@.planning/phases/60-unified-viewer-rendering-ux-integrity/60-VERIFICATION.md
@.planning/phases/60-unified-viewer-rendering-ux-integrity/60-05-SUMMARY.md
@.planning/phases/60-unified-viewer-rendering-ux-integrity/60-04-SUMMARY.md
@.data/ontologies/coding-ontology.json
@.data/ontologies/coding.lower.json
@.data/ontologies/upper.json
@lib/km-core/src/api/handlers/ontology.ts
@lib/km-core/src/types/hierarchy-roots.ts
@lib/km-core/src/ontology/registry.ts
@integrations/unified-viewer/src/panels/filters/OntologyFilter.tsx

<gap_recap>
From 60-VERIFICATION.md § SC#5:
- API GET /api/v1/ontology/classes?withDisplay=true on port 3848 returns 145 entries
- All entries have level=null, no parent
- Sample: `[{"name":"File"},{"name":"Service"},{"name":"Feature"}]`
- OntologyFilter availSet falls through to flat-row path; L0/L1/L2 hierarchy not rendered
- Plan 60-05 viewer code is correct; gap is upstream API not honoring `withDisplay=true` enrichment
- `coding.lower.json` carries 10 L2 classes via `extends: <L1>` but no explicit `level`/`parent`
- `coding-ontology.json` Component/SubComponent/Detail have `properties.level.description: "always 1 for Component"` (schema description, NOT class-level metadata)
- `upper.json` has 14 root classes, none of which include System / Project / Component (mismatch — the coding-system L1 hierarchy lives in coding-ontology.json, not upper.json)
- `coding-ontology.json` has `System: null`, `Project: null`, `Insight: null`, `Digest: null` — these names appear as `ontologyClass` strings on entity instances but are NOT registered as ontology classes
</gap_recap>

<existing_assets>
- `lib/km-core/src/types/hierarchy-roots.ts` (Plan 60-04 deliverable) — `HIERARCHY_ROOTS` tuple, `HIERARCHY_ROOT_CLASS` map. Authoritative source for which names are L0 anchors and what class they belong to (`CollectiveKnowledge: 'System'`, `Coding/DynArch/Timeline/Normalisa: 'Project'`).
- `lib/km-core/src/api/handlers/ontology.ts:138-208` — enriched-path branch already exists, reads `cls.level` (number) + `cls.parent` (string) verbatim from resolved class. The gap is the resolved class doesn't carry these fields.
- `lib/km-core/src/ontology/registry.ts:165-170` — `extends` chain resolution copies parent's `relationships` + `properties` to children but does NOT propagate or derive `level` / `parent` as first-class fields.
- `integrations/unified-viewer/src/panels/filters/OntologyFilter.tsx:452-484` — L1→L2 group construction logic already handles `availSet`-filtered display; will light up automatically once the API supplies `{level, parent}`.
</existing_assets>

<approach_decision_for_research>
**Open question — must resolve during plan execution research (not deferred):**

Where should L0 anchors come from? Two viable paths:

**Path A — Data fix:** add `System` and `Project` as top-level classes in
`.data/ontologies/coding-ontology.json` with `level: 0` and no `extends`. Pros: handler stays
ignorant of HIERARCHY_ROOTS; data is self-describing; same shape as L1/L2. Cons: duplicates the
HIERARCHY_ROOTS source of truth across two files; risk of drift.

**Path B — Code synthesis:** handler imports `HIERARCHY_ROOTS` / `HIERARCHY_ROOT_CLASS` from
@fwornle/km-core (already exported per Plan 60-04 SUMMARY.md), synthesizes L0 entries with
`{name: <root-class>, level: 0}` (where root-class ∈ {System, Project}) and prepends them to the
enriched response. Pros: single source of truth for the closed-set roots; data files stay focused on
classes that have proper class definitions. Cons: handler grows a tiny dependency on the constants.

Planner picks during execution research; **Path B (HIERARCHY_ROOT_CLASS synthesis) is recommended**
because Plan 60-04 already established HIERARCHY_ROOTS as the single source of truth for closed-set
roots, and the discuss-phase D-14 + D-23 explicitly said "constants imported from a new
`lib/km-core/src/types/hierarchy-roots.ts` so the source of truth is sharable". Document the choice
in the task notes.

**Conditional acceptance criteria:** if Path A picked, additional acceptance gates on
`coding-ontology.json` must include "`System` and `Project` exist as top-level classes with
`level: 0`"; if Path B picked, additional acceptance gates on `ontology.ts` must include
"`HIERARCHY_ROOTS` imported AND synthesized L0 entries appear in enriched output for the coding
system regardless of registry contents".
</approach_decision_for_research>

<vite_proxy_note>
The verification smoke from Plan 60-06 found that `fetch("/api/v1/ontology/classes?withDisplay=true")`
from inside the viewer (Vite dev server on :5173) returned `Content-Type: text/html` — i.e. the SPA
index, not the API. This is a separate Vite-proxy config issue, NOT part of this gap-closure scope.
The smoke check used the obs-api directly at port 3848. The viewer is already correctly calling
`apiClient.listOntologyClasses()` per `ApiClient.ts:134-138` — and the runtime config that points
`apiClient` at the correct base URL is out-of-scope for this plan (it works in production routing
through coding-services).

Verification at the end of this plan MUST use both:
(a) `curl http://localhost:3848/api/v1/ontology/classes?withDisplay=true | jq` for the API gate, AND
(b) `gsd-browser` against `http://localhost:5173/viewer/coding` for the visual gate (assuming the
    viewer's apiClient base URL is correctly configured to reach 3848; if not, add a smaller
    follow-up todo and capture the gap explicitly in the new SUMMARY).
</vite_proxy_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data fix — annotate coding-ontology.json L1 + coding.lower.json L2 with explicit level / parent</name>
  <files>
    .data/ontologies/coding-ontology.json
    .data/ontologies/coding.lower.json
    lib/km-core/src/api/__tests__/ontology-data-shape.test.ts (NEW)
  </files>
  <read_first>
    .data/ontologies/coding-ontology.json (current Component/SubComponent/Detail definitions — note level is in properties, not at class level)
    .data/ontologies/coding.lower.json (10 L2 classes via `extends`)
    .data/ontologies/upper.json (none of System/Project/Component live here — surprise from research)
    lib/km-core/src/ontology/registry.ts:140-180 (`extends` chain resolution — confirm `level`/`parent` aren't being added or stripped during resolveExtends)
    lib/km-core/src/types/ontology.ts (ResolvedClass shape — confirm `level?` and `parent?` are extensible without TypeScript pushback)
  </read_first>
  <behavior>
    - Test 1: `coding-ontology.json` Component class object has top-level `level: 1` (NOT nested under properties)
    - Test 2: SubComponent has top-level `level: 1` (per discuss-phase: L1 mid-tier; or `level: 2` if the discuss decides SubComponent is L2 — confirm against 60-CONTEXT.md before coding)
    - Test 3: Detail has top-level `level: 1` (or `level: 3` per the 4-level-hierarchy comment in its description — confirm during research)
    - Test 4: Every class in `coding.lower.json` has `level: 2` AND `parent` matching its `extends` value (10 classes — LiveLoggingSystem, ConstraintMonitor, KnowledgeManagement, BatchSemanticAnalysis, RapidLlmProxy, DockerizedServices, OnlineObservation, OnlineDigest, OnlineInsight, EtmDaemon)
    - Test 5: BC — no class in coding-ontology.json has its `description` / `relationships` / `properties` mutated (additive change only)
    - Test 6: `coding.display.json` is NOT touched (display overlay is a separate concern)
  </behavior>
  <action>
    1. Read `coding-ontology.json`. For each class in `{Component, SubComponent, Detail}`, add a top-level `"level": 1` field. (Note: SubComponent/Detail's narrative-level documentation may say "L2"/"L4 leaf" in description text but the OntologyFilter spec from 60-CONTEXT.md D-17 treats them as L1 anchors with L2 children below. The viewer's group logic in `OntologyFilter.tsx:452+` keys off whether a class has L2 children — so the `level` value must be `1` for all three L1 anchors to render as group headers. Verify by reading OntologyFilter group-building logic before editing — adjust this acceptance if the viewer reads a different numeric range.)

    2. For each class in `coding.lower.json`:
       - Read its `extends` value (e.g., `"Component"`)
       - Add top-level fields `"level": 2` and `"parent": "<extends-value>"`
       - Leave all other fields untouched

    3. Per Task 4 deferment, do NOT add explicit `System` / `Project` classes here — that's
       resolved in Task 2 (Path B synthesis) per the approach decision. If during research the
       planner picks Path A instead, this task adds System + Project as top-level classes with
       `level: 0` and no `extends` to `coding-ontology.json`.

    4. Create `lib/km-core/src/api/__tests__/ontology-data-shape.test.ts` — a vitest unit test that
       reads both JSON files (use `import.meta.url` for path resolution) and asserts the 6 behaviors above.

    5. Run `cd lib/km-core && npx vitest run src/api/__tests__/ontology-data-shape.test.ts` and `npm run build`. Build must exit 0 (no schema-validation regressions from
       `lib/km-core/src/validation/ontology.ts` — if the validator rejects the new fields, widen
       the schema in the same task and document why).

    6. Verify via jq from repo root:
       ```
       jq '.classes | {Component, SubComponent, Detail}' .data/ontologies/coding-ontology.json
       jq '.classes | to_entries | map({key, level: .value.level, parent: .value.parent}) | .[:3]' .data/ontologies/coding.lower.json
       ```
       Both must show the new fields populated.
  </action>
  <verify>
    <automated>cd lib/km-core && npx vitest run src/api/__tests__/ontology-data-shape.test.ts && jq -e '.classes.Component.level == 1' /Users/Q284340/Agentic/coding/.data/ontologies/coding-ontology.json && jq -e '[.classes | to_entries[] | .value.level] | unique == [2]' /Users/Q284340/Agentic/coding/.data/ontologies/coding.lower.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.classes.Component.level' .data/ontologies/coding-ontology.json` outputs `1`
    - `jq '.classes.SubComponent.level' .data/ontologies/coding-ontology.json` outputs `1`
    - `jq '.classes.Detail.level' .data/ontologies/coding-ontology.json` outputs `1`
    - `jq '[.classes | to_entries[] | .value.parent] | unique | length' .data/ontologies/coding.lower.json` outputs `3` (Component, SubComponent, Detail — three distinct L1 parents)
    - `jq '[.classes | to_entries[] | .value.level] | unique' .data/ontologies/coding.lower.json` outputs `[2]`
    - `cd lib/km-core && npm run build` exits 0
    - 6-test data-shape vitest exits 0
  </acceptance_criteria>
  <done>
    L1 (coding-ontology.json) and L2 (coding.lower.json) data carry literal `level`/`parent` fields the API handler can pass through.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API handler — synthesize L0 anchors from HIERARCHY_ROOTS + derive parent from extends fallback</name>
  <files>
    lib/km-core/src/api/handlers/ontology.ts
    lib/km-core/src/api/__tests__/ontology-handler.withDisplay-level-parent.test.ts (NEW)
  </files>
  <read_first>
    lib/km-core/src/api/handlers/ontology.ts:124-215 (enriched-path branch — the file containing the `cls as unknown as { level?, parent? }` cast)
    lib/km-core/src/types/hierarchy-roots.ts (HIERARCHY_ROOTS, HIERARCHY_ROOT_CLASS — single source of truth for L0)
    lib/km-core/src/ontology/registry.ts:140-180 (resolveExtends — to confirm `extends` is preserved on resolved class)
    lib/km-core/src/types/ontology.ts (ResolvedClass shape — confirm `extends?: string` field for parent derivation)
    .planning/phases/60-unified-viewer-rendering-ux-integrity/60-04-SUMMARY.md (confirms HIERARCHY_ROOTS export from @fwornle/km-core)
  </read_first>
  <behavior>
    - Test 1 (Path B synthesis): for the coding system, the enriched response includes synthesized entries `{name: "System", level: 0}` and `{name: "Project", level: 0}` even when the registry has no class definition for them
    - Test 2 (synthesis idempotency): if System or Project DOES exist as a registered class (Path A future), no duplicate entry is emitted — handler dedups by `name`
    - Test 3 (parent fallback): for each L2 class without an explicit `parent` field but with `extends: <X>`, the handler emits `parent: <X>` in the response
    - Test 4 (explicit parent precedence): when a class has BOTH explicit `parent: "A"` and `extends: "B"`, explicit `parent` wins
    - Test 5 (level: 1 surfacing): Component/SubComponent/Detail (post-Task-1) carry `level: 1` in the response
    - Test 6 (level: 2 surfacing): every Phase-57 L2 (post-Task-1) carries `level: 2` AND `parent` matching its `extends`
    - Test 7 (display preserved): when an overlay entry exists for a class, the `display` field still appears in the response (no regression to Plan 45-04)
    - Test 8 (BC string-array path): GET /ontology/classes WITHOUT ?withDisplay=true returns `{success: true, data: [...names...]}` byte-identically to pre-60.07 (T-45-04-03 mitigation)
    - Test 9 (non-coding system): enriched-response for a non-coding system (e.g., the test fixture used by existing handler tests) is NOT augmented with HIERARCHY_ROOTS L0 entries unless the system is the coding system OR an opt-in flag is set — confirm scope during research; conservative default is "synthesize only for the configured coding system"
  </behavior>
  <action>
    1. Import `HIERARCHY_ROOTS` and `HIERARCHY_ROOT_CLASS` from `'../../types/hierarchy-roots.js'` (the source) — NOT from `@fwornle/km-core` (this is the SAME km-core, internal import).

    2. In the enriched-path branch (lines ~165-208 of the current file), after building the
       `enriched` array, BEFORE `res.json({...})`:

       a. Build a set of class names already present: `const presentNames = new Set(enriched.map(e => e.name))`

       b. Determine system identity. The handler currently uses `resolveOverlaySystem(req, opts, registry)` for the display overlay. Reuse that result. If `overlaySystem === 'coding'` (or however the constant is identified — confirm during research), proceed with L0 synthesis. Otherwise skip.

       c. For each unique `lockedClass` value in `HIERARCHY_ROOT_CLASS` (currently 'System' and 'Project'):
          - If `lockedClass` is NOT in `presentNames`, append `{name: lockedClass, level: 0}` to the enriched array.
          - This adds System and Project as L0 ungrouped anchors for the coding system without duplicating if upstream data ever adds them as proper classes.

       d. Sort or position L0 entries at the top of the array (handler-side pre-sort is optional — the viewer's L1→L2 group construction handles ordering; safest is to prepend L0 entries to the response array so the viewer renders them in order).

    3. For the parent-derivation fallback: in the `enriched.map(name => ...)` block (lines ~193-208), after reading `extra.level` and `extra.parent`, if `entry.parent === undefined` AND the resolved class has an `extends` field (read via `cls?.extends` — confirm field name in ResolvedClass type), set `entry.parent = cls.extends`. Document the derivation rule with a `// Phase 60.07 D-3` comment.

    4. Create `lib/km-core/src/api/__tests__/ontology-handler.withDisplay-level-parent.test.ts` with the 9 behaviors above using vitest + supertest (mirror conventions in the existing handler test suite — confirm pattern during research).

    5. Run `cd lib/km-core && npm run build` AND `cd lib/km-core && npx vitest run`. All tests must pass.

    6. **Submodule rebuild gate (CLAUDE.md mandatory):**
       Per the Phase 60 dependency model:
       - `lib/km-core/` requires `cd lib/km-core && npm run build` to emit dist consumed by coding-services container via bind-mount.
       - `coding-services` container picks up the new dist via bind-mount; restart the obs-api service so it re-loads km-core: `docker exec coding-services supervisorctl restart obs-api`.
       - No Docker image rebuild needed because km-core/dist is bind-mounted.

    7. Smoke against live API:
       ```
       curl -s "http://localhost:3848/api/v1/ontology/classes?withDisplay=true" | jq '[.data[] | select(.level == 0)] | length'
       ```
       Expected: `2` (System + Project synthesized).
       ```
       curl -s "http://localhost:3848/api/v1/ontology/classes?withDisplay=true" | jq '[.data[] | select(.level == 2)] | length'
       ```
       Expected: `>= 10` (the Phase-57 L2 classes — value may be higher if non-coding lowers are also present in the registry; confirm scope).
  </action>
  <verify>
    <automated>cd lib/km-core && npm run build && npx vitest run src/api/__tests__/ontology-handler.withDisplay-level-parent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "HIERARCHY_ROOTS\\|HIERARCHY_ROOT_CLASS" lib/km-core/src/api/handlers/ontology.ts` returns ≥ 1 match
    - `grep -n "extends" lib/km-core/src/api/handlers/ontology.ts | grep -v "//"` returns a parent-fallback line (cls.extends usage)
    - 9 vitest tests pass against the new handler test file
    - `cd lib/km-core && npm run build` exits 0
    - After service restart: `curl -s 'http://localhost:3848/api/v1/ontology/classes?withDisplay=true' | jq '[.data[] | select(.level == 0)] | length'` outputs `2`
    - After service restart: `curl -s 'http://localhost:3848/api/v1/ontology/classes?withDisplay=true' | jq '[.data[] | select(.level == 1)] | map(.name) | sort'` includes `Component`, `SubComponent`, `Detail`
    - After service restart: `curl -s 'http://localhost:3848/api/v1/ontology/classes?withDisplay=true' | jq '[.data[] | select(.level == 2 and .parent != null)] | length'` outputs `>= 10`
    - BC: `curl -s 'http://localhost:3848/api/v1/ontology/classes' | jq '.data | type'` outputs `"array"` AND `jq '.data[0] | type'` outputs `"string"` (string-array BC preserved)
  </acceptance_criteria>
  <done>
    API enriched response now carries the L0/L1/L2 hierarchy for the coding system; BC string-array path unchanged; viewer can build L1→L2 groups.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator re-runs Phase 60 SC#5 visual smoke + updates 60-VERIFICATION.md status</name>
  <what-built>
    Tasks 1 + 2 produced: (a) data fix on coding-ontology.json + coding.lower.json carrying explicit
    level/parent; (b) handler emits L0 anchors via HIERARCHY_ROOTS synthesis and parent fallback
    from extends. The API smoke gates in Task 2 confirm the enriched response shape is correct.
    Task 3 confirms the viewer USES the new metadata as expected.
  </what-built>
  <how-to-verify>
    **Pre-flight — viewer connectivity:**

    The Phase 60 verification smoke found that `fetch("/api/v1/ontology/classes")` from inside the
    viewer at `localhost:5173` returns `Content-Type: text/html` (Vite SPA fall-through). Before
    re-running the SC#5 smoke, confirm the viewer is actually talking to the API:

    ```
    gsd-browser navigate http://localhost:5173/viewer/coding
    gsd-browser eval 'fetch("/api/v1/ontology/classes?withDisplay=true").then(r => r.headers.get("content-type"))'
    ```

    If the result is `text/html`, the viewer apiClient is not reaching port 3848 from the Vite dev
    server. Two options:

    - Configure a Vite proxy entry routing `/api/v1` → `localhost:3848` (out-of-scope for this
      plan; file a small follow-up todo if hit).
    - Confirm the production path (apiClient base URL via `VITE_API_BASE` env or
      configured-in-coding-services-container) works and run the smoke against the
      production-like URL (e.g. via the dashboard's iframe or the deployed unified-viewer dist).

    Document whichever path the operator picks in the SUMMARY.

    **SC#5 re-smoke:**

    ```
    gsd-browser navigate http://localhost:5173/viewer/coding
    ```

    1. Expand the Ontology Class section in the FilterRail.

    2. `gsd-browser eval` to verify L0 / L1 / L2 are rendered:
       ```
       gsd-browser eval '(() => {
         const section = document.querySelector("[data-testid=\"filter-ontology-section\"]");
         if (!section) return "no-section";
         const labels = Array.from(section.querySelectorAll("label"));
         return {
           labelCount: labels.length,
           classes: labels.map(l => l.textContent.replace(/\\s+/g," ").trim().slice(0, 50)),
         };
       })()'
       ```
       Expected `labelCount` ≥ ~15 (2 L0 anchors + 3 L1 group headers + 10 L2 children + Insight/Digest flat rows that were already showing).

    3. Verify L0 anchors visible:
       ```
       gsd-browser eval 'document.body.innerText.includes("System") && document.body.innerText.includes("Project")'
       ```
       Expected: `true`.

    4. Verify Phase-57 L2 children render under Component:
       ```
       gsd-browser eval '["LiveLoggingSystem","ConstraintMonitor","KnowledgeManagement","BatchSemanticAnalysis","RapidLlmProxy","DockerizedServices"].every(n => document.body.innerText.includes(n))'
       ```
       Expected: `true`.

    5. Verify Detail group has its 3 Online L2 children:
       ```
       gsd-browser eval '["OnlineObservation","OnlineDigest","OnlineInsight"].every(n => document.body.innerText.includes(n))'
       ```
       Expected: `true`.

    6. Verify SubComponent group has EtmDaemon:
       ```
       gsd-browser eval 'document.body.innerText.includes("EtmDaemon")'
       ```
       Expected: `true`.

    7. Click an L1 disclosure triangle (e.g., Component) — verify the L2 children hide but
       `selectedOntologyClasses` is UNCHANGED (collapse is UI-only):
       ```
       gsd-browser eval '(() => {
         const componentTriangle = Array.from(document.querySelectorAll("button[aria-expanded]")).find(b => b.textContent.includes("Component"));
         const beforeState = window.__VIEWER_STORE__?.getState?.()?.selectedOntologyClasses ?? "no-store";
         componentTriangle?.click();
         return { beforeState, afterStateCheck: "verify selectedOntologyClasses unchanged via re-eval" };
       })()'
       ```

    8. Verify `[all]` / `[none]` link-buttons present per L1 group:
       ```
       gsd-browser eval 'document.querySelectorAll("[data-testid^=\"ontology-group\"] button").length > 0'
       ```
       (Adjust selector to actual data-testid used by 60-05 OntologyFilter — confirm during execution.)

    9. Confirm `Typed Views` is STILL absent:
       ```
       gsd-browser eval 'document.body.innerText.includes("Typed Views")'
       ```
       Expected: `false`.

    10. Capture new screenshots replacing the PARTIAL ones:
       ```
       gsd-browser screenshot --output .planning/phases/60-unified-viewer-rendering-ux-integrity/screenshots/sc5-ontology-filter.png --format png
       ```
       (Optionally also capture sc5-l1-collapsed.png with one L1 group collapsed.)

    11. Update 60-VERIFICATION.md:
        - SC#5 outcome: PARTIAL → PASS
        - SC#5 § Gap section: replace with § Pass evidence summarizing the new screenshots and eval
          outcomes
        - Frontmatter: `status: gaps_found` → `status: passed`, `sc_passed: 4` → `sc_passed: 5`,
          `sc_partial: 1` → `sc_partial: 0`, `gaps_open: 2` → `gaps_open: 1` (the VOKB W-1
          inconclusive remains — that's pre-existing okb data warmth, NOT a Phase-60 gap closed
          here).

    12. Commit:
        ```
        gsd-sdk query commit "test(60-07): SC#5 PASS after L0/L1/L2 hierarchy lights up" --files .planning/phases/60-unified-viewer-rendering-ux-integrity/60-VERIFICATION.md .planning/phases/60-unified-viewer-rendering-ux-integrity/screenshots/sc5-ontology-filter.png .planning/phases/60-unified-viewer-rendering-ux-integrity/screenshots/sc5-l1-collapsed.png
        ```
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 SCs of the Phase 60 verification now read PASS (was: 4 PASS + 1 PARTIAL on SC#5) and the L0/L1/L2 hierarchy renders correctly per the eval checks above. If anything fails, paste the failing eval output + screenshot path so the gap can be triaged.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `coding.lower.json` → km-core ontology registry | trusted load path; adding `level`/`parent` is additive and validated by the registry's schema validator (lib/km-core/src/validation/ontology.ts) |
| HIERARCHY_ROOTS synthesis in handler → API response | trusted synthesis; closed-set vocabulary from Plan 60-04 — no user input flows into the synthesized entries |
| viewer fetch → enriched response | the viewer trusts the API response shape; if the handler emits a malformed entry the viewer falls back to flat rows (defensive in OntologyFilter.tsx:480+) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-07-01 | Tampering | data fix on coding-ontology.json bypassing ontology validation | mitigate | Task 1's vitest test runs the registry validator over the modified file; build fails if schema rejects |
| T-60-07-02 | Information Disclosure | enriched API response leaks an L0 anchor name not in HIERARCHY_ROOTS | accept | HIERARCHY_ROOTS is the closed-set source of truth from Plan 60-04 — Coding/DynArch/Timeline/Normalisa + CollectiveKnowledge. None are sensitive. |
| T-60-07-03 | Denial of Service | handler synthesis loop is unbounded | accept | HIERARCHY_ROOTS is a 5-tuple; loop is O(5). No DoS surface. |
| T-60-07-04 | Repudiation | viewer renders L1→L2 hierarchy without audit trail of source files | accept | OntologyRegistry already exposes provenance; not a regression. |
</threat_model>

<verification>
- `jq` on coding-ontology.json + coding.lower.json shows new level/parent fields
- `curl http://localhost:3848/api/v1/ontology/classes?withDisplay=true | jq` returns L0 (level=0, name=System|Project), L1 (level=1, name=Component|SubComponent|Detail), L2 (level=2, parent=<L1>) entries
- BC: `curl http://localhost:3848/api/v1/ontology/classes` still returns flat string array
- `cd lib/km-core && npm run build` succeeds; all tests pass
- gsd-browser smoke on /viewer/coding shows L1→L2 hierarchy rendered
- 60-VERIFICATION.md status flipped passed; sc_passed:5
</verification>

<success_criteria>
- LOWERONTO-03 SC#5 mechanics fully lit up — operator sees L0 anchors, L1 groups with L2 children, [all]/[none] link-buttons on each group, Typed Views absent
- HIERARCHY_ROOTS remains the single source of truth for closed-set system + project anchors (Plan 60-04 invariant preserved)
- BC string-array path unchanged (T-45-04-03 mitigation preserved)
- No new ontology schema additions beyond `level` (number) + `parent` (string) on classes that already had `extends`
- Phase 60 closes with `passed` status; no open gaps from this phase
</success_criteria>

<output>
Create `.planning/phases/60-unified-viewer-rendering-ux-integrity/60-07-SUMMARY.md` when done
</output>
