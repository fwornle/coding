---
phase: 75-measurement-attribution-accuracy-observation-linkage
plan: 06
type: execute
wave: 3
depends_on: ["75-02", "75-04"]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/runs-table.tsx
  - integrations/system-health-dashboard/src/components/performance/timeline.tsx
  - integrations/system-health-dashboard/src/components/performance/score-drawer.tsx
autonomous: false
requirements: [ATTR-02]
user_setup: []

must_haves:
  truths:
    - "The runs table renders two model columns: canonical/chat model and background-service models, consistently with the score drawer and timeline"
    - "A Run with no canonical capture shows the 'unmeasured' sentinel; an empty background column shows the em-dash"
    - "All three surfaces READ the persisted Run.metadata fields (canonical_model/background_models) — no per-surface recompute"
  artifacts:
    - path: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      provides: "Run interface extended with canonical_model/canonical_agent/background_models (ATTR-02)"
      contains: "canonical_model"
    - path: "integrations/system-health-dashboard/src/components/performance/runs-table.tsx"
      provides: "two model columns + data-testid hooks + unmeasured sentinel"
      contains: "run-canonical-model"
  key_links:
    - from: "integrations/system-health-dashboard/src/components/performance/runs-table.tsx"
      to: "Run.canonical_model / Run.background_models"
      via: "render persisted fields (read-only)"
      pattern: "canonical_model"
    - from: "integrations/system-health-dashboard/src/components/performance/score-drawer.tsx"
      to: "Run.canonical_model"
      via: "drawer header reads persisted canonical"
      pattern: "canonical_model"
---

<objective>
Render the ATTR-02 two-column model display across all three Performance surfaces (runs table, score drawer, timeline) by READING the persisted `Run.metadata` fields (`canonical_model`, `canonical_agent`, `background_models`) — never recomputing per surface (per-surface recompute is exactly how finding B's dominant-vs-first-row divergence arose). Empty canonical shows the D-05 "unmeasured" sentinel, never a dominant fallback.

Purpose: Show the operator the honest chat model (e.g. Opus) in one column and the concurrent background-service models in another, consistently everywhere.
Output: Extended `performanceSlice.ts` Run interface + two-column render in `runs-table.tsx` (+ score-drawer/timeline reads). Turns tests/e2e/performance/canonical-columns.spec.ts green. Dashboard is bind-mounted: rebuild + frontend restart, no Docker rebuild.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-RESEARCH.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md
@integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
@integrations/system-health-dashboard/src/components/performance/runs-table.tsx
@integrations/system-health-dashboard/src/components/performance/timeline.tsx
@integrations/system-health-dashboard/src/components/performance/score-drawer.tsx

<interfaces>
performanceSlice.ts Run interface (:42-79) — extend with:
  canonical_model?: string | null
  canonical_agent?: string | null
  background_models?: { model: string; process: string; total_tokens: number }[]

runs-table.tsx:
  :114  "Model" TableHead
  :159-161  model cell (replace with canonical column + add a background column)
  :41-43  num()/em-dash convention to reuse for the empty-background sentinel

timeline.tsx: :77-102 TurnLabel renders row.process + row.model (row-level fg/bg already); read Run-level canonical for any header/summary.
score-drawer.tsx: :51-58 rationaleFor / best-effort run?.… reads; show canonical model in the drawer header.

D-05 empty-canonical sentinel: "unmeasured" (canonical column) / em-dash (empty background column).
e2e contract (Plan 01 spec): data-testid="run-canonical-model", data-testid="run-background-models".

Build/deploy (CLAUDE.md): bind-mounted dashboard — after .tsx edits run `npm run build` in integrations/system-health-dashboard then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`. NO docker-compose build. E2E-verify with gsd-browser on localhost:3032.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: extend Run interface + two-column render across all three surfaces</name>
  <files>integrations/system-health-dashboard/src/store/slices/performanceSlice.ts, integrations/system-health-dashboard/src/components/performance/runs-table.tsx, integrations/system-health-dashboard/src/components/performance/timeline.tsx, integrations/system-health-dashboard/src/components/performance/score-drawer.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (:42-79 Run interface, :44-46 optional-nullable style, :677-680 facet keying on run.model)
    - integrations/system-health-dashboard/src/components/performance/runs-table.tsx (:114 Model header, :159-161 model cell, :41-43 num()/em-dash)
    - integrations/system-health-dashboard/src/components/performance/timeline.tsx (:77-102 TurnLabel, :25-36 fmtTime/tokens null-guards)
    - integrations/system-health-dashboard/src/components/performance/score-drawer.tsx (:51-58 run?.… reads)
    - tests/e2e/performance/canonical-columns.spec.ts (the e2e contract from Plan 01)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md (dashboard section ~201-234)
  </read_first>
  <action>
    Extend the performanceSlice.ts Run interface with the three optional-nullable fields per the interfaces block, mirroring the existing optional field style (:44-46). In runs-table.tsx: replace the single run.model cell (:159-161) with a canonical-model cell that renders run.canonical_model (font-mono) OR the "unmeasured" italic sentinel when null/empty (D-05 — NEVER fall back to a dominant value), tagged data-testid="run-canonical-model"; ADD a second cell for background-service models rendering run.background_models mapped to model names joined by comma OR the em-dash sentinel when empty, tagged data-testid="run-background-models". Add the matching second TableHead next to the existing "Model" header (:114). In score-drawer.tsx, show run.canonical_model (with the same unmeasured sentinel) in the drawer header via the existing best-effort run?.… idiom. In timeline.tsx, read the Run-level canonical_model/background_models for the header/summary while keeping the per-turn row.process/row.model rendering and the fmtTime/tokens null-guards. All three surfaces READ the persisted fields — do NOT recompute canonical anywhere. Reuse the existing em-dash/null-not-zero convention.
  </action>
  <verify>
    <automated>grep -n "canonical_model" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts integrations/system-health-dashboard/src/components/performance/runs-table.tsx integrations/system-health-dashboard/src/components/performance/score-drawer.tsx; grep -n "run-canonical-model" integrations/system-health-dashboard/src/components/performance/runs-table.tsx; grep -n "run-background-models" integrations/system-health-dashboard/src/components/performance/runs-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - canonical_model appears in the Run interface (performanceSlice.ts) and in runs-table.tsx + score-drawer.tsx (grep gate above returns matches in all three).
    - runs-table.tsx exposes data-testid="run-canonical-model" and data-testid="run-background-models".
    - The "unmeasured" sentinel renders when canonical_model is null/empty; grep for "unmeasured" in runs-table.tsx matches; NO dominant fallback (grep for byAgentModel or dominant in the performance/*.tsx components returns nothing).
    - No per-surface recompute: the three components only READ run.canonical_model/run.background_models.
  </acceptance_criteria>
  <done>Run interface extended; runs table shows two model columns with testids + unmeasured/em-dash sentinels; score-drawer + timeline read the persisted canonical; no recompute, no dominant fallback.</done>
</task>

<task type="auto">
  <name>Task 2: build the bind-mounted dashboard + restart the frontend</name>
  <files>integrations/system-health-dashboard (build artifact only)</files>
  <read_first>
    - CLAUDE.md (bind-mount rebuild rule: npm run build + supervisorctl restart web-services:health-dashboard-frontend; NO docker-compose build)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-RESEARCH.md (Runtime State Inventory — build artifacts row)
  </read_first>
  <action>
    Build the dashboard frontend bundle and restart the bind-mounted frontend service so the .tsx changes take effect: run npm run build in integrations/system-health-dashboard, then docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend. Do NOT run docker-compose build (bind-mounted). This is config/wiring only — no source edits in this task.
  </action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npm run build in integrations/system-health-dashboard exits 0 (bundle compiles with the new fields/columns).
    - The frontend service is restarted via supervisorctl (operator confirms in the checkpoint below).
  </acceptance_criteria>
  <done>Dashboard bundle rebuilt clean and the bind-mounted frontend restarted; new columns served on localhost:3032.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify two-column model display across runs table, score drawer, timeline</name>
  <action>Operator checkpoint: visually verify the two-column model display across all three Performance surfaces and run the e2e spec (see how-to-verify below). No code is written in this task.</action>
  <verify>
    <human-check>Two model columns render consistently across runs table, score drawer, and timeline; legacy Runs show the unmeasured sentinel; canonical-columns.spec.ts green.</human-check>
  </verify>
  <what-built>The Performance runs table now shows two model columns — canonical chat model and background-service models — and the score drawer + timeline read the same persisted Run.metadata fields. Legacy Runs (no foreground capture) show the "unmeasured" sentinel; empty background shows an em-dash.</what-built>
  <how-to-verify>
    1. gsd-browser navigate http://localhost:3032 and open the Performance tab.
    2. Confirm the runs table has TWO model columns: a canonical/chat-model column and a background-service-models column.
    3. Confirm a legacy Run (pre-Phase-75, no canonical capture) shows "unmeasured" in the canonical column, not a haiku/sonnet dominant value (the finding-B regression).
    4. Open a Run's score drawer — confirm the same canonical model shows in the header (consistent with the table row).
    5. Open the timeline — confirm Run-level canonical/background reads are consistent with the table.
    6. Run the e2e spec: npx playwright test tests/e2e/performance/canonical-columns.spec.ts — confirm green.
  </how-to-verify>
  <resume-signal>Type "approved" if the two columns render consistently across all three surfaces and the unmeasured sentinel shows for legacy Runs; otherwise describe the inconsistency.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vkb-server /api/experiments/runs → dashboard | Run.metadata fields are read-only render inputs; no write surface here. |
| operator browser → dashboard | Local-only dashboard; no auth surface introduced by this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-61 | Tampering | per-surface recompute reintroduces divergence | mitigate | All three surfaces READ persisted Run.metadata only; grep-gate forbids byAgentModel/dominant in performance components. |
| T-75-62 | Information disclosure | model names in UI | accept | Model/process names are non-sensitive operational metadata; local dashboard. |
| T-75-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH audit N/A); npm run build uses existing deps only. |
</threat_model>

<verification>
- grep gate (Task 1) confirms canonical_model + testids across the three surfaces; no dominant fallback.
- npm run build exits 0 (Task 2).
- gsd-browser + npx playwright test tests/e2e/performance/canonical-columns.spec.ts green (checkpoint).
</verification>

<success_criteria>
ATTR-02 display: the canonical chat model and background-service models render as two columns consistently across runs table, score drawer, and timeline; legacy Runs show the "unmeasured" sentinel; no per-surface recompute and no dominant fallback.
</success_criteria>

<output>
Create `.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-06-SUMMARY.md` when done
</output>
