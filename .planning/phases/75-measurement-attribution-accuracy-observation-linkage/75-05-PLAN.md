---
phase: 75-measurement-attribution-accuracy-observation-linkage
plan: 05
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - scripts/enhanced-transcript-monitor.js
  - src/live-logging/ObservationWriter.js
autonomous: false
requirements: [OBS-01, OBS-02]
user_setup: []

must_haves:
  truths:
    - "ETM fires observations mid-prompt-set on AskUserQuestion decisions and on significant tool-activity batches, not only at prompt-set start"
    - "Each observation's created_at is the batch's real event time (last-message timestamp), not the prompt-set start (T0)"
    - "Each observation carries metadata.task_id resolved via resolveLiveTaskIdSafe (same reader as the token path)"
    - "Re-capture does not re-emit the same exchange — dedup advances a per-transcript lastFiredExchangeUuid cursor; OBS dedup key = (task_id, batch-last-message-uuid)"
  artifacts:
    - path: "scripts/enhanced-transcript-monitor.js"
      provides: "per-decision/per-batch fire boundary + true event-time + task_id + cursor dedup (OBS-02)"
      contains: "lastFiredExchangeUuid"
    - path: "src/live-logging/ObservationWriter.js"
      provides: "task_id in observation metadata (OBS-01); created_at already honors earliest message"
      contains: "task_id"
  key_links:
    - from: "scripts/enhanced-transcript-monitor.js"
      to: "lib/lsl/token/task-id.mjs"
      via: "resolveLiveTaskIdSafe() at fire time"
      pattern: "resolveLiveTaskIdSafe"
    - from: "scripts/enhanced-transcript-monitor.js"
      to: "src/live-logging/ObservationWriter.js"
      via: "metadata.task_id + batch-local messages passed to processMessages/writeObservation"
      pattern: "task_id"
---

<objective>
Fix the observation under-capture and timestamp-collapse (finding D / OBS-02) and link observations to the Run (OBS-01). ETM today snapshots a typed-prompt prompt-set once and stamps it at the first exchange (T0), so a 9-hour GSD session with one typed prompt yields ~8 observations all dated T0. Refactor ETM's fire boundary to also fire mid-set on (a) each AskUserQuestion decision and (b) significant tool-activity batches, passing ONLY the batch's messages so ObservationWriter's earliest-createdAt resolves to the batch's REAL time. Stamp each observation's metadata with the active task_id via the same single-span reader the token path uses.

Purpose: Turn the e0af5b8b acceptance case (decisions 05:30-06:03Z) into observations dated ~05:30-06:03Z that are queryable per Run.
Output: Modified `enhanced-transcript-monitor.js` (fire boundary + dedup + task_id) and `ObservationWriter.js` (task_id in metadata). Turns tests/live-logging/ETM-recapture.test.js green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-RESEARCH.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md
@src/live-logging/ObservationWriter.js
@scripts/enhanced-transcript-monitor.js

<interfaces>
ETM (enhanced-transcript-monitor.js):
  :1375  prompt-set boundary — `if (exchange.isUserPrompt)` starts a new set (the bug: ONLY a typed prompt starts/ends a set)
  :768   _firePromptSetObservation(...) — the fire path; created_at sourced from first exchange
  :799/:835  createdAt: new Date(exchange.timestamp).toISOString()  // already per-message — keep
  :774-788  _firedPromptKeys dedup (key=count|lastExchangeId, 15s window)
  :522-523/:594-595/:633-634  code already prefers lastMessageUuid over id for cursor advance
  :865-870  metadata object built and passed to ObservationWriter

ObservationWriter (src/live-logging/ObservationWriter.js):
  :1069  writeObservation
  :1126  const messageTimestamp = messages.find(m => m.createdAt)?.createdAt;  // earliest — DO NOT change
  :1147  metadata serialized into entity
  :1163  entity.metadata = { ...entity.metadata, source:'auto', ... }  // extend with task_id
  :1116-1121 _redact runs before persist; :1096-1111 content-hash + 4h semantic dedup; :1164 skipOntologyCheck:true

Shared reader:
  resolveLiveTaskIdSafe(overrideDataDir)  // lib/lsl/token/task-id.mjs:64 — best-effort, returns ''/never throws

Discretion thresholds (RESEARCH A4): fire on a "significant tool-activity batch" when ≥ 8 tool_use blocks OR ≥ 10 min elapsed since the last fire within a question-less stretch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ETM mid-set fire boundary + true event-time + cursor dedup (OBS-02)</name>
  <files>scripts/enhanced-transcript-monitor.js</files>
  <read_first>
    - scripts/enhanced-transcript-monitor.js (:768-870 _firePromptSetObservation, :1375 boundary, :774-788 _firedPromptKeys dedup, :799/:835 createdAt, :522-523/:594-595 lastMessageUuid cursor, :33-34 no-console-log header)
    - tests/live-logging/ETM-recapture.test.js (the RED contract from Plan 01)
    - tests/live-logging/_fixtures/e0af5b8b-recapture.jsonl (Plan 01 fixture)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md (ETM section ~180-197; Pitfall 3/4)
  </read_first>
  <action>
    Add mid-set fire points to ETM WITHOUT removing the existing typed-prompt-set flush. Introduce two new fire triggers while accumulating exchanges within a set: (a) when an AskUserQuestion tool_use + tool_result pair is observed (decision boundary), fire an observation for the batch of exchanges accumulated since the last fire; (b) when accumulated tool_use blocks since the last fire reach ≥ 8 OR ≥ 10 min elapsed since the last fire within a question-less stretch, fire a batch. Each fire passes ONLY the batch's messages to _firePromptSetObservation so created_at resolves to the batch's last-message timestamp (the per-message createdAt stamping at :799/:835 is already correct — keep it). Maintain a per-transcript `lastFiredExchangeUuid` cursor (use the batch's LAST message uuid, mirroring the existing lastMessageUuid preference at :522-523) so the next batch starts AFTER the cursor and earlier exchanges are never re-emitted. Change the OBS fire-dedup key from `count|lastExchangeId` to `(task_id, batch-last-message-uuid)` per Pitfall 4. Output via process.stderr.write only (no-console-log).
  </action>
  <verify>
    <automated>npx jest tests/live-logging/ETM-recapture.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lastFiredExchangeUuid" scripts/enhanced-transcript-monitor.js` shows the per-transcript cursor.
    - `grep -nE "AskUserQuestion" scripts/enhanced-transcript-monitor.js` shows the decision-boundary fire trigger.
    - `grep -nE ">= ?8|>=8|tool_use.*8|600000|10 ?\\* ?60" scripts/enhanced-transcript-monitor.js` shows the ≥8-tool_use OR ≥10-min batch threshold.
    - ETM-recapture.test.js asserts pass: ≥2 observations, ≥1 dated in 05:30-06:03Z (NOT 21:00:43Z), no duplicate (task_id, batch-last-message-uuid) keys.
    - The existing typed-prompt-set flush is preserved (not deleted) — `grep -n "isUserPrompt" scripts/enhanced-transcript-monitor.js` still present.
  </acceptance_criteria>
  <done>ETM fires mid-set on decisions + tool-batches, stamps each observation at the batch's real time, dedups via lastFiredExchangeUuid cursor; ETM-recapture.test.js timestamp + dedup assertions green.</done>
</task>

<task type="auto">
  <name>Task 2: task_id on observation metadata (OBS-01)</name>
  <files>src/live-logging/ObservationWriter.js, scripts/enhanced-transcript-monitor.js</files>
  <read_first>
    - src/live-logging/ObservationWriter.js (:1069 writeObservation, :1126 created_at, :1147 metadata serialize, :1163 metadata spread, :1116-1121 redact, :1164 skipOntologyCheck)
    - scripts/enhanced-transcript-monitor.js (:865-870 metadata object passed to ObservationWriter — from Task 1)
    - lib/lsl/token/task-id.mjs (:64 resolveLiveTaskIdSafe)
    - tests/live-logging/ETM-recapture.test.js (task_id assertions)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md (ObservationWriter section ~159-176; D-09)
  </read_first>
  <action>
    Resolve the active task_id at the ETM fire site (preferred — keeps the writer proxy-free): call `resolveLiveTaskIdSafe()` once per fire and put `task_id` on the metadata object built at enhanced-transcript-monitor.js:865-870. In ObservationWriter.js, ensure that metadata.task_id flows into the persisted entity: extend the existing `entity.metadata = { ...entity.metadata, source:'auto', ... }` spread at :1163 to include `task_id: metadata.task_id ?? (await resolveLiveTaskIdSafe())` as a best-effort fallback (so direct callers without ETM still get linkage). Do NOT change the created_at logic at :1126 (it already honors the earliest message). Preserve _redact-before-persist (:1116-1121), content-hash + semantic dedup (:1096-1111), and skipOntologyCheck:true (:1164). task_id is metadata only — no ontology edit. The same reader the token path uses (resolveLiveTaskIdSafe) guarantees token attribution and observation linkage agree on the active task_id (D-09).
  </action>
  <verify>
    <automated>npx jest tests/live-logging/ETM-recapture.test.js tests/live-logging/ObservationWriter.pre-llm-dedup.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resolveLiveTaskIdSafe" scripts/enhanced-transcript-monitor.js` shows it called at the fire site.
    - `grep -n "task_id" src/live-logging/ObservationWriter.js` shows task_id added to the metadata spread at the entity build.
    - ETM-recapture.test.js: every emitted observation carries a non-empty metadata.task_id.
    - Existing ObservationWriter dedup/redact tests stay green (no regression): ObservationWriter.pre-llm-dedup.test.js passes.
    - created_at logic unchanged: `grep -n "messages.find(m => m.createdAt)" src/live-logging/ObservationWriter.js` still present and untouched.
  </acceptance_criteria>
  <done>Observations carry the active task_id (resolved via the shared single-span reader) and are queryable per Run; redaction/dedup/created_at behavior preserved; no ontology change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live ETM re-capture (multiple, time-spread, task_id-linked observations)</name>
  <action>Operator checkpoint: kickstart the launchd ETM daemon to pick up the source change and verify live re-capture behavior (see how-to-verify below). No code is written in this task.</action>
  <verify>
    <human-check>Observations for the active task_id are multiple, time-spread across the session, and each carries task_id (per how-to-verify).</human-check>
  </verify>
  <what-built>ETM now fires observations mid-prompt-set on AskUserQuestion decisions + tool-activity batches, stamps each at its real event time, and links them to the active Run via task_id. The launchd ETM daemon (com.coding.etm) must be kickstarted to pick up the source change (it is launchd-managed; no re-registration since args are unchanged).</what-built>
  <how-to-verify>
    1. Kickstart ETM: `launchctl kickstart -k gui/$(id -u)/com.coding.etm` and confirm it is running: `launchctl list | grep com.coding.etm`.
    2. Tail `.logs/etm.log` and confirm no [STALL-DETECT] / crash after kickstart.
    3. During or after a real multi-decision session, query observations for the active task_id and confirm: multiple observations exist (not one), their event times span the session (not all at T0), and each carries the task_id.
    4. (If a fresh session is impractical) re-run the e0af5b8b fixture path and confirm observations dated ~05:30-06:03Z.
  </how-to-verify>
  <resume-signal>Type "approved" if observations are multiple, time-spread, and task_id-linked; otherwise describe what's still collapsed/missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript stream → ETM | Local session transcripts; per-message content may contain secrets/PII. |
| ETM → ObservationWriter → km-core | Observation content persisted after redaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-51 | Information disclosure | secrets/PII in observation content | mitigate | _redact (ConfigurableRedactor) runs before persist (existing, unchanged) — re-capture passes content through the same redactor. |
| T-75-52 | Denial of service | re-capture O(n²) re-emission inflation | mitigate | per-transcript lastFiredExchangeUuid cursor + content-hash/semantic dedup + (task_id, batch-last-message-uuid) key (Pitfall 4). |
| T-75-53 | Tampering | task_id spoof on observation | accept | task_id resolved from the single local span reader; local-only data, low risk. |
| T-75-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx jest tests/live-logging/ETM-recapture.test.js tests/live-logging/ObservationWriter.pre-llm-dedup.test.js` → green.
- Acceptance greps pass (cursor, decision trigger, batch threshold, task_id, created_at untouched).
- Human-verify checkpoint: ETM kickstarted, observations multiple/time-spread/task_id-linked.
</verification>

<success_criteria>
OBS-02: a session whose only typed prompt is at T0 yields observations dated at their real event times (decisions at T0+n → observations ~T0+n), not collapsed to T0. OBS-01: those observations carry the active task_id and are queryable per Run. No re-emission of the same exchange; redaction preserved.
</success_criteria>

<output>
Create `.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-05-SUMMARY.md` when done
</output>
