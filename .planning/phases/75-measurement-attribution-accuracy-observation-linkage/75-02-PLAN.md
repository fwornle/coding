---
phase: 75-measurement-attribution-accuracy-observation-linkage
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - lib/experiments/token-aggregate.mjs
  - lib/experiments/run-write.mjs
autonomous: true
requirements: [ATTR-01, ATTR-02]
user_setup: []

must_haves:
  truths:
    - "byAgentModel rows carry user_hash + process so lineage can be classified at aggregation time (no proxy-DB schema change)"
    - "isForegroundGroup classifies adapter rows (cladpt/copadt, non-denylisted process) as foreground and everything else as background"
    - "writeRun persists canonical_model, canonical_agent, and background_models[] onto Run.metadata"
    - "An empty canonical is persisted as null (never coerced to a dominant-by-count fallback)"
  artifacts:
    - path: "lib/experiments/token-aggregate.mjs"
      provides: "fg/bg lineage classifier + user_hash/process in the breakdown (ATTR-01)"
      contains: "isForegroundGroup"
    - path: "lib/experiments/run-write.mjs"
      provides: "canonical_model/canonical_agent/background_models[] in Run.metadata (ATTR-02/D-06)"
      contains: "canonical_model"
  key_links:
    - from: "lib/experiments/run-write.mjs"
      to: "Run.metadata"
      via: "putEntity metadata block"
      pattern: "canonical_model"
    - from: "lib/experiments/query.mjs"
      to: "Run.metadata"
      via: "readRuns ...meta spread (verify-only, no change)"
      pattern: "\\.\\.\\.meta|\\.\\.\\.e\\.metadata"
---

<objective>
Implement the aggregation-layer fg/bg lineage classifier (ATTR-01) and persist the canonical + background-model fields on the Run entity (ATTR-02/D-06). Per the Discretion call locked in RESEARCH: derive fg/bg AT AGGREGATION TIME from `user_hash` + a background-process denylist — NO new column on the proxy-owned token-usage.db. Persist the computed result once on Run.metadata so all three dashboard surfaces read it without recomputing.

Purpose: Stop daemon traffic from being attributed to a foreground task, and give the Run one canonical model source-of-truth.
Output: Extended `token-aggregate.mjs` (classifier + enriched breakdown) and `run-write.mjs` (3 metadata fields). Turns the lineage/canonical assertions in tests/experiments/canonical-attribution.test.mjs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-RESEARCH.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md
@lib/experiments/token-aggregate.mjs
@lib/experiments/run-write.mjs
@lib/experiments/query.mjs

<interfaces>
Current (token-aggregate.mjs):
  export function aggregateByTaskId(taskId, dbPathOverride);
  // returns { totals, byAgentModel: Array<{agent,model,provider,granularity_tier,total_tokens,calls}> }
  // open is { readonly:true, fileMustExist:true }; task_id bound as `?` (NEVER interpolate)
  // GROUP BY agent, model, provider, granularity_tier ORDER BY total_tokens DESC

Current (run-write.mjs writeRun):
  putEntity({ entityType:'Run', metadata:{ domain:'experiment', task_id, ...8 tags... } })
  // idempotent: iterate({entityType:'Run'}) scan on metadata.task_id; null is meaningful (never ?? 0)

Denylist + foreground signal (RESEARCH §Code Examples — use VERBATIM):
  BACKGROUND_PROCESS_RE = /^(consolidator-|health-coordinator$|observation-writer$|backfill$|reproject-|route-judge$)/
  FOREGROUND_USER_HASHES = new Set(['cladpt','copadt'])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: aggregation-time fg/bg lineage classifier (ATTR-01)</name>
  <read_first>
    - lib/experiments/token-aggregate.mjs (full — :62-100 aggregateByTaskId, :76 readonly open, :88-94 byAgentModel SELECT/GROUP BY, :42-50 zeroTotals fallback)
    - tests/experiments/canonical-attribution.test.mjs (the RED contract from Plan 01)
    - tests/experiments/token-aggregate.test.mjs (existing tests that must STAY green)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md (token-aggregate section ~124-137)
  </read_first>
  <behavior>
    - isForegroundGroup({user_hash:'cladpt', process:'gsd-execute'}) === true
    - isForegroundGroup({user_hash:'c197ef', process:'consolidator-mentions'}) === false
    - isForegroundGroup({user_hash:'cladpt', process:'observation-writer'}) === false (denylist wins even with adapter hash)
    - aggregateByTaskId byAgentModel rows now expose user_hash and process
    - existing token-aggregate.test.mjs assertions remain green (totals, zero-fallback, bound-param)
  </behavior>
  <action>
    Add exported `isForegroundGroup(group)` to token-aggregate.mjs implementing: `FOREGROUND_USER_HASHES.has(group.user_hash) && !BACKGROUND_PROCESS_RE.test(group.process || '')` with the regex + Set defined VERBATIM per the interfaces block (these denylist values are confirmed against real token_usage data — do not re-derive). Add `user_hash` and `process` to the byAgentModel SELECT column list and to the GROUP BY clause (append, keep `ORDER BY total_tokens DESC` and the zero-totals COALESCE fallback). Preserve the `{ readonly:true, fileMustExist:true }` open and bind every task_id as a `?` placeholder (never interpolate — T-71-03-01). Do NOT add any column to the DB; this is read-side derivation only (D-02, survives re-aggregation).
  </action>
  <verify>
    <automated>node --test tests/experiments/canonical-attribution.test.mjs tests/experiments/token-aggregate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isForegroundGroup" lib/experiments/token-aggregate.mjs` shows an `export`.
    - `grep -nE "user_hash.*process|process.*user_hash" lib/experiments/token-aggregate.mjs` shows them added to the SELECT/GROUP BY.
    - The lineage assertions in canonical-attribution.test.mjs (fg vs bg segregation) pass; existing token-aggregate.test.mjs stays green.
    - No string-interpolated SQL introduced: `grep -nE "task_id.*\\$\\{|\\+ *taskId" lib/experiments/token-aggregate.mjs` returns nothing.
    - DB open remains readonly: `grep -n "readonly: true" lib/experiments/token-aggregate.mjs` still present.
  </acceptance_criteria>
  <done>isForegroundGroup exported and correct; breakdown carries user_hash+process; readonly/bound-param discipline preserved; both node:test files green for the lineage portion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: persist canonical_model + background_models[] on Run.metadata (ATTR-02/D-06)</name>
  <read_first>
    - lib/experiments/run-write.mjs (full — :88 putEntity Run, :94-121 metadata block, :58-60 null-not-zero, :66-72 idempotent task_id scan)
    - lib/experiments/query.mjs (:106-110 readRuns ...meta spread — verify new fields auto-flow)
    - tests/experiments/canonical-attribution.test.mjs (canonical-persistence assertions)
    - tests/experiments/run-write.test.mjs (existing — must stay green)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md (run-write section ~141-155)
  </read_first>
  <behavior>
    - writeRun(store,{...,tags:{canonical_model:'claude-opus-4-8', canonical_agent:'claude', background_models:[{model:'haiku',process:'consolidator-mentions',total_tokens:9999}]}}) persists those exact values on Run.metadata
    - writeRun with no canonical tags persists canonical_model:null and background_models:[] (never a dominant fallback)
    - re-running writeRun for the same task_id updates the same node (idempotent) and preserves the canonical fields
    - readRuns surfaces canonical_model/canonical_agent/background_models via the ...meta spread (no query.mjs change)
  </behavior>
  <action>
    In writeRun's Run `metadata` block, add three fields alongside the existing 8 tags: `canonical_model: t.canonical_model ?? null`, `canonical_agent: t.canonical_agent ?? null`, `background_models: t.background_models ?? []` (using the same `t`/tags accessor the existing tags use). Preserve null-is-meaningful (NEVER `?? 0`), the strict-path putEntity with synthetic provenance, `domain:'experiment'`, and the idempotent metadata.task_id scan. No ontology edit — these ride in metadata exactly as the six route heuristics already do (A5). Confirm query.mjs `readRuns` `...meta` spread carries the new fields; if it already spreads metadata, make NO change to query.mjs and assert it in the test instead.
  </action>
  <verify>
    <automated>node --test tests/experiments/canonical-attribution.test.mjs tests/experiments/run-write.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "canonical_model\|canonical_agent\|background_models" lib/experiments/run-write.mjs` shows all three in the metadata block.
    - The canonical-persistence assertions in canonical-attribution.test.mjs pass: explicit values persisted; absent → null/[]; idempotent re-write preserves them.
    - `grep -nE "canonical_model.*\\?\\? 0|background_models.*\\?\\? 0" lib/experiments/run-write.mjs` returns nothing (no zero-coercion).
    - Existing run-write.test.mjs stays green (idempotency, strict-path, 8-tags-always-present).
    - If query.mjs is unchanged, a test assertion proves readRuns surfaces the new fields via ...meta.
  </acceptance_criteria>
  <done>Three metadata fields persisted with correct null/[] defaults, idempotent, readable via readRuns; no ontology or query.mjs change required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coding → proxy-owned token-usage.db | coding is a READ-ONLY second consumer at this layer; the DB is owned by rapid-llm-proxy. |
| stop-path tags → km-core experiment store | Computed canonical/background values written to the experiment store (separate, ontologyDir-aware). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-21 | Tampering | token-usage.db | mitigate | Aggregation stays `{readonly:true, fileMustExist:true}`; no column/migration added to the proxy-owned DB (derive at read). |
| T-75-22 | Tampering | SQL via task_id | mitigate | task_id bound as `?` placeholder only; new user_hash/process columns are SELECT/GROUP BY identifiers, not user input. |
| T-75-23 | Spoofing | cross-attribution via fake adapter hash | accept | denylist (BACKGROUND_PROCESS_RE) overrides adapter user_hash so an observation-writer row can't masquerade as foreground; local-only data, low risk. |
| T-75-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH audit N/A). |
</threat_model>

<verification>
- `node --test tests/experiments/canonical-attribution.test.mjs tests/experiments/token-aggregate.test.mjs tests/experiments/run-write.test.mjs` → green for lineage + canonical-persistence portions.
- Acceptance greps above all pass.
</verification>

<success_criteria>
ATTR-01 lineage classification and ATTR-02 canonical/background persistence are implemented at the correct tier (aggregation-time derivation + Run.metadata persistence), with no proxy-DB schema change and no per-surface recompute. Empty canonical is null, never a dominant fallback.
</success_criteria>

<output>
Create `.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-02-SUMMARY.md` when done
</output>
