---
phase: 75-measurement-attribution-accuracy-observation-linkage
plan: 04
type: execute
wave: 3
depends_on: ["75-02", "75-03"]
files_modified:
  - scripts/measurement-stop.mjs
autonomous: true
requirements: [ATTR-01, ATTR-02, ATTR-03]
user_setup: []

must_haves:
  truths:
    - "measurement-stop captures foreground tokens BEFORE aggregating, so cladpt rows exist when the sum runs"
    - "canonical = fgGroups[0] (foreground chat agent), NEVER byAgentModel[0] (the rejected dominant selector)"
    - "background_models[] is the segregated non-foreground groups, passed into writeRun's tags"
    - "A stop-time bypass-guard warns when an in-scope agent ran with no proxy rows AND no adapter rows (Anthropic-direct bypass, A1)"
  artifacts:
    - path: "scripts/measurement-stop.mjs"
      provides: "D-03 capture + D-05/D-06 canonical compute wiring at the convergence point"
      contains: "captureForegroundTokens"
  key_links:
    - from: "scripts/measurement-stop.mjs"
      to: "lib/lsl/token/stop-adapter-registry.mjs"
      via: "captureForegroundTokens(span,...) before aggregateByTaskId"
      pattern: "captureForegroundTokens"
    - from: "scripts/measurement-stop.mjs"
      to: "lib/experiments/token-aggregate.mjs"
      via: "isForegroundGroup filter on byAgentModel"
      pattern: "isForegroundGroup"
    - from: "scripts/measurement-stop.mjs"
      to: "lib/experiments/run-write.mjs"
      via: "writeRun tags carry canonical_model/canonical_agent/background_models"
      pattern: "canonical_model"
---

<objective>
Wire the foreground capture (Plan 03) and canonical/background derivation (Plan 02) into `measurement-stop.mjs` — the close orchestrator and the single convergence point for D-03/D-05/D-06. Replace today's finding-B bug (`dominant = byAgentModel[0]`) with: capture foreground tokens → aggregate → split fg/bg via isForegroundGroup → canonical = fgGroups[0] (or null, NEVER dominant) → pass canonical + background_models into writeRun's tags. Add the A1 bypass-guard warning.

Purpose: Make every newly-closed Run carry an honest canonical model and a segregated background-model list.
Output: Modified `measurement-stop.mjs`. This is the integration plan; it makes the canonical/background fields actually get computed and persisted on real closes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-RESEARCH.md
@.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md
@scripts/measurement-stop.mjs

<interfaces>
Seam to REPLACE (measurement-stop.mjs:297-313):
  const { totals, byAgentModel } = aggregateByTaskId(span.task_id);
  const dominant = byAgentModel[0] ?? {};          // ← REJECTED by D-05
  const normAgent = normalizeAgent(dominant);
  const tags = { task_hash, agent: normAgent ?? dominant.agent ?? null, model: dominant.model ?? null, framework:..., trace_id: span.task_id };
  ... writeRun(store, { span, taskClass, pending, tags, totals, heuristics })  // :334

Imports available:
  import { aggregateByTaskId, isForegroundGroup } from '../lib/experiments/token-aggregate.mjs';  // isForegroundGroup NEW (Plan 02)
  import { captureForegroundTokens } from '../lib/lsl/token/stop-adapter-registry.mjs';            // NEW (Plan 03)
  import { writeRun } from '../lib/experiments/run-write.mjs';
  import { normalizeAgent, buildTraceSeam } from '../lib/experiments/route-trace-resolve.mjs';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: capture-then-derive canonical/background at stop (D-03/D-05/D-06)</name>
  <read_first>
    - scripts/measurement-stop.mjs (full — :60-65 imports, :269-298 goal/aggregate seam, :297-313 dominant tags block, :324-325 buildTraceSeam time-window, :334 writeRun call, :336-352 best-effort score path)
    - lib/experiments/token-aggregate.mjs (isForegroundGroup + enriched byAgentModel from Plan 02)
    - lib/lsl/token/stop-adapter-registry.mjs (captureForegroundTokens from Plan 03)
    - lib/experiments/run-write.mjs (the canonical_model/background_models metadata fields from Plan 02)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-PATTERNS.md (measurement-stop section ~78-122)
  </read_first>
  <action>
    Import `isForegroundGroup` from token-aggregate.mjs and `captureForegroundTokens` from stop-adapter-registry.mjs. BEFORE the `aggregateByTaskId(span.task_id)` call, add a best-effort `await captureForegroundTokens(span, { agent: <in-scope foreground agent> })` so foreground cladpt rows exist when the sum runs (the agent for an interactive Claude session is 'claude'; derive it from the span/known foreground or default to 'claude'). After aggregation: `const fgGroups = byAgentModel.filter(isForegroundGroup); const bgGroups = byAgentModel.filter(g => !isForegroundGroup(g)); const canonical = fgGroups[0] ?? null; const canonicalModel = canonical?.model ?? null; const canonicalAgent = canonical ? normalizeAgent(canonical) : null; const backgroundModels = bgGroups.map(g => ({ model: g.model, process: g.process, total_tokens: g.total_tokens }));`. DELETE the `const dominant = byAgentModel[0]` line and its uses. In the `tags` object set `agent: canonicalAgent`, `model: canonicalModel`, `framework: span.meta?.framework ?? canonicalAgent`, and ADD `canonical_model: canonicalModel`, `canonical_agent: canonicalAgent`, `background_models: backgroundModels`. Keep `trace_id: span.task_id` and `task_hash`. Leave the buildTraceSeam route block intact but feed it `canonicalAgent` where it currently used `normAgent` (the time-window Claude locator is still valid). Wrap captureForegroundTokens best-effort exactly like the score path at :336-352 — on failure `process.stderr.write` and continue (close must never crash). Output via process.stdout/stderr only (no-console-log).
  </action>
  <verify>
    <automated>node --test tests/lsl/token/stop-adapter-registry.test.mjs tests/experiments/canonical-attribution.test.mjs && node --check scripts/measurement-stop.mjs && echo "syntax-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "captureForegroundTokens" scripts/measurement-stop.mjs` shows it called BEFORE the aggregateByTaskId line (capture-then-sum order).
    - `grep -n "byAgentModel\[0\]\|const dominant" scripts/measurement-stop.mjs` returns NOTHING (the rejected dominant selector is gone).
    - `grep -n "isForegroundGroup\|fgGroups\|backgroundModels" scripts/measurement-stop.mjs` shows the fg/bg split.
    - `grep -n "canonical_model\|canonical_agent\|background_models" scripts/measurement-stop.mjs` shows all three passed into the tags object.
    - `node --check scripts/measurement-stop.mjs` passes (valid syntax).
    - captureForegroundTokens is wrapped best-effort: `grep -nE "catch|stderr" scripts/measurement-stop.mjs` shows a guard around the capture (non-fatal).
  </acceptance_criteria>
  <done>Stop path captures foreground tokens first, derives canonical from fgGroups[0] (never dominant), and passes canonical + background_models into writeRun; dominant selector deleted; syntax valid; capture is non-fatal.</done>
</task>

<task type="auto">
  <name>Task 2: A1 Anthropic-direct bypass-guard warning</name>
  <read_first>
    - scripts/measurement-stop.mjs (the new fg/bg block from Task 1; :33-34 no-console-log header)
    - .planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-RESEARCH.md (Assumptions A1, §Open Questions 2, Pattern double-count guard)
  </read_first>
  <action>
    After the fg/bg split, add a non-fatal bypass-guard: if an in-scope foreground agent (claude/copilot/opencode/mastra) is expected for this measurement but produced NEITHER proxy rows (no matching group in bgGroups/byAgentModel for that agent) NOR adapter rows (no fgGroups for it), emit ONE `process.stderr.write('[measurement-stop] WARN: agent <X> ran with no proxy rows and no adapter rows — possible Anthropic-direct bypass; tokens may be uncaptured (A1)\n')`. This detects the network-dependent case where opencode/mastra ran Anthropic-direct (outside VPN) and silently bypassed the proxy. Warning only — never block the close. No new dependency; reuse byAgentModel/fgGroups already computed.
  </action>
  <verify>
    <automated>node --check scripts/measurement-stop.mjs && grep -nq "Anthropic-direct bypass\|possible Anthropic-direct" scripts/measurement-stop.mjs && echo "guard-present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Anthropic-direct bypass" scripts/measurement-stop.mjs` shows exactly the warning string.
    - The guard uses already-computed byAgentModel/fgGroups (no new query) and is non-fatal (`process.stderr.write`, no throw/exit).
    - `node --check scripts/measurement-stop.mjs` passes.
  </acceptance_criteria>
  <done>A single non-fatal stderr warning fires when an in-scope agent has neither proxy nor adapter rows; close still completes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| span (.data/active-measurement.json) → stop orchestrator | Local span file drives task_id + time-window; trusted local input. |
| stop orchestrator → km-core experiment store | Writes computed canonical/background to the experiment store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-41 | Tampering | task_id spoof / cross-attribution | mitigate | task_id flows from the single span reader; canonical derived only from isForegroundGroup (denylist-guarded). |
| T-75-42 | Denial of service | capture/aggregate failure crashes close | mitigate | captureForegroundTokens + score path wrapped best-effort; close never hard-blocks. |
| T-75-43 | Repudiation | silent token loss on proxy bypass | mitigate | A1 bypass-guard emits an explicit stderr warning so uncaptured tokens are visible, not silent. |
| T-75-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH audit N/A). |
</threat_model>

<verification>
- `node --test tests/lsl/token/stop-adapter-registry.test.mjs tests/experiments/canonical-attribution.test.mjs` → green.
- `node --check scripts/measurement-stop.mjs` → valid.
- Acceptance greps pass (no dominant selector; capture-before-sum; canonical fields in tags; bypass-guard present).
- Phase-gate live check (operator, at /gsd-verify-work): close a short real Claude session and confirm `SELECT COUNT(*) FROM token_usage WHERE task_id=? AND user_hash='cladpt'` > 0 and the Run's canonical_model is the session model (e.g. Opus), idempotent across recompute.
</verification>

<success_criteria>
The stop path no longer attributes daemon traffic as canonical: foreground tokens are captured first, canonical = foreground chat agent (or null), background models are segregated and persisted, and proxy-bypass is surfaced as a warning rather than silent loss.
</success_criteria>

<output>
Create `.planning/phases/75-measurement-attribution-accuracy-observation-linkage/75-04-SUMMARY.md` when done
</output>
