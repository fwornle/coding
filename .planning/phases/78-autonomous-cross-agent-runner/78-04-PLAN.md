---
phase: 78-autonomous-cross-agent-runner
plan: 04
type: execute
wave: 3
depends_on: [78-03]
files_modified:
  - scripts/experiment-run.mjs
  - tests/experiments/experiment-runner.integration.test.mjs
autonomous: true
requirements: [RUN-02, RUN-03, RUN-04]
user_setup: []

must_haves:
  truths:
    - "An operator can run a whole experiment matrix from one CLI: node scripts/experiment-run.mjs --spec <file>"
    - "The CLI narrows a run via --variant / --repeats / --timeout without editing the spec file"
    - "A full matrix with a stub agent lands exactly one Run per variant×repeat cell (SC#4)"
    - "The CLI exits non-zero on failure and prints diagnostics via stderr"
    - "Implements decisions D-06, D-10: the --timeout override of the 20-min default, and the SC#4 idempotency proof (exactly one Run per cell, re-run adds none)"
  artifacts:
    - path: "scripts/experiment-run.mjs"
      provides: "thin operator CLI over runMatrix (mirrors scripts/experiment-restore.mjs skeleton)"
      contains: "runMatrix"
    - path: "tests/experiments/experiment-runner.integration.test.mjs"
      provides: "SC#4 end-to-end via a stub agent (node -e process.exit(0)) → exactly one Run per cell"
  key_links:
    - from: "scripts/experiment-run.mjs"
      to: "lib/experiments/experiment-runner.mjs runMatrix"
      via: "import + await"
      pattern: "runMatrix"
    - from: "integration test"
      to: "readRuns after a matrix run"
      via: "assert one Run per variant×repeat cell"
      pattern: "readRuns"
---

<objective>
Add the thin operator CLI `scripts/experiment-run.mjs` over the 78-03 `runMatrix` engine, and prove SC#4 (exactly one Run per variant×repeat cell) end-to-end with a stub agent — no live LLM. Near-perfect sibling: `scripts/experiment-restore.mjs` (copy its skeleton, swap the body).

Purpose: operator entry point + automated SC#4 proof.
Output: `scripts/experiment-run.mjs` + `tests/experiments/experiment-runner.integration.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-autonomous-cross-agent-runner/78-CONTEXT.md
@.planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md
@.planning/phases/78-autonomous-cross-agent-runner/78-PATTERNS.md
@lib/experiments/experiment-runner.mjs

<interfaces>
<!-- From scripts/experiment-restore.mjs (the sibling CLI skeleton to copy — header, parseStrArg, main().catch): -->
  parseStrArg(argv, flag) inline helper (identical to measurement-start.mjs:56-60 — do NOT add an arg-parse lib)
  Env: CODING_REPO → repoRoot; LLM_PROXY_DATA_DIR → dataDir
  process.exit(2) for usage errors; main().catch → `FATAL: ...` stderr + exit(1)
  Test seam via env (experiment-restore.mjs uses EXPERIMENT_RESTORE_FAKE) — expose a parallel
  EXPERIMENT_RUN_FAKE-style seam so the integration test drives the CLI with a stub agent.

From lib/experiments/experiment-runner.mjs (78-03):
  runMatrix(spec, { repoRoot, dataDir, ontologyDir, timeoutMs, restore, spawnAgent, runMeasurement, readDone, probeCopilot, openStore }) → summary[]

From lib/experiments/query.mjs:77 : readRuns(store, {includePending}) → rows[]  (SC#4 assertion source)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operator CLI scripts/experiment-run.mjs</name>
  <files>scripts/experiment-run.mjs</files>
  <read_first>
    - scripts/experiment-restore.mjs (the sibling CLI to copy wholesale — header, parseStrArg, env resolution, main().catch)
    - lib/experiments/experiment-runner.mjs (78-03 — runMatrix signature)
    - scripts/measurement-start.mjs:56-60 (parseStrArg idiom)
    - .planning/phases/78-autonomous-cross-agent-runner/78-PATTERNS.md (the scripts/experiment-run.mjs section — full skeleton)
  </read_first>
  <action>
    Copy the scripts/experiment-restore.mjs skeleton and swap the body to call `runMatrix`. Parse `--spec <file>` (required; error+usage+exit 2 if absent), optional `--variant <name>` (narrow to one cell), `--repeats <N>` (override), `--timeout <sec>` (override the 20-min default — D-06; convert seconds→ms). Resolve `repoRoot` from `CODING_REPO`||cwd and `dataDir` from `LLM_PROXY_DATA_DIR`||`<repoRoot>/.data`. `await runMatrix(spec, {...})`, print a per-cell summary to stderr, exit non-zero if any REQUIRED-agent (claude/opencode) cell failed. Expose a test-only `EXPERIMENT_RUN_FAKE` env seam (fail-soft, ignored in production) that injects a stub agent so the CLI is drivable without a live agent. Diagnostics via `process.stderr.write` only; `main().catch` → `FATAL` + exit 1.
  </action>
  <acceptance_criteria>
    - `scripts/experiment-run.mjs imports runMatrix from ../lib/experiments/experiment-runner.mjs`.
    - `node scripts/experiment-run.mjs` with no `--spec` prints a usage error and exits 2.
    - `grep -n "console\." scripts/experiment-run.mjs` returns nothing (stderr only).
    - `grep -n "\-\-timeout\|--variant\|--repeats\|--spec" scripts/experiment-run.mjs` shows all four flags parsed.
    - `grep -n "EXPERIMENT_RUN_FAKE" scripts/experiment-run.mjs` shows the test seam.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/experiment-run.mjs; test $? -eq 2</automated>
  </verify>
  <done>An operator can drive/narrow a matrix run from one CLI; usage errors exit 2, runtime failures exit 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#4 integration test — exactly one Run per cell with a stub agent</name>
  <files>tests/experiments/experiment-runner.integration.test.mjs</files>
  <read_first>
    - tests/experiments/experiment-restore.test.mjs:1-44 (node:test + env-seam CLI-drive idiom to mirror)
    - lib/experiments/query.mjs:77-118 (readRuns — the SC#4 assertion source)
    - lib/experiments/experiment-runner.mjs (78-03 — runMatrix)
    - lib/experiments/store.mjs:40-54 (openExperimentStore — repo-global; use a tmp repoRoot for isolation)
  </read_first>
  <behavior>
    - Test: a small matrix (e.g. 2 variants × 2 repeats) driven with a stub agent (`node -e "process.exit(0)"`) via runMatrix (or the CLI EXPERIMENT_RUN_FAKE seam) lands exactly 4 Runs, one per composite task_id, each terminal_state==='complete'.
    - Test: re-running the same matrix adds NO new Runs (D-10 idempotency — readRuns count unchanged).
    - Test: a variant×repeat set including a stub-abort cell records that cell with terminal_state==='abort' (not dropped) and it is retried on a resume run.
  </behavior>
  <action>
    Author `tests/experiments/experiment-runner.integration.test.mjs` (node:test + node:assert/strict) that drives a real `runMatrix` against a tmp `repoRoot` (isolated `.data/experiments`) using injected/stub seams so no live agent or git worktree is needed: inject a fake `restore` returning a tmp worktree/sandboxDataDir, a stub `spawnAgent` (or the stub-agent fixture) mapping to complete/abort, and a fake `runMeasurement` that writes a Run via the shipped writeRun so readRuns can assert cell count. Assert the three behaviors. Run command: `node --test tests/experiments/experiment-runner.integration.test.mjs`.
  </action>
  <acceptance_criteria>
    - After a 2×2 matrix run, `readRuns` returns exactly 4 rows, each with a distinct composite `task_id` and `terminal_state==='complete'`.
    - A second identical run leaves the Run count at 4 (idempotent — D-10).
    - An abort cell is present with `terminal_state==='abort'` and is re-attempted on resume.
    - `node --test tests/experiments/experiment-runner.integration.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/experiment-runner.integration.test.mjs</automated>
  </verify>
  <done>SC#4 is proven automatically: exactly one Run per variant×repeat cell, idempotent on re-run, terminal states recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI flags → runMatrix | `--spec/--variant/--repeats/--timeout` operator input |
| test env seam → stub agent | EXPERIMENT_RUN_FAKE only active in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04-01 | Tampering | `--spec` path / flags reaching runMatrix | mitigate | Spec content is validated by Phase-77 `resolveExperimentSpec` (fail-fast) before any cell runs; CLI passes flags as typed values, not shell strings. |
| T-78-04-02 | Elevation of Privilege | EXPERIMENT_RUN_FAKE stub-agent seam active in production | mitigate | Seam is fail-soft and gated on the env var (mirrors EXPERIMENT_RESTORE_FAKE); ignored when unset; documented test-only. |
| T-78-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (in-repo `.mjs`); slopcheck N/A. |
</threat_model>

<verification>
- `node scripts/experiment-run.mjs` (no args) exits 2 with usage.
- `node --test tests/experiments/experiment-runner.integration.test.mjs` green (SC#4 + idempotency + abort-recorded).
- `node --test tests/experiments` (module suite) green.
</verification>

<success_criteria>
- One operator CLI drives/narrows a matrix run (RUN-02/03).
- SC#4 proven automatically with a stub agent: exactly one Run per cell, idempotent, terminal states recorded.
</success_criteria>

<output>
Create `.planning/phases/78-autonomous-cross-agent-runner/78-04-SUMMARY.md` when done.
</output>
