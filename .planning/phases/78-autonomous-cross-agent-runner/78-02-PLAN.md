---
phase: 78-autonomous-cross-agent-runner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/experiments/agent-headless.mjs
  - tests/experiments/agent-headless.test.mjs
autonomous: true
requirements: [RUN-02, RUN-04]
user_setup: []

must_haves:
  truths:
    - "Each agent resolves to its launch binary via the config/agents/*.sh registry (not the interactive tmux launcher)"
    - "Each agent emits a fixed-argv array for a headless one-shot turn against a goal"
    - "A one-turn Copilot headless probe returns a boolean without stalling the matrix"
  artifacts:
    - path: "lib/experiments/agent-headless.mjs"
      provides: "agent→argv adapter table + AGENT_COMMAND resolver + copilot probe helper"
      exports: ["argvForAgent", "resolveAgentBinary", "probeCopilotHeadless"]
      min_lines: 40
    - path: "tests/experiments/agent-headless.test.mjs"
      provides: "node:test coverage for the adapter table + probe (fake spawnSync)"
  key_links:
    - from: "lib/experiments/agent-headless.mjs resolveAgentBinary"
      to: "config/agents/<name>.sh AGENT_COMMAND"
      via: "regex parse (re-exposed from agent-detector.js:23-39)"
      pattern: "AGENT_COMMAND"
    - from: "lib/experiments/agent-headless.mjs probeCopilotHeadless"
      to: "copilot -p ... --allow-all-tools"
      via: "fixed-argv spawnSync, assert exit 0"
      pattern: "allow-all-tools"
---

<objective>
Build the net-new agent→headless-argv adapter (RUN-02) and the one-turn Copilot capability probe (RUN-04). This is a pure transform/utility module with no orchestration — the runner engine (78-03) imports it.

CRITICAL research finding (RESEARCH Risk R1): `config/agents/*.sh` are INTERACTIVE tmux launchers, NOT headless. Reuse them ONLY as the agent→binary registry (via the `AGENT_COMMAND` line), then spawn each binary DIRECTLY with its documented non-interactive flag. Do NOT use `AgentRegistry.getAdapter(...)` (interactive MCP/HTTP shims). Do NOT use `bin/claude-mcp` for claude (that boots an interactive MCP session) — use the `claude` binary + `-p`.

Purpose: encode the confirmed per-agent headless invocations as fixed-argv arrays.
Output: `lib/experiments/agent-headless.mjs` exporting `argvForAgent`, `resolveAgentBinary`, `probeCopilotHeadless`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-autonomous-cross-agent-runner/78-CONTEXT.md
@.planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md
@.planning/phases/78-autonomous-cross-agent-runner/78-PATTERNS.md

<interfaces>
<!-- Confirmed headless invocations (RESEARCH Unknown 1, live --help 2026-07-03). Fixed argv arrays only. -->

  claude    → bin `claude` (NOT bin/claude-mcp); argv ['-p', goal, '--model', model, '--permission-mode', 'acceptEdits']
             (headless edits REQUIRE --permission-mode acceptEdits or --dangerously-skip-permissions — Pitfall 6)
  opencode  → bin `opencode`; argv ['run', goal, '-m', model]   (cwd via spawn cwd; --dir also sets it)
  mastracode→ bin `mastracode`; argv ['--prompt', goal, '-m', model]  (cwd via spawn cwd; NO --dir; exit 0/1/2)
  copilot   → bin `copilot`; argv ['-p', goal, '--allow-all-tools', '--model', model]
             (--allow-all-tools REQUIRED for non-interactive)

From lib/agent-detector.js:23-39 (parseAgentConfig — MODULE-PRIVATE, re-expose the ~10-line regex, do NOT import):
  const m = content.match(new RegExp(`^${key}="?([^"\\n]+)"?`, 'm'));  // get('AGENT_COMMAND')

Copilot probe (RESEARCH Unknown 2): spawnSync('copilot', ['-p','Reply with the single word OK and nothing else.','--allow-all-tools'], {timeout: 90_000}) → assert !r.error && r.status === 0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent→argv adapter table + AGENT_COMMAND binary resolver</name>
  <files>lib/experiments/agent-headless.mjs, tests/experiments/agent-headless.test.mjs</files>
  <read_first>
    - lib/experiments/experiment-restore.mjs:1-42 (module-header + import idiom for lib/experiments/*.mjs — copy the header shape verbatim)
    - lib/agent-detector.js:23-39 (parseAgentConfig regex — re-expose, module-private)
    - lib/experiments/evidence-harness.mjs:245-266 (SHELL_META_RE export + fixed-argv posture)
    - config/agents/claude.sh, config/agents/opencode.sh, config/agents/mastra.sh, config/agents/copilot.sh (the AGENT_COMMAND lines the resolver reads)
  </read_first>
  <behavior>
    - Test: argvForAgent('claude', goal, {model}) returns ['-p', goal, '--model', model, '--permission-mode', 'acceptEdits'] and bin 'claude'.
    - Test: argvForAgent('opencode', goal, {model}) returns ['run', goal, '-m', model].
    - Test: argvForAgent('mastracode', goal, {model}) returns ['--prompt', goal, '-m', model].
    - Test: argvForAgent('copilot', goal, {model}) returns ['-p', goal, '--allow-all-tools', '--model', model].
    - Test: every returned argv is an array of strings (no shell string); goal is a single argv element even when it contains spaces.
    - Test: resolveAgentBinary('claude', agentsDir) parses AGENT_COMMAND from claude.sh; the table overrides claude's registry binary to the `claude` binary (NOT bin/claude-mcp) for the -p headless path.
  </behavior>
  <action>
    Create `lib/experiments/agent-headless.mjs` with the standard lib/experiments header block (phase/plan, exported signatures, decisions D-01/D-02, "Diagnostics via process.stderr.write only"). Export `argvForAgent(agentName, goal, {model})` returning a fixed-argv ARRAY per the interfaces table, `resolveAgentBinary(agentName, agentsDir)` re-exposing the ~10-line AGENT_COMMAND regex parse (do not import the private symbol). Encode the per-agent permission flags exactly: claude `--permission-mode acceptEdits`, copilot `--allow-all-tools` (both required for autonomous edits inside the throwaway sandbox — D-02). Never build a shell string. Diagnostics via `process.stderr.write` only. Write agent-headless.test.mjs (node:test + node:assert/strict) covering the six behaviors.
  </action>
  <acceptance_criteria>
    - `lib/experiments/agent-headless.mjs exports function argvForAgent(` and `export function resolveAgentBinary(`.
    - `grep -n "shell: true\|shell:true" lib/experiments/agent-headless.mjs` returns nothing.
    - `grep -n "claude-mcp" lib/experiments/agent-headless.mjs` returns nothing (uses the `claude` binary directly).
    - `grep -n "acceptEdits\|allow-all-tools" lib/experiments/agent-headless.mjs` shows both permission flags present.
    - `node --test tests/experiments/agent-headless.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/agent-headless.test.mjs</automated>
  </verify>
  <done>Each supported agent emits the confirmed fixed-argv headless invocation; claude resolves to the `claude` binary + `-p`, never the interactive wrapper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Copilot one-turn headless probe (RUN-04)</name>
  <files>lib/experiments/agent-headless.mjs, tests/experiments/agent-headless.test.mjs</files>
  <read_first>
    - lib/experiments/agent-headless.mjs (Task 1 output — extend it)
    - lib/experiments/evidence-harness.mjs:278-298 (runTestCommand — fixed-argv spawnSync, bounded timeout, fail-soft on res.error — the probe analog)
    - .planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md (Unknown 2 — probe shape + the pessimistic gate finding)
  </read_first>
  <behavior>
    - Test: probeCopilotHeadless({ spawn: fakeExit0 }) returns true when the injected spawnSync yields status 0 and no error.
    - Test: probeCopilotHeadless({ spawn: fakeNonZero }) returns false on non-zero exit.
    - Test: probeCopilotHeadless({ spawn: fakeError }) returns false when res.error is set (ENOENT / timeout) — never throws.
    - Test: the probe argv is exactly ['-p', <trivial one-turn prompt>, '--allow-all-tools'] (a trivial one-turn check, NOT a full drive — D-07).
  </behavior>
  <action>
    Add `probeCopilotHeadless({ spawn = spawnSync } = {})` to agent-headless.mjs. It runs a fixed-argv `copilot -p '<trivial prompt>' --allow-all-tools` with a short bounded timeout (~90s), returns `!r.error && r.status === 0` (never throws — fail-soft like runTestCommand). Inject `spawn` as a default-arg seam so tests exercise pass/fail/error without invoking real copilot. Cache is the runner's responsibility (78-03 calls it once per matrix), not this helper's. Diagnostics via `process.stderr.write` only.
  </action>
  <acceptance_criteria>
    - `lib/experiments/agent-headless.mjs exports function probeCopilotHeadless(` with an injectable `spawn` seam.
    - Source assertion: the probe argv contains `'-p'` and `'--allow-all-tools'` and passes the goal/prompt as a single argv element (no shell string).
    - `node --test tests/experiments/agent-headless.test.mjs` passes the three pass/fail/error cases; probe never throws on spawn error.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/agent-headless.test.mjs</automated>
  </verify>
  <done>A trivial one-turn Copilot headless probe returns a boolean via a fixed-argv spawn, fail-soft, injectable for tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| goal_sentence → agent process argv | The (spec-validated) goal string is passed to a spawned agent CLI |
| config/agents/*.sh → binary name | Registry file content parsed to resolve the launch binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02-01 | Tampering | Command injection via goal/model into agent launch | mitigate | `argvForAgent` returns a fixed-argv ARRAY (goal is one element); NEVER a shell string; no `shell:true` (acceptance grep gate). |
| T-78-02-02 | Elevation of Privilege | `--allow-all-tools` / `--permission-mode acceptEdits` grant autonomous edits | accept | Acceptable ONLY because 78-03 spawns with cwd=throwaway sandbox worktree (D-02); this module only emits the flag, it does not choose cwd. Documented in the module header. |
| T-78-02-03 | Denial of Service | Copilot probe hanging | mitigate | Probe uses a short bounded `timeout` (~90s) + fail-soft on `res.error`; a hung probe returns false, never blocks. |
| T-78-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (in-repo `.mjs` + already-installed CLIs); slopcheck N/A. |
</threat_model>

<verification>
- `node --test tests/experiments/agent-headless.test.mjs` green (adapter table + resolver + probe pass/fail/error).
- `grep -rn "shell: true\|getAdapter\|claude-mcp" lib/experiments/agent-headless.mjs` returns nothing (no interactive path, no shell).
</verification>

<success_criteria>
- Every supported agent has a confirmed fixed-argv headless invocation (RUN-02).
- The Copilot probe (RUN-04) is a trivial one-turn, fail-soft, injectable capability check.
</success_criteria>

<output>
Create `.planning/phases/78-autonomous-cross-agent-runner/78-02-SUMMARY.md` when done.
</output>
