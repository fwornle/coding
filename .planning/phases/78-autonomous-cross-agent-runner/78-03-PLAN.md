---
phase: 78-autonomous-cross-agent-runner
plan: 03
type: execute
wave: 2
depends_on: [78-01, 78-02]
files_modified:
  - lib/experiments/experiment-runner.mjs
  - tests/experiments/experiment-runner.test.mjs
  - tests/experiments/_fixtures/stub-agent.mjs
autonomous: true
requirements: [RUN-02, RUN-03, RUN-04]
user_setup: []

must_haves:
  truths:
    - "For each variant×repeat, the runner restores an isolated sandbox then launches the agent with cwd=worktree and LLM_PROXY_DATA_DIR=sandbox .data"
    - "Agent exit(0)→complete, wall-clock timeout kill→timeout, non-zero/killed→abort — all three recorded on the Run"
    - "measurement-stop runs in a finally on EVERY terminal state, so no cell is silently dropped"
    - "A probe-failed copilot cell lands a Run with skip_reason='copilot-headless-unsupported', never silently absent"
    - "A re-run skips cells whose composite task_id already has a complete Run; timeout/abort cells are retried"
    - "Cells execute strictly sequentially (single-owner experiment store)"
  artifacts:
    - path: "lib/experiments/experiment-runner.mjs"
      provides: "runMatrix sequential idempotent loop: restore→launch→timer→terminal-state→inline-score→resume→skip-Run"
      exports: ["runMatrix"]
      min_lines: 120
    - path: "tests/experiments/experiment-runner.test.mjs"
      provides: "node:test unit coverage via injected seams (restore/spawnAgent/measurementStop/readDone/probeCopilot)"
    - path: "tests/experiments/_fixtures/stub-agent.mjs"
      provides: "stub agent that exits 0 / hangs / exits non-zero on demand for terminal-state tests"
  key_links:
    - from: "experiment-runner.mjs"
      to: "lib/experiments/experiment-restore.mjs restoreForCell"
      via: "injectable restore seam"
      pattern: "restoreForCell"
    - from: "experiment-runner.mjs launch"
      to: "spawn(bin, argv, {cwd: worktree, env: {LLM_PROXY_DATA_DIR: sandboxDataDir}})"
      via: "async spawn + SIGTERM→SIGKILL timer"
      pattern: "LLM_PROXY_DATA_DIR"
    - from: "experiment-runner.mjs resume ledger"
      to: "lib/experiments/query.mjs readRuns"
      via: "done-set filtered on terminal_state==='complete'"
      pattern: "terminal_state"
---

<objective>
Build the net-new orchestration engine `lib/experiments/experiment-runner.mjs` — the sequential, idempotent, resumable matrix loop that wires the shipped parts into "one scored Run per variant×repeat cell." Per cell: `restoreForCell` (isolated sandbox) → `measurement-start` (span open, sandbox env) → spawn the resolved agent under a 20-min SIGTERM→SIGKILL timer → `measurement-stop --headless --terminal-state <enum>` (inline scoring, D-11) in a `finally` → record terminal state. Copilot cells are gated by the once-per-matrix probe (78-02); a probe-failed cell lands a recorded skip-Run.

Resolves RESEARCH open questions:
- Q1 (copilot gate): run the probe STANDALONE once before the loop, cache the boolean. Copilot variants are authored under `env: default` so they pass Phase-77 `validateCells` (the frozen `copilot+headless` gate is NOT mutated at runtime); the probe alone gates whether copilot cells run vs. land a skip-Run. Document this in the module header.
- Q3 (retry policy): the resume done-set skips ONLY cells whose composite `task_id` has a Run with `terminal_state === 'complete'`; `timeout`/`abort`/unscored cells are retried on resume.

Purpose: the autonomous cross-agent drive loop (RUN-02/03/04, D-02/04/06/07/08/09/10/11/12).
Output: `runMatrix(spec, opts)` with injectable seams + unit tests + a stub-agent fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-autonomous-cross-agent-runner/78-CONTEXT.md
@.planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md
@.planning/phases/78-autonomous-cross-agent-runner/78-PATTERNS.md
@lib/experiments/agent-headless.mjs

<interfaces>
<!-- Shipped contracts the runner wires (RESEARCH Standard Stack). Use directly — no exploration. -->

  restoreForCell(snapshotId, {repoRoot, dataDir, ontologyDir, restore?}) → { worktree, sandboxDataDir, digest }
    (lib/experiments/experiment-restore.mjs:168; inPlace:false always; sandboxDataDir = <worktree>/.data)
  openExperimentStore({repoRoot}) → GraphKMStore   (lib/experiments/store.mjs:40; repo-global .data/experiments; single-owner; caller owns close())
  readRuns(store, {includePending}) → rows[]        (lib/experiments/query.mjs:77; rows carry ...metadata incl. task_id + terminal_state)
  resolveExperimentSpec(fileOrObj) → { goal_sentence, repeats, cells[] }  (lib/experiments/experiment-spec.mjs:211; cells carry {agent,model,framework,env,test_command})

  measurement-start.mjs  per-cell CLI (child, env LLM_PROXY_DATA_DIR=sandboxDataDir):
    --task-id "<expId>--<variant>--r<rep>"  --agent <a> --model <m> --framework <f>
    --test-command "<fixed-argv>" --variant "<cellName>" --repeat <rep> --goal "<goal_sentence>"
  measurement-stop.mjs   per-cell CLI (child, same env): --headless --task-class <cls>
    --terminal-state <complete|timeout|abort> [--skip-reason <str>]   (78-01 added the last two)

  argvForAgent(agent, goal, {model}) → string[]        (78-02)
  resolveAgentBinary(agent, agentsDir) → string        (78-02)
  probeCopilotHeadless({spawn?}) → boolean             (78-02)

Terminal-state machine template: SIGTERM → wait GRACE_MS → SIGKILL (lib/vkb-server/server-manager.js:162-167).
MUST use async `spawn` (NOT spawnSync — a sync call blocks the wall-clock timer; Pitfall 2).
Composite cell key = `<expId>--<variant>--r<rep>` (Pitfall 1 — task_hash is constant across the matrix).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Terminal-state machine — spawn + 20-min SIGTERM→SIGKILL timer</name>
  <files>lib/experiments/experiment-runner.mjs, tests/experiments/experiment-runner.test.mjs, tests/experiments/_fixtures/stub-agent.mjs</files>
  <read_first>
    - lib/experiments/experiment-restore.mjs:1-42,168-193 (the golden sibling orchestrator — header, injectable-seam idiom, restoreForCell return shape)
    - lib/vkb-server/server-manager.js:162-167 (SIGTERM→SIGKILL escalation template)
    - .planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md (Unknown 5 — terminal-state machine; Pitfall 2/4)
    - lib/experiments/agent-headless.mjs (78-02 — argvForAgent/resolveAgentBinary)
  </read_first>
  <behavior>
    - Test: a stub agent exiting 0 resolves terminal_state 'complete'.
    - Test: a stub agent exiting non-zero resolves 'abort'.
    - Test: a stub agent that hangs is SIGTERM→SIGKILL killed after the (test-shortened) timeout and resolves 'timeout'.
    - Test: the child is spawned with cwd=worktree and env.LLM_PROXY_DATA_DIR=sandboxDataDir (assert via the fixture echoing cwd/env, or an injected fake spawn capturing opts).
    - Test: launch uses async spawn (a hanging agent does not block the returned promise before the timer fires).
  </behavior>
  <action>
    Create `lib/experiments/experiment-runner.mjs` with the standard lib/experiments header (phase 78, exports, D-02/04/06 decisions, "Diagnostics via process.stderr.write only"). Implement an internal `launchCell({ bin, argv, worktree, sandboxDataDir, timeoutMs = 20 * 60_000, spawn = realSpawn })` returning a Promise that resolves to `complete | timeout | abort` (D-04 verbatim enum). Use async `spawn` with `{ cwd: worktree, env: { ...process.env, LLM_PROXY_DATA_DIR: sandboxDataDir } }` (D-02), a `setTimeout(timeoutMs)` that sets a `killedByTimer` flag → `child.kill('SIGTERM')` → nested `setTimeout(GRACE_MS)` → `child.kill('SIGKILL')`; on `exit` clear the timer and map `killedByTimer ? 'timeout' : (code === 0 ? 'complete' : 'abort')`. `timeoutMs` overridable (D-06). Fixed argv only — never a shell string. Create `tests/experiments/_fixtures/stub-agent.mjs` (a tiny node script that exits 0 / exits non-zero / hangs based on argv or an env flag, and can echo cwd + LLM_PROXY_DATA_DIR). Add the five behaviors to experiment-runner.test.mjs.
  </action>
  <acceptance_criteria>
    - `grep -n "spawn(" lib/experiments/experiment-runner.mjs` uses async spawn; `grep -n "spawnSync" lib/experiments/experiment-runner.mjs` is NOT used for the agent launch.
    - `grep -n "shell: true\|shell:true" lib/experiments/experiment-runner.mjs` returns nothing.
    - Source assertion: the terminal enum is exactly `complete`, `timeout`, `abort`.
    - Source assertion: spawn opts set `cwd` and `env` with `LLM_PROXY_DATA_DIR`.
    - `node --test tests/experiments/experiment-runner.test.mjs` passes the complete/abort/timeout + cwd/env cases (timer shortened via the timeoutMs seam).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/experiment-runner.test.mjs</automated>
  </verify>
  <done>A driven agent child maps exit/timeout/abort to the terminal enum under a bounded wall-clock timer, spawned into the isolated sandbox.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-cell wiring — restore → measured span → launch → inline score in a finally</name>
  <files>lib/experiments/experiment-runner.mjs, tests/experiments/experiment-runner.test.mjs</files>
  <read_first>
    - lib/experiments/experiment-runner.mjs (Task 1 output)
    - scripts/measurement-start.mjs:90-165 and scripts/measurement-stop.mjs:415-475 (the per-cell CLI surface the runner shells out to; 78-01 added --repeat/--terminal-state/--skip-reason)
    - lib/experiments/experiment-restore.mjs:168-193 (restoreForCell return: worktree, sandboxDataDir)
    - .planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md (Unknown 3/4 — exact CLI surface + inline-score reuse; Pitfall 4)
  </read_first>
  <behavior>
    - Test: runCell restores via the injected restore seam, then invokes measurement-start with --task-id="<expId>--<variant>--r<rep>", --variant, --repeat, --agent/--model/--framework, and env LLM_PROXY_DATA_DIR=sandboxDataDir.
    - Test: on a normal complete, measurement-stop is invoked once with --headless --terminal-state complete.
    - Test: on abort/timeout, measurement-stop STILL runs (in a finally) with the mapped --terminal-state (no cell dropped — D-04/Pitfall 4).
    - Test: measurement-start/stop are shelled as fixed-argv child processes (injected runMeasurement seam captures argv; no shell string).
  </behavior>
  <action>
    Add an internal `runCell({ cell, rep, expId, goal, sandbox, timeoutMs, ...seams })` that: (1) calls `restore` (default restoreForCell) → { worktree, sandboxDataDir }; (2) shells `measurement-start.mjs` (fixed argv) with the composite `--task-id`, `--variant`, `--repeat`, cell `--agent/--model/--framework/--test-command`, `--goal`, and child env `LLM_PROXY_DATA_DIR=sandboxDataDir`; (3) resolves the agent via `resolveAgentBinary`/`argvForAgent` and calls Task 1's `launchCell` → terminal state; (4) in a `finally`, shells `measurement-stop.mjs --headless --task-class <cls> --terminal-state <state>` (same env) so the Run + inline score land on EVERY terminal state (D-11, Pitfall 4). Note in a header comment that `test_command` runs at score time only (D-05), NOT as the completion gate. Inject `restore`, `spawnAgent`(launchCell), and `runMeasurement`(child-proc) as default-arg seams. Add the four behaviors to the test.
  </action>
  <acceptance_criteria>
    - Source assertion: measurement-start argv includes `--task-id`, `--variant`, `--repeat`, `--goal`; measurement-stop argv includes `--headless` and `--terminal-state`.
    - Source assertion: the measurement-stop call is inside a `finally` (grep shows `finally` around the stop invocation).
    - Source assertion: child env sets `LLM_PROXY_DATA_DIR` to sandboxDataDir for BOTH start and stop.
    - `node --test tests/experiments/experiment-runner.test.mjs` passes the complete + abort-still-scores cases.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/experiment-runner.test.mjs</automated>
  </verify>
  <done>Each cell restores into isolation, runs inside a measured span, and lands exactly one scored Run tagged with its terminal state even on abort/timeout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sequential idempotent matrix loop + copilot probe gate + skip-Run</name>
  <files>lib/experiments/experiment-runner.mjs, tests/experiments/experiment-runner.test.mjs</files>
  <read_first>
    - lib/experiments/experiment-runner.mjs (Task 1+2 output)
    - lib/experiments/query.mjs:77-118 (readRuns — resume ledger; rows carry task_id + terminal_state)
    - lib/experiments/store.mjs:28-54 (openExperimentStore — repo-global, single-owner LevelDB → forces sequential; caller owns close)
    - lib/experiments/agent-headless.mjs (probeCopilotHeadless — 78-02)
    - .planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md (Unknown 6 — resume loop; Q1/Q3; Pitfall 3/5)
    - .planning/phases/78-autonomous-cross-agent-runner/78-CONTEXT.md (D-08/09/10/12 — recorded skip, sequential, idempotency, agent scope)
  </read_first>
  <behavior>
    - Test: runMatrix iterates cells × repeats strictly sequentially (calls ordered; no concurrent store open — assert single openExperimentStore/close usage or serialized cell calls).
    - Test: a cell whose composite task_id already has a Run with terminal_state==='complete' (injected readDone) is SKIPPED; a cell with a prior 'timeout'/'abort' Run is RE-RUN (Q3).
    - Test: with probeCopilot→false, each copilot cell lands a recorded skip-Run (skip_reason='copilot-headless-unsupported') via measurement-start/stop with --skip-reason, and no agent is launched for it (D-08, never silently absent).
    - Test: with probeCopilot→true, copilot cells run through the normal launch path.
    - Test: a mastra cell whose launch fails (spawn error) is recorded (best-effort, D-12) — the matrix does not throw/stall.
    - Test: runMatrix returns a summary listing one result per attempted cell (skipped/complete/timeout/abort).
  </behavior>
  <action>
    Implement and export `runMatrix(spec, { repoRoot, dataDir, ontologyDir, timeoutMs, restore, spawnAgent, runMeasurement, readDone = readRuns, probeCopilot = probeCopilotHeadless, openStore = openExperimentStore } = {})`. Steps: (a) resolve spec (goal_sentence, repeats, cells) — spec already validated by Phase-77 resolveExperimentSpec; (b) open the repo-global store ONCE, build the done-set from `readDone(store,{includePending:true})` filtered to `r.terminal_state === 'complete'`, then close it before launching cells (single-owner discipline — measurement-stop reopens per cell); (c) if any cell is copilot, run `probeCopilot()` ONCE and cache the boolean; (d) `for (cell of cells) for (rep in repeats)` — build composite `task_id`, skip if in done-set (D-10), for a copilot cell with a failed probe write a skip-Run (measurement-start/stop with `--skip-reason copilot-headless-unsupported`, no agent launch), otherwise `runCell` (Task 2). Strictly sequential (`await` each cell — D-09; never Promise.all). Wrap each cell in try/catch so a mastra/copilot failure is recorded, not fatal (D-12). Return a per-cell summary array. Document Q1 (probe standalone, copilot authored env:default) + Q3 (skip only complete) in the header. Diagnostics via `process.stderr.write`. Add the six behaviors to the test.
  </action>
  <acceptance_criteria>
    - `lib/experiments/experiment-runner.mjs exports async function runMatrix(`.
    - Source assertion: the done-set filter is `terminal_state === 'complete'` (Q3) and keys off the composite `task_id`.
    - Source assertion: cells are awaited sequentially — `grep -n "Promise.all\|Promise.allSettled" lib/experiments/experiment-runner.mjs` returns nothing for the cell loop (D-09).
    - Source assertion: the copilot skip path passes `--skip-reason` and the string `copilot-headless-unsupported`, and does NOT call the agent launch seam.
    - `node --test tests/experiments/experiment-runner.test.mjs` passes all six behaviors incl. resume-skip, retry-on-timeout, probe-fail skip-Run, and best-effort mastra failure.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/experiments/experiment-runner.test.mjs</automated>
  </verify>
  <done>runMatrix drives the full matrix sequentially, resumes by skipping only completed cells, gates copilot on the cached probe, and records probe-skips and best-effort agent failures — exactly one Run per attempted cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → agent child process | Untrusted autonomous agent spawned with sandbox cwd/env |
| agent → sandbox worktree + sandbox .data | Agent edits + proxy token/route rows confined to the isolated cell |
| agent output → judge/score | Agent-produced files consumed as data by the inline scorer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03-01 | Tampering | Command injection via goal/agent/model/test_command into spawn | mitigate | Fixed-argv `spawn` arrays only (argvForAgent from 78-02); measurement-start/stop shelled as fixed argv; test_command already SHELL_META_RE-guarded upstream; no `shell:true` (acceptance grep). |
| T-78-03-02 | Elevation of Privilege | Autonomous agent (`--allow-all-tools`/`acceptEdits`) escaping the sandbox | mitigate | Assert spawn `cwd === worktree` and `env.LLM_PROXY_DATA_DIR === sandboxDataDir` from restoreForCell (inPlace:false, zero blast radius, T-77-08); NEVER the repo root — Task 1 acceptance + Task 2 behavior. |
| T-78-03-03 | Denial of Service | Hung/rogue agent stalling the matrix | mitigate | Async `spawn` + 20-min SIGTERM→SIGKILL wall-clock timer (D-06); sequential execution bounds concurrent damage. |
| T-78-03-04 | Information Disclosure | Token/route rows leaking into the live KB | mitigate | `LLM_PROXY_DATA_DIR`=sandbox routes all proxy rows into the cell `.data`; the experiment Run store stays repo-global but is append-only via shipped writeRun. |
| T-78-03-05 | Repudiation | Timeout/abort/probe-skip silently dropped | mitigate | measurement-stop in a `finally` on every terminal state; probe-fail writes an explicit skip-Run (RUN-03/04 "never silently absent"). |
| T-78-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (in-repo `.mjs` + already-installed agent CLIs); slopcheck N/A. |
</threat_model>

<verification>
- `node --test tests/experiments/experiment-runner.test.mjs` green (terminal-state machine, per-cell wiring, resume loop, probe gate, skip-Run, best-effort failure).
- `grep -rn "shell: true\|Promise.all\|spawnSync" lib/experiments/experiment-runner.mjs` shows no shell launch, no parallel cell loop, no sync agent spawn.
- `node --test tests/experiments` (module suite) green — no regression.
</verification>

<success_criteria>
- runMatrix produces exactly one Run per attempted variant×repeat cell, tagged with its terminal state (RUN-02/03).
- Copilot cells are probe-gated; failures land a recorded skip-Run (RUN-04).
- Re-running the matrix skips completed cells and retries timeout/abort cells (D-10/Q3); execution is strictly sequential (D-09).
</success_criteria>

<output>
Create `.planning/phases/78-autonomous-cross-agent-runner/78-03-SUMMARY.md` when done.
</output>
