---
phase: 78-autonomous-cross-agent-runner
plan: 05
type: execute
wave: 4
depends_on: [78-04]
files_modified:
  - config/experiments/smoke-cross-agent.yaml
autonomous: false
requirements: [RUN-02, RUN-03, RUN-04]
user_setup: []

must_haves:
  truths:
    - "A real 2-agent (claude + opencode) matrix completes end-to-end unattended and lands exactly one scored Run per cell (SC#4, D-12)"
    - "Each cell's agent edits landed in the isolated sandbox worktree — the live checkout/KB was never touched (D-02)"
    - "The Copilot RUN-04 probe ran and its result was recorded (pass→ran, fail→skip-Run with reason) — never silently absent"
    - "A Mastra cell that cannot be driven headlessly lands a recorded skip, not a stall (D-12 best-effort)"
  artifacts:
    - path: "config/experiments/smoke-cross-agent.yaml"
      provides: "a minimal real 2-agent (+copilot env:default, +mastra best-effort) smoke spec with a trivial goal + fast test_command"
      contains: "goal_sentence"
  key_links:
    - from: "scripts/experiment-run.mjs --spec config/experiments/smoke-cross-agent.yaml"
      to: ".data/experiments Run KB"
      via: "live matrix run → readRuns"
      pattern: "readRuns"
---

<objective>
Prove the phase end-to-end with a LIVE cross-agent smoke run (the RESEARCH phase gate). Author a minimal real spec with a trivial goal and drive it through `scripts/experiment-run.mjs` against the two REQUIRED headless-drivable CLIs (claude + opencode, D-12), plus the RUN-04 Copilot probe and a best-effort Mastra cell. This exercises the real spawn/timer/inline-score/skip paths that the unit + integration tests stub, and confirms zero blast radius against the live checkout.

This plan is `autonomous: false` — it requires the rapid-llm-proxy up, consumes real tokens, is nondeterministic, and ends in a human verification of the outcome.

Purpose: live acceptance of RUN-02/03/04 + SC#4 on real agents.
Output: `config/experiments/smoke-cross-agent.yaml` + a verified live Run set + operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-autonomous-cross-agent-runner/78-CONTEXT.md
@.planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md
@config/experiments/example-experiment.yaml
@scripts/experiment-run.mjs

<interfaces>
<!-- Phase-77 spec shape (config/experiments/example-experiment.yaml) — axes over {agent,model,framework,env} × repeats:N. -->
<!-- Copilot authored under env: default so Phase-77 validateCells passes (Q1); the RUN-04 probe alone gates it. -->
<!-- Preflight (RESEARCH Runtime State): rapid-llm-proxy :12435 up; claude/opencode installed; mastra needs prior OAuth. -->
  CLI: node scripts/experiment-run.mjs --spec config/experiments/smoke-cross-agent.yaml
  Verify: openExperimentStore + readRuns → one Run per variant×repeat; each with terminal_state + agent + score.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the smoke spec and drive the live cross-agent matrix</name>
  <files>config/experiments/smoke-cross-agent.yaml</files>
  <read_first>
    - config/experiments/example-experiment.yaml (the Phase-77 spec shape to mirror — axes, repeats, test_command)
    - lib/experiments/experiment-spec.mjs:65-71,211 (UNSUPPORTED_COMBINATIONS + resolveExperimentSpec — why copilot must be authored env:default, Q1)
    - scripts/experiment-run.mjs (78-04 — the CLI being driven)
    - .planning/phases/78-autonomous-cross-agent-runner/78-RESEARCH.md (Sampling Rate + Environment Availability — preflight the proxy + agents)
  </read_first>
  <action>
    Author `config/experiments/smoke-cross-agent.yaml`: a trivial goal_sentence (e.g. "Create a file HELLO.txt containing the single word hello."), `repeats: 1`, axes covering agent ∈ {claude, opencode} (the two REQUIRED CLIs, D-12) plus one copilot cell authored with `env: default` (so resolveExperimentSpec passes — Q1) and one mastra cell (best-effort, D-12), a single model per agent, and a fast fixed-argv `test_command` (e.g. `node --test tests/experiments` or a trivial assertion) per D-04/D-05. Preflight: confirm rapid-llm-proxy `:12435` is up and `claude`/`opencode` resolve on PATH. Run `node scripts/experiment-run.mjs --spec config/experiments/smoke-cross-agent.yaml` and capture the per-cell summary + `readRuns` output. Do NOT use `--in-place`; the runner is sandbox-only by construction (D-02).
  </action>
  <acceptance_criteria>
    - `config/experiments/smoke-cross-agent.yaml` resolves cleanly via `resolveExperimentSpec` (no validation throw) and includes claude + opencode cells.
    - The live run lands a Run for every attempted cell (claude/opencode complete; copilot per probe result; mastra complete-or-recorded-skip) — `readRuns` count == attempted cells.
    - `git status` in the LIVE repo shows no HELLO.txt and no agent edits in the working tree (edits confined to the sandbox worktree — D-02).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/experiment-run.mjs --spec config/experiments/smoke-cross-agent.yaml</automated>
  </verify>
  <done>A real 2-agent+ matrix ran unattended and produced one Run per cell in the experiment KB without touching the live checkout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A live cross-agent smoke matrix (config/experiments/smoke-cross-agent.yaml) driven through scripts/experiment-run.mjs: claude + opencode required end-to-end, a Copilot RUN-04 probe, and a best-effort Mastra cell — each landing exactly one scored Run tagged with its terminal state.
  </what-built>
  <how-to-verify>
    1. Review the per-cell summary printed by the run — confirm each attempted variant×repeat cell has one outcome (complete / timeout / abort / skipped).
    2. Query the Run KB: `node -e "import('./lib/experiments/store.mjs').then(async m=>{const s=await m.openExperimentStore();const q=await import('./lib/experiments/query.mjs');const r=await q.readRuns(s,{includePending:true});process.stderr.write(JSON.stringify(r.map(x=>({task_id:x.task_id,agent:x.agent,variant:x.variant,repeat:x.repeat,terminal_state:x.terminal_state,skip_reason:x.skip_reason})),null,2));await s.close();})"` — confirm exactly one row per cell, claude+opencode terminal_state='complete', and the copilot row is either 'complete' OR carries skip_reason='copilot-headless-unsupported' (never absent).
    3. Confirm `git status` in the live repo is clean of agent edits (blast radius = zero, D-02).
    4. Confirm the mastra cell is present as complete OR a recorded skip (best-effort, D-12) — not a stall.
  </how-to-verify>
  <resume-signal>Type "approved" if one scored Run landed per cell with terminal states recorded and the live tree is clean, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live run → real agent CLIs | Real autonomous agents spawned against a trivial goal in isolated sandboxes |
| agents → live checkout | Must NOT be crossed — edits confined to the sandbox worktree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-05-01 | Elevation of Privilege | Real agent (`--allow-all-tools`/`acceptEdits`) editing the live tree | mitigate | Runner is sandbox-only (restoreForCell inPlace:false, D-02); human-verify step 3 asserts `git status` clean — a dirty live tree FAILS the checkpoint. |
| T-78-05-02 | Denial of Service | A real agent hanging | mitigate | 20-min SIGTERM→SIGKILL timer (78-03); the human-verify confirms a hung cell recorded 'timeout', not a stall. |
| T-78-05-03 | Repudiation | Copilot/Mastra silently absent | mitigate | Human-verify step 2/4 asserts a recorded row (complete or skip_reason) for every cell (RUN-04/D-12). |
| T-78-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (a YAML fixture + already-installed CLIs); slopcheck N/A. |
</threat_model>

<verification>
- The live smoke run lands one scored Run per attempted cell; claude+opencode complete end-to-end (SC#4, D-12).
- Copilot probe result recorded; Mastra complete-or-recorded-skip; live checkout clean.
- Operator sign-off on the human-verify checkpoint.
</verification>

<success_criteria>
- A full matrix across ≥2 agents completes end-to-end unattended and lands exactly one Run per variant×repeat cell (SC#4).
- Timeouts/aborts/probe-skips are recorded, never dropped or silently absent (RUN-03/04).
- Zero blast radius on the live checkout (D-02).
</success_criteria>

<output>
Create `.planning/phases/78-autonomous-cross-agent-runner/78-05-SUMMARY.md` when done.
</output>
