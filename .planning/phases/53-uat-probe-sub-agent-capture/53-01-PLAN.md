---
phase: 53-uat-probe-sub-agent-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/uat-probes/2026-05-27-sub-agent-capture-probe.md
autonomous: true
requirements: []
gap_closure: false
tags: [throwaway, uat, sub-agent-capture, phase-51-closure]

must_haves:
  truths:
    - "Executor sub-agent registers in live-claude watcher within 5 seconds of spawn"
    - "/health/state.sub_agent_capture.live_registrations.claude.running becomes >= 1 during execution"
    - "At least one observation row with source='sub-agent' is written within 15 minutes of wave start (AC #3)"
    - "An LSL file matching `_S[0-9]+-` appears under `.specstory/history/2026/05/`"
    - "Marker file `.planning/uat-probes/2026-05-27-sub-agent-capture-probe.md` is created"
  artifacts:
    - path: .planning/uat-probes/2026-05-27-sub-agent-capture-probe.md
      provides: "Throwaway marker proving the probe phase executed; archived/removed after Phase 51 closes"
  key_links: []
---

<objective>
Throwaway UAT probe phase. Sole purpose: trigger ONE executor sub-agent spawn via `/gsd-execute-phase 53` so the live-claude FSEvents watcher (Plan 51-07) can register it, the writer path (Plan 51-14) can emit observations with `source='sub-agent'`, and the LSL parity writer (Plan 51-06) can land a `_S<slot>-<idx>-<hash>.md` file under `.specstory/history/`. This closes Phase 51 AC #3 (live-tier observation parity ≤15 min) by demonstrating the path end-to-end on a fresh, clean wave with the post-CR-fix code on main.

The plan does no real engineering work — it writes a tiny marker file. The marker file's content is irrelevant; what matters is the SIDE EFFECTS in the live-logging system. After Phase 51 closes, this entire phase directory (`.planning/phases/53-uat-probe-sub-agent-capture/`) plus the marker file may be removed or archived.

Per HUMAN-UAT.md line 61 (lesson from 2026-05-27 incident): the operator MUST run this from a SEPARATE Claude Code session, NOT nested inside the current `/gsd-execute-phase 51` session.
</objective>

<tasks>

### Task 1 — Write the throwaway marker file

**Description:** Create `.planning/uat-probes/2026-05-27-sub-agent-capture-probe.md` containing a 5-line note documenting the probe ran. This is the entirety of the task.

**Files modified:**
- `.planning/uat-probes/2026-05-27-sub-agent-capture-probe.md` (created)

**Steps:**
1. Ensure `.planning/uat-probes/` directory exists (it should already; mkdir -p is fine).
2. Write the marker file with the following content:
   ```markdown
   # Phase 51 Sub-Agent Capture UAT Probe

   This file proves that on 2026-05-27, a throwaway `/gsd-execute-phase 53` was
   driven by the operator to exercise the live-claude FSEvents watcher post
   CR-01..CR-04 fixes. The executor sub-agent that wrote this file was
   detected by `lib/lsl/live/claude-fs-watch.mjs` and its exchanges produced
   observations with `metadata.source='sub-agent'` (NOT `-backfill`), closing
   Phase 51 AC #3.

   Safe to delete after Phase 51 closes.
   ```
3. Commit the marker file with message: `feat(53-01): UAT probe — create sub-agent capture marker`.

**Acceptance:**
- `.planning/uat-probes/2026-05-27-sub-agent-capture-probe.md` exists and contains the expected content.
- A single commit on main with the expected message body.

</tasks>

<success_criteria>
- [ ] Marker file created with the documented content
- [ ] Task 1 committed atomically
- [ ] SUMMARY.md written and committed
- [ ] No other files touched (no source code, no test changes, no shared planning artifacts beyond SUMMARY + the marker)
</success_criteria>
