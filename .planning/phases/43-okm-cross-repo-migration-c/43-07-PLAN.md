---
phase: 43-okm-cross-repo-migration-c
plan: 07
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
  - 43-03
  - 43-04
  - 43-05
  - 43-06
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/migrate-json-replay.test.ts
autonomous: true
requirements:
  - INT-03
user_setup: []

must_haves:
  truths:
    - "`scripts/migrate-okm-json-to-kmcore.mjs` reads `.data/exports/{general,kpifw,raas}.json` (the authoritative source per OKM CLAUDE.md 'KB Persistence Rules'), ingests every entity + relation through km-core's GraphKMStore, and writes to a NEW km-core LevelDB at `.data/leveldb-kmcore/` (NOT clobbering the legacy `.data/leveldb/` — Plan 08 atomically swaps them)."
    - "Every entity migrated carries a top-level `legacyId: { system: 'C', id: <existing-okm-id> }` stamp (D-G4.1 explicit; mirrors Phase 42 D-54 stamping). Plan 08's cleanup grep gate verifies."
    - "Per-domain bucketing preserved: entities from `general.json` get `metadata.subsystem: 'general'`, from `kpifw.json` get `'kpifw'`, from `raas.json` get `'raas'`. Drives km-core's per-domain export bucketing on the new side."
    - "Script is idempotent: re-running produces the same final km-core LevelDB state (no duplicates, no errors). Acceptance grep: a second run reports `merged=0, errors=0`."
    - "Script honors the CLAUDE.md km-core CLI rule: constructs GraphKMStore with explicit `ontologyDir` resolved via `import.meta.resolve('@fwornle/km-core')` + walk up to package root. Grep gate: `grep -c 'ontologyDir' migrate-okm-json-to-kmcore.mjs` returns >= 1."
    - "Post-migration entity count MATCHES pre-migration count from the three JSON files (allow up to 5% error budget per Phase 42 Plan 5 precedent; abort if exceeded)."
    - "Integration test `tests/integration/migrate-json-replay.test.ts` proves the script works against a fixture-sized export (NOT production data) — covers happy path, idempotence, and the 5% error budget guard."
  artifacts:
    - path: "scripts/migrate-okm-json-to-kmcore.mjs"
      provides: "One-shot operator-driven migration; flags --dry-run, --source, --target, --batch-size, --resume, --run-id"
    - path: "tests/integration/migrate-json-replay.test.ts"
      provides: "Vitest integration test exercising the migration with a fixture export"
  key_links:
    - from: "migrate-okm-json-to-kmcore.mjs"
      to: "km-core GraphKMStore"
      via: "new GraphKMStore({ ontologyDir, dataDir, exportDir, debounceMs })"
      pattern: "new GraphKMStore"
    - from: "migrate-okm-json-to-kmcore.mjs"
      to: "every entity gets legacyId stamp"
      via: "entity.legacyId = { system: 'C', id: existing }"
      pattern: "legacyId.*system.*['\"]C['\"]"
    - from: "Plan 08 final cleanup"
      to: "the migrated .data/leveldb-kmcore/ produced by this plan"
      via: "atomic dir swap"
      pattern: "leveldb-kmcore"
---

<objective>
D-G4.1 — JSON-replay one-shot migration. The legacy LevelDB at `.data/leveldb/` is **NOT** deleted in this plan (Plan 08 owns the atomic swap + backup); this plan strictly populates a NEW km-core LevelDB at `.data/leveldb-kmcore/` from the git-tracked JSON exports.

**Why JSON-replay over in-place LevelDB rewrite (D-G4.1):**
- OKM's git-tracked `.data/exports/{general,kpifw,raas}.json` are AUTHORITATIVE per OKM's CLAUDE.md "KB Persistence Rules" — fresh environments restore from JSON, not from LevelDB.
- Aligns with km-core's Phase 37 D-22 LevelDB-empty → JSON-fallback semantics (defense in depth; D-G4.3).
- Avoids building a Phase-42-D-54-style key-shape rewriter (OKM's LevelDB schema differs; JSON-replay sidesteps the format mismatch entirely).

**Per Phase 42 Plan 5 precedent:**
- Script structure + flag set + error budget + provenance stamping pattern.
- Idempotency contract: legacyId existence check before write (mirrors Phase 42's UUIDv7 check).
- 5% error budget: aborts if `errors.length / totalEntities > 0.05`.

**Script does NOT replace the live LevelDB.** Plan 08 owns:
- Stop OKM container (if applicable; OKM may run locally not in Docker).
- `mv .data/leveldb .data/leveldb.pre-43-backup && mv .data/leveldb-kmcore .data/leveldb`
- Restart + verify.

This plan ends with: BOTH `.data/leveldb/` (legacy) AND `.data/leveldb-kmcore/` (new) coexist; OKM still runs on legacy because `OKB_STORE_BACKEND=default` is still in place (Plan 08 flips it).

Purpose: Materialize the canonical-shape km-core dataset before the cutover so Plan 08's swap is data-equivalent.

Output: 1 new script + 1 new integration test; `.data/leveldb-kmcore/` populated locally (the LevelDB dir itself is NOT committed — only the script that produces it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md

<interfaces>
<!-- Phase 42 Plan 5 template (PATTERNS.md §scripts/migrate-okm-json-to-kmcore.mjs) -->

Property mapping (Phase 42 → Phase 43):

| Property | Phase 42 Plan 5 | Phase 43 (OKM-side) |
|---|---|---|
| Input format | LevelDB at .data/knowledge-graph/ | JSON files at .data/exports/{general,kpifw,raas}.json |
| Output | km-core LevelDB (new dataDir) | km-core LevelDB at .data/leveldb-kmcore/ |
| Idempotency | required | required |
| legacyId stamp | { system: 'B', id: <existing-id> } | { system: 'C', id: <existing-id> } |
| metadata.subsystem | 'wave-analysis' | 'general' \| 'kpifw' \| 'raas' (per-file) |
| Error budget | 5% | same |
| Output flags | --dry-run, --batch-size=N, --resume, --source, --target | same |
| Provenance stamp | { provider: 'phase-42-migration', model: 'b-to-km-core', runId, timestamp } | { provider: 'phase-43-migration', model: 'okm-to-km-core', runId, timestamp } |
| Run discipline | one-shot operator-driven (not CI) | same |

<!-- km-core construction per CLAUDE.md (PATTERNS.md §LLM-Proxy + km-core Construction) -->

```javascript
import { fileURLToPath } from 'url';
import path from 'path';
import fs from 'fs/promises';

const kmCoreEntry = import.meta.resolve('@fwornle/km-core');
const kmCorePath = fileURLToPath(kmCoreEntry);
let kmCoreRoot = path.dirname(kmCorePath);
while (kmCoreRoot !== '/') {
  try {
    await fs.access(path.join(kmCoreRoot, 'package.json'));
    break;
  } catch {
    kmCoreRoot = path.dirname(kmCoreRoot);
  }
}
const ontologyDir = path.join(kmCoreRoot, 'config', 'ontology');
```

<!-- Expected JSON export shape (per OKM CLAUDE.md "KB Persistence Rules") -->

Each .data/exports/{domain}.json is a Graphology serialized export:
```json
{
  "options": { "type": "directed", "multi": true, "allowSelfLoops": true },
  "attributes": {},
  "nodes": [
    {
      "key": "evidence:<uuid>",   // OKM uses layer:id composite keys
      "attributes": {
        "id": "<uuid>",
        "name": "...",
        "entityType": "...",
        "layer": "evidence",
        "description": "...",
        "metadata": {...},
        ...
      }
    },
    ...
  ],
  "edges": [
    { "key": "...", "source": "...", "target": "...", "attributes": {...} },
    ...
  ]
}
```

The script unwraps `nodes[].attributes` into km-core Entity objects + maps `edges` to km-core Relations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the migration script with full flag handling + km-core construction</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs (Phase 42 Plan 5 — the structural template; copy header style, flag parsing, error-budget logic, provenance stamping)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-05-PLAN.md (Phase 42 Plan 5 PLAN — confirms the script properties + idempotency contract)
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md (§`scripts/migrate-okm-json-to-kmcore.mjs` — full property table + the ontologyDir resolver block to copy verbatim)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/exports/general.json (peek first 100 lines — confirm the JSON shape matches the assumed Graphology export format; if not, adapt the parser)
    - /Users/Q284340/Agentic/coding/CLAUDE.md ("Mandatory Rules" — km-core CLI ontologyDir rule; this script MUST honor it)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/km-store-adapter.ts (existing strangler bridge — peek to confirm how the running OKM constructs GraphKMStore today; mirror that)
  </read_first>
  <action>
    1. Read the first ~100 lines of EACH actual export to confirm shape:
       ```bash
       head -100 /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/exports/general.json
       head -100 /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/exports/kpifw.json
       head -100 /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/exports/raas.json
       ```
       Cross-reference against the assumed Graphology export shape in <interfaces>. Note any deltas (e.g., different field names, missing metadata fields, different relation key format). Adapt the script parser.

    2. Create `scripts/migrate-okm-json-to-kmcore.mjs` with `#!/usr/bin/env node` shebang + `chmod +x`. Structure:

       ```
       // CLI flags via process.argv parsing (no external dep) — mirrors Phase 42 Plan 5
       --source=<file-glob>       default: .data/exports/{general,kpifw,raas}.json
       --target=<dir>             default: .data/leveldb-kmcore
       --batch-size=<N>           default: 100
       --dry-run                  skip writes; only report would-be actions
       --resume                   skip entities whose legacyId already exists in target
       --run-id=<string>          default: `okm-mig-${Date.now()}`
       --help
       ```

       Diagnostic output via `process.stderr.write` (OKM scripts/ files MAY use console.* per OKM CLAUDE.md, but `process.stderr.write` plays nicer with operator-driven JSON capture).

    3. Bootstrap km-core per the CLAUDE.md rule (block from <interfaces>). Construct:
       ```
       const store = new GraphKMStore({
         ontologyDir,                              // resolved per CLAUDE.md rule
         dataDir: targetDir,                       // .data/leveldb-kmcore
         exportDir: '.data/exports-kmcore',       // separate from .data/exports/ to avoid clobbering source during migration
         debounceMs: 0,                            // immediate flush; one-shot script
       });
       await store.open();
       ```
       Note: the export dir is separate so the source JSON files stay untouched during migration. Plan 08 swaps both dirs (leveldb + exports) atomically.

    4. For each source file (general/kpifw/raas), parse the Graphology export, then for each `node`:
       - Extract `attrs = node.attributes`.
       - Compute the new km-core entity shape:
         ```
         const entity = {
           id: attrs.id,                           // PRESERVE OKM's existing UUID (do NOT mint a new one — that breaks legacyId integrity)
           name: attrs.name,
           entityType: attrs.entityType,
           ontologyClass: attrs.ontologyClass,    // may be undefined; km-core accepts
           layer: attrs.layer,                    // 'evidence' or 'pattern' — Plan 01 confirmed support
           description: attrs.description,
           createdAt: attrs.createdAt ?? <fixed default>,
           updatedAt: attrs.updatedAt ?? <fixed default>,
           metadata: {
             ...attrs.metadata,
             subsystem: domain,                    // 'general' | 'kpifw' | 'raas'
           },
           validFrom: attrs.validFrom,
           validUntil: attrs.validUntil,
           supersedes: attrs.supersedes,
           legacyId: { system: 'C', id: attrs.id }, // D-G4.1 stamp
         };
         ```
       - Idempotency check: if `--resume` set, skip if `await store.getEntity(entity.id)` returns non-null AND `result.legacyId?.system === 'C'`.
       - Write: `await store.putEntity(entity, { skipOntologyCheck: true, provenance: { provider: 'phase-43-migration', model: 'okm-to-km-core', runId, timestamp: now } })`. skipOntologyCheck is true because the bulk-import path bypasses validation per Phase 37 D-19 trusted-path semantics (preserves OKM's existing classes even if any aren't in km-core's ontology yet).
       - Accumulate `migrated++` or `errors.push({ id, message })`.

    5. After all nodes from all files are written, process edges. For each edge:
       - `await store.addRelation({ type: attrs.type, from: srcId, to: tgtId, metadata: attrs.metadata ?? {} })`.
       - Track `relationsMigrated` + `relationErrors`.
       - **Edge key delta** (PATTERNS.md §intelligence/*.ts node-key delta): OKM may use `${layer}:${id}` composite keys for nodes; edges reference nodes by composite key. STRIP the layer prefix when computing `srcId`/`tgtId` for km-core (e.g., `srcId = source.includes(':') ? source.split(':')[1] : source`). km-core uses bare EntityId. Verify by checking the actual export edge keys in step 1.

    6. Error budget: if `errors.length / (totalEntities + totalRelations) > 0.05`, abort + non-zero exit + JSON summary line:
       ```
       process.stderr.write(JSON.stringify({ aborted: true, reason: 'error-budget-exceeded', errors: errors.slice(0, 20), totalEntities, totalRelations }) + '\n');
       process.exit(1);
       ```

    7. On success: print a JSON summary line to STDOUT (operator can pipe to jq):
       ```
       console.log(JSON.stringify({
         status: 'ok',
         runId,
         totalSourceFiles: files.length,
         totalEntities,
         entitiesMigrated,
         entitiesSkipped,           // --resume skipped these
         totalRelations,
         relationsMigrated,
         relationErrors: relationErrors.length,
         errorBudget: errors.length / (totalEntities + totalRelations),
         targetDir,
         elapsedMs: Date.now() - startMs,
       }));
       ```

    8. Final `await store.close()` (flushes any pending writes).

    9. Lint: `node --check scripts/migrate-okm-json-to-kmcore.mjs` (syntax). MUST exit 0.

    10. **DRY-RUN against production exports** to validate parsing without modifying anything:
        ```
        cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
        node scripts/migrate-okm-json-to-kmcore.mjs --dry-run
        ```
        Capture the JSON summary. `errorBudget` MUST be 0.0 (or < 0.05). Record `totalEntities` + `totalRelations` from each source file in SUMMARY — this becomes Plan 08's post-cutover count target.

    11. **DO NOT** run a non-dry-run against production in this task. Task 2's integration test exercises the write path against a fixture-sized export.
  </action>
  <verify>
    <automated>test -x /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs &amp;&amp; node --check /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs &amp;&amp; grep -c 'ontologyDir' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs</automated>
  </verify>
  <done>Script exists, executable, syntax-clean, honors km-core CLI ontologyDir rule, dry-run reports zero errors against production exports.</done>
  <acceptance_criteria>
    - File: `test -x /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs` exits 0.
    - Syntax: `node --check /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs` exits 0.
    - km-core CLI rule (CLAUDE.md): `grep -c 'ontologyDir' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs` returns >= 1.
    - legacyId stamp present: `grep -cE "legacyId.*system.*['\"]C['\"]" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs` returns >= 1.
    - Dry-run behavior: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && node scripts/migrate-okm-json-to-kmcore.mjs --dry-run | tail -1 | node -e "process.exit(JSON.parse(require('fs').readFileSync(0,'utf8')).errorBudget < 0.05 ? 0 : 1)"` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integration test against a fixture-sized export (happy path + idempotence + error budget)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/migrate-json-replay.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/km-core-backend.test.ts (bootstrap pattern reuse)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/fixtures/pre-migration/seed-dataset.json (Plan 06 deliverable — can be reused or a NEW migration-shaped fixture is authored here)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/migrate-okm-json-to-kmcore.mjs (the SUT)
  </read_first>
  <action>
    1. Create `tests/integration/migrate-json-replay.test.ts`. Test cases:

       a. **Happy path** — Construct an in-memory Graphology-shape JSON in a tmpdir (or reuse seed-dataset.json with a Graphology wrapper), 10 entities + 5 relations, all valid. Spawn the migration script via `child_process.spawn('node', ['scripts/migrate-okm-json-to-kmcore.mjs', '--source=' + fixturePath, '--target=' + tmpTargetDir])`. Capture stdout JSON; assert `status === 'ok'`, `entitiesMigrated === 10`, `relationsMigrated === 5`, `errorBudget === 0`.

       b. **Idempotence** — Run the script TWICE against the same fixture; second run with `--resume`. Assert second run reports `entitiesSkipped === 10` (or equivalent — depends on the resume implementation).

       c. **legacyId stamp** — After migration, open the target km-core LevelDB, iterate, assert EVERY entity has `legacyId.system === 'C'` and `legacyId.id` matches the original OKM id.

       d. **Error budget** — Construct a fixture with deliberately-broken entities (e.g., missing `name` field); ensure the script aborts cleanly when error budget exceeds 5%, exits non-zero, prints `aborted: true` to stderr.

       e. **Per-domain bucketing** — Construct a multi-domain fixture (2 entities per domain × 3 domains = 6 total); verify each migrated entity has `metadata.subsystem` set to the correct domain.

    2. Run: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test -- migrate-json-replay.test.ts`. All cases pass.

    3. Full suite: `npm test`. No regression.
  </action>
  <verify>
    <automated>test -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/migrate-json-replay.test.ts &amp;&amp; cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management &amp;&amp; npm test -- migrate-json-replay.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Integration test covers happy path, idempotence, legacyId stamp, error budget, per-domain bucketing; all pass.</done>
  <acceptance_criteria>
    - File: `test -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/migrate-json-replay.test.ts` exits 0.
    - Test: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test -- migrate-json-replay.test.ts` exits 0; pass count >= 5.
    - Full-suite green: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test` exits 0; monotone non-decreasing pass count vs Plan 06 baseline.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Execute the migration against PRODUCTION exports into .data/leveldb-kmcore/ + commit script + test</name>
  <files>(production data migration — produces local artifacts at .data/leveldb-kmcore/ which are NOT committed; commit is script + test only)</files>
  <read_first>
    - Output of Task 1 dry-run (the JSON summary — `totalEntities` is the count Plan 08 will verify)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/CLAUDE.md (NO destructive operations to .data/leveldb/ without operator approval; this task ONLY writes to .data/leveldb-kmcore/ which is a NEW dir)
  </read_first>
  <action>
    1. Pre-flight: verify .data/leveldb-kmcore/ does NOT yet exist (this is a NEW dir; if it exists from a prior plan run, delete it after operator confirmation — surface in SUMMARY):
       ```
       test ! -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore
       ```
       If exists, prompt — do NOT silently delete.

    2. Run the PRODUCTION migration (NO --dry-run):
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       node scripts/migrate-okm-json-to-kmcore.mjs --run-id="phase-43-prod-$(date -u +%Y%m%dT%H%M%SZ)" | tee /tmp/43-07-migrate-output.json
       ```
       Capture the stdout JSON line. Assert `status === 'ok'`, `errorBudget < 0.05`. If any failure, do NOT proceed to commit.

    3. Verify target populated:
       ```
       ls /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore/ | wc -l
       ```
       MUST be > 5 (LevelDB has multiple sst/log files).

    4. Verify entity count match with a quick inline node:
       ```
       node -e "
       import('@fwornle/km-core').then(async m => {
         const path = await import('path');
         const fs = await import('fs/promises');
         // Resolve ontologyDir per CLAUDE.md rule (inline)
         const kmCoreEntry = import.meta.resolve('@fwornle/km-core');
         const { fileURLToPath } = await import('url');
         let kmCoreRoot = path.dirname(fileURLToPath(kmCoreEntry));
         while (kmCoreRoot !== '/') {
           try { await fs.access(path.join(kmCoreRoot, 'package.json')); break; }
           catch { kmCoreRoot = path.dirname(kmCoreRoot); }
         }
         const ontologyDir = path.join(kmCoreRoot, 'config', 'ontology');
         const store = new m.GraphKMStore({ ontologyDir, dataDir: '.data/leveldb-kmcore' });
         await store.open();
         let count = 0; let withLegacyC = 0;
         for await (const e of store.iterate()) {
           count++;
           if (e.legacyId?.system === 'C') withLegacyC++;
         }
         console.log(JSON.stringify({ count, withLegacyC }));
         await store.close();
       });
       "
       ```
       MUST: `count` matches Task 1 dry-run's `totalEntities` ± 5%; `withLegacyC === count` (every entity stamped).

    5. Commit script + test inside OKM:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       git add scripts/migrate-okm-json-to-kmcore.mjs tests/integration/migrate-json-replay.test.ts
       git status                                    # confirm .data/leveldb-kmcore/ is NOT staged (it's a runtime artifact)
       git commit -m "$(cat <<'EOF'
feat(migration): JSON-replay one-shot migration script + integration test (Phase 43 D-G4.1)

Reads .data/exports/{general,kpifw,raas}.json (authoritative per OKM
CLAUDE.md), ingests every entity + relation through km-core's GraphKMStore,
writes to .data/leveldb-kmcore/. Mirrors Phase 42 Plan 5 pattern with
JSON-source vs LevelDB-source delta.

Every entity stamped with legacyId: { system: 'C', id: <existing> } per
D-G4.1. Per-domain metadata.subsystem preserved (general/kpifw/raas).

Idempotent: --resume flag skips entities whose legacyId already exists.
Error budget: 5% (mirrors Phase 42 Plan 5).

Plan 08 owns the cutover (.data/leveldb → .data/leveldb.pre-43-backup;
.data/leveldb-kmcore → .data/leveldb). This plan strictly populates the
new dir; the live OKM is still on the legacy path.

Production migration run: <Task-1-dry-run-totalEntities> entities,
<Task-1-totalRelations> relations, errorBudget < 0.05.
EOF
)"
       ```
       (Replace `<...>` with actual values from the production run before committing — or use a sed substitution.)

    6. Outer gitlink bump:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations
       git add integrations/operational-knowledge-management
       git commit -m "chore: bump OKM submodule — Phase 43 Plan 07 (JSON-replay migration)"
       ```

    7. Surface SHAs + production migration summary in SUMMARY.
  </action>
  <verify>
    <automated>test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore &amp;&amp; git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s | grep -q 'D-G4.1'</automated>
  </verify>
  <done>Production .data/leveldb-kmcore/ populated with ≥ Task-1-dry-run-count entities; every entity has legacyId.system='C'; script + test committed; outer gitlink bumped.</done>
  <acceptance_criteria>
    - Dir assertion: `test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore` exits 0.
    - File assertion: `ls /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore/ | wc -l` returns > 5.
    - Behavior: post-migration inline count check (from action step 4) reports `withLegacyC === count` (100% stamped) AND `count >= 0.95 * <pre-migration count>` (5% error budget honored).
    - Inner commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s` matches `D-G4.1`.
    - Outer commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations log -1 --format=%s` matches `Plan 07`.
    - Source dir untouched: `git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management status -s .data/exports/` returns no modified files (the migration READS .data/exports/, never writes there).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .data/exports/*.json (source) → km-core LevelDB (target) | One-shot, operator-driven; backup is the JSON sources themselves (untouched) |
| Idempotency contract | --resume must skip cleanly; otherwise double-write produces duplicates |
| Error budget | 5% threshold prevents silent partial migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07-01 | Tampering | source JSON corruption during migration | mitigate | Script READS source files only; never writes. Plan 08's atomic dir swap also preserves .data/exports/ untouched. |
| T-43-07-02 | Information Disclosure | legacyId leak of internal OKM ids | accept | OKM internal UUIDs become km-core legacyId.id values — fine for cross-system traceability; OKM is the only system that uses them. |
| T-43-07-03 | Denial of Service | partial migration leaves target in inconsistent state | mitigate | Target dir is NEW (.data/leveldb-kmcore); on error budget exceeded, exit non-zero so Plan 08 sees the failure before swapping. |
| T-43-07-04 | Repudiation | provenance stamps allow tracing of every migrated entity | mitigate | All stamps use fixed provider/model + the user-provided runId; SUMMARY records the exact runId for audit. |
| T-43-07-05 | Tampering | km-core writes without ontology validation | accept | skipOntologyCheck: true is required for bulk import (Phase 37 D-19 trusted-path semantics); OKM-specific classes may not all exist in km-core's ontology yet. Plan 09 re-embed pass + Plan 10 verification will surface any class-mismatch issues. |
</threat_model>

<verification>
- Script exists + executable + syntax-clean + honors km-core CLI rule.
- Dry-run against production exports reports zero errors.
- Integration test covers happy path / idempotence / legacyId / error budget / per-domain bucketing.
- Production migration produces .data/leveldb-kmcore/ with full entity count + 100% legacyId stamping.
- Inner + outer commits land; source JSON untouched.
</verification>

<success_criteria>
1. Migration script exists with full flag set + idempotency + error budget.
2. Integration test passes 5+ cases.
3. Production .data/leveldb-kmcore/ populated; entity count matches source ± 5%; 100% legacyId stamping.
4. .data/leveldb/ (legacy) and .data/exports/*.json untouched.
5. Commits land inside OKM + outer gitlink bumped.
</success_criteria>

<output>
Create `.planning/phases/43-okm-cross-repo-migration-c/43-07-SUMMARY.md` when done, recording:
- Production dry-run JSON summary (totalEntities + totalRelations per source file)
- Production migration runId + JSON summary
- Post-migration entity count + 100% legacyId stamping verification
- Integration test pass count (must be >= 5)
- Inner + outer commit SHAs
- Confirmation: .data/exports/ + .data/leveldb/ both UNTOUCHED
</output>
</content>
</invoke>