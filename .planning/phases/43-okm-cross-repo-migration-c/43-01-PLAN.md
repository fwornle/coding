---
phase: 43-okm-cross-repo-migration-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/km-core/src/types/entity.ts
  - /Users/Q284340/Agentic/km-core/tests/unit/entity-layer-field.test.ts
  - /Users/Q284340/Agentic/km-core/src/ontology/registry.ts
  - /Users/Q284340/Agentic/km-core/package.json
  - /Users/Q284340/Agentic/km-core/CHANGELOG.md
autonomous: true
requirements:
  - INT-03
user_setup: []

must_haves:
  truths:
    - "km-core canonical Entity type has `layer: 'evidence' | 'pattern'` available to OKM consumers (whether the field stays REQUIRED as-is, OR was made optional in this plan, the OKM-side imports of `Entity` compile cleanly with both layer values)."
    - "km-core OntologyRegistry exposes EVERY accessor OKM's local registry currently exposes (`isValidClass`, `getClass`, `getLoadedDomains`, `getAllClassNames`, `getClassesForPrompt`, `getDefaultLayer`) — confirmed by grep against ~/Agentic/km-core/src/ontology/registry.ts. Any missing accessor is ADDED in this plan with the same name + signature shape."
    - "A version tag is cut on km-core HEAD (e.g. v0.2.0) so Plan 02 has a stable SHA to pin the OKM submodule against."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/types/entity.ts"
      provides: "Canonical Entity type — verified to support D-G4.2 (`layer: 'evidence'|'pattern'`); no breaking change to A/B."
    - path: "/Users/Q284340/Agentic/km-core/tests/unit/entity-layer-field.test.ts"
      provides: "Round-trip test asserting that an entity with `layer:'pattern'` (OKM's evidence/pattern split) survives putEntity → iterate → first result has layer === 'pattern' verbatim."
    - path: "/Users/Q284340/Agentic/km-core/CHANGELOG.md"
      provides: "Version entry recording Phase 43 pre-req (layer field confirmation + any registry accessor additions); referenced by D-G1.4 commit message."
  key_links:
    - from: "OKM consumers (post-Plan-04+) importing `Entity` from `@fwornle/km-core`"
      to: "km-core Entity type with layer field"
      via: "Both 'evidence' and 'pattern' values are valid and round-trippable"
      pattern: "layer:\\s*['\"](evidence|pattern)['\"]"
    - from: "OKM consumers (post-Plan-04+) importing `OntologyRegistry` from `@fwornle/km-core/ontology`"
      to: "km-core registry's public methods"
      via: "Every accessor OKM's registry exposes is present on km-core's registry with matching signature"
      pattern: "(isValidClass|getClass|getLoadedDomains|getAllClassNames|getClassesForPrompt|getDefaultLayer)"
---

<objective>
Pre-req plan against km-core (independent repo at `~/Agentic/km-core/`) that unblocks every subsequent OKM plan. Two responsibilities:

  1. **Verify D-G4.2 `layer?: 'evidence'|'pattern'`.** PATTERNS.md flagged that km-core's `Entity.layer` is currently REQUIRED, not optional. Read `~/Agentic/km-core/src/types/entity.ts` line ~120 to confirm. Two outcomes:
     - **Outcome A (most likely per PATTERNS.md analysis):** The field already exists as required `layer: Layer` and the `Layer = 'evidence' | 'pattern'` union (line 27) is exactly the union OKM uses. D-G4.2's "add `layer?`" wording reflects OKM's local-Entity perspective — km-core needs **no schema change**. Just write the round-trip test that proves `layer: 'pattern'` works (OKM uses both values) and confirm the OKM consumer path compiles.
     - **Outcome B (only if Outcome A is wrong):** km-core lacks the field or rejects `'pattern'`. Then add it as **REQUIRED** (not optional — that would be a breaking change for B which assumes `'evidence'`) and gate every existing call site. **Do NOT make the field optional without an explicit RFC-style decision** — flag back to the operator before making a breaking change.
  2. **Verify km-core OntologyRegistry surface parity.** D-G2.2 deletes OKM's local `src/ontology/registry.ts` and re-imports `OntologyRegistry` from `@fwornle/km-core/ontology`. Plan 04 will do the swap. Before that, this plan grep-confirms every accessor OKM uses (`isValidClass`, `getClass`, `getLoadedDomains`, `getAllClassNames`, `getClassesForPrompt`, `getDefaultLayer`) exists on km-core's registry. If ANY is missing, ADD it here with the same name + signature shape. No call-site rewiring (that's Plan 04).

After both verifications, cut a version tag (`v0.2.0` if any change shipped, else just promote `v0.1.0`'s HEAD SHA) so Plan 02 has a stable pin point.

**Anti-shallow guard:** Read the actual km-core source. Do NOT assume PATTERNS.md's "likely interpretation" is correct without verifying. If the actual state differs, surface in SUMMARY.

Purpose: Unblock Plans 02-10 without making cross-repo PRs reactive.

Output: 1 km-core test file (always); 1 km-core registry edit (if missing accessors); 1 km-core CHANGELOG entry; 1 km-core version tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md

<interfaces>
<!-- km-core current state (verify against source before changing) -->

km-core Entity (`~/Agentic/km-core/src/types/entity.ts` lines 27, 113-173):
  - `export type Layer = 'evidence' | 'pattern'` at line 27 — the union OKM uses.
  - `Entity.layer: Layer` at line 120 — REQUIRED, not optional. This is the field PATTERNS.md flagged.
  - `Entity.embedding?: number[]` at line 172 — Phase 42 D-52 precedent for optional first-class field additions.

km-core OntologyRegistry (`~/Agentic/km-core/src/ontology/registry.ts`):
  - Accessors known to exist per Phase 38: `loadFromDisk()`, `reload()`, `classCatalog`, `domains`, `parentChainOf()`, `provenanceOf()` — these are the Phase 38 SC#4 surface.
  - Plan 38-03 SUMMARY mentions: registry has `registerClasses`, atomic reload, collision warnings.
  - **MUST GREP TO CONFIRM:** `isValidClass`, `getClass`, `getLoadedDomains`, `getAllClassNames`, `getClassesForPrompt`, `getDefaultLayer` — these are the names OKM's local registry uses (PATTERNS.md §`extractor.ts + deduplicator.ts` — import OntologyRegistry).

OKM's local registry (`~/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/registry.ts`) — the accessors that MUST be exposed by km-core:
  - `isValidClass(className: string): boolean`
  - `getClass(className: string): OntologyClass | undefined`
  - `getLoadedDomains(): string[]`
  - `getAllClassNames(): string[]`
  - `getClassesForPrompt(): string` (formatted text for LLM prompts)
  - `getDefaultLayer(className: string): Layer | undefined`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify km-core Entity.layer + write round-trip test for `layer:'pattern'`</name>
  <files>/Users/Q284340/Agentic/km-core/src/types/entity.ts, /Users/Q284340/Agentic/km-core/tests/unit/entity-layer-field.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (full file — confirm line 27 `Layer` union + line 120 `layer` REQUIRED, line 172 `embedding?` precedent for optional-add comment style)
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md (§`entity.ts` — the "Resolve this in the pre-req plan against km-core" guidance + the round-trip test pattern at lines 70-76)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-04-PLAN.md (the Phase 42 D-52 `embedding?` optional-field-extension pattern — use as template for any comment-block additions here)
  </read_first>
  <action>
    1. Open `/Users/Q284340/Agentic/km-core/src/types/entity.ts`. Confirm at line ~27 that `export type Layer = 'evidence' | 'pattern'` exists verbatim. Confirm at line ~120 that `Entity.layer: Layer` is REQUIRED (no `?`).

    2. **Decision branch (NO code change unless Outcome B):**
       - **Outcome A (NO schema change):** The field is REQUIRED with the union `'evidence'|'pattern'`. This satisfies D-G4.2 for OKM's purposes — OKM consumers will set `layer: 'evidence'` or `layer: 'pattern'` explicitly. NO edit to `entity.ts`. Document this in SUMMARY as "verified, no schema change needed; D-G4.2 wording was OKM-side perspective".
       - **Outcome B (extremely unlikely — only if line-120 inspection finds the field MISSING or restricted to one value):** STOP. Do NOT make a breaking change unilaterally. Write a `BLOCKER:` line to stderr describing the actual state and emit it in SUMMARY. The operator decides whether to make the field optional (breaks B) or accept Outcome A's "already there" path.

    3. Create `/Users/Q284340/Agentic/km-core/tests/unit/entity-layer-field.test.ts` with vitest. Use existing Phase 37/38 test files as templates (e.g., `~/Agentic/km-core/tests/unit/graph-store.test.ts` for the GraphKMStore setup boilerplate). Test contract:

       - Setup: `mkdtemp` for a tmpdir; instantiate `new GraphKMStore({ dataDir, exportDir, debounceMs: 0 })` (no ontologyDir needed since this test uses `skipOntologyCheck: true` for bulk shape verification).
       - **Test 1 — `layer:'evidence'` round-trip:** mint an Entity with `layer: 'evidence'`, all required fields populated (name, entityType, description, createdAt, updatedAt, metadata: {}), and a valid UUIDv7 id from `mintEntityId()`. Call `await store.putEntity(entity, { skipOntologyCheck: true, provenance: <test stamp> })`. Iterate via `for await (const e of store.iterate())`; assert `entities[0].layer === 'evidence'`.
       - **Test 2 — `layer:'pattern'` round-trip (the OKM use case):** Same as Test 1 but `layer: 'pattern'`. Assert `entities[0].layer === 'pattern'` verbatim. This is the critical OKM-side parity test.
       - **Test 3 — `layer:'pattern'` survives JSON export round-trip:** After Test 2's putEntity, force a flush via `await store.flush()` (or whatever the public flush API is named — grep `~/Agentic/km-core/src/store/GraphKMStore.ts` for `flush\|forceExport`). Read the resulting export JSON from `exportDir/general.json` (or whichever domain the test entity routes to). Assert the JSON contains the literal `"layer":"pattern"` substring.

       Diagnostic output via `process.stderr.write` (km-core convention per CODING repo's no-console-log).

    4. Run the test: `cd /Users/Q284340/Agentic/km-core && npm test -- entity-layer-field.test.ts`. MUST exit 0 with 3/3 passing. If any test fails, surface the failure mode in SUMMARY — likely a missing flush API or a JSON-export filename mismatch (recoverable in this same task).

    5. Run the full km-core test suite: `cd /Users/Q284340/Agentic/km-core && npm test`. MUST exit 0 with ZERO regression vs the pre-edit baseline (Phase 38-06 SUMMARY reports 56 tests / 56 passed). Capture the new total — should be 56 + 3 = 59.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core &amp;&amp; npm test -- entity-layer-field.test.ts 2>&amp;1 | grep -E "Tests +([0-9]+) passed"</automated>
  </verify>
  <done>Entity.layer field state is documented (Outcome A or B); new round-trip test file exists with 3 tests covering both layer values + JSON export; full km-core test suite still green.</done>
  <acceptance_criteria>
    - File assertion: `test -f /Users/Q284340/Agentic/km-core/tests/unit/entity-layer-field.test.ts` exits 0.
    - Test assertion: `cd /Users/Q284340/Agentic/km-core && npm test -- entity-layer-field.test.ts` exits 0 with 3/3 tests passing.
    - Regression assertion: `cd /Users/Q284340/Agentic/km-core && npm test` exits 0; total pass count >= 59 (56 Phase 38 baseline + 3 new).
    - Grep assertion (Outcome A — likely path — NO schema change): `grep -E "^\s*layer:\s*Layer;" /Users/Q284340/Agentic/km-core/src/types/entity.ts` returns exactly 1 match (the existing REQUIRED field, unchanged).
    - Behavior assertion: `grep -F '"layer":"pattern"' /Users/Q284340/Agentic/km-core/src/types/entity.ts` returns at least 0 matches (no source change required); if Outcome B took effect, this assertion is replaced by a documented breaking-change BLOCKER in SUMMARY.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Audit km-core OntologyRegistry surface for OKM-required accessors; ADD any missing</name>
  <files>/Users/Q284340/Agentic/km-core/src/ontology/registry.ts, /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (full file — list every public method/getter; capture each signature)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/registry.ts (full file, 90 lines — every accessor OKM currently exposes; their signatures are the required parity surface for Plan 04)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/extractor.ts (grep for `ontologyRegistry\.` — every call site is a name + signature OKM needs km-core to support)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts (grep for `ontologyRegistry\.` — additional call sites)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts (grep for `ontologyRegistry\.` — `/api/ontology/*` endpoint accessors)
    - /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts (Phase 38-06 test surface — append new tests here, do not create a parallel file)
  </read_first>
  <action>
    1. Grep all `ontologyRegistry\.<method>(...)` invocations across OKM source:

       ```bash
       grep -rEhn 'ontologyRegistry\.[a-zA-Z]+' \
         ~/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ \
         --include='*.ts' | sed -E 's/.*ontologyRegistry\.([a-zA-Z]+).*/\1/' | sort -u
       ```

       This produces the deduped accessor list OKM relies on. Expected names (from PATTERNS.md): `isValidClass`, `getClass`, `getLoadedDomains`, `getAllClassNames`, `getClassesForPrompt`, `getDefaultLayer`. There may be a few more — capture the full set.

    2. Grep km-core's OntologyRegistry for its current public surface:

       ```bash
       grep -En '^\s*(public\s+)?(async\s+)?[a-zA-Z]+\s*\(' \
         /Users/Q284340/Agentic/km-core/src/ontology/registry.ts \
         | grep -v 'private\s' | grep -v 'constructor' | grep -v 'registerClasses'
       ```

       (Filter out private members + the constructor + internal `registerClasses`.) Capture method names + signatures.

    3. **Diff the two lists.** For each name in OKM's required set that is MISSING from km-core's public surface, add a minimal implementation in `~/Agentic/km-core/src/ontology/registry.ts`. Use OKM's signature shape verbatim — the OKM analog is the source of truth. Each new method must:
       - Match OKM's parameter list + return type EXACTLY (so Plan 04 can do a single-line import swap with zero downstream call-site changes).
       - Operate against km-core's internal `classCatalog: Map<string, OntologyClass>` + `domains: Set<string>` (already exposed per Phase 38-03 SUMMARY).
       - Be implemented as a thin wrapper — no business logic beyond what OKM's analog does.

       Likely outcome (best-case): km-core already has `isValidClass`, `getClass`, and a `domains` getter. The missing pieces are likely `getLoadedDomains()` (km-core may expose `domains` as a `ReadonlySet`; add `getLoadedDomains(): string[]` returning `Array.from(this.domains)`), `getAllClassNames()` (returns `Array.from(this.classCatalog.keys())`), `getClassesForPrompt()` (format whatever OKM formats), `getDefaultLayer()` (lookup `class.defaultLayer`).

    4. For EACH new method added, append a vitest test to `/Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` (do NOT create a new test file — see Phase 38-06 SUMMARY for the existing structure):
       - Test: instantiate registry against an existing fixture (`tests/fixtures/ontology/coding-ontology.json` or upper.json — Phase 38-02 SUMMARY catalogs available fixtures).
       - Assert the new method returns the expected shape on a known input from the fixture.

    5. Compile + test: `cd /Users/Q284340/Agentic/km-core && npm run build && npm test`. BOTH must exit 0. Capture the new test count.

    6. Diff-and-document: emit a clear stderr block listing each accessor that was: (a) already present (no-op), (b) added with signature `<sig>`, (c) flagged as ambiguous. Plan 04's executor reads this from SUMMARY before doing the swap.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core &amp;&amp; npm run build &amp;&amp; npm test -- ontology-registry.test.ts 2>&amp;1 | grep -E "Tests +([0-9]+) passed"</automated>
  </verify>
  <done>OKM's required accessor list captured; km-core registry surface diffed; any missing methods added with OKM-matching signatures; each addition covered by appended vitest in ontology-registry.test.ts; build + test green.</done>
  <acceptance_criteria>
    - Grep assertion (OKM names exist on km-core): `grep -cE 'isValidClass|getClass\(|getLoadedDomains|getAllClassNames|getClassesForPrompt|getDefaultLayer' /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` returns >= 6 (each OKM accessor present, in some form).
    - Build assertion: `cd /Users/Q284340/Agentic/km-core && npm run build` exits 0.
    - Test assertion: `cd /Users/Q284340/Agentic/km-core && npm test -- ontology-registry.test.ts` exits 0 with N tests where N >= prior baseline (Phase 38-06 reported 21 tests in ontology-registry.test.ts — must be >= 21).
    - Regression assertion: full suite `npm test` exits 0; total pass count is monotone non-decreasing vs the pre-edit baseline.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Bump km-core version + tag + CHANGELOG entry; commit on main</name>
  <files>/Users/Q284340/Agentic/km-core/package.json, /Users/Q284340/Agentic/km-core/CHANGELOG.md</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/package.json (current version field — confirm it's `0.1.0`)
    - /Users/Q284340/Agentic/km-core/CHANGELOG.md (if exists; check format; if missing, create it with this entry as the first entry)
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md (§`package.json` version-bump-on-vendor-tarball pattern — D-G1.4 commit message format)
    - Outputs of Tasks 1 + 2 (stderr blocks reporting Outcome A/B + accessor diff) — drive the version-bump decision: any code change → bump 0.1.0 → 0.2.0; no code change → stay at 0.1.0 and just cut the tag.
  </read_first>
  <action>
    1. Determine the bump:
       - If Task 1 took Outcome A AND Task 2 found all accessors already present → NO source code changed in km-core; version stays at `0.1.0`. Cut a tag `v0.1.0-phase43-verified` (or simply move forward without a new tag if `v0.1.0` already exists on the current HEAD — `git tag --points-at HEAD` to check).
       - If Task 1 took Outcome B (BLOCKED — should not reach this branch) OR Task 2 added any accessors → bump `0.1.0` → `0.2.0`. Edit `package.json` `version` field. Create/append `CHANGELOG.md` entry per Task 3's body.

    2. Create or append `/Users/Q284340/Agentic/km-core/CHANGELOG.md` with the Phase 43 entry. Format (Keep-a-Changelog style):

       ```
       # Changelog

       ## [0.2.0] - 2026-MM-DD (Phase 43 pre-req)

       ### Verified (no schema change)
       - `Entity.layer: 'evidence' | 'pattern'` REQUIRED field confirmed; supports OKM's evidence/pattern split.
       - Round-trip tests added: `tests/unit/entity-layer-field.test.ts`.

       ### Added (only if Task 2 added any)
       - `OntologyRegistry.getLoadedDomains()`: returns Array<string> of loaded ontology domains.
       - `OntologyRegistry.getAllClassNames()`: returns Array<string> of all class names across upper + lower.
       - `OntologyRegistry.getClassesForPrompt()`: returns formatted prompt text for LLM consumers.
       - `OntologyRegistry.getDefaultLayer(className)`: returns Layer | undefined per class.

       ### Why
       Phase 43 (OKM Cross-Repo Migration C) replaces OKM's local OntologyRegistry with this one; the new accessors match OKM's existing surface so the swap is a single-line import change.
       ```

       Drop the "Added" subsection entirely if Task 2 found everything already present.

    3. Run `cd /Users/Q284340/Agentic/km-core && npm run build && npm test` one more time to confirm green from a clean state.

    4. Commit in the km-core repo (operator's HEAD must be on the right branch — verify with `git -C ~/Agentic/km-core branch --show-current`; if main, proceed; if not, ABORT and surface). Single atomic commit:

       ```bash
       cd /Users/Q284340/Agentic/km-core
       git add src/types/entity.ts src/ontology/registry.ts tests/unit/entity-layer-field.test.ts tests/unit/ontology-registry.test.ts package.json CHANGELOG.md
       git commit -m "$(cat <<'EOF'
chore(release): 0.2.0 — Phase 43 OKM cross-repo migration pre-req

- Verify Entity.layer field supports OKM evidence/pattern split (round-trip tests).
- Add any OntologyRegistry accessors missing for OKM consumer parity.
- Unblocks Phase 43 plans 02-11 (OKM-side migration onto km-core).
EOF
)"
       ```

    5. Cut the git tag pointing at the new commit:

       ```bash
       git -C ~/Agentic/km-core tag -a v0.2.0 -m "Phase 43 pre-req: layer field verified + registry parity"
       ```

       (If no source change in Task 1 OR Task 2, skip the tag step entirely — Plan 02 just pins the current `main` SHA. Surface this decision in SUMMARY.)

    6. **Do NOT push.** Pushing is outside this plan's authority — operator pushes when satisfied with the Phase 43 PR train. Surface the local tag/commit SHA in SUMMARY so Plan 02 can pin it.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core &amp;&amp; git log -1 --format=%H &amp;&amp; (git tag --list 'v0.2.0' | grep -q '.' || git log -1 --format=%s | grep -q 'Phase 43')</automated>
  </verify>
  <done>km-core repo is in a stable, taggable (or freshly-tagged) state; commit lands on the right branch; SUMMARY records the commit SHA + tag name so Plan 02 can pin it.</done>
  <acceptance_criteria>
    - File assertion: `test -f /Users/Q284340/Agentic/km-core/CHANGELOG.md` exits 0 (created if it didn't exist).
    - Build + test green: `cd /Users/Q284340/Agentic/km-core && npm run build && npm test` exits 0.
    - Git assertion (commit landed): `git -C ~/Agentic/km-core log -1 --format=%s` matches regex `Phase 43`.
    - Git assertion (HEAD reachable for Plan 02 pin): `git -C ~/Agentic/km-core rev-parse HEAD` returns a 40-char SHA.
    - SUMMARY assertion (Plan 02 prereq): SUMMARY file contains the literal SHA + tag (if any) Plan 02 will pin.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| km-core repo | Independent npm package on github.com (PUBLIC); changes here affect B (Phase 42) consumers in addition to OKM |
| Schema field on Entity | Any change from REQUIRED → optional, or new field, propagates to every consumer's compile + runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-01 | Tampering | Entity schema | mitigate | Outcome A (no schema change) is the default path; Outcome B blocks before any breaking change ships. Full km-core test suite must pass (catches B regression). |
| T-43-01-02 | Tampering | OntologyRegistry surface | mitigate | Additive-only API changes (new methods); never modify existing signatures. Phase 38 tests must stay green. |
| T-43-01-03 | Repudiation | unrecorded version bump | mitigate | CHANGELOG entry + git tag + SUMMARY all record the SHA Plan 02 pins against. |
| T-43-01-04 | Denial of Service | km-core test suite regression | mitigate | Run `npm test` at every step; abort on regression vs Phase 38-06 baseline (56 tests). |
</threat_model>

<verification>
- Entity.layer state documented; new round-trip tests cover both 'evidence' and 'pattern'.
- Every OKM-required OntologyRegistry accessor exists on km-core.
- Full km-core test suite green after all changes.
- CHANGELOG entry + commit SHA + (optional) git tag available for Plan 02 to pin.
</verification>

<success_criteria>
1. km-core Entity.layer supports both 'evidence' and 'pattern' for OKM consumers (verified by Task 1 tests).
2. km-core OntologyRegistry exposes every accessor OKM's local registry exposes (verified by Task 2 grep + tests).
3. km-core HEAD is at a stable, recorded SHA Plan 02 can pin via submodule.
4. ZERO regression in km-core's existing test suite (Phase 37 + 38 baselines preserved).
</success_criteria>

<output>
Create `.planning/phases/43-okm-cross-repo-migration-c/43-01-SUMMARY.md` when done, recording:
- Outcome A or B for Task 1 (with line-number citation from entity.ts).
- Full accessor diff from Task 2 (OKM-required vs km-core-present); for each missing one, the added signature.
- Final commit SHA + git tag name (if any) Plan 02 must pin.
- New km-core test count (must be >= 59 if Task 1 went Outcome A + no Task 2 additions; higher otherwise).
- Any unexpected findings (e.g., signature mismatches that need Plan 04 adaptation).
</output>
</content>
</invoke>