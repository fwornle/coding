---
phase: 43-okm-cross-repo-migration-c
plan: 11
type: execute
wave: 5
depends_on:
  - 43-10
files_modified: []
autonomous: false
requirements:
  - INT-03
user_setup: []

must_haves:
  truths:
    - "All 10 prior plans' inner-OKM commits AND outer rapid-automations gitlink-bumps are accounted for and ready for push (operator-driven push)."
    - "The operator pushes the migration branch in BOTH repos: OKM submodule (bmw.ghe.com:adpnext-apps/operational-knowledge-management.git) AND rapid-automations outer (bmw.ghe.com:adpnext-apps/rapid-automations.git). Push uses HTTPS-token auth per memory/feedback_bmw_ghe_https.md — NOT SSH."
    - "rapid-automations CI runs to completion on the migration branch; verdict GREEN (per the CI workflow at ~/Agentic/_work/rapid-automations/.github/workflows/ci.yml which excludes integrations/* from lint+conflict-marker per CONTEXT.md note — the real verification is OKM's own vitest run). SC#1 explicit close gate."
    - "If CI fails, capture the workflow run URL + failure logs in SUMMARY and ABORT. Do NOT merge until CI is green; do NOT modify code in this plan (return to a fix plan)."
    - "Phase 43 is declared COMPLETE in the human checkpoint. STATE.md update + .planning/RETROSPECTIVE.md entry happen post-checkpoint (operator-driven; or as a follow-up housekeeping plan)."
  artifacts: []
  key_links:
    - from: "Plan 11 push step"
      to: "bmw.ghe.com (HTTPS, token auth)"
      via: "git push origin <migration-branch>"
      pattern: "git push.*https://"
    - from: "Plan 11 CI verification"
      to: "rapid-automations GitHub Actions workflow"
      via: "gh run watch (or web URL polling)"
      pattern: "gh run|workflow run"
---

<objective>
Phase 43 close-out. Two responsibilities:

  1. **Push + CI verification (SC#1):** push the OKM-internal commits + the outer rapid-automations gitlink-bumps to bmw.ghe.com. Watch rapid-automations CI; assert GREEN. This is the formal SC#1 close gate (ROADMAP.md line 569: "rapid-automations CI pipeline green on migration branch and on main after merge").

  2. **Phase 43 sign-off:** human checkpoint confirms all 4 SCs verified; operator approves Phase 43 complete; SUMMARY records the close-out for STATE.md update + RETROSPECTIVE entry.

**No source code changes in this plan.** All implementation is locked from Plans 01-10. This plan is purely orchestration + verification + sign-off.

**Critical constraints:**
- **HTTPS push, NOT SSH:** per memory/feedback_bmw_ghe_https.md, the user's SSH key fails publickey against bmw.ghe.com. ALL pushes use HTTPS-token-auth via `gh auth` setup (already configured per the memory note).
- **No force-push.** The migration branch is the only branch — feature branch in OKM + matching branch in rapid-automations outer. PR/merge is operator-driven post-CI-green.
- **CI excludes integrations/*** at the rapid-automations root level (per CONTEXT.md `~/Agentic/_work/rapid-automations/.github/workflows/ci.yml` note) — the real verification bar is OKM's own vitest suite which Plan 10 already confirmed green. SC#1 here is about CI infra not failing on the cutover-introduced changes (gitlink bumps, package.json edit, .gitmodules).

Out of scope:
- PR creation/merge — operator-driven post-CI-green.
- Phase 43 RETROSPECTIVE.md entry — follow-up plan or operator-driven.
- v7.1 chain handoff (Phase 44 kickoff) — separate phase.

Purpose: Lock SC#1; declare Phase 43 done.

Output: CI workflow URL + green verdict; operator approval; (optional) STATE.md update touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md

<interfaces>
<!-- Phase 43 SC roll-up (ROADMAP.md lines 567-574) -->

SC#1: rapid-automations CI green on migration branch + main — THIS PLAN closes
SC#2: km-core via submodule + vendor tarball, no copy/fork — Plans 02 + 03 + 07 + Plan 10 Task 3 step 3 confirmed
SC#3: REST shape stable — Plan 10 Gates 1+2+3 closed
SC#4: per-domain JSON export commit hygiene — Plan 10 Task 2 step 3 closed

<!-- Push targets -->

- OKM submodule: bmw.ghe.com:adpnext-apps/operational-knowledge-management.git
  HTTPS URL: https://bmw.ghe.com/adpnext-apps/operational-knowledge-management.git
  Push from: ~/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/
  Branch: <captured-from-Plan-02-SUMMARY-onwards>

- Outer rapid-automations: bmw.ghe.com:adpnext-apps/rapid-automations.git
  HTTPS URL: https://bmw.ghe.com/adpnext-apps/rapid-automations.git
  Push from: ~/Agentic/_work/rapid-automations/
  Branch: <matching migration branch>

<!-- gh CLI verification -->

`gh run watch` polls for workflow completion against a specific run ID; can be triggered via:
  - `gh run list --branch <branch> --limit 1 --json databaseId | jq -r '.[0].databaseId'`
  - `gh run watch <run-id> --exit-status`   (exits non-zero on workflow failure)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate commit inventory + verify all-ready-to-push state</name>
  <files>(no source change — verification only)</files>
  <read_first>
    - SUMMARYs from Plans 01-10 (each one records inner + outer commit SHAs)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.git/HEAD or branch tracking — confirm the migration branch name (likely `main` if Phase 43 work was done directly on main per the OKM repo policy; OR a feature branch if the operator was disciplined)
    - /Users/Q284340/Agentic/_work/rapid-automations/.git/HEAD — same check for the outer branch
  </read_first>
  <action>
    1. Inside OKM, capture the migration commit list:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       BRANCH=$(git branch --show-current)
       echo "OKM branch: $BRANCH"
       # If a feature branch, find the divergence from upstream main:
       git log --oneline origin/main..HEAD 2>/dev/null | head -30   # if feature branch
       # OR if on main and unpushed:
       git log --oneline @{push}..HEAD 2>/dev/null | head -30
       ```
       Capture the full list of unpushed commits (should be ~10 commits, one per Plan 02/04/05/06/07/08/09/10 + any sub-commits).

    2. Inside rapid-automations outer, capture the gitlink-bump commits:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations
       BRANCH_OUTER=$(git branch --show-current)
       echo "RA branch: $BRANCH_OUTER"
       git log --oneline @{push}..HEAD 2>/dev/null | head -30
       ```
       Should be ~10 commits (one per inner OKM commit's pointer-bump, plus the Plan 03 root-package.json edit).

    3. Verify both branches' working trees are clean (no uncommitted changes):
       ```
       git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management status -s
       git -C /Users/Q284340/Agentic/_work/rapid-automations status -s
       ```
       Both MUST return empty output. If anything dirty, ABORT and surface — push should never include unintended changes.

    4. Verify HTTPS auth is wired up (memory/feedback_bmw_ghe_https.md):
       ```
       gh auth status 2>&1 | grep -E "Logged in|Active account"
       ```
       Confirm `gh` is logged in with HTTPS token. If missing, surface — the operator must run `gh auth login --hostname bmw.ghe.com --git-protocol https --web` first.

    5. Pre-push sanity: confirm BOTH origins use HTTPS:
       ```
       git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management remote get-url origin
       git -C /Users/Q284340/Agentic/_work/rapid-automations remote get-url origin
       ```
       Both should be `https://bmw.ghe.com/...` (not `git@bmw.ghe.com:...`). If either is SSH, ABORT and surface — operator runs `git remote set-url origin https://bmw.ghe.com/...` first (do NOT silently rewrite per memory/feedback_bmw_ghe_https.md).

    6. Aggregate the SC roll-up by reading each Plan SUMMARY:
       ```
       for n in 01 02 03 04 05 06 07 08 09 10; do
         echo "=== Plan $n ==="
         tail -20 /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-${n}-SUMMARY.md 2>/dev/null
       done
       ```
       Confirm each plan's SUMMARY exists + reports success. If any Plan SUMMARY is missing or reports failure, ABORT and route back to that plan.

    7. Surface readiness JSON to stderr:
       ```
       echo '{"okm_branch":"'$BRANCH'","ra_branch":"'$BRANCH_OUTER'","okm_pending_commits":<count>,"ra_pending_commits":<count>,"auth_ready":true,"https_remotes":true,"plans_complete":10,"ready_to_push":true}' >&2
       ```
  </action>
  <verify>
    <automated>test -z "$(git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management status -s)" &amp;&amp; test -z "$(git -C /Users/Q284340/Agentic/_work/rapid-automations status -s)" &amp;&amp; git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management remote get-url origin | grep -q '^https://' &amp;&amp; git -C /Users/Q284340/Agentic/_work/rapid-automations remote get-url origin | grep -q '^https://'</automated>
  </verify>
  <done>Commit inventory captured; both working trees clean; HTTPS remotes confirmed; gh auth ready; SC roll-up reviewed.</done>
  <acceptance_criteria>
    - Working tree clean (OKM): `test -z "$(git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management status -s)"` exits 0.
    - Working tree clean (outer): `test -z "$(git -C /Users/Q284340/Agentic/_work/rapid-automations status -s)"` exits 0.
    - OKM remote HTTPS: `git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management remote get-url origin | grep -q '^https://'` exits 0.
    - Outer remote HTTPS: `git -C /Users/Q284340/Agentic/_work/rapid-automations remote get-url origin | grep -q '^https://'` exits 0.
    - gh auth ready: `gh auth status 2>&1 | grep -qE "Logged in|Active account"` exits 0.
    - All 10 plan SUMMARYs exist: `ls /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-*-SUMMARY.md | wc -l` returns >= 10.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator pushes both repos to bmw.ghe.com</name>
  <action>Operator executes the two git push commands listed in <how-to-verify> below (inner OKM + outer rapid-automations). The plan does NOT proceed until the operator types "pushed" or describes push failures. This task is intentionally human-gated because pushing to bmw.ghe.com is a privileged action.</action>
  <what-built>
    - Plans 01-10 fully landed in local git history.
    - Task 1 confirmed: clean working trees, HTTPS remotes, gh auth ready, all plan SUMMARYs present.
  </what-built>
  <how-to-verify>
    1. **Push OKM submodule first (inner):**
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       BRANCH=$(git branch --show-current)
       git push origin $BRANCH    # HTTPS push — uses gh auth token
       ```
       Capture the push output. If a feature branch, create the PR via `gh pr create` (operator's discretion).

    2. **Push rapid-automations outer:**
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations
       BRANCH_OUTER=$(git branch --show-current)
       git push origin $BRANCH_OUTER
       ```
       Capture push output. If feature branch, create PR.

    3. **Surface the push results.** Specifically:
       - Inner OKM: branch name + last-pushed SHA + (optional) PR URL.
       - Outer RA: same.

    Respond "pushed" once both pushes succeed. Describe failures (likely auth-related — if so, the operator re-runs `gh auth login --hostname bmw.ghe.com` and re-pushes). Task 3 then watches CI.
  </how-to-verify>
  <resume-signal>Type "pushed" or describe push failures</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Watch rapid-automations CI on the pushed branch; assert GREEN (SC#1 close)</name>
  <files>(no source change — CI verification only)</files>
  <read_first>
    - Output from Task 2 (the pushed branch name + last SHA)
    - /Users/Q284340/Agentic/_work/rapid-automations/.github/workflows/ci.yml (CI workflow definition — confirm trigger paths + job names so the wait is correctly targeted)
  </read_first>
  <action>
    1. From the operator's resume-signal in Task 2, capture the outer rapid-automations branch name + commit SHA.

    2. Find the CI workflow run for the pushed branch:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations
       RUN_ID=$(gh run list --branch "$BRANCH_OUTER" --limit 1 --json databaseId | jq -r '.[0].databaseId')
       gh run view "$RUN_ID" --json status,conclusion,workflowName,url
       ```
       If no run appears within 30s of push, the workflow may not be configured to trigger on this branch — surface the .github/workflows/ci.yml `on:` block in SUMMARY and re-think.

    3. Watch the run to completion:
       ```
       gh run watch "$RUN_ID" --exit-status
       ```
       This blocks until the run finishes; exits 0 on success, non-zero on failure. Timeout: 30 minutes (CI for rapid-automations historically runs <10 min per OKM-light history; budget extra for cold-cache first runs).

    4. Capture the verdict:
       ```
       gh run view "$RUN_ID" --json status,conclusion,jobs,url > /tmp/43-11-ci-verdict.json
       cat /tmp/43-11-ci-verdict.json | jq '{status, conclusion, url, jobs: [.jobs[] | {name, status, conclusion}]}'
       ```
       Save to SUMMARY. The verdict line:
       - `{ status: "completed", conclusion: "success", url: "..." }` → SC#1 GREEN. Proceed.
       - Any other conclusion → SC#1 FAIL. Capture failed-job logs via `gh run view "$RUN_ID" --log-failed`. Write to `43-11-VERIFY-FAIL.log`. ABORT before Task 4 sign-off.

    5. **Do NOT merge or create PRs** in this task. Operator handles merge post-checkpoint.
  </action>
  <verify>
    <automated>test -f /tmp/43-11-ci-verdict.json &amp;&amp; jq -e '.conclusion == "success"' /tmp/43-11-ci-verdict.json</automated>
  </verify>
  <done>CI run identified, watched, GREEN; verdict JSON captured for SUMMARY.</done>
  <acceptance_criteria>
    - CI verdict captured: `test -f /tmp/43-11-ci-verdict.json` exits 0.
    - CI conclusion success: `jq -e '.conclusion == "success"' /tmp/43-11-ci-verdict.json` exits 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 43 final sign-off + STATE.md update authorization</name>
  <action>Operator reviews the SC roll-up in <how-to-verify> below and declares Phase 43 complete (or routes back for triage). The plan does NOT proceed until the operator types "approved — Phase 43 complete" or describes failures. This task is intentionally human-gated as the final sign-off boundary.</action>
  <what-built>
    - All 11 Phase 43 plans landed end-to-end.
    - Plans 01-10 source/data work committed inside OKM + outer rapid-automations gitlink-bumps committed.
    - Task 2: branches pushed to bmw.ghe.com.
    - Task 3: rapid-automations CI GREEN on the pushed branch (SC#1 close).
    - Plan 10: SC#2 + SC#3 + SC#4 verified at the human-checkpoint level.
  </what-built>
  <how-to-verify>
    1. **SC roll-up final:**
       - SC#1 (CI green): VERIFIED — Task 3 captured the workflow URL + success conclusion.
       - SC#2 (km-core via submodule+vendor tarball, no copy/fork): VERIFIED at Plan 10 Gate 3 step 3.
       - SC#3 (REST shape stable): VERIFIED at Plan 10 Gates 1+2+3.
       - SC#4 (export hygiene): VERIFIED at Plan 10 Task 2 step 3.

    2. **Inspect the CI workflow URL** (Task 3 output) — open in browser, confirm all jobs green, screenshot for the SUMMARY (operator-discretion).

    3. **Confirm OKM live behavior end-to-end** (any operational check that gives the operator confidence):
       - `/api/stats` returns total entity count matching Plan 09 SUMMARY.
       - VOKB viewer loads cleanly (no stale-cache or hardcoded-filter issues from Gate 3 surfaced anything).
       - Recent activity in `.data/exports/{general,kpifw,raas}.json` (mtime within hours, content has recent provenance entries).

    4. **Phase 43 STATUS in coding repo's STATE.md** (this is the coding repo, NOT OKM):
       - Operator updates `.planning/STATE.md` post-checkpoint to flip Phase 43 from "NEXT" to "Complete" (per the v7.1 milestone status table).
       - Operator updates `.planning/ROADMAP.md` Phase 43 status to `[x]`.
       - Operator updates `.planning/REQUIREMENTS.md` INT-03 status from "Pending" to "Complete".
       - Operator (optional) writes a Phase 43 entry to `.planning/RETROSPECTIVE.md` covering: what worked (cross-repo submodule pattern; three-gate REST verification; idempotent migration script reused across Plans 7+9), what was inefficient (the constructor-shape diff in Plan 01→04 — Plan 01's accessor audit avoided a worse divergence; the cross-repo HTTPS-vs-SSH gotcha avoided via early bmw.ghe.com lesson).

    5. **PR merge (operator-discretion):** if feature branches were used, the operator opens PRs in both repos (`gh pr create` in each), gets approvals (if required by branch protection), merges. STATE.md update can happen either pre- or post-merge per the operator's discipline.

    6. **Phase 44 kick-off readiness:** Phase 43 is the last v7.1 migration. With C complete, the chain unblocks 44 (REST API & Git Snapshots) which now has A + B + C all on km-core ready for a unified router. The operator decides when to invoke `/gsd-discuss-phase 44`.

    Respond "approved — Phase 43 complete" once all SCs green + comfortable with the operational state. If anything failed, describe for triage.
  </how-to-verify>
  <resume-signal>Type "approved — Phase 43 complete" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Push to bmw.ghe.com | HTTPS-token-auth path; failure surfaces auth issue before code lands remotely |
| rapid-automations CI on bmw.ghe.com | Real-world CI verdict; SC#1 close gate; failure halts Phase 43 close |
| Operator final sign-off | Last human review before Phase 43 declared complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-11-01 | Tampering | partial push (one repo pushed, other failed) | mitigate | Task 2 sequences inner-then-outer; on outer failure with inner already pushed, the SUMMARY documents the partial state for operator triage. Re-pushing outer is safe (HTTPS gitlink only). |
| T-43-11-02 | Information Disclosure | accidental push of .data/leveldb.pre-43-backup | accept | .gitignore protects .data/ + .gitignore was reviewed in Plan 02; backups never enter git index. |
| T-43-11-03 | Repudiation | CI run verdict undocumented | mitigate | Task 3 captures `gh run view --json` to /tmp + SUMMARY; URL + conclusion preserved. |
| T-43-11-04 | Denial of Service | CI red on rapid-automations | mitigate | ABORT path — fix in a follow-up plan, do NOT modify code here. SC#1 stays open until fixed. |
| T-43-11-05 | Repudiation | Phase 43 closure without STATE.md sync | mitigate | Task 4 checkpoint explicitly prompts operator to update STATE.md + ROADMAP + REQUIREMENTS + RETROSPECTIVE; SUMMARY captures whether each happened. |
</threat_model>

<verification>
- Task 1: commit inventory + readiness verified.
- Task 2: both repos pushed.
- Task 3: CI green; verdict captured.
- Task 4: operator confirms all 4 SCs + authorizes STATE.md sync.
</verification>

<success_criteria>
1. Both OKM submodule + outer rapid-automations branches pushed.
2. rapid-automations CI GREEN; URL recorded.
3. SC#1-#4 all VERIFIED at the human level.
4. Phase 43 declared complete by operator.
5. STATE.md + ROADMAP + REQUIREMENTS prepared for sync (operator-driven; or a follow-up housekeeping plan executes the writes).
</success_criteria>

<output>
Create `.planning/phases/43-okm-cross-repo-migration-c/43-11-SUMMARY.md` when done, recording:
- Aggregate commit inventory (every inner + outer commit SHA + message from Plans 01-10)
- Push outputs (both inner + outer)
- CI workflow URL + verdict JSON
- Per-SC final status (#1, #2, #3, #4 — each green/triaged)
- Operator approval timestamp
- STATE.md / ROADMAP / REQUIREMENTS / RETROSPECTIVE update status (done/deferred)
- Phase 44 kick-off readiness note
</output>
</content>
</invoke>