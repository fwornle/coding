---
phase: 43-okm-cross-repo-migration-c
plan: 10
type: execute
wave: 5
depends_on:
  - 43-06
  - 43-08
  - 43-09
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs
autonomous: false
requirements:
  - INT-03
user_setup: []

must_haves:
  truths:
    - "All three D-G5.1 gates run + pass: (a) Plan 06's rest-contract.test.ts (Zod schemas + byte-equal fixtures) passes against the post-cutover server; (b) a NEW verification script `scripts/verify-post-migration.mjs` records fresh responses against the SAME seed dataset and byte-diffs vs Plan 06 fixtures (zero diff); (c) VOKB viewer smoke — operator-driven visual check confirms graph renders + entity click works + RCA lookup returns results."
    - "If the VOKB smoke surfaces a REAL bug (e.g., a hardcoded `layer === 'evidence'` filter that no longer matches), in-place fix is allowed (D-G6.1 explicit). NO refactor. NO feature work."
    - "OKB-baseline guard hygiene: `.data/exports/{general,kpifw,raas}.json` regenerate after an OKM operation (ingest, manual write, debounce settle). Verified by mtime check + content sample (recent runId in provenance)."
    - "SC#1 (rapid-automations CI green): Push the migration branch to bmw.ghe.com IF AND ONLY IF the operator approves at the human checkpoint. Plan 11 owns the formal CI green-light; this plan does the local pre-push verification."
    - "Phase 43 SC#1-#4 (per ROADMAP.md lines 567-574) all green by end of this plan; SC#1 (CI green) deferred to Plan 11 post-push."
  artifacts:
    - path: "scripts/verify-post-migration.mjs"
      provides: "Re-recorder + byte-diff verification; runs the same seed → same endpoints → diff against pre-migration fixtures"
    - path: "tests/fixtures/post-migration/api-*.json (8 files, NOT committed)"
      provides: "Output of the verification recorder — temporary artifacts compared against fixtures, deleted on success"
  key_links:
    - from: "verify-post-migration.mjs"
      to: "tests/fixtures/pre-migration/api-*.json"
      via: "JSON.stringify(post, null, 2) === readFileSync(prePath)"
      pattern: "pre-migration.*api-.*\\.json"
    - from: "Plan 10 human checkpoint"
      to: "VOKB viewer smoke (operator-driven)"
      via: "manual checklist (load → render → click → RCA)"
      pattern: "VOKB|viewer"
---

<objective>
D-G5.1 verification — all three gates run against the post-cutover OKM. Final automated + manual check before Plan 11 declares Phase 43 complete.

**Three-gate structure (D-G5.1 explicit):**

  1. **Gate 1 — Zod contract tests:** Plan 06's `tests/integration/rest-contract.test.ts` runs again against the cutover'd OKM. Should pass — Plan 08 Task 3 already validated this. Re-running here is the formal verification step.

  2. **Gate 2 — Recorded-fixtures byte-diff:** A new `scripts/verify-post-migration.mjs` script (NOT the same as Plan 06's `record-rest-fixtures.mjs` — they share helpers but diverge in semantics):
     - Spins up the post-cutover OKM in-process.
     - Ingests the SAME seed dataset Plan 06 used.
     - Hits the SAME endpoint list.
     - Writes responses to `tests/fixtures/post-migration/api-*.json` (NOT committed; temporary).
     - Byte-diffs each post-migration file against the matching pre-migration fixture.
     - Reports per-endpoint diff status; ZERO diff across the board = pass.

  3. **Gate 3 — VOKB viewer smoke (operator-driven):** Human checkpoint. Operator opens VOKB, confirms graph renders, clicks an entity to see details, runs an RCA lookup. If a real bug appears (D-G6.1 allows bug-fix-only edits), the operator triages — but the smoke is a checkpoint, not an automated test.

**Plus a fourth check (OKB-baseline hygiene per SC#4):** `.data/exports/*.json` regenerate after an OKM operation. Verify by triggering an ingest or by waiting for the debounce, then checking `.data/exports/{general,kpifw,raas}.json` mtime + content (recent provenance runId visible).

**D-G6.1 bug-fix-only allowance:** if the VOKB smoke surfaces a real regression (e.g., hardcoded layer filter), apply minimal in-place fix in this plan, document in SUMMARY. Anything beyond bug-fix scope (refactor, feature, viewer overhaul) is Phase 45.

Out of scope: pushing to bmw.ghe.com (Plan 11). VOKB feature work (Phase 45).

Purpose: Prove Phase 43 SC#3 (REST stability) + SC#4 (commit hygiene) before close-out.

Output: 1 new verification script + 1 human checkpoint approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md

<interfaces>
<!-- Plan 06 fixtures + script (the verification target) -->

- tests/fixtures/pre-migration/seed-dataset.json — seed used by both Plan 06 + Plan 10
- tests/fixtures/pre-migration/api-*.json (8 files) — locked baseline
- scripts/record-rest-fixtures.mjs — Plan 06's recorder; Plan 10's verifier REUSES the bootstrap path (same in-process server + same seed ingest) but diverges in the per-endpoint loop (verify writes to tests/fixtures/post-migration/* AND diffs vs pre-migration)

<!-- Phase 43 SC list (ROADMAP.md lines 567-574) -->

  SC#1: rapid-automations CI pipeline green on migration branch + main after merge — DEFERRED to Plan 11
  SC#2: OKM consumes km-core via agreed packaging (submodule + vendor tarball) without copying/forking — VERIFIED by Plans 02 + 03 + 07 (no copied source; only the submodule + tarball pipeline)
  SC#3: REST consumer endpoints return same shape — THIS PLAN's Gates 1 + 2 + 3 verify
  SC#4: per-domain JSON exports continue same commit hygiene — THIS PLAN's hygiene check verifies

<!-- VOKB viewer location (CONTEXT.md canonical_refs) -->

integrations/operational-knowledge-management/viewer/  (D-G6.1 — no feature changes; bug-fix-only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/verify-post-migration.mjs (Gate 2 — byte-diff vs pre-migration fixtures)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/record-rest-fixtures.mjs (Plan 06 — copy bootstrap + seed-ingest + endpoint-loop; diverge on what happens with the response)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/fixtures/pre-migration/ (full directory — confirm fixture file names + count)
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-06-SUMMARY.md (the endpoint list Plan 06 actually used — Plan 10 must hit the same set)
  </read_first>
  <action>
    1. Create `scripts/verify-post-migration.mjs` (`#!/usr/bin/env node`, chmod +x). Structure mirrors `record-rest-fixtures.mjs`:
       - Same flag set (`--port`, etc.) — minimum needed.
       - Same OKM bootstrap (in-process server, same deterministic LLM stub, same seed ingest).
       - Diverges in the per-endpoint loop: instead of writing the response straight to disk, the script:
         a. Records the response to `tests/fixtures/post-migration/api-<endpoint>.json` (mkdir-p first; the dir is NOT committed — added to .gitignore at end of task).
         b. Reads the corresponding `tests/fixtures/pre-migration/api-<endpoint>.json`.
         c. Diffs via `JSON.stringify(post, null, 2) === JSON.stringify(pre, null, 2)` (same formatting both sides — recorder MUST use same `null, 2` indent).
         d. On match: log `[verify] <endpoint>: MATCH` to stderr.
         e. On mismatch: log `[verify] <endpoint>: DIFF` + write a unified diff to `tests/fixtures/post-migration/api-<endpoint>.diff.txt` via `diffLines` from the `diff` npm package (if not in deps, fall back to a simple line-by-line diff in pure JS — keep deps zero-add for this script).

    2. Final JSON summary line to stdout:
       ```
       console.log(JSON.stringify({
         status: allMatched ? 'ok' : 'fail',
         total: endpoints.length,
         matched,
         diff,
         endpoints: results,    // [{endpoint, status: 'match'|'diff', diffFile?}]
       }));
       process.exit(allMatched ? 0 : 1);
       ```

    3. Append `tests/fixtures/post-migration/` to OKM's `.gitignore` (one-line addition; the post-migration dir is verification scratch, never committed):
       ```
       grep -q '^tests/fixtures/post-migration' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.gitignore 2>/dev/null || echo 'tests/fixtures/post-migration/' >> /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.gitignore
       ```

    4. Syntax: `node --check scripts/verify-post-migration.mjs` exits 0.
  </action>
  <verify>
    <automated>test -x /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs &amp;&amp; node --check /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs &amp;&amp; grep -q '^tests/fixtures/post-migration' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.gitignore</automated>
  </verify>
  <done>Verification script exists; gitignore prevents accidental commit of post-migration scratch files.</done>
  <acceptance_criteria>
    - File: `test -x /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs` exits 0.
    - Syntax: `node --check /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs` exits 0.
    - Diff logic present: `grep -cE 'JSON\.stringify|diffLines|byte-equal|MATCH|DIFF' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs` returns >= 2.
    - gitignore protects scratch: `grep -q '^tests/fixtures/post-migration' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.gitignore` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run Gate 1 (Zod) + Gate 2 (byte-diff) + SC#4 (commit hygiene) automated checks</name>
  <files>(no source change — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-08-SUMMARY.md (Plan 08 — confirms cutover complete + REST shapes preserved)
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-09-SUMMARY.md (Plan 09 — confirms embeddings done; relevant for /api/search response shape)
  </read_first>
  <action>
    1. **Gate 1 — Zod contract tests:**
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       npm test -- rest-contract.test.ts 2>&1 | tee /tmp/43-10-gate1-zod.log
       ```
       Exit 0 required. Pass count >= 8 (per Plan 06 endpoint count). Capture summary in SUMMARY.

    2. **Gate 2 — Byte-diff verification:**
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       node scripts/verify-post-migration.mjs 2>&1 | tee /tmp/43-10-gate2-bytediff.log
       ```
       Exit 0 (status:ok) required. Per-endpoint matched count === total endpoint count.

       **If any DIFF:** capture each `.diff.txt` file's content into SUMMARY. Apply minimal fix per D-G6.1 (bug-fix-only) IF the diff is a real regression from the cutover; surface for the human checkpoint to triage. Re-run until clean OR escalate to operator.

       **Common drift sources to expect:**
       - Field ordering inside objects (Plan 06 fixtures used `JSON.stringify(body, null, 2)` which is property-insertion-order sensitive; if km-core's response builder iterates entities in a different order, the diff fires).
       - Array ordering (entities returned in different order by `iterate()` vs the old GraphStore's `getAllEntities()`).
       - Timestamps (if a field leaked a "now" timestamp without seeding).

       For ordering drifts, the fix is to sort deterministically on the response builder side (e.g., `entities.sort((a, b) => a.id.localeCompare(b.id))`) before serializing. Surface decisions in SUMMARY.

    3. **SC#4 — Commit hygiene (OKB-baseline guard):**
       Trigger an OKM write (e.g., a tiny ingest via POST /api/ingest with a single test entity, OR wait the 5s debounce after server start). Then verify:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       ls -la .data/exports/{general,kpifw,raas}.json
       ```
       Each file's mtime should be recent (within the past minute). Open one (e.g., general.json) + spot-check it has the new provenance runId from the test write. If exports stayed stale (mtime didn't update), the post-cutover debounce/export pipeline is broken — surface for triage.

    4. Surface all three gates' results into Task 3's checkpoint.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management &amp;&amp; npm test -- rest-contract.test.ts 2>&amp;1 | tail -5 &amp;&amp; node scripts/verify-post-migration.mjs 2>&amp;1 | tail -3 | grep -q '"status":"ok"'</automated>
  </verify>
  <done>Gate 1 + Gate 2 + SC#4 all green; results captured for the checkpoint.</done>
  <acceptance_criteria>
    - Gate 1 (Zod): `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test -- rest-contract.test.ts` exits 0.
    - Gate 2 (byte-diff): `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && node scripts/verify-post-migration.mjs | tail -1 | node -e "process.exit(JSON.parse(require('fs').readFileSync(0,'utf8')).status === 'ok' ? 0 : 1)"` exits 0.
    - SC#4 (export hygiene): after a write trigger, `find /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/exports -name '*.json' -mmin -2` returns >= 1 file (recent mtime).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Gate 3 + final approval): VOKB smoke + SC#1-4 sign-off</name>
  <action>Operator runs the VOKB smoke check + the SC#1-4 sign-off checklist in <how-to-verify> below. The plan does NOT proceed until the operator types "approved" or describes failures for triage. This task is intentionally human-gated per D-G5.1's Gate 3 requirement (operator-driven visual smoke).</action>
  <what-built>
    - Plans 43-01 through 43-09 landed: km-core pre-req + cross-repo packaging + ontology unification + maintenance routing + REST baseline + migration + cutover + re-embed.
    - Plan 10 Task 1: scripts/verify-post-migration.mjs authored.
    - Plan 10 Task 2: Gate 1 (Zod) green; Gate 2 (byte-diff) green; SC#4 (export hygiene) green.
    - Phase 43 SC#2 + SC#3 + SC#4 fully verified at the automated level.
    - SC#1 (CI green) is Plan 11's responsibility.
  </what-built>
  <how-to-verify>
    1. **Gate 3 — VOKB viewer smoke (D-G5.1 third gate):** Open the VOKB viewer.
       - Locate the VOKB entry point (likely `integrations/operational-knowledge-management/viewer/` — start with `npm start` or however the viewer launches; check the viewer's README).
       - Load it in a browser pointed at the post-cutover OKM API.
       - **Check 1 — graph renders:** the graph view loads without errors; node count looks reasonable (matches Plan 09 SUMMARY's total).
       - **Check 2 — entity click works:** click any entity; the detail panel opens; description + metadata + provenance + legacyId visible.
       - **Check 3 — RCA lookup returns:** trigger an RCA lookup on a known entity; results appear.
       - **Check 4 — layer values display:** both `evidence` and `pattern` entities are visible in the graph (look for visual differentiation or use a filter).

    2. **D-G6.1 bug-fix-only allowance:** if a real regression appears (e.g., hardcoded `entity.layer === 'evidence'` filter that no longer applies), apply minimal in-place fix to the viewer code. NO refactor. NO feature work. Surface every fix in SUMMARY with a commit SHA so Phase 45's verifier can read the trail.

    3. **SC#2 — packaging confirmation:** open `integrations/operational-knowledge-management/package.json` line 20 (or wherever the @fwornle/km-core line is) — confirms `file:vendor/fwornle-km-core-<version>.tgz`; `lib/km-core/` exists; `.gitmodules` declares the submodule.

    4. **SC#4 — export hygiene re-check:** trigger one more OKM operation after this checkpoint reads; confirm `.data/exports/*.json` updates within ~10s (5s debounce + write).

    5. **Backup retention decision:** Plan 08's `.data/leveldb.pre-43-backup` and the three `.data/exports/*.pre-43-backup` files have been preserved since Plan 08's cutover. If all the above smoke checks pass AND 24-48h has passed since Plan 08 with no rollback needed, you MAY delete them:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       rm -rf .data/leveldb.pre-43-backup
       rm .data/exports/general.json.pre-43-backup .data/exports/kpifw.json.pre-43-backup .data/exports/raas.json.pre-43-backup
       ```
       Recommended retention: at least 48h post-Plan-08 before deletion.

    6. **Phase 43 SC roll-up:**
       - SC#1 (CI green): Plan 11 owns; this checkpoint defers
       - SC#2 (packaging strategy without copying/forking): VERIFIED by step 3 above
       - SC#3 (REST shape stability): VERIFIED by Gates 1 + 2 + 3
       - SC#4 (export hygiene): VERIFIED by step 4

    Respond "approved" if Gate 3 + all smoke + roll-up green. If anything failed, describe what failed for triage. Plan 11 (CI green-light) waits on this approval.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit verification script + bug-fix-only viewer edits (if any from Gate 3)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.gitignore, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/viewer/* (only if D-G6.1 bug-fix landed)</files>
  <read_first>
    - Output of Task 3 (any viewer bug-fix details + commit list)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/CLAUDE.md (NO PARALLEL VERSIONS — viewer fix is in-place)
  </read_first>
  <action>
    1. Stage Plan 10 deliverables inside OKM:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       git add scripts/verify-post-migration.mjs .gitignore
       # If Gate 3 prompted any viewer fixes, also:
       # git add viewer/<files>
       git status                                    # confirm scope
       git diff --cached --stat                      # for SUMMARY
       git commit -m "$(cat <<'EOF'
test(verify): post-migration REST byte-diff verification + Phase 43 SC#3 close (D-G5.1)

scripts/verify-post-migration.mjs reuses Plan 06's deterministic seed +
deterministic LLM stub, hits the same endpoint set, byte-diffs each
response against tests/fixtures/pre-migration/*.json. ZERO diff =
SC#3 green.

Three-gate run verdict:
- Gate 1 (Zod contract tests, Plan 06's rest-contract.test.ts): PASS
- Gate 2 (byte-diff via verify-post-migration.mjs): PASS — <N>/<N> endpoints matched
- Gate 3 (VOKB viewer smoke, operator-verified): PASS
  - Graph rendered: <count> entities
  - Entity click: detail panel + provenance + legacyId visible
  - RCA lookup: returned results
  - layer=evidence + layer=pattern both displayed

SC#4 (commit hygiene): .data/exports/*.json regenerate on debounce; VERIFIED.

[If any D-G6.1 bug-fix-only viewer edits landed:
 - <file>: <one-line description>
 - <file>: <one-line description>
]

Phase 43 SC#1 deferred to Plan 11 (rapid-automations CI green-light).
EOF
)"
       ```

    2. Outer gitlink bump:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations
       git add integrations/operational-knowledge-management
       git commit -m "chore: bump OKM submodule — Phase 43 Plan 10 (REST byte-diff + VOKB smoke)"
       ```

    3. **DO NOT push.** Plan 11 owns the push + CI gate.
  </action>
  <verify>
    <automated>git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s | grep -q 'D-G5.1' &amp;&amp; git -C /Users/Q284340/Agentic/_work/rapid-automations log -1 --format=%s | grep -q 'Plan 10'</automated>
  </verify>
  <done>verify-post-migration.mjs committed; .gitignore updated; any bug-fix-only viewer edits committed; outer gitlink bumped.</done>
  <acceptance_criteria>
    - Inner commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s` matches `D-G5.1`.
    - Outer commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations log -1 --format=%s` matches `Plan 10`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 06 fixtures (authoritative) ⇄ post-cutover responses | Any drift = SC#3 failure; lock is byte-equal |
| VOKB viewer ⇄ OKM REST API | Read-only consumer; bug-fix-only allowed (D-G6.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-10-01 | Tampering | byte-diff false positive (ordering drift) | mitigate | Sort deterministically on response-builder side; surface fix in SUMMARY. |
| T-43-10-02 | Information Disclosure | viewer smoke surfaces hardcoded layer filter | mitigate | D-G6.1 bug-fix-only edit lands in-plan; documented in SUMMARY. |
| T-43-10-03 | Repudiation | gate verdicts undocumented | mitigate | Per-gate JSON + log outputs captured into SUMMARY; commit message records the verdict. |
| T-43-10-04 | Denial of Service | export hygiene drift (SC#4) | mitigate | Active write trigger + mtime check; if broken, surface for triage before Plan 11 push. |
</threat_model>

<verification>
- Gate 1 (Zod) green.
- Gate 2 (byte-diff via new verify script) green.
- Gate 3 (VOKB smoke, operator-verified) green.
- SC#4 (export hygiene) green.
- Bug-fix-only edits documented + committed (if any).
- Inner + outer commits land; outer gitlink ready for Plan 11 push.
</verification>

<success_criteria>
1. All three D-G5.1 gates pass.
2. SC#3 + SC#4 verified at the OKM-local level.
3. SC#2 confirmed at the human checkpoint.
4. Verification script committed for future use.
5. Plan 11 unblocked.
</success_criteria>

<output>
Create `.planning/phases/43-okm-cross-repo-migration-c/43-10-SUMMARY.md` when done, recording:
- Gate 1 verdict (npm test -- rest-contract.test.ts output)
- Gate 2 verdict (verify-post-migration.mjs JSON summary)
- Per-endpoint match/diff status from Gate 2
- Any sort-order or formatting fixes applied to restore byte-diff parity
- Gate 3 verdict (operator's per-check responses)
- Any D-G6.1 bug-fix-only viewer edits with commit SHAs
- SC#4 (export hygiene) verification output
- Inner + outer commit SHAs
- Backup deletion decision (kept / deleted; if kept, when to revisit)
</output>
</content>
</invoke>