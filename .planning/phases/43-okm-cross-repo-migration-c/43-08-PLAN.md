---
phase: 43-okm-cross-repo-migration-c
plan: 08
type: execute
wave: 4
depends_on:
  - 43-04
  - 43-05
  - 43-06
  - 43-07
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/index.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/pipeline.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/extractor.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/clustering.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/confidence.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/connectivity.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/correlation.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/rca-lookup.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/search.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/temporal.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/km-store-adapter.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/graph-store.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/sync-manager.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/persistence.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/types/graph-store.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/registry.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/loader.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/unit/km-store-adapter.test.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/unit/graph-store.test.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/unit/sync-manager.test.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/unit/persistence.test.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/unit/ontology-loader.test.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/km-core-backend.test.ts
autonomous: false
requirements:
  - INT-03
user_setup: []

must_haves:
  truths:
    - "Atomic dir swap complete: `.data/leveldb/` is renamed to `.data/leveldb.pre-43-backup/` and `.data/leveldb-kmcore/` (Plan 07 output) is renamed to `.data/leveldb/`. Backup preserved until human checkpoint clears it."
    - "Three-phase final cleanup mirrors Phase 42 Plan 7's A/B/C structure (D-G3.1):"
    - "  Phase A — Dir swap (Task 1)."
    - "  Phase B — Delete legacy modules + IGraphStore + adapter; refactor every consumer to await km-core async API directly (Task 2)."
    - "  Phase C — Build + tests + REST contract tests + human verify (Task 3 + Task 4)."
    - "Files DELETED (Phase B):  src/store/km-store-adapter.ts, src/store/graph-store.ts, src/store/sync-manager.ts, src/store/persistence.ts, src/types/graph-store.ts (IGraphStore interface), src/ontology/registry.ts, src/ontology/loader.ts, plus their matching tests/unit/*.test.ts files."
    - "OKB_STORE_BACKEND flag + every conditional branch in src/index.ts referencing it: REMOVED."
    - "src/api/server.ts createServer signature: graphStore: IGraphStore → kmStore: GraphKMStore. createServer body uses await on every store call."
    - "Every consumer (pipeline, deduplicator, extractor, intelligence/*, api/routes.ts): switched from sync `graphStore.X()` calls to `await kmStore.X()` calls per D-G3.2's method mapping table (PATTERNS.md §intelligence/*.ts)."
    - "Grep gate (Phase B verification): zero matches for `IGraphStore|GraphKMStoreAdapter|OKB_STORE_BACKEND|from.*store/graph-store|from.*store/sync-manager|from.*store/persistence|from.*ontology/registry|from.*ontology/loader` across src/ + tests/."
    - "OKM build + ALL tests green (pre-existing OKM suite + Plan 06's rest-contract.test.ts + Plan 07's migrate-json-replay.test.ts)."
    - "source-document-store.ts SURVIVES (OKM-specific, not a km-core concern per CONTEXT.md)."
  artifacts:
    - path: "(deletions × 12)"
      provides: "All legacy storage + ontology + adapter modules + their unit tests removed"
    - path: "src/index.ts"
      provides: "Direct km-core bootstrap; no flag; no adapter; injects GraphKMStore into pipeline + extractor + dedup + routes"
    - path: "src/api/server.ts"
      provides: "createServer(kmStore: GraphKMStore, ...) — single store param"
    - path: "src/intelligence/*.ts (7 files)"
      provides: "All await km-core async API; node-key delta (strip layer prefix) handled per file"
  key_links:
    - from: "every consumer file"
      to: "km-core GraphKMStore (async)"
      via: "await kmStore.<iterate|getEntity|putEntity|findRelations|addRelation>"
      pattern: "await\\s+\\w+\\.(iterate|getEntity|putEntity|findRelations|addRelation|mergeAttributes)"
    - from: "everywhere in src/"
      to: "(no leftover legacy symbols)"
      via: "grep gate Phase B step 4"
      pattern: "(NOT) IGraphStore|GraphKMStoreAdapter|OKB_STORE_BACKEND|from.*store/(graph-store|sync-manager|persistence)|from.*ontology/(registry|loader)"
---

<objective>
The Phase 43 cutover — mirrors Phase 42 Plan 7's three-phase final-cleanup template (CONTEXT.md D-G3.1). This is the BIG plan; ~12 file deletions + ~10 file refactors. After it lands: zero strangler, zero adapter, zero legacy backend, zero feature flag. OKM consumers speak km-core natively.

**Three phases (one task each, plus a human checkpoint):**

  - **Phase A — Atomic LevelDB dir swap (Task 1):** Plan 07 already populated `.data/leveldb-kmcore/`. This task renames `.data/leveldb/` to `.data/leveldb.pre-43-backup/` and `.data/leveldb-kmcore/` to `.data/leveldb/`. OKM, on next start, sees the canonical-shape store at its expected path. Done BEFORE Phase B's source changes so the running OKM (if any) is consistent during the diff.

  - **Phase B — Delete legacy + IGraphStore + adapter; refactor consumers (Task 2):** The biggest task. Two sub-phases internally:
      - **Step B1 — Refactor consumers to await km-core directly.** For each consumer (pipeline, deduplicator, extractor, intelligence/*, api/routes.ts), apply the method mapping table (PATTERNS.md §intelligence/*.ts). Switch sync IGraphStore calls (`graphStore.X()`) to async km-core calls (`await kmStore.X()`). Handle the node-key delta (strip `${layer}:` prefix per PATTERNS.md). Update src/index.ts bootstrap: remove the `OKB_STORE_BACKEND` flag + the legacy-else branch; the km-core path is unconditional. Update src/api/server.ts: replace `graphStore: IGraphStore` with `kmStore: GraphKMStore`.
      - **Step B2 — Delete legacy modules.** `git rm` the 7 deleted source files + their matching test files. Run the cleanup grep gate (PATTERNS.md §Cleanup grep gate template) — MUST return zero matches.

  - **Phase C — Build + tests + REST contract tests + human verify (Task 3 + Task 4):** `npm run build` + `npm test` (full OKM suite including Plan 06's rest-contract.test.ts and Plan 07's migrate-json-replay.test.ts). Then a human-verify checkpoint (autonomous:false) — operator visually confirms OKM starts, REST endpoints respond, dashboard renders.

**Critical:** D-G3.2 chose the AGGRESSIVE option — delete IGraphStore interface AND the adapter (not just the legacy backend). The diff is ~10-15 files but no leaky abstraction remains. Every consumer becomes async-native.

**Out of scope:** Re-embed pass (Plan 09 owns it). Final REST verification (Plan 10 owns the byte-diff + VOKB smoke).

Purpose: Eliminate the strangler completely; complete OKM's storage cutover.

Output: ~12 deletions; ~10 refactors; backup preserved; build + tests green; human checkpoint approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-07-PLAN.md

<interfaces>
<!-- D-G3.2 method mapping table (PATTERNS.md §intelligence/*.ts) -->

| OKM IGraphStore (sync) | km-core GraphKMStore (async) |
|---|---|
| getAllEntities(): Entity[] | for await (const e of store.iterate()) {...} |
| getEntity(nodeId): Entity \| null | await store.getEntity(entityId) — strip layer prefix first |
| addEntity(entity): string (nodeId) | await store.putEntity(entity, { skipOntologyCheck: false }) |
| addEdge(src, tgt, edge): string | await store.addRelation({ type, from, to, metadata }) |
| getEdges(nodeId, opts): EdgeResult[] | await store.findRelations({ from?, to?, type? }) |
| filterByLayer(layer): Entity[] | iterate + filter (no direct km-core method) |
| getStats(): {nodes, edges} | iterate + count |
| getOrphanNodeIds(): string[] | iterate + await store.getDegree(...) per Phase 41 Plan 03 |

<!-- Node-key delta (PATTERNS.md §intelligence/*.ts) -->

OKM uses ${layer}:${id} composite keys (e.g., "evidence:abc-123"); km-core uses bare EntityId (UUIDv7).
Recommendation: drop layer-prefixed keys entirely and use entity.layer field for filtering.
For each consumer file, every place that parses a nodeId via `nodeId.split(':')` becomes a plain entityId lookup.

<!-- Bootstrap target state (PATTERNS.md §index.ts Target state) -->

```typescript
const { GraphKMStore } = await import('@fwornle/km-core');
const { defaultOntologyDir } = await import('@fwornle/km-core/ontology');   // OR the resolver block per CLAUDE.md rule

const kmStore = new GraphKMStore({
  ontologyDir: ontologyDir,   // resolved per CLAUDE.md rule (same block as scripts use)
  dataDir: path.join(dataDir, 'leveldb'),
  exportDir: path.join(dataDir, 'exports'),
  debounceMs: 5000,
});
await kmStore.open();
// No adapter — consumers await km-core directly
```

<!-- Cleanup grep gate (PATTERNS.md §Cleanup grep gate, verbatim) -->

```bash
grep -rln 'IGraphStore\|GraphKMStoreAdapter\|OKB_STORE_BACKEND\|from.*store/graph-store\|from.*store/sync-manager\|from.*store/persistence\|from.*ontology/registry\|from.*ontology/loader' \
  ~/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ \
  ~/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/ \
  --include='*.ts' --include='*.js'
```

MUST return zero matches before build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Phase A): Atomic LevelDB dir swap + backup preservation</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb (renamed to .pre-43-backup), /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore (renamed to leveldb)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-07-SUMMARY.md (Plan 07 output — totalEntities + production runId for verification)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-07-PLAN.md (Phase A pattern — stop processes that hold LevelDB handles, atomic mv, restart-and-verify)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/CLAUDE.md (any OKM-specific runtime / process management notes; OKM may run in Docker via the rapid-automations docker-compose, OR locally as a Node process — verify before any stop)
  </read_first>
  <action>
    1. Pre-flight: confirm both source dirs exist + neither is mid-migration:
       ```
       test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb &&
       test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore &&
       test ! -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb.pre-43-backup
       ```
       If `leveldb.pre-43-backup` already exists, ABORT — a prior cutover attempt left state behind; operator must triage.

    2. Pre-backup OKM's git-tracked exports per CLAUDE.md "KB Persistence Rules":
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       cp .data/exports/general.json .data/exports/general.json.pre-43-backup
       cp .data/exports/kpifw.json .data/exports/kpifw.json.pre-43-backup
       cp .data/exports/raas.json .data/exports/raas.json.pre-43-backup
       ```
       These backups stay until the human checkpoint clears them.

    3. Stop any OKM process holding LevelDB handles. OKM may run as:
       - Local `npm start` (dev mode) — `pkill -f 'node dist/index.js'` (verify with `pgrep -fl okb-server` first)
       - Docker container (if OKM is wired into rapid-automations docker-compose) — `cd ~/Agentic/_work/rapid-automations/docker 2>/dev/null && docker-compose stop okm 2>/dev/null` (if applicable)
       Surface in SUMMARY what was stopped + whether the operator needs to restart it post-swap.

    4. Atomic swap:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data
       mv leveldb leveldb.pre-43-backup
       mv leveldb-kmcore leveldb
       ```

    5. Smoke-check the new canonical store loads — inline node:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       node -e "
       import('@fwornle/km-core').then(async m => {
         const path = await import('path');
         const fs = await import('fs/promises');
         const { fileURLToPath } = await import('url');
         const kmCoreEntry = import.meta.resolve('@fwornle/km-core');
         let kmCoreRoot = path.dirname(fileURLToPath(kmCoreEntry));
         while (kmCoreRoot !== '/') {
           try { await fs.access(path.join(kmCoreRoot, 'package.json')); break; }
           catch { kmCoreRoot = path.dirname(kmCoreRoot); }
         }
         const ontologyDir = path.join(kmCoreRoot, 'config', 'ontology');
         const store = new m.GraphKMStore({ ontologyDir, dataDir: '.data/leveldb' });
         await store.open();
         let count = 0;
         for await (const _ of store.iterate()) count++;
         await store.close();
         console.log(JSON.stringify({ count }));
       });
       "
       ```
       Capture stdout. Assert `count >= Plan-07-totalEntities * 0.95` (within error budget).

    6. **DO NOT restart OKM yet.** OKM's source still imports the legacy modules — restarting would crash. Task 2 refactors source first; Task 3 builds + restarts.
  </action>
  <verify>
    <automated>test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb.pre-43-backup &amp;&amp; test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb &amp;&amp; test ! -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore</automated>
  </verify>
  <done>Dir swap complete; backup preserved; new canonical store iterates with ≥ 95% of Plan-07 entity count; OKM process stopped (recorded for restart in Task 3).</done>
  <acceptance_criteria>
    - Backup preserved: `test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb.pre-43-backup` exits 0.
    - New canonical at expected path: `test -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb` exits 0.
    - Migration source dir consumed: `test ! -d /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/.data/leveldb-kmcore` exits 0.
    - Behavior (post-swap iteration): inline node check reports `count` >= 0.95 * Plan-07-totalEntities.
    - Export backup: 3 *.pre-43-backup files exist at .data/exports/.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (Phase B): Refactor every consumer to await km-core; delete legacy + IGraphStore + adapter</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/index.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/pipeline.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/extractor.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/intelligence/*.ts (7 files), /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/km-store-adapter.ts, src/store/graph-store.ts, src/store/sync-manager.ts, src/store/persistence.ts, src/types/graph-store.ts, src/ontology/registry.ts, src/ontology/loader.ts, plus matching tests/unit/*.test.ts files</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md (§`intelligence/*.ts` — method mapping table + node-key delta; §`km-store-adapter.ts` — full deletion sequence; §`Cleanup grep gate template` — copy verbatim)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-07-PLAN.md (Task 2 action — the wave-controller flag-removal pattern, mirror for src/index.ts; content-validation-agent rewire pattern, mirror for src/intelligence/*.ts)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/km-store-adapter.ts (full 479 lines — confirm which IGraphStore methods OKM consumers actually use vs which are NotImplementedError stubs)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/types/graph-store.ts (full 61 lines — capture IGraphStore interface BEFORE deleting; surface method-set in SUMMARY for traceability)
    - Each consumer file's current state (`src/intelligence/*.ts`, `src/ingestion/*.ts`, `src/api/routes.ts`) — grep `graphStore\.` per file to enumerate the call set
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/CLAUDE.md (NO PARALLEL VERSIONS — edit each file in place; do NOT create *-v2.ts or *-async.ts)
  </read_first>
  <action>
    1. **Step B1.1 — Refactor src/index.ts bootstrap** (the strangler removal):
       - Open `src/index.ts`. Locate the `OKB_STORE_BACKEND` flag block (PATTERNS.md §`index.ts` lines 32-70).
       - Delete: the `storeBackend` variable, the `if (storeBackend === 'km-core') { ... } else { graphStore = new GraphStore(); }` block in its entirety.
       - Replace with the unconditional km-core construction (PATTERNS.md target state in <interfaces>). The `ontologyDir` resolver block (current lines 41-55) is REUSED as-is — it already works against the OKM-local submodule path post-Plan-02.
       - Update the `createServer(...)` call site to pass `kmStore` (not `graphStore`) per Task 1 step 1 of this plan. The pipeline + extractor + dedup constructors that previously took `graphStore: IGraphStore` now take `kmStore: GraphKMStore`.
       - **Do NOT use a dynamic `await import('@fwornle/km-core')`** if it's not needed. Static `import { GraphKMStore } from '@fwornle/km-core'` at top-of-file is simpler — only Plan 2's strangler needed dynamic import to avoid hard-dep. Now that km-core IS the hard dep, static import wins.

    2. **Step B1.2 — Refactor src/api/server.ts:**
       - Change `createServer(graphStore: IGraphStore, ...)` to `createServer(kmStore: GraphKMStore, ...)`.
       - Drop the `IGraphStore` + `GraphStore` imports.
       - If server.ts forwards graphStore to apiRoutes setup, forward kmStore instead.

    3. **Step B1.3 — Refactor src/api/routes.ts:**
       - Replace every `await pipeline.X()` or `graphStore.X()` with the km-core async equivalent per the method mapping table.
       - Wherever a route handler reads from the store, it becomes async (e.g., `const ents = []; for await (const e of kmStore.iterate()) ents.push(e);` instead of `const ents = graphStore.getAllEntities()`).
       - Node-key delta: any place routes.ts parses or constructs `${layer}:${id}` composite keys — drop the layer prefix. Use `entity.layer` field for filtering.
       - **REST response shapes MUST be preserved** (Plan 06 fixtures lock them). After this refactor, Plan 06's rest-contract.test.ts runs again in Task 3 — any drift surfaces immediately.

    4. **Step B1.4 — Refactor src/ingestion/pipeline.ts, deduplicator.ts, extractor.ts:**
       - These files currently consume `IGraphStore` (sync) via constructor injection. Change the constructor signature to `kmStore: GraphKMStore`.
       - Every `this.graphStore.X()` becomes `await this.kmStore.X()`.
       - Any method that was previously sync becomes async (cascade: a sync `IngestionPipeline.ingest()` that called sync `graphStore.addEntity()` becomes `async ingest()` that awaits `kmStore.putEntity()`). Trace the async cascade through to the route handler that invokes `pipeline.ingest()`; that handler is already async (route handlers always are), so the change is contained.

    5. **Step B1.5 — Refactor src/intelligence/*.ts (7 files):**
       - For EACH of clustering, confidence, connectivity, correlation, rca-lookup, search, temporal:
         - Constructor signature: `kmStore: GraphKMStore`.
         - Every sync `graphStore.X()` becomes `await this.kmStore.X()` per the mapping table.
         - Iteration patterns: `const all = graphStore.getAllEntities()` becomes `const all: Entity[] = []; for await (const e of this.kmStore.iterate()) all.push(e);`.
         - Node-key delta: strip layer prefixes; use `entity.layer` field directly.
         - If any method returns a Promise that wasn't a Promise before, update CALLERS (likely just the route handlers in routes.ts).

    6. **Step B2.1 — Delete legacy files (PATTERNS.md §km-store-adapter.ts deletion sequence):**
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       git rm src/store/km-store-adapter.ts
       git rm src/store/graph-store.ts
       git rm src/store/sync-manager.ts
       git rm src/store/persistence.ts
       git rm src/types/graph-store.ts
       git rm src/ontology/registry.ts
       git rm src/ontology/loader.ts
       git rm tests/unit/km-store-adapter.test.ts 2>/dev/null || true
       git rm tests/unit/graph-store.test.ts 2>/dev/null || true
       git rm tests/unit/sync-manager.test.ts 2>/dev/null || true
       git rm tests/unit/persistence.test.ts 2>/dev/null || true
       git rm tests/unit/ontology-loader.test.ts 2>/dev/null || true
       ```
       (Use `2>/dev/null || true` for test files that may not exist — they exist per PATTERNS.md but defend against partial-state.)

    7. **Step B2.2 — Cleanup grep gate (BLOCKING — copy verbatim from PATTERNS.md):**
       ```
       grep -rln 'IGraphStore\|GraphKMStoreAdapter\|OKB_STORE_BACKEND\|from.*store/graph-store\|from.*store/sync-manager\|from.*store/persistence\|from.*ontology/registry\|from.*ontology/loader' \
         /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ \
         /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/ \
         --include='*.ts' --include='*.js'
       ```
       MUST return zero matches. If any remain, fix in-place (likely a missed import in a test file or a missed `IGraphStore` type annotation) BEFORE proceeding.

    8. **Step B2.3 — Rename tests/integration/km-core-backend.test.ts** (CONTEXT canonical_refs note: "renames to .../storage-backend.test.ts (or similar) post-cutover since 'km-core' is no longer a non-default backend"):
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       git mv tests/integration/km-core-backend.test.ts tests/integration/storage-backend.test.ts
       ```
       Inside the renamed file: drop any `OKB_STORE_BACKEND=km-core` env setup (no longer applicable); the test now exercises the unconditional km-core path. Update describe text from "km-core backend" to "storage backend".

    9. **Step B2.4 — Build verification:**
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       npm run build
       ```
       MUST exit 0. Any TS error is a missed refactor — fix in-place and rebuild.

    10. **Defer test run to Task 3** — Task 2 ends at build-green. Running tests now may surface SC#3 contract drift; Task 3 handles tests + REST contract + Plan 06 fixture lock in a single pass with clearer logging.
  </action>
  <verify>
    <automated>! grep -rln 'IGraphStore\|GraphKMStoreAdapter\|OKB_STORE_BACKEND\|from.*store/graph-store\|from.*store/sync-manager\|from.*store/persistence\|from.*ontology/registry\|from.*ontology/loader' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/ --include='*.ts' --include='*.js' &amp;&amp; cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management &amp;&amp; npm run build</automated>
  </verify>
  <done>Every consumer await km-core; 7 source modules + 5 test files deleted; tests/integration/km-core-backend.test.ts renamed; cleanup grep gate green; build green.</done>
  <acceptance_criteria>
    - Grep gate (cleanup): `grep -rln 'IGraphStore\|GraphKMStoreAdapter\|OKB_STORE_BACKEND\|from.*store/graph-store\|from.*store/sync-manager\|from.*store/persistence\|from.*ontology/registry\|from.*ontology/loader' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/ --include='*.ts' --include='*.js'` returns no output.
    - Source deletions: `test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/km-store-adapter.ts && test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/graph-store.ts && test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/sync-manager.ts && test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/persistence.ts && test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/types/graph-store.ts && test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/registry.ts && test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/loader.ts` exits 0.
    - Survival assertion: `test -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/source-document-store.ts` exits 0 (OKM-specific, retained per CONTEXT.md).
    - Build assertion: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm run build` exits 0.
    - Rename: `test -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/storage-backend.test.ts && test ! -f /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/integration/km-core-backend.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (Phase C automated): Test suite + REST contract verification + restart OKM</name>
  <files>(no source change — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-06-SUMMARY.md (Plan 06 output — confirms fixtures + Zod schemas Plan 10 will run against; Task 3 here PROVES the cutover preserves contract before Plan 10 runs its formal byte-diff)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-07-PLAN.md (Task 3 Phase C action — workflow run + verification script pattern; adapt for OKM)
  </read_first>
  <action>
    1. Run the full OKM test suite:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       npm test 2>&1 | tee /tmp/43-08-test-output.log
       ```
       Exit 0 required. Capture pass count.

    2. **Specifically verify Plan 06's rest-contract.test.ts passes** — this is the SC#3 preview (Plan 10 owns the formal verification but a failure here would block Plan 10):
       ```
       npm test -- rest-contract.test.ts 2>&1 | tee /tmp/43-08-rest-contract-output.log
       ```
       Exit 0 required. If Zod parse fails or toEqual diffs, the cutover changed the REST shape — diagnose in-Task-3 (likely a missed async/sync return type or a node-key prefix that leaked into the response). The fixtures are LOCKED — fix the source, not the fixtures.

    3. **Verify Plan 07's migrate-json-replay.test.ts still passes** (the migration script itself shouldn't have broken):
       ```
       npm test -- migrate-json-replay.test.ts
       ```
       Exit 0. (This test uses tmpdir LevelDBs; the production .data/leveldb is untouched.)

    4. **Restart OKM** (whatever the stop step in Task 1 stopped). Operator-driven if a Docker container; programmatic if a local Node process. Surface restart command in SUMMARY.

    5. Smoke check: hit `/api/health` (or whatever OKM exposes — confirm from src/api/routes.ts):
       ```
       curl -s http://localhost:<port>/api/health
       ```
       Returns 200 + healthy. If unhealthy, capture logs into 43-08-VERIFY-FAIL.log and ABORT before human checkpoint.

    6. Spot-check a fixture endpoint against live OKM:
       ```
       curl -s http://localhost:<port>/api/entities | jq '.data | length'
       ```
       Returns >= 0.95 * Plan-07-totalEntities (within error budget; this is OKM's actual production data, not the seed fixture). Document the count in SUMMARY for the human checkpoint.

    7. Commit ONLY the source changes (NOT the production .data/leveldb state — that's a runtime artifact):
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       git add -A src/ tests/
       git status                                    # confirm .data/ is NOT staged
       git diff --cached --stat                      # for SUMMARY
       git commit -m "$(cat <<'EOF'
refactor(storage): cutover to km-core; delete legacy backend + IGraphStore + adapter (Phase 43 D-G3.1, D-G3.2)

Final-cleanup plan mirroring Phase 42 Plan 7's three-phase template:

Phase A (Task 1 — atomic dir swap):
- mv .data/leveldb → .data/leveldb.pre-43-backup
- mv .data/leveldb-kmcore → .data/leveldb  (Plan 07 populated this)
- Backup preserved until human checkpoint clears.
- Pre-cutover .data/exports/*.json backed up to *.pre-43-backup.

Phase B (Task 2 — source cleanup):
- DELETED (7 modules + matching tests):
  src/store/{km-store-adapter,graph-store,sync-manager,persistence}.ts
  src/types/graph-store.ts (IGraphStore interface)
  src/ontology/{registry,loader}.ts
  tests/unit/{km-store-adapter,graph-store,sync-manager,persistence,ontology-loader}.test.ts
- RENAMED tests/integration/km-core-backend.test.ts → storage-backend.test.ts.
- OKB_STORE_BACKEND flag REMOVED from src/index.ts.
- src/api/server.ts createServer signature: graphStore: IGraphStore → kmStore: GraphKMStore.
- Every consumer (pipeline, deduplicator, extractor, intelligence/*, api/routes.ts)
  switched from sync graphStore.X() to async await kmStore.X() per D-G3.2's
  mapping table. Node-key delta (strip ${layer}: prefix) handled per file.

Phase C (Task 3 — verification):
- Full OKM test suite: <pass-count>/<pass-count> green.
- Plan 06's rest-contract.test.ts: green — REST shapes preserved post-cutover.
- Plan 07's migrate-json-replay.test.ts: green.
- OKM restarted; /api/health 200; /api/entities returns ~<N> entities
  (Plan 07's totalEntities ± 5%).

Cleanup grep gate: zero residual references to deleted symbols in src/ + tests/.
source-document-store.ts SURVIVES (OKM-specific evidence traceability).

Plan 10 owns the formal byte-diff verification.
EOF
)"
       ```

    8. Outer gitlink bump:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations
       git add integrations/operational-knowledge-management
       git commit -m "chore: bump OKM submodule — Phase 43 Plan 08 (storage cutover + legacy deletion)"
       ```
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management &amp;&amp; npm test 2>&amp;1 | tail -5 &amp;&amp; git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s | grep -q 'D-G3.1'</automated>
  </verify>
  <done>Full OKM test suite green; rest-contract.test.ts green; OKM restarted; entity count matches expected; inner + outer commits land.</done>
  <acceptance_criteria>
    - Test assertion (full suite): `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test` exits 0.
    - Test assertion (REST contract preserved): `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test -- rest-contract.test.ts` exits 0.
    - Behavior assertion (live OKM): `curl -s http://localhost:<port>/api/health` returns 200 (port from OKM's config — surface in SUMMARY).
    - Behavior assertion (entity count): `curl -s http://localhost:<port>/api/entities | jq '.data | length'` returns >= 0.95 * Plan-07-totalEntities.
    - Inner commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s` matches `D-G3.1`.
    - Outer commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations log -1 --format=%s` matches `Plan 08`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (Phase C human gate): Operator confirms cutover health + approves backup cleanup window</name>
  <action>Operator runs the verification commands listed in <how-to-verify> below. The plan does NOT proceed until the operator types "approved" or describes failures for triage. This task is intentionally human-gated per autonomous: false.</action>
  <what-built>
    - Plans 43-01 through 43-07 landed: km-core layer pre-req + submodule + RA-root cleanup + ontology unification + maintenance routing + REST baseline + JSON-replay migration.
    - Plan 43-08 Phase A: production LevelDB swapped; `.data/leveldb.pre-43-backup/` preserved; `.data/exports/*.pre-43-backup` preserved.
    - Plan 43-08 Phase B: 7 modules + 5 unit tests deleted; OKB_STORE_BACKEND removed; every consumer refactored to await km-core; cleanup grep gate green.
    - Plan 43-08 Phase C automated: full OKM suite green; rest-contract.test.ts green; OKM restarted; entity count matches Plan-07 ± 5%.
  </what-built>
  <how-to-verify>
    1. **OKM live response shape (matches pre-migration):** Hit each fixture endpoint and spot-check structure:
       ```
       curl -s http://localhost:<port>/api/entities | jq '.data[0] | keys'
       curl -s http://localhost:<port>/api/relations | jq '.data[0] | keys'
       curl -s http://localhost:<port>/api/stats | jq .
       ```
       Field names should match what `tests/fixtures/pre-migration/api-entities.json` first object exposes.

    2. **Entity count parity:** `curl -s http://localhost:<port>/api/stats | jq .totalEntities` returns the same value as Plan 07 SUMMARY's `totalEntities` (± 5%; surfaces any silent dedup or drop).

    3. **Layer values represented:** `curl -s http://localhost:<port>/api/entities | jq '[.data[] | .layer] | unique'` returns at least `["evidence", "pattern"]` (both layers preserved through migration).

    4. **legacyId stamp visible:** `curl -s http://localhost:<port>/api/entities | jq '.data[0] | .legacyId'` returns `{ system: 'C', id: <uuid> }`.

    5. **OKB-baseline guard hygiene:** confirm `.data/exports/` files regenerate on next OKM commit-trigger event. After an ingest call (or after waiting the 5s debounce), `ls -la .data/exports/*.json` should show recent mtime.

    6. **Backup retention decision:** if SC#1-3 above all green, the operator MAY delete `.data/leveldb.pre-43-backup/` to free 17MB+. Recommended: keep at least 48h.

    7. **Restart OKM service (if needed)** — Task 3 attempted a restart but if OKM is wired into rapid-automations docker-compose, the operator may need to explicitly `docker-compose restart okm` (or equivalent).

    Respond "approved" if all above check out. If anything failed, describe what failed for triage. Plan 09 (re-embed) + Plan 10 (formal SC#3 verification) wait on this approval.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| atomic dir swap | mass file move on production data; recovery depends on backup integrity |
| consumer-by-consumer async refactor | every changed file is a potential silent contract drift point |
| REST response shape | locked by Plan 06 fixtures — any drift caught by rest-contract.test.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08-01 | Tampering | dir swap | mitigate | Backup .pre-43-backup preserved; OKM process stopped before mv (clean handle release); restart-and-verify-count BEFORE Task 2's source changes lock in. |
| T-43-08-02 | Tampering | sync→async refactor missed a call site | mitigate | TS build catches missing await on Promise return; cleanup grep gate covers known legacy symbols; rest-contract.test.ts catches shape drift. |
| T-43-08-03 | Information Disclosure | layer prefix stripping reveals previously-hidden cross-layer overlaps | accept | If two layers had entities with identical bare-UUIDs (shouldn't happen — UUIDs are unique), the second putEntity would no-op. Surface in SUMMARY if observed. |
| T-43-08-04 | Denial of Service | post-cutover OKM crashes | mitigate | Task 3 restart + /api/health smoke + human checkpoint visual verification. If crashed, rollback path: stop OKM, mv .data/leveldb .data/leveldb.failed-cutover, mv .data/leveldb.pre-43-backup .data/leveldb, restore from git an older OKM commit (pre-Plan-04). |
| T-43-08-05 | Repudiation | silent contract drift | mitigate | Plan 06 rest-contract.test.ts shape lock + Plan 10 formal byte-diff are the contract-drift defense; Task 3 runs the shape lock immediately. |
</threat_model>

<verification>
- Phase A: dir swap + backup preserved + smoke iteration matches Plan-07 count.
- Phase B: 7 source + 5 test files deleted; cleanup grep gate clean; build green.
- Phase C: full OKM suite + rest-contract.test.ts + migrate-json-replay.test.ts all green; live OKM /api/health returns 200; entity count matches.
- Human gate: operator confirms layer parity + legacyId stamps + REST shapes visually.
</verification>

<success_criteria>
1. .data/leveldb is the canonical-shape km-core store; .pre-43-backup preserved.
2. 12 files deleted (7 source + 5 test); zero residual references in src/ or tests/.
3. OKB_STORE_BACKEND flag + every conditional branch GONE.
4. Every consumer awaits km-core async API; build green.
5. Full OKM test suite green; rest-contract.test.ts green (REST shape preserved).
6. Live OKM healthy; entity count matches Plan-07 ± 5%.
7. Human operator confirms via checkpoint.
</success_criteria>

<output>
Create `.planning/phases/43-okm-cross-repo-migration-c/43-08-SUMMARY.md` when done, recording:
- Atomic swap outcome (timestamps; backup dir path)
- Per-file refactor summary (file → describe sync→async changes + node-key delta handling)
- 12 file deletions (paths)
- LoC delta (deletions vs additions)
- Cleanup grep gate output (must be empty)
- Test pass counts: pre-Plan-08 baseline vs post-Plan-08
- rest-contract.test.ts pass status
- Live OKM verification (curl outputs, entity count)
- Inner + outer commit SHAs
- Operator approval timestamp + any deferred items
</output>
</content>
</invoke>