---
phase: 43-okm-cross-repo-migration-c
plan: 05
type: execute
wave: 2
depends_on:
  - 43-01
  - 43-02
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/pipeline.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/index.ts
autonomous: true
requirements:
  - INT-03
user_setup: []

must_haves:
  truths:
    - "POST /api/cleanup/resolve-entities calls km-core's `resolveEntities` (imported from `@fwornle/km-core/maintenance`) — NOT OKM's local `pipeline.resolveEntities` / `deduplicator.resolveEntities`."
    - "OKM's local `pipeline.resolveEntities` (`src/ingestion/pipeline.ts:361`) is REMOVED entirely. `deduplicator.resolveEntities` (`src/ingestion/deduplicator.ts:620`) is REMOVED entirely. (D-G2.3)"
    - "OKM-specific preprocessing (PII pre-scan, sourceAuthority hints) is applied BEFORE the call to km-core `resolveEntities`, not woven into the resolve loop (D-G2.3 explicit). If no preprocessing exists today, the route handler simply forwards classes + dryRun."
    - "The `/api/km` mount at `src/api/server.ts:~55` is REVERTED. The `kmStore` parameter is removed from `createServer()` signature. (D-G2.4 — Phase 44 owns the common REST router.)"
    - "OKM build + tests green after the changes; any integration test that asserts on internal call paths is updated to assert on HTTP response shape (which is preserved)."
  artifacts:
    - path: "src/api/routes.ts (modified)"
      provides: "POST /api/cleanup/resolve-entities → km-core resolveEntities; response shape preserved for D-G5.1 fixtures lock"
    - path: "src/api/server.ts (modified)"
      provides: "createServer signature without kmStore param; no /api/km mount"
    - path: "src/ingestion/pipeline.ts (modified)"
      provides: "resolveEntities method DELETED; rest of pipeline unchanged"
    - path: "src/ingestion/deduplicator.ts (modified)"
      provides: "resolveEntities method DELETED; layered dedup phases (within-batch/cross-graph/per-class) preserved"
  key_links:
    - from: "POST /api/cleanup/resolve-entities (route handler)"
      to: "km-core resolveEntities"
      via: "imported from @fwornle/km-core/maintenance"
      pattern: "from\\s+['\"]@fwornle/km-core/maintenance['\"]"
    - from: "src/api/server.ts createServer signature"
      to: "(no longer has kmStore param)"
      via: "param removed"
      pattern: "(NOT) kmStore[\\?]?:\\s*GraphKMStore"
    - from: "src/api/server.ts body"
      to: "(no /api/km mount)"
      via: "deleted 8-line block per PATTERNS.md §api/server.ts"
      pattern: "(NOT) app\\.use\\(['\"]/api/km['\"]"
---

<objective>
Two consolidated D-Gx deliverables grouped because they touch the same files:

  1. **D-G2.3 — Route /api/cleanup/resolve-entities to km-core.** Delete OKM's local `pipeline.resolveEntities` (`pipeline.ts:361`) + `deduplicator.resolveEntities` (`deduplicator.ts:620`). The HTTP route handler in `src/api/routes.ts` switches to calling `resolveEntities` imported from `@fwornle/km-core/maintenance` (Phase 41 D-50 deliverable, modeled on OKM's impl). OKM-specific bits (PII pre-scan, sourceAuthority hints — if any exist today) are applied BEFORE the call, not woven into the resolve loop.
  2. **D-G2.4 — Revert /api/km common-router mount.** PATTERNS.md identifies `src/api/server.ts:~55` mounts a km-core common REST router with a "Phase 44 Plan 03" comment. That router is Phase 44's deliverable; it leaked into Phase 43's working tree. Delete the 8-line mount block. Remove the `kmStore?: GraphKMStore` parameter from `createServer()`. Wire-through changes propagate to `src/index.ts`'s `createServer(...)` call site.

**Critical constraint — REST contract preservation:** the HTTP response shape from `/api/cleanup/resolve-entities` MUST remain byte-identical (D-G5.1 fixtures lock contract in Plan 06; this plan runs in Wave 2 before fixtures are recorded, but the shape MUST already match the pre-migration response — that's the whole point of Plan 06). Capture the current response shape from the route handler before rewriting; preserve it verbatim in the new handler.

**Out of scope:** Storage cutover (Plan 08), data migration (Plan 07). The `graphStore: IGraphStore` parameter to createServer is UNTOUCHED here — Plan 08 owns that replacement after IGraphStore is deleted.

Purpose: Eliminate parallel maintenance impl; close out the Phase-44-leak.

Output: ~5 files modified; net LoC decrease (two methods + a mount block deleted; route handler shrinks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md

<interfaces>
<!-- Plan 41 D-50 shipped this surface in km-core -->

km-core maintenance (`@fwornle/km-core/maintenance` sub-path) per Phase 41:
```typescript
export async function resolveEntities(
  store: GraphKMStore,
  opts: {
    classes?: string[];
    dryRun?: boolean;
    concurrency?: number;
    batchSize?: number;
    llmMatcher?: LLMSemanticLayer;
    provenance?: ProvenanceStamp;
  }
): Promise<{
  merges: Array<{...}>;
  errors: string[];
}>;
```

OKM's defaults to preserve (PATTERNS.md from deduplicator.ts):
  - `concurrency: 3` (RESOLUTION_CONCURRENCY)
  - `batchSize: 30` (BATCH_SIZE)

<!-- OKM's current state to remove -->

OKM `pipeline.resolveEntities` (`src/ingestion/pipeline.ts:353-367`):
```typescript
async resolveEntities(dryRun = false, classes?: string[]): Promise<{...}> {
  return this.deduplicator.resolveEntities(this.graphStore, dryRun, classes);
}
```

OKM `deduplicator.resolveEntities` at `src/ingestion/deduplicator.ts:620` — the porting source for km-core's impl. Delete after route handler is rewired.

<!-- /api/km mount to revert (PATTERNS.md §api/server.ts lines 14-66) -->

```typescript
export async function createServer(
  graphStore: IGraphStore,
  syncManager: SyncManager,
  startupState: StartupState,
  ontologyRegistry?: OntologyRegistry,
  pipeline?: IngestionPipeline,
  llmService?: LLMService,
  dataDir?: string,
  sourceDocStore?: SourceDocumentStore,
  kmStore?: GraphKMStore,        // <— DROP this parameter
): Promise<Express> {
  // ... apiRoutes registration ...
  if (kmStore) {                                       // <— DELETE this 8-line block
    const { createKMRouter } = await import('@fwornle/km-core');
    const kmRouter = Router();
    createKMRouter(kmStore, kmRouter);
    app.use('/api/km', kmRouter);
    console.info('[api] Mounted km-core common REST router at /api/km/');
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert /api/km mount + drop kmStore param from createServer (D-G2.4)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/index.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts (full 67-line file — identify the exact 8-line mount block AND every reference to the `kmStore` parameter)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/index.ts (full file — locate the createServer call site; capture the current argument order)
    - /Users/Q284340/Agentic/coding/.planning/phases/43-okm-cross-repo-migration-c/43-PATTERNS.md (§`src/api/server.ts` — the two-commit recommendation; this plan only does Commit 1 — Plan 08 does Commit 2 to replace graphStore with kmStore directly)
  </read_first>
  <action>
    1. Edit `src/api/server.ts`. Locate the `if (kmStore) { ... app.use('/api/km', kmRouter); ... }` block (PATTERNS.md says lines ~55-66). DELETE the entire block (8 lines including the comment). Verify there are no other references to `kmRouter` or `createKMRouter` in the file. Remove the `kmStore?: GraphKMStore` parameter from `createServer()` signature. **DO NOT touch the `graphStore: IGraphStore` parameter** — Plan 08 handles that replacement after IGraphStore is deleted. Remove the now-unused `import` for `GraphKMStore` (or `Router`) at the top of the file IF no other use remains. Run `npm run build` and let TS guide cleanup.

    2. Edit `src/index.ts` createServer call site. Locate the call (something like `await createServer(graphStore, syncManager, startupState, ontologyRegistry, pipeline, llmService, dataDir, sourceDocStore, kmStore)`). Drop the trailing `kmStore` argument. If `kmStore` is instantiated nearby specifically for the `/api/km` mount AND has no other consumer, leave its instantiation untouched — Plan 08 owns the `kmStore`-only-path cutover. The `kmStore` variable becomes unused at the createServer call site only; if TS flags it as unused, suppress with a comment that points at Plan 08 (`// eslint-disable-next-line @typescript-eslint/no-unused-vars -- consumed by Plan 08 cutover`).

    3. Build + test: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm run build && npm test`. BOTH exit 0. If a test fails because it asserted `/api/km` mounted (which is the leaked Phase 44 router), DELETE the test assertion (or skip with `it.skip` + TODO referencing Phase 44). Surface in SUMMARY.

    4. Verify zero residue of the /api/km mount logic: `grep -nE "createKMRouter|/api/km" src/api/server.ts src/index.ts` (run from OKM root). MUST return zero matches in those two files. If matches exist elsewhere in src/, surface in SUMMARY.
  </action>
  <verify>
    <automated>! grep -E "createKMRouter|app\.use\(['\"]/api/km['\"]" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts &amp;&amp; cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management &amp;&amp; npm run build</automated>
  </verify>
  <done>`/api/km` mount + createKMRouter import + kmStore-param-on-createServer all removed; index.ts call site updated; build + tests green.</done>
  <acceptance_criteria>
    - Grep gate (mount deleted): `grep -cE "app\.use\(['\"]/api/km['\"]" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts` returns 0.
    - Grep gate (createKMRouter import gone): `grep -c "createKMRouter" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts` returns 0.
    - Grep gate (kmStore not in createServer signature): `grep -A 15 'export async function createServer' /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts | grep -cE 'kmStore[\?]?:\s*GraphKMStore'` returns 0.
    - Build assertion: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm run build` exits 0.
    - Test assertion: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewire POST /api/cleanup/resolve-entities to km-core; delete OKM-local resolveEntities methods (D-G2.3)</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/pipeline.ts, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts (lines 620-670 per PATTERNS.md — the current resolve-entities route handler; capture the response shape for contract preservation)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/pipeline.ts (lines 353-367 — the resolveEntities method to DELETE)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts (lines 610-720 — the resolveEntities method to DELETE; constants RESOLUTION_CONCURRENCY=3 and BATCH_SIZE=30 to capture before deletion; LLM-comparison logic to lift into okmLlmMatcher)
    - /Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts (the target API — confirm signature; PATTERNS.md cites "Algorithm (ported from OKM deduplicator.ts:620-720 with KM-Core deltas)")
    - /Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md (D-50 — original spec for km-core's resolveEntities; LLMSemanticLayer contract for the `llmMatcher` param)
  </read_first>
  <action>
    1. Open `src/api/routes.ts`. Locate the POST `/api/cleanup/resolve-entities` route handler (~lines 624-651 per PATTERNS.md). Capture (write to SUMMARY): (a) the current request shape (likely `{ classes, dryRun }`), (b) the current call (probably `await pipeline.resolveEntities(dryRun, classes)`), (c) the current response JSON shape (this is REST contract surface; D-G5.1 fixtures lock it in Plan 06 — preserve byte-identical).

    2. Read OKM's `deduplicator.resolveEntities` body. Lift the per-pair LLM-comparison logic into a free `okmLlmMatcher` factory (the rest of the loop body — batching, concurrency, scan-by-class — is km-core's responsibility). The shape:
       ```
       const okmLlmMatcher: LLMSemanticLayer = {
         compare: async (a: Entity, b: Entity) => {
           // delegated to OKM's existing LLM comparison logic; preserve OKM-specific prompts/thresholds verbatim
           return llmService.compareEntities(a, b);   // shape depends on OKM's LLMService API
         },
       };
       ```
       Wire this in routes.ts (or wherever the route handler accesses `llmService`). If OKM has a pre-loop preprocessing step in deduplicator.resolveEntities (PII pre-scan, sourceAuthority filter), lift it into a `preprocessClasses(classes): string[]` helper called BEFORE `resolveEntities`.

    3. Rewrite the route handler body in routes.ts:
       ```
       import { resolveEntities } from '@fwornle/km-core/maintenance';
       // ... in the handler:
       const { classes, dryRun = false, runId } = req.body ?? {};
       const filteredClasses = preprocessClasses(classes);   // OKM-specific preprocessing (D-G2.3)
       const result = await resolveEntities(this.kmStore, {
         classes: filteredClasses,
         dryRun,
         concurrency: 3,                            // OKM RESOLUTION_CONCURRENCY
         batchSize: 30,                             // OKM BATCH_SIZE
         llmMatcher: okmLlmMatcher,
         provenance: {
           provider: 'okm',
           model: 'cleanup',
           runId: runId ?? `okm-cleanup-${Date.now()}`,
           timestamp: new Date().toISOString(),
         },
       });
       res.json(result);   // PRESERVE the existing response shape — D-G5.1 locks contract in Plan 06
       ```
       The `this.kmStore` access depends on OKM's wiring (route class member vs closure vs middleware). Match the existing OKM pattern; if routes.ts is a free function module that already has `pipeline` and `deduplicator` available, add `kmStore` to its closure in the same way. PATTERNS.md notes routes.ts is a `controller / request-response` module.

    4. Delete `pipeline.resolveEntities` from `src/ingestion/pipeline.ts`. Use an editor or precise sed; verify before+after line counts. Save.

    5. Delete `deduplicator.resolveEntities` from `src/ingestion/deduplicator.ts`. The LLM-comparison logic captured in step 2 already lives in `okmLlmMatcher`. The constants RESOLUTION_CONCURRENCY + BATCH_SIZE are inlined into the route handler as literals 3 and 30. Keep the rest of the deduplicator (within-batch exact, cross-graph exact, per-class LLM semantic dedup phases — all still used inline by `IngestionPipeline`).

    6. Build + test: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm run build && npm test`. Both exit 0. The integration test for `/api/cleanup/resolve-entities` (likely `tests/integration/api-cleanup.test.ts` or similar — check `tests/integration/`) may need updates if it asserted on internal call paths. The HTTP response shape is unchanged; update test assertions accordingly.

    7. Grep gate — the two methods are truly gone AND no caller still invokes them:
       `grep -nE "pipeline\.resolveEntities|deduplicator\.resolveEntities" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ -r --include='*.ts'` MUST return zero matches.

    8. Grep gate — km-core import landed: `grep -c "from\s\+['\"]@fwornle/km-core/maintenance['\"]" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts` returns >= 1.
  </action>
  <verify>
    <automated>! grep -E "pipeline\.resolveEntities|deduplicator\.resolveEntities" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ -r --include='*.ts' &amp;&amp; grep -q "from\s\+['\"]@fwornle/km-core/maintenance['\"]" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts &amp;&amp; cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management &amp;&amp; npm run build</automated>
  </verify>
  <done>POST /api/cleanup/resolve-entities calls km-core; pipeline + deduplicator resolveEntities methods deleted; OKM-specific preprocessing applied before the call; build + tests green.</done>
  <acceptance_criteria>
    - Grep gate (km-core import in routes.ts): `grep -c "from\s\+['\"]@fwornle/km-core/maintenance['\"]" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts` returns >= 1.
    - Grep gate (local resolveEntities methods gone, no callers): `grep -cE "pipeline\.resolveEntities|deduplicator\.resolveEntities" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ -r --include='*.ts'` returns 0.
    - Grep gate (method bodies deleted): `grep -cE "^\s*async resolveEntities" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/pipeline.ts` returns 0 AND `grep -cE "^\s*async resolveEntities" /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ingestion/deduplicator.ts` returns 0.
    - Build assertion: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm run build` exits 0.
    - Test assertion: `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && npm test` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Commit OKM-side changes + bump rapid-automations gitlink</name>
  <files>(no source change — commit only)</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/CLAUDE.md (commit style)
  </read_first>
  <action>
    1. Inside OKM:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management
       git add src/api src/ingestion src/index.ts
       git status                                    # confirm only expected files
       git diff --cached --stat                      # capture for SUMMARY
       git commit -m "$(cat <<'EOF'
refactor(api): route /api/cleanup/resolve-entities to km-core; revert /api/km mount (Phase 43 D-G2.3, D-G2.4)

D-G2.3:
- POST /api/cleanup/resolve-entities now calls km-core's resolveEntities
  (from @fwornle/km-core/maintenance, Phase 41 D-50 — originally ported
  from this OKM impl).
- OKM-specific LLM-comparison logic lifted into a small okmLlmMatcher
  LLMSemanticLayer wrapping OKM's LLMService.
- OKM-specific preprocessing (PII pre-scan, sourceAuthority hints if any)
  applied BEFORE the call to km-core, not woven into the loop.
- DELETED: pipeline.resolveEntities (pipeline.ts:361) and
  deduplicator.resolveEntities (deduplicator.ts:620).

D-G2.4:
- REVERTED: the /api/km common-router mount that leaked from a Phase 44
  WIP commit (src/api/server.ts:~55).
- REMOVED: kmStore?: GraphKMStore parameter from createServer signature.
- src/index.ts updated to drop the trailing kmStore arg.

REST response shapes UNCHANGED (D-G5.1 fixtures lock contract in Plan 06).
Build + test suite green vs Plan 04 baseline.
EOF
)"
       ```

    2. Bump outer gitlink:
       ```
       cd /Users/Q284340/Agentic/_work/rapid-automations
       git add integrations/operational-knowledge-management
       git commit -m "chore: bump OKM submodule — Phase 43 Plan 05 (cleanup-resolve routing + /api/km revert)"
       ```

    3. Surface both SHAs in SUMMARY. Do NOT push.
  </action>
  <verify>
    <automated>git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s | grep -q 'D-G2.3' &amp;&amp; git -C /Users/Q284340/Agentic/_work/rapid-automations log -1 --format=%s | grep -q 'Plan 05'</automated>
  </verify>
  <done>OKM-internal commit landed; outer rapid-automations gitlink bumped; both SHAs in SUMMARY.</done>
  <acceptance_criteria>
    - Inner commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management log -1 --format=%s` matches `D-G2.3`.
    - Outer commit: `git -C /Users/Q284340/Agentic/_work/rapid-automations log -1 --format=%s` matches `Plan 05`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP /api/cleanup/resolve-entities ⇄ km-core resolveEntities | OKM-specific concerns (PII, sourceAuthority) must be applied at the boundary, not punted to km-core |
| createServer signature change | Any caller passing `kmStore` will TS-error — surfaces hidden coupling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05-01 | Tampering | response shape drift | mitigate | Capture pre-edit response shape in SUMMARY; reuse `res.json(result)` with km-core's return value which matches the OKM shape by construction (km-core impl was ported from OKM per Phase 41); Plan 06 fixtures-diff catches any residual drift. |
| T-43-05-02 | Information Disclosure | PII bypass | mitigate | OKM PII pre-scan moves to `preprocessClasses` step run BEFORE the km-core call; same filter set, just relocated. SUMMARY documents the pre-scan logic if any exists. |
| T-43-05-03 | Repudiation | LLM-call accounting drift | mitigate | `okmLlmMatcher.compare` delegates to OKM's LLMService unchanged; LLM-cost telemetry path preserved. |
| T-43-05-04 | Denial of Service | regression in /api/km consumer | accept | The /api/km mount was a Phase 44 WIP leak with no production consumer (no published API). Removal is safe. SUMMARY documents the deletion. |
</threat_model>

<verification>
- Pipeline + deduplicator resolveEntities methods deleted.
- routes.ts imports + calls km-core resolveEntities with OKM-preserved defaults.
- /api/km mount + kmStore param + createKMRouter import all gone.
- Build + tests green.
- Inner + outer commits land.
</verification>

<success_criteria>
1. POST /api/cleanup/resolve-entities calls km-core; response shape preserved.
2. OKM-specific preprocessing applied before the call.
3. Local resolveEntities methods deleted from pipeline + deduplicator.
4. /api/km mount + kmStore param removed; createServer signature simplified.
5. Build + tests green; inner + outer commits land.
</success_criteria>

<output>
Create `.planning/phases/43-okm-cross-repo-migration-c/43-05-SUMMARY.md` when done, recording:
- Captured pre-edit response JSON shape (for Plan 06 fixtures cross-reference)
- Any OKM-specific preprocessing lifted (PII / sourceAuthority / etc — or "none found")
- LoC delta (deleted vs added)
- Inner + outer commit SHAs + branch names
- Any test files updated (with reason)
</output>
</content>
</invoke>