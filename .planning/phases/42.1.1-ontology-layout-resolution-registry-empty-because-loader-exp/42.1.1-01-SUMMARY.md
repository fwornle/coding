---
phase: 42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp
plan: 01
subsystem: ontology
tags: [ontology, loader, path-resolution, registry, km-core, node-test]

# Dependency graph
requires:
  - phase: 42.1
    provides: findBestParent + ensureProjectAnchor + post-sweep code (correct, but blocked by SC#6 loader bug)
  - phase: 42-03
    provides: OntologyRegistry adoption via LegacyOntologyAdapter (registry-adoption.test.ts canary)
  - phase: 38
    provides: km-core OntologyRegistry public API (isValidClass, getLoadedDomains)
provides:
  - ontologyPathResolver helper module (layout-tolerant resolver — both two-tier and flat)
  - OntologyConfigManager.{validatePaths, injectOntology} routed through the resolver
  - Public re-export of resolveOntologyPath via ontology barrel
  - 13 new node:test cases (9 resolver unit + 4 layout integration) — all green
affects: [42.1, 42.1-verifier, future-callers, insight-generation-agent-cleanup]

# Tech tracking
tech-stack:
  added: []  # no new packages — pure source-side helper
  patterns:
    - "Layout-tolerant ontology lookup via dirname-walk + ordered probe list"
    - "Test-only probe counter (`__resetProbeCounter`/`__getProbeCount`) for cache-hit assertions without filesystem mutation"
    - "Module-level Map cache keyed by `(kind, team, ontologyDir)` with `__clearCache` test-flush hook"
    - "Structured one-shot stderr layout-detected log (`[OntologyConfigManager] <kind> ontology resolved: layout=… alias=… path=…`)"

key-files:
  created:
    - src/ontology/ontologyPathResolver.ts (resolver + OntologyLayoutDetected + OntologyPathNotFoundError)
    - src/ontology/ontologyPathResolver.test.ts (9 unit tests)
    - src/ontology/OntologyConfigManager.layout.test.ts (4 integration tests)
  modified:
    - src/ontology/OntologyConfigManager.ts (both `fs.existsSync` probe sites — validatePaths and injectOntology)
    - src/ontology/index.ts (single-line barrel re-export)

key-decisions:
  - "Test C softened per user Option A — dropped isValidClass('Project') (impossible — Project not declared in any on-disk ontology), added domains.length === 8, kept isValidClass('Component') canary"
  - "Path-a invariant preserved — zero modifications to caller files (tools.ts, ontology-classification-agent.ts, insight-generation-agent.ts, persistence-agent.ts) and zero modifications to .data/ontologies/*"
  - "Resolver uses synchronous fs.existsSync to match OntologyConfigManager's existing pattern; cache-hit assertion uses probe-counter strategy not 'delete and retry'"
  - "Team value sanitised via path.basename() before substitution (T-42.1.1-01 mitigation)"
  - "Verification boundary maintained — 42.1.1 proves the loader works (layer 1); SC#6 wave-analysis re-run remains Phase 42.1's promotion gate"

patterns-established:
  - "dirname-walk ancestor location: walk up from configHint until basename === 'ontologies' && parent basename === '.data', fall back to <basePath>/.data/ontologies on miss"
  - "Probed-paths error message: OntologyPathNotFoundError lists every absolute path tried, one per line, so layout drift produces immediately actionable errors"
  - "Test repo-root walk: 4 `..` segments from dist/ontology/<file>.test.js (dist → mcp-server-semantic-analysis → integrations → coding) — 2-segment walk silently lands on dist/.data/ontologies and is the documented regression pattern"

requirements-completed: []

# Metrics
duration: 17m
completed: 2026-05-24
---

# Phase 42.1.1 Plan 01: Ontology Layout Resolution Summary

**Layout-tolerant `ontologyPathResolver` wired into `OntologyConfigManager.{validatePaths, injectOntology}` — loader now accepts the two-tier paths callers construct and finds the flat-layout files actually on disk (zero caller changes, zero `.data/ontologies/*` changes).**

## Performance

- **Duration:** 17 min (continuation-agent slice; full plan including prior session ~50 min)
- **Started (continuation):** 2026-05-24T17:12:00Z (approx — session resumed)
- **Completed:** 2026-05-24T17:29:13Z
- **Tasks:** 3 (1 prior + 2 in this slice — Task 2 GREEN + Task 3 barrel)
- **Files modified (this slice):** 3 (`OntologyConfigManager.ts`, `OntologyConfigManager.layout.test.ts`, `index.ts`)

## Accomplishments

- `OntologyConfigManager.validatePaths()` and `injectOntology()` both route through `resolveOntologyPath` — initialise succeeds against the real flat `.data/ontologies/` layout for every team currently on disk (agentic, cluster-reprocessing, code-entities, coding, raas, resi, ui).
- Resolved upper/lower paths are written back into the config in place so downstream watchers and `getStatus()` see the absolute resolved paths (not the caller-passed two-tier strings).
- One-shot structured stderr log line per resolve: `[OntologyConfigManager] <kind> ontology resolved: layout=<two-tier|flat> alias=<canonical|upper.json> path=<absolute>` — operators can confirm post-incident which layout served the registry (T-42.1.1-03).
- Fresh `OntologyRegistry({ ontologyDir })` constructed over the post-initialise `ontologyDir` loads all 8 on-disk domain JSONs (upper + 7 team JSONs) — proves the resolver-then-registry chain reads every flat-layout file.
- Resolver helpers (`resolveOntologyPath`, `OntologyLayoutDetected`, `OntologyPathNotFoundError`) publicly re-exported via `src/ontology/index.ts` — accessible via `import { resolveOntologyPath } from '@coding/mcp-server-semantic-analysis/dist/ontology/index.js'`.
- 18/18 node:test cases pass across 3 test files; existing Phase 42-03 `registry-adoption.test.js` still green (zero regression).

## Task Commits

Each task was committed atomically (this slice continued from a checkpoint after Task 1 + the test-RED of Task 2 already landed):

1. **Task 1 RED: ontologyPathResolver failing tests** — `6bde70ba0` (test)
2. **Task 1 GREEN: ontologyPathResolver implementation** — `6933264ed` (feat)
3. **Task 2 RED: OntologyConfigManager layout integration failing tests** — `ef9a4e9bd` (test)
4. **Task 2 GREEN: wire OntologyConfigManager + soften Test C** — `e341152a8` (feat) ← this slice
5. **Task 3: public re-export of resolveOntologyPath via barrel** — `00c6ca154` (feat) ← this slice

## Files Created/Modified

- `src/ontology/ontologyPathResolver.ts` (created Task 1) — `resolveOntologyPath` helper, `OntologyLayoutDetected` shape, `OntologyPathNotFoundError` class, module-level Map cache, test-only probe counter helpers.
- `src/ontology/ontologyPathResolver.test.ts` (created Task 1) — 9 unit tests across both layouts × every on-disk team + cache-hit probe-counter assertion + error-message shape + team-input sanitisation.
- `src/ontology/OntologyConfigManager.ts` (modified Task 2) — both `fs.existsSync` probe-and-throw sites (Site 1: `validatePaths()` lines 143-193; Site 2: `injectOntology()` lines 332-396) replaced by `resolveOntologyPath` calls inside try/catch with config-rewrite + structured stderr log on success.
- `src/ontology/OntologyConfigManager.layout.test.ts` (created Task 2, softened in this slice) — 4 integration tests against the REAL `.data/ontologies/` flat layout (Test A parameterised across 7 teams, Test B config-rewrite assertions, Test C softened verification-boundary smoke, Test D probed-paths error message).
- `src/ontology/index.ts` (modified Task 3) — single-line `export * from './ontologyPathResolver.js';` added immediately after the existing `export * from './OntologyConfigManager.js';` re-export. `createOntologySystem` factory preserved verbatim.

## Detected on-disk layout (Test A integration run)

| Kind  | Layout | Alias        | Resolved path                                                         |
|-------|--------|--------------|-----------------------------------------------------------------------|
| upper | flat   | `upper.json` | `/Users/Q284340/Agentic/coding/.data/ontologies/upper.json`           |
| lower | flat   | `canonical`  | `/Users/Q284340/Agentic/coding/.data/ontologies/<team>-ontology.json` |

For team `coding`: `/Users/Q284340/Agentic/coding/.data/ontologies/coding-ontology.json`. All 7 teams resolved via the flat fallback exactly as expected from the on-disk reality documented in CONTEXT.md `<interfaces>` lines 167-178.

## Test C registry forensic trail

Captured stderr from `node --test dist/ontology/OntologyConfigManager.layout.test.js`:

```
[Test C] ontologyDir resolved to: /Users/Q284340/Agentic/coding/.data/ontologies
[Test C] getLoadedDomains().length = 8
[Test C] domains = upper, agentic-ontology, cluster-reprocessing-ontology, code-entities-ontology, coding-ontology, raas-ontology, resi-ontology, ui-ontology
[Test C] domains.length=8, sample classes: Component=valid, Project=NOT loaded (see SUMMARY known-residuals)
```

km-core registry also emitted (informational, expected per D-27 in 38-CONTEXT.md):

```
[km-core/ontology-registry] class 'IntervalSet' redefined: cluster-reprocessing-ontology → raas-ontology (last-loaded wins)
[km-core/ontology-registry] class 'FunctionOrchestrator' redefined: cluster-reprocessing-ontology → resi-ontology (last-loaded wins)
```

## Layout-detected stderr lines (Test A — 7 teams × 2 kinds)

Captured during Test A's 7-team initialise sweep (one matched pair per team, 14 lines total — `upper` always resolves to the same `upper.json` alias because the upper ontology is team-agnostic):

```
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/Users/Q284340/Agentic/coding/.data/ontologies/agentic-ontology.json
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/Users/Q284340/Agentic/coding/.data/ontologies/cluster-reprocessing-ontology.json
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/Users/Q284340/Agentic/coding/.data/ontologies/code-entities-ontology.json
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/Users/Q284340/Agentic/coding/.data/ontologies/coding-ontology.json
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/Users/Q284340/Agentic/coding/.data/ontologies/raas-ontology.json
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/Users/Q284340/Agentic/coding/.data/ontologies/resi-ontology.json
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/Users/Q284340/Agentic/coding/.data/ontologies/ui-ontology.json
```

Every line landed on the `flat` layout with the expected alias — no `two-tier` resolution in production today, exactly matching CONTEXT.md's on-disk reality observation. Deviation from the locked alias table in CONTEXT.md: **none**.

## Path-a invariant — caller files + on-disk ontologies UNCHANGED

```
$ git status --short \
    integrations/mcp-server-semantic-analysis/src/agents/ \
    integrations/mcp-server-semantic-analysis/src/tools.ts \
    .data/ontologies/
(empty — zero modifications)
```

Confirms:
- `integrations/mcp-server-semantic-analysis/src/tools.ts` UNCHANGED
- `integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts` UNCHANGED
- `integrations/mcp-server-semantic-analysis/src/agents/insight-generation-agent.ts` UNCHANGED
- `integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts` UNCHANGED
- `.data/ontologies/upper.json` UNCHANGED
- `.data/ontologies/{agentic,cluster-reprocessing,code-entities,coding,raas,resi,ui}-ontology.json` UNCHANGED

Path-a (loader-side only, no file moves, no caller path-construction changes) preserved exactly per CONTEXT.md `<decisions>`.

## Build + test commands executed

| Command                                                                                                                                                                                                                | Exit |
|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------|
| `cd integrations/mcp-server-semantic-analysis && npm run build` (Task 2 GREEN gate)                                                                                                                                    | 0    |
| `cd integrations/mcp-server-semantic-analysis && node --test dist/ontology/OntologyConfigManager.layout.test.js` (4/4 pass)                                                                                            | 0    |
| `cd integrations/mcp-server-semantic-analysis && npm run build` (Task 3 gate)                                                                                                                                          | 0    |
| `cd integrations/mcp-server-semantic-analysis && node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/OntologyConfigManager.layout.test.js dist/ontology/registry-adoption.test.js` (18/18 pass)       | 0    |
| `cd integrations/mcp-server-semantic-analysis && node -e "import('./dist/ontology/index.js').then(m => { if (!m.resolveOntologyPath) { process.stderr.write('FAIL\n'); process.exit(1); } process.stderr.write('OK\n'); });"` (prints `OK` on stderr) | 0    |

## Decisions Made

- **Followed CONTEXT.md verification boundary** — Tests A/B/D match the plan verbatim; Test C softened only to handle the on-disk reality the plan did not anticipate (no `Project` class declared).
- **Did NOT use TDD `delete-and-retry` cache assertion** — chose the probe-counter strategy explicitly because the plan locks it as "the single canonical assertion" (PLAN.md line 234).
- **Stderr logging via `process.stderr.write`** — per CLAUDE.md no-console-log constraint; preserved across both `validatePaths()` and `injectOntology()` log sites and the Test C forensic logs.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4 → user-approved Option A] Test C `isValidClass('Project') === true` assertion impossible**

- **Found during:** Task 2 GREEN (previous executor checkpoint)
- **Issue:** The plan's Task 2 must-haves and Test C step (6) required `registry.isValidClass('Project') === true` after a fresh `OntologyRegistry` is constructed over the post-initialise `ontologyDir`. However, after inspecting every on-disk `.data/ontologies/*.json`, the `Project` class is NOT declared anywhere:

  | File                                  | Sample class names (first 5)                                                              |
  |---------------------------------------|--------------------------------------------------------------------------------------------|
  | `upper.json`                          | File, Service, Feature, Contract, RuntimeDiagnostics                                       |
  | `coding-ontology.json`                | LSLSession, ClassificationLayer, ConstraintRule, TrajectoryState, MCPAgent                 |
  | `agentic-ontology.json`               | AgentFramework, RAGSystem, CommunicationProtocol, ModuleContent, Exercise                  |
  | `cluster-reprocessing-ontology.json`  | RecordedData, VirtualTarget, RPU, CompoundReprocessing, KPIFramework                       |
  | `code-entities-ontology.json`         | CodeFunction, CodeClass, CodeModule, DocumentationLink, CodeRelationship                   |
  | `raas-ontology.json`                  | EventMeshNode, DataContract, SchemaRegistry, ArgoWorkflowTemplate, KubernetesCluster       |
  | `resi-ontology.json`                  | EmbeddedFunction, FunctionOrchestrator, MF4Container, MCAPContainer, ProtobufSPP           |
  | `ui-ontology.json`                    | AgentInstance, WorkflowOrchestration, AWSLambdaFunction, AWSStepFunction, APIGatewayEndpoint |

  `Project` is what `ensureProjectAnchor('Coding')` mints at runtime, but no ontology JSON declares it. Asserting `isValidClass('Project') === true` would either (a) require editing `.data/ontologies/upper.json` (path-b, explicitly rejected) or (b) require running a one-shot bootstrap to register `Project` (out of scope for the loader fix).

- **Fix (Rule 4 → user Option A):** Softened Test C:
  - **DROPPED:** `assert.equal(registry.isValidClass('Project'), true, ...)`
  - **ADDED:** `assert.equal(domains.length, 8, ...)` — proves all 8 on-disk JSONs loaded (loader-chain works end-to-end)
  - **KEPT:** `assert.ok(domains.length > 0, ...)` (loader works at all)
  - **KEPT:** `assert.equal(registry.isValidClass('Component'), true, ...)` (canary that IS in coding-ontology.json — matches `registry-adoption.test.ts:88`)
  - **KEPT:** `assert.equal(registry.isValidClass('NonExistentClassXyz123'), false, ...)` (negative-case sanity)
  - **UPDATED Test C log line:** captures `domains.length`, the actual domain list, and "Project=NOT loaded (see SUMMARY known-residuals)" for forensic trail

- **Rationale (per CONTEXT.md line 93-94):** *"SC#6 pass for Phase 42.1 (re-run its verifier) is the PROMOTION GATE, not a 42.1.1 acceptance gate — keep them separate so 42.1.1 can land independently."* 42.1.1's contract is to prove the loader works (layer 1 of the SC#6 root cause). Registering the `Project` class on disk (layer 2) is a separate follow-up tracked below as a NEW known residual.

- **Files modified:** `src/ontology/OntologyConfigManager.layout.test.ts` (Test C only)
- **Verification:** `node --test dist/ontology/OntologyConfigManager.layout.test.js` exits 0; 4/4 tests pass; Test C confirms `getLoadedDomains().length = 8` matching the on-disk JSON count.
- **Committed in:** `e341152a8` (Task 2 GREEN commit)

---

**Total deviations:** 1 (user-approved Option A, surfaced via Rule 4 → architectural checkpoint).
**Impact on plan:** Test C still proves the verification-boundary intent (loader chain reads every on-disk domain), just via a different assertion that matches on-disk reality. Plan scope unchanged. No other tasks deferred.

## Known Residuals (in addition to the 3 from the plan)

**NEW (introduced by Option A finding):**

- **Layer 2 of SC#6 root cause — `Project` class not declared on disk.** `ensureProjectAnchor('Coding')` mints a node of class `Project`, but no `.data/ontologies/*.json` declares a `Project` class. The loader fix from this phase unblocks layer 1 (registry now loads classes successfully), but `ensureProjectAnchor` will still raise `Unknown ontology class: Project` at runtime against the current on-disk JSONs. **Follow-up ticket (separate from 42.1.1 and 42.1):** add `Project` to `.data/ontologies/upper.json` (or `coding-ontology.json`) with a minimal schema (id, name, description, team) sufficient for `ensureProjectAnchor` and the post-sweep code path. This is the second layer of the SC#6 cascade and must land before Phase 42.1's `/gsd-verify-phase 42.1` will pass. Detected at 2026-05-24T17:25Z; deferred per CONTEXT.md path-a scope.

**Carried from the plan (per `<output>` § "Known residual limitations"):**

- `integrations/mcp-server-semantic-analysis/src/agents/insight-generation-agent.ts:350-395` (`loadOntologyDescriptions()`) still uses raw `fs.existsSync` against the two-tier path. Decorative-only (pretty-print description strings); does NOT block SC#6 unblock. Separate ticket should route this caller through `resolveOntologyPath` for consistency.
- Legacy Jest ontology suite under `tests/unit/ontology/` (≥ 60 failing tests) is pre-existing tech debt from Phase 42-03's deletion of `src/ontology/OntologyManager.ts`. Separate hygiene ticket.
- Forensic-report Anomalies 3 (anchor-pass bookkeeping), 5 (`registry.hasClass is not a function` API drift — note the current registry surface is `isValidClass`, not `hasClass`), and 6 (docker-logs recipe) per CONTEXT.md `<deferred>`.

## Issues Encountered

- During the previous executor's RED phase, all 4 layout integration tests went red as designed before Site 1 + Site 2 were migrated. The previous session reached a checkpoint when the Test C `isValidClass('Project')` assertion could not be satisfied; user supplied Option A. This continuation slice picked up at that checkpoint, applied the softening, committed Task 2 GREEN, then executed Task 3.

## User Setup Required

None — no environment variables, no external service configuration, no secrets.

## Operator hand-off (post-merge, required for production deployment)

Per `CLAUDE.md` § "Rebuilding After Code Changes", the submodule `integrations/mcp-server-semantic-analysis` runs in the `coding-services` Docker container. To pick up the recompiled `dist/ontology/`, the operator MUST run:

```bash
cd /Users/Q284340/Agentic/coding/docker && \
  docker-compose build coding-services && \
  docker-compose up -d coding-services
```

The in-repo `node --test dist/ontology/*.test.js` runs above are the AC for code correctness; the Docker rebuild is the deployment vehicle and is explicitly NOT a Task `<verify>` step. After Docker comes up, the operator can verify the loader log line appears in container stderr by spawning any pipeline path that constructs an `OntologyConfigManager` (e.g. a wave-analysis dry run); expected output:

```
[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/coding/.data/ontologies/upper.json
[OntologyConfigManager] lower ontology resolved: layout=flat alias=canonical path=/coding/.data/ontologies/coding-ontology.json
```

After the Docker rebuild AND the `Project` class is added on disk (layer-2 follow-up above), Phase 42.1's SC#6 re-run via `/gsd-verify-phase 42.1` becomes the promotion gate.

## Next Phase Readiness

- **Phase 42.1 verifier UNBLOCKED for layer 1 (loader).** Layer 2 (Project class on disk) is the gating follow-up before `/gsd-verify-phase 42.1` will pass.
- **Future callers** that want layout-tolerant ontology lookup can `import { resolveOntologyPath } from '@coding/mcp-server-semantic-analysis/dist/ontology/index.js'` — the cleanup of `insight-generation-agent.ts:350-395` is the obvious first consumer.
- **No regressions.** Existing Phase 42-03 `registry-adoption.test.js` (5 tests) still green; no caller files modified; no `.data/ontologies/*` modified.

## Self-Check

| Check                                                                                              | Status |
|----------------------------------------------------------------------------------------------------|--------|
| `src/ontology/ontologyPathResolver.ts` exists                                                      | FOUND  |
| `src/ontology/ontologyPathResolver.test.ts` exists                                                 | FOUND  |
| `src/ontology/OntologyConfigManager.layout.test.ts` exists                                         | FOUND  |
| `src/ontology/OntologyConfigManager.ts` contains `resolveOntologyPath` import                      | FOUND  |
| `src/ontology/index.ts` contains `export * from './ontologyPathResolver.js'`                       | FOUND  |
| Commit `6bde70ba0` (test RED Task 1) in git log                                                    | FOUND  |
| Commit `6933264ed` (feat GREEN Task 1) in git log                                                  | FOUND  |
| Commit `ef9a4e9bd` (test RED Task 2) in git log                                                    | FOUND  |
| Commit `e341152a8` (feat GREEN Task 2 + Test C softened) in git log                                | FOUND  |
| Commit `00c6ca154` (feat Task 3 barrel re-export) in git log                                       | FOUND  |
| `node --test dist/ontology/ontologyPathResolver.test.js` exits 0 (9 pass)                          | PASS   |
| `node --test dist/ontology/OntologyConfigManager.layout.test.js` exits 0 (4 pass)                  | PASS   |
| `node --test dist/ontology/registry-adoption.test.js` exits 0 (5 pass, zero regression)            | PASS   |
| Barrel-import sanity (`m.resolveOntologyPath` truthy after `import('./dist/ontology/index.js')`)   | PASS   |
| Caller files unchanged (tools.ts + 3 agents)                                                       | PASS   |
| `.data/ontologies/*` unchanged                                                                     | PASS   |

## Self-Check: PASSED

---
*Phase: 42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp*
*Completed: 2026-05-24*
