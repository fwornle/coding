---
phase: 42.1.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ontology/ontologyPathResolver.ts
  - src/ontology/ontologyPathResolver.test.ts
  - src/ontology/OntologyConfigManager.ts
  - src/ontology/OntologyConfigManager.layout.test.ts
  - src/ontology/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "OntologyConfigManager.initialize() succeeds against the current flat .data/ontologies/ layout (no file moves required) for team coding"
    - "After OntologyConfigManager.initialize() succeeds against the flat layout, an OntologyRegistry constructed with the same ontologyDir (from getConfig() post-initialize) loads >0 entity classes for team coding (assertion via km-core's getLoadedDomains() length AND isValidClass('Project') returning true — the specific class ensureProjectAnchor mints)"
    - "After initialize(), getConfig().upperOntologyPath and getConfig().lowerOntologyPath return the resolved absolute paths to on-disk files (not the caller-passed config strings — proves the resolver rewrote the config in place)"
    - "The path resolver finds upper + lower ontologies under BOTH the two-tier layout (.data/ontologies/{upper,lower}/<file>.json) AND the current flat layout (.data/ontologies/<file>.json, plus .data/ontologies/upper.json alias for upper, plus .data/ontologies/<team>-ontology.json for lower)"
    - "When neither layout contains the requested file, the resolver throws an error whose message lists every absolute path it probed"
    - "All 4 callers (tools.ts, ontology-classification-agent.ts, insight-generation-agent.ts, persistence-agent.ts) compile cleanly (tsc 0 errors) AND their fs.existsSync / path-construction code is unmodified (git status shows zero changes to those 4 files)"
  artifacts:
    - path: "src/ontology/ontologyPathResolver.ts"
      provides: "resolveOntologyPath({kind, team, configHint?, basePath?}) helper + OntologyLayoutDetected type + OntologyPathNotFoundError class"
      exports: ["resolveOntologyPath", "OntologyLayoutDetected", "OntologyPathNotFoundError"]
      min_lines: 80
    - path: "src/ontology/ontologyPathResolver.test.ts"
      provides: "Unit test coverage of the resolver — both layouts × {upper, lower} × every team currently in .data/ontologies/"
      min_lines: 100
    - path: "src/ontology/OntologyConfigManager.ts"
      provides: "validatePaths() + injectOntology() route through resolveOntologyPath instead of raw fs.existsSync on config strings"
      contains: "resolveOntologyPath"
    - path: "src/ontology/OntologyConfigManager.layout.test.ts"
      provides: "Integration test — initialize() succeeds against actual .data/ontologies/ flat layout AND a freshly-constructed OntologyRegistry against the resolved ontologyDir loads >0 classes including 'Project' for team coding"
      min_lines: 60
    - path: "src/ontology/index.ts"
      provides: "Public re-export of the resolver helpers"
      contains: "ontologyPathResolver"
  key_links:
    - from: "src/ontology/OntologyConfigManager.ts validatePaths()"
      to: "src/ontology/ontologyPathResolver.ts resolveOntologyPath"
      via: "direct import"
      pattern: "import.*resolveOntologyPath.*from.*ontologyPathResolver"
    - from: "src/ontology/OntologyConfigManager.ts injectOntology()"
      to: "src/ontology/ontologyPathResolver.ts resolveOntologyPath"
      via: "direct import (second fs.existsSync site)"
      pattern: "resolveOntologyPath"
    - from: "src/ontology/OntologyConfigManager.ts (structured-log object)"
      to: "src/ontology/ontologyPathResolver.ts OntologyLayoutDetected"
      via: "type import for the stderr-log payload shape"
      pattern: "import.*OntologyLayoutDetected.*from.*ontologyPathResolver"
    - from: "src/ontology/index.ts"
      to: "src/ontology/ontologyPathResolver.ts"
      via: "public re-export"
      pattern: "export \\* from './ontologyPathResolver"
---

<objective>
Resolve the empty-ontology-registry blocker (Phase 42.1 SC#6 root cause) by adding a path-resolution layer to the central ontology loader so `OntologyConfigManager.validatePaths()` can find ontology JSONs under BOTH the legacy two-tier layout (`.data/ontologies/{upper,lower}/<file>.json`) AND the current flat layout (`.data/ontologies/<file>.json` + the historical `.data/ontologies/upper.json` alias).

Purpose: Phase 42.1's findBestParent + ensureProjectAnchor + post-sweep code is already correct and merged, but it cannot be validated end-to-end because `validatePaths()` throws "Upper ontology not found …" against the actual on-disk flat layout — registry initialises with zero classes, ensureProjectAnchor fails, 68 in-scope orphans + 7 duplicated L2 Components in production. This is "path (a)" per the forensic report: smallest blast radius, loader-side only, no file moves, no caller path-construction changes.

Output: A new `ontologyPathResolver.ts` helper module + tests, `OntologyConfigManager` wired to use it at both `fs.existsSync` sites (lines 126-146 and 280-310), and a public re-export so `import { resolveOntologyPath } from '@/ontology'` works for any future caller that wants the same layout-tolerance.

**Build pipeline note (CRITICAL):** `src/ontology/` is symlinked from the repo root into `integrations/mcp-server-semantic-analysis/src/ontology` (verified via `ls -la integrations/mcp-server-semantic-analysis/src/ontology` → `-> ../../../src/ontology`). The submodule's `tsconfig.json` includes `src/**/*`, so the canonical compile path for these files is `cd integrations/mcp-server-semantic-analysis && npm run build` (which runs `tsc && shx chmod +x dist/*.js`). The repo root `package.json` has NO `build` script — only `test`, `lint`, etc. All `<verify>` blocks in this plan therefore invoke the submodule build. Note that the submodule SUMMARY operator hand-off MUST remind the operator to run `docker-compose build coding-services && docker-compose up -d coding-services` (per `CLAUDE.md` § "Rebuilding After Code Changes") before this fix reaches production — but that Docker step is an operator action, NOT a Task `<verify>` (in-repo `node --test dist/ontology/*.test.js` is the acceptance signal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-CONTEXT.md
@.planning/forensics/report-20260524-130355.md
@CLAUDE.md

<interfaces>
<!-- Loader surface (will be modified). Extracted from src/ontology/OntologyConfigManager.ts -->

From src/ontology/OntologyConfigManager.ts (lines 126-146 — first fs.existsSync site):
```
private async validatePaths(): Promise<void> {
  const errors: string[] = [];
  if (this.config.upperOntologyPath) {
    const upperPath = this.resolvePath(this.config.upperOntologyPath);
    if (!fs.existsSync(upperPath)) {
      errors.push(`Upper ontology not found: ${upperPath}`);
    }
  }
  if (this.config.lowerOntologyPath) {
    const lowerPath = this.resolvePath(this.config.lowerOntologyPath);
    if (!fs.existsSync(lowerPath)) {
      errors.push(`Lower ontology not found: ${lowerPath}`);
    }
  }
  if (errors.length > 0) {
    throw new Error(`Ontology configuration validation failed:\n${errors.join('\n')}`);
  }
}
```

From src/ontology/OntologyConfigManager.ts (lines 280-310 — second fs.existsSync site in `injectOntology`):
The same upper/lower existsSync-then-throw pair appears inside `async injectOntology(options)` — both sites must route through the resolver.

From src/ontology/OntologyConfigManager.ts (line 151 — current resolvePath helper):
```
private resolvePath(filePath: string): string {
  if (path.isAbsolute(filePath)) return filePath;
  return path.resolve(process.cwd(), filePath);
}
```
The resolver must compose with this — the new helper accepts an already-resolved absolute base candidate (or the raw config string) and probes layout variants relative to its `.data/ontologies/` ancestor.

From src/ontology/types.ts:
```
export interface OntologyConfig {
  enabled: boolean;
  upperOntologyPath: string;
  team?: string;
  lowerOntologyPath?: string;
  // ...
}
export type OntologyType = 'upper' | 'lower';
```

From src/ontology/index.ts (current re-exports — the resolver will be added here):
```
export * from './types.js';
export * from './LegacyOntologyAdapter.js';
export * from './OntologyValidator.js';
export * from './OntologyClassifier.js';
export * from './OntologyConfigManager.js';
export * from './heuristics/index.js';
export * from './metrics.js';
```

From src/ontology/registry-adoption.test.ts (existing test file in this directory — convention reference):
Uses `node:test` + `node:assert/strict`; the test file header documents the run command. **Crucially**, line 29 establishes the canonical __dirname-relative repo-root walk for tests compiled into `dist/ontology/`:
```
const REPO_ROOT = resolve(__dirname, '..', '..', '..', '..');
```
That is FOUR `..` segments because the compiled test runs at `integrations/mcp-server-semantic-analysis/dist/ontology/<file>.test.js`; the walk is: `dist/ontology → dist → mcp-server-semantic-analysis → integrations → coding (repo root)`. Any new test in this plan that needs the repo-root `.data/ontologies/` directory MUST use the same 4-level walk; using 2 levels (the wrong pattern in the previous draft of this plan) resolves to `integrations/mcp-server-semantic-analysis/dist/.data/ontologies` which does not exist.

From integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts (lines 195-215 — canonical km-core registry construction, READ-ONLY reference):
```
const config = this.configManager.getConfig();
const ontologyDir = path.join(this.basePath, '.data/ontologies');
const registry = new OntologyRegistry({ ontologyDir });
this.ontology = new LegacyOntologyAdapter(registry);
```
The km-core `OntologyRegistry` public API surface used in this project (confirmed by inspecting `node_modules/@fwornle/km-core/src/ontology/registry.ts` lines 173-211):
- `isValidClass(className: string): boolean` — class membership check (REPLACES the legacy `hasClass` that Anomaly 5 reports as drifted)
- `getClass(className: string): ResolvedClass | undefined`
- `getLoadedDomains(): string[]` — returns the list of ontology domains successfully loaded (length > 0 proves classes loaded)
- `getClassesForPrompt(): string`

There is NO `getEntityClasses()` and NO `hasClass()` on the current registry — use `isValidClass()` for membership checks and `getLoadedDomains()` for the "non-zero load" assertion.

From integrations/mcp-server-semantic-analysis/src/agents/insight-generation-agent.ts (lines 350-395 — READ-ONLY, NOT modified by this plan):
`loadOntologyDescriptions()` directly reads `.data/ontologies/upper/development-knowledge-ontology.json` and `.data/ontologies/lower/coding-ontology.json` via `fs.existsSync` + `JSON.parse(fs.readFileSync(...))` — i.e. the same two-tier path mismatch as `OntologyConfigManager`, but on a strictly decorative path (loads pretty-print description strings used by `synthesizePatternDescription`). When the files are missing it silently logs a warning and falls back to hard-coded descriptions. This is a residual limitation that 42.1.1 does NOT address — see `<output>` § "Known residual limitations" for the operator hand-off note.

Existing on-disk reality (from .data/ontologies/ directory listing — May 23 2026):
```
.data/ontologies/upper.json                          (flat, historical alias for upper)
.data/ontologies/coding-ontology.json                (flat, team=coding)
.data/ontologies/agentic-ontology.json               (flat, team=agentic)
.data/ontologies/cluster-reprocessing-ontology.json  (flat, team=cluster-reprocessing)
.data/ontologies/code-entities-ontology.json         (flat, team=code-entities)
.data/ontologies/raas-ontology.json                  (flat, team=raas)
.data/ontologies/resi-ontology.json                  (flat, team=resi)
.data/ontologies/ui-ontology.json                    (flat, team=ui)
```
No `upper/` or `lower/` subdirectories exist on disk today. The two-tier layout is what callers PASS but is NOT what is stored.

From integrations/mcp-server-semantic-analysis/src/tools.ts (lines 2779-2780 — caller, read-only):
```
upperOntologyPath: path.join(basePath, '.data/ontologies/upper/development-knowledge-ontology.json'),
lowerOntologyPath: path.join(basePath, '.data/ontologies/lower/coding-ontology.json'),
```
This is what callers construct. The resolver must accept these two-tier paths AND find the flat-layout files at `.data/ontologies/development-knowledge-ontology.json` (or its alias `upper.json`) and `.data/ontologies/coding-ontology.json` respectively.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ontologyPathResolver helper + unit tests (both layouts × all teams)</name>
  <files>src/ontology/ontologyPathResolver.ts, src/ontology/ontologyPathResolver.test.ts</files>

  <read_first>
    - .planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-CONTEXT.md (the locked alias table in `<decisions>` § "Filename Aliases the Resolver MUST Recognise" — this is the single source of truth for what aliases the resolver recognises; do not invent additional aliases)
    - src/ontology/OntologyConfigManager.ts (the existing `resolvePath` private helper at line 151 — the new resolver must compose with it, not replace its semantics)
    - src/ontology/index.ts (current public re-export shape — new module follows the same `export *` pattern; do not add default-only exports)
    - src/ontology/registry-adoption.test.ts (test-file convention reference — node:test + node:assert/strict; canonical 4-level `resolve(__dirname, '..', '..', '..', '..')` repo-root walk at line 29)
    - src/ontology/types.ts (OntologyType union, error class shapes like OntologyLoadError — match the existing error-naming convention if introducing a new error class)
  </read_first>

  <behavior>
    Resolver inputs:
      - kind: 'upper' | 'lower'
      - team?: string (required when kind='lower')
      - configHint?: string (the path string the caller already passed, e.g. `.data/ontologies/upper/development-knowledge-ontology.json` from tools.ts:2779)
      - basePath?: string (optional project root; defaults to process.cwd())

    Resolver behaviour:
      1. Identifies the `.data/ontologies/` ancestor directory by walking up from configHint when present, else `path.resolve(basePath ?? process.cwd(), '.data/ontologies')`.
      2. For kind='upper', probes in order: (a) two-tier `<ontologyDir>/upper/development-knowledge-ontology.json`, (b) flat `<ontologyDir>/development-knowledge-ontology.json`, (c) flat alias `<ontologyDir>/upper.json`.
      3. For kind='lower', probes in order: (a) two-tier `<ontologyDir>/lower/<team>-ontology.json`, (b) flat `<ontologyDir>/<team>-ontology.json`.
      4. Returns the first existing absolute path. Caches the result keyed by `(kind, team, ontologyDir)`.
      5. On miss: throws an `OntologyPathNotFoundError` (new class, extends Error) whose `.message` includes the kind, the team if any, and the FULL list of every probed absolute path (one per line) — Phase 42.1 lost half a day because the original error showed only one path; this MUST list every path probed.
      6. Emits a structured `OntologyLayoutDetected` payload (return shape: `{ resolvedPath: string, layout: 'two-tier' | 'flat', alias: 'canonical' | 'upper.json' }`) so the caller can log which layout served the registry — recommended in CONTEXT.md `<specifics>`.
      7. Exposes a debug-only probe counter for cache-hit testing: a module-level `_probeCount` field (or a `__resetProbeCounter()` + `__getProbeCount()` pair) that increments inside the resolver on every `fs.existsSync` call. Documented as test-only; not part of the public contract.

    Test cases (cover ALL combinations the project currently uses):
      - Test 1: kind='upper', flat-only fixture dir with only `upper.json` present → resolves to `<fixture>/upper.json`, layout='flat', alias='upper.json'.
      - Test 2: kind='upper', flat-only fixture dir with `development-knowledge-ontology.json` present → resolves to that file, layout='flat', alias='canonical'.
      - Test 3: kind='upper', two-tier fixture dir with `upper/development-knowledge-ontology.json` present → resolves to that file, layout='two-tier'.
      - Test 4: kind='lower', team='coding', flat-only fixture dir with `coding-ontology.json` → resolves to that file, layout='flat'.
      - Test 5: kind='lower', team='coding', two-tier fixture dir with `lower/coding-ontology.json` → resolves to that file, layout='two-tier'.
      - Test 6: Iterate every team currently in `.data/ontologies/` (agentic, cluster-reprocessing, code-entities, coding, raas, resi, ui) against the actual repo `.data/ontologies/`. For each team, the resolver returns an existing absolute file (uses real on-disk layout, not a mock). Additionally, assert that the resolved `ontologyDir` basename is literally `ontologies` AND that `upper.json` AND `coding-ontology.json` both exist in that directory (sanity gate: proves the test actually points at the real repo dir, not a relative-resolution artifact).
      - Test 7: kind='upper', empty fixture dir → throws OntologyPathNotFoundError whose message contains BOTH `upper/development-knowledge-ontology.json` AND `development-knowledge-ontology.json` AND `upper.json` (assertion: all three substrings present).
      - Test 8: kind='lower', team='nonexistent-team', empty fixture dir → throws OntologyPathNotFoundError listing both `lower/nonexistent-team-ontology.json` and `nonexistent-team-ontology.json`.
      - Test 9: Cache hit semantics via the probe counter. Sequence:
          (a) call `__resetProbeCounter()`
          (b) call `resolveOntologyPath({kind:'upper', basePath: <fixture>})` against a fixture containing `upper.json` only — assert returned `resolvedPath` ends in `/upper.json`
          (c) capture `n1 = __getProbeCount()` — assert n1 >= 1 (at least one `fs.existsSync` probe happened)
          (d) call `resolveOntologyPath({kind:'upper', basePath: <same fixture>})` a second time — assert returned `resolvedPath` is identical
          (e) assert `__getProbeCount() === n1` (i.e. the second call added ZERO new probes — cache hit)
        Do NOT use the "delete file and retry" alternate strategy; the probe-counter strategy is the single canonical assertion for cache behaviour.
  </behavior>

  <action>
    Create the new module `src/ontology/ontologyPathResolver.ts` exporting `resolveOntologyPath`, the result type `OntologyLayoutDetected`, and an `OntologyPathNotFoundError` class. Function shape: `resolveOntologyPath({ kind, team?, configHint?, basePath? }): OntologyLayoutDetected`. Internals: use `fs.existsSync` for probe checks (synchronous, matches OntologyConfigManager's existing pattern); ancestor-walk from configHint by repeatedly calling `path.dirname` until the candidate ends in `.data/ontologies` or root is reached (fall back to `<basePath>/.data/ontologies` otherwise). Implement a module-level Map cache keyed by `${kind}::${team ?? ''}::${ontologyDir}` so repeat lookups skip the probe loop. The error message format MUST be: `Ontology not found (kind=<upper|lower>, team=<team or none>). Probed paths:\n  - <abs path 1>\n  - <abs path 2>\n  - <abs path 3>`. Sanitise the team value via `path.basename(team)` before substituting into the probe filename so a malicious or malformed team string cannot traverse out of the ontology dir (mitigates T-42.1.1-01). Match the surrounding module's TypeScript style: explicit interface exports, no default export, JSDoc on each public symbol matching the prose style in OntologyConfigManager.ts.

    Add the debug-only probe counter as: a module-level `let _probeCount = 0;`, increment inside the resolver immediately before each `fs.existsSync` call, and export two test-only helpers `__resetProbeCounter()` and `__getProbeCount()` (double-underscore prefix marks them as non-public API). Document in JSDoc that these are for tests only.

    Create the unit test `src/ontology/ontologyPathResolver.test.ts` using node:test + node:assert/strict to match `registry-adoption.test.ts` conventions; document the run command in the file header (`cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/ontologyPathResolver.test.js`). For fixture-based tests, build per-test tmpdirs under `os.tmpdir()` and create only the files the test requires (no shared fixture state). For Test 6 (real-layout iteration), resolve the on-disk `.data/ontologies/` directory using the canonical 4-level walk from `registry-adoption.test.ts:29`:
    ```
    const __filename = fileURLToPath(import.meta.url);
    const __dirname = dirname(__filename);
    const REPO_ROOT = resolve(__dirname, '..', '..', '..', '..');
    const ONTOLOGY_DIR = join(REPO_ROOT, '.data', 'ontologies');
    ```
    Then assert `path.basename(ONTOLOGY_DIR) === 'ontologies'`, `existsSync(join(ONTOLOGY_DIR, 'upper.json'))`, AND `existsSync(join(ONTOLOGY_DIR, 'coding-ontology.json'))` BEFORE iterating teams — this is the sanity gate that prevents a relative-path arithmetic regression from silently passing the test against a non-existent directory.
  </action>

  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/ontologyPathResolver.test.js</automated>
  </verify>

  <acceptance_criteria>
    - File `src/ontology/ontologyPathResolver.ts` exists with named exports `resolveOntologyPath`, `OntologyLayoutDetected`, `OntologyPathNotFoundError` (verify: `grep -E "^export (function|class|interface|type) (resolveOntologyPath|OntologyLayoutDetected|OntologyPathNotFoundError)" src/ontology/ontologyPathResolver.ts | wc -l` is 3).
    - File `src/ontology/ontologyPathResolver.test.ts` exists with at least 9 `test(` or `it(` blocks covering the 9 cases in `<behavior>` (verify: `grep -cE "^\s*(test|it)\(" src/ontology/ontologyPathResolver.test.ts` is ≥ 9).
    - `cd integrations/mcp-server-semantic-analysis && npm run build` exits 0 — TypeScript compiles cleanly with no new errors anywhere in `src/ontology/`.
    - `node --test dist/ontology/ontologyPathResolver.test.js` (run from `integrations/mcp-server-semantic-analysis/`) exits 0 — all resolver tests pass.
    - Test 6 explicitly iterates the literal team list `agentic, cluster-reprocessing, code-entities, coding, raas, resi, ui` (verify: each team string appears in the test file).
    - Test 6 sanity gate present: assertions on `path.basename(ONTOLOGY_DIR) === 'ontologies'` AND `existsSync(join(ONTOLOGY_DIR, 'upper.json'))` AND `existsSync(join(ONTOLOGY_DIR, 'coding-ontology.json'))`. Sanity gate prevents a 2-vs-4 segment regression from silently passing (verify: `grep -E "basename.*ontologies|upper\\.json|coding-ontology\\.json" src/ontology/ontologyPathResolver.test.ts | wc -l` is ≥ 3).
    - Test file uses the 4-level repo-root walk (verify: `grep -E "resolve\\(.*__dirname.*'\\.\\.'.*'\\.\\.'.*'\\.\\.'.*'\\.\\.'" src/ontology/ontologyPathResolver.test.ts | wc -l` is ≥ 1).
    - On miss, the error message contains the literal substring `Probed paths:` followed by every probed absolute path on its own line (verify in Test 7 assertion).
    - Module-level cache present (verify: `grep -v '^\s*//' src/ontology/ontologyPathResolver.ts | grep -E "new Map" | wc -l` is ≥ 1; the `grep -v '^\s*//'` strip filters comment lines so doc references to "Map cache" do not satisfy the gate).
    - Probe counter present and exported (verify: `grep -E "^export (function|const) (__resetProbeCounter|__getProbeCount)" src/ontology/ontologyPathResolver.ts | wc -l` is 2). Test 9 calls both helpers (verify: `grep -E "__resetProbeCounter|__getProbeCount" src/ontology/ontologyPathResolver.test.ts | wc -l` is ≥ 2).
    - Team-input sanitisation present (verify: `grep -E "path\\.basename\\(team" src/ontology/ontologyPathResolver.ts | wc -l` is ≥ 1).
  </acceptance_criteria>

  <done>
    The resolver compiles inside the submodule, all 9 unit tests pass, and `.data/ontologies/` is iterated for every team currently on disk with each one resolving to an existing absolute file. Cache-hit semantics are proven via the probe counter (zero new probes on second call).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire OntologyConfigManager.validatePaths() + injectOntology() through resolveOntologyPath + add layout integration test + registry smoke sub-test</name>
  <files>src/ontology/OntologyConfigManager.ts, src/ontology/OntologyConfigManager.layout.test.ts</files>

  <read_first>
    - src/ontology/OntologyConfigManager.ts (the FULL file — both `fs.existsSync` sites at lines 126-146 and 280-310 must be migrated together; do NOT change the public class surface, do NOT change the `resolvePath` private helper, do NOT change the singleton or hot-reload code paths)
    - src/ontology/ontologyPathResolver.ts (the helper from Task 1 — the new module being imported; review its signature and `OntologyLayoutDetected` return shape so the integration is correct)
    - .planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-CONTEXT.md (the `<decisions>` § "Layout-Resolution Strategy" AND § "Verification Boundary" — verification boundary requires (1) non-zero class count after initialize against real flat layout AND (2) a smoke that proves `ensureProjectAnchor` would NOT see "Unknown ontology class: Project"; both are covered by Test C below)
    - .data/ontologies/coding-ontology.json (the real lower ontology for team coding — Test C reads it indirectly via OntologyRegistry to assert >0 loaded classes)
    - .data/ontologies/upper.json (the real upper ontology file — the integration test asserts initialize() succeeds against this exact file via the flat alias)
    - integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts (lines 195-215 — canonical km-core registry construction pattern: `new OntologyRegistry({ ontologyDir })`. READ-ONLY: do not modify, but mirror this construction inside the integration test's Test C)
    - integrations/mcp-server-semantic-analysis/src/tools.ts (lines 2767-2810 only — caller config shape; READ-ONLY, do not modify)
  </read_first>

  <action>
    Replace BOTH `fs.existsSync` probe sites in `src/ontology/OntologyConfigManager.ts` with calls to `resolveOntologyPath` from `./ontologyPathResolver.js`. Concretely:

    Site 1 — `validatePaths()` (around lines 126-146): For each non-empty `this.config.upperOntologyPath` and `this.config.lowerOntologyPath`, call `resolveOntologyPath({ kind, team: this.config.team, configHint: this.config.<upper|lower>OntologyPath })` inside a try/catch; on success, store the resolved absolute path back into `this.config.<upper|lower>OntologyPath` (so downstream watchers and getters see the real on-disk path) and emit a one-shot stderr log line via `process.stderr.write` of the shape `[OntologyConfigManager] <kind> ontology resolved: layout=<two-tier|flat> alias=<canonical|upper.json> path=<absolute>\n` (recommended in CONTEXT.md `<specifics>` — DO NOT use console.log per the project no-console-log constraint). Use the `OntologyLayoutDetected` type import for the structured-log object so the field shape is type-checked. On `OntologyPathNotFoundError`, push the error's message verbatim into the `errors[]` array so the final `throw new Error('Ontology configuration validation failed:\n' + ...)` includes the full probed-path list.

    Site 2 — `injectOntology(options)` (around lines 280-310): Identical migration. For each provided `options.upperOntologyPath` or `options.lowerOntologyPath`, route through `resolveOntologyPath`; on success update `this.config.<kind>OntologyPath` to the resolved absolute path; on `OntologyPathNotFoundError` rethrow as `Upper ontology not found: <error.message>` / `Lower ontology not found: <error.message>` to preserve the existing error-text prefix that callers may grep on, but include the full probed-path list as the suffix.

    Do NOT modify `resolvePath` (the existing private helper for relative→absolute resolution), do NOT modify `startWatching` / `watchFile` / `stopWatching`, do NOT modify the singleton `getInstance` / `resetInstance` / `initialize` outer-shape, do NOT change any public method signature, do NOT touch `normalizeConfig` or any team-config registration code. The change is strictly: import the resolver + replace the two existsSync-throw blocks.

    Create the integration test `src/ontology/OntologyConfigManager.layout.test.ts` using node:test + node:assert/strict (same convention as Task 1 and `registry-adoption.test.ts`). Document run command in header. Resolve the repo-root `.data/ontologies/` directory using the canonical 4-level walk (NOT 2 levels — 2 levels resolves to `dist/.data/ontologies` which does not exist):
    ```
    const __filename = fileURLToPath(import.meta.url);
    const __dirname = dirname(__filename);
    const REPO_ROOT = resolve(__dirname, '..', '..', '..', '..');
    const ONTOLOGY_DIR = join(REPO_ROOT, '.data', 'ontologies');
    ```
    Up-front sanity gate before any test runs: assert `path.basename(ONTOLOGY_DIR) === 'ontologies'` AND `existsSync(join(ONTOLOGY_DIR, 'upper.json'))` AND `existsSync(join(ONTOLOGY_DIR, 'coding-ontology.json'))`.

    Test contents:
      - Test A: `OntologyConfigManager.resetInstance()` then `OntologyConfigManager.getInstance({ enabled: true, upperOntologyPath: <two-tier path>, team: 'coding', lowerOntologyPath: <two-tier path> })` followed by `await manager.initialize()` succeeds (does not throw) against the REAL repo `.data/ontologies/` directory. Use the literal two-tier paths from `integrations/mcp-server-semantic-analysis/src/tools.ts:2779-2780` (`<REPO_ROOT>/.data/ontologies/upper/development-knowledge-ontology.json` and `<REPO_ROOT>/.data/ontologies/lower/coding-ontology.json`) — these will resolve via the flat fallback. Parameterise this test (use `for (const team of TEAMS)` with `TEAMS = ['agentic','cluster-reprocessing','code-entities','coding','raas','resi','ui']`) — for each team, `resetInstance()` + `getInstance({ enabled: true, upperOntologyPath: <two-tier upper>, team, lowerOntologyPath: <two-tier per-team lower> })` + `await initialize()` succeeds. Catches per-team path-construction bugs (hyphenated team names like `cluster-reprocessing`).
      - Test B: After Test A's `initialize()` (for at least team='coding'), capture `before = '<two-tier upper config string>'` and `after = manager.getConfig().upperOntologyPath`. Assert `before !== after` (config was rewritten in place) AND `path.isAbsolute(after)` AND `existsSync(after)` AND `path.basename(after)` is one of `upper.json` OR `development-knowledge-ontology.json`. Same three-part assertion for `lowerOntologyPath` (resolved value is absolute, exists, basename is `coding-ontology.json`).
      - Test C (the verification-boundary smoke — covers CONTEXT.md `<decisions>` § "Verification Boundary" bullets 2 AND 3 in a single in-process assertion):
          (1) `OntologyConfigManager.resetInstance()` + fresh `getInstance(...)` for team='coding' + `await manager.initialize()`.
          (2) Read the resolved ontology directory via `path.dirname(manager.getConfig().lowerOntologyPath)` — this is the actual ontologyDir the registry will scan.
          (3) Dynamically import `@fwornle/km-core` and construct `const registry = new m.OntologyRegistry({ ontologyDir })` — exactly mirroring `ontology-classification-agent.ts:203`.
          (4) Assert `registry.getLoadedDomains().length > 0` — proves >0 classes loaded for team coding (was zero in the bug state). Capture the actual count in a test-output log line for forensic trail.
          (5) Assert `registry.isValidClass('Project') === true` — proves the SPECIFIC class `ensureProjectAnchor` mints is in the loaded set. This is the in-process equivalent of the "no 'Unknown ontology class: Project' error" smoke gate.
          (6) Assert `registry.isValidClass('Component') === true` — second canary class, sourced from coding-ontology.json (matches `registry-adoption.test.ts:88`).
          (7) Assert `registry.isValidClass('NonExistentClassXyz123') === false` — negative-case sanity: the registry actually discriminates, isn't trivially returning true.
        Together, steps (4)+(5) ARE the verification-boundary smoke: if step (5) passes, `ensureProjectAnchor('Coding')` will NOT throw "Unknown ontology class: Project" on the next wave-analysis run. The full SC#6 wave re-run remains owned by Phase 42.1 per CONTEXT.md, but this in-process assertion is sufficient evidence that the cascade is unblocked.
      - Test D: `OntologyConfigManager.resetInstance()` then re-construct with a bogus team `nonexistent-team-xyz` AND a bogus upper path that does not exist anywhere on disk; `await manager.initialize()` rejects with an Error whose message contains `Probed paths:` and lists every probed alias.

    NOTE on the legacy Jest ontology suite: Task 3 does NOT run `npm test -- --testPathPattern=ontology` because the legacy Jest suite under `tests/unit/ontology/` is known-broken on main (Phase 42-03 deleted `src/ontology/OntologyManager.ts` — see `src/ontology/registry-adoption.test.ts` lines 6-7 header comment for context). The legacy Jest suite is pre-existing tech debt and is NOT addressed by 42.1.1; cleanup belongs to a separate hygiene ticket. The canonical regression signal for this plan is the node:test invocation in Task 3 below.

    After file edits, run `cd integrations/mcp-server-semantic-analysis && npm run build` and confirm 0 TypeScript errors.
  </action>

  <acceptance_criteria>
    - `src/ontology/OntologyConfigManager.ts` imports the resolver AND the `OntologyLayoutDetected` type (verify: `grep -E "import.*\\{[^}]*resolveOntologyPath[^}]*\\}.*from.*ontologyPathResolver" src/ontology/OntologyConfigManager.ts | wc -l` is ≥ 1 AND `grep -E "OntologyLayoutDetected" src/ontology/OntologyConfigManager.ts | wc -l` is ≥ 1).
    - Both `fs.existsSync` probe-and-throw blocks for upper/lower ontologies are removed from `validatePaths()` AND `injectOntology()` (verify: `grep -c "fs.existsSync(upperPath)\\|fs.existsSync(lowerPath)\\|fs.existsSync(resolvedPath)" src/ontology/OntologyConfigManager.ts` is 0; the existing `fs.existsSync` inside `watchFile`/`statSync` paths is unrelated and must remain).
    - The `validatePaths()` method emits a stderr log line containing `[OntologyConfigManager]` on successful resolve (verify: `grep -E "process\\.stderr\\.write.*OntologyConfigManager" src/ontology/OntologyConfigManager.ts | wc -l` is ≥ 1; do not use console.log per the project no-console-log constraint).
    - Public method signatures unchanged (verify: `grep -E "^\\s*(async\\s+)?(initialize|validatePaths|injectOntology|getStatus|getInstance|getConfig)\\s*\\(" src/ontology/OntologyConfigManager.ts` returns the same signatures as before — no added/removed params).
    - `cd integrations/mcp-server-semantic-analysis && npm run build` exits 0.
    - `node --test dist/ontology/OntologyConfigManager.layout.test.js` (run from `integrations/mcp-server-semantic-analysis/`) exits 0 — Tests A, B, C, D all pass against the REAL `.data/ontologies/` directory.
    - Test A is parameterised across all 7 teams (verify: `grep -E "agentic.*cluster-reprocessing.*code-entities.*coding.*raas.*resi.*ui" src/ontology/OntologyConfigManager.layout.test.ts | wc -l` is ≥ 1 OR each team string appears individually: `grep -cE "'(agentic|cluster-reprocessing|code-entities|coding|raas|resi|ui)'" src/ontology/OntologyConfigManager.layout.test.ts` is ≥ 7).
    - Test B explicitly asserts `before !== after`, `isAbsolute(after)`, `existsSync(after)` for BOTH upper and lower paths (verify: `grep -cE "isAbsolute|existsSync" src/ontology/OntologyConfigManager.layout.test.ts` is ≥ 4).
    - Test C constructs `new OntologyRegistry({ ontologyDir })` from `@fwornle/km-core` (verify: `grep -E "new\\s+(m\\.)?OntologyRegistry" src/ontology/OntologyConfigManager.layout.test.ts | wc -l` is ≥ 1) AND asserts `getLoadedDomains().length > 0` AND `isValidClass('Project')` AND `isValidClass('Component')` AND `isValidClass('NonExistentClassXyz123') === false` (verify: each of the four substrings appears in the test file).
    - Test D explicitly asserts the error message contains the literal substring `Probed paths:` (verify in test file).
    - Test file uses the 4-level repo-root walk (verify: `grep -E "resolve\\(.*__dirname.*'\\.\\.'.*'\\.\\.'.*'\\.\\.'.*'\\.\\.'" src/ontology/OntologyConfigManager.layout.test.ts | wc -l` is ≥ 1).
    - Up-front sanity gate present (verify: `grep -E "basename.*ontologies|upper\\.json|coding-ontology\\.json" src/ontology/OntologyConfigManager.layout.test.ts | wc -l` is ≥ 3).
    - No caller file has been modified (verify: `git status integrations/mcp-server-semantic-analysis/src/tools.ts integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts integrations/mcp-server-semantic-analysis/src/agents/insight-generation-agent.ts integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts` shows no changes).
  </acceptance_criteria>

  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/OntologyConfigManager.layout.test.js</automated>
  </verify>

  <done>
    OntologyConfigManager's two `fs.existsSync` probe sites both route through `resolveOntologyPath`. Integration test passes against the real `.data/ontologies/` flat layout. Test C proves >0 classes load AND the `Project` + `Component` classes resolve via a fresh km-core OntologyRegistry constructed against the post-initialize ontologyDir — this is the in-process smoke that the SC#6 "Unknown ontology class: Project" error will no longer fire. Public surface unchanged. No caller in the semantic-analysis submodule has been touched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Public re-export + node:test cross-sweep + barrel-import sanity check</name>
  <files>src/ontology/index.ts</files>

  <read_first>
    - src/ontology/index.ts (current re-exports — append the resolver re-export in the same `export *` style; preserve the existing import-and-factory function `createOntologySystem` block at the bottom of the file unchanged)
    - src/ontology/ontologyPathResolver.ts (the module being re-exported — confirm its exports are stable named exports, not default exports)
    - src/ontology/registry-adoption.test.ts (existing Phase 42-03 test in this directory — must still pass after the Task 1 + Task 2 changes; this task re-runs the full ontology node:test suite to confirm zero regression)
    - CLAUDE.md (the "Rebuilding After Code Changes" section — `src/ontology/` is symlinked into the semantic-analysis submodule, so the submodule's `npm run build` IS the canonical compile path for this fix. The Docker rebuild step (`docker-compose build coding-services && docker-compose up -d coding-services`) is an OPERATOR step required for production deployment but is NOT a Task `<verify>` — the in-repo node:test signal is the AC)
  </read_first>

  <action>
    Add a single line `export * from './ontologyPathResolver.js';` to `src/ontology/index.ts` placed adjacent to the existing `export * from './OntologyConfigManager.js';` line (group exports by source file). Preserve every existing re-export and the `createOntologySystem` factory function block at the bottom of the file verbatim.

    Then run, in order, from the repo root:
      1. `cd integrations/mcp-server-semantic-analysis && npm run build` — confirm 0 TypeScript errors.
      2. `cd integrations/mcp-server-semantic-analysis && node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/OntologyConfigManager.layout.test.js dist/ontology/registry-adoption.test.js` — runs Task 1's resolver test, Task 2's layout+smoke test, and the existing Phase 42-03 `registry-adoption.test.js`. All three files must exit 0 (the registry-adoption test is the regression canary).
      3. Sanity-check that an external caller can import the resolver via the package barrel: write a one-line temp script using `process.stderr.write` (NOT console.error per project no-console-log constraint):
         ```
         cd integrations/mcp-server-semantic-analysis && node -e "import('./dist/ontology/index.js').then(m => { if (!m.resolveOntologyPath) { process.stderr.write('FAIL: resolveOntologyPath not exported from barrel\\n'); process.exit(1); } process.stderr.write('OK\\n'); });"
         ```
         Confirm exit 0 and `OK` on stderr.

    No code changes other than the one-line re-export and running the above verification steps. If the node:test sweep reveals a regression in `registry-adoption.test.js` introduced by Task 1 or Task 2, STOP and report — do not silently patch it; the planner-locked invariant is that public surface and existing tests are untouched.

    **Pre-existing tech debt — explicitly NOT addressed here:** The legacy Jest ontology suite under `tests/unit/ontology/` (60+ tests) is broken on main because Phase 42-03 deleted `src/ontology/OntologyManager.ts` (see `src/ontology/registry-adoption.test.ts` lines 6-7 header comment). Those tests fail today; they were failing before this plan; they will continue to fail after this plan. Fixing them is out of scope for 42.1.1 — separate hygiene ticket. The previous draft of this plan included `npm test -- --testPathPattern=ontology` as an acceptance criterion; that command is impossible to satisfy and has been removed.

    Do NOT modify any submodule file under `integrations/mcp-server-semantic-analysis/src/agents/` or `integrations/mcp-server-semantic-analysis/src/tools.ts`. Do NOT rebuild Docker as part of task verification. Do NOT touch `.data/ontologies/*` on disk (Path b explicitly rejected in CONTEXT.md `<decisions>`).
  </action>

  <acceptance_criteria>
    - `src/ontology/index.ts` contains the line `export * from './ontologyPathResolver.js';` (verify: `grep -E "export \\* from '\\./ontologyPathResolver\\.js'" src/ontology/index.ts | wc -l` is 1).
    - `src/ontology/index.ts` still contains every existing re-export (verify: `grep -v '^\\s*//' src/ontology/index.ts | grep -cE "^export \\* from " ` equals (prior count + 1); the comment-stripping `grep -v` prevents header prose from inflating the count. The pre-edit baseline has 7 `export * from` lines: types, LegacyOntologyAdapter, OntologyValidator, OntologyClassifier, OntologyConfigManager, heuristics, metrics — all 7 must still be present, with `ontologyPathResolver` as the 8th).
    - `createOntologySystem` factory function preserved verbatim (verify: `grep -c "export async function createOntologySystem" src/ontology/index.ts` is 1).
    - `cd integrations/mcp-server-semantic-analysis && npm run build` exits 0.
    - `cd integrations/mcp-server-semantic-analysis && node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/OntologyConfigManager.layout.test.js dist/ontology/registry-adoption.test.js` exits 0 — all three node:test files pass, including the existing `registry-adoption.test.js` (zero regression).
    - Barrel-import sanity check passes (verify: the one-line `node -e ...` script above prints `OK` to stderr and exits 0).
    - No files under `integrations/mcp-server-semantic-analysis/src/agents/` or `integrations/mcp-server-semantic-analysis/src/tools.ts` have been modified (verify: `git status integrations/mcp-server-semantic-analysis/src/agents/ integrations/mcp-server-semantic-analysis/src/tools.ts | grep -E '^\\s*(modified|new file|deleted)'` returns no lines).
    - No files under `.data/ontologies/` have been modified (verify: `git status .data/ontologies/ | grep -E '^\\s*(modified|new file|deleted)'` returns no lines).
  </acceptance_criteria>

  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/OntologyConfigManager.layout.test.js dist/ontology/registry-adoption.test.js</automated>
  </verify>

  <done>
    Resolver helpers are publicly re-exported from `src/ontology/index.ts`. All three node:test files (new resolver test + new layout/smoke test + existing registry-adoption test) pass. Barrel import works. No submodule callers and no on-disk ontology JSONs were touched. The loader fix is complete; the Phase 42.1 SC#6 wave-analysis re-run is now unblocked but is owned by Phase 42.1's verifier per CONTEXT.md verification boundary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → loader | OntologyConfigManager reads JSON paths constructed by callers in a different module; a malformed or attacker-controlled path could cause unexpected file reads |
| caller → loader | The 4 callers pass already-constructed path strings; the loader must not silently rewrite them to point at an attacker-chosen file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42.1.1-01 | Tampering | resolveOntologyPath ancestor walk + team substitution | mitigate | The dirname-walk that locates `.data/ontologies/` STOPS at the filesystem root and falls back to `<basePath>/.data/ontologies`; resolver only probes 2-3 statically-known filenames under that ancestor (development-knowledge-ontology.json, upper.json, `<team>-ontology.json`). Team value is normalised via `path.basename(team)` before substitution into the probe filename so `../foo-ontology.json` style traversal is blocked. Acceptance: `grep -E "path\\.basename\\(team" src/ontology/ontologyPathResolver.ts` returns ≥ 1 line (Task 1). |
| T-42.1.1-02 | Information Disclosure | OntologyPathNotFoundError message | accept | The error lists absolute filesystem paths under `.data/ontologies/` — these are in-repo, already public via the on-disk fixtures and the existing Phase 42.1 verifier output. Including them in the error message is REQUIRED by CONTEXT.md `<specifics>` so future layout drift surfaces immediately. Risk is low (no PII, paths within the project tree). |
| T-42.1.1-03 | Repudiation | layout-detected logging | mitigate | The one-shot stderr log line on successful resolve (Task 2) captures `layout=<two-tier|flat> alias=<canonical|upper.json> path=<absolute>` so post-incident forensics can confirm which layout served the registry, eliminating the half-day mystery that drove Phase 42.1's failure. |
| T-42.1.1-04 | Denial of Service | cache unbounded growth | accept | Cache keyed by `(kind, team, ontologyDir)` — bounded by the project's small finite team list (currently 7) × 2 kinds × ≤ a handful of distinct base paths in practice. No expiration needed; manager `resetInstance()` is the documented test-time cache flush. |

No package-manager install tasks are introduced in this plan — package legitimacy gate is N/A. No external service is involved — no `user_setup` block.
</threat_model>

<verification>
The three task `<verify>` commands collectively cover:

1. Resolver unit tests (Task 1): `cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/ontologyPathResolver.test.js` — 9 tests across both layouts × all teams × cache-via-probe-counter + error-message shape + team-input sanitisation.
2. OntologyConfigManager integration + smoke test (Task 2): `cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/OntologyConfigManager.layout.test.js` — 4 tests against the REAL `.data/ontologies/` flat layout (NOT a mock; CONTEXT.md `<specifics>` mandates this), parameterised across all 7 teams in Test A, and Test C constructs a fresh `OntologyRegistry` and asserts `>0 loaded domains` + `isValidClass('Project') === true` — the in-process equivalent of CONTEXT.md `<decisions>` § "Verification Boundary" bullets 2 AND 3.
3. Regression + barrel sweep (Task 3): `cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/OntologyConfigManager.layout.test.js dist/ontology/registry-adoption.test.js` — all new tests + existing `registry-adoption.test.js` pass; barrel import works.

The Phase 42.1 SC#6 re-run on a fresh `wave-analysis` is the PROMOTION GATE for Phase 42.1 — explicitly OUT-OF-SCOPE for 42.1.1 per CONTEXT.md `<decisions>` § "Verification Boundary". After this phase closes, the operator runs the Docker rebuild (per CLAUDE.md submodule contract) then re-runs Phase 42.1's verifier; that work is owned by Phase 42.1, not this plan.

The 3 deferred cleanup tickets identified by the forensic report (Anomaly 3 anchor-pass bookkeeping, Anomaly 5 `registry.hasClass is not a function` API drift, Anomaly 6 docker-logs recipe) are explicitly DEFERRED per CONTEXT.md `<deferred>` — no task in this plan addresses them. Additionally, the legacy Jest ontology suite under `tests/unit/ontology/` (broken since Phase 42-03 deleted `OntologyManager.ts`) is pre-existing tech debt and is also out of scope.
</verification>

<success_criteria>
Phase 42.1.1 is complete when ALL of the following are true:

1. `cd integrations/mcp-server-semantic-analysis && npm run build` exits 0 with no new TypeScript errors anywhere in `src/ontology/`.
2. `cd integrations/mcp-server-semantic-analysis && node --test dist/ontology/ontologyPathResolver.test.js` exits 0 — 9 unit tests pass, including the cache-hit probe-counter assertion (Test 9) and the Test 6 sanity gate (basename === 'ontologies' + upper.json present + coding-ontology.json present).
3. `cd integrations/mcp-server-semantic-analysis && node --test dist/ontology/OntologyConfigManager.layout.test.js` exits 0 — Tests A (parameterised across 7 teams), B (config rewritten to resolved absolute paths), C (fresh OntologyRegistry over post-initialize ontologyDir loads >0 classes AND `isValidClass('Project') === true`), and D (probed-paths error message) all pass.
4. `cd integrations/mcp-server-semantic-analysis && node --test dist/ontology/registry-adoption.test.js` exits 0 — no regression in the existing Phase 42-03 test.
5. `git status integrations/mcp-server-semantic-analysis/src/agents/ integrations/mcp-server-semantic-analysis/src/tools.ts .data/ontologies/` shows zero modifications — no caller files and no on-disk ontology files were touched (path-a invariant from CONTEXT.md `<decisions>`).
6. The resolver helpers (`resolveOntologyPath`, `OntologyLayoutDetected`, `OntologyPathNotFoundError`) are publicly importable via `import { resolveOntologyPath } from '<repo>/integrations/mcp-server-semantic-analysis/dist/ontology/index.js'` (barrel-import sanity check in Task 3).
7. Calling `OntologyConfigManager.getInstance({ ..., upperOntologyPath: '<REPO>/.data/ontologies/upper/development-knowledge-ontology.json', team: 'coding', lowerOntologyPath: '<REPO>/.data/ontologies/lower/coding-ontology.json' })` followed by `await manager.initialize()` succeeds against the current flat on-disk layout — proven by Test A of Task 2.

The Phase 42.1 SC#6 wave-analysis re-run remains outside this phase's gate; once the plan closes, the operator runs the Docker rebuild then `/gsd-verify-phase 42.1` to confirm the cascade unblock.
</success_criteria>

<output>
Create `.planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-01-SUMMARY.md` when done. The SUMMARY MUST record:
  - Whether any deviation from the locked alias table in CONTEXT.md was necessary (expected: none).
  - The detected on-disk layout at integration-test time (expected: `flat` for upper via `upper.json` alias, `flat` for lower via `coding-ontology.json`).
  - Confirmation that the 4 callers (tools.ts + 3 agents) were not modified.
  - The exact `cd integrations/mcp-server-semantic-analysis && npm run build` + node:test commands that were run and their exit codes.
  - The actual `getLoadedDomains().length` value captured by Test C (forensic-trail evidence that classes loaded — expected: ≥ 1, and ≥ 8 if the registry sees all 8 flattened JSONs per `registry-adoption.test.ts` Test 5).
  - Any layout-detected stderr lines captured during Test A of Task 2 (for forensic-trail completeness).

  ## Operator hand-off (post-merge, required for production deployment)

  Per `CLAUDE.md` § "Rebuilding After Code Changes", the submodule `integrations/mcp-server-semantic-analysis` runs in the `coding-services` Docker container. After this plan merges, the operator MUST run:
  ```
  cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services
  ```
  to pick up the recompiled `dist/ontology/`. The in-repo `node:test` AC proves correctness of the code; the Docker rebuild is the deployment vehicle and is explicitly NOT a Task `<verify>` step.

  ## Known residual limitations (out of scope for 42.1.1 — separate tickets)

  - `integrations/mcp-server-semantic-analysis/src/agents/insight-generation-agent.ts:350-395` (`loadOntologyDescriptions()`) still uses raw `fs.existsSync` against the two-tier path `.data/ontologies/upper/development-knowledge-ontology.json` and `.data/ontologies/lower/coding-ontology.json`, and silently skips loading descriptions when those files are absent (falls back to hard-coded description strings in `descriptionToActionPhrase`). Out of scope per CONTEXT.md path (a) decision; descriptions are decorative (only used for pretty-printed `synthesizePatternDescription` output), they are NOT required for class loading or `ensureProjectAnchor`. This does NOT block SC#6 unblock. Separate ticket should route this caller through `resolveOntologyPath` for consistency.
  - Legacy Jest ontology suite under `tests/unit/ontology/` (≥ 60 failing tests on main) is pre-existing tech debt from Phase 42-03's deletion of `src/ontology/OntologyManager.ts`. Separate hygiene ticket.
  - Anomaly 3 (anchor-pass bookkeeping), Anomaly 5 (`registry.hasClass is not a function` API drift — note: the current registry surface is `isValidClass`, not `hasClass`), and Anomaly 6 (docker-logs recipe) per CONTEXT.md `<deferred>`.
</output>
