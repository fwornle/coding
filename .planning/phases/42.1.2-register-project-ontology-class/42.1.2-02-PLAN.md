---
phase: 42.1.2-register-project-ontology-class
plan: 02
type: execute
wave: 2
depends_on: [42.1.2-01]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts
autonomous: true
requirements: []
tags: [ontology, project-class, test-c-upgrade, node-test, unit-smoke]

must_haves:
  truths:
    - "Test C in `OntologyConfigManager.layout.test.ts` asserts `registry.isValidClass('Project') === true` for every team in the existing `TEAMS` literal (`agentic`, `cluster-reprocessing`, `code-entities`, `coding`, `raas`, `resi`, `ui`)."
    - "The pre-existing comment line `Project=NOT loaded (see SUMMARY known-residuals)` and its `process.stderr.write` log line are replaced by a positive forensic-trail log line confirming Project loaded."
    - "The Phase 42.1.1 Option-A NOTE block above Test C is updated to reflect that the Project class is now registered (the carve-out is closed)."
    - "All other tests in the file (A, B, D, E) are untouched — only Test C is upgraded; `before()` sanity gate and the `TEAMS` literal stay byte-identical."
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts"
      provides: "Test C upgraded to positive `isValidClass('Project')` assertion across all 7 teams"
      contains: "registry.isValidClass('Project')"
  key_links:
    - from: "Test C"
      to: "km-core OntologyRegistry"
      via: "fresh registry construction over the post-initialize ontologyDir"
      pattern: "new m\\.OntologyRegistry\\(\\{ ontologyDir \\}\\)"
    - from: "Test C"
      to: ".data/ontologies/upper.json"
      via: "registry loads Project class from upper (which extends every team's lower ontology)"
      pattern: "isValidClass\\('Project'\\)"
---

<objective>
Upgrade Test C in
`integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts`
from the Phase 42.1.1 Option-A softening (logs "Project=NOT loaded") to a positive
assertion: for every team in the existing `TEAMS` literal, a freshly-constructed km-core
`OntologyRegistry` over the post-`initialize()` `ontologyDir` reports
`registry.isValidClass('Project') === true`.

Purpose: this is the unit-level smoke gate that proves the Plan 42.1.2-01 JSON change
flows end-to-end through the loader chain (resolver → OntologyConfigManager → km-core
registry) and exposes Project as a valid class for every team. The 42.1.1 known-residual
"layer-2 follow-up — Project class not on disk" closes when this test goes green.

Output: an upgraded Test C with the positive per-team assertion, the comment block
revised to reflect the closed carve-out, and node:test exit 0 across all tests in the
file after `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/42.1.2-register-project-ontology-class/42.1.2-CONTEXT.md
@.planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-01-SUMMARY.md
@integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts
@CLAUDE.md

<interfaces>
<!-- km-core OntologyRegistry public surface — read-only reference; do NOT modify km-core. -->
<!-- From lib/km-core/src/ontology/registry.ts (Phase 38 API contract). -->

```typescript
// Public methods used by Test C:
class OntologyRegistry {
  constructor(opts: { ontologyDir: string; strict?: boolean });
  getLoadedDomains(): string[];
  isValidClass(className: string): boolean;
  getClass(className: string): OntologyClass | undefined;
}
```

<!-- TEAMS literal already in the test file (lines 40-48) — re-use as-is, do NOT redefine. -->

```typescript
const TEAMS = [
  'agentic',
  'cluster-reprocessing',
  'code-entities',
  'coding',
  'raas',
  'resi',
  'ui',
];
```

<!-- Current Test C structure (lines 177-231). The pattern to extend across all teams: -->
<!-- 1. resetInstance(); getInstance({...team-specific config...}); await initialize(); -->
<!-- 2. derive ontologyDir from manager.getConfig().lowerOntologyPath; -->
<!-- 3. construct fresh OntologyRegistry({ ontologyDir }); -->
<!-- 4. assert isValidClass('Project') === true. -->
</interfaces>

<build_pipeline>
<!-- CLAUDE.md mandate (RECURRING failure mode if skipped): -->
<!-- TypeScript edits to src/ontology/*.ts only land in dist/ after npm run build. -->
<!-- node --test reads from dist/ — running it against TS source files is impossible. -->

```bash
cd integrations/mcp-server-semantic-analysis && npm run build
node --test dist/ontology/OntologyConfigManager.layout.test.js
```

The npm run build step MUST run before node --test or the change will not be visible
to the test runner (test reads compiled JS from dist/, not TS source).
</build_pipeline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Test C to assert isValidClass('Project') across all 7 teams</name>
  <files>integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts</files>
  <read_first>
    - integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts (current state — lines 40-48 hold the `TEAMS` literal; lines 149-231 hold Test C including the Option-A NOTE comment block and the current assertions)
    - .planning/phases/42.1.2-register-project-ontology-class/42.1.2-CONTEXT.md (`<decisions>` § "Verification Boundary (LOCKED)" — bullet 1 specifies the unit-level smoke contract this task implements)
    - .planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-01-SUMMARY.md (context: why Test C was softened in 42.1.1 — Option A — and what the layer-2 follow-up was)
    - .data/ontologies/upper.json (post Plan 42.1.2-01 state — confirms the `Project` class block is present)
  </read_first>
  <action>
    Edit Test C in `OntologyConfigManager.layout.test.ts` to upgrade it from a single-team
    (`coding`) smoke into a parameterised per-team positive assertion.

    Changes:

    1. **Update the Option-A NOTE comment block above Test C** (currently lines 149-176): rewrite to record that the Phase 42.1.1 carve-out is closed by Phase 42.1.2 — Project is now declared in `.data/ontologies/upper.json`, so the positive `isValidClass('Project') === true` assertion that the original 42.1.1 plan asked for is now achievable. Keep the cross-reference to Phase 42.1.1 Option A for forensic continuity, but flip the framing from "DROPPED" to "RESTORED". Reference Phase 42.1.2 CONTEXT.md `<decisions>` § "Verification Boundary (LOCKED)" bullet 1.

    2. **Rewrite the Test C body** (currently lines 177-231) to iterate `TEAMS` (the existing literal at lines 40-48 — re-use as-is, do NOT redefine). For each team:
       - `OntologyConfigManager.resetInstance()`
       - `getInstance({ enabled: true, upperOntologyPath: upperTwoTier(), team, lowerOntologyPath: lowerTwoTier(team), hotReload: false })`
       - `await manager.initialize()`
       - derive `ontologyDir = dirname(manager.getConfig().lowerOntologyPath!)`
       - `const m = await import('@fwornle/km-core')`
       - `const registry = new m.OntologyRegistry({ ontologyDir })`
       - **Primary assertion (the new positive gate):** `assert.equal(registry.isValidClass('Project'), true, \`team=${team}: registry.isValidClass('Project') must return true (Phase 42.1.2 — Project registered in upper.json)\`)`

    3. **Preserve canary assertions** from the prior Test C body (these prove the registry is not trivially returning true and that the rest of the loader chain still works):
       - Keep `assert.equal(registry.isValidClass('Component'), true, ...)` per team (Component is in coding-ontology.json — for non-coding teams it may or may not be present; if the existing test asserted it only for `coding`, keep that scope; otherwise gate by team).
       - Keep `assert.equal(registry.isValidClass('NonExistentClassXyz123'), false, ...)` per team (negative-case sanity).
       - Keep the `domains.length` assertion if you want a per-team forensic log; otherwise drop in favour of per-team `[Test C] team=<team> Project=valid` stderr line.

    4. **Replace the forensic-trail log line** at the end of the per-team loop iteration: instead of `process.stderr.write(\`[Test C] domains.length=..., sample classes: Component=valid, Project=NOT loaded (see SUMMARY known-residuals)\n\`)`, write `process.stderr.write(\`[Test C] team=${team} Project=valid (Phase 42.1.2 layer-2 closed)\n\`)`. This is the visible signal in test output that the carve-out is closed.

    5. **End-of-test cleanup:** `OntologyConfigManager.resetInstance()` at the end of the test function (matches existing pattern).

    6. **Test name (the `it(...)` title) must explicitly call out the upgrade:** suggest `'Test C: fresh OntologyRegistry over post-initialize ontologyDir reports isValidClass(\\'Project\\') === true for every team (Phase 42.1.2 layer-2 of SC#6)'`.

    Hard prohibitions:
    - DO NOT modify the `TEAMS` literal (lines 40-48), the `upperTwoTier`/`lowerTwoTier` helpers (lines 53-58), the `before()` sanity gate (lines 63-77), or any of Tests A/B/D/E.
    - DO NOT add new imports beyond what is already in the file (the dynamic `await import('@fwornle/km-core')` is already present in the existing Test C body — keep that pattern).
    - DO NOT delete the Phase 42.1.1 Option-A NOTE comment block entirely — rewrite it to record the closure. Forensic continuity matters for future readers.
    - DO NOT touch `.data/ontologies/*` (that change landed in Plan 42.1.2-01).
    - DO NOT touch `src/agents/wave-controller.ts` (the integration smoke for `ensureProjectAnchor` is Plan 42.1.2-03's job).

    After the source edit, the executor MUST rebuild the submodule before running tests:
    `cd integrations/mcp-server-semantic-analysis && npm run build` — this is the
    CLAUDE.md mandate. `node --test` runs from `dist/`, not `src/`, so an edit without
    a rebuild is invisible to the test runner.
  </action>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/ontology/OntologyConfigManager.layout.test.js 2>&1 | tee /tmp/42.1.2-02-test-c.log && grep -F "Project=valid (Phase 42.1.2 layer-2 closed)" /tmp/42.1.2-02-test-c.log | wc -l | awk '{ if ($1 == 7) { print "OK: 7 teams logged Project=valid"; exit 0 } else { print "FAIL: expected 7 team log lines, got "$1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `OntologyConfigManager.layout.test.ts` still contains the `TEAMS` literal byte-identical to pre-change state (`git diff` shows no line changes in lines 40-48).
    - Source: the test file still contains exactly 5 top-level `it(...)` calls (Tests A, B, C, D, E) — same count as before; ONLY Test C body and its docblock are modified.
    - Source: the Test C body references `isValidClass('Project')` with `true` expectation inside a `for (const team of TEAMS)` (or equivalent `TEAMS.forEach(...)`) loop.
    - Source: the file no longer contains the literal string `Project=NOT loaded (see SUMMARY known-residuals)` (the Phase 42.1.1 forensic log line is gone).
    - Source: the file contains the new forensic log template `Project=valid (Phase 42.1.2 layer-2 closed)`.
    - Behavior: `cd integrations/mcp-server-semantic-analysis && npm run build` exits 0 (TypeScript compiles cleanly — strict-TS per CLAUDE.md).
    - Behavior: `node --test dist/ontology/OntologyConfigManager.layout.test.js` exits 0; ALL tests in the file pass (A, B, C, D, E — 5 total).
    - Behavior: the test output (stderr) contains exactly 7 lines matching `[Test C] team=<team> Project=valid (Phase 42.1.2 layer-2 closed)` — one per team in TEAMS.
    - Behavior: the verify command's grep gate emits `OK: 7 teams logged Project=valid` and exits 0.
    - Behavior (regression — separately verify): `node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/registry-adoption.test.js` still exits 0 (zero regression in Phase 42.1.1 / 42-03 test suites).
  </acceptance_criteria>
  <done>
    Test C asserts `registry.isValidClass('Project') === true` for every team in `TEAMS`; the file builds cleanly; `node --test dist/ontology/OntologyConfigManager.layout.test.js` passes all 5 tests; the new positive forensic-trail log line `Project=valid (Phase 42.1.2 layer-2 closed)` appears 7 times in stderr; the Phase 42.1.1 negative log line is gone; no other tests in the file modified; sibling Phase 42.1.1 and 42-03 test suites still green (zero regression).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies a test file inside the repo. No production code path is altered; no user input, no network, no file writes outside the test source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42.1.2-02-00 | (none — test-only change) | `src/ontology/OntologyConfigManager.layout.test.ts` | accept | No new threats introduced. The change is a test-file edit that strengthens an existing assertion (Project from "log only" to "assert"). Tests run in a sandboxed `node --test` environment; no privilege boundary is crossed; no production code is modified. STRIDE coverage: Spoofing N/A, Tampering — the test edit IS the intended change, guarded by `git diff` review + the build/test gates in `<verify>`, Repudiation N/A (git commit), Information disclosure N/A (no secrets — just ontology class names + team names already public in the repo), Denial of service — a broken test would fail loudly during `node --test`, mitigated by the verify gate, Elevation of privilege N/A. The submodule build step (`npm run build`) and `node --test` gate guarantee any regression is surfaced at execution time, not in production. |
</threat_model>

<verification>
- Plan 42.1.2-01 must have completed successfully (the `Project` class block is present in `.data/ontologies/upper.json`) — this is the wave-1 dependency.
- Run the Task 1 verify command from the repo root and confirm:
  1. `npm run build` exits 0,
  2. `node --test dist/ontology/OntologyConfigManager.layout.test.js` exits 0 with all 5 tests passing,
  3. the grep gate emits `OK: 7 teams logged Project=valid` (Project loaded for every team in TEAMS).
- Spot-check `git diff integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts` — modifications should be confined to the Test C docblock + body region (roughly lines 149-231).
- Run the Phase 42.1.1 / 42-03 regression gate: `node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/registry-adoption.test.js` — both must exit 0 (no regression in sibling suites).
</verification>

<success_criteria>
- Test C asserts `isValidClass('Project') === true` per team in `TEAMS`.
- `npm run build` compiles cleanly (no strict-TS errors).
- All 5 tests in `OntologyConfigManager.layout.test.js` pass after build.
- 7 stderr lines confirm Project loaded for every team.
- Phase 42.1.1 Option-A residual is now closed; this is the visible signal in test output.
- Zero regression in sibling test suites (`ontologyPathResolver.test.js`, `registry-adoption.test.js`).
</success_criteria>

<output>
Create `.planning/phases/42.1.2-register-project-ontology-class/42.1.2-02-SUMMARY.md` when done.
</output>
