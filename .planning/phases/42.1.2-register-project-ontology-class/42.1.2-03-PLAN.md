---
phase: 42.1.2-register-project-ontology-class
plan: 03
type: execute
wave: 2
depends_on: [42.1.2-01]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/wave-controller-ensure-project-anchor.test.ts
autonomous: true
requirements: []
tags: [ontology, project-class, ensure-project-anchor, integration-smoke, node-test, idempotency]

must_haves:
  truths:
    - "`ensureProjectAnchor('test-run-id')` does NOT throw `Unknown ontology class: Project` when invoked against a stubbed `kmCoreAdapter`."
    - "On the cold path (no pre-existing Project entity), `kmCoreAdapter.storeEntity` is called exactly once with `{ name: 'Coding', entityType: 'Project', ontologyClass: 'Project' }` and the `{ team: 'coding' }` options object."
    - "On the warm path (Project/Coding already returned by `queryEntities`), `kmCoreAdapter.storeEntity` is NOT called — idempotency proven by invoking `ensureProjectAnchor` twice and asserting the spy was called exactly once across both invocations."
    - "The new integration test runs in isolation from Plans 42.1.2-01 and -02 — does NOT require initialize() of the OntologyConfigManager singleton, does NOT touch `.data/ontologies/*`, does NOT require real km-core; the stub captures the storeEntity contract."
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/wave-controller-ensure-project-anchor.test.ts"
      provides: "Integration smoke test for ensureProjectAnchor — cold/warm path + storeEntity spy contract + idempotency"
      contains: "ensureProjectAnchor"
  key_links:
    - from: "wave-controller-ensure-project-anchor.test.ts"
      to: "src/agents/wave-controller.ts (line 2262)"
      via: "instantiates WaveController with stubbed kmCoreAdapter and invokes ensureProjectAnchor"
      pattern: "ensureProjectAnchor"
    - from: "test stub"
      to: "kmCoreAdapter contract"
      via: "queryEntities + storeEntity spies"
      pattern: "storeEntity"
---

<objective>
Add an integration smoke test that proves the runtime call path that originally surfaced
the SC#6 cascade — `wave-controller.ensureProjectAnchor('test-run-id')` — no longer
throws `Unknown ontology class: Project` after Plan 42.1.2-01 lands the class on disk,
AND that the call still satisfies the locked contract: a single `storeEntity` call on
the cold path, zero `storeEntity` calls on the warm path (idempotency).

Purpose: this is the second verification gate from CONTEXT.md `<decisions>` § "Verification
Boundary (LOCKED)" bullet 2. Plan 42.1.2-02 covers the unit-level smoke (registry reports
Project as a valid class). This plan covers the integration-level smoke (the actual caller
that motivated the phase no longer breaks, and its idempotency invariant is preserved).

Output: a new test file
`integrations/mcp-server-semantic-analysis/src/agents/wave-controller-ensure-project-anchor.test.ts`
running under `node --test`, exercising the cold and warm paths of `ensureProjectAnchor`
against a fake `kmCoreAdapter` whose `queryEntities` and `storeEntity` methods are spied
on. Per CONTEXT.md `<decisions>` § "Claude's Discretion", a new file is chosen over
appending to `OntologyConfigManager.layout.test.ts` because the test surface here is
`wave-controller`, not `OntologyConfigManager` — keeping the file boundaries clear.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/42.1.2-register-project-ontology-class/42.1.2-CONTEXT.md
@integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts
@CLAUDE.md

<interfaces>
<!-- ensureProjectAnchor — the method under test (wave-controller.ts:2262-2305). -->
<!-- Private method on WaveController; the test will need a way to invoke it. -->
<!-- Two options for access from the test (executor's choice; both are valid TS): -->
<!-- (a) Cast the instance to `any` and call `(controller as any).ensureProjectAnchor(runId)`. -->
<!-- (b) Expose a thin internal accessor on WaveController solely for tests (DISCOURAGED — -->
<!--     CONTEXT.md `<decisions>` § "Claude's Discretion" says the test boundary should  -->
<!--     stay clear; modifying production code to expose a method just for testing is a  -->
<!--     bigger change than this phase warrants. Prefer option (a).) -->

```typescript
// From src/agents/wave-controller.ts (lines 2262-2305) — read-only reference:
private async ensureProjectAnchor(runId: string): Promise<void> {
  if (!this.kmCoreAdapter) {
    throw new Error('ensureProjectAnchor called without kmCoreAdapter bootstrapped');
  }
  try {
    const existing = await this.kmCoreAdapter.queryEntities({ entityType: 'Project' });
    if (existing.some((e) => e.name === 'Coding')) {
      return;  // idempotent re-run — warm path
    }
    await this.kmCoreAdapter.storeEntity(
      {
        name: 'Coding',
        entityType: 'Project',
        ontologyClass: 'Project',
        observations: [...],
        significance: 10,
        metadata: { subsystem: 'wave-analysis', source: 'wave-analysis', runId },
      },
      { team: this.team },
    );
    this.persistedEntityNames.add('Coding');
    // ... log line ...
  } catch (err) {
    // fail-soft — writes stderr line, does NOT rethrow
  }
}
```

<!-- KmCoreAdapter surface (relevant subset — read-only): -->
<!-- - queryEntities(filter: { entityType?: string; ... }): Promise<Array<{ name: string; entityType: string; ... }>> -->
<!-- - storeEntity(entity: { name, entityType, ontologyClass, observations, significance, metadata }, opts: { team: string }): Promise<unknown> -->
<!-- - storeRelationship(...) — NOT exercised by this test -->
<!-- - mergeAttributes(...) — NOT exercised by this test -->

<!-- WaveController constructor — read it from wave-controller.ts to understand which -->
<!-- fields the test needs to set. At minimum: `this.team`, `this.kmCoreAdapter`, and  -->
<!-- `this.persistedEntityNames` (a Set<string>). The test can construct a minimal-surface -->
<!-- instance by either: -->
<!--   (a) calling `new WaveController(...)` with realistic minimal args, then patching -->
<!--       `(controller as any).kmCoreAdapter = stub`, OR -->
<!--   (b) constructing the controller via a factory if one exists, then patching. -->
<!-- The exact constructor args are at the executor's discretion — `<read_first>` covers -->
<!-- the source so the executor can pick the lightest-weight constructor form. -->
</interfaces>

<build_pipeline>
<!-- CLAUDE.md mandate (RECURRING failure mode if skipped): -->
<!-- TypeScript edits to src/agents/*.ts only land in dist/ after npm run build. -->

```bash
cd integrations/mcp-server-semantic-analysis && npm run build
node --test dist/agents/wave-controller-ensure-project-anchor.test.js
```

The npm run build step MUST run before node --test. The test file lives under
src/agents/, so its compiled output lands at dist/agents/<file>.test.js.
</build_pipeline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wave-controller-ensure-project-anchor.test.ts — cold/warm path + idempotency</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/wave-controller-ensure-project-anchor.test.ts</files>
  <read_first>
    - integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts (lines 2262-2305 for `ensureProjectAnchor` exact signature + behaviour; constructor + class header for the minimum-args needed to instantiate WaveController; line ~95 for the `kmCoreAdapter?: KmCoreAdapter` field declaration so the test knows what shape to stub)
    - integrations/mcp-server-semantic-analysis/src/ontology/OntologyConfigManager.layout.test.ts (existing node:test convention in this submodule — uses `describe`/`it`/`before` from `node:test` and `assert` from `node:assert/strict`; the test team adopts the same imports + style)
    - .planning/phases/42.1.2-register-project-ontology-class/42.1.2-CONTEXT.md (`<decisions>` § "Verification Boundary (LOCKED)" bullet 2 — locks the spy contract: cold path = one call with `{ name: 'Coding', entityType: 'Project', ontologyClass: 'Project', team: 'coding' }`; warm path = zero additional calls; idempotency proven by calling twice and asserting spy was called once total)
    - .planning/phases/42.1.2-register-project-ontology-class/42.1.2-CONTEXT.md (`<specifics>` § note about `entityType` and `ontologyClass` both being `'Project'` — the test should explicitly assert both fields)
    - CLAUDE.md (`Rebuilding After Code Changes` — confirms submodule build pipeline; `process.stderr.write` over `console.log` for `no-console-log` constraint compliance)
  </read_first>
  <action>
    Create `integrations/mcp-server-semantic-analysis/src/agents/wave-controller-ensure-project-anchor.test.ts`.

    File layout (TypeScript, strict mode per CLAUDE.md, ES modules to match the rest of the submodule):

    1. **Imports** (use only built-in node:test + node:assert/strict + the WaveController class; no third-party test framework):
       - `import { describe, it } from 'node:test';`
       - `import assert from 'node:assert/strict';`
       - Import `WaveController` (or whatever the exported class name in `wave-controller.ts` is — the executor confirms from the source file's `export class` declaration).
       - Optional: import any types needed for the stub adapter shape — prefer structural typing (define a local minimal `FakeKmCoreAdapter` shape) over importing the production `KmCoreAdapter` type if doing so pulls a heavy dependency tree.

    2. **Test harness factory** (a helper inside the file, NOT in production code). It builds:
       - A `storeEntityCalls` array (records every `storeEntity` invocation: `{ entity, opts }`).
       - A `queryEntitiesCalls` array (records every `queryEntities` invocation: `{ filter }`).
       - A `fakeAdapter` object exposing `queryEntities` and `storeEntity` that push to the arrays. `queryEntities` returns whatever the test setup configured (an empty array for cold path; an array containing `{ name: 'Coding', entityType: 'Project' }` for warm path).
       - A `WaveController` instance with `kmCoreAdapter` and `team: 'coding'` and `persistedEntityNames: new Set<string>()` patched in (cast to `any` to bypass private/protected access). The executor picks the lightest constructor form — read the constructor signature from `wave-controller.ts` first.

    3. **Test 1: Cold path — no existing Project entity, ensureProjectAnchor mints one.**
       - Setup: `fakeAdapter.queryEntities` returns `[]`.
       - Action: `await (controller as any).ensureProjectAnchor('test-run-id-cold')`.
       - Assertions:
         - No throw (implicit — the test would fail with an unhandled rejection if it threw).
         - `queryEntitiesCalls.length === 1` and `queryEntitiesCalls[0].filter.entityType === 'Project'`.
         - `storeEntityCalls.length === 1`.
         - `storeEntityCalls[0].entity.name === 'Coding'`.
         - `storeEntityCalls[0].entity.entityType === 'Project'`.
         - `storeEntityCalls[0].entity.ontologyClass === 'Project'`.
         - `storeEntityCalls[0].opts.team === 'coding'`.
         - (Optional defence-in-depth) `(controller as any).persistedEntityNames.has('Coding') === true` — the existing fail-soft path adds `Coding` to the set after a successful storeEntity. If the executor exposes this via cast it's a useful regression guard.

    4. **Test 2: Warm path — Project/Coding already exists, ensureProjectAnchor is a no-op for storeEntity.**
       - Setup: `fakeAdapter.queryEntities` returns `[{ name: 'Coding', entityType: 'Project' }]`.
       - Action: `await (controller as any).ensureProjectAnchor('test-run-id-warm')`.
       - Assertions:
         - No throw.
         - `queryEntitiesCalls.length === 1`.
         - `storeEntityCalls.length === 0` (warm path returns early — does NOT mint a duplicate).

    5. **Test 3: Idempotency — two back-to-back calls only mint once.**
       - Setup: start with `queryEntities` returning `[]`. After the first `storeEntity` call lands, flip the stub: subsequent `queryEntities` calls return `[{ name: 'Coding', entityType: 'Project' }]` (simulates the entity being persisted between calls).
       - Action: `await (controller as any).ensureProjectAnchor('run-1')`; `await (controller as any).ensureProjectAnchor('run-2')`.
       - Assertions:
         - No throw on either call.
         - `storeEntityCalls.length === 1` (mint happened once total across two calls).
         - `queryEntitiesCalls.length === 2` (each call queries once before deciding cold vs warm).

    6. **Forensic stderr line per test** (matching the pattern in `OntologyConfigManager.layout.test.ts`): `process.stderr.write('[wave-controller-ensure-project-anchor] cold path: storeEntity called once with { name: Coding, entityType: Project, ontologyClass: Project, team: coding }\n')` etc. These are informational — they help the operator confirm the spy contract in the test log without parsing assertions.

    Hard prohibitions:
    - DO NOT modify `src/agents/wave-controller.ts` — the test must work against the existing `private` `ensureProjectAnchor` method, accessed via a typed cast (`as any` or `as unknown as { ensureProjectAnchor(runId: string): Promise<void> }`). The phase boundary is strict per CONTEXT.md.
    - DO NOT import the real `KmCoreAdapter` implementation or a real km-core registry — the test must run without km-core IO or a real `.data/knowledge-graph/` LevelDB. Stubs only.
    - DO NOT use `console.log` anywhere — `process.stderr.write` only (per CLAUDE.md `no-console-log` constraint).
    - DO NOT use `vitest`, `jest`, or any third-party test framework. Built-in `node:test` only (matches Phase 42.1.1's pattern).
    - DO NOT touch `.data/ontologies/*` or `OntologyConfigManager.layout.test.ts` (those are Plans -01 and -02).

    After writing the file, rebuild the submodule (`cd integrations/mcp-server-semantic-analysis && npm run build`) and then run `node --test dist/agents/wave-controller-ensure-project-anchor.test.js`. Both must exit 0.
  </action>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/agents/wave-controller-ensure-project-anchor.test.js 2>&1 | tee /tmp/42.1.2-03-integration.log && grep -E "tests [0-9]+|pass [0-9]+|fail [0-9]+" /tmp/42.1.2-03-integration.log && awk '/^# fail [0-9]+/ { if ($3 != "0") { print "FAIL: at least one test failed"; exit 1 } } /^# pass [0-9]+/ { if ($3 != "3") { print "FAIL: expected 3 passing tests (cold/warm/idempotency), got "$3; exit 1 } } END { print "OK: 3/3 integration smoke tests pass" }'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `integrations/mcp-server-semantic-analysis/src/agents/wave-controller-ensure-project-anchor.test.ts` exists and parses as strict-TS (compiles cleanly via `npm run build`).
    - Source: the file contains exactly 3 top-level `it(...)` blocks (cold path / warm path / idempotency) within a single `describe(...)` block.
    - Source: the file imports `WaveController` from the production source (`./wave-controller.js` after compile, `./wave-controller` or similar in the TS source).
    - Source: the file does NOT import `vitest`, `jest`, `mocha`, or any third-party test framework.
    - Source: the file does NOT contain any `console.log`/`console.error`/`console.warn` calls (CLAUDE.md `no-console-log`); only `process.stderr.write` for forensic lines.
    - Source: `src/agents/wave-controller.ts` is byte-identical to its pre-change state (`git diff src/agents/wave-controller.ts` is empty).
    - Behavior: `npm run build` exits 0 (strict-TS clean).
    - Behavior: `node --test dist/agents/wave-controller-ensure-project-anchor.test.js` exits 0 with 3 passing tests and 0 failing tests.
    - Behavior: the verify command emits `OK: 3/3 integration smoke tests pass` and exits 0.
    - Behavior (contract): the test verifies on the cold path that `storeEntity` is called exactly once with `entity.name === 'Coding'`, `entity.entityType === 'Project'`, `entity.ontologyClass === 'Project'`, and `opts.team === 'coding'` (the full CONTEXT.md locked spy contract).
    - Behavior (contract): the test verifies on the warm path that `storeEntity` is not called at all.
    - Behavior (contract): the test verifies idempotency — two calls produce exactly one `storeEntity` invocation across both calls.
  </acceptance_criteria>
  <done>
    The new test file exists, compiles cleanly, runs under `node --test`, and proves: (1) `ensureProjectAnchor` no longer throws `Unknown ontology class: Project` on a cold path against the stubbed adapter; (2) the cold-path `storeEntity` call matches the locked contract exactly (`name`, `entityType`, `ontologyClass`, `team`); (3) the warm path is a no-op for `storeEntity`; (4) idempotency holds across two back-to-back invocations. Wave-controller production source is unchanged. No third-party test framework introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a test file that stubs the km-core adapter; no production code path is exercised against real IO. No user input, no network, no real LevelDB writes, no real km-core registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42.1.2-03-00 | (none — test-only change with stubbed adapters) | `src/agents/wave-controller-ensure-project-anchor.test.ts` | accept | No new threats introduced. The change is a new test file that uses a hand-rolled in-memory stub for `kmCoreAdapter`. No real km-core, no LevelDB, no network, no privilege boundary. STRIDE coverage: Spoofing N/A, Tampering — the file is new code under test-only scope, guarded by the strict-TS build gate, Repudiation N/A (git commit), Information disclosure N/A (no secrets — entity name 'Coding' and team 'coding' are public repo artefacts), Denial of service — a broken test fails loudly during `node --test`, the verify gate would catch it, Elevation of privilege N/A (no privilege boundary; the `as any` cast to access the private method is a TypeScript visibility hatch, not a runtime privilege boundary). The phase boundary forbids modifying `wave-controller.ts` production source so there is no possibility of accidentally weakening the production code's invariants. |
</threat_model>

<verification>
- Plan 42.1.2-01 must have completed (Project class in upper.json) — the test's logical premise is that Project is now a valid ontology class, although this specific test only exercises the storeEntity contract via stubs and does NOT load the registry. Still, depending on -01 keeps the wave ordering crisp.
- Run the Task 1 verify command and confirm:
  1. `npm run build` exits 0,
  2. `node --test dist/agents/wave-controller-ensure-project-anchor.test.js` exits 0,
  3. the awk gate emits `OK: 3/3 integration smoke tests pass`.
- `git diff integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` is empty (production source unchanged).
- `git status integrations/mcp-server-semantic-analysis/src/agents/` shows the new test file as the ONLY modification.
- Run the cross-plan regression gate from Plan 42.1.2-02: `node --test dist/ontology/OntologyConfigManager.layout.test.js dist/ontology/ontologyPathResolver.test.js dist/ontology/registry-adoption.test.js` — all three still exit 0.
</verification>

<success_criteria>
- New integration smoke test file lives at `src/agents/wave-controller-ensure-project-anchor.test.ts`.
- 3/3 test cases pass (cold path, warm path, idempotency).
- The locked spy contract from CONTEXT.md `<decisions>` § "Verification Boundary" bullet 2 is verified: cold path mints once with `{ name: 'Coding', entityType: 'Project', ontologyClass: 'Project', team: 'coding' }`; warm path mints zero times; two calls together mint exactly once.
- Production `wave-controller.ts` source unchanged.
- No third-party test framework introduced; built-in `node:test` only.
- Sibling test suites (Phase 42.1.1, Phase 42-03, Plan 42.1.2-02) still green — zero regression.
</success_criteria>

<output>
Create `.planning/phases/42.1.2-register-project-ontology-class/42.1.2-03-SUMMARY.md` when done.
</output>
