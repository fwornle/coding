---
phase: 42.1.2-register-project-ontology-class
verified: 2026-05-25T05:08:28Z
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 42.1.2: Register Project Ontology Class — Verification Report

**Phase Goal:** Add a `Project` class definition to the on-disk ontology so that `km-core` `OntologyRegistry.isValidClass('Project')` returns `true` after `OntologyConfigManager.initialize()`. This unblocks `ensureProjectAnchor('Coding')` at runtime and closes the layer-2 follow-up identified by Phase 42.1.1 Test C. Scope is strictly the ontology JSON change + a smoke test that proves `ensureProjectAnchor('Coding')` no longer throws.

**Verified:** 2026-05-25T05:08:28Z
**Status:** passed
**Re-verification:** No — initial verification
**Requirement IDs:** none — phase uses decision-coverage gate against CONTEXT.md `<decisions>` blocks (PLAN frontmatter `requirements: []` for all 3 plans)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | `.data/ontologies/upper.json` declares `Project` class with LOCKED shape (name required + facet:structure, repoRoot optional + facet:structure, relationships {}) | VERIFIED | Plan 01 inline `node -e` integrity check emits `OK: Project class registered correctly` (exit 0); `grep -n '"Project"' .data/ontologies/upper.json` → line 149; properties block matches CONTEXT.md verbatim |
| 2 | All 13 pre-existing classes in upper.json byte-identical (File/Service/Feature/Contract/RuntimeDiagnostics/StaticDiagnostics/Port/Config/Container/Process/Fault/Limitation/Revision still present) | VERIFIED | `git diff HEAD~10 HEAD -- .data/ontologies/upper.json --stat` → 17 insertions, 0 deletions; diff shows ONLY the new `"Project": { ... }` block inserted between Feature (ends ~148) and Contract (line 166) |
| 3 | Single ontology file modified — no per-team duplication | VERIFIED | `git log --name-only` for phase commits shows only `.data/ontologies/upper.json`; `meta.extends: "upper"` propagation confirmed in test runtime (7 teams all see Project) |
| 4 | `OntologyRegistry.isValidClass('Project') === true` for every team (agentic, cluster-reprocessing, code-entities, coding, raas, resi, ui) after `OntologyConfigManager.initialize()` | VERIFIED | `node --test dist/ontology/OntologyConfigManager.layout.test.js` exits 0 with 5/5 pass; stderr contains 7 lines `[Test C] team=<team> Project=valid (Phase 42.1.2 layer-2 closed)` (count verified: `wc -l` → 7) |
| 5 | Phase 42.1.1 Option-A "DROPPED" framing replaced by "RESTORED" framing referencing Phase 42.1.2 | VERIFIED | `src/ontology/OntologyConfigManager.layout.test.ts:155` contains `NOTE (Phase 42.1.2 — Option-A carve-out RESTORED, 2026-05-25)`; lines 156-175 contain the closure narrative referencing Phase 42.1.2 Plan 01 + CONTEXT.md `<decisions>` § Verification Boundary bullet 1 |
| 6 | Negative log line `Project=NOT loaded (see SUMMARY known-residuals)` is gone | VERIFIED | `grep -F "Project=NOT loaded" src/ontology/OntologyConfigManager.layout.test.ts` exits 1 (no match) |
| 7 | `ensureProjectAnchor` cold path: `storeEntity` called once with `{ name: 'Coding', entityType: 'Project', ontologyClass: 'Project' }` and `{ team: 'coding' }` | VERIFIED | `node --test dist/agents/wave-controller-ensure-project-anchor.test.js` cold-path test PASS; assertions on entity.name, entityType, ontologyClass, opts.team all present (test lines 194-205) |
| 8 | `ensureProjectAnchor` warm path: `storeEntity` NOT called when Project/Coding already exists | VERIFIED | Warm-path test PASS; assertion `storeEntityCalls.length === 0` at test line 240-244 |
| 9 | `ensureProjectAnchor` idempotency: two back-to-back calls produce exactly 1 storeEntity invocation | VERIFIED | Idempotency test PASS; `storeEntityCalls.length === 1` AND `queryEntitiesCalls.length === 2` (test lines 278-287); locked contract re-asserted on the sole mint (lines 292-304) |
| 10 | Production source `integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` byte-identical to pre-phase state | VERIFIED | Submodule `git show --stat 7f71c8f` shows ONLY `wave-controller-ensure-project-anchor.test.ts` (1 file, 310 insertions); `git diff 7f71c8f^ 7f71c8f -- src/agents/wave-controller.ts` is empty (exit 0, no output) |
| 11 | All 4 plans' verify commands exit 0 and zero regression in sibling test suites | VERIFIED | `node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/registry-adoption.test.js` → 14/14 pass; combined wave-controller test (3/3) + layout test (5/5) + sibling regression (14/14) = 22/22 |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `.data/ontologies/upper.json` | Project class registered, LOCKED shape, structure-facet anchor grouped with File/Service/Feature | VERIFIED | line 149-165: name (required, facet:structure) + repoRoot (optional, facet:structure) + relationships {} + non-empty description referencing `ensureProjectAnchor`. Inserted between Feature (line 148) and Contract (line 166). Plan 01 integrity check exits 0. |
| `src/ontology/OntologyConfigManager.layout.test.ts` | Test C upgraded to positive per-team `isValidClass('Project')` assertion + RESTORED docblock | VERIFIED | Test C at lines 190-245 iterates `TEAMS` (7 teams), constructs fresh registry, asserts `isValidClass('Project') === true` per team; docblock at lines 155-188 captures RESTORED framing with Phase 42.1.2 references. 5 it() blocks total (A/B/C/D/E unchanged in count). |
| `integrations/mcp-server-semantic-analysis/src/agents/wave-controller-ensure-project-anchor.test.ts` | New integration smoke covering cold/warm/idempotency contract | VERIFIED | 310 lines, 1 describe block, 3 it() blocks; uses only `node:test` + `node:assert/strict` + os + path + `./wave-controller.js` — no jest/vitest/mocha; no console.* calls; `as unknown as { ... }` visibility hatch; per-test `os.tmpdir()` sandbox for WorkflowReportAgent's mkdirSync |
| `integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` | UNCHANGED (production source not modified) | VERIFIED | `git diff 7f71c8f^ 7f71c8f -- src/agents/wave-controller.ts` empty; only test file added in commit 7f71c8f |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `.data/ontologies/coding-ontology.json` | `.data/ontologies/upper.json` | `meta.extends: "upper"` | WIRED | Test C runtime evidence: 7 teams each load registry over `.data/ontologies/` and `isValidClass('Project')` returns true → propagation through meta.extends confirmed observably (not just by grep) |
| `km-core OntologyRegistry` | `.data/ontologies/upper.json` | loaded via `new OntologyRegistry({ ontologyDir })` after `OntologyConfigManager.initialize()` | WIRED | Layout test stderr shows `[OntologyConfigManager] upper ontology resolved: layout=flat alias=upper.json path=/Users/Q284340/Agentic/coding/.data/ontologies/upper.json` for each team, followed by `[Test C] team=<team> Project=valid` |
| `wave-controller-ensure-project-anchor.test.ts` | `WaveController.ensureProjectAnchor` (private, line 2262 of wave-controller.ts) | TypeScript `as unknown as { ensureProjectAnchor: ... }` visibility hatch | WIRED | Test imports `WaveController` from `./wave-controller.js`, constructs instance with tmpdir repositoryPath + team='coding', patches `kmCoreAdapter` onto the cast, invokes `ensureProjectAnchor(runId)` — all 3 it() blocks PASS |
| Test stub `fakeAdapter` | `KmCoreAdapter` contract (queryEntities + storeEntity) | structural-typed spies | WIRED | `storeEntityCalls`/`queryEntitiesCalls` arrays record every invocation; cold-path test asserts full locked contract on the captured call (name/entityType/ontologyClass/team) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| upper.json | `classes.Project` | static JSON declaration | Yes — read by km-core registry on construction; 7 teams confirm `isValidClass('Project')==true` at runtime | FLOWING |
| Test C | `registry.isValidClass('Project')` return | `new OntologyRegistry({ ontologyDir })` over real `.data/ontologies/` | Yes — registry actually loads + parses upper.json; per-team `[Test C] Project=valid` lines confirm Project flows from upper.json through registry construction | FLOWING |
| Wave-controller integration test | spy capture arrays `storeEntityCalls`/`queryEntitiesCalls` | hand-rolled fakeAdapter spies | Yes — cold-path test asserts captured `entity.name === 'Coding'`, `entity.entityType === 'Project'`, `entity.ontologyClass === 'Project'`; warm-path asserts length===0; data flow from invocation through spy fully verified | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Plan 01 ontology integrity | `node -e "..."` (inline structural assertion check) | exit 0; stderr `OK: Project class registered correctly` | PASS |
| Plan 02 layout test | `node --test dist/ontology/OntologyConfigManager.layout.test.js` | exit 0; `tests 5 / pass 5 / fail 0` | PASS |
| Plan 02 grep gate | `grep -F "Project=valid (Phase 42.1.2 layer-2 closed)" /tmp/42.1.2-layout-test.log \| wc -l` | `7` | PASS |
| Plan 03 integration smoke | `node --test dist/agents/wave-controller-ensure-project-anchor.test.js` | exit 0; `tests 3 / pass 3 / fail 0` | PASS |
| Cross-plan regression | `node --test dist/ontology/ontologyPathResolver.test.js dist/ontology/registry-adoption.test.js` | exit 0; `tests 14 / pass 14 / fail 0` | PASS |
| Submodule build | `cd integrations/mcp-server-semantic-analysis && npm run build` | exit 0; tsc clean + shx chmod | PASS |
| Wave-controller byte-identical | `git diff 7f71c8f^ 7f71c8f -- src/agents/wave-controller.ts` | empty output (exit 0) | PASS |

### Probe Execution

No project-convention probes (`scripts/*/tests/probe-*.sh`) declared for this phase. Verification fully covered by `node --test` gates above.

### Requirements Coverage

No formal requirement IDs declared for this phase. PLAN frontmatter for all 3 plans has `requirements: []`. Per ROADMAP.md: "**Requirements**: none (decision-coverage gate against CONTEXT.md `<decisions>`)". Coverage is verified via the LOCKED decisions in CONTEXT.md (mapped 1:1 to truths above):

| LOCKED Decision (CONTEXT.md) | Coverage |
| ---------------------------- | -------- |
| Target File LOCKED: add to `.data/ontologies/upper.json` only (not per-team) | SATISFIED by Truth 3 (single ontology file modified) |
| Class Shape LOCKED: name (required, facet:structure) + repoRoot (optional, facet:structure) + relationships {} + description | SATISFIED by Truth 1 (Plan 01 integrity check exit 0; on-disk JSON exactly matches CONTEXT.md verbatim block) |
| Relationships LOCKED: anchor-only `{}` (no contains/owns/anchors) | SATISFIED by Truth 1 (`relationships: {}` enforced by integrity check assertion `Object.keys(p.relationships).length === 0`) |
| Verification Boundary LOCKED bullet 1: per-team `isValidClass('Project') === true` for all 7 teams in TEAMS | SATISFIED by Truths 4 + 5 + 6 (Test C runs, 7 stderr lines confirm, RESTORED framing in docblock, negative log line removed) |
| Verification Boundary LOCKED bullet 2: cold (one storeEntity with locked contract) + warm (zero storeEntity) + idempotency (one mint over two calls) | SATISFIED by Truths 7 + 8 + 9 (3/3 wave-controller tests PASS) |
| Phase 42.1 SC#6 wave-analysis re-run OUT OF SCOPE | RESPECTED — no SC#6 re-run attempted; production wave-controller.ts byte-identical (Truth 10) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | — | — | None. `grep -nE "TBD\|FIXME\|XXX"` over all 3 modified files → no matches. `grep -nE "console\.(log\|error\|warn)"` over both test files → no matches. No empty implementations, no hardcoded empty data for rendered output, no placeholder strings. |

### REVIEW.md Findings Classification

The phase REVIEW.md flagged 3 WARNINGs and 4 INFOs. Each classified against the LOCKED decision contract:

| Finding | Severity (REVIEW) | Verifier Classification | Rationale |
| ------- | ----------------- | ---------------------- | --------- |
| WR-01: Project has zero relationships — anchor edges have no schema home | WARNING | Non-blocking | CONTEXT.md `<decisions>` § "Relationships (LOCKED)" explicitly LOCKS `relationships: {}` as anchor-only. Adding `part_of` or any outgoing edge would VIOLATE the LOCKED decision. The reviewer's concern is for future hierarchical-restructure work (MEMORY.md "NEXT" project) — explicitly deferred per `<deferred>` in CONTEXT.md. Honoring the lock is correct; the residual is owned by the future restructure project. |
| WR-02: Project added to `classes` but absent from `_legacy.entities` — split-brain risk | WARNING | Non-blocking | CONTEXT.md `<decisions>` and Plan 01 explicitly forbid touching the `_legacy` block. The reviewer suggests a forensic grep gate for `_legacy.entities` consumers. Test C runtime evidence (7 teams all see Project via km-core registry over the post-initialize ontologyDir) confirms the active codepath uses the new `classes` block, not `_legacy`. Latent risk surfaces as a separate hygiene item, not a Phase 42.1.2 acceptance blocker. |
| WR-03: Idempotency test flag-flip couples to ordering | WARNING | Non-blocking | Documented in Plan 03 SUMMARY as a deliberate "mutable closure-captured `codingMinted` flag" decision (line 91 of 42.1.2-03-SUMMARY.md). The test currently PASSES the locked contract correctly (`storeEntity.length === 1` AND `queryEntities.length === 2` after two calls). The reviewer's refactor suggestion (move flip into spy) is a cleanup, not a contract violation. The test's failure mode they describe would still surface the regression — just with a less-precise message. |
| IN-01: Add Project-existence assertion to `before()` block | INFO | Optional improvement | Non-blocking; current before() sanity gate is sufficient for the LOCKED verification boundary. |
| IN-02: Inline comment on `persistedEntityNames` usage scope | INFO | Optional improvement | Pure documentation polish. |
| IN-03: Test C N+1 file-reads cost comment | INFO | Optional improvement | Performance concern flagged as explicitly out of v1 scope. |
| IN-04: Em-dash UTF-8 encoding | INFO | Optional informational | Consistent with prior art in upper.json. |

**Conclusion:** All 3 WARNINGs are either (a) honoring an explicit LOCKED decision (WR-01) that the reviewer would have the phase violate, (b) a latent future-work item not in this phase's contract (WR-02), or (c) a deliberate documented design choice that does not break the LOCKED contract (WR-03). None of the 7 findings escalate to a gap against the LOCKED decision contract.

### Human Verification Required

None — verification fully covered by automated `node --test` gates:
- Plan 01: inline `node -e` structural assertion exit 0
- Plan 02: 5/5 layout test pass + 7 forensic stderr lines
- Plan 03: 3/3 integration smoke pass (cold/warm/idempotency)
- Cross-plan regression: 14/14 sibling tests (ontologyPathResolver + registry-adoption)
- Byte-identity check on production source: `git diff` empty

No visual UI, real-time behavior, external service integration, or human-judgment item requires manual verification for this phase. The Phase 42.1 SC#6 wave-analysis re-run (which DOES require operator action — Docker rebuild + `ukb full`) is explicitly OUT OF SCOPE for Phase 42.1.2 per CONTEXT.md line 132-134 and remains owned by Phase 42.1's verifier.

### Gaps Summary

No gaps. All 11 truths VERIFIED, all 4 required artifacts VERIFIED, all 4 key links WIRED, all data flows FLOWING, all 7 behavioral spot-checks PASS, all 6 LOCKED decisions SATISFIED, zero blocking anti-patterns, zero regressions in sibling test suites (14/14 pass), production `wave-controller.ts` byte-identical to pre-phase state. The phase goal — `OntologyRegistry.isValidClass('Project') === true` after `OntologyConfigManager.initialize()` so `ensureProjectAnchor('Coding')` no longer throws — is observably achieved end-to-end through real km-core registry loads (7 teams) AND the integration spy gate on the actual caller.

---

*Verified: 2026-05-25T05:08:28Z*
*Verifier: Claude (gsd-verifier)*
