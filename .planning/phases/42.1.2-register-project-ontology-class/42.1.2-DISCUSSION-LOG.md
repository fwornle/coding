# Phase 42.1.2: Discussion Log

**Date:** 2026-05-24
**Mode:** /gsd-discuss-phase (default, no flags)

This log captures the raw discussion that produced
`42.1.2-CONTEXT.md`. Downstream agents read CONTEXT.md, not this file —
this is for audit / retrospective only.

---

## Areas Selected

User selected all 4 gray areas:
1. Target file (upper.json vs coding-ontology.json)
2. Class shape (minimal vs standard vs rich)
3. Relationships (anchor-only vs declared edges)
4. Verification boundary (smoke vs wave-analysis re-run)

---

## Area 1 — Target File

**Options presented:**
- upper.json — system-level (Recommended)
- coding-ontology.json — team-specific only
- Both — minimal in upper + CodingProject extension in coding-ontology

**User selected:** upper.json — system-level

**Notes:** Project is a universal concept; every team's wave-analysis needs an
anchor. coding-ontology.json `meta.extends: "upper"`, so adding once to upper.json
gives all 7 teams Project automatically. Zero duplication, zero drift risk.

---

## Area 2 — Class Shape

**Options presented:**
- Minimal: name + description
- Standard: name + repoRoot + facet annotation (Recommended)
- Rich: name + repoRoot + team + ecosystem + lifecycle

**User selected:** Standard: name + repoRoot + facet annotation

**Notes:** `name` matches what ensureProjectAnchor already stores (`name: 'Coding'`).
`repoRoot` is a forward-looking property useful for findBestParent path-prefix matching.
`facet: "structure"` matches upper.json's existing convention (six facets across the
Structure/Behavior/Operations/Resilience/Quality/Evolution axes).

---

## Area 3 — Relationships

**Options presented:**
- Anchor-only: `relationships: {}` (Recommended)
- Declare `contains: ['Component']` only
- Declare full hierarchy: `contains: [Component, Service, Feature]`

**User selected:** Anchor-only: `relationships: {}` (empty)

**Notes:** Matches existing upper.json convention (File, Service, Feature, Component
all declare `relationships: {}`). Declaring `contains: ['Component']` would couple
upper→lower (Component lives in coding-ontology.json), breaking layering. Full
hierarchy is the planned hierarchical-restructure project's work, not this phase's.

---

## Area 4 — Verification Boundary

**Options presented:**
- Smoke + integration test (Recommended)
- Smoke only — minimal
- Full — include wave-analysis re-run

**User selected:** Smoke + integration test

**Notes:** Two gates:
1. Unit-level: extend 42.1.1 Test C to assert `isValidClass('Project') === true`
   for all 7 teams (replace the existing "Project=NOT loaded" log line with a
   positive assertion).
2. Integration: stub kmCoreAdapter, call `ensureProjectAnchor`, assert no throw
   AND storeEntity called with correct payload; exercise idempotency (cold + warm
   path).

Phase 42.1 SC#6 wave-analysis re-run remains owned by Phase 42.1's verifier —
same boundary as 42.1.1 CONTEXT line 93-94.

---

## Cross-Reference Todos

- `2026-05-23-orphan-digest-observation-refs.md` — score 0.6 (keywords-only
  match: "surfaced, phase, run"). **Reviewed, not folded.** Topic is
  observation/digest data integrity, not ontology class registration. Stays in
  pending.

---

## Scope Creep Redirected

None — user stayed within phase boundary; the broader hierarchical knowledge
restructuring was already pre-flagged as a separate project in MEMORY.md and was
correctly captured in deferred ideas without prompting.

---

## Claude's Discretion (carried into CONTEXT.md)

- Exact wording of description fields.
- Test file location (extend `OntologyConfigManager.layout.test.ts` Test F vs new
  `wave-controller-ensure-project-anchor.test.ts`).
- Whether the integration test stubs km-core directly or extends an existing
  harness.
- Commit message wording.

---

## Deferred Ideas Captured

- Hierarchical knowledge restructuring (Project → Component → SubComponent →
  Detail with declared edges) — explicitly its own future project per MEMORY.md.
- Per-team Project subtypes (CodingProject extends Project).
- Cross-class edges from Project (`contains`/`owns`/`anchors`).
- `registry.hasClass` API drift (Anomaly 5 from 42.1 forensic; WR-06 from 42.1.1
  REVIEW.md). Pre-existing residual, separate hygiene ticket.
