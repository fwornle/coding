---
phase: 42.1.2-register-project-ontology-class
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .data/ontologies/upper.json
autonomous: true
requirements: []
tags: [ontology, upper-ontology, project-class, layer-2-of-sc6]

must_haves:
  truths:
    - "`.data/ontologies/upper.json` declares a `Project` class with `name` (required, facet: structure) and `repoRoot` (optional, facet: structure)."
    - "The new `Project` class has `relationships: {}` (anchor-only, no outgoing edges)."
    - "The Project class block sits inside the top-level `classes` map, grouped with the other structure-facet anchors (File/Service/Feature) for visual consistency with upper.json's existing facet ordering."
    - "Existing classes in upper.json are byte-identical to pre-change state — only insertion, no edits to File/Service/Feature/Contract/RuntimeDiagnostics/StaticDiagnostics/Port/Config/Container/Process/Fault/Limitation/Revision or the `_legacy` block."
  artifacts:
    - path: ".data/ontologies/upper.json"
      provides: "Project class declaration registered in the upper ontology"
      contains: "\"Project\":"
  key_links:
    - from: ".data/ontologies/coding-ontology.json"
      to: ".data/ontologies/upper.json"
      via: "meta.extends: \"upper\""
      pattern: "\"extends\":\\s*\"upper\""
    - from: "km-core OntologyRegistry"
      to: ".data/ontologies/upper.json"
      via: "loaded automatically when ontologyDir is set to .data/ontologies/"
      pattern: "registry\\.isValidClass\\('Project'\\)"
---

<objective>
Register the `Project` class in `.data/ontologies/upper.json` so that km-core
`OntologyRegistry.isValidClass('Project')` returns `true` for every team after
`OntologyConfigManager.initialize()`.

Purpose: closes layer-2 of the SC#6 root cause. Phase 42.1.1 fixed the loader (layer-1);
this plan makes `Project` a real ontology class so the runtime
`ensureProjectAnchor('Coding')` call from `wave-controller.persistWithKmCore` no longer
throws `Unknown ontology class: Project`.

Output: a single modified file — `.data/ontologies/upper.json` — with a `Project` class
declaration that matches the LOCKED shape from CONTEXT.md exactly, inserted in the
structure-facet group (after `Feature`, before `Contract`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42.1.2-register-project-ontology-class/42.1.2-CONTEXT.md
@.planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-01-SUMMARY.md
@.data/ontologies/upper.json
@.data/ontologies/coding-ontology.json

<interfaces>
<!-- Existing classes map shape in upper.json (lines 7-473). Every class block follows
     this contract — Project must conform. -->

Each entry under `"classes": { ... }` is keyed by ClassName (PascalCase) and has:

```
"<ClassName>": {
  "description": "<one-line human description>",
  "relationships": { ... } | {},
  "properties": {
    "<propName>": {
      "type": "string" | "number" | "array",
      "description": "<description>",
      "required"?: true,
      "facet"?: "structure" | "behavior" | "operations" | "resilience" | "quality" | "evolution",
      "enum"?: [ ... ],
      "items"?: "string" | { ... },
      "examples"?: [ ... ]
    }
  }
}
```

Reference precedents in the SAME file (read for facet-annotation pattern):
- `"File"` at line 8 — structure-facet, has both `required` and `facet: "quality"` props.
- `"Service"` at line 57 — structure-facet, `serviceName` required.
- `"Feature"` at line 122 — structure-facet, simplest required-only block.

The `_legacy` block at line 474 onwards is the older entities/relationships schema and is
NOT in scope for this plan — DO NOT add Project there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Project class block into upper.json classes map</name>
  <files>.data/ontologies/upper.json</files>
  <read_first>
    - .planning/phases/42.1.2-register-project-ontology-class/42.1.2-CONTEXT.md (the LOCKED Class Shape JSON block in `<decisions>` § "Class Shape (LOCKED)" — the executor MUST replicate this verbatim including property descriptions, required-flag, and facet annotations)
    - .data/ontologies/upper.json (current state — confirm insertion point and that no `Project` class exists yet)
    - .data/ontologies/coding-ontology.json (confirms `meta.extends: "upper"` so that one class in upper.json is visible to every team)
  </read_first>
  <action>
    Insert a new `Project` class entry into the `classes` object in `.data/ontologies/upper.json`.

    Insertion point: inside the `classes` object, between the existing `"Feature"` block (ends around line 148, closing `}` before the comma) and the `"Contract"` block (starts around line 149). This keeps the structure-facet anchors (File → Service → Feature → Project) grouped together before the behavior-facet `Contract` block, consistent with upper.json's existing facet-ordered grouping.

    Shape: replicate the LOCKED JSON block from CONTEXT.md `<decisions>` § "Class Shape (LOCKED)" — the planner does NOT inline the full JSON here because that is what `<read_first>` covers; the executor reads CONTEXT.md and matches it.

    Required properties of the inserted block (assertion-grade; for the executor's self-check before writing):
    - Top-level key: `"Project"` (PascalCase).
    - `description`: present, non-empty, mentions "anchor" and `ensureProjectAnchor` per CONTEXT.md.
    - `relationships`: `{}` (empty object — NOT omitted, NOT `null`). This matches the convention used by `File`/`Service`/`Feature`/`Contract` etc in upper.json.
    - `properties.name`: `{ "type": "string", "description": "...", "required": true, "facet": "structure" }` — note required AND facet both present per CONTEXT.md LOCKED shape.
    - `properties.repoRoot`: `{ "type": "string", "description": "...", "facet": "structure" }` — NO `required` key (it is optional).

    Hard prohibitions (per CONTEXT.md `<decisions>` § "Relationships (LOCKED)" + `<deferred>`):
    - DO NOT add `contains`, `owns`, `anchors`, or any other outgoing edge to `Project.relationships` — anchor-only is locked.
    - DO NOT add additional properties beyond `name` and `repoRoot` (no `team`, no `ecosystem`, no `lifecycle`).
    - DO NOT modify any OTHER class in upper.json (File/Service/Feature/Contract/RuntimeDiagnostics/StaticDiagnostics/Port/Config/Container/Process/Fault/Limitation/Revision).
    - DO NOT touch the `_legacy` block (lines 474-1229 — that is the old entities/relationships schema kept for backward compatibility; Project lives ONLY in the new top-level `classes` map per CONTEXT.md).
    - DO NOT modify `.data/ontologies/coding-ontology.json` or any other team ontology file (CONTEXT.md `<decisions>` § "Target File" explicitly rejects per-team duplication — `meta.extends: "upper"` propagates the class to every team automatically).

    JSON formatting: 2-space indentation matching the rest of the file; comma between `"Feature": { ... }` and `"Project": { ... }` and between `"Project": { ... }` and `"Contract": { ... }`. Final file MUST parse with `JSON.parse(fs.readFileSync(...))` without error.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('.data/ontologies/upper.json','utf8')); const p=j.classes && j.classes.Project; if(!p){process.stderr.write('FAIL: classes.Project missing\n'); process.exit(1);} if(typeof p.description!=='string' || p.description.length===0){process.stderr.write('FAIL: Project.description must be non-empty string\n'); process.exit(1);} if(!p.relationships || typeof p.relationships!=='object' || Object.keys(p.relationships).length!==0){process.stderr.write('FAIL: Project.relationships must be empty object {}\n'); process.exit(1);} if(!p.properties || !p.properties.name || p.properties.name.type!=='string' || p.properties.name.required!==true || p.properties.name.facet!=='structure'){process.stderr.write('FAIL: Project.properties.name must be {type:string, required:true, facet:structure}\n'); process.exit(1);} if(!p.properties.repoRoot || p.properties.repoRoot.type!=='string' || p.properties.repoRoot.facet!=='structure' || p.properties.repoRoot.required===true){process.stderr.write('FAIL: Project.properties.repoRoot must be {type:string, facet:structure} and NOT required\n'); process.exit(1);} if(Object.keys(p.properties).length!==2){process.stderr.write('FAIL: Project.properties must have EXACTLY 2 keys (name, repoRoot), got: '+Object.keys(p.properties).join(',')+'\n'); process.exit(1);} const expectedOthers=['File','Service','Feature','Contract','RuntimeDiagnostics','StaticDiagnostics','Port','Config','Container','Process','Fault','Limitation','Revision']; for(const k of expectedOthers){if(!j.classes[k]){process.stderr.write('FAIL: classes.'+k+' must still be present (no other classes deleted)\n'); process.exit(1);}} process.stderr.write('OK: Project class registered correctly\n');"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.data/ontologies/upper.json` parses as valid JSON.
    - Source: `JSON.parse(...).classes.Project` is an object (not undefined, not null).
    - Source: `classes.Project.relationships` is an empty object `{}` (length-0 keys).
    - Source: `classes.Project.properties` has EXACTLY two keys: `name` and `repoRoot`.
    - Source: `classes.Project.properties.name === { type: 'string', description: <non-empty>, required: true, facet: 'structure' }` (description text is at Claude's discretion per CONTEXT.md `<decisions>` § "Claude's Discretion").
    - Source: `classes.Project.properties.repoRoot === { type: 'string', description: <non-empty>, facet: 'structure' }` (no `required` key — explicitly absent so it defaults to optional).
    - Source: all 13 pre-existing classes (File, Service, Feature, Contract, RuntimeDiagnostics, StaticDiagnostics, Port, Config, Container, Process, Fault, Limitation, Revision) are still present (no accidental deletions).
    - Source: `git diff .data/ontologies/upper.json` shows insertions ONLY — no modifications to existing class blocks or the `_legacy` block.
    - Source: NO modifications to `.data/ontologies/coding-ontology.json` or any other team ontology file (verify with `git status .data/ontologies/`).
    - Behavior: the verify command above exits 0 with stderr `OK: Project class registered correctly`.
  </acceptance_criteria>
  <done>
    `.data/ontologies/upper.json` parses as valid JSON, contains a `Project` class block matching the CONTEXT.md `<decisions>` § "Class Shape (LOCKED)" specification exactly, all 13 pre-existing classes are still present and byte-identical (only insertions, no deletions or edits), no other ontology files modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan touches only a static JSON metadata file inside the repo. No user input, no network calls, no file writes outside `.data/ontologies/upper.json`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42.1.2-01-00 | (none — read-only metadata change) | `.data/ontologies/upper.json` | accept | No new threats introduced. The change is a JSON metadata addition to an existing on-disk ontology file. No user input is parsed, no network or process boundary is crossed, no file outside the repo's ontology directory is touched. The km-core OntologyRegistry already validates ontology JSON shape on load (existing Phase 38 / 42.1.1 contract); a malformed Project block would surface as a load-time error during the next test run (caught by Plans 42.1.2-02 and 42.1.2-03), not as an exploitable runtime path. STRIDE coverage: Spoofing N/A (no identity), Tampering — the JSON edit IS the intended tampering; guarded by the strict JSON-parse + property-shape assertions in the verify command, Repudiation N/A (committed via git), Information disclosure N/A (no secrets — just class metadata), Denial of service — a syntax-broken JSON would crash registry load, mitigated by the verify command's `JSON.parse` + shape assertions, Elevation of privilege N/A (no privilege boundary). |
</threat_model>

<verification>
- Run the Task 1 verify command (inline `node -e`) from the repo root and confirm it exits 0 with stderr `OK: Project class registered correctly`.
- `git status .data/ontologies/` shows ONLY `.data/ontologies/upper.json` modified — no other ontology files touched.
- `git diff .data/ontologies/upper.json` shows ONLY insertions (the new `"Project": { ... }` block + the surrounding comma/newline) — no lines deleted, no existing class blocks modified.
</verification>

<success_criteria>
- `.data/ontologies/upper.json` declares the `Project` class per the CONTEXT.md LOCKED shape.
- All 13 pre-existing classes byte-identical to pre-change state (only insertion).
- No other ontology files modified.
- File parses as valid JSON.
- This plan unblocks Plans 42.1.2-02 and 42.1.2-03 (both depend on the JSON change landing first).
</success_criteria>

<output>
Create `.planning/phases/42.1.2-register-project-ontology-class/42.1.2-01-SUMMARY.md` when done.
</output>
