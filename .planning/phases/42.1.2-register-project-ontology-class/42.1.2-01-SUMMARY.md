---
phase: 42.1.2-register-project-ontology-class
plan: 01
subsystem: ontology
tags: [ontology, upper-ontology, project-class, layer-2-of-sc6]
requires:
  - .planning/phases/42.1.1-ontology-layout-resolution-registry-empty-because-loader-exp/42.1.1-01-SUMMARY.md
provides:
  - "Project ontology class registered in upper.json; OntologyRegistry.isValidClass('Project') === true for every team that extends 'upper'"
affects:
  - .data/ontologies/upper.json
tech-stack:
  added: []
  patterns:
    - "structure-facet anchor group (File → Service → Feature → Project) before behavior-facet Contract"
    - "anchor-only relationships: {} convention (no outgoing edges from upper-level Project to lower-level classes)"
    - "meta.extends: 'upper' propagation — single declaration visible to all 7 teams via OntologyRegistry"
key-files:
  created: []
  modified:
    - .data/ontologies/upper.json
decisions:
  - "Insert Project as 4th structure-facet anchor (between Feature and Contract) — preserves upper.json's facet-ordered grouping"
  - "Exactly 2 properties: name (required, facet: structure) + repoRoot (optional, facet: structure) per CONTEXT.md LOCKED shape"
  - "relationships: {} — no outgoing edges from Project; reverse anchor edges remain implied by wave-controller.persistWithKmCore logic"
metrics:
  duration: "~5min"
  completed: "2026-05-25"
  tasks: 1
  files: 1
---

# Phase 42.1.2 Plan 01: Register Project Ontology Class — Summary

Registered `Project` as the 4th structure-facet anchor class in `.data/ontologies/upper.json`, making `OntologyRegistry.isValidClass('Project') === true` for every team that extends `upper` (closes layer-2 of the SC#6 root cause cascade from Phase 42.1.1).

## What Was Built

- **New `Project` class block** inserted between `Feature` (line 148) and `Contract` (line 149) inside the top-level `classes` map of `.data/ontologies/upper.json`.
- **Shape (LOCKED per CONTEXT.md):**
  - `description` — references the anchor role + `ensureProjectAnchor` runtime mint point.
  - `relationships: {}` — anchor-only, no outgoing edges (matches File/Service/Feature/Contract precedents).
  - `properties.name` — `string`, `required: true`, `facet: "structure"`.
  - `properties.repoRoot` — `string`, optional (no `required` key), `facet: "structure"`.
- **Insertion is additive only** — 17 insertions, 0 deletions per `git diff --numstat`. Every pre-existing class (File / Service / Feature / Contract / RuntimeDiagnostics / StaticDiagnostics / Port / Config / Container / Process / Fault / Limitation / Revision) and the legacy block remain byte-identical.
- **Single file modified** — no `.data/ontologies/coding-ontology.json` (or any other team ontology) edit, because `meta.extends: "upper"` propagates the new class to all 7 teams automatically.

## Tasks Completed

| Task | Name                                                   | Commit    | Files                          |
| ---- | ------------------------------------------------------ | --------- | ------------------------------ |
| 1    | Insert Project class block into upper.json classes map | `00a0d7d` | `.data/ontologies/upper.json`  |

## Verification Results

Plan-defined inline `node -e` verification (assertion-grade structural check):

```
OK: Project class registered correctly
exit=0
```

Asserted:
- `JSON.parse(...)` succeeds (valid JSON).
- `classes.Project` is an object.
- `description` is a non-empty string.
- `relationships` is an empty object (`Object.keys(...).length === 0`).
- `properties.name === {type:'string', required:true, facet:'structure'}` (plus description).
- `properties.repoRoot === {type:'string', facet:'structure'}` (NO `required` key, optional).
- `Object.keys(properties).length === 2` (exactly name + repoRoot, no extras).
- All 13 pre-existing classes still present.

Git-diff sanity check:
- `git diff --numstat .data/ontologies/upper.json` → `17  0  .data/ontologies/upper.json` (insertions only).
- `git status .data/ontologies/` → only `upper.json` modified.

## Deviations from Plan

None — plan executed exactly as written. The LOCKED shape from CONTEXT.md was replicated verbatim including property descriptions, the `required` flag on `name`, and the `facet: "structure"` annotations on both properties.

## Success Criteria

- [x] `.data/ontologies/upper.json` declares the `Project` class per the CONTEXT.md LOCKED shape.
- [x] All 13 pre-existing classes byte-identical to pre-change state (only insertion).
- [x] No other ontology files modified.
- [x] File parses as valid JSON.
- [x] This plan unblocks Plans 42.1.2-02 and 42.1.2-03 (both depend on the JSON change landing first).

## Downstream Impact

- **Plan 42.1.2-02** (unit-level smoke — extends Phase 42.1.1 Test C): can now flip the softened `domains.length === 8` assertion back to a positive `registry.isValidClass('Project') === true` check across all 7 teams.
- **Plan 42.1.2-03** (integration smoke — `ensureProjectAnchor` no-throw test): can now construct a real `OntologyRegistry` over the post-`OntologyConfigManager.initialize()` `ontologyDir` and assert `storeEntity` is called with `entityType: 'Project'` + `ontologyClass: 'Project'` without `Unknown ontology class: Project` being raised by km-core's `registryBackedValidator`.
- **Phase 42.1 SC#6 promotion**: still owned by Phase 42.1's verifier per the CONTEXT.md boundary (42.1.1-CONTEXT.md line 93-94). Once Plans 42.1.2-02 and 42.1.2-03 land, the operator runs the Docker rebuild and re-invokes `/gsd-verify-phase 42.1` separately — that gate is not in this phase's scope.

## Known Stubs

None — the Project class is a complete anchor declaration per the LOCKED shape. Both properties (`name`, `repoRoot`) are fully wired with concrete `type` / `description` / `facet` (and `required` where applicable) values, not placeholders.

## Self-Check: PASSED

- File exists: `.data/ontologies/upper.json` — FOUND (modified in working tree, committed).
- Commit exists: `00a0d7d84` — FOUND in `git log --oneline` (HEAD of `worktree-agent-ace2884d9d7b1ac6d`).
- SUMMARY.md exists at `.planning/phases/42.1.2-register-project-ontology-class/42.1.2-01-SUMMARY.md` — written and to be committed in the metadata commit immediately following this self-check.
