---
phase: 85-experiment-control-center
plan: 03
type: execute
wave: 2
depends_on: ["85-01", "85-02"]
files_modified:
  - scripts/health-coordinator.js
  - scripts/health-remediation-actions.js
  - tests/experiments/experiment-executor.test.mjs
autonomous: true
requirements: [D-01, D-02, D-08]

must_haves:
  truths:
    - "The host health-coordinator (:3034) exposes an experiment-executor seam that spawns the detached runner on the HOST (where the agent CLIs live), not in the container"
    - "The executor delegates spawn to run-launch.launchRun and cancel to run-launch.cancelRun (Plan 02)"
    - "The executor endpoint accepts the container/host-gateway origin but rejects arbitrary external callers"
    - "On cancel, the executor writes the cancelled progress patch AND clears any stale active-measurement.json owned by the run (OQ3), so the 409 slot frees even after a hard SIGKILL"
  artifacts:
    - path: "scripts/health-coordinator.js"
      provides: "POST /experiments/run + POST /experiments/cancel host endpoints (delegating to run-launch)"
      contains: "/experiments/run"
    - path: "scripts/health-remediation-actions.js"
      provides: "experiment_run / experiment_cancel executeAction cases (if extending the dispatcher)"
      contains: "experiment_run"
  key_links:
    - from: "scripts/health-coordinator.js"
      to: "lib/experiments/run-launch.mjs"
      via: "launchRun / cancelRun delegation"
      pattern: "launchRun|cancelRun"
    - from: "scripts/health-coordinator.js"
      to: "lib/experiments/run-progress.mjs"
      via: "canceller writes the cancelled progress patch (Plan 01 writeProgress)"
      pattern: "writeProgress"
    - from: "scripts/health-coordinator.js"
      to: ".data/active-measurement.json"
      via: "canceller clears stale span (OQ3)"
      pattern: "active-measurement"
---

<objective>
Extend the host-side health-coordinator (`:3034`) with an experiment-executor seam that performs the detached spawn and process-group cancel ON THE HOST — the D-01 amended architecture that fixes the Pitfall-4 container-spawn dead end (the container has only `node`, no agent CLIs). vkb-server (Plan 04) delegates spawn/kill to this seam over HTTP; the coordinator delegates to the Plan-02 run-launch primitives and, on cancel, writes the terminal progress patch via the Plan-01 progress emitter.

Purpose: This is the load-bearing architectural correction the research flagged as BLOCKING. Without a host executor, every experiment cell aborts with `spawn claude ENOENT`. Reusing the coordinator's `/health/remediate` dispatch precedent honors "no new daemon" (D-01).
Output: two host endpoints (`POST /experiments/run`, `POST /experiments/cancel`) on the coordinator (or two `executeAction` cases) delegating to run-launch, with the OQ3 stale-span clearing on cancel + the V4 origin gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-experiment-control-center/85-CONTEXT.md
@.planning/phases/85-experiment-control-center/85-PATTERNS.md
@.planning/phases/85-experiment-control-center/85-01-SUMMARY.md
@.planning/phases/85-experiment-control-center/85-02-SUMMARY.md

<interfaces>
<!-- Contracts + analogs. From live code this session. -->

Coordinator dispatch seam (scripts/health-coordinator.js:2577-2592):
  app.post('/health/remediate', async (req,res) => {
    const { action, service, details } = req.body || {}
    const dispatcher = await getRemediationDispatcher()
    const result = await dispatcher.executeAction(action, { service, ...details })
    return res.status(result.success ? 200 : 500).json({ ok: result.success, ...result })
  })

executeAction switch (scripts/health-remediation-actions.js:114-177):
  switch(actionName){ case 'kill_lock_holder': ...; case 'restart_vkb_server': ...; }
  each case returns { success, message, ... }

Loopback gate NOTE (health-coordinator.js ~:2618 /test/*): the /test/* LOOPBACK_IPS gate REJECTS the container (host-gateway is a non-loopback IP). Do NOT copy that gate onto the experiment endpoint verbatim — the vkb container proxy must be able to reach it. Accept container/host-gateway origin; reject arbitrary external callers (port not published beyond localhost).

run-launch API (Plan 02, lib/experiments/run-launch.mjs):
  launchRun({ specPath, runId, runDir, overrides, env }) → { pid, runDir }
  cancelRun({ pid, graceMs, runJson }) → { killed, reason? }
  isRunAlive(pid) → boolean

run-progress API (Plan 01, lib/experiments/run-progress.mjs):
  writeProgress(runDir, patch) → atomic never-throw progress.json merge (header shallow-merge + cells upsert-by-{variant,rep})
  The canceller uses this to write overall='cancelled' + in-flight cell 'abort' + remaining 'pending'.

OQ3 stale-span clearing: after a hard cancel, remove .data/experiments/... span coupling — clear .data/active-measurement.json IF its task_id belongs to the cancelled run, so the 409 slot (D-02) frees. Also write progress.json overall='cancelled' + in-flight cell 'abort' + remaining 'pending' via run-progress.writeProgress (Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: experiment-executor host endpoints on the coordinator (D-01)</name>
  <files>scripts/health-coordinator.js, scripts/health-remediation-actions.js</files>
  <read_first>
    - scripts/health-coordinator.js (the /health/remediate dispatch seam :2577-2592; the ETM detached-spawn env shape :1596-1607; the /test/* LOOPBACK_IPS gate ~:2618 you must NOT copy verbatim; how the coordinator resolves REPO_ROOT + its own env)
    - scripts/health-remediation-actions.js (executeAction switch :114-177 — the `{ success, message }` return contract each case obeys)
    - lib/experiments/run-launch.mjs (Plan 02 — launchRun/cancelRun/isRunAlive signatures you delegate to)
    - lib/experiments/run-progress.mjs (Plan 01 — writeProgress; the canceller writes the cancelled progress patch through it)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (health-coordinator.js + health-remediation-actions.js entries; V4 origin gate note; Pitfall 4)
    - .planning/phases/85-experiment-control-center/85-01-SUMMARY.md (Plan 01 writeProgress exact signature + progress.json shape the cancel patch targets)
    - .planning/phases/85-experiment-control-center/85-02-SUMMARY.md (Plan 02 run-launch exact API + how it clears/owns the span on cancel)
  </read_first>
  <action>
    Add a host executor seam to the coordinator. Discretion (RESEARCH OQ1/A1, already user-resolved to "extend the coordinator"): implement EITHER two dedicated endpoints `POST /experiments/run` and `POST /experiments/cancel` mounted next to `/health/remediate`, OR two `executeAction` cases (`experiment_run`, `experiment_cancel`) reached via the existing `/health/remediate` dispatcher — pick the one that keeps the coordinator diff smallest and note the choice. Whichever seam:

    - `run`: accept `{ spec, run_id, run_dir, overrides }`; build `hostEnv` from the coordinator's own `process.env` (which already carries CODING_REPO, LLM_PROXY_DATA_DIR, LLM_PROXY_PORT, CODING_PROXY_ROUTE); call `launchRun({ specPath: spec, runId: run_id, runDir: run_dir, overrides, env: hostEnv })`; return `{ success:true, pid }`.
    - `cancel`: accept `{ run_id, run_dir, pid }`; call `cancelRun({ pid, runJson })`; then write the terminal progress patch (overall='cancelled', in-flight cell 'abort', remaining 'pending') via `writeProgress` (Plan 01) AND clear `.data/active-measurement.json` if its task_id belongs to this run (OQ3, so the D-02 slot frees even on a hard SIGKILL); return `{ success:true, killed }`.

    V4 origin gate: gate the endpoint to accept the container/host-gateway origin and reject arbitrary external callers — do NOT reuse the `/test/*` loopback-only gate (it would reject the container proxy — PATTERNS note). Diagnostics via the coordinator's existing `log(...)` / `process.stderr.write` (never `console.*`). The spawn/kill argv is fixed-argv only (delegated to run-launch — no shell strings anywhere).
  </action>
  <verify>
    <automated>node -e "require('./scripts/health-remediation-actions.js')" 2>&1 | head -3 ; grep -n "experiment_run\|/experiments/run" scripts/health-coordinator.js scripts/health-remediation-actions.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/experiments/run\|experiment_run" scripts/health-coordinator.js scripts/health-remediation-actions.js` shows the run seam
    - `grep -n "/experiments/cancel\|experiment_cancel" scripts/health-coordinator.js scripts/health-remediation-actions.js` shows the cancel seam
    - `grep -n "launchRun\|cancelRun" scripts/health-coordinator.js scripts/health-remediation-actions.js` shows delegation to Plan-02 run-launch
    - `grep -n "writeProgress" scripts/health-coordinator.js scripts/health-remediation-actions.js` shows the cancel path writing the terminal progress patch via Plan-01
    - `grep -n "active-measurement" scripts/health-coordinator.js scripts/health-remediation-actions.js` shows the OQ3 stale-span clear on cancel
    - `grep -n "shell: *true" scripts/health-coordinator.js scripts/health-remediation-actions.js` returns nothing new (fixed-argv via run-launch)
    - `grep -n "console\.log\|console\.error" scripts/health-coordinator.js` shows no NEW console.* added by this task (use log()/stderr)
    - the endpoint is NOT wrapped in the `/test/*` LOOPBACK_IPS-only gate (container origin accepted)
  </acceptance_criteria>
  <done>The coordinator exposes a host experiment-executor seam that spawns/cancels via run-launch, writes the cancelled progress patch via Plan-01 writeProgress, clears the stale span on cancel, and accepts the container origin while rejecting external callers.</done>
</task>

<task type="auto">
  <name>Task 2: Executor contract test (delegation + stale-span clear, injected seams)</name>
  <files>tests/experiments/experiment-executor.test.mjs</files>
  <read_first>
    - scripts/health-remediation-actions.js (the executeAction return-contract shape your test asserts against; if the seam is dedicated endpoints instead, import/exercise the handler function directly)
    - tests/experiments/runs-endpoint.test.mjs (the handler-isolation idiom: `Object.create(Prototype)` + injected repoRoot — mirror it for the executor)
    - lib/experiments/run-launch.mjs (the launchRun/cancelRun signatures your fakes stand in for)
    - lib/experiments/run-progress.mjs (writeProgress — assert the cancel path writes overall='cancelled' through it)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (the injected-seam test convention)
  </read_first>
  <behavior>
    - The run seam, given {spec, run_id, run_dir, overrides}, calls launchRun with those values and the coordinator's contract env, and returns {success:true, pid}
    - The cancel seam, given {run_id, run_dir, pid}, calls cancelRun with pid, writes progress overall='cancelled' via writeProgress, and clears active-measurement.json when its task_id belongs to the run
    - The cancel seam LEAVES an unrelated active-measurement.json (different task_id) untouched
    - No real process is spawned or killed in the test (launchRun/cancelRun are injected fakes)
  </behavior>
  <action>
    Write `tests/experiments/experiment-executor.test.mjs` (node:test + node:assert/strict) exercising the seam with injected fakes for launchRun/cancelRun (import the executor factory with seam injection, or use the handler-isolation `Object.create` idiom from runs-endpoint.test.mjs). Assert: (a) run delegates to launchRun with the passed spec/run_id/run_dir/overrides and returns the pid; (b) cancel delegates to cancelRun and, via a temp run-dir + a temp active-measurement.json whose task_id belongs to the run, that progress.json overall becomes 'cancelled' (written through writeProgress) and the span file is removed; (c) a span file with a FOREIGN task_id survives cancel (OQ3 scoping). Use tmp dirs under the scratchpad, never touch real `.data/`.
  </action>
  <verify>
    <automated>node --test tests/experiments/experiment-executor.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/experiment-executor.test.mjs` passes
    - the test asserts launchRun is called with the exact {spec, run_id, run_dir, overrides} it received
    - the test asserts the cancel path writes progress overall='cancelled' via writeProgress
    - the test asserts the OQ3 stale-span clear is SCOPED (own task_id removed, foreign task_id preserved)
    - the test spawns/kills NO real process (injected fakes only)
  </acceptance_criteria>
  <done>An isolated contract test proves the executor delegates to run-launch, writes the cancelled progress patch via Plan-01 writeProgress, and scopes the stale-span clear to the cancelled run, without touching real processes or real .data/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container → coordinator :3034 | The in-container vkb proxy triggers a HOST process spawn — the sole dashboard-triggered host-spawn surface |
| external caller → coordinator | An arbitrary localhost/network caller could hit the experiment endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-01 | Elevation of Privilege | experiment-run host endpoint | mitigate | Origin gate accepts only container/host-gateway; rejects arbitrary external callers; coordinator port not published beyond localhost |
| T-85-03-02 | Tampering (command injection) | spawn argv via the executor | mitigate | Delegates to run-launch fixed-argv (Plan 02); the endpoint never builds a shell string; spec path is a server-known value, not a raw client path (Plan 04 restricts it) |
| T-85-03-03 | DoS (stale span never frees slot) | active-measurement.json after hard SIGKILL | mitigate | Canceller clears the run-owned span file (OQ3) + writes the cancelled progress patch (Plan-01 writeProgress), so the D-02 slot frees deterministically |
| T-85-03-04 | Info disclosure (path traversal via run_dir) | run_dir on the host | mitigate | run_dir is composed server-side from a validated run_id (Plan 04 `_validRunId`) under .data/experiments/runs/ — never a raw client path |
| T-85-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs — N/A |
</threat_model>

<verification>
- `node --test tests/experiments/experiment-executor.test.mjs` green
- Coordinator seam delegates to run-launch; cancel clears the run-owned span + writes the cancelled progress patch via Plan-01; container origin accepted; no new console.* / shell:true
- NOTE (deferred to Plan 06 live gate): the coordinator is a launchd daemon — a live restart (`launchctl kickstart -k gui/$(id -u)/com.coding.health-coordinator`) is required for the seam to serve on the host; the Plan 06 live gate exercises the running host process.
</verification>

<success_criteria>
- Host executor seam spawns/cancels the detached runner via run-launch (D-01 amended)
- Cancel frees the 409 slot even on hard SIGKILL (OQ3 stale-span clear, scoped) and writes the cancelled progress patch via Plan-01
- Endpoint reachable from the container proxy, closed to external callers (V4)
</success_criteria>

<output>
Create `.planning/phases/85-experiment-control-center/85-03-SUMMARY.md` when done
</output>
</content>
