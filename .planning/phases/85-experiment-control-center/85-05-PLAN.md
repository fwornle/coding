---
phase: 85-experiment-control-center
plan: 05
type: execute
wave: 4
depends_on: ["85-04"]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx
  - integrations/system-health-dashboard/src/components/performance/run-monitor.tsx
  - integrations/system-health-dashboard/src/components/performance/runs-table.tsx
autonomous: true
requirements: [D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "The launcher lists specs, previews the resolved matrix (variants × repeats = N cells) BEFORE launch, and offers override fields (repeats, timeout, variant subset, capture_raw_bodies checkbox)"
    - "A 409 launch surfaces the holder message to the operator (never a silent failure)"
    - "The monitor renders progress.json (run header + variant×repeat cell grid with per-cell state chips + abort/skip reasons), polling every 5s"
    - "The Cancel button dispatches cancelRun and acts immediately (no graceful-after-cell latency — D-08)"
    - "A Re-run button on a completed experiment run opens the launcher pre-filled (same spec + snapshot_id, rerun_of set) with the D-06 override fields"
    - "The dashboard launches/monitors but never edits a spec (no YAML editor — D-09)"
  artifacts:
    - path: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      provides: "fetchSpecList / launchExperiment / fetchRunStatus / cancelRun thunks + selectors + reducers"
      contains: "fetchRunStatus"
    - path: "integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx"
      provides: "spec picker + matrix preview + override fields + capture_raw_bodies checkbox + re-run pre-fill"
      min_lines: 60
    - path: "integrations/system-health-dashboard/src/components/performance/run-monitor.tsx"
      provides: "run header + variant×repeat grid + 5s poll + Cancel button"
      min_lines: 50
  key_links:
    - from: "run-monitor.tsx"
      to: "/api/experiments/run-status/:runId"
      via: "fetchRunStatus 5s poll"
      pattern: "setInterval.*5000"
    - from: "runs-table.tsx"
      to: "experiment-launcher.tsx"
      via: "Re-run button opens launcher pre-filled (rerun_of)"
      pattern: "rerun"
---

<objective>
Build the launcher + monitor UI on the Performance tab (D-09/D-10/D-11) plus the D-12 capture_raw_bodies checkbox: a spec picker with a pre-launch matrix preview + override fields, a 5s-polling cell-grid monitor with an immediate-acting Cancel, and a Re-run button on completed runs that opens the launcher pre-filled. Every piece mirrors `measurement-control.tsx` / `performanceSlice.ts`.

Purpose: This is the operator-facing control surface — the whole reason the phase exists. It is a control surface, not an authoring surface (D-09: launches, never edits).
Output: new thunks/selectors in `performanceSlice.ts`, two new components (`experiment-launcher.tsx`, `run-monitor.tsx`), and a Re-run button in `runs-table.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-experiment-control-center/85-CONTEXT.md
@.planning/phases/85-experiment-control-center/85-PATTERNS.md
@.planning/phases/85-experiment-control-center/85-04-SUMMARY.md

<interfaces>
<!-- Contracts + analogs. From live code this session. -->

Thunk template (performanceSlice.ts:608-642 — copy createAsyncThunk + rejectWithValue shape):
  fetchActiveMeasurement: GET /api/experiments/measurement/active
  startMeasurement: POST … → surfaces data?.message on !ok via rejectWithValue (the 409-holder path to copy)

5s-poll template (measurement-control.tsx:35-39):
  useEffect(() => { dispatch(fetchX()); const t = setInterval(() => dispatch(fetchX()), 5000); return () => clearInterval(t) }, [dispatch])

Card/form template (measurement-control.tsx:54-115): Card/CardHeader/CardContent + Input + Button + selector-driven loading/error + data-testid on every control

runs-table row-action (runs-table.tsx:276-304): <Button variant="ghost" size="sm" onClick={e => { e.stopPropagation(); dispatch(...) }} data-testid=...>  ; lucide-react icons imported :3

New API endpoints (Plan 04 — confirm exact response shapes in 85-04-SUMMARY.md):
  GET  /api/experiments/specs           → { specs: [{ file, goal_sentence, repeats, variantCount, cellCount, snapshot_id, variants }] }
  POST /api/experiments/run             → { run_id, pid }  (409 → { error, message, holder })
  GET  /api/experiments/run-status/:id  → progress.json ({ run_id, spec, snapshot_id, pid, done, total, overall, cells:[…] })
  POST /api/experiments/run-cancel      → { killed }

Redux slice discretion (D-slice): EXTEND performanceSlice (reuse selectRuns for the D-11 re-run pre-fill) per RESEARCH recommendation.

CLAUDE.md dashboard rebuild recipe (do NOT skip — VirtioFS): after editing, `npm run build` in integrations/system-health-dashboard THEN `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`. Visual verification is Plan 06.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: performanceSlice thunks + selectors (spec-list, launch, run-status, cancel)</name>
  <files>integrations/system-health-dashboard/src/store/slices/performanceSlice.ts</files>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (fetchActiveMeasurement/startMeasurement/stopMeasurement thunks :608-662; the extraReducers addCase pattern :884-912; selectors :945/:974-975; selectRuns :945 — needed for the re-run pre-fill)
    - .planning/phases/85-experiment-control-center/85-04-SUMMARY.md (the EXACT endpoint response shapes — confirm run-status path is /run-status/:id and the 409 body carries .holder/.message)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (performanceSlice.ts entry — thunk names + register-in-extraReducers rule)
  </read_first>
  <action>
    Extend `performanceSlice.ts` with four `createAsyncThunk`s copying the `startMeasurement`/`fetchActiveMeasurement` shape (rejectWithValue with `data?.message` so a 409 holder surfaces): `fetchSpecList` (GET `/api/experiments/specs`), `launchExperiment` (POST `/api/experiments/run` with `{ spec, overrides, rerun_of }`), `fetchRunStatus` (GET `/api/experiments/run-status/${encodeURIComponent(runId)}`), `cancelRun` (POST `/api/experiments/run-cancel` with `{ run_id, run_dir }`). Add state fields (`specList`, `specListLoading`, `activeRunId`, `runStatus`, `runStatusError`, `launchError`, `launcherPrefill`) + a `setLauncherPrefill` reducer (for the D-11 re-run pre-fill payload: `{ spec, snapshot_id, rerun_of, overrides }`). Register each thunk in `extraReducers` (.addCase pending/fulfilled/rejected, pattern :884-912). Add selectors mirroring `selectActiveMeasurement`/`selectMeasurementLoading`/`selectMeasurementError`: `selectSpecList`, `selectRunStatus`, `selectActiveRunId`, `selectLaunchError`, `selectLauncherPrefill`. Keep TypeScript strict (define `RunProgress`, `SpecSummary`, `LauncherPrefill` interfaces — do NOT use `any`; CLAUDE.md TS strict). Do NOT modify a working API shape to satisfy types — fix the type.
  </action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "performanceSlice" | head -5 || echo "no performanceSlice type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetchSpecList\|launchExperiment\|fetchRunStatus\|cancelRun" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` shows all four thunks
    - `grep -n "setLauncherPrefill\|selectLauncherPrefill" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` shows the D-11 pre-fill plumbing
    - `grep -n ": any" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` shows no NEW `: any` added for the new thunks (TS strict — named interfaces)
    - `cd integrations/system-health-dashboard && npx tsc --noEmit` reports no errors originating in performanceSlice.ts
    - each new thunk is registered in extraReducers (grep the thunk name appears in an .addCase)
  </acceptance_criteria>
  <done>Four typed thunks + selectors + a re-run-prefill reducer are added to performanceSlice, registered in extraReducers, with no `any` and no tsc errors in the slice.</done>
</task>

<task type="auto">
  <name>Task 2: experiment-launcher.tsx (spec picker + matrix preview + overrides + capture_raw_bodies) — D-09/D-11/D-12</name>
  <files>integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/measurement-control.tsx (the whole file — Card/Input/Button + thunk-dispatch + loading/error selector + data-testid convention; the TASK_CLASSES <select> idiom :77-85 to copy for the variant-subset picker)
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (the Task-1 thunks/selectors this component consumes: fetchSpecList/launchExperiment/selectSpecList/selectLaunchError/selectLauncherPrefill)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (experiment-launcher.tsx entry — no YAML editor, D-09; pre-fill from re-run, D-11; capture_raw_bodies checkbox, D-12)
  </read_first>
  <action>
    Create `experiment-launcher.tsx` mirroring `measurement-control.tsx`. On mount dispatch `fetchSpecList`. Render a `<select>` of `selectSpecList` specs; on selection, show the resolved-matrix preview `variants × repeats = N cells` (from the spec summary's `cellCount` — computed server-side, D-09, do NOT recompute axes client-side). Render override `<Input>`s: repeats, timeout, variant subset (copy the TASK_CLASSES `<select>` multi-pick idiom for variant subset). Render a `capture_raw_bodies` `<Checkbox>` (default OFF — D-12). A Launch `<Button>` dispatches `launchExperiment({ spec, overrides, rerun_of })`; on `launchExperiment.rejected` render the holder/`launchError` message via `role="alert"` (409 surfaced, never silent). Consume `selectLauncherPrefill` — when set (from the Re-run button), pre-fill the spec select + snapshot_id + rerun_of + override fields (D-11). NO YAML editor / spec builder (D-09 — launches, never edits). Put `data-testid` on EVERY control (`spec-select`, `matrix-preview`, `override-repeats`, `override-timeout`, `variant-subset`, `capture-raw-bodies`, `launch-experiment`) — the Plan-06 e2e gate reads them. Keep TypeScript strict (no `any`).
  </action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "experiment-launcher" | head -5 || echo "no launcher type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "data-testid=\"spec-select\"\|data-testid=\"matrix-preview\"\|data-testid=\"launch-experiment\"\|data-testid=\"capture-raw-bodies\"" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows all four testids
    - `grep -n "cellCount\|variants × repeats\|= N cells\|cells" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows the preview reads the server-computed count (not a client-side axes recompute)
    - `grep -n "selectLauncherPrefill\|rerun_of" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows the D-11 pre-fill path
    - `grep -n "role=\"alert\"\|launchError" integrations/system-health-dashboard/src/components/performance/experiment-launcher.tsx` shows the 409 holder surfaced
    - no YAML/textarea spec editor present (`grep -ni "textarea\|yaml" experiment-launcher.tsx` returns nothing — D-09 launches-never-edits)
    - `cd integrations/system-health-dashboard && npx tsc --noEmit` reports no errors in experiment-launcher.tsx
  </acceptance_criteria>
  <done>The launcher lists specs, previews the server-resolved matrix, offers overrides + a capture_raw_bodies checkbox, surfaces a 409 holder, pre-fills from a re-run, and contains no spec editor.</done>
</task>

<task type="auto">
  <name>Task 3: run-monitor.tsx (5s poll grid + Cancel) + runs-table Re-run button — D-10/D-11</name>
  <files>integrations/system-health-dashboard/src/components/performance/run-monitor.tsx, integrations/system-health-dashboard/src/components/performance/runs-table.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/measurement-control.tsx (the 5s-poll useEffect :35-39 — copy exactly; Card/Badge usage)
    - integrations/system-health-dashboard/src/components/performance/runs-table.tsx (row-action button block :276-304; e.stopPropagation + dispatch idiom; lucide-react imports :3; the row onClick that drives the timeline :225)
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (fetchRunStatus/cancelRun/selectRunStatus/selectActiveRunId/setLauncherPrefill from Task 1)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (run-monitor.tsx + runs-table.tsx entries — cell grid, immediate Cancel D-08, Re-run guard on completed runs)
  </read_first>
  <action>
    Create `run-monitor.tsx`: a 5s poll of `fetchRunStatus(activeRunId)` copying measurement-control's `useEffect` (guard `if (!activeRunId) return`). Render the run header (`Card` + `Badge` for `overall` state, `done`/`total`, elapsed) and a variant×repeat grid (map `runStatus.cells`) with per-cell `Badge` state chips + abort/skip `reason` text — a STRAIGHT render of progress.json (D-10, no log-tail). A Cancel `<Button>` dispatches `cancelRun({ run_id, run_dir })` and acts immediately (D-08 — no confirm-after-cell). Completed cells link to the existing runs table / timeline (dispatch `setSelectedTaskId`/the runs-table row selection). `data-testid` on every control (`run-monitor`, `run-overall-state`, `cell-<variant>-<rep>`, `cancel-run`).

    In `runs-table.tsx`: add a "Re-run" `<Button>` in the same row-action `<div>` (:275 region), copying the `variant="ghost"` + `e.stopPropagation()` + dispatch idiom. `onClick` dispatches `setLauncherPrefill({ spec, snapshot_id, rerun_of: run.task_id, overrides })` to open the launcher pre-filled (D-11). Add a `lucide-react` icon (e.g. `RotateCcw`) to the :3 import line. Only render on COMPLETED experiment runs (guard on `run.terminal_state`/experiment provenance — a non-experiment or in-progress row shows no Re-run). `data-testid="rerun-experiment"`. Keep TypeScript strict (no `any`).
  </action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "run-monitor|runs-table" | head -5 || echo "no monitor/table type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "setInterval(.*5000" integrations/system-health-dashboard/src/components/performance/run-monitor.tsx` shows the 5s poll
    - `grep -n "data-testid=\"cancel-run\"\|data-testid=\"run-monitor\"" integrations/system-health-dashboard/src/components/performance/run-monitor.tsx` shows the monitor testids
    - the monitor maps `runStatus.cells` to a grid (grep `cells.map` or a per-cell Badge render)
    - `grep -n "data-testid=\"rerun-experiment\"\|RotateCcw\|setLauncherPrefill" integrations/system-health-dashboard/src/components/performance/runs-table.tsx` shows the D-11 Re-run button wired to the pre-fill
    - the Re-run button is guarded to completed experiment runs (grep a `terminal_state`/provenance conditional around it)
    - `cd integrations/system-health-dashboard && npx tsc --noEmit` reports no errors in run-monitor.tsx / runs-table.tsx
  </acceptance_criteria>
  <done>The monitor polls progress.json every 5s into a cell grid with an immediate Cancel; the runs table gains a completed-run-only Re-run button that opens the launcher pre-filled with rerun_of.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → same-origin /api/experiments | Operator form input (spec choice, overrides) submitted to the proxied API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-05-01 | Tampering | client-supplied overrides / spec | mitigate | The UI only submits a spec CHOSEN from the server-listed set + typed override values; all authoritative validation is server-side (Plan 04 V5) — the client is not a trust boundary |
| T-85-05-02 | Info disclosure | 409 holder message rendered | accept | Localhost dashboard, no auth in scope (matches existing endpoints); holder message names an in-project task_id/run_id only |
| T-85-05-03 | Tampering (XSS via progress reason text) | per-cell abort/skip reason render | mitigate | Render reason as React text content (auto-escaped), never dangerouslySetInnerHTML |
| T-85-05-SC | Tampering | npm/pip/cargo installs | mitigate | No installs (reuses shadcn/@reduxjs/toolkit already vendored) — N/A |
</threat_model>

<verification>
- `cd integrations/system-health-dashboard && npx tsc --noEmit` clean for the touched files (TS strict, no `any`)
- data-testid coverage on every launcher + monitor control (the Plan-06 e2e gate depends on them)
- Build + restart recipe deferred to Plan 06 (which does the bind-mount rebuild + live visual verification)
</verification>

<success_criteria>
- Launcher previews the server-resolved matrix, offers overrides + capture_raw_bodies, surfaces 409, pre-fills on re-run, never edits a spec
- Monitor renders the cell grid at 5s with an immediate Cancel
- Re-run button (completed runs only) opens the launcher pre-filled with rerun_of
</success_criteria>

<output>
Create `.planning/phases/85-experiment-control-center/85-05-SUMMARY.md` when done
</output>
