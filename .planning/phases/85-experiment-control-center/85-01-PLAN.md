---
phase: 85-experiment-control-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/experiments/run-progress.mjs
  - lib/experiments/experiment-runner.mjs
  - lib/experiments/run-write.mjs
  - scripts/experiment-run.mjs
  - scripts/measurement-start.mjs
  - scripts/measurement-stop.mjs
  - tests/experiments/run-progress.test.mjs
  - tests/experiments/run-write.test.mjs
  - tests/experiments/measurement-start-variant.test.mjs
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07, D-12]

must_haves:
  truths:
    - "runMatrix writes progress.json at every cell state transition (restoring/running/scoring/terminal) when a runDir is supplied"
    - "A manual CLI run (no runDir) is a no-op for the emitter — existing experiment-runner.integration.test stays green"
    - "progress.json is written atomically (tmp+rename) so a 5s poller never reads a torn file"
    - "A re-run's Run records carry rerun_of: <original run_id> and mutated variants carry base_variant"
    - "task_hash stays constant across a run_id salt (comparability preserved, D-05)"
    - "capture_raw_bodies=true flows spec/launcher → span.meta.capture_raw_bodies (D-12), default OFF"
  artifacts:
    - path: "lib/experiments/run-progress.mjs"
      provides: "atomic never-throw writeProgress(runDir, patch) + readProgress(runDir)"
      exports: ["writeProgress", "readProgress"]
    - path: "lib/experiments/experiment-runner.mjs"
      provides: "progress-emitter hooks threaded through runMatrix/runCell (optional runDir)"
      contains: "emitProgress"
    - path: "lib/experiments/run-write.mjs"
      provides: "rerun_of + base_variant null-preserved metadata tags"
      contains: "rerun_of"
  key_links:
    - from: "scripts/experiment-run.mjs"
      to: "lib/experiments/run-progress.mjs"
      via: "init progress.json + pass runDir into runMatrix"
      pattern: "writeProgress"
    - from: "scripts/measurement-stop.mjs"
      to: "lib/experiments/run-write.mjs"
      via: "buildRunTags folds rerun_of/base_variant from span.meta"
      pattern: "rerun_of"
---

<objective>
Land the non-API backend foundation for the Experiment Control Center: the atomic never-throw progress emitter (D-03/D-04), the `rerun_of`/`base_variant` Run metadata plumbing (D-05/D-07), and the new CLI flags that carry run identity, per-variant overrides, and the D-12 `capture_raw_bodies` passthrough into the measurement span.

Purpose: Every dashboard-visible run state, re-run pairing, and raw-capture toggle originates in these source-of-truth modules. The API (Plan 04) and UI (Plan 05) only read/trigger what this plan makes real.
Output: `run-progress.mjs` (NEW), progress hooks in `experiment-runner.mjs`, two new null-preserved Run tags in `run-write.mjs`, and new flags in `experiment-run.mjs` / `measurement-start.mjs` / `measurement-stop.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-experiment-control-center/85-CONTEXT.md
@.planning/phases/85-experiment-control-center/85-PATTERNS.md

<interfaces>
<!-- Contracts the executor implements. Extracted from live code this session. -->

progress.json shape (D-04) — run-level header + per-cell array:
  header keys: run_id, spec, snapshot_id, pid, done, total, overall
  overall ∈ pending | running | scoring | complete | cancelled | timeout | abort | unknown
  cells[]: { variant, rep, task_id, state, started_at, ended_at, reason }
  cell state ∈ pending | restoring | running | scoring | complete | timeout | abort | skipped

run-write.mjs writeRun metadata tag block (lib/experiments/run-write.mjs:126-129) — existing null-preserved idiom to extend:
  variant: t.variant ?? null
  repeat: t.repeat ?? null
  terminal_state: t.terminal_state ?? null
  skip_reason: t.skip_reason ?? null
  ADD: rerun_of: t.rerun_of ?? null ; base_variant: t.base_variant ?? null

measurement-stop.mjs buildRunTags (scripts/measurement-stop.mjs:153-171) — folds span.meta into tags:
  variant: span.meta?.variant ?? null
  repeat: span.meta?.repeat ?? null
  ADD: rerun_of: span.meta?.rerun_of ?? null ; base_variant: span.meta?.base_variant ?? null

runMatrix/runCell hook points (lib/experiments/experiment-runner.mjs):
  composeTaskId(expId, cell, rep) at :97 ; cellName(cell) at :84
  runCell at :346 (restore→restoring, measurement-start→running, finally measurement-stop→scoring→terminal)
  runMatrix at :481 ; doneSet built :502-507 ; cell loop :522-557
  DEFAULT_GRACE_MS=5000 at :81 (reused by Plan 03 cancel, NOT here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic never-throw progress emitter (run-progress.mjs) + runMatrix hooks</name>
  <files>lib/experiments/run-progress.mjs, lib/experiments/experiment-runner.mjs, tests/experiments/run-progress.test.mjs</files>
  <read_first>
    - lib/experiments/experiment-runner.mjs (the whole file — runMatrix :481, runCell :346, cell loop :522-557, cellName :84, composeTaskId :97; you thread an OPTIONAL runDir/onProgress through and emit at each transition WITHOUT changing existing behavior)
    - lib/experiments/run-write.mjs (the never-throw + `process.stderr.write` diagnostic idiom + null-preserved metadata style you must copy)
    - tests/experiments/experiment-runner.integration.test.mjs (this MUST stay green — the fake-seam run passes NO runDir, so the emitter must be a pure no-op in that path)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (Pattern 5: emitter threaded through runMatrix; Shared Patterns: never-throw / null-preserved)
  </read_first>
  <behavior>
    - writeProgress(runDir, patch) merges patch into progress.json via write-to-`progress.json.tmp` then fs.rename (atomic on same FS); a concurrent readProgress never observes a torn/partial JSON
    - writeProgress with a runDir whose dir does not exist → never throws; emits a `[experiment-runner] progress emit failed (non-fatal): …` line to stderr and returns
    - readProgress(runDir) returns the parsed object; ENOENT → returns null (never throws)
    - emitProgress(runDir, patch) is a no-op (returns immediately) when runDir is falsy
    - When runMatrix is called WITH a runDir: after the cell loop, progress.json header `done`/`total`/`overall` reflect the terminal counts and each cell carries a terminal `state`
    - When runMatrix is called WITHOUT a runDir (existing callers, integration test): zero progress writes, byte-identical behavior
  </behavior>
  <action>
    Create `lib/experiments/run-progress.mjs` exporting `writeProgress(runDir, patch)` and `readProgress(runDir)`, both never-throw. writeProgress reads the current progress.json (or starts from `{}`), deep-merges the patch (header keys shallow-merge; a `cells` patch upserts by `{variant, rep}` composite key — do NOT clobber the whole array), serializes with `JSON.stringify(obj, null, 2)`, writes to `path.join(runDir, 'progress.json.tmp')`, then `fs.rename` to `progress.json`. On any error use `process.stderr.write` (never `console.*` — CLAUDE.md no-console-log) and return. readProgress reads+parses progress.json, returns null on ENOENT, never-throws.

    In `experiment-runner.mjs`: add a module-local `async function emitProgress(runDir, patch)` that returns immediately when `!runDir`, else calls `writeProgress` inside try/catch with the stderr fallback (Pattern 5). Thread an OPTIONAL `runDir` through `runMatrix(spec, opts)` (read `opts.runDir`) and into `runCell({ …, runDir })`. Emit cell-state transitions: in the cell loop at doneSet-hit → `skipped`/`complete`; at copilot-probe skip → `skipped`; wrapping runCell → the restore/running/scoring/terminal states; catch → terminal `abort`. Maintain the run-level header `done`/`total`/`overall`. CRITICAL: when `runDir` is undefined, `emitProgress` short-circuits so existing callers and the integration test are unaffected — verify by leaving all existing runMatrix call sites unchanged.

    Write `tests/experiments/run-progress.test.mjs` using `node:test` + `node:assert/strict`: assert atomic write (no `.tmp` left, valid JSON after write), never-throw on a missing dir, ENOENT→null on read, cells upsert-by-key, and the no-op path (runMatrix with no runDir does not create a progress.json — use a fake/seam or assert on a tmp dir that stays empty).
  </action>
  <verify>
    <automated>node --test tests/experiments/run-progress.test.mjs tests/experiments/experiment-runner.integration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/run-progress.test.mjs` passes
    - `node --test tests/experiments/experiment-runner.integration.test.mjs` still passes (zero behavior change for no-runDir path)
    - `grep -n "console\." lib/experiments/run-progress.mjs` returns nothing (stderr-only diagnostics)
    - `grep -c "fs.rename\|renameSync" lib/experiments/run-progress.mjs` ≥ 1 (atomic write present)
    - `grep -n "if (!runDir)" lib/experiments/experiment-runner.mjs` shows the emitProgress no-op guard
  </acceptance_criteria>
  <done>run-progress.mjs exists with atomic never-throw writeProgress/readProgress; runMatrix emits per-transition progress only when a runDir is passed; the integration test is untouched-green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rerun_of / base_variant Run metadata + measurement-stop fold (D-05/D-07)</name>
  <files>lib/experiments/run-write.mjs, scripts/measurement-stop.mjs, tests/experiments/run-write.test.mjs</files>
  <read_first>
    - lib/experiments/run-write.mjs (writeRun :51, the metadata tag block :94-137, the null-preserved `?? null` idiom for variant/repeat/terminal_state/skip_reason — you add two keys the SAME way)
    - scripts/measurement-stop.mjs (buildRunTags :153-171 where variant/repeat fold from span.meta; the tags feed writeRun at :787)
    - tests/experiments/run-write.test.mjs (extend — do not rewrite; the existing null-preserved assertions are the template)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (run-write.mjs entry; Shared: null-preserved metadata — never `?? 0`, never `''`)
  </read_first>
  <behavior>
    - writeRun stamps Run.metadata.rerun_of from tags.rerun_of ?? null and Run.metadata.base_variant from tags.base_variant ?? null
    - A first (non-rerun) Run carries rerun_of: null and base_variant: null (never '' — the null-not-zero house rule)
    - buildRunTags folds rerun_of and base_variant from span.meta with the ?? null idiom, exactly like variant/repeat
    - An existing Run read after this change still surfaces rerun_of: null (no migration needed — read via ?? null on next write)
  </behavior>
  <action>
    In `run-write.mjs` add to the writeRun metadata tag block (after `skip_reason: t.skip_reason ?? null` at :129): `rerun_of: t.rerun_of ?? null` and `base_variant: t.base_variant ?? null`. Preserve the null-not-zero idiom EXACTLY — never coerce to `''` (RESEARCH anti-pattern). In `measurement-stop.mjs` buildRunTags (:170-171 region) add `rerun_of: span.meta?.rerun_of ?? null` and `base_variant: span.meta?.base_variant ?? null` folding from span.meta the same way variant/repeat do. Do NOT touch task_hash — the run_id salt (Task 3) rides only the composite task_id; task_hash stays constant for comparability (D-05).

    Extend `tests/experiments/run-write.test.mjs`: assert (a) a Run written with tags.rerun_of set surfaces metadata.rerun_of === that value; (b) a Run with no rerun_of surfaces metadata.rerun_of === null (not '' , not undefined); (c) same for base_variant.
  </action>
  <verify>
    <automated>node --test tests/experiments/run-write.test.mjs tests/experiments/measurement-stop-tags.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/run-write.test.mjs` passes including the new rerun_of/base_variant assertions
    - `grep -n "rerun_of" lib/experiments/run-write.mjs` shows `t.rerun_of ?? null` (null-preserved, not `?? ''`)
    - `grep -n "base_variant" scripts/measurement-stop.mjs` shows the span.meta fold with `?? null`
    - `grep -c "?? ''" lib/experiments/run-write.mjs` does not increase for the new keys (no empty-string coercion)
  </acceptance_criteria>
  <done>rerun_of + base_variant land on Run.metadata null-preserved; measurement-stop folds them from span.meta; existing run-write tests plus new assertions pass.</done>
</task>

<task type="auto">
  <name>Task 3: CLI flags — run identity, per-variant overrides, capture_raw_bodies (D-05/D-06/D-12)</name>
  <files>scripts/experiment-run.mjs, scripts/measurement-start.mjs, tests/experiments/measurement-start-variant.test.mjs</files>
  <read_first>
    - scripts/experiment-run.mjs (main() arg parse :93-162, parseStrArg idiom + positive-int validation :106-125, env contract :127-128, runMatrix call :162 — you add flags and init progress.json before the loop)
    - scripts/measurement-start.mjs (resolveVariantMeta → span.meta :68-101, the --variant/--repeat presence-checked fold :96-101, the shell-metacharacter guard :105-108 you must keep for new string flags)
    - lib/experiments/experiment-runner.mjs (composeTaskId :97 — the salt must stay SHORT + path-safe or Pitfall-1 slug truncation re-breaks D-10 resume)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (experiment-run.mjs + measurement-start.mjs entries; Pitfall 1 truncation)
  </read_first>
  <action>
    In `experiment-run.mjs`: add flags via the existing `parseStrArg` idiom — `--run-id`, `--run-dir`, `--rerun-of`, and per-variant `--model` / `--agent` overrides (D-06). Validate `--run-id` is short + path-safe: charset `[A-Za-z0-9._-]`, length ≤ 12 (a 6-8 char suffix is the target — Pitfall 1: a long salt blows composeTaskId's slug/path-key limit and re-breaks D-10 resume). Before the runMatrix loop, when `--run-dir` is set, initialize progress.json from `resolveExperimentSpec` cells as a `pending` grid (header run_id/spec/snapshot_id/pid=process.pid/done=0/total=N/overall='running') via `writeProgress` from Task 1. Pass `runDir` into `runMatrix(specObj, { …, runDir })`. Thread `--rerun-of` and per-variant model/agent overrides into the cell resolution so the salted composite task_id carries run_id while task_hash stays constant.

    In `measurement-start.mjs`: add `--capture-raw-bodies` (presence flag → `span.meta.capture_raw_bodies = true`; default absent → key omitted/false; D-12) and `--base-variant` (string → `span.meta.base_variant`; D-07), mirroring the `--variant`/`--repeat` presence-checked fold. Keep the shell-metacharacter guard (:105-108) for `--base-variant`. NO proxy changes — the proxy's `rawBodyCaptureEnabled(span)` strict `=== true` gate already honors `span.meta.capture_raw_bodies`.

    Extend `tests/experiments/measurement-start-variant.test.mjs`: assert `--capture-raw-bodies` sets span.meta.capture_raw_bodies === true; absence leaves it falsy/undefined; `--base-variant X` sets span.meta.base_variant === 'X'.
  </action>
  <verify>
    <automated>node --test tests/experiments/measurement-start-variant.test.mjs && node scripts/experiment-run.mjs --help 2>&1 | grep -qi "run-id\|run-dir" || node scripts/experiment-run.mjs 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/measurement-start-variant.test.mjs` passes with the new capture_raw_bodies + base_variant assertions
    - `grep -n "capture-raw-bodies\|capture_raw_bodies" scripts/measurement-start.mjs` shows the presence-flag → span.meta mapping
    - `grep -n "run-id\|run-dir\|rerun-of" scripts/experiment-run.mjs` shows all three flags parsed
    - `grep -n "run-id" scripts/experiment-run.mjs` region includes a length/charset guard (≤12, `[A-Za-z0-9._-]`) — the Pitfall-1 salt bound
    - `grep -n "shell:" scripts/experiment-run.mjs scripts/measurement-start.mjs` shows no `shell:true` (fixed-argv only)
  </acceptance_criteria>
  <done>experiment-run.mjs parses --run-id/--run-dir/--rerun-of + per-variant overrides, inits progress.json, and threads runDir into runMatrix; measurement-start.mjs threads --capture-raw-bodies + --base-variant into span.meta; run_id salt is length-bounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → runner | Operator/coordinator-supplied flags (--run-id, --spec, overrides) become filesystem paths + composite task_ids |
| span.meta → proxy gate | capture_raw_bodies flows into span meta read by the proxy's raw-body capture gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01-01 | Tampering | `--run-id` used as run-dir path segment / task_id salt | mitigate | Charset `[A-Za-z0-9._-]` + length ≤12 guard in experiment-run.mjs BEFORE the value touches any path or composeTaskId (also fixes Pitfall-1 slug truncation) |
| T-85-01-02 | Tampering | `--base-variant` string flag | mitigate | Keep the existing measurement-start shell-metacharacter guard (:105-108) on the new string flag |
| T-85-01-03 | Info disclosure | capture_raw_bodies enables full request/response capture | accept | Default OFF; opt-in only; proxy applies 27-pattern redaction (Phase-84 gold path, unchanged) — no new exposure surface here |
| T-85-01-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (RESEARCH Package Legitimacy Audit: zero packages) — N/A |
</threat_model>

<verification>
- `node --test tests/experiments/` green (full experiment suite, no regressions)
- `experiment-runner.integration.test.mjs` untouched-green (no-runDir no-op proven)
- No `console.*` in run-progress.mjs; no `shell:true` in the touched scripts
</verification>

<success_criteria>
- Progress emitter is atomic + never-throw + no-op without a runDir
- rerun_of/base_variant land null-preserved on Run.metadata and fold from span.meta
- experiment-run.mjs carries run identity + overrides + progress init; run_id salt is length-bounded (Pitfall 1)
- measurement-start.mjs threads capture_raw_bodies (D-12) + base_variant (D-07) into span.meta
</success_criteria>

<output>
Create `.planning/phases/85-experiment-control-center/85-01-SUMMARY.md` when done
</output>
