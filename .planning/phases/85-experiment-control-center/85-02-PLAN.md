---
phase: 85-experiment-control-center
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/experiments/run-launch.mjs
  - tests/experiments/run-launch.test.mjs
autonomous: true
requirements: [D-01, D-08]

must_haves:
  truths:
    - "buildRunArgv produces a fixed-argv array (no shell string) invoking node scripts/experiment-run.mjs with --spec/--run-id/--run-dir + override argv"
    - "launchRun spawns the runner detached + unref'd so a coordinator/vkb restart does NOT kill the run (orphan survives)"
    - "launchRun writes run.json {run_id, pid, spec, started_at} into the run-dir"
    - "cancelRun signals the process GROUP (process.kill(-pid, sig)) SIGTERM→SIGKILL — the agent CLI grandchild dies, no zombie"
    - "isRunAlive(pid) is a process.kill(pid, 0) liveness probe returning true/false (never throws)"
    - "cancelRun has a pid-reuse sanity guard so a stale run.json pid reused by another process is not blindly killed"
  artifacts:
    - path: "lib/experiments/run-launch.mjs"
      provides: "host-side detached spawn + process-group cancel + pid liveness (fixed-argv only)"
      exports: ["buildRunArgv", "launchRun", "cancelRun", "isRunAlive"]
  key_links:
    - from: "lib/experiments/run-launch.mjs"
      to: "scripts/experiment-run.mjs"
      via: "spawn(process.execPath, [experiment-run.mjs, ...argv], {detached:true}).unref()"
      pattern: "detached:\\s*true"
    - from: "lib/experiments/run-launch.mjs"
      to: "the runner process group"
      via: "process.kill(-pid, sig)"
      pattern: "process\\.kill\\(-"
---

<objective>
Land the host-side execution primitives the coordinator seam (Plan 03) calls: a fixed-argv detached spawn of `experiment-run.mjs` that survives a launcher restart (D-01 amended intent), a process-GROUP SIGTERM→SIGKILL cancel (D-08), and a pid-liveness probe with a reuse-sanity guard.

Purpose: These are the only genuinely net-new host mechanisms in the phase. Isolating them in one module keeps the coordinator diff (Plan 03) tiny and makes the spawn/kill logic unit-testable via injected seams — the live agent-group reap is proven in the Plan 06 gate.
Output: `lib/experiments/run-launch.mjs` (NEW) with `buildRunArgv`, `launchRun`, `cancelRun`, `isRunAlive` + unit tests using injected spawn/kill seams.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-experiment-control-center/85-CONTEXT.md
@.planning/phases/85-experiment-control-center/85-PATTERNS.md

<interfaces>
<!-- Contracts + analog signatures. Extracted from live code this session. -->

Detached spawn analog (lib/vkb-server/server-manager.js:128-138):
  spawn(command[0], command.slice(1), { detached:true, stdio, cwd }) ; writeFile(pidFile, String(child.pid)) ; child.unref()

pid-liveness analog (lib/vkb-server/server-manager.js:51-58):
  isProcessRunning(pid) { try { process.kill(pid, 0); return true } catch { return false } }

ETM detached spawn env analog (scripts/health-coordinator.js:1596-1607):
  spawn('node', [script, arg], { detached:true, stdio:'ignore', env:{...process.env, CODING_REPO}, cwd }) ; child.unref()

Process-group kill (D-08, RESEARCH Pattern 4 — negated pid REQUIRED for detached tree; nodejs #2098):
  process.kill(-pid, 'SIGTERM') ; setTimeout(() => process.kill(-pid, 'SIGKILL'), graceMs)

run-dir + run.json (RESEARCH Recommended Structure): .data/experiments/runs/<run_id>/{run.json, progress.json}
  run.json = { run_id, pid, spec, started_at }

Runner env contract the spawn must pass through (scripts/experiment-run.mjs:26-37):
  CODING_REPO, LLM_PROXY_DATA_DIR, LLM_PROXY_PORT, CODING_PROXY_ROUTE
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildRunArgv + launchRun detached spawn + run.json (D-01)</name>
  <files>lib/experiments/run-launch.mjs, tests/experiments/run-launch.test.mjs</files>
  <read_first>
    - lib/vkb-server/server-manager.js (detached spawn + pid persist :128-138; isProcessRunning :51-58 — the exact analog for spawn shape and liveness)
    - scripts/health-coordinator.js (ETM detached spawn env-passthrough shape :1596-1607 — how hostEnv is built from process.env)
    - scripts/experiment-run.mjs (the flags this argv must produce: --spec, --run-id, --run-dir, --rerun-of, per-variant overrides; env contract :26-37)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (run-launch.mjs entry; Shared: fixed-argv no-shell mandatory; Pitfall 2 negated-pid)
  </read_first>
  <behavior>
    - buildRunArgv(specPath, runId, runDir, overrides) returns a flat string[] beginning with the experiment-run.mjs script path then --spec <specPath> --run-id <runId> --run-dir <runDir> and the override flags (--rerun-of, --repeats, --timeout, --variant, --model, --agent, --capture-raw-bodies) only when present — never a single shell string, never containing a shell metacharacter it did not receive
    - launchRun({specPath, runId, runDir, overrides, env, spawnFn}) creates the run-dir, spawns process.execPath with the argv detached+unref'd, writes run.json {run_id, pid, spec, started_at}, returns {pid, runDir}
    - launchRun uses an injectable spawnFn seam (default node:child_process spawn) so the unit test asserts detached:true + unref() called + fixed-argv without spawning a real process
    - The spawned child's env is a merge of process.env + the four contract vars (CODING_REPO, LLM_PROXY_DATA_DIR, LLM_PROXY_PORT, CODING_PROXY_ROUTE) resolved from the caller's env
  </behavior>
  <action>
    Create `lib/experiments/run-launch.mjs`. `buildRunArgv(specPath, runId, runDir, overrides = {})` returns `[experimentRunScriptPath, '--spec', specPath, '--run-id', runId, '--run-dir', runDir, ...conditionalOverrideFlags]` — push each override flag+value only when defined (rerun_of→`--rerun-of`, repeats→`--repeats`, timeout→`--timeout`, variant subset→`--variant`, per-variant model→`--model`, agent→`--agent`, capture_raw_bodies===true→`--capture-raw-bodies`). Resolve the script path relative to the module (import.meta.url → ../scripts/experiment-run.mjs) or accept a repoRoot. `launchRun({ specPath, runId, runDir, overrides, env, spawnFn })`: `await fs.mkdir(runDir, { recursive: true })`; `const child = (spawnFn ?? spawn)(process.execPath, buildRunArgv(...), { cwd: repoRoot, detached: true, stdio: 'ignore', env: hostEnv })`; `child.unref()`; write run.json via atomic write (reuse the tmp+rename idiom / node:fs); return `{ pid: child.pid, runDir }`. NEVER `shell:true`, NEVER template-string argv (CLAUDE.md fixed-argv, T-78-03-01). Diagnostics via `process.stderr.write` only.

    Write `tests/experiments/run-launch.test.mjs` (node:test): inject a fake spawnFn returning `{ pid: 4242, unref: () => {…} }`; assert buildRunArgv shape (fixed array, override flags conditional), assert detached:true passed, assert unref() called, assert run.json written with pid 4242.
  </action>
  <verify>
    <automated>node --test tests/experiments/run-launch.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/run-launch.test.mjs` passes
    - `grep -n "detached: *true" lib/experiments/run-launch.mjs` present; `grep -n "unref" lib/experiments/run-launch.mjs` present
    - `grep -n "shell:" lib/experiments/run-launch.mjs` returns nothing (no shell exec)
    - `grep -n "console\." lib/experiments/run-launch.mjs` returns nothing
    - buildRunArgv output is asserted to be an Array in the test (fixed-argv, not a string)
  </acceptance_criteria>
  <done>run-launch.mjs exports buildRunArgv + launchRun; the spawn is detached+unref'd fixed-argv; run.json is written; unit test proves the seam without spawning a real process.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cancelRun process-group kill + isRunAlive + pid-reuse guard (D-08)</name>
  <files>lib/experiments/run-launch.mjs, tests/experiments/run-launch.test.mjs</files>
  <read_first>
    - lib/vkb-server/server-manager.js (isProcessRunning :51-58 — the process.kill(pid,0) liveness template)
    - lib/experiments/experiment-runner.mjs (DEFAULT_GRACE_MS=5000 :81, the SIGTERM→SIGKILL grace pattern :255-260 — reuse the escalation timing for D-08)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (Pattern 4 / Pitfall 2 — negated pid for the GROUP; RESEARCH OQ2 pid-reuse guard, OQ3 stale span)
  </read_first>
  <behavior>
    - isRunAlive(pid) returns true when process.kill(pid, 0) succeeds, false on ESRCH — never throws
    - cancelRun({pid, graceMs, killFn, runJson}) calls killFn(-pid, 'SIGTERM') then after graceMs killFn(-pid, 'SIGKILL') — the NEGATED pid (process group), so the runner + its agent CLI grandchildren are reaped (Pitfall 2)
    - cancelRun applies a pid-reuse sanity guard: if run.json carries a started_at and the pid's observed process no longer plausibly belongs to the run (guard hook), it declines to kill and returns {killed:false, reason:'pid-reuse-guard'} — accept residual small reuse risk documented (RESEARCH OQ2, matches server-manager/coordinator precedent)
    - cancelRun on an already-dead pid is a safe no-op (kill throws ESRCH → caught → {killed:false, reason:'already-gone'})
    - killFn is injectable so the unit test asserts the negated-pid escalation without signalling a real process
  </behavior>
  <action>
    Add to `lib/experiments/run-launch.mjs`: `isRunAlive(pid)` = `try { process.kill(pid, 0); return true } catch { return false }`. `cancelRun({ pid, graceMs = 5000, killFn = process.kill, runJson })`: guard against pid-reuse — if the pid is not alive → return `{ killed: false, reason: 'already-gone' }`; apply a best-effort reuse sanity check (a `pidLooksLikeRunner` hook comparing against run.json metadata such as start-time, or accept the small residual risk as the coordinator/server-manager patterns do — document which); then `killFn(-pid, 'SIGTERM')` and schedule `killFn(-pid, 'SIGKILL')` after `graceMs` (unref'd timer). Return `{ killed: true }`. Use the NEGATED pid EVERYWHERE (Pitfall 2: positive pid leaves the agent CLI grandchild burning tokens + holding the span). Note in a comment that RESEARCH OQ3 — a hard SIGKILL bypasses the runner's measurement-stop finally, leaving a stale active-measurement.json — is cleared by the coordinator/canceller (Plan 03) which owns writing the `cancelled` progress patch AND clearing the span file.

    Extend `tests/experiments/run-launch.test.mjs`: inject a killFn recording calls; assert cancelRun issues `-pid, SIGTERM` then (after fake-timer/grace) `-pid, SIGKILL`; assert isRunAlive returns false for an impossible pid; assert already-gone → {killed:false,reason:'already-gone'}.
  </action>
  <verify>
    <automated>node --test tests/experiments/run-launch.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/experiments/run-launch.test.mjs` passes including cancel + liveness cases
    - `grep -n "process.kill(-\|killFn(-" lib/experiments/run-launch.mjs` shows NEGATED-pid signalling (group kill)
    - `grep -c "process.kill(pid, 0)\|process.kill(pid,0)" lib/experiments/run-launch.mjs` ≥ 1 (liveness probe uses signal 0)
    - test asserts BOTH SIGTERM and SIGKILL are issued to `-pid` (escalation proven with the injected killFn)
    - a comment references the pid-reuse guard (OQ2) and the stale-span clearing ownership (OQ3, Plan 03)
  </acceptance_criteria>
  <done>cancelRun group-kills via negated pid with SIGTERM→SIGKILL grace + a pid-reuse/already-gone guard; isRunAlive is a never-throw signal-0 probe; unit tests prove the escalation with injected seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → host spawn | Spec path + run identity + overrides become a spawned host process argv |
| run.json pid → kill signal | A pid persisted on disk becomes a kill target after a possible launcher restart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02-01 | Tampering (command injection) | buildRunArgv / launchRun spawn | mitigate | Fixed-argv array only, no `shell:true`, no template-string interpolation (T-78-03-01); values are flags never concatenated into a command line |
| T-85-02-02 | DoS (cancel wrong / reused pid) | cancelRun target | mitigate | pid-reuse sanity guard + already-gone short-circuit before signalling; documented residual-risk acceptance matching server-manager/coordinator |
| T-85-02-03 | DoS (agent CLI escapes cancel) | detached process tree | mitigate | Process-GROUP kill (negated pid); live-cancel reap proven in Plan 06 gate (RESEARCH A3) |
| T-85-02-SC | Tampering | npm/pip/cargo installs | mitigate | No installs (Node built-ins only) — N/A |
</threat_model>

<verification>
- `node --test tests/experiments/run-launch.test.mjs` green
- No `shell:true`, no positive-pid kill, no `console.*` in run-launch.mjs
</verification>

<success_criteria>
- Detached fixed-argv spawn survives launcher restart (unref'd, run.json persisted)
- Cancel reaps the whole process group via negated pid with grace escalation
- pid liveness + reuse guard prevent killing the wrong process
</success_criteria>

<output>
Create `.planning/phases/85-experiment-control-center/85-02-SUMMARY.md` when done
</output>
