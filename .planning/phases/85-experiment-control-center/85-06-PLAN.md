---
phase: 85-experiment-control-center
plan: 06
type: execute
wave: 5
depends_on: ["85-01", "85-02", "85-03", "85-04", "85-05"]
files_modified:
  - integrations/system-health-dashboard/server.js
  - tests/e2e/performance/experiment-control.spec.ts
autonomous: false
requirements: [D-01, D-02, D-08, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "The dashboard server.js proxy forwards the four new /api/experiments subpaths (run, run-status, run-cancel, specs) — verified by curl, edited + full container restart only if needed"
    - "A real launch from the dashboard spawns the runner ON THE HOST (agent CLIs resolve), progress.json advances, and the monitor grid reflects it via 5s poll"
    - "The Cancel button reaps the whole runner process group (no zombie agent CLI) and frees the 409 slot"
    - "A Re-run from a completed run opens the launcher pre-filled and mints a Run with rerun_of set + task_hash unchanged"
    - "capture_raw_bodies checkbox ON produces raw-bodies.jsonl(.gz) for the cell; OFF produces none (D-12)"
  artifacts:
    - path: "tests/e2e/performance/experiment-control.spec.ts"
      provides: "Playwright launcher→monitor→cancel + re-run click-through against :3032"
      contains: "experiment"
  key_links:
    - from: "browser :3032"
      to: "host runner via coordinator :3034"
      via: "dashboard proxy → vkb :8080 → coordinator delegate → detached spawn"
      pattern: "run-status"
---

<objective>
Prove the whole Experiment Control Center works live end-to-end: deploy the UI (bind-mount rebuild), verify the proxy passthrough, then a human-gated click-through on `localhost:3032` — launch a real experiment, watch the monitor advance, cancel it (verify the process group is reaped and the slot frees), re-run a completed run (verify `rerun_of` + constant `task_hash`), and confirm the D-12 capture_raw_bodies toggle produces raw bodies only when ON. This closes the phase per the CLAUDE.md rule: never claim UI works from DB queries alone.

Purpose: Every prior plan is unit/fake-seam proven. The container↔host spawn boundary (Pitfall 4), the live process-group cancel reap (RESEARCH A3), and the 5s-poll visual render can only be proven live.
Output: verified proxy passthrough, a Playwright spec under tests/e2e/performance/, and an operator-approved live gate with screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-experiment-control-center/85-CONTEXT.md
@.planning/phases/85-experiment-control-center/85-PATTERNS.md
@.planning/phases/85-experiment-control-center/85-03-SUMMARY.md
@.planning/phases/85-experiment-control-center/85-04-SUMMARY.md
@.planning/phases/85-experiment-control-center/85-05-SUMMARY.md

<interfaces>
<!-- Deploy + verify contracts. From live code + CLAUDE.md this session. -->

server.js proxy (integrations/system-health-dashboard/server.js:346-363): req.path passthrough to host.docker.internal:8080/api/experiments${req.path} — likely forwards run/run-status/run-cancel/specs FOR FREE (A2/Pitfall 5). VERIFY with curl before editing.

Dashboard rebuild recipe (CLAUDE.md — VirtioFS):
  Frontend UI bundle: cd integrations/system-health-dashboard && npm run build ; docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend
  server.js edit: full `cd docker && docker-compose restart coding-services` (supervisor-only restart re-reads STALE cache — Pitfall 5)

Coordinator live restart (Plan 03 seam serves on the host daemon):
  launchctl kickstart -k gui/$(id -u)/com.coding.health-coordinator

Visual verification: gsd-browser CLI (navigate/screenshot/click/eval) against :3032 — NOT a hand-rolled node playwright script (CLAUDE.md prefer-gsd-browser). Structured spec lives at tests/e2e/performance/experiment-control.spec.ts run via `npx playwright test`.

capture_raw_bodies output (D-12): span with meta.capture_raw_bodies===true → proxy writes .data/measurements/<task_id>/raw-bodies.jsonl → gzipped to .gz at span close (measurement-stop.mjs:~431). OFF → no such file.

Pitfall 2 cancel proof: after Cancel, `pgrep -f experiment-run.mjs` AND the cell's agent CLI child are gone; .data/active-measurement.json for the run is cleared (OQ3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify proxy passthrough + deploy UI + author the Playwright spec</name>
  <files>integrations/system-health-dashboard/server.js, tests/e2e/performance/experiment-control.spec.ts</files>
  <read_first>
    - integrations/system-health-dashboard/server.js (the /api/experiments reverse proxy :346-363 — confirm req.path passthrough covers the new subpaths BEFORE editing; Pitfall 5 VirtioFS cache)
    - tests/e2e/performance/canonical-columns.spec.ts (the existing Performance-tab Playwright idiom — selectors, base URL, wait strategy to mirror)
    - .planning/phases/85-experiment-control-center/85-05-SUMMARY.md (the exact data-testid values the launcher/monitor emit — the spec asserts against them)
    - .planning/phases/85-experiment-control-center/85-PATTERNS.md (server.js VERIFY-only entry; A2/Pitfall 5)
  </read_first>
  <action>
    First VERIFY the proxy: `curl -s http://localhost:8080/api/experiments/specs` (direct vkb) and `curl -s http://localhost:3032/api/experiments/specs` (through the dashboard proxy) — if both return the spec list, the req.path passthrough works FOR FREE and server.js needs NO edit. ONLY if the proxied call 404/502s while the direct call works, add the subpath passthrough to server.js and then do a FULL `cd docker && docker-compose restart coding-services` (a supervisor-only restart re-reads the stale VirtioFS cache — Pitfall 5). Record which path (no-edit vs edit) in the summary.

    Deploy the Plan-05 UI: `cd integrations/system-health-dashboard && npm run build` then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`.

    Ensure the coordinator serves the Plan-03 seam: `launchctl kickstart -k gui/$(id -u)/com.coding.health-coordinator`, then `curl -s http://localhost:3034/...` the experiment seam (per 85-03-SUMMARY.md path) returns a sane response.

    Author `tests/e2e/performance/experiment-control.spec.ts` (Playwright, mirror canonical-columns.spec.ts): navigate to the Performance tab, assert the launcher renders (`spec-select`, `matrix-preview`, `launch-experiment`), assert the matrix preview shows a cell count after selecting a spec, assert a Re-run button appears on a completed experiment run row (`rerun-experiment`) and opens the launcher pre-filled, assert the capture_raw_bodies checkbox exists. Keep the spec resilient (it may run against a seeded/idle dashboard) — assert UI structure + testids; the true launch→cancel dynamics are the human-gated Task 2.
  </action>
  <verify>
    <automated>curl -s http://localhost:3032/api/experiments/specs | head -c 200 ; echo ; npx playwright test tests/e2e/performance/experiment-control.spec.ts --reporter=line 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s http://localhost:3032/api/experiments/specs` returns the spec list JSON (proxy passthrough confirmed) — the summary records no-edit vs edit
    - IF server.js was edited: the summary notes a full `docker-compose restart coding-services` was performed (not supervisor-only)
    - `npx playwright test tests/e2e/performance/experiment-control.spec.ts` passes (launcher structure + testids + re-run button + capture checkbox present)
    - the coordinator experiment seam responds on :3034 after kickstart
  </acceptance_criteria>
  <done>Proxy passthrough verified (edited + full-restart only if required); UI deployed; coordinator seam live; a Playwright spec asserts the launcher/monitor/re-run structure and passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live launch → monitor → cancel → re-run → capture_raw_bodies gate</name>
  <what-built>
    The full Experiment Control Center: a dashboard launcher (spec picker + matrix preview + overrides + capture_raw_bodies checkbox), a 5s-polling cell-grid monitor with an immediate Cancel, a Re-run button on completed runs, all delegating the actual host spawn/cancel to the health-coordinator :3034 seam (the container cannot spawn agent CLIs — Pitfall 4).
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3032 → Performance tab. Confirm the Experiment Launcher card renders beside Measurement Control.
    2. Pick a small spec (e.g. `smoke-cross-agent.yaml` or `compare-fizzbuzz.yaml`). Confirm the matrix preview shows `variants × repeats = N cells` BEFORE launch (D-09).
    3. (D-02) With an interactive measurement span active, click Launch — confirm it is REFUSED with a 409 message naming the holder. Stop the span.
    4. Leave capture_raw_bodies OFF. Click Launch. Confirm: a run_id appears, the monitor grid populates, and cells advance (pending→restoring→running→…) on the 5s poll. On the HOST, confirm `pgrep -f experiment-run.mjs` shows a live runner (NOT ENOENT-aborting instantly — proves the host-spawn split works).
    5. (D-08) Click Cancel. Confirm the monitor flips overall→cancelled, the in-flight cell→abort, remaining cells→pending. On the HOST, confirm `pgrep -f experiment-run.mjs` AND the cell's agent CLI child are GONE (no zombie — Pitfall 2), and `.data/active-measurement.json` for the run is cleared (the slot frees — OQ3): a fresh Launch is now accepted.
    6. (D-11) On a COMPLETED experiment run row in the runs table, click Re-run. Confirm the launcher opens pre-filled (same spec + snapshot_id, rerun_of set). Launch it and confirm the new Run carries `rerun_of: <original>` with the SAME `task_hash` (D-05 comparability) — check via the runs table / `node scripts/experiments-query...` or the Run metadata.
    7. (D-12) Re-run once with capture_raw_bodies ON. Confirm `.data/measurements/<cell task_id>/raw-bodies.jsonl` (or `.gz` after span close) EXISTS; and that a run with the box OFF produces NO such file. (No proxy change — the proxy's `=== true` gate already honors span.meta.capture_raw_bodies.)
    8. Capture gsd-browser screenshots of: the launcher with matrix preview, the running monitor grid, the post-cancel cancelled state, and the pre-filled re-run launcher.

    Use gsd-browser for all :3032 screenshots (NOT a hand-rolled node playwright script — CLAUDE.md).
  </how-to-verify>
  <resume-signal>Type "approved" if the launch/monitor/cancel/re-run/capture flow works and the process group is reaped on cancel; otherwise describe what failed (which step, which state, any zombie process or unfreed slot).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dashboard proxy → vkb → coordinator → host spawn | The full live path from a click to a host process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-06-01 | DoS (agent escapes cancel — live) | detached runner process group on macOS | mitigate | Live-cancel gate step 5 proves `pgrep` shows no runner AND no agent CLI child after Cancel (RESEARCH A3 — the one behavior only a live test can confirm) |
| T-85-06-02 | DoS (slot never frees) | active-measurement.json after live cancel | mitigate | Gate step 5 confirms a fresh Launch is accepted post-cancel (OQ3 stale-span clear worked live) |
| T-85-06-03 | Info disclosure (raw bodies leak when unintended) | capture_raw_bodies default | mitigate | Gate step 7 confirms OFF → no raw-bodies file; ON is explicit opt-in; proxy redaction unchanged |
| T-85-06-SC | Tampering | npm/pip/cargo installs | mitigate | No installs (deploy uses existing npm build) — N/A |
</threat_model>

<verification>
- `curl` proxy passthrough confirmed; UI deployed via the bind-mount recipe; coordinator seam live
- `npx playwright test tests/e2e/performance/experiment-control.spec.ts` green
- Operator-approved live launch→monitor→cancel→re-run→capture flow with screenshots; process group reaped on cancel; slot frees
</verification>

<success_criteria>
- Live launch spawns the runner on the host (not container-abort) and the monitor reflects progress at 5s
- Cancel reaps the whole process group and frees the 409 slot (live-proven)
- Re-run pairs via rerun_of with constant task_hash; capture_raw_bodies produces raw bodies only when ON
</success_criteria>

<output>
Create `.planning/phases/85-experiment-control-center/85-06-SUMMARY.md` when done (record the proxy no-edit-vs-edit outcome, the live cancel-reap evidence, and screenshot paths)
</output>
