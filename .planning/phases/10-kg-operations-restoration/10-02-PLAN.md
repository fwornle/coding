---
phase: 10-kg-operations-restoration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts
autonomous: true
requirements: [KGOP-01, KGOP-04, KGOP-05]

must_haves:
  truths:
    - "Dedup key includes parentId so same-name entities under different parents are NOT merged"
    - "Semantic dedup second pass merges entities above cosine 0.9 with same parent and same level"
    - "Edge prediction skips entity pairs in the same L1 branch (same-branch pairs already have explicit relations)"
    - "Conv operator builds BatchContext from run metadata (timestamp, git commits, sessions) not batch dates"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts"
      provides: "Hierarchy-aware dedup, cross-branch pred, run-metadata conv"
      contains: "parentId"
  key_links:
    - from: "deduplication()"
      to: "mergeEntities()"
      via: "hierarchy-scoped dedup key"
      pattern: "parentId.*normalizedName"
    - from: "edgePrediction()"
      to: "getL1Ancestor()"
      via: "cross-branch filter"
      pattern: "hierarchyPath"
---

<objective>
Fix operator logic for hierarchy-aware dedup, cross-branch edge prediction, and run-metadata conv context.

Purpose: Make dedup hierarchy-aware so same-name entities under different parents survive. Make edge prediction cross-branch only to find inter-component relationships. Adapt conv to wave mode using run metadata.
Output: Updated kg-operators.ts with all three operator fixes
</objective>

<execution_context>
@/Users/Q284340/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Q284340/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-kg-operations-restoration/10-RESEARCH.md

<interfaces>
<!-- Current dedup key (line 355-361 of kg-operators.ts -- name only, no parentId): -->
```typescript
// Current broken dedup key:
const normalizedName = entity.name.toLowerCase().replace(/[^a-z0-9]/g, '');
seen.set(normalizedName, entity);

// Current edgePrediction (line 476-511): compares entities vs accumulatedKG.entities
// No cross-branch filter -- compares ALL pairs regardless of hierarchy branch
for (const newEntity of entities) {
  for (const accEntity of accumulatedKG.entities) {
    // ... scoring
  }
}

// BatchContext interface (line 58):
export interface BatchContext {
  batchId: string;
  startDate: Date;
  endDate: Date;
  commits: Array<{ hash: string; message: string; date: Date }>;
  sessions: Array<{ filename: string; timestamp: Date }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make dedup hierarchy-aware with two-pass strategy</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts</files>
  <action>
In `deduplication()` (line ~342):

**Pass 1 -- Exact normalized name match (hierarchy-aware):**
- Change dedup key from `normalizedName` to `${entity.parentId || 'root'}::${normalizedName}`
- Apply this change in BOTH the accumulated entities loop (line ~354) and the new batch entities loop (line ~360)
- This ensures "Configuration" under "LiveLogging" is NOT merged with "Configuration" under "KnowledgeManagement"

**Pass 2 -- Semantic dedup using embedding cosine similarity:**
- After the first pass, iterate over all deduplicated entities
- For each pair with: same parentId AND same level AND both have non-empty embeddings
- Calculate cosine similarity using existing `this.cosineSimilarity()` method
- If cosine > 0.9: merge the pair using `this.mergeEntities()`, keep the one with higher significance
- Log merged pairs in the mergeLog
- This catches near-duplicates like "ConfigurationManagement" and "ConfigManagement" under same parent

Update log message to include pass 1 and pass 2 merge counts separately.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npx tsc --noEmit</automated>
  </verify>
  <done>Dedup key includes parentId. Second pass merges by cosine similarity (>0.9) with same parent+level constraint. Two-pass merge counts logged.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-branch filter to edge prediction and adapt conv for run metadata</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts</files>
  <action>
**Edge prediction cross-branch filter:**

1. Add a private method `getL1Ancestor(entity: KGEntity): string | null`:
   - If entity has no hierarchyPath, return null
   - Split hierarchyPath by '/' -- parts[0] is Project (L0), parts[1] is L1 Component
   - Return parts[1] if it exists, otherwise null

2. Modify `edgePrediction()` (line ~457):
   - The current code compares `entities` against `accumulatedKG.entities`
   - Since operators run post-pipeline with accumulatedKG = empty (full-replace mode), change the prediction to compare entities against THEMSELVES (within-run cross-branch prediction)
   - Use double loop over `entities` (not entities vs accumulatedKG)
   - Skip if entityA.id === entityB.id
   - Skip if entityA.id > entityB.id (avoid duplicate pairs)
   - Get L1 ancestor for both entities
   - If both have an L1 ancestor AND they are the same: `continue` (same branch, skip)
   - Proceed with cosine similarity + Adamic-Adar + common ancestors scoring as before
   - Build neighbor map from provided `relations` (the run's own relations, not accumulatedKG.relations which is empty)
   - Keep existing threshold and scoring logic

**Conv operator adaptation:**

3. The `contextConvolution()` method (line ~188) already accepts BatchContext and enriches entities with temporal context from commits and sessions. No changes needed to the method itself -- it already works with any BatchContext shape.
   - The caller (wave-controller, Plan 03) will construct the BatchContext with run metadata. This task just ensures the conv method works correctly with the BatchContext interface as-is.
   - Verify the method handles empty commits/sessions arrays gracefully (it should -- check and add guards if missing).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npx tsc --noEmit</automated>
  </verify>
  <done>edgePrediction() has getL1Ancestor() helper and skips same-branch pairs. Prediction compares within-run entities (not vs empty accumulatedKG). Conv handles any BatchContext shape. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with `npx tsc --noEmit`
- deduplication() uses `parentId::normalizedName` as key
- deduplication() has second pass using cosine similarity > 0.9
- edgePrediction() has getL1Ancestor() and skips same-branch pairs
- edgePrediction() compares entities within the run, not vs empty accumulatedKG
</verification>

<success_criteria>
- Same-name entities under different parents survive dedup
- Near-duplicate entities under same parent get merged by semantic similarity
- Edge prediction only creates edges between entities in different L1 branches
- Conv operator works with any valid BatchContext
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-kg-operations-restoration/10-02-SUMMARY.md`
</output>
