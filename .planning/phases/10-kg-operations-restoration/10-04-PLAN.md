---
phase: 10-kg-operations-restoration
plan: 04
type: execute
wave: 1
depends_on: ["10-03"]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts
autonomous: true
gap_closure: true
requirements: [KGOP-01, KGOP-02, KGOP-03, KGOP-04, KGOP-05, KGOP-06]

must_haves:
  truths:
    - "After operators run, re-persisted entities retain embedding arrays, role field, and enrichedContext field"
    - "After operators run, re-persisted entities retain ontology classification metadata from per-wave classification"
    - "Dashboard shows each operator step before the operator executes (not skipped due to SSE interval)"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts"
      provides: "Fixed persistWaveResult flow with operator-enriched fields and ontology preservation"
      contains: "embedding.*role.*enrichedContext"
  key_links:
    - from: "wave-controller.ts persistWaveResult()"
      to: "mapEntityToSharedMemory()"
      via: "KGEntity fields mapped to SharedMemoryEntity"
      pattern: "embedding|role|enrichedContext"
    - from: "wave-controller.ts operator section"
      to: "mapEntityToSharedMemory _ontologyMetadata"
      via: "ontology metadata map preservation"
      pattern: "_ontologyMetadata"
---

<objective>
Close 3 runtime gaps found during Phase 10 verification: (1) persistEntities() strips operator-enriched fields (embedding, role, enrichedContext), (2) ontology classification lost on operator-refined entities, (3) dashboard operator progress too fast for SSE broadcast.

Purpose: Without these fixes, operators run but their output is silently discarded during re-persistence, making KGOP-01 through KGOP-06 functionally incomplete at runtime.
Output: Updated wave-controller.ts with all 3 gaps closed.
</objective>

<execution_context>
@/Users/Q284340/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Q284340/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-kg-operations-restoration/10-03-SUMMARY.md
@.planning/phases/10-kg-operations-restoration/10-VERIFICATION.md
@integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix persistEntities field stripping and preserve ontology metadata through operators</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts</files>
  <action>
Fix Gap 1 (CRITICAL) -- persistEntities strips operator-enriched fields:

The re-persist at line 298 calls `this.persistWaveResult(refinedWaveResult)` which goes through `mapEntityToSharedMemory()` (line 782). The `mapEntityToSharedMemory()` method maps KGEntity fields to SharedMemoryEntity but does NOT carry `embedding`, `role`, or `enrichedContext`. These fields are set by operators (conv sets enrichedContext, aggr sets role, embed sets embedding) but are lost during re-persistence.

Fix `mapEntityToSharedMemory()` (line 782) to include these fields:
- Add `embedding: entity.embedding` to the returned object (after line 824, before the closing brace)
- Add `role: entity.role` to the returned object
- Add `enrichedContext: entity.enrichedContext` to the returned object

Also check the `persistEntities()` call at lines 599-611 -- it manually maps fields and does NOT include embedding/role/enrichedContext. This is the per-wave persist path (not the operator re-persist path), so those fields don't exist yet at that point. No change needed there.

ALSO fix Gap 2 (MEDIUM) -- ontology classification lost on operator-refined entities:

The operator section (lines 220-276) processes entities through 6 operators. Each operator returns fresh entity objects. The `_ontologyMetadata` field was attached to entities during `classifyWaveEntities()` (called per-wave before persist), but operator-refined entities are new objects without this field.

Before the operator section begins (around line 220, after `let currentEntities = allEntities;`), build a Map preserving ontology metadata:

```typescript
// Preserve ontology metadata from per-wave classification before operators strip it
const ontologyMap = new Map<string, any>();
for (const entity of allEntities) {
  const meta = (entity as any)._ontologyMetadata;
  if (meta) {
    ontologyMap.set(entity.name, meta);
  }
}
```

After all operators complete and before re-persist (around line 278, before the `log('[WaveController] Re-persisting...')` line), re-attach ontology metadata:

```typescript
// Re-attach ontology metadata that operators may have stripped
for (const entity of currentEntities) {
  if (!(entity as any)._ontologyMetadata && ontologyMap.has(entity.name)) {
    (entity as any)._ontologyMetadata = ontologyMap.get(entity.name);
  }
}
```

This ensures that when `persistWaveResult` calls `mapEntityToSharedMemory`, the `_ontologyMetadata` field is present and the real ontology classification (not the auto-assigned fallback) is used.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - mapEntityToSharedMemory() includes embedding, role, and enrichedContext fields in its return value
    - Ontology metadata Map built before operators, re-attached after operators complete
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Force progress file write after each operator updateProgress call</name>
  <files>integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts</files>
  <action>
Fix Gap 3 (LOW) -- dashboard operator progress too fast for SSE broadcast:

The 6 operator `updateProgress()` calls (lines 228, 235, 243, 251, 259, 267) update in-memory progress state, but the SSE server broadcasts on a timer interval. Fast operators (conv, aggr, dedup, pred) complete before the next broadcast tick, so the dashboard never shows those steps.

After each `updateProgress()` call in the operator section (the 6 calls at lines 228, 235, 243, 251, 259, 267), add a call to force-flush the progress file so the SSE broadcast picks it up on the next tick. Check what method `updateProgress` uses internally -- look for `writeProgressPreservingDetails` or similar file-write method.

If `updateProgress()` already writes to file synchronously, the fix is simpler: add a small `await new Promise(r => setTimeout(r, 50))` delay after each operator `updateProgress()` call to give the SSE broadcast interval time to pick up the state change. This is a 300ms total overhead (6 x 50ms) which is negligible.

The approach:
1. After each of the 6 `this.updateProgress(...)` calls in the operator section, add:
   ```typescript
   await new Promise(r => setTimeout(r, 50)); // Allow SSE broadcast to pick up operator step
   ```

This ensures each operator step appears on the dashboard briefly before the operator runs, giving visual confirmation of progress through the operator pipeline.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Each operator updateProgress() call is followed by a 50ms delay
    - TypeScript compiles without errors
    - Total overhead is ~300ms (negligible vs pipeline runtime)
  </done>
</task>

</tasks>

<verification>
After both tasks, verify:
1. `npx tsc --noEmit` passes in the submodule
2. Grep for `embedding` in `mapEntityToSharedMemory` confirms the field is mapped
3. Grep for `ontologyMap` confirms the preservation logic exists
4. Grep for `setTimeout.*50` in the operator section confirms SSE flush delays

Full runtime verification requires Docker rebuild + `ukb full` (human verification from 10-VERIFICATION.md).
</verification>

<success_criteria>
- mapEntityToSharedMemory returns embedding, role, and enrichedContext from KGEntity
- Ontology metadata preserved through operator pipeline via name-keyed Map
- Dashboard shows all 6 operator steps (not just embed and merge)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-kg-operations-restoration/10-04-SUMMARY.md`
</output>
