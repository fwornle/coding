---
phase: 71-experiment-kb-task-taxonomy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/experiments/token-aggregate.mjs
  - tests/experiments/token-aggregate.test.mjs
autonomous: true
requirements: [KB-02]
user_setup: []

must_haves:
  truths:
    - "aggregateByTaskId(taskId) reads the proxy-owned token-usage.db READ-ONLY and returns per-task token totals + a per-(agent,model,provider,tier) breakdown."
    - "The aggregation is a pure recompute (WHERE task_id=?), so re-running after a timestamp-join backfill recomputes COMPLETE totals (D-14 self-healing)."
    - "The DB is opened {readonly:true} — coding never becomes a second writer to the proxy-owned DB."
  artifacts:
    - path: "lib/experiments/token-aggregate.mjs"
      provides: "aggregateByTaskId(taskId) → { totals, byAgentModel } via better-sqlite3 readonly parameterized query"
      contains: "readonly: true"
    - path: "tests/experiments/token-aggregate.test.mjs"
      provides: "KB-02 proof: read-only open + parameterized WHERE + recompute correctness against a seeded temp DB"
      exports: []
  key_links:
    - from: "lib/experiments/token-aggregate.mjs"
      to: ".data/llm-proxy/token-usage.db"
      via: "better-sqlite3 readonly, WHERE task_id = ?"
      pattern: "task_id = \\?"
---

<objective>
Build the thin read-only token aggregator (KB-02) over the proxy-owned `token-usage.db`. A pure `WHERE task_id=?` recompute that the Run-write (71-04) and a future `experiments refresh` call to materialize/self-heal a Run's token totals (D-14). The proxy stays the sole writer (Phase 70 principle); coding opens `{readonly:true}`.

Purpose: Satisfy the token-sourcing half of KB-02 (SC-2 "totals sourced from token_usage, not invented") and the D-14 refreshable-totals contract. The dominant `byAgentModel` row also sources the Run's `agent`/`model` tags.

Output: `lib/experiments/token-aggregate.mjs`, `tests/experiments/token-aggregate.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-CONTEXT.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md

<interfaces>
<!-- token_usage attribution columns the aggregation reads (token-usage.ts:83-128): -->
task_id, agent, model, provider, input_tokens, output_tokens, total_tokens,
reasoning_tokens, granularity_tier
<!-- Return shape (RESEARCH §"token_usage read-only aggregation"): -->
{ totals: { input_tokens, output_tokens, total_tokens, reasoning_tokens, calls },
  byAgentModel: [ { agent, model, provider, granularity_tier, total_tokens, calls } ] }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write lib/experiments/token-aggregate.mjs + tests/experiments/token-aggregate.test.mjs</name>
  <behavior>
    - aggregateByTaskId('t1') against a seeded temp DB with 3 rows for t1 → totals.total_tokens === sum of those 3 rows; totals.calls === 3.
    - byAgentModel groups by (agent,model,provider,granularity_tier) ordered by total_tokens DESC; the first row is the dominant agent/model.
    - A task_id with zero rows → totals all 0 (COALESCE), byAgentModel === [] (graceful, no throw).
    - Re-running after inserting 2 MORE rows for t1 (simulating a backfill re-attribution) recomputes the new, higher total (D-14 self-healing recompute).
    - The DB connection is opened with readonly:true (a write attempt would throw — assert by opening readonly and confirming a prepared INSERT throws, OR by asserting the open option).
  </behavior>
  <read_first>
    - scripts/backfill-task-id-by-timestamp.mjs (lines 38-45 createRequire+better-sqlite3 import; 56-64 resolveDataDir/resolveDbPath; 131-147 parameterized bound-param queries; 195 readonly open; 213-289 node:test self-test + temp-DB fixture construction) — THE analog, copy these patterns
    - _work/rapid-llm-proxy/src/token-usage.ts (lines 83-128 attribution columns; 384-405 schema) — the column names to SELECT/SUM
    - .planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md §"token_usage read-only aggregation (Q4)" (the full aggregateByTaskId skeleton — use it) + §"Validation Architecture" KB-02-idemp test-map row
    - .planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md §"lib/experiments/token-aggregate.mjs" (LLM_PROXY_DATA_DIR env var precedence; ALWAYS readonly:true)
  </read_first>
  <action>
    Create `lib/experiments/token-aggregate.mjs` exporting `aggregateByTaskId(taskId, dbPathOverride?)`. Use `createRequire(import.meta.url)` + `require('better-sqlite3')`. Resolve the db path from `process.env.LLM_PROXY_DATA_DIR` (fallback `/Users/Q284340/Agentic/coding/.data`) + `/llm-proxy/token-usage.db`, allowing a `dbPathOverride` (for tests). Open `new Database(dbPath, { readonly: true, fileMustExist: true })` — the literal `readonly: true` MUST appear (never a second writer — Security V5/D-14 guardrail). Two prepared statements, BOTH using bound `?` params (NEVER string-interpolate task_id — SQL-injection mitigation): (1) totals = `SELECT COALESCE(SUM(input_tokens),0) AS input_tokens, COALESCE(SUM(output_tokens),0) AS output_tokens, COALESCE(SUM(total_tokens),0) AS total_tokens, COALESCE(SUM(reasoning_tokens),0) AS reasoning_tokens, COUNT(*) AS calls FROM token_usage WHERE task_id = ?`; (2) byAgentModel = `SELECT agent, model, provider, granularity_tier, SUM(total_tokens) AS total_tokens, COUNT(*) AS calls FROM token_usage WHERE task_id = ? GROUP BY agent, model, provider, granularity_tier ORDER BY total_tokens DESC`. Return `{ totals, byAgentModel }`. `db.close()` in a finally. If the DB file is absent, handle gracefully (return zero totals + empty array) rather than throwing — the close path must survive a missing DB (Environment Availability fallback). Then create `tests/experiments/token-aggregate.test.mjs` (node:test, temp-DB fixture mirroring the backfill self-test: CREATE TABLE token_usage with the attribution columns, INSERT seed rows, point aggregateByTaskId at the temp DB via the override) covering the behavior cases. Gate any live assertion on `EXPERIMENTS_LIVE` env var (never `--live` argv — MEMORY.md gotcha). No console.log.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; node --test tests/experiments/token-aggregate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Test: `node --test tests/experiments/token-aggregate.test.mjs` exits 0; recompute-after-backfill case asserts the higher total.
    - Source: the literal `readonly: true` appears in the Database open (`grep -c "readonly: true" lib/experiments/token-aggregate.mjs` ≥ 1).
    - Source: both queries use bound `?` params — `grep -c "WHERE task_id = ?" lib/experiments/token-aggregate.mjs` ≥ 1; NO string interpolation of taskId into SQL.
    - Behavior: zero-rows task_id returns all-zero totals + empty byAgentModel (no throw); missing DB file handled gracefully.
    - Source: zero `console.*`; `db.close()` in finally.
  </acceptance_criteria>
  <done>token-aggregate.mjs exports aggregateByTaskId (readonly, parameterized, pure recompute); token-aggregate.test.mjs passes proving read-only + correct + self-healing recompute (KB-02 token-sourcing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coding CLI → proxy-owned token-usage.db | Read-only cross-process access to a DB owned and written solely by the rapid-llm-proxy. |
| task_id param → SQL WHERE clause | task_id (already proxy-sanitized to `[A-Za-z0-9._-]`) bound into a parameterized query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03-01 | Tampering | SQL injection via task_id in aggregation | mitigate | Parameterized `WHERE task_id = ?` bound params only — never interpolate (Security V5; backfill precedent). Task asserts the bound-param grep. |
| T-71-03-02 | Tampering/DoS | second writer corrupting token-usage.db | mitigate | Open `{ readonly: true }` — coding never writes the proxy DB (Phase 70 sole-writer principle). Asserted by the readonly grep + behavior test. |
| T-71-03-03 | Tampering | npm installs | mitigate | No new package installs (better-sqlite3 already transitively present — RESEARCH Package Legitimacy Audit). slopcheck N/A. |
</threat_model>

<verification>
- `grep -c "readonly: true"` and `grep -c "WHERE task_id = ?"` both ≥ 1 (read-only + parameterized invariants).
- `node --test tests/experiments/token-aggregate.test.mjs` proves correct totals, grouping, zero-row graceful path, and recompute-after-backfill self-healing.
</verification>

<success_criteria>
- KB-02 token-sourcing half satisfied: Run token totals come from a real read-only recompute over token_usage, not invented values; the recompute self-heals against late-attributed orphan rows (D-14).
- The proxy remains the sole writer to token-usage.db.
</success_criteria>

<output>
Create `.planning/phases/71-experiment-kb-task-taxonomy/71-03-SUMMARY.md` when done.
</output>
