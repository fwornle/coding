---
phase: 71-experiment-kb-task-taxonomy
plan: 04
type: execute
wave: 2
depends_on: ["71-01", "71-03"]
files_modified:
  - lib/experiments/run-write.mjs
  - tests/experiments/run-write.test.mjs
autonomous: true
requirements: [KB-02]
user_setup: []

must_haves:
  truths:
    - "writeRun() materializes a Run entity in the dedicated experiment store with all 8 tags always present (D-13), keyed idempotently by metadata.task_id."
    - "Re-closing the same span UPDATES the same Run node (never duplicates) — idempotency via iterate({entityType:'Run'}) metadata.task_id scan + mintEntityId once."
    - "writeRun also writes a basic Outcome stub (token totals + closedState) and a Run--produces-->Outcome relation (D-12); Route/Step/Decision/Report stay schema-only."
  artifacts:
    - path: "lib/experiments/run-write.mjs"
      provides: "writeRun(store, {span, taskClass, pending, tags, totals}) → idempotent Run + Outcome stub"
      contains: "mintEntityId"
    - path: "tests/experiments/run-write.test.mjs"
      provides: "SC-2 proof: 8 tags + idempotent re-write + Outcome stub against a temp store"
      exports: []
  key_links:
    - from: "lib/experiments/run-write.mjs"
      to: "openExperimentStore() (71-01)"
      via: "store.putEntity / store.iterate / store.addRelation"
      pattern: "putEntity"
    - from: "writeRun"
      to: "task_id"
      via: "metadata.task_id idempotency key (NOT the entity id)"
      pattern: "metadata"
---

<objective>
Build the idempotent Run-write path (KB-02): materialize each measurement span as an independent, queryable km-core Run entity with all 8 tags, plus a basic Outcome stub (D-12). The write is keyed on `metadata.task_id` (NOT the km-core entity id — task_id is not a valid UUIDv7), so a re-close updates the same node rather than duplicating (D-14).

Purpose: Satisfy the materialization half of KB-02 (SC-2): a real Run with all 8 tags sourced from the span + token aggregation, idempotent and self-healing. Consumes `openExperimentStore()` (71-01) and `aggregateByTaskId()` (71-03).

Output: `lib/experiments/run-write.mjs`, `tests/experiments/run-write.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-CONTEXT.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-01-SUMMARY.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-03-SUMMARY.md

<interfaces>
<!-- From 71-01: openExperimentStore() — async, returns an open GraphKMStore with ontologyDir set. -->
import { openExperimentStore } from '../../lib/experiments/store.mjs';
<!-- From 71-03: aggregateByTaskId(taskId) → { totals, byAgentModel } (totals feed Outcome; byAgentModel[0] feeds agent/model tags). -->
<!-- km-core surface (GraphKMStore.d.ts:165-185 putEntity strict path needs provenance; entity.d.ts:9-14 ProvenanceStamp): -->
import { mintEntityId } from '@fwornle/km-core';
store.iterate({ entityType: 'Run' })            // async iterator for idempotency scan
store.putEntity(entity, { provenance })          // strict path — validates entityType against the registry
store.addRelation({ type, from, to })
<!-- Span shape (.data/measurements/<task_id>.json — telem-live-68.json): { task_id, started_at, ended_at, goal_sentence? } -->
<!-- 8 tags (D-13, ALWAYS present): task_hash, task_class, agent, model, framework, spec_level(null), snapshot_id(null), trace_id -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write lib/experiments/run-write.mjs — idempotent writeRun() + Outcome stub</name>
  <behavior>
    - First writeRun for span {task_id:'t1'} mints a UUIDv7 id (via mintEntityId), writes a Run with metadata.task_id==='t1' and all 8 tags present (spec_level/snapshot_id null).
    - The Run entityType validates against the registry (strict-path putEntity with a provenance stamp); using task_id as the entity id would throw — assert mintEntityId is used, not task_id.
    - Re-running writeRun for the same task_id finds the existing Run via iterate({entityType:'Run'}) metadata.task_id scan and UPDATES it (same id) — exactly 1 Run node after two calls (no duplicate).
    - writeRun also writes an Outcome entity (token totals + closedState) and a Run--produces-->Outcome relation.
    - When pending===true, Run.metadata.pending===true and Outcome.closedState==='quarantined'; otherwise pending false + closedState 'closed'.
  </behavior>
  <read_first>
    - scripts/backfill-project-tag.mjs (putEntity/iterate usage against a GraphKMStore; provenance/skipOntologyCheck handling)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md §"Pattern 2: Idempotent Run write" + §"Run + Outcome stub write with relation (Q8 / D-12)" (the full skeletons — use them) + §"Common Pitfalls" Pitfall 1 (task_id is NOT a valid id) + Pitfall 4 (recompute self-heal)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md §"lib/experiments/run-write.mjs" (key contract points: mint+metadata.task_id, strict-path provenance, 8 tags, Outcome+relation)
    - node_modules/@fwornle/km-core/dist/store/GraphKMStore.d.ts (lines 165-185 putEntity signature/provenance; iterate; addRelation) and dist/types/entity.d.ts (lines 9-14 ProvenanceStamp shape) — confirm the EXACT provenance fields and putEntity option key before writing
    - .planning/phases/71-experiment-kb-task-taxonomy/71-01-SUMMARY.md (openExperimentStore signature) + 71-03-SUMMARY.md (aggregateByTaskId return shape)
  </read_first>
  <action>
    Create `lib/experiments/run-write.mjs` exporting `async function writeRun(store, { span, taskClass, pending, tags, totals })`. (1) Idempotency: `for await (const e of store.iterate({ entityType: 'Run' }))` find the one whose `e.metadata?.task_id === span.task_id`; capture its `id` if found, else `const id = mintEntityId()`. NEVER use `span.task_id` as the entity `id` (parseEntityId requires UUIDv7 — Pitfall 1). (2) Build a synthetic `provenance` stamp `{ provider:'coding-measure-stop', model:'n/a', runId:span.task_id, timestamp: new Date().toISOString() }` (the store never invents one — strict path). (3) `putEntity` the Run: `entityType:'Run'`, `layer:'evidence'`, `name: span.task_id`, `description: span.goal_sentence ?? ''`, and `metadata` carrying `task_id` (idempotency key) PLUS ALL 8 tags ALWAYS (D-13): `task_hash, task_class (= taskClass, may be 'unclassified'), agent, model, framework, spec_level: null, snapshot_id: null, trace_id`, plus `pending: pending===true`, `started_at`, `ended_at`. Pass `{ provenance }` as the second putEntity arg. On re-write include the existing `id`. (4) Mint a second id and `putEntity` an Outcome stub (`entityType:'Outcome'`, metadata `totalTokens/inputTokens/outputTokens/reasoningTokens` from `totals`, `closedState: pending ? 'quarantined' : 'closed'`). (5) `addRelation({ type:'produces', from: runId, to: outcomeId })`. Return the run id. Do NOT populate Route/Step/Decision/Report (schema-only — D-12). No console.log (process.stderr.write only). Then create `tests/experiments/run-write.test.mjs` (node:test) using a TEMP store: copy `.data/ontologies-experiment/` into a tmp ontologyDir, set CODING_REPO (or pass a temp-store factory) so `openExperimentStore()` uses a throwaway leveldb, run writeRun twice with the same span, assert exactly 1 Run + 8 tags + Outcome + relation + idempotency. Gate any live assertion on `EXPERIMENTS_LIVE` env var.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; node --test tests/experiments/run-write.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Test: `node --test tests/experiments/run-write.test.mjs` exits 0.
    - Behavior: after two writeRun calls for the same task_id, exactly ONE Run node exists with that metadata.task_id (idempotent — Pitfall 1/D-14).
    - Behavior: the written Run carries ALL 8 tags in metadata, with `spec_level` and `snapshot_id` explicitly null (D-13).
    - Source: `mintEntityId` is imported and used for the entity id; `span.task_id` is NEVER assigned to `id` (grep: no `id: span.task_id` / `id: *.task_id`).
    - Behavior: an Outcome entity + a `produces` relation from Run to Outcome are written; closedState reflects pending.
    - Source: putEntity uses the strict path with a `provenance` stamp (the second arg), NOT `skipOntologyCheck` (so entityType:'Run' is validated against the registry — KB-01 enforcement preserved).
  </acceptance_criteria>
  <done>run-write.mjs exports an idempotent writeRun writing a Run (8 tags) + Outcome stub + produces relation via strict-path putEntity; run-write.test.mjs passes proving SC-2 materialization + idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close orchestrator → experiment GraphKMStore | Run/Outcome entity writes via strict-path putEntity (entityType validated against the registry). |
| span.task_id → entity metadata + name | proxy-sanitized task_id stored as the idempotency key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04-01 | Spoofing/Integrity | unvalidated entityType bypassing the ontology | mitigate | Strict-path putEntity with a provenance stamp validates `entityType:'Run'` against the registry (NOT skipOntologyCheck). Task asserts strict path + provenance. |
| T-71-04-02 | Integrity | duplicate Runs on re-close polluting comparisons | mitigate | Idempotent metadata.task_id scan + mint-once-or-update; Task asserts exactly 1 node after two writes (D-14). |
| T-71-04-03 | Tampering | npm installs | mitigate | No new package installs (km-core already present — RESEARCH Package Legitimacy Audit). slopcheck N/A. |
</threat_model>

<verification>
- `node --test tests/experiments/run-write.test.mjs` proves: 8 tags always present, exactly-1-node idempotency on re-close, Outcome stub + produces relation, strict-path provenance write.
- Source grep confirms mintEntityId used (not task_id as id) and no skipOntologyCheck bypass.
</verification>

<success_criteria>
- KB-02 materialization half satisfied: a span close materializes a queryable Run with all 8 tags sourced from span + token aggregation, idempotent and self-healing (D-13/D-14).
- D-12 population depth honored: Run + Outcome stub only; Route/Step/Decision/Report remain schema-only.
</success_criteria>

<output>
Create `.planning/phases/71-experiment-kb-task-taxonomy/71-04-SUMMARY.md` when done.
</output>
