---
phase: 71-experiment-kb-task-taxonomy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/task-taxonomy.yaml
  - lib/experiments/taxonomy.mjs
  - tests/experiments/taxonomy.test.mjs
autonomous: true
requirements: [KB-03]
user_setup: []

must_haves:
  truths:
    - "A task taxonomy v0 of exactly 6 classes (refactor, bugfix, new-feature, migration, debug, docs) exists, each with a non-empty definition."
    - "isValidClass() accepts the closed-6 and rejects any free string (the write-path enforcement primitive for SC-4)."
    - "deriveClassFromText() is a zero-LLM deterministic keyword scorer that maps phase/goal verbs to a class (e.g. 'migrate the db' → migration)."
  artifacts:
    - path: "config/task-taxonomy.yaml"
      provides: "Single source of truth for the closed-6 taxonomy (D-10): id + definition + disambiguation keywords per class"
      contains: "new-feature"
    - path: "lib/experiments/taxonomy.mjs"
      provides: "loadTaxonomy() + isValidClass() + deriveClassFromText() (D-09 enforcement primitive + D-11 heuristic)"
      contains: "deriveClassFromText"
    - path: "tests/experiments/taxonomy.test.mjs"
      provides: "SC-3 proof: 6 defs load + verb→class map + isValidClass rejects free strings"
      exports: []
  key_links:
    - from: "lib/experiments/taxonomy.mjs"
      to: "config/task-taxonomy.yaml"
      via: "loadTaxonomy() YAML parse"
      pattern: "task-taxonomy"
    - from: "deriveClassFromText"
      to: "taxonomy.classes[*].keywords"
      via: "keyword scoring"
      pattern: "keywords"
---

<objective>
Author the curated task taxonomy v0 (KB-03) as a YAML single-source-of-truth config plus the pure, unit-testable taxonomy module that both the write-path validator (D-09 closed-6 enforcement) and the /gsd derivation heuristic (D-11 zero-LLM verb→class) read from.

Purpose: Satisfy KB-03 (SC-3) and provide the two enforcement/derivation primitives — `isValidClass()` and `deriveClassFromText()` — that the close orchestrator (71-05) and the classify CLI (71-05) consume. Decoupled from the ontology (D-10).

Output: `config/task-taxonomy.yaml`, `lib/experiments/taxonomy.mjs`, `tests/experiments/taxonomy.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-CONTEXT.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author config/task-taxonomy.yaml (closed-6 single source of truth)</name>
  <read_first>
    - config/llm-providers.yaml (the established repo YAML-config format precedent — confirms config/*.yaml convention)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md §"task-taxonomy.yaml (Q6 / D-10 / KB-03)" (the FULL 6-class file — copy it)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md §"config/task-taxonomy.yaml" (closed-6 enum; NO `unclassified` here — that is the quarantine sentinel, not a taxonomy class)
  </read_first>
  <action>
    Create `config/task-taxonomy.yaml` with `version: 0` and a `classes:` map of EXACTLY the closed-6 (D-09): `refactor`, `bugfix`, `new-feature`, `migration`, `debug`, `docs`. Each class carries a `definition:` (human-readable, non-empty) and a `keywords:` list (disambiguation hints driving the D-11 heuristic). Use the RESEARCH §"task-taxonomy.yaml" content verbatim as the starting set. Do NOT include `unclassified` (that is the quarantine sentinel applied at write-time by the orchestrator, not a member of the curated taxonomy). Do NOT add a 7th class — expansion to v1 is a deliberate versioned change (D-09).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; node -e "const fs=require('fs');const yaml=require('js-yaml');const t=yaml.load(fs.readFileSync('config/task-taxonomy.yaml','utf8'));const k=Object.keys(t.classes).sort();const want=['bugfix','debug','docs','migration','new-feature','refactor'];if(JSON.stringify(k)!==JSON.stringify(want)){process.stderr.write('classes mismatch: '+k+'\n');process.exit(1)}for(const c of k){if(!t.classes[c].definition||!t.classes[c].definition.trim()){process.stderr.write('empty def '+c+'\n');process.exit(1)}}process.stdout.write('ok 6 classes with defs\n')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `classes` has EXACTLY the 6 keys {refactor,bugfix,new-feature,migration,debug,docs} — no more, no fewer, and NO `unclassified`.
    - Behavior: each class has a non-empty `definition` string and a non-empty `keywords` list.
    - Source: top-level `version: 0`.
  </acceptance_criteria>
  <done>task-taxonomy.yaml parses; has exactly the 6 classes each with a non-empty definition; verify prints "ok 6 classes with defs".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write lib/experiments/taxonomy.mjs + tests/experiments/taxonomy.test.mjs</name>
  <behavior>
    - loadTaxonomy(): parses config/task-taxonomy.yaml; returns { version, classes }.
    - isValidClass('refactor') === true; isValidClass('unclassified') === false; isValidClass('frobnicate') === false (free string rejected — SC-4 primitive).
    - deriveClassFromText('migrate the database to postgres', tax) → { taskClass: 'migration', confident: true }.
    - deriveClassFromText('fix the broken login bug', tax) → { taskClass: 'bugfix', confident: true }.
    - deriveClassFromText('add a new export button', tax) → { taskClass: 'new-feature' } (hyphenated keyword 'new-feature' matched as substring + 'add'/'new' tokens).
    - deriveClassFromText('xyzzy plugh', tax) → { taskClass: null, confident: false } (no keyword hit).
  </behavior>
  <read_first>
    - config/task-taxonomy.yaml (the config under test — written in Task 1)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md §"verb→class heuristic (Q5 / D-11, zero-LLM)" (the scorer skeleton — use directly) + §"task-taxonomy.yaml" (the keyword lists)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md §"lib/experiments/taxonomy.mjs" (exports: loadTaxonomy / isValidClass / deriveClassFromText; hyphenated keywords matched as substrings, bare keywords on tokenized set)
    - scripts/backfill-task-id-by-timestamp.mjs (lines 213-289 — node:test self-test + import-guard pattern for the test file)
  </read_first>
  <action>
    Create `lib/experiments/taxonomy.mjs` exporting three pure functions. `loadTaxonomy(filePath?)`: read `config/task-taxonomy.yaml` (default repo path), parse with `js-yaml` (or `yaml` — both installed), return `{ version, classes }`. `isValidClass(cls, taxonomy?)`: return true ONLY for one of the closed-6 keys — reject `unclassified` and any free string (this is the D-09/SC-4 write-path enforcement primitive). `deriveClassFromText(text, taxonomy)`: zero-LLM keyword scorer per the RESEARCH skeleton — lowercase + tokenize on word boundaries; hyphenated/multiword keywords matched as substrings (score +2), bare keywords matched against the token set (score +1); return `{ taskClass: <best>, confident: bestScore >= 1 }` or `{ taskClass: null, confident: false }` when no keyword hits. No LLM, no network. Output only via `process.stderr.write` if anything (no console.log). Then create `tests/experiments/taxonomy.test.mjs` (node:test + node:assert/strict, import-guarded) covering every case in the behavior block above. Any live gate uses `EXPERIMENTS_LIVE` env var, not `--live` argv.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; node --test tests/experiments/taxonomy.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Test: `node --test tests/experiments/taxonomy.test.mjs` exits 0; all behavior-block cases assert.
    - Behavior: `isValidClass('unclassified')` and `isValidClass('anything-else')` both false; the closed-6 all true.
    - Behavior: deriveClassFromText resolves 'migrate'→migration, 'fix...bug'→bugfix, 'add new'→new-feature; gibberish → null/not-confident.
    - Source: zero `console.*`; no LLM/network calls; functions are pure (deterministic given the same input).
  </acceptance_criteria>
  <done>taxonomy.mjs exports loadTaxonomy/isValidClass/deriveClassFromText; taxonomy.test.mjs passes proving SC-3 (6 defs + verb→class + free-string rejection).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/derive input → isValidClass | task_class string (free-string at classify time, or heuristic-derived) validated against the closed-6 enum before any write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02-01 | Tampering/Integrity | free-string task_class polluting WHERE-clause partitions | mitigate | `isValidClass()` is a strict closed-6 allowlist (D-09); free strings and `unclassified` rejected; Task 2 asserts rejection. The orchestrator/classify CLI route unknowns to quarantine, never write a free string. |
| T-71-02-02 | Tampering | npm installs | mitigate | No new package installs (yaml/js-yaml already present — RESEARCH Package Legitimacy Audit). slopcheck N/A. |
</threat_model>

<verification>
- `node -e` taxonomy parse check (Task 1): exactly 6 classes, each with a non-empty definition.
- `node --test tests/experiments/taxonomy.test.mjs` proves SC-3: closed-6 defs load, isValidClass rejects free strings, deriveClassFromText maps verbs deterministically.
</verification>

<success_criteria>
- KB-03 / SC-3 satisfied: a 6-class task taxonomy v0 with definitions exists in a single config source of truth.
- The closed-6 enforcement primitive (isValidClass) and the zero-LLM derivation heuristic (deriveClassFromText) are unit-proven, ready for the close orchestrator and classify CLI to consume.
</success_criteria>

<output>
Create `.planning/phases/71-experiment-kb-task-taxonomy/71-02-SUMMARY.md` when done.
</output>
