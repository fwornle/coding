---
phase: 71-experiment-kb-task-taxonomy
plan: 05
type: execute
wave: 2
depends_on: ["71-02", "71-04"]
files_modified:
  - scripts/measurement-stop.mjs
  - scripts/experiments-query.mjs
  - scripts/experiments-classify.mjs
  - tests/experiments/enforcement.test.mjs
autonomous: false
requirements: [KB-02, KB-03]
user_setup: []

must_haves:
  truths:
    - "`node scripts/measurement-stop.mjs` closes the span (stopMeasurement), derives/prompts a task_class, enforces the closed-6, and writes a Run — a real close lands a queryable Run with the enforced task_class + correct token totals."
    - "A headless close with no confidently-derived class writes the Run with task_class='unclassified' + pending:true (quarantine) and NEVER hard-blocks the close (D-06)."
    - "`experiments query` EXCLUDES pending Runs from all results (D-06); `experiments classify` lists pending Runs, assigns a closed-6 class, flips pending→false (re-includes), and surfaces a pending count (D-08)."
  artifacts:
    - path: "scripts/measurement-stop.mjs"
      provides: "Extended close orchestrator: stopMeasurement → derive/prompt → enforce → aggregate → writeRun (D-07)"
      contains: "writeRun"
    - path: "scripts/experiments-query.mjs"
      provides: "Read CLI over the experiment store (runs-by-task_class, agent-vs-model cost); excludes pending (D-03/D-06)"
      contains: "ontologyDir"
    - path: "scripts/experiments-classify.mjs"
      provides: "Quarantine resolver CLI: list pending → assign → re-include + pending count (D-08)"
      contains: "ontologyDir"
    - path: "tests/experiments/enforcement.test.mjs"
      provides: "SC-4 proof: free-string rejected; headless→quarantine; query excludes pending; classify re-includes"
      exports: []
  key_links:
    - from: "scripts/measurement-stop.mjs"
      to: "stopMeasurement (LOCAL proxy dist)"
      via: "LLM_PROXY_DIST_DIR import of measurement-span.js"
      pattern: "stopMeasurement"
    - from: "scripts/measurement-stop.mjs"
      to: "writeRun / aggregateByTaskId / deriveClassFromText"
      via: "lib/experiments/* imports"
      pattern: "lib/experiments"
    - from: "scripts/experiments-query.mjs"
      to: "openExperimentStore()"
      via: "shared store factory (ontologyDir grep)"
      pattern: "openExperimentStore"
---

<objective>
Wire the coding-side close orchestrator (D-07) and ship the two read/resolve CLIs (D-03/D-08) that make comparisons-as-queries real and enforce the task_class tag at run-end (SC-4). The orchestrator extends the EXISTING `scripts/measurement-stop.mjs` (which already calls the proxy's unwired `stopMeasurement()`) to derive/prompt → enforce closed-6 → aggregate tokens → write a Run. Enforcement is quarantine-not-hard-block (D-06).

Purpose: Satisfy the orchestration + queryability + enforcement of KB-02/KB-03 (SC-2, SC-4): a real close lands a queryable Run with an enforced task_class + correct totals; a headless/no-class close lands in quarantine and is excluded from queries; the classify CLI resolves the backlog. Consumes 71-02 (taxonomy) + 71-04 (writeRun) + 71-01 (store) + 71-03 (aggregate).

Output: extended `scripts/measurement-stop.mjs`, `scripts/experiments-query.mjs`, `scripts/experiments-classify.mjs`, `tests/experiments/enforcement.test.mjs`. Plus a live blocking human-verify and a decision checkpoint on the /gsd run-end auto-invoke hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-CONTEXT.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-02-SUMMARY.md
@.planning/phases/71-experiment-kb-task-taxonomy/71-04-SUMMARY.md

<interfaces>
<!-- From 71-01: openExperimentStore() (async, ontologyDir set). From 71-02: loadTaxonomy/isValidClass/deriveClassFromText. -->
<!-- From 71-03: aggregateByTaskId(taskId)→{totals,byAgentModel}. From 71-04: writeRun(store,{span,taskClass,pending,tags,totals}). -->
<!-- Existing measurement-stop.mjs (lines 25-47): LOCAL proxy-dist import + main()/catch envelope: -->
const PROXY_DIST = process.env.LLM_PROXY_DIST_DIR || '/Users/Q284340/Agentic/_work/rapid-llm-proxy/dist';
const { stopMeasurement, resolveMeasurementPaths } = await import(pathToFileURL(path.join(PROXY_DIST,'measurement-span.js')).href);
const archived = stopMeasurement();   // returns SpanRecord or null (no active span)
<!-- Span shape: { task_id, started_at, ended_at, goal_sentence? } ; archive path: <archiveDir>/<task_id>.json -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/measurement-stop.mjs into the close orchestrator (D-07)</name>
  <read_first>
    - scripts/measurement-stop.mjs (the ENTIRE existing 47-line file — extend in place; keep the LOCAL proxy-dist import at 25-30 and the main()/catch envelope at 44-47)
    - scripts/measurement-start.mjs (LOCAL-dist import convention, for parity)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md §"scripts/measurement-stop.mjs" (wire steps 2-5 AFTER the existing archive; D-06 must not throw; process.stdout/stderr.write only)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md §"System Architecture Diagram" (the 5-step pipeline) + §"verb→class heuristic" (the /gsd vs freeform vs headless branching) + Open Question 1 (auto-invoke hook is a SEPARATE concern — do NOT wire it here)
    - lib/experiments/{store,taxonomy,token-aggregate,run-write}.mjs (the modules to import — from 71-01..04)
  </read_first>
  <action>
    Extend `scripts/measurement-stop.mjs` AFTER its existing `stopMeasurement()` archive call (do NOT create a parallel orchestrator — D-07 extends this exact file). New steps: (2) read the archived span JSON (`<archiveDir>/<task_id>.json`) for `goal_sentence`/metadata; derive a candidate task_class: build the derive text from `goal_sentence` (+ any `--phase`/`--goal` CLI arg if present), call `deriveClassFromText`. Branch on close mode — accept a `--task-class <cls>` explicit override (validate against the closed-6 via `isValidClass`, REJECT free strings with a non-zero exit and a clear stderr message — SC-4 write-path enforcement); if interactive/freeform and TTY present, prompt via `node:readline` to confirm/override the candidate (D-05); if HEADLESS (no TTY / `--headless` / `CI`) and not confident, set `taskClass='unclassified'` + `pending=true` and DO NOT throw (D-06 quarantine — the close must never hard-block). (3) `aggregateByTaskId(span.task_id)` for `{totals, byAgentModel}`; source `agent`/`model` tags from `byAgentModel[0]` (dominant row); compute `task_hash` = sha256 of `goal_sentence || ''` (A3 — null if neither, allowed by D-13); `framework` from span.meta/agent (A2 — null allowed); `trace_id = span.task_id`. (4) `openExperimentStore()`; `writeRun(store, { span, taskClass, pending, tags, totals })` in a `try { ... } finally { await store.close(); }`. (5) Print a close summary to stdout including the resolved task_class, total tokens, and (if any) the current pending/quarantine count. Keep the top-level `main().catch` FATAL envelope. All output via process.stdout.write/process.stderr.write (no console.log). Do NOT wire any /gsd auto-invoke here (Task checkpoint below owns that decision).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; node scripts/measurement-stop.mjs 2>&amp;1 | grep -qi "no active measurement span" &amp;&amp; grep -q "writeRun" scripts/measurement-stop.mjs &amp;&amp; grep -q "isValidClass" scripts/measurement-stop.mjs &amp;&amp; echo "orchestrator wired + idempotent no-span path intact"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: running with NO active span still prints "no active measurement span" and exits 0 (existing idempotent path preserved).
    - Source: imports `writeRun`, `aggregateByTaskId`, `openExperimentStore`, and the taxonomy `deriveClassFromText`/`isValidClass` from `lib/experiments/*`.
    - Behavior: a `--task-class frobnicate` (free string) is REJECTED with a non-zero exit + clear message (SC-4 write-path enforcement); a closed-6 value is accepted.
    - Behavior: the headless/no-class path sets `task_class='unclassified'` + `pending=true` and does NOT throw (D-06) — the close completes.
    - Source: stopMeasurement still imported from the LOCAL proxy dist via `LLM_PROXY_DIST_DIR` (not node_modules); zero `console.*`.
    - Source: NO /gsd auto-invoke hook wiring in this file (that is the checkpoint decision).
  </acceptance_criteria>
  <done>measurement-stop.mjs orchestrates stopMeasurement→derive/prompt→enforce→aggregate→writeRun; rejects free strings; quarantines headless-no-class without throwing; no-span path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/experiments-query.mjs + scripts/experiments-classify.mjs CLIs</name>
  <read_first>
    - scripts/backfill-project-tag.mjs (CLI arg parse → openExperimentStore-style store open → iterate → close() in finally; usage/help text shape)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md §"query CLI canned queries (Q9 / D-03)" (the iterate + pending-exclusion + group-by skeleton) + §"experiments-classify" flow (D-08)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-PATTERNS.md §"scripts/experiments-query.mjs" + §"scripts/experiments-classify.mjs" (both open via openExperimentStore — satisfies the ontologyDir grep; node:readline for assign)
    - lib/experiments/{store,taxonomy}.mjs (openExperimentStore + isValidClass)
  </read_first>
  <action>
    Create `scripts/experiments-query.mjs`: parse args (`--task-class <c>`, `--agent <a>`, plus a canned-query selector e.g. `--query runs-by-class|agent-vs-cost`); `openExperimentStore()`; `for await (const e of store.iterate({ entityType:'Run' }))` SKIP any `e.metadata?.pending === true` (D-06 exclusion — non-negotiable), then apply optional task_class/agent filters; ship 2-3 canned queries: "runs by task_class" (count/list per class) and "agent vs model cost" (group by agent+model, sum the linked Outcome.totalTokens). Print results to stdout; `await store.close()` in finally. Create `scripts/experiments-classify.mjs`: `openExperimentStore()`; list all Runs where `metadata.pending === true`; surface the pending COUNT (D-08); for each (or a `--task-id <id> --task-class <c>` non-interactive form), validate the assigned class via `isValidClass` (reject free strings), then update the Run (`putEntity`/`mergeAttributes`) flipping `task_class` to the assigned value and `pending` to `false` (re-includes it in queries); interactive assign via `node:readline` when TTY. Both CLIs open via the shared `openExperimentStore()` (the literal `ontologyDir` must therefore reach the constructor — mandatory CLAUDE.md grep, satisfied TRANSITIVELY through the store factory). To make a literal `ontologyDir`-token grep pass on the CLI file itself, add a one-line comment at the import/open site of each CLI: `// opens via openExperimentStore() — ontologyDir set in lib/experiments/store.mjs`. No console.log (process.stdout/stderr.write).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; grep -q "openExperimentStore" scripts/experiments-query.mjs &amp;&amp; grep -q "openExperimentStore" scripts/experiments-classify.mjs &amp;&amp; grep -q "pending" scripts/experiments-query.mjs &amp;&amp; grep -q "isValidClass" scripts/experiments-classify.mjs &amp;&amp; node scripts/experiments-query.mjs --query runs-by-class 2>&amp;1 | head -1 &amp;&amp; echo "both CLIs present + exclusion + validation wired"</automated>
  </verify>
  <acceptance_criteria>
    - Source (MANDATORY CLAUDE.md grep): both CLIs reach `ontologyDir` via `openExperimentStore()` — `grep -c openExperimentStore scripts/experiments-query.mjs` ≥ 1 and likewise for classify; and the store factory's constructor carries the literal `ontologyDir` (asserted in 71-01).
    - Source (ontologyDir grep chain — EXPLICIT): the CLAUDE.md `ontologyDir` requirement is satisfied **transitively through the store factory** — `experiments-query.mjs` and `experiments-classify.mjs` both `grep -q openExperimentStore` (≥1), and the literal `ontologyDir` construction is asserted in `lib/experiments/store.mjs` (71-01 Task 2). Each CLI also carries the one-line `// opens via openExperimentStore() — ontologyDir set in lib/experiments/store.mjs` comment so a literal `ontologyDir`-token grep on the CLI file passes directly.
    - Behavior: `experiments-query.mjs` SKIPS Runs with `metadata.pending === true` (D-06 exclusion) — present in source as a `pending` guard.
    - Behavior: `experiments-classify.mjs` validates the assigned class via `isValidClass` (free strings rejected) before flipping `pending:false`; surfaces a pending count.
    - Behavior: `experiments-query.mjs --query runs-by-class` runs against the (possibly empty) store without throwing and exits 0.
    - Source: zero `console.*`; `store.close()` in finally in both.
  </acceptance_criteria>
  <done>Both CLIs exist, open via openExperimentStore (ontologyDir satisfied), query excludes pending, classify validates+re-includes and surfaces a pending count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/experiments/enforcement.test.mjs (SC-4 proof)</name>
  <behavior>
    - A free-string task_class is rejected at the write path (isValidClass guard) — assert the orchestrator/classify rejects 'frobnicate'.
    - A headless close with no confident class writes a Run with task_class='unclassified' + pending:true (writeRun called with pending).
    - experiments-query excludes pending Runs: seed one classified + one pending Run; the query result contains only the classified one.
    - experiments-classify flips a pending Run's class to a closed-6 value + pending:false; a subsequent query now INCLUDES it.
  </behavior>
  <read_first>
    - scripts/backfill-task-id-by-timestamp.mjs (lines 213-289 — node:test self-test + temp fixture + import-guard)
    - .planning/phases/71-experiment-kb-task-taxonomy/71-RESEARCH.md §"Validation Architecture" → SC-4 test-map row (the observable signals) + §"Wave 0 Gaps" (argv gotcha: EXPERIMENTS_LIVE env, not --live)
    - lib/experiments/{run-write,taxonomy,store}.mjs + scripts/experiments-query.mjs + scripts/experiments-classify.mjs (the units under test — import their pure functions; import-guard the CLIs so importing doesn't run main())
  </read_first>
  <action>
    Create `tests/experiments/enforcement.test.mjs` (node:test + node:assert/strict). Use a TEMP experiment store (temp ontologyDir = copy of `.data/ontologies-experiment/` + throwaway leveldb, via CODING_REPO override or a temp-store factory). Seed via `writeRun`: one Run with a closed-6 task_class + pending:false, one with task_class='unclassified' + pending:true. Assert: (1) `isValidClass('frobnicate')===false` and the classify-assign path rejects it; (2) the query-exclusion — iterate-with-pending-skip returns only the non-pending Run (reuse the query module's filter, or assert the documented exclusion predicate); (3) after running the classify update (flip pending→false + assign a closed-6 class) the query now returns both. Import-guard the CLIs (mirror backfill's `pathToFileURL(process.argv[1])` guard) so importing them in the test does not execute `main()`. Gate any live-only assertion on `EXPERIMENTS_LIVE` env var (NOT a `--live` CLI flag — MEMORY.md argv gotcha). No console.log.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding &amp;&amp; node --test tests/experiments/enforcement.test.mjs &amp;&amp; node --test tests/experiments/</automated>
  </verify>
  <acceptance_criteria>
    - Test: `node --test tests/experiments/enforcement.test.mjs` exits 0; AND the full `node --test tests/experiments/` suite (ontology+taxonomy+token-aggregate+run-write+enforcement) is green (per-wave merge gate).
    - Behavior: free-string rejection, headless→quarantine, query-excludes-pending, classify-re-includes all asserted.
    - Source: CLIs are import-guarded (importing does not run main()); any live gate uses EXPERIMENTS_LIVE env, never --live argv.
  </acceptance_criteria>
  <done>enforcement.test.mjs passes and the full experiments suite is green — SC-4 proven (enforcement + quarantine + exclusion + re-inclusion).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>How should `coding measure stop` be auto-invoked at /gsd run-end (D-05 auto-derive wiring)?</decision>
  <context>
    RESEARCH Assumptions A1/A5 + PATTERNS "No Analog Found": there is NO `/gsd` run-end auto-invoke hook in this checkout's `.claude/commands/`, and `bin/coding` is a bash launcher with no subcommand dispatch. The close orchestrator (Task 1) works standalone today via `node scripts/measurement-stop.mjs`. The auto-invoke wiring is the ONE piece the planner cannot determine from artifacts — it depends on where you want /gsd to fire the close, and functionality (derive→enforce→write) does NOT depend on it. This checkpoint exists because guessing the hook point risks wiring into a non-existent or wrong surface.
  </context>
  <options>
    <option id="defer-hook">
      <name>Defer auto-invoke; ship manual close only this phase</name>
      <pros>Zero risk of wiring a wrong/non-existent hook; the orchestrator + CLIs fully satisfy KB-02/KB-03/SC-1..SC-4 standalone; auto-invoke becomes a tracked follow-up.</pros>
      <cons>Operator must run `node scripts/measurement-stop.mjs` (or `--task-class`) manually at run-end until the hook lands.</cons>
    </option>
    <option id="bin-coding-subcommand">
      <name>Add a `coding measure stop` / `coding experiments ...` bash subcommand dispatch to bin/coding</name>
      <pros>Gives the locked-decision CLI surface (`coding measure stop`); small bash router.</pros>
      <cons>Adds bash-router scope; still needs a separate /gsd run-end trigger to be truly auto.</cons>
    </option>
    <option id="gsd-run-end-hook">
      <name>Wire a concrete /gsd run-end hook (you point to the exact hook file/surface)</name>
      <pros>True auto-derive at /gsd run-end (D-05 fully realized).</pros>
      <cons>Requires you to identify the real hook point (none found in this checkout); risk of coupling to an unstable surface.</cons>
    </option>
  </options>
  <resume-signal>Select: defer-hook, bin-coding-subcommand, or gsd-run-end-hook (and if the last, point to the exact hook file)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase-71 close path: a real `node scripts/measurement-stop.mjs` close that derives/enforces a task_class, aggregates token totals from `token-usage.db`, and writes an idempotent Run (+Outcome) into the dedicated `.data/experiments/` store; plus `experiments query` (excludes pending) and `experiments classify` (resolves quarantine).</what-built>
  <how-to-verify>
    1. Start a measurement span for a real or synthetic task: `node scripts/measurement-start.mjs --task-id verify-71` (or your start CLI form), do a tiny bit of work that produces token_usage rows under that task_id, then close: `node scripts/measurement-stop.mjs --task-class refactor`. Expect a close summary printing task_class=refactor + a non-zero total-tokens sourced from token_usage.
    2. Confirm the Run landed and is queryable: `node scripts/experiments-query.mjs --query runs-by-class` — the `verify-71` run appears under `refactor` with token totals matching `token-usage.db WHERE task_id='verify-71'`.
    3. Idempotency: re-run the close path for the same task_id (or re-aggregate) and confirm the query still shows exactly ONE `verify-71` Run (updated, not duplicated).
    4. Quarantine: simulate a headless close with no class (`--headless`, or no TTY + no --task-class) for a second task_id; confirm the Run is written with task_class='unclassified'+pending and is ABSENT from `experiments query` results.
    5. Resolve: `node scripts/experiments-classify.mjs` — it surfaces the pending count and lets you assign a closed-6 class; after assigning, `experiments query` now INCLUDES that run.
    6. Confirm the experiment store is the dedicated one: `ls .data/experiments/` shows leveldb + exports, and `.data/knowledge-graph/` is untouched by this close.
  </how-to-verify>
  <resume-signal>Type "approved" (a Run lands with enforced task_class + correct totals, idempotent; headless lands in quarantine excluded from queries; classify re-includes) or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args (`--task-class`) → write path | Operator-supplied task_class enum-validated against the closed-6 before any write. |
| close orchestrator → proxy `stopMeasurement()` + token-usage.db (read-only) | Coding orchestrates the generic proxy span SDK + reads the proxy-owned DB; proxy stays sole writer. |
| classify CLI → experiment store update | Operator re-assigns a class on a quarantined Run (enum-validated). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05-01 | Tampering/Integrity | free-string `--task-class` polluting partitions | mitigate | `isValidClass` enum gate at both the orchestrator write path and classify-assign; free strings rejected with non-zero exit (D-09/SC-4). Task 1/2/3 assert rejection. |
| T-71-05-02 | DoS | headless close hard-blocking on missing class | mitigate | D-06 quarantine path: write `unclassified`+pending, never throw — the close always completes (Task 1 behavior + Task 3 assertion). |
| T-71-05-03 | Tampering | path traversal via task_id in span filename | mitigate | task_id is proxy-sanitized (`sanitizeTaskId` charset + basename) before archive; coding reads the archived file by the sanitized name (Security V5). |
| T-71-05-04 | Tampering/DoS | second writer to token-usage.db | mitigate | Aggregation opens `{readonly:true}` (71-03); the orchestrator never writes the proxy DB. |
| T-71-05-05 | Tampering | npm installs | mitigate | No new package installs (RESEARCH Package Legitimacy Audit — zero registry installs). slopcheck N/A. |
</threat_model>

<verification>
- Task 1 grep: orchestrator wires writeRun/isValidClass while preserving the LOCAL-dist stopMeasurement import and the no-span idempotent path.
- Task 2 grep: both CLIs reach `ontologyDir` via openExperimentStore (mandatory CLAUDE.md grep); query excludes pending; classify validates.
- `node --test tests/experiments/` full suite green (SC-1..SC-4 + KB-02 idemp).
- Decision checkpoint resolves the /gsd auto-invoke hook surface (A1/A5).
- Live human-verify proves a real close lands an enforced+idempotent Run with correct totals, headless→quarantine excluded from queries, classify re-includes, and `.data/knowledge-graph/` is untouched.
</verification>

<success_criteria>
- KB-02 / SC-2 satisfied end-to-end: a real close materializes a queryable Run with the 8 tags sourced from token_usage + span.
- KB-03 / SC-4 satisfied: task_class is enforced at run-end (closed-6; free strings rejected); a run cannot be COUNTED without a class (quarantine excludes unclassified from all queries until resolved) — without hard-blocking headless closes (D-06).
- D-03/D-08: query + classify CLIs ship; comparisons-as-queries proven by canned queries; quarantine backlog resolvable with a surfaced pending count.
- The /gsd auto-invoke hook decision is recorded (functionality is independent of it).
</success_criteria>

<output>
Create `.planning/phases/71-experiment-kb-task-taxonomy/71-05-SUMMARY.md` when done.
</output>
