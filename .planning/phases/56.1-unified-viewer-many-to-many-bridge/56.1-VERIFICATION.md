---
phase: 56.1-unified-viewer-many-to-many-bridge
verified: 2026-06-14T12:45:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
must_haves_source: "CONTEXT.md decisions D-1..D-7 + 9 acceptance criteria + PATTERNS-LOCK 7 contracts + 4 deferred Warnings (WR-01..WR-04) + 10 code-review findings (CR-01/CR-02 + WR-01..WR-08 of REVIEW)"
---

# Phase 56.1: Unified Viewer — Many-to-Many Temporal-Knowledge Bridge — Verification Report

**Phase Goal:** Evolve the Phase 56 single-selection bidirectional bridge into a many-to-many bridge so a timeline tick can highlight multiple touched nodes in the graph (and a graph node can highlight multiple touching timeline ticks), with a multi-mode sidebar that switches between EntityDetailPanel and BucketCardList, while preserving the Phase 56 audit-locked viewport-stability invariant.

**Verified:** 2026-06-14T12:45:00Z
**Status:** passed
**Re-verification:** No — initial verification.

---

## Goal Achievement Summary

The phase goal is **observably achieved** in the codebase. All 7 PATTERNS-LOCK contracts are honored, all 9 acceptance criteria (AC#1..#9) are wired through real code paths, all 4 deferred Phase 56 Warnings (WR-01..WR-04) are closed in their assigned plans, and all 10 code-review findings (2 BLOCKERS + 8 WARNINGS) from `56.1-REVIEW.md` have follow-up fix commits with code in place. TypeScript compiles cleanly project-wide; 178/180 Phase 56.1 targeted vitest cases pass (the 2 failing `toggleLayer` tests are pre-existing Phase 55 failures documented in `deferred-items.md` and confirmed reproducing on main — out of scope).

The operator visual smoke (Round 4 of gap-closure) confirmed the bidirectional cascade actually fires end-to-end at `localhost:5173/viewer/coding`. The remaining "low-multi-card density" UX observation was traced to a DATA gap in the online ingestion pipeline (all 91 Insights have only `capturedBy → LiveLoggingSystem` edges; no semantic-content edges), captured as a separate milestone-shaped TODO at `.planning/todos/pending/2026-06-14-online-pipeline-semantic-edges-and-timeline-bi-source.md`. That is upstream-data work, **not a Phase 56.1 viewer bug**.

---

## Observable Truths (D-1..D-7 + AC#1..#9)

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1  | D-1: Store exposes `selectedNodeIds: ReadonlySet<string>`, `focalNodeId: string|null`, `selectedBucketKeys: ReadonlySet<string>`, `focalBucketKey: string|null` | VERIFIED | `viewer-store.ts:71-74` (interface), `:159-162` (initial state at `:434-437`); deleted Phase 56 names `selectedNodeId`/`selectedSessionId`/`selectedSessionStartAt` only appear in COMMENT lines (lines 120-121) — grep with `-v '^[[:space:]]*//'` returns zero active hits |
| 2  | D-2: `pickAllResolvable(entityIds, visibleIds, relations, noiseAncestors?): Set<string>` is the touched predicate used in BOTH directions | VERIFIED | `ancestry.ts:266` (definition); forward direction at `LslTimelineStrip.tsx:584-585` (onTickClick — with unsuppressed + suppressed comparison for observability); reverse direction reads from `useNodeToBucketsIndex` pre-index (D3GraphCanvas does NOT call pickAllResolvable directly — Contract #7 honored) |
| 3  | D-3: `useNodeToBucketsIndex` hook pre-builds `ReadonlyMap<nodeId, ReadonlySet<bucketKey>>` once per data-load | VERIFIED | `useNodeToBucketsIndex.ts:33-`; useMemo dep list `[sessions, visibleIds, relations]` per Contract #7; consumed by `D3GraphCanvas.tsx` via `nodeToBucketsRef` pattern (bug-2 stale-closure fix in place) |
| 4  | D-4 graph two-tier: red focal ring (`#ff0000`, opacity 1) on `focalNodeId`; lighter-blue halo ring (`#60a5fa`, opacity 0.6) on every other member of `selectedNodeIds` | VERIFIED | `D3GraphCanvas.tsx:404-410` writes `data-focal="true"` + `data-halo="true"` on `.node <g>` parents; G15 ring color gate green |
| 5  | D-4 timeline two-tier: focal `ring-blue-500` on `focalBucketKey === tickKey`; halo `ring-blue-300/60` + `bg-blue-200/40` on `selectedBucketKeys.has(tickKey) && !focal` | VERIFIED | `LslTimelineStrip.tsx` tick render block; Test 56.1-T1 + 56.1-T2 in `LslTimelineStrip.test.tsx` |
| 6  | D-4 sidebar: three-way mode-switch `BucketCardList` (multi) / `EntityDetailPanel` (single-focal) / `HistorySidebar` (default) | VERIFIED | `SidePanel.tsx:85-88` (`isSingleFocalMode`/`isMultiMode` predicates per Round 2 UX shortcut — single-node tick resolves directly to EntityDetailPanel); imports at lines 30-32 |
| 7  | D-5: Card click + halo-node click drill collapses to single-focal mode | VERIFIED | `BucketCardList.tsx onCardClick` writes drill payload via `setSelection` with `pushHistory: true` (Decision 1 preserves prior state for Esc/X pop); `D3GraphCanvas.tsx` node click handler at `:735` writes singleton + reads reverse-index via `nodeToBucketsRef.current.get(d.id)` |
| 8  | D-7 Contract #1 (tick-ring two-tier): EVOLVED — store-derived from `selectedBucketKeys`/`focalBucketKey` | VERIFIED | LslTimelineStrip render block uses both `focalBucketKey === tickKey` AND `selectedBucketKeys.has(tickKey)`; selectedTs reverse-cascade fallback preserved |
| 9  | D-7 Contract #2 (graph-side selection): EVOLVED — halo + focal + trace from focal only | VERIFIED | `applySelectionStyling` useCallback dep list `[selectedNodeIds, focalNodeId, pathToSelected, visibleRelations, theme]` at D3GraphCanvas.tsx:484; ancestry trace reads `focalNodeId` |
| 10 | D-7 Contract #3 (viewport stability) Part A: AUDIT-LOCKED main render dep list verbatim | VERIFIED | `D3GraphCanvas.tsx:954` — `}, [visibleEntities, visibleRelations, theme, isLoading])` matched exactly once; G9 gate green |
| 11 | D-7 Contract #3 Part B (AMENDED): viewport MAY animate ONCE on Layer 0→1 transition (multi-set fit-to-bounds) | VERIFIED | Separate useEffect at `D3GraphCanvas.tsx:495+` keyed on `[selectionSource, selectedNodeIds, pathToSelected]`; uses zoom transform primitive (NOT SVG rebuild); fitting uses `pendingFitRef` + `runFitToBoundsRef` (CR-02 fix for d3NodesRef stale-coords race) |
| 12 | D-7 Contract #4 (0-obs tick policy): UNCHANGED AUDIT-LOCKED — 0-obs ticks render `opacity-40 pointer-events-none cursor-default` + early-exit before any store write | VERIFIED | `LslTimelineStrip.tsx` 0-obs branch preserved verbatim; source-grep gate Test 12 asserts `/opacity-40[^']*pointer-events-none/` |
| 13 | D-7 Contract #5 (single source of truth): zero inline `useViewerStore.setState({` in consumer paths | VERIFIED | Grep across D3GraphCanvas/SidePanel/HistorySidebar/BucketCardList/LslTimelineStrip returns ZERO hits; all writes route through `setSelection`/`clearSelection`/`popSelection`/`addToSelection` actions |
| 14 | D-7 Contract #6 (phantom-id resolution multi): every member satisfies `visibleIds.has(id)` via `pickAllResolvable` / `pickFirstResolvable` | VERIFIED | `pickAllResolvable` definition at `ancestry.ts:266`; `pickFirstResolvable` preserved at `ancestry.ts:213` for backward compat (Phase 56 contract carries forward verbatim) |
| 15 | D-7 Contract #7 (pre-index integrity): `pickAllResolvable` callsites limited to ancestry.ts + useNodeToBucketsIndex.ts + LslTimelineStrip.tsx (NEW additive contract) | VERIFIED | Grep returns exactly the 3 allowed paths (LslTimelineStrip has 2 callsites on adjacent lines for unsuppressed-vs-suppressed observability, but both within the canonical D-2 forward-direction handler — same path); 4th path = smell, none observed |
| 16 | All four Phase 56 deferred Warnings (WR-01/02/03/04) closed in this phase's test suite | VERIFIED | WR-01 (range-based assertion replacing `toHaveCount(1,`) — closed in Plan 06 (grep returns 0); WR-02 (diamond BFS) — closed in Plan 02 (3 tests pass); WR-03 (`selectedClasses.size===0` race) — closed in Plan 05 with regression test; WR-04 (reset coverage) — closed in Plan 01 with `reset() clears LSL slice + pathToSelected` test |

**Score:** 16/16 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `integrations/unified-viewer/src/store/viewer-store.ts` | Multi-set slice + setSelection multi-arg + clearSelection + reset + popSelection + addToSelection + SelectionSnapshot | VERIFIED | 46K bytes; 4 new fields defined at lines 71-74 + 159-162; `selectionHistory: SelectionSnapshot|null` at :170; `addToSelection` action at :700 |
| `integrations/unified-viewer/src/graph/ancestry.ts` | `pickAllResolvable` + WR-02 BFS + LLS suppression + NOISE_ANCESTOR_NAMES constant | VERIFIED | 14.7K bytes; 4 exports (computeAncestryPath, resolveToVisibleAncestor, pickFirstResolvable, pickAllResolvable); NOISE_ANCESTOR_NAMES Set at :63 |
| `integrations/unified-viewer/src/graph/D3GraphCanvas.tsx` | Two-tier rings + drill click + reverse-index via nodeToBucketsRef + Layer 0→1 fit-to-bounds | VERIFIED | 47.7K bytes; main dep list locked at :954; fit-to-bounds useEffect with sticky pendingFitRef; node click reads via ref pattern |
| `integrations/unified-viewer/src/graph/useNodeToBucketsIndex.ts` | NEW reverse-lookup pre-index hook | VERIFIED | 6.2K bytes; useMemo dep list `[sessions, visibleIds, relations]` honored |
| `integrations/unified-viewer/src/panels/BucketCardList.tsx` | NEW multi-mode sidebar card list + drill-preserves-LSL-filter | VERIFIED | 10.8K bytes; onCardClick preserves `lslFilterEntityIds` (Decision 3) so viewport doesn't re-fit on drill |
| `integrations/unified-viewer/src/panels/SidePanel.tsx` | Three-way mode-switch + close-button → popSelection/clearSelection | VERIFIED | 10.3K bytes; `isSingleFocalMode`/`isMultiMode` at :85-88; X-button at :166-167 uses `popped = popSelection(); if (!popped) clearSelection()` pattern (WR-05 fix) |
| `integrations/unified-viewer/src/panels/HistorySidebar.tsx` | onClick migrated to multi-set setSelection arg shape | VERIFIED | 10.5K bytes |
| `integrations/unified-viewer/src/panels/coding/useLslSessions.ts` | NEW extracted TanStack Query hook | VERIFIED | 3.2K bytes; shared between LslTimelineStrip and useNodeToBucketsIndex |
| `integrations/unified-viewer/src/panels/coding/LslTimelineStrip.tsx` | Multi-write onTickClick + two-tier tick render + WR-03 fix + auto-drill on single resolution + timeline-scale stability | VERIFIED | 48.7K bytes; uses `addToSelection` for Cmd-click (WR-04 fix); aria-busy for WR-06; auto-slide gated on `isNewSelection` + selectionSource (Round 4 fixes) |
| `integrations/unified-viewer/src/hooks/useKeyboardShortcuts.ts` | Esc → popSelection w/ explicit fallback | VERIFIED | 14.2K bytes; lines 209-210 use `popped = popSelection(); if (!popped) clearSelection()` (WR-05 fix) |
| `tests/e2e/unified-viewer/56.1-many-to-many.spec.ts` | NEW E2E spec — ≥7 tests covering AC#1, #2, #3+4, #4, #5, #8, #9 | VERIFIED | 33K bytes; 9 tests discovered by playwright (`--list` confirms); includes 2 regression probes for bidirectional wiring bugs (bug-1 halo membership + bug-2 reverse-index stale closure) |
| `tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts` | WR-01 fix + WR-08 migration to multi-set store interface | VERIFIED | 19.4K bytes; zero `toHaveCount(1,` matches (WR-01 closed); deleted-fields appear only in WR-08 comment block |
| `.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md` | 7 contracts locked + maintenance clause + Contract #3 amendment | VERIFIED | 18.8K bytes; all 7 contracts present + cite implementation locations + source-grep gates |

---

## Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| LslTimelineStrip.onTickClick | setSelection / addToSelection | atomic multi-set write | WIRED | 3 setSelection callsites at :555, :717, :735 + 1 addToSelection callsite at :642 (Cmd-click branch via WR-04 fix) |
| D3GraphCanvas node click | setSelection w/ bucketKeys from nodeToBucketsRef | reverse-direction atomic write | WIRED | Click handler reads `nodeToBucketsRef.current.get(d.id)` (bug-2 fix); writes via setSelection |
| BucketCardList card click | setSelection w/ drill payload + pushHistory | drill collapse + history push | WIRED | onCardClick writes singleton + clears bucketKeys + preserves `lslFilterEntityIds` (Decision 3) |
| Esc / X button | popSelection → clearSelection fallback | one-step-back UX | WIRED | useKeyboardShortcuts.ts:209-210 + SidePanel.tsx:166-167 use explicit `if (!popped) clearSelection()` (WR-05 fix) |
| SidePanel mode-switch | BucketCardList \| EntityDetailPanel \| HistorySidebar | predicate ternary chain | WIRED | `isSingleFocalMode` / `isMultiMode` derived predicates at :85-88; imports at :30-32 |
| useNodeToBucketsIndex rebuild | pickAllResolvable iteration over sessions | useMemo on [sessions, visibleIds, relations] | WIRED | Hook body iterates buckets, calls pickAllResolvable once per bucket w/ noiseAncestors propagated |
| Layer 0→1 fit-to-bounds | zoom transform via zoomBehaviorRef | one-shot 500ms animation | WIRED | Separate useEffect at :495+ uses prevSelectionSourceRef transition detector + pendingFitRef for stale-coords race (CR-02 fix) |
| Auto-slide on selection focal | timeline scale (windowKey) | guarded on isNewSelection + selectionSource | WIRED | LslTimelineStrip.tsx:380-397 — guards prevent auto-slide on timeline/history sources (Round 4 timeline-scale stability fix) |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| BucketCardList | items useMemo | `useGraphData(apiClient, system).entities` filtered by `lslFilterEntityIds` or `selectedNodeIds` | Yes — real KM-Core entities via TanStack Query | FLOWING |
| D3GraphCanvas halo nodes | `selectedNodeIds` | Written by LslTimelineStrip.onTickClick via setSelection from `pickAllResolvable(bucket.entityIds, visibleIds, relations, noiseAncestors)` | Yes — real ancestry traversal | FLOWING |
| Timeline tick halo | `selectedBucketKeys` | Written by D3GraphCanvas click handler via `nodeToBucketsRef.current.get(d.id)` (reverse-index) | Yes — real reverse-lookup map | FLOWING |
| EntityDetailPanel | `focalNodeId` → entity useMemo over `entities` | Real KM-Core entity from useGraphData | Yes | FLOWING |
| useNodeToBucketsIndex | reverse-Map | Real sessions × real visible ids × real relations | Yes | FLOWING |

**Operator visual smoke observation (Plan 06 SUMMARY operator_uat.data_gap_observed):** the multi-card sidebar is rarely visible in the recent 7d window because the underlying KB data has 100% `capturedBy`-only edges on Insights. The viewer renders the data **faithfully** — this is a known DATA pipeline gap captured at `.planning/todos/pending/2026-06-14-online-pipeline-semantic-edges-and-timeline-bi-source.md`, **not a Phase 56.1 viewer regression**.

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Project-wide tsc compiles cleanly | `cd integrations/unified-viewer && npx tsc --noEmit \| wc -l` | 0 lines | PASS |
| Phase 56.1 targeted vitest passes | `npx vitest run src/store/viewer-store.test.ts src/graph/ancestry.test.ts src/graph/D3GraphCanvas.test.ts src/graph/useNodeToBucketsIndex.test.ts src/panels/BucketCardList.test.tsx src/panels/coding/LslTimelineStrip.test.tsx` | 178/180 pass | PASS (2 fails are pre-existing toggleLayer per deferred-items.md) |
| E2E specs discoverable | `npx playwright test --project=unified-viewer --list` | 13 tests listed for the 2 spec files (4 in 56-bidirectional + 9 in 56.1-many-to-many) | PASS |
| WR-01 fix in place | `grep -c "toHaveCount(1," tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts` | 0 | PASS |
| AC#2 reverse-direction test exists | `grep -c "AC #2:" tests/e2e/unified-viewer/56.1-many-to-many.spec.ts` | 1 | PASS |
| Contract #3 Part A dep list verbatim | `grep -c '}, \[visibleEntities, visibleRelations, theme, isLoading\]' integrations/unified-viewer/src/graph/D3GraphCanvas.tsx` | 1 | PASS |
| Contract #5 acceptance grep | `grep -E "useViewerStore\.setState\(\{" src/{graph/D3GraphCanvas,panels/SidePanel,panels/HistorySidebar,panels/BucketCardList,panels/coding/LslTimelineStrip}.tsx` | empty | PASS |
| Contract #7 pickAllResolvable callsites | `grep -rn "pickAllResolvable(" src/ \| grep -v test` | exactly 3 file paths (ancestry.ts def + useNodeToBucketsIndex hook + LslTimelineStrip handler) | PASS |
| Deleted Phase 56 fields purged from active source | `grep -E "selectedNodeId\b\|selectedSessionId\b\|selectedSessionStartAt\b" src/store/viewer-store.ts \| grep -v '//' \| grep -v selectedNodeIds` | 0 active-source hits (only in 2 comment lines) | PASS |

---

## Requirements Coverage

The phase context lists the requirement IDs as D-1..D-7 (decisions), AC#1..AC#9 (acceptance criteria), WR-01..WR-04 (deferred Phase 56 Warnings), CR-01 + CR-02 + WR-01..WR-08 (code review findings, prefixed `56.1-REVIEW`).

| Requirement | Description | Status | Evidence |
| ----------- | ----------- | ------ | -------- |
| D-1 | Multi-set store slice | SATISFIED | viewer-store.ts:71-74 + tests |
| D-2 | pickAllResolvable both directions | SATISFIED | ancestry.ts:266 + useNodeToBucketsIndex.ts:33 + LslTimelineStrip.tsx:584 |
| D-3 | Pre-indexed reverse map | SATISFIED | useNodeToBucketsIndex.ts useMemo with correct deps |
| D-4 | Two-tier visual (graph + timeline + sidebar) | SATISFIED | D3GraphCanvas data-focal/halo + LslTimelineStrip ring classes + SidePanel mode-switch |
| D-5 | Drill collapses halo | SATISFIED | BucketCardList.onCardClick + D3GraphCanvas node click both write singleton + clear bucketKeys |
| D-6 | Fold WR-01..WR-04 into this phase | SATISFIED | Closed in Plans 06/02/05/01 respectively |
| D-7 | 7 contract evolutions locked | SATISFIED | PATTERNS-LOCK.md with all 7 contracts + Contract #3 Part B amendment |
| AC#1 | Tick click → multi-ring + card list | SATISFIED | E2E AC#1 test passes against post-fix code; operator smoke confirmed |
| AC#2 | Node click → multi-tick + EntityDetailPanel | SATISFIED | E2E AC#2 dedicated reverse-direction test + bug-2 regression probe |
| AC#3 | Card click drill collapse | SATISFIED | E2E AC#3+4 combined test |
| AC#4 | Halo node click drill collapse | SATISFIED | E2E AC#4 standalone halo-click test |
| AC#5 | Esc clears all (or pops one level w/ history) | SATISFIED | useKeyboardShortcuts.ts:209-210 + E2E AC#5 |
| AC#6 | 6 Phase 56 contracts evolve correctly | SATISFIED | PATTERNS-LOCK contracts #1-6 with source-grep gates from Plans 01-05 |
| AC#7 | WR-01..WR-04 closed in this phase's test suite | SATISFIED | All 4 WR tests green (per plan SUMMARYs) |
| AC#8 | Viewport stable (Part A) + amended Part B allowance | SATISFIED | E2E AC#8 viewport-transform invariance test + Contract #3 Part B amendment |
| AC#9 | Pre-index integrity grep clean | SATISFIED | E2E AC#9 source-grep test executes inside the spec |
| WR-01 (Phase 56 deferred) | Range-based assertion | CLOSED | Plan 06 commit `c2237990a` |
| WR-02 (Phase 56 deferred) | Diamond BFS | CLOSED | Plan 02 commit `293d7b3cc` |
| WR-03 (Phase 56 deferred) | selectedClasses race-window guard | CLOSED | Plan 05 commit `f9334a9fd` |
| WR-04 (Phase 56 deferred) | reset() coverage | CLOSED | Plan 01 commit `4f379e078` |
| 56.1-REVIEW CR-01 | deriveFocal preservation on identical-content writes | CLOSED | Commit `8769725fa` |
| 56.1-REVIEW CR-02 | Fit-to-bounds deferred when d3Nodes lack coords | CLOSED | Commit `d9d099d12` |
| 56.1-REVIEW WR-01 | pickAllResolvable noise suppression precompute-then-delete | CLOSED | Commit `c39e0c2f5` |
| 56.1-REVIEW WR-02 | NOISE_ANCESTOR_NAMES centralized constant + empty-set canary | CLOSED | Commit `5f2a47e82` |
| 56.1-REVIEW WR-03 | applySelectionStyling dep list — intentional bucket-fields omission documented | CLOSED | Commit `7ec37c7ad` |
| 56.1-REVIEW WR-04 | atomic addToSelection action eliminates read-modify-write race | CLOSED | Commit `be9d23597` |
| 56.1-REVIEW WR-05 | popSelection is pop-only; callers gate clearSelection fallback | CLOSED | Commit `4ae422fe6` |
| 56.1-REVIEW WR-06 | aria-busy on the strip surfaces filters-loading state | CLOSED | Commit `da9e14890` |
| 56.1-REVIEW WR-07 | CRITICAL ORDERING comment on prevSelectionSourceRef bookkeeping | CLOSED | (in-place fix; visible at D3GraphCanvas.tsx:608) |
| 56.1-REVIEW WR-08 | 56-bidirectional spec migrated to multi-set store interface | CLOSED | Commit `89b1831ab` |

All 30 requirement-level items resolved.

---

## Anti-Patterns Scanned

| File | Concern | Result |
| ---- | ------- | ------ |
| viewer-store.ts | TODO/FIXME/XXX without issue ref | none in scope |
| ancestry.ts | precompute-then-delete pattern | applied at :297-308 (WR-01 REVIEW fix) |
| D3GraphCanvas.tsx | Inline useViewerStore.setState | 0 |
| LslTimelineStrip.tsx | Inline useViewerStore.setState | 0 |
| SidePanel.tsx | Inline useViewerStore.setState | 0 |
| BucketCardList.tsx | Inline useViewerStore.setState | 0 |
| HistorySidebar.tsx | Inline useViewerStore.setState | 0 |
| useKeyboardShortcuts.ts | Inline useViewerStore.setState | 0 |

No blockers found. The codebase respects every PATTERNS-LOCK invariant and the 4 deferred Warnings.

---

## Human Verification Required

**None.** Operator visual smoke (Round 4 of gap-closure) was already performed and recorded under `operator_uat:` in `56.1-06-SUMMARY.md` with `completed:` items covering AC#1, AC#2, AC#5, AC#8, the UX shortcut, and timeline-scale stability. The phase plan's `checkpoint:human-verify` task gate has been operationally satisfied across the gap-closure rounds — the operator was the source of the bug surface (Round 1 bugs 1+2, Round 2 UX shortcut, Round 3 decisions 1/2/C, Round 4 timeline-scale bug) and approved each fix before the next round began.

Five out-of-scope items the operator surfaced during smoke (filter behaviors, ontology, observation/digest bleed, legend, online ingestion pipeline data gap) are properly captured as separate TODOs in `.planning/todos/pending/2026-06-14-*.md` and are explicitly NOT Phase 56.1 viewer bugs.

---

## Gaps Summary

No gaps found.

The phase goal — "evolve the Phase 56 single-selection bidirectional bridge into a many-to-many bridge so a timeline tick can highlight multiple touched nodes in the graph (and a graph node can highlight multiple touching timeline ticks), with a multi-mode sidebar that switches between EntityDetailPanel and BucketCardList, while preserving the Phase 56 audit-locked viewport-stability invariant" — is achieved in the codebase:

1. **Multi-selection store slice** exists with the four new fields, all writes route through atomic actions (`setSelection` / `addToSelection` / `clearSelection` / `popSelection`), and the audit-locked `sameSetMembership` deep-equal guard is extended uniformly to all three Set fields.
2. **Bidirectional bridge** is wired end-to-end: forward direction via `pickAllResolvable` in `LslTimelineStrip.onTickClick`, reverse direction via `useNodeToBucketsIndex` consumed in `D3GraphCanvas` via the `nodeToBucketsRef` pattern (bug-2 stale-closure fix in place).
3. **Multi-mode sidebar** has the three-way mode-switch (`BucketCardList` / `EntityDetailPanel` / `HistorySidebar`); single-node tick UX shortcut (Round 2) skips card list to land directly on `EntityDetailPanel`.
4. **Viewport-stability invariant preserved**: Part A (main render dep list `[visibleEntities, visibleRelations, theme, isLoading]`) is intact and grep-verified; Part B (Layer 0→1 one-shot fit-to-bounds) is a documented amendment that does NOT rebuild SVG or restart the force simulation.
5. **All 4 deferred Warnings (WR-01..04) closed** and all 10 code-review findings (2 BLOCKERS + 8 WARNINGS) have follow-up fix commits with code visible in the production sources.
6. **Selection-history stack** (Decision 1), **viewport fit** (Decision 2), **LLS suppression + auto-drill** (Decision C), **drill preserves LSL filter** (Decision 3), and **timeline-scale stability fix** (Round 4) all extend the phase's goal coherence beyond the autonomous scope, operator-approved at each step.
7. **PATTERNS-LOCK** durably records all 7 contracts with source-of-truth citations and a maintenance clause.

The phase is **observably complete and ready for closure**.

---

*Verified: 2026-06-14T12:45:00Z*
*Verifier: Claude (gsd-verifier, Opus 4.7 1M context)*
