---
phase: 56.1-unified-viewer-many-to-many-bridge
plan: 06
type: execute
wave: 3
depends_on:
  - 56.1-01
  - 56.1-02
  - 56.1-03
  - 56.1-04
  - 56.1-05
files_modified:
  - tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts
  - tests/e2e/unified-viewer/56.1-many-to-many.spec.ts
  - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md
autonomous: false
requirements:
  - WR-01
  - AC-1-9
  - D-7-contract-7
tags:
  - unified-viewer
  - e2e
  - playwright

must_haves:
  truths:
    - "56-bidirectional-selection.spec.ts WR-01 assertion is RANGE-based (>= 1 OR seed-deterministic), NOT count===1"
    - "56.1-many-to-many.spec.ts NEW spec covers AC#1, AC#2, AC#3, AC#4, AC#5, AC#8, AC#9 via automated E2E (AC#6 + AC#7 are covered via source-grep gates from Plans 01-05 / PATTERNS-LOCK)"
    - "AC#2 is exercised by a dedicated E2E test that clicks a graph node known to touch ≥2 buckets and asserts: (a) focal ring on graph node; (b) ≥1 halo tick on timeline; (c) EntityDetailPanel visible in sidebar (NOT bucket-card-list)"
    - "Operator visual smoke at localhost:5173/viewer/coding confirms tick click -> multi-ring graph + card list; node click -> multi-tick timeline + entity detail; card click -> drill collapse; Esc clears all; viewport stays stable"
    - "56.1-PATTERNS-LOCK.md durably records the 7 evolved/new contracts (D-7) — locked as the authoritative reference for future plans extending 56.1"
    - "All 6 Phase 56 PATTERNS contracts (evolved per D-7) hold post-refactor — verified by source-grep gates from Plans 01-05 + by E2E AC#6 smoke"
    - "Pre-index integrity contract #7 — acceptance grep clean (pickAllResolvable callsites limited to 3 sources per Plan 05)"
  artifacts:
    - path: "tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts"
      provides: "WR-01 fix — range-based assertion replaces count===1"
      contains: "toBeGreaterThanOrEqual"
    - path: "tests/e2e/unified-viewer/56.1-many-to-many.spec.ts"
      provides: "E2E AC#1, AC#2, AC#3, AC#4, AC#5, AC#8, AC#9 coverage for the many-to-many bridge (AC#6 + AC#7 covered by source-grep from prior plans + PATTERNS-LOCK)"
      contains: "Phase 56.1 many-to-many bridge"
    - path: ".planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md"
      provides: "Durable contract artifact — 7 evolved/new contracts locked"
      contains: "Contract #7"
  key_links:
    - from: "tests/e2e/unified-viewer/56.1-many-to-many.spec.ts"
      to: "data-testid markers seeded by Plans 03/04/05 (bucket-card-list, bucket-card-*, lsl-tick-*, entity-detail-panel, data-focal, data-halo)"
      via: "Playwright locator selectors against the rendered DOM"
      pattern: "data-testid|data-focal|data-halo"
    - from: "Operator visual smoke (gsd-browser)"
      to: "localhost:5173/viewer/coding"
      via: "Manual verification of AC#1-AC#5"
      pattern: "gsd-browser (navigate|screenshot|click|eval)"
---

<objective>
Close Phase 56.1 with three deliverables: (1) fix WR-01 by replacing the data-dependent `count === 1` assertion in `56-bidirectional-selection.spec.ts` with a range-based or deterministic-seed approach; (2) add a NEW `56.1-many-to-many.spec.ts` Playwright spec covering AC#1, AC#2, AC#3, AC#4, AC#5, AC#8, AC#9 via automated E2E (AC#6 + AC#7 are covered through source-grep gates from Plans 01-05 plus PATTERNS-LOCK); (3) durably lock the 7 evolved/new contracts (D-7) in `56.1-PATTERNS-LOCK.md` so future plans don't re-litigate. Then a `checkpoint:human-verify` task gates phase closure on operator visual smoke at `localhost:5173/viewer/coding` per CLAUDE.md `gsd-browser` rule — Phase 56's smoke-loop history makes this mandatory.

Purpose: Phase 56.1 cannot ship without operator visual confirmation that the bidirectional many-to-many cascade actually fires on a live data set. The 4 Phase 56 deferred Warnings (WR-01/02/03/04) each have a closure home — WR-02 in Plan 02, WR-03 in Plan 05, WR-04 in Plan 01, WR-01 here. Locking the 7 contracts as a durable artifact prevents future plans from quietly regressing them under refactor pressure.

Output: WR-01 closure in the existing Phase 56 spec; NEW Phase 56.1 spec for the bidirectional cascade (covering AC#1, AC#2, AC#3, AC#4, AC#5, AC#8, AC#9 via E2E); PATTERNS-LOCK doc; operator-approved visual smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-CONTEXT.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md
@.planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-PATTERNS.md
@.planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-REVIEW.md
@tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts
@tests/e2e/unified-viewer/entity-detail.spec.ts
@CLAUDE.md
@playwright.config.ts
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-01-SUMMARY.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-02-SUMMARY.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-03-SUMMARY.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-04-SUMMARY.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-05-SUMMARY.md

<interfaces>
E2E driver selectors (seeded by Plans 01-05):

- `[data-testid="viewer-canvas"]` — graph SVG container
- `.node[data-focal="true"]` — focal node (Plan 03 marker)
- `.node[data-halo="true"]` — halo node (Plan 03 marker)
- `.node[data-node-id]` — every node element exposes its id (D3GraphCanvas convention — verify exists; if not, fall back to `.selection-ring.focal` + DOM lineage to read id)
- `[stroke="#ff0000"]` — focal-ring inline attr from Plan 03's `applySelectionStyling`
- `[stroke="#60a5fa"]` — halo-ring inline attr
- `[data-testid="bucket-card-list"]` — multi-mode sidebar container (Plan 04)
- `[data-testid^="bucket-card-"]` — individual card click target (Plan 04)
- `[data-testid="entity-detail-panel"]` — single-focal sidebar (Plan 04 SidePanel mode-switch)
- `[data-testid^="lsl-tick-"]` — timeline tick (Plan 05)
- `.timeline-bucket.halo` OR `[data-halo="true"]` on tick — halo bucket marker (Plan 05 two-tier render — match whichever marker the strip code emits)
- `.ring-blue-500` — focal tick ring (Plan 05 — preserved from Phase 56)
- `.ring-blue-300` — halo tick ring (Plan 05)
- `.bg-blue-200` — halo tick fill (Plan 05)

Playwright project: `unified-viewer` (per playwright.config.ts — baseURL http://localhost:5173, testDir tests/e2e/unified-viewer, run via `npx playwright test --project=unified-viewer`).

CLAUDE.md visual-smoke rule: use `gsd-browser` for any manual visual verification (NOT inline chromium scripts). Structured E2E specs go via `npx playwright test`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WR-01 + add Phase 56.1 E2E spec + lock 7 contracts in PATTERNS-LOCK.md</name>
  <files>tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts, tests/e2e/unified-viewer/56.1-many-to-many.spec.ts, .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md</files>
  <read_first>
    - tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts (full — find the `count === 1` assertion that WR-01 flagged; the test sets up tick click and asserts `.ring-blue-500` count exactly 1, which is data-dependent)
    - tests/e2e/unified-viewer/entity-detail.spec.ts (full — the canonical pattern for driving graph selection through the store via page.evaluate())
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md §8 (WR-01 fix — range-based or deterministic-seed) + §9 (NEW E2E spec template covering AC#1-AC#9)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-CONTEXT.md `<acceptance_criteria>` (AC #1 through AC #9 — full enumeration; AC#2 = "Click a graph node → timeline shows existing red ring on focal-touching bucket + lighter blue tint on all other buckets where the node was touched; sidebar shows EntityDetailPanel for the focal")
    - .planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-REVIEW.md (WR-01 root cause — data-dependent assertion holds only when seed puts a single tick in the selected bucket range)
    - playwright.config.ts (the `unified-viewer` project shape + baseURL)
  </read_first>
  <acceptance_criteria>
    - 56-bidirectional-selection.spec.ts no longer contains `toHaveCount(1` immediately for the `.ring-blue-500` selector. The replacement uses `toBeGreaterThanOrEqual(1)` plus a `toBeLessThan(10)` sanity upper bound, OR a deterministic seed that justifies count===1.
    - 56.1-many-to-many.spec.ts file exists, has a `test.describe('Unified Viewer — Phase 56.1 many-to-many bridge')` block, contains at minimum 7 tests covering AC#1, AC#2 (dedicated reverse-direction test), AC#3+AC#4 (combined drill-collapse), AC#5, AC#8, AC#9.
    - The AC#2 test clicks a graph node with multiple touching buckets and asserts: focal ring on graph node, ≥1 halo tick on timeline, EntityDetailPanel visible AND bucket-card-list NOT visible.
    - 56.1-PATTERNS-LOCK.md file exists with all 7 contracts named (#1 tick-ring, #2 graph-side, #3 viewport stability, #4 0-obs tick policy, #5 single source of truth, #6 phantom-id resolution multi, #7 pre-index integrity) + the maintenance clause.
    - Command `npx playwright test --project=unified-viewer --list` discovers both spec files without parse errors.
    - The Phase 56.1 spec's AC#9 source-grep test asserts pickAllResolvable callsites are limited to ancestry.ts, useNodeToBucketsIndex.ts, and LslTimelineStrip.tsx.
    - No `console.*` calls in either new/edited spec file.
  </acceptance_criteria>
  <action>
    Three subtasks, executed in order.

    **(a) Edit `tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts` to close WR-01.**

    Locate the assertion that grep-matches `toHaveCount(1` on the `.ring-blue-500` selector. The exact line was emitted by Phase 56's E2E author (around the middle of the file — confirm via grep before editing). Replace the single-line `toHaveCount(1, ...)` assertion with a four-line range-based block per 56.1-PATTERNS.md §8:

    Pseudocode for the replacement:
    - Create a const `tickRings` locator for `[data-testid^="lsl-tick-"].ring-blue-500`.
    - `await expect(tickRings).not.toHaveCount(0, { timeout: 5_000 })` — at least one ring must appear.
    - Read the actual count via `await tickRings.count()`.
    - Assert `count >= 1` AND `count < 10` (sanity upper bound — anything beyond is a bug).

    Add a header comment block above the test referencing WR-01 + 56-REVIEW.md. The wording must mention "Phase 56.1 WR-01 fix" so a grep can find the closure site.

    **(b) Create `tests/e2e/unified-viewer/56.1-many-to-many.spec.ts`** following PATTERNS.md §9 + entity-detail.spec.ts's `test.describe` + `page.goto('/viewer/coding')` driver pattern.

    Implementation per CONTEXT.md AC#1-AC#9:

    - `test.beforeEach`: navigate to `/viewer/coding`, wait for `viewer-canvas` visible, then poll for `document.querySelectorAll('.node').length > 0` via `page.evaluate` (the D3 canvas renders `.node` elements per Plan 03). Timeout 30s. Use a small helper `waitForBucketsLoaded(page)` that also polls for at least one `[data-testid^="lsl-tick-"]:not([aria-disabled="true"])` to be present so multi-bucket reverse-direction tests are deterministic.

    - `test('AC #1: tick click -> multi-ring graph halo + focal + bucket card list')`: click first `[data-testid^="lsl-tick-"]:not([aria-disabled="true"])`. Wait 200ms. Assert exactly 1 `.node[data-focal="true"]` exists. Assert `.node[data-halo="true"]` count >= 0 (graceful — data-dependent how many halo nodes). Assert `[data-testid="bucket-card-list"]` is visible within 3s.

    - `test('AC #2: graph node click → multi-tick timeline halo + EntityDetailPanel mount')` — **NEW dedicated reverse-direction test (revision):** Clear any prior state with `Escape`. Pick a node deterministically — the simplest strategy is to click the FIRST `.node[data-node-id]` (or the first `.node` element if `data-node-id` is absent — fall back to reading the id from the DOM-tree). After click, wait 200ms. Assertions:
       - Focal ring on graph: `await expect(page.locator('.node[data-focal="true"]')).toHaveCount(1)`.
       - Read the focal node's id back via `page.evaluate` for trace data only — not asserted (deterministic-seed not assumed).
       - Halo ticks on timeline: locate `[data-testid^="lsl-tick-"].ring-blue-300, [data-testid^="lsl-tick-"][data-halo="true"], [data-testid^="lsl-tick-"].ring-blue-500` (use whichever marker the Plan 05 strip code emits — try each; the OR selector handles either implementation). Assert `count >= 1` with a 2s timeout. **If the clicked node has zero touching buckets** the assertion may legitimately fail — in that case the test must retry with a different node (loop over the first 5 `.node` elements, click each, capture screenshot, and accept the first one that produces ≥1 halo tick). If all 5 fail, throw with a clear message indicating the seeded data has no graph-to-bucket touches — the test fixture needs updating, not the implementation.
       - EntityDetailPanel visible: `await expect(page.locator('[data-testid="entity-detail-panel"]')).toBeVisible({ timeout: 3000 })`.
       - bucket-card-list NOT visible: `await expect(page.locator('[data-testid="bucket-card-list"]')).not.toBeVisible()`.
       Reference selector strategy in comments: cite PATTERNS.md §9 + this revision (the dedicated AC#2 test was added during planner revision iteration 1 — record this in the test header so future readers know it's load-bearing).

    - `test('AC #3 + #4: card click + halo node click -> drill collapse')`: setup as AC#1; click first `[data-testid^="bucket-card-"]`. Wait 200ms. Assert exactly 1 focal node, ZERO halo nodes (halo collapsed). Assert `[data-testid="entity-detail-panel"]` visible. Assert `[data-testid="bucket-card-list"]` NOT visible.

    - `test('AC #5: Esc clears all selection (graph + timeline + sidebar)')`: setup as AC#1; press `Escape`. Wait 200ms. Assert focal + halo counts both 0. Assert `bucket-card-list` not visible.

    - `test('AC #8: viewport stable on tick click — no zoom/pan')`: Capture `transform` attr of the graph's outer `<g>` element BEFORE click. Click a tick. Wait 300ms. Re-read the `transform`. Assert before === after. This is the Phase 56 audit Locked Contract #3 viewport-stability live smoke.

    - `test('AC #9: pre-index integrity — pickAllResolvable callsites limited to allowed sources')`: This is a source-grep gate executed inside the spec (not a runtime browser check). Use `require('fs')` + `require('path')` (top-level in the spec). Recursively walk `process.cwd() + '/integrations/unified-viewer/src/'`. For each `.ts` or `.tsx` file (excluding `*.test.*` and `*.spec.*`), readFileSync and test for `/\bpickAllResolvable\b/`. Build an allowed Set containing the three approved paths (`ancestry.ts`, `useNodeToBucketsIndex.ts`, `LslTimelineStrip.tsx`). Filter found callsites against allowed; assert the unexpected list is empty.

    The AC#2 test goes BEFORE the AC#3+4 test in the file — reverse direction is the second confirmation; forward direction (AC#1) is first; drill collapse (AC#3+4) is third.

    Do NOT use `console.*` anywhere. Do NOT introduce a `data-testid` attribute that Plans 03/04/05 didn't seed (cross-check the `<interfaces>` block above for the canonical selector list).

    **(c) Create `.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md`** — durable contract artifact mirroring 56-PATTERNS.md "Locked design contracts" section format.

    Required sections:

    - Header: "Phase 56.1 PATTERNS LOCK — 7 evolved/new contracts" + creation date + scope statement ("Durable reference for any future plan extending or refactoring the many-to-many bridge. Any relaxation of a contract below requires the relaxer's SUMMARY to justify the deviation against the cited source-of-truth.").

    - Contract #1 (evolved per D-7 row 1): Tick-ring predicate. Halo: `selectedBucketKeys.has('${X.id}|${X.startAt}')`. Focal: `focalBucketKey === '${X.id}|${X.startAt}'`. Reverse cascade preserved (selectedTs fallback). Implementation cite: `LslTimelineStrip.tsx` two-tier render block (Plan 05 Task 1 step 8).

    - Contract #2 (evolved per D-7 row 2): Graph-side selection — focal red ring on `focalNodeId`, halo lighter blue ring on `selectedNodeIds` minus focal. pathToSelected trace drawn from focalNodeId ONLY. Implementation cite: `D3GraphCanvas.tsx applySelectionStyling` (Plan 03 Task 1 step 2). Source-grep gate: G20 in `D3GraphCanvas.test.ts`.

    - Contract #3 (UNCHANGED — AUDIT-LOCKED): Viewport stability. Main render-effect dep list verbatim `[visibleEntities, visibleRelations, theme, isLoading]`. NEVER contains any selection field. Source-grep gate: G9 in D3GraphCanvas.test.ts. Implementation cite: D3GraphCanvas.tsx main render useEffect (Plan 03 preserves verbatim). Live E2E gate: AC#8 in 56.1-many-to-many.spec.ts (viewport transform before === after).

    - Contract #4 (UNCHANGED — AUDIT-LOCKED): 0-obs tick policy. Sessions with `observationCount === 0` render with `opacity-40 pointer-events-none cursor-default`, carry `aria-disabled="true"`, are NOT selectable. onTickClick early-exits before any store write. Implementation cite: LslTimelineStrip.tsx 0-obs branch (preserved verbatim in Plan 05). Source-grep gate (NEW per Plan 05): LslTimelineStrip.test.tsx Test 12 asserts `/opacity-40[^']*pointer-events-none/`.

    - Contract #5 (UNCHANGED in spirit; field renames propagate): Single source of truth. NO local React state shadows a selection-related store field. All cross-pane writes route through `setSelection` / `clearSelection`. Acceptance grep: zero `useViewerStore.setState({` callsites in consumer production paths. Verified via Plan 03 G17 (D3GraphCanvas) + Plan 04 (SidePanel close-button migration + BucketCardList + HistorySidebar migrations).

    - Contract #6 (evolved per D-7 row 6): Phantom-id resolution. Every member of `selectedNodeIds` (and every focal write) satisfies `visibleIds.has(id)`. The resolution helper `pickAllResolvable` returns `Set<string>` of only visible-ancestor resolutions. Implementation cite: `ancestry.ts pickAllResolvable` (Plan 02). Phase 56's `pickFirstResolvable` contract preserved unchanged for backward compatibility.

    - Contract #7 (NEW): Pre-index integrity. `nodeToBuckets: Map<string, Set<string>>` is rebuilt iff `sessions`, `visibleIds`, or `relations` change. The hook `useNodeToBucketsIndex` is the canonical consumer; `LslTimelineStrip.onTickClick` is the canonical forward-direction handler. Acceptance grep: pickAllResolvable callsites in src/ are limited to `ancestry.ts` (definition), `useNodeToBucketsIndex.ts` (pre-index build), and `LslTimelineStrip.tsx` (forward-direction click handler). Any 4th callsite is a smell. Source-grep gate: AC#9 in `56.1-many-to-many.spec.ts`. Implementation cite: `useNodeToBucketsIndex.ts` (Plan 05 Task 1 step 2-3).

    - Maintenance contract: any future plan that relaxes one of these contracts MUST justify the relaxation in its own SUMMARY against the cited source-of-truth (CONTEXT.md D-7 evolution table; 56-PATTERNS.md Locked Contracts #1-#6; 56-STATE-FLOW.md audit; 56.1-PATTERNS.md §"Locked Design Contracts").

    Finish with a "References" section listing the parent docs by absolute path within `.planning/`.
  </action>
  <verify>
    <automated>ROOT="$(git rev-parse --show-toplevel)"; cd "$ROOT" && npx playwright test --project=unified-viewer --list 2>&1 | grep -E "56\.1-many-to-many|56-bidirectional" | head -10; grep -c "toHaveCount(1," tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts || true; ls .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md; grep -c "AC #2:" tests/e2e/unified-viewer/56.1-many-to-many.spec.ts</automated>
    <expectation>playwright --list: both spec file names listed (one or more tests each — at minimum 7 tests for 56.1-many-to-many). grep "toHaveCount(1,": 0 (the data-dependent assertion was removed) — note grep returns non-zero exit on zero-match, hence `|| true`. ls: file exists. grep "AC #2:": ≥ 1 (the dedicated reverse-direction test exists).</expectation>
  </verify>
  <done>
    - WR-01 fixed in 56-bidirectional-selection.spec.ts (range-based assertion)
    - 56.1-many-to-many.spec.ts exists with ≥ 7 tests covering AC#1, AC#2, AC#3+4, AC#5, AC#8, AC#9
    - AC#2 dedicated E2E test exists and asserts focal node ring + ≥1 halo tick + EntityDetailPanel visible + bucket-card-list NOT visible
    - 56.1-PATTERNS-LOCK.md exists with all 7 contracts + maintenance clause + references
    - npx playwright test --list discovers both specs
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator visual smoke at localhost:5173/viewer/coding — confirm AC#1-AC#5 + AC#8 via gsd-browser</name>
  <files>N/A — operator-only checkpoint, no source files modified; screenshots emitted to /tmp/56.1-smoke-*.png</files>
  <action>See &lt;how-to-verify&gt; below — operator runs the 8-step gsd-browser recipe and reports outcome via the resume signal.</action>
  <verify>
    <human-check>Operator confirms via resume signal that AC#1-AC#5 + AC#8 visual checks all pass per the &lt;how-to-verify&gt; recipe. Screenshots saved at /tmp/56.1-smoke-{00,01,02,03,04,05}.png.</human-check>
  </verify>
  <done>Operator types "approved" in the resume signal — Phase 56.1 is then phase-complete. Any non-approve resume signal returns to planner for revision.</done>
  <what-built>
    Phase 56.1 deliverables across Plans 01-05 + this plan's E2E spec + PATTERNS-LOCK doc. The full many-to-many cascade:
    - Click a timeline tick → graph shows red focal + lighter blue halo on touched nodes; sidebar shows History-style card list of bucket entities; path-to-CK trace from focal.
    - Click a graph node → timeline shows red focal-bucket ring + lighter blue halo on all other buckets where the node was touched; sidebar shows EntityDetailPanel for the focal.
    - Click a card in the sidebar list → halo collapses; focal becomes the clicked id; sidebar switches to EntityDetailPanel.
    - Click a halo node in the graph → same drill collapse behavior.
    - Esc clears every selection field.
    - Viewport stays put — no zoom/pan on tick/node click.
  </what-built>
  <how-to-verify>
    Per CLAUDE.md `prefer-gsd-browser` rule, use the `gsd-browser` CLI (NOT inline chromium scripts). Recommended invocation:

    Step 1: Start the dev server.
    ```
    cd /Users/Q284340/Agentic/coding/integrations/unified-viewer && npm run dev
    ```
    Verify the Vite server starts on port 5173. (If port collision, adjust the URL below.)

    Step 2: Navigate to the coding viewer.
    ```
    gsd-browser navigate http://localhost:5173/viewer/coding
    ```
    Wait 5-10s for graph data + LSL sessions to load.

    Step 3: Capture baseline screenshot.
    ```
    gsd-browser screenshot /tmp/56.1-smoke-00-baseline.png
    ```

    Step 4: AC#1 (tick click → multi-ring + card list). Click the first non-disabled timeline tick.
    ```
    gsd-browser click '[data-testid^="lsl-tick-"]:not([aria-disabled="true"])'
    gsd-browser screenshot /tmp/56.1-smoke-01-tick-click.png
    ```
    Verify in the screenshot:
    - Graph: exactly one node has a red ring (focal). Other touched nodes have a lighter blue halo ring.
    - Sidebar: shows a list of cards (NOT the EntityDetailPanel single-entity view). The cards have entity names + class chips + relative time.
    - Path-to-CK trace: faint blue lines from the red-focal node toward the CollectiveKnowledge root.

    Step 5: AC#3 (card click → drill collapse). Click the first bucket card.
    ```
    gsd-browser click '[data-testid^="bucket-card-"]'
    gsd-browser screenshot /tmp/56.1-smoke-02-card-drill.png
    ```
    Verify:
    - Graph: exactly one node has a red ring (the clicked card's id). ZERO halo rings remain.
    - Sidebar: switched to EntityDetailPanel (no more card list).

    Step 6: AC#2 (graph node click → multi-tick timeline). First clear, then click a graph node.
    ```
    gsd-browser eval "document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }))"
    gsd-browser screenshot /tmp/56.1-smoke-03-after-esc.png
    gsd-browser click '.node'
    gsd-browser screenshot /tmp/56.1-smoke-04-node-click.png
    ```
    Verify in step 04 screenshot:
    - Graph: one red focal ring on the clicked node.
    - Timeline: the buckets that touch the clicked node have visible halo (lighter blue tint) or focal (existing blue-500) rings. If no buckets touch the node (graph-only entity), the timeline stays unchanged — that's also correct behavior.
    - Sidebar: shows EntityDetailPanel for the clicked node.

    Step 7: AC#5 (Esc clears all). Press Esc.
    ```
    gsd-browser eval "document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }))"
    gsd-browser screenshot /tmp/56.1-smoke-05-after-esc-2.png
    ```
    Verify:
    - Graph: no red ring, no halo rings.
    - Timeline: no focal ring, no halo tints (only the default pink tick fill on non-zero buckets).
    - Sidebar: shows the default no-selection HistorySidebar.

    Step 8: AC#8 (viewport stability). Pick a tick, capture the graph's `<g transform>` before + after.
    ```
    gsd-browser eval "JSON.stringify({ before: document.querySelector('svg g')?.getAttribute('transform') })"
    gsd-browser click '[data-testid^="lsl-tick-"]:not([aria-disabled="true"])'
    gsd-browser eval "JSON.stringify({ after: document.querySelector('svg g')?.getAttribute('transform') })"
    ```
    Verify before === after. If they differ, the viewport-stability invariant (Locked Contract #3) is broken — STOP and reject closure.

    Resume signal expectations:
    - "approved" — every screenshot matches the description above. Phase 56.1 ships.
    - Detailed description of any anomaly + screenshot path — the planner returns to revise the relevant plan.
  </how-to-verify>
  <resume-signal>Type "approved" if all AC#1-AC#5 + AC#8 visual checks pass. Otherwise describe the anomaly (which AC failed, paste screenshot path or visible class/data-attribute mismatch).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test environment | Playwright drives a real chromium against a live Vite dev server. Trust boundary is the seeded test data — assumes localhost service is the project's own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56.1-06-01 | Tampering | Phase 56 E2E spec quietly regresses to data-dependent assertion after merge | mitigate | Task 1(a) explicitly removes the `toHaveCount(1` pattern. The verify step's grep asserts zero matches. |
| T-56.1-06-02 | Information Disclosure | Operator visual smoke screenshots may capture entity names/content from live data | accept | Smoke screenshots are written to /tmp/ which is operator-local; they are not committed. If the operator wishes to share screenshots, they sanitize per their judgment. |
| T-56.1-06-03 | Denial of Service | E2E spec hangs on a slow graph load → 30s timeout cascade | mitigate | beforeEach uses toPass({ timeout: 30_000 }) — a generous but bounded wait. Failure surfaces as a fail rather than hang. |
| T-56.1-06-04 | Tampering | PATTERNS-LOCK.md drift — a future planner edits a contract without updating the source-of-truth cites | mitigate | Maintenance clause requires SUMMARY justification. CI doesn't enforce this; relies on planner review discipline. Acceptable risk. |
| T-56.1-06-05 | Tampering | Operator skips the visual smoke (rubber-stamps approval) | mitigate | Task 2 is `checkpoint:human-verify gate="blocking"`. The 8-step recipe is concrete enough that skipping is obvious to a code reviewer. Plan 06 SUMMARY should attach screenshots or note their paths. |
| T-56.1-06-06 | Tampering | AC#2 reverse-direction test omitted — feature ships but no automated coverage for graph-click → multi-tick halo + EntityDetailPanel | mitigate | Task 1(b) now mandates a dedicated AC#2 test BEFORE AC#3+4 (revision iteration 1 fix). The acceptance_criteria block enforces "≥7 tests" so omitting AC#2 fails the verify step. |
| T-56.1-06-SC | Tampering | npm install (none added) | mitigate | Phase 56.1 adds ZERO npm dependencies. No package legitimacy gate. |

</threat_model>

<verification>
1. Task 1(a): `grep -c "toHaveCount(1," tests/e2e/unified-viewer/56-bidirectional-selection.spec.ts || true` — output 0 (WR-01 fix).
2. Task 1(b): `npx playwright test --project=unified-viewer --list 2>&1 | grep -c "Phase 56.1 many-to-many bridge"` — output ≥ 1.
3. Task 1(b) AC#2 coverage: `grep -c "AC #2:" tests/e2e/unified-viewer/56.1-many-to-many.spec.ts` — output ≥ 1 (dedicated reverse-direction test).
4. Task 1(c): `test -f .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md && grep -c "Contract #7" .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS-LOCK.md` — file exists + ≥ 1 mention of Contract #7.
5. Task 2: operator types "approved" after the 8-step visual smoke.
</verification>

<success_criteria>
- 56-bidirectional-selection.spec.ts WR-01 closed: range-based assertion replaces count===1
- 56.1-many-to-many.spec.ts exists with ≥ 7 tests covering AC#1, AC#2, AC#3+4, AC#5, AC#8, AC#9
- AC#2 dedicated E2E test asserts focal node + ≥1 halo tick + EntityDetailPanel visible + bucket-card-list NOT visible
- npx playwright test --project=unified-viewer --list discovers both specs without parse errors
- 56.1-PATTERNS-LOCK.md exists with all 7 contracts + maintenance clause + references to source-of-truth docs
- Operator visual smoke at localhost:5173/viewer/coding passes — AC#1 through AC#5 + AC#8 verified per the gsd-browser recipe
- Operator resume signal "approved"
- WR-01 + AC#1, AC#2, AC#3, AC#4, AC#5, AC#8, AC#9 (E2E) + AC#6, AC#7 (source-grep from prior plans + PATTERNS-LOCK) + D-7 contract #7 closed in this plan
</success_criteria>

<output>
Create `.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-06-SUMMARY.md` when done. Summarise: (a) WR-01 fix (the old/new assertion delta); (b) 56.1-many-to-many.spec.ts test inventory + which ACs each covers (explicitly enumerate AC#1, AC#2, AC#3, AC#4, AC#5, AC#8, AC#9 by test title); (c) 56.1-PATTERNS-LOCK.md contract count + key sections; (d) operator smoke screenshot paths + the resume-signal verdict; (e) one-line phase closure declaration referencing all 4 deferred Warnings (WR-01 here, WR-02 in Plan 02, WR-03 in Plan 05, WR-04 in Plan 01) as closed.
</output>
</output>
