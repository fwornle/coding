---
phase: 56.1-unified-viewer-many-to-many-bridge
plan: 04
type: execute
wave: 2
depends_on:
  - 56.1-01
files_modified:
  - integrations/unified-viewer/src/panels/SidePanel.tsx
  - integrations/unified-viewer/src/panels/BucketCardList.tsx
  - integrations/unified-viewer/src/panels/BucketCardList.test.tsx
  - integrations/unified-viewer/src/panels/HistorySidebar.tsx
autonomous: true
requirements:
  - D-4-sidebar
  - D-5-drill-collapse
  - D-7-contract-5
tags:
  - unified-viewer
  - sidebar
  - selection-rendering

must_haves:
  truths:
    - "BucketCardList.tsx is a NEW component that renders a History-style card list (cloned from HistorySidebar.tsx:181-214 visual rhythm) for multi-mode sidebar content"
    - "BucketCardList card click drills into single-focal mode: setSelection({ nodeIds: new Set([cardId]), bucketKeys: empty Set, focal: { nodeId: cardId, bucketKey: null }, source: 'history', ... })"
    - "SidePanel.tsx Entity-tab content uses a mode-switch predicate: isMultiMode (selectedBucketKeys.size > 0 OR selectedNodeIds.size > 1) → BucketCardList; isSingleFocalMode (selectedNodeIds.size === 1 && selectedBucketKeys.size === 0) → EntityDetailPanel; else → HistorySidebar (no-selection default)"
    - "SidePanel.tsx is the SOLE OWNER of all SidePanel.tsx edits in wave 2: (a) line-45 subscription migrated to focalNodeId; (b) entity useMemo (lines 49-52) rekeys on focalNodeId; (c) close button (lines 113-124) routes through clearSelection(); (d) Entity TabsContent body uses three-way mode-switch"
    - "HistorySidebar.tsx onClick handler (lines 117-132) migrated to the new setSelection arg shape (nodeIds: new Set([id]) + bucketKeys: new Set() + focal: { nodeId: id, bucketKey: null } — Phase 56's nodeId/sessionId/sessionStartAt args are gone)"
    - "BucketCardList Logger discipline: Logger.info(Logger.Categories.PANELS, ...) on click; ZERO console.* in source"
    - "Acceptance grep: zero useViewerStore.setState({ in BucketCardList.tsx, HistorySidebar.tsx, SidePanel.tsx production paths (Locked Contract #5)"
    - "BucketCardList carries data-testid='bucket-card-list' for E2E observability; each card carries data-testid='bucket-card-${id}' for per-card click"
  artifacts:
    - path: "integrations/unified-viewer/src/panels/BucketCardList.tsx"
      provides: "Multi-mode sidebar card list — bucket-driven or graph-multi-driven content"
      contains: "data-testid=\"bucket-card-list\""
    - path: "integrations/unified-viewer/src/panels/BucketCardList.test.tsx"
      provides: "Vitest RTL coverage — empty state, card list render, click drill, Logger discipline"
      contains: "BucketCardList"
    - path: "integrations/unified-viewer/src/panels/SidePanel.tsx"
      provides: "All SidePanel migrations: focalNodeId subscription + entity useMemo rekey + close-button → clearSelection + three-way mode-switch (BucketCardList / EntityDetailPanel / HistorySidebar)"
      contains: "isMultiMode"
    - path: "integrations/unified-viewer/src/panels/HistorySidebar.tsx"
      provides: "onClick handler migrated to Phase 56.1 setSelection arg shape"
      contains: "nodeIds: new Set"
  key_links:
    - from: "SidePanel.tsx Entity TabsContent body"
      to: "BucketCardList / EntityDetailPanel / HistorySidebar based on predicate"
      via: "isMultiMode + isSingleFocalMode mode-switch"
      pattern: "isMultiMode|isSingleFocalMode"
    - from: "SidePanel.tsx close button onClick (lines 113-124)"
      to: "useViewerStore.getState().clearSelection()"
      via: "Phase 56 audit §8 opportunistic compliance — closes the deferred inline setState site"
      pattern: "clearSelection\\(\\)"
    - from: "BucketCardList.tsx onCardClick"
      to: "useViewerStore.getState().setSelection({...})"
      via: "drill collapse: nodeIds=Set([id]), bucketKeys empty, focal=id"
      pattern: "setSelection\\(\\{[\\s\\S]*nodeIds:\\s*new Set"
---

<objective>
Build `BucketCardList.tsx` — a NEW multi-mode sidebar component cloned from `HistorySidebar.tsx`'s card rhythm (lines 181-214). The list renders cards in TWO modes per CONTEXT.md §D-4 / discretion #4: (a) `selectionSource === 'timeline'` + `selectedBucketKeys.size > 0` → cards = entities from the union of `selectedBucketKeys`' raw entityIds; (b) `selectionSource === 'graph'` + `selectedNodeIds.size > 1` → cards = entities from the buckets touching `focalNodeId` (the buckets in `nodeToBuckets.get(focalNodeId)` — but `nodeToBuckets` doesn't exist yet, so initial render falls back to entities from `selectedNodeIds` itself; Plan 05 hooks up the bucket-driven branch). Cards click-drill into single-focal mode (D-5) — `selectedNodeIds = new Set([cardId])`, `selectedBucketKeys` empties, focal becomes the clicked id.

Refactor `SidePanel.tsx` end-to-end (sole owner of SidePanel.tsx in wave 2 — Plan 03 explicitly does NOT touch SidePanel.tsx to keep the two plans' file scopes disjoint and parallel-safe):
- Migrate the line-45 subscription `selectedNodeId` → `focalNodeId`.
- Rekey the entity useMemo at lines 49-52 from `selectedNodeId` to `focalNodeId`.
- Add multi-mode subscriptions (`selectedNodeIds`, `selectedBucketKeys`).
- Migrate the close-button onClick (lines 113-124) from inline `useViewerStore.setState({ ... })` to `useViewerStore.getState().clearSelection()` — closes the Phase 56 audit §8 opportunistic compliance site.
- Replace the Entity TabsContent body with the three-way mode-switch (`BucketCardList` / `EntityDetailPanel` / `HistorySidebar`).

Migrate `HistorySidebar.tsx` onClick handler to the new setSelection arg shape (`nodeIds: Set` instead of `nodeId: string` — Plan 01 deleted the scalar arg).

Purpose: Closes the sidebar side of the many-to-many bridge. After this plan + Plan 03, the sidebar correctly renders multi-selection content even though Plan 05 hasn't yet wired the timeline-driven multi-write — internal consistency before integration.

Output: NEW `BucketCardList.tsx` + `BucketCardList.test.tsx`. Comprehensive `SidePanel.tsx` migration (subscription + entity useMemo + close button + mode-switch). `HistorySidebar.tsx` onClick migrated to new arg shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-CONTEXT.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md
@.planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-PATTERNS.md
@integrations/unified-viewer/src/panels/SidePanel.tsx
@integrations/unified-viewer/src/panels/HistorySidebar.tsx
@integrations/unified-viewer/src/panels/EntityDetailPanel.tsx
@integrations/unified-viewer/src/store/viewer-store.ts
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-01-SUMMARY.md

<interfaces>
Consumes (from Plan 01 store):
```typescript
useViewerStore((s) => s.selectedNodeIds)       // ReadonlySet<string>
useViewerStore((s) => s.focalNodeId)           // string | null
useViewerStore((s) => s.selectedBucketKeys)    // ReadonlySet<string>
useViewerStore((s) => s.selectionSource)       // 'graph' | 'timeline' | 'history' | null
useViewerStore.getState().setSelection({ nodeIds, bucketKeys, focal, source, pathToSelected?, lslSessionFilter?, lslFilterEntityIds? })
useViewerStore.getState().clearSelection()
```

Existing import surface to consume in BucketCardList:
```typescript
// HistorySidebar.tsx — card render lines 181-214 (the visual rhythm to clone)
// useGraphData(apiClient, system) — returns { entities, relations, ontology, isLoading, error } from @/graph/useGraphData
// Logger from @/lib/logging — Logger.info(Logger.Categories.PANELS, ...)
```

Mode-switch predicate (CONTEXT.md `<specifics>`):
```typescript
const isMultiMode = selectedBucketKeys.size > 0 || selectedNodeIds.size > 1
const isSingleFocalMode = selectedNodeIds.size === 1 && selectedBucketKeys.size === 0
// Otherwise: no-selection default (HistorySidebar)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BucketCardList.tsx + BucketCardList.test.tsx</name>
  <files>integrations/unified-viewer/src/panels/BucketCardList.tsx, integrations/unified-viewer/src/panels/BucketCardList.test.tsx</files>
  <read_first>
    - integrations/unified-viewer/src/panels/HistorySidebar.tsx (full — clone the card visual rhythm at lines 181-214 + the `useGraphData` hook usage pattern + Logger discipline)
    - integrations/unified-viewer/src/panels/HistorySidebar.test.tsx (skim — testing idioms for the QueryClientProvider + fetch mock pattern; mirror the same RTL setup)
    - integrations/unified-viewer/src/store/viewer-store.ts (post-Plan-01 — confirms setSelection arg shape)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md §6 (BucketCardList — analog source + click handler + data source mode-driven content) + §C (Logger discipline) + §E (test boilerplate)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-CONTEXT.md §D-4 (sidebar visual) + §D-5 (drill collapse semantics) + `<discretion>` #4 (selectionSource interpretation in multi-mode)
  </read_first>
  <behavior>
    - BucketCardList renders nothing when isMultiMode predicate is false (empty Set + empty Set, or single-focal mode)
    - BucketCardList in 'timeline' mode (selectionSource='timeline' AND selectedBucketKeys.size > 0): cards = entities derived from union of bucket entityIds across selectedBucketKeys. Initial implementation: render entities found via useGraphData() entities.filter(e => some bucket in selectedBucketKeys contains e.id). Bucket data source (sessions cache) is not loaded by this component yet — use the entity ids in lslFilterEntityIds Set as a proxy for "entities in selected buckets" (already populated by timeline plain-click in Phase 56 / Plan 05). When lslFilterEntityIds is null, render an empty-state placeholder.
    - BucketCardList in 'graph' mode (selectionSource='graph' AND selectedNodeIds.size > 1): cards = entities in selectedNodeIds (a graph multi-select produces cards for each selected node). Plan 05 may override this to "buckets touching focal" via the reverse-index hook when that hook ships — but for now (Wave 2), this provides a coherent display.
    - Each card click writes setSelection({ nodeIds: new Set([cardId]), bucketKeys: new Set(), focal: { nodeId: cardId, bucketKey: null }, source: 'history', highlightedRowKey: cardId, pathToSelected: new Set(), lslSessionFilter: [], lslFilterEntityIds: null }) — full drill collapse per D-5
    - BucketCardList carries data-testid='bucket-card-list' on the outer container
    - Each card button carries data-testid='bucket-card-${id}'
    - On click, Logger.info(Logger.Categories.PANELS, ...) fires with the drill target id
    - Empty state ('No cards to show' or similar) renders when isMultiMode but the derived entity list is empty
    - ZERO raw console.* in BucketCardList.tsx source — source-grep gate test
    - ZERO inline useViewerStore.setState({ in BucketCardList.tsx — source-grep gate test
  </behavior>
  <action>
    Create `integrations/unified-viewer/src/panels/BucketCardList.tsx`:

    1) **Imports.** Clone HistorySidebar.tsx's import section (lines 1-15 approximately):
       - `import { useMemo } from 'react'`
       - `import type { ApiClient } from '@/api/ApiClient'`
       - `import type { System } from '@/config/system-endpoints'`
       - `import { useViewerStore } from '@/store/viewer-store'`
       - `import { useGraphData } from '@/graph/useGraphData'`
       - `import { Logger } from '@/lib/logging'`
       Do NOT import `useSessions` or anything else not currently available; Plan 05 may upgrade BucketCardList to consume `useNodeToBucketsIndex` when that hook ships.

    2) **Component signature:**
       ```typescript
       export interface BucketCardListProps {
         apiClient: ApiClient
         system: System
       }
       export function BucketCardList({ apiClient, system }: BucketCardListProps) { ... }
       ```

    3) **Store subscriptions** (use the (s) => s.field hook form — Phase 56 convention):
       - `const selectedNodeIds = useViewerStore((s) => s.selectedNodeIds)`
       - `const selectedBucketKeys = useViewerStore((s) => s.selectedBucketKeys)`
       - `const selectionSource = useViewerStore((s) => s.selectionSource)`
       - `const lslFilterEntityIds = useViewerStore((s) => s.lslFilterEntityIds)`
       - `const focalNodeId = useViewerStore((s) => s.focalNodeId)`

    4) **`items` useMemo** (cards to render): Build a list of `{ id, name, entityType, source: 'auto' | 'manual', createdAt: string, team: string }` shape mirroring HistorySidebar's HistoryItem. Derive from useGraphData().entities:
       - If `selectionSource === 'timeline' && selectedBucketKeys.size > 0 && lslFilterEntityIds !== null`: filter entities whose `e.id` is in `lslFilterEntityIds`. The display label is `e.name` + ontology class + `formatRelativeTime(e.createdAt ?? e.metadata?.createdAt)`.
       - Else if `selectionSource === 'graph' && selectedNodeIds.size > 1`: filter entities whose `e.id` is in `selectedNodeIds`.
       - Else: return empty array (the SidePanel mode-switch should not have mounted this component, but defensive).
       Use the entity's `createdAt` or `metadata.createdAt` for sorting (newest first — clone HistorySidebar's sort) and the entity's `ontologyClass` for the entityType label.
       Cards take a `source` field: 'auto' if entity has `metadata.discovery === 'auto'` (or any heuristic — for simplicity, default 'auto'); 'manual' otherwise. The visual chip in HistorySidebar lines 196-205 uses this — mirror it but err on the side of always 'auto' if the field is absent.

    5) **`onCardClick(id: string)` handler** (after items useMemo):
       ```typescript
       const onCardClick = (id: string) => {
         useViewerStore.getState().setSelection({
           nodeIds: new Set<string>([id]),
           bucketKeys: new Set<string>(),
           focal: { nodeId: id, bucketKey: null },
           pathToSelected: new Set<string>(),
           highlightedRowKey: id,
           source: 'history',
           lslSessionFilter: [],
           lslFilterEntityIds: null,
         })
         Logger.info(Logger.Categories.PANELS, `BucketCardList drill → ${id}`)
       }
       ```
       This is the D-5 drill-collapse contract — fully clear bucket selection, focal becomes cardId.

    6) **Render:** Outer `<div data-testid="bucket-card-list" className="h-full overflow-y-auto px-3 py-3">`. Inside:
       - A header `<div>` with `<h2>` "Selected" + `<p>` "${items.length} item${items.length === 1 ? '' : 's'}".
       - Empty state branch: if items.length === 0, render the same "No insights, digests, or observations to show." message HistorySidebar uses at lines 163-167. Vary text to "No cards to show." if the empty state should be distinct (cosmetic — either works).
       - List branch: `<ul className="space-y-1.5">` with `items.map(item => ...)`. The `<li>` body is verbatim cloned from HistorySidebar.tsx lines 181-214 (the button card with name + Auto/Manual chip + entityType + relative time + team). The `<button>` carries `data-testid={\`bucket-card-${item.id}\`}` and `onClick={() => onCardClick(item.id)}`.
       - Use a local `formatRelativeTime` helper if HistorySidebar exports one, otherwise inline a simple `(iso: string) => new Date(iso).toLocaleString()` fallback.

    Do NOT add a no-op cleanup useEffect. Do NOT log on render (only on click). Do NOT add new types to the global type catalog — keep types local.

    Create `integrations/unified-viewer/src/panels/BucketCardList.test.tsx`:

    7) Clone HistorySidebar.test.tsx's setup (imports + QueryClientProvider + fetch mock). Use `vi.mock('@/graph/useGraphData', () => ({ useGraphData: () => ({ entities: mockEntities, relations: [], ontology: [], isLoading: false, error: null }) }))` to bypass the network entirely.

    8) Tests to write:
       - `renders empty state when isMultiMode but no derived items` — set the store to selectionSource='timeline', selectedBucketKeys=new Set(['sess-X|t1']), lslFilterEntityIds=new Set() (empty). Render. Expect empty-state text.
       - `renders cards in timeline mode from lslFilterEntityIds` — set store: selectionSource='timeline', selectedBucketKeys=new Set(['sess-X|t1']), lslFilterEntityIds=new Set(['e1','e2']). mockEntities includes e1 + e2 (with valid name + createdAt). Render. Expect 2 cards visible by data-testid='bucket-card-e1' + 'bucket-card-e2'.
       - `renders cards in graph multi mode from selectedNodeIds` — set store: selectionSource='graph', selectedNodeIds=new Set(['e1','e2','e3']). mockEntities has e1+e2+e3. Render. Expect 3 cards.
       - `card click drills via setSelection (nodeIds=Set([id]) + bucketKeys empty + focal=id + source='history')` — set timeline-mode store. Click `bucket-card-e1`. Assert useViewerStore.getState().selectedNodeIds equals Set(['e1']); selectedBucketKeys.size === 0; focalNodeId === 'e1'; selectionSource === 'history'; lslSessionFilter === [] (length 0); lslFilterEntityIds === null.
       - `Logger discipline — no raw console.* in BucketCardList.tsx` — source-grep gate. readFileSync(path) + expect(src).not.toMatch(/console\\.(log|warn|error|info|debug|trace)/).
       - `Locked Contract #5 — no inline useViewerStore.setState({ in BucketCardList.tsx` — source-grep gate. expect(src).not.toMatch(/useViewerStore\\.setState\\(\\{/).
       - `source-grep: data-testid markers present` — expect(src).toMatch(/data-testid=["'`]bucket-card-list["'`]/) AND expect(src).toMatch(/data-testid=\{`bucket-card-\$\{/) (or equivalent).

    Use beforeEach to fully reset the store state (mirror HistorySidebar.test.tsx pattern). Call cleanup() between tests.
  </action>
  <verify>
    <automated>ROOT="$(git rev-parse --show-toplevel)"; cd "$ROOT/integrations/unified-viewer" && npm run tsc --noEmit 2>&1 | grep -E "BucketCardList" | head -5; cd "$ROOT" && npx vitest run integrations/unified-viewer/src/panels/BucketCardList.test.tsx 2>&1 | tail -20</automated>
    <expectation>tsc: zero errors in BucketCardList.tsx or .test.tsx. vitest: all tests pass (≥ 7 cases including 2 source-grep gates).</expectation>
  </verify>
  <done>
    - BucketCardList.tsx exists with the component signature + mode-driven items useMemo + onCardClick drill handler + data-testid markers
    - BucketCardList.test.tsx has ≥ 7 tests (4 behavior + 2 source-grep gates + 1 Logger discipline) all passing
    - Logger.info(Logger.Categories.PANELS, ...) is the only Logger call on the source path; no console.* anywhere
    - Zero inline useViewerStore.setState in source
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SidePanel.tsx end-to-end migration (subscription + entity rekey + close-button + mode-switch) + HistorySidebar.tsx onClick migration</name>
  <files>integrations/unified-viewer/src/panels/SidePanel.tsx, integrations/unified-viewer/src/panels/HistorySidebar.tsx</files>
  <read_first>
    - integrations/unified-viewer/src/panels/SidePanel.tsx (full — current line-45 selectedNodeId subscription; the entity useMemo at lines 49-52; the close button at lines 112-124 with inline useViewerStore.setState({...}); the Entity TabsContent body branch at lines 127-137)
    - integrations/unified-viewer/src/panels/HistorySidebar.tsx (full — current onClick at lines 117-132 + selectedNodeId subscription at line 140 + highlightedRowKey at line 141 + scroll-into-view useEffect at lines 143-149 + render at lines 151-219)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md §7 (SidePanel REFACTOR — mode-switch predicate verbatim code) + §D (single-source-of-truth — opportunistic compliance for close button)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-CONTEXT.md §`<specifics>` (mode-switch predicate selectedNodeIds.size === 1 && selectedBucketKeys.size === 0)
    - integrations/unified-viewer/src/store/viewer-store.ts (post-Plan-01 — setSelection arg shape; the nodeId scalar arg is GONE)
  </read_first>
  <behavior>
    - SidePanel.tsx subscribes to focalNodeId (NOT selectedNodeId) — line 45 migration
    - SidePanel.tsx adds subscriptions: selectedNodeIds + selectedBucketKeys
    - SidePanel.tsx entity useMemo (lines 49-52) keys on focalNodeId (NOT selectedNodeId)
    - SidePanel.tsx close button onClick (lines 113-124) routes through useViewerStore.getState().clearSelection() — replaces the inline useViewerStore.setState({...}); Phase 56 audit §8 opportunistic compliance closure
    - SidePanel.tsx Entity TabsContent body renders BucketCardList when isMultiMode (selectedBucketKeys.size > 0 OR selectedNodeIds.size > 1)
    - SidePanel.tsx renders EntityDetailPanel when isSingleFocalMode (selectedNodeIds.size === 1 && selectedBucketKeys.size === 0)
    - SidePanel.tsx renders HistorySidebar (no-selection default) when neither predicate matches
    - HistorySidebar.tsx onClick handler uses the new setSelection arg shape: nodeIds=Set([id]), bucketKeys=new Set(), focal={nodeId: id, bucketKey: null}, source='history' (replaces the deleted nodeId/sessionId/sessionStartAt args)
    - HistorySidebar.tsx selectedNodeId subscription is replaced with focalNodeId (the auto-scroll selector keys on the mounted entity which is the focal)
  </behavior>
  <action>
    Edit `integrations/unified-viewer/src/panels/SidePanel.tsx`:

    1) **Migrate line-45 subscription:** Change `const selectedNodeId = useViewerStore((s) => s.selectedNodeId)` to `const focalNodeId = useViewerStore((s) => s.focalNodeId)`. (This was previously in Plan 03's scope; revised to Plan 04 to keep wave-2 file scopes disjoint.)

    2) **Rekey the entity useMemo (lines 49-52)** — derive from `focalNodeId` not `selectedNodeId`. The panel still shows a single entity — focal is the right slot. Example: `const entity = useMemo(() => focalNodeId ? entities.find(e => e.id === focalNodeId) ?? null : null, [focalNodeId, entities])`. Update all in-component references that read `selectedNodeId` to read `focalNodeId`.

    3) **Migrate close-button onClick (lines 113-124):** Replace the inline `useViewerStore.setState({ selectedNodeId: null, pathToSelected: new Set() })` (or any equivalent inline mutation in that handler) with `useViewerStore.getState().clearSelection()`. clearSelection() covers ALL deleted fields plus the four new 56.1 fields (per Plan 01 invariant #4) — this is the Phase 56 audit §8 opportunistic compliance closure.

    4) **Add multi-mode subscriptions** near line 45 (immediately after the focalNodeId migration in step 1):
       - `const selectedNodeIds = useViewerStore((s) => s.selectedNodeIds)`
       - `const selectedBucketKeys = useViewerStore((s) => s.selectedBucketKeys)`

    5) **Add mode-switch predicate** above the return (or inline in the TabsContent body):
       ```typescript
       const isMultiMode = selectedBucketKeys.size > 0 || selectedNodeIds.size > 1
       const isSingleFocalMode = selectedNodeIds.size === 1 && selectedBucketKeys.size === 0
       ```

    6) **Replace the Entity TabsContent body** (currently lines 127-137 — `{entity ? <EntityDetailPanel/> : <HistorySidebar/>}`) with the three-way switch:
       ```typescript
       {isMultiMode ? (
         <BucketCardList apiClient={apiClient} system={system} />
       ) : isSingleFocalMode && entity ? (
         <EntityDetailPanel apiClient={apiClient} system={system} />
       ) : (
         <HistorySidebar apiClient={apiClient} system={system} />
       )}
       ```
       Import BucketCardList at the top of the file: `import { BucketCardList } from './BucketCardList'`.

    7) **Width predicate preservation:** the `widthClass` useMemo at lines 55-75 keys on `entity` (which is derived from focalNodeId via the entity useMemo). The widthClass is fine — focal-derived width is the right semantics in multi-mode (BucketCardList shows the same default width as HistorySidebar). No change required.

    Edit `integrations/unified-viewer/src/panels/HistorySidebar.tsx`:

    8) **Migrate selectedNodeId subscription at line 140:** change `const selectedNodeId = useViewerStore((s) => s.selectedNodeId)` to `const focalNodeId = useViewerStore((s) => s.focalNodeId)`. Update the scroll-into-view useEffect at lines 143-149 to key on focalNodeId (rename the variable in the body too — the `CSS.escape(selectedNodeId)` query becomes `CSS.escape(focalNodeId)`; the dep list at line 149 becomes `[focalNodeId]`). The highlight predicate at line 174-175 — `item.id === selectedNodeId` becomes `item.id === focalNodeId`. PRESERVE the existing highlightedRowKey subscription at line 141 (unchanged).

    9) **Migrate the onClick handler at lines 117-132** to the new setSelection arg shape:
       ```typescript
       const onClick = (id: string) => {
         useViewerStore.getState().setSelection({
           nodeIds: new Set<string>([id]),
           bucketKeys: new Set<string>(),
           focal: { nodeId: id, bucketKey: null },
           pathToSelected: new Set<string>(),
           highlightedRowKey: id,
           source: 'history',
           lslSessionFilter: [],
           lslFilterEntityIds: null,
         })
         Logger.info(Logger.Categories.PANELS, `HistorySidebar → ${id}`)
       }
       ```
       The deleted Phase 56 args (`nodeId`, `sessionId`, `sessionStartAt`) are GONE — Plan 01 removed them from the action signature. The remaining setSelection args are kept verbatim from the Phase 56 cascade-clear behavior at the same site.

    10) Do NOT modify HistorySidebar.test.tsx in this plan (the test file may have stale references to selectedNodeId — they're a downstream concern handled by the test grep gates the developer will fix as part of normal CI; but adding HistorySidebar.test.tsx fixes here would balloon the plan size. The store-level invariants are covered by Plan 01's tests).

    Do NOT add any `console.*` to either source file. Do NOT introduce new dependencies.
  </action>
  <verify>
    <automated>ROOT="$(git rev-parse --show-toplevel)"; cd "$ROOT/integrations/unified-viewer" && npm run tsc --noEmit 2>&1 | grep -E "(SidePanel|HistorySidebar)\.tsx" | head -10; cd "$ROOT" && grep -E "useViewerStore\.setState\(\{" integrations/unified-viewer/src/panels/SidePanel.tsx integrations/unified-viewer/src/panels/HistorySidebar.tsx integrations/unified-viewer/src/panels/BucketCardList.tsx | grep -v "test" | wc -l</automated>
    <expectation>tsc: zero compile errors in the three modified files. grep: 0 — zero inline useViewerStore.setState in any of the three production sources (SidePanel close button is now clearSelection(); HistorySidebar onClick uses setSelection action; BucketCardList uses setSelection action).</expectation>
  </verify>
  <done>
    - SidePanel.tsx subscribes to focalNodeId (line 45 migration) + selectedNodeIds + selectedBucketKeys
    - SidePanel.tsx entity useMemo rekeys on focalNodeId (lines 49-52)
    - SidePanel.tsx close button (lines 113-124) routes through clearSelection() — Phase 56 audit §8 closure
    - SidePanel.tsx Entity TabsContent body uses the three-way mode switch
    - BucketCardList is imported and used in SidePanel.tsx
    - HistorySidebar.tsx onClick uses the new setSelection arg shape (nodeIds: Set; bucketKeys: Set; focal: {nodeId, bucketKey})
    - HistorySidebar.tsx subscriptions migrated to focalNodeId (the auto-scroll target)
    - Zero inline useViewerStore.setState across SidePanel.tsx + HistorySidebar.tsx + BucketCardList.tsx
    - tsc passes for the three files
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side React render | BucketCardList renders entity name + ontology class. Content comes from useGraphData (KM-Core API JSON). Trust boundary is the JSON shape from the API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56.1-04-01 | Spoofing / XSS | Card name renders untrusted entity name from API | mitigate | React text escaping applies automatically to `{item.name}`. No `dangerouslyInnerHTML`. Mirror HistorySidebar's render verbatim (uses React text nodes). |
| T-56.1-04-02 | Tampering | SidePanel mode-switch falls into the wrong branch (multi-mode displayed when single-focal expected) → user sees card list when they expected entity detail | mitigate | Plan 06 E2E AC#3 asserts halo collapse + EntityDetailPanel mount after card click. The predicate is small and bool-only — unit-testable via the BucketCardList.test.tsx + SidePanel.test.tsx (existing) framework. |
| T-56.1-04-03 | Tampering | HistorySidebar onClick regresses to old arg shape after merge with concurrent Plan 03 | mitigate | Plan 04 task 2 explicitly migrates the onClick body. tsc would fail loudly if the Phase 56 args remained (the nodeId/sessionId/sessionStartAt fields no longer exist on the setSelection arg type). |
| T-56.1-04-04 | Information Disclosure | Logger.info logs entity ids — typically opaque UUIDs but could leak if names are interpolated | accept | Phase 56 convention: log id only (not name). Existing HistorySidebar logs `HistorySidebar → ${id}` — IDs are not PII in this domain. |
| T-56.1-04-05 | Tampering | Close-button onClick regresses to inline useViewerStore.setState({...}) — Locked Contract #5 violation | mitigate | Task 2 step 3 explicitly migrates the close-button to clearSelection(). The grep gate in Task 2 verify asserts zero inline useViewerStore.setState across SidePanel.tsx. |
| T-56.1-04-SC | Tampering | npm install (none added) | mitigate | Phase 56.1 adds ZERO npm dependencies. No package legitimacy gate. |

</threat_model>

<verification>
1. `ROOT="$(git rev-parse --show-toplevel)" && cd "$ROOT/integrations/unified-viewer" && npm run tsc --noEmit 2>&1 | grep -E "(BucketCardList|SidePanel|HistorySidebar)\.tsx"` — output empty (no errors in any of the four files).
2. `cd "$ROOT/integrations/unified-viewer" && npx vitest run src/panels/BucketCardList.test.tsx` — all tests pass.
3. `grep -E "useViewerStore\.setState\(\{" integrations/unified-viewer/src/panels/{BucketCardList,SidePanel,HistorySidebar}.tsx` — output empty.
4. `grep -c "BucketCardList" integrations/unified-viewer/src/panels/SidePanel.tsx` — output ≥ 2 (import + usage in TabsContent body).
5. `grep -c "isMultiMode\|isSingleFocalMode" integrations/unified-viewer/src/panels/SidePanel.tsx` — output ≥ 4 (definition + 2 uses + 1 doc reference, approximately).
6. `grep -c "clearSelection" integrations/unified-viewer/src/panels/SidePanel.tsx` — output ≥ 1 (close-button onClick).
</verification>

<success_criteria>
- BucketCardList.tsx exists and renders correctly in both timeline-mode and graph-multi mode
- BucketCardList.test.tsx ≥ 7 tests pass including source-grep gates
- SidePanel.tsx is fully migrated end-to-end in this plan (focalNodeId subscription + entity useMemo + close button + three-way mode-switch) — no SidePanel edits leak out to Plan 03
- HistorySidebar.tsx onClick handler migrated to new setSelection arg shape
- Zero inline useViewerStore.setState in any of the three production source files
- Logger discipline maintained — Logger.info on click; no console.*
- D-4 sidebar + D-5 drill collapse (sidebar half) + D-7 contract #5 closed in this plan
</success_criteria>

<output>
Create `.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-04-SUMMARY.md` when done. Summarise: (a) BucketCardList shape + mode-driven data source; (b) SidePanel comprehensive migration (subscription + entity useMemo + close button + mode-switch); (c) HistorySidebar migration; (d) Locked Contract #5 grep gate results; (e) note that this plan owns the SOLE SidePanel.tsx edit in wave-2 (Plan 03 explicitly does not touch SidePanel.tsx — disjoint file scopes for parallel safety).
</output>
</output>
