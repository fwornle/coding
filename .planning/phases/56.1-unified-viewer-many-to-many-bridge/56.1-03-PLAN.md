---
phase: 56.1-unified-viewer-many-to-many-bridge
plan: 03
type: execute
wave: 2
depends_on:
  - 56.1-01
  - 56.1-02
files_modified:
  - integrations/unified-viewer/src/graph/D3GraphCanvas.tsx
  - integrations/unified-viewer/src/graph/D3GraphCanvas.test.ts
autonomous: true
requirements:
  - D-4-graph-two-tier
  - D-5-drill-collapse
  - D-7-contract-2
  - D-7-contract-3
tags:
  - unified-viewer
  - d3
  - selection-rendering

must_haves:
  truths:
    - "D3GraphCanvas.tsx subscribes to selectedNodeIds + focalNodeId (NOT the deleted selectedNodeId)"
    - "applySelectionStyling renders TWO-TIER rings: red (#ff0000, opacity 1, stroke-width 4) for d.id === focalNodeId; lighter blue (#60a5fa, opacity 0.6, stroke-width 2) for d.id in selectedNodeIds && d.id !== focalNodeId"
    - "Non-focal, non-halo nodes have ring opacity 0 (preserved Phase 56 behavior)"
    - "pathToSelected trace is drawn from focalNodeId ONLY (NOT from every halo node — D-4 trace decision)"
    - "Node click writes setSelection({ nodeIds: new Set([d.id]), bucketKeys: new Set(), focal: { nodeId: d.id, bucketKey: null }, source: 'graph', ... }) — drill collapses any prior halo to single-focal (D-5)"
    - "Background SVG click routes through clearSelection() — unchanged"
    - "SidePanel close button drill-collapse behavior is owned by Plan 04 — verified there (NOT in this plan; Plans 03+04 are wave-2 parallel-safe with disjoint file scopes)"
    - "Main render-effect dep list is EXACTLY [visibleEntities, visibleRelations, theme, isLoading] — selectedNodeIds/focalNodeId/selectedBucketKeys/focalBucketKey MUST NOT appear (audit-locked invariant — viewport stability)"
    - "applySelectionStyling useCallback dep list is [selectedNodeIds, focalNodeId, pathToSelected, visibleRelations, theme] — renames Phase 56's [selectedNodeId, pathToSelected, visibleRelations, theme]"
  artifacts:
    - path: "integrations/unified-viewer/src/graph/D3GraphCanvas.tsx"
      provides: "Two-tier halo+focal ring rendering + drill-collapse click handler"
      contains: "selectedNodeIds"
    - path: "integrations/unified-viewer/src/graph/D3GraphCanvas.test.ts"
      provides: "Updated G-gates (G9 → selectedNodeIds; new gates for halo/focal class names + trace-from-focal-only)"
      contains: "selectedNodeIds"
  key_links:
    - from: "D3GraphCanvas.tsx applySelectionStyling"
      to: ".selection-ring stroke/opacity differentiation per focal vs halo"
      via: "single circle element per node, attrs vary by predicate"
      pattern: "\\.selection-ring"
    - from: "D3GraphCanvas.tsx main render useEffect dep list"
      to: "viewport stability invariant (Phase 56-PATTERNS Locked Contract #3)"
      via: "ONLY [visibleEntities, visibleRelations, theme, isLoading]"
      pattern: "}, \\[visibleEntities, visibleRelations, theme, isLoading\\]"
---

<objective>
Refactor `D3GraphCanvas.tsx` to consume the new `selectedNodeIds: ReadonlySet<string>` + `focalNodeId: string | null` store fields and render TWO-TIER selection rings: a red focal ring on `focalNodeId` (matching Phase 56's single-selection red ring) and a lighter blue halo ring (#60a5fa, opacity 0.6) on every other member of `selectedNodeIds`. The `pathToSelected` ancestry trace is drawn from `focalNodeId` only (D-4). Node click drills into single-focal mode per D-5 — writes `selectedNodeIds = new Set([d.id])`, clears `selectedBucketKeys`, makes the clicked node the focal. Background-SVG click routes through `clearSelection()`. The main render-effect dep list MUST stay `[visibleEntities, visibleRelations, theme, isLoading]` — Phase 56-PATTERNS Locked Contract #3 (viewport stability) is the load-bearing invariant.

**SidePanel.tsx edits are owned by Plan 04** (sole owner of SidePanel.tsx in wave 2). Plan 04 handles: (a) close-button → clearSelection() migration; (b) selectedNodeId → focalNodeId subscription; (c) entity useMemo rekey. This keeps Plan 03 + Plan 04 parallel-safe (no file collisions at merge time).

Purpose: Closes the graph side of the many-to-many bridge. After this plan + Plan 04, the graph renders multi-selection correctly even though the timeline (Plan 05) hasn't yet started WRITING multi-selection — internal consistency before integration.

Output: `D3GraphCanvas.tsx` consuming the new field names + two-tier render + drill-collapse click. `D3GraphCanvas.test.ts` G-gates renamed (selectedNodeId → selectedNodeIds) + new gates for halo + focal class + trace-from-focal-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-CONTEXT.md
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md
@.planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-PATTERNS.md
@.planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-STATE-FLOW.md
@integrations/unified-viewer/src/graph/D3GraphCanvas.tsx
@integrations/unified-viewer/src/graph/D3GraphCanvas.test.ts
@integrations/unified-viewer/src/store/viewer-store.ts
@.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-01-SUMMARY.md

<interfaces>
Consumes (from Plan 01 — already shipped to the store):
```typescript
useViewerStore((s) => s.selectedNodeIds)      // ReadonlySet<string>
useViewerStore((s) => s.focalNodeId)          // string | null
useViewerStore.getState().setSelection({ nodeIds, bucketKeys, focal, source, ... })
useViewerStore.getState().clearSelection()
```

PRESERVED contracts (Phase 56-PATTERNS Locked Contracts):
```typescript
// Contract #3 viewport stability — main render useEffect dep list:
}, [visibleEntities, visibleRelations, theme, isLoading])
// Listing selectedNodeIds/focalNodeId here forces SVG rebuild on every selection click → re-layout
// → audit V1 finding, re-litigation = blocking violation.

// Contract #5 single-source-of-truth — NO inline useViewerStore.setState in production paths.
// graph click goes through setSelection action; bg click goes through clearSelection action.
```

NEW visual contract (Phase 56.1 D-4):
- Focal ring: stroke '#ff0000', opacity 1, stroke-width 4 (Phase 56 single-selection visual preserved)
- Halo ring: stroke '#60a5fa', opacity 0.6, stroke-width 2 (NEW)
- ring class names (DOM): focal node carries 'ring-red-500' OR data-attr 'data-focal="true"'; halo nodes carry 'ring-blue-300/60' OR 'data-halo="true"' — choose ONE marker the E2E spec (Plan 06) can assert against. Inline stroke is applied via d3 attr; data-attr is a parallel observable marker for tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor D3GraphCanvas.tsx to two-tier rings + drill-collapse click handler</name>
  <files>integrations/unified-viewer/src/graph/D3GraphCanvas.tsx</files>
  <read_first>
    - integrations/unified-viewer/src/graph/D3GraphCanvas.tsx (full — current store subscriptions at 213-227 + applySelectionStyling at 308-364 + selection useEffect at 369-372 + main render at 387+ + click handler at ~493-499 + background click at ~427-431 + dep list at ~648-655)
    - integrations/unified-viewer/src/store/viewer-store.ts (post-Plan-01 — confirms setSelection arg shape + clearSelection availability)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md §4 (D3GraphCanvas REFACTOR — two-tier render code + click handler payload + AUDIT-LOCKED dep-list invariant)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-CONTEXT.md §D-4 (visual treatment), §D-5 (drill collapse)
    - .planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-PATTERNS.md §Locked Contract #3 + #5 (viewport stability + single source of truth)
  </read_first>
  <behavior>
    - D3GraphCanvas subscribes to selectedNodeIds and focalNodeId (NOT selectedNodeId)
    - With focalNodeId='a' AND selectedNodeIds={'a','b','c'}: node 'a' gets focal styling (red ring, opacity 1); nodes 'b','c' get halo styling (lighter blue, opacity 0.6); all other nodes have ring opacity 0
    - With focalNodeId=null AND selectedNodeIds=empty Set: no rings (all opacity 0), no ancestry trace, all nodes opacity 1 (Phase 56 "no selection" behavior preserved)
    - pathToSelected ancestry trace draws from focalNodeId only (not from halo members)
    - Node click writes setSelection with nodeIds=Set([d.id]), bucketKeys=empty Set, focal=d.id, source='graph' — drill collapse semantics
    - Background SVG click routes through clearSelection()
    - Main render-effect dep list is EXACTLY [visibleEntities, visibleRelations, theme, isLoading] — does NOT contain any selection field
    - applySelectionStyling useCallback dep list includes selectedNodeIds, focalNodeId, pathToSelected, visibleRelations, theme (renames Phase 56's selectedNodeId entry)
  </behavior>
  <action>
    Edit `integrations/unified-viewer/src/graph/D3GraphCanvas.tsx` per 56.1-PATTERNS.md §4 + CONTEXT.md D-4/D-5:

    1) **Store subscriptions (lines ~213-227):**
       - DELETE: `const selectedNodeId = useViewerStore((s) => s.selectedNodeId)` at line 215.
       - ADD: `const selectedNodeIds = useViewerStore((s) => s.selectedNodeIds)` + `const focalNodeId = useViewerStore((s) => s.focalNodeId)` in the same group.
       - PRESERVE: `const pathToSelected = useViewerStore((s) => s.pathToSelected)` at line 227 (unchanged).

    2) **applySelectionStyling (lines ~308-364) — REFACTOR per PATTERNS §4 two-tier render:**
       - The "no selection" branch: change the guard from `if (!selectedNodeId)` to `if (focalNodeId === null && selectedNodeIds.size === 0)`. The body inside (resetting links + nodes + labels to defaults) stays unchanged.
       - DELETE the existing single-ring block (lines ~320-323: `.filter((d) => d.id === selectedNodeId).select('.selection-ring').attr('opacity', 1)`).
       - REPLACE with the two-tier `.each` block per PATTERNS §4. Single `<circle class="selection-ring">` per node (the existing init at lines ~551-557 — DO NOT add a second circle). Inside `.each` callback:
         - If `d.id === focalNodeId`: ring.attr('stroke', '#ff0000').attr('opacity', 1).attr('stroke-width', 4)
         - Else if `selectedNodeIds.has(d.id)`: ring.attr('stroke', '#60a5fa').attr('opacity', 0.6).attr('stroke-width', 2)
         - Else: ring.attr('opacity', 0)
       - Also write a `data-focal` and `data-halo` attribute on the parent `.node` `<g>` for E2E test observability: focal → `data-focal="true"` and `data-halo` removed; halo → `data-halo="true"` and `data-focal` removed; neither → both removed.
       - The pathToSelected ancestry section (lines ~334-356) — change the `selectedNodeId` reference at line 337 (`computeAncestryPath(selectedNodeId, ...)`) to `focalNodeId`; guard with `if (focalNodeId !== null) { ... } else { ancestry = { edges: new Set(), nodeDepths: new Map(), pathLength: 0 } }`. The downstream `pathNodeOpacity` + `isPathEdge` logic stays verbatim — they read `nodeDepths` and `pathEdges`, both empty when no focal.
       - useCallback dep list (line ~363): change `[selectedNodeId, pathToSelected, visibleRelations, theme]` to `[selectedNodeIds, focalNodeId, pathToSelected, visibleRelations, theme]`.

    3) **Lightweight selection useEffect (lines ~369-372)** — unchanged in body; the dep `[applySelectionStyling]` already captures the new fields via the useCallback dep list.

    4) **Main render useEffect (~387 → end at lines ~648-655) — PRESERVE the dep list verbatim:**
       - Dep list MUST remain `[visibleEntities, visibleRelations, theme, isLoading]` — Locked Contract #3.
       - Update the in-effect closure reference to `focalNodeId` if any inline `selectedNodeId` reference exists in the main render path (the existing render uses `selectedNodeId` indirectly via `applySelectionStyling` only — no direct ref expected; verify with grep, fix any drift).
       - The comment block above the dep list (lines ~599-606 / ~648-654) — UPDATE wording from `selectedNodeId` to `selectedNodeIds, focalNodeId, selectedBucketKeys, focalBucketKey` so future readers know the dep-list invariant covers the multi-set evolution.

    5) **Node click handler (~493-499) — REWRITE per CONTEXT.md D-5 drill collapse:**
       Replace the existing `useViewerStore.setState({ selectedNodeId: d.id, pathToSelected: new Set(path.nodeDepths.keys()) })` with:
       ```typescript
       useViewerStore.getState().setSelection({
         nodeIds: new Set<string>([d.id]),
         bucketKeys: new Set<string>(),
         focal: { nodeId: d.id, bucketKey: null },
         pathToSelected: new Set<string>(path.nodeDepths.keys()),
         highlightedRowKey: d.id,
         source: 'graph',
       })
       ```
       PRESERVE `event.stopPropagation()` at the end of the handler — critical so the SVG background click doesn't fire next.

    6) **Background SVG click (~427-431) — REWRITE per Contract #5:**
       Replace `useViewerStore.setState({ selectedNodeId: null, pathToSelected: new Set() })` with `useViewerStore.getState().clearSelection()`. clearSelection() already covers ALL the deleted fields plus the new ones (per Plan 01 invariant #4).

    7) **SidePanel.tsx is OUT OF SCOPE for this plan.** All edits to `integrations/unified-viewer/src/panels/SidePanel.tsx` (close-button onClick → clearSelection(); selectedNodeId → focalNodeId subscription at line 45; entity useMemo rekey at lines 49-52) are owned by Plan 04, which has SidePanel.tsx in its `files_modified` frontmatter. Plan 04's mode-switch refactor folds these migrations in as a single coherent edit, keeping wave-2 parallel-safe (disjoint file scopes between Plan 03 and Plan 04).

    Logger.* calls (graph click + bg click — currently absent) stay absent; D3GraphCanvas does no logging by convention (PATTERNS §"Source: D3GraphCanvas.tsx (no logging)").

    Do NOT add any `console.*` anywhere.
  </action>
  <verify>
    <automated>ROOT="$(git rev-parse --show-toplevel)"; cd "$ROOT/integrations/unified-viewer" && npm run tsc --noEmit 2>&1 | grep -E "D3GraphCanvas" | head -10; cd "$ROOT" && grep -E "useViewerStore\.setState\(\{" integrations/unified-viewer/src/graph/D3GraphCanvas.tsx | grep -v "test" | wc -l</automated>
    <expectation>tsc: zero errors in D3GraphCanvas.tsx. grep: 0 — no inline useViewerStore.setState in D3GraphCanvas.tsx production source path (both writes route through the store actions setSelection / clearSelection). NOTE: SidePanel.tsx close-button → clearSelection() migration is verified in Plan 04 (this plan does not touch SidePanel.tsx; parallel-safe wave-2 disjoint file scope).</expectation>
  </verify>
  <done>
    - D3GraphCanvas.tsx subscribes to selectedNodeIds + focalNodeId
    - applySelectionStyling renders two-tier rings (red focal + lighter blue halo) and data-focal/data-halo attrs
    - pathToSelected ancestry trace draws from focalNodeId only
    - Node click writes setSelection with nodeIds=Set([d.id]) + bucketKeys=empty + focal=d.id (drill collapse)
    - Background SVG click routes through clearSelection()
    - Main render-effect dep list is unchanged: [visibleEntities, visibleRelations, theme, isLoading]
    - applySelectionStyling useCallback dep list updated to [selectedNodeIds, focalNodeId, pathToSelected, visibleRelations, theme]
    - tsc passes for D3GraphCanvas.tsx
    - Zero inline useViewerStore.setState in D3GraphCanvas.tsx
    - SidePanel.tsx is NOT modified by this plan (Plan 04 owns those edits)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update D3GraphCanvas.test.ts G-gates + add halo/focal/trace gates</name>
  <files>integrations/unified-viewer/src/graph/D3GraphCanvas.test.ts</files>
  <read_first>
    - integrations/unified-viewer/src/graph/D3GraphCanvas.test.ts (full — current G1-G13 source-grep gates; the gates lock the dep list + the source-of-truth contract + selectedNodeId references — these must be renamed)
    - .planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-PATTERNS.md §4 + §"Locked Design Contracts"
    - .planning/phases/56-unified-viewer-bidirectional-selection-timeline-scale/56-PATTERNS.md (the G9 / G13 gates that lock the dep list)
  </read_first>
  <behavior>
    - Gate G9 (renamed): asserts the main render-effect dep list is `[visibleEntities, visibleRelations, theme, isLoading]` and does NOT include selectedNodeIds / focalNodeId / selectedBucketKeys / focalBucketKey
    - Gate G13 (preserved): asserts sameSetMembership / setLslFilterEntityIds reference-stability code is intact
    - NEW gate: source contains '#60a5fa' (halo blue) AND '#ff0000' (focal red) AND 'data-focal' AND 'data-halo' — the two-tier rendering markers
    - NEW gate: source contains '`if (focalNodeId === null && selectedNodeIds.size === 0)`' (no-selection guard predicate evolution)
    - NEW gate: source uses `useViewerStore.getState().setSelection({` on graph click (NOT inline `useViewerStore.setState({` for selection writes — Contract #5 enforcement)
    - NEW gate: source uses `useViewerStore.getState().clearSelection()` on background click
    - NEW gate: applySelectionStyling useCallback dep list contains both `selectedNodeIds` and `focalNodeId`
    - All Phase 56 G1-G12 source-grep gates that referenced `selectedNodeId` are renamed to `selectedNodeIds` / `focalNodeId` as appropriate
  </behavior>
  <action>
    Edit `integrations/unified-viewer/src/graph/D3GraphCanvas.test.ts`:

    1) Find each G-gate test that grep-checks for `selectedNodeId` (without trailing 's'). For each:
       - If it's asserting the OLD name appears in source: rename the test title + replace `selectedNodeId` with the appropriate new name (`selectedNodeIds` for the Set field; `focalNodeId` for the singleton). PATTERN: use `\bselectedNodeIds\b` regex when the gate must distinguish from the deleted `selectedNodeId`.
       - If it's asserting the OLD name does NOT appear (Contract #3 dep-list lock): keep the negative assertion but rename to `selectedNodeIds` / `focalNodeId`. The dep list must NOT contain those names.

    2) Add new source-grep gates after the existing G1-G13:
       - `G14: applySelectionStyling no-selection guard uses focalNodeId === null && selectedNodeIds.size === 0` — assert `src.match(/focalNodeId === null && selectedNodeIds.size === 0/)`.
       - `G15: two-tier ring colors present in source (#60a5fa halo + #ff0000 focal)` — assert `src.includes('#60a5fa')` AND `src.includes('#ff0000')`.
       - `G16: data-focal + data-halo markers present` — assert `src.match(/data-focal/)` AND `src.match(/data-halo/)`.
       - `G17: graph click routes through setSelection action (not inline setState)` — assert `src.match(/useViewerStore\.getState\(\)\.setSelection\(\{[\s\S]*nodeIds:\s*new Set/)`. Bonus: count inline `useViewerStore.setState({` occurrences and assert `=== 0`.
       - `G18: background click routes through clearSelection()` — assert `src.match(/useViewerStore\.getState\(\)\.clearSelection\(\)/)`.
       - `G19: pathToSelected trace reads focalNodeId (not selectedNodeIds)` — assert the source includes `computeAncestryPath(focalNodeId,` (or equivalent — `if (focalNodeId !== null)` guarding the trace section). Negative: source does NOT contain `computeAncestryPath(selectedNodeId,` (with trailing 'd' not 'ds').
       - `G20: applySelectionStyling useCallback dep list contains selectedNodeIds AND focalNodeId` — assert both names are present near a `useCallback` block (use `src.match(/useCallback\([\s\S]*?\[[\s\S]*selectedNodeIds[\s\S]*focalNodeId[\s\S]*?\]\s*\)/)` — note multi-line match).

    3) PRESERVE the existing G9/G13 dep-list lock test wording AS-IS for clarity, just rename the field names. Use regex `/}, \[visibleEntities, visibleRelations, theme, isLoading\]/` to lock the main render dep list.

    Do NOT introduce new imports beyond `fs.readFileSync` + `path.resolve`. Do NOT add any rendering/RTL tests in this file — D3GraphCanvas's RTL coverage is out of scope (covered by E2E in Plan 06).
  </action>
  <verify>
    <automated>ROOT="$(git rev-parse --show-toplevel)"; cd "$ROOT/integrations/unified-viewer" && npx vitest run src/graph/D3GraphCanvas.test.ts 2>&1 | tail -30</automated>
    <expectation>vitest reports every G-gate passing (G1-G13 renamed + G14-G20 new = ≥ 20 source-grep tests, zero failures).</expectation>
  </verify>
  <done>
    - All G1-G13 gates renamed (selectedNodeId → selectedNodeIds/focalNodeId)
    - G14-G20 new gates added covering halo/focal/data-attrs/setSelection routing/clearSelection routing/trace from focal/useCallback dep list
    - Dep-list lock (G9) asserts no selection field appears in main render-effect dep list
    - All tests in D3GraphCanvas.test.ts pass
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side DOM mutation | D3GraphCanvas mutates the live SVG. Selection events come from user clicks or store subscriptions; no external network input. Trust boundary is the store action contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56.1-03-01 | Denial of Service | Two-tier halo render on very large selectedNodeIds set (>1000 ids) causes main-thread stall | accept | Per CONTEXT.md `<discretion>` #6 — K-cap deferred. Real-world bucket sizes ~3 nodes per bucket; halo selection ~10-30 nodes; well under any stall threshold. Future plan can add K-cap behind a feature flag. |
| T-56.1-03-02 | Tampering | Main render-effect dep list gains a selection field → viewport regression (Phase 56 audit V1) | mitigate | G9 source-grep gate (renamed) asserts dep list is exactly `[visibleEntities, visibleRelations, theme, isLoading]`. Any rename that lists a selection field fails the gate. |
| T-56.1-03-03 | Tampering | Inline useViewerStore.setState in production paths regresses Contract #5 | mitigate | G17/G18 source-grep gates count inline setState occurrences and assert === 0 in D3GraphCanvas.tsx. SidePanel.tsx close-button compliance is verified in Plan 04 (which owns the SidePanel.tsx edit). |
| T-56.1-03-04 | Information Disclosure | Halo nodes show a faint blue ring without intent — could leak which nodes were last touched in a previous session if drill collapse fails | mitigate | Drill click handler clears selectedBucketKeys to empty Set; clearSelection() empties everything. Plan 06's E2E AC#3 asserts halo collapses on card click. |
| T-56.1-03-SC | Tampering | npm install (none added) | mitigate | Phase 56.1 adds ZERO npm dependencies. No package legitimacy gate. |

</threat_model>

<verification>
1. `ROOT="$(git rev-parse --show-toplevel)" && cd "$ROOT/integrations/unified-viewer" && npm run tsc --noEmit 2>&1 | grep -E "D3GraphCanvas\.tsx"` — output should not contain compile errors for D3GraphCanvas.tsx.
2. `cd "$ROOT/integrations/unified-viewer" && npx vitest run src/graph/D3GraphCanvas.test.ts` — all G-gates pass.
3. `grep -E "useViewerStore\.setState\(\{" integrations/unified-viewer/src/graph/D3GraphCanvas.tsx` — output empty (no inline setState left in D3GraphCanvas.tsx production paths).
4. `grep -c "selectedNodeIds" integrations/unified-viewer/src/graph/D3GraphCanvas.tsx` — output ≥ 3 (subscription + applySelectionStyling reference + useCallback dep list).
5. `grep -c "focalNodeId" integrations/unified-viewer/src/graph/D3GraphCanvas.tsx` — output ≥ 4 (subscription + applySelectionStyling branches + useCallback dep list + click handler `focal: { nodeId: d.id, ... }`).
6. `grep "}, \[visibleEntities, visibleRelations, theme, isLoading\]" integrations/unified-viewer/src/graph/D3GraphCanvas.tsx` — match exactly once (main render-effect dep list verbatim — Locked Contract #3).
</verification>

<success_criteria>
- D3GraphCanvas subscribes to selectedNodeIds + focalNodeId
- Two-tier ring rendering: focal red, halo lighter blue, others invisible
- pathToSelected trace draws from focalNodeId only
- Node click triggers drill-collapse via setSelection (nodeIds=Set([d.id]), bucketKeys empty, focal=d.id, source='graph')
- Background SVG click routes through clearSelection()
- Main render-effect dep list unchanged from Phase 56: [visibleEntities, visibleRelations, theme, isLoading]
- All Phase 56 G-gates renamed + 7 new gates added (G14-G20), all pass
- Zero inline useViewerStore.setState in D3GraphCanvas.tsx
- D-4 graph-side + D-5 drill collapse + D-7 contract #2 + #3 closed in this plan
- SidePanel.tsx NOT touched by this plan (Plan 04 owns the SidePanel edits)
</success_criteria>

<output>
Create `.planning/phases/56.1-unified-viewer-many-to-many-bridge/56.1-03-SUMMARY.md` when done. Summarise: (a) two-tier render mechanism; (b) drill-collapse click payload; (c) audit-locked dep list invariant preserved; (d) G-gate counts; (e) note that SidePanel.tsx edits were handed off to Plan 04 to keep wave-2 file scopes disjoint.
</output>
</output>
