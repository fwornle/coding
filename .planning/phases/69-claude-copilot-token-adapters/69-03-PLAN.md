---
phase: 69-claude-copilot-token-adapters
plan: 03
type: execute
wave: 2
depends_on: ["69-01", "69-02"]
files_modified:
  - lib/lsl/token/claude-token-rows.mjs
  - tests/token-adapters/claude-token-rows.test.js
  - tests/token-adapters/claude-parent-linkage.test.js
  - tests/token-adapters/claude-reasoning-rows.test.js
autonomous: true
requirements: [ADAPT-01]
must_haves:
  truths:
    - "A Claude assistant usage block becomes a per-turn token_usage row with correct input/output token counts"
    - "Each extended-thinking block becomes a distinct per-reasoning-step row with estimated reasoning_tokens and tokens_estimated=1"
    - "A sub-agent row carries a parent_call_id derived from the existing claude-jsonl-tree parent linkage"
  artifacts:
    - path: "lib/lsl/token/claude-token-rows.mjs"
      provides: "Claude usage-block → TokenUsageRow extraction (per-turn + per-reasoning-step), parent linkage, uid-check"
      exports: ["buildClaudeTokenRows", "estimateReasoningTokens"]
    - path: "tests/token-adapters/claude-reasoning-rows.test.js"
      provides: "D-05 estimated per-reasoning-step assertion"
  key_links:
    - from: "lib/lsl/token/claude-token-rows.mjs"
      to: "lib/lsl/adapters/claude-jsonl-tree.mjs"
      via: "import parentSessionFromClaudeSubagentPath / agentIdFromClaudeSubagentPath / isSidechain gate reuse"
      pattern: "parentSessionFromClaudeSubagentPath"
    - from: "lib/lsl/token/claude-token-rows.mjs"
      to: "token_usage row"
      via: "per-turn granularity_tier + per-reasoning-step granularity_tier"
      pattern: "per-reasoning-step"
---

<objective>
Build `claude-token-rows.mjs`: the transform that turns Claude session/sub-agent JSONL records into `TokenUsageRow`-shaped objects — one `per-turn` row per assistant `usage` block (ADAPT-01), plus a distinct `per-reasoning-step` row per extended-thinking block (D-01/D-05), with sub-agent rows linked to their parent turn via `parent_call_id` reused from `claude-jsonl-tree.mjs` (D-02). This is pure extraction; the DB INSERT and live/sweep wiring come in Plan 05.

Purpose: Satisfy ROADMAP Phase-69 SC#1 (per-turn + per-reasoning-step) and SC#2 (parent linkage) at the row-construction layer, honoring the verified reality that Claude's `usage` carries NO native reasoning-token field (D-05 — estimate + `tokens_estimated=1`).
Output: `lib/lsl/token/claude-token-rows.mjs` + three unit/integration test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md
@.planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md

<interfaces>
<!-- Reuse targets from the locked Phase-51 parser. -->
From lib/lsl/adapters/claude-jsonl-tree.mjs (EXPORTED — import, do not re-implement):
```
export const SUBAGENT_PATH_RE                          // L38
export function parentSessionFromClaudeSubagentPath(p) // L87
export function agentIdFromClaudeSubagentPath(p)       // L95
export function subHashFromAgentId(agentId)            // L104
export function computeSubIndexes(rows)                // L185
```
uid-check gate (L251-258) + isSidechain first-record gate (L280) live in the NON-exported
buildRow — RE-APPLY the same gates in this module; do not bypass them by reading files raw.

Verified Claude usage block (RESEARCH § Code Examples): usage.input_tokens, usage.output_tokens
(thinking folded in — NO separate reasoning field), usage.cache_creation_input_tokens,
usage.cache_read_input_tokens; record-level `requestId` (unique → tool_call_id + dedup key),
`uuid`, `parentUuid`, `isSidechain`. Thinking block: {"type":"thinking","thinking":"...","signature":"..."}.

TokenUsageRow target columns (from Plan 02 token-db.mjs / RESEARCH § contract):
agent='claude', provider='claude-code', model, process='token-adapter-claude',
input_tokens, output_tokens, total_tokens, latency_ms=0, prompt_preview='',
tokens_estimated (0 for per-turn, 1 for per-reasoning-step), user_hash=ADAPTER_USER_HASH_CLAUDE,
model_raw, overhead_ms=null, task_id (stamped by caller in Plan 05), tool_call_id=requestId,
parent_call_id (sub-agent → parent turn id), granularity_tier ('per-turn' | 'per-reasoning-step'),
reasoning_tokens (0 for per-turn; estimated for per-reasoning-step).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-turn row extraction + reasoning-token estimator (D-05)</name>
  <files>lib/lsl/token/claude-token-rows.mjs, tests/token-adapters/claude-token-rows.test.js, tests/token-adapters/claude-reasoning-rows.test.js</files>
  <read_first>
    - lib/lsl/adapters/claude-jsonl-tree.mjs:243-332 (buildRow — the analog row builder, uid-check L251-258, isSidechain gate L280, JSON.parse try/catch L266-274)
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § Pitfall 1 (NO native reasoning field; D-05 estimate from thinking-block content length, tokens_estimated=1)
    - tests/token-adapters/fixtures/claude-session-sample.jsonl (Wave 0 — usage blocks + thinking blocks)
    - lib/lsl/token/token-db.mjs (Plan 02 — ADAPTER_USER_HASH_CLAUDE, the row shape consumed by insertTokenRow)
  </read_first>
  <behavior>
    - buildClaudeTokenRows(jsonlPath) reads a session JSONL, honoring the uid-check gate (st.uid===process.getuid()) — returns [] for non-owned files.
    - Each assistant record with a `usage` object yields exactly one `granularity_tier='per-turn'` row: input_tokens/output_tokens/total_tokens from usage (coalesced ?? 0), tool_call_id=record.requestId, tokens_estimated=0, reasoning_tokens=0, agent='claude', provider='claude-code', model from the record.
    - Each `{"type":"thinking"}` block in an assistant record's content yields one ADDITIONAL `granularity_tier='per-reasoning-step'` row with reasoning_tokens=estimateReasoningTokens(thinkingText), tokens_estimated=1, tool_call_id=`${requestId}:reason:<n>` (stable per block index).
    - estimateReasoningTokens(text) returns a deterministic positive integer derived from content length (e.g. ceil(chars/4)); never claims native extraction.
    - A malformed JSONL line is skipped (try/catch per line), never throws.
  </behavior>
  <action>
    Create `lib/lsl/token/claude-token-rows.mjs` exporting `buildClaudeTokenRows(jsonlPath, ctx)` and `estimateReasoningTokens(text)`. Re-apply the uid-check gate verbatim from claude-jsonl-tree.mjs:251-258 (skip + stderr on non-owned files) and the per-line `JSON.parse` try/catch from L266-274 (malformed line → skip). For every assistant record carrying a `usage` object, emit one `per-turn` row: `agent='claude'`, `provider='claude-code'`, `process='token-adapter-claude'`, `model` from the record, `input_tokens = usage.input_tokens ?? 0`, `output_tokens = usage.output_tokens ?? 0`, `total_tokens = (usage.input_tokens ?? 0) + (usage.output_tokens ?? 0)`, `latency_ms=0`, `prompt_preview=''`, `tokens_estimated=0`, `user_hash=ADAPTER_USER_HASH_CLAUDE` (import from token-db.mjs), `model_raw = record.model`, `overhead_ms=null`, `tool_call_id = record.requestId ?? record.uuid ?? ''`, `parent_call_id=''` (set in Task 2 for sub-agents), `granularity_tier='per-turn'`, `reasoning_tokens=0`, `task_id` left to the caller (Plan 05). For each `{"type":"thinking"}` element in the record's `content` array, emit one ADDITIONAL `per-reasoning-step` row (D-01/D-05): identical envelope but `granularity_tier='per-reasoning-step'`, `reasoning_tokens = estimateReasoningTokens(block.thinking)`, `tokens_estimated=1`, `tool_call_id = \`${baseToolCallId}:reason:${idx}\``. `estimateReasoningTokens(text)` returns `Math.max(1, Math.ceil((text?.length ?? 0) / 4))` (deterministic length-derived estimate — NEVER assert native extraction; coalesce empty/undefined text to 1). All stderr via process.stderr.write (no console.log). Author `claude-token-rows.test.js`: feed the Wave-0 session fixture, assert N per-turn rows with correct coalesced token counts and `granularity_tier='per-turn'`, `tokens_estimated=0`. Author `claude-reasoning-rows.test.js`: assert one per-reasoning-step row per thinking block, each with `granularity_tier='per-reasoning-step'`, `tokens_estimated=1`, a positive `reasoning_tokens`, and distinct `:reason:<n>` tool_call_ids; assert estimateReasoningTokens is monotonic in input length and returns ≥1 for empty input.
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/claude-token-rows.test.js tests/token-adapters/claude-reasoning-rows.test.js</automated>
  </verify>
  <acceptance_criteria>
    Both jest files pass. `grep -c "per-reasoning-step" lib/lsl/token/claude-token-rows.mjs` ≥ 1; `grep -c "tokens_estimated" lib/lsl/token/claude-token-rows.mjs` ≥ 1 with a literal `1` assigned on the reasoning path. `grep -E "process.getuid" lib/lsl/token/claude-token-rows.mjs` matches (uid gate present). `grep -v '^[[:space:]]*//' lib/lsl/token/claude-token-rows.mjs | grep -c "console.log"` == 0. No claim of native reasoning extraction (the estimator is length-derived).
  </acceptance_criteria>
  <done>Per-turn rows + estimated per-reasoning-step rows are produced from the session fixture, uid-gated, with tokens_estimated=1 on reasoning rows; unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sub-agent parent_call_id linkage (D-02 reuse)</name>
  <files>lib/lsl/token/claude-token-rows.mjs, tests/token-adapters/claude-parent-linkage.test.js</files>
  <read_first>
    - lib/lsl/adapters/claude-jsonl-tree.mjs:38 (SUBAGENT_PATH_RE), :87 (parentSessionFromClaudeSubagentPath), :95 (agentIdFromClaudeSubagentPath), :104 (subHashFromAgentId), :185 (computeSubIndexes), :280 (isSidechain first-record gate)
    - tests/token-adapters/fixtures/claude-subagent-sample.jsonl (Wave 0 — sidechain transcript)
    - .planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md § claude-token-rows (D-02 reuse, do NOT re-implement parent resolution)
  </read_first>
  <behavior>
    - When buildClaudeTokenRows is given a sub-agent JSONL path matching SUBAGENT_PATH_RE, the first-record isSidechain gate is honored (isSidechain:false first record → [] / skip).
    - parent_call_id on each sub-agent row is derived from parentSessionFromClaudeSubagentPath(path) via the imported claude-jsonl-tree exports — NOT a re-walked subagents dir.
    - A non-sidechain (main-session) path produces rows with parent_call_id=''.
  </behavior>
  <action>
    Extend `claude-token-rows.mjs` so that when `jsonlPath` matches `SUBAGENT_PATH_RE` (imported from claude-jsonl-tree.mjs), it (a) applies the isSidechain first-record gate from claude-jsonl-tree.mjs:280 (if the first record has `isSidechain === false`, skip the file and return []), and (b) sets `parent_call_id` on every emitted row to the parent session id resolved via `parentSessionFromClaudeSubagentPath(jsonlPath)` (D-02 — reuse the locked tree resolution; do NOT re-walk the subagents directory). Use `agentIdFromClaudeSubagentPath` / `subHashFromAgentId` for any sub-agent identifier the row needs. For non-subagent (main session) paths, `parent_call_id` stays `''`. Author `claude-parent-linkage.test.js`: construct a temp path that matches SUBAGENT_PATH_RE pointing at the Wave-0 subagent fixture, assert every emitted row carries the expected non-empty `parent_call_id` (== the value `parentSessionFromClaudeSubagentPath` returns for that path); assert a main-session path yields rows with `parent_call_id===''`; assert a subagent fixture whose first record is `isSidechain:false` yields [].
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/claude-parent-linkage.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/claude-parent-linkage.test.js` passes. `grep -c "parentSessionFromClaudeSubagentPath" lib/lsl/token/claude-token-rows.mjs` ≥ 1 (reuse, not re-implement); `grep -c "readdirSync\|subagents" lib/lsl/token/claude-token-rows.mjs` shows NO new subagents-dir walk (count of a raw readdir over subagents == 0). The test proves sub-agent rows carry a non-empty parent_call_id and the isSidechain:false gate returns [].
  </acceptance_criteria>
  <done>Sub-agent rows carry parent_call_id from the locked claude-jsonl-tree linkage; the isSidechain gate is honored; no parent resolution is re-implemented; unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk Claude JSONL → adapter | Untrusted local file content (could be malformed or non-owned) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-traversal | Information disclosure | reading ~/.claude session files | mitigate | uid-check gate (st.uid===process.getuid()) reused from claude-jsonl-tree.mjs:251-258 — skip non-owned files |
| T-69-input | Tampering | malformed usage block → NaN tokens | mitigate | Coalesce every numeric `?? 0`; per-line JSON.parse try/catch (L266-274) skips bad lines |
| T-69-dos | Denial of Service | malformed JSONL line | mitigate | try/catch per line; one bad line never aborts the pass |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- All three jest files pass.
- Per-reasoning-step rows carry tokens_estimated=1 and a length-derived reasoning_tokens (no native-extraction claim).
- Sub-agent parent_call_id comes from the imported claude-jsonl-tree exports (reuse verified by grep).
- uid-check gate present; no console.log.
</verification>

<success_criteria>
- Claude per-turn + per-reasoning-step rows are produced from real-shaped fixtures (ROADMAP SC#1).
- Sub-agent rows are linked via parent_call_id (ROADMAP SC#2) reusing the locked tree.
- D-05 estimation is explicit (tokens_estimated=1), never claiming native extraction.
</success_criteria>

<output>
Create `.planning/phases/69-claude-copilot-token-adapters/69-03-SUMMARY.md` when done.
</output>
