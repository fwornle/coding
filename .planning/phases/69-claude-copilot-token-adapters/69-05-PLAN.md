---
phase: 69-claude-copilot-token-adapters
plan: 05
type: execute
wave: 3
depends_on: ["69-02", "69-03"]
files_modified:
  - lib/lsl/live/claude-fs-watch.mjs
  - scripts/sub-agent-live-claude.mjs
  - scripts/sweep-sub-agents.mjs
  - tests/token-adapters/claude-taskid.test.js
  - tests/token-adapters/dedup.test.js
autonomous: true
requirements: [ADAPT-01]
must_haves:
  truths:
    - "An in-progress Claude session live-tailed by the daemon emits per-turn token rows stamped with the live task_id"
    - "A completed Claude session swept on startup emits rows then backfills task_id by timestamp-join"
    - "The same requestId captured both live and by sweep is inserted only once"
    - "A token-emission failure never crashes the LSL observation path"
  artifacts:
    - path: "lib/lsl/live/claude-fs-watch.mjs"
      provides: "onTokenRow hook alongside onMessage (additive, isolated)"
      contains: "onTokenRow"
    - path: "scripts/sub-agent-live-claude.mjs"
      provides: "token-db + claude-token-rows wired into the existing supervisor"
    - path: "scripts/sweep-sub-agents.mjs"
      provides: "completed-session Claude row emission + reused timestamp-join backfill"
  key_links:
    - from: "lib/lsl/live/claude-fs-watch.mjs"
      to: "lib/lsl/token/claude-token-rows.mjs + token-db.mjs + task-id.mjs"
      via: "onTokenRow callback: build rows, stamp resolveLiveTaskIdSafe, insertTokenRow"
      pattern: "resolveLiveTaskIdSafe"
    - from: "scripts/sweep-sub-agents.mjs"
      to: "scripts/backfill-task-id-by-timestamp.mjs runSweep"
      via: "reuse the timestamp-join after emitting completed-session rows"
      pattern: "runSweep|UPDATE token_usage SET task_id"
---

<objective>
Wire the Claude token-row layer (Plans 02–03) into the running infrastructure: add an additive `onTokenRow` hook to the live watcher, construct + pass the token-db/row modules in the existing Phase-51 `sub-agent-live-claude` supervisor (no plist change, D-08), and emit completed-session rows in the sweep then reuse the locked timestamp-join backfill for their `task_id` (D-03). Stamp live rows via `resolveLiveTaskIdSafe()` and dedup live-vs-sweep on `requestId` (Pitfall 4). All token writes stay best-effort so a failure never breaks the LSL path (D-08).

Purpose: Satisfy ROADMAP Phase-69 SC#2 (live-tail + sweep) and SC#5 (live task_id stamp / sweep backfill) for Claude.
Output: extended `claude-fs-watch.mjs`, `sub-agent-live-claude.mjs`, `sweep-sub-agents.mjs` + two integration test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md
@.planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md

<interfaces>
<!-- Insertion points + reuse targets. -->
lib/lsl/live/claude-fs-watch.mjs: tailFile onMessage (L103,L129), the watcher onMessage at L489
(carries the assistant exchange), failure isolation at L530-534 (catch → stderr → opts.onError).
scripts/sub-agent-live-claude.mjs: ObservationWriter dynamic-import + construct (L161-207), error
budget (L181-198), startClaudeWatcher({...}) call (L202-207).
scripts/backfill-task-id-by-timestamp.mjs: runSweep (L120-149), UPDATE (L122-123:
`UPDATE token_usage SET task_id = ? WHERE task_id = '' AND timestamp >= ? AND timestamp <= ?`),
loadArchivedSpans (L74-111), DB open (L171).

From Plan 02: openTokenDb, insertTokenRow, ADAPTER_USER_HASH_CLAUDE (lib/lsl/token/token-db.mjs);
resolveLiveTaskIdSafe (lib/lsl/token/task-id.mjs).
From Plan 03: buildClaudeTokenRows (lib/lsl/token/claude-token-rows.mjs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive onTokenRow hook in the live watcher + supervisor wiring</name>
  <files>lib/lsl/live/claude-fs-watch.mjs, scripts/sub-agent-live-claude.mjs, tests/token-adapters/claude-taskid.test.js</files>
  <read_first>
    - lib/lsl/live/claude-fs-watch.mjs:483-536 (the onMessage path + failure isolation L530-534 to mirror)
    - scripts/sub-agent-live-claude.mjs:157-207 (ObservationWriter dynamic-import/construct + error budget + startClaudeWatcher call)
    - lib/lsl/token/token-db.mjs, lib/lsl/token/task-id.mjs, lib/lsl/token/claude-token-rows.mjs (Plans 02-03 — the contracts to wire)
    - .planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md § claude-fs-watch / sub-agent-live (D-08 best-effort wiring; no plist change)
  </read_first>
  <action>
    In `claude-fs-watch.mjs`, add an OPTIONAL `opts.onTokenRow` callback invoked alongside the existing `opts.observationWriter.processMessages(...)` at L489-513, passing the same parsed assistant exchange/record. Wrap the `onTokenRow` invocation in ITS OWN try/catch that writes a `[claude-fs-watch] onTokenRow threw (non-fatal): <msg>` stderr line and continues — it must NEVER propagate into the observation path or the error budget (D-08 failure isolation; mirror the existing L530-534 isolation but keep token failures separate from `opts.onError`). In `sub-agent-live-claude.mjs`, dynamic-import (guarded, same pattern as ObservationWriter at L161-167 so `--help` works without SQLite) the token modules; construct a token-db handle via `openTokenDb` against `process.env.LLM_PROXY_DATA_DIR ?? <repo>/.data` + `/llm-proxy/token-usage.db`; define an `onTokenRow(record)` that calls `buildClaudeTokenRows`, stamps each row's `task_id = await resolveLiveTaskIdSafe()`, sets `row.user_hash = ADAPTER_USER_HASH_CLAUDE`, and `insertTokenRow(db, row)` per row — all inside try/catch. Pass `onTokenRow` into `startClaudeWatcher({...})` at L202-207. No plist change (D-08); document `launchctl kickstart -k gui/$(id -u)/com.coding.sub-agent-live-claude` as the post-change reload in the SUMMARY. Author `claude-taskid.test.js` (integration): with `LLM_PROXY_DIST_DIR` pointed at a stub exporting `resolveLiveTaskId: () => 'task-live-1'`, drive `buildClaudeTokenRows` over the Wave-0 fixture, stamp via resolveLiveTaskIdSafe, insert into a temp DB, and assert rows carry `task_id==='task-live-1'`; then point the stub at one returning `''` and assert rows carry `task_id===''`; assert that an insertTokenRow throwing does not propagate (onTokenRow swallows it).
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/claude-taskid.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/claude-taskid.test.js` passes. `grep -c "onTokenRow" lib/lsl/live/claude-fs-watch.mjs` ≥ 1; `grep -c "onTokenRow" scripts/sub-agent-live-claude.mjs` ≥ 1; `grep -c "resolveLiveTaskIdSafe" scripts/sub-agent-live-claude.mjs` ≥ 1. The token-write call site is inside a try/catch distinct from `opts.onError` (grep shows the onTokenRow catch does NOT call onError). No plist files modified (git status shows no `.plist` change).
  </acceptance_criteria>
  <done>The live watcher emits per-turn rows stamped with the live task_id via an isolated onTokenRow hook wired into the existing supervisor; a token failure never reaches the observation path or error budget.</done>
</task>

<task type="auto">
  <name>Task 2: Completed-session sweep emission + reused backfill + live/sweep dedup</name>
  <files>scripts/sweep-sub-agents.mjs, tests/token-adapters/dedup.test.js</files>
  <read_first>
    - scripts/sweep-sub-agents.mjs (the completed-session pass — where Claude JSONLs are enumerated)
    - scripts/backfill-task-id-by-timestamp.mjs:74-111 (loadArchivedSpans), :120-149 (runSweep), :122-123 (the UPDATE)
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § Pitfall 4 (dedup on requestId) + § "The completed-session backfill (D-03)"
    - lib/lsl/token/claude-token-rows.mjs, lib/lsl/token/token-db.mjs (Plan 02-03 contracts)
  </read_first>
  <action>
    In `sweep-sub-agents.mjs`, during the completed-session Claude pass, call `buildClaudeTokenRows` for each completed session JSONL, set `row.user_hash = ADAPTER_USER_HASH_CLAUDE` and `task_id=''` (the sweep does NOT call the live reader), and `insertTokenRow` each — but FIRST guard against live/sweep double-count (Pitfall 4): skip insert when a row with the same `(user_hash, tool_call_id)` already exists (`SELECT 1 FROM token_usage WHERE user_hash = ? AND tool_call_id = ? LIMIT 1`, parameterized). After emitting completed-session rows, REUSE the locked timestamp-join: import `runSweep` + `loadArchivedSpans` from `backfill-task-id-by-timestamp.mjs` (D-03 — do NOT re-implement the join) and run it so the just-written `task_id=''` adapter rows get stamped from archived spans; it never clobbers a live-stamped value (`WHERE task_id = ''`) and is idempotent. Wrap the whole emission+backfill in try/catch so a sweep token failure never aborts the sub-agent sweep. `sub-agent-sweep-job.sh` needs NO edit if the emission rides inside `sweep-sub-agents.mjs` (PATTERNS — additive in the .mjs). Author `dedup.test.js` (integration): build rows from the Wave-0 fixture, insert them once (simulating live), then run the sweep emission path over the same fixture into the same temp DB, and assert the total row count did NOT increase for the already-present `requestId`s (each `tool_call_id` appears exactly once); separately assert that running the reused `runSweep` against a temp DB with an archived span stamps the adapter rows' `task_id` for in-window timestamps and leaves out-of-window rows at `''`.
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/dedup.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/dedup.test.js` passes. `grep -E "runSweep|loadArchivedSpans" scripts/sweep-sub-agents.mjs` matches (reuse, not re-implement). `grep -c "tool_call_id = ?" scripts/sweep-sub-agents.mjs` ≥ 1 (parameterized dedup probe). The test proves a re-swept `requestId` is not double-inserted AND the reused backfill stamps in-window task_id while leaving out-of-window rows at `''`.
  </acceptance_criteria>
  <done>Completed Claude sessions emit rows once (dedup on requestId) and get task_id via the reused timestamp-join backfill; the sweep token path is best-effort and never aborts the sub-agent sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live/sweep daemon → token-usage.db | Second writer during live tail + 30-min sweep |
| archived span JSON → backfill | task_id resolution from archived measurement files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-dos | Denial of Service | token-emission failure crashing the LSL daemon | mitigate | onTokenRow + sweep emission in isolated try/catch separate from observation path / error budget (D-08) |
| T-69-id | Tampering (integrity) | live+sweep double-count | mitigate | Dedup on `(user_hash, tool_call_id)` via parameterized SELECT before insert (Pitfall 4) |
| T-69-sql | Tampering | dedup probe + backfill UPDATE with log-derived values | mitigate | Parameterized `?` placeholders only; reuse the locked runSweep UPDATE (backfill-task-id-by-timestamp.mjs:122-123) |
| T-69-span | DoS | corrupt active-measurement.json / archived span | mitigate | resolveLiveTaskIdSafe returns '' on failure; loadArchivedSpans skips bad spans (best-effort) |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Both integration tests pass.
- onTokenRow + sweep token writes are isolated (no propagation to onError / error budget).
- Dedup prevents live+sweep double-count; reused runSweep stamps in-window task_id only.
- No plist change; `launchctl kickstart` documented as the reload step.
</verification>

<success_criteria>
- Live-tail emits per-turn rows stamped with the live task_id (ROADMAP SC#2, SC#5).
- Sweep emits completed-session rows then backfills task_id via the locked join (SC#5).
- Live/sweep dedup holds; token failures never crash the LSL path (D-08).
</success_criteria>

<output>
Create `.planning/phases/69-claude-copilot-token-adapters/69-05-SUMMARY.md` when done.
</output>
