---
phase: 69-claude-copilot-token-adapters
plan: 06
type: execute
wave: 4
depends_on: ["69-02", "69-04", "69-05"]
files_modified:
  - lib/lsl/live/copilot-events-tail.mjs
  - scripts/sub-agent-live-copilot.mjs
  - scripts/sweep-sub-agents.mjs
  - tests/token-adapters/copilot-wiring.test.js
  - tests/token-adapters/best-effort.test.js
autonomous: true
requirements: [ADAPT-02]
must_haves:
  truths:
    - "An in-progress Copilot session emits a per-session-aggregate row at session.shutdown stamped with the live task_id"
    - "A completed Copilot session swept on startup emits aggregate rows then backfills task_id by timestamp-join"
    - "A corrupt span, locked DB, or malformed events line never throws out of Copilot ingestion"
  artifacts:
    - path: "lib/lsl/live/copilot-events-tail.mjs"
      provides: "session.shutdown branch emitting the aggregate row (isolated)"
      contains: "session.shutdown"
    - path: "scripts/sub-agent-live-copilot.mjs"
      provides: "token-db + copilot-token-rows wired into the existing supervisor"
    - path: "tests/token-adapters/best-effort.test.js"
      provides: "failure-isolation assertion across both adapters"
  key_links:
    - from: "lib/lsl/live/copilot-events-tail.mjs"
      to: "lib/lsl/token/copilot-token-rows.mjs + token-db.mjs + task-id.mjs"
      via: "session.shutdown handler: build aggregate rows, stamp resolveLiveTaskIdSafe, insertTokenRow"
      pattern: "session.shutdown"
    - from: "scripts/sweep-sub-agents.mjs"
      to: "backfill-task-id-by-timestamp.mjs runSweep"
      via: "completed Copilot rows backfilled by the same reused join"
      pattern: "runSweep|UPDATE token_usage SET task_id"
---

<objective>
Wire the Copilot token-row layer (Plans 02, 04) into the running infrastructure: add a `session.shutdown` branch to the live events tail that emits the per-session-aggregate row, construct + pass the token modules in the existing Phase-51 `sub-agent-live-copilot` supervisor (no plist change, D-08), and emit completed-session Copilot rows in the shared sweep then reuse the locked timestamp-join backfill for their `task_id` (D-03). Live rows stamp `task_id` via `resolveLiveTaskIdSafe()`. All token writes are best-effort (D-08). A final cross-adapter best-effort test proves no failure path throws out of ingestion.

Purpose: Satisfy ROADMAP Phase-69 SC#3 (aggregate ingestion through the live/sweep paths) and SC#5 (task_id stamp/backfill) for Copilot, plus the overall best-effort failure-isolation guarantee.
Output: extended `copilot-events-tail.mjs`, `sub-agent-live-copilot.mjs`, `sweep-sub-agents.mjs` (Copilot branch) + two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md
@.planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md

<interfaces>
<!-- Insertion points + reuse targets. -->
lib/lsl/live/copilot-events-tail.mjs: tailEventsFile per-line listener (L281-302); currently
dispatches only `parsed.type === 'subagent'` (L289); failure isolation via
`Promise.resolve(handler(...)).catch((err) => onError(err))` (L293-297).
scripts/sub-agent-live-copilot.mjs: sibling of sub-agent-live-claude.mjs — wire identically.
scripts/sweep-sub-agents.mjs: the completed-session pass (Plan 05 added the Claude branch; this
plan adds the Copilot branch in the SAME file — sequenced after Plan 05 via shared-file dependency).
scripts/backfill-task-id-by-timestamp.mjs: runSweep / loadArchivedSpans (reuse for Copilot rows too).

From Plan 02: openTokenDb, insertTokenRow, ADAPTER_USER_HASH_COPILOT (token-db.mjs);
resolveLiveTaskIdSafe (task-id.mjs).
From Plan 04: buildCopilotTokenRows, checkCopilotVocabulary (copilot-token-rows.mjs).
</interfaces>

<note>
This plan's `sweep-sub-agents.mjs` edit is the Copilot branch; Plan 05 owns the Claude branch in
the same file. Plan 05 runs in Wave 3; this plan (06) runs in Wave 4 — the wave bump is due to the
shared `scripts/sweep-sub-agents.mjs` file dependency, which forces 06 to land AFTER 05's sweep
edit. The live-watcher + supervisor edits here touch Copilot-only files (no overlap with 05).
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: session.shutdown branch in the live tail + Copilot supervisor wiring</name>
  <files>lib/lsl/live/copilot-events-tail.mjs, scripts/sub-agent-live-copilot.mjs, tests/token-adapters/copilot-wiring.test.js</files>
  <read_first>
    - lib/lsl/live/copilot-events-tail.mjs:281-302 (per-line listener; subagent dispatch L289; isolation L293-297)
    - scripts/sub-agent-live-copilot.mjs (sibling supervisor — mirror the Claude wiring from Plan 05)
    - lib/lsl/token/copilot-token-rows.mjs, token-db.mjs, task-id.mjs (Plans 02,04 contracts)
    - .planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md § copilot-events-tail / sub-agent-live (D-08; no plist change)
  </read_first>
  <action>
    In `copilot-events-tail.mjs`, add a branch to the per-line listener (L281-302) for `parsed.type === 'session.shutdown'` that invokes an OPTIONAL `cfg.onTokenRow(parsed)` handler, wrapped in the SAME `Promise.resolve(...).catch((err) => onError(err))` isolation pattern already used at L293-297 — but route token-write failures to a `[copilot-events-tail] onTokenRow threw (non-fatal): <msg>` stderr line, NOT into the subagent error path (D-08 failure isolation). In `sub-agent-live-copilot.mjs`, mirror Plan 05's Claude supervisor wiring: dynamic-import the token modules (guarded so `--help` works without SQLite), construct a token-db handle via `openTokenDb` against the `.data/llm-proxy/token-usage.db` path, define an `onTokenRow(shutdownEvt)` that calls `buildCopilotTokenRows`-style aggregate extraction on the shutdown event, stamps each row `task_id = await resolveLiveTaskIdSafe()` + `user_hash = ADAPTER_USER_HASH_COPILOT`, and `insertTokenRow(db, row)` per row — all inside try/catch. Optionally run `checkCopilotVocabulary` once at startup and log the verdict + version-drift warning. Pass `onTokenRow` into the watcher start call. No plist change (D-08); document `launchctl kickstart -k gui/$(id -u)/com.coding.sub-agent-live-copilot` as the reload. Author `copilot-wiring.test.js` (integration): with a stub `resolveLiveTaskId: () => 'task-cop-1'`, feed the Wave-0 events fixture's session.shutdown through the onTokenRow path into a temp DB, assert two aggregate rows carry `task_id==='task-cop-1'`, `user_hash==='copadt'`, `granularity_tier==='per-session-aggregate'`; assert that with the stub returning `''` rows carry `task_id===''`.
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/copilot-wiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/copilot-wiring.test.js` passes. `grep -c "session.shutdown" lib/lsl/live/copilot-events-tail.mjs` ≥ 1; `grep -c "onTokenRow" scripts/sub-agent-live-copilot.mjs` ≥ 1; `grep -c "resolveLiveTaskIdSafe" scripts/sub-agent-live-copilot.mjs` ≥ 1. The onTokenRow catch does NOT route into the subagent onError path. No `.plist` modified (git status clean of plist).
  </acceptance_criteria>
  <done>The live Copilot tail emits the per-session-aggregate row at session.shutdown, stamped with the live task_id via an isolated hook wired into the existing supervisor; token failures never reach the subagent path.</done>
</task>

<task type="auto">
  <name>Task 2: Copilot sweep emission + reused backfill + cross-adapter best-effort proof</name>
  <files>scripts/sweep-sub-agents.mjs, tests/token-adapters/best-effort.test.js</files>
  <read_first>
    - scripts/sweep-sub-agents.mjs (the Claude branch added by Plan 05 — add the Copilot branch alongside; re-read after 05 lands)
    - scripts/backfill-task-id-by-timestamp.mjs:74-111,120-149 (loadArchivedSpans + runSweep reuse)
    - lib/lsl/token/copilot-token-rows.mjs, token-db.mjs (Plans 02,04 contracts)
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § Pitfall 4 (dedup: Copilot key = session-uuid + model) + Security Domain (best-effort, never throws)
  </read_first>
  <action>
    In `sweep-sub-agents.mjs`, during the completed-session Copilot pass, call `buildCopilotTokenRows` for each completed `events.jsonl`, set `user_hash = ADAPTER_USER_HASH_COPILOT` + `task_id=''`, and `insertTokenRow` each — guarding double-count by skipping insert when a row with the same `(user_hash, tool_call_id)` already exists (Copilot natural key = session-uuid + model; PATTERNS — parameterized `SELECT 1 ... LIMIT 1`). After emitting, REUSE the same `runSweep` + `loadArchivedSpans` import already wired by Plan 05 (D-03 — single join for both adapters; the `WHERE task_id = ''` clause covers the new Copilot rows too). Wrap the Copilot emission+backfill in try/catch so it never aborts the sweep. Author `best-effort.test.js` (the cross-adapter failure-isolation gate, ROADMAP/VALIDATION final task): assert that (a) buildClaudeTokenRows over a fixture containing a deliberately malformed JSONL line skips the bad line and still returns the good rows (no throw); (b) buildCopilotTokenRows over a malformed events line does the same; (c) insertTokenRow against a closed/locked db returns false and does not throw; (d) resolveLiveTaskIdSafe against a broken dist path returns '' and does not throw. This single test certifies the D-08 best-effort guarantee end-to-end.
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/best-effort.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/best-effort.test.js` passes (all four no-throw assertions green). `grep -E "runSweep|loadArchivedSpans" scripts/sweep-sub-agents.mjs` matches (reused, not duplicated). `grep -c "ADAPTER_USER_HASH_COPILOT" scripts/sweep-sub-agents.mjs` ≥ 1. `npx jest tests/token-adapters/` (whole suite) is green.
  </acceptance_criteria>
  <done>Completed Copilot sessions emit aggregate rows once and get task_id via the reused join; a single cross-adapter best-effort test certifies no failure path throws out of ingestion (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live/sweep daemon → token-usage.db | Second writer (Copilot path) |
| on-disk events.jsonl → adapter | Untrusted local file content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-dos | Denial of Service | token-emission failure crashing the LSL daemon / malformed events line | mitigate | session.shutdown handler + sweep emission in isolated try/catch separate from the subagent path; malformed line skipped (D-08) |
| T-69-id | Tampering (integrity) | live+sweep double-count | mitigate | Dedup on `(user_hash, tool_call_id)` (session-uuid+model) via parameterized SELECT |
| T-69-sql | Tampering | dedup probe + backfill UPDATE | mitigate | Parameterized `?` placeholders only; reuse the locked runSweep UPDATE |
| T-69-input | Tampering | malformed usage → NaN | mitigate | Coalescing in buildCopilotTokenRows (Plan 04); insertTokenRow coalesces again (Plan 02) |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Both test files pass; whole `tests/token-adapters/` suite green.
- Copilot live emits at session.shutdown stamped with live task_id; sweep emits + backfills via the reused join.
- best-effort.test.js certifies all four no-throw paths.
- No plist change; reload via launchctl kickstart documented.
</verification>

<success_criteria>
- Copilot per-session-aggregate ingestion flows through live + sweep with task_id (ROADMAP SC#3, SC#5).
- The reused timestamp-join backfills both adapters' completed-session rows.
- The D-08 best-effort/failure-isolation guarantee is proven cross-adapter.
</success_criteria>

<output>
Create `.planning/phases/69-claude-copilot-token-adapters/69-06-SUMMARY.md` when done.
</output>
