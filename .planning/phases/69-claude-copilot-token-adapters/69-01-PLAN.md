---
phase: 69-claude-copilot-token-adapters
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/token-adapters/wal-concurrency.test.mjs
  - tests/token-adapters/fixtures/claude-session-sample.jsonl
  - tests/token-adapters/fixtures/claude-subagent-sample.jsonl
  - tests/token-adapters/fixtures/copilot-events-sample.jsonl
  - tests/token-adapters/fixtures/README.md
autonomous: true
requirements: [ADAPT-01, ADAPT-02]
must_haves:
  truths:
    - "A second-process INSERT alongside the live proxy daemon completes with zero SQLITE_BUSY"
    - "A redacted Claude session JSONL fixture with usage blocks + thinking blocks exists for downstream unit tests"
    - "A Copilot events.jsonl fixture with session.shutdown.modelMetrics exists for downstream unit tests"
  artifacts:
    - path: "tests/token-adapters/wal-concurrency.test.mjs"
      provides: "Guardrail WAL coexistence acceptance test (node:test)"
      contains: "busy_timeout"
    - path: "tests/token-adapters/fixtures/claude-session-sample.jsonl"
      provides: "Claude per-turn + thinking-block fixture"
    - path: "tests/token-adapters/fixtures/copilot-events-sample.jsonl"
      provides: "Copilot session.shutdown.modelMetrics fixture"
  key_links:
    - from: "tests/token-adapters/wal-concurrency.test.mjs"
      to: ".data/llm-proxy/token-usage.db"
      via: "better-sqlite3 second-writer INSERT with busy_timeout=5000"
      pattern: "busy_timeout"
---

<objective>
Establish the Wave 0 test scaffold for Phase 69: the mandatory WAL-concurrency acceptance fixture (D-07 guardrail) plus the shared, redacted JSONL fixtures every downstream adapter unit test consumes. These MUST exist before implementation waves so each later task's `<acceptance_criteria>` has a runnable target.

Purpose: Nyquist compliance — no implementation task may merge without an automated verify, and the WAL/concurrency story (a CONTEXT.md guardrail) is captured as a permanent, reproducible criterion.
Output: `tests/token-adapters/wal-concurrency.test.mjs` (node:test) + three redacted fixture files under `tests/token-adapters/fixtures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md
@.planning/phases/69-claude-copilot-token-adapters/69-VALIDATION.md
@.planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md

<interfaces>
<!-- The 21-column token_usage schema the WAL test INSERTs against. Order is load-bearing. -->
From _work/rapid-llm-proxy/src/token-usage.ts (insertStmt L574-581):
```
INSERT INTO token_usage (
  id, timestamp, provider, model, process, subscription,
  input_tokens, output_tokens, total_tokens, latency_ms,
  prompt_preview, tokens_estimated, user_hash, model_raw, overhead_ms,
  agent, task_id, tool_call_id, parent_call_id, granularity_tier, reasoning_tokens
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
```
Composite PRIMARY KEY (user_hash, id) — no AUTOINCREMENT; id allocated via
`SELECT COALESCE(MAX(id),0)+1 FROM token_usage WHERE user_hash = ?`.

node:test self-test fixture shape: scripts/backfill-task-id-by-timestamp.mjs:191-256
(temp DB + CREATE TABLE + assert via node:assert/strict; createRequire for better-sqlite3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the WAL-concurrency acceptance test (node:test)</name>
  <files>tests/token-adapters/wal-concurrency.test.mjs</files>
  <read_first>
    - scripts/backfill-task-id-by-timestamp.mjs:38-44 (host-side createRequire + `new Database(dbPath,{fileMustExist:true})` open pattern)
    - scripts/backfill-task-id-by-timestamp.mjs:191-256 (the `--self-test` node:test + temp-DB + CREATE TABLE fixture shape to mirror)
    - _work/rapid-llm-proxy/src/token-usage.ts:574-581 (the 21-column INSERT order, load-bearing)
    - _work/rapid-llm-proxy/src/token-usage.ts:46-47 (user_hash validation `/^[a-z][a-z0-9]{5}$/`)
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § "The WAL/Concurrency Acceptance Test" (PASS condition `ok===N && busy===0 && rowsReadBack===N && cleanup===N`)
  </read_first>
  <action>
    Create a `node:test` file with two test bodies. (A) CI-portable: build a temp DB, `CREATE TABLE token_usage` with the full 21-column Phase-68 shape (mirror the backfill self-test's CREATE TABLE), set `db.pragma('journal_mode = wal')` and `db.pragma('busy_timeout = 5000')`, then INSERT N=50 rows from a second `better-sqlite3` connection while a first connection holds the DB, reading each `id` via `SELECT COALESCE(MAX(id),0)+1 FROM token_usage WHERE user_hash = ?`. Use sentinel `agent='__waltest__'` and a distinct sentinel `user_hash` (matching `/^[a-z][a-z0-9]{5}$/`, e.g. `waltst`) so the rows never collide with real adapter rows. Assert `ok===50 && busy===0 && rowsReadBack===50`, then DELETE the sentinel rows and assert `cleanup===50`. (B) Live-coexistence (gated on env `LLM_PROXY_LIVE=1` per the node --test argv-drop gotcha): open the real `.data/llm-proxy/token-usage.db` with `fileMustExist:true` + `busy_timeout=5000`, INSERT 50 sentinel rows under the same `agent='__waltest__'`/sentinel `user_hash`, assert `busy===0`, read back, and DELETE all sentinel rows in a `finally` so the live DB is left pristine. Count SQLITE_BUSY by catching errors whose `.code === 'SQLITE_BUSY'`. Use `process.stderr.write` for any forensic output (no console.log). Resolve the proxy data dir via `process.env.LLM_PROXY_DATA_DIR ?? path.join(repoRoot,'.data')`.
  </action>
  <verify>
    <automated>node --test tests/token-adapters/wal-concurrency.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    `node --test tests/token-adapters/wal-concurrency.test.mjs` exits 0; the CI-portable test body asserts `ok===50 && busy===0 && rowsReadBack===50 && cleanup===50`. The live body is skipped without `LLM_PROXY_LIVE=1` and leaves zero `agent='__waltest__'` rows behind. `grep -c "busy_timeout" tests/token-adapters/wal-concurrency.test.mjs` ≥ 1.
  </acceptance_criteria>
  <done>The WAL acceptance fixture exists, passes against a temp DB, gates its live body on LLM_PROXY_LIVE=1, and cleans up all sentinel rows.</done>
</task>

<task type="auto">
  <name>Task 2: Capture redacted Claude + Copilot JSONL fixtures</name>
  <files>tests/token-adapters/fixtures/claude-session-sample.jsonl, tests/token-adapters/fixtures/claude-subagent-sample.jsonl, tests/token-adapters/fixtures/copilot-events-sample.jsonl, tests/token-adapters/fixtures/README.md</files>
  <read_first>
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § "Code Examples" (verified Claude `usage` block shape with `requestId`/`uuid`/`isSidechain`; verified Copilot `session.shutdown.modelMetrics` shape with `reasoningTokens`; the verified event vocabulary list)
    - lib/lsl/adapters/claude-jsonl-tree.mjs:38 (SUBAGENT_PATH_RE — the subagent path shape the subagent fixture filename should reflect in README)
    - lib/lsl/adapters/copilot-events.mjs:38 (parseCopilot import — fixture lines must be parseable by it)
  </read_first>
  <action>
    Create three fixtures with redacted-but-structurally-faithful content. `claude-session-sample.jsonl`: at least 3 assistant records each with a `usage` object carrying `input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`, `model`, a unique `requestId` (e.g. `req_TEST0001`...), a `uuid`, `parentUuid`, and `isSidechain:false`; include at least 2 assistant records that ALSO carry a `content` array with a `{"type":"thinking","thinking":"...","signature":"..."}` block (varying thinking-text length so the D-05 estimator has distinct inputs) and NO native reasoning-token field. `claude-subagent-sample.jsonl`: a sidechain transcript whose first record has `isSidechain:true` and subsequent assistant records carry `usage` + unique `requestId`s, so downstream parent-linkage tests can resolve `parent_call_id`. `copilot-events-sample.jsonl`: a sequence of the verified v1.0.63 event vocabulary (`session.start`, `user.message`, `assistant.turn_start`, `assistant.message` carrying `reasoningOpaque` + NO token counts, `assistant.turn_end`, `tool.execution_start/complete`, then `session.shutdown`) where `session.shutdown.data.modelMetrics` has TWO models — one with a `usage.reasoningTokens` key and one WITHOUT it (Pitfall 5 coalescing target). Write `README.md` documenting each fixture's provenance (redacted from live research-session captures, 2026-06-22, CLI v1.0.63), the path-shape each represents, and that no real tokens/PII remain. Redact any user-prompt text to `[REDACTED]`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const rl=p=>fs.readFileSync(p,'utf8').trim().split('\n').forEach(l=>JSON.parse(l));['tests/token-adapters/fixtures/claude-session-sample.jsonl','tests/token-adapters/fixtures/claude-subagent-sample.jsonl','tests/token-adapters/fixtures/copilot-events-sample.jsonl'].forEach(rl);console.error('all fixtures parse')"</automated>
  </verify>
  <acceptance_criteria>
    All three fixture files parse line-by-line as JSON (the node -e check prints `all fixtures parse` and exits 0). `grep -c '"thinking"' tests/token-adapters/fixtures/claude-session-sample.jsonl` ≥ 2. `grep -c '"session.shutdown"' tests/token-adapters/fixtures/copilot-events-sample.jsonl` ≥ 1. `grep -c '"reasoningTokens"' tests/token-adapters/fixtures/copilot-events-sample.jsonl` ≥ 1 (exactly one model carries it). The subagent fixture's first line contains `"isSidechain":true`.
  </acceptance_criteria>
  <done>Three structurally-faithful redacted fixtures + a provenance README exist; all parse; thinking blocks, session.shutdown, and a mixed-reasoningTokens modelMetrics are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk fixture/DB file → test process | Test reads/writes local SQLite + JSONL; no network, no untrusted remote input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01-write | Tampering | live token-usage.db second-writer INSERT | mitigate | Parameterized `?` placeholders only; sentinel `agent='__waltest__'` + distinct sentinel `user_hash`; DELETE all sentinel rows in a `finally` so the live DB is left pristine |
| T-69-01-id | Tampering (integrity) | id allocation under concurrency | mitigate | Sentinel distinct `user_hash` so the test's `MAX(id)+1` id-space never overlaps proxy/adapter rows |
| T-69-01-SC | Tampering | npm/pip/cargo installs | accept | Phase installs NO packages (RESEARCH Package Legitimacy Audit: all deps present); nothing to slopcheck |
</threat_model>

<verification>
- `node --test tests/token-adapters/wal-concurrency.test.mjs` exits 0 (CI body); live body cleans up.
- All three fixtures parse as line-delimited JSON.
- No `console.log` in the test (grep clean); `process.stderr.write` only.
</verification>

<success_criteria>
- WAL-concurrency acceptance fixture exists and passes with `busy===0`.
- Shared Claude + Copilot fixtures exist and are consumed by Waves 1–3 unit tests.
- The live-coexistence body is operator-runnable via `LLM_PROXY_LIVE=1` and leaves no residue.
</success_criteria>

<output>
Create `.planning/phases/69-claude-copilot-token-adapters/69-01-SUMMARY.md` when done.
</output>
