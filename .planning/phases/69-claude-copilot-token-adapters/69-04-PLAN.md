---
phase: 69-claude-copilot-token-adapters
plan: 04
type: execute
wave: 2
depends_on: ["69-01", "69-02"]
files_modified:
  - lib/lsl/token/copilot-token-rows.mjs
  - tests/token-adapters/copilot-token-rows.test.js
  - tests/token-adapters/copilot-vocab.test.js
autonomous: true
requirements: [ADAPT-02]
must_haves:
  truths:
    - "A Copilot session.shutdown.modelMetrics block becomes one per-session-aggregate row per model"
    - "Numeric token fields are coalesced so a model missing reasoningTokens never writes NaN/null"
    - "The Phase-1 event-vocabulary check enumerates the distinct event type: values and records the version-keyed verdict"
  artifacts:
    - path: "lib/lsl/token/copilot-token-rows.mjs"
      provides: "Copilot events.jsonl → per-session-aggregate rows + version-keyed vocabulary check"
      exports: ["buildCopilotTokenRows", "checkCopilotVocabulary", "COPILOT_PROBED_VERSION"]
    - path: "tests/token-adapters/copilot-vocab.test.js"
      provides: "Phase-1 vocabulary-check assertion (D-04/D-09 deliverable)"
  key_links:
    - from: "lib/lsl/token/copilot-token-rows.mjs"
      to: "src/live-logging/TranscriptNormalizer.js"
      via: "parseCopilot line parser (reuse, no new JSONL parser)"
      pattern: "parseCopilot"
    - from: "lib/lsl/token/copilot-token-rows.mjs"
      to: "token_usage row"
      via: "per-session-aggregate granularity_tier from session.shutdown.modelMetrics"
      pattern: "per-session-aggregate"
---

<objective>
Build `copilot-token-rows.mjs`: the transform that reads a Copilot `events.jsonl`, finds `session.shutdown.modelMetrics`, and emits one `per-session-aggregate` row per model (ADAPT-02 / D-04), reusing the locked `parseCopilot` primitive — plus the Phase-1 event-vocabulary check (D-04/D-09 deliverable) that enumerates the distinct event `type:` values and bakes the version-keyed verdict (CLI v1.0.63 → no per-turn usage). This is pure extraction; the live/sweep wiring comes in Plan 06.

Purpose: Satisfy ROADMAP Phase-69 SC#3 (per-session-aggregate from modelMetrics) and SC#4 (vocabulary check + per-turn upgrade path), honoring that v1.0.63 surfaces token totals ONLY in `session.shutdown.modelMetrics.<model>.usage`.
Output: `lib/lsl/token/copilot-token-rows.mjs` + two unit test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md
@.planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md

<interfaces>
<!-- Reuse targets from the locked Phase-51 Copilot parser. -->
From lib/lsl/adapters/copilot-events.mjs:
```
import { parseCopilot } from '../../../src/live-logging/TranscriptNormalizer.js'  // L38 — reuse, no new parser
const PROJECT_ALLOWLIST_RE = /^[a-z0-9-]+$/i                                       // L41
// uid-check on each session dir: L251 / L271-285
// STALE docstring: L18/L78 says v1.0.48 — refresh to v1.0.63 when touched
```

Verified Copilot aggregate source (RESEARCH § Code Examples — session.shutdown):
data.modelMetrics.<model>.usage = { inputTokens, outputTokens, cacheReadTokens,
cacheWriteTokens, reasoningTokens? }  // reasoningTokens key MAY be absent (Pitfall 5)

Verified v1.0.63 event vocabulary (RESEARCH): session.start, session.model_change,
system.message, user.message, assistant.turn_start, assistant.message (carries
reasoningOpaque + toolRequests but NO token counts), assistant.turn_end, tool.execution_start,
tool.execution_complete, hook.start, hook.end, session.shutdown, abort.

TokenUsageRow target columns (from token-db.mjs / RESEARCH § contract):
agent='copilot', provider='copilot', model, process='token-adapter-copilot',
input_tokens, output_tokens, total_tokens, reasoning_tokens, latency_ms=0, prompt_preview='',
tokens_estimated=0, user_hash=ADAPTER_USER_HASH_COPILOT, model_raw, overhead_ms=null,
tool_call_id=model, parent_call_id='', granularity_tier='per-session-aggregate',
task_id (stamped by caller in Plan 06).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-session-aggregate row extraction from modelMetrics (D-04)</name>
  <files>lib/lsl/token/copilot-token-rows.mjs, tests/token-adapters/copilot-token-rows.test.js</files>
  <read_first>
    - lib/lsl/adapters/copilot-events.mjs:142-158 (readSubAgentEvents — the line-stream parseCopilot try/catch-per-line skeleton to mirror)
    - lib/lsl/adapters/copilot-events.mjs:271-285 (uid-check on session dir)
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § Pattern 3 + Pitfall 5 (one row per model; coalesce reasoningTokens ?? 0 — key may be absent)
    - tests/token-adapters/fixtures/copilot-events-sample.jsonl (Wave 0 — session.shutdown with two models, one missing reasoningTokens)
    - lib/lsl/token/token-db.mjs (Plan 02 — ADAPTER_USER_HASH_COPILOT)
  </read_first>
  <behavior>
    - buildCopilotTokenRows(eventsJsonlPath) streams the file line-by-line, parsing each via parseCopilot in a try/catch (bad line → skip).
    - On the `session.shutdown` event it emits one per-session-aggregate row per entry in data.modelMetrics: input_tokens/output_tokens/reasoning_tokens coalesced (?? 0), total_tokens=input+output, granularity_tier='per-session-aggregate', tool_call_id=model, agent='copilot', provider='copilot'.
    - A model whose usage omits reasoningTokens writes reasoning_tokens=0 (no NaN/null).
    - The session-dir uid-check is honored; non-owned dirs yield [].
  </behavior>
  <action>
    Create `lib/lsl/token/copilot-token-rows.mjs` exporting `buildCopilotTokenRows(eventsJsonlPath, ctx)`. Stream the file with readline, parsing each line via the imported `parseCopilot` (lib/lsl/adapters/copilot-events.mjs:38 — reuse, do NOT write a new JSONL parser) inside a per-line try/catch (bad line → continue). Honor the session-dir uid-check from copilot-events.mjs:271-285 (skip non-owned). For the `session.shutdown` event, read `evt.data.modelMetrics` and emit one row per `[model, m]` entry: `agent='copilot'`, `provider='copilot'`, `process='token-adapter-copilot'`, `model`, `input_tokens = m.usage?.inputTokens ?? 0`, `output_tokens = m.usage?.outputTokens ?? 0`, `total_tokens = (m.usage?.inputTokens ?? 0) + (m.usage?.outputTokens ?? 0)`, `reasoning_tokens = m.usage?.reasoningTokens ?? 0` (Pitfall 5 — key may be absent), `latency_ms=0`, `prompt_preview=''`, `tokens_estimated=0`, `user_hash=ADAPTER_USER_HASH_COPILOT` (import from token-db.mjs), `model_raw=model`, `overhead_ms=null`, `tool_call_id=model`, `parent_call_id=''`, `granularity_tier='per-session-aggregate'`, `task_id` left to the caller (Plan 06), `timestamp = evt.timestamp ?? new Date().toISOString()`. NEVER decode `reasoningOpaque` (V6 — ignore it). All stderr via process.stderr.write. Author `copilot-token-rows.test.js`: feed the Wave-0 events fixture, assert exactly two per-session-aggregate rows (one per model), correct coalesced token counts, `granularity_tier='per-session-aggregate'`, and that the model lacking reasoningTokens yields `reasoning_tokens===0` (not NaN/null).
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/copilot-token-rows.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/copilot-token-rows.test.js` passes. `grep -c "parseCopilot" lib/lsl/token/copilot-token-rows.mjs` ≥ 1 (reuse). `grep -c "per-session-aggregate" lib/lsl/token/copilot-token-rows.mjs` ≥ 1. `grep -c "reasoningOpaque" lib/lsl/token/copilot-token-rows.mjs` shows it is never decoded (any reference is a skip/ignore comment only — no JSON.parse/base64 of it). `grep -v '^[[:space:]]*//' lib/lsl/token/copilot-token-rows.mjs | grep -c "console.log"` == 0. The test proves the reasoningTokens-absent model yields reasoning_tokens===0.
  </acceptance_criteria>
  <done>One per-session-aggregate row per model is produced from the fixture, reusing parseCopilot, with coalesced numerics and uid-gating; unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase-1 event-vocabulary check + version-keyed verdict (D-04/D-09)</name>
  <files>lib/lsl/token/copilot-token-rows.mjs, tests/token-adapters/copilot-vocab.test.js</files>
  <read_first>
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § "Verified Copilot event vocabulary" + Pitfall 3 (version-keyed verdict; re-probe on version change; v1.0.63)
    - lib/lsl/adapters/copilot-events.mjs:18,78 (STALE v1.0.48 docstrings to refresh)
    - tests/token-adapters/fixtures/copilot-events-sample.jsonl (Wave 0 — full event vocabulary present)
    - .planning/phases/69-claude-copilot-token-adapters/69-CONTEXT.md D-04/D-09 (vocabulary check is a DELIVERABLE, not optional)
  </read_first>
  <behavior>
    - checkCopilotVocabulary(eventsJsonlPath) returns the set of distinct event `type:` values plus a boolean `perTurnUsagePresent` (true iff any assistant.* event carries a token-usage payload).
    - For v1.0.63 fixtures perTurnUsagePresent is false (assistant.message has reasoningOpaque but no token counts) → verdict = 'per-session-aggregate'.
    - COPILOT_PROBED_VERSION === '1.0.63'; a warning is emitted (stderr) when the installed `copilot --version` differs from COPILOT_PROBED_VERSION.
  </behavior>
  <action>
    Extend `copilot-token-rows.mjs` with `checkCopilotVocabulary(eventsJsonlPath)` and a `COPILOT_PROBED_VERSION = '1.0.63'` constant (D-09 — version-keyed). The function streams the file via parseCopilot, collects the set of distinct event `type:` strings, and sets `perTurnUsagePresent=true` only if some `assistant.*` event carries a non-empty token-usage payload (inputTokens/outputTokens on a per-turn event — which v1.0.63 does NOT have). Return `{ types: string[], perTurnUsagePresent, verdict }` where `verdict='per-turn'` when perTurnUsagePresent else `'per-session-aggregate'` (D-04 — the upgrade path is wired but inert on v1.0.63). Add a `warnOnVersionDrift(installedVersion)` helper that writes a `[token-adapter-copilot] CLI version drift: installed=<x> probed=1.0.63 — re-run vocabulary check` stderr line when `installedVersion !== COPILOT_PROBED_VERSION` (Pitfall 3). While here, refresh the stale `v1.0.48` docstrings in copilot-events.mjs to note v1.0.63 (the conclusion — aggregate-only — still holds). Author `copilot-vocab.test.js`: feed the Wave-0 fixture, assert `types` contains `session.shutdown`, `assistant.message`, `assistant.turn_end`; assert `perTurnUsagePresent===false` and `verdict==='per-session-aggregate'`; assert warnOnVersionDrift('1.0.99') writes a drift warning and warnOnVersionDrift('1.0.63') does not.
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/copilot-vocab.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/copilot-vocab.test.js` passes. `grep -E "COPILOT_PROBED_VERSION *= *'1.0.63'" lib/lsl/token/copilot-token-rows.mjs` matches. The test asserts `verdict==='per-session-aggregate'` and `perTurnUsagePresent===false` for the v1.0.63 fixture, and that version-drift warns. `grep -c "v1.0.48" lib/lsl/adapters/copilot-events.mjs` == 0 (stale docstring refreshed).
  </acceptance_criteria>
  <done>The Phase-1 vocabulary check enumerates event types, bakes the v1.0.63 verdict (per-session-aggregate), wires the per-turn upgrade path inertly, warns on version drift, and refreshes the stale docstring; unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk Copilot events.jsonl → adapter | Untrusted local file content; `reasoningOpaque` is an opaque blob |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-traversal | Information disclosure | reading ~/.copilot session dirs | mitigate | uid-check + PROJECT_ALLOWLIST_RE reused from copilot-events.mjs:271-285,41 |
| T-69-input | Tampering | model missing reasoningTokens → NaN | mitigate | Coalesce `m.usage?.reasoningTokens ?? 0` and every numeric (Pitfall 5) |
| T-69-dos | Denial of Service | malformed events.jsonl line | mitigate | parseCopilot in per-line try/catch; bad line skipped |
| T-69-crypto | Information disclosure | reasoningOpaque encrypted blob | accept | Never decode it (V6) — ignored entirely |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Both jest files pass.
- One per-session-aggregate row per model; reasoningTokens-absent model coalesces to 0.
- Vocabulary check verdict is per-session-aggregate for v1.0.63; upgrade path present but inert.
- Stale v1.0.48 docstring refreshed; reasoningOpaque never decoded; no console.log.
</verification>

<success_criteria>
- Copilot per-session-aggregate rows from session.shutdown.modelMetrics (ROADMAP SC#3).
- Phase-1 vocabulary check is a delivered artifact with a version-keyed verdict (ROADMAP SC#4).
- Coalescing prevents NaN/null; reasoningOpaque is ignored.
</success_criteria>

<output>
Create `.planning/phases/69-claude-copilot-token-adapters/69-04-SUMMARY.md` when done.
</output>
