---
phase: 69-claude-copilot-token-adapters
plan: 02
type: execute
wave: 1
depends_on: ["69-01"]
files_modified:
  - lib/lsl/token/token-db.mjs
  - lib/lsl/token/task-id.mjs
  - tests/token-adapters/token-db.test.js
  - tests/token-adapters/task-id.test.js
autonomous: true
requirements: [ADAPT-01, ADAPT-02]
must_haves:
  truths:
    - "An adapter can INSERT a token_usage row best-effort without ever throwing out of ingestion"
    - "Adapter rows use a distinct adapter user_hash so their id-space never collides with the proxy's counter"
    - "D-03: An adapter can resolve the live task_id via the single span reader (getActiveMeasurement/resolveLiveTaskId), getting '' when no span is open; completed-session backfill reuses the locked timestamp-join (no second reader/join)"
  artifacts:
    - path: "lib/lsl/token/token-db.mjs"
      provides: "Host-side better-sqlite3 open + best-effort INSERT helper (the ONLY file touching the DB)"
      exports: ["openTokenDb", "insertTokenRow", "ADAPTER_USER_HASH_CLAUDE", "ADAPTER_USER_HASH_COPILOT"]
      contains: "busy_timeout"
    - path: "lib/lsl/token/task-id.mjs"
      provides: "Single-reader import of resolveLiveTaskId from the proxy dist"
      exports: ["resolveLiveTaskIdSafe"]
  key_links:
    - from: "lib/lsl/token/token-db.mjs"
      to: ".data/llm-proxy/token-usage.db"
      via: "better-sqlite3 INSERT with busy_timeout=5000 + distinct user_hash"
      pattern: "busy_timeout = 5000"
    - from: "lib/lsl/token/task-id.mjs"
      to: "_work/rapid-llm-proxy/dist/measurement-span.js"
      via: "dynamic import(pathToFileURL(...)) of resolveLiveTaskId"
      pattern: "resolveLiveTaskId"
---

<objective>
Build the two shared primitives both adapters depend on: the host-side DB writer (`token-db.mjs`, the single file that touches `token-usage.db`) and the live `task_id` resolver (`task-id.mjs`, a thin wrapper over the Phase-68 single span reader). These are the interface contracts the Claude (Plan 03) and Copilot (Plan 04) row modules write against — define them first so downstream tasks receive the contracts in-plan, not via codebase exploration.

Purpose: Centralize the id-collision-safe INSERT (D-06), the WAL-coexistence open (D-07), and the single-span-reader import (D-03) in one place, with best-effort never-throw discipline (D-08) so an emission failure can never crash the LSL path.
Output: `lib/lsl/token/token-db.mjs` + `lib/lsl/token/task-id.mjs` + their unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md
@.planning/phases/69-claude-copilot-token-adapters/69-PATTERNS.md

<interfaces>
<!-- The exact INSERT the adapter must replicate (column order load-bearing). -->
From _work/rapid-llm-proxy/src/token-usage.ts insertStmt (L574-581):
```
INSERT INTO token_usage (
  id, timestamp, provider, model, process, subscription,
  input_tokens, output_tokens, total_tokens, latency_ms,
  prompt_preview, tokens_estimated, user_hash, model_raw, overhead_ms,
  agent, task_id, tool_call_id, parent_call_id, granularity_tier, reasoning_tokens
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
```
id allocation (L588-595): `SELECT COALESCE(MAX(id),0)+1 AS next FROM token_usage WHERE user_hash = ?`
user_hash validation (L46-47): `/^[a-z][a-z0-9]{5}$/`

From _work/rapid-llm-proxy/dist/measurement-span.js:
`resolveLiveTaskId(overrideDataDir?: string): string`  // '' when no open span; NEVER throws

Host-side dist-import pattern — scripts/measurement-start.mjs:31-52:
```
const PROXY_DIST = process.env.LLM_PROXY_DIST_DIR || '/Users/Q284340/Agentic/_work/rapid-llm-proxy/dist';
const { resolveLiveTaskId } = await import(pathToFileURL(path.join(PROXY_DIST,'measurement-span.js')).href);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: token-db.mjs — host-side best-effort INSERT helper</name>
  <files>lib/lsl/token/token-db.mjs, tests/token-adapters/token-db.test.js</files>
  <read_first>
    - scripts/backfill-task-id-by-timestamp.mjs:38-44 (createRequire + `new Database(dbPath,{fileMustExist:true})`)
    - _work/rapid-llm-proxy/src/token-usage.ts:574-581 (insertStmt — 21-col order, copy verbatim)
    - _work/rapid-llm-proxy/src/token-usage.ts:588-595 (MAX(id)+1 seed per user_hash)
    - _work/rapid-llm-proxy/src/token-usage.ts:788-824 (logCall best-effort try/catch + coalescing discipline)
    - _work/rapid-llm-proxy/src/token-usage.ts:46-47 (user_hash regex)
    - tests/token-adapters/wal-concurrency.test.mjs (Wave 0 — the CREATE TABLE shape + busy_timeout pattern)
  </read_first>
  <behavior>
    - openTokenDb(dbPath) returns a db opened with `fileMustExist:true` and `busy_timeout = 5000` set; re-asserting `journal_mode=wal` is harmless.
    - insertTokenRow(db, row) allocates id via `SELECT COALESCE(MAX(id),0)+1 ... WHERE user_hash = ?` for the row's adapter user_hash, then INSERTs all 21 columns with `?` placeholders.
    - Every numeric field is coalesced `?? 0`; every TEXT `?? ''`; `overhead_ms ?? null` (better-sqlite3 rejects undefined).
    - A locked DB / malformed row NEVER throws out of insertTokenRow — it catches, writes a `[token-adapter]` stderr line, and returns falsy.
    - ADAPTER_USER_HASH_CLAUDE === 'cladpt', ADAPTER_USER_HASH_COPILOT === 'copadt' (both match /^[a-z][a-z0-9]{5}$/).
  </behavior>
  <action>
    Create `lib/lsl/token/token-db.mjs` exporting `openTokenDb(dbPath)`, `insertTokenRow(db, row)`, and the constants `ADAPTER_USER_HASH_CLAUDE='cladpt'` / `ADAPTER_USER_HASH_COPILOT='copadt'` (D-06 — distinct id-space from the proxy; both match the `/^[a-z][a-z0-9]{5}$/` validation at token-usage.ts:47). `openTokenDb` uses createRequire for `better-sqlite3`, opens with `{ fileMustExist: true }`, and immediately sets `db.pragma('busy_timeout = 5000')` (D-07 — second-writer per-connection setting). `insertTokenRow` reads the caller-supplied `row.user_hash` (one of the two adapter constants), seeds the id via `SELECT COALESCE(MAX(id),0)+1 AS next FROM token_usage WHERE user_hash = ?`, and runs the verbatim 21-column INSERT from token-usage.ts:574-581 with parameterized `?` placeholders ONLY (never interpolate model/task_id — V5/SQL-injection). Coalesce every numeric `?? 0`, every TEXT `?? ''`, `overhead_ms ?? null`. Wrap the whole body in try/catch mirroring logCall (token-usage.ts:788-824): on failure write `[token-adapter] insert failed (non-fatal): <msg>` to stderr (NOT console.log) and return false; on success return true. Author `tests/token-adapters/token-db.test.js` (jest) against a temp DB created with the Wave-0 CREATE TABLE shape: assert a row inserts and reads back with the distinct user_hash; assert two sequential inserts get incrementing ids in the adapter's user_hash space; assert a row with `undefined` numeric fields coalesces to 0 (no NaN/null in NOT-NULL columns); assert insertTokenRow returns false and does NOT throw when handed a closed/locked db.
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/token-db.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/token-db.test.js` passes. `grep -c "busy_timeout = 5000" lib/lsl/token/token-db.mjs` ≥ 1. `grep -E "ADAPTER_USER_HASH_CLAUDE *= *'cladpt'" lib/lsl/token/token-db.mjs` matches; `grep -E "ADAPTER_USER_HASH_COPILOT *= *'copadt'" lib/lsl/token/token-db.mjs` matches. `grep -c "console.log" lib/lsl/token/token-db.mjs` (excluding comments) == 0. The SQL contains 21 `?` placeholders and no string interpolation of model/task_id.
  </acceptance_criteria>
  <done>token-db.mjs is the single best-effort DB writer with busy_timeout, distinct adapter user_hash, coalesced columns, and never-throw INSERT; unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: task-id.mjs — single-reader live task_id resolver</name>
  <files>lib/lsl/token/task-id.mjs, tests/token-adapters/task-id.test.js</files>
  <read_first>
    - scripts/measurement-start.mjs:29-52 (host-side dist import of measurement-span.js via pathToFileURL)
    - _work/rapid-llm-proxy/src/measurement-span.ts:256-265 (resolveLiveTaskId returns '' / never throws)
    - .planning/phases/69-claude-copilot-token-adapters/69-RESEARCH.md § "The single span reader (D-03)" (LLM_PROXY_DATA_DIR resolution; do NOT add a second JSON parser)
  </read_first>
  <behavior>
    - resolveLiveTaskIdSafe() dynamic-imports resolveLiveTaskId from the proxy dist and returns its string value.
    - When the dist import fails OR the reader throws, resolveLiveTaskIdSafe returns '' (best-effort, never throws).
    - It does NOT add a second parser of active-measurement.json (D-03 — it only calls resolveLiveTaskId).
  </behavior>
  <action>
    Create `lib/lsl/token/task-id.mjs` exporting `async resolveLiveTaskIdSafe(overrideDataDir?)`. It resolves the proxy dist via `process.env.LLM_PROXY_DIST_DIR ?? '/Users/Q284340/Agentic/_work/rapid-llm-proxy/dist'`, dynamic-imports `measurement-span.js` via `pathToFileURL(path.join(distDir,'measurement-span.js')).href` (copy the import block from measurement-start.mjs:31-52), and calls `resolveLiveTaskId(overrideDataDir)` — the ONLY active-span reader (D-03; do NOT re-read active-measurement.json). Wrap the import+call in try/catch; on ANY failure write a `[token-adapter] task_id resolve failed (non-fatal): <msg>` stderr line and return `''`. Memoize the imported module so repeated calls don't re-import. Author `tests/token-adapters/task-id.test.js` (jest): assert that with `LLM_PROXY_DIST_DIR` pointed at a temp stub module exporting `resolveLiveTaskId: () => 'task-abc'`, resolveLiveTaskIdSafe returns `'task-abc'`; assert that with the stub's `resolveLiveTaskId` throwing, it returns `''` and does not throw; assert that with `LLM_PROXY_DIST_DIR` pointed at a nonexistent path it returns `''`.
  </action>
  <verify>
    <automated>npx jest tests/token-adapters/task-id.test.js</automated>
  </verify>
  <acceptance_criteria>
    `npx jest tests/token-adapters/task-id.test.js` passes. `grep -c "resolveLiveTaskId" lib/lsl/token/task-id.mjs` ≥ 1; `grep -c "active-measurement.json" lib/lsl/token/task-id.mjs` == 0 (proves no second parser). `grep -v '^[[:space:]]*//' lib/lsl/token/task-id.mjs | grep -c "console.log"` == 0.
  </acceptance_criteria>
  <done>task-id.mjs wraps the single span reader, returns '' on any failure, never throws, and adds no second JSON parser; unit-tested with a stubbed dist module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter process → token-usage.db | Second writer into the proxy-owned WAL DB |
| adapter process → proxy dist module | Dynamic import of getActiveMeasurement/resolveLiveTaskId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-id | Tampering (integrity) | id allocation in shared DB | mitigate | Distinct adapter `user_hash` ('cladpt'/'copadt') so `MAX(id)+1` never overlaps the proxy's in-memory counter (D-06) |
| T-69-sql | Tampering | INSERT with log-derived strings (model, task_id) | mitigate | Parameterized `?` placeholders only; never interpolate (token-usage.ts:574-581 discipline) |
| T-69-nan | Tampering | NOT-NULL numeric columns | mitigate | Coalesce every numeric `?? 0`, TEXT `?? ''`, overhead_ms `?? null` before INSERT (V5) |
| T-69-dos | Denial of Service | locked DB / malformed row | mitigate | Whole INSERT in try/catch, never throws out of ingestion (D-08 best-effort) |
| T-69-span | Information disclosure / DoS | corrupt active-measurement.json | mitigate | resolveLiveTaskIdSafe catches all and returns ''; uses only the single span reader (D-03) |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Both unit suites pass.
- token-db.mjs sets busy_timeout=5000 and uses the two distinct adapter user_hash constants.
- task-id.mjs contains no second parser of active-measurement.json.
- No console.log in either new module.
</verification>

<success_criteria>
- The shared DB writer + task_id resolver exist as the contracts Plans 03/04 build against.
- Both are best-effort and never throw out of ingestion.
- Distinct adapter user_hash isolates the id-space (D-06); WAL coexistence open (D-07).
</success_criteria>

<output>
Create `.planning/phases/69-claude-copilot-token-adapters/69-02-SUMMARY.md` when done.
</output>
