---
phase: 46-per-system-documentation-onboarding
plan: 05
type: execute
wave: 3
depends_on:
  - 46-01
  - 46-02
  - 46-03
  - 46-04
files_modified:
  - lib/km-core/docs/ONBOARDING.md
  - tests/onboarding/cleanup-verifier.spec.ts
autonomous: false
requirements:
  - DOC-01
user_setup: []
tags: [documentation, onboarding, km-core, lsl-heartbeat-rotator]

must_haves:
  truths:
    - "lib/km-core/docs/ONBOARDING.md exists and walks the LslHeartbeatRotator exercise per D-46-04"
    - "Each of the 7 onboarding steps has a runnable shell command AND an expected-output assertion (per SC-3 verifiable-step requirement)"
    - "Step 4 uses POST /api/v1/entities (NOT /api/v1/ingest) per PATTERNS.md P-3 resolved planner item"
    - "Step 6 verifies via the Phase 45 unified viewer at http://localhost:3032/viewer/coding, with a precheck curl to /api/health"
    - "Step 7 (cleanup) is in a `!!! danger` MkDocs admonition per P-7 + RESEARCH Pitfall #2"
    - "ONBOARDING.md surfaces the no-evolutionary-names constraint risk for LslHeartbeatRotator with OVERRIDE_CONSTRAINT rationale (per RESEARCH Pitfall #7)"
    - "tests/onboarding/cleanup-verifier.spec.ts NEW — asserts no LslHeartbeatRotator entity exists in the live KG (catches cleanup-amnesia per RESEARCH Pitfall #11)"
  artifacts:
    - path: lib/km-core/docs/ONBOARDING.md
      provides: "7-step verifiable contributor onboarding guide for the LslHeartbeatRotator exercise (SC-3 enforcement surface)"
      contains: "LslHeartbeatRotator"
    - path: tests/onboarding/cleanup-verifier.spec.ts
      provides: "Test that fails if a tutorial LslHeartbeatRotator entity is still in the live KG (cleanup-amnesia mitigation)"
  key_links:
    - from: lib/km-core/docs/ONBOARDING.md
      to: lib/km-core/src/api/handlers/entities.ts
      via: Step 7 cleanup uses DELETE /api/v1/entities/{id} (handler at lines 166-170)
      pattern: "curl -X DELETE"
    - from: lib/km-core/docs/ONBOARDING.md
      to: http://localhost:12436/api/v1/entities
      via: Step 4 POST + Step 5 GET commands
      pattern: "api/v1/entities"
    - from: lib/km-core/docs/ONBOARDING.md
      to: http://localhost:3032/viewer/coding
      via: Step 6 viewer-verification URL
      pattern: "viewer/coding"
    - from: lib/km-core/README.md
      to: lib/km-core/docs/ONBOARDING.md
      via: Tests/Verify section link (this plan satisfies the forward-ref from Plan 46-01)
      pattern: "ONBOARDING.md"
  threats:
    - id: T-46-05-WRONG-ENDPOINT
      category: Information disclosure (guide uses wrong write endpoint, contributor's tutorial entity never lands)
      component: Step 4 POST endpoint
      disposition: mitigate
      mitigation: "PATTERNS.md P-3 resolved to POST /api/v1/entities via direct inspection of lib/km-core/src/api/handlers/entities.ts:110-145; verification asserts the literal endpoint string in ONBOARDING.md"
    - id: T-46-05-CLEANUP-AMNESIA
      category: Tampering (tutorial entity pollutes live KG long-term)
      component: Step 7 cleanup discipline
      disposition: mitigate
      mitigation: "Step 7 in `!!! danger` admonition; new cleanup-verifier spec catches stale tutorial entities in the live KG"
    - id: T-46-05-NAME-FALSE-POSITIVE
      category: Tampering (no-evolutionary-names regex blocks the exercise)
      component: LslHeartbeatRotator name in ONBOARDING.md prose
      disposition: mitigate
      mitigation: "ONBOARDING.md includes the OVERRIDE_CONSTRAINT: no-evolutionary-names header with rationale for contributors; the same override applies when the contributor runs through the exercise"
    - id: T-46-05-VIEWER-DOWN
      category: Denial of service (Phase 45 viewer not running, contributor interprets step failure as their own bug)
      component: Step 6 viewer dependency
      disposition: mitigate
      mitigation: "Step 6 begins with a precheck `curl http://localhost:3032/api/health` and surfaces a recovery hint if it fails"
---

<objective>
Author `lib/km-core/docs/ONBOARDING.md` — the 7-step verifiable contributor onboarding guide walking the fictional `LslHeartbeatRotator` SubComponent exercise per D-46-04 + SC-3. Add a cleanup-verifier test to mitigate cleanup amnesia.

Purpose: SC-3 requires a verifiable onboarding flow (clone -> tests -> ontology -> ingest -> verify) with each step assertable. The exercise is the KM-Core's documented "first-touch" experience for new contributors. The planner-resolved facts from PATTERNS.md P-3 (POST /api/v1/entities, LiveLoggingSystem is a Component instance not a class, port 12436) lock the canonical commands.

Output: NEW `lib/km-core/docs/ONBOARDING.md` (the guide) + NEW `tests/onboarding/cleanup-verifier.spec.ts` (the cleanup-amnesia mitigation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-per-system-documentation-onboarding/46-CONTEXT.md
@.planning/phases/46-per-system-documentation-onboarding/46-RESEARCH.md
@.planning/phases/46-per-system-documentation-onboarding/46-PATTERNS.md
@.planning/phases/46-per-system-documentation-onboarding/46-01-SUMMARY.md
@lib/km-core/README.md
@lib/km-core/src/api/handlers/entities.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author lib/km-core/docs/ONBOARDING.md (7 verifiable steps)</name>
  <files>lib/km-core/docs/ONBOARDING.md</files>
  <action>Create `lib/km-core/docs/ONBOARDING.md` transcribing the exercise from 46-RESEARCH.md § "Onboarding Exercise Script" verbatim, with these mandatory adjustments per PATTERNS.md P-3 (planner-resolved facts):

  Top of file (frontmatter / preamble):
  - HTML comment block: `<!-- OVERRIDE_CONSTRAINT: no-evolutionary-names -->` with rationale: `LslHeartbeatRotator is a real architectural name for a tutorial SubComponent — NOT an "Enhanced"/"v2" rename. The constraint regex may pattern-match the "Rotator" suffix; the override prevents false-positive blocks during the exercise.` (per RESEARCH Pitfall #7)
  - Title: `# KM-Core Onboarding: The LslHeartbeatRotator Exercise`
  - 2-sentence intro stating SC-3 alignment (clone -> tests -> ontology -> ingest -> verify -> cleanup).
  - 1-sentence pointer back to `../README.md`.

  The 7 steps verbatim from RESEARCH § Onboarding Exercise Script, with each step formatted as:
  - `## Step N — <title>`
  - prose paragraph (1-3 sentences)
  - shell code block (the command)
  - bold "Expected output:" line + expected output

  Step-by-step content (per PATTERNS.md P-3 + RESEARCH):
  - **Step 0 — Prerequisites:** clone + submodule update
  - **Step 1 — Build + test KM-Core:** `cd lib/km-core && npm install && npm run build && npm test`; expected: vitest all GREEN
  - **Step 2 — Inspect ontology:** `cat .data/ontologies/coding-ontology.json | jq '.classes.SubComponent'`; expected: JSON object showing properties `componentName`, `parentComponent`, `level`. Include the prose note from RESEARCH: "LiveLoggingSystem is a Component instance in the live KG, NOT an ontology class — the exercise registers a new SubComponent INSTANCE whose componentName field references it"
  - **Step 3 — Inspect existing LSL SubComponents:** the `jq` query from RESEARCH that lists LSL* SubComponents from `general.json`; expected: 5-8 LSL* entries
  - **Step 4 — Ingest the tutorial entity:** the `curl -X POST http://localhost:12436/api/v1/entities` command from RESEARCH (LOCKED to /api/v1/entities per P-3). Include the exact JSON body from RESEARCH (with name, entityType, ontologyClass, layer, componentName, description fields). Expected: `{"id": "019...", "status": "created"}` (UUIDv7)
  - **Step 5 — Verify via API:** `curl 'http://localhost:12436/api/v1/entities?ontologyClass=SubComponent' | jq '.data[] | select(.name == "LslHeartbeatRotator")'`; expected: tutorial entity surfaces
  - **Step 6 — Verify via unified viewer (Phase 45):** PRECHECK: `curl http://localhost:3032/api/health`. THEN: open `http://localhost:3032/viewer/coding` in a browser; search FilterRail for `LslHeartbeatRotator`; expected: entity appears in WebGL graph + EntityDetailPanel shows tutorial description. (Add prose: "If precheck curl fails, restart Phase 45 viewer per the system-health-dashboard ops in CLAUDE.md before retrying.")
  - **Step 7 — Cleanup (MANDATORY):** wrap this entire step in a `!!! danger "Cleanup is mandatory — DO NOT skip"` MkDocs admonition. Reason for switch from purge-knowledge-entities.js: the script only filters by date+team, NOT by entity name — running it would catastrophically sweep ALL entities created on the tutorial date. We delete only the tutorial entity by id via the km-core REST DELETE handler (`lib/km-core/src/api/handlers/entities.ts:166-170`).

    Show two commands. First, a safe preview that lists what will be deleted:
    ```bash
    curl -s "http://localhost:12436/api/v1/entities?ontologyClass=SubComponent" \
      | jq '.data[] | select(.name=="LslHeartbeatRotator") | {id, name}'
    ```
    Expected: a single JSON object with the tutorial entity's UUIDv7 id and the name `LslHeartbeatRotator`. If the result is empty, the entity is already gone — skip the delete.

    Then the actual delete:
    ```bash
    ENTITY_ID=$(curl -s "http://localhost:12436/api/v1/entities?ontologyClass=SubComponent" \
      | jq -r '.data[] | select(.name=="LslHeartbeatRotator") | .id')
    curl -X DELETE "http://localhost:12436/api/v1/entities/${ENTITY_ID}"
    ```
    Expected: `{"success":true}` (or HTTP 200 with the deleted entity body). Add a post-cleanup verification: re-run the SubComponent query and assert the result list does NOT contain `LslHeartbeatRotator`.

  After Step 7: a "Caveats" subsection enumerating the four caveats from RESEARCH § "Caveats the planner must surface" (dedup overlap, cleanup mandatory, no-evolutionary-names override, REST endpoint stability).</action>
  <verify>
    <automated>test -f lib/km-core/docs/ONBOARDING.md && grep -q 'OVERRIDE_CONSTRAINT: no-evolutionary-names' lib/km-core/docs/ONBOARDING.md && grep -q 'LslHeartbeatRotator' lib/km-core/docs/ONBOARDING.md && grep -q 'POST http://localhost:12436/api/v1/entities' lib/km-core/docs/ONBOARDING.md && ! grep -q '/api/v1/ingest' lib/km-core/docs/ONBOARDING.md && grep -q 'http://localhost:3032/viewer/coding' lib/km-core/docs/ONBOARDING.md && grep -q 'http://localhost:3032/api/health' lib/km-core/docs/ONBOARDING.md && grep -qE '!!! danger|!!! warning' lib/km-core/docs/ONBOARDING.md && grep -q 'curl -X DELETE' lib/km-core/docs/ONBOARDING.md && ! grep -q 'purge-knowledge-entities.js' lib/km-core/docs/ONBOARDING.md && grep -cE '^## Step [0-9]' lib/km-core/docs/ONBOARDING.md | grep -qE '^[78]$' && grep -q 'componentName' lib/km-core/docs/ONBOARDING.md</automated>
  </verify>
  <done>ONBOARDING.md exists with 7-8 `## Step N` sections (Step 0 prerequisites + Steps 1-7), LslHeartbeatRotator named everywhere appropriate, POST /api/v1/entities locked (no /api/v1/ingest occurrences), Phase 45 viewer + health precheck both present, `!!! danger` admonition wrapping cleanup, no-evolutionary-names override present.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/onboarding/cleanup-verifier.spec.ts (cleanup-amnesia mitigation)</name>
  <files>tests/onboarding/cleanup-verifier.spec.ts</files>
  <action>Create a Vitest spec at `tests/onboarding/cleanup-verifier.spec.ts`. The test queries the live KG (via the obs-api at `http://localhost:12436/api/v1/entities?ontologyClass=SubComponent`) and asserts that NO entity named `LslHeartbeatRotator` is present. Failure modes the test must handle gracefully:
  - obs-api not running -> `it.skip` or `test.skip` with a clear console message ("obs-api at :12436 not reachable; skipping cleanup verifier")
  - obs-api running but returns malformed response -> fail loudly with the diagnostic
  - tutorial entity present -> FAIL with a remediation hint that points to Step 7 of `lib/km-core/docs/ONBOARDING.md` (the `curl -X DELETE http://localhost:12436/api/v1/entities/{id}` command) for cleanup. The hint message must include the literal command form so a contributor can copy-paste it without re-reading the guide.

  The test SHOULD use `fetch` (or `node-fetch` if the project's vitest setup requires it — match the convention of other specs in `tests/`). Add a top-of-file comment explaining the role: "Mitigation for T-46-05-CLEANUP-AMNESIA: ensures Phase 46 onboarding contributors haven't left their LslHeartbeatRotator tutorial entity in the live KG."

  Do NOT use `console.log` (per CLAUDE.md no-console-log rule); use `process.stderr.write` if any diagnostic output is needed. Do NOT add this spec to default `npm test` if doing so would slow CI; instead, add it under `tests/onboarding/` and document in ONBOARDING.md that contributors can run `npx vitest run tests/onboarding/cleanup-verifier.spec.ts` after the exercise.</action>
  <verify>
    <automated>test -f tests/onboarding/cleanup-verifier.spec.ts && grep -q 'LslHeartbeatRotator' tests/onboarding/cleanup-verifier.spec.ts && grep -qE 'http://localhost:12436|api/v1/entities' tests/onboarding/cleanup-verifier.spec.ts && ! grep -E '^\s*console\.' tests/onboarding/cleanup-verifier.spec.ts && grep -qE 'describe|it|test' tests/onboarding/cleanup-verifier.spec.ts</automated>
  </verify>
  <done>Spec file exists, queries obs-api for LslHeartbeatRotator presence, uses vitest primitives, contains no `console.*` calls.</done>
</task>

<task type="auto">
  <name>Task 3: Verify ONBOARDING.md is reachable from KM-Core README (closes Plan 46-01 forward-ref)</name>
  <files>lib/km-core/README.md</files>
  <action>Plan 46-01 wrote a forward-ref to `docs/ONBOARDING.md` in the KM-Core README's Tests / Verify section, BEFORE this file existed. Now that ONBOARDING.md exists (Task 1), verify the link in `lib/km-core/README.md` actually resolves. Run: `grep -F 'ONBOARDING.md' lib/km-core/README.md` to confirm the reference; run `test -f lib/km-core/docs/ONBOARDING.md` to confirm the target. If the reference in 46-01 used a slightly different path (e.g., `docs/onboarding.md` lowercase), correct the README. Do NOT modify the README's structural sections; this task is a targeted link-fix only if needed.</action>
  <verify>
    <automated>grep -q 'docs/ONBOARDING.md\|ONBOARDING.md' lib/km-core/README.md && test -f lib/km-core/docs/ONBOARDING.md</automated>
  </verify>
  <done>KM-Core README's ONBOARDING.md reference resolves to an actual file on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator dry-run — walk the exercise end-to-end</name>
  <what-built>
    - `lib/km-core/docs/ONBOARDING.md` (7-step verifiable guide)
    - `tests/onboarding/cleanup-verifier.spec.ts` (cleanup-amnesia mitigation)
  </what-built>
  <how-to-verify>
    1. Open `lib/km-core/docs/ONBOARDING.md` in a markdown previewer. Verify all 7 steps render with code blocks + Expected-output lines.
    2. Walk the exercise yourself end-to-end:
       - Run Step 2's `jq` command. Expected: JSON object with `componentName / parentComponent / level` properties.
       - Run Step 3's `jq` query. Expected: 5-8 LSL* SubComponent entries.
       - Run Step 4's POST command. Expected: a JSON response with `id` (UUIDv7) and `status: "created"` OR equivalent km-core success envelope.
       - Run Step 5's GET command. Expected: the tutorial entity surfaces.
       - Run Step 6's precheck (`curl http://localhost:3032/api/health`). If GREEN, open the viewer URL and verify the entity appears in the FilterRail.
       - Run Step 7's cleanup command. Expected: purge confirmation.
       - Run the post-cleanup curl. Expected: empty/null.
    3. Run the cleanup verifier spec: `npx vitest run tests/onboarding/cleanup-verifier.spec.ts`. Expected: PASS (entity is gone after Step 7).
    4. If any step fails, capture the failure mode and either fix the guide (path/command) or document it as a known caveat — surface to operator before approving.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Plan 46-05 complete + Wave 3 done, or describe the failing step (which command, what error, what was expected vs got).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The guide's instructions cross the **contributor -> live KG** boundary at Step 4 (ingest). The cleanup step crosses the **contributor -> production data** boundary. No code execution on untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-05-WRONG-ENDPOINT | Information disclosure | Step 4 POST endpoint | mitigate | PATTERNS.md P-3 locked to POST /api/v1/entities; verification asserts literal string + asserts /api/v1/ingest absent |
| T-46-05-CLEANUP-AMNESIA | Tampering (long-term KG pollution) | Step 7 cleanup discipline | mitigate | `!!! danger` admonition wrapping Step 7; cleanup-verifier spec catches stale entities in the live KG |
| T-46-05-NAME-FALSE-POSITIVE | Tampering (no-evolutionary-names blocks the exercise) | LslHeartbeatRotator name | mitigate | OVERRIDE_CONSTRAINT header in ONBOARDING.md with rationale |
| T-46-05-VIEWER-DOWN | Denial of service (false-failure on Step 6) | Phase 45 viewer dependency | mitigate | Step 6 begins with `curl http://localhost:3032/api/health` precheck + recovery hint |
| T-46-05-DEDUP-COLLAPSE | Information disclosure (tutorial entity silently fuzzy-merged into real LSL entity) | Wave-analysis dedup | mitigate | Caveat #1 in Caveats subsection: warn contributors that name changes need re-verification of dedup safety; LslHeartbeatRotator chosen because Jaccard 0.45 / containment 0.7 thresholds will not collapse it against existing LSL* names |
</threat_model>

<verification>
```bash
# ONBOARDING.md structure + content
test -f lib/km-core/docs/ONBOARDING.md
grep -q 'OVERRIDE_CONSTRAINT: no-evolutionary-names' lib/km-core/docs/ONBOARDING.md
grep -q 'LslHeartbeatRotator' lib/km-core/docs/ONBOARDING.md
grep -q 'POST http://localhost:12436/api/v1/entities' lib/km-core/docs/ONBOARDING.md
! grep -q '/api/v1/ingest' lib/km-core/docs/ONBOARDING.md
grep -q 'http://localhost:3032/viewer/coding' lib/km-core/docs/ONBOARDING.md
grep -q 'http://localhost:3032/api/health' lib/km-core/docs/ONBOARDING.md
grep -qE '!!! danger|!!! warning' lib/km-core/docs/ONBOARDING.md
grep -q 'curl -X DELETE' lib/km-core/docs/ONBOARDING.md
! grep -q 'purge-knowledge-entities.js' lib/km-core/docs/ONBOARDING.md
grep -q 'componentName' lib/km-core/docs/ONBOARDING.md
STEPS=$(grep -cE '^## Step [0-9]' lib/km-core/docs/ONBOARDING.md)
[ "$STEPS" -ge 7 ] && [ "$STEPS" -le 8 ]

# Cleanup verifier spec
test -f tests/onboarding/cleanup-verifier.spec.ts
grep -q 'LslHeartbeatRotator' tests/onboarding/cleanup-verifier.spec.ts
! grep -E '^\s*console\.' tests/onboarding/cleanup-verifier.spec.ts

# KM-Core README forward-ref now closes
grep -q 'ONBOARDING.md' lib/km-core/README.md
```
</verification>

<success_criteria>
- ONBOARDING.md exists with 7-8 Step sections, LSL canon facts (componentName), and locked POST endpoint
- `!!! danger` cleanup admonition + cleanup-verifier spec both present
- Phase 45 viewer precheck included
- OVERRIDE_CONSTRAINT: no-evolutionary-names header documented with rationale
- KM-Core README's forward-ref to ONBOARDING.md resolves
- Operator dry-run (Task 4) approved
</success_criteria>

<output>
Create `.planning/phases/46-per-system-documentation-onboarding/46-05-SUMMARY.md` when done. Record:
- Final step count + any variant text deviating from the RESEARCH script (and why)
- Any step that failed during operator dry-run + the fix applied
- Whether the cleanup-verifier spec ran in CI or stays as a manual contributor-run check
</output>
