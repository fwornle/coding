---
phase: 46-per-system-documentation-onboarding
plan: 06
type: execute
wave: 4
depends_on:
  - 46-01
  - 46-02
  - 46-03
  - 46-04
  - 46-05
files_modified:
  - README.md
  - lib/km-core/README.md
  - integrations/mcp-server-semantic-analysis/README.md
  - .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md
autonomous: false
requirements:
  - DOC-01
user_setup: []
tags: [documentation, cross-reference, sc-4]

must_haves:
  truths:
    - "Every Phase 46 README (A=root, B=mcp-server-semantic-analysis, C=OKM, KM-Core=lib/km-core) contains a Related Systems section linking to the OTHER THREE per SC-4"
    - "All four READMEs render the same canonical link form for KM-Core (no broken or stale relative paths)"
    - "All four READMEs render the same canonical link form for the BMW GHE OKM URL"
    - "Cross-ref audit document records the verified link matrix (4x3 = 12 inbound links) and the explicit resolution check for each (file-exists for relative paths, http-200 or non-DNS-failure for external URLs)"
    - "If any inbound link is broken in any README, this plan FIXES it in the offending README — does NOT defer"
  artifacts:
    - path: .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md
      provides: "Documented audit matrix proving SC-4 (12 cross-references verified)"
      contains: "Cross-Reference Audit"
  key_links:
    - from: README.md
      to: lib/km-core/README.md
      via: Related Systems markdown link
      pattern: "lib/km-core/README.md"
    - from: lib/km-core/README.md
      to: README.md
      via: Related Systems markdown link
      pattern: "../../README.md"
    - from: integrations/mcp-server-semantic-analysis/README.md
      to: lib/km-core/README.md
      via: Related Systems markdown link
      pattern: "lib/km-core/README.md"
    - from: integrations/mcp-server-semantic-analysis/README.md
      to: README.md
      via: Related Systems markdown link
      pattern: "../../README.md"
  threats:
    - id: T-46-06-BROKEN-XREF
      category: Information disclosure (broken cross-ref strands contributor)
      component: All 4 READMEs' Related Systems sections
      disposition: mitigate
      mitigation: "Task 1 audit grep-checks every inbound link from each README; Task 2 fixes any breaks; Task 3 records the verified matrix"
    - id: T-46-06-STALE-PATH
      category: Information disclosure (relative path correct in one README but wrong in another)
      component: relative path consistency across A/B/KM-Core
      disposition: mitigate
      mitigation: "Audit verifies each relative target resolves via `test -f` from the README's directory; OKM's HTTPS URL is verified by string-match against the canonical BMW GHE URL"
---

<objective>
Final cross-reference sweep across all four Phase 46 READMEs to verify SC-4: each README links to the other three (12 inbound links total).

Purpose: SC-4 is the connective-tissue success criterion. Plans 46-01 through 46-05 each authored their own Related Systems section; this plan audits the resulting 4-way link matrix end-to-end and fixes any breaks. Without this sweep, SC-4 could silently fail if (say) Plan 46-03 used `../../lib/km-core/README.md` while Plan 46-02 used `lib/km-core/README.md`.

Output: An audit document at `.planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md` recording the 12-link verification matrix, and any fix-edits to A/B/KM-Core READMEs needed to make all links resolve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-per-system-documentation-onboarding/46-CONTEXT.md
@.planning/phases/46-per-system-documentation-onboarding/46-RESEARCH.md
@.planning/phases/46-per-system-documentation-onboarding/46-PATTERNS.md
@.planning/phases/46-per-system-documentation-onboarding/46-01-SUMMARY.md
@.planning/phases/46-per-system-documentation-onboarding/46-02-SUMMARY.md
@.planning/phases/46-per-system-documentation-onboarding/46-03-SUMMARY.md
@.planning/phases/46-per-system-documentation-onboarding/46-04-SUMMARY.md
@.planning/phases/46-per-system-documentation-onboarding/46-05-SUMMARY.md
@README.md
@lib/km-core/README.md
@integrations/mcp-server-semantic-analysis/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 12 inbound cross-references across the four READMEs</name>
  <files>.planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md</files>
  <action>For each of the four READMEs in the matrix (A=root, B=mcp-server-semantic-analysis, C=OKM, KM-Core=lib/km-core), audit its Related Systems section by extracting every link target and verifying it resolves. The matrix has 4 source READMEs × 3 outbound links each = 12 links total.

Audit procedure for each link:
1. **For relative paths to a coding-repo file** (e.g., `../../lib/km-core/README.md` from B, or `lib/km-core/README.md` from root): run `test -f <resolved-absolute-path>` from the source README's directory. Record PASS / FAIL.
2. **For the BMW GHE OKM URL** (`https://bmw.ghe.com/adpnext-apps/operational-knowledge-management`): string-match against the canonical URL in PATTERNS.md P-4. Do NOT try to `curl` it — operator may not be on the BMW network. Record STRING-MATCH PASS / FAIL.
3. **For the C README itself** (the one written by Plan 46-04 in `/Users/Q284340/Agentic/_work/...`): the audit cannot directly `test -f` from the coding repo because the file lives outside. Instead, record the absolute path and note "external; verified by Plan 46-04 Task 5".

The audit document at `.planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md` MUST be a markdown table with columns: `Source README | Target | Link form (literal) | Resolution check | PASS/FAIL`. Include one row per link (12 rows minimum). At the bottom, summarize: "X/12 links PASS, Y need fixing".

**Heading variant note:** A's Related Systems section uses the prefixed heading `## A: coding system — Related Systems` (per Plan 46-02 D-46-01 — A's docs are sections within the project-root README, not a standalone README, so they carry the `A: coding system — ` prefix to disambiguate from other sections). B, C, and KM-Core all use the plain `## Related Systems` heading. Record the literal heading text for each row in the audit table — do not normalize. The verification grep below correctly handles both forms via the `|` alternation.</action>
  <verify>
    <automated>test -f .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md && grep -qi 'Cross-Reference Audit\|Cross-Ref Audit\|cross-reference audit' .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md && [ "$(grep -cE '^\| ' .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md)" -ge 12 ]</automated>
  </verify>
  <done>Audit document exists, contains a 12+ row markdown table, summarizes pass/fail counts at the bottom.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any broken cross-references discovered in the audit</name>
  <files>README.md, lib/km-core/README.md, integrations/mcp-server-semantic-analysis/README.md</files>
  <action>For every FAIL row in the audit table from Task 1, open the source README and correct the link form so the target resolves. Common breakage modes:
  - Wrong relative depth (e.g., `../README.md` when `../../README.md` is correct from a deeply-nested README)
  - Wrong filename case (`README.md` vs `readme.md`)
  - Wrong BMW GHE URL (must match `https://bmw.ghe.com/adpnext-apps/operational-knowledge-management` exactly)
  - Missing leading slash or extra trailing slash

  If the audit reports ZERO failures, this task is a no-op — just record that in the SUMMARY. Do NOT modify any README that did not have a failing link.

  CRITICAL: If the OKM README (in the external repo, written by Plan 46-04) has broken inbound links, Plan 46-06 cannot fix it directly because it lives in a different git repo. In that case, record the broken-OKM-link in the audit as a follow-up requiring a separate OKM commit, and surface to operator in the checkpoint.</action>
  <verify>
    <automated>SUMMARY=$(grep -ciE '0/12 (PASS|need fixing)|12/12 PASS' .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md || true); RUN=$(grep -ciE 'FAIL' .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md || true); echo "summary-keyword:$SUMMARY fail-count-in-doc:$RUN"; true</automated>
  </verify>
  <done>Either: zero failing rows (no edits needed) AND the SUMMARY records that, or all fixable failures fixed AND any external-repo follow-ups surfaced in Task 3 checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator final SC-4 verification + Phase 46 closeout</name>
  <what-built>
    - Cross-reference audit document at `.planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md`
    - Any fix-edits to A / B / KM-Core READMEs for broken inbound links (Task 2)
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md`. Verify the 12-row table exists and the bottom summary explicitly states the PASS/FAIL count.
    2. For each of the 3 coding-repo READMEs (A=root, B=mcp-server-semantic-analysis, KM-Core=lib/km-core), open in a markdown previewer and CLICK each Related Systems link. Verify each resolves (no 404, no broken-path icon).
    3. Spot-check at least one round-trip path: from root README -> click KM-Core link -> from KM-Core README click back to root. Confirm round-trip works (the contributor can navigate among the four "doors" — direct SC-4 enforcement).
    4. Acknowledge any external-repo (OKM) follow-up surfaced by Task 2. If any, describe the remediation path before approval (separate OKM commit needed, or accept-the-gap).
    5. Run the per-plan acceptance from earlier plans one more time as a holistic smoke (no functional dependency, just confirming nothing regressed):
       - `grep -c '^## ' README.md lib/km-core/README.md integrations/mcp-server-semantic-analysis/README.md` — all three should have >= 5 second-level headings
       - `test -f lib/km-core/docs/ONBOARDING.md`
       - `test -f docs/images/km-core-architecture.png && test -f docs/images/b-architecture.png`
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 46 complete (all 6 plans done + SC-4 verified), or describe broken links + which README needs another revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The cross-repo boundary (coding -> external OKM) limits what Plan 46-06 can directly fix. No untrusted input, no auth, no code execution.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-06-BROKEN-XREF | Information disclosure | 4 READMEs' Related Systems sections | mitigate | Task 1 12-row audit + Task 2 in-repo fixes |
| T-46-06-STALE-PATH | Information disclosure (relative-path consistency) | A / B / KM-Core relative link forms | mitigate | Audit verifies each relative target via `test -f` from the source README's directory |
| T-46-06-EXTERNAL-BREAK | Information disclosure (OKM README's inbound links broken, cannot fix from coding repo) | OKM Related Systems | mitigate | Audit records external-repo failures as follow-ups; operator decides at checkpoint whether to dispatch a separate OKM commit |
</threat_model>

<verification>
```bash
# Audit doc exists with the required table size
test -f .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md
[ "$(grep -cE '^\| ' .planning/phases/46-per-system-documentation-onboarding/46-06-CROSS-REF-AUDIT.md)" -ge 12 ]

# All three coding-repo READMEs still have a Related Systems section
for r in README.md lib/km-core/README.md integrations/mcp-server-semantic-analysis/README.md; do grep -q '^## Related Systems\|## A: coding system — Related Systems' "$r" || { echo "MISSING Related Systems in $r"; exit 1; }; done

# Every coding-repo README's relative links to each other resolve
test -f lib/km-core/README.md  # link target from root
test -f README.md  # link target from B/KM-Core (via ../../README.md)
test -f integrations/mcp-server-semantic-analysis/README.md  # link target from root and KM-Core
test -f lib/km-core/docs/ONBOARDING.md  # final reachability of Plan 46-05 output

# BMW GHE OKM URL string match
for r in README.md lib/km-core/README.md integrations/mcp-server-semantic-analysis/README.md; do grep -qF 'bmw.ghe.com/adpnext-apps/operational-knowledge-management' "$r" || { echo "MISSING OKM URL in $r"; exit 1; }; done
```
</verification>

<success_criteria>
- Cross-reference audit document exists with a 12-row matrix and a PASS/FAIL summary
- All in-repo broken links fixed (or zero failures discovered)
- All three coding-repo READMEs link to each other AND to the BMW GHE OKM URL
- Operator final-verification (Task 3) approved — Phase 46 closes out
</success_criteria>

<output>
Create `.planning/phases/46-per-system-documentation-onboarding/46-06-SUMMARY.md` when done. Record:
- The audit's 12/12 PASS state (or the exact count of fixes applied)
- Any external-repo (OKM) follow-up needed + the remediation plan
- A roll-up confirmation that SC-1 through SC-4 are all satisfied by Phase 46 (Plan 46-06 closes the phase)
</output>
