---
phase: 46-per-system-documentation-onboarding
plan: 04
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/README.md
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docs/puml/okm-architecture.puml
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docs/images/okm-architecture.png
  # Conditional bootstrap copy — only created if OKM's docs/puml/_standard-style.puml is absent (Task 2 checks first):
  - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docs/puml/_standard-style.puml
autonomous: false
requirements:
  - DOC-01
user_setup: []
tags: [documentation, c-system, okm, external-repo, bmw-ghe]

must_haves:
  truths:
    - "A new README.md exists at /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/README.md (currently no README)"
    - "The new C README conforms to the 6-section template from Plan 46-01"
    - "Configurations Owned section maps OKM's actual config artifacts: ontology/*.json (RAPID upper + RaaS + KPI-FW + business lower), lib/llm/, src/ingest/adapters/, src/intelligence/"
    - "Architecture section references docs/images/okm-architecture.png (NEW PNG generated by this plan — no existing diagram per RESEARCH § Diagram Plan row 4)"
    - "Where to Edit table covers all four template rows with paths AND verify commands"
    - "Related Systems links to KM-Core (via npm registry path), A (coding root README), and B (the submodule README) per SC-4"
    - "Commit lands on OKM's currently-checked-out branch gsd/44-09-rest-cutover-v2 per P-4 (planner-resolved PR target)"
    - "Commit is pushed to BMW GHE origin via HTTPS (per P-4 + feedback_bmw_ghe_https.md)"
    - "Coding repo is NOT modified — OKM is an external sibling repo, NOT a coding-repo submodule"
  artifacts:
    - path: /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/README.md
      provides: "New C README — 6-section template, navigation entry point for the OKM repo"
      contains: "## Configurations Owned"
    - path: /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docs/puml/okm-architecture.puml
      provides: "OKM architecture PUML — context diagram (Express :8090 + viewer :3002 + ingest adapters)"
    - path: /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docs/images/okm-architecture.png
      provides: "Generated PNG for OKM README rendering (lives in OKM repo, not coding)"
  key_links:
    - from: /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/README.md
      to: docs/images/okm-architecture.png
      via: relative image ref (relative to OKM repo)
      pattern: "okm-architecture.png"
    - from: /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/README.md
      to: bmw.ghe.com upstream OKM URL OR npm @fwornle/km-core
      via: Related Systems link to KM-Core
      pattern: "km-core"
  threats:
    - id: T-46-04-WRONG-REPO
      category: Tampering (file written in wrong working tree)
      component: OKM README author tree
      disposition: mitigate
      mitigation: "Task 1 explicitly cd's into the OKM working tree before writing; Task 5 verifies via `git rev-parse --show-toplevel` that the commit landed in OKM, not coding"
    - id: T-46-04-WRONG-BRANCH
      category: Tampering (commit lands on upstream main instead of gsd/44-09-rest-cutover-v2)
      component: OKM branch state
      disposition: mitigate
      mitigation: "Task 5 verifies `git rev-parse --abbrev-ref HEAD` == `gsd/44-09-rest-cutover-v2` BEFORE pushing"
    - id: T-46-04-CONTENT-DUP
      category: Information disclosure (README duplicates long-form prose already in docs/)
      component: README.md vs OKM docs/index.md, docs/architecture.md
      disposition: mitigate
      mitigation: "Author per P-4 directive: the new README is a NAVIGATION entry point that LINKS to OKM's existing docs/ tree, not a content fork — verified by line-count budget (<= 200 lines)"
    - id: T-46-04-PNG-LOCATION
      category: Tampering (PNG written to coding repo's docs/images/ by accident)
      component: PNG output path
      disposition: mitigate
      mitigation: "Task 3 PUML render uses OKM-repo-relative `mv` paths; verification asserts PNG exists in OKM repo, NOT in coding/docs/images/"
---

<objective>
Author a new README.md for the OKM repo at `/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/` (the C system), conforming to the 6-section template, and commit it to OKM's currently-checked-out branch `gsd/44-09-rest-cutover-v2`.

Purpose: OKM currently has NO README.md — only `docs/index.md` serves as a de facto entry point, which has no Configurations Owned / Where to Edit answers. Phase 46 mandates per-system contributor docs per SC-1, and Plan 46-06 will verify SC-4 cross-references between all four READMEs (A, B, C, KM-Core). C must have a README that conforms to the template.

CRITICAL: This plan operates in an EXTERNAL git repo (BMW GHE, NOT the coding repo). The PR target (per planner resolution in PATTERNS.md P-4) is OKM's currently-checked-out branch `gsd/44-09-rest-cutover-v2` — NOT upstream main. The coding repo is NOT modified by this plan.

Output: NEW `README.md` + NEW `docs/puml/okm-architecture.puml` + NEW `docs/images/okm-architecture.png` inside the OKM repo, committed to the active branch and pushed via HTTPS to BMW GHE origin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-per-system-documentation-onboarding/46-CONTEXT.md
@.planning/phases/46-per-system-documentation-onboarding/46-RESEARCH.md
@.planning/phases/46-per-system-documentation-onboarding/46-PATTERNS.md
@.planning/phases/46-per-system-documentation-onboarding/46-01-SUMMARY.md
@lib/km-core/docs/README-TEMPLATE.md
@CLAUDE.md
@.claude/commands/documentation-style.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify OKM working tree state and branch</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management</files>
  <action>BEFORE authoring anything, validate the OKM working tree is in the expected state per PATTERNS.md P-4. Run:
  1. `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management` and `git rev-parse --show-toplevel` to confirm we are inside the OKM repo (the toplevel may be `rapid-automations` if OKM is a submodule of `rapid-automations`, OR the OKM dir itself if it's a peer — either is fine; what matters is that the OKM dir's tracked files are managed by some git repo).
  2. `git rev-parse --abbrev-ref HEAD` — expected: `gsd/44-09-rest-cutover-v2` (per P-4). If anything else, STOP and surface to operator — do NOT proceed to write the README on the wrong branch.
  3. Read `docs/index.md`, `docs/architecture.md` (if present), `docs/api-reference.md` (if present), and `CLAUDE.md` in the OKM dir, to capture content for the new README without duplicating prose.
  4. Verify no `README.md` exists yet at the OKM dir root (RESEARCH OQ-1 confirms absence). If a README somehow exists, surface to operator — Plan 46-04 is "create new", not "rewrite".</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && [ "$(git rev-parse --abbrev-ref HEAD)" = "gsd/44-09-rest-cutover-v2" ] && [ ! -f README.md ] && test -f docs/index.md</automated>
  </verify>
  <done>OKM is on branch `gsd/44-09-rest-cutover-v2`, no pre-existing README.md, docs/index.md present for content sourcing.</done>
</task>

<task type="auto">
  <name>Task 2: Author okm-architecture.puml + render + place PNG inside OKM repo</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docs/puml/okm-architecture.puml, /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/docs/images/okm-architecture.png</files>
  <action>FIRST: invoke the `documentation-style` skill. Then, operating in the OKM working tree, create `docs/puml/okm-architecture.puml`. Begin with the canonical PUML preamble: the start-uml directive with the lowercase-hyphenated name `okm-architecture`, followed by the include of `_standard-style.puml`. **Caveat:** the OKM repo may NOT have a `_standard-style.puml` in `docs/puml/`. Check first: `ls docs/puml/_standard-style.puml`. If absent: copy `/Users/Q284340/Agentic/coding/docs/puml/_standard-style.puml` into the OKM repo's `docs/puml/` (one-shot bootstrap) and add it to that file's `files_modified` list as a NOTE in the SUMMARY. If present, use as-is.

  Author the OKM context diagram per RESEARCH § Diagram Plan row 4: show the Express API on port 8090, the OKB viewer on port 3002, the ingest adapters (MkDocs, Confluence, CodeBeamer), the LLM provider chain, and the GraphKMStore (km-core consumer). Group by layer: ingest -> intelligence -> store -> api -> viewer.

  Render via `plantuml docs/puml/okm-architecture.puml`. Move generated PNG into OKM repo: `mv docs/puml/okm-architecture.png docs/images/okm-architecture.png`. **DO NOT** copy/move this PNG to the coding repo's `docs/images/` or `docs-content/images/` — this PNG belongs to the OKM repo. There is NO two-image-dir gotcha for OKM (it's not in the coding-repo MkDocs tree).</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && test -f docs/puml/okm-architecture.puml && head -5 docs/puml/okm-architecture.puml | grep -q 'okm-architecture' && grep -q '_standard-style.puml' docs/puml/okm-architecture.puml && test -f docs/images/okm-architecture.png && ! test -f /Users/Q284340/Agentic/coding/docs/images/okm-architecture.png</automated>
  </verify>
  <done>OKM PUML + PNG exist inside OKM repo; PNG was NOT placed in coding repo's docs/images/.</done>
</task>

<task type="auto">
  <name>Task 3: Author OKM README.md to 6-section template</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/README.md</files>
  <action>Operating in the OKM working tree, create new `README.md` conforming to the 6-section template from Plan 46-01, using RESEARCH § System-by-System Edit Map > C row for content. Target length: <= 200 lines. The README is a NAVIGATION entry point — link to OKM's existing `docs/` content; do NOT duplicate long-form prose.

Section 1 (Header + 1-sentence role): one-line description of OKM as the graph-backed operational knowledge base for RAPID-Automations, built on km-core.

Section 2 (Configurations Owned) bullets:
- Ontology: `ontology/*.json` (RAPID upper + RaaS + KPI-FW + business lower ontologies)
- LLM providers: `lib/llm/providers/` + `lib/llm/config.json`
- Ingest adapters: `src/ingest/adapters/` (MkDocs, Confluence, CodeBeamer)
- Domain dedup/scoring: `src/intelligence/` (dedup, clustering, scoring overlays on top of km-core baseline)

Section 3 (Architecture): image ref `![OKM architecture](docs/images/okm-architecture.png)` + 3-5 sentence summary describing the Express API on :8090, the OKB viewer on :3002, and the ingest -> intelligence -> store -> api -> viewer layering. End with: `For detailed architecture, see [docs/architecture.md](docs/architecture.md).` (if that file exists; otherwise link to `docs/index.md`).

Section 4 (Where to Edit) table with these 4 rows:
- "add a lower ontology" -> `ontology/<domain>-ontology.json` + register in `ontology/index.json` (if present); verify: `npm test -- ontology`
- "add an LLM provider" -> `lib/llm/providers/<provider>.ts`; verify: `npm test -- llm`
- "add an ingest source" -> `src/ingest/adapters/<adapter>.ts`; verify: `npm test -- ingest`
- "change a dedup/scoring rule" -> `src/intelligence/dedup.ts` or `src/intelligence/scoring.ts`; verify: `npm test -- intelligence`

Section 5 (Related Systems) bullets:
- `[KM-Core](https://www.npmjs.com/package/@fwornle/km-core)` — shared types / store / REST contracts that OKM builds on
- `[A: coding](https://github.com/fwornle/coding)` — sister-system using same km-core
- `[B: mcp-server-semantic-analysis](https://github.com/fwornle/mcp-server-semantic-analysis)` — semantic analysis agent pipeline using same km-core

Section 6 (Tests / Verify) code block: `npm test` + `curl http://localhost:8090/api/stats`

DO NOT duplicate long-form prose from `docs/index.md` or `docs/api-reference.md` — LINK to those files instead.</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && L=$(wc -l < README.md); [ "$L" -le 200 ] && grep -q '^## Configurations Owned' README.md && grep -q '^## Architecture' README.md && grep -q '^## Where to Edit' README.md && grep -q '^## Related Systems' README.md && grep -q '^## Tests / Verify' README.md && grep -q 'okm-architecture.png' README.md && grep -qE 'km-core|@fwornle/km-core' README.md && grep -q 'coding' README.md && grep -q 'mcp-server-semantic-analysis' README.md</automated>
  </verify>
  <done>README.md is <= 200 lines, contains all five section headings in order, references the new PNG, and contains the three cross-system links per SC-4.</done>
</task>

<task type="auto">
  <name>Task 4: Commit + push to OKM origin (HTTPS) on gsd/44-09-rest-cutover-v2</name>
  <files>/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management</files>
  <action>Operating in the OKM working tree, run a final pre-commit safety check: `git rev-parse --abbrev-ref HEAD` MUST equal `gsd/44-09-rest-cutover-v2` (per P-4). If not, ABORT — do NOT push to the wrong branch.

  Stage the new files: `git add README.md docs/puml/okm-architecture.puml docs/images/okm-architecture.png` (and `docs/puml/_standard-style.puml` if the bootstrap copy from Task 2 was needed). Commit with message: `docs(46-04): add README.md per 6-section template + okm-architecture PUML` followed by a Co-Authored-By trailer.

  Push to origin via HTTPS (per `feedback_bmw_ghe_https.md`): `git push origin gsd/44-09-rest-cutover-v2`. If the push fails because the BMW GHE tunnel is not active OR HTTPS proxy is misconfigured, surface clearly to operator — do NOT silently leave the commit unpushed. Do NOT use `--no-verify`. Do NOT rewrite origin to SSH.

  **CRITICAL:** Do NOT touch the coding repo working tree in this task. Verify before declaring done: `cd /Users/Q284340/Agentic/coding && git status` shows NO modifications attributable to this plan (other than the planning files themselves).</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && [ "$(git rev-parse --abbrev-ref HEAD)" = "gsd/44-09-rest-cutover-v2" ] && git log -1 --format='%s' | grep -qiE '46-04|README' && git status --porcelain README.md docs/puml/okm-architecture.puml docs/images/okm-architecture.png | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>OKM commit exists on `gsd/44-09-rest-cutover-v2` with the README + PUML + PNG; OKM working tree is clean for the touched paths; coding repo has no unintended modifications attributable to this plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator visual check — OKM README + branch + push</name>
  <what-built>
    - NEW `README.md` at `/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/`
    - NEW `docs/puml/okm-architecture.puml` + generated PNG inside OKM repo
    - Commit on OKM branch `gsd/44-09-rest-cutover-v2`, pushed to BMW GHE origin via HTTPS
  </what-built>
  <how-to-verify>
    1. `cd /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management && git log --oneline -3` — verify the most recent commit is the docs(46-04) commit.
    2. Confirm branch: `git rev-parse --abbrev-ref HEAD` MUST print `gsd/44-09-rest-cutover-v2`.
    3. Open the OKM README in a markdown previewer. Verify the five `## ` sections appear in order; verify the architecture PNG renders.
    4. Click the three Related Systems links — verify they open the KM-Core npm page, the coding repo, and the mcp-server-semantic-analysis repo (or at least do not 404 if you have access).
    5. Confirm `git status` in the coding repo (`cd /Users/Q284340/Agentic/coding && git status`) shows NO modifications outside `.planning/` — the OKM work is contained in the external repo.
    6. Confirm `git ls-remote origin gsd/44-09-rest-cutover-v2` (from OKM) shows your push landed on BMW GHE.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Plan 46-04 complete, or describe issues (wrong branch, push not pushed, README on wrong template, image broken, coding-repo accidentally modified).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The cross-repo boundary (coding repo working tree <-> external OKM/BMW GHE repo working tree) is the dominant trust concern. No untrusted input handling, no auth changes beyond using HTTPS for BMW GHE push.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-04-WRONG-REPO | Tampering (file written in wrong working tree) | OKM README write path | mitigate | Task 1 explicit `cd` and `git rev-parse --show-toplevel` precheck; Task 4 post-commit check that coding repo has no unintended modifications |
| T-46-04-WRONG-BRANCH | Tampering (commit lands on upstream main) | OKM branch state | mitigate | Task 1 + Task 4 both verify branch == `gsd/44-09-rest-cutover-v2` before write/push |
| T-46-04-CONTENT-DUP | Information disclosure (README forks docs/) | README.md vs OKM docs/ | mitigate | Per P-4: README is navigation; line-budget <= 200 enforced |
| T-46-04-PNG-LOCATION | Tampering (PNG written to coding repo by accident) | PNG output path | mitigate | Verification: PNG in OKM repo path, NOT in coding/docs/images/ |
| T-46-04-BMW-GHE-AUTH | Tampering / Spoofing (push fails silently or routed via wrong remote) | OKM push to origin | mitigate | Per `feedback_bmw_ghe_https.md` use HTTPS; Task 4 surfaces push failures to operator; do NOT silently leave commit unpushed |
</threat_model>

<verification>
```bash
# OKM dir state
OKM=/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management

# README structural conformance
test -f "$OKM/README.md"
L=$(wc -l < "$OKM/README.md"); [ "$L" -le 200 ]
for h in '^## Configurations Owned' '^## Architecture' '^## Where to Edit' '^## Related Systems' '^## Tests / Verify'; do grep -q "$h" "$OKM/README.md"; done

# PUML + PNG in OKM repo
test -f "$OKM/docs/puml/okm-architecture.puml"
test -f "$OKM/docs/images/okm-architecture.png"
head -5 "$OKM/docs/puml/okm-architecture.puml" | grep -q 'okm-architecture'
grep -q '_standard-style.puml' "$OKM/docs/puml/okm-architecture.puml"

# PNG was NOT written to coding repo
! test -f /Users/Q284340/Agentic/coding/docs/images/okm-architecture.png
! test -f /Users/Q284340/Agentic/coding/docs-content/images/okm-architecture.png

# OKM branch + commit
(cd "$OKM" && [ "$(git rev-parse --abbrev-ref HEAD)" = "gsd/44-09-rest-cutover-v2" ])
(cd "$OKM" && git log -1 --format='%s' | grep -qiE '46-04|README')

# Coding repo not unintentionally modified
(cd /Users/Q284340/Agentic/coding && git status --porcelain | awk '$2 !~ /^\.planning\// {print; bad=1} END{exit bad?1:0}')

# Cross-refs in OKM README
grep -qE 'km-core|@fwornle/km-core' "$OKM/README.md"
grep -qF 'coding' "$OKM/README.md"
grep -qF 'mcp-server-semantic-analysis' "$OKM/README.md"
```
</verification>

<success_criteria>
- OKM repo has new README.md on branch `gsd/44-09-rest-cutover-v2` conforming to template (<= 200 lines)
- OKM repo has new `docs/puml/okm-architecture.puml` + `docs/images/okm-architecture.png`
- Commit pushed to BMW GHE origin via HTTPS
- Coding repo working tree is clean for Phase 46-04 (only `.planning/` modifications)
- SC-4 cross-refs to KM-Core / A / B present in OKM README
- Operator visual-check (Task 5) approved
</success_criteria>

<output>
Create `.planning/phases/46-per-system-documentation-onboarding/46-04-SUMMARY.md` when done. Record:
- OKM commit hash + branch confirmation
- Final OKM README line count
- Whether `_standard-style.puml` bootstrap copy into OKM was needed (and committed)
- Confirmation that no unintended coding-repo modifications occurred
- BMW GHE push success / push-failure-and-resolution notes
</output>
