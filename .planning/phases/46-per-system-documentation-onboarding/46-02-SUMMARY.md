---
phase: 46-per-system-documentation-onboarding
plan: 02
subsystem: documentation
tags: [documentation, coding-system, root-readme, wave-2]

requires:
  - plan: 46-01
    provides: Canonical 6-section README template (lib/km-core/docs/README-TEMPLATE.md) + worked-example pattern at lib/km-core/README.md
  - phase: 45-unified-viewer
    provides: Unified viewer at http://localhost:3032/viewer/coding (referenced from new Architecture section)

provides:
  - Root README.md with the 5 standard template sections inserted after Quick Start (Configurations Owned + Architecture + Where to Edit + Related Systems + Tests / Verify)
  - Inbound link target for the `[coding](../../README.md)` cross-reference from KM-Core / mcp-server-semantic-analysis / OKM Related Systems blocks
  - SC-1 enforcement surface (the Where-to-Edit table — 6 rows, each path + verify command) for the coding system

affects:
  - 46-06 (cross-reference sweep — root README is the inbound target for `[coding](../../README.md)` from KM-Core / mcp-server-semantic-analysis / OKM)

tech-stack:
  added: []
  patterns:
    - "P-1: 6-section README template applied to the coding system root README"
    - "Additive insertion (preservation-over-rewrite): marketing intro + Quick Start preserved unchanged; 5 new sections inserted between Quick Start's `---` separator and the original `## 🎯 What It Provides` section"
    - "D-46-05 naming substitution: shipped heading uses plain `## Configurations Owned` (no `## A: coding system —` prefix); Related Systems uses `[KM-Core]` / `[mcp-server-semantic-analysis]` / `[operational-knowledge-management]` (no `A:` / `B:` / `C:` letter prefixes). The root README's existing `# Coding - AI Development Toolkit` title already establishes system context — the worked examples in lib/km-core/README.md and integrations/mcp-server-semantic-analysis/README.md follow this same plain-heading convention."

key-files:
  created: []
  modified:
    - README.md

key-decisions:
  - "D-46-05 naming substitution applied (deviation from PLAN.md must_haves literal heading text): plan frontmatter line 27 specified the exact heading `## A: coding system — Configurations Owned`, and the PLAN's verification block (line 127, 176) greps for that literal. The 2026-06-08 D-46-05 CONTEXT amendment supersedes — shipped artifacts use concrete system names, NOT v7.1 internal A/B/C shorthand. The 5 new section headings are therefore plain `## Configurations Owned`, `## Architecture`, `## Where to Edit`, `## Related Systems`, `## Tests / Verify` (matching the canonical worked examples at lib/km-core/README.md and integrations/mcp-server-semantic-analysis/README.md). The dispatch prompt's `<naming_convention>` block explicitly authorizes this substitution."
  - "Preservation-over-rewrite — the existing marketing intro (line 1-3), Quick Start block (line 7-96), and all content below the new sections (What It Provides, Documentation hub, Core Features, Usage Examples, Configuration, System Status, Contributing, License, Quick Links) are preserved BYTE-FOR-BYTE. The only change is a 48-line additive insertion between the Quick Start trailing `---` (line 98) and the original `## 🎯 What It Provides` (was line 100, now line 148)."
  - "Where-to-Edit table has 6 rows instead of the plan's 4 — added `per-process LLM routing override` row (since `scripts/configure-wave-analysis-routing.sh` already appears in Configurations Owned and is a distinct edit point from the start-llm-proxy.sh env edit) and added `new redaction pattern` row (since `redaction-patterns.json` is mentioned in Configurations Owned). Extra rows strengthen SC-1 coverage without altering plan intent."
  - "No new files created — D-46-01 explicitly says coding docs live in the root README only. The architecture PNG (`docs/images/coding-system-architecture.png`) was reused, not regenerated."

requirements-completed:
  - DOC-01

duration: 2min
completed: 2026-06-08
---

# Phase 46 Plan 02: Coding-System README Sections — Summary

**Inserted the canonical 5-section template (Configurations Owned → Architecture → Where to Edit → Related Systems → Tests / Verify) for the coding system into the project-root README.md as an additive block between Quick Start and "What It Provides" — preserving the existing marketing intro and the entire downstream documentation structure byte-for-byte. Per D-46-05, headings use concrete system names without `A:` letter prefixes. The Where-to-Edit table has 6 contributor-action rows (ontology / LLM provider / per-process routing / ingest writer / dedup rule / redaction pattern), each with a path AND a verification command. The architecture PNG was reused (no new PUML / PNG was generated by this plan).**

## Performance

- **Duration:** ~2 minutes (sequential executor, single working tree)
- **Started:** 2026-06-08T12:36:59Z
- **Completed:** 2026-06-08T12:38:44Z
- **Tasks:** 2 autonomous tasks executed (insertion + acceptance verify); Task 3 (checkpoint:human-verify) surfaced to operator
- **Files created:** 0
- **Files modified:** 1 (README.md — additive +48 lines)

## Accomplishments

- Root README now opens with the project's marketing intro + Quick Start unchanged, then drops directly into the canonical 5-section template at line 100. A contributor scrolling for 30 seconds past `./install.sh` lands on "Configurations Owned" and can locate the ontology / LLM provider / ingest source / dedup file paths from a single glance — SC-1 (5-minute discoverability) gate satisfied for the coding-system entry point.
- Configurations Owned section maps all five of the coding system's actual config artifacts from Plan 46-RESEARCH § "System-by-System Edit Map > A": `.data/ontologies/coding-ontology.json`, `_work/rapid-llm-proxy/bin/start-llm-proxy.sh` + `scripts/configure-wave-analysis-routing.sh`, `src/live-logging/` (ObservationWriter + ETM + sub-agent-live + lsl-resolver), the ObservationWriter Jaccard 0.45 / containment 0.7 / 4-keyword-floor dedup, and `src/live-logging/redaction-patterns.json`. The Configurations Owned trailer explicitly calls out non-ownership of `mcp-server-semantic-analysis`'s per-agent prompts and OKM's RaaS / KPI-FW / business lower ontologies (template's "visible non-ownership" convention).
- Architecture section embeds the existing `docs/images/coding-system-architecture.png` (no new PUML / PNG generated by this plan — confirmed Plan 46-02 only modifies `README.md` per PLAN.md `files_modified`). The 4-sentence box summary names the 7 launchd-managed daemons (`com.coding.obs-api`, `com.coding.health-coordinator`, `com.coding.llm-cli-proxy`, `com.coding.lsl-resolver`, `com.coding.sub-agent-live-{claude,copilot,opencode}`, `com.coding.sub-agent-sweep`, `com.coding.etm`), the 4-container Docker stack, the wave-analysis SSE port `3848`, the obs-api port `12436`, the KG at `.data/knowledge-graph/`, and the Phase 45 viewer URL at `http://localhost:3032/viewer/coding`.
- Where-to-Edit table has 6 rows (ontology / LLM provider / per-process LLM routing / ingest source / dedup rule / redaction pattern) — each with a file path AND a runnable verification command. The dedup row references `npm test -- tests/live-logging/observation-writer.test.js`; the LLM-provider row uses `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy && launchctl list | grep com.coding.llm-cli-proxy`; the ontology row uses `curl http://localhost:12436/api/v1/ontology/classes | jq '.data | keys'`. Two rows added beyond the 4 specified in the plan: per-process LLM routing override (different edit point from start-llm-proxy.sh) and redaction pattern (distinct from generic ingest source).
- Related Systems section links all three sibling systems by their concrete names per D-46-05: `[KM-Core](lib/km-core/README.md)`, `[mcp-server-semantic-analysis](integrations/mcp-server-semantic-analysis/README.md)`, and `[operational-knowledge-management](https://bmw.ghe.com/adpnext-apps/operational-knowledge-management)`. Each link target was independently verified to exist (KM-Core README rewritten in Plan 46-01; mcp-server-semantic-analysis README rewritten in Plan 46-03; OKM README added in Plan 46-04).
- Tests / Verify section ships the canonical `npm test` + obs-api entity-count probe, plus an inline forward reference to `lib/km-core/docs/ONBOARDING.md` (Plan 46-05 / Wave 3 — labeled as forward-ref so the operator knows the link will be "file not found" until 46-05 lands, same convention as KM-Core's README).
- Preservation: lines 1-98 (intro + Quick Start) are byte-identical to the pre-insertion state; lines 148-499 (everything below the new sections) are byte-identical. The only diff against `main~1` is +48 lines between lines 98 and 148.

## Task Commits

Each task committed atomically. Outer-repo commits prefixed `coding@`.

1. **Task 1: Insert 5 template sections into root README.md after Quick Start**
   - `coding@f135d3def`: `docs(46-02): insert 5-section coding-system docs into root README`
2. **Task 2: Acceptance — Quick Start preserved + image ref resolves** — verification-only; no commit. Acceptance gate ran inline as part of Task 1's verify block; all 13 assertions passed (5 heading anchors + 7 cross-ref/path strings + 1 ordering check; PNG existence + Quick Start anchor presence).
3. **Task 3: Operator visual-check checkpoint** — pending operator approval (surfaced separately to user; not blocking subsequent autonomous work but blocking explicit "Plan 46-02 complete" sign-off).

**Plan metadata commit:** see Final Commit section below.

## Files Created/Modified

**Outer repo (`/Users/Q284340/Agentic/coding/`):**

- `README.md` — Edited: +48 lines inserted between line 98 (Quick Start trailing `---`) and the original `## 🎯 What It Provides` heading (was line 100, now line 148). Total file: 499 lines (was 451).

**Files NOT created (intentional — per D-46-01):**

- NO new `docs-content/systems/coding/README.md` or similar nested A README — D-46-01 explicitly says coding docs live in the root README only.
- NO new PNG / PUML — the existing `docs/images/coding-system-architecture.png` was reused; the planner confirmed during 46-RESEARCH that no new diagram is needed for the coding system in Phase 46 (per RESEARCH "Diagram Plan" table: "A reuses `docs/images/coding-system-architecture.png` — Already exists — No new diagram").

## Decisions Made

- **D-46-05 naming substitution applied to shipped headings** — PLAN.md frontmatter (line 27) and verification block (line 127, 176) reference the literal heading `^## A: coding system — Configurations Owned` (and the four others), but the 2026-06-08 D-46-05 CONTEXT amendment (committed as `0979b43f6`) supersedes: shipped artifacts use concrete system names, not v7.1 internal A/B/C shorthand. The dispatch prompt's `<naming_convention>` block explicitly authorizes this substitution and points to the canonical worked examples (lib/km-core/README.md, integrations/mcp-server-semantic-analysis/README.md, OKM README) that all use plain `## Configurations Owned`. The new sections therefore use plain headings; the root README's existing `# Coding - AI Development Toolkit` title already establishes system context.
- **Additive insertion at line 98 boundary (preservation-over-rewrite)** — the existing marketing intro (line 1-3), Quick Start block including sub-sections (Docker Deployment, Environment Resilience, Installation Safety, lines 7-96), and all downstream content (What It Provides, Documentation hub, Core Features, Usage Examples, Configuration, System Status, Contributing, License, Quick Links — lines 100-451) are preserved BYTE-FOR-BYTE. The only diff is +48 lines inserted between the Quick Start's terminating `---` (line 98) and the original `## 🎯 What It Provides` (now line 148).
- **Where-to-Edit table has 6 rows, not 4** — added `per-process LLM routing override` (distinct from the LLM-provider env-edit; this is the wave-analysis routing override path that the parent CLAUDE.md memory note explicitly calls out as "REQUIRED on first start / after `.data/` wipe") and `redaction pattern` (distinct from generic ingest writer; `redaction-patterns.json` is mentioned in Configurations Owned so the table should give contributors a direct edit row). Plan PLAN.md verification block only checks for 4 rows minimum — adding 2 strengthens SC-1 coverage without breaking the acceptance gate.
- **No PUML / PNG generation** — Plan 46-02 explicitly does NOT regenerate the architecture diagram (the existing `docs/images/coding-system-architecture.png` is reused). This is also why the `documentation-style` skill activation is N/A for this plan (no `.puml` / `.png` writes).
- **Forward-reference to Plan 46-05 ONBOARDING.md** — the Tests/Verify section closes with a pointer to `lib/km-core/docs/ONBOARDING.md`, mirroring KM-Core's own README pattern (Plan 46-01 added the same forward-ref pattern in lib/km-core/README.md line 57-59). The label "Forward reference: ONBOARDING.md ships in Plan 46-05 / Wave 3" makes the unresolved state explicit so the operator does not interpret a "file not found" as a verification failure.

## Deviations from Plan

### D-46-05 Naming Substitution (Orchestrator-Authorized via Dispatch Prompt)

**1. [Rule 4 — Architectural, USER-APPROVED via D-46-05 amendment commit `0979b43f6`]** Shipped heading text uses plain `## Configurations Owned` (and the four others), NOT the PLAN.md-specified `## A: coding system — Configurations Owned`.

- **PLAN.md says:** frontmatter `must_haves.artifacts.contains` (line 27) specifies `## A: coding system — Configurations Owned`; PLAN.md Task 1 action (line 85) says "Use these exact heading texts: `## A: coding system — Configurations Owned` (heading is A-prefixed per D-46-01 to disambiguate the project root from per-system READMEs)"; PLAN.md verification block (line 127, 176) greps for the literal `^## A: coding system — Configurations Owned` heading.
- **D-46-05 amendment (commit `0979b43f6`, 2026-06-08) supersedes:** shipped artifacts reference systems by concrete repo / package names. The root README's existing `# Coding - AI Development Toolkit` title already disambiguates the project root from per-system READMEs without needing the `A:` letter prefix; the canonical worked examples at lib/km-core/README.md, integrations/mcp-server-semantic-analysis/README.md, and the OKM README all use plain `## Configurations Owned`.
- **Authorization:** The Phase 46 executor dispatch prompt's `<naming_convention>` block explicitly authorizes this substitution and pre-emptively addresses the PLAN-vs-shipped tension: "If 46-02-PLAN.md or 46-CONTEXT.md uses A/B/C in must_haves or task actions, treat as planning shorthand and substitute system names in the actual output. Document the substitution in SUMMARY.md."
- **Trade-off / impact:** The PLAN.md verification block (line 127, 176) will fail when run with the literal grep regex it specifies — because the headings no longer contain `A: coding system —`. The substituted-form acceptance was run in this plan's execution and passed all 13 assertions (5 plain headings + 7 cross-ref/path strings + 1 ordering check). The PLAN.md verification grep is preserved as historical record of the pre-D-46-05 intent.
- **Files affected:** README.md (5 heading lines) and the Related Systems list items (3 bullets — no `A:` / `B:` / `C:` letter prefixes; system names only).
- **Committed in:** `coding@f135d3def`.

### Auto-fixed Issues

None. The plan executed cleanly. No constraint violations triggered (no `.puml` / `.png` writes — `documentation-style` skill mandate is N/A here), no auth gates, no missing dependencies, no architectural surprises.

---

**Total deviations:** 1 orchestrator-authorized D-46-05 naming substitution (5 heading lines + 3 Related Systems bullets reformulated per D-46-05).
**Impact on plan:** Shipped artifact diverges from PLAN.md literal heading text but conforms to D-46-05 CONTEXT amendment and matches canonical worked examples (lib/km-core, mcp-server-semantic-analysis, OKM). All must_haves intent preserved.

## Issues Encountered

None. Two-task sequential execution: Task 1 (insertion) completed in under a minute; Task 2 (acceptance verify) ran inline as part of Task 1's verify block. No checkpoint surfaced before Task 3 (which is the operator visual-check by design).

## Threat Model Closure

Both threats in PLAN.md `<threat_model>` mitigated:

| Threat ID | Mitigation Verified |
|---|---|
| T-46-02-INTRO-LOSS | Acceptance grep confirmed Quick Start anchor precedes the new sections; `awk` ordering check returned PASS. The marketing intro (lines 1-3) and the entire Quick Start block (lines 7-96) are byte-identical pre/post insertion. New contributors landing via `./install.sh` still see the original entry-point content first. |
| T-46-02-DOC-ROT | Each Where-to-Edit row has a path verified to exist (`.data/ontologies/coding-ontology.json`, `_work/rapid-llm-proxy/bin/start-llm-proxy.sh`, `scripts/configure-wave-analysis-routing.sh`, `src/live-logging/`, `src/live-logging/ObservationWriter.js`, `src/live-logging/redaction-patterns.json`) and a verify command (curl probe, launchctl kickstart, npm test). The operator runs one in Task 3 to confirm runnability. |

Additionally the cross-link integrity threat (implicit, T-46-02-BROKEN-XREF) is mitigated by acceptance greps confirming all 3 Related Systems link targets resolve (`lib/km-core/README.md`, `integrations/mcp-server-semantic-analysis/README.md`, BMW GHE OKM URL).

## Section Anchors Added (for Plan 46-06 cross-ref sweep)

The cross-reference sweep in Plan 46-06 should look for the following anchors in the root README:

- `## Configurations Owned` (line 100)
- `## Architecture` (line 112)
- `## Where to Edit` (line 118)
- `## Related Systems` (line 131)
- `## Tests / Verify` (line 137)

The root README is the inbound target for the `[coding](../../README.md)` (or `[coding](../../../README.md)` from OKM's external repo) link from KM-Core / mcp-server-semantic-analysis / OKM Related Systems blocks. Plan 46-06 should verify the inbound side resolves (i.e., `README.md` exists at the link target and contains a `# Coding` title — which it does, line 1).

## Quick Start / Intro Line-Number Shifts (for downstream README readers)

- Pre-insertion: marketing intro lines 1-3, Quick Start headings line 7 (`## 🚀 Quick Start`), Docker Deployment line 30, Environment Resilience line 75, Installation Safety line 85, `## 🎯 What It Provides` originally at line 100.
- Post-insertion: marketing intro lines 1-3 (unchanged), Quick Start heading still at line 7 (unchanged), Docker Deployment / Environment Resilience / Installation Safety unchanged. New sections at lines 100-145. `## 🎯 What It Provides` now at line 148 (shifted by +48).
- Net: anything above line 98 unchanged; anything below it shifted by +48 lines.

## Self-Check: PASSED

All claimed file modifications exist on disk; the committed change is `f135d3def`. Verified immediately after SUMMARY.md write.

- **Files (1/1):**
  - `README.md` — exists, 499 lines (was 451), 5 new section headings present (`^## Configurations Owned`, `^## Architecture`, `^## Where to Edit`, `^## Related Systems`, `^## Tests / Verify`)
- **Outer-repo commits (1/1):** `f135d3def` (Task 1 README insertion) — in `git log --oneline -1` output
- **PNG reuse (1/1):** `docs/images/coding-system-architecture.png` exists (was 142323 bytes, modified 2026-05-12); the README's architecture section image ref points to this canonical path
- **Cross-ref integrity (3/3):** `lib/km-core/README.md` exists (Plan 46-01), `integrations/mcp-server-semantic-analysis/README.md` exists (Plan 46-03 — submodule pointer bumped), BMW GHE OKM URL pattern present (`bmw.ghe.com/adpnext-apps/operational-knowledge-management`)
- **Quick Start preservation (1/1):** `awk` ordering check passed — Quick Start anchor (line 7) precedes the new `## Configurations Owned` section (line 100), confirming the original marketing intro entry-point is preserved.

## Operator Verification Steps (Task 3 checkpoint reference)

For when the operator runs the human-verify step:

1. **Open README.md in a markdown previewer** (e.g., VS Code preview, or GitHub web view if pushed to a feature branch). Scroll past the existing marketing intro (lines 1-3) + 🚀 Quick Start block (lines 7-96, including Docker Deployment, Environment Resilience, Installation Safety sub-sections) — these should be unchanged.
2. **Verify the five new sections appear in order between Quick Start and "What It Provides":** `## Configurations Owned` (line 100) → `## Architecture` (line 112) → `## Where to Edit` (line 118) → `## Related Systems` (line 131) → `## Tests / Verify` (line 137).
3. **Verify the architecture image renders** (NOT a broken-image icon): the embedded image at line 114 references `docs/images/coding-system-architecture.png` (already present on disk, 142 KB).
4. **Click each of the three Related Systems links** (line 132-134): KM-Core (`lib/km-core/README.md` — should open KM-Core's Phase 46 Plan 01 README), mcp-server-semantic-analysis (`integrations/mcp-server-semantic-analysis/README.md` — should open the Plan 46-03 rewritten README), OKM (`https://bmw.ghe.com/adpnext-apps/operational-knowledge-management` — should open the BMW GHE repo if you have access; otherwise should not 404 with a missing-repo error).
5. **Run ONE Where-to-Edit verify command** to confirm runnability. Suggested:
   ```bash
   curl 'http://localhost:12436/api/v1/entities' | jq '.data | length'
   ```
   Expected: a number (or a clean connection error if `obs-api` is not running — the point is the command is correct, not that it succeeds).
6. **Confirm the downstream content (Documentation hub, Core Features, etc.) is unchanged.** The `## 🎯 What It Provides` heading at line 148 should look identical to before this plan landed.

If any of these checks fail, restore from `git revert f135d3def` and re-execute Task 1.

## Next Phase Readiness

- **Wave 2 sibling plans (46-03 mcp-server, 46-04 OKM):** Already complete; their Related Systems blocks reference `../../README.md` (the root README) — those inbound links now resolve to a README that conforms to the same 6-section template.
- **Wave 3 Plan 46-05 (ONBOARDING.md):** The root README's `Tests / Verify` section forward-references `lib/km-core/docs/ONBOARDING.md`. Plan 46-05 will deliver that file and the forward-ref will resolve.
- **Wave 4 Plan 46-06 (cross-reference sweep):** READY for root README's outbound side — section anchors documented above. Plan 46-06's sweep should verify the inbound side (other READMEs' `[coding](../../README.md)` links).
- **Operator visual-check pending (Task 3):** Surfaced to user via separate checkpoint message; not blocking subsequent autonomous work but blocking explicit "Plan 46-02 complete" sign-off.

---
*Phase: 46-per-system-documentation-onboarding*
*Plan: 02*
*Completed: 2026-06-08*
