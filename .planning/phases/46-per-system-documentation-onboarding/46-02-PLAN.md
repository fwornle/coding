---
phase: 46-per-system-documentation-onboarding
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - README.md
autonomous: false
requirements:
  - DOC-01
user_setup: []
tags: [documentation, a-system, readme]

must_haves:
  truths:
    - "Project-root README.md adds the 6-section template structure per D-46-01 + D-46-02 (Configurations Owned, Architecture, Where to Edit, Related Systems, Tests / Verify) inserted after Quick Start"
    - "Existing marketing intro + Quick Start sections are PRESERVED (not deleted) per D-46-01 — A's docs LIVE here, no new dedicated A README"
    - "Configurations Owned section maps A's actual config artifacts: config/agents/*.json, .data/ontologies/coding-ontology.json, src/live-logging/redaction-patterns.json, scripts/configure-wave-analysis-routing.sh (per RESEARCH § System-by-System Edit Map > A row)"
    - "Architecture section references the EXISTING docs/images/coding-system-architecture.png (no new PUML needed for A)"
    - "Where to Edit table covers all four template rows (ontology / LLM provider / ingest source / dedup rule) with paths AND verify commands"
    - "Related Systems section links to lib/km-core/README.md, integrations/mcp-server-semantic-analysis/README.md, and OKM BMW GHE URL (per SC-4)"
  artifacts:
    - path: README.md
      provides: "Project-root README with A's Configurations Owned + cross-refs to KM-Core/B/C"
      contains: "## A: coding system — Configurations Owned"
  key_links:
    - from: README.md
      to: lib/km-core/README.md
      via: Related Systems markdown link
      pattern: "lib/km-core/README.md"
    - from: README.md
      to: integrations/mcp-server-semantic-analysis/README.md
      via: Related Systems markdown link
      pattern: "integrations/mcp-server-semantic-analysis/README.md"
    - from: README.md
      to: docs/images/coding-system-architecture.png
      via: Architecture section image ref
      pattern: "coding-system-architecture.png"
  threats:
    - id: T-46-02-INTRO-LOSS
      category: Information disclosure
      component: README.md marketing intro / Quick Start
      disposition: mitigate
      mitigation: "Acceptance grep verifies the install.sh / Quick Start anchor sentence is preserved (so new contributors still land correctly)"
    - id: T-46-02-DOC-ROT
      category: Information disclosure
      component: README.md Where to Edit table
      disposition: mitigate
      mitigation: "Each row's path is verified by file-existence grep; verify commands are runnable (curl localhost:12436 / launchctl kickstart) — operator runs at least one in Task 3"
---

<objective>
Add A's "Configurations Owned" + cross-references to the project-root `README.md` per D-46-01 (A's docs live in the root README, no new dedicated file).

Purpose: The root README is where `./install.sh` lands new contributors. Adding A's per-system documentation here — rather than nesting it under `docs-content/systems/coding/README.md` — keeps SC-1 (5-min discoverability) intact for the A entry point. The existing marketing intro is preserved; the template sections are inserted after Quick Start.

Output: Edited `README.md` with five new top-level sections inserted after Quick Start, conforming to the template from Plan 46-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-per-system-documentation-onboarding/46-CONTEXT.md
@.planning/phases/46-per-system-documentation-onboarding/46-RESEARCH.md
@.planning/phases/46-per-system-documentation-onboarding/46-PATTERNS.md
@.planning/phases/46-per-system-documentation-onboarding/46-01-SUMMARY.md
@lib/km-core/docs/README-TEMPLATE.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert 5 template sections into root README.md after Quick Start</name>
  <files>README.md</files>
  <action>Edit `README.md`. PRESERVE the existing marketing intro + Quick Start sections (lines 1 through approximately line 28 — locate the end of the Quick Start section). After Quick Start, INSERT the five template sections per `lib/km-core/docs/README-TEMPLATE.md` (Plan 46-01 output), with A-specific content from 46-RESEARCH.md § "System-by-System Edit Map > A". Use these exact heading texts:

  `## A: coding system — Configurations Owned` (heading is A-prefixed per D-46-01 to disambiguate the project root from per-system READMEs)

  Bullet list under Configurations Owned:
  - `**Ontology:** `.data/ontologies/coding-ontology.json` — coding-domain class declarations (Component, SubComponent, Detail, etc.)`
  - `**LLM providers:** `_work/rapid-llm-proxy/bin/start-llm-proxy.sh` + `scripts/configure-wave-analysis-routing.sh` — provider routing (claude-code, copilot, claude-haiku per process)`
  - `**Ingest adapters:** `src/live-logging/` (ObservationWriter, ETM, sub-agent-live-*) — host-side observation pipeline writing to obs-api`
  - `**Domain dedup:** `src/live-logging/ObservationWriter.js` — Jaccard 0.45 / containment 0.7 / 4-keyword floor (per CLAUDE.md memory `ObservationWriter` notes)`
  - `**Redaction:** `src/live-logging/redaction-patterns.json` — PII redaction patterns applied at ingest`

  `## A: coding system — Architecture`

  Embed the existing diagram: `![Coding system architecture](docs/images/coding-system-architecture.png)`. Add a 3-5 sentence summary describing: the launchd-managed daemons (obs-api, ETM, lsl-resolver, sub-agent-live-*), the wave-analysis workflow on port 3848, and the KG storage at `.data/knowledge-graph/`.

  `## A: coding system — Where to Edit`

  Markdown table with these four rows:
  | To add… | Edit… | Verify |
  |---|---|---|
  | A new ontology class | `.data/ontologies/coding-ontology.json` (`classes` block) | `curl http://localhost:12436/api/v1/ontology/classes \| jq '.data \| keys'` |
  | A new LLM provider | `_work/rapid-llm-proxy/bin/start-llm-proxy.sh` (env exports) + restart `com.coding.llm-cli-proxy` | `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy && launchctl list \| grep com.coding.llm-cli-proxy` |
  | A new ingest source | New writer under `src/live-logging/<Writer>.js` + test in `tests/live-logging/*.test.js` | `npm test -- tests/live-logging` |
  | A domain dedup rule change | `src/live-logging/ObservationWriter.js` (Jaccard/containment/keyword constants) | `npm test -- tests/live-logging/observation-writer.test.js` |

  `## A: coding system — Related Systems`

  Bullet list:
  - `[KM-Core](lib/km-core/README.md) — shared types / store / REST / viewer`
  - `[B: mcp-server-semantic-analysis](integrations/mcp-server-semantic-analysis/README.md) — agent pipeline, wave-analysis workflow`
  - `[C: OKM (BMW GHE)](https://bmw.ghe.com/adpnext-apps/operational-knowledge-management) — RCA / operational ingest (external)`

  `## A: coding system — Tests / Verify`

  Code block:
  ```bash
  npm test
  curl 'http://localhost:12436/api/v1/entities' | jq '.data | length'
  ```

  Followed by a 1-sentence pointer: "For a guided contributor onboarding walkthrough, see [`lib/km-core/docs/ONBOARDING.md`](lib/km-core/docs/ONBOARDING.md)." (Forward-ref to Plan 46-05.)

  Do NOT alter content below Quick Start that is unrelated to these five sections.</action>
  <verify>
    <automated>grep -q '^## A: coding system — Configurations Owned' README.md && grep -q '^## A: coding system — Architecture' README.md && grep -q '^## A: coding system — Where to Edit' README.md && grep -q '^## A: coding system — Related Systems' README.md && grep -q '^## A: coding system — Tests / Verify' README.md && grep -q 'coding-ontology.json' README.md && grep -q 'configure-wave-analysis-routing.sh' README.md && grep -q 'coding-system-architecture.png' README.md && grep -q 'lib/km-core/README.md' README.md && grep -q 'integrations/mcp-server-semantic-analysis/README.md' README.md && grep -q 'bmw.ghe.com/adpnext-apps/operational-knowledge-management' README.md</automated>
  </verify>
  <done>All five `## A: coding system — *` headings present in order; all four cross-ref targets linked; existing marketing intro preserved (Quick Start section still present above the new sections).</done>
</task>

<task type="auto">
  <name>Task 2: Acceptance — Quick Start preserved + image ref resolves</name>
  <files>README.md</files>
  <action>Verify the existing Quick Start anchor (e.g., the `./install.sh` mention or "Quick Start" heading) is still present and precedes the new "A: coding system" sections (i.e., the insertion did not accidentally delete or relocate the original intro). Also verify the architecture PNG actually exists at `docs/images/coding-system-architecture.png` (it should — Plan 46-02 does not generate any new PNG). If either check fails, restore from git diff and re-insert without touching the intro.</action>
  <verify>
    <automated>grep -qE 'install\.sh|## Quick Start|Quick Start' README.md && test -f docs/images/coding-system-architecture.png && awk '/^## A: coding system — Configurations Owned/{f=NR}/install\.sh|Quick Start/{q=NR}END{exit (q && f && q<f)?0:1}' README.md</automated>
  </verify>
  <done>Quick Start anchor precedes the new A: sections in the README; the referenced PNG exists on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual check — root README renders</name>
  <what-built>
    - Project-root `README.md` with five new `## A: coding system — *` sections inserted after Quick Start, conforming to the Plan 46-01 template, with all SC-4 cross-references in place.
  </what-built>
  <how-to-verify>
    1. Open `README.md` in a markdown previewer (or GitHub web view if pushed to a feature branch). Scroll past the existing marketing intro and Quick Start.
    2. Verify the five `## A: coding system — *` sections appear in order: Configurations Owned → Architecture → Where to Edit → Related Systems → Tests / Verify.
    3. Verify the architecture image renders (NOT a broken-image icon).
    4. Click each of the three Related Systems links (KM-Core, B, OKM). Verify the local-repo paths resolve and the BMW GHE OKM link opens (or at least does not 404 if you have access).
    5. Run ONE row from the Where to Edit "Verify" column to confirm it's actually runnable. Suggested: `curl 'http://localhost:12436/api/v1/entities' | jq '.data | length'` — should return a number (or fail cleanly with a connection error if obs-api is not running, which is fine — the point is the command is correct).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Plan 46-02 complete, or describe issues (missing sections, broken image, broken Related Systems links, or unrunnable Where to Edit commands).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The only trust boundary is the **constraint monitor → markdown write** boundary. No untrusted input handling, no auth, no code execution.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-02-INTRO-LOSS | Information disclosure (contributor doesn't reach install.sh) | README.md intro / Quick Start preservation | mitigate | Acceptance grep verifies Quick Start anchor precedes new A: sections (Task 2) |
| T-46-02-DOC-ROT | Information disclosure | Where to Edit paths | mitigate | Acceptance greps verify each path string in README; operator runs one verify command in Task 3 |
| T-46-02-BROKEN-XREF | Information disclosure | Related Systems links to KM-Core / B / C | mitigate | Acceptance greps verify all three link targets; operator visual check confirms targets resolve |
</threat_model>

<verification>
```bash
# Five required A: sections present
for h in '^## A: coding system — Configurations Owned' '^## A: coding system — Architecture' '^## A: coding system — Where to Edit' '^## A: coding system — Related Systems' '^## A: coding system — Tests / Verify'; do grep -q "$h" README.md || { echo "MISSING: $h"; exit 1; }; done

# Quick Start preserved AND precedes new A: sections
awk '/^## A: coding system — Configurations Owned/{f=NR}/install\.sh|Quick Start/{q=NR}END{exit (q && f && q<f)?0:1}' README.md

# Required path references
for p in 'coding-ontology.json' 'configure-wave-analysis-routing.sh' 'ObservationWriter' 'coding-system-architecture.png'; do grep -qF "$p" README.md || { echo "MISSING: $p"; exit 1; }; done

# Cross-refs
grep -qF 'lib/km-core/README.md' README.md
grep -qF 'integrations/mcp-server-semantic-analysis/README.md' README.md
grep -qF 'bmw.ghe.com/adpnext-apps/operational-knowledge-management' README.md
```
</verification>

<success_criteria>
- All five `## A: coding system — *` sections present in fixed order after Quick Start
- Existing marketing intro + Quick Start preserved (T-46-02-INTRO-LOSS mitigated)
- Configurations Owned section maps A's actual config artifacts (5 bullets per RESEARCH)
- Where to Edit table has 4 rows, each with a path AND a runnable verify command
- Related Systems links to all 3 cross-systems (KM-Core / B / C) per SC-4
- Operator visual-check (Task 3) approved
</success_criteria>

<output>
Create `.planning/phases/46-per-system-documentation-onboarding/46-02-SUMMARY.md` when done. Record:
- Final list of section anchors added (so Plan 46-06 cross-ref sweep knows where to look)
- Any Quick Start / intro line-numbers that shifted (for downstream README readers)
- Confirmation that the architecture PNG was reused (no new PNG generated by this plan)
</output>
