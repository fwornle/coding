---
phase: 46-per-system-documentation-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/km-core/docs/README-TEMPLATE.md
  - lib/km-core/README.md
  - lib/km-core/docs/puml/km-core-architecture.puml
  - lib/km-core/docs/puml/km-core-ingest-sequence.puml
  - docs/images/km-core-architecture.png
  - docs/images/km-core-ingest-sequence.png
  - docs-content/images/km-core-architecture.png
  - docs-content/images/km-core-ingest-sequence.png
autonomous: false
requirements:
  - DOC-01
user_setup: []
tags: [documentation, plantuml, km-core, template]

must_haves:
  truths:
    - "lib/km-core/docs/README-TEMPLATE.md exists and contains the six locked section headings from D-46-02 in order"
    - "lib/km-core/README.md conforms to the 6-section template (per D-46-02) with no Mermaid blocks"
    - "lib/km-core/README.md 'Configurations Owned' section explicitly states KM-Core is the shared core and owns no per-system config (per RESEARCH § System-by-System Edit Map > KM-Core row)"
    - "lib/km-core/README.md 'Where to Edit' table covers entity types, REST endpoints, ingest stages, and display overlay extension (per RESEARCH § System-by-System Edit Map > KM-Core row)"
    - "lib/km-core/README.md references Phase 44 + Phase 45 surface additions (REST contracts, SnapshotManager, display-overlay) per RESEARCH OQ-2"
    - "Two PUMLs exist: lib/km-core/docs/puml/km-core-architecture.puml + lib/km-core/docs/puml/km-core-ingest-sequence.puml"
    - "Both PUMLs begin with the standard preamble + standard-style include (per P-2)"
    - "Both PNGs exist in docs/images/ AND docs-content/images/ (per P-2 two-image-dir gotcha)"
    - "lib/km-core/README.md image refs use relative paths ../../docs/images/km-core-*.png"
  artifacts:
    - path: lib/km-core/docs/README-TEMPLATE.md
      provides: "Canonical 6-section README skeleton with placeholder syntax"
      contains: "## Configurations Owned"
    - path: lib/km-core/README.md
      provides: "KM-Core README conforming to template; links to ONBOARDING.md (forward-ref to Plan 46-05)"
      contains: "## Where to Edit"
    - path: lib/km-core/docs/puml/km-core-architecture.puml
      provides: "High-level architecture PUML — shared core vs per-system config (satisfies SC-2)"
    - path: lib/km-core/docs/puml/km-core-ingest-sequence.puml
      provides: "Ingest sequence PUML — consumer → IngestPipeline → dedup → store → exports → events"
    - path: docs/images/km-core-architecture.png
      provides: "Generated PNG for repo README rendering"
    - path: docs-content/images/km-core-architecture.png
      provides: "Generated PNG duplicated for MkDocs"
    - path: docs/images/km-core-ingest-sequence.png
      provides: "Generated sequence PNG for repo README rendering"
    - path: docs-content/images/km-core-ingest-sequence.png
      provides: "Generated sequence PNG duplicated for MkDocs"
  key_links:
    - from: lib/km-core/README.md
      to: docs/images/km-core-architecture.png
      via: relative image ref
      pattern: "km-core-architecture.png"
    - from: lib/km-core/README.md
      to: lib/km-core/docs/ONBOARDING.md
      via: Tests/Verify section link (forward-ref — Plan 46-05 creates the target)
      pattern: "ONBOARDING.md"
  threats:
    - id: T-46-01-DOC-ROT
      category: Information disclosure
      component: lib/km-core/README.md
      disposition: mitigate
      mitigation: "Acceptance greps verify all 6 section headings present; Phase 44/45 surface refs verified by grep on SnapshotManager + display-overlay"
    - id: T-46-01-PNG-DRIFT
      category: Tampering
      component: docs/images/km-core-*.png vs docs/puml/km-core-*.puml
      disposition: mitigate
      mitigation: "Verification block asserts both PNG paths exist after render; PUML source under version control alongside generated PNG"
    - id: T-46-01-CONSTRAINT-EVASION
      category: Tampering
      component: KM-Core PUML placement (lib/km-core/docs/puml/ vs canonical docs/puml/)
      disposition: mitigate
      mitigation: "OVERRIDE_CONSTRAINT: documentation-filename-format annotation in each KM-Core PUML with rationale (submodule-locality); PNG outputs still land in canonical docs/images/"
---

<objective>
Establish the canonical README skeleton (D-46-02) and rewrite KM-Core's README + diagrams as the template anchor for downstream plans (A, B, C in Wave 2).

Purpose: Without a settled template and a worked example (KM-Core), Plans 46-02 / 46-03 / 46-04 cannot start. KM-Core is the deepest documentation surface (existing Mermaid → PUML migration, two new PUMLs, Phase 44/45 surface refresh) AND the natural anchor — the other three systems consume KM-Core as the shared core, so their READMEs cross-link to its diagrams.

Output: `lib/km-core/docs/README-TEMPLATE.md` (skeleton), rewritten `lib/km-core/README.md` (template-conformant + current to Phase 44/45), two PUML sources (`km-core-architecture.puml` + `km-core-ingest-sequence.puml`), four generated PNGs (each in `docs/images/` + `docs-content/images/`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-per-system-documentation-onboarding/46-CONTEXT.md
@.planning/phases/46-per-system-documentation-onboarding/46-RESEARCH.md
@.planning/phases/46-per-system-documentation-onboarding/46-PATTERNS.md
@CLAUDE.md
@lib/km-core/CLAUDE.md
@lib/km-core/README.md
@docs/puml/_standard-style.puml
@docs/puml/adaptive-lsl-system.puml
@.claude/commands/documentation-style.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author README-TEMPLATE.md (canonical 6-section skeleton)</name>
  <files>lib/km-core/docs/README-TEMPLATE.md</files>
  <action>Create the literal markdown skeleton per D-46-02 and PATTERNS.md P-1, using the structure transcribed in 46-RESEARCH.md § "README Template Skeleton". Use `{placeholder}` syntax (e.g., `{System Name}`, `{path/to/ontology.json}`) so downstream plans copy-paste-and-fill. Include all six sections in fixed order with their exact headings. Include the explicit-non-ownership convention (`— (owned by KM-Core / B / etc.)`) note. Include the four standard "Where to Edit" rows (ontology class / LLM provider / ingest source / domain dedup rule) as placeholder rows. Include all four Related Systems links with placeholder targets. NO marketing prose — this is a skeleton.</action>
  <verify>
    <automated>test -f lib/km-core/docs/README-TEMPLATE.md && grep -c '^## ' lib/km-core/docs/README-TEMPLATE.md | grep -q '^[56]$' && grep -q '^## Configurations Owned' lib/km-core/docs/README-TEMPLATE.md && grep -q '^## Architecture' lib/km-core/docs/README-TEMPLATE.md && grep -q '^## Where to Edit' lib/km-core/docs/README-TEMPLATE.md && grep -q '^## Related Systems' lib/km-core/docs/README-TEMPLATE.md && grep -q '^## Tests / Verify' lib/km-core/docs/README-TEMPLATE.md</automated>
  </verify>
  <done>README-TEMPLATE.md exists with all five `## ` second-level section headings (1 H1 + 5 H2) in fixed order, contains placeholder syntax, contains the four standard Where-to-Edit rows.</done>
</task>

<task type="auto">
  <name>Task 2: Author km-core-architecture.puml (migrate Mermaid → PUML)</name>
  <files>lib/km-core/docs/puml/km-core-architecture.puml</files>
  <action>First, invoke the `documentation-style` skill (`.claude/commands/documentation-style.md`) as the very first action — mandatory per P-2 and project CLAUDE.md. Then create the PUML file at `lib/km-core/docs/puml/km-core-architecture.puml`. Add the OVERRIDE_CONSTRAINT annotation header comment with rationale "KM-Core scope; PUML stays with its source per submodule-locality principle" (per PATTERNS.md P-6). Begin the PUML body with the canonical preamble: start-uml directive carrying the lowercase-hyphenated diagram name `km-core-architecture`, followed on the next line by the include of `../../../docs/puml/_standard-style.puml`. Migrate the existing Mermaid `flowchart TB` block from `lib/km-core/README.md:9-19` into PlantUML syntax. The diagram MUST visually distinguish: SHARED CORE (types, store, registry, pipeline, dedup, REST, viewer) on one side; PER-SYSTEM CONFIG (ontology files, LLM config, ingest adapters, domain eval) on the other side — this is the SC-2 enforcement surface. Use boxes / packages / colors per `docs/puml/_standard-style.puml` conventions. End with the standard end-uml directive.</action>
  <verify>
    <automated>test -f lib/km-core/docs/puml/km-core-architecture.puml && head -5 lib/km-core/docs/puml/km-core-architecture.puml | grep -q 'km-core-architecture' && grep -q '_standard-style.puml' lib/km-core/docs/puml/km-core-architecture.puml && grep -q 'OVERRIDE_CONSTRAINT: documentation-filename-format' lib/km-core/docs/puml/km-core-architecture.puml</automated>
  </verify>
  <done>PUML source exists with diagram name `km-core-architecture`, standard-style include, OVERRIDE annotation, both SHARED CORE and PER-SYSTEM CONFIG groupings present in body.</done>
</task>

<task type="auto">
  <name>Task 3: Author km-core-ingest-sequence.puml (new sequence diagram)</name>
  <files>lib/km-core/docs/puml/km-core-ingest-sequence.puml</files>
  <action>Create the PUML file at `lib/km-core/docs/puml/km-core-ingest-sequence.puml`. Add the same OVERRIDE_CONSTRAINT annotation header comment as Task 2. Begin the PUML body with the canonical preamble: start-uml directive carrying the lowercase-hyphenated diagram name `km-core-ingest-sequence`, followed on the next line by the include of `../../../docs/puml/_standard-style.puml`. Author a sequence diagram showing the ingest flow: actor `Consumer` → participant `IngestPipeline` → participant `Dedup` → participant `GraphKMStore` → participant `Exports` → participant `Events`. Show at minimum: (1) consumer calls IngestPipeline.ingest(entity), (2) IngestPipeline runs Dedup.check, (3) Dedup returns decision, (4) IngestPipeline persists via GraphKMStore.putEntity, (5) GraphKMStore emits Events, (6) Exports observe Events and update on-disk exports. This is the sequence referenced by ONBOARDING.md (Plan 46-05). End with the standard end-uml directive.</action>
  <verify>
    <automated>test -f lib/km-core/docs/puml/km-core-ingest-sequence.puml && head -5 lib/km-core/docs/puml/km-core-ingest-sequence.puml | grep -q 'km-core-ingest-sequence' && grep -q '_standard-style.puml' lib/km-core/docs/puml/km-core-ingest-sequence.puml && grep -qE '(IngestPipeline|Dedup|GraphKMStore)' lib/km-core/docs/puml/km-core-ingest-sequence.puml</automated>
  </verify>
  <done>Sequence PUML exists with diagram name `km-core-ingest-sequence`, includes IngestPipeline / Dedup / GraphKMStore participants.</done>
</task>

<task type="auto">
  <name>Task 4: Render PUMLs and duplicate PNGs to two image dirs</name>
  <files>docs/images/km-core-architecture.png, docs/images/km-core-ingest-sequence.png, docs-content/images/km-core-architecture.png, docs-content/images/km-core-ingest-sequence.png</files>
  <action>Render both PUMLs via the `plantuml` CLI (NEVER `java -jar plantuml.jar` — CLAUDE.md mandate). Use:
    `plantuml lib/km-core/docs/puml/km-core-architecture.puml`
    `plantuml lib/km-core/docs/puml/km-core-ingest-sequence.puml`
  The plantuml CLI writes PNGs alongside the PUML source. Then move each generated PNG to `docs/images/`:
    `mv lib/km-core/docs/puml/km-core-architecture.png docs/images/km-core-architecture.png`
    `mv lib/km-core/docs/puml/km-core-ingest-sequence.png docs/images/km-core-ingest-sequence.png`
  Then duplicate (cp, NOT mv) each PNG to `docs-content/images/` per P-2 two-image-dir gotcha:
    `cp docs/images/km-core-architecture.png docs-content/images/km-core-architecture.png`
    `cp docs/images/km-core-ingest-sequence.png docs-content/images/km-core-ingest-sequence.png`
  All four PNG paths MUST exist after this task.</action>
  <verify>
    <automated>test -f docs/images/km-core-architecture.png && test -f docs/images/km-core-ingest-sequence.png && test -f docs-content/images/km-core-architecture.png && test -f docs-content/images/km-core-ingest-sequence.png && cmp -s docs/images/km-core-architecture.png docs-content/images/km-core-architecture.png && cmp -s docs/images/km-core-ingest-sequence.png docs-content/images/km-core-ingest-sequence.png</automated>
  </verify>
  <done>All four PNG files exist; the `docs/images/` and `docs-content/images/` copies are byte-identical for both diagrams.</done>
</task>

<task type="auto">
  <name>Task 5: Rewrite lib/km-core/README.md to template (Phase 44/45 current)</name>
  <files>lib/km-core/README.md</files>
  <action>Rewrite `lib/km-core/README.md` to conform to the 6-section template from Task 1, using the System-by-System Edit Map § KM-Core row in 46-RESEARCH.md as the content source. Specifics:
  - Section 1 (H1 + 1-sentence role): Keep `@fwornle/km-core` intro; bump status note from v0.1 to reflect Phase 44/45 maturity (per RESEARCH OQ-2). One sentence.
  - Section 2 (Configurations Owned): EXPLICITLY state "KM-Core is the SHARED CORE — owns no per-system config" with the four placeholder lines (`Ontology: — (owned by per-system: A in .data/ontologies/coding-ontology.json, B reads same, C in OKM ontology/*.json)`, etc.). This non-ownership statement is mandatory per RESEARCH and P-1.
  - Section 3 (Architecture): TWO image refs — `![KM-Core architecture](../../docs/images/km-core-architecture.png)` and `![KM-Core ingest sequence](../../docs/images/km-core-ingest-sequence.png)`. Followed by a 3-5 sentence summary of each (NOT a long discussion).
  - Section 4 (Where to Edit): Table with rows: "add Entity type" → `src/types/entity.ts` + `npm test`; "add REST endpoint" → `src/api/handlers/<resource>.ts` + register in `src/api/router.ts` + `npm test`; "add ingest stage" → `src/pipeline/IngestPipeline.ts` + IngestPipeline tests; "extend display overlay" → `src/ontology/display-overlay.ts` (Phase 45 surface) + display-overlay tests. Each row MUST include a verify command.
  - Section 5 (Related Systems): Links to root README (A), `integrations/mcp-server-semantic-analysis/README.md` (B), and OKM's BMW GHE URL (C).
  - Section 6 (Tests / Verify): `cd lib/km-core && npm test` AND a link to `docs/ONBOARDING.md` (forward-ref to Plan 46-05; the file will not exist until Wave 3 — that's expected and called out in a brief comment).
  - REMOVE the existing inline Mermaid `flowchart TB` block entirely (migrated to the architecture PUML).
  - PRESERVE the existing Install section + Build / Test section as-is (per RESEARCH OQ-2 audit — they're accurate).
  - UPDATE the Public API exports list to include Phase 44 additions (`@fwornle/km-core/api/contracts`, `SnapshotManager`, ontology `registry`) and Phase 45 additions (`display-overlay`) per RESEARCH OQ-2.</action>
  <verify>
    <automated>grep -q '^## Configurations Owned' lib/km-core/README.md && grep -q '^## Architecture' lib/km-core/README.md && grep -q '^## Where to Edit' lib/km-core/README.md && grep -q '^## Related Systems' lib/km-core/README.md && grep -q '^## Tests / Verify' lib/km-core/README.md && grep -q 'km-core-architecture.png' lib/km-core/README.md && grep -q 'km-core-ingest-sequence.png' lib/km-core/README.md && grep -q 'ONBOARDING.md' lib/km-core/README.md && ! grep -q 'flowchart TB' lib/km-core/README.md && ! grep -qi '```mermaid' lib/km-core/README.md && grep -q 'SnapshotManager\|display-overlay' lib/km-core/README.md</automated>
  </verify>
  <done>README.md contains all five `## ` headings in order; references both PNGs; references ONBOARDING.md; NO Mermaid blocks remain; Phase 44/45 surfaces (SnapshotManager or display-overlay) referenced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Operator visual check — KM-Core README + diagrams</name>
  <what-built>
    - `lib/km-core/docs/README-TEMPLATE.md` (template skeleton)
    - `lib/km-core/README.md` (rewritten to template)
    - Two PUML sources + four generated PNGs (architecture + ingest sequence, each duplicated to docs/images/ + docs-content/images/)
  </what-built>
  <how-to-verify>
    1. Open `lib/km-core/README.md` in a markdown previewer (or VS Code preview). Verify the six section headings appear in order: Header → Configurations Owned → Architecture → Where to Edit → Related Systems → Tests / Verify.
    2. Verify both architecture images render in the preview (NOT broken image icons).
    3. Open `docs/images/km-core-architecture.png` in an image viewer. Verify it visually distinguishes SHARED CORE (types/store/registry/pipeline/dedup/REST/viewer) from PER-SYSTEM CONFIG (ontology/LLM/ingest/domain-eval). This is the SC-2 enforcement surface.
    4. Open `docs/images/km-core-ingest-sequence.png` in an image viewer. Verify the sequence shows: Consumer → IngestPipeline → Dedup → GraphKMStore → Exports → Events.
    5. Run `diff docs/images/km-core-architecture.png docs-content/images/km-core-architecture.png` — must report no differences. Repeat for the ingest sequence PNG.
    6. Open `lib/km-core/docs/README-TEMPLATE.md`. Verify the skeleton is reusable (placeholders are clearly marked, sections match the README structure).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2 (Plans 46-02 / 46-03 / 46-04 in parallel), or describe issues (broken image refs, missing sections, diagram misclassification) and the executor will revise before unblocking Wave 2.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 46 produces docs only; the only trust boundary in scope is the **constraint monitor → file write** boundary (markdown / PUML / PNG outputs must satisfy `plantuml-standard-styling`, `plantuml-diagram-name-format`, `documentation-filename-format`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-01-DOC-ROT | Information disclosure | lib/km-core/README.md | mitigate | Acceptance greps verify all 6 section headings + Phase 44/45 surface refs; Plan 46-06 cross-ref sweep verifies inbound links |
| T-46-01-PNG-DRIFT | Tampering | docs/images/km-core-*.png vs docs/puml/km-core-*.puml | mitigate | `cmp -s` verification asserts byte-identity of the two-dir-duplicated PNGs; PUML sources committed alongside generated PNGs |
| T-46-01-CONSTRAINT-EVASION | Tampering | KM-Core PUML placement (`lib/km-core/docs/puml/` vs canonical `docs/puml/`) | mitigate | `OVERRIDE_CONSTRAINT: documentation-filename-format` annotation with explicit submodule-locality rationale; PNGs still land in canonical `docs/images/` |
| T-46-01-MERMAID-RESIDUE | Information disclosure | lib/km-core/README.md | mitigate | Verification block asserts `! grep -q 'flowchart TB'` and `! grep -qi '```mermaid'` — no Mermaid residue post-migration |
</threat_model>

<verification>
After all tasks complete, run the following as the plan's acceptance gate:

```bash
# Section presence in README.md
for h in '^## Configurations Owned' '^## Architecture' '^## Where to Edit' '^## Related Systems' '^## Tests / Verify'; do grep -q "$h" lib/km-core/README.md || { echo "MISSING: $h"; exit 1; }; done

# Section presence in template
for h in '^## Configurations Owned' '^## Architecture' '^## Where to Edit' '^## Related Systems' '^## Tests / Verify'; do grep -q "$h" lib/km-core/docs/README-TEMPLATE.md || { echo "TEMPLATE MISSING: $h"; exit 1; }; done

# Mermaid residue
! grep -q 'flowchart TB' lib/km-core/README.md && ! grep -qi '```mermaid' lib/km-core/README.md

# All four PNGs exist + byte-identical across dirs
test -f docs/images/km-core-architecture.png && test -f docs-content/images/km-core-architecture.png
test -f docs/images/km-core-ingest-sequence.png && test -f docs-content/images/km-core-ingest-sequence.png
cmp -s docs/images/km-core-architecture.png docs-content/images/km-core-architecture.png
cmp -s docs/images/km-core-ingest-sequence.png docs-content/images/km-core-ingest-sequence.png

# PUML preamble + override annotation
grep -q '_standard-style.puml' lib/km-core/docs/puml/km-core-architecture.puml
grep -q '_standard-style.puml' lib/km-core/docs/puml/km-core-ingest-sequence.puml
grep -q 'OVERRIDE_CONSTRAINT: documentation-filename-format' lib/km-core/docs/puml/km-core-architecture.puml
grep -q 'OVERRIDE_CONSTRAINT: documentation-filename-format' lib/km-core/docs/puml/km-core-ingest-sequence.puml

# Phase 44/45 surfaces referenced
grep -qE 'SnapshotManager|display-overlay' lib/km-core/README.md
```
</verification>

<success_criteria>
- README-TEMPLATE.md exists with all six locked section headings (1 H1 + 5 H2) in fixed order
- lib/km-core/README.md conforms to the template; no Mermaid residue; references both new PNGs
- Both KM-Core PUMLs exist with standard preamble + standard-style include + OVERRIDE annotation
- All four PNGs (2 diagrams × 2 dirs) exist and are byte-identical across docs/images/ vs docs-content/images/
- Phase 44 (REST/SnapshotManager) and Phase 45 (display-overlay) public surfaces referenced in README
- Operator visual-check (Task 6) approved
</success_criteria>

<output>
Create `.planning/phases/46-per-system-documentation-onboarding/46-01-SUMMARY.md` when done. Record:
- Final README-TEMPLATE.md structure (the literal 6 headings as authored)
- Public API exports list now surfaced in KM-Core README (so Plan 46-05 ONBOARDING.md can reference it)
- Render command sequence used (to seed Plan 46-03 for B's PUML and Plan 46-04 for OKM's PUML if needed)
- Any constraint-monitor overrides actually triggered + their rationale (for Wave 2 plans to anticipate)
</output>
