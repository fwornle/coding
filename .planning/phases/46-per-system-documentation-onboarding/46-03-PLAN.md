---
phase: 46-per-system-documentation-onboarding
plan: 03
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - integrations/mcp-server-semantic-analysis/README.md
  - integrations/mcp-server-semantic-analysis/docs/AGENTS.md
  - docs/puml/b-architecture.puml
  - docs/images/b-architecture.png
  - docs-content/images/b-architecture.png
  - integrations/mcp-server-semantic-analysis
autonomous: false
requirements:
  - DOC-01
user_setup: []
tags: [documentation, b-system, submodule, plantuml]

must_haves:
  truths:
    - "integrations/mcp-server-semantic-analysis/README.md conforms to the 6-section template from Plan 46-01"
    - "B's README.md is <= 200 lines (5-min readable per SC-1; current is 573 lines)"
    - "14-agent catalog is preserved inline as the Architecture section's box list (names + 1-line roles only — no per-agent enhancement narratives)"
    - "integrations/mcp-server-semantic-analysis/docs/AGENTS.md NEW — contains the long-form content moved from the old README (per-agent enhancements, MCP tool list, use cases, project structure)"
    - "docs/puml/b-architecture.puml NEW — visualizes 14 agents grouped by role (8 LLM-enhanced / 2 infrastructure / orchestration + wave-controller)"
    - "docs/images/b-architecture.png exists AND docs-content/images/b-architecture.png exists; the two are byte-identical (P-2 two-image-dir gotcha)"
    - "B's submodule pointer in the coding repo is bumped to include the new README/AGENTS commit (per P-5)"
  artifacts:
    - path: integrations/mcp-server-semantic-analysis/README.md
      provides: "B's README rewritten to template; <= 200 lines for SC-1"
      contains: "## Configurations Owned"
    - path: integrations/mcp-server-semantic-analysis/docs/AGENTS.md
      provides: "Long-form per-agent + use-case + project-structure docs (moved from old README)"
    - path: docs/puml/b-architecture.puml
      provides: "B's architecture PUML — 14 agents grouped by role"
    - path: docs/images/b-architecture.png
      provides: "Generated PNG for repo README rendering"
    - path: docs-content/images/b-architecture.png
      provides: "Generated PNG duplicated for MkDocs"
  key_links:
    - from: integrations/mcp-server-semantic-analysis/README.md
      to: integrations/mcp-server-semantic-analysis/docs/AGENTS.md
      via: "Architecture section link 'for per-agent detail see AGENTS.md'"
      pattern: "AGENTS.md"
    - from: integrations/mcp-server-semantic-analysis/README.md
      to: docs/images/b-architecture.png
      via: relative image ref
      pattern: "b-architecture.png"
    - from: integrations/mcp-server-semantic-analysis/README.md
      to: lib/km-core/README.md
      via: Related Systems markdown link
      pattern: "km-core"
  threats:
    - id: T-46-03-CONTENT-LOSS
      category: Information disclosure (long-form content silently dropped during split)
      component: README.md to AGENTS.md migration
      disposition: mitigate
      mitigation: "Acceptance grep verifies AGENTS.md contains content markers from the old README (per-agent narratives, use cases, project structure)"
    - id: T-46-03-SUBMODULE-DRIFT
      category: Tampering (README change committed in submodule but pointer bump skipped)
      component: integrations/mcp-server-semantic-analysis submodule pointer
      disposition: mitigate
      mitigation: "Verification block asserts git status in coding repo shows the submodule pointer as modified (then committed) after the submodule's README change"
    - id: T-46-03-PNG-DRIFT
      category: Tampering
      component: docs/images/b-architecture.png vs docs/puml/b-architecture.puml
      disposition: mitigate
      mitigation: "cmp -s verification asserts byte-identity of the two-dir-duplicated PNGs"
---

<objective>
Rewrite B's README to the 6-section template (<= 200 lines for SC-1 readability), split the long-form content into a new `AGENTS.md` companion doc, add a B architecture PUML, and bump the coding repo's submodule pointer.

Purpose: B's existing 573-line README buries the "Configurations Owned" answer under 20+ headings of per-agent enhancement narrative. SC-1 ("5-min discoverability") fails as-is. Splitting preserves all content (no information loss) while making the new README a 5-min entry point.

Output: Rewritten `integrations/mcp-server-semantic-analysis/README.md`, NEW `integrations/mcp-server-semantic-analysis/docs/AGENTS.md`, NEW `docs/puml/b-architecture.puml` + generated PNG (duplicated to both image dirs), and a submodule pointer bump commit in the coding repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/46-per-system-documentation-onboarding/46-CONTEXT.md
@.planning/phases/46-per-system-documentation-onboarding/46-RESEARCH.md
@.planning/phases/46-per-system-documentation-onboarding/46-PATTERNS.md
@.planning/phases/46-per-system-documentation-onboarding/46-01-SUMMARY.md
@lib/km-core/docs/README-TEMPLATE.md
@integrations/mcp-server-semantic-analysis/README.md
@docs/puml/_standard-style.puml
@docs/puml/adaptive-lsl-system.puml
@.claude/commands/documentation-style.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AGENTS.md and move long-form content from B's README</name>
  <files>integrations/mcp-server-semantic-analysis/docs/AGENTS.md</files>
  <action>Operating in the submodule working tree `integrations/mcp-server-semantic-analysis/`, create the new file `docs/AGENTS.md`. Move the long-form content out of the current `README.md` per RESEARCH OQ-3's section map: per-agent enhancement narratives (current README lines 88-141 and 234-394), LLM provider chain narrative (lines 126-141), use cases / walkthroughs (lines 413-462), project structure (lines 513-545). Preserve the content verbatim where reasonable — re-wrap headings to one-level-deeper since AGENTS.md becomes the long-form companion. Add a header to AGENTS.md: `# B — Agent Details` + 1-sentence pointer back to `../README.md`. The content moved here is the OPERATIONAL DEPTH that the slim README links to. Do NOT delete content from README.md yet; this task only ADDS AGENTS.md. Task 3 deletes the migrated sections from README.md.</action>
  <verify>
    <automated>test -f integrations/mcp-server-semantic-analysis/docs/AGENTS.md && grep -qE 'Use Cases?|use cases?|project structure|Per-Agent|per-agent' integrations/mcp-server-semantic-analysis/docs/AGENTS.md && grep -q '^# ' integrations/mcp-server-semantic-analysis/docs/AGENTS.md && [ "$(wc -l < integrations/mcp-server-semantic-analysis/docs/AGENTS.md)" -gt 100 ]</automated>
  </verify>
  <done>AGENTS.md exists with a top-level header, >100 lines, and contains anchor content from the old README's per-agent / use-cases / project-structure sections.</done>
</task>

<task type="auto">
  <name>Task 2: Author b-architecture.puml + render + duplicate PNG</name>
  <files>docs/puml/b-architecture.puml, docs/images/b-architecture.png, docs-content/images/b-architecture.png</files>
  <action>FIRST: invoke the `documentation-style` skill (mandatory per P-2). Then operating in the coding repo working tree (NOT the submodule — PUMLs live in the parent `docs/puml/` per CLAUDE.md placement rule), create `docs/puml/b-architecture.puml`. Begin with the canonical PUML preamble: the start-uml directive carrying the lowercase-hyphenated diagram name `b-architecture`, followed by the include of `_standard-style.puml` (same directory). Author the diagram showing the 14 agents grouped by role per RESEARCH § Diagram Plan row 3: LLM-enhanced agents (8) — derive concrete names from the catalog in the old README lines 9-27; infrastructure agents (2); orchestration — Coordinator + WaveController. Use packages/boxes per `docs/puml/_standard-style.puml` conventions. Render via `plantuml docs/puml/b-architecture.puml`. Move the generated PNG: `mv docs/puml/b-architecture.png docs/images/b-architecture.png`. Duplicate: `cp docs/images/b-architecture.png docs-content/images/b-architecture.png`. Both PNG paths MUST exist.</action>
  <verify>
    <automated>test -f docs/puml/b-architecture.puml && head -5 docs/puml/b-architecture.puml | grep -q 'b-architecture' && grep -q '_standard-style.puml' docs/puml/b-architecture.puml && test -f docs/images/b-architecture.png && test -f docs-content/images/b-architecture.png && cmp -s docs/images/b-architecture.png docs-content/images/b-architecture.png</automated>
  </verify>
  <done>PUML source exists with canonical preamble and lowercase-hyphenated name; both PNG dirs contain byte-identical generated PNGs.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite B's README.md to 6-section template (<= 200 lines)</name>
  <files>integrations/mcp-server-semantic-analysis/README.md</files>
  <action>Operating in the submodule working tree, REWRITE `README.md` to conform to the 6-section template (`lib/km-core/docs/README-TEMPLATE.md`), using RESEARCH § System-by-System Edit Map > B row for content. The new file MUST be <= 200 lines.

Section 1 (Header + 1-sentence role): one-line description of B as the MCP server driving wave-analysis, ingest, and ontology classification for the coding KG.

Section 2 (Configurations Owned) bullets:
- Ontology: not owned (READS .data/ontologies/coding-ontology.json from A)
- LLM providers: `lib/llm/` + `config/agents/*.json` (per-agent prompts) + `config/workflows/*.json` (wave-analysis processOverrides routing)
- Ingest adapters: `src/agents/*.ts` (each agent ingests via persistence-agent into km-core)
- Domain dedup: `src/agents/dedup-agent.ts` (Jaccard / containment overlay on top of km-core baseline)

Section 3 (Architecture): image ref `![B architecture](../../docs/images/b-architecture.png)` + 3-5 sentence summary describing the 8 LLM agents + 2 infra + orchestration grouping. Followed by the 14-agent catalog INLINE (just names + 1-line roles each — preserve from the old README lines 9-27). End with: `For per-agent enhancement detail, see [docs/AGENTS.md](docs/AGENTS.md).`

Section 4 (Where to Edit) table with these 4 rows:
- "add an agent" -> new file `src/agents/<NewAgent>.ts` + register in `src/agents/index.ts` + add to `src/coordinator.ts` workflow; verify: `npm run build && npm test -- agents`
- "change LLM routing for a process" -> `config/workflows/<workflow>.json` `processOverrides`; verify: restart container + check `/api/ukb/llm-mode` in dashboard
- "add a wave step" -> `src/wave-controller.ts` (step registration) + step impl; verify: `npm test -- wave-controller`
- "change dedup rule" -> `src/agents/dedup-agent.ts` (calculateNameSimilarity / fuzzy thresholds); verify: `npm test -- dedup`

Section 5 (Related Systems) bullets:
- `[KM-Core](../../lib/km-core/README.md)` — shared store / REST contracts B writes through
- `[A: coding](../../README.md)` — host runtime + observation source
- `[C: OKM (BMW GHE)](https://bmw.ghe.com/adpnext-apps/operational-knowledge-management)` — sister-system using same km-core

Section 6 (Tests / Verify) code block: `cd integrations/mcp-server-semantic-analysis && npm run build && npm test` + a note pointing to CLAUDE.md § "Rebuilding After Code Changes" because submodule TS changes require BOTH `npm run build` AND a Docker rebuild of `coding-services`.

DELETE the content that was moved to AGENTS.md (per-agent enhancement narratives, use cases, project structure). The result MUST be <= 200 lines.</action>
  <verify>
    <automated>L=$(wc -l < integrations/mcp-server-semantic-analysis/README.md); [ "$L" -le 200 ] && grep -q '^## Configurations Owned' integrations/mcp-server-semantic-analysis/README.md && grep -q '^## Architecture' integrations/mcp-server-semantic-analysis/README.md && grep -q '^## Where to Edit' integrations/mcp-server-semantic-analysis/README.md && grep -q '^## Related Systems' integrations/mcp-server-semantic-analysis/README.md && grep -q '^## Tests / Verify' integrations/mcp-server-semantic-analysis/README.md && grep -q 'b-architecture.png' integrations/mcp-server-semantic-analysis/README.md && grep -q 'docs/AGENTS.md' integrations/mcp-server-semantic-analysis/README.md && grep -q 'lib/km-core/README.md' integrations/mcp-server-semantic-analysis/README.md</automated>
  </verify>
  <done>README.md is <= 200 lines, all five `## ` second-level headings present in order, links to AGENTS.md + b-architecture.png + KM-Core / A / C.</done>
</task>

<task type="auto">
  <name>Task 4: Commit submodule README + AGENTS, then bump coding-repo submodule pointer</name>
  <files>integrations/mcp-server-semantic-analysis</files>
  <action>Per PATTERNS.md P-5: inside the submodule working tree at `integrations/mcp-server-semantic-analysis/`, stage and commit the new `README.md` + `docs/AGENTS.md` with a message like `docs(46-03): rewrite README to 6-section template + split long-form to docs/AGENTS.md`. Push to the submodule's `origin/main` (HTTPS). Then in the coding repo working tree, `git add integrations/mcp-server-semantic-analysis` to capture the submodule pointer bump and commit (this is a separate commit from the submodule's own commit). Verify the coding-repo `git status` is CLEAN for the submodule pointer (no `modified content` / `untracked content` markers in the submodule) before declaring done — if any markers remain, the submodule has uncommitted changes that must be addressed first. Do NOT use `--no-verify` to skip hooks.</action>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && git log -1 --format='%s' | grep -qiE 'README|docs/AGENTS|46-03' && cd ../.. && git status integrations/mcp-server-semantic-analysis | grep -qiE 'new commits|nothing to commit' && ! git status integrations/mcp-server-semantic-analysis 2>&1 | grep -qiE 'modified content|untracked content'</automated>
  </verify>
  <done>Submodule has a fresh commit on its main branch matching Plan 46-03; coding-repo's submodule pointer is either bumped (showing "new commits") or already committed; no uncommitted content remains inside the submodule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator visual check — B's README + AGENTS.md + architecture PNG</name>
  <what-built>
    - Rewritten `integrations/mcp-server-semantic-analysis/README.md` (<= 200 lines, 6-section template)
    - NEW `integrations/mcp-server-semantic-analysis/docs/AGENTS.md` (long-form companion)
    - NEW `docs/puml/b-architecture.puml` + generated PNGs in both `docs/images/` and `docs-content/images/`
    - Submodule commit pushed + coding-repo submodule pointer bumped
  </what-built>
  <how-to-verify>
    1. Open `integrations/mcp-server-semantic-analysis/README.md` in a markdown previewer. Verify the five `## ` section headings appear in order and the file is visually compact (one-screen scroll roughly).
    2. Verify the architecture image renders (NOT a broken icon).
    3. Click the inline link to `docs/AGENTS.md`. Verify the companion file opens and contains the moved long-form content (per-agent enhancements, use cases, project structure).
    4. Click the Related Systems links to verify KM-Core and root README targets resolve.
    5. Open `docs/images/b-architecture.png` in an image viewer. Verify the 14 agents are visibly grouped by role (LLM / infra / orchestration).
    6. Confirm `diff docs/images/b-architecture.png docs-content/images/b-architecture.png` returns no differences.
    7. Confirm `git log -1 --format='%H %s' integrations/mcp-server-semantic-analysis` shows the submodule's new commit; confirm the coding repo's pointer was advanced (e.g., `git submodule status integrations/mcp-server-semantic-analysis` shows no leading `+` or `-` marker).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Plan 46-03 complete, or describe issues (README too long, missing image, broken AGENTS link, submodule pointer not bumped).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The only trust boundary is the **constraint monitor + git submodule pointer** boundary. No untrusted input handling, no auth, no code execution.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-03-CONTENT-LOSS | Information disclosure | README -> AGENTS.md migration | mitigate | Acceptance grep on AGENTS.md verifies it contains anchor markers from old README's long-form sections (>100 lines minimum) |
| T-46-03-SUBMODULE-DRIFT | Tampering | submodule pointer in coding repo | mitigate | Task 4 explicit submodule-pointer bump + verification grep on `git status` (no `modified content` / `untracked content`) |
| T-46-03-PNG-DRIFT | Tampering | `docs/images/b-architecture.png` vs `docs/puml/b-architecture.puml` | mitigate | `cmp -s` verification asserts byte-identity between `docs/images/` and `docs-content/images/` copies |
| T-46-03-CONSTRAINT-FAIL | Tampering | PUML diagram-name-format constraint | mitigate | Diagram name `b-architecture` is lowercase-hyphenated; standard-style include is the second PUML line |
</threat_model>

<verification>
```bash
# All five required headings in B README, in order
for h in '^## Configurations Owned' '^## Architecture' '^## Where to Edit' '^## Related Systems' '^## Tests / Verify'; do grep -q "$h" integrations/mcp-server-semantic-analysis/README.md || { echo "MISSING: $h"; exit 1; }; done

# README size budget
L=$(wc -l < integrations/mcp-server-semantic-analysis/README.md)
[ "$L" -le 200 ] || { echo "README too long: $L lines"; exit 1; }

# AGENTS.md exists and has content
test -f integrations/mcp-server-semantic-analysis/docs/AGENTS.md
[ "$(wc -l < integrations/mcp-server-semantic-analysis/docs/AGENTS.md)" -gt 100 ]

# PUML + both PNGs
test -f docs/puml/b-architecture.puml
test -f docs/images/b-architecture.png
test -f docs-content/images/b-architecture.png
cmp -s docs/images/b-architecture.png docs-content/images/b-architecture.png

# Cross-refs in B README
grep -qF 'docs/AGENTS.md' integrations/mcp-server-semantic-analysis/README.md
grep -qF 'lib/km-core/README.md' integrations/mcp-server-semantic-analysis/README.md
grep -qF '../../README.md' integrations/mcp-server-semantic-analysis/README.md
grep -qF 'bmw.ghe.com/adpnext-apps/operational-knowledge-management' integrations/mcp-server-semantic-analysis/README.md

# Submodule pointer is clean (no uncommitted modified or untracked content inside the submodule)
! git status integrations/mcp-server-semantic-analysis 2>&1 | grep -qiE 'modified content|untracked content'
```
</verification>

<success_criteria>
- B's README <= 200 lines and conforms to the 6-section template
- AGENTS.md exists with long-form content moved from old README (>100 lines)
- B's architecture PUML + PNG exist; PNGs duplicated to both image dirs (byte-identical)
- Submodule has fresh commit with new README + AGENTS.md, pushed to origin
- Coding repo's submodule pointer bumped (no uncommitted content inside submodule)
- All SC-4 cross-refs present (KM-Core, A, C)
- Operator visual-check (Task 5) approved
</success_criteria>

<output>
Create `.planning/phases/46-per-system-documentation-onboarding/46-03-SUMMARY.md` when done. Record:
- Final line count of the rewritten B README
- AGENTS.md line count + summary of what content moved
- The submodule commit hash + the coding-repo pointer-bump commit hash
- Any constraint-monitor overrides triggered during PUML authoring
</output>
