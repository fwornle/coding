---
phase: 66-dashboard-latency-observability
plan: 04
type: execute
wave: 2
depends_on: [66-03]
files_modified:
  - integrations/system-health-dashboard/src/components/llm-latency-tile.tsx
  - integrations/system-health-dashboard/src/pages/token-usage.tsx
autonomous: false
gap_closure: true
requirements: [PERF-03]
tags: [perf, observability, dashboard, react, overhead, latency, PERF-03]

user_setup: []

must_haves:
  truths:
    - "The :3032 LLM Latency tile renders the per-model OVERHEAD median (overhead_ms), not total latency_ms — labelled 'Spawn Overhead' / 'Pool Overhead'"
    - "A warm-pool steady state shows the sonnet/opus overhead median <=3s with a GREEN 'OK' badge (the ~14s->near-zero speedup is glanceable)"
    - "With the pool disabled (cold-spawn storm), the overhead median climbs toward ~14s and the badge flips red ('Regressed') within a poll cycle — SC-2 detectable"
    - "The Token Usage by-model table shows an OVERHEAD column (p50_overhead_ms) with the green/amber/red envelope; the existing total-latency Median column stays as forensic context"
    - "Quiet models still render as fixed rows ('no recent pool calls'); haiku stays a non-graded reference (direct path, no pool overhead)"
  artifacts:
    - path: "integrations/system-health-dashboard/src/components/llm-latency-tile.tsx"
      provides: "headline tile re-pointed to overhead_ms from the /recent feed, ≤3s/amber/red envelope, relabelled"
      contains: "overhead_ms"
    - path: "integrations/system-health-dashboard/src/pages/token-usage.tsx"
      provides: "by-model Overhead column reading p50_overhead_ms with threshold badge"
      contains: "p50_overhead_ms"
  key_links:
    - from: "llm-latency-tile.tsx"
      to: "/api/token-usage/recent (overhead_ms per row)"
      via: "same-origin fetch, median over last ~50 non-null overhead samples per family"
      pattern: "overhead_ms"
    - from: "token-usage.tsx by-model row"
      to: "summary.by_model[].p50_overhead_ms"
      via: "formatLatency + threshold badge"
      pattern: "p50_overhead_ms"
---

<objective>
Re-point both dashboard surfaces from TOTAL end-to-end latency to the per-model SPAWN/QUEUE OVERHEAD median that Plan 66-03 added to the proxy (overhead_ms on /recent; p50_overhead_ms on the summary by_model rows). The headline LlmLatencyTile and the Token Usage by-model table now grade the metric the worker pool actually affects, so the existing ≤3s green / amber / red envelope becomes MEANINGFUL: warm pool -> overhead near zero -> green; pool regression -> overhead climbs toward ~14s -> red.

Purpose: closes the PERF-03 UAT gap. Phase 66 wired the threshold machinery correctly but pointed it at total latency (sonnet median ~123s), which can never go green and hides pool regressions in generation-time noise. Swapping the value to overhead restores SC-1 (speedup visible) and SC-2 (regression detectable) WITHOUT touching the proven threshold/sparkline/always-keep-rows UX from 66-02 v4.

Output: re-labelled tile + new Overhead column, visually re-verified with gsd-browser, gated by an operator checkpoint for the live warm-green and pool-disabled-red confirmations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-dashboard-latency-observability/66-HUMAN-UAT.md
@.planning/phases/66-dashboard-latency-observability/66-02-SUMMARY.md
@.planning/phases/66-dashboard-latency-observability/66-REVIEW.md
@./CLAUDE.md

<read_first>
1. integrations/system-health-dashboard/src/components/llm-latency-tile.tsx — the 66-02 v4 last-N-calls tile. Key anchors:
   - RecentCall interface (~53-57): currently reads timestamp, model, latency_ms. ADD overhead_ms (number or null).
   - TRACKED_FAMILIES (~47-51): sonnet/opus (graded), haiku (reference). UNCHANGED — but note haiku is the direct path with no pool overhead.
   - recentSamplesForFamily (~123-128): filters by family + Number.isFinite(latency_ms). RE-POINT to filter on Number.isFinite(overhead_ms) and read overhead_ms.
   - median() (~114-118): unchanged (lower-mid).
   - latencyStatus() (~88-92): the ≤3000 / ≤5000 / >5000 envelope — UNCHANGED (now meaningful against overhead).
   - the fetch (~189) of /api/token-usage/recent?limit=1000 — UNCHANGED endpoint; the rows now carry overhead_ms (66-03).
   - the headline/sparkline build (~198-229) reading c.latency_ms — RE-POINT to c.overhead_ms.
   - subtitle/legend (~278): "Per-model median · warm target ≤3s · last ~50 calls" — relabel to overhead wording.
   - tooltips (in the TileItem build) — relabel to overhead wording.
2. integrations/system-health-dashboard/src/pages/token-usage.tsx — the by-model table. Key anchors:
   - TokenSummary.by_model type (~121-128): currently p50_latency_ms?. ADD p50_overhead_ms? (number).
   - meta map build (~733-741): currently captures p50_latency_ms. ADD p50_overhead_ms.
   - the table headers (~751-752): "Avg Latency", "Median Latency". ADD a new "Spawn Overhead" (or "Pool Overhead") column header.
   - the median cell render (~786-803): the green/amber/red badge via latencyThresholdStatus + isHaikuModel gating. MIRROR it for the overhead column reading p50_overhead_ms. KEEP the existing total-latency Median Latency column intact (forensic context per the gap note — do NOT remove p50_latency_ms).
   - latencyThresholdStatus (~204) + isHaikuModel (~194) + formatLatency (~185): reuse as-is.
3. CLAUDE.md — dashboard rebuild: npm run build then docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend (bind-mounted, NO docker-compose build). gsd-browser mandate for visual verification. Ports 3032 (dashboard) / 3033 (Health API, NOT this).
</read_first>

<interfaces>
From 66-03 (proxy, now live):
  /api/token-usage/recent -> data[] rows each: { timestamp, model, latency_ms, overhead_ms (number|null), ... }
  /api/token-usage/summary?hours=N -> by_model[] each: { model, calls, total_tokens, avg_latency, p50_latency_ms?, p50_overhead_ms? }

overhead_ms semantics: spawn+queue+dispatch-to-first-output (ms). Warm reuse near zero; cold spawn ~10-14s; NULL for direct-OAuth (haiku) and execFile-overflow calls.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the LlmLatencyTile headline to the overhead median and relabel</name>
  <files>integrations/system-health-dashboard/src/components/llm-latency-tile.tsx</files>
  <action>
    Swap the tile's graded value from total latency_ms to the pool OVERHEAD (overhead_ms from 66-03), preserving the 66-02 v4 last-N-calls + always-keep-rows + sparkline UX exactly (per the gap note: keep the proven UX, swap only the value).

    1. RecentCall interface (~53-57): add overhead_ms (number or null).
    2. recentSamplesForFamily (~123-128): filter on Number.isFinite(c.overhead_ms) instead of latency_ms, so a row with NULL overhead (direct/haiku calls, or pre-66-03 legacy rows) is excluded from the graded sample. Keep the family keyword match + newest-first + slice(SAMPLE_SIZE).
    3. The per-family build (~198-229): compute the median over samples.map(c => c.overhead_ms); build the sparkline from the same overhead samples (oldest->newest). Status = latencyStatus(p50overhead) for graded families (sonnet/opus); reference for haiku. KEEP latencyStatus thresholds (≤3000 OK / ≤5000 Elevated / >5000 Regressed) — they now grade overhead and are meaningful.
    4. haiku family: it is the direct path with ~no pool overhead, so its overhead_ms rows are NULL -> recentSamplesForFamily yields zero samples -> it renders the existing "no recent pool calls" placeholder OR keep it as an explicit reference row. Decision (state it in code + SUMMARY, D-04 spirit): render haiku as a reference row labelled "direct path — no pool overhead" rather than a graded row. Do NOT grade haiku.
    5. Relabel for overhead: card subtitle/legend (~278) to "Per-model spawn overhead · warm target ≤3s · last ~50 calls"; the description strings (~226-229) to "... overhead median ..."; tooltips to "Median spawn/queue overhead over the last ~50 calls. Warm reuse near 0; amber 3–5s; red >5s climbing toward the ~14s cold-spawn baseline. Excludes model generation time." haiku tooltip: "Direct-path OAuth — no worker-pool overhead (reference only)." Update the card title from "LLM Latency" to "LLM Pool Overhead" (or "Spawn Latency" — Claude's discretion, keep it clearly about overhead not total latency).
    6. Empty/quiet handling: a graded family with zero non-null overhead samples in the feed renders the muted "no recent pool calls" placeholder (NOT dropped) — reuse the existing empty-row path. This also covers the realistic case where recent traffic was all warm-direct or all overflow.

    Do NOT change the fetch endpoint or limit; /recent already carries overhead_ms after 66-03. Do NOT remove the sparkline. NO fenced code in this plan.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/system-health-dashboard && echo tile-overhead $(grep -c overhead_ms src/components/llm-latency-tile.tsx); echo no-latency-grade $(grep -c "latency_ms" src/components/llm-latency-tile.tsx); npx tsc --noEmit 2>&1 | grep -iE "llm-latency-tile" | grep -v "deferred" | head; echo tsc-done</automated>
  </verify>
  <done>The tile fetches /recent, filters to non-null overhead_ms, computes the per-family median + sparkline over overhead, grades sonnet/opus with the ≤3s/amber/red envelope, renders haiku as a no-pool-overhead reference, keeps quiet graded families as "no recent pool calls" placeholders, and is relabelled throughout to "spawn/pool overhead". No NEW tsc errors on llm-latency-tile.tsx (pre-existing deferred-items errors excepted).</done>
</task>

<task type="auto">
  <name>Task 2: Add the Overhead column to the Token Usage by-model table and rebuild + restart the frontend</name>
  <files>integrations/system-health-dashboard/src/pages/token-usage.tsx</files>
  <action>
    Add a per-model Overhead column reading p50_overhead_ms (66-03), mirroring the existing Median Latency column's badge treatment, and KEEP the existing Median Latency (total) column as forensic context.

    1. TokenSummary.by_model type (~121-128): add p50_overhead_ms (optional number).
    2. meta map build (~733-741): capture p50_overhead_ms alongside p50_latency_ms into the per-model meta entry.
    3. Table header (~751-752): add a "Spawn Overhead" header cell after "Median Latency" (right-aligned to match).
    4. The new Overhead cell: mirror the median cell render (~786-803) but read m.p50_overhead_ms. Apply latencyThresholdStatus + the green/amber/red badge for non-haiku models in the model group-by view; render haiku plain/muted (isHaikuModel gate). When p50_overhead_ms is absent (no pool data for the model), render a muted dash. Reuse formatLatency.
    5. Do NOT remove or alter the existing Median Latency (p50_latency_ms) column — it stays as total-latency forensic context per the gap note. The threshold treatment now legitimately applies to the OVERHEAD column; the total-latency column may keep its existing badge OR drop the badge to avoid implying total latency should be ≤3s (Claude's discretion; if kept, add a header tooltip clarifying it is total end-to-end, not pool overhead).

    6. Rebuild + restart per CLAUDE.md: cd integrations/system-health-dashboard && npm run build, then docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend. Confirm the bundle carries overhead.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/system-health-dashboard && echo page-overhead $(grep -c p50_overhead_ms src/pages/token-usage.tsx); npx tsc --noEmit 2>&1 | grep -iE "token-usage.tsx" | grep -vE "recharts|Formatter|TS2322" | head; npm run build 2>&1 | tail -3; echo bundle-overhead $(grep -rl "p50_overhead_ms" dist/assets/ 2>/dev/null | head -1)</automated>
  </verify>
  <done>The by-model table shows a Spawn Overhead column reading p50_overhead_ms with the green/amber/red badge for sonnet/opus and a muted haiku/absent rendering; the existing total-latency Median column is retained. npm run build exits 0, the bundle carries p50_overhead_ms, and the frontend service was restarted (RUNNING). No NEW tsc errors beyond the pre-existing recharts/Formatter cluster.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual re-verification — warm overhead green + pool-disabled overhead red (gsd-browser)</name>
  <action>This is a human-verify checkpoint. See what-built / how-to-verify below; resume only on the operator signal.</action>
  <what-built>
    Both dashboard surfaces now grade the worker-pool SPAWN OVERHEAD (overhead_ms / p50_overhead_ms from 66-03) instead of total end-to-end latency:
    - :3032 LLM Pool Overhead tile — per-model overhead median over the last ~50 pool calls, ≤3s green ("OK") / amber ("Elevated") / red ("Regressed"), haiku as a no-pool-overhead reference, quiet graded families show "no recent pool calls", sparkline rides the overhead samples.
    - Token Usage by-model table — a Spawn Overhead column (p50_overhead_ms) with the threshold badge, alongside the retained total-latency Median column.
  </what-built>
  <how-to-verify>
    Use gsd-browser per CLAUDE.md (navigate / screenshot), NOT a hand-rolled Playwright script.

    1. Warm-pool GREEN (SC-1):
       - Ensure the pool is enabled (no LLM_PROXY_DISABLE_WORKER_POOL=1) and drive a few sonnet claude-code fallback calls so warm overhead rows accumulate (POST http://localhost:12435/api/complete, process "phase-66-uat", model "standard", "say OK").
       - gsd-browser navigate http://localhost:3032 ; screenshot the LLM Pool Overhead tile. EXPECT: sonnet overhead median ≤3s with a GREEN "OK" badge (warm reuse overhead near zero). This is the visible ~14s->≤3s speedup the prior tile could never show.
       - Also open Token Usage -> Evolution -> By Model; confirm the Spawn Overhead column shows the small warm overhead with the green badge while the total-latency Median column still shows the large generation-dominated number.

    2. Pool-disabled RED (SC-2):
       - Restart the proxy with the pool disabled: LLM_PROXY_DISABLE_WORKER_POOL=1 (set in the launchd env / wrapper, then launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy), OR drive cold-spawn-heavy traffic.
       - Drive 3-5 sonnet fallback calls; wait ~30s for the tile poll; screenshot. EXPECT: the sonnet overhead median climbs toward ~14s and the badge flips to red "Regressed" — confirming the metric tracks the pool's contribution and a regression is now attributable (which it was NOT on total latency).
       - Re-enable the pool afterward (remove the env var, kickstart again) and confirm it returns toward green as warm calls age in.

    3. Quiet/reference rows: confirm sonnet/opus/haiku always render — quiet graded families show "no recent pool calls"; haiku shows the reference label (no pass/fail).
  </how-to-verify>
  <resume-signal>Type "approved" if (a) warm overhead reads ≤3s green, (b) pool-disabled overhead climbs red, and (c) all three rows always render with haiku as reference. Otherwise describe what renders wrong (attach the gsd-browser screenshots).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> dashboard same-origin /api proxy | the tile + page read overhead_ms / p50_overhead_ms via server.js reverse proxy (no direct host:port) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-04-01 | Information disclosure | tile/page fetch path | mitigate | tile uses same-origin /api/token-usage/* (66-02 pattern preserved); no hardcoded localhost:12435 added to the tile |
| T-66-04-02 | Tampering | NULL/absent overhead_ms in the feed | mitigate | Number.isFinite(overhead_ms) filter + muted-dash/absent rendering so NULL rows (direct/legacy) never crash the median or render NaN |
| T-66-04-03 | DoS | unbounded /recent fetch | accept | limit=1000 is server-clamped to 500 (token-usage.ts getRecent); pre-existing bound, unchanged |
| T-66-04-SC | Tampering | npm/pip/cargo installs | mitigate | NO new package added (re-points existing fields; recharts deliberately avoided per 66-02); slopcheck N/A |
</threat_model>

<verification>
- grep: llm-latency-tile.tsx reads overhead_ms; token-usage.tsx reads p50_overhead_ms.
- tsc --noEmit introduces NO new errors on the two touched files (pre-existing recharts/Formatter + StatusItem clusters excepted).
- npm run build exits 0; the bundle carries the overhead fields; frontend service RUNNING after restart.
- gsd-browser screenshots: warm overhead green, pool-disabled overhead red, all three rows always render.
</verification>

<success_criteria>
- The :3032 tile grades pool spawn overhead (overhead_ms), not total latency; warm -> ≤3s green, pool-disabled -> red.
- The Token Usage table shows a Spawn Overhead column (p50_overhead_ms) with the threshold badge; the total-latency Median column is retained as forensic context.
- haiku is a non-graded reference (direct path, no pool overhead); quiet graded families show "no recent pool calls".
- Operator checkpoint confirms SC-1 (warm green) and SC-2 (pool-disabled red) on live traffic, closing the PERF-03 UAT gap.
</success_criteria>

<output>
Create `.planning/phases/66-dashboard-latency-observability/66-04-SUMMARY.md` when done. Record the relabelled wording chosen, the haiku reference decision, the warm-green and pool-disabled-red gsd-browser evidence paths, and confirm p50_latency_ms (total) was retained. On checkpoint approval, note that PERF-03 / SC-1 / SC-2 are closed so REQUIREMENTS.md PERF-03 can move from "Implemented & verified (live pending)" to "Complete".
</output>
