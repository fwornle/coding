---
phase: 66-dashboard-latency-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.test.ts
autonomous: true
requirements: [PERF-03]

must_haves:
  truths:
    - "getSummary()'s by_model rows each carry a p50_latency_ms field (median latency_ms over the 24h window)"
    - "The median is computed over ALL rows in the window including cold spawns — no warm/cold filtering"
    - "GET /api/token-usage/summary?hours=24 JSON contains by_model[].p50_latency_ms for each model"
    - "An empty or young token-usage DB returns p50_latency_ms: 0 (or null) rather than throwing a 500"
  artifacts:
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts"
      provides: "MEDIAN(latency_ms) per-model query injected into getSummary by_model block"
      contains: "p50_latency_ms"
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/dist/token-usage.js"
      provides: "Compiled output the running proxy loads"
      contains: "p50_latency_ms"
  key_links:
    - from: "getSummary by_model SELECT"
      to: "token_usage table"
      via: "parameterized prepare(...).all(since) with clamped hours window"
      pattern: "p50_latency_ms"
---

<objective>
Add a per-model median (p50) latency query to the rapid-llm-proxy token-usage SQL layer and
surface it on the existing `getSummary()` `by_model` rows, so the dashboard (Plan 66-02) can
read a `p50_latency_ms` field with zero new endpoint or proxy wiring.

Purpose: PERF-03 requires the dashboard to show MEDIAN claude-code latency (not the existing
skewed AVG). The median is the single new computation this whole phase introduces — SQLite has
no native `MEDIAN`, and no percentile query exists anywhere in either repo today (66-PATTERNS
§ "No Analog Found"). Computing it in the proxy SQL layer (Claude's discretion per D-07; the
recommended path in 66-CONTEXT § Claude's Discretion) keeps the dashboard change to pure render.

Output: `getSummary()` returns `p50_latency_ms` on every `by_model` row; the field flows through
the unchanged `/api/token-usage/summary` endpoint and the unchanged dashboard reverse-proxy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-dashboard-latency-observability/66-CONTEXT.md
@.planning/phases/66-dashboard-latency-observability/66-PATTERNS.md

<interfaces>
<!-- Extracted from /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts. -->
<!-- Use these directly — do not re-explore the file beyond <read_first>. -->

getSummary() typed return (token-usage.ts:747-766) — by_model is the block to extend:
  by_model: Array<Record<string, number | string>>;   // top-level return field
  // current by_model SELECT (lines 807-814):
  //   SELECT model, COUNT(*) AS calls, SUM(total_tokens) AS total_tokens,
  //          ROUND(AVG(latency_ms)) AS avg_latency
  //   FROM token_usage WHERE timestamp >= ? GROUP BY model ORDER BY total_tokens DESC
  // .all(since) — `since` is an ISO string already clamped to the earliest real row.

Rolling-24h `since` clamp already computed once at lines 774-779 (rawSince = Date.now() - hours*3600_000,
clamped up to MIN(timestamp)); reuse the SAME `since` variable for the median query — do NOT recompute.

`hours` defaults to 24 (line 767: opts.hours ?? 24) — the canonical D-05 window. The caller
(server.mjs) clamps `hours=all` to 10y and parseInt's the param; do not trust it unbounded — see threat T-66-01-02.

return object assembled at lines 937-947 — by_model is already spread into it, so once the
SELECT carries p50_latency_ms the field propagates with no return-statement edit.

Column facts (from 66-PATTERNS): `model` is canonicalized at log time (stable GROUP BY keys);
`provider='claude-code'` is the fallback path, haiku is a different provider (direct path).
The median query GROUPs by model only — Plan 66-02 applies the regression threshold per provider.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MEDIAN(latency_ms) per-model query and expose p50_latency_ms on by_model rows</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts, /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts lines 740-947 (getSummary: typed return, since-clamp at 774-779, by_model SELECT at 807-814, return assembly at 937-947) — the file being modified and its self-analog
    - .planning/phases/66-dashboard-latency-observability/66-PATTERNS.md § "src/token-usage.ts — add MEDIAN(latency_ms)" (SQLite median approaches; since-window idiom; best-effort never-throw rule)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.test.ts (existing getSummary test shape, to mirror for the new assertion)
  </read_first>
  <behavior>
    - Test 1: getSummary over a DB seeded with claude-code/sonnet latencies [1000,2000,3000,4000,5000] returns a by_model row for that model with p50_latency_ms === 3000 (true median of an odd/even set — pick the documented convention, e.g. lower-mid for even counts; assert the exact value you implement).
    - Test 2: getSummary over an EMPTY token_usage DB returns without throwing and every by_model entry is absent (no rows) — i.e. the median query does not 500 on an empty/young DB.
    - Test 3: getSummary with rows OUTSIDE the 24h window (timestamp older than `since`) excludes them from the median — a single in-window row of 2500 yields p50_latency_ms === 2500 regardless of out-of-window outliers.
  </behavior>
  <action>
    Inject a per-model median into getSummary()'s existing by_model block (token-usage.ts ~807-814) so each by_model row carries a numeric `p50_latency_ms` field, computed over the SAME already-clamped `since` window (reuse the `since` variable from lines 774-779 — D-05 rolling 24h; do NOT recompute or add a second window). The median MUST be over ALL in-window rows for that model including cold spawns per D-06 — no warm/cold or provider filter in this query (Plan 66-02 owns the per-provider regression threshold; the proxy just reports the honest number per D-07).

    SQLite has no native MEDIAN — implement one parameterized approach (Claude's discretion per D-07): either an ordered-offset subquery per model (`SELECT latency_ms ... WHERE model=? AND timestamp>=? ORDER BY latency_ms LIMIT 1 OFFSET (cnt-1)/2`, computing the per-model count first), OR pull the per-model latency rows and compute p50 in JS (cheap at 24h volumes). Whichever you pick: ALL params (`since`, model, offset/limit) MUST be passed as bound `.all(...)`/`.get(...)` parameters — NEVER string-interpolated into the SQL (threat T-66-01-01). Keep it inside the same best-effort `prepare(...).all(since)` idiom that the surrounding aggregates use; a malformed/empty DB must yield no row / `p50_latency_ms` absent, not throw into the hot path (threat T-66-01-03, mirrors logCall try/catch at 701-724).

    Add `p50_latency_ms` to the by_model row's effective shape (the row type is `Record<string, number | string>`, so attach the field on each row object before it is spread into the return at 937-947 — the median may need a JS post-pass over the by_model array if you compute per-model offsets separately). Do NOT change the top-level return-statement structure; by_model is already spread in. Document the median convention (lower-mid vs interpolated for even counts) in a one-line code comment so the test's expected value is unambiguous.

    Do NOT add a new exported function or a new endpoint — piggyback on getSummary per the lowest-cost path in 66-PATTERNS § "Two exposure options" (option 1). No `proxy-bridge/server.mjs` edit is in this plan's files_modified; the summary endpoint surfaces the new field automatically.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` in the proxy repo passes including the 3 new behavior assertions (median value, empty-DB no-throw, out-of-window exclusion).
    - Source assertion: `grep -c "p50_latency_ms" /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts` returns ≥ 1 (count over the non-comment source; the median field is referenced in the by_model assembly).
    - Source assertion: the median SQL passes `since` and any model/offset values as bound parameters — `grep -nE "prepare\(\`?[^)]*latency_ms" /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts` shows the new query uses `?` placeholders, NOT `${...}` interpolation of user-derived values.
    - Behavior assertion: seeded median test yields the exact documented p50 value; empty-DB test does not throw.
  </acceptance_criteria>
  <done>by_model rows carry p50_latency_ms computed over the clamped 24h window via a parameterized median query; new tests green; no new endpoint introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Build the proxy and restart the launchd daemon so the running proxy serves p50_latency_ms</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/dist/token-usage.js</files>
  <read_first>
    - CLAUDE.md § "rapid-llm-proxy" facts (planning_context): build via `npm run build` in the proxy repo; the proxy runs as launchd daemon `com.coding.llm-cli-proxy` on host port 12435; restart with `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`
    - .planning/phases/66-dashboard-latency-observability/66-PATTERNS.md § "Build reminder: token-usage.ts compiles to dist/token-usage.js — npm run build in the proxy repo before the running proxy picks up the new query"
  </read_first>
  <action>
    Compile the proxy TS so `dist/token-usage.js` carries the new median query, then restart the launchd daemon so the live proxy on port 12435 serves the field. Run `npm run build` in `/Users/Q284340/Agentic/_work/rapid-llm-proxy`, then `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`. After restart, probe `GET http://localhost:12435/api/token-usage/summary?hours=24` and confirm the JSON `by_model[]` array carries `p50_latency_ms` on each row (the field is present even if 0 / absent on a model with no in-window rows). This is the live-data integration proof that the proxy SQL change reached the wire; Plan 66-02 consumes exactly this field.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && npm run build 2>&1 | tail -5 && grep -c p50_latency_ms dist/token-usage.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `grep -c p50_latency_ms /Users/Q284340/Agentic/_work/rapid-llm-proxy/dist/token-usage.js` returns ≥ 1.
    - CLI assertion: after `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`, `curl -s 'http://localhost:12435/api/token-usage/summary?hours=24' | python3 -c "import sys,json; d=json.load(sys.stdin); print('p50' if any('p50_latency_ms' in r for r in d.get('by_model',[])) or d.get('by_model')==[] else 'MISSING')"` prints `p50` (field present on rows, or by_model empty on a quiet DB).
    - Endpoint reachable on 12435 (not the 3033 Health API — per CLAUDE.md the Health API silently returns HTML for unknown paths).
  </acceptance_criteria>
  <done>dist/token-usage.js carries p50_latency_ms; daemon restarted; live summary endpoint serves the field per by_model row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard/operator → proxy HTTP | `hours` query param crosses into the token-usage SQL layer (untrusted, attacker-influenceable) |
| proxy SQL → token-usage.db | the new median query reads the SQLite token-usage telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01-01 | Tampering (SQLi) | new median SELECT in getSummary | mitigate | All values (`since`, model, offset/limit) passed as bound `?` parameters via better-sqlite3 `.all()/.get()`; NO string interpolation of any request-derived value into SQL. Verified by the acceptance grep. |
| T-66-01-02 | Denial of Service | `hours` param → unbounded window read | mitigate | Reuse the existing clamped `since` (lines 774-779) which already bounds the scan to MIN(timestamp); `hours` is parseInt'd in server.mjs and `hours=all` is capped to 10y by existing code. The median query inherits the SAME bounded `since`; no new unbounded scan or `limit` param is introduced. Residual is low (read-only, local-only daemon). |
| T-66-01-03 | Denial of Service | malformed/empty DB throwing into hot path | mitigate | Median query stays inside the best-effort `prepare(...).all(since)` idiom (mirrors logCall try/catch 701-724); empty/young DB yields no row, not a 500 — proven by the empty-DB behavior test. |
| T-66-01-04 | Information Disclosure | per-model latency telemetry exposed via summary | accept | The proxy is a local-only daemon bound to localhost:12435; per-model latency carries no PII/secret (only model name + latency_ms). No new sensitive field added (latency already on /recent today). |
| T-66-01-SC | Tampering | npm install (build deps) | accept | No new package added — `npm run build` uses the proxy repo's existing pinned deps (better-sqlite3 already present). No package-manager install task; legitimacy gate N/A. |

No threat reaches `high`: the only attacker-influenceable input (`hours`) is clamped, and the query is fully parameterized.
</threat_model>

<verification>
- Proxy unit suite green (`npm test`) including the 3 median behavior tests.
- `dist/token-usage.js` rebuilt and carries `p50_latency_ms`.
- Live `GET /api/token-usage/summary?hours=24` on port 12435 returns `by_model[].p50_latency_ms`.
- Median query is parameterized (acceptance grep) and never-throws on empty DB (behavior test).
</verification>

<success_criteria>
- A median (p50) per-model latency over the rolling 24h window is computed in the proxy SQL layer and exposed on `getSummary()`'s `by_model` rows as `p50_latency_ms`, sourced from the same fallback-path token-usage telemetry the pool serves (D-07), over ALL in-window calls including cold spawns (D-06).
- The field is live on the running proxy with no new endpoint or reverse-proxy wiring (piggyback on summary).
</success_criteria>

<output>
Create `.planning/phases/66-dashboard-latency-observability/66-01-SUMMARY.md` when done
</output>
