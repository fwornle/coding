---
phase: 80-experiment-surface-dashboard-skill-packaging
plan: 03
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/comparison-matrix.tsx
  - integrations/system-health-dashboard/src/pages/performance.tsx
  - tests/e2e/performance/comparison-tab.spec.ts
autonomous: false
requirements: [CMP-04]
must_haves:
  truths:
    - "A new 'Comparison' sub-tab in the Performance tab renders the variant matrix (variants as columns, metrics/variance/gate rows), ranked best-first"
    - "The failed / ungated / unscored groups are visibly separated from the ranked group — a variant with no successful runs shows 'no successful runs', never a cheap winner (honesty spine)"
    - "The matrix is fed live by GET /api/experiments/comparison via a fetchComparison Redux thunk keyed by task_hash (no page-local useState, no fetch in the component)"
    - "The Comparison tab renders at localhost:3032 (verified via gsd-browser) and a Playwright E2E asserts the variant columns + the four honesty-spine groupings"
  artifacts:
    - path: "integrations/system-health-dashboard/src/components/performance/comparison-matrix.tsx"
      provides: "the variant-column matrix component (ranked + failed/ungated/unscored sections)"
      min_lines: 60
    - path: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      provides: "fetchComparison thunk + comparisonByTaskHash state + selectComparisonFor + ComparisonReport type"
      contains: "fetchComparison"
    - path: "integrations/system-health-dashboard/src/pages/performance.tsx"
      provides: "Comparison TabsTrigger + TabsContent (5th tab)"
      contains: "comparison"
    - path: "tests/e2e/performance/comparison-tab.spec.ts"
      provides: "Playwright E2E asserting the variant-column matrix + honesty-spine groups render"
  key_links:
    - from: "comparison-matrix.tsx"
      to: "performanceSlice fetchComparison / selectComparisonFor"
      via: "useAppDispatch(fetchComparison) + useAppSelector(selectComparisonFor)"
      pattern: "fetchComparison|selectComparisonFor"
    - from: "performanceSlice fetchComparison"
      to: "/api/experiments/comparison"
      via: "fetch keyed by task_hash"
      pattern: "api/experiments/comparison"
    - from: "performance.tsx TabsContent value=comparison"
      to: "comparison-matrix.tsx"
      via: "component mount"
      pattern: "ComparisonMatrix"
---

<objective>
Add the "Comparison" sub-tab to the Performance dashboard tab (CMP-04 frontend): a `fetchComparison` Redux thunk that reads the Plan-01 live endpoint keyed by `task_hash`, a `comparison-matrix.tsx` component rendering variants-as-columns / metrics-variance-gate-as-rows (ranked best-first, with the failed/ungated/unscored honesty-spine groups visibly separated), and the `TabsTrigger`/`TabsContent` wiring. Then MANDATORY visual verification: gsd-browser render check at localhost:3032 + a structured Playwright E2E under tests/e2e/.

Purpose: Completes CMP-04 (the comparison is viewable as variant columns without re-running) and success-criterion 2/3 (the one-line command's report surfaces in the dashboard). Depends on Plan 01's frozen endpoint schema.
Output: A rendered Comparison tab, live-fed, with a passing Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-experiment-surface-dashboard-skill-packaging/80-CONTEXT.md
@.planning/phases/80-experiment-surface-dashboard-skill-packaging/80-PATTERNS.md
@.planning/phases/80-experiment-surface-dashboard-skill-packaging/80-01-PLAN.md

<interfaces>
<!-- Extracted from the codebase — executor should use these directly. -->

Endpoint (from Plan 01) — GET /api/experiments/comparison?task_hash=X&rank_by= returns the FROZEN Phase 79 doc:
  { task_hash, rank_by, generated_at,
    ranked:   VariantEntry[],   // best-first, each with rank + composite
    failed:   VariantEntry[],   // "no successful runs"
    ungated:  VariantEntry[],   // no test_command
    unscored: VariantEntry[] }  // successful+gated but null/zero rubric
  VariantEntry = { variant, n, gate_outcome:'passed'|'failed'|'ungated'|'unscored',
                   rank?, composite?, reason?,
                   metrics: { <metric>: { mean, stddev, median, min, max, n } } }
  metric keys: totalTokens, wallclock, loop_count, edit_revert_count, redundant_read_count,
    abandoned_tool_count, total_step_count, wallclock_per_step, rubric_score,
    goal_achieved, code_quality, test_coverage, regressions, spec_drift.

Redux thunk analog — fetchReconciliation (performanceSlice.ts:681-702): keyed-by-id fetch, graceful on absence,
  stored into a Record<string,...> map; reducer at 1380-1382 (fulfilled only, no pending/rejected banner).
  fetchReports (815-827) = typed-generic shape; fetchRunNarrative (708-736) = URLSearchParams query build.
  selectSelectedTaskId (performanceSlice.ts:1628) = D-03 task context to key the fetch by.
  PerformanceState interface near 271-274; initial state near 532.

Component analogs — run-compare.tsx (Card + useAppSelector/useAppDispatch + fmtNum:39-42 + Delta:44-60);
  runs-table.tsx (Table/TableBody/TableCell/TableHead/TableHeader/TableRow from @/components/ui/table + Badge + Tooltip).
  D-02 wants variants-as-COLUMNS (TableHead cells) / metrics-as-ROWS (transposed vs runs-table).
  CLI section labels to mirror (experiments-compare.mjs renderTable:262-303): "RANKED …", "FAILED (no successful runs …)", "UNGATED …", "UNSCORED …".

Tabs analog — performance.tsx:198-237 controlled Tabs (Runs/Avenues/Compare/Reports triggers at 201-203),
  activeTab useState('runs') at 135. Add a 5th value "comparison". Do NOT overload "Compare" or "Reports" (D-01 audit trap).
  Component imports block at 19-30.

Dashboard = Redux ONLY (project_dashboard_redux_state): no page-local useState for shared state, no fetch() in the component.
Dashboard bind-mount rebuild: cd integrations/system-health-dashboard && npm run build
  → docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend (VirtioFS caches).
E2E convention: tests/e2e/<area>/<spec>.spec.ts, run via npx playwright test. data-testid on tabs (avenues-tab/compare-tab/reports-tab, performance.tsx:201-203).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fetchComparison thunk + comparisonByTaskHash state + ComparisonReport type + selector</name>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts:162+ (ReconciliationSummary type style), :271-274 (state fields), :532 (initial state), :681-702 (fetchReconciliation analog), :815-827 (fetchReports), :1380-1382 (reconciliation reducer), :1628 (selectSelectedTaskId)
    - .planning/phases/79-comparison-aggregation-report/79-03-SUMMARY.md (the frozen schema field names for the ComparisonReport type)
  </read_first>
  <behavior>
    - fetchComparison({ taskHash, rankBy? }) fetches /api/experiments/comparison?task_hash=<h>[&rank_by=<r>] and stores the report under comparisonByTaskHash[taskHash]
    - a non-ok response resolves to report:null (graceful — no thrown UI crash), mirroring fetchReconciliation
    - selectComparisonFor(state, taskHash) returns the stored report or null
    - the ComparisonReport TS type carries { task_hash, rank_by, generated_at, ranked, failed, ungated, unscored } with VariantEntry.gate_outcome + metrics block
  </behavior>
  <action>
    In `performanceSlice.ts`: declare a `ComparisonReport` type (and `VariantEntry`) modelling the frozen Phase 79 schema (mirror the `ReconciliationSummary` declaration style near 162). Add `fetchComparison` as a `createAsyncThunk<{ taskHash: string; report: ComparisonReport | null }, { taskHash: string; rankBy?: string }, { rejectValue: string }>` — mirror `fetchReconciliation` (681-702): build the query with `URLSearchParams` (`task_hash`, optional `rank_by`), `fetch('/api/experiments/comparison?...')`, on non-ok return `{ taskHash, report: null }` (graceful, not a throw), else parse and return `{ taskHash, report }`. Add `comparisonByTaskHash: Record<string, ComparisonReport | null>` to `PerformanceState` (near 271-274) + `comparisonByTaskHash: {}` to initial state (near 532). Add the `.addCase(fetchComparison.fulfilled, ...)` reducer mirroring reconciliation (1380-1382) — fulfilled only, no pending/rejected banner. Add `export const selectComparisonFor = (state, taskHash) => taskHash ? (state.performance.comparisonByTaskHash[taskHash] ?? null) : null`.
  </action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npx tsc --noEmit 2>&1 | grep -E "performanceSlice" || echo "no slice type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchComparison" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` >= 3 (thunk def + reducer case + export)
    - `grep -c "comparisonByTaskHash" performanceSlice.ts` >= 3 (interface + initial + reducer)
    - `grep -c "selectComparisonFor" performanceSlice.ts` >= 1 AND `grep -c "api/experiments/comparison" performanceSlice.ts` == 1
    - `grep -c "ComparisonReport" performanceSlice.ts` >= 2 (type decl + usages)
    - `npx tsc --noEmit` reports no NEW errors originating in performanceSlice.ts (pre-existing out-of-scope diagnostics in node-details-sidebar.tsx/token-usage.tsx are allowed per 86-02 note)
  </acceptance_criteria>
  <done>The slice exposes a graceful task_hash-keyed fetchComparison thunk, its state map, selector, and the ComparisonReport type — typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build comparison-matrix.tsx + wire the Comparison TabsTrigger/TabsContent + build the bundle</name>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/run-compare.tsx:1-18,39-60 (Card + hooks + fmtNum + Delta helpers)
    - integrations/system-health-dashboard/src/components/performance/runs-table.tsx:1-40 (Table/Badge/Tooltip primitives)
    - integrations/system-health-dashboard/src/pages/performance.tsx:19-30 (imports), :135 (activeTab), :198-237 (Tabs block)
    - scripts/experiments-compare.mjs:262-303 (renderTable section labels to mirror)
    - performanceSlice.ts (fetchComparison/selectComparisonFor/selectSelectedTaskId from Task 1)
  </read_first>
  <behavior>
    - the component fetches on mount via dispatch(fetchComparison({ taskHash })) keyed by selectSelectedTaskId; holds NO local fetch and NO shared useState
    - variants render as columns (TableHead), metrics/variance/gate_outcome as rows (TableCell); the ranked group is sorted best-first with rank + composite
    - the failed / ungated / unscored groups render in a visually distinct "not ranked" region carrying each entry's .reason; a variant with no successful runs shows "no successful runs", never a cheap winner
    - each metric cell shows mean ± stddev with median/range available (e.g. Tooltip)
  </behavior>
  <action>
    Create `integrations/system-health-dashboard/src/components/performance/comparison-matrix.tsx` exporting `ComparisonMatrix`. Mirror run-compare.tsx imports (Card, useAppSelector/useAppDispatch). Read `const taskHash = useAppSelector(selectSelectedTaskId)` (D-03), `const report = useAppSelector(s => selectComparisonFor(s, taskHash))`, and `useEffect(() => { if (taskHash) dispatch(fetchComparison({ taskHash })) }, [dispatch, taskHash])`. Render with the `@/components/ui/table` primitives TRANSPOSED: variants across `TableHead` columns, one row per surfaced metric (totalTokens, wallclock, wallclock_per_step, rubric_score + the loop/edit-revert/redundant-read/abandoned-tool/total-step + 5 rubric dims), plus a gate_outcome row and an `n` row. Each metric cell = `mean ± stddev` (reuse fmtNum from run-compare.tsx) with median/min/max on a Tooltip (runs-table.tsx Tooltip). Render the honesty spine (D-02): a "RANKED" section first (rank + composite, best-first), then DISTINCT "FAILED (no successful runs)", "UNGATED", "UNSCORED" sections each showing the variant, n, gate_outcome badge, and `.reason` — mirror the CLI renderTable labels. Add `data-testid` hooks: `comparison-matrix`, `comparison-variant-col` (per variant column header), `comparison-group-ranked`/`-failed`/`-ungated`/`-unscored`. Empty/absent report → a graceful "no comparison for this task_hash" placeholder, never a crash.
    In `performance.tsx`: import `{ ComparisonMatrix }` in the imports block (19-30); add `<TabsTrigger value="comparison" data-testid="comparison-tab">Comparison</TabsTrigger>` after the reports trigger (~203); add `<TabsContent value="comparison" className="mt-4"><ComparisonMatrix /></TabsContent>` after the reports content (~236). Do NOT touch the "Compare" or "Reports" tabs (D-01).
    Then rebuild the bind-mounted bundle and restart the frontend: `cd integrations/system-health-dashboard && npm run build` then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`.
  </action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `integrations/system-health-dashboard/src/components/performance/comparison-matrix.tsx` exists, exports `ComparisonMatrix`, and is >= 60 lines
    - `grep -c "useState" comparison-matrix.tsx` == 0 for SHARED state (only trivial local UI state like a hover tooltip is acceptable; no fetch state) AND `grep -c "fetch(" comparison-matrix.tsx` == 0 (Redux-only, project rule)
    - `grep -c 'data-testid="comparison-group-ranked"' comparison-matrix.tsx` == 1 AND the failed/ungated/unscored testids are each present == 1
    - `grep -c 'value="comparison"' integrations/system-health-dashboard/src/pages/performance.tsx` == 2 (trigger + content) AND `grep -c "ComparisonMatrix" performance.tsx` >= 2 (import + mount)
    - `npm run build` exits 0 (vite bundle builds clean)
  </acceptance_criteria>
  <done>The Comparison tab is wired, the matrix renders variants-as-columns with the four honesty-spine groups, the bundle builds, and the frontend is restarted.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E asserting the variant-column matrix + honesty-spine groups</name>
  <read_first>
    - tests/e2e/performance/ (existing Playwright specs — reuse the harness + data-testid convention)
    - integrations/system-health-dashboard/src/pages/performance.tsx (the comparison-tab / comparison-matrix testids from Task 2)
    - .planning/phases/80-experiment-surface-dashboard-skill-packaging/80-PATTERNS.md § 7 (E2E mandate)
  </read_first>
  <action>
    Create `tests/e2e/performance/comparison-tab.spec.ts` (structured Playwright under tests/e2e/<area>/, run via `npx playwright test`). Navigate to the Performance page on localhost:3032, click the `comparison-tab` trigger, and assert: the `comparison-matrix` is visible; variant columns render (>=1 `comparison-variant-col` when the fixture task_hash has runs); and the four honesty-spine group regions (`comparison-group-ranked`/`-failed`/`-ungated`/`-unscored`) are present in the DOM (visible or explicitly empty-state, never a cheap winner collapsing failed into ranked). Use a task_hash that exists in the store (or seed/select one) so the matrix has data; if no scored runs exist, assert the group scaffolding + placeholder rather than fabricating a winner. Follow the memory rule (feedback_green_nodes): the spec asserts the grouped-UI structure, not just tab presence.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/performance/comparison-tab.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/performance/comparison-tab.spec.ts` exists and references the `comparison-tab` + `comparison-matrix` + the four `comparison-group-*` testids
    - `npx playwright test tests/e2e/performance/comparison-tab.spec.ts` exits 0
    - the spec asserts >= 1 variant column renders for a task_hash with runs AND asserts the failed/ungated/unscored groups are separately present (not merged into ranked)
  </acceptance_criteria>
  <done>A structured Playwright spec exits 0, asserting the variant-column matrix and the four honesty-spine groupings render.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification of the Comparison tab (gsd-browser)</name>
  <action>Operator runs the gsd-browser render check in <how-to-verify> below (navigate → click the Comparison tab → screenshot at localhost:3032). The plan does NOT proceed until the operator types "approved" or describes what looks wrong. Intentionally human-gated per autonomous: false — visual verification is a mandatory standing preference (never claim "works" from endpoint/DB queries alone).</action>
  <what-built>The Comparison sub-tab in the Performance dashboard tab: variants-as-columns matrix fed live by GET /api/experiments/comparison (Plan 01), with ranked + failed/ungated/unscored honesty-spine groups. Automated: the frontend bundle was rebuilt + the frontend service restarted, and a Playwright E2E (tests/e2e/performance/comparison-tab.spec.ts) asserts the matrix + groupings.</what-built>
  <how-to-verify>
    1. Run the gsd-browser visual check (NOT a hand-rolled Playwright node script — CLAUDE.md prefer-gsd-browser):
       `gsd-browser navigate "http://localhost:3032/performance"` (or the route that hosts the Performance tab)
       `gsd-browser click` the "Comparison" tab (data-testid="comparison-tab")
       `gsd-browser screenshot --output /tmp/claude-502/comparison-tab.png`
    2. Confirm in the screenshot: (a) the Comparison tab is a DISTINCT 5th tab (not the "Compare" or "Reports" tab); (b) variants render as COLUMNS with metric/variance/gate rows; (c) the ranked group is best-first; (d) failed/ungated/unscored variants are VISIBLY separated — a variant with no successful runs shows "no successful runs", not a top-ranked winner.
    3. Optionally end-to-end: run the `experiment` skill (Plan 02) for a small matrix, then confirm the same task_hash's report appears live in this tab (success-criterion 3).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong in the screenshot.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (dashboard) → GET /api/experiments/comparison | the task_hash the tab fetches with is server-validated at the endpoint (Plan 01); the frontend only renders the returned report |
| endpoint report → DOM render | untrusted-shaped JSON (variant names, reasons) rendered into React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03-01 | Tampering | task_hash → endpoint | mitigate | Validation is enforced server-side in Plan 01 (sanitizeTaskHash + 400); the frontend passes the selected task_hash via URLSearchParams (no string concatenation into a path) |
| T-80-03-02 | Information Disclosure | comparison JSON in DOM | accept | Aggregate metrics for the operator's own experiments; no PII |
| T-80-03-03 | Tampering (XSS) | variant / reason strings rendered | mitigate | Render report strings as React text children only — NO dangerouslySetInnerHTML (mirror the 86-03 ZERO-dangerouslySetInnerHTML discipline) |
| T-80-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — Radix Tabs / table primitives / Playwright already present; if any install is proposed, halt for a legitimacy checkpoint |
</threat_model>

<verification>
- `npm run build` (dashboard) exits 0; the frontend service was restarted (bind-mount VirtioFS refresh).
- `npx playwright test tests/e2e/performance/comparison-tab.spec.ts` exits 0.
- gsd-browser screenshot of the Comparison tab at localhost:3032 shows N variant columns + the four honesty-spine groups (human-verify checkpoint APPROVED).
- No dangerouslySetInnerHTML; component holds no fetch/shared useState.
</verification>

<success_criteria>
- CMP-04 frontend complete: the comparison is viewable as variant columns in the Performance tab, live-fed, without re-running the experiment.
- Honesty spine carried into the UI: failed/ungated/unscored visibly separated.
- Visual (gsd-browser) + structured Playwright E2E both confirm the render — never claimed from DB/endpoint alone.
</success_criteria>

<output>
Create `.planning/phases/80-experiment-surface-dashboard-skill-packaging/80-03-SUMMARY.md` when done.
</output>
