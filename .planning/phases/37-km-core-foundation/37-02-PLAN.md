---
phase: 37-km-core-foundation
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - ~/Agentic/km-core/src/types/entity.ts
  - ~/Agentic/km-core/src/types/index.ts
  - ~/Agentic/km-core/src/store/types.ts
  - ~/Agentic/km-core/src/events/types.ts
  - ~/Agentic/km-core/src/ids/branded.ts
  - ~/Agentic/km-core/src/ids/mint.ts
  - ~/Agentic/km-core/src/ids/parse.ts
  - ~/Agentic/km-core/src/validation/ontology.ts
autonomous: true
requirements: [CORE-01, CORE-03]
requirements_addressed: [CORE-01, CORE-03]
must_haves:
  truths:
    - "Consumers can import `Entity`, `Relation`, `Layer`, `EntityId`, `SerializedGraph` types from `@fwornle/km-core` and they compile under `strict: true`."
    - "`mintEntityId()` returns a fresh UUIDv7 branded as `EntityId`. `parseEntityId(s)` validates format and throws on invalid input (locks decisions: D-08 UUIDv7 variant, D-09 `uuidv7` npm package, D-11 branded `EntityId` type, D-13 structured `legacyId` field on Entity for Phase 39 backfill bridge — established for Phase 39 backfill per D-12)."
    - "`EntityId` is a branded string - raw `string` values do not satisfy `EntityId` parameters at compile time."
    - "Plan 01's `tests/unit/entity.test.ts` and `tests/unit/ids.test.ts` are GREEN."
  artifacts:
    - path: "~/Agentic/km-core/src/types/entity.ts"
      provides: "Canonical Entity, Relation, Layer, SerializedGraph, provenance subtypes"
      contains: "export interface Entity"
    - path: "~/Agentic/km-core/src/ids/branded.ts"
      provides: "Branded EntityId type definition"
      contains: "__brand"
    - path: "~/Agentic/km-core/src/ids/mint.ts"
      provides: "mintEntityId factory wrapping uuidv7"
      exports: ["mintEntityId"]
    - path: "~/Agentic/km-core/src/ids/parse.ts"
      provides: "parseEntityId validator using UUID.parse plus v7 charAt(14) check"
      exports: ["parseEntityId"]
    - path: "~/Agentic/km-core/src/store/types.ts"
      provides: "BatchOp discriminated union, FilterObject"
    - path: "~/Agentic/km-core/src/events/types.ts"
      provides: "EntityPutEvent, EntityDeleteEvent, RelationAddedEvent, RelationRemovedEvent payload types"
    - path: "~/Agentic/km-core/src/validation/ontology.ts"
      provides: "OntologyValidator interface and a no-op default implementation"
  key_links:
    - from: "~/Agentic/km-core/src/types/entity.ts"
      to: "~/Agentic/km-core/src/ids/branded.ts"
      via: "import type { EntityId } from '../ids/branded.js'"
      pattern: "import.*EntityId.*ids/branded"
---

<objective>
Land the canonical type surface and UUIDv7 ID layer for `@fwornle/km-core` per D-08 through D-13 and D-14 type shapes. Adopt OKM's `Entity` shape verbatim (it is the strict superset per RESEARCH section "Pattern 1") with three deltas: brand `id` as `EntityId`, rename `Edge` to `Relation` with explicit `from`/`to` fields, add `legacyId` for the Phase 39 backfill bridge. Implement `mintEntityId` and `parseEntityId` per RESEARCH section "Pattern 4" plus the `s.charAt(14) === '7'` defensive check per 37-PATTERNS.md section "src/ids/parse.ts" DELTAS.

Purpose: Address CORE-01 (canonical types exported) and CORE-03 (UUIDv7-stamped branded `EntityId`). After this plan, `tests/unit/entity.test.ts` and `tests/unit/ids.test.ts` transition from RED to GREEN.

Output: 8 source files + barrel re-exports. No store / persistence / exporter code in this plan - those are Plans 03 and 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-km-core-foundation/37-CONTEXT.md
@.planning/phases/37-km-core-foundation/37-RESEARCH.md
@.planning/phases/37-km-core-foundation/37-PATTERNS.md
@.planning/phases/37-km-core-foundation/37-VALIDATION.md

<interfaces>
<!-- OKM's canonical Entity type - this plan's primary source. Read BEFORE writing entity.ts. -->

OKM Entity types (full source):
  Path: /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/types/entity.ts
  Action: adopt VERBATIM (lines 1-108) then apply 4 deltas (37-PATTERNS.md section "src/types/entity.ts" DELTAS).

uuidv7 package (Context7 ID: /liosk/uuidv7):
  - `uuidv7(): string` returns a v7 UUID in standard hyphenated form
  - `UUID.parse(s: string): UUID` throws `SyntaxError` on invalid format
  - Version v7 detection: position 14 (0-indexed) of the 36-char string equals '7'

Test files that go GREEN after this plan (created in Plan 01):
  /Users/Q284340/Agentic/km-core/tests/unit/entity.test.ts
  /Users/Q284340/Agentic/km-core/tests/unit/ids.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land ID layer (branded type + mint + parse)</name>
  <files>
    ~/Agentic/km-core/src/ids/branded.ts,
    ~/Agentic/km-core/src/ids/mint.ts,
    ~/Agentic/km-core/src/ids/parse.ts
  </files>
  <read_first>
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-PATTERNS.md (sections "src/ids/branded.ts", "src/ids/mint.ts", "src/ids/parse.ts"),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md section "Pattern 4" (lines 407-451) plus section "Pattern 5" (lines 453-478),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md section "Pitfall 2" (caller-supplied id collision),
    ~/Agentic/km-core/tests/unit/ids.test.ts (created in Plan 01 - these tests will go GREEN once this task completes)
  </read_first>
  <action>
    `src/ids/branded.ts` (5 lines): exports `type EntityId = string & { readonly __brand: 'EntityId' }`. Add a top-of-file JSDoc comment citing D-11 and explaining the zero-runtime-cost compile-time tag.

    `src/ids/mint.ts`: per 37-PATTERNS.md section "src/ids/mint.ts" - import `{ uuidv7 } from 'uuidv7'`, import `type { EntityId } from './branded.js'`, export `function mintEntityId(): EntityId { return uuidv7() as EntityId; }`. JSDoc cites D-08 (UUIDv7), D-09 (uuidv7 package), D-11 (branded type).

    `src/ids/parse.ts`: per 37-PATTERNS.md section "src/ids/parse.ts" plus DELTAS:
      - import `{ UUID } from 'uuidv7'`, import `type { EntityId } from './branded.js'`.
      - export `function parseEntityId(s: string): EntityId`:
          1. Call `UUID.parse(s)` which throws `SyntaxError` on invalid format.
          2. **DEFENSIVE CHECK** (37-PATTERNS.md DELTAS recommendation): `if (s.charAt(14) !== '7') throw new SyntaxError("Not a UUIDv7: variant byte mismatch at position 14");` - this rejects v4 UUIDs, which would otherwise pass `UUID.parse` since the package accepts any RFC 9562 UUID per RESEARCH section "src/ids/parse.ts" comment. This is the CORE-03 acceptance criterion "putEntity with caller-supplied invalid id throws" (RESEARCH section "Validation Architecture" row 3 of CORE-03).
          3. Return `s as EntityId`.
      - JSDoc cites: D-10 (caller-supplied IDs validated), Pitfall 2 (id collision via wide-string).

    Use ESM `.js` extensions in imports (matches `module: NodeNext` in tsconfig).

    NO `console.*` calls (CLAUDE.md `no-console-log`). Errors are thrown, not logged.

    Commit: `feat(37-02): land branded EntityId plus mintEntityId plus parseEntityId (CORE-03)`.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && npx tsc --noEmit && npm test -- tests/unit/ids.test.ts</automated>
    Confirm grep gates: `grep -q "export type EntityId = string &" ~/Agentic/km-core/src/ids/branded.ts` exits 0; `grep -q "export function mintEntityId" ~/Agentic/km-core/src/ids/mint.ts` exits 0; `grep -q "export function parseEntityId" ~/Agentic/km-core/src/ids/parse.ts` exits 0; `grep -q "s.charAt(14)" ~/Agentic/km-core/src/ids/parse.ts` exits 0; `grep -rE "console\.(log|info|warn|error)" ~/Agentic/km-core/src/ids/` returns zero lines.
  </verify>
  <acceptance_criteria>
    - All 3 files exist; `npx tsc --noEmit` exits 0 (whole package compiles cleanly under strict mode).
    - `npm test -- tests/unit/ids.test.ts` reports ALL `ids.test.ts` tests PASSING (UUIDv7 stamp, UUIDv7 regex match, valid v7 round-trip, invalid id throws, v4 UUID rejected via position-14 check, 100-id k-sortability check passes).
    - `mintEntityId()` returns strings matching the standard hyphenated 36-char UUID format with `7` at position 14.
    - `parseEntityId('00000000-0000-4000-8000-000000000000')` (v4 UUID) throws `SyntaxError` (the defensive `charAt(14)` check fires).
    - No `console.*` references in any of the 3 files.
    - Commit `feat(37-02): land branded EntityId plus mintEntityId plus parseEntityId (CORE-03)` exists.
  </acceptance_criteria>
  <done>ID layer green; `tests/unit/ids.test.ts` all passing.</done>
</task>

<task type="auto">
  <name>Task 2: Land canonical Entity / Relation types plus supporting type modules</name>
  <files>
    ~/Agentic/km-core/src/types/entity.ts,
    ~/Agentic/km-core/src/types/index.ts,
    ~/Agentic/km-core/src/store/types.ts,
    ~/Agentic/km-core/src/events/types.ts,
    ~/Agentic/km-core/src/validation/ontology.ts
  </files>
  <read_first>
    /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/types/entity.ts (full 108-line file - adopt VERBATIM minus deltas),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-PATTERNS.md (sections "src/types/entity.ts", "src/store/types.ts", "src/events/types.ts"),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-CONTEXT.md decisions D-13 (legacyId shape), D-14 (repository API surface), D-16 (event names), D-17 (BatchOp), D-18 (FilterObject),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md section "Pattern 1: Canonical Entity Type" (lines 246-287),
    ~/Agentic/km-core/src/ids/branded.ts (created in Task 1 - the EntityId import target),
    ~/Agentic/km-core/tests/unit/entity.test.ts (created in Plan 01 - tests go GREEN after this task)
  </read_first>
  <action>
    `src/types/entity.ts`: adopt OKM's file VERBATIM (read_first path) then apply the 4 deltas from 37-PATTERNS.md section "src/types/entity.ts" DELTAS:
      1. `import type { EntityId } from '../ids/branded.js';` at the top; change `id: string` to `id: EntityId` in `Entity`; change `supersedes?: EntityId` (was either missing or `string` in OKM).
      2. Rename `Edge` to `Relation` and add `from: EntityId; to: EntityId;` fields (OKM's `addEdge(source, target, edge)` keeps source/target outside the type, but per D-14's `addRelation(r: Relation)` signature KM-Core moves them inside).
      3. Add `legacyId?: { system: 'A' | 'B' | 'C'; id: string };` per D-13 (explicit origin for Phase 39 backfill).
      4. Drop any `bin`-related types (KM-Core is library-only, no CLI).

      Preserve VERBATIM the OKM provenance subtypes (`ProvenanceStamp`, `EntityProvenance`, `SegmentConfirmation`, `DescriptionSegment`, `ResolutionRecord`) and `SerializedGraph` per 37-PATTERNS.md - Phase 39 will populate these but Phase 37 must declare them.

    `src/types/index.ts`: barrel that re-exports `Entity, Relation, Layer, EntityId, SerializedGraph` from `'./entity.js'` and the provenance subtypes.

    `src/store/types.ts`: net-new, per 37-PATTERNS.md section "src/store/types.ts":
      ```
      import type { Entity, Relation, EntityId } from '../types/entity.js';

      export type BatchOp =
        | { type: 'putEntity'; entity: Partial<Entity> & { name: string; entityType: string } }
        | { type: 'deleteEntity'; id: EntityId }
        | { type: 'addRelation'; relation: Relation }
        | { type: 'removeRelation'; relation: Relation };

      export interface FilterObject {
        entityType?: string;
        ontologyClass?: string;
        layer?: 'evidence' | 'pattern';
      }
      ```

    `src/events/types.ts`: net-new, per 37-PATTERNS.md section "src/events/types.ts" - event payload types for D-16 events (`entity:put`, `entity:delete`, `relation:added`, `relation:removed`):
      ```
      import type { Entity, Relation } from '../types/entity.js';

      export interface EntityPutEvent     { entity: Entity }
      export interface EntityDeleteEvent  { id: string }
      export interface RelationAddedEvent  { relation: Relation }
      export interface RelationRemovedEvent { relation: Relation }
      ```

    `src/validation/ontology.ts`: net-new, ships a no-op default per 37-PATTERNS.md "v0.1 ships a no-op" - Phase 38 wires the real registry:
      ```
      /**
       * OntologyValidator: pluggable interface for entityType validation per D-19.
       * v0.1 ships a no-op default; Phase 38 implements OntologyRegistry which conforms.
       */
      export interface OntologyValidator {
        /** Throws an Error if entityType is not a registered class. */
        validate(entityType: string): void;
      }

      /**
       * Default no-op validator. Accepts any entityType. Plan 04 uses this when
       * GraphKMStoreOptions.ontologyValidator is unset.
       */
      export const noopOntologyValidator: OntologyValidator = {
        validate(_entityType: string): void { /* no-op */ }
      };
      ```

    NO `console.*` anywhere. Use ESM `.js` import extensions throughout.

    Commit: `feat(37-02): land canonical Entity/Relation types plus store/events/validation type modules (CORE-01)`.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && npx tsc --noEmit && npm test -- tests/unit/entity.test.ts</automated>
    Confirm grep gates: `grep -q "export interface Entity" ~/Agentic/km-core/src/types/entity.ts` exits 0; `grep -q "id: EntityId" ~/Agentic/km-core/src/types/entity.ts` exits 0; `grep -q "export interface Relation" ~/Agentic/km-core/src/types/entity.ts` exits 0; `grep -q "from: EntityId" ~/Agentic/km-core/src/types/entity.ts` exits 0; `grep -q "to: EntityId" ~/Agentic/km-core/src/types/entity.ts` exits 0; `grep -q "legacyId" ~/Agentic/km-core/src/types/entity.ts` exits 0; `grep -q "export type BatchOp" ~/Agentic/km-core/src/store/types.ts` exits 0; `grep -q "export interface OntologyValidator" ~/Agentic/km-core/src/validation/ontology.ts` exits 0.
  </verify>
  <acceptance_criteria>
    - All 5 files exist; `npx tsc --noEmit` exits 0.
    - `npm test -- tests/unit/entity.test.ts` reports ALL `entity.test.ts` tests PASSING (type imports resolve, `EntityId` brand rejects raw `string`, Entity shape has the mandatory + optional fields, Relation has `from`/`to` as `EntityId`).
    - `src/types/entity.ts` contains exactly one `export interface Entity` declaration and exactly one `export interface Relation` declaration.
    - `src/types/entity.ts` retains OKM's provenance subtypes (`ProvenanceStamp`, `EntityProvenance`, `SegmentConfirmation`, `DescriptionSegment`, `ResolutionRecord`) - verified by `grep -c "export interface Provenance\|export interface Segment\|export interface Description\|export interface Resolution" ~/Agentic/km-core/src/types/entity.ts` returns greater than or equal to 4.
    - `src/store/types.ts` exports both `BatchOp` and `FilterObject`.
    - `src/events/types.ts` exports all 4 event payload types named in D-16.
    - `src/validation/ontology.ts` exports `OntologyValidator` interface and `noopOntologyValidator` const.
    - No `console.*` references in any file.
    - Commit recorded.
  </acceptance_criteria>
  <done>Canonical types green; `tests/unit/entity.test.ts` all passing; tsc clean across whole package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller of `parseEntityId` to `parseEntityId` | The caller supplies a wide `string`; the function MUST validate format AND v7 variant before returning a branded `EntityId`. |
| TypeScript module boundary (cross-package import) | Consumers will import `Entity`, `EntityId`, etc.; type-level brand enforcement is a compile-time gate only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Tampering | `parseEntityId(s)` accepting caller-supplied wide string | mitigate | `UUID.parse(s)` throws `SyntaxError` on invalid format; the additional `s.charAt(14) !== '7'` check rejects v4 UUIDs (which would otherwise pass `UUID.parse` since the uuidv7 package accepts any RFC 9562 UUID per RESEARCH section "src/ids/parse.ts" comment). This is CORE-03's "caller-supplied invalid id throws" acceptance criterion AND Pitfall 2's defense (id collisions via wide strings). |
| T-37-02-02 | Spoofing | Caller-supplied `id` in `putEntity` shortcircuits writer-side stamp | mitigate | D-10 explicitly allows caller-supplied IDs for idempotency; Plan 04 will route caller IDs through `parseEntityId` before storage. This plan does NOT implement `putEntity`; the surface-area mitigation is in `parseEntityId`. |
| T-37-02-03 | Information Disclosure | Entity types exposing `legacyId` payload | accept | `legacyId.system` (`'A' | 'B' | 'C'`) is non-sensitive; `legacyId.id` is a system-internal identifier. Out-of-band: Phase 39 backfill (which populates this field) will run scoped to dev workstations only. ASVS L1 sufficient. |
| T-37-02-04 | Tampering | `validation/ontology.ts` no-op default | accept | v0.1 ships no-op per D-19 + 37-PATTERNS.md "Phase 38 wires the real registry". A no-op validator means `entityType: 'Bogus'` is accepted in v0.1 unless the consumer passes their own validator. Disposition: ACCEPT for v0.1; Phase 38 closes by wiring `OntologyRegistry`. Documented in noopOntologyValidator JSDoc. |
</threat_model>

<verification>
After both tasks:
- `cd ~/Agentic/km-core && npx tsc --noEmit` exits 0.
- `npm test -- tests/unit/entity.test.ts tests/unit/ids.test.ts` reports ALL tests in those two files PASSING.
- The other 3 unit test files plus integration test files remain RED (Plans 03/04/05 land them).
- `git log --oneline | head -3` shows two new commits from this plan plus Plan 01's commits.
- `grep -rE "console\.(log|info|warn|error)" ~/Agentic/km-core/src/` returns zero lines.
</verification>

<success_criteria>
- CORE-01: `Entity`, `Relation`, `Layer`, `EntityId`, `SerializedGraph` types exported from `@fwornle/km-core`.
- CORE-03 (first half): `mintEntityId()` returns branded UUIDv7; `parseEntityId(s)` validates format + v7 variant.
- 2 of 5 unit test files transition RED to GREEN: `entity.test.ts`, `ids.test.ts`.
- The full package still compiles under `strict: true` and `module: NodeNext`.
- No `console.*` violations.
</success_criteria>

<output>
Create `.planning/phases/37-km-core-foundation/37-02-SUMMARY.md` when done. Record:
- Number of unit tests now green (expect: `entity.test.ts` + `ids.test.ts` fully green; others still red).
- Confirm OKM's provenance subtypes preserved verbatim (CORE-01 superset commitment for Phase 39).
- Note any deviations from 37-PATTERNS.md deltas (none expected).
</output>
