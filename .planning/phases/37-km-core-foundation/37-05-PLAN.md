---
phase: 37-km-core-foundation
plan: 05
type: execute
wave: 5
depends_on: [37-04]
files_modified:
  - /Users/Q284340/Agentic/coding/.gitmodules
  - /Users/Q284340/Agentic/coding/lib/km-core
  - /Users/Q284340/Agentic/coding/docker/Dockerfile.coding-services
  - /Users/Q284340/Agentic/coding/docker/docker-compose.yml
  - /Users/Q284340/Agentic/coding/scripts/migrate-exports-to-symlinks.mjs
  - /Users/Q284340/Agentic/coding/.data/exports/coding.json
  - /Users/Q284340/Agentic/coding/.data/knowledge-export/coding.json
autonomous: false
requirements: [CORE-01, CORE-02, CORE-03]
requirements_addressed: [CORE-01, CORE-02, CORE-03]
user_setup:
  - service: github
    why: "Push ~/Agentic/km-core/ to github.com/fwornle/km-core so the SSH submodule URL git@github.com:fwornle/km-core.git resolves. RESEARCH section Pattern 6 Option B."
    dashboard_config:
      - task: "Push ~/Agentic/km-core/ to git@github.com:fwornle/km-core.git (main branch). The empty repo was created in Plan 01's user_setup; this is the actual push."
        location: "Terminal at ~/Agentic/km-core/: git remote add origin git@github.com:fwornle/km-core.git && git push -u origin main"
must_haves:
  truths:
    - "`coding/lib/km-core/` is a registered git submodule pointing at git@github.com:fwornle/km-core.git, mounted on disk, on the same SHA as ~/Agentic/km-core/ (locks decision: D-04 submodule mount at `coding/lib/km-core/`)."
    - "`coding/docker/Dockerfile.coding-services` builds km-core via the same pattern as lib/llm (install --ignore-scripts plus npm run build) and rebuilds classic-level native binding for the container's arch."
    - "`coding/docker/docker-compose.yml` bind-mounts `lib/km-core/dist` read-only into the container at `/coding/lib/km-core/dist`."
    - "`coding/.data/knowledge-export/coding.json` is a symlink to `../exports/coding.json` (relative). The canonical file at `coding/.data/exports/coding.json` has the migrated content."
    - "A scratch TS file in `coding/` importing `type { Entity } from '@fwornle/km-core'` compiles via `npx tsc --noEmit` (cross-repo CORE-01 manual verification)."
    - "The OKB-baseline-guard hook fires correctly on the new layout: knowledge-export AND observation-export staged together still triggers; knowledge-export alone (or exports alone) passes (locks decision: D-23 OKB-baseline guard pre-commit hook stays source-of-truth — regex still matches `.data/exports/*` and the two-commit pattern still holds)."
    - "CORE-02 parity gate green at the km-core fixture layer; consumer-side close (B's GraphDatabaseAdapter replacement, C's persistence.ts replacement) deferred to Phase 42 and Phase 43 respectively."
    - "SC#1 (developer can import in both coding/ and rapid-automations/) is partially closed in Phase 37 — only the coding/ half is verified by Task 4. The rapid-automations/ half ships in Phase 43 per D-03 and REQUIREMENTS Out-of-Scope."
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/.gitmodules"
      provides: "Submodule registration for lib/km-core"
      contains: "lib/km-core"
    - path: "/Users/Q284340/Agentic/coding/lib/km-core"
      provides: "Submodule mount point (directory, not file)"
    - path: "/Users/Q284340/Agentic/coding/docker/Dockerfile.coding-services"
      provides: "Container build steps for km-core install + native rebuild + npm run build"
      contains: "lib/km-core"
    - path: "/Users/Q284340/Agentic/coding/docker/docker-compose.yml"
      provides: "Bind-mount of lib/km-core/dist into container"
      contains: "lib/km-core/dist"
    - path: "/Users/Q284340/Agentic/coding/scripts/migrate-exports-to-symlinks.mjs"
      provides: "Idempotent one-shot migration that turns .data/knowledge-export/coding.json into a symlink"
    - path: "/Users/Q284340/Agentic/coding/.data/exports/coding.json"
      provides: "Canonical export file (the migration target)"
    - path: "/Users/Q284340/Agentic/coding/.data/knowledge-export/coding.json"
      provides: "Symlink to ../exports/coding.json (legacy-path BC per D-21)"
  key_links:
    - from: "/Users/Q284340/Agentic/coding/lib/km-core"
      to: "/Users/Q284340/Agentic/coding/.gitmodules"
      via: "submodule registration"
      pattern: "lib/km-core"
    - from: "/Users/Q284340/Agentic/coding/docker/docker-compose.yml"
      to: "/Users/Q284340/Agentic/coding/lib/km-core/dist"
      via: "bind-mount declaration"
      pattern: "lib/km-core/dist"
    - from: "/Users/Q284340/Agentic/coding/.data/knowledge-export/coding.json"
      to: "/Users/Q284340/Agentic/coding/.data/exports/coding.json"
      via: "POSIX symlink (readlink resolves to '../exports/coding.json')"
      pattern: "exports/coding.json"
---

<objective>
Wire the now-shipped `@fwornle/km-core` v0.1 into `coding/` as a git submodule at `coding/lib/km-core/`, update `Dockerfile.coding-services` + `docker-compose.yml` to install / rebuild / build / bind-mount it like `lib/llm`, and run the BC migration that converts `coding/.data/knowledge-export/coding.json` into a symlink to the canonical `coding/.data/exports/coding.json` (D-21, Pattern 7). Then human-verify (a) the cross-repo `tsc --noEmit` smoke from a scratch file in `coding/` (proves CORE-01 success criterion 1: "developer can import Entity and Relation types in coding/ and rapid-automations/") and (b) the OKB-baseline-guard hook still fires correctly on representative commit shapes (proves SC#4: "knowledge-export and exports paths still load via KM-Core without breaking the two-commit / OKB-baseline guard hygiene").

Purpose: Close the consumer-side half of CORE-01 + CORE-02 + SC#4. After this plan, Phase 37 is feature-complete and `/gsd:verify-work 37` can run end-to-end.

Output: 5 modified coding-side files (`.gitmodules`, `Dockerfile.coding-services`, `docker-compose.yml`, migration script, plus the symlink/canonical file pair) + 1 new submodule mount point + 1 human-verify checkpoint that the OKB-baseline-guard hygiene still works.

**TWO-COMMIT REQUIREMENT (OKB BASELINE GUARD):** The pre-commit hook at `coding/scripts/hooks/pre-commit-okb-guard.sh` regex matches BOTH `.data/knowledge-export/*.json` AND `.data/exports/*.json` (verified by Plan 01 Task 3's `symlink-bc.sh`). The migration step in Task 3 produces TWO sets of changes that the hook will see as "knowledge-export changes" - **the actual file deletion of `.data/knowledge-export/coding.json` (replaced by symlink)** AND **the canonical write to `.data/exports/coding.json`**. These MUST be split into separate commits OR landed in the same commit as `.data/observation-export/*.json` is NOT touched (Phase 41 owns that). Plan 03 of this migration handles this by writing one commit that has ONLY the `.data/exports/` + symlink changes - that commit is the equivalent of B's existing "knowledge baseline" commit per the hook's rules. Document this in the migration script's banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-km-core-foundation/37-CONTEXT.md
@.planning/phases/37-km-core-foundation/37-RESEARCH.md
@.planning/phases/37-km-core-foundation/37-PATTERNS.md
@.planning/phases/37-km-core-foundation/37-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Same-file analogs in coding/. Read each BEFORE writing the edit. -->

Existing .gitmodules (5 entries, lib/km-core appended after):
  Path: /Users/Q284340/Agentic/coding/.gitmodules
  Format reference: lines 7-9 (mcp-server-semantic-analysis entry) - use SSH URL `git@github.com:fwornle/...`.

Existing Dockerfile.coding-services (lib/llm + lib/vkb-server build pattern):
  Path: /Users/Q284340/Agentic/coding/docker/Dockerfile.coding-services
  Key lines:
    - Around L102-106: `RUN cd lib/llm && npm install --ignore-scripts` (the pattern to mirror at L107-ish)
    - Around L123: `npm rebuild better-sqlite3 classic-level` (already covers km-core's classic-level via dedupe, but Pitfall 1 says add a defensive explicit rebuild)
    - Around L126: `RUN cd lib/llm && npm run build` (mirror at L127-ish)

Existing docker-compose.yml (lib/vkb-server bind-mount pattern):
  Path: /Users/Q284340/Agentic/coding/docker/docker-compose.yml
  Key lines:
    - Around L92: `${CODING_REPO:-.}/integrations/mcp-server-semantic-analysis/dist:/coding/integrations/mcp-server-semantic-analysis/dist:ro` - shape reference
    - Around L130: `${CODING_REPO:-.}/lib/vkb-server:/coding/lib/vkb-server:ro` - closest analog (mounts lib/ source/dist read-only). Mirror this shape for `lib/km-core/dist`.

Pre-commit hook (the guard we must not break):
  Path: /Users/Q284340/Agentic/coding/scripts/hooks/pre-commit-okb-guard.sh
  Verified by Plan 01's symlink-bc.sh: the KB_PATTERN regex covers both knowledge-export and exports paths. NO changes to the hook in this plan (D-23).

km-core repository (now contains v0.1 implementation from Plans 01-04):
  Path: ~/Agentic/km-core/
  State: built, tested, ready to push. Plan 01's user_setup ensured the empty GitHub repo exists; this plan's user_setup ensures it has commits pushed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push km-core to GitHub then add as submodule at coding/lib/km-core</name>
  <files>
    /Users/Q284340/Agentic/coding/.gitmodules,
    /Users/Q284340/Agentic/coding/lib/km-core
  </files>
  <read_first>
    /Users/Q284340/Agentic/coding/.gitmodules (current 5 submodule entries),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-PATTERNS.md (section "coding/.gitmodules (append)"),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md section "Pattern 6" (lines 480-512 - submodule integration steps, especially Option B chicken-and-egg note),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-CONTEXT.md decisions D-01 (public GH repo), D-04 (mount at lib/km-core)
  </read_first>
  <action>
    Preconditions: Plan 01's user_setup created the empty public repo at github.com/fwornle/km-core; this plan's user_setup directs the operator to push the local working tree there. Verify the push has happened before this task runs:
    ```
    cd ~/Agentic/km-core
    git remote -v | grep -q "git@github.com:fwornle/km-core.git" || { echo "remote not set"; exit 1; }
    git ls-remote --heads origin main | grep -q "refs/heads/main" || { echo "push not complete"; exit 1; }
    ```
    If preconditions fail, instruct the operator to run the user_setup commands and pause; do not proceed.

    Then from `coding/` root:
    1. `cd /Users/Q284340/Agentic/coding`.
    2. `git submodule add git@github.com:fwornle/km-core.git lib/km-core` - this appends to `.gitmodules` AND clones the submodule contents into `lib/km-core/`.
    3. Verify `.gitmodules` now contains the entry:
       ```
       [submodule "lib/km-core"]
           path = lib/km-core
           url = git@github.com:fwornle/km-core.git
       ```
       (D-04: lib/km-core, NOT integrations/km-core. SSH URL matches the existing 3 fwornle submodules per 37-PATTERNS.md.)
    4. Confirm `git submodule status` lists `lib/km-core` on the same SHA as `~/Agentic/km-core/`'s `main` HEAD.
    5. Commit: `feat(37-05): add @fwornle/km-core as submodule at lib/km-core (Phase 37 D-04)` - includes the `.gitmodules` change AND the gitlink entry for `lib/km-core`. NO `.data/*.json` files staged in this commit (the symlink migration is Task 3, separate commit per OKB-guard hygiene).

    Threat-model mitigation: T-37-05-01 (submodule URL spoofing) - verified URL is `git@github.com:fwornle/km-core.git` (matches existing fwornle submodules). The SSH key auth + GitHub user/org check at push time is the trust gate. NO `https://` or alternative-domain URLs.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && git submodule status lib/km-core | grep -q "lib/km-core" && grep -q '\[submodule "lib/km-core"\]' .gitmodules && grep -q "git@github.com:fwornle/km-core.git" .gitmodules && test -f lib/km-core/package.json && test -f lib/km-core/dist/index.js</automated>
    Also: `cd /Users/Q284340/Agentic/coding/lib/km-core && grep -q '"name": "@fwornle/km-core"' package.json` exits 0; `git log -1 --pretty=%s` in coding root returns the submodule-add commit subject.
  </verify>
  <acceptance_criteria>
    - `coding/.gitmodules` contains a `[submodule "lib/km-core"]` block with `path = lib/km-core` and `url = git@github.com:fwornle/km-core.git`.
    - `git submodule status lib/km-core` (from coding root) returns a line with a SHA and `lib/km-core` (no `-` prefix indicating uninitialized).
    - `coding/lib/km-core/` directory exists, contains the cloned km-core repo, including `package.json` with `"name": "@fwornle/km-core"`.
    - `coding/lib/km-core/dist/index.js` exists (the build artifact from km-core's main branch).
    - Commit `feat(37-05): add @fwornle/km-core as submodule at lib/km-core (Phase 37 D-04)` is HEAD on coding's main. The commit's diff touches ONLY `.gitmodules` and the gitlink entry - no `.data/` files in this commit.
    - URL uses SSH (`git@github.com:`), not HTTPS - matches the existing fwornle submodule pattern per 37-PATTERNS.md.
  </acceptance_criteria>
  <done>Submodule registered and mounted; km-core code accessible at coding/lib/km-core/.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Dockerfile install/rebuild/build + docker-compose bind-mount</name>
  <files>
    /Users/Q284340/Agentic/coding/docker/Dockerfile.coding-services,
    /Users/Q284340/Agentic/coding/docker/docker-compose.yml
  </files>
  <read_first>
    /Users/Q284340/Agentic/coding/docker/Dockerfile.coding-services (lines 90-140 - the lib/llm + lib/vkb-server install/rebuild/build pattern),
    /Users/Q284340/Agentic/coding/docker/docker-compose.yml (lines 70-140 - the bind-mount patterns),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-PATTERNS.md (sections "coding/Dockerfile.coding-services (modify L106-126)" + "coding/docker-compose.yml (modify L91-130)"),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md section "Pattern 6" (lines 480-512) plus section "Pitfall 1" (classic-level native binding) plus section "Pitfall 5" (do NOT use --production strip on TS dev deps; ship built dist/),
    /Users/Q284340/Agentic/coding/CLAUDE.md section "Rebuilding After Code Changes" (recurring submodule build pipeline issue)
  </read_first>
  <action>
    **Dockerfile.coding-services edits** (per 37-PATTERNS.md section "coding/Dockerfile.coding-services"):

    1. After the existing `RUN cd lib/llm && npm install --ignore-scripts 2>/dev/null || true` line (around L106), add:
       ```
       # Install KM-Core dependencies (Phase 37)
       # devDeps required for in-image fallback build (typescript). Primary delivery path is the dist/ bind-mount; this RUN is the CI/fresh-image safety net.
       RUN cd lib/km-core && npm install --ignore-scripts 2>/dev/null || true
       ```
       Note Pitfall 5: KM-Core is shipped as built `dist/` (the host builds it; container bind-mounts `dist/`). We mirror `lib/llm`'s `--ignore-scripts`-only pattern; do NOT pass `--production` here because the in-container `npm run build` (step 3 below) requires `typescript` which is a devDependency. `--ignore-scripts` skips the classic-level `node-gyp-build` postinstall (correct - the rebuild happens at L123-ish).

    2. After the existing `npm rebuild better-sqlite3 classic-level` line (around L123), add a defensive explicit rebuild per Pitfall 1:
       ```
       # Defensive rebuild of km-core's classic-level binding (Pitfall 1: dedupe may fail across arch)
       RUN cd lib/km-core && npm rebuild classic-level 2>/dev/null || true
       ```

    3. After the existing `RUN cd lib/llm && npm run build 2>/dev/null || true` (around L126), add:
       ```
       RUN cd lib/km-core && npm run build
       ```
       (Builds inside the container as a safety net; the bind-mount of host's `dist/` overrides this at runtime, but the in-container build catches CI / fresh-image scenarios. Note: NO `|| true` here — failures should propagate so a broken fallback build fails the image build rather than producing a silently-broken container.)

    **docker-compose.yml edit** (per 37-PATTERNS.md section "coding/docker-compose.yml"):

    Append a new bind-mount line near L130 inside `services.coding-services.volumes:`, matching the existing `lib/vkb-server` shape:
    ```
          # KM-Core dist (live-mounted to avoid Docker rebuild for TS changes - Phase 37)
          - ${CODING_REPO:-.}/lib/km-core/dist:/coding/lib/km-core/dist:ro
    ```
    Per Pitfall 5: mount **`dist/` only** (NEVER the full submodule source; container has no `tsc`). Read-only (`:ro`) matches sibling bind-mounts.

    **Validate**: `docker-compose -f docker/docker-compose.yml config 2>&1 | grep -v "WARN" | grep -E "(error|Error|ERROR)"` returns empty (the file is syntactically valid YAML and resolves).

    Commit: `feat(37-05): wire km-core install + native rebuild + dist bind-mount into coding container (Phase 37)`.

    DO NOT rebuild the container as part of this task - the operator decides when. CLAUDE.md says rebuilding is a 3-5min operation; we describe it but leave it to the operator. The Task 4 checkpoint discusses the rebuild step.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && grep -q "cd lib/km-core && npm install" docker/Dockerfile.coding-services && grep -q "cd lib/km-core && npm rebuild classic-level" docker/Dockerfile.coding-services && grep -q "cd lib/km-core && npm run build" docker/Dockerfile.coding-services && grep -q "lib/km-core/dist:/coding/lib/km-core/dist:ro" docker/docker-compose.yml && docker-compose -f docker/docker-compose.yml config > /dev/null</automated>
    Also: line count of `Dockerfile.coding-services` increased by exactly 3 (compared to git HEAD~1 + ~) since we added 3 RUN lines plus 3 comment lines = 6 lines, but accept 3-7 added; `grep -c "lib/km-core" docker/Dockerfile.coding-services` returns greater than or equal to 3.
  </verify>
  <acceptance_criteria>
    - `Dockerfile.coding-services` contains all 3 new RUN lines: `lib/km-core && npm install --ignore-scripts` (no `--production`, devDeps needed for fallback build), `lib/km-core && npm rebuild classic-level`, `lib/km-core && npm run build` (no trailing `|| true` — failures must propagate). Order is install (after lib/llm install), rebuild (after global rebuild), build (after lib/llm build).
    - `docker-compose.yml` contains the new bind-mount line `${CODING_REPO:-.}/lib/km-core/dist:/coding/lib/km-core/dist:ro` inside `services.coding-services.volumes`.
    - `docker-compose -f docker/docker-compose.yml config` succeeds (exit 0, no parse errors).
    - The mount is `:ro` (read-only) AND points at `dist/` (NOT the full submodule).
    - Commit `feat(37-05): wire km-core install + native rebuild + dist bind-mount into coding container (Phase 37)` is HEAD.
  </acceptance_criteria>
  <done>Docker wiring complete; container build pipeline knows how to install/rebuild/build km-core; bind-mount in place for dev iteration without Docker rebuild on TS edits.</done>
</task>

<task type="auto">
  <name>Task 3: Write idempotent symlink migration script + run it (BC for D-21)</name>
  <files>
    /Users/Q284340/Agentic/coding/scripts/migrate-exports-to-symlinks.mjs,
    /Users/Q284340/Agentic/coding/.data/exports/coding.json,
    /Users/Q284340/Agentic/coding/.data/knowledge-export/coding.json
  </files>
  <read_first>
    /Users/Q284340/Agentic/coding/.data/knowledge-export/coding.json (existing file - the migration source),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-PATTERNS.md (section "coding/scripts/migrate-exports-to-symlinks.mjs"),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md section "Pattern 7" (lines 513-541 - 5-step idempotent migration) plus section "Pitfall 3" (symlink-overwrite-canonical),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-CONTEXT.md decisions D-21 (symlinks for legacy paths), D-23 (OKB-guard stays SoT),
    /Users/Q284340/Agentic/coding/scripts/hooks/pre-commit-okb-guard.sh (the hook regex confirms both paths still match - this script does NOT modify the hook per D-23)
  </read_first>
  <action>
    Write `scripts/migrate-exports-to-symlinks.mjs` per 37-PATTERNS.md section "coding/scripts/migrate-exports-to-symlinks.mjs" + RESEARCH section "Pattern 7":

    ```
    #!/usr/bin/env node
    /**
     * One-shot migration: turn .data/knowledge-export/coding.json into a symlink
     * pointing at canonical .data/exports/coding.json per CONTEXT D-21 + RESEARCH section Pattern 7.
     *
     * SCOPE: Phase 37 - only coding.json (B's path). The .data/observation-export/*.json
     * symlinks are Phase 41 work (when A's adapter ships).
     *
     * IDEMPOTENT: if the symlink is already in place pointing to the correct target,
     * exit 0 with no-op. Pitfall 3 defense.
     *
     * SECURITY: refuses to follow existing symlinks at the source path OR overwrite
     * real files outside .data/ (T-37-05-02 symlink-traversal mitigation).
     *
     * OKB-GUARD: the generated changes (delete legacy file, write canonical file, create symlink)
     * MUST land in a separate commit from any observation-export changes - the pre-commit hook
     * (scripts/hooks/pre-commit-okb-guard.sh) enforces this. This script does NOT auto-commit;
     * the operator stages and commits the result.
     */
    import fs from 'node:fs';
    import path from 'node:path';

    const REPO_ROOT = process.env.CODING_REPO || process.cwd();
    const LEGACY = path.join(REPO_ROOT, '.data/knowledge-export/coding.json');
    const CANONICAL = path.join(REPO_ROOT, '.data/exports/coding.json');
    const CANONICAL_DIR = path.dirname(CANONICAL);
    const RELATIVE_TARGET = '../exports/coding.json';

    function log(msg) { process.stderr.write(`[migrate] ${msg}\n`); }

    function ensureRepoRootSafe() {
      // T-37-05-02: refuse to operate outside the coding repo
      const expected = path.resolve('/Users/Q284340/Agentic/coding');
      const actual = path.resolve(REPO_ROOT);
      if (!actual.startsWith(expected) && !process.env.ALLOW_NONSTANDARD_REPO) {
        throw new Error(`Refusing to operate outside coding repo: ${actual}`);
      }
    }

    function isAlreadyMigrated() {
      if (!fs.existsSync(LEGACY)) return false;
      const stat = fs.lstatSync(LEGACY);
      if (!stat.isSymbolicLink()) return false;
      const target = fs.readlinkSync(LEGACY);
      return target === RELATIVE_TARGET;
    }

    async function main() {
      ensureRepoRootSafe();

      if (isAlreadyMigrated()) {
        log('symlink already in place; no-op');
        process.exit(0);
      }

      // Step 0: ensure canonical dir exists
      fs.mkdirSync(CANONICAL_DIR, { recursive: true });

      // Step 1: read legacy content (if exists)
      const legacyExists = fs.existsSync(LEGACY) && !fs.lstatSync(LEGACY).isSymbolicLink();
      const canonicalExists = fs.existsSync(CANONICAL);

      if (legacyExists && canonicalExists) {
        // Both exist - backup canonical (in case content differs) and use legacy as authoritative
        const backup = `${CANONICAL}.bak.${Date.now()}`;
        log(`both legacy and canonical exist; backing up canonical to ${backup}`);
        fs.copyFileSync(CANONICAL, backup);
      }

      if (legacyExists) {
        log(`copying legacy ${LEGACY} -> canonical ${CANONICAL}`);
        fs.copyFileSync(LEGACY, CANONICAL);
        // Step 2: remove legacy file (we'll replace with symlink)
        fs.unlinkSync(LEGACY);
      } else if (!canonicalExists) {
        // Neither exists - create empty canonical (so the symlink target is valid)
        log('neither legacy nor canonical exists; creating empty canonical');
        fs.writeFileSync(CANONICAL, '{"nodes":[],"edges":[]}\n', 'utf-8');
      }

      // Step 3: create symlink at legacy path -> relative canonical path
      log(`creating symlink ${LEGACY} -> ${RELATIVE_TARGET}`);
      fs.symlinkSync(RELATIVE_TARGET, LEGACY);

      // Step 4: verify
      const verifyTarget = fs.readlinkSync(LEGACY);
      if (verifyTarget !== RELATIVE_TARGET) {
        throw new Error(`Symlink verification failed: ${verifyTarget} != ${RELATIVE_TARGET}`);
      }
      log('migration complete; reminder: stage and commit .data/exports/coding.json + .data/knowledge-export/coding.json SEPARATELY from any observation-export changes (OKB-baseline guard)');
    }

    main().catch(err => {
      process.stderr.write(`[migrate] FATAL: ${err.message}\n`);
      process.exit(1);
    });
    ```

    Make the script executable: `chmod +x scripts/migrate-exports-to-symlinks.mjs`.

    Run it: `node scripts/migrate-exports-to-symlinks.mjs` from coding repo root. Expect it to complete with exit 0 and produce the symlink.

    Stage the result. The OKB-baseline-guard hook will inspect what is staged. Per RESEARCH Pattern 7 the staged file set will be: `.data/exports/coding.json` (new canonical file) AND `.data/knowledge-export/coding.json` (the file replaced by symlink - git sees this as a content change from JSON to symlink). Both paths match the OKB-guard's KB_PATTERN regex (`\.data/(knowledge-export|exports)/.*\.json$`), so the hook treats them BOTH as "knowledge-baseline" changes - the two-commit rule says these can land together IF no observation-export files are mixed in. Per D-23, the hook IS the source of truth - we accept whatever it allows.

    Commit shape (Task 4's checkpoint verifies the hook actually fires correctly): `chore(37-05): migrate .data/knowledge-export/coding.json to symlink -> .data/exports/coding.json (D-21)`. Stage ONLY `.data/exports/coding.json` and `.data/knowledge-export/coding.json` in this commit; ANY `.data/observation-export/*` changes (none in Phase 37 scope) must be in a separate commit.

    Threat-model mitigation: T-37-05-02 (symlink traversal) - the script's `ensureRepoRootSafe()` refuses to operate outside `/Users/Q284340/Agentic/coding/` unless `ALLOW_NONSTANDARD_REPO` is set; the symlink is RELATIVE (`../exports/coding.json`), not absolute, so it cannot reach outside `.data/`.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && test -L .data/knowledge-export/coding.json && test "$(readlink .data/knowledge-export/coding.json)" = "../exports/coding.json" && test -f .data/exports/coding.json && node -e "JSON.parse(require('fs').readFileSync('.data/exports/coding.json','utf8'))" && test -x scripts/migrate-exports-to-symlinks.mjs</automated>
    Re-run idempotency: `node scripts/migrate-exports-to-symlinks.mjs` (second time) MUST exit 0 with the `symlink already in place; no-op` log line.
    Confirm migration script does not use console.log: `! grep -E "console\\.(log|info|warn|error)" scripts/migrate-exports-to-symlinks.mjs` exits 0.
  </verify>
  <acceptance_criteria>
    - `scripts/migrate-exports-to-symlinks.mjs` exists, executable bit set.
    - `.data/knowledge-export/coding.json` is a symlink (`test -L` exits 0).
    - `readlink .data/knowledge-export/coding.json` returns exactly `../exports/coding.json` (relative, NOT absolute - Pattern 7 footnote: relative symlinks survive across machine clones).
    - `.data/exports/coding.json` exists as a real file, parses as JSON.
    - The migration script can be re-run without harm (idempotent - second run exits 0 with no-op).
    - The script's only logging path is `process.stderr.write` (no `console.*`).
    - Commit `chore(37-05): migrate .data/knowledge-export/coding.json to symlink -> .data/exports/coding.json (D-21)` is HEAD, and its diff touches ONLY the two `.data/` paths (knowledge-export, exports). NO observation-export files included.
    - The pre-commit hook FIRED during this commit (proving it sees the changes) AND ALLOWED it - because no observation-export files were in the same commit. Verified by `git log -1 --stat | grep -E "knowledge-export|exports"`.
  </acceptance_criteria>
  <done>Legacy path is now a symlink to canonical; migration is idempotent; commit shape respects OKB-guard hygiene.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification - cross-repo TS import smoke + OKB-guard end-to-end + (optional) container rebuild</name>
  <what-built>
    Plans 01-04 produced the `@fwornle/km-core` v0.1 library; Tasks 1-3 of this plan registered the submodule, wired Docker, and migrated the legacy export path to a symlink. This checkpoint verifies the THREE things that cannot reasonably be unit-tested:

    1. **Cross-repo CORE-01 acceptance:** A scratch TypeScript file inside `coding/` that imports `Entity` and `Relation` types from `@fwornle/km-core` compiles cleanly under `tsc --noEmit`. This is the cross-repo half of CORE-01 success criterion 1 (the in-repo half was Plan 04's `/tmp/km-core-consumer-smoke.ts`).
    2. **OKB-baseline-guard end-to-end:** The pre-commit hook still ALLOWS commits that touch only `.data/exports/` or only `.data/knowledge-export/`, and still BLOCKS commits that mix knowledge-export with observation-export.
    3. **(Optional but recommended)** A full Docker rebuild + container smoke that proves the km-core build pipeline in Dockerfile actually works. This is the "Rebuilding After Code Changes" step from CLAUDE.md - takes 3-5 min per CLAUDE.md memory feedback_docker_build_time.md.
  </what-built>
  <how-to-verify>
    **Step 1 - Cross-repo TS import smoke (CORE-01 cross-repo):**
    From coding repo root:
    ```
    cat > /tmp/coding-km-core-smoke.ts <<'EOF'
    import type { Entity, Relation, EntityId } from '@fwornle/km-core';
    import { mintEntityId } from '@fwornle/km-core';
    const id: EntityId = mintEntityId();
    const e: Partial<Entity> = { id, name: 'CodingProject', entityType: 'Project' };
    const r: Relation = { type: 'CONTAINS', from: id, to: id };
    process.stderr.write(JSON.stringify({ e, r }) + '\n');
    EOF
    npx --no-install tsc --noEmit --module nodenext --moduleResolution nodenext --target ES2022 --strict --esModuleInterop /tmp/coding-km-core-smoke.ts 2>&1
    ```
    Expected: exit code 0, no diagnostic output.

    **Step 2 - OKB-baseline-guard end-to-end (SC#4 cross-check):**
    From coding repo root, on a clean working tree (`git status` shows nothing):
    ```
    # 2a: ALLOW path - only knowledge-export changes pass
    echo '{"nodes":[{"key":"smoke","attributes":{"name":"smoke"}}],"edges":[]}' > .data/exports/coding.json
    git add .data/exports/coding.json
    git commit -m "test: smoke-canonical-only [should succeed]" 2>&1 | tee /tmp/okb-smoke-allow.log
    git reset --hard HEAD~1  # rollback
    ```
    Expected: 2a commit SUCCEEDS (the hook is fine with knowledge-baseline-only commits).

    ```
    # 2b: BLOCK path - knowledge-export + observation-export together must be rejected
    echo '{"nodes":[],"edges":[]}' > .data/exports/coding.json
    echo '{}' > .data/observation-export/observations.json  # touch (will revert)
    git add .data/exports/coding.json .data/observation-export/observations.json
    git commit -m "test: smoke-mixed [should fail]" 2>&1 | tee /tmp/okb-smoke-block.log
    echo "Exit code: $?"
    git checkout HEAD -- .data/exports/coding.json .data/observation-export/observations.json
    ```
    Expected: 2b commit FAILS with a "OKB BASELINE GUARD" message (the hook fires); exit code is non-zero. The error message should mention separating into two commits.

    **Step 3 (recommended but OPTIONAL) - Docker rebuild + container smoke:**
    Per CLAUDE.md "Rebuilding After Code Changes":
    ```
    cd /Users/Q284340/Agentic/coding/docker
    docker-compose build coding-services  # 3-5 min per CLAUDE.md memory
    docker-compose up -d coding-services
    sleep 10
    docker exec coding-services ls /coding/lib/km-core/dist
    docker exec coding-services node -e "import('/coding/lib/km-core/dist/index.js').then(m => { console.log(Object.keys(m).join(',')); }).catch(e => { console.error(e); process.exit(1); })"
    ```
    Expected: the `ls` shows km-core's compiled artifacts; the `node -e` import resolves and prints the exported names (including `GraphKMStore`, `mintEntityId`, etc.).

    NOTE: if you skip Step 3, document it - it can be done in the verify-work phase or as part of integrating km-core into B during Phase 42.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` - all 3 steps verified (or 1+2 verified + 3 explicitly skipped) - proceed to summary.
    - `approved-skip-docker` - 1+2 verified, 3 skipped (record in summary).
    - `failed: <description>` - one of the steps surfaced a real problem; planner replans gap closure. Specify which step failed and the exact error output.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub remote (`git@github.com:fwornle/km-core.git`) to coding `lib/km-core` | Submodule URL is SSH; auth via SSH key (operator's GitHub keys). Tampering at the GitHub level is out of model. |
| `coding/.data/knowledge-export/coding.json` symlink target | Symlink is RELATIVE (`../exports/coding.json`); cannot escape `.data/`. |
| Migration script `scripts/migrate-exports-to-symlinks.mjs` to coding `.data/` filesystem | Script refuses to operate outside the coding repo unless `ALLOW_NONSTANDARD_REPO` env override is set. |
| Docker bind-mount `lib/km-core/dist:/coding/lib/km-core/dist:ro` | Read-only mount; container cannot write back to host. |
| Pre-commit hook to staged files | Hook regex covers BOTH knowledge-export and exports paths; D-23 makes the hook the source of truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Spoofing / Tampering | Submodule URL spoofing (typo-squatted GH org) | mitigate | URL is explicitly `git@github.com:fwornle/km-core.git` - matches existing 3 fwornle submodules (mcp-constraint-monitor, memory-visualizer, mcp-server-semantic-analysis) per 37-PATTERNS.md. SSH key auth at push time ties to operator's GH account. |
| T-37-05-02 | Tampering | Symlink-traversal attack during BC migration | mitigate | Migration script: (a) `ensureRepoRootSafe()` refuses non-coding-repo unless override env set; (b) symlink target is RELATIVE (`../exports/coding.json`), cannot reach outside `.data/`; (c) checks `lstatSync(legacyPath).isSymbolicLink()` BEFORE operating (Pitfall 3 defense - idempotency without overwriting existing symlinks). Documented in script header. |
| T-37-05-03 | Tampering | Docker bind-mount allowing container-to-host write | mitigate | Mount is `:ro` (read-only) per 37-PATTERNS.md. Container processes cannot modify host files at `lib/km-core/dist/`. |
| T-37-05-04 | Tampering | Pre-commit hook bypassed via `--no-verify` | accept | Operator-controlled escape hatch (`git commit --no-verify`). NOT mitigated at the plan level; the hook is best-effort, not a security boundary. D-23 acknowledges the hook is policy-enforcement, not cryptographic. CLAUDE.md memory feedback_worktree_verification.md is the documented mitigation: verify on `main` not worktrees. |
| T-37-05-05 | Denial of Service | Migration script run twice corrupting data | mitigate | Idempotent: `isAlreadyMigrated()` check at top exits 0 with no-op if symlink already correct. Pitfall 3 defense. |
| T-37-05-SC | Tampering | npm/git installs at submodule add time | mitigate | Submodule contents come from a developer-controlled GH repo (km-core). The km-core repo itself has the supply-chain mitigations from Plan 01 Task 1 (slopcheck [OK], pinned versions, npm ci in CI). |
</threat_model>

<verification>
After all 4 tasks (3 auto + 1 checkpoint):
- `git submodule status lib/km-core` (in coding) shows the submodule on the same SHA as the km-core repo's main HEAD.
- `docker-compose -f docker/docker-compose.yml config` exits 0; no parse errors.
- `test -L .data/knowledge-export/coding.json && readlink .data/knowledge-export/coding.json` returns `../exports/coding.json`.
- The cross-repo TS smoke (`tsc --noEmit` on `/tmp/coding-km-core-smoke.ts`) exits 0.
- The OKB-guard smoke (Task 4 Step 2) showed the hook ALLOWS knowledge-baseline-only commits AND BLOCKS mixed commits.
- 3 commits added to coding's main (submodule add, Docker wiring, symlink migration) - none of which mix knowledge-export with observation-export.
- Operator has approved (or explicit-skipped) the Docker rebuild step.
</verification>

<success_criteria>
- CORE-01 (cross-repo half): consumer in coding can `import type { Entity, Relation, EntityId } from '@fwornle/km-core'` and tsc compiles clean.
- CORE-02 (consumer wiring): coding/lib/km-core/ is the live submodule mount; Docker pipeline knows how to build it; bind-mount serves the host-built dist/ into the container.
- SC#4 (BC + OKB-guard): legacy path resolves via symlink; pre-commit hook still fires correctly on representative commit shapes (validated by Task 4 Step 2).
- Phase 37 feature-complete: ready for `/gsd:verify-work 37`.
</success_criteria>

<output>
Create `.planning/phases/37-km-core-foundation/37-05-SUMMARY.md` when done. Record:
- The exact SHA the submodule points at (km-core main HEAD).
- Confirm Dockerfile additions show up (3 new RUN lines with `lib/km-core`).
- Confirm docker-compose bind-mount line for `lib/km-core/dist`.
- The symlink readlink output (`../exports/coding.json`).
- Task 4 results: cross-repo TS smoke exit code, OKB-guard 2a/2b outcomes, Docker rebuild step (approved / approved-skip-docker / failed).
- Total commits added to coding's main during this plan (expect 3: submodule add, Docker wiring, symlink migration).
- Note: Phase 37 should now be ready for `/gsd:verify-work 37`. The 4 phase success criteria from ROADMAP.md should all be defensible:
  1. Importable Entity/Relation types across coding/ and rapid-automations/ - the coding/ side is proven; rapid-automations/ side waits for Phase 43.
  2. GraphKMStore passes parity tests - Plan 04 closed this gate.
  3. UUIDv7 IDs survive export/restore round-trip - Plan 04 closed this gate via the round-trip integration test.
  4. .data/knowledge-export/coding.json + .data/exports/*.json still load via KM-Core without breaking two-commit/OKB-baseline guard - this plan closed this gate via Tasks 3+4.
</output>
