---
phase: 37-km-core-foundation
plan: 04
type: execute
wave: 4
depends_on: [37-03]
files_modified:
  - ~/Agentic/km-core/src/store/GraphKMStore.ts
  - ~/Agentic/km-core/src/index.ts
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]
requirements_addressed: [CORE-01, CORE-02, CORE-03]
must_haves:
  truths:
    - "`GraphKMStore` exposes the repository pattern API per D-14: `putEntity`, `getEntity`, `deleteEntity`, `findByOntologyClass`, `addRelation`, `findRelations`, `batch`, `iterate`, `exportJson`, `open`, `close` — every method returns a Promise (reinforces D-15 async-only API across the full GraphKMStore repository surface)."
    - "`putEntity` stamps a fresh UUIDv7 when `id` is missing (D-10); validates caller-supplied `id` via `parseEntityId` (D-10, Pitfall 2); validates `entityType` via the injected `OntologyValidator` (D-19) unless `skipOntologyCheck: true`."
    - "`GraphKMStore` extends Node's `EventEmitter` and emits `entity:put`, `entity:delete`, `relation:added`, `relation:removed` (D-16)."
    - "`batch(ops[])` is all-or-nothing: validate ALL ops, then LevelDB `db.batch(...)` atomic, then in-memory + events fired (D-17)."
    - "`iterate(filter?)` is an async iterator that lazily yields entities matching `entityType` / `ontologyClass` / `layer` (D-18)."
    - "Public barrel `src/index.ts` exports the full surface needed by Plan 01's tests (Entity, Relation, EntityId, mintEntityId, parseEntityId, GraphKMStore, GraphKMStoreOptions, BatchOp, FilterObject, event payload types)."
    - "Plan 01's `tests/unit/graph-store.test.ts` and `tests/integration/round-trip.test.ts` are GREEN."
  artifacts:
    - path: "~/Agentic/km-core/src/store/GraphKMStore.ts"
      provides: "Composition class wrapping Graphology MultiDirectedGraph + PersistenceManager + Exporter + UUIDv7 stamping + ontology validation + event emission"
      contains: "export class GraphKMStore extends EventEmitter"
    - path: "~/Agentic/km-core/src/index.ts"
      provides: "Public API barrel of @fwornle/km-core (replaces the placeholder from Plan 01)"
      contains: "export { GraphKMStore }"
  key_links:
    - from: "~/Agentic/km-core/src/store/GraphKMStore.ts"
      to: "~/Agentic/km-core/src/ids/mint.ts"
      via: "import { mintEntityId, parseEntityId }"
      pattern: "ids/mint|ids/parse"
    - from: "~/Agentic/km-core/src/store/GraphKMStore.ts"
      to: "~/Agentic/km-core/src/store/persistence.ts"
      via: "composition - GraphKMStore creates a PersistenceManager"
      pattern: "new PersistenceManager"
    - from: "~/Agentic/km-core/src/store/GraphKMStore.ts"
      to: "~/Agentic/km-core/src/store/exporter.ts"
      via: "composition - wires events to exporter.schedule()"
      pattern: "new Exporter\\(|exporter\\.schedule"
---

<objective>
Compose `PersistenceManager` (Plan 03), `Exporter` (Plan 03), `MultiDirectedGraph` (graphology), and the ID/validation/event types (Plan 02) into the `GraphKMStore` repository class per D-14 through D-19. This is the final assembly: every API method maps to a small dispatch over Graphology + LevelDB + event emit + debounced export.

Adopt RESEARCH section "Pattern 2" (skeleton, lines 289-368) plus 37-PATTERNS.md section "src/store/GraphKMStore.ts" (lists 7 deltas relative to B's `GraphDatabaseAdapter`). Strip B's VKB-API fork; switch event names from B's `entity:stored`/`relationship:stored` to KM-Core's `entity:put`/`entity:delete`/`relation:added`/`relation:removed`; use plain UUIDv7 nodeIds (NOT C's `${layer}:${id}` prefix scheme per RESEARCH Open Question #2); options-object constructor (NOT B's positional `(dbPath, team)`); drop the `team` concept entirely.

Purpose: Closes CORE-01 (typed surface exported), CORE-02 (parity store consumable), CORE-03 (UUIDv7 stamping enforced on every write). After this plan, `tests/unit/graph-store.test.ts` and `tests/integration/round-trip.test.ts` go GREEN - the full library is consumable end-to-end.

Output: 2 source files - the main composition class and the public barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-km-core-foundation/37-CONTEXT.md
@.planning/phases/37-km-core-foundation/37-RESEARCH.md
@.planning/phases/37-km-core-foundation/37-PATTERNS.md
@.planning/phases/37-km-core-foundation/37-VALIDATION.md

<interfaces>
<!-- The two production analogs feeding into GraphKMStore composition. Read in this order before writing. -->

B's GraphDatabaseAdapter (API shape source):
  Path: /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/graph-database-adapter.ts
  Key sections (per 37-PATTERNS.md):
    - Lines 240-420: storeEntity, queryEntities, exportToJSON, mergeAttributes, deleteEntity, close
    - Lines 343-347: mergeAttributes hot path (preserve verbatim for operator-enriched updates)
  Strip:
    - Lines 55-93, 259-275, 309-322, 337-341, 373-378: VKB-API fork (useApi/apiClient branches) - KM-Core has NO VKB. Use only the "direct" path.

C's graph-store (in-memory ops source):
  Path: /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/graph-store.ts
  Key sections:
    - Line 69: addEntity
    - Line 80: getEntity
    - Line 85: updateEntity
    - Line 135: deleteEntity
    - Line 141: addEdge
    - Line 217: getEdges
    - Line 256: filterByLayer
    - Lines 328-332: export/import
    - Line 70: NodeId scheme - C uses `${layer}:${id}` - DO NOT REPLICATE. KM-Core uses plain UUIDv7 per RESEARCH Open Question #2.

Plan 02/03 modules this plan imports:
  ~/Agentic/km-core/src/types/entity.ts (Entity, Relation, EntityId, Layer, SerializedGraph)
  ~/Agentic/km-core/src/ids/branded.ts (EntityId brand)
  ~/Agentic/km-core/src/ids/mint.ts (mintEntityId)
  ~/Agentic/km-core/src/ids/parse.ts (parseEntityId)
  ~/Agentic/km-core/src/store/types.ts (BatchOp, FilterObject)
  ~/Agentic/km-core/src/events/types.ts (EntityPutEvent, EntityDeleteEvent, RelationAddedEvent, RelationRemovedEvent)
  ~/Agentic/km-core/src/store/persistence.ts (PersistenceManager)
  ~/Agentic/km-core/src/store/exporter.ts (Exporter)
  ~/Agentic/km-core/src/validation/ontology.ts (OntologyValidator, noopOntologyValidator)

Tests that go GREEN after this plan:
  /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts  (11 named tests from Plan 01)
  /Users/Q284340/Agentic/km-core/tests/integration/round-trip.test.ts  (4 fixture parity tests from Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose GraphKMStore (repository class extending EventEmitter)</name>
  <files>
    ~/Agentic/km-core/src/store/GraphKMStore.ts
  </files>
  <read_first>
    /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/graph-database-adapter.ts (lines 240-420, plus lines 343-347 for mergeAttributes - 37-PATTERNS.md item 1),
    /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/store/graph-store.ts (full file - in-memory Graphology operations source),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-PATTERNS.md (section "src/store/GraphKMStore.ts" - 7 deltas listed),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md section "Pattern 2" (lines 289-368) plus section "Pattern 4" (UUIDv7 stamping semantics) plus section "Pattern 5" (Branded EntityId ergonomics),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-CONTEXT.md decisions D-10..D-19 (all UUID + API surface decisions),
    ~/Agentic/km-core/src/store/persistence.ts (created Plan 03),
    ~/Agentic/km-core/src/store/exporter.ts (created Plan 03),
    ~/Agentic/km-core/tests/unit/graph-store.test.ts (11 RED tests this task makes GREEN),
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-RESEARCH.md Open Question #2 (use plain UUIDv7 nodeIds, NOT `layer:uuid` prefix)
  </read_first>
  <action>
    Write `src/store/GraphKMStore.ts` per RESEARCH section "Pattern 2" + 37-PATTERNS.md section "src/store/GraphKMStore.ts". The skeleton is the line 289-368 RESEARCH excerpt; apply the 7 deltas listed in 37-PATTERNS.md.

    Imports:
    ```
    import { MultiDirectedGraph } from 'graphology';
    import { EventEmitter } from 'node:events';
    import * as path from 'node:path';
    import { mintEntityId } from '../ids/mint.js';
    import { parseEntityId } from '../ids/parse.js';
    import { PersistenceManager } from './persistence.js';
    import { Exporter } from './exporter.js';
    import { noopOntologyValidator, type OntologyValidator } from '../validation/ontology.js';
    import type { Entity, Relation, EntityId, Layer, SerializedGraph } from '../types/entity.js';
    import type { BatchOp, FilterObject } from './types.js';
    ```

    Public exports:
    ```
    export interface GraphKMStoreOptions {
      dbPath: string;
      exportDir: string;
      domains?: readonly string[];     // forwarded to Exporter + PersistenceManager
      debounceMs?: number;             // forwarded to Exporter; default 5000 (D-22)
      ontologyValidator?: OntologyValidator;   // default noopOntologyValidator (D-19)
    }

    export class GraphKMStore extends EventEmitter { /* ... */ }
    ```

    Apply the 7 deltas (37-PATTERNS.md section "src/store/GraphKMStore.ts" DELTAS):

    **Delta 1 - Strip the VKB-API fork from B's adapter.** Use only the "direct" path (no `useApi`/`apiClient` branches).

    **Delta 2 - Switch event names** from B's `entity:stored`/`relationship:stored` to KM-Core's `entity:put`/`entity:delete`/`relation:added`/`relation:removed` (D-16). Payload types come from `src/events/types.ts`.

    **Delta 3 - Use plain UUIDv7 as nodeId.** NodeId is `entity.id` (the UUIDv7 string), NOT C's `${layer}:${id}` prefix scheme (RESEARCH Open Question #2). The `layer` field on `Entity` already discriminates; prefix-encoding is a C artifact that should not propagate. C's migration in Phase 43 strips the prefix.

    **Delta 4 - Extend Node's `EventEmitter`** (NOT Redis pub/sub - D-16 says in-process emitter; the Redis bridge is a consumer responsibility).

    **Delta 5 - NO `setEntityProvenance`/`setProvenance` methods in v0.1.** Phase 39 adds those; v0.1 just declares the provenance types (already done in Plan 02).

    **Delta 6 - Constructor takes `GraphKMStoreOptions` object, NOT `(dbPath, team)` positional.** B's positional signature was historical accident; D-14 says repository pattern with typed methods => options object.

    **Delta 7 - The `team` field is GONE.** KM-Core has no concept of "team". Domain bucketing in the exporter replaces it.

    **Method implementations** (use the body sketches from 37-PATTERNS.md and RESEARCH section "Pattern 2", lightly elaborated):

    `constructor(opts: GraphKMStoreOptions)`:
      - Initialize `this.graph = new MultiDirectedGraph<Entity, Relation>()`.
      - Initialize `this.persistence = new PersistenceManager(opts.dbPath, opts.exportDir, { domains: opts.domains })`.
      - Initialize `this.exporter = new Exporter({ exportDir: opts.exportDir, domains: opts.domains, debounceMs: opts.debounceMs, getSnapshot: () => this.graph.export() as SerializedGraph })`.
      - Store `this.validator = opts.ontologyValidator ?? noopOntologyValidator`.

    `async open(): Promise<void>`:
      - Hydrate via `this.persistence.hydrate()`; if non-null, `this.graph.import(hydrated)`.
      - Mark `this.initialized = true`.

    `async putEntity(e: Partial<Entity> & { name: string; entityType: string }, opts?: { skipOntologyCheck?: boolean }): Promise<EntityId>`:
      - Per 37-PATTERNS.md `putEntity` semantics (lines 349-385):
        1. D-19 validation: `if (!opts?.skipOntologyCheck) this.validator.validate(e.entityType);` (throws on invalid class).
        2. D-10 stamp-or-keep: `let id: EntityId; if (e.id) { id = parseEntityId(e.id as string); } else { id = mintEntityId(); }`. `parseEntityId` throws `SyntaxError` on caller-supplied invalid id (Pitfall 2 defense).
        3. Build full Entity with defaults: `createdAt`, `updatedAt`, `layer ?? 'evidence'`, `description ?? ''`, `metadata ?? {}`.
        4. `this.graph.mergeNode(id, entity)` (Graphology - C analog graph-store.ts:71).
        5. emit `'entity:put'` with `{ entity }` payload (D-16).
        6. `this.exporter.schedule()` to trigger debounced flush.
        7. Return `id`.

    `async getEntity(id: EntityId): Promise<Entity | null>`:
      - `if (!this.graph.hasNode(id)) return null;`
      - Return `this.graph.getNodeAttributes(id) as Entity`.

    `async deleteEntity(id: EntityId): Promise<boolean>`:
      - `if (!this.graph.hasNode(id)) return false;`
      - `this.graph.dropNode(id);`
      - emit `'entity:delete'` with `{ id }` payload.
      - `this.exporter.schedule();`
      - Return `true`.

    `async findByOntologyClass(cls: string): Promise<Entity[]>`:
      - Iterate `this.graph.nodes()` (Graphology) - filter where `entity.entityType === cls` OR `entity.ontologyClass === cls` (both fields per D-19 + 37-PATTERNS.md note on ontologyClass alias). Return array.

    `async addRelation(r: Relation): Promise<void>`:
      - `parseEntityId(r.from)` and `parseEntityId(r.to)` (validate both ends; throws if either is malformed).
      - `if (!this.graph.hasNode(r.from)) throw new Error('Source node not found: ' + r.from);` (same for `r.to`).
      - `this.graph.addEdgeWithKey(<key derived from r.from + r.to + r.type>, r.from, r.to, r)`.
      - emit `'relation:added'` with `{ relation: r }` payload.
      - `this.exporter.schedule();`

    `async findRelations(filter: Partial<Relation>): Promise<Relation[]>`:
      - Iterate `this.graph.edges()` (Graphology), get attributes, filter by `filter.type`/`filter.from`/`filter.to`. Return array.

    `async batch(ops: BatchOp[]): Promise<void>`:
      - **All-or-nothing per D-17**: validate ALL ops first (parseEntityId on all caller-supplied ids + ontology validation), then apply in-memory + emit events + schedule export. If any validation fails, throw BEFORE mutating state.
      - LevelDB-level atomicity: in v0.1 we rely on Graphology in-memory mutations being applied AFTER ALL validations pass; full LevelDB `db.batch(...)` round-trip is delegated to PersistenceManager which can be enhanced in v0.2 (current `persist()` writes the whole graph snapshot anyway, so a single debounced flush captures the batch state).
      - Note: This is a slight simplification of RESEARCH section "Pattern 2" Example 2 because we don't keep per-mutation LevelDB rows - we snapshot the whole graph on debounced flush. The batch-test acceptance criterion is "state unchanged on mid-batch validation failure" - that holds because we validate-all-first.

    `async *iterate(filter?: FilterObject): AsyncIterable<Entity>`:
      - `for (const nodeId of this.graph.nodes()) { const e = this.graph.getNodeAttributes(nodeId) as Entity; if (this.matches(e, filter)) yield e; }`
      - Lazy by default (D-18).

    `async exportJson(): Promise<void>`:
      - Direct call: `await this.exporter.flush()`. Used by the round-trip integration test which sets `debounceMs: 0` and calls `exportJson()` to force-flush.

    `mergeAttributes(nodeId: EntityId, attributes: Partial<Entity>): Promise<void>`:
      - **PRESERVE B's mergeAttributes hot path** (37-PATTERNS.md item showing lines 343-347 of analog).
      - `if (!this.graph.hasNode(nodeId)) throw new Error('Node ' + nodeId + ' not found in graph');`
      - `this.graph.mergeNodeAttributes(nodeId, attributes);`
      - emit `'entity:put'` with the merged entity.
      - `this.exporter.schedule();`

    `async close(): Promise<void>`:
      - `await this.exporter.flush()` (flush any pending debounced write).
      - `await this.persistence.persist(this.graph.export() as SerializedGraph)` (durable LevelDB write).
      - `await this.persistence.close()`.
      - `this.initialized = false;`

    Private helpers:
    - `private matches(e: Entity, filter?: FilterObject): boolean` - field-by-field check.

    Typed events (TS strictness): use `EventEmitter` directly in v0.1 with explicit string event names. Type-safe wrapper is a v0.2 concern (RESEARCH section "EventEmitter base class" note).

    NO `console.*` anywhere. Use ESM `.js` extensions throughout.

    Commit: `feat(37-04): compose GraphKMStore - repository API + UUIDv7 stamping + events + atomic export (CORE-01/02/03)`.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && npx tsc --noEmit && npm test -- tests/unit/graph-store.test.ts</automated>
    Confirm grep gates: `grep -q "export class GraphKMStore extends EventEmitter" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0; `grep -q "mintEntityId\(\)" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0; `grep -q "parseEntityId" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0; `grep -q "this.emit('entity:put'" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0; `grep -q "this.emit('entity:delete'" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0; `grep -q "this.emit('relation:added'" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0; `grep -q "this.emit('relation:removed'" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0; `grep -q "mergeNodeAttributes" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0 (B's hot path preserved); `! grep -E "entity:stored|relationship:stored|useApi|apiClient" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0 (B's VKB-API artifacts stripped); `! grep -E "\\$\\{layer\\}:|layer + ':'" ~/Agentic/km-core/src/store/GraphKMStore.ts` exits 0 (no `layer:uuid` prefix per RESEARCH Open Question #2); `grep -rE "console\.(log|info|warn|error)" ~/Agentic/km-core/src/store/GraphKMStore.ts` returns zero lines.
  </verify>
  <acceptance_criteria>
    - File exists; `npx tsc --noEmit` exits 0.
    - `npm test -- tests/unit/graph-store.test.ts` reports ALL 11 named tests PASSING (the exact list from Plan 01 Task 3).
    - `GraphKMStore` extends `EventEmitter` from `node:events`.
    - Event names emitted are exactly `entity:put`, `entity:delete`, `relation:added`, `relation:removed` - verified by grep gates.
    - NodeIds in Graphology are plain UUIDv7 strings (no `layer:uuid` prefix) - verified by negative grep on `${layer}:` patterns and by the round-trip test's id-survival assertion in Task 2.
    - B's `mergeAttributes(nodeId, attributes)` hot path is preserved as a method on the class.
    - B's `useApi`/`apiClient` VKB-API fork is GONE (negative grep).
    - Constructor accepts `GraphKMStoreOptions` (object), not positional args.
    - `putEntity` with caller-supplied invalid id throws `SyntaxError` (via `parseEntityId`).
    - `batch([valid, invalid, valid])` throws AND leaves the graph state unchanged AND emits zero events (validate-all-first behavior).
    - `iterate({ entityType: 'Component' })` is an AsyncIterable that yields only matching entities (and is lazy - the test asserts laziness by counting iterations).
    - `skipOntologyCheck: true` bypasses validation; with a strict validator and an unknown class, `putEntity` succeeds when flag set, throws when flag absent.
    - No `console.*` references.
    - Commit recorded.
  </acceptance_criteria>
  <done>GraphKMStore green; graph-store test file all 11 tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder src/index.ts with the public API barrel + run full parity</name>
  <files>
    ~/Agentic/km-core/src/index.ts
  </files>
  <read_first>
    /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-PATTERNS.md (section "src/index.ts" - lists the exact exports for the barrel),
    ~/Agentic/km-core/src/types/entity.ts (created Plan 02),
    ~/Agentic/km-core/src/store/types.ts (created Plan 02),
    ~/Agentic/km-core/src/events/types.ts (created Plan 02),
    ~/Agentic/km-core/src/ids/mint.ts (created Plan 02),
    ~/Agentic/km-core/src/ids/parse.ts (created Plan 02),
    ~/Agentic/km-core/src/store/GraphKMStore.ts (created Task 1),
    ~/Agentic/km-core/tests/integration/round-trip.test.ts (the 4 fixture parity tests this task makes GREEN)
  </read_first>
  <action>
    Replace the Plan 01 placeholder at `src/index.ts` with the full public API barrel per 37-PATTERNS.md section "src/index.ts":

    ```
    export type {
      Entity, Relation, Layer, EntityId, SerializedGraph,
      ProvenanceStamp, EntityProvenance, SegmentConfirmation, DescriptionSegment, ResolutionRecord
    } from './types/entity.js';
    export type { BatchOp, FilterObject } from './store/types.js';
    export type {
      EntityPutEvent, EntityDeleteEvent,
      RelationAddedEvent, RelationRemovedEvent
    } from './events/types.js';
    export { mintEntityId } from './ids/mint.js';
    export { parseEntityId } from './ids/parse.js';
    export { GraphKMStore } from './store/GraphKMStore.js';
    export type { GraphKMStoreOptions } from './store/GraphKMStore.js';
    export type { OntologyValidator } from './validation/ontology.js';
    export { noopOntologyValidator } from './validation/ontology.js';
    ```

    No more `KM_CORE_VERSION` placeholder. (Optionally re-export it as `export const KM_CORE_VERSION = '0.1.0';` since 0.1.0 is now feature-complete - planner's discretion; recommend keeping a version export for runtime introspection by Plan 05 smoke test.)

    Run the FULL test suite to confirm the parity test in `tests/integration/round-trip.test.ts` passes across all 4 fixtures. This is the CORE-02 SC#2 acceptance criterion (parity-by-fixture across B's `coding.json` and C's three exports).

    If the round-trip test fails on a specific fixture (e.g., the byte-equal canonical-key-sort fails on `c-kpifw-snapshot.json` because of a metadata field shape difference), DO NOT modify the fixture. Investigate which side of the round-trip is dropping/adding the field, and fix in GraphKMStore (Task 1) or PersistenceManager/Exporter (Plan 03). The fixtures are frozen reference inputs.

    Commit: `feat(37-04): wire public barrel src/index.ts; CORE-01/02/03 full surface green`.
  </action>
  <verify>
    <automated>cd ~/Agentic/km-core && npx tsc --noEmit && npm run build && npm test</automated>
    The full suite (`npm test` with no args) must report ALL test files GREEN: `entity.test.ts`, `ids.test.ts`, `graph-store.test.ts`, `persistence.test.ts`, `exporter.test.ts`, `round-trip.test.ts`. The shell test `symlink-bc.sh` is run separately (`bash tests/integration/symlink-bc.sh`).

    Confirm: `grep -q "export { GraphKMStore }" ~/Agentic/km-core/src/index.ts` exits 0; `grep -q "export { mintEntityId }" ~/Agentic/km-core/src/index.ts` exits 0; `grep -q "export { parseEntityId }" ~/Agentic/km-core/src/index.ts` exits 0; `grep -q "export type { Entity" ~/Agentic/km-core/src/index.ts` exits 0; `grep -q "export type { Relation" ~/Agentic/km-core/src/index.ts` exits 0; `grep -q "export type { EntityId" ~/Agentic/km-core/src/index.ts` exits 0.

    Run an external-consumer smoke check: create a scratch file at `/tmp/km-core-consumer-smoke.ts` containing `import { GraphKMStore, mintEntityId, type Entity } from '@fwornle/km-core'; const id = mintEntityId(); console.log(id);` then `cd ~/Agentic/km-core && npx tsc --noEmit --module nodenext --moduleResolution nodenext /tmp/km-core-consumer-smoke.ts` - expect exit 0 (this proves CORE-01's "developer can import Entity and Relation types" from a consumer perspective, locally - the cross-repo `coding/`-side check is in Plan 05).
  </verify>
  <acceptance_criteria>
    - `src/index.ts` no longer contains the placeholder version-only export; it exports the full public surface (verified by grep gates).
    - `npm test` (full suite) reports ALL test files GREEN (5 unit files + 1 integration TS file). Exit code 0.
    - `bash tests/integration/symlink-bc.sh` exits 0 (was already green from Plan 01; verify still passing).
    - `npm run build` exits 0 and produces `dist/index.js`, `dist/index.d.ts`, plus all the substructure (types/, ids/, store/, events/, validation/).
    - The 4 parity tests in `round-trip.test.ts` PASS - meaning all 4 frozen fixtures (b-coding, c-raas, c-kpifw, c-general) round-trip byte-equal after canonical key-sort. THIS IS THE CORE-02 SC#2 GATE.
    - The external consumer smoke at `/tmp/km-core-consumer-smoke.ts` compiles cleanly under `tsc --noEmit` (proves the `exports` map + barrel resolution works from a consumer's perspective on the same machine - Plan 05 does the cross-repo version).
    - No `console.*` references in `src/index.ts` (CLAUDE.md constraint - the scratch consumer file uses `console.log` but lives in `/tmp`, not in the library).
    - Commit recorded.
  </acceptance_criteria>
  <done>Library is feature-complete and consumer-importable; all in-repo tests GREEN; CORE-02 parity gate cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library consumer to `GraphKMStore.putEntity` | Caller-supplied id can be a wide string; the writer-side gate validates via `parseEntityId` (D-10, Pitfall 2). |
| Library consumer to `GraphKMStore.batch` | Atomic-all-or-nothing semantics protect against partial-state corruption when one op is malformed. |
| Library to LevelDB (constructor `dbPath`) | The caller controls `dbPath`; library does not validate against system directories. Defense-in-depth deferred to consumer. |
| In-process EventEmitter to subscribers | All listeners run in-process; D-16 explicitly says no cross-process subscription. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Tampering | Caller-supplied `EntityId` injection via `putEntity` | mitigate | Per CORE-03 acceptance criterion: `parseEntityId` validates UUID format + v7 variant (charAt(14)) BEFORE the entity is stored. Documented in `putEntity` JSDoc. Test `putEntity with caller-supplied invalid id throws SyntaxError` enforces this. |
| T-37-04-02 | Tampering | `batch` partial-state corruption on mid-op failure | mitigate | D-17 validate-all-first design: every op's `parseEntityId` + `validator.validate` runs BEFORE any in-memory mutation. If ANY op fails validation, the entire batch throws and the graph state is unchanged. Test `batch is all-or-nothing on validation failure` enforces this. |
| T-37-04-03 | Spoofing | Event listener replay / impersonation | accept | EventEmitter is in-process (D-16). Cross-process spoofing not in scope. Redis bridge (consumer-side, e.g., coding's existing pub/sub) is the consumer's responsibility - documented in Pattern 2 footnote. |
| T-37-04-04 | Repudiation | Mutations without provenance tracking | accept | Phase 39 owns `createdBy`/`lastConfirmedBy`/`confirmationCount`. Phase 37 declares the types but does NOT populate them. Accept for v0.1. |
| T-37-04-05 | Information Disclosure | `iterate(filter)` lazy yield could leak entity data via timing side-channel | accept | Library-level filter is just a `Partial<Entity>` shape match; no encrypted attributes; no row-level security. ACCEPT - not in v0.1 threat model. |
| T-37-04-06 | Tampering | `mergeAttributes` allows partial-update without ontology re-validation | accept | The hot path is preserved verbatim from B's adapter (37-PATTERNS.md). `mergeNodeAttributes` does not change `entityType`, so the ontology class stays fixed. If a consumer passes `attributes: { entityType: 'DifferentClass' }`, the type system allows it but the ontology validator is NOT re-run on merge. Document the limitation; CORE-02 does not require it. Phase 38 may revisit. |
| T-37-04-07 | Denial of Service | Unbounded `iterate(filter)` on huge graphs | accept | Async iterator is inherently lazy; consumer controls how many entities they pull. No timeout. Accept - matches B/C current behavior. |
</threat_model>

<verification>
After both tasks:
- `cd ~/Agentic/km-core && npx tsc --noEmit && npm run build && npm test && bash tests/integration/symlink-bc.sh && echo ALL-GREEN`.
- All 5 unit test files + 1 integration TS file + 1 integration shell file PASSING.
- `git log --oneline | head -8` shows the bootstrap plus Plans 01/02/03/04 commits (8+ commits total).
- `grep -rE "console\.(log|info|warn|error)" ~/Agentic/km-core/src/` returns zero lines.
- The external-consumer scratch at `/tmp/km-core-consumer-smoke.ts` compiles cleanly via tsc - CORE-01's "developer can import types" criterion proven in single-machine local scope.
</verification>

<success_criteria>
- CORE-01: Full canonical type surface exported and consumable from the public barrel.
- CORE-02: GraphKMStore composition complete; parity test passes against all 4 frozen fixtures (B + C-raas + C-kpifw + C-general). CORE-02 parity gate green at the km-core fixture layer; consumer-side close (B's GraphDatabaseAdapter replacement, C's persistence.ts replacement) deferred to Phase 42 and Phase 43 respectively.
- CORE-03: Every `putEntity` stamps or validates a UUIDv7; round-trip preserves IDs unchanged.
- All in-repo tests GREEN.
- The whole library is feature-complete for v0.1; only the coding-side wiring (Plan 05) and the cross-repo `tsc --noEmit` smoke remain.
</success_criteria>

<output>
Create `.planning/phases/37-km-core-foundation/37-04-SUMMARY.md` when done. Record:
- Confirmation that all 4 fixture parity tests pass (CORE-02 SC#2 gate).
- Summary of the 7 deltas applied to GraphKMStore vs B's GraphDatabaseAdapter.
- Note any behavioral differences between KM-Core's GraphKMStore and B's/C's existing stores that consumers should know about during the upcoming migrations (e.g., "no `team` field; consumer must populate `metadata.domain` for per-domain bucketing").
- Confirm CORE-01/CORE-02/CORE-03 are all green from inside the km-core repo. Note that Plan 05 still has the cross-repo `coding/`-side import smoke + Docker wiring + symlink BC to land before phase verification.
</output>
