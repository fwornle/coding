---
phase: 44-rest-api-git-snapshots
plan: 06
type: execute
wave: 3
depends_on:
  - 44-03
  - 44-04
  - 44-05
files_modified:
  - lib/km-core/src/api/router.ts
  - lib/km-core/src/api/handlers/entities.ts
  - lib/km-core/src/api/handlers/relations.ts
  - lib/km-core/src/api/handlers/query.ts
  - lib/km-core/src/api/handlers/ontology.ts
  - lib/km-core/src/api/handlers/clusters.ts
  - lib/km-core/src/api/handlers/snapshots.ts
  - lib/km-core/src/api/index.ts
  - lib/km-core/src/intelligence/clustering.ts
  - lib/km-core/src/index.ts
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "km-core ships `createKmCoreRouter(store, router, opts)` factory that attaches 15 canonical endpoint handlers to a caller-supplied Express-like router."
    - "All handlers return `{ success: true, data }` envelope (or `{ success: false, error }` on failure) per CONTEXT C-2."
    - "Snapshot restore handler wraps SnapshotManager output with `{ restored, restartRequired: true, restartCommand: <opts.restartCommand ?? 'restart per system'>, ... }` per CONTEXT S-2 revised — handler does NOT call process.exit."
    - "Cluster handler invokes a Louvain implementation lifted INTO km-core (per CONTEXT C-3 + RESEARCH Open Q3) — km-core never depends on OKM."
    - "Root barrel exports `createKmCoreRouter`, `SnapshotManager`, `observationToLegacy`/`digestToLegacy`/`insightToLegacy`, and the Zod schemas via the relevant subpaths."
    - "Wave 0 api-router.test.ts flips RED → GREEN; all Wave 0 km-core-side stubs (4/4) are GREEN after this plan."
  artifacts:
    - path: lib/km-core/src/api/router.ts
      provides: "createKmCoreRouter factory + KmCoreRouterOptions + RouterLike interface; framework-agnostic (no import express)"
      min_lines: 100
      contains: "createKmCoreRouter"
    - path: lib/km-core/src/api/handlers/entities.ts
      provides: "GET/POST/GET-by-id/PUT/DELETE for /entities; ontologyClass filter (Pitfall 3 two-field OR-check)"
      min_lines: 80
    - path: lib/km-core/src/api/handlers/relations.ts
      provides: "POST/GET/DELETE for /relations + /relations/:key"
      min_lines: 50
    - path: lib/km-core/src/api/handlers/query.ts
      provides: "POST /query, GET /export, GET /stats"
      min_lines: 60
    - path: lib/km-core/src/api/handlers/ontology.ts
      provides: "GET /ontology/classes, /ontology/entity-types, /ontology/schema/:className"
      min_lines: 40
    - path: lib/km-core/src/api/handlers/clusters.ts
      provides: "GET /clusters?algorithm=louvain&seed=N&minSize=M"
      min_lines: 30
    - path: lib/km-core/src/api/handlers/snapshots.ts
      provides: "POST /snapshots, GET /snapshots, POST /snapshots/:id/restore with restartRequired wrapping + destructive-confirmation gate"
      min_lines: 70
    - path: lib/km-core/src/intelligence/clustering.ts
      provides: "Louvain clustering port from OKM (pure function; no OKM type deps)"
      min_lines: 50
    - path: lib/km-core/src/index.ts
      provides: "Root barrel re-export of createKmCoreRouter + SnapshotManager + observation-view + types"
      contains: "createKmCoreRouter"
  key_links:
    - from: lib/km-core/src/api/router.ts
      to: lib/km-core/src/api/handlers/*.ts
      via: "import each handler-group + attach to router via routes.push pattern"
      pattern: "import.*handlers/"
    - from: lib/km-core/src/api/handlers/snapshots.ts
      to: lib/km-core/src/snapshots/SnapshotManager.ts
      via: "constructor injection through opts.snapshotDir"
      pattern: "SnapshotManager"
    - from: lib/km-core/src/api/handlers/clusters.ts
      to: lib/km-core/src/intelligence/clustering.ts
      via: "direct import"
      pattern: "clusterEntities"
---

<objective>
Land km-core's `createKmCoreRouter` — the central factory that attaches the 15 canonical endpoint handlers to a caller-supplied Router instance. Per CONTEXT R-2 (revised) km-core does NOT `import express`; the consumer constructs `Router()` and passes it in. Handler bodies are extracted from OKM `src/api/routes.ts:450-527` per the 44-RESEARCH.md § Example 2 map; Pitfall 3 two-field OR-check (`entity.entityType !== cls && entity.ontologyClass !== cls`) is honored when filtering. Snapshots wrapper sets `restartRequired: true` (CONTEXT S-2 revised). Cluster handler uses a Louvain port LIFTED into km-core (CONTEXT C-3 + RESEARCH Open Q3) — graphology-communities-louvain dep direction enforced (km-core never depends on OKM).

Purpose: This is the keystone km-core deliverable. After this plan A/B/C consumers (Plans 07/08/09) become one-line mounts.
Output: 8 km-core source files (router + 6 handler modules + intelligence/clustering) + root barrel update. Plan 01 api-router.test.ts: GREEN. All 4 Wave 0 km-core stubs GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@lib/km-core/CLAUDE.md
@lib/km-core/dist/api/router.js
@lib/km-core/src/api/contracts.ts
@lib/km-core/src/api/index.ts
@lib/km-core/src/snapshots/SnapshotManager.ts
@lib/km-core/src/adapters/observation-view.ts
@lib/km-core/src/store/GraphKMStore.ts
@lib/km-core/src/index.ts
@lib/km-core/tests/integration/api-router.test.ts

<interfaces>
<!-- The store surface the handlers consume — already shipped by Phase 37/38. -->

From lib/km-core/src/store/GraphKMStore.ts (line numbers approximate per Phase 42 grep):
- `async findByOntologyClass(cls: string): Promise<Entity[]>` (line ~556; two-field OR-check at line 565-566 — Pitfall 3)
- `iterate(opts?: { ontologyClass?: string }): AsyncIterable<Entity>` (line ~804)
- `async putEntity(entity: Entity, opts?: { skipOntologyCheck?: boolean }): Promise<void>`
- `async getEntity(id: EntityId): Promise<Entity | undefined>`
- `async updateEntity(id, partial): Promise<void>` (via mergeAttributes)
- `async deleteEntity(id): Promise<void>`
- `async addRelation(relation): Promise<void>`
- `async listRelations(opts?): Promise<Relation[]>`
- `async deleteRelation(key): Promise<void>`
- `async exportJson(): Promise<void>` (flushes debounced exports)
- `get graph(): Graph` (the underlying Graphology graph for cluster/stats)
- `getOntologyRegistry(): OntologyRegistry | undefined`

From lib/km-core/src/ontology/registry.ts:
- `classCatalog: ReadonlyMap<string, OntologyClass>`
- `domains: ReadonlySet<string>`
- `isValidClass(name: string): boolean`

From lib/km-core/src/api/contracts.ts (Plan 03 — emit these envelopes):
- ApiSuccessEnvelope(schema) — used in handlers via res.json({success:true,data})

From lib/km-core/src/snapshots/SnapshotManager.ts (Plan 04 — already path-traversal-safe + injection-safe + destructive-gated):
- new SnapshotManager({ exportDir })
- createSnapshot(label), listSnapshots(), restoreSnapshot(id, { confirmDestructive })

From lib/km-core/src/adapters/observation-view.ts (Plan 05 — re-exported through root barrel here):
- observationToLegacy, digestToLegacy, insightToLegacy + Legacy* interfaces

From OKM routes.ts:450-527 (per 44-RESEARCH.md § Example 2 endpoint map):
- 15 canonical handler bodies — entities CRUD, relations CRUD, query, export, stats, ontology/*, graph/*, cleanup/*, clusters, snapshots/*
- Response envelope `{success:true,data}` (ApiSuccessEnvelope at rest-contract.test.ts:124)

From OKM src/intelligence/clustering.ts (per RESEARCH Open Q3 — lift the pure function; do NOT import from OKM):
- exports a function that takes a Graph + opts → returns clusters; uses graphology-communities-louvain.
- 44-RESEARCH.md confirms "lift the pure function into km-core, dropping any OKM-specific types. Verify by reading clustering.ts line-by-line before plan execution."
- Add `graphology-communities-louvain` to km-core deps in this task; do NOT depend on OKM.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author handler modules + clustering port + router factory</name>
  <files>lib/km-core/src/api/router.ts, lib/km-core/src/api/handlers/entities.ts, lib/km-core/src/api/handlers/relations.ts, lib/km-core/src/api/handlers/query.ts, lib/km-core/src/api/handlers/ontology.ts, lib/km-core/src/api/handlers/clusters.ts, lib/km-core/src/api/handlers/snapshots.ts, lib/km-core/src/intelligence/clustering.ts, lib/km-core/package.json</files>
  <read_first>
    - lib/km-core/CLAUDE.md (no-parallel-versions; verification rule — actual test pass before claiming done)
    - lib/km-core/dist/api/router.js (orphan draft — keep the routes-array structure + Express-convenience-wrapper at lines 419-436; REPLACE handler bodies with canonical extracts; REPLACE envelope `res.json(entities)` with `res.json({success:true,data:entities})`)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Example 1 (router skeleton lines 581-707), § Example 2 (endpoint map lines 711-737 — authoritative list of which OKM handler to lift for which canonical endpoint), § Pattern 1 (framework-agnostic factory lines 149-200), § Pitfall 3 (two-field OR-check)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `lib/km-core/src/api/router.ts` (lines 81-161 — pattern deviations to apply: rename, mount path, snapshot shape, cluster, envelope)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § Shared Patterns (response envelope, error handling wrapper, logging via process.stderr.write)
    - lib/km-core/src/api/contracts.ts (Plan 03 — schemas to import where useful; handlers can `.parse()` request bodies for V5 input validation per ASVS)
    - lib/km-core/src/snapshots/SnapshotManager.ts (Plan 04 — restoreSnapshot signature: `(id, { confirmDestructive })`)
    - lib/km-core/src/adapters/observation-view.ts (Plan 05 — handlers do NOT use these directly; A's server consumer (Plan 07) does)
    - lib/km-core/tests/integration/api-router.test.ts (the RED test set — read FIRST to confirm signatures + endpoint paths the test expects)
    - lib/km-core/src/store/GraphKMStore.ts (the store surface to consume; verify Pitfall 3 OR-check at line 565-566)
    - For Open Q3 (Louvain lift): the OKM file `src/intelligence/clustering.ts` is NOT locally checked out. Use `npm view graphology-communities-louvain version` to confirm package availability + standard usage shape (it's a documented graphology plugin). Implement clusterEntities(graph, opts) directly using the public API: `import louvain from 'graphology-communities-louvain'; const communities = louvain(graph, { rng, resolution }); group nodes by community id; filter by minSize`.
  </read_first>
  <behavior>
    Per Plan 01 api-router.test.ts:
    - GET /entities on empty store: 200 + `{success:true,data:[]}`
    - POST /entities valid body: 201 + envelope; subsequent GET returns it
    - GET /entities?ontologyClass=Component: filters via two-field OR-check
    - GET /stats: returns 200 + envelope with entityCount + relationCount
    - All 15 endpoints registered (no 404 on smoke probe)
    - readOnly:true gates mutating methods
    Additional behaviors required by CONTEXT/RESEARCH:
    - Snapshot restore: POST /snapshots/:id/restore with body `{ confirmDestructive: true }` returns `{success:true,data:{restored,id,commit_sha,restartRequired:true,restartCommand:<opts.restartCommand ?? null>}}`; without confirmDestructive returns 400.
    - Cluster: GET /clusters?algorithm=louvain&seed=42&minSize=3 returns 200 + `{success:true,data:[{communityId, size, members:[...]}]}` (deterministic given seed; minSize filters).
    - Path-traversal already enforced at SnapshotManager level — handler does no additional sanitization (manager throws on bad ids, handler maps to 400).
    - Error responses use `{success:false,error:<safe-message>}` (V7 control); no stack traces.
  </behavior>
  <action>Step 1 — add `graphology-communities-louvain` to km-core dependencies via `cd lib/km-core && npm install graphology-communities-louvain --save`; verify with `grep graphology-communities-louvain lib/km-core/package.json`.

Step 2 — author `lib/km-core/src/intelligence/clustering.ts`: `import louvain from 'graphology-communities-louvain'; import type { GraphKMStore } from '../store/GraphKMStore.js';`. Export `clusterEntities(graph: Graph, opts: { algorithm?: 'louvain', seed?: number, minSize?: number }): Array<{communityId:string|number,size:number,members:string[]}>`. Implementation: call `louvain(graph, { rng: opts.seed != null ? seededRng(opts.seed) : Math.random, resolution: 1.0 })`; group by communityId; filter by minSize. Include a `seededRng(seed)` helper (mulberry32 PRNG — 6-line standard impl). Pure synchronous function. No console.log.

Step 3 — author each handler module under `lib/km-core/src/api/handlers/`:
  - `entities.ts`: exports `entityRoutes(store, opts)` returning a routes array `[{method, path, handler}, ...]`. Routes: GET /entities (with ontologyClass + limit + offset query params via Pitfall 3 OR-check; falls through to store.iterate for unfiltered), POST /entities (mintEntityId via existing helper if id absent; validate `entityType` present; envelope), GET /entities/:id, PUT /entities/:id, DELETE /entities/:id. Mutating routes only added when `!opts.readOnly`.
  - `relations.ts`: exports `relationRoutes(store, opts)`. Routes: GET /relations (with from/to/relationType query filters), POST /relations (validate from+to+relationType present), DELETE /relations/:key. Mutating gated.
  - `query.ts`: exports `queryRoutes(store, opts)`. Routes: POST /query (body `{ontologyClass?:string,filters?:object,limit?:number}`; returns matching entities), GET /export (returns full graph dump as `{entities:[...],relations:[...]}` envelope), GET /stats (returns `{entityCount:graph.order, relationCount:graph.size, ontologyClasses: opts.ontologyRegistry?.classCatalog.size ?? 0, domainsActive: Array.from(opts.ontologyRegistry?.domains ?? [])}`).
  - `ontology.ts`: exports `ontologyRoutes(opts)`. Routes: GET /ontology/classes (returns `Array.from(opts.ontologyRegistry?.classCatalog.values() ?? [])`), GET /ontology/entity-types (unique entityType strings across catalog), GET /ontology/schema/:className (single class lookup). All read-only.
  - `clusters.ts`: exports `clusterRoutes(store)`. Single route GET /clusters. Imports `clusterEntities` from intelligence module. Reads query params seed/minSize/algorithm.
  - `snapshots.ts`: exports `snapshotRoutes(store, opts)`. Routes: POST /snapshots (calls `await store.exportJson()` first, then `snapshotMgr.createSnapshot(label)`; envelope), GET /snapshots (snapshotMgr.listSnapshots), POST /snapshots/:id/restore (validates `req.body.confirmDestructive === true` → maps to SnapshotManager.restoreSnapshot(id, {confirmDestructive:true}) → wraps result with `{...restoreResult, restartRequired: true, restartCommand: opts.restartCommand ?? null}`). 400 when confirmDestructive missing/false.

Step 4 — author `lib/km-core/src/api/router.ts`: keep the framework-agnostic pattern from the orphan draft + the createKMRoutes/createKMRouter split (RESEARCH § Pattern 1 lines 149-200 and PATTERNS.md lines 104-149 verbatim). Two exports: `createKMRoutes(store, opts)` returns routes-array (called internally), and `createKmCoreRouter(store, router, opts)` is the public surface that calls createKMRoutes + attaches via the error-handling wrapper from PATTERNS.md lines 130-149. Compose all 6 handler-route arrays inside createKMRoutes via spread: `return [...entityRoutes(store,opts), ...relationRoutes(store,opts), ...queryRoutes(store,opts), ...ontologyRoutes(opts), ...clusterRoutes(store), ...snapshotRoutes(store,opts)]`. KmCoreRouterOptions: `{ ontologyRegistry?, snapshotDir?, enableSnapshots?, readOnly?, restartCommand? }`. Diagnostic emission via `process.stderr.write` (PATTERNS.md Shared Patterns) — only on error paths.

Step 5 — update `lib/km-core/src/api/index.ts` to also re-export `createKmCoreRouter` + types: `export { createKmCoreRouter, createKMRoutes } from './router.js'; export type { KmCoreRouterOptions, RouterLike } from './router.js';` (alongside the existing contracts re-export from Plan 03).

Step 6 — update `lib/km-core/src/index.ts` (root barrel) per 44-PATTERNS.md lines 537-567 verbatim — add Phase 44 export block after Plan 38's block: re-export createKmCoreRouter, SnapshotManager, observationToLegacy/digestToLegacy/insightToLegacy + types.

Build via `cd lib/km-core && npm run build`. Verify Plan 01 api-router.test.ts: GREEN (6/6). Full km-core suite: no regression.

File names: ALL exactly as listed; no version/enhanced suffixes (km-core CLAUDE.md no-evolutionary-names rule).</action>
  <verify>
    <automated>cd lib/km-core && npm run build 2>&1 | tail -5 && npx vitest run tests/integration/api-router.test.ts</automated>
  </verify>
  <done>
    All 8 source files exist with the line-count minimums. createKmCoreRouter exported from root barrel + ./api subpath. Plan 01 api-router.test.ts: GREEN (6/6). All 4 km-core Wave 0 stubs: GREEN. Full km-core suite: no regression. graphology-communities-louvain in deps.
  </done>
  <acceptance_criteria>
    - All 8 new files exist: `for f in lib/km-core/src/api/router.ts lib/km-core/src/api/handlers/entities.ts lib/km-core/src/api/handlers/relations.ts lib/km-core/src/api/handlers/query.ts lib/km-core/src/api/handlers/ontology.ts lib/km-core/src/api/handlers/clusters.ts lib/km-core/src/api/handlers/snapshots.ts lib/km-core/src/intelligence/clustering.ts; do test -f "$f" || (echo "MISSING: $f"; false); done && echo ALL_PRESENT`
    - Router line count: `wc -l lib/km-core/src/api/router.ts | awk '{print $1}'` >= 100
    - `grep -c "export function createKmCoreRouter" lib/km-core/src/api/router.ts` returns 1
    - `grep -c "export function createKMRoutes" lib/km-core/src/api/router.ts` returns 1
    - `grep -cE "import\\s+express" lib/km-core/src/api/router.ts lib/km-core/src/api/handlers/*.ts` returns 0 (R-2 — km-core stays Express-free)
    - `grep -c "graphology-communities-louvain" lib/km-core/package.json` returns 1
    - `grep -c "graphology-communities-louvain" lib/km-core/src/intelligence/clustering.ts` returns 1
    - `grep -c "entityType !== cls && entity.ontologyClass !== cls\\|entityType !== ontologyClass && .*ontologyClass !== ontologyClass" lib/km-core/src/api/handlers/entities.ts` >= 1 (Pitfall 3 OR-check)
    - `grep -c "restartRequired: true\\|restartRequired:true" lib/km-core/src/api/handlers/snapshots.ts` returns 1
    - `grep -c "confirmDestructive" lib/km-core/src/api/handlers/snapshots.ts` >= 2
    - `grep -cE "(process\\.exit|process\\.kill)" lib/km-core/src/api/handlers/snapshots.ts` returns 0 (CONTEXT S-2 revised — NO process.exit)
    - `grep -cE "success:\\s*true" lib/km-core/src/api/handlers/entities.ts lib/km-core/src/api/handlers/relations.ts lib/km-core/src/api/handlers/query.ts lib/km-core/src/api/handlers/ontology.ts lib/km-core/src/api/handlers/clusters.ts lib/km-core/src/api/handlers/snapshots.ts | head -1 | awk -F: '{print $2}'` >= 6 (every handler module uses the success envelope at least once)
    - `grep -cE "success:\\s*false" lib/km-core/src/api/handlers/entities.ts lib/km-core/src/api/handlers/relations.ts lib/km-core/src/api/handlers/query.ts lib/km-core/src/api/handlers/ontology.ts lib/km-core/src/api/handlers/clusters.ts lib/km-core/src/api/handlers/snapshots.ts lib/km-core/src/api/router.ts | head -1 | awk -F: '{print $2}'` >= 1 (error envelope used at least somewhere — typically the wrapper)
    - `grep -cE "console\\.(log|error|warn|info|debug)" lib/km-core/src/api/router.ts lib/km-core/src/api/handlers/*.ts lib/km-core/src/intelligence/clustering.ts` returns 0 (no-console-log rule)
    - `grep -c "createKmCoreRouter" lib/km-core/src/index.ts` >= 1 (root barrel updated)
    - `grep -c "SnapshotManager" lib/km-core/src/index.ts` >= 1
    - `grep -c "observationToLegacy" lib/km-core/src/index.ts` >= 1
    - `cd lib/km-core && npm run build 2>&1 | grep -cE "error TS"` returns 0
    - `cd lib/km-core && npx vitest run tests/integration/api-router.test.ts 2>&1 | grep -cE "(Tests\\s+\\d+\\s+passed|all passing)"` >= 1 (6/6 GREEN)
    - `cd lib/km-core && npm test 2>&1 | tail -10 | grep -cE "(Tests Files\\s+\\d+\\s+passed|Test Files\\s+\\d+\\s+passed)"` >= 1 (full suite passes; no regression)
    - All 4 Wave 0 km-core stubs GREEN: `cd lib/km-core && npx vitest run tests/integration/api-router.test.ts tests/unit/contracts.test.ts tests/integration/snapshot-roundtrip.test.ts tests/integration/observation-view.test.ts 2>&1 | tail -5 | grep -cE "(4 passed|passed.*0 failed)"` >= 1
    - No evolutionary file names: `find lib/km-core/src/api lib/km-core/src/intelligence -name '*-v[0-9]*' -o -name '*-enhanced*' -o -name '*-new*' -o -name '*-fixed*'` returns empty
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → router → handler | All path params (entity id, relation key, snapshot id, ontology class name) cross trust boundary; validate or pass to layer that validates |
| handler → SnapshotManager.restoreSnapshot | destructive op — handler MUST require `confirmDestructive:true` in body BEFORE delegating |
| handler → louvain library | clustering takes graph as input (server-side construct); no user-controlled tampering surface beyond seed/minSize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06-01 | Tampering / V12 File and Resources | POST /snapshots/:id/restore destructive op | mitigate | Handler-side gate: body must include `confirmDestructive:true` → 400 otherwise. SnapshotManager also enforces (Plan 04 T-44-04-03). Two layers of defense |
| T-44-06-02 | Tampering / V12 | restore path traversal via id | mitigate | SnapshotManager regex (Plan 04 T-44-04-01) rejects before git invocation; handler catches the thrown Error and returns 400 |
| T-44-06-03 | Information Disclosure / V7 | Stack-trace leak in error responses | mitigate | error wrapper at router.ts (PATTERNS.md lines 130-149) returns `{success:false,error: err.message}` ONLY — never `err.stack` |
| T-44-06-04 | Denial of Service / V5 | iterate() on huge graphs in GET /entities (no limit) | mitigate | Default LIMIT applied when query.limit absent and count > 1000 — handler sets `effectiveLimit = limit || 1000`; honors caller's higher limit when explicit |
| T-44-06-05 | Spoofing / V4 | No auth on /snapshots/* mutating routes | accept | internal-only per CONTEXT out-of-scope (auth) — Phase 45+ may add. Documented |
| T-44-06-SC | Tampering | `graphology-communities-louvain` install | mitigate | Documented graphology plugin (same author org as graphology); MEDIUM-trust per ecosystem inspection. RESEARCH § Open Q3 explicitly endorses lift over OKM-dependency. Confirm via `npm view graphology-communities-louvain` before install for currency check (acceptance assertions verify presence in package.json). NOTE: Plan executor MUST run `npm view graphology-communities-louvain` BEFORE install and refuse to proceed if downloads/week < 5000 — graceful-degradation legitimacy check |
</threat_model>

<verification>
```bash
cd lib/km-core && npm run build
cd lib/km-core && npx vitest run tests/integration/api-router.test.ts
cd lib/km-core && npm test                # full suite, no regression
```
Expected: build clean; api-router 6/6 GREEN; full suite all-green; all 4 Wave 0 km-core stubs GREEN (sets the stage for Plans 07/08/09 cross-system mounts).
</verification>

<success_criteria>
- All 15 canonical endpoints registered via createKmCoreRouter
- Framework-agnostic (zero express imports in km-core source)
- Snapshot restore wraps SnapshotManager output with restartRequired/restartCommand; destructive-confirmation enforced at handler layer
- Louvain clustering ported to km-core (no OKM dep direction violation)
- Root barrel re-exports the keystone surfaces
- All 4 Wave 0 km-core stubs GREEN; no regression
- All security mitigations (path-traversal at manager, destructive-gate at handler, no stack-trace leak, default LIMIT) in place
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-06-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) Wave 0 km-core scoreboard (4/4 GREEN), (b) all 15 endpoints' registered list (one line each), (c) restartRequired wrapping snippet, (d) Louvain provider chosen (graphology-communities-louvain version), (e) build + full-suite result lines.
</output>
