---
phase: 44-rest-api-git-snapshots
plan: 09
type: execute
wave: 4
depends_on:
  - 44-06
files_modified:
  - _work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts
  - _work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts
  - _work/rapid-automations/integrations/operational-knowledge-management/tests/integration/rest-contract.test.ts
  - _work/rapid-automations/integrations/operational-knowledge-management/scripts/record-rest-fixtures.mjs
  - _work/rapid-automations/integrations/operational-knowledge-management/tests/fixtures/pre-migration
  - _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/api/okbClient.ts
  - _work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs
  - _work/rapid-automations/integrations/operational-knowledge-management/lib/km-core/vendor
  - _work/rapid-automations/integrations/operational-knowledge-management/package.json
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "OKM's `src/api/routes.ts` no longer defines the 15 canonical endpoints in-line — they are delegated to `createKmCoreRouter` mounted at /api/v1; the OKM-specific ~15 ops (PII scan, ingest, RCA, source-docs, etc.) stay under /api/okm/*."
    - "OKM's `tests/integration/rest-contract.test.ts` IMPORTS Zod schemas from `@fwornle/km-core/api/contracts` instead of re-declaring them — single source of truth lock."
    - "All ~20 URL constants in `viewer/src/api/okbClient.ts` for the 15 canonical endpoints are rewritten from `/api/*` to `/api/v1/*` (R-4 hard cutover); OKM-specific endpoints (RCA, source-documents, governance, etc.) keep `/api/*` (now mounted at /api/okm/*)."
    - "`scripts/verify-post-migration.mjs` URL rewrites match the viewer changes."
    - "`scripts/record-rest-fixtures.mjs` ENDPOINTS array rewritten to /api/v1/* paths; fixture JSON files re-recorded against the new mount; byte-equal diff against pre-existing fixture files (response BODIES are URL-independent per Pitfall 5)."
    - "OKM's vendored km-core .tgz is bumped to the Phase 44 build (`@fwornle/km-core@0.1.0` with /api/* subpaths exported)."
    - "Cross-repo per CLAUDE.md feedback_bmw_ghe_https.md: HTTPS-token auth used for the OKM clone + push."
    - "Plan 02 cross-system-parity.mjs C-leg flips from `fetch failed` to 200 + envelope; all 3 legs now GREEN."
  artifacts:
    - path: _work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts
      provides: "Canonical 15 endpoint handler bodies DELETED; mount block `app.use('/api/v1', createKmCoreRouter(...))` inserted; OKM-specific routes refactored under /api/okm/*"
    - path: _work/rapid-automations/integrations/operational-knowledge-management/tests/integration/rest-contract.test.ts
      provides: "Schemas imported from km-core (single source of truth); request URLs rewritten to /api/v1/*"
    - path: _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/api/okbClient.ts
      provides: "20 canonical URL rewrites from /api/* to /api/v1/*; OKM-specific URLs unchanged (now under /api/okm/*)"
    - path: _work/rapid-automations/integrations/operational-knowledge-management/scripts/record-rest-fixtures.mjs
      provides: "ENDPOINTS array updated; fixture files re-recorded byte-equal"
    - path: _work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs
      provides: "URL rewrites mirroring the viewer + recorder"
    - path: _work/rapid-automations/integrations/operational-knowledge-management/lib/km-core/vendor
      provides: "Fresh fwornle-km-core-0.1.0.tgz bumped from Phase 44 build"
  key_links:
    - from: _work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts
      to: "@fwornle/km-core (vendored .tgz)"
      via: "app.use('/api/v1', createKmCoreRouter(...))"
      pattern: "createKmCoreRouter"
    - from: _work/rapid-automations/integrations/operational-knowledge-management/tests/integration/rest-contract.test.ts
      to: "@fwornle/km-core/api/contracts"
      via: "import { EntitySchema } from '@fwornle/km-core/api/contracts'"
      pattern: "from '@fwornle/km-core/api/contracts'"
---

<objective>
C-side hard cutover: replace OKM's in-line canonical 15 endpoint handlers with the `createKmCoreRouter` mount; refactor OKM-specific operations (~15 endpoints — PII scan, ingest, RCA, source-docs, governance, /llm/*, /api/patterns/trending, /api/analyze/correlations, /api/proxy/raas-job/:uuid) under `/api/okm/*`; rewrite ALL viewer URL constants (~20 sites in `viewer/src/api/okbClient.ts`) + `scripts/verify-post-migration.mjs` + `scripts/record-rest-fixtures.mjs` ENDPOINTS array from `/api/*` to `/api/v1/*` for the canonical 15; lift Zod schemas in `tests/integration/rest-contract.test.ts` from local declarations to `@fwornle/km-core/api/contracts` imports; re-record fixture JSON files (byte-equal expected per Pitfall 5 — response bodies are URL-independent).

OKM lives in a SEPARATE git repo on bmw.ghe.com — per CLAUDE.md feedback_bmw_ghe_https.md, use HTTPS-token auth (`gh` CLI) for clone + push. Per Phase 43 lessons, OKM is the de-facto template for the wire format — the lift IS the contract definition; this plan simultaneously proves the contract and the migration.

Per CLAUDE.md `feedback_worktree_verification.md`: verify "complete" claims against `main`, NOT worktree branches.

Purpose: Cross-system parity completes after this plan (all three legs of cross-system-parity.mjs GREEN). Phase 45's unified viewer can now target any backend identically.
Output: 7 OKM files edited + fixture re-recording + vendored km-core .tgz bump. OKM full suite (vitest) GREEN. Plan 02 cross-system-parity.mjs C-leg GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@CLAUDE.md
@lib/km-core/CLAUDE.md

<interfaces>
<!-- OKM is NOT locally checked out. Use HTTPS-token gh CLI to clone before editing. -->

From 44-RESEARCH.md § Example 2 endpoint map (lines 711-737 — authoritative canonical-vs-OKM-specific split):
- Canonical (move to km-core router via /api/v1/*): GET/POST/PUT/DELETE /entities (+ /entities/:id), POST/GET/DELETE /relations (+ /relations/:key), POST /query, GET /export, GET /stats, GET /ontology/{classes,entity-types,schema/:className}, GET /graph/{connectivity,orphans}, POST /cleanup/{relations-by-type,deduplicate-edges,resolve-entities}, DELETE /cleanup/orphans, GET /clusters, GET /search, POST /snapshots, GET /snapshots, POST /snapshots/:id/restore
- OKM-specific (keep at /api/okm/*): GET /cleanup/pii-scan, DELETE /cleanup/pii-entities, DELETE /purge, POST /ingest, POST /ingest/batch, POST /synthesize, GET/PUT /llm/settings, GET /llm/metrics, GET /llm/history, GET /ingest/progress (SSE), POST /rca/ingest, POST /rca/lookup, GET /rca/dirs, GET /rca/status, GET /source-documents/:runId, GET /source-documents, GET /api/patterns/trending, POST /api/analyze/correlations, GET /api/confidence/*, GET /api/proxy/raas-job/:uuid

From 44-RESEARCH.md § Runtime State Inventory line 498:
- viewer/src/api/okbClient.ts URL constants at lines 225, 271, 282, 291, 299, 317, 326, 335, 349, 359, 383, 400, 415, 425, 468, 475, 480, 485, 497, 508, 519 — ~20 sites; rewrite ONLY the canonical ones to /api/v1/; OKM-specific URLs become /api/okm/

From 44-RESEARCH.md § Pitfall 5:
- Fixture files are byte-snapshots of response bodies; bodies are URL-independent; only recorder + test request URLs change; fixture JSON files themselves should diff to ZERO bytes against pre-existing fixtures (sanity-check this AFTER re-recording — any non-zero diff is unexpected and must be investigated before lock)

From 44-PATTERNS.md § C-side OKM table (lines 64-76):
- Per-file modification matrix + per-file research-line references

From Phase 43 SUMMARY (43-10-SUMMARY.md per ROADMAP line 609):
- Gate 1 (Zod fixtures-diff) has been the canonical wire-format proof since Phase 43 closed; this plan migrates that test from in-tree schemas to km-core-imported schemas; same diff result expected

From CLAUDE.md feedback_bmw_ghe_https.md:
- Use HTTPS for all bmw.ghe.com pushes; do not rewrite stored remotes (clone fresh into _work/rapid-automations if needed)

From CLAUDE.md feedback_worktree_verification.md:
- Cross-repo "complete" claims must be verified against main, NOT worktree branches; this plan's smoke tests run against the merged main state in OKM

From CLAUDE.md Submodules & Build Pipeline (per-repo rule for OKM):
- km-core build → repack .tgz → vendor into OKM (lib/km-core/vendor/) → bump OKM's package.json km-core version → OKM rebuild → OKM test suite

From CLAUDE.md `km-core scripts` mandatory rule:
- ALL GraphKMStore construction MUST pass ontologyDir (Phase 41 lesson); verify OKM's existing construction does so or fix in this plan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone OKM via HTTPS; bump vendored km-core; refactor src/api/routes.ts to mount createKmCoreRouter + per-system sub-router</name>
  <files>_work/rapid-automations/integrations/operational-knowledge-management/lib/km-core/vendor, _work/rapid-automations/integrations/operational-knowledge-management/package.json, _work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts, _work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts</files>
  <read_first>
    - lib/km-core/CLAUDE.md (no-parallel-versions; verification rule)
    - CLAUDE.md feedback_bmw_ghe_https.md (HTTPS-token auth for bmw.ghe.com); feedback_worktree_verification.md (verify against main)
    - CLAUDE.md Submodules & Build Pipeline (per-repo km-core repack+vendor sequence for OKM)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Example 2 (authoritative endpoint split — canonical vs OKM-specific)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § C-side OKM table (per-file modification matrix)
    - After cloning OKM: read `src/api/routes.ts` lines 450-527 (canonical handler bodies — these are the SOURCE we LIFTED INTO km-core via Plan 06; here we DELETE the in-line definitions and replace with the mount) AND lines 528-end (OKM-specific routes — these stay; refactor into a createOkmSpecificRouter() factory mounted at /api/okm/*)
    - Read `src/api/server.ts` (the createServer/express bootstrap — confirms where the mount goes; also the place to add the bind-mount for `/api/okm`)
  </read_first>
  <behavior>
    - OKM is cloned into _work/rapid-automations via gh CLI (`gh repo clone <bmw.ghe.com>/rapid-automations _work/rapid-automations` if absent; OR pull-rebase main if present). All work on a feature branch like `feat/phase-44-api-cutover` to be pushed via PR.
    - `cd lib/km-core && npm pack` (in coding repo) produces fwornle-km-core-0.1.0.tgz with the Plan 06 build (createKmCoreRouter + api/contracts + snapshots + observation-view sub-paths). Copy into OKM's `lib/km-core/vendor/`. Bump OKM's `package.json` km-core entry pin to point at the new .tgz (typically `"@fwornle/km-core": "file:lib/km-core/vendor/fwornle-km-core-0.1.0.tgz"`). Run `npm install` in OKM to materialize node_modules.
    - `src/api/routes.ts` is REFACTORED: DELETE the 15 canonical endpoint handler bodies in-line (the same handlers that became km-core's). REPLACE with `app.use('/api/v1', createKmCoreRouter(this.graphStore, kmRouter, { ontologyRegistry: this.graphStore.getOntologyRegistry?.(), snapshotDir: <OKM_EXPORTS_DIR>, restartCommand: 'OKM restart per operator runbook' }))` AND create a sibling factory `createOkmSpecificRouter(graphStore, ...)` that defines the OKM-specific ~15 handlers (PII, ingest, RCA, source-docs, governance, /llm/*) — mounted at `/api/okm/*`. The mount block lives in `src/api/server.ts`.
    - `src/api/server.ts` createServer signature simplifies: after the mount lands, the `routes.ts` exports a `mountOkmSpecific(app)` plus a `mountKmCoreCanonical(app, store)` rather than a big single mountAll. server.ts orchestrates.
    - After build + restart OKM (per OKM's existing dev script — typically `npm run dev` or via docker-compose if containerized — verify in OKM's README/package.json scripts), HTTP probes work:
      - `curl -s http://localhost:3002/api/v1/stats` returns 200 + envelope (canonical via km-core)
      - `curl -s http://localhost:3002/api/v1/entities?limit=1` returns 200 + envelope
      - `curl -s http://localhost:3002/api/okm/source-documents?limit=1` returns 200 (OKM-specific still mounted)
      - `curl -i http://localhost:3002/api/entities` returns 404 (R-4 hard cutover — pre-v1 path gone for canonical; if OKM-specific routes overlap, keep /api/okm/* path)
    - Per CLAUDE.md feedback_worktree_verification.md: confirm the mount works against main after merge — NOT on a worktree.
  </behavior>
  <action>Step 1 — OKM working copy. If `_work/rapid-automations` exists, `cd _work/rapid-automations && git fetch origin && git checkout main && git pull --rebase origin main`. If missing, `gh repo clone <bmw.ghe.com org>/rapid-automations _work/rapid-automations` (per the user's HTTPS-token convention; if `gh` CLI fails on bmw.ghe.com, fall back to HTTPS-with-PAT URL — `git clone https://<user>:<token>@bmw.ghe.com/<org>/rapid-automations.git _work/rapid-automations`). Create branch: `git checkout -b feat/phase-44-api-cutover`.
    Step 2 — Repack km-core: `cd /Users/Q284340/Agentic/coding/lib/km-core && npm pack` (produces fwornle-km-core-0.1.0.tgz). Copy: `cp fwornle-km-core-0.1.0.tgz /Users/Q284340/Agentic/coding/_work/rapid-automations/integrations/operational-knowledge-management/lib/km-core/vendor/` (overwriting the existing .tgz; verify with `ls -la _work/.../okm/lib/km-core/vendor/`).
    Step 3 — OKM dep install: `cd _work/rapid-automations/integrations/operational-knowledge-management && npm install` (npm picks up the new .tgz via the package.json file: spec).
    Step 4 — Refactor `src/api/routes.ts`. With OKM checked out, run `grep -n "router\\.\\(get\\|post\\|put\\|delete\\)" src/api/routes.ts | head -40` to enumerate ALL routes. Categorize each per the canonical/OKM-specific split from <interfaces>. DELETE the 15 canonical handler bodies. EXTRACT the OKM-specific ~15 handler bodies into a new `createOkmSpecificRouter(store, opts): RouterLike` factory function inside the same file (mirror the km-core factory pattern). Export both the legacy mount function (now thinned) AND the new factory.
    Step 5 — Refactor `src/api/server.ts`. Import `createKmCoreRouter` and the new `createOkmSpecificRouter`. In the createServer body, instantiate kmRouter + okmRouter via `express.Router()`, attach via the factories, and mount: `app.use('/api/v1', kmRouter); app.use('/api/okm', okmRouter);`. Verify ontologyDir is passed to whatever GraphKMStore is constructed in OKM's bootstrap (per CLAUDE.md mandatory rule); if absent, fix here.
    Step 6 — Honor lib/km-core/CLAUDE.md no-parallel-versions: file names stay `routes.ts` and `server.ts` (no -v2, -refactored, -new suffixes).
    Step 7 — Build OKM: `cd _work/rapid-automations/integrations/operational-knowledge-management && npm run build` (per OKM's package.json scripts — verify command).
    Step 8 — Start OKM (or rebuild OKM container if dockerized — verify in OKM's docker-compose or run scripts). Health probe `/api/v1/stats` AND `/api/okm/source-documents` per <behavior>.
    Step 9 — Document the topology: outer-rapid-automations gitlink bump + OKM submodule commit pair (Phase 43 D-G3 precedent — outer is `_work/rapid-automations` checkout; OKM is `_work/rapid-automations/integrations/operational-knowledge-management` checkout; both are git repos).</action>
  <verify>
    <automated>cd _work/rapid-automations/integrations/operational-knowledge-management && npm run build 2>&1 | tail -5 && curl -s -o /tmp/c-v1-stats.json -w "%{http_code}\n" http://localhost:3002/api/v1/stats | head -1 | awk '{exit ($1=="200")?0:1}' && grep -c '"success":true' /tmp/c-v1-stats.json && curl -s -o /dev/null -w "%{http_code}" http://localhost:3002/api/okm/source-documents | awk '{exit ($1=="200")?0:1}'</automated>
  </verify>
  <done>
    OKM clones via HTTPS-token; km-core .tgz refreshed; npm install clean; routes.ts canonical handlers deleted + OKM-specific refactored under /api/okm/*; server.ts mounts both; build clean; /api/v1/stats 200 + envelope; /api/okm/* still serves OKM-specific ops.
  </done>
  <acceptance_criteria>
    - `test -d _work/rapid-automations/integrations/operational-knowledge-management/.git || test -f _work/rapid-automations/integrations/operational-knowledge-management/.git` (OKM checked out as either dir-repo or gitlink)
    - `ls -la _work/rapid-automations/integrations/operational-knowledge-management/lib/km-core/vendor/fwornle-km-core-0.1.0.tgz` (vendored .tgz exists)
    - `grep -c "createKmCoreRouter" _work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts` >= 1
    - `grep -c "createKmCoreRouter\\|createOkmSpecificRouter" _work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts` >= 1
    - `grep -c "/api/v1" _work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts` >= 1
    - `grep -c "/api/okm" _work/rapid-automations/integrations/operational-knowledge-management/src/api/server.ts` >= 1
    - `grep -cE "^\\s*router\\.(get|post|put|delete)\\(\\s*'/entities'" _work/rapid-automations/integrations/operational-knowledge-management/src/api/routes.ts` returns 0 (canonical handler bodies REMOVED — no in-line `/entities` route)
    - `cd _work/rapid-automations/integrations/operational-knowledge-management && npm run build 2>&1 | grep -cE "error TS"` returns 0
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3002/api/v1/stats` returns `200`
    - `curl -s http://localhost:3002/api/v1/stats | grep -c '"success":true'` returns 1
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3002/api/okm/source-documents?limit=1` returns `200` (OKM-specific endpoint still works)
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3002/api/entities` returns `404` (R-4 hard cutover — pre-v1 canonical path gone)
    - File names unchanged: `find _work/rapid-automations/integrations/operational-knowledge-management/src/api -name '*-v[0-9]*' -o -name '*-enhanced*' -o -name '*-new*'` returns empty
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite viewer + scripts URL constants (/api/* → /api/v1/* for canonical); regenerate fixtures; migrate rest-contract.test.ts to import schemas from km-core</name>
  <files>_work/rapid-automations/integrations/operational-knowledge-management/viewer/src/api/okbClient.ts, _work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs, _work/rapid-automations/integrations/operational-knowledge-management/scripts/record-rest-fixtures.mjs, _work/rapid-automations/integrations/operational-knowledge-management/tests/fixtures/pre-migration, _work/rapid-automations/integrations/operational-knowledge-management/tests/integration/rest-contract.test.ts</files>
  <read_first>
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pitfall 5 (fixture re-recording — byte-equal expected against pre-existing files; if non-zero diff appears, INVESTIGATE before lock — some response field unexpectedly depends on URL is a bug)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Runtime State Inventory line 498 (viewer URL constants at lines 225, 271, 282, 291, 299, 317, 326, 335, 349, 359, 383, 400, 415, 425, 468, 475, 480, 485, 497, 508, 519 — the 20 sites; identify canonical vs OKM-specific per Example 2 endpoint map)
    - After OKM checkout: read `viewer/src/api/okbClient.ts` to confirm the 20 line numbers + understand the URL templating pattern (is it a `const API_PREFIX = '/api'; ... fetch(\`${API_PREFIX}/entities\`)` style, or hardcoded paths? — affects edit strategy)
    - Read `scripts/record-rest-fixtures.mjs` to identify the ENDPOINTS array
    - Read `tests/integration/rest-contract.test.ts` to identify the Zod schema declarations (currently in-tree per Phase 43 SUMMARY — replace with imports from `@fwornle/km-core/api/contracts`)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § C-side OKM table for cross-references
  </read_first>
  <behavior>
    - All ~20 URL constants in `viewer/src/api/okbClient.ts` for CANONICAL endpoints (entities, relations, query, stats, ontology/*, graph/*, cleanup/{resolve-entities,deduplicate-edges,orphans,relations-by-type}, clusters, search, snapshots/*) are rewritten from `/api/*` to `/api/v1/*`.
    - OKM-specific endpoint URLs (PII, ingest, RCA, source-documents, governance, /llm/*, /api/patterns/trending, /api/analyze/correlations, /api/proxy/raas-job/*) — the URL prefix `/api/` BECOMES `/api/okm/` to match the mount in Task 1.
    - `scripts/verify-post-migration.mjs` URL rewrites mirror the viewer.
    - `scripts/record-rest-fixtures.mjs` ENDPOINTS array updated to canonical /api/v1/* + OKM-specific /api/okm/*. Re-run recorder. Diff against pre-existing fixture JSON files: byte-equal expected (Pitfall 5 — response bodies are URL-independent). If non-zero diff appears, INVESTIGATE before lock.
    - `tests/integration/rest-contract.test.ts` removes local Zod schema declarations (lines 94-167 per RESEARCH); replaces with `import { EntitySchema, RelationSchema, ApiSuccessEnvelope, OntologyClassSchema, StatsSchema } from '@fwornle/km-core/api/contracts';`. Request URLs in the test rewritten from /api/ to /api/v1/ for canonical endpoints. Asserts EXACT same diff result as the pre-existing fixture lock.
    - Phase 43 Gate 1 (Zod fixtures-diff) lock: still GREEN after schemas are imported from km-core instead of in-tree.
    - OKM full test suite (`cd _work/.../okm && npm test`) GREEN.
    - VOKB viewer renders unchanged (manual verification deferred to Plan 11 via gsd-browser).
  </behavior>
  <action>Step 1 — viewer URL rewrite. `cd _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/api/` and `grep -n "/api/" okbClient.ts | head -25` to enumerate. Categorize each URL per Example 2 endpoint map. For CANONICAL URLs, replace `/api/` prefix with `/api/v1/`. For OKM-SPECIFIC URLs, replace `/api/` prefix with `/api/okm/`. Preserve query strings + path params; do NOT change URL paths post-prefix.
    Step 2 — script URL rewrites. Apply the same canonical-vs-OKM-specific categorization to `scripts/verify-post-migration.mjs` and `scripts/record-rest-fixtures.mjs` ENDPOINTS array.
    Step 3 — re-record fixtures. `cd _work/.../okm && node scripts/record-rest-fixtures.mjs` (or whatever the recorder's invocation is — verify in OKM's README/scripts). Diff: `git diff tests/fixtures/pre-migration/*.json | wc -l` should be 0 (byte-equal per Pitfall 5). If non-zero, manually review each diff; expected differences ONLY if a response body included an absolute URL field (unexpected; investigate before locking).
    Step 4 — rest-contract.test.ts migration. DELETE the local Zod schema declarations at lines 94-167. INSERT `import { EntitySchema, RelationSchema, OntologyClassSchema, StatsSchema, ApiSuccessEnvelope } from '@fwornle/km-core/api/contracts';` at the top. Rewrite the supertest request URLs in the test bodies from /api/ to /api/v1/ for canonical endpoints. Run `cd _work/.../okm && npx vitest run tests/integration/rest-contract.test.ts` — must be GREEN (Phase 43 Gate 1 lock preserved with the schema source-of-truth swap).
    Step 5 — full OKM suite. `cd _work/.../okm && npm test` — all GREEN.
    Step 6 — commit OKM submodule + outer rapid-automations gitlink bump per Phase 43 D-G3 precedent. Use HTTPS-token auth for both pushes (CLAUDE.md feedback_bmw_ghe_https.md).
    Step 7 — verify against main per CLAUDE.md feedback_worktree_verification.md: after merge to main, re-run the full test suite to confirm green on main (no stranded worktree state).
    All file names unchanged — no version suffixes.</action>
  <verify>
    <automated>cd _work/rapid-automations/integrations/operational-knowledge-management && npm test 2>&1 | tail -10 | grep -cE "(passed|0 failing|Tests Files\\s+\\d+\\s+passed)"</automated>
  </verify>
  <done>
    Viewer URLs rewritten (canonical → /api/v1/, OKM-specific → /api/okm/); recorder URLs rewritten; fixtures byte-equal after re-record; rest-contract.test.ts imports schemas from km-core + uses /api/v1/ URLs; OKM full suite GREEN; per CLAUDE.md verified against main.
  </done>
  <acceptance_criteria>
    - `grep -cE "/api/v1/(entities|relations|query|stats|ontology|graph|cleanup|clusters|search|snapshots)" _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/api/okbClient.ts` >= 10 (canonical URLs rewritten — minimum 10 of the 15 endpoint paths show up across the 20 sites)
    - `grep -cE "/api/okm/(pii|ingest|rca|source-documents|llm|governance|patterns|analyze|confidence|proxy)" _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/api/okbClient.ts` >= 5 (OKM-specific URLs rewritten)
    - `grep -cE "^[^/].*'/api/entities'|\"/api/entities\"" _work/rapid-automations/integrations/operational-knowledge-management/viewer/src/api/okbClient.ts | grep -v '^#'` returns 0 (no pre-v1 canonical URLs left for the entities endpoint — used as canary; same check applied to other canonical paths via the count above)
    - `grep -c "/api/v1" _work/rapid-automations/integrations/operational-knowledge-management/scripts/record-rest-fixtures.mjs` >= 5 (recorder ENDPOINTS array uses /api/v1/)
    - `grep -c "/api/v1" _work/rapid-automations/integrations/operational-knowledge-management/scripts/verify-post-migration.mjs` >= 5
    - `cd _work/rapid-automations/integrations/operational-knowledge-management && git diff --stat tests/fixtures/pre-migration/ 2>&1 | tail -3 | awk '{print $1}'` returns the literal `0` OR an empty line (byte-equal per Pitfall 5; if non-zero, the SUMMARY documents the per-file diff and the investigation outcome)
    - `grep -c "from '@fwornle/km-core/api/contracts'" _work/rapid-automations/integrations/operational-knowledge-management/tests/integration/rest-contract.test.ts` >= 1 (schema source-of-truth migrated)
    - `grep -cE "^const EntitySchema = z\\.object|^const RelationSchema = z\\.object" _work/rapid-automations/integrations/operational-knowledge-management/tests/integration/rest-contract.test.ts` returns 0 (in-tree schemas removed — schemas now imported)
    - `cd _work/rapid-automations/integrations/operational-knowledge-management && npx vitest run tests/integration/rest-contract.test.ts 2>&1 | tail -5 | grep -cE "(Tests\\s+\\d+\\s+passed|0 failing)"` >= 1 (Phase 43 Gate 1 lock preserved)
    - `cd _work/rapid-automations/integrations/operational-knowledge-management && npm test 2>&1 | tail -10 | grep -cE "(Test Files\\s+\\d+\\s+passed|Tests\\s+\\d+\\s+passed)"` >= 1 (full OKM suite GREEN)
    - Cross-system parity C-leg passes: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3002/api/v1/entities?limit=1` returns `200`
    - No evolutionary file names introduced: `find _work/rapid-automations/integrations/operational-knowledge-management/viewer/src _work/rapid-automations/integrations/operational-knowledge-management/scripts -name '*-v[0-9]*' -o -name '*-enhanced*' -o -name '*-new*'` returns empty
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External client → OKM :3002 | same trust model as A/B; ASVS V5 via Plan 06 handlers + OKM-specific handlers (unchanged) |
| OKM viewer → OKM API | URL rewrite changes the path but not the trust model; CORS already configured per RESEARCH § Stack |
| Cross-repo push to bmw.ghe.com | HTTPS-token auth (feedback_bmw_ghe_https.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-09-01 | Tampering / V12 | Snapshot/restore destructive ops on C | mitigate | Plan 06 handler-side gate + Plan 04 manager-side gate; same code path A/B use; OKM operator restart via documented runbook |
| T-44-09-02 | Information Disclosure / V7 | OKM-specific handler error responses (PII, etc.) | mitigate | OKM-specific handlers stay as-is (pre-existing Phase 43 hardening); no changes to error envelope under /api/okm/* |
| T-44-09-03 | Tampering / V10 | OKM cross-repo push via HTTPS | mitigate | feedback_bmw_ghe_https.md HTTPS-token auth; never SSH for bmw.ghe.com; PR-then-merge to main per Phase 43 D-G6 |
| T-44-09-04 | Tampering | Fixture re-record produces a non-byte-equal diff | mitigate | Pitfall 5 INVESTIGATION step BEFORE lock — any non-zero diff is a real signal (response body unexpectedly depends on URL = bug worth fixing OR documenting); not auto-locked |
| T-44-09-SC | Tampering | OKM-side npm install picks up fresh fwornle-km-core .tgz | mitigate | Same Phase 44 build the coding side runs; manifest verified by km-core CI; same package the legitimacy audit blessed |
</threat_model>

<verification>
```bash
# A-leg + B-leg already GREEN from Plans 07/08. This task verifies C-leg.
cd _work/rapid-automations/integrations/operational-knowledge-management && npm test
curl -s http://localhost:3002/api/v1/stats        # 200 + envelope
curl -s http://localhost:3002/api/okm/source-documents?limit=1   # 200 (OKM-specific)
curl -i http://localhost:3002/api/entities         # 404 (R-4 hard cutover)
node /Users/Q284340/Agentic/coding/tests/integration/cross-system-parity.mjs   # all 3 legs GREEN
```
Operator verifies VOKB viewer renders correctly in Plan 11 via gsd-browser.
</verification>

<success_criteria>
- All 7 OKM files edited per the file-list above
- Vendored km-core .tgz bumped to Phase 44 build
- 20 viewer URL constants rewritten with correct canonical-vs-OKM-specific split
- Fixtures byte-equal after re-record
- rest-contract.test.ts imports schemas from km-core (single source of truth lock)
- OKM full suite GREEN
- Cross-system parity all 3 legs GREEN
- HTTPS-token auth used for bmw.ghe.com push
- Verified against main, not worktree
- No evolutionary file names
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-09-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) OKM clone command + branch name + final commit SHA, (b) per-file URL rewrite counts (viewer, scripts/verify-post-migration, scripts/record-rest-fixtures), (c) fixture re-record diff (byte-equal expected; document any unexpected diffs), (d) Phase 43 Gate 1 lock status after schema-source swap, (e) cross-system parity 3/3 GREEN proof, (f) HTTPS push acknowledged + verified-against-main acknowledged.
</output>
