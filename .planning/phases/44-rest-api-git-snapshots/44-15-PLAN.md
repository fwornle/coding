---
phase: 44-rest-api-git-snapshots
plan: 15
type: execute
wave: 6.1
depends_on:
  - 44-11
files_modified:
  - .gitignore
  - docker/docker-compose.yml
  - .planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-3.md
autonomous: false
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "A leg unblocked: `POST http://localhost:12436/api/v1/snapshots` with `{label:'phase-44-verify'}` returns HTTP 201 + `{success:true, data:{tag:'snapshot/phase-44-verify-<ts>'}}` instead of the gitignore error. Mechanism: `.gitignore` adds `!.data/knowledge-graph/exports/` + `!.data/knowledge-graph/exports/**` un-ignore exceptions, mirroring the existing `!.data/knowledge-graph/insights/` pattern (.gitignore:205-208). No `git add -f` workaround in SnapshotManager."
    - "A leg restore round-trip: `POST .../api/v1/snapshots/<tag>/restore -d '{\"confirmDestructive\":true}'` returns HTTP 200 + `{restored:true, restartRequired:true, restartCommand:'launchctl kickstart -k gui/$(id -u) com.coding.obs-api'}`. After operator kickstart + obs-api respawn (auto via launchd KeepAlive), pre-snapshot `entityCount` is restored bit-for-bit (verified via `/api/v1/stats`)."
    - "B leg unblocked: `POST http://localhost:3848/api/v1/snapshots` returns HTTP 201 + tag (not `Read-only file system`). Mechanism: `docker/docker-compose.yml` line 85 changes `${CODING_REPO:-.}/.git:/coding/.git:ro` to `${CODING_REPO:-.}/.git:/coding/.git:rw`. Rebuild not needed (mount-only change); `docker-compose up -d coding-services` re-mounts on next service start."
    - "B leg restore round-trip: `POST http://localhost:3848/api/v1/snapshots/<tag>/restore` returns 200 + `restartCommand:'cd /Users/Q284340/Agentic/coding/docker && docker-compose restart coding-services'`. After operator-issued restart, pre-snapshot `entityCount` is restored."
    - "C leg gated: depends on operator merging OKM PR #5 + restarting C's service so `/api/v1` actually mounts. Plan does NOT block on it — Task 3 records `BLOCKED:awaiting-OKM-PR-5` with operator runbook if the gate isn't open at execution time; once C is up, Task 3 re-runs verify."
    - "Cross-system-parity test (`tests/integration/cross-system-parity.mjs`) advances from 2/6 PASS (Plan 44-11 SC#1 baseline: A+B GREEN, C HTML fallback) to 6/6 PASS once Tasks 1-3 complete with C up. Re-running mid-plan with C still down captures 4/6 PASS as the intermediate evidence."
    - "Acceptance grep for the .gitignore change: `grep -cE '^\\!\\.data/knowledge-graph/exports/' .gitignore == 1` AND `grep -cE '^\\!\\.data/knowledge-graph/exports/\\*\\*' .gitignore == 1`. The two exceptions sit immediately after the existing insights/ exceptions, preserving the file's existing pattern (.gitignore:203-208)."
    - "Acceptance grep for the docker-compose change: `grep -cE '\\$\\{CODING_REPO:-\\.\\}/\\.git:/coding/\\.git:rw' docker/docker-compose.yml == 1` AND `grep -cE '\\.git:ro' docker/docker-compose.yml == 0`. Exactly one .git mount line, with `:rw`."
    - "Plan 44-CONTEXT amendment recorded: `.planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-3.md` documents the S-1 path drift (CONTEXT said `.data/exports/`; live code routes to `.data/knowledge-graph/exports/` since Phase 41 km-core landing) and ratifies the live path as canonical. Future planners read this instead of inferring divergence."
  artifacts:
    - path: .gitignore
      provides: "Two un-ignore exceptions for `.data/knowledge-graph/exports/` and its contents, mirroring the existing insights/ pattern. Snapshot commits over this dir now succeed without `git add -f`."
      contains: "!.data/knowledge-graph/exports/"
    - path: docker/docker-compose.yml
      provides: "coding-services bind-mount of host `.git` changes from `:ro` to `:rw`, enabling B's SnapshotManager to issue git ops inside the container."
      contains: ".git:rw"
    - path: .planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-3.md
      provides: "S-1 path-routing amendment: ratifies `.data/knowledge-graph/exports/` as the canonical snapshot dir (the GraphKMStore-managed location, where live JSON exports already land). Original CONTEXT S-1 wrote `.data/exports/` before km-core was the persistence layer (pre-Phase-41); the live code routes elsewhere and that is now the contract."
      contains: "S-1 path amendment"
  key_links:
    - from: .gitignore
      to: lib/km-core/src/snapshots/SnapshotManager.ts (execGit `add -A`)
      via: "un-ignore exception lets `git add -A -- .data/knowledge-graph/exports/` succeed"
      pattern: "!.data/knowledge-graph/exports/"
    - from: docker/docker-compose.yml (.git:rw mount)
      to: integrations/mcp-server-semantic-analysis/src/sse-server.ts (snapshotDir wiring)
      via: "writable .git lets B's SnapshotManager inside the container run `git add/commit/tag`"
      pattern: ".git:rw"
    - from: .planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-3.md
      to: 44-CONTEXT.md § S-1
      via: "ratifies the path drift; future planners read the amendment alongside the original CONTEXT"
      pattern: "S-1 path"
threat_model:
  - id: T-44-15-01
    threat: "Un-ignoring `.data/knowledge-graph/exports/**` causes the live JSON exports (debounced from LevelDB, currently 80-200kB per file, growing) to be staged on every developer commit, polluting unrelated PRs with churn."
    severity: medium
    mitigation: "The exports dir is only mutated by SnapshotManager git ops (which use OKB_SNAPSHOT=1 + a scoped path argument) and by the GraphKMStore's debounced writer. Developer commits should not touch this dir directly. Pitfall 1 (OKB-baseline guard) ALREADY blocks mixed commits without OKB_SNAPSHOT=1 — that guard prevents the churn case. Verification: re-run `tests/integration/okb-guard-snapshot-bypass.sh` post-change → exit=0 (no regression; the un-ignore doesn't change the guard's behavior because the guard is path-based, not gitignore-based)."
  - id: T-44-15-02
    threat: "Switching coding-services `.git` from `:ro` to `:rw` lets container code mutate host git history — accidental `git push` from inside the container could ship malformed commits to origin."
    severity: high
    mitigation: "Container code already has filesystem access to the project tree; the `:ro` was defense-in-depth, not a hard isolation boundary. Bound the risk with two layers: (a) the OKB-baseline guard from Pitfall 1 still applies to all commits regardless of mount mode — non-snapshot commits without OKB_SNAPSHOT=1 are rejected; (b) document a `git config --global --add safe.directory '*'` precondition in the SUMMARY's operator runbook (B's container runs as a different UID than the host, so git refuses to operate without this) — operators who haven't set this will get a clear error rather than a silent push. ACCEPT the residual risk; the alternative (host-side helper proxying snapshots) is a much larger refactor for the same dev-only attack surface."
  - id: T-44-15-03
    threat: "C leg cannot verify because OKM PR #5 hasn't merged — Task 3 has no runnable evidence path."
    severity: low
    mitigation: "Task 3 is dual-mode: if C's `/api/v1/snapshots` returns 200, run the round-trip; if it returns SPA HTML (PR #5 unmerged), record `BLOCKED:awaiting-OKM-PR-5` + the operator runbook + cross-system-parity intermediate count (expected 4/6 PASS with C still down). Task 3 does NOT block the plan close-out — the SUMMARY captures the partial verification + the operator follow-up. Re-running Plan 44-11 after C is up exercises the C leg end-to-end."
  - id: T-44-15-04
    threat: "The un-ignore exception order in .gitignore matters — putting `!.data/knowledge-graph/exports/` before `.data/knowledge-graph/*` is ignored (git evaluates rules in order)."
    severity: medium
    mitigation: "The existing pattern at .gitignore:203-208 already proves the correct order: `.data/knowledge-graph/*` first, then `!.data/knowledge-graph/insights/` and friends. Place the new exceptions immediately after `!.data/knowledge-graph/insights/images/` (line 208). Acceptance: `git check-ignore -v .data/knowledge-graph/exports/coding.json` returns NO ignore rule (was `gitignore:204:.data/knowledge-graph/* .data/knowledge-graph/exports/coding.json` before)."
  - id: T-44-15-05
    threat: "Existing .data/knowledge-graph/exports/ files are currently untracked (because gitignored); after un-ignore, the SnapshotManager's first `git add -A` will stage ALL of them at once — the snapshot becomes a flood of historical state rather than a clean baseline."
    severity: medium
    mitigation: "Acceptable — that flood is the snapshot's intended baseline. SnapshotManager S-1 is whole-dir atomic; the first snapshot capturing all pre-existing exports IS the correct starting state. The OKB_SNAPSHOT=1 env var on the git invocation lets it through Pitfall 1's guard. Subsequent snapshots are deltas over that baseline. Verification: Task 1 captures the first snapshot tag's diff (`git show --stat <tag>`) in the SUMMARY — it should show the baseline file set."
---

<objective>
Plan 44-11 SC#2 was BLOCKED on all three legs:

- **A** (obs-api :12436): `git add -A -- ".data/knowledge-graph/exports/"` rejected because `.gitignore:204` (`.data/knowledge-graph/*`) ignores the dir. SnapshotManager surfaces the literal `The following paths are ignored by one of your .gitignore files: .data/knowledge-graph/exports — hint: Use -f if you really want to add them.`
- **B** (semantic-analysis SSE :3848): `fatal: Unable to create '/coding/.git/index.lock': Read-only file system` because `docker/docker-compose.yml:85` mounts `${CODING_REPO:-.}/.git:/coding/.git:ro`. Container cannot mutate host git state.
- **C** (OKM :3002): `/api/v1` not mounted yet — operator-owned (OKM PR #5 unmerged + service not restarted). No code change in this repo unblocks C.

This plan closes A + B with the smallest viable changes and stubs C's verification for the post-OKM-merge round.

**Also surfaced**: a CONTEXT S-1 path drift. CONTEXT mandates `.data/exports/` as the snapshot dir; the live code (`scripts/observations-api-server.mjs:1159` + `integrations/mcp-server-semantic-analysis/src/sse-server.ts:53,79`) routes to `.data/knowledge-graph/exports/`. Both A and B already operate on the live path; only B's mount mode and A's gitignore are blocking. Rather than refactoring 4 km-core consumers to literally match CONTEXT, this plan ratifies the live path as canonical and writes 44-CONTEXT-amendment-3.md.

`autonomous: false` because Task 2 mutates `docker/docker-compose.yml` (operator owns container restarts) and Task 3 may need to wait on OKM PR #5 (operator-owned out-of-band).
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md
@CLAUDE.md
@.gitignore
@docker/docker-compose.yml
@lib/km-core/src/snapshots/SnapshotManager.ts
@scripts/observations-api-server.mjs
@integrations/mcp-server-semantic-analysis/src/sse-server.ts
@tests/integration/cross-system-parity.mjs
@tests/integration/okb-guard-snapshot-bypass.sh

<interfaces>

## Current state — three legs, three blockers

| Leg | Service | Port | Blocking error (literal, from 44-11-SUMMARY) | Root cause |
|-----|---------|------|----------------------------------------------|------------|
| A   | obs-api | 12436 | `git add -A -- ".data/knowledge-graph/exports/" — paths are ignored by one of your .gitignore files: .data/knowledge-graph/exports` | `.gitignore:204` ignores `.data/knowledge-graph/*`; un-ignore exception missing for `exports/` |
| B   | semantic-analysis SSE | 3848 | `fatal: Unable to create '/coding/.git/index.lock': Read-only file system` | `docker-compose.yml:85` mounts host `.git` as `:ro`; SnapshotManager git ops fail inside container |
| C   | OKM | 3002 | `/api/v1/*` returns SPA HTML fallback | OKM PR #5 unmerged + service not restarted (operator-owned, out-of-band) |

## .gitignore — existing pattern to mirror

Lines 203-208 already establish the un-ignore pattern for `insights/`:

```
.data/knowledge-graph/*
!.data/knowledge-graph/insights/
.data/knowledge-graph/insights/*
!.data/knowledge-graph/insights/puml/
!.data/knowledge-graph/insights/images/
```

Apply the same pattern for `exports/`:

```
!.data/knowledge-graph/exports/
!.data/knowledge-graph/exports/**
```

Place these immediately after line 208 (after the last `!images/` exception).

## docker-compose.yml — single-line change

Line 85:

```
      - ${CODING_REPO:-.}/.git:/coding/.git:ro
```

becomes:

```
      - ${CODING_REPO:-.}/.git:/coding/.git:rw
```

No image rebuild needed — Docker re-applies bind mounts on `docker-compose up -d coding-services`. Container needs to restart; SUMMARY captures the operator command for the checkpoint.

## C leg — no code, gated verification

The C leg is a verification stub. Task 3 probes `http://localhost:3002/api/v1/stats`:

- If 200 with canonical envelope: C is up — run snapshot/restore round-trip.
- If 200 with `text/html` body (SPA fallback) or 404: record `BLOCKED:awaiting-OKM-PR-5` + the operator runbook for completing C once PR #5 merges.

`.planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-3.md` is created in Task 4 to document the S-1 path ratification.

</interfaces>
</context>

<tasks>

<task id="1">
<description>A leg: add `.data/knowledge-graph/exports/` un-ignore exception to `.gitignore`; verify A's snapshot create + restore round-trip end-to-end.</description>
<action>
1. Edit `.gitignore` — insert two lines immediately after line 208 (`!.data/knowledge-graph/insights/images/`):
   ```
   !.data/knowledge-graph/exports/
   !.data/knowledge-graph/exports/**
   ```
   Preserve the existing trailing newline and the next block intact.
2. Verify the rule resolves correctly:
   ```bash
   git check-ignore -v .data/knowledge-graph/exports/coding.json
   ```
   Expected: no output (no matching ignore rule). Pre-change: `.gitignore:204:.data/knowledge-graph/*    .data/knowledge-graph/exports/coding.json`.
3. Stage and commit just the .gitignore change: `chore(44-15): un-ignore .data/knowledge-graph/exports/ for snapshot baseline`.
4. Pre-snapshot probe:
   ```bash
   /usr/bin/curl -s http://localhost:12436/api/v1/stats | jq '{nodes: .data.nodes, activeSnapshot: .data.activeSnapshot}'
   ```
   Capture the literal output.
5. Create the verification snapshot:
   ```bash
   /usr/bin/curl -s -X POST http://localhost:12436/api/v1/snapshots \
     -H 'Content-Type: application/json' \
     -d '{"label":"phase-44-verify-A"}'
   ```
   Expected: HTTP 201 + `{success:true, data:{tag:"snapshot/phase-44-verify-A-<UTC>"}}`. Capture the tag id.
6. Mutate to prove restore wipes the change:
   ```bash
   /usr/bin/curl -s -X POST http://localhost:12436/api/v1/entities \
     -H 'Content-Type: application/json' \
     -d '{"name":"verify-stub-A","entityType":"VerifyStub","ontologyClass":"VerifyStub","layer":"evidence","description":"phase 44-15 A-leg verification","metadata":{}}'
   ```
   Expected: HTTP 201 + entity id. Re-probe `/api/v1/stats` → `nodes` should be `pre + 1`.
7. Restore:
   ```bash
   /usr/bin/curl -s -X POST "http://localhost:12436/api/v1/snapshots/<tag>/restore" \
     -H 'Content-Type: application/json' \
     -d '{"confirmDestructive":true}'
   ```
   Expected: HTTP 200 + `{restored:true, restartRequired:true, restartCommand:"launchctl kickstart -k gui/$(id -u) com.coding.obs-api"}`. Capture the response body.
8. Operator-equivalent action (auto in this task because launchd handles A): kick A:
   ```bash
   launchctl kickstart -k gui/$(id -u)/com.coding.obs-api
   sleep 10
   ```
9. Post-restart probe:
   ```bash
   /usr/bin/curl -s http://localhost:12436/api/v1/stats | jq '.data.nodes'
   ```
   Assert: equals the pre-snapshot `nodes` count from step 4. The verify-stub entity is gone.
10. Re-run the OKB-baseline guard to confirm no regression:
    ```bash
    bash tests/integration/okb-guard-snapshot-bypass.sh; echo "exit=$?"
    ```
    Expected: `exit=0`.
11. Commit the verification evidence as a SUMMARY-internal artifact only (Task 4 writes the canonical SUMMARY).
</action>
<acceptance>
- `.gitignore` contains the two un-ignore lines, placed immediately after the existing insights/ exceptions.
- `git check-ignore -v .data/knowledge-graph/exports/coding.json` returns no output (or returns the un-ignore rule, not the parent ignore).
- A's snapshot POST returns HTTP 201 + a tag id matching `^snapshot/phase-44-verify-A-`.
- A's restore POST returns HTTP 200 + literal `restartCommand:"launchctl kickstart -k gui/$(id -u) com.coding.obs-api"`.
- Post-kickstart `/api/v1/stats.data.nodes` equals the pre-snapshot count.
- `okb-guard-snapshot-bypass.sh` still exits 0 (T-44-15-01 mitigation evidence).
- One commit for the .gitignore change.
</acceptance>
</task>

<task id="2" type="checkpoint:human-verify" gate="blocking">
<description>B leg: change `docker-compose.yml` line 85 from `.git:ro` to `.git:rw`; operator restarts coding-services; verify B's snapshot round-trip.</description>
<what-built>
Single-line change in `docker/docker-compose.yml` line 85:
- before: `${CODING_REPO:-.}/.git:/coding/.git:ro`
- after:  `${CODING_REPO:-.}/.git:/coding/.git:rw`

Operator action required (Docker container restart):
```bash
cd /Users/Q284340/Agentic/coding/docker && docker-compose up -d coding-services
```

After restart, the assistant runs the B-leg snapshot/restore round-trip:
1. Pre-probe `http://localhost:3848/api/v1/stats`
2. `POST http://localhost:3848/api/v1/snapshots {label:'phase-44-verify-B'}` → capture HTTP 201 + tag
3. Mutate: POST verify-stub-B entity → assert `nodes` increments
4. Restore: `POST http://localhost:3848/api/v1/snapshots/<tag>/restore` → capture HTTP 200 + `restartCommand`
5. Operator-issued restart: `cd docker && docker-compose restart coding-services`
6. Post-restart probe → assert `nodes` returns to pre-snapshot count

If the container is launched with a host UID that differs from the .git owner UID, the operator may need to set `git config --global --add safe.directory /coding` INSIDE the container to silence git's safe.directory check (T-44-15-02 mitigation note). The SUMMARY runbook captures this.
</what-built>
<how-to-verify>
1. Confirm the diff is exactly one line in `docker/docker-compose.yml` — only `:ro` → `:rw` on line 85.
2. After `docker-compose up -d coding-services`, the assistant exec's:
   ```bash
   docker exec coding-services ls -la /coding/.git/HEAD
   ```
   Expected: file is listable AND `touch /coding/.git/snapshot-rw-probe` succeeds inside the container (then immediately `rm` it).
3. Snapshot POST returns 201 (not the prior `Read-only file system` error).
4. Restore + restart cycle returns `nodes` to pre-snapshot count on port 3848.
5. T-44-15-02 acceptance: container's `.git` mount mode visible in `docker inspect coding-services | jq '.[0].Mounts[] | select(.Destination=="/coding/.git")'` shows `"RW":true`.

Type "approved" to continue to Task 3. Type "issues: <description>" if the container fails to restart or the snapshot still rejects.
</how-to-verify>
<resume-signal>Type "approved" or "issues: <description>"</resume-signal>
</task>

<task id="3" type="checkpoint:human-verify" gate="blocking">
<description>C leg: gated on OKM PR #5 merge + C service restart. Probe C; either run the round-trip OR record BLOCKED with operator runbook.</description>
<what-built>
Dual-mode verification. The assistant probes:
```bash
/usr/bin/curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:3002/api/v1/stats
```

**Mode A — `/api/v1` is up** (HTTP 200 with `application/json`):
1. Run the same snapshot create + mutate + restore + restart cycle as Task 1 (A) / Task 2 (B), against port 3002.
2. Operator-issued restart command: OKM-specific (varies by deployment; common: `docker-compose restart <okm-container>` or `npm run dev` restart).
3. Capture the same per-step evidence as Tasks 1 and 2.

**Mode B — `/api/v1` is still SPA fallback** (HTTP 200 with `text/html`) or 404:
1. Record `BLOCKED:awaiting-OKM-PR-5` in the SUMMARY's C section.
2. Capture the literal probe response for evidence.
3. Document the operator runbook for completing C post-merge:
   - Merge OKM PR #5
   - Restart C's service (operator-known command)
   - Re-run Plan 44-11 (the verification gate) — that re-execution covers C's round-trip and completes Phase 44 SC#1+SC#2 across all 3 systems.

Either way, this task ALWAYS terminates — it does not block plan close-out.
</what-built>
<how-to-verify>
1. C probe captured in the SUMMARY with literal HTTP status + content-type.
2. If Mode A: same evidence shape as Task 1/2 — pre-stats, tag, mutate response, restore response with restartCommand, post-restart stats.
3. If Mode B: BLOCKED record + operator runbook reference + the cross-system-parity intermediate count (expected 4/6 PASS with A+B GREEN, C HTML fallback).
4. T-44-15-03 acceptance: plan close-out does NOT depend on Mode A. Mode B is a valid terminating state.

Type "approved" to proceed to Task 4 (regardless of Mode A or Mode B outcome). Type "issues: <description>" only if C is in some unexpected third state (e.g., 500s, network-unreachable from the host).
</how-to-verify>
<resume-signal>Type "approved" or "issues: <description>"</resume-signal>
</task>

<task id="4">
<description>Author 44-CONTEXT-amendment-3.md + 44-15-SUMMARY.md; re-run cross-system-parity.mjs; commit; update STATE.md.</description>
<action>
1. Write `.planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-3.md`:
   - Heading: "Amendment 3 — S-1 snapshot dir path ratification"
   - Reference the original CONTEXT § S-1 verbatim quote (`.data/exports/`)
   - Document the live path (`.data/knowledge-graph/exports/`) and its provenance (km-core landed it in Phase 41; both A `scripts/observations-api-server.mjs:1159` and B `integrations/mcp-server-semantic-analysis/src/sse-server.ts:53,79` route there; C inherits via OKM's own km-core wiring).
   - Ratify the live path as canonical for Phase 44 + Phase 45.
   - List the dependent code sites a future migrator would need to touch if the canonical path ever moves back to `.data/exports/`.
2. Re-run cross-system-parity:
   ```bash
   cd /Users/Q284340/Agentic/coding && node --test tests/integration/cross-system-parity.mjs 2>&1 | tail -20
   ```
   Capture the per-leg result. Expected post-Task-1+Task-2 with C still blocked: 4/6 PASS (A+B green for /stats and /ontology/classes). Expected with C up: 6/6 PASS.
3. Write `.planning/phases/44-rest-api-git-snapshots/44-15-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Sections:
   - Per-task evidence (Task 1 A-leg full round-trip; Task 2 B-leg full round-trip; Task 3 C-leg Mode A or Mode B outcome)
   - Per-SC progress against Plan 44-11 SC#2: A PASS, B PASS, C PASS-or-BLOCKED-awaiting-OKM-PR-5
   - cross-system-parity before/after counts
   - Operator runbook for the still-open items (C if Mode B; the docker config mode change going forward)
   - Files modified: .gitignore (1 chunk), docker/docker-compose.yml (1 line), 44-CONTEXT-amendment-3.md (new)
4. Stage all three files + the SUMMARY; commit: `feat(44-15): snapshot-dir routing fix — A gitignore exception + B docker .git:rw mount + CONTEXT S-1 amendment`.
5. Update STATE.md:
   - Current focus: still Phase 44 (SC#2 progressing; SC#3 + Phase 44 close-out still pending — wait for Plan 44-16 + re-run 44-11)
   - Last Activity: note 44-15 close + per-leg status
6. Final commit if STATE.md was updated separately: `docs(44-15): STATE — record snapshot-dir routing fix progress`.
</action>
<acceptance>
- `.planning/phases/44-rest-api-git-snapshots/44-CONTEXT-amendment-3.md` exists, ≥30 lines, contains literal `S-1 path amendment` heading text.
- `.planning/phases/44-rest-api-git-snapshots/44-15-SUMMARY.md` exists, ≥80 lines, per-task evidence captured.
- `cross-system-parity.mjs` re-run output captured (pass count compared to Plan 44-11 baseline of 2/6).
- One feat commit covering .gitignore + docker-compose.yml + CONTEXT amendment + SUMMARY. Optional separate STATE.md docs commit.
- STATE.md reflects 44-15 close; ROADMAP Phase 44 entry stays `[ ]` (Plan 44-11 re-run is the close-out gate, not this plan).
</acceptance>
</task>

</tasks>

<success_criteria>
- A leg: `.gitignore` un-ignore exception lands; A's snapshot POST returns 201; restore round-trip returns nodes to pre-snapshot count after kickstart.
- B leg: `docker-compose.yml` mount mode changes to `:rw`; operator restarts coding-services; B's snapshot round-trip mirrors A's success.
- C leg: dual-mode verification — either Mode A round-trip success, or Mode B `BLOCKED:awaiting-OKM-PR-5` with operator runbook. Both are valid terminating states for this plan.
- CONTEXT S-1 path drift documented in 44-CONTEXT-amendment-3.md; the live `.data/knowledge-graph/exports/` path is the ratified canonical.
- cross-system-parity advances from Plan 44-11's 2/6 PASS toward 4/6 (C blocked) or 6/6 (C up).
- OKB-baseline guard (`tests/integration/okb-guard-snapshot-bypass.sh`) still exits 0 — the gitignore change does NOT regress Pitfall 1.
- Phase 44 close-out remains gated on Plan 44-16 (typed-view shape lock for digests + insights) + Plan 44-11 re-run. This plan moves SC#2 forward; it does NOT itself close Phase 44.
</success_criteria>

<rollback>
If T-44-15-02 (writable `.git` mount) surfaces an unexpected side effect in CI or local dev:
1. `git revert HEAD` on the Task 2 + Task 4 commits, restoring `.git:ro`.
2. B leg returns to BLOCKED; A leg's gitignore change is independent and stays.
3. Re-open the B leg as a separate plan with a host-side helper approach (the more expensive alternative noted in T-44-15-02's mitigation).
4. The gitignore exception in Task 1 has no rollback trigger — it mirrors an existing pattern and is independently safe.
</rollback>
