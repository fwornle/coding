---
phase: 44-rest-api-git-snapshots
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/km-core/tests/integration/api-router.test.ts
  - lib/km-core/tests/unit/contracts.test.ts
  - lib/km-core/tests/integration/snapshot-roundtrip.test.ts
  - lib/km-core/tests/integration/observation-view.test.ts
  - lib/km-core/package.json
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "km-core test infrastructure for Phase 44 exists as RED tests asserting the four canonical artifacts (router factory, Zod contracts, SnapshotManager, observation-view adapter) before any feature lands."
    - "supertest devDep is installed in km-core for in-process Express-based router testing."
    - "A single `cd lib/km-core && npx vitest run tests/integration/api-router.test.ts tests/unit/contracts.test.ts tests/integration/snapshot-roundtrip.test.ts tests/integration/observation-view.test.ts` invocation enumerates 4 RED tests with deterministic failure messages naming the missing exports."
  artifacts:
    - path: lib/km-core/tests/integration/api-router.test.ts
      provides: "supertest harness asserting createKmCoreRouter mounts the canonical 15 endpoints under /api/v1; expects { success:true, data:[] } envelope on GET /entities against empty store."
      min_lines: 80
    - path: lib/km-core/tests/unit/contracts.test.ts
      provides: "Zod accept-good + reject-bad payload suite for EntitySchema, RelationSchema, ApiSuccessEnvelope; asserts z.infer types compile."
      min_lines: 60
    - path: lib/km-core/tests/integration/snapshot-roundtrip.test.ts
      provides: "tmpdir + git-init fixture; create → mutate → restore → assert graph equals snapshot state; asserts snapshot ID format snapshot/<label>-<UTC-ts>."
      min_lines: 100
    - path: lib/km-core/tests/integration/observation-view.test.ts
      provides: "Pure round-trip: SQLite-shaped row → km-core Entity (via Phase 41 mapper conventions) → observationToLegacy → original shape; asserts legacyId.id preference over entity.id."
      min_lines: 50
    - path: lib/km-core/package.json
      provides: "supertest + @types/supertest in devDependencies"
      contains: "supertest"
  key_links:
    - from: lib/km-core/tests/integration/api-router.test.ts
      to: "src/api/index.ts (does not yet exist)"
      via: "import { createKmCoreRouter } from '../../src/api/index.js'"
      pattern: "createKmCoreRouter"
    - from: lib/km-core/tests/unit/contracts.test.ts
      to: "src/api/contracts.ts (does not yet exist)"
      via: "import { EntitySchema, ApiSuccessEnvelope } from '../../src/api/contracts.js'"
      pattern: "EntitySchema"
    - from: lib/km-core/tests/integration/snapshot-roundtrip.test.ts
      to: "src/snapshots/SnapshotManager.ts (does not yet exist)"
      via: "import { SnapshotManager } from '../../src/snapshots/SnapshotManager.js'"
      pattern: "SnapshotManager"
---

<objective>
Land Wave 0 km-core test stubs as RED tests (assertions present, expected to fail until Wave 1/2 features land). Per 44-VALIDATION.md the four km-core-side tests are required before any feature task can claim Nyquist compliance.

Purpose: Stop the "executor builds feature + writes test at the end" anti-pattern that produced the orphan dist/ draft in the first place. Plans 03/04/05/06 verify against these tests.
Output: 4 vitest files in `lib/km-core/tests/` + supertest devDep wired in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@.planning/phases/44-rest-api-git-snapshots/44-VALIDATION.md
@lib/km-core/CLAUDE.md
@lib/km-core/package.json
@lib/km-core/tests/integration/round-trip.test.ts
@lib/km-core/tests/unit/entity.test.ts

<interfaces>
<!-- Existing km-core test harness conventions (Phase 37/41/42). All tests use vitest + tmpdir + GraphKMStore lifecycle. -->

From lib/km-core/tests/integration/round-trip.test.ts (template for tmpdir+store lifecycle):
- describe(...) block; beforeEach mkdtempSync; afterEach store.close() + rmSync.
- Imports use relative .js paths: `import { GraphKMStore } from '../../src/store/GraphKMStore.js'`.

From lib/km-core/src/store/GraphKMStore.ts (the consumed surface — tests must use the existing API):
- `new GraphKMStore({ dbPath, exportDir, ontologyDir?, debounceMs?, domains? })`
- `await store.open()`, `await store.close()`, `await store.exportJson()`
- `await store.putEntity(entity)`, `await store.findByOntologyClass(cls)`
- `for await (const e of store.iterate())` async-iter

From lib/km-core/src/index.ts (will export after Wave 1+2 land):
- `createKmCoreRouter(store, router, opts)` — Plan 06
- `SnapshotManager` class — Plan 04
- `observationToLegacy`, `digestToLegacy`, `insightToLegacy` — Plan 05
- `EntitySchema`, `ApiSuccessEnvelope`, etc. — Plan 03 via subpath `@fwornle/km-core/api/contracts`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install supertest devDep + author api-router.test.ts (RED)</name>
  <files>lib/km-core/package.json, lib/km-core/tests/integration/api-router.test.ts</files>
  <read_first>
    - lib/km-core/CLAUDE.md (no-parallel-versions rule + no evolutionary suffixes — Test file MUST be `api-router.test.ts`, not `api-router-new.test.ts` etc.)
    - lib/km-core/package.json (existing devDependencies block at lines 70-75; preserve alphabetical order)
    - lib/km-core/tests/integration/round-trip.test.ts (tmpdir + store lifecycle template — lines 1-90)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Example 1 (router skeleton with the 15-endpoint behavior the test must assert) AND § Validation Architecture Wave 0 row 1
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `lib/km-core/tests/integration/api-router.test.ts` (analog identification + supertest adaptation pattern, lines 642-694)
  </read_first>
  <behavior>
    - Test 1: GET /api/v1/entities on empty store returns 200 + `{ success: true, data: [] }`.
    - Test 2: POST /api/v1/entities with valid body returns 201 + envelope; subsequent GET returns the created entity.
    - Test 3: GET /api/v1/entities?ontologyClass=Component filters via store.findByOntologyClass (two-field OR check — Pitfall 3).
    - Test 4: GET /api/v1/stats returns 200 + envelope with `entityCount` + `relationCount`.
    - Test 5: All 15 canonical endpoints registered — drive a smoke probe that asserts each path returns either 200/201 or a structured error (NOT 404). Endpoint list per 44-RESEARCH.md § Example 2 table (entities CRUD, relations CRUD, query, export, stats, ontology/{classes,entity-types,schema/:className}, graph/{connectivity,orphans}, cleanup/*, clusters, snapshots).
    - Test 6: createKmCoreRouter with `readOnly: true` rejects POST/PUT/DELETE with 405 or absence of route (asserts the readOnly gate).
  </behavior>
  <action>Add `supertest` (^7.0.0) and `@types/supertest` (^6.0.0) to `lib/km-core/package.json` devDependencies (alphabetically between existing entries — preserve sort order; do NOT bump version of unrelated deps). Run `npm install` in `lib/km-core/` to materialize node_modules. Create `lib/km-core/tests/integration/api-router.test.ts` as a vitest spec using the tmpdir + GraphKMStore lifecycle from 44-PATTERNS.md lines 642-694 (round-trip.test.ts template). Imports: `import { describe, test, expect, beforeEach, afterEach } from 'vitest'; import express, { Router } from 'express'; import request from 'supertest'; import { GraphKMStore } from '../../src/store/GraphKMStore.js'; import { createKmCoreRouter } from '../../src/api/index.js';` (the createKmCoreRouter import resolves to a NOT-YET-EXISTING module — that is the RED state). Per task per behavior block above; assert envelope shape from 44-PATTERNS.md § Pattern: Response Envelope. Use `await store.open()` after construction (Pitfall: not opening throws on first putEntity). Hydration-gate is OUT OF SCOPE for this test (mount is plain — gating is consumer-side per Plan 07/08). Do NOT add `xfail` / `test.skip` — tests must be RED, not skipped (44-VALIDATION.md Wave 0 line 84). Honor CLAUDE.md no-console-log rule — if diagnostic emission needed, use `process.stderr.write`.</action>
  <verify>
    <automated>cd lib/km-core && npx vitest run tests/integration/api-router.test.ts 2>&1 | grep -qE "(Cannot find module|Failed to resolve).*api/index" && echo "RED-AS-EXPECTED"</automated>
  </verify>
  <done>
    `lib/km-core/package.json` contains literal strings `"supertest"` and `"@types/supertest"` in devDependencies. `lib/km-core/tests/integration/api-router.test.ts` exists with at least 80 lines, imports `createKmCoreRouter` from `'../../src/api/index.js'`, declares 6 test blocks. Running the test produces a module-not-found error against `src/api/index.js` (RED). `node_modules/supertest` exists post-install.
  </done>
  <acceptance_criteria>
    - `grep -c '"supertest"' lib/km-core/package.json` returns 1
    - `grep -c '"@types/supertest"' lib/km-core/package.json` returns 1
    - `test -d lib/km-core/node_modules/supertest && echo OK` prints OK
    - `wc -l lib/km-core/tests/integration/api-router.test.ts | awk '{print $1}'` >= 80
    - `grep -cE "^\s*test\(" lib/km-core/tests/integration/api-router.test.ts` >= 6
    - `grep -c "createKmCoreRouter" lib/km-core/tests/integration/api-router.test.ts` >= 2
    - `cd lib/km-core && npx vitest run tests/integration/api-router.test.ts 2>&1 | grep -E "(Cannot find module|Failed to resolve).*api"` returns at least one match (RED-as-expected)
    - File name is EXACTLY `api-router.test.ts` — no `-v2`, `-new`, `-enhanced` suffixes (km-core CLAUDE.md no-parallel-versions rule)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author contracts.test.ts + snapshot-roundtrip.test.ts + observation-view.test.ts (RED)</name>
  <files>lib/km-core/tests/unit/contracts.test.ts, lib/km-core/tests/integration/snapshot-roundtrip.test.ts, lib/km-core/tests/integration/observation-view.test.ts</files>
  <read_first>
    - lib/km-core/tests/unit/entity.test.ts (vitest unit-test convention + type-shape assertion pattern)
    - lib/km-core/tests/integration/round-trip.test.ts (tmpdir+store lifecycle — same template as Task 1)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pattern 2 (Zod schema lines 207-251 — the schemas the test asserts against), § Pattern 4 (SnapshotManager lines 343-466 — git-tag semantics + getGitEnv + execGit envelope), § Pattern 3 (observationToLegacy lines 282-315 — round-trip shape)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md lines 165-209 (contracts.ts schema template), lines 213-262 (SnapshotManager pattern deviations), lines 265-329 (observation-view template)
    - .planning/phases/44-rest-api-git-snapshots/44-VALIDATION.md Wave 0 list (8 stubs — these are stubs 2, 3, 4 of 8)
  </read_first>
  <behavior>
    contracts.test.ts:
      - Test 1: EntitySchema.parse accepts a minimal valid Phase 39 entity (id, name, entityType, layer:'evidence', description, createdAt, updatedAt, metadata:{}).
      - Test 2: EntitySchema.parse REJECTS entity missing required `entityType` field with ZodError.
      - Test 3: EntitySchema.parse accepts entity with optional Phase 39 fields (validFrom, validUntil:null, supersedes:[], createdBy:{provider,model,runId,timestamp}, confirmationCount:0, legacyId:{system:'A',id:'x'}).
      - Test 4: ApiSuccessEnvelope(EntitySchema).parse accepts `{ success: true, data: <entity> }`; rejects `{ success: false, ... }`.
      - Test 5: z.infer<typeof EntitySchema> compiles (type assertion via `const _x: Entity = {...}` — TS-level check).
    snapshot-roundtrip.test.ts:
      - Test 1: createSnapshot('test-label') in a tmpdir+git-init fixture produces a tag matching `/^snapshot\/test-label-\d{4}-\d{2}-\d{2}T.+$/` and a commit with message `chore(snapshot): test-label`.
      - Test 2: listSnapshots returns the created entry with `id`, `label`, `timestamp`, `commit_sha`, `domains_present` fields.
      - Test 3: Round-trip — putEntity → exportJson → createSnapshot('s1') → putEntity(another) → restoreSnapshot('snapshot/s1-<ts>') → assert `.data/exports/*.json` files match the snapshot state (byte-equal on a deterministic key set).
      - Test 4: createSnapshot wraps git commit with OKB_SNAPSHOT=1 env-var (assert via a stub commit script that records env or via inspecting recently-created commit succeeds even when a hook script in the fixture would otherwise fail without the env var).
    observation-view.test.ts:
      - Test 1: observationToLegacy of an entity with `legacyId={system:'A',id:'old-id'}, metadata:{agent,project,summary,artifacts:[]}, validFrom:'2026-01-01T00:00:00Z'` returns `{ id:'old-id', agent, project, content: summary, artifacts:[], timestamp:'2026-01-01T00:00:00Z' }`.
      - Test 2: When metadata.summary is absent, falls back to metadata.content; when both absent, falls back to entity.description.
      - Test 3: When legacyId is absent, uses entity.id as legacy id.
      - Test 4: digestToLegacy and insightToLegacy follow the analogous pattern (each gets a smoke test).
  </behavior>
  <action>Create all three test files as vitest specs. Imports per file:
    - `contracts.test.ts`: `import { describe, test, expect } from 'vitest'; import { EntitySchema, ApiSuccessEnvelope } from '../../src/api/contracts.js'; import type { Entity } from '../../src/api/contracts.js';` (NOT-YET-EXISTING → RED).
    - `snapshot-roundtrip.test.ts`: `import { describe, test, expect, beforeEach, afterEach } from 'vitest'; import { execSync } from 'node:child_process'; import * as fs from 'node:fs'; import * as path from 'node:path'; import * as os from 'node:os'; import { GraphKMStore } from '../../src/store/GraphKMStore.js'; import { SnapshotManager } from '../../src/snapshots/SnapshotManager.js';` (NOT-YET-EXISTING → RED). beforeEach: mkdtempSync; `execSync('git init', { cwd: tmpdir })` + `git config user.email/name`; construct GraphKMStore with `dbPath`, `exportDir: path.join(tmpdir, '.data/exports')`, `debounceMs: 0`. afterEach: store.close + rmSync recursive force.
    - `observation-view.test.ts`: `import { describe, test, expect } from 'vitest'; import { observationToLegacy, digestToLegacy, insightToLegacy } from '../../src/adapters/observation-view.js';` (NOT-YET-EXISTING → RED). Pure tests — no tmpdir.
    Use process.stderr.write for any diagnostic emission (no console.log). File names EXACTLY as listed — no evolutionary suffixes (km-core CLAUDE.md). All test blocks asserting on the listed behaviors. Tests RED via missing module imports, NOT skipped.</action>
  <verify>
    <automated>cd lib/km-core && npx vitest run tests/unit/contracts.test.ts tests/integration/snapshot-roundtrip.test.ts tests/integration/observation-view.test.ts 2>&1 | grep -cE "(Cannot find module|Failed to resolve).*(api/contracts|snapshots/SnapshotManager|adapters/observation-view)" | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <done>
    All three test files exist with minimum line counts (60 / 100 / 50). Each fails with a module-not-found error referencing the eventual Wave 1 source path. No test.skip / xfail / test.todo present.
  </done>
  <acceptance_criteria>
    - `wc -l lib/km-core/tests/unit/contracts.test.ts | awk '{print $1}'` >= 60
    - `wc -l lib/km-core/tests/integration/snapshot-roundtrip.test.ts | awk '{print $1}'` >= 100
    - `wc -l lib/km-core/tests/integration/observation-view.test.ts | awk '{print $1}'` >= 50
    - `grep -c "EntitySchema" lib/km-core/tests/unit/contracts.test.ts` >= 3
    - `grep -c "SnapshotManager" lib/km-core/tests/integration/snapshot-roundtrip.test.ts` >= 2
    - `grep -cE "snapshot/test-label-" lib/km-core/tests/integration/snapshot-roundtrip.test.ts` >= 1
    - `grep -c "observationToLegacy" lib/km-core/tests/integration/observation-view.test.ts` >= 2
    - `grep -cE "(test\.skip|test\.todo|xfail|\.skip\()" lib/km-core/tests/unit/contracts.test.ts lib/km-core/tests/integration/snapshot-roundtrip.test.ts lib/km-core/tests/integration/observation-view.test.ts` returns 0 (no skipped tests in Wave 0)
    - `cd lib/km-core && npx vitest run tests/unit/contracts.test.ts tests/integration/snapshot-roundtrip.test.ts tests/integration/observation-view.test.ts 2>&1 | grep -cE "(Cannot find module|Failed to resolve)"` >= 3 (one per missing source module — RED-as-expected)
    - File names are EXACTLY `contracts.test.ts`, `snapshot-roundtrip.test.ts`, `observation-view.test.ts` — no version/enhanced suffixes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-process → tmpdir filesystem | Tests create temp dirs and git-init them; controlled-scope filesystem writes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | Tampering | snapshot-roundtrip.test.ts tmpdir handling | accept | tests use os.tmpdir() + mkdtempSync + afterEach rmSync; no cross-test interference; test-only scope |
| T-44-01-SC | Tampering | npm install of supertest@^7.0.0 + @types/supertest@^6.0.0 | mitigate | both are extremely high-trust packages (ladjs/supertest, DefinitelyTyped), well-known in OKM devDeps as transitive; existing approved per 44-RESEARCH.md § Package Legitimacy Audit (supertest already used in OKM tests). No legitimacy checkpoint needed — explicit allowlist in research |
</threat_model>

<verification>
Run all four Wave 0 km-core test files; expect all to FAIL with module-not-found errors against the not-yet-existing Wave 1+2 source modules.
```bash
cd lib/km-core && npx vitest run \
  tests/integration/api-router.test.ts \
  tests/unit/contracts.test.ts \
  tests/integration/snapshot-roundtrip.test.ts \
  tests/integration/observation-view.test.ts
```
Expected: 4 files reported, all failed at import time. No test.skip. Total RED count >= 4.
</verification>

<success_criteria>
- supertest + @types/supertest landed in `lib/km-core/package.json` devDependencies (alphabetical)
- 4 RED test files exist with the line counts and test-block counts specified
- Module resolution errors point at the eventual Wave 1+2 source paths
- No test.skip / xfail / test.todo present anywhere
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-01-SUMMARY.md` when done following @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) RED test count, (b) module-not-found errors observed (paste literal failure lines), (c) npm install summary line.
</output>
