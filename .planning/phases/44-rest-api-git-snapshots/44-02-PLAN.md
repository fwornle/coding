---
phase: 44-rest-api-git-snapshots
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/integration/cross-system-parity.mjs
  - tests/integration/typed-views.test.js
  - tests/integration/okb-guard-snapshot-bypass.sh
  - tests/e2e/dashboard-observations.spec.ts
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "Coding-repo Wave 0 test infrastructure for Phase 44 exists as RED tests asserting the cross-system parity, A-side typed views, OKB hook bypass, and dashboard render-after-migration behaviors."
    - "All four coding-side stubs fail with deterministic messages naming the missing services / files / fixtures."
    - "The 8-stub Wave 0 set listed in 44-VALIDATION.md is complete after Plans 01 + 02."
  artifacts:
    - path: tests/integration/cross-system-parity.mjs
      provides: "Drives /api/v1/{entities,stats,ontology/classes} against A=12436, B=3848, C=3002; normalizes timestamps + UUIDs; deep-diffs."
      min_lines: 80
    - path: tests/integration/typed-views.test.js
      provides: "Asserts /api/coding/observations|digests|insights legacy shape lock (Pitfall 2 contract)."
      min_lines: 70
    - path: tests/integration/okb-guard-snapshot-bypass.sh
      provides: "Bash test — stage .data/exports/foo.json; with OKB_SNAPSHOT=0 expect hook exit 1; with OKB_SNAPSHOT=1 expect hook exit 0."
      min_lines: 40
    - path: tests/e2e/dashboard-observations.spec.ts
      provides: "Playwright spec asserting dashboard at :3032 renders non-empty agent + project cells after A-2 migration (Pitfall 2 UI lock)."
      min_lines: 50
  key_links:
    - from: tests/integration/cross-system-parity.mjs
      to: "A:12436, B:3848, C:3002 /api/v1 endpoints (do not yet exist)"
      via: "fetch / node-fetch against each service URL"
      pattern: "/api/v1/(entities|stats|ontology/classes)"
    - from: tests/integration/typed-views.test.js
      to: "scripts/observations-api-server.mjs /api/coding/observations handler (still SQLite-backed in this wave)"
      via: "fetch http://localhost:12436/api/coding/observations"
      pattern: "/api/coding/(observations|digests|insights)"
    - from: tests/integration/okb-guard-snapshot-bypass.sh
      to: "scripts/hooks/pre-commit-okb-guard.sh (no OKB_SNAPSHOT bypass yet — Plan 04 adds it)"
      via: "OKB_SNAPSHOT=1 bash scripts/hooks/pre-commit-okb-guard.sh"
      pattern: "OKB_SNAPSHOT"
    - from: tests/e2e/dashboard-observations.spec.ts
      to: "http://localhost:3032 dashboard observations panel"
      via: "playwright page.goto + locator"
      pattern: "data-testid=\"observations-table\""
---

<objective>
Land the coding-repo Wave 0 stubs covering the 4 cross-cutting behaviors that Plans 03-11 must satisfy. These are RED today because the canonical /api/v1 surface does not exist (A/B/C), the typed-view handlers still query SQLite, the hook has no OKB_SNAPSHOT bypass, and the dashboard panel has not been migration-tested.

Purpose: Make cross-system parity, typed-view shape lock, hook bypass, and dashboard render-after-migration measurable from a single command sequence per category.
Output: 4 RED test files under `tests/integration/` and `tests/e2e/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@.planning/phases/44-rest-api-git-snapshots/44-VALIDATION.md
@CLAUDE.md
@tests/integration/http-api.test.js
@tests/e2e/dashboard/workflow-graph-colors.spec.ts
@scripts/hooks/pre-commit-okb-guard.sh

<interfaces>
<!-- Existing test conventions for the coding repo: Jest 29 for tests/integration/*.test.js; node-fetch pattern; playwright for tests/e2e/**. -->

From tests/integration/http-api.test.js (template):
- `describe(...)`; `beforeAll`/`afterAll`; uses `fetch` (node-fetch); single-service URL constants.

From tests/e2e/dashboard/workflow-graph-colors.spec.ts (template):
- `import { test, expect, type Page } from '@playwright/test'`
- `__filename` / `__dirname` via `fileURLToPath` pattern
- `page.goto('http://localhost:3032')` then `page.waitForSelector(...)` + `page.waitForTimeout(2000)`

From scripts/hooks/pre-commit-okb-guard.sh (current state):
- `set -euo pipefail` at line 13
- KB_PATTERN regex; `git diff --cached --name-only | grep -E "$KB_PATTERN"`
- Exits 0 if no KB files staged; otherwise enforces "docs: update KB" commit message convention
- NO `OKB_SNAPSHOT` bypass exists today — Plan 04 inserts the 4-line guard

From CLAUDE.md `gsd-browser` rule:
- For ad-hoc screenshots use `gsd-browser`. For structured E2E, place under `tests/e2e/<area>/<spec>.spec.ts` and run via `npx playwright test`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author cross-system-parity.mjs + typed-views.test.js (RED)</name>
  <files>tests/integration/cross-system-parity.mjs, tests/integration/typed-views.test.js</files>
  <read_first>
    - tests/integration/http-api.test.js (jest + fetch single-service template — lines 1-78 per 44-PATTERNS.md citation)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Validation Architecture (commands + behavior expectations) AND § Pitfall 2 (typed-view shape lock — agent/project/content/artifacts/timestamp/session_id/quality)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `tests/integration/cross-system-parity.mjs` (lines 698-747) — service URLs, normalize() pattern, deep-diff per endpoint
    - scripts/observations-api-server.mjs lines 466-549, 689-703, 705-751 (current SQLite-backed handler bodies; the legacy response shapes typed-views.test.js asserts against)
  </read_first>
  <behavior>
    cross-system-parity.mjs:
      - SERVICES array with 3 entries: {name:'A', url:'http://localhost:12436/api/v1'}, {name:'B', url:'http://localhost:3848/api/v1'}, {name:'C', url:'http://localhost:3002/api/v1'}.
      - normalize(obj) helper strips volatile fields: any keys matching /timestamp|createdAt|updatedAt|validFrom|validUntil|id|commit_sha|runId/i are replaced with placeholder `'<normalized>'`.
      - For each endpoint in ['/entities?limit=1','/stats','/ontology/classes']: fetch from all 3, normalize, assert all 3 deep-equal AT THE SHAPE LEVEL (top-level keys + nested key shapes match; data array length is NOT asserted because each system has different data).
      - Smoke test: each service responds 200 to /entities?limit=1 (proves /api/v1 is mounted at all).
      - Honest fail mode: if any service is down, log which one to stderr and fail the test with a clear message (not a generic timeout).
    typed-views.test.js:
      - Test 1: GET http://localhost:12436/api/coding/observations?limit=1 returns 200 + body matches `{ data: [{id:string, agent:string, project:string, content:string, artifacts:Array, timestamp:string, session_id?:string, quality?:string}], total:number, limit:number, offset:number, _metadata?:object }` per Pitfall 2 lock.
      - Test 2: GET /api/coding/digests?limit=1 returns the legacy digest shape (id, date, theme, summary, observation_ids, agents, files_touched, project).
      - Test 3: GET /api/coding/insights?limit=1 returns the legacy insight shape (id, topic, summary, confidence, digest_ids, last_updated, project).
      - Test 4: Query-string contract — passing `agent=claude&project=coding` filters server-side (length(filtered) <= length(unfiltered)).
  </behavior>
  <action>Create `tests/integration/cross-system-parity.mjs` as an ESM script using `node-fetch` (already a coding-side dep — verify via `grep node-fetch package.json` first; if absent use built-in `fetch` from Node 22). Service constants per 44-PATTERNS.md lines 720-728. Use `describe`/`test`/`expect` from a Jest-compatible runner — use built-in node:test instead of Jest to avoid Jest config plumbing for an .mjs script: `import { test, describe, before, after } from 'node:test'; import assert from 'node:assert/strict';`. Tests RED today because /api/v1 mounts do not exist on any of A/B/C (Plans 07/08/09 land them) — failure mode is `fetch failed` or 404. Document the expected failure path in a leading comment. Create `tests/integration/typed-views.test.js` as a Jest spec (the repo's standard runner per package.json). Imports: `const fetch = require('node-fetch'); // or built-in`; tests RED because A's `/api/coding/observations` handler does NOT EXIST today — only `/api/observations` exists. The test asserts the FUTURE typed-view URL Plan 07 adds. No console.log — use throw with descriptive Error messages for diagnostics. File names EXACTLY as listed.</action>
  <verify>
    <automated>test -f tests/integration/cross-system-parity.mjs && test -f tests/integration/typed-views.test.js && grep -c "SERVICES" tests/integration/cross-system-parity.mjs && grep -cE "/api/coding/(observations|digests|insights)" tests/integration/typed-views.test.js | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <done>
    Both files exist with the minimum line counts. cross-system-parity.mjs declares SERVICES with all 3 entries. typed-views.test.js asserts all 3 legacy URL shapes. Both fail today against running A obs-api (network 404 on /api/v1 + /api/coding routes do not exist yet).
  </done>
  <acceptance_criteria>
    - `wc -l tests/integration/cross-system-parity.mjs | awk '{print $1}'` >= 80
    - `wc -l tests/integration/typed-views.test.js | awk '{print $1}'` >= 70
    - `grep -c "localhost:12436/api/v1" tests/integration/cross-system-parity.mjs` >= 1
    - `grep -c "localhost:3848/api/v1" tests/integration/cross-system-parity.mjs` >= 1
    - `grep -c "localhost:3002/api/v1" tests/integration/cross-system-parity.mjs` >= 1
    - `grep -c "normalize" tests/integration/cross-system-parity.mjs` >= 2
    - `grep -cE "/api/coding/observations" tests/integration/typed-views.test.js` >= 1
    - `grep -cE "/api/coding/digests" tests/integration/typed-views.test.js` >= 1
    - `grep -cE "/api/coding/insights" tests/integration/typed-views.test.js` >= 1
    - File names EXACTLY `cross-system-parity.mjs` and `typed-views.test.js` (no evolutionary suffixes)
    - No `test.skip` / `test.todo` / `xdescribe` / `xit` present: `grep -cE "(test\.skip|test\.todo|xdescribe|xit|describe\.skip)" tests/integration/cross-system-parity.mjs tests/integration/typed-views.test.js` returns 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author okb-guard-snapshot-bypass.sh + dashboard-observations.spec.ts (RED)</name>
  <files>tests/integration/okb-guard-snapshot-bypass.sh, tests/e2e/dashboard-observations.spec.ts</files>
  <read_first>
    - scripts/hooks/pre-commit-okb-guard.sh (current state — has NO OKB_SNAPSHOT bypass; Plan 04 inserts the 4-line guard between `set -euo pipefail` and `KB_PATTERN=`)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `scripts/hooks/pre-commit-okb-guard.sh` (lines 571-595 — exact insertion point + canonical bypass snippet)
    - tests/e2e/dashboard/workflow-graph-colors.spec.ts (playwright template — lines 1-90 per 44-PATTERNS.md)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pitfall 2 (dashboard at :3032 reads /api/observations expecting `agent`, `project`, `content`, `artifacts`, `timestamp` — blank fields = migration regression)
    - CLAUDE.md `gsd-browser` rule (for structured E2E, place under `tests/e2e/<area>/<spec>.spec.ts`)
  </read_first>
  <behavior>
    okb-guard-snapshot-bypass.sh:
      - Setup: mkdtemp; `cd $tmp`; `git init -q`; `git config user.email/name`; write a placeholder `.data/exports/foo.json`; `git add -A`.
      - Case A: with `OKB_SNAPSHOT=0` (or unset) invoke `bash $REPO_ROOT/scripts/hooks/pre-commit-okb-guard.sh` and expect non-zero exit (hook complains about KB file commit message convention).
      - Case B: with `OKB_SNAPSHOT=1` invoke the same hook and expect exit 0.
      - Teardown: rm -rf $tmp.
      - Final report: print PASS / FAIL summary to stdout; exit 0 if all cases passed, 1 otherwise.
    dashboard-observations.spec.ts:
      - Test 1: Navigate to http://localhost:3032 and wait for the observations panel (selector `[data-testid="observations-table"]` OR a fallback such as a header element with text matching /observations/i — the planner documents the actual selector in the spec; if the dashboard does not yet expose a `data-testid`, the spec MUST add a comment noting Plan 07 / 11 verification will add the testid OR the test selector will be relaxed).
      - Test 2: After load, locate the first row `[data-testid^="observation-row-"]` (or `tr` under the observations table); assert the `agent` and `project` cells are non-empty per Pitfall 2.
      - Test 3: Smoke screenshot save under `tests/e2e/_artifacts/dashboard-observations-${Date.now()}.png` for audit trail.
  </behavior>
  <action>Create `tests/integration/okb-guard-snapshot-bypass.sh` per the behavior block; use `set -euo pipefail`; resolve REPO_ROOT via `git rev-parse --show-toplevel`; do NOT modify the hook (Plan 04 owns the hook edit); expect Case B to FAIL today because the bypass guard does not exist yet. Document the expected fail in a leading comment. Make the script executable (`chmod +x` via a parallel ls/test in acceptance). Create `tests/e2e/dashboard-observations.spec.ts` per the playwright template from `tests/e2e/dashboard/workflow-graph-colors.spec.ts:13-23` and 65-74. Use Page typing per CLAUDE.md gsd-browser preferences (the spec runs via `npx playwright test`). RED today because: (a) dashboard may not have `data-testid="observations-table"`, (b) post-A-2 migration is what guarantees agent/project populated. Include a leading comment: "RED until Plan 10 migration + Plan 07 typed-view + Plan 11 verification." Use process.stderr.write / console only via test framework reporter — no inline console.log statements. File names EXACTLY as listed.</action>
  <verify>
    <automated>test -x tests/integration/okb-guard-snapshot-bypass.sh && test -f tests/e2e/dashboard-observations.spec.ts && grep -c "OKB_SNAPSHOT=1" tests/integration/okb-guard-snapshot-bypass.sh && grep -c "data-testid" tests/e2e/dashboard-observations.spec.ts | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>
    Both files exist with the minimum line counts. Shell test is executable. Playwright spec uses gsd-browser-compatible selector pattern. Both fail today — shell test fails at Case B (no bypass), playwright fails at selector wait (no data-testid OR empty cells pre-migration).
  </done>
  <acceptance_criteria>
    - `wc -l tests/integration/okb-guard-snapshot-bypass.sh | awk '{print $1}'` >= 40
    - `wc -l tests/e2e/dashboard-observations.spec.ts | awk '{print $1}'` >= 50
    - `test -x tests/integration/okb-guard-snapshot-bypass.sh && echo OK` prints OK
    - `grep -c "OKB_SNAPSHOT=0" tests/integration/okb-guard-snapshot-bypass.sh` >= 1
    - `grep -c "OKB_SNAPSHOT=1" tests/integration/okb-guard-snapshot-bypass.sh` >= 1
    - `grep -c "pre-commit-okb-guard.sh" tests/integration/okb-guard-snapshot-bypass.sh` >= 2
    - `grep -cE "(localhost:3032|3032)" tests/e2e/dashboard-observations.spec.ts` >= 1
    - `grep -c "@playwright/test" tests/e2e/dashboard-observations.spec.ts` >= 1
    - `grep -cE "(test\.skip|test\.todo|describe\.skip)" tests/e2e/dashboard-observations.spec.ts` returns 0
    - File names EXACTLY `okb-guard-snapshot-bypass.sh` and `dashboard-observations.spec.ts` (no evolutionary suffixes)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell-test → repo hook script | Test invokes the hook script with env-var override; isolated tmpdir scope |
| playwright headless browser → :3032 | Local dashboard; internal-only network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Tampering | shell-test tmpdir cleanup | accept | trap-on-exit rm-rf or explicit afterAll; tmpdir confined under os.tmpdir() |
| T-44-02-02 | Information Disclosure | playwright screenshot artifacts | accept | screenshots saved under tests/e2e/_artifacts/ which is gitignored per existing convention (verify with `grep _artifacts .gitignore`); no PII (synthetic test data) |
</threat_model>

<verification>
- Shell test fails with Case B exit non-zero (no OKB_SNAPSHOT bypass yet)
- Playwright spec fails at selector wait or empty-cell assertion (pre-migration state)
- Both RED states are expected per Wave 0 contract
</verification>

<success_criteria>
- 4 coding-side Wave 0 stubs exist and are RED with deterministic failure modes
- File names match exactly (no evolutionary suffixes)
- Combined with Plan 01, the 8-stub Wave 0 set from 44-VALIDATION.md is complete
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-02-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) RED stub count from this plan, (b) running Wave 0 count vs target (4 → 8), (c) literal failure messages observed.
</output>
