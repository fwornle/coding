---
phase: 44-rest-api-git-snapshots
plan: 18
type: execute
wave: 9
depends_on:
  - 44-17
files_modified:
  - src/live-logging/ObservationPruner.js
  - src/retrieval/retrieval-service.js
  - scripts/observations-api-server.mjs
  - tests/integration/observation-pruner.km-core.test.js
  - tests/integration/retrieval-service.freshness-rerank.km-core.test.js
autonomous: false
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "`src/live-logging/ObservationPruner.js` no longer reads or writes SQLite. The 3 SQL statements (cutoff calc + 2 DELETEs) become km-core `findByOntologyClass('Observation'/'Digest')` + per-entity `deleteEntity` calls scoped by `metadata.created_at` ISO comparison."
    - "`src/retrieval/retrieval-service.js` no longer touches SQLite. The single `SELECT json_extract(metadata, '$.codeVerification.verificationRatio') FROM insights WHERE id IN (...)` at line ~341 becomes a sequence of `kmStore.getEntity(legacyId)` lookups reading `entity.metadata.codeVerification.verificationRatio` (or whatever path the post-cutover Insight entity carries — confirmed in Task 1 audit)."
    - "`scripts/observations-api-server.mjs` drops the `_legacyDb`, `ensureLegacyDb()`, and `openLegacyDb` plumbing (lines 40, 89-100, 1539-1540 in the pre-cutover file). The `SafeDatabase` import dies with it. The pruner + retrieval are constructed with `kmStore` instead of `db: legacyDb`."
    - "Acceptance grep: `grep -cE '\\b(db|_legacyDb|legacyDb)\\.(prepare|exec|get|all|run|close)\\b' src/live-logging/ObservationPruner.js src/retrieval/retrieval-service.js scripts/observations-api-server.mjs == 0`."
    - "Acceptance grep: `grep -cE 'observations\\.db|openLegacyDb|SafeDatabase|ensureLegacyDb' scripts/observations-api-server.mjs src/live-logging/ObservationPruner.js src/retrieval/retrieval-service.js == 0` (post-cutover all three files are free of these references — `DB_PATH` constant can remain for the archive-target rename only, then is dropped in Task 5 if archiving)."
    - "`/api/retrieve` keyword + semantic search continues to return correct results post-cutover. The freshness rerank still penalizes insights with `verificationRatio < 1` per the existing 0.3 + 0.7·ratio multiplier."
    - "`ObservationPruner.run()` continues to prune observations older than `retentionDays` (default 7) — verified by integration test that seeds rows with `metadata.created_at = now() - 10d` and asserts they are removed."
    - "`.observations/observations.db` is RENAMED to `.observations/observations.db.archived.<UTC-YYYY-MM-DD>` at Task 5 (operator-gated). All launchd-managed processes — `com.coding.obs-api`, `com.coding.health-coordinator`, `com.coding.etm`, `com.coding.sub-agent-live-{claude,copilot,opencode}`, `com.coding.sub-agent-sweep`, `com.coding.lsl-resolver` — stay alive without errors for ≥1 hour after the rename."
    - "The deferred Plan 44-12 § \"fully unused observations.db\" success criterion (carried through 44-13, 44-14, 44-17) is FINALLY honored — `.observations/observations.db` is archived, not actively read or written by any production code path."
    - "Integration tests: `tests/integration/observation-pruner.km-core.test.js` (≥120 lines, 4 tests GREEN) and `tests/integration/retrieval-service.freshness-rerank.km-core.test.js` (≥80 lines, 3 tests GREEN)."
  artifacts:
    - path: src/live-logging/ObservationPruner.js
      provides: "Pruner reads + deletes through km-core only. Constructor takes `{ kmStore, retentionDays }` instead of `{ db, retentionDays }`. Cutoff comparison done in JS via `Date.now() - retentionDays·24·3600·1000`."
      contains: "kmStore"
    - path: src/retrieval/retrieval-service.js
      provides: "Freshness-rerank metadata lookup goes through `kmStore.getEntity(legacyId)`. The `dbGetter` constructor option is replaced with `kmStoreGetter` (or `kmStore` directly — decide in Task 1)."
      contains: "kmStore"
    - path: scripts/observations-api-server.mjs
      provides: "Removes `_legacyDb`, `ensureLegacyDb`, `openLegacyDb` import, `SafeDatabase` import (transitively), and DB_PATH-as-legacy-handle plumbing. Pruner + retrieval are constructed with `kmStore` from `ensureKMStore()`."
      contains: "kmStore"
    - path: tests/integration/observation-pruner.km-core.test.js
      provides: "Tests pruner against tmpdir GraphKMStore: seed observations + digests across retention boundary, run prune, assert correct rows deleted, perf assertion (1000 obs prune <1s)."
      min_lines: 120
      contains: "ObservationPruner"
    - path: tests/integration/retrieval-service.freshness-rerank.km-core.test.js
      provides: "Tests freshness-rerank: seed insights with varying verificationRatio, assert RRF scores are multiplied correctly per the 0.3+0.7·ratio formula."
      min_lines: 80
      contains: "RetrievalService"
  key_links:
    - from: src/live-logging/ObservationPruner.js
      to: km-core GraphKMStore (findByOntologyClass + deleteEntity)
      via: "store.findByOntologyClass('Observation', { predicate: e => e.metadata.created_at < cutoff }) + store.deleteEntity(id)"
      pattern: "findByOntologyClass"
    - from: src/retrieval/retrieval-service.js
      to: km-core GraphKMStore (getEntity + metadata read)
      via: "store.getEntity(id) -> entity.metadata.codeVerification.verificationRatio"
      pattern: "getEntity"
threat_model:
  - id: T-44-18-01
    threat: "Pruner's per-row `deleteEntity` is O(N) network calls vs SQLite's single DELETE statement. At production scale (~4k observations growing) a daily prune of ≥1k expired rows could dominate."
    severity: medium
    mitigation: "Batch the delete loop into chunks (e.g. 100 ids/batch) and log progress so a long prune is observable. Perf gate: 1000 obs prune ≤1s on this machine (matches T-44-17-01 budget). If km-core gains a `deleteEntities([])` bulk helper later, swap in."
  - id: T-44-18-02
    threat: "`retrieval-service.js` is called per request; replacing one SQL fetch with N getEntity round-trips multiplies latency on the rerank path."
    severity: medium
    mitigation: "Promise.all the getEntity batch (km-core LevelDB reads are sub-ms locally). Perf gate: rerank lookup for 20 insight ids ≤50ms. If perf regresses, add `getEntities([])` to km-core in Task 1."
  - id: T-44-18-03
    threat: "Archiving `.observations/observations.db` could break a forgotten consumer not surfaced by grep — e.g. a launchd plist env var, a script in `.specstory/history/scripts/`, or an external user-facing process."
    severity: medium
    mitigation: "Task 4 runs `grep -rl 'observations\\.db' . | grep -v node_modules | grep -v specstory | grep -v archive` to confirm zero referenced sites. Operator gate at Task 5 includes a 1-hour soak post-rename, watching launchd + obs-api + ETM + health-coordinator + sub-agent-live + sweep + lsl-resolver logs for errors."
  - id: T-44-18-04
    threat: "Test infra still uses `observations.db` in `tests/integration/*.legacy*.test.js` and similar — renaming the production file could break tests."
    severity: low
    mitigation: "Task 1 audit lists all test references. Tests use their own tmpdir SQLite files, NOT the production `.observations/observations.db`, so the rename is invisible to test runs. Confirmed by reading the test setup of `tests/integration/observation-writer.dedup.test.js`."
  - id: T-44-18-05
    threat: "Pruner-via-deleteEntity does not clean up Qdrant vectors associated with removed observations — vector store keeps drifting from km-core truth."
    severity: low
    mitigation: "Document as a known follow-up. Plan 44-18 scope is the SQLite cutover; Qdrant sync is a separate maintenance concern (Phase 42 has open work on this). Acceptance: SUMMARY records the deferred item."
---

<objective>
Plan 44-17 (commit `f3701499f`) cut ObservationConsolidator to km-core. Task 5 of that plan recorded the two LAST intentional SQLite consumers still on `.observations/observations.db`:

- `ObservationPruner` (`scripts/observations-api-server.mjs:165`) — periodic retention DELETE pass
- `RetrievalService` (`src/retrieval/retrieval-service.js:341`) — freshness-rerank metadata lookup for `/api/retrieve`

Both are small and well-bounded. After this plan, `.observations/observations.db` becomes truly unused — the deferred Plan 44-12 § "fully unused observations.db" success criterion (carried through 44-13, 44-14, 44-17) is finally honored, and the file can be archived.

`autonomous: false` because Task 5 is the SQLite-archive operator gate (rename + 1-hour soak verification).
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-17-SUMMARY.md
@.planning/phases/44-rest-api-git-snapshots/44-17-AUDIT.md
@CLAUDE.md
@src/live-logging/ObservationPruner.js
@src/retrieval/retrieval-service.js
@scripts/observations-api-server.mjs

<interfaces>

## SQLite touch surface remaining (2 sites total)

| File | Line | Statement | Purpose |
|------|------|-----------|---------|
| `src/live-logging/ObservationPruner.js` | 67 | `SELECT datetime('now', ?) AS t` | Cutoff calc — easily replaced with JS `Date.now() - retentionDays*24*3600*1000` |
| `src/live-logging/ObservationPruner.js` | 70 | `DELETE FROM observations WHERE created_at < datetime('now', ?)` | Retention prune (observations) |
| `src/live-logging/ObservationPruner.js` | 71 | `DELETE FROM digests WHERE created_at < datetime('now', ?)` | Retention prune (digests) |
| `src/retrieval/retrieval-service.js` | 341 | `SELECT id, json_extract(metadata, '$.codeVerification.verificationRatio') AS ratio FROM insights WHERE id IN (...)` | Freshness-rerank lookup for `/api/retrieve` |

## Migration mapping

| SQLite | km-core |
|--------|---------|
| `SELECT datetime('now', ?)` | JS: `new Date(Date.now() - retentionDays*86400000).toISOString()` |
| `DELETE FROM observations WHERE created_at < ?` | `kmStore.findByOntologyClass('Observation', { predicate: e => e.metadata.created_at < cutoff })` → loop `kmStore.deleteEntity(e.id)` |
| `DELETE FROM digests WHERE created_at < ?` | Same pattern for `'Digest'` class |
| `SELECT json_extract(metadata, '$.codeVerification.verificationRatio') FROM insights WHERE id IN (...)` | `await Promise.all(ids.map(id => kmStore.findByLegacyId({system:'A', id}).then(e => e?.metadata?.codeVerification?.verificationRatio)))` |

## What this plan does NOT touch

- `scripts/migrate-sqlite-to-kmcore.mjs` — already done its job; out of scope (does still reference SQLite but it's a migration script, not a runtime consumer)
- Any test file under `tests/` that uses tmpdir SQLite for SQLite-era reference behavior — those use throwaway DBs, not the production file
- The deleted `scripts/bridge-obs-from-kmcore.mjs` (removed in Plan 44-17 Task 4) — already gone
- Qdrant vector cleanup post-prune — out of scope; deferred (see T-44-18-05)

After this plan, the **only** intentional consumers of `.observations/observations.db` should be: none. The file is archivable.

</interfaces>
</context>

<tasks>

<task id="1">
<description>Audit the 4 SQLite call sites + the `_legacyDb`/`openLegacyDb`/`SafeDatabase` plumbing in obs-api. Confirm the freshness-rerank metadata path on post-cutover Insight entities. Confirm zero external consumers reference `observations.db`.</description>
<action>
1. Read each of the 4 call sites. Document the SQL, intended semantic, and km-core replacement.
2. Read a sample post-cutover Insight entity from km-core to confirm the metadata path. The pre-cutover SQLite shape used `json_extract(metadata, '$.codeVerification.verificationRatio')`. The post-cutover Insight entity carries this via `legacyInsightToEntity` (already exists in km-core's legacy-ingest adapter). Confirm via:
   ```bash
   curl -s 'http://localhost:12436/api/coding/insights?limit=1' | python3 -m json.tool
   ```
   and grep the response shape for `codeVerification` / `verificationRatio`. If the path differs post-cutover, document the actual path and use that in Task 2.
3. Audit external references:
   ```bash
   grep -rlE 'observations\.db' . --include='*.js' --include='*.mjs' --include='*.ts' --include='*.sh' --include='*.json' --include='*.md' | grep -v node_modules | grep -v specstory | grep -v .planning | grep -v archived
   ```
   Document each match: (a) production code (will be cut in Task 2-3), (b) migration script (out of scope), (c) docs/comments (will be updated in Task 4).
4. Decide bulk-vs-loop strategy for pruner (T-44-18-01). Recommend: per-row `deleteEntity` with chunked progress log every 100 rows.
5. Commit Task 1 docs: `docs(44-18): audit pruner + retrieval SQLite surface`.
</action>
<acceptance>
- `.planning/phases/44-rest-api-git-snapshots/44-18-AUDIT.md` exists, lists all 4 call sites + the metadata-path confirmation + the external-reference grep results.
- One docs commit.
</acceptance>
</task>

<task id="2">
<description>Cut `ObservationPruner.js` to km-core. Replace constructor signature, cutoff calc, and 2 DELETEs.</description>
<action>
1. Replace `ObservationPruner` constructor signature: `{ db, retentionDays }` → `{ kmStore, retentionDays }`.
2. Replace the cutoff calc (line 67) with `const cutoffISO = new Date(Date.now() - this.retentionDays * 86400000).toISOString();`.
3. Replace the 2 DELETEs (lines 70-71) with:
   ```js
   const obsToDelete = await this.kmStore.findByOntologyClass('Observation', {
     predicate: e => (e.metadata?.created_at || '') < cutoffISO,
   });
   for (let i = 0; i < obsToDelete.length; i += 100) {
     const chunk = obsToDelete.slice(i, i + 100);
     await Promise.all(chunk.map(e => this.kmStore.deleteEntity(e.id)));
     process.stderr.write(`[ObservationPruner] Pruned ${i + chunk.length}/${obsToDelete.length} observations\n`);
   }
   // same pattern for 'Digest' class
   ```
4. Update `scripts/observations-api-server.mjs` to construct the pruner with `kmStore` instead of `_legacyDb`. The `_pruner` initializer at line 163-165 becomes:
   ```js
   const kmStore = await ensureKMStore();
   if (!kmStore) return null;
   _pruner = new ObservationPruner({ kmStore, retentionDays: _writer.retentionDays });
   ```
5. Update `tests/live-logging/ObservationPruner.test.js` (existing pre-cutover test) — it likely uses a SQLite tmpdir setup. Either:
   - (a) Rewrite to use a tmpdir GraphKMStore (preferred — matches the new contract), or
   - (b) Skip-comment those tests with a pointer to the new integration test in Task 3.
   Choose based on what's reasonable in the existing test's scope.
6. Acceptance greps:
   - `grep -cE '\bdb\.(prepare|exec|get|all|run)\b' src/live-logging/ObservationPruner.js == 0`
   - `grep -c 'DELETE FROM' src/live-logging/ObservationPruner.js == 0`
   - `grep -c 'kmStore' src/live-logging/ObservationPruner.js >= 3`
7. Run the existing test suite — `npx vitest run tests/live-logging/ObservationPruner.test.js` — must stay GREEN (with the test rewrite or skip-comment from step 5).
8. Commit: `feat(44-18): cut ObservationPruner to km-core`.
</action>
<acceptance>
- All 3 grep gates pass.
- `tests/live-logging/ObservationPruner.test.js` stays GREEN.
- `_pruner` initializer in obs-api uses `kmStore`, not `_legacyDb`.
- One commit.
</acceptance>
</task>

<task id="3">
<description>Cut `RetrievalService` freshness-rerank to km-core. Drop the `dbGetter` constructor option in favor of `kmStoreGetter` (or direct kmStore).</description>
<action>
1. Replace the constructor's `dbGetter` option with `kmStoreGetter` (or `kmStore` directly — decide based on existing construction sites).
2. Replace the single `db.prepare(...).all(...ids)` at line 341 with:
   ```js
   const entities = await Promise.all(
     ids.map(id => kmStore.findByLegacyId({ system: 'A', id }).catch(() => null))
   );
   for (let i = 0; i < ids.length; i++) {
     const entity = entities[i];
     if (!entity) continue;
     const ratio = Number(entity.metadata?.codeVerification?.verificationRatio);
     if (Number.isFinite(ratio)) ratioById.set(String(ids[i]), ratio);
   }
   ```
3. Update construction sites in `scripts/observations-api-server.mjs` (lines 130-145 area — the `RetrievalService` constructor passes `dbGetter: ensureLegacyDb`). Pass `kmStore: ensureKMStore` (lazy via getter, or eager — match the existing pattern).
4. Acceptance greps:
   - `grep -c 'db\.prepare' src/retrieval/retrieval-service.js == 0`
   - `grep -c 'kmStore' src/retrieval/retrieval-service.js >= 2`
5. Run `tests/integration/retrieval-service*.test.js` — must stay GREEN.
6. Create `tests/integration/retrieval-service.freshness-rerank.km-core.test.js` covering:
   - Seed 5 insights in tmpdir kmStore with varying `metadata.codeVerification.verificationRatio` (0.5, 0.8, 1.0, undefined, missing-metadata).
   - Assert the RRF rerank multiplier matches the 0.3 + 0.7·ratio formula for ratio in [0,1].
   - Assert `verificationRatio === undefined` and missing-metadata cases leave the score untouched.
7. Run the new test — GREEN.
8. Commit: `feat(44-18): cut RetrievalService freshness-rerank to km-core`.
</action>
<acceptance>
- Both grep gates pass.
- New `tests/integration/retrieval-service.freshness-rerank.km-core.test.js` exists, ≥80 lines, 3+ tests GREEN.
- Existing retrieval-service tests stay GREEN.
- One commit (or one feat commit + one test commit).
</acceptance>
</task>

<task id="4">
<description>Drop `_legacyDb`/`openLegacyDb`/`SafeDatabase` plumbing from obs-api. Audit remaining `observations.db` references (none should remain in production code).</description>
<action>
1. Remove from `scripts/observations-api-server.mjs`:
   - `import { openDatabase as openLegacyDb } from '../src/live-logging/SafeDatabase.js'` (line 40)
   - `let _legacyDb = null` (line 90)
   - `function ensureLegacyDb()` (lines 91-101)
   - `if (_legacyDb) { try { _legacyDb.close() } ... }` (shutdown handler around line 1539)
2. Confirm no remaining references with:
   ```bash
   grep -nE '_legacyDb|openLegacyDb|SafeDatabase|ensureLegacyDb' scripts/observations-api-server.mjs
   # expected: 0 results
   grep -nE 'observations\.db' scripts/observations-api-server.mjs
   # expected: 0 results (or only the DB_PATH constant if a comment still references the archived path)
   ```
3. Optionally drop the `DB_PATH` constant if it's no longer referenced. (It may still be needed for the archive-target rename in Task 5; if so, leave it but add a comment.)
4. Run all obs-api integration tests — must stay GREEN:
   ```bash
   npx vitest run tests/integration/obs-api*.test.js tests/integration/observation-*.test.js
   ```
5. Add integration test `tests/integration/observation-pruner.km-core.test.js`:
   - Tmpdir GraphKMStore + ObservationPruner.
   - Seed 1000 observation entities with `metadata.created_at` spanning the retention boundary (some 30d old, some 1d old).
   - Run prune with `retentionDays=7`.
   - Assert only the 30d-old rows deleted.
   - Perf gate: prune <1s on this machine (matches T-44-18-01 budget).
   - Test 4: same for Digest entities.
6. Commit: `feat(44-18): drop _legacyDb plumbing from obs-api + integration tests`.
</action>
<acceptance>
- All 2 grep gates pass.
- `tests/integration/observation-pruner.km-core.test.js` exists, ≥120 lines, 4+ tests GREEN including perf gate.
- All obs-api + observation-* integration tests stay GREEN.
- One commit.
</acceptance>
</task>

<task id="5" type="checkpoint:human-verify" gate="blocking">
<description>Operator gate: archive `.observations/observations.db` after a 1-hour soak confirms no remaining consumer.</description>
<what-built>
After Tasks 1-4 land, no production code path opens `.observations/observations.db`. obs-api now constructs ObservationPruner + RetrievalService with km-core. The audit grep in Task 4 confirms zero remaining references in production code.

Operator decides:

- **Approved (archive)**: rename `.observations/observations.db` → `.observations/observations.db.archived.<UTC-YYYY-MM-DD>`. obs-api keeps running cleanly (no `_legacyDb` to open). The `DB_PATH` constant in obs-api becomes truly unused — remove it as a final cleanup commit.
- **Approved (defer)**: Keep the file in place for a manual review window. Plan 44-18 still completed (the cutover happened); the archive is a separate manual step the operator runs whenever.
- **Issues**: operator notes specific concerns; plan re-runs or pivots.

Plan 44-12 § "fully unused observations.db" promise is now ready to be honored — the file is archived on the "approved" path, or marked-for-archive-pending on the "defer" path.
</what-built>
<how-to-verify>
1. Confirm `grep -rE 'observations\.db' src/ scripts/ integrations/ --include='*.js' --include='*.mjs' --include='*.ts'` returns no production code matches (only comments or test setup with tmpdir paths).
2. Confirm dashboard at `localhost:3032` still renders correctly:
   - `/digests` shows the 6 today's digests + historical
   - `/insights` shows current insights
   - `/retrieve?q=<test query>` returns results with freshness rerank applied
3. Confirm `launchctl list | grep com.coding` shows all 8 daemons in state `0` (no kickstart_count anomalies for 1 hour post-archive):
   - `com.coding.obs-api`
   - `com.coding.health-coordinator`
   - `com.coding.etm`
   - `com.coding.sub-agent-live-{claude,copilot,opencode}`
   - `com.coding.sub-agent-sweep`
   - `com.coding.lsl-resolver`
4. Confirm `/tmp/obs-api.log` shows no `legacyDb` / `observations.db` errors in the soak window.
5. Confirm next consolidator pass logs digest creation cleanly via km-core (no SQLite reads).
</how-to-verify>
<resume-signal>Type "approved (archive)", "approved (defer)", or "issues: <description>"</resume-signal>
</task>

</tasks>

<success_criteria>
- ObservationPruner reads + deletes exclusively through km-core (0 SQL statements).
- RetrievalService freshness-rerank reads exclusively through km-core (0 SQL statements).
- obs-api drops `_legacyDb` / `openLegacyDb` / `SafeDatabase` plumbing.
- 2 new integration tests cover the cutover (pruner + freshness-rerank), 7+ tests total GREEN.
- `.observations/observations.db` is archived (Task 5 operator gate, "approved (archive)" path).
- The deferred Plan 44-12 § "fully unused observations.db" promise — carried through 44-13, 44-14, 44-17 — is FINALLY honored.
</success_criteria>

<rollback>
If Task 2 or 3 breaks production semantics:
1. `git revert HEAD` on the offending commit. Task 1's audit doc stays.
2. Pruner / retrieval falls back to its SQLite path.
3. Re-open as a smaller-scope plan: split per-consumer (pruner cutover first, retrieval cutover second).

Task 5's rename is the only destructive step in this plan and is operator-gated. If it goes wrong:
- `mv .observations/observations.db.archived.YYYY-MM-DD .observations/observations.db` restores the file.
- Re-kickstart obs-api if it crashed.

The km-core additions in earlier tasks are pure and never need to be reverted.
</rollback>
