---
phase: 44-rest-api-git-snapshots
plan: 05
type: execute
wave: 1
depends_on:
  - 44-01
files_modified:
  - lib/km-core/src/adapters/observation-view.ts
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "km-core ships `observationToLegacy`, `digestToLegacy`, `insightToLegacy` — pure reverse-direction reshape functions that turn km-core entities back into A's legacy SQLite row shapes."
    - "All three functions handle missing optional fields gracefully (legacyId fallback to entity.id; description fallback chain summary → content → entity.description; artifacts default []; agent/project default 'unknown')."
    - "Wave 0 observation-view.test.ts flips RED → GREEN."
  artifacts:
    - path: lib/km-core/src/adapters/observation-view.ts
      provides: "Three pure functions + their TypeScript interfaces — Entity → LegacyObservation/LegacyDigest/LegacyInsight"
      min_lines: 100
      contains: "observationToLegacy"
  key_links:
    - from: lib/km-core/src/adapters/observation-view.ts
      to: lib/km-core/src/types/entity.ts
      via: "type-only import { Entity }"
      pattern: "import type \\{ Entity \\}"
---

<objective>
Land the A-4 typed-view reshape primitives in km-core. CONTEXT A-4 mandates that A's legacy `/api/coding/observations|digests|insights` endpoints become typed views over km-core entities — the reshape function lives in km-core (single source of truth) and is consumed by A's server (Plan 07). This plan is the smallest atomic unit before Plan 06 imports it into the root barrel and Plan 07 wires it into the obs-api server.

Purpose: Pure functions with zero side-effects are the lowest-risk km-core deliverable; landing them here in Wave 1 (parallel to Plans 03/04) keeps Plan 06's surface focused on routing.
Output: `lib/km-core/src/adapters/observation-view.ts` with three reshape functions. Plan 01 observation-view.test.ts goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@lib/km-core/CLAUDE.md
@lib/km-core/src/adapters/online/mapper.ts
@lib/km-core/src/types/entity.ts
@lib/km-core/tests/integration/observation-view.test.ts
@scripts/observations-api-server.mjs

<interfaces>
<!-- The reshape target shapes. These are the field sets the legacy /api/observations|digests|insights responses must include. Pitfall 2 says missing fields = blank cells on dashboard. -->

From scripts/observations-api-server.mjs handlers (legacy shape source-of-truth):
- LegacyObservation: { id, agent, project, content, artifacts:string[], timestamp, session_id?, quality? } — derived from observations SELECT projection at line 466-549
- LegacyDigest: { id, date, theme, summary, observation_ids:string[], agents:string[], files_touched:string[], project } — derived from digests SELECT at line 689-703
- LegacyInsight: { id, topic, summary, confidence, digest_ids:string[], last_updated, project } — derived from insights SELECT at line 705-751

From lib/km-core/src/adapters/online/mapper.ts (Phase 41 — REVERSE direction template):
- File structure: type-only Entity import; pure functions; no console.log; no I/O.
- Header comment block explaining canonical legacyId placement (top-level field, not metadata).

From CONTEXT C-2 + Phase 39 entity shape: legacyId.system='A' for A-side migrated entities; metadata holds all other legacy fields (agent, project, summary, etc.).

From RESEARCH Pattern 3 (44-RESEARCH.md lines 282-315 — observationToLegacy implementation skeleton).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author observation-view.ts (observationToLegacy + digestToLegacy + insightToLegacy)</name>
  <files>lib/km-core/src/adapters/observation-view.ts</files>
  <read_first>
    - lib/km-core/CLAUDE.md (no-parallel-versions — file is EXACTLY `observation-view.ts`)
    - lib/km-core/src/adapters/online/mapper.ts (full file — REVERSE direction template; mirror file layout/header pattern/no-console-log discipline)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pattern 3 (lines 282-315 — observationToLegacy skeleton)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `lib/km-core/src/adapters/observation-view.ts` (lines 265-329 — file-layout + reverse-direction reshape template)
    - lib/km-core/tests/integration/observation-view.test.ts (Plan 01 RED test — 4 test blocks; the test asserts the EXACT signatures and fallback semantics this task delivers)
    - scripts/observations-api-server.mjs lines 466-549 (legacy observation shape SELECT — verify field names match), lines 689-703 (digests), lines 705-751 (insights)
  </read_first>
  <behavior>
    - observationToLegacy(entity): id = entity.legacyId?.id ?? entity.id; agent = metadata.agent ?? 'unknown'; project = metadata.project ?? 'unknown'; content = metadata.summary ?? metadata.content ?? entity.description ?? ''; artifacts = Array.isArray(metadata.artifacts) ? metadata.artifacts : []; timestamp = metadata.createdAt ?? entity.validFrom ?? ''; session_id = metadata.session_id; quality = metadata.quality ?? 'normal'.
    - digestToLegacy(entity): id (same fallback chain); date = metadata.date ?? entity.validFrom; theme = metadata.theme ?? ''; summary = metadata.summary ?? entity.description ?? ''; observation_ids = Array.isArray(metadata.observation_ids) ? ... : []; agents = Array.isArray(metadata.agents) ? ... : []; files_touched = Array.isArray(metadata.files_touched) ? ... : []; project = metadata.project ?? 'unknown'.
    - insightToLegacy(entity): id (same chain); topic = metadata.topic ?? entity.name; summary = metadata.summary ?? entity.description ?? ''; confidence = typeof metadata.confidence === 'number' ? metadata.confidence : 0; digest_ids = Array.isArray(metadata.digest_ids) ? ... : []; last_updated = metadata.last_updated ?? entity.validFrom ?? ''; project = metadata.project ?? 'unknown'.
    - All functions are PURE — no I/O, no async, no side effects.
    - All fallbacks logged in the function's leading comment.
  </behavior>
  <action>Create `lib/km-core/src/adapters/observation-view.ts`. Use the Phase 41 mapper.ts header pattern:
    - Leading block-comment naming the SOURCE (CONTEXT A-4, Phase 41 mapper REVERSE direction, RESEARCH Pattern 3), the CANONICAL LEGACYID PLACEMENT note (top-level Entity field per CF-D37/entity.ts:147), and the no-console-log discipline.
    - `import type { Entity } from '../types/entity.js';`
    - Three exported interfaces: `LegacyObservation`, `LegacyDigest`, `LegacyInsight` (field shape per the <interfaces> block above).
    - Three exported functions with the fallback chains per the <behavior> block.
    - One comment line per function header explaining the legacy shape mapping (mention the scripts/observations-api-server.mjs line numbers the shape derives from).
    Honor `no-console-log` (no diagnostic emission — diagnostics live in caller). File name EXACTLY `observation-view.ts` (km-core CLAUDE.md no-evolutionary-names). NOT EXPORTED from `src/index.ts` in this plan — Plan 06 owns the root barrel update.</action>
  <verify>
    <automated>cd lib/km-core && npm run build 2>&1 | tail -3 && npx vitest run tests/integration/observation-view.test.ts</automated>
  </verify>
  <done>
    observation-view.ts exists with 3 exported functions + 3 interfaces; all 4 test blocks in Plan 01 observation-view.test.ts pass. No console.log calls. Build clean.
  </done>
  <acceptance_criteria>
    - `test -f lib/km-core/src/adapters/observation-view.ts` (file exists)
    - `wc -l lib/km-core/src/adapters/observation-view.ts | awk '{print $1}'` >= 100
    - `grep -c "export function observationToLegacy" lib/km-core/src/adapters/observation-view.ts` returns 1
    - `grep -c "export function digestToLegacy" lib/km-core/src/adapters/observation-view.ts` returns 1
    - `grep -c "export function insightToLegacy" lib/km-core/src/adapters/observation-view.ts` returns 1
    - `grep -c "export interface LegacyObservation" lib/km-core/src/adapters/observation-view.ts` returns 1
    - `grep -c "export interface LegacyDigest" lib/km-core/src/adapters/observation-view.ts` returns 1
    - `grep -c "export interface LegacyInsight" lib/km-core/src/adapters/observation-view.ts` returns 1
    - `grep -c "import type { Entity }" lib/km-core/src/adapters/observation-view.ts` returns 1 (type-only import — erases at compile time)
    - `grep -cE "console\\.(log|error|warn|info|debug)" lib/km-core/src/adapters/observation-view.ts` returns 0 (no-console-log rule)
    - `grep -cE "\\bawait\\b" lib/km-core/src/adapters/observation-view.ts` returns 0 (pure synchronous functions)
    - `cd lib/km-core && npm run build 2>&1 | grep -cE "error TS"` returns 0
    - `cd lib/km-core && npx vitest run tests/integration/observation-view.test.ts 2>&1 | grep -cE "(Tests\\s+\\d+\\s+passed|all passing)"` >= 1 (4/4 GREEN — Plan 01 stub goes GREEN)
    - File name EXACTLY `observation-view.ts`; no `observation-view-v2.ts` or `enhanced-observation-view.ts` variants: `find lib/km-core/src/adapters -name 'observation-view*' | wc -l | awk '{exit ($1==1)?0:1}'`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| km-core entity → legacy shape | reshape happens server-side; no trust boundary crossing in this pure function |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05-01 | Information Disclosure | Reshape includes metadata fields verbatim | accept | metadata is internal-only; A's dashboard is the consumer; Pitfall 2 lock keeps shape stable. If externalized later, reshape becomes the place to redact |
| T-44-05-02 | Tampering | Pure functions; no I/O | n/a | nothing to tamper with |
| T-44-05-SC | n/a | No package installs in this plan | n/a | type-only import + Node stdlib |
</threat_model>

<verification>
```bash
cd lib/km-core && npx vitest run tests/integration/observation-view.test.ts
```
Expected: 4/4 GREEN. Wave 1 km-core deliverables status after this plan: contracts (Plan 03), SnapshotManager (Plan 04), observation-view (this plan) — all GREEN; api-router.test.ts still RED until Plan 06.
</verification>

<success_criteria>
- 3 reshape functions land with consistent fallback semantics
- Type-only Entity import (no runtime dep on entity.ts)
- Plan 01 observation-view.test.ts: GREEN
- No console.log; no async; no I/O
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-05-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) test result (4/4), (b) Wave 1 km-core status (3/3 plans GREEN: Plans 03/04/05), (c) confirmation that root barrel export is deferred to Plan 06.
</output>
