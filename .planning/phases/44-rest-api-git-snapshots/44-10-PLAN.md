---
phase: 44-rest-api-git-snapshots
plan: 10
type: execute
wave: 5
depends_on:
  - 44-07
files_modified:
  - scripts/migrate-sqlite-to-kmcore.mjs
  - .observations/observations.db
autonomous: false
requirements:
  - API-01
must_haves:
  truths:
    - "`scripts/migrate-sqlite-to-kmcore.mjs` reads ~800 observations + ~250 digests + ~77 insights from `.data/knowledge.db` (or `.observations/observations.db` — verify path during execution) and mints km-core entities with `legacyId={system:'A', id:<rowid>}`, ontologyClass set per record kind, all legacy fields preserved under metadata."
    - "Pitfall 3 honored: every migrated entity has BOTH `entityType='Observation|Digest|Insight'` AND `ontologyClass='Observation|Digest|Insight'` set."
    - "Migration is idempotent via legacyId match — rerun is safe and prints `skipped=N`."
    - "5% error budget per Phase 42 Plan 5 precedent — fail-loud if exceeded."
    - "Pre-cutover safety: `.data/knowledge.db.backup-pre-phase-44` (or `.observations/observations.db.backup-pre-phase-44`) copy taken BEFORE table drops; documented rollback procedure in SUMMARY (restore the backup + restart with `USE_SQLITE_LEGACY=1` env flag)."
    - "After migration runs successfully + dashboard smoke (Plan 11) passes, SQLite tables `observations`, `digests`, `insights` are DROPPED; tables `budget_events`, `session_metrics`, `embedding_cache` STAY (CONTEXT A-3)."
    - "Plan 02 typed-views.test.js DATA shape: post-migration `data:[<actual observation>]` not `data:[]` (was shape-only-GREEN in Plan 07; now data-GREEN)."
    - "A-side dashboard at :3032 renders observations correctly after migration (Plan 11 verifies via gsd-browser smoke)."
  artifacts:
    - path: scripts/migrate-sqlite-to-kmcore.mjs
      provides: "One-shot SQLite → km-core migration with --dry-run, --resume, --run-id, --batch-size flags; idempotent via legacyId; structured stdout summary"
      min_lines: 250
      contains: "legacyId"
    - path: .observations/observations.db.backup-pre-phase-44
      provides: "Pre-cutover safety backup; documented rollback path"
      contains: "(binary)"
  key_links:
    - from: scripts/migrate-sqlite-to-kmcore.mjs
      to: km-core LevelDB at .data/knowledge-graph/leveldb
      via: "GraphKMStore.putEntity with trusted-path opts.skipOntologyCheck:true (Phase 37 BC-2 widening) for A's ad-hoc Observation/Digest/Insight classes"
      pattern: "putEntity"
    - from: .observations/observations.db (post-migration)
      to: scripts/observations-api-server.mjs typed views (Plan 07)
      via: "/api/coding/observations now returns the migrated entities"
      pattern: "findByOntologyClass.*Observation"
---

<objective>
A-2 + A-3 execution: author `scripts/migrate-sqlite-to-kmcore.mjs` (mirroring Phase 43 Plan 07 template + RESEARCH Example 4), take a pre-cutover backup of the SQLite DB, run the migration on the actual A install, then (after Plan 11 dashboard smoke confirms typed views work with migrated data) DROP the three legacy tables. Per CONTEXT A-1 this CLOSES the parallel-path asymmetry deferred since Phase 42 + 43.

The migration script is autonomous; the actual production run, backup, and table-drop steps are HUMAN-VERIFY checkpoints because they are destructive on operator's live `.observations/observations.db`. Pitfall 3 (set BOTH `entityType` and `ontologyClass`) is hardcoded into the entity construction so the typed views (Plan 07) find the migrated rows immediately.

Per RESEARCH Open Q4: the SQLite table-drop is split into a SEPARATE step AFTER fixtures + dashboard smoke pass. This plan's last task is therefore a human-verify checkpoint gating the destructive drop on operator confirmation that Plan 11 verification passed.

Purpose: Bring A's live data onto km-core. Without this plan, Plan 07's typed views return empty arrays forever.
Output: Migration script + executed migration + backed-up SQLite + table drops gated on operator confirmation. Plan 02 typed-views.test.js now passes with REAL data; cross-system-parity A-leg returns populated /api/v1/entities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@.planning/phases/43-okm-cross-repo-migration-c/43-CONTEXT.md
@CLAUDE.md
@lib/km-core/CLAUDE.md
@scripts/observations-api-server.mjs

<interfaces>
<!-- The SQLite source schema + km-core target shape. -->

From scripts/observations-api-server.mjs (verify the actual DB path BEFORE editing; CONTEXT mentions `.data/knowledge.db` but the file system more likely uses `.observations/observations.db` per CLAUDE.md `feedback_e2e_verify.md` lesson — `find . -maxdepth 4 -name '*.db' -path '*observations*' 2>/dev/null` should reveal):
- SQLite schema for `observations`: id TEXT PK, summary TEXT, messages TEXT, agent TEXT, session_id TEXT, source_file TEXT, created_at TEXT, metadata TEXT, content_hash TEXT, quality TEXT, digested_at TEXT
- SQLite schema for `digests`: per existing handler at lines 689-703 — id, date, theme, summary, observation_ids, agents, files_touched, project
- SQLite schema for `insights`: per existing handler at lines 705-751 — id, topic, summary, confidence, digest_ids, last_updated, project
- Tables to PRESERVE (CONTEXT A-3): budget_events, session_metrics, embedding_cache

From 44-RESEARCH.md § Example 4 (lines 797-927): nearly-complete TS skeleton for the migration script — argv parsing, ontologyDir resolution, SQLite read, km-core write with Pitfall 3 BOTH fields set, error budget, structured summary.

From CLAUDE.md `km-core scripts` mandatory rule:
- `resolveOntologyDir()` helper REQUIRED before GraphKMStore construction (Phase 41 lesson) — pattern in RESEARCH Example 4 lines 827-838.

From Phase 42-05 SUMMARY (per STATE.md line 162):
- B's migration used trusted-path bulk writes (`skipOntologyCheck:true`) for ad-hoc classes not in the ontology. A's Observation/Digest/Insight classes are similarly NOT in `.data/ontologies/upper.json` today (verify via grep). Use `skipOntologyCheck:true` per the same precedent.
- 5% error budget per Phase 42 Plan 5 precedent: fail-loud if exceeded.

From Phase 43 Plan 07 SUMMARY:
- migrate-okm-json-to-kmcore.mjs is the template: flags `--dry-run`, `--resume`, `--batch-size`, `--run-id`; structured stdout JSON summary; exit code: 0 on ok, 1 on budget exceeded, 2 on fatal.

From RESEARCH § Pitfall 3:
- entity.entityType AND entity.ontologyClass MUST BOTH be set to 'Observation|Digest|Insight'. GraphKMStore.findByOntologyClass checks both via OR-gate; failing to set both means typed views return empty results.

From RESEARCH § Open Q4:
- A-3 table drop happens AFTER fixtures + dashboard smoke. Backup taken FIRST. Rollback path: restore backup + restart with USE_SQLITE_LEGACY=1 env flag (which is a future-phase escape hatch — for now we document it but don't wire the flag into observations-api-server.mjs because Plan 07's deletion of the SQLite handlers is the actual cutover).

From CONTEXT autonomous:false:
- destructive operations (backup + migration on live DB + table drops) gated on `checkpoint:human-verify` per the user-decision protocol.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/migrate-sqlite-to-kmcore.mjs</name>
  <files>scripts/migrate-sqlite-to-kmcore.mjs</files>
  <read_first>
    - CLAUDE.md no-evolutionary-names (file is EXACTLY `migrate-sqlite-to-kmcore.mjs`)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Example 4 (lines 797-927 — full TS skeleton ready to copy)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `scripts/migrate-sqlite-to-kmcore.mjs` (lines 333-391 — imports + CLI + ontologyDir + Pitfall 3 patterns)
    - scripts/observations-api-server.mjs full file (locate exact DB path — `OBSERVATIONS_DB_PATH` env or hardcoded) AND the SELECT projections for digests + insights tables (verify column names — RESEARCH Example 4 covers observations table only)
    - 44-RESEARCH.md § Don't Hand-Roll table row 6 (use Phase 43 migrate-okm-json-to-kmcore.mjs as template — NOT locally checked out; use RESEARCH Example 4 instead)
    - lib/km-core/CLAUDE.md (no-parallel-versions; verification rule)
  </read_first>
  <behavior>
    - argv parsing: `--source=<path>` (default DB path; verify exact path), `--target=<path>` (default `.data/knowledge-graph/leveldb`), `--batch-size=<n>` (default 100), `--dry-run`, `--resume`, `--run-id=<id>`, `--help`.
    - `resolveOntologyDir()` per RESEARCH Example 4 lines 827-838 (import.meta.resolve walks up from km-core package root).
    - Construct GraphKMStore with `{ dbPath: target, exportDir: <sibling of target>, ontologyDir: <resolved>, domains: ['coding'] }`. await open.
    - Idempotency: build `seen: Set<legacyId.id>` from store.iterate when `--resume`; entries with `legacyId.system === 'A'` go into the set.
    - For each observations row: mint entity with Pitfall 3 BOTH fields set (entityType + ontologyClass = 'Observation'), legacyId `{system:'A', id:row.id}`, name = row.summary?.slice(0,80) ?? '(no summary)', layer='evidence', description=row.summary ?? '', metadata = `{...JSON.parse(row.metadata ?? '{}'), agent: row.agent, project: meta.project ?? null, session_id, source_file, content_hash, quality, digested_at, messages}`, createdAt=row.created_at, updatedAt=row.created_at, validFrom=row.created_at, validUntil=null, createdBy={provider:'phase-44-migration',model:'a-sqlite-to-kmcore',runId,timestamp:new Date().toISOString()}.
    - SAME pattern for digests (ontologyClass='Digest'; metadata preserves date, theme, observation_ids, agents, files_touched, project) and insights (ontologyClass='Insight'; metadata preserves topic, summary, confidence, digest_ids, last_updated, project).
    - Trusted-path bulk write: `await store.putEntity(entity, { skipOntologyCheck: true })` — A's Observation/Digest/Insight classes are NOT in upper.json (Phase 42-05 precedent for the same trap).
    - Per-row error handling: try/catch; stderr emit; counter++. Fail-loud at end if errors > 5% of total source rows (per Phase 42 Plan 5 budget).
    - Structured stdout: `process.stdout.write(JSON.stringify({status, runId, totals:{observations:obsCount, digests:digCount, insights:insCount}, migrated, skipped, errors, targetDir}) + '\n')` on completion.
    - Exit codes: 0 on ok, 1 on budget exceeded, 2 on fatal.
    - Honors CLAUDE.md no-console-log rule — no console.* calls; process.stderr.write for diagnostics; process.stdout.write for the summary.
    - Edge cases noted in RESEARCH Example 4 § Edge cases to test: orphan digest refs (digests.observation_ids may reference missing observations — skip the relation, store the digest); NULL columns (agent/project/session_id may be NULL); multi-line content with special chars.
  </behavior>
  <action>Create `scripts/migrate-sqlite-to-kmcore.mjs` by transcribing the TS skeleton from RESEARCH Example 4 lines 800-927. Additions versus the research skeleton:
    1. Verify the exact DB path BEFORE running — grep `OBSERVATIONS_DB_PATH` in scripts/observations-api-server.mjs and use that path as default. (CONTEXT says `.data/knowledge.db` but reality likely says `.observations/observations.db` — verify by `ls .observations/*.db .data/*.db 2>/dev/null`.)
    2. Add digests + insights migration loops (RESEARCH Example 4 stubs them; this plan fills them in based on the SELECT projections at observations-api-server.mjs lines 689-703 and 705-751).
    3. Add `--help` output documenting all flags + exit codes.
    4. Add a `--verify` flag that runs a post-migration sanity grep: iterate store, count entities with ontologyClass in ('Observation','Digest','Insight'), assert >=99% of source rows accounted for.
    5. After authoring, run `--help` smoke: `node scripts/migrate-sqlite-to-kmcore.mjs --help | head -30` shows the flag list.
    6. Run `--dry-run` smoke against the LIVE A DB (no writes): `node scripts/migrate-sqlite-to-kmcore.mjs --dry-run --run-id=phase-44-dryrun-$(date +%s) | tail -5` shows the projected counts.
    Do NOT actually write to the live km-core LevelDB in this task (Task 2 owns the live run). File name EXACTLY `migrate-sqlite-to-kmcore.mjs`.</action>
  <verify>
    <automated>node --check scripts/migrate-sqlite-to-kmcore.mjs && node scripts/migrate-sqlite-to-kmcore.mjs --help 2>&1 | grep -c "dry-run\\|resume\\|run-id\\|batch-size"</automated>
  </verify>
  <done>
    Migration script exists, ≥250 lines, syntactically valid, --help works, --dry-run runs against live DB and prints projected counts. NO actual writes to km-core LevelDB yet.
  </done>
  <acceptance_criteria>
    - `test -f scripts/migrate-sqlite-to-kmcore.mjs && wc -l scripts/migrate-sqlite-to-kmcore.mjs | awk '{exit ($1>=250)?0:1}'`
    - `grep -c "import Database from 'better-sqlite3'" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `grep -c "import.*GraphKMStore.*from.*@fwornle/km-core" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `grep -c "resolveOntologyDir" scripts/migrate-sqlite-to-kmcore.mjs` >= 2 (defined + called)
    - `grep -c "ontologyClass.*'Observation'" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `grep -c "ontologyClass.*'Digest'" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `grep -c "ontologyClass.*'Insight'" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `grep -c "entityType.*'Observation'" scripts/migrate-sqlite-to-kmcore.mjs` >= 1 (Pitfall 3 BOTH fields)
    - `grep -c "system: 'A'" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `grep -c "skipOntologyCheck.*true" scripts/migrate-sqlite-to-kmcore.mjs` >= 1 (trusted-path bulk write per Phase 42-05)
    - `grep -cE "console\\.(log|error|warn|info|debug)" scripts/migrate-sqlite-to-kmcore.mjs` returns 0 (no-console-log)
    - `grep -c "--dry-run" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `grep -c "--resume" scripts/migrate-sqlite-to-kmcore.mjs` >= 1
    - `node --check scripts/migrate-sqlite-to-kmcore.mjs && echo OK` prints OK
    - `node scripts/migrate-sqlite-to-kmcore.mjs --help 2>&1 | grep -cE "(dry-run|resume|run-id|batch-size)"` >= 4
    - File name EXACT: `find scripts -maxdepth 1 -name 'migrate-sqlite-to-kmcore*'` returns ONLY `migrate-sqlite-to-kmcore.mjs` (no version variants)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN-VERIFY — Backup live SQLite + run migration + verify counts</name>
  <what-built>
    Migration script `scripts/migrate-sqlite-to-kmcore.mjs` (Task 1) is now ready for production execution against the live A SQLite database. This task wraps the destructive run with a backup-first / verify-after operator gate.

    Automation (script already executed at checkpoint time):
    1. `cp .observations/observations.db .observations/observations.db.backup-pre-phase-44` (verified path BEFORE — if path differs, gate stops for operator confirmation)
    2. `node scripts/migrate-sqlite-to-kmcore.mjs --run-id=phase-44-live-$(date +%s)` (runs against the live DB; writes to .data/knowledge-graph/leveldb)
    3. Post-run summary printed (totals, migrated, skipped, errors counts)
    4. Post-run sanity grep: count entities with ontologyClass in ('Observation','Digest','Insight') via `node -e "import('@fwornle/km-core').then(async (m) => { const s = new m.GraphKMStore({...}); await s.open(); let c=0; for await (const e of s.iterate()) if (e.legacyId?.system==='A') c++; process.stdout.write(c+'\\n'); })"`
    5. Restart obs-api: `launchctl kickstart -k gui/$(id -u) com.coding.obs-api`
    6. Probe: `curl -s http://localhost:12436/api/coding/observations?limit=3 | head -c 500` to verify typed views now return populated data.
  </what-built>
  <how-to-verify>
    1. Confirm backup exists at `.observations/observations.db.backup-pre-phase-44` with the same size as the original: `ls -la .observations/observations.db*`
    2. Confirm migration completed within budget: tail of `/tmp/migrate-phase-44.log` should show `"status":"ok"` (not `"partial"`) AND `errors / totalSourceRows < 0.05`
    3. Confirm km-core LevelDB now contains the migrated entities: the post-run sanity grep printed at the checkpoint should be ~1130 (~800 obs + ~250 dig + ~77 ins per CONTEXT — variance OK)
    4. Restart obs-api + probe `/api/coding/observations?limit=3`: response should now contain 3 populated objects, not empty array
    5. Compare a single row: pick one SQLite row (`sqlite3 .observations/observations.db 'SELECT id, summary, agent FROM observations LIMIT 1'`) and probe the typed view to verify the same id/agent/content surfaces correctly through the reshape (Pitfall 2 dashboard contract verification)
    6. Open the dashboard at http://localhost:3032 in a browser via `gsd-browser navigate http://localhost:3032`, take a screenshot, and confirm observations panel shows populated rows (Plan 11 will repeat this as the formal gate)

    Type "approved" to proceed to Task 3 (table drop). Type "rollback" to invoke the documented rollback: `cp .observations/observations.db.backup-pre-phase-44 .observations/observations.db` (and the operator manually restarts obs-api). Type "issues: <description>" if migration produced unexpected results — Plan 10 will be revised before proceeding.
  </how-to-verify>
  <resume-signal>Type "approved", "rollback", or "issues: <description>"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-VERIFY — Drop SQLite tables observations|digests|insights</name>
  <what-built>
    Per CONTEXT A-3 + RESEARCH Open Q4, this is the SEPARATE-from-migration table drop. Task 2's `approved` signal gates execution of this destructive operation.

    Automation (script-driven at checkpoint time):
    1. Re-confirm backup exists and matches original size (defense in depth)
    2. Execute `sqlite3 .observations/observations.db "DROP TABLE observations; DROP TABLE digests; DROP TABLE insights;"` (keeps budget_events, session_metrics, embedding_cache per A-3 explicit)
    3. Verify remaining tables: `sqlite3 .observations/observations.db ".tables"` should list `budget_events`, `session_metrics`, `embedding_cache` and NOT list observations / digests / insights
    4. Re-probe `/api/coding/observations?limit=1` — confirm typed views still return populated data (proves km-core LevelDB is now authoritative)
    5. Re-probe `/api/v1/entities?limit=1&ontologyClass=Observation` — confirms canonical surface also returns migrated entities
  </what-built>
  <how-to-verify>
    1. Confirm legacy tables gone: `sqlite3 .observations/observations.db ".tables" | tr ' ' '\\n' | grep -E "^(observations|digests|insights)$" | wc -l` returns 0
    2. Confirm survivor tables intact: `sqlite3 .observations/observations.db ".tables" | tr ' ' '\\n' | grep -E "^(budget_events|session_metrics|embedding_cache)$" | wc -l` returns 3
    3. Re-probe typed views: `curl -s http://localhost:12436/api/coding/observations?limit=1 | head -c 200` returns populated data (NOT empty)
    4. Re-probe canonical: `curl -s "http://localhost:12436/api/v1/entities?ontologyClass=Observation&limit=1" | head -c 200` returns populated data
    5. Dashboard at :3032: open in `gsd-browser`, screenshot, confirm observations panel still populated
    6. Operator note: the backup at `.observations/observations.db.backup-pre-phase-44` is the ONLY restore path now — backup retention policy is operator-owned per CONTEXT out-of-scope (snapshot retention/garbage-collection)

    Type "approved" to proceed to Plan 10 SUMMARY commit. Type "rollback" to restore the backup + re-create dropped tables from the backup (`sqlite3 .observations/observations.db.backup-pre-phase-44 ".dump observations digests insights" > /tmp/restore.sql && sqlite3 .observations/observations.db < /tmp/restore.sql`). Type "issues: <description>" to halt and revise.
  </how-to-verify>
  <resume-signal>Type "approved", "rollback", or "issues: <description>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration script → live LevelDB | one-shot bulk write; trusted-path; idempotent re-run safe |
| Migration script → live SQLite | read-only |
| Backup file → operator filesystem | local; same access permissions as the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-10-01 | Tampering / V12 | Migration writes to live LevelDB | mitigate | --dry-run smoke first; backup taken pre-migration; idempotent via legacyId match (rerun safe); 5% error budget with fail-loud exit |
| T-44-10-02 | Information Disclosure | metadata.messages preserves the FULL message text from observations | accept | A's metadata.messages already contains the full prompt text on SQLite; migration preserves it verbatim — no new disclosure surface; data stays internal-only per CONTEXT |
| T-44-10-03 | Tampering / V12 / Destructive op | DROP TABLE observations/digests/insights | mitigate | Two-checkpoint gate (Task 2 approves migration + dashboard smoke; Task 3 approves drop only after Task 2 GREEN); pre-cutover backup mandatory; rollback procedure documented + restore-from-backup commands provided in checkpoints |
| T-44-10-04 | Denial of Service | Migration runs slow on 1130 rows | accept | Phase 42-05 dry-run took seconds for ~800 entities; production run expected ~30s |
| T-44-10-05 | Information Disclosure / V7 | Migration script stderr emits row IDs on error | accept | Internal-only; IDs are UUIDv7/nanoid; no PII |
| T-44-10-SC | Tampering | No new npm packages | n/a | better-sqlite3 already in coding root deps |
</threat_model>

<verification>
```bash
# Task 1 (script readiness):
node --check scripts/migrate-sqlite-to-kmcore.mjs
node scripts/migrate-sqlite-to-kmcore.mjs --help
node scripts/migrate-sqlite-to-kmcore.mjs --dry-run --run-id=phase-44-dryrun

# Task 2 (live migration; gated):
# operator approves → automation runs:
cp .observations/observations.db .observations/observations.db.backup-pre-phase-44
node scripts/migrate-sqlite-to-kmcore.mjs --run-id=phase-44-live-$(date +%s) | tee /tmp/migrate-phase-44.log
launchctl kickstart -k gui/$(id -u) com.coding.obs-api
sleep 5
curl -s http://localhost:12436/api/coding/observations?limit=3 | head -c 500    # populated

# Task 3 (table drop; gated):
sqlite3 .observations/observations.db "DROP TABLE observations; DROP TABLE digests; DROP TABLE insights;"
sqlite3 .observations/observations.db ".tables"   # only budget_events, session_metrics, embedding_cache
curl -s http://localhost:12436/api/coding/observations?limit=1   # still populated (LevelDB is now authoritative)
```
Operator runs Plan 11 next for the formal dashboard smoke + cross-system parity gate.
</verification>

<success_criteria>
- Migration script lands per Pattern 3 + RESEARCH Example 4
- Pitfall 3 BOTH fields set on every migrated entity
- Pre-cutover backup mandatory (Task 2 gate stops if absent)
- 5% error budget enforced
- SQLite legacy tables dropped only AFTER dashboard smoke (Task 3 gate)
- budget_events / session_metrics / embedding_cache preserved (A-3 explicit)
- Documented rollback procedure available at both gates
- Plan 02 typed-views.test.js data-GREEN after migration
- No evolutionary file names
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-10-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) script smoke output (--help and --dry-run), (b) backup path + sizes (original vs backup), (c) migration summary JSON, (d) post-migration sanity grep (entity counts by ontologyClass), (e) Task 2 approval signal + Task 3 approval signal (operator's literal response), (f) post-drop .tables listing, (g) post-drop typed-view probe showing populated data, (h) rollback procedure as documented for operator reference.
</output>
