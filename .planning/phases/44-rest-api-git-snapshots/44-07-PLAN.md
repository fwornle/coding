---
phase: 44-rest-api-git-snapshots
plan: 07
type: execute
wave: 4
depends_on:
  - 44-06
files_modified:
  - scripts/observations-api-server.mjs
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "A's host-side obs-api on :12436 mounts `createKmCoreRouter(kmStore, kmRouter, opts)` at `/api/v1` AFTER deleting the legacy `/api/km/` mount."
    - "The existing 503-until-ready hydration-gate middleware is preserved and applied to the new /api/v1 mount (RESEARCH Open Q5 symmetry)."
    - "Legacy URL surface `/api/observations|digests|insights` becomes typed views routed at `/api/coding/observations|digests|insights` — handlers call `kmStore.findByOntologyClass('Observation|Digest|Insight')` + reshape via `observationToLegacy|digestToLegacy|insightToLegacy` (km-core barrel)."
    - "Plan 02 typed-views.test.js flips RED → GREEN; Plan 02 cross-system-parity.mjs's A-leg flips from `fetch failed` to a 200 + envelope response (B+C still RED until Plans 08/09)."
    - "The original SQLite-backed `/api/observations|digests|insights` handlers are REPLACED in place (R-4 hard cutover — no `/api/legacy/*` fallback)."
    - "Query-string contract preserved EXACTLY (agent, project, from, to, q, quality, limit, offset) — Pitfall 2."
    - "Response shape preserved EXACTLY for typed views (`{data, total, limit, offset, _metadata}`) — Pitfall 2."
    - "SQLite tables observations|digests|insights are NOT dropped in this plan — Plan 10 owns the data migration + table drop after dashboard smoke."
  artifacts:
    - path: scripts/observations-api-server.mjs
      provides: "/api/v1 mount via createKmCoreRouter + /api/coding/{observations,digests,insights} typed-view handlers; legacy /api/km mount + handler bodies REMOVED in same plan."
      contains: "createKmCoreRouter"
  key_links:
    - from: scripts/observations-api-server.mjs
      to: "@fwornle/km-core (root barrel)"
      via: "import { createKmCoreRouter, observationToLegacy, digestToLegacy, insightToLegacy } from '@fwornle/km-core'"
      pattern: "createKmCoreRouter"
    - from: scripts/observations-api-server.mjs (/api/coding/observations handler)
      to: kmStore.findByOntologyClass('Observation')
      via: "iterate matching entities, reshape via observationToLegacy, apply legacy filters in JS"
      pattern: "findByOntologyClass.*Observation"
---

<objective>
A-side hard cutover: mount the canonical /api/v1 surface on A's obs-api server AND replace the SQLite-backed legacy `/api/observations|digests|insights` handlers with typed views that read km-core entities and reshape via `observationToLegacy` etc. Per CONTEXT R-4 the legacy `/api/km/` mount and the SQLite handler bodies are DELETED in this same plan (no dual-mount, no deprecation window). Query-string contract and response shape preserved exactly — A's dashboard at :3032 is brittle to changes (Pitfall 2).

Per CONTEXT A-3, SQLite tables observations|digests|insights are NOT dropped in this plan. Plan 10 owns the actual data migration (`scripts/migrate-sqlite-to-kmcore.mjs` execution) and the table drops after dashboard smoke proves the typed views work. Between Plan 07 landing and Plan 10 executing, the typed views return EMPTY data (km-core has no Observations yet because no migration has run) — this is by design. Plan 02 typed-views.test.js will GREEN-on-shape (200 + envelope structure) but data is empty until Plan 10.

Purpose: Bring A onto the canonical contract. After this plan A serves /api/v1/* correctly and the cross-system-parity test's A-leg returns 200 (shape match).
Output: One file edited in place — `scripts/observations-api-server.mjs`. Plan 02 typed-views.test.js: GREEN (shape lock; data fill comes from Plan 10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@CLAUDE.md
@scripts/observations-api-server.mjs
@lib/km-core/src/index.ts
@tests/integration/typed-views.test.js

<interfaces>
<!-- A's existing obs-api server topology — modifications happen at the marked anchor points. -->

From scripts/observations-api-server.mjs (current state):
- Line 39-41: existing imports `import { GraphKMStore } from '../lib/km-core/dist/store/GraphKMStore.js'; import { createKMRouter } from '../lib/km-core/dist/api/router.js';` (DELETE — deep-import path goes away when root barrel is used; createKMRouter is the orphan-draft name)
- Lines 376, 397, 436: SQLite write paths (observations + digests + insights inserts)
- Lines 466-549: GET /api/observations handler (SQLite-backed) — REPLACE with /api/coding/observations typed view
- Lines 689-703: GET /api/digests handler — REPLACE with /api/coding/digests typed view
- Lines 705-751: GET /api/insights handler — REPLACE with /api/coding/insights typed view
- Lines 1172-1191: legacy `/api/km/` mount + mountKMRoutes function — DELETE the mount block; REPLACE with /api/v1 mount per RESEARCH Pattern 1 lines 186-200 + PATTERNS.md lines 425-441
- Lines 1178-1183: hydration-gate middleware (`if (!_kmStoreReady || !_kmStore) return res.status(503)...`) — PRESERVE the gate; apply to the new /api/v1 mount
- The store instance `_kmStore` is constructed elsewhere in the file (likely in the bootstrap sequence) — PRESERVE; do not double-construct.

From @fwornle/km-core root barrel (post-Plan 06):
- `createKmCoreRouter(store, router, opts)`
- `observationToLegacy(entity)`, `digestToLegacy(entity)`, `insightToLegacy(entity)`
- `GraphKMStore` (if needed; A already constructs its own)

From CLAUDE.md `km-core scripts` mandatory rule:
- ALL GraphKMStore construction MUST pass `ontologyDir` (Phase 41 lesson). Verify A's existing construction does so; if not, fix in this plan.

From CLAUDE.md launchd:
- `com.coding.obs-api` runs this script. After the edit, restart via `launchctl kickstart -k gui/$(id -u) com.coding.obs-api`. Health probe `curl http://localhost:12436/api/v1/stats` should return 200 + envelope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace /api/km mount with /api/v1; switch deep-import to root barrel</name>
  <files>scripts/observations-api-server.mjs</files>
  <read_first>
    - scripts/observations-api-server.mjs FULL FILE (read top-to-bottom — large file but mandatory; specific anchors at lines 39-41, 1172-1191, plus locate the bootstrap sequence that constructs _kmStore + the existing hydration-gate)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `scripts/observations-api-server.mjs` (lines 395-441 — exact DELETE/REPLACE blocks and the replacement mount pattern)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pattern 1 (lines 186-200 — consumer-side wiring) and § Runtime State Inventory (line 498 — VOKB URL constants are OKM's problem, not A's; only A's internal /api/km mount changes here)
    - lib/km-core/src/index.ts (post-Plan 06 — confirm the barrel exports createKmCoreRouter)
    - CLAUDE.md `km-core scripts` mandatory rule (ontologyDir requirement)
    - CLAUDE.md Submodules & Build Pipeline (A's host-side launchd-managed; restart with launchctl kickstart)
  </read_first>
  <behavior>
    - The deep import `import { createKMRouter } from '../lib/km-core/dist/api/router.js'` is removed.
    - A new import `import { createKmCoreRouter } from '@fwornle/km-core'` (root barrel) is added at the top with the other km-core imports.
    - The `app.use('/api/km', kmSubRouter)` mount + the `mountKMRoutes(store)` helper are DELETED.
    - A new `app.use('/api/v1', kmRouter)` mount lands at the same location, with the existing 503-until-ready middleware applied to the new router.
    - createKmCoreRouter is called with: `{ ontologyRegistry: _kmStore.getOntologyRegistry(), snapshotDir: path.join(REPO_ROOT, '.data', 'knowledge-graph', 'exports'), restartCommand: 'launchctl kickstart -k gui/$(id -u) com.coding.obs-api' }`.
    - The existing _kmStore construction MUST pass `ontologyDir` (CLAUDE.md rule); if it does not, FIX it in this task by adding the proper resolveOntologyDir() call (lift the helper from RESEARCH Example 4 lines 827-838).
    - After launchctl kickstart restart, `curl http://localhost:12436/api/v1/stats` returns 200 + `{success:true,data:{entityCount:N,...}}` (envelope from Plan 06).
    - `curl http://localhost:12436/api/v1/entities?limit=1` returns 200 + envelope.
    - The legacy `/api/observations` etc. SQLite handlers are UNTOUCHED in this task (Task 2's scope) — only the km-core mount swap.
  </behavior>
  <action>Edit `scripts/observations-api-server.mjs`:
    Step 1 — DELETE lines 39-41 (the two existing imports for GraphKMStore deep-path + createKMRouter).
    Step 2 — INSERT a new import block at the same location: `import { GraphKMStore, createKmCoreRouter } from '@fwornle/km-core';` (single line — root barrel exposes both per Plan 06). If A's import statement style uses deep imports elsewhere (verify via grep first — the file may import other km-core modules deeply), keep this consistent with the existing pattern, but the createKmCoreRouter import MUST come from the root barrel per CONTEXT R-2 (revised) so all 15 endpoints are wired through the canonical surface.
    Step 3 — Locate the _kmStore construction in the bootstrap sequence (grep for `new GraphKMStore`). Verify the constructor opts pass `ontologyDir`. If MISSING, add the `await resolveOntologyDir()` helper (lift the 12-line function verbatim from RESEARCH Example 4 lines 827-838) and pass `ontologyDir: await resolveOntologyDir()` to the constructor. This is mandatory per CLAUDE.md.
    Step 4 — DELETE lines 1172-1191 (the legacy `/api/km/` mount block including `import { Router } from 'express'; const kmSubRouter = Router(); ... app.use('/api/km', kmSubRouter); function mountKMRoutes(store) { createKMRouter(store, kmSubRouter, { readOnly: false }); ... }` and the eventual `mountKMRoutes(_kmStore)` call wherever it lands).
    Step 5 — INSERT at the SAME LOCATION the replacement block per PATTERNS.md lines 425-441:
      - `import { Router } from 'express';` (preserve if already imported elsewhere; do not double-import)
      - `const kmRouter = Router();`
      - 503-until-ready middleware preserved verbatim: `kmRouter.use((_req, res, next) => { if (!_kmStoreReady || !_kmStore) { return res.status(503).json({ error: 'Knowledge graph store not ready' }); } next(); });`
      - `createKmCoreRouter(_kmStore, kmRouter, { ontologyRegistry: _kmStore.getOntologyRegistry?.(), snapshotDir: path.join(REPO_ROOT, '.data', 'knowledge-graph', 'exports'), restartCommand: 'launchctl kickstart -k gui/$(id -u) com.coding.obs-api' });`
      - `app.use('/api/v1', kmRouter);`
      - `process.stderr.write('[obs-api] km-core /api/v1 routes mounted\\n');` (matches existing log convention at line 1190 per 44-PATTERNS.md Shared Patterns)
    Step 6 — Honor CLAUDE.md `no-console-log` constraint by writing the diagnostic via `process.stderr.write` only (no console.log).
    Step 7 — Restart obs-api: `launchctl kickstart -k gui/$(id -u) com.coding.obs-api`. Poll health for up to 15s: `until curl -sf http://localhost:12436/api/v1/stats >/dev/null 2>&1; do sleep 1; done`.
    Step 8 — Smoke-verify: `curl -s http://localhost:12436/api/v1/stats | head -c 200`; `curl -s http://localhost:12436/api/v1/entities?limit=1 | head -c 200`. Both should be 200 + `{success:true,...}`.
    No file is renamed; same `scripts/observations-api-server.mjs` (no -v2 / -enhanced suffix per CLAUDE.md no-evolutionary-names rule).</action>
  <verify>
    <automated>launchctl kickstart -k gui/$(id -u) com.coding.obs-api 2>/dev/null; sleep 3; curl -s -o /tmp/v1-stats.json -w "%{http_code}\n" http://localhost:12436/api/v1/stats | head -1 | awk '{exit ($1=="200")?0:1}' && grep -c '"success":true' /tmp/v1-stats.json</automated>
  </verify>
  <done>
    `/api/km/` mount is gone; `/api/v1/` mount returns 200 + envelope; 503-until-ready preserved; root-barrel import lands; obs-api restarts cleanly via launchctl.
  </done>
  <acceptance_criteria>
    - `grep -c "createKMRouter" scripts/observations-api-server.mjs` returns 0 (legacy name gone)
    - `grep -c "createKmCoreRouter" scripts/observations-api-server.mjs` returns 1
    - `grep -c "/api/km" scripts/observations-api-server.mjs` returns 0 (mount path gone)
    - `grep -c "/api/v1" scripts/observations-api-server.mjs` >= 1
    - `grep -c "@fwornle/km-core" scripts/observations-api-server.mjs` >= 1 (root-barrel import)
    - `grep -c "lib/km-core/dist/api/router.js" scripts/observations-api-server.mjs` returns 0 (orphan deep-import gone)
    - `grep -c "ontologyDir" scripts/observations-api-server.mjs` >= 1 (CLAUDE.md mandatory rule)
    - `grep -c "_kmStoreReady" scripts/observations-api-server.mjs` >= 2 (503-until-ready preserved)
    - `grep -c "launchctl kickstart" scripts/observations-api-server.mjs` >= 1 (restartCommand set on opts)
    - `grep -cE "console\\.(log|error|warn|info|debug)" scripts/observations-api-server.mjs | awk '{print $1}'` — DOCUMENT count vs baseline (Don't introduce NEW console.log — verify count is <= baseline before edit; baseline = `git show HEAD:scripts/observations-api-server.mjs | grep -cE "console\\.(log|error|warn|info|debug)"`)
    - `node --check scripts/observations-api-server.mjs && echo OK` prints OK (syntax)
    - `launchctl list | grep com.coding.obs-api` returns a line (daemon registered)
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:12436/api/v1/stats` returns `200`
    - `curl -s http://localhost:12436/api/v1/stats | grep -c '"success":true'` returns 1
    - `curl -s http://localhost:12436/api/v1/entities?limit=1 | grep -c '"success":true'` returns 1
    - File name unchanged: `ls scripts/observations-api-server.mjs && find scripts -maxdepth 1 -name 'observations-api-server-*' | wc -l | awk '{exit ($1==0)?0:1}'` (no version suffixes introduced)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace SQLite handlers /api/{observations,digests,insights} with typed views under /api/coding/*</name>
  <files>scripts/observations-api-server.mjs</files>
  <read_first>
    - scripts/observations-api-server.mjs lines 466-549 (current /api/observations handler — SQLite SELECT projection + WHERE-builder + response envelope `{data, total, limit, offset, _metadata}`)
    - scripts/observations-api-server.mjs lines 689-703 (current /api/digests handler)
    - scripts/observations-api-server.mjs lines 705-751 (current /api/insights handler)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `scripts/observations-api-server.mjs` typed-view section (lines 443-455 — Pitfall 2 exact shape preservation rule)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pattern 3 (lines 317-335 — query side example: iterate by ontologyClass, reshape, apply legacy filters)
    - tests/integration/typed-views.test.js (Plan 02 RED test — exact assertions on /api/coding/* URLs and the legacy shape)
    - lib/km-core/src/adapters/observation-view.ts (Plan 05 — observationToLegacy + digestToLegacy + insightToLegacy)
  </read_first>
  <behavior>
    - DELETE the existing GET /api/observations handler body (lines 466-549) and REPLACE with: a new route `app.get('/api/coding/observations', ...)` whose handler: (a) checks _kmStoreReady (503 if not), (b) iterates `for await (const entity of _kmStore.iterate()) { if (entity.ontologyClass === 'Observation' || entity.entityType === 'Observation') all.push(observationToLegacy(entity)); }` — Pitfall 3 two-field OR-check, (c) applies the existing legacy filters in JS (agent, project, from, to, q, quality match the SQLite handler verbatim), (d) returns `{data: filtered.slice(offset, offset+limit), total: filtered.length, limit, offset, _metadata: <preserved metadata block>}` — Pitfall 2 shape lock.
    - SAME pattern for /api/coding/digests and /api/coding/insights using digestToLegacy / insightToLegacy and matching the SQLite SELECT projections from lines 689-703 and 705-751 respectively.
    - The OLD route paths `/api/observations`, `/api/digests`, `/api/insights` are REMOVED entirely (R-4 hard cutover — no fallback). Dashboard at :3032 currently hits these URLs; verification step in Task 1's wave-gate is the user's `gsd-browser` smoke (Plan 11). For this plan: confirmation that the new /api/coding/* URLs return shape-correct empty arrays (data:[] because km-core has no migrated Observations until Plan 10).
    - Query-string contract preserved: same param names (`agent`, `project`, `from`, `to`, `q`, `quality`, `limit`, `offset`) and same defaults (`limit=100, offset=0` per existing handler).
    - Plan 02 typed-views.test.js asserts the SHAPE lock (top-level keys present) — it does NOT assert data length, so empty `data:[]` passes. Test goes GREEN on shape.
  </behavior>
  <action>Edit `scripts/observations-api-server.mjs`:
    Step 1 — DELETE the three legacy handlers in place (lines 466-549, 689-703, 705-751 per the line ranges in 44-PATTERNS.md). Track exact lines via grep before editing since line numbers may shift after Task 1.
    Step 2 — INSERT at the same locations three new typed-view handlers per the <behavior> block above:
      - `app.get('/api/coding/observations', async (req, res) => { ... iterate + filter + reshape + envelope ... })`
      - `app.get('/api/coding/digests', async (req, res) => { ... })`
      - `app.get('/api/coding/insights', async (req, res) => { ... })`
    Each handler:
      - Imports `observationToLegacy|digestToLegacy|insightToLegacy` from `@fwornle/km-core` (add to the import block from Task 1 — single line `import { createKmCoreRouter, GraphKMStore, observationToLegacy, digestToLegacy, insightToLegacy } from '@fwornle/km-core';`).
      - Uses `for await (const entity of _kmStore.iterate())` with the Pitfall 3 OR-check.
      - Applies the SAME filter logic as the SQLite version (re-implement WHERE-builder in JS — agent === query.agent, project === query.project, etc.; substring match on `q` against content; date-range on timestamp via from/to).
      - Returns the SAME response envelope structure (`{data, total, limit, offset, _metadata}`) — Pitfall 2 mandatory.
    Step 3 — Verify NO other code in the file references `/api/observations` / `/api/digests` / `/api/insights` (grep before commit; if some POST endpoints exist for writes, those stay because writes still go through the legacy SQLite path until Plan 10 migrates them — confirm via grep and leave write paths alone OR document explicitly that they will be addressed in Plan 10).
    Step 4 — restart `launchctl kickstart -k gui/$(id -u) com.coding.obs-api` and smoke-test:
      - `curl -s http://localhost:12436/api/coding/observations?limit=1 | head -c 200` should be `{"data":[],"total":0,"limit":1,"offset":0,...}` (empty until Plan 10).
      - Same for /api/coding/digests, /api/coding/insights.
    Step 5 — run Plan 02 typed-views.test.js: `npm test -- typed-views.test.js` — should be GREEN on shape (passes even with empty data because the test asserts top-level keys, not data length per Plan 02 acceptance).
    File name unchanged: still `scripts/observations-api-server.mjs`.</action>
  <verify>
    <automated>launchctl kickstart -k gui/$(id -u) com.coding.obs-api 2>/dev/null; sleep 3; curl -s -o /tmp/coding-obs.json -w "%{http_code}\n" "http://localhost:12436/api/coding/observations?limit=1" | head -1 | awk '{exit ($1=="200")?0:1}' && curl -s "http://localhost:12436/api/coding/digests?limit=1" | grep -c '"data"' && curl -s "http://localhost:12436/api/coding/insights?limit=1" | grep -c '"data"'</automated>
  </verify>
  <done>
    Three typed-view handlers land. Legacy `/api/observations` etc. routes return 404 (R-4 hard cutover). Plan 02 typed-views.test.js: GREEN on shape. Dashboard at :3032 is BROKEN until Plan 11 verifies (operator-aware: data is empty until Plan 10 migration). Both /api/v1/* (Task 1) and /api/coding/* (Task 2) endpoints return 200 + envelope.
  </done>
  <acceptance_criteria>
    - `grep -c "/api/coding/observations" scripts/observations-api-server.mjs` >= 1
    - `grep -c "/api/coding/digests" scripts/observations-api-server.mjs` >= 1
    - `grep -c "/api/coding/insights" scripts/observations-api-server.mjs` >= 1
    - `grep -cE "app\\.get\\(\\s*'/api/observations'" scripts/observations-api-server.mjs` returns 0 (legacy GET removed)
    - `grep -cE "app\\.get\\(\\s*'/api/digests'" scripts/observations-api-server.mjs` returns 0
    - `grep -cE "app\\.get\\(\\s*'/api/insights'" scripts/observations-api-server.mjs` returns 0
    - `grep -c "observationToLegacy" scripts/observations-api-server.mjs` >= 1
    - `grep -c "digestToLegacy" scripts/observations-api-server.mjs` >= 1
    - `grep -c "insightToLegacy" scripts/observations-api-server.mjs` >= 1
    - `grep -cE "entityType === 'Observation' \\|\\| .*ontologyClass === 'Observation'|ontologyClass === 'Observation' \\|\\| .*entityType === 'Observation'" scripts/observations-api-server.mjs` >= 1 (Pitfall 3 OR-check)
    - `node --check scripts/observations-api-server.mjs && echo OK` prints OK
    - `curl -s -o /dev/null -w "%{http_code}" "http://localhost:12436/api/coding/observations?limit=1"` returns `200`
    - `curl -s "http://localhost:12436/api/coding/observations?limit=1" | grep -cE '"data":\\s*\\[' ` returns 1 (shape lock — Pitfall 2)
    - `curl -s "http://localhost:12436/api/coding/observations?limit=1" | grep -cE '"total":' ` returns 1
    - `curl -s "http://localhost:12436/api/coding/observations?limit=1" | grep -cE '"limit":' ` returns 1
    - `curl -s "http://localhost:12436/api/coding/observations?limit=1" | grep -cE '"offset":' ` returns 1
    - `curl -s -o /dev/null -w "%{http_code}" "http://localhost:12436/api/observations?limit=1"` returns `404` (R-4 hard cutover — legacy URL gone)
    - `cd /Users/Q284340/Agentic/coding && npm test -- typed-views 2>&1 | tail -10 | grep -cE "(passing|0 failing|Tests:.*passed)"` >= 1 (Plan 02 stub GREEN)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → A's /api/v1 | external HTTP; validated by km-core handlers (Plan 06) |
| Client → A's /api/coding/* typed views | external HTTP; query params parsed without injection (no SQL anymore) |
| typed-view handler → km-core store | internal, trusted iteration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07-01 | Tampering / V5 | Legacy filter query params (agent/project/q/from/to) re-parsed in JS | mitigate | Reshape happens BEFORE SQL ran historically (already user-controlled); now the same params are JS-string-compared — NO SQL injection surface remains; substring `q` match uses `.includes` not `eval` |
| T-44-07-02 | Information Disclosure / V7 | Typed-view error responses | mitigate | Wrap handler body in try/catch; on error: `res.status(500).json({error:'Internal error'})` — no stack trace |
| T-44-07-03 | Denial of Service / V5 | Iterate over all entities then filter — O(n) per request | mitigate | Default LIMIT applied (defaults to 100 per existing SQLite handler); add 1000 ceiling on `limit` param (typed-view-handler explicit cap) |
| T-44-07-04 | Spoofing | Dashboard at :3032 forwards client requests to obs-api with no auth | accept | internal-only (CONTEXT out-of-scope auth) |
| T-44-07-SC | Tampering | No new npm packages | n/a | km-core deps already approved |
</threat_model>

<verification>
```bash
launchctl kickstart -k gui/$(id -u) com.coding.obs-api
sleep 3
curl -s http://localhost:12436/api/v1/stats         # 200 + envelope
curl -s http://localhost:12436/api/v1/entities      # 200 + envelope
curl -s http://localhost:12436/api/coding/observations?limit=1   # 200 + shape (empty data until Plan 10)
curl -i http://localhost:12436/api/observations                  # 404 (R-4)
cd /Users/Q284340/Agentic/coding && npm test -- typed-views.test.js
```
Operator awareness: dashboard at :3032 will display ZERO observations until Plan 10 migration runs. This is by design. Plan 11 wraps a verification gate.
</verification>

<success_criteria>
- /api/km/ removed; /api/v1/ live; 503-until-ready preserved
- Three SQLite handlers REPLACED in place with typed views routed at /api/coding/*
- Pitfall 2 response shape preserved verbatim
- Plan 02 typed-views.test.js: GREEN on shape
- launchd-managed daemon restarts cleanly
- No new console.log calls; no evolutionary file names; ontologyDir present at GraphKMStore construction
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-07-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) baseline vs final HTTP probe matrix (/api/v1/stats, /api/v1/entities, /api/coding/observations, /api/observations) with status codes, (b) Plan 02 stub status (typed-views.test.js GREEN; cross-system-parity.mjs A-leg now 200), (c) operator note: "dashboard at :3032 shows empty observations until Plan 10 migration runs."
</output>
