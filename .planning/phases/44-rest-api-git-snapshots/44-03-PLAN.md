---
phase: 44-rest-api-git-snapshots
plan: 03
type: execute
wave: 1
depends_on:
  - 44-01
files_modified:
  - lib/km-core/src/api/contracts.ts
  - lib/km-core/src/api/index.ts
  - lib/km-core/package.json
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "km-core ships Zod schemas for the canonical REST surface that consumers can `import { EntitySchema, ApiSuccessEnvelope } from '@fwornle/km-core/api/contracts'`."
    - "z.infer types are exported alongside the schemas (Entity, Relation, EntityResponse, etc.) — consumers get TS types for free."
    - "Wave 0 contracts.test.ts flips RED → GREEN after this plan; remaining Wave 0 tests stay RED (no router/SnapshotManager/observation-view yet)."
  artifacts:
    - path: lib/km-core/src/api/contracts.ts
      provides: "Zod schemas verbatim per CONTEXT C-2; codifies OKM response shape lock from rest-contract.test.ts:94-167; includes Phase 39 entity fields, Phase 41 legacyId, Phase 42 embedding."
      min_lines: 100
      contains: "EntitySchema"
    - path: lib/km-core/src/api/index.ts
      provides: "Sub-barrel re-exporting contracts (createKmCoreRouter comes from Plan 06; for now only contracts surface is live)."
      min_lines: 10
      contains: "EntitySchema"
    - path: lib/km-core/package.json
      provides: "zod ^3.25.76 in dependencies; ./api, ./api/contracts, AND ./snapshots subpath exports — Plan 03 owns ALL package.json edits for Phase 44 to avoid Wave 1 file-conflict with Plan 04."
      contains: "zod"
  key_links:
    - from: lib/km-core/package.json
      to: lib/km-core/dist/api/contracts.js (post-build artifact)
      via: "exports map subpath ./api/contracts"
      pattern: "\"./api/contracts\""
    - from: lib/km-core/src/api/index.ts
      to: lib/km-core/src/api/contracts.ts
      via: "export * (re-export)"
      pattern: "from './contracts.js'"
---

<objective>
Land km-core's Zod contracts layer — the shipped wire format for the canonical /api/v1 surface. CONTEXT C-2 mandates "OKM response shapes verbatim, codified as Zod"; 44-PATTERNS.md identifies the verbatim source at OKM `tests/integration/rest-contract.test.ts:94-167` (lifted in 44-RESEARCH.md § Pattern 2 lines 207-251). The schemas become an importable contract that Plans 06/07/08/09 implement against, and that the regenerated OKM fixtures (Plan 09) validate against without re-declaration.

Purpose: Single source of truth for the wire format. Plan 06's router uses these schemas implicitly via `res.json({ success:true, data })`; consumers can `import` them for tests and TypeScript types. Per CONTEXT R-2 corrected wording, this also adds the `./api` and `./api/contracts` subpath exports to `package.json` so external consumers (OKM rest-contract.test.ts in Plan 09) can `import from '@fwornle/km-core/api/contracts'`.
Output: `lib/km-core/src/api/contracts.ts` + `lib/km-core/src/api/index.ts` (sub-barrel) + `package.json` zod dep + ALL THREE subpath exports (./api, ./api/contracts, ./snapshots — pre-added so Plan 04 only adds source files, no package.json touch — avoids Wave 1 file-conflict). Plan 01 contracts.test.ts goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@lib/km-core/CLAUDE.md
@lib/km-core/package.json
@lib/km-core/src/types/entity.ts
@lib/km-core/src/index.ts
@lib/km-core/tests/unit/contracts.test.ts

<interfaces>
<!-- Phase 39 Entity shape — schemas MUST mirror these field names exactly. -->

From lib/km-core/src/types/entity.ts:
- `interface Entity { id: EntityId; name: string; entityType: string; ontologyClass?: string; layer: 'evidence'|'pattern'; description: string; metadata: Record<string,unknown>; validFrom?: string; validUntil?: string|null; supersedes?: string[]; createdBy?: ProvenanceStamp; lastConfirmedBy?: ProvenanceStamp; confirmationCount?: number; legacyId?: { system:'A'|'B'|'C', id:string }; embedding?: number[]; }`
- `interface Relation { from: EntityId; to: EntityId; key?: string; relationType: string; createdAt: string; ...metadata }`
- `interface ProvenanceStamp { provider: string; model: string; runId: string; timestamp: string; }`

From package.json exports map (existing pattern at lines 12-35 — template for new entries):
- `"./adapters/online": { "types": "...d.ts", "import": "...js" }` — copy this structure
- Subpaths in alphabetical order — insert `./api` between `./adapters/online` and `./dedup`; `./api/contracts` after `./api`.

From OKM tests/integration/rest-contract.test.ts:94-167 (verbatim source — REMOTE; reproduced in 44-RESEARCH.md lines 207-251):
- ProvenanceStampSchema, EntitySchema, ApiSuccessEnvelope factory, EntitiesEndpointResponse
- All optional fields use `.optional()`; legacyId.system is `z.enum(['A','B','C'])`; layer is `z.enum(['evidence','pattern'])`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/km-core/src/api/contracts.ts (Zod schemas + inferred types)</name>
  <files>lib/km-core/src/api/contracts.ts, lib/km-core/src/api/index.ts</files>
  <read_first>
    - lib/km-core/CLAUDE.md (no-parallel-versions rule — file is EXACTLY `contracts.ts`, NOT `contracts-v2.ts`, `enhanced-contracts.ts`, etc.)
    - lib/km-core/src/types/entity.ts (full Entity / Relation / ProvenanceStamp surface — schemas must mirror these field-for-field)
    - lib/km-core/tests/unit/contracts.test.ts (the RED test from Plan 01 — schemas must satisfy all 5 test blocks; read the test BEFORE writing the schemas so the exports match the imports the test uses)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pattern 2 (lines 207-251 — verbatim Zod block to LIFT)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `lib/km-core/src/api/contracts.ts` (lines 165-209 — schema template + deviations list = "none, lift verbatim")
    - lib/km-core/src/ontology/index.ts (sub-barrel template — copy the file shape for the new src/api/index.ts)
  </read_first>
  <behavior>
    - Test 1 (from Plan 01 contracts.test.ts): EntitySchema.parse accepts the minimal valid entity payload.
    - Test 2: parse REJECTS missing entityType with a ZodError.
    - Test 3: parse accepts all Phase 39 optional fields (validFrom, validUntil:null, supersedes, createdBy, confirmationCount, legacyId, embedding).
    - Test 4: ApiSuccessEnvelope(EntitySchema).parse accepts `{success:true,data:<entity>}`; rejects on success:false.
    - Test 5: `type Entity = z.infer<typeof EntitySchema>` compiles and is assignable from a literal object.
    - Additional schemas needed for Plan 06 router smoke (api-router.test.ts asserts /relations + /stats + /ontology/classes envelopes): RelationSchema, OntologyClassSchema, StatsSchema. Define them now even if Plan 01 contracts.test.ts only directly asserts EntitySchema — they are part of the canonical contract per CONTEXT C-2.
  </behavior>
  <action>Create `lib/km-core/src/api/contracts.ts` lifting the Zod block verbatim from 44-RESEARCH.md § Pattern 2 lines 207-251 (Phase 39 fields + ApiSuccessEnvelope per D-39/D-41/D-52 — see field list in <interfaces> block). Add additional schemas required by the canonical contract: RelationSchema (`{ from:string, to:string, key:string.optional, relationType:string, createdAt:string, metadata:z.record(z.string(),z.unknown()).optional() }`), OntologyClassSchema (`{ name:string, parent:string.optional, properties:z.record(...).optional, relationships:z.record(z.string(),z.array(z.string())).optional }` per Phase 38 registry surface), StatsSchema (`{ entityCount:number, relationCount:number, ontologyClasses:number, domainsActive:string[] }` per OKM /api/stats response shape), and ResponseEnvelopes for each (e.g., `EntitiesEndpointResponse = ApiSuccessEnvelope(z.array(EntitySchema))`, `RelationsEndpointResponse`, etc.). Export `z.infer<typeof ...>` types alongside: `export type Entity = z.infer<typeof EntitySchema>; export type Relation = z.infer<typeof RelationSchema>; ...`. Then create `lib/km-core/src/api/index.ts` as a thin re-export: `export * from './contracts.js';` plus a top-of-file comment noting Plan 06 will add the createKmCoreRouter export. File names EXACTLY as listed. Honor `no-console-log` rule (schemas are pure data — no diagnostic emission). Use `.js` import suffixes per km-core ESM/NodeNext convention (seen throughout src/).</action>
  <verify>
    <automated>cd lib/km-core && npm install zod@^3.25.76 --save 2>&1 | tail -2 && npm run build 2>&1 | tail -5 && npx vitest run tests/unit/contracts.test.ts</automated>
  </verify>
  <done>
    `contracts.ts` exists with EntitySchema, RelationSchema, OntologyClassSchema, StatsSchema, ApiSuccessEnvelope, and the corresponding response-envelope schemas. `index.ts` re-exports them. Plan 01 contracts.test.ts goes GREEN (all 5 test blocks pass). km-core build succeeds.
  </done>
  <acceptance_criteria>
    - `test -f lib/km-core/src/api/contracts.ts && test -f lib/km-core/src/api/index.ts` (both files exist)
    - `wc -l lib/km-core/src/api/contracts.ts | awk '{print $1}'` >= 100
    - `grep -c "export const EntitySchema" lib/km-core/src/api/contracts.ts` returns 1
    - `grep -c "export const RelationSchema" lib/km-core/src/api/contracts.ts` returns 1
    - `grep -c "export const OntologyClassSchema" lib/km-core/src/api/contracts.ts` returns 1
    - `grep -c "export const StatsSchema" lib/km-core/src/api/contracts.ts` returns 1
    - `grep -c "ApiSuccessEnvelope" lib/km-core/src/api/contracts.ts` >= 2
    - `grep -cE "z\.enum\(\['evidence', ?'pattern'\]\)" lib/km-core/src/api/contracts.ts` >= 1
    - `grep -cE "system: z\.enum\(\['A', ?'B', ?'C'\]\)" lib/km-core/src/api/contracts.ts` >= 1
    - `grep -c "export type Entity" lib/km-core/src/api/contracts.ts` >= 1
    - `grep -c "from './contracts.js'" lib/km-core/src/api/index.ts` >= 1
    - `cd lib/km-core && npm run build 2>&1 | grep -cE "error TS"` returns 0
    - `cd lib/km-core && npx vitest run tests/unit/contracts.test.ts 2>&1 | grep -cE "(✓|passed)" | awk '{exit ($1>=1)?0:1}'` (test passes — RED → GREEN)
    - `test -f lib/km-core/dist/api/contracts.js && test -f lib/km-core/dist/api/contracts.d.ts && test -f lib/km-core/dist/api/index.js` (build output exists)
    - No evolutionary suffix: `find lib/km-core/src/api -name '*-v2*' -o -name '*-enhanced*' -o -name '*-new*' -o -name '*-fixed*'` returns empty
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire zod dep + subpath exports in lib/km-core/package.json</name>
  <files>lib/km-core/package.json</files>
  <read_first>
    - lib/km-core/package.json (current exports map at lines 7-36 + dependencies at lines 63-69)
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `lib/km-core/package.json` (lines 508-533 — explicit add-after-line-35 + add-alphabetically-in-dependencies instructions)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Pattern 2 (subpath export wiring lines 253-272)
  </read_first>
  <behavior>
    - `import { EntitySchema } from '@fwornle/km-core/api/contracts'` resolves in a consumer (tested via tmpdir smoke).
    - `import { ... } from '@fwornle/km-core/api'` resolves (sub-barrel).
    - Root barrel import `import { EntitySchema } from '@fwornle/km-core'` continues to NOT resolve (we expose only via subpath in this plan — Plan 06 adds the root re-export).
    - `npm install` succeeds with zod ^3.25.76 added; no peer-dep warnings on zod.
  </behavior>
  <action>Edit `lib/km-core/package.json`. Plan 03 owns ALL package.json edits for Phase 44 (to avoid Wave 1 file-conflict with Plan 04 which would otherwise also edit this file for the ./snapshots subpath):
    1. In `exports` (insert in alphabetical order — `./adapters/online` → `./api` → `./api/contracts` → `./dedup` → ... → `./pipeline` → `./snapshots`), add all THREE Phase 44 subpath entries:
       - `"./api": { "types": "./dist/api/index.d.ts", "import": "./dist/api/index.js" }`
       - `"./api/contracts": { "types": "./dist/api/contracts.d.ts", "import": "./dist/api/contracts.js" }`
       - `"./snapshots": { "types": "./dist/snapshots/index.d.ts", "import": "./dist/snapshots/index.js" }` (placed after ./pipeline in alphabetical position; this entry resolves to a NOT-YET-EXISTING dist file at this plan's completion — Plan 04 adds the source then build regenerates the dist — but the package.json declaration must land HERE to prevent Plan 04 from racing on the same file in Wave 1)
    2. In `dependencies` insert `"zod": "^3.25.76"` AFTER `uuidv7` (alphabetical position — `uuidv7` → `zod`). Preserve trailing-comma JSON rules — add comma after `uuidv7` line.
    Run `cd lib/km-core && npm install` to materialize node_modules/zod. Run `cd lib/km-core && npm run build`. (Build may emit a warning that ./snapshots dist files do not exist yet — that is expected and resolves in Plan 04; the package.json declaration is FINE because the subpath is not consumed by any caller until Plan 06's barrel re-export lands.)
    Smoke-verify the existing subpath resolution: `node -e "import('@fwornle/km-core/api/contracts').then(m => { if (!m.EntitySchema) throw new Error('EntitySchema not exported'); process.stdout.write('OK\n'); })"` — run from `lib/km-core/` cwd.
    Honor `no-console-log` rule. No evolutionary file names introduced.</action>
  <verify>
    <automated>cd lib/km-core && npm install 2>&1 | tail -3 && npm run build 2>&1 | tail -3 && node -e "import('@fwornle/km-core/api/contracts').then(m => { if (!m.EntitySchema) { process.stderr.write('FAIL: EntitySchema not exported\n'); process.exit(1); } process.stdout.write('OK\n'); }).catch(e => { process.stderr.write('FAIL: '+e.message+'\n'); process.exit(2); })"</automated>
  </verify>
  <done>
    `package.json` has `zod` in deps and `./api` + `./api/contracts` in exports. Build succeeds. Subpath import resolves and exposes `EntitySchema`. Plan 01 contracts.test.ts still GREEN after this edit (no regression).
  </done>
  <acceptance_criteria>
    - `grep -c '"zod":' lib/km-core/package.json` returns 1
    - `grep -cE '"zod": "\^?3\.' lib/km-core/package.json` returns 1
    - `grep -c '"./api":' lib/km-core/package.json` returns 1
    - `grep -c '"./api/contracts":' lib/km-core/package.json` returns 1
    - `grep -c '"./snapshots":' lib/km-core/package.json` returns 1 (pre-added; Plan 04 will fill in the actual source files)
    - `test -d lib/km-core/node_modules/zod && echo OK` prints OK
    - `cd lib/km-core && node -e "import('@fwornle/km-core/api/contracts').then(m => { if (m.EntitySchema && m.RelationSchema && m.ApiSuccessEnvelope) { process.stdout.write('ALL_OK\n'); } else { throw new Error('missing exports'); } })" 2>&1 | grep -c ALL_OK` returns 1
    - `cd lib/km-core && npm test 2>&1 | grep -cE "(Test Files\s+\d+\s+(passed|failed)|Tests\s+\d+\s+passed)"` >= 1 (full km-core suite runs to completion — no regression from existing 56+ tests; api-router.test.ts and snapshot-roundtrip.test.ts and observation-view.test.ts still RED is expected)
    - JSON parses cleanly: `node -e "JSON.parse(require('fs').readFileSync('lib/km-core/package.json','utf8'))" 2>&1 | grep -cE "(SyntaxError|Unexpected)"` returns 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer process → km-core schemas | Schemas validate request bodies / query params crossing trust boundary into the canonical /api/v1 surface (V5 input validation per ASVS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | Tampering | EntitySchema.parse on incoming POST /entities body | mitigate | Schemas are strict-by-default (Zod 3.x); unknown fields preserved via spread but type-required fields enforced; Plan 06 router uses .parse() at handler entry per RESEARCH § V5 control |
| T-44-03-02 | Information Disclosure | Schema rejection error messages | mitigate | ZodError messages reveal field names but no PII; Plan 06 wraps in safe envelope `{success:false,error:<safe message>}` per V7 control |
| T-44-03-SC | Tampering | zod@^3.25.76 install from npm | mitigate | zod is HIGH-trust per 44-RESEARCH.md § Package Legitimacy Audit (already in OKM; 30M+ wk downloads; github.com/colinhacks/zod 8yo). No legitimacy checkpoint needed |
</threat_model>

<verification>
```bash
cd lib/km-core && npm run build && npx vitest run tests/unit/contracts.test.ts
```
Expected: contracts.test.ts GREEN. Other Wave 0 tests stay RED (api-router, snapshot-roundtrip, observation-view) — Plans 04/05/06 will green them in subsequent waves.
</verification>

<success_criteria>
- contracts.ts ships ≥5 Zod schemas (Entity, Relation, OntologyClass, Stats, ApiSuccessEnvelope) + matching z.infer types
- ./api and ./api/contracts subpaths resolve at consumer side
- Plan 01 contracts.test.ts flips RED → GREEN
- km-core build clean; no regression on existing 56+ tests
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-03-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) Wave 0 test progress (which RED → GREEN this plan), (b) final exports map block, (c) tmpdir smoke output proving subpath resolution.
</output>
