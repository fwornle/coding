---
phase: 44-rest-api-git-snapshots
plan: 08
type: execute
wave: 4
depends_on:
  - 44-06
files_modified:
  - integrations/mcp-server-semantic-analysis/src/sse-server.ts
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "B's SSE server on :3848 mounts `createKmCoreRouter(kmStore, kmRouter, opts)` at /api/v1 ALONGSIDE the existing /health + /sse + /workflow-events + /messages routes — same-port strategy per RESEARCH Example 3."
    - "B's existing kmStore (constructed in Phase 42) is reused (no double-construct); ontologyDir present per CLAUDE.md mandatory rule."
    - "Hydration-gate (503 until store ready) mirrors A's pattern (RESEARCH Open Q5)."
    - "After build + container restart, `curl http://localhost:3848/api/v1/stats` returns 200 + envelope and Plan 02 cross-system-parity.mjs's B-leg flips from `fetch failed` to 200."
    - "The km-core build → submodule rebuild → docker-compose container restart chain is followed per CLAUDE.md Submodules & Build Pipeline rules."
  artifacts:
    - path: integrations/mcp-server-semantic-analysis/src/sse-server.ts
      provides: "/api/v1 mount via createKmCoreRouter; same Express app as the existing SSE routes"
      contains: "createKmCoreRouter"
  key_links:
    - from: integrations/mcp-server-semantic-analysis/src/sse-server.ts
      to: "@fwornle/km-core (container-side via bind-mount /coding/node_modules/@fwornle/km-core)"
      via: "import { createKmCoreRouter } from '@fwornle/km-core'"
      pattern: "createKmCoreRouter"
---

<objective>
B-side mount: attach the canonical /api/v1 surface to B's existing SSE/Express server in `integrations/mcp-server-semantic-analysis/src/sse-server.ts`. Per CONTEXT C-4 + RESEARCH Example 3, this is a same-port strategy — REST + SSE share Express app on port 3848.

B already constructs `kmStore` (Phase 42 — verify via grep in the submodule), already passes `ontologyDir` per CLAUDE.md mandatory rule (Phase 42), and already mounts to Docker via bind-mount on `coding-services`. The CRITICAL operational gotcha is the CLAUDE.md two-step build chain: `npm run build` inside the submodule THEN `docker-compose build coding-services && docker-compose up -d coding-services`. Forgetting either step silently runs old code.

Purpose: Cross-system parity by construction — same factory, same envelope, same endpoints.
Output: One file edited in B's submodule + build pipeline run + container restart. Plan 02 cross-system-parity.mjs B-leg returns 200 + envelope; same Plan 06 contract on port 3848 as on port 12436.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md
@CLAUDE.md
@integrations/mcp-server-semantic-analysis/src/sse-server.ts
@docker/docker-compose.yml
@tests/integration/cross-system-parity.mjs

<interfaces>
<!-- B's existing SSE server topology — modifications are additive. -->

From integrations/mcp-server-semantic-analysis/src/sse-server.ts:
- Lines 1-22: existing imports + `app = express()` + `app.use(express.json())` + heartbeat config
- Existing routes: GET /health, GET /sse, GET /workflow-events, POST /messages (per 44-CONTEXT.md `<domain>` description)
- `app.listen(PORT, ...)` near line 169 (PORT is 3848)
- B's kmStore is constructed elsewhere in B's source tree (NOT in sse-server.ts directly per Phase 42 lessons — typically in a bootstrap or coordinator module). Verify via `grep -rn "new GraphKMStore" integrations/mcp-server-semantic-analysis/src/` BEFORE deciding mount strategy.
- If kmStore is not accessible from sse-server.ts, the plan must wire it through — typically by importing from a shared module OR by constructing one HERE with the same opts (`dbPath: '/coding/.data/knowledge-graph/leveldb', exportDir: '/coding/.data/knowledge-graph/exports', ontologyDir: '/coding/.data/ontologies', domains: ['coding']`).

From CLAUDE.md Submodules & Build Pipeline (mandatory rule for B):
```
cd integrations/mcp-server-semantic-analysis && npm run build
cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services
```
Forgetting `npm run build` is the recurring failure mode noted in CLAUDE.md.

From docker/docker-compose.yml: km-core lives at `/coding/node_modules/@fwornle/km-core` inside the container via bind-mount.

From RESEARCH Example 3 (lines 745-783) + 44-PATTERNS.md § sse-server.ts (lines 468-504) — the additive block AFTER line 22.

From CONTEXT R-2 (revised): km-core ships framework-agnostic; consumer constructs Router(); same pattern A uses on Plan 07.

From RESEARCH Open Q5: gate /api/v1 mount behind store-ready (mirror A's pattern via 503-until-ready middleware).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /api/v1 mount via createKmCoreRouter in sse-server.ts; rebuild + restart container</name>
  <files>integrations/mcp-server-semantic-analysis/src/sse-server.ts</files>
  <read_first>
    - integrations/mcp-server-semantic-analysis/src/sse-server.ts FULL FILE — identify the precise location of the existing `app.use(express.json())` call AND of `app.listen(PORT, ...)`; the mount goes between these
    - .planning/phases/44-rest-api-git-snapshots/44-PATTERNS.md § `integrations/mcp-server-semantic-analysis/src/sse-server.ts` (lines 468-504 — the additive block + verify-store-open-before-listen pattern)
    - .planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md § Example 3 (lines 745-783 — full skeleton) + § Pitfall 4 (no in-process restart; SSE long-lived connections must not be killed by snapshot restore — caller restarts via docker-compose) + § Open Q5 (hydration-gate symmetry)
    - CLAUDE.md Submodules & Build Pipeline (mandatory two-step rebuild for B) AND feedback_docker_build_time.md (use 600s timeout on docker-compose build)
    - Run `grep -rn "new GraphKMStore" integrations/mcp-server-semantic-analysis/src/` BEFORE editing to find kmStore construction site
    - Run `ls -la integrations/mcp-server-semantic-analysis/src/sse-server.ts` to confirm file is a REAL file (not a symlink) — determines commit topology (real → submodule commit + outer pointer-bump pair; symlink → outer-only commit) per Phase 42.1.2-03 lesson
  </read_first>
  <behavior>
    - After the existing `app.use(express.json())` call (around line 22), the file now contains:
      - new top-of-file imports `import { Router } from 'express';` (if not already present) and `import { createKmCoreRouter, GraphKMStore } from '@fwornle/km-core';`
      - kmStore acquisition: if a module-level kmStore exists elsewhere in B, import + reuse; OTHERWISE construct one here with Phase-42-canonical opts (dbPath, exportDir, ontologyDir, domains)
      - `await kmStore.open()` BEFORE `app.listen` (verify ordering)
      - `const kmRouter = Router();`
      - 503-until-ready middleware: `kmRouter.use((_req, res, next) => { if (!kmStore.isReady?.() && !kmStore.graph) { return res.status(503).json({ error: 'Knowledge graph store not ready' }); } next(); });`
      - `createKmCoreRouter(kmStore, kmRouter, { ontologyRegistry: kmStore.getOntologyRegistry?.(), snapshotDir: '/coding/.data/knowledge-graph/exports', restartCommand: 'docker-compose restart coding-services' });`
      - `app.use('/api/v1', kmRouter);`
      - `process.stderr.write('[sse-server] km-core /api/v1 routes mounted on port 3848\\n');`
    - Build + restart sequence per CLAUDE.md:
      - `cd integrations/mcp-server-semantic-analysis && npm run build` (mandatory; failing this silently runs old code)
      - `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services`
      - Poll container health: `until curl -sf http://localhost:3848/health >/dev/null 2>&1; do sleep 2; done` (max 60s)
      - Smoke: `curl -s http://localhost:3848/api/v1/stats` returns 200 + `{success:true,data:{...}}`
    - SSE routes (/sse, /workflow-events, /messages) MUST still work after the mount — verify with `curl -s http://localhost:3848/health` returns the existing /health body.
  </behavior>
  <action>Edit `integrations/mcp-server-semantic-analysis/src/sse-server.ts`:
    Step 1 — `grep -rn "new GraphKMStore" integrations/mcp-server-semantic-analysis/src/` to locate kmStore construction. Document the path in commit message. If a shared kmStore module exists, import from there; if NOT, construct in sse-server.ts with the canonical Phase-42 opts.
    Step 2 — `ls -la integrations/mcp-server-semantic-analysis/src/sse-server.ts` to determine commit topology (real-dir submodule commit + outer pointer-bump vs symlinked outer-only commit). Document determination for the SUMMARY.
    Step 3 — Add imports at the top of sse-server.ts: `import { Router } from 'express';` and `import { createKmCoreRouter, GraphKMStore } from '@fwornle/km-core';` (only the missing pieces — preserve existing imports).
    Step 4 — Insert the mount block per <behavior> at the marked position (after express.json() body parser, before app.listen). If kmStore needs construction, do it AT THE TOP of the bootstrap sequence and `await kmStore.open()` AT THE PROPER LOCATION (before app.listen is called).
    Step 5 — Honor CLAUDE.md no-console-log rule: diagnostic emission via `process.stderr.write` only.
    Step 6 — Submodule build: `cd integrations/mcp-server-semantic-analysis && npm run build`. Fix any TypeScript errors HERE before proceeding (do not defer to operator). Common error: missing `@fwornle/km-core` types — confirm km-core is importable from the submodule's tsconfig path.
    Step 7 — Container rebuild: `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` (use 600s timeout per feedback_docker_build_time.md). Then `docker-compose up -d coding-services`.
    Step 8 — Health + smoke: wait for /health 200, then probe /api/v1/stats and /api/v1/entities?limit=1.
    Step 9 — SSE regression check: `curl -s http://localhost:3848/health | head -c 100` returns the EXISTING health body shape (compare to baseline captured BEFORE the rebuild).
    File name unchanged: `sse-server.ts` only — no -v2 / -with-rest suffix (CLAUDE.md no-evolutionary-names rule).</action>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build 2>&1 | tail -5 && cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services 2>&1 | tail -3 && docker-compose up -d coding-services && sleep 15 && curl -s -o /tmp/b-v1-stats.json -w "%{http_code}\n" http://localhost:3848/api/v1/stats | head -1 | awk '{exit ($1=="200")?0:1}' && grep -c '"success":true' /tmp/b-v1-stats.json</automated>
  </verify>
  <done>
    /api/v1 mount lands; container restarts cleanly; /api/v1/stats returns 200 + envelope; SSE /health still works; cross-system-parity.mjs B-leg passes (smoke).
  </done>
  <acceptance_criteria>
    - `grep -c "createKmCoreRouter" integrations/mcp-server-semantic-analysis/src/sse-server.ts` >= 1
    - `grep -c "/api/v1" integrations/mcp-server-semantic-analysis/src/sse-server.ts` >= 1
    - `grep -c "@fwornle/km-core" integrations/mcp-server-semantic-analysis/src/sse-server.ts` >= 1
    - `grep -c "isReady\\|kmStore.graph" integrations/mcp-server-semantic-analysis/src/sse-server.ts` >= 1 (503 gate)
    - `grep -c "docker-compose restart" integrations/mcp-server-semantic-analysis/src/sse-server.ts` >= 1 (restartCommand)
    - `grep -c "ontologyDir" integrations/mcp-server-semantic-analysis/src/sse-server.ts` >= 1 OR kmStore is imported from a module that already passes ontologyDir (CLAUDE.md mandatory rule — document the resolution path in SUMMARY)
    - `grep -cE "console\\.(log|error|warn|info|debug)" integrations/mcp-server-semantic-analysis/src/sse-server.ts | awk '{print $1}'` — compare to git-show baseline; no NET INCREASE
    - `cd integrations/mcp-server-semantic-analysis && npm run build 2>&1 | grep -cE "error TS"` returns 0
    - `docker exec coding-services ls /coding/integrations/mcp-server-semantic-analysis/dist/sse-server.js | wc -l | awk '{exit ($1>=1)?0:1}'` (dist file shipped into container — proves build chain ran)
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3848/api/v1/stats` returns `200`
    - `curl -s http://localhost:3848/api/v1/stats | grep -c '"success":true'` returns 1
    - `curl -s http://localhost:3848/api/v1/entities?limit=1 | grep -c '"success":true'` returns 1
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3848/health` returns `200` (SSE regression check)
    - File name unchanged: `find integrations/mcp-server-semantic-analysis/src -maxdepth 1 -name 'sse-server*'` returns ONLY `sse-server.ts` (no version variants)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTTP → B :3848 | Same trust model as A; ASVS V5 input validation via Plan 06 handlers |
| /api/v1 mount → kmStore | Internal in-container; Phase 42 ontologyDir contract enforced |
| Snapshot/restore handler → docker-compose restart | NOT in-process; operator restarts via the documented command in restartRequired response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08-01 | Tampering / V12 | /snapshots/:id/restore destructive op gated identically to A | mitigate | Handler-side confirmDestructive enforced by Plan 06 — same code path; SnapshotManager regex validation at Plan 04 |
| T-44-08-02 | Denial of Service | Snapshot restore could kill long-lived SSE connections if process.exit were called | mitigate | CONTEXT S-2 revised: NO process.exit anywhere in restore path; SSE survives; operator restarts via docker-compose when ready (restartRequired:true signal) |
| T-44-08-03 | Information Disclosure / V7 | Error envelope leaks per-system internal paths | mitigate | err.message-only envelope from Plan 06; container-side paths (`/coding/...`) are not external secrets |
| T-44-08-SC | Tampering | No new npm packages; bind-mount km-core already present | n/a | Phase 42 lockdown stands |
</threat_model>

<verification>
```bash
cd integrations/mcp-server-semantic-analysis && npm run build
cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services
sleep 15
curl -s http://localhost:3848/api/v1/stats      # 200 + envelope
curl -s http://localhost:3848/api/v1/entities?limit=1   # 200 + envelope
curl -s http://localhost:3848/health             # 200 + existing health body (regression check)
```
Expected: all three return 200. Plan 02 cross-system-parity.mjs B-leg goes from fetch failed → 200 envelope. Phase 43 C-leg still RED until Plan 09.
</verification>

<success_criteria>
- /api/v1 mount lands on B port 3848 same-port strategy
- 503-until-ready hydration gate present
- SSE routes (/health, /sse, /workflow-events, /messages) regression-free
- ontologyDir contract maintained (verify wherever kmStore is constructed)
- CLAUDE.md two-step build chain followed; container runs new dist/
- No console.log net increase
- No evolutionary file names
</success_criteria>

<output>
Create `.planning/phases/44-rest-api-git-snapshots/44-08-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include: (a) where kmStore was constructed (line:col grep hit), (b) commit topology determination (real dir vs symlink — therefore submodule+outer commit pair OR outer-only), (c) build + container restart timing (~3-5min for docker-compose build per feedback_docker_build_time.md), (d) probe matrix (/api/v1/stats, /api/v1/entities, /health) with status codes, (e) Plan 02 cross-system-parity.mjs B-leg status.
</output>
