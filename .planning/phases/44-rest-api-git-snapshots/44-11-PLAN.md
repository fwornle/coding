---
phase: 44-rest-api-git-snapshots
plan: 11
type: execute
wave: 6
depends_on:
  - 44-07
  - 44-08
  - 44-09
  - 44-10
files_modified:
  - .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md
autonomous: false
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "All three systems (A:12436, B:3848, C:3002) serve `/api/v1/*` with shape-identical responses for the canonical 15-endpoint contract (SC#1)."
    - "Snapshot create → mutate → restore round-trip succeeds end-to-end on all three systems with operator-triggered restart per system (SC#2 + SC#4)."
    - "A's legacy `/api/coding/observations|digests|insights` typed views return populated data after Plan 10 migration (SC#3)."
    - "OKB-baseline guard rejects mixed snapshot+other-files commits WITHOUT OKB_SNAPSHOT=1; accepts them WITH (SC#4)."
    - "Dashboard at :3032 renders observations correctly (gsd-browser smoke; Pitfall 2)."
    - "VOKB viewer at OKM's port renders correctly (gsd-browser smoke against the OKM viewer URL after Plan 09)."
    - "All 8 Wave 0 test stubs are GREEN."
    - "Cross-system parity test (Plan 02 cross-system-parity.mjs) all 3 legs GREEN."
  artifacts:
    - path: .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md
      provides: "Phase 44 close-out verification log — all SCs + Wave 0 status + dashboard + VOKB smoke + restore round-trip evidence"
  key_links:
    - from: 44-11-SUMMARY.md
      to: ROADMAP.md Phase 44 Success Criteria entries
      via: "Per-SC PASS/FAIL evidence"
      pattern: "SC#"
---

<objective>
End-of-phase verification gate. Run the complete Wave 0 test set + the cross-system parity test + the snapshot/restore round-trip on each system + the dashboard + VOKB visual smokes. Capture per-SC evidence in the SUMMARY. Operator approves phase close-out via human-verify checkpoint.

Per ROADMAP Phase 44 Success Criteria (4 SCs):
- SC#1 — Shape-identical responses for canonical endpoints on A/B/C
- SC#2 — Snapshot + restore round-trip identical across systems
- SC#3 — A's legacy `/api/observations|digests|insights` resolve internally to typed views without consumer breakage
- SC#4 — Two-commit pattern + OKB-baseline guard hold under unified snapshot endpoint

Purpose: Goal-backward verification that the phase shipped what the user asked for. Operator's APPROVED signal closes the phase; OPERATOR signals issues if any SC fails.
Output: 44-11-SUMMARY.md with full evidence trail + per-SC PASS/FAIL determination.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md
@.planning/phases/44-rest-api-git-snapshots/44-RESEARCH.md
@.planning/phases/44-rest-api-git-snapshots/44-VALIDATION.md
@CLAUDE.md
@tests/integration/cross-system-parity.mjs
@tests/integration/typed-views.test.js
@tests/integration/okb-guard-snapshot-bypass.sh
@tests/e2e/dashboard-observations.spec.ts
@lib/km-core/tests/integration/api-router.test.ts
@lib/km-core/tests/unit/contracts.test.ts
@lib/km-core/tests/integration/snapshot-roundtrip.test.ts
@lib/km-core/tests/integration/observation-view.test.ts

<interfaces>
<!-- ROADMAP Phase 44 Success Criteria (verbatim source) -->

From ROADMAP.md lines 611-624:
- SC#1: The same REST request (e.g. `GET /api/entities?ontologyClass=...`) returns shape-identical responses against A, B, and C — only the data and ontology classes differ.
- SC#2: A user can take a git snapshot of `.data/exports/` in any of the three systems and restore an earlier state via the shared snapshot/restore endpoint, with the resulting graph identical to the snapshot.
- SC#3: A's existing `/api/observations|digests|insights` endpoints remain callable but resolve internally to typed views over `/api/entities?ontologyClass=...` (no consumer breakage during transition). NOTE: per R-4 hard cutover, the URL paths moved to `/api/coding/observations|digests|insights` — the spirit of SC#3 (typed-view fan-in to km-core entities) is preserved; the URL change is documented in 44-07-SUMMARY.
- SC#4: The git two-commit pattern and OKB-baseline guard from existing export hygiene still hold under the unified snapshot endpoint.

From 44-VALIDATION.md Wave 0 list — 8 stubs total:
- km-core side (4): api-router.test.ts, contracts.test.ts, snapshot-roundtrip.test.ts, observation-view.test.ts
- coding side (4): cross-system-parity.mjs, typed-views.test.js, okb-guard-snapshot-bypass.sh, dashboard-observations.spec.ts

From CLAUDE.md `gsd-browser` rule:
- Visual UI smoke MUST use gsd-browser (not hand-rolled chromium scripts; not bare playwright scripts in /tmp)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Wave 0 set + cross-system parity + record results</name>
  <files>.planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md</files>
  <read_first>
    - .planning/phases/44-rest-api-git-snapshots/44-VALIDATION.md (Wave 0 stub list — 8 tests)
    - $HOME/.claude/get-shit-done/templates/summary.md (the SUMMARY template format)
    - .planning/phases/44-rest-api-git-snapshots/44-CONTEXT.md (the 4 SCs from ROADMAP + the spirit-vs-letter notes for SC#3)
    - All four prior SUMMARYs (44-07, 44-08, 44-09, 44-10) — verify they reported the smoke status accurately and that the consumer mounts + migration are in known-good state
  </read_first>
  <behavior>
    Step 1 — km-core tests (Wave 0 - 4 stubs):
      - `cd lib/km-core && npx vitest run tests/integration/api-router.test.ts tests/unit/contracts.test.ts tests/integration/snapshot-roundtrip.test.ts tests/integration/observation-view.test.ts` — all 4 GREEN.
      - Capture pass count per file.
    Step 2 — coding-side jest tests (2 of 4 Wave 0 stubs):
      - `cd /Users/Q284340/Agentic/coding && npm test -- typed-views` (Plan 02 stub — should be data-GREEN post-Plan-10).
      - `bash tests/integration/okb-guard-snapshot-bypass.sh; echo exit=$?` — exit 0 expected (Plan 04 landed the bypass).
    Step 3 — cross-system parity (1 of 4 Wave 0 stubs):
      - `node --test tests/integration/cross-system-parity.mjs` (uses node:test runner per Plan 02 design) — all 3 legs (A/B/C) GREEN.
      - Capture per-leg HTTP status + envelope key check.
    Step 4 — restore round-trip per system (SC#2 + SC#4):
      - For each system in (A, B, C):
        - Capture pre-snapshot stats: `curl -s http://<port>/api/v1/stats | jq .data.entityCount`
        - Create snapshot: `curl -s -X POST http://<port>/api/v1/snapshots -H 'Content-Type: application/json' -d '{"label":"phase-44-verify"}'` → capture tag id
        - Mutate: `curl -s -X POST http://<port>/api/v1/entities -H 'Content-Type: application/json' -d '{"name":"verify-stub","entityType":"VerifyStub","ontologyClass":"VerifyStub","layer":"evidence","description":"phase 44 verification","metadata":{}}'`
        - Restore: `curl -s -X POST http://<port>/api/v1/snapshots/<tag>/restore -H 'Content-Type: application/json' -d '{"confirmDestructive":true}'` → capture restartRequired + restartCommand
        - Operator-runs the restartCommand (gated to Task 2's checkpoint — Task 1 only records the response)
        - Post-restart probe (Task 2 runs this AFTER operator confirms restart): `curl -s http://<port>/api/v1/stats | jq .data.entityCount` — assert equals pre-snapshot count
      - This task records the create + mutate + restore commands and their responses; the restart cycle is in Task 2.
    Step 5 — OKB-baseline guard check (SC#4):
      - Already covered by Plan 04 SUMMARY + Plan 02 stub (okb-guard-snapshot-bypass.sh). Re-affirm here: the snapshot commits created in step 4 went through (OKB_SNAPSHOT=1 set by SnapshotManager); manually attempt a mixed-files commit WITHOUT the env var (`git add foo.unrelated && git add .data/exports/bar.json && git commit -m "test"`) → expect rejection. Roll back via `git reset HEAD~ --hard` immediately.
    Step 6 — author 44-11-SUMMARY.md with per-SC evidence sections. Use $HOME/.claude/get-shit-done/templates/summary.md template. Per-SC sections:
      - SC#1: cross-system-parity.mjs 3/3 GREEN + per-leg envelope sample (paste literal head -c 200 from each /api/v1/stats response)
      - SC#2: restore round-trip per-system command + response trail (defer post-restart verification to Task 2)
      - SC#3: typed-views.test.js GREEN + literal head -c 200 from /api/coding/observations?limit=1 (post-Plan-10 populated)
      - SC#4: okb-guard-snapshot-bypass.sh exit=0 + mixed-commit reject demonstration + snapshot commits from step 4 succeeded (proves SnapshotManager sets OKB_SNAPSHOT=1)
    All file names unchanged.
  </behavior>
  <action>Execute steps 1-6 per <behavior>. Emit each command's literal stdout into the SUMMARY (truncated to first 500 bytes per probe for readability). For each Wave 0 test, record file → pass/fail + per-suite pass count. For cross-system parity, table the per-leg result. For the restore round-trip, capture: (a) pre-snapshot entityCount, (b) snapshot tag id, (c) post-mutate entityCount, (d) restore response body (including restartRequired + restartCommand), (e) the restartCommand the operator must run in Task 2. Honors CLAUDE.md no-console-log + plays the diagnostics through process.stderr / shell stdout — no console wrapper in node scripts.</action>
  <verify>
    <automated>cd lib/km-core && npx vitest run tests/integration/api-router.test.ts tests/unit/contracts.test.ts tests/integration/snapshot-roundtrip.test.ts tests/integration/observation-view.test.ts 2>&1 | tail -5 | grep -c "passed" && bash /Users/Q284340/Agentic/coding/tests/integration/okb-guard-snapshot-bypass.sh > /dev/null 2>&1 && echo "OKB-BYPASS-PASS" && node --test /Users/Q284340/Agentic/coding/tests/integration/cross-system-parity.mjs 2>&1 | tail -5 | grep -c "pass"</automated>
  </verify>
  <done>
    All 8 Wave 0 stubs GREEN; cross-system parity 3/3 GREEN; restore round-trip commands captured per system + restartRequired response recorded; 44-11-SUMMARY.md drafted with per-SC sections.
  </done>
  <acceptance_criteria>
    - `test -f .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md && wc -l .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md | awk '{exit ($1>=50)?0:1}'`
    - `grep -cE "SC#1\\|SC#2\\|SC#3\\|SC#4" .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md` >= 4
    - `grep -c "Wave 0" .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md` >= 1
    - `grep -c "cross-system-parity" .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md` >= 1
    - `grep -cE "(12436|3848|3002).*api/v1" .planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md` >= 3 (per-system evidence)
    - `cd lib/km-core && npx vitest run tests/integration/api-router.test.ts tests/unit/contracts.test.ts tests/integration/snapshot-roundtrip.test.ts tests/integration/observation-view.test.ts 2>&1 | tail -3 | grep -cE "(passed|0 failing)"` >= 1
    - `bash tests/integration/okb-guard-snapshot-bypass.sh; echo exit=$?` ends with `exit=0`
    - `node --test tests/integration/cross-system-parity.mjs 2>&1 | tail -3 | grep -cE "(pass|0 failed)"` >= 1
    - `curl -s "http://localhost:12436/api/coding/observations?limit=1" | grep -cE "\\b(agent|project|content)\\b"` >= 3 (populated data per Plan 10)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN-VERIFY — Visual UI smokes (dashboard :3032 + VOKB viewer) + restart-then-verify-restore</name>
  <what-built>
    Visual + restore-cycle verification — these are operator-owned because (a) launchd / docker / OKM-restart commands need operator authority on the live machine, (b) the dashboard render check uses gsd-browser per CLAUDE.md (no headless-script substitution), (c) the VOKB viewer in OKM lives on bmw.ghe.com — operator must have current OKM checkout running.

    Automation runs at checkpoint time:
    1. `gsd-browser navigate http://localhost:3032` + screenshot → save under `.planning/phases/44-rest-api-git-snapshots/_artifacts/dashboard-44-${Date.now()}.png`. Verify observations panel populated.
    2. `gsd-browser navigate http://localhost:3002/<OKM_VIEWER_PATH>` (operator confirms the exact path; typically `/viewer` or `/`) + screenshot → save under `_artifacts/vokb-44-${Date.now()}.png`. Verify graph renders, search works, cluster overlay loads.
    3. For each system, execute the restart command from Task 1's restore response, then re-probe /api/v1/stats to confirm post-restart entity count equals pre-snapshot count:
       - A: `launchctl kickstart -k gui/$(id -u) com.coding.obs-api` then `sleep 5; curl http://localhost:12436/api/v1/stats | jq .data.entityCount`
       - B: `cd /Users/Q284340/Agentic/coding/docker && docker-compose restart coding-services` then `sleep 10; curl http://localhost:3848/api/v1/stats | jq .data.entityCount`
       - C: operator-known OKM restart command (typically `docker-compose restart <okm-container>` or `npm run dev` restart)
    4. Append the post-restart entity counts to 44-11-SUMMARY.md SC#2 section.

    Operator decision authority:
    - "approved" → phase passes; update ROADMAP Phase 44 entry checkbox to [x]; STATE.md Current Position advances to Phase 45.
    - "issues: <description>" → operator notes specific SC failures; planner/executor revisits.
    - "rollback" → restore the .observations/observations.db backup; documented in Plan 10 Task 3 SUMMARY.
  </what-built>
  <how-to-verify>
    1. Dashboard at :3032: open in gsd-browser, verify the observations panel populated, the agent + project columns non-empty (Pitfall 2 lock holds), screenshot saved under `_artifacts/dashboard-44-*.png`.
    2. VOKB viewer at OKM's URL: verify graph renders, search/filter/cluster work, snapshot list shows snapshots created in Task 1, screenshot saved under `_artifacts/vokb-44-*.png`.
    3. Restart-then-restore verification per system:
       - A: post-launchctl restart, /api/v1/stats entityCount == pre-snapshot count (the verify-stub entity created in Task 1 step 4 is gone — restore wiped it)
       - B: post-docker-compose restart, /api/v1/stats entityCount on port 3848 == pre-snapshot count
       - C: post-OKM-restart, /api/v1/stats entityCount on port 3002 == pre-snapshot count
    4. ROADMAP Phase 44 entry status: confirm `[x] Phase 44: REST API & Git Snapshots` at line 245 after operator approval.
    5. Per-SC sign-off:
       - SC#1 GREEN if cross-system-parity 3/3 + dashboard renders + VOKB renders
       - SC#2 GREEN if restore round-trip on all 3 systems shows post-restart entity count == pre-snapshot count
       - SC#3 GREEN if typed-views.test.js + dashboard observations show populated data
       - SC#4 GREEN if okb-guard-snapshot-bypass.sh passes + snapshot commits succeeded in Task 1

    Type "approved" to close phase. Type "issues: <description>" to halt and revise. Type "partial" + per-SC notes if some SCs pass and others don't (planner re-plans the failing legs only).
  </how-to-verify>
  <resume-signal>Type "approved", "partial: <SC-status>", or "issues: <description>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live A/B/C services | restart commands run with operator authority; gated by checkpoint |
| Verification scripts → live HTTP endpoints | read-only probes + the restore round-trip's snapshot/mutate/restore cycle (gated by Plan 04/06 destructive-confirmation already) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-11-01 | Tampering / V12 / Destructive op | restore-then-restart cycle on live systems | mitigate | Same gates as Plan 04 (SnapshotManager destructive-confirm) and Plan 06 (handler-side confirmDestructive); the verify-stub entity created in Task 1 is single-purpose and gets wiped by the restore — by design |
| T-44-11-02 | Information Disclosure | Screenshots may contain real observation content | accept | _artifacts directory is gitignored per convention (verify with `grep _artifacts .gitignore`); internal-only review |
| T-44-11-03 | Denial of Service | Restart cycle briefly interrupts the service | accept | <30s downtime per system; operator-scheduled; CONTEXT internal-only |
| T-44-11-SC | n/a | No new package installs | n/a | verification reuses existing test infrastructure |
</threat_model>

<verification>
After Task 2's operator approval, the SUMMARY captures per-SC PASS/FAIL with literal evidence (probe outputs, test counts, screenshot paths). ROADMAP Phase 44 entry advances to checkbox checked. STATE.md current position to Phase 45.
</verification>

<success_criteria>
- All 8 Wave 0 stubs GREEN
- Cross-system parity 3/3 legs GREEN
- Restore round-trip on A/B/C succeeds with restart triggered by operator + post-restart entity count matches pre-snapshot
- Dashboard at :3032 visually renders observations correctly (gsd-browser smoke)
- VOKB viewer (OKM) visually renders correctly
- OKB-baseline-guard bypass for snapshot commits + rejection without env var both confirmed
- 4 SCs from ROADMAP individually evidenced in SUMMARY
- Operator's "approved" signal recorded
</success_criteria>

<output>
Append final lines to `.planning/phases/44-rest-api-git-snapshots/44-11-SUMMARY.md`:
- Operator approval signal (literal text)
- ROADMAP update line ("Phase 44 status: COMPLETE")
- STATE.md transition note ("Current Position advances to Phase 45")
Then the orchestrator may update ROADMAP.md + STATE.md per the operator's approval.
</output>
