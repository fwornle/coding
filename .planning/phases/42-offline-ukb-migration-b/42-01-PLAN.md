---
phase: 42-offline-ukb-migration-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.test.ts
autonomous: true
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "B has a strangler-pattern km-core GraphKMStore adapter at `src/storage/km-core-adapter.ts` that exposes the GraphDatabaseService hot read/write surface (queryEntities, storeEntity, storeRelationship, getEntity, deleteEntity) backed by km-core."
    - "A feature flag `process.env.KM_CORE_PERSISTENCE` (default `legacy`) routes writes to either the new km-core adapter or the legacy GraphDatabaseService."
    - "The Phase 10 embedding bug is fixed: `wave-controller.ts:1373` no longer calls a non-existent method; the new adapter exposes `mergeAttributes(nodeId, attrs)` that delegates to km-core's existing `GraphKMStore.mergeAttributes` (see GraphKMStore.ts:854)."
    - "After a `ukb full` run with `KM_CORE_PERSISTENCE=km-core`, every entity returned by the adapter's `findByOntologyClass('Detail')` query has `embedding.length === 384` (SC#2)."
    - "The MCP interface (`mcp__semantic-analysis__execute_workflow`) signature is unchanged (SC#1 / D-51b)."
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts"
      provides: "Strangler adapter — implements the GraphDatabaseService consumer surface against km-core's GraphKMStore"
      exports: ["KmCoreAdapter", "createKmCoreAdapter"]
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts"
      provides: "Single config switch gating new vs legacy persistence backend"
      exports: ["getPersistenceBackend"]
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts"
      provides: "Rewired bypass write path that calls adapter.mergeAttributes when km-core backend is active"
      contains: "kmCoreAdapter.mergeAttributes"
  key_links:
    - from: "src/agents/wave-controller.ts:~1373"
      to: "src/storage/km-core-adapter.ts mergeAttributes"
      via: "feature-flag-gated call replaces the broken `this.graphDB.mergeAttributes(...)`"
      pattern: "kmCoreAdapter\\.mergeAttributes"
    - from: "src/storage/km-core-adapter.ts"
      to: "@fwornle/km-core GraphKMStore"
      via: "import { GraphKMStore } from '@fwornle/km-core/store'"
      pattern: "from '@fwornle/km-core"
---

<objective>
Land the strangler-pattern km-core adapter that replaces B's broken bypass-write path (Phase 10 bug — `wave-controller.ts:1373` calling a non-existent `graphDB.mergeAttributes`). The adapter exposes the hot read/write surface used by B's four main consumer files (per RESEARCH §3 caller heat map: `queryEntities`, `storeEntity`, `storeRelationship`, `getEntity`, `deleteEntity`, and the missing `mergeAttributes`) but routes through km-core's `GraphKMStore`.

A feature flag `process.env.KM_CORE_PERSISTENCE` (default `legacy`, opt-in to `km-core`) lets both backends coexist during the strangler migration. New entities flow through km-core when the flag is on; reads still go through `GraphDatabaseService` until Plan 5 migrates wave-controller emit shapes.

Phase 10 fix lands HERE per D-52b: the wave-controller bypass call at line 1373 is rewired through the new adapter's `mergeAttributes` (which delegates to km-core's existing `GraphKMStore.mergeAttributes` at `GraphKMStore.ts:854` — see `<context>`). When the flag is active, the embedding lands on the entity via Graphology's `mergeNodeAttributes` directly, with no 7-layer pipeline to swallow it.

Purpose: SC#1 (MCP interface unchanged), SC#2 (Phase 10 embedding fix verifiable end-to-end). This is the keystone plan — subsequent plans depend on the adapter existing.

Output: 1 new adapter file (~250 LoC) + 1 new flag config file + edits to wave-controller.ts + 1 test file. NO deletion of GraphDatabaseService.js in this plan (deferred to Plan 7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/STATE.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- Key km-core surfaces this plan consumes. Read from km-core/src/store/GraphKMStore.ts. -->

From /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts:854-866 (mergeAttributes already exists — DO NOT reinvent):

  async mergeAttributes(nodeId: EntityId, attributes: Partial<Entity>): Promise<void>

  - throws if !this.graph.hasNode(nodeId)
  - calls this.graph.mergeNodeAttributes(nodeId, attributes) (Graphology native)
  - emits 'entity:put' event
  - triggers debounced JSON export

From /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (public API surface to consume from the adapter):

  - new GraphKMStore({ ontologyDir?, ontologyStrict?, dataDir, exportDir, debounceMs })
  - async putEntity(entity, opts?): Promise<Entity>
  - async getEntity(id: EntityId): Promise<Entity | undefined>
  - async findByName(name: string): Promise<Entity | undefined>
  - async findByOntologyClass(klass: string): Promise<Entity[]>
  - async iterate(): AsyncIterable<Entity>
  - async addRelation(r: Relation): Promise<void>
  - async findRelations(opts): Promise<Relation[]>
  - async mergeAttributes(id: EntityId, attrs: Partial<Entity>): Promise<void>
  - async batch(ops: BatchOp[]): Promise<void>

From /Users/Q284340/Agentic/km-core/src/types/entity.ts:113-148 (Entity shape — Phase 39 lock):

  interface Entity {
    id: EntityId;                    // branded UUIDv7
    name: string;
    entityType: string;
    ontologyClass?: string;
    layer: 'evidence' | 'pattern';
    description: string;
    createdAt: string;
    updatedAt: string;
    metadata: Record<string, unknown>;
    validFrom?: string;
    validUntil?: string;
    supersedes?: EntityId;
    legacyId?: { system: 'A' | 'B' | 'C'; id: string };
    // NOTE: `embedding?: number[]` is added by Plan 6 (cross-repo); not present yet.
    // This plan defers writes of `embedding` through `mergeAttributes` because
    // mergeAttributes takes `Partial<Entity>` and Graphology stores arbitrary
    // node attributes — the field passes through even before Plan 6 types it.
  }

From B's existing consumer surface (RESEARCH §3 heat map — confirms the methods the adapter MUST implement):

  - queryEntities(options): 13 callers (persistence-agent + wave-controller + content-validation-agent)
  - storeEntity(entity, options): 6 callers
  - storeRelationship(fromEntity, toEntity, type, metadata): 4 callers
  - getEntity(name, team): 4 callers
  - deleteEntity(name, team, options): 6 callers

The cold methods (`queryRelations`, `queryByOntologyClass`, `findRelated`) have 0 callers in src/ — the adapter throws NotImplementedError on these per RESEARCH §3 closing recommendation. They get filled in later if needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapter type contract + feature flag</name>
  <files>/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§3 — full caller heat map and method-by-method signature reference)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/GraphDatabaseService.js (lines 204-250 storeEntity, 489-512 getEntity, 598-684 storeRelationship, 1047-1275 queryEntities, 1336-1436 queryByOntologyClass — the consumer-facing shapes the adapter must mirror)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (lines 854-866 mergeAttributes — the canonical surface to wrap; do NOT add a duplicate method here)
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (lines 113-148 Entity shape — adapter return rows must match)
    - /Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-05-PLAN.md (Phase 41 cross-repo and adapter precedent)
  </read_first>
  <behavior>
    - Test 1: getPersistenceBackend() returns 'legacy' when KM_CORE_PERSISTENCE is unset.
    - Test 2: getPersistenceBackend() returns 'km-core' when KM_CORE_PERSISTENCE='km-core'.
    - Test 3: getPersistenceBackend() returns 'legacy' on any other value (defensive default).
    - Test 4: createKmCoreAdapter({ store }) returns an object exposing mergeAttributes, queryEntities, storeEntity, storeRelationship, getEntity, deleteEntity as async functions.
    - Test 5: Adapter's queryRelations / queryByOntologyClass / findRelated throw `NotImplementedError: <method>` (cold-path placeholders per RESEARCH §3 closing recommendation).
  </behavior>
  <action>
    Create `src/config/persistence-flag.ts` exporting `getPersistenceBackend(): 'legacy' | 'km-core'` that reads `process.env.KM_CORE_PERSISTENCE` and returns `'km-core'` ONLY when the env var literally equals `'km-core'`; any other value (including undefined) returns `'legacy'`. This is the D-51a feature flag.

    Create `src/storage/km-core-adapter.ts` exporting `KmCoreAdapter` interface AND `createKmCoreAdapter(opts: { store: GraphKMStore; team: string }): KmCoreAdapter` factory. The `KmCoreAdapter` interface MUST expose this hot-path surface (RESEARCH §3 caller heat map confirms these have non-zero callers in B):

      - `mergeAttributes(nodeId: string, attrs: Record<string, unknown>): Promise<void>` — delegates to `store.mergeAttributes` (km-core surface at GraphKMStore.ts:854). NodeId mapping: B uses `${team}:${entityName}` keys; the adapter looks the entity up via `store.findByName(entityName)` to resolve the EntityId, then calls `store.mergeAttributes(entity.id, attrs)`. Throws if entity is not found.
      - `queryEntities(options: { team?: string; searchTerm?: string; ontologyClass?: string; entityType?: string; limit?: number; offset?: number }): Promise<Entity[]>` — iterates via `store.iterate()` and applies the options filters. For ontologyClass / entityType filters, prefer `store.findByOntologyClass` when only that filter is set.
      - `storeEntity(entity: Record<string, unknown>, options: { team: string }): Promise<{ id: string }>` — translates B's SharedMemoryEntity shape to km-core Entity (use the D-54 mapping table from 42-RESEARCH.md §4 — top-level legacyId, ontologyClass copied from entityType, layer='evidence', metadata.subsystem='wave-analysis', mintEntityId for new entities, etc.). Calls `store.putEntity(entity, { skipOntologyCheck: false })`. Returns `{ id }` matching B's existing return contract.
      - `storeRelationship(fromName: string, toName: string, type: string, metadata?: Record<string, unknown>): Promise<void>` — resolves both names to EntityIds via `store.findByName`, then calls `store.addRelation({ type, from, to, metadata })`.
      - `getEntity(name: string, team: string): Promise<Entity | undefined>` — calls `store.findByName(name)`. Team is currently ignored (km-core is single-tenant per store instance); Plan 5 migrates the team-tagging into `metadata.team`.
      - `deleteEntity(name: string, team: string, _options?: Record<string, unknown>): Promise<void>` — resolves name to EntityId via `store.findByName`, then constructs a single `store.batch([{ op: 'removeEntity', id }])`. If entity not found, returns silently (matches B's no-op behavior at GraphDatabaseService.js:513-597).

    Cold-path stubs (RESEARCH §3 confirms zero callers in src/ — defer until needed):
      - `queryRelations(...): Promise<never>` — throws `Error('NotImplementedError: km-core-adapter.queryRelations — no callers in src/, fill in when needed')`.
      - `queryByOntologyClass(...): Promise<never>` — throws `Error('NotImplementedError: km-core-adapter.queryByOntologyClass — no callers in src/, fill in when needed')`.
      - `findRelated(...): Promise<never>` — throws `Error('NotImplementedError: km-core-adapter.findRelated — no callers in src/, fill in when needed')`.

    Use `process.stderr.write(...)` for any diagnostic output per the project's `no-console-log` constraint (CLAUDE.md). All TypeScript types imported from `@fwornle/km-core/store` (`GraphKMStore`) and `@fwornle/km-core` (`Entity`, `Relation`, `EntityId`). Per Phase 39 CF-D37, `legacyId` lives at the top level of Entity, not inside metadata.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; node --test dist/storage/km-core-adapter.test.js 2&gt;&amp;1 | grep -E "(pass|fail|ok|not ok).*(feature flag|surface)"</automated>
  </verify>
  <done>Adapter file + flag file exist; 5 unit tests pass; `KM_CORE_PERSISTENCE=legacy` (the default) is a no-op for all existing call sites; `KM_CORE_PERSISTENCE=km-core` routes writes through the new adapter.</done>
  <acceptance_criteria>
    - Source assertion: `grep -c "from '@fwornle/km-core" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts` returns a non-zero count (adapter imports km-core).
    - Source assertion: `grep -c "process.env.KM_CORE_PERSISTENCE" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts` returns ≥ 1.
    - Source assertion: `grep -v '^//' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts | grep -c 'mergeAttributes' | xargs -I{} test {} -ge 2` (declared in interface AND called on store).
    - Test: 5 unit tests in `src/storage/km-core-adapter.test.ts` all pass via `npm run build && node --test dist/storage/km-core-adapter.test.js`.
    - Behavior assertion: cold-path methods throw with the literal text `NotImplementedError: km-core-adapter.<method>` (verified by test 5).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire wave-controller.ts mergeAttributes bypass path (Phase 10 fix)</name>
  <files>/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts (lines 1351-1411 — the bypass write block; specifically line 1373 broken `this.graphDB.mergeAttributes(nodeId, enrichedAttrs)` call)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§1 — full Phase 10 trace; both bypass path A and re-persist path B)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts:854-866 (the surface the adapter wraps — already exists)
  </read_first>
  <behavior>
    - Test 1: When `KM_CORE_PERSISTENCE=legacy`, the wave-controller's bypass block at lines 1351-1411 still calls `this.graphDB.mergeAttributes(...)` (legacy path unchanged, retains existing broken behavior — but unflagged).
    - Test 2: When `KM_CORE_PERSISTENCE=km-core`, the wave-controller's bypass block calls `kmCoreAdapter.mergeAttributes(nodeId, enrichedAttrs)` instead; `directWriteSuccess` increments, `directWriteFail` stays at 0 for entities with embedding present (mock adapter records the call).
    - Test 3: After the call, the mock km-core store's `mergeAttributes` was invoked with the literal `embedding`, `role`, and `enrichedContext` fields from the source entity (no field-stripping in the adapter's `mergeAttributes` translation).
  </behavior>
  <action>
    In `src/agents/wave-controller.ts` around lines 1351-1411 (the existing bypass write block introduced for Phase 10):

    1. Add a private `kmCoreAdapter?: KmCoreAdapter` field on the WaveController class. Inject it via the constructor as an optional dependency (existing constructor takes a `graphDB` field; add `kmCoreAdapter` alongside it, kept undefined when the flag is off).
    2. At wave-controller bootstrap time (existing `initialize` or equivalent — locate the existing GraphDatabaseService instantiation site and rewire there), import `getPersistenceBackend` from `'../config/persistence-flag.js'`. When `getPersistenceBackend() === 'km-core'`, instantiate `GraphKMStore` (from `@fwornle/km-core/store`) with the ontology dir from D-53a (`{ ontologyDir: <repo-root>/.data/ontologies }` — Plan 3 lands the flatten so this directory shape is correct; until then, this code path is gated off behind the flag, so the broken non-flat layout does not matter). Pass that store to `createKmCoreAdapter` from `'../storage/km-core-adapter.js'`.
    3. Rewrite the bypass write loop at lines 1361-1387: replace the broken `await this.graphDB.mergeAttributes(nodeId, enrichedAttrs)` at line 1373 with a flag-gated branch:

       - If `this.kmCoreAdapter` is defined (flag on), call `await this.kmCoreAdapter.mergeAttributes(nodeId, enrichedAttrs)` — this resolves the Phase 10 bug because the adapter's `mergeAttributes` delegates to km-core's existing `GraphKMStore.mergeAttributes` (verified existing at GraphKMStore.ts:854).
       - Else (flag off / legacy), preserve the existing broken `this.graphDB.mergeAttributes(...)` call exactly as today. This keeps the legacy code path identical so the flag can be toggled back without behavior change.

    4. Preserve all surrounding logging, counters (`directWriteSuccess`, `directWriteFail`), and the catch-block debug log (line 1376-1378) verbatim. The summary log at line 1384 stays.

    Add the 3 new tests (Test 1, 2, 3) to `src/storage/km-core-adapter.test.ts` (same file as Task 1) by constructing a stub WaveController-shaped object that exercises only the bypass loop with a mock adapter / mock graphDB. Do NOT spin up the full WaveController — just the bypass loop. Use the field name `embedding`, `role`, `enrichedContext` per `wave-controller.ts:1363-1372` source.

    Do NOT modify the re-persist fallback path at lines 1388-1410 (`persistWaveResult` / `persistEntities`). That path is Plan 5's territory; this plan is mechanical fix-the-bypass only.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; node --test dist/storage/km-core-adapter.test.js 2&gt;&amp;1 | grep -E "(pass|fail|ok|not ok).*(wave-controller|bypass|phase 10)" &amp;&amp; grep -c 'kmCoreAdapter\.mergeAttributes' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>3 new wave-controller tests pass; `wave-controller.ts:1373` is now flag-gated and routes through the adapter when KM_CORE_PERSISTENCE=km-core; legacy path is preserved verbatim; no other wave-controller code is touched.</done>
  <acceptance_criteria>
    - Source assertion: `grep -v '^[[:space:]]*//' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts | grep -c "kmCoreAdapter\.mergeAttributes"` is ≥ 1.
    - Source assertion: `grep -v '^[[:space:]]*//' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts | grep -c "this\.graphDB\.mergeAttributes"` is still ≥ 1 (legacy branch preserved).
    - Behavior: 3 new tests pass.
    - Behavior: Existing wave-controller test suite still passes — `cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/agents/wave-controller*.test.js` returns exit 0 (or 0 tests matched is acceptable if no wave-controller-specific tests existed pre-Phase 42).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Submodule build + Docker rebuild + SC#2 sanity verification</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md ("Rebuilding After Code Changes" section — the mandatory submodule + Docker dance)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§1 closing — Phase 10 SC#2 verification recipe)
  </read_first>
  <action>
    Run the mandatory submodule + Docker rebuild dance, then verify the Phase 10 fix end-to-end with the flag on:

    1. `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` — compiles TS to `dist/`. Must exit 0; if compilation fails, fix and re-run.
    2. `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` — rebuilds the container image so the new `dist/` lands inside. Must exit 0.
    3. `docker-compose up -d coding-services` — restarts the container.
    4. Verify the new code is in the running container: `docker exec coding-services grep -c 'kmCoreAdapter' /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js`. Must return ≥ 1.
    5. With `KM_CORE_PERSISTENCE=legacy` (default), confirm the legacy path is still callable — no behavior change is required, the flag defaults preserve today's behavior.

    SC#2 deferred-verification recipe (full end-to-end on entities is verified in Plan 7's `ukb full` checkpoint; this Task 3 only verifies the build wiring): run `docker exec coding-services node -e "import('@fwornle/km-core/store').then(m => console.log(Object.keys(m).join(',')))"`. Must include `GraphKMStore`. This proves the km-core package resolves inside the container — the next plans depend on this.

    NO `ukb full` run in this task — Plan 7 owns the end-to-end SC verification. This task only verifies build-time correctness and import resolution.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; cd /Users/Q284340/Agentic/coding/docker &amp;&amp; docker-compose build coding-services &amp;&amp; docker-compose up -d coding-services &amp;&amp; sleep 8 &amp;&amp; docker exec coding-services grep -c 'kmCoreAdapter' /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>npm build green; Docker rebuild green; container restart green; compiled `dist/agents/wave-controller.js` contains the new `kmCoreAdapter` symbol; km-core package resolves inside the container.</done>
  <acceptance_criteria>
    - CLI: `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` exits 0.
    - CLI: `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` exits 0.
    - CLI: `docker exec coding-services grep -c 'kmCoreAdapter' /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js` returns ≥ 1.
    - CLI: `docker exec coding-services node -e "import('@fwornle/km-core/store').then(m => process.exit(m.GraphKMStore ? 0 : 1))"` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wave-controller → km-core store | Operator-enriched fields cross from B's wave pipeline into the canonical km-core store via mergeAttributes |
| feature flag env var → process | KM_CORE_PERSISTENCE env var controls runtime backend selection; an attacker setting this on the host changes persistence semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Tampering | feature flag env var | accept | KM_CORE_PERSISTENCE is a developer-toggle, not security-sensitive; documented in 42-CONTEXT.md D-51a; runs only inside the local dev Docker container |
| T-42-01-02 | Information Disclosure | adapter mergeAttributes | mitigate | Adapter throws on entity-not-found (no silent overwrite); km-core's existing mergeAttributes already enforces hasNode check at GraphKMStore.ts:858 |
| T-42-01-03 | Denial of Service | adapter cold-path stubs | accept | NotImplementedError throws are diagnostic; zero callers per RESEARCH §3 means no live-path impact |
</threat_model>

<verification>
- Compilation: TypeScript build is clean (`npm run build` exit 0).
- Unit: 8 new unit tests in src/storage/km-core-adapter.test.ts (5 from Task 1, 3 from Task 2) all pass.
- Container: km-core symbols resolve inside the container.
- Source: feature-flag-gated rewire is in place (greps in acceptance criteria).
</verification>

<success_criteria>
1. `src/storage/km-core-adapter.ts` exists and exposes `KmCoreAdapter` + `createKmCoreAdapter` with the hot-path methods listed above.
2. `src/config/persistence-flag.ts` exposes `getPersistenceBackend()` returning `'legacy'` by default and `'km-core'` only on the literal env value.
3. `wave-controller.ts:~1373` is flag-gated: legacy path preserved, km-core path routes through the new adapter.
4. Submodule + Docker rebuild dance completes green.
5. Phase 10 fix verification deferred to Plan 7's end-to-end `ukb full` SC#2 check; build-time wiring verified here.
</success_criteria>

<output>
Create `.planning/phases/42-offline-ukb-migration-b/42-01-SUMMARY.md` when done, recording:
- New files created (paths)
- Wave-controller line numbers touched (cite line ranges)
- Test count delta
- Docker rebuild durations
- Confirmation that `KM_CORE_PERSISTENCE=legacy` is the safe default
</output>
