---
phase: 42-offline-ukb-migration-b
plan: 07
type: execute
wave: 4
depends_on:
  - 42-01
  - 42-02
  - 42-03
  - 42-04
  - 42-05
  - 42-06
files_modified:
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/GraphDatabaseService.js
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/KnowledgeStorageService.js
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/QdrantSyncService.js
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/graph-database-adapter.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/content-validation-agent.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/42-07-end-to-end-verify.mjs
autonomous: false
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "GraphDatabaseService.js + KnowledgeStorageService.js + QdrantSyncService.js are DELETED from src/ AND from dist/."
    - "persistence-agent.ts is DELETED — the 7-layer pipeline is gone in favor of the km-core write path landed in Plan 6."
    - "wave-controller.ts no longer has the legacy branch (the else-branch from Plan 6 that called persistenceAgent.persistEntities is removed); the km-core path is the only path. graph-database-adapter.ts is DELETED (no other consumers per RESEARCH §3)."
    - "Feature flag `KM_CORE_PERSISTENCE` is REMOVED — `src/config/persistence-flag.ts` deleted; wave-controller and any other consumer no longer reads it."
    - "content-validation-agent.ts (RESEARCH §3 reports 4 hits to GraphDatabaseService) is rewired to consume the km-core adapter directly — no remaining GraphDatabaseService references in src/."
    - "Goal-backward verification gate: a full wave-analysis workflow run completes successfully against the migrated graph; SC#1 (canonical shape) + SC#2 (every Detail entity has embedding.length === 384) + SC#3 (no '0/0 race' log spam) + SC#4 (dashboard reflects terminal state) + SC#5 (B's component-manifest works against km-core registry) all PASS."
    - "Atomic dir-swap of the migrated LevelDB: `.data/knowledge-graph` becomes the canonical-shape store; `.data/knowledge-graph.pre-42-backup/` preserved until the operator verifies SC #1-5 then explicitly deletes it."
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/42-07-end-to-end-verify.mjs"
      provides: "SC #1-5 verification script — runs the migration, wave-analysis workflow, syncQdrantFromStore, then asserts each SC; emits a verdict JSON to stderr"
    - path: "(deletions)"
      provides: "GraphDatabaseService.js + KnowledgeStorageService.js + QdrantSyncService.js + persistence-agent.ts + graph-database-adapter.ts + persistence-flag.ts ALL removed"
  key_links:
    - from: "wave-controller.ts (post-cleanup)"
      to: "km-core adapter (the only path)"
      via: "no flag branch — direct adapter call"
      pattern: "kmCoreAdapter\\."
    - from: "scripts/42-07-end-to-end-verify.mjs"
      to: "all five SC assertions"
      via: "wave-analysis workflow run + concrete CLI checks for each SC"
      pattern: "SC#1|SC#2|SC#3|SC#4|SC#5"
---

<objective>
Final cleanup + end-to-end SC verification gate. After this plan lands, Phase 42 is complete: no dead code, no strangler flag, no legacy storage modules, and all five success criteria verified against a real production-mode wave-analysis workflow run on production data.

The plan unfolds in three phases:

  1. **Phase A — Atomic dir swap + dry-run verification.** Run Plan 5's migration script against the production LevelDB without `--dry-run`. Atomically rename `.data/knowledge-graph` to `.data/knowledge-graph.pre-42-backup` and `.data/knowledge-graph-migrated` to `.data/knowledge-graph`. Verify entity count match (per RESEARCH §4 — ~727).

  2. **Phase B — Delete dead code.** Remove `GraphDatabaseService.js` + `KnowledgeStorageService.js` + `QdrantSyncService.js` + `persistence-agent.ts` + `graph-database-adapter.ts` + `persistence-flag.ts`. Remove the now-orphaned else-branch from wave-controller.ts's persistWaveResult (the legacy `persistenceAgent.persistEntities` call). Rewire `content-validation-agent.ts` (RESEARCH §3 — 4 GraphDatabaseService hits) directly onto the adapter. Final grep gate: ZERO references in src/ to any deleted module.

  3. **Phase C — Goal-backward verification.** Run the wave-analysis workflow end-to-end (production mode per CLAUDE.md / MEMORY.md). Verify SC#1 (canonical shape on every persisted entity), SC#2 (every Detail entity has embedding.length === 384), SC#3 (zero "0/0 race" log spam), SC#4 (dashboard reports `completed` within 5s of process exit), SC#5 (B's component-manifest works against km-core registry — already proven in Plan 3, re-verified after cleanup). Run `syncQdrantFromStore` to populate Qdrant from the migrated + embedded entities (per RESEARCH §6 Risk 2's mandated sequencing).

This plan has a checkpoint:human-verify at the end of Phase C — the operator confirms each SC visually (via dashboard + log inspection) before declaring Phase 42 complete.

Purpose: SC#1-5 verification + dead-code elimination. Phase 42 closes when this plan lands.

Output: 6 file deletions, ~3 file edits (wave-controller + content-validation-agent + the script), 1 new verification script. NO `must_haves` changes from prior plans — those still bind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-01-PLAN.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-02-PLAN.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-04-PLAN.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-05-PLAN.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-06-PLAN.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- Verification surfaces consumed by this plan -->

From km-core (via Plan 4 + Plan 6 wiring):

  - syncQdrantFromStore(store, opts) — Phase 42 Plan 4 deliverable
  - GraphKMStore.findByOntologyClass(klass) — Phase 37 surface, used by SC#2 verification

From the migration script (Plan 5):

  - node scripts/migrate-leveldb-to-kmcore.mjs --source=.data/knowledge-graph --target=.data/knowledge-graph-migrated --run-id=42-07-prod
  - Idempotent: a second run is a no-op (RESEARCH §4 idempotency contract)

From Plan 2 (race condition fix):
  - coordinator.writeProgressFile is field-preserving — SC#3 + SC#4 already provisionally green from Plan 2's wave-analysis smoke run; this plan re-asserts them after cleanup

From RESEARCH §3 caller heat map (the consumer files affected by GraphDatabaseService deletion):

  - persistence-agent.ts (DELETED in this plan along with GraphDatabaseService — the deletion is bilateral)
  - wave-controller.ts (cleaned up — legacy else-branch removed)
  - content-validation-agent.ts (REWIRED — 4 hits to GraphDatabaseService become 4 hits to kmCoreAdapter)
  - graph-database-adapter.ts (DELETED — no consumers remain after the others are migrated)

From RESEARCH §6 Open Question 3:
  - dist/ artifacts may have stale references to deleted modules; this plan's cleanup-grep gate covers both src/ AND dist/ to ensure the compiled output is clean.

From RESEARCH §6 Risk 2:
  - Qdrant blank window during migration is real; sequencing is: migration then wave-analysis workflow then syncQdrantFromStore. This plan's Phase C executes the sequence.

CHECKPOINT format (operator verifies all 5 SCs after Phase C completes):

  - SC#1: emit-shape — `docker exec coding-services node -e "<inline check>"` confirms 5 random entities from the migrated graph have top-level legacyId + ontologyClass + layer='evidence'
  - SC#2: embeddings — `docker exec coding-services node -e "<inline check>"` confirms every Detail entity has embedding.length === 384
  - SC#3: race log — `docker logs coding-services --since <pre-run> | grep -c 'Race condition detected (0/0 steps)'` returns 0
  - SC#4: dashboard terminal-state — within 5s of `.data/workflow-progress.json` showing `status: completed`, the dashboard `/api/status` returns `completed`
  - SC#5: registry — `docker exec coding-services node -e "<OntologyRegistry load + class lookup>"` confirms Component + Detail + SubComponent all hasClass === true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Phase A): Run production migration + atomic dir swap</name>
  <files>/Users/Q284340/Agentic/coding/.data/knowledge-graph (atomic rename target), /Users/Q284340/Agentic/coding/.data/knowledge-graph-migrated (source of swap), /Users/Q284340/Agentic/coding/.data/knowledge-graph.pre-42-backup (backup), /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/42-07-end-to-end-verify.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-05-PLAN.md (the migration script Plan 5 produced — script + flags reference)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§4 production baseline — 727 entities expected)
    - /Users/Q284340/Agentic/coding/CLAUDE.md (Docker context — all operations inside coding-services container)
  </read_first>
  <action>
    1. Pre-check: ensure no wave-analysis workflow is currently running. Hit `curl -s http://localhost:3033/api/ukb/status` and parse `.running` via jq; MUST be `false`. If a workflow is in progress, ABORT this task and re-attempt later.

    2. Backup the existing exports first: `cp .data/exports/coding.json .data/exports/coding.json.pre-42-backup`. This is the OKB-baseline guard hygiene per Phase 37 D-21 / CLAUDE.md commit conventions.

    3. Run the production migration via Plan 5's script (NO --dry-run this time). Invoke via `docker exec coding-services node /coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs --source=/coding/.data/knowledge-graph --target=/coding/.data/knowledge-graph-migrated --run-id=42-07-prod-<UTC-timestamp>`.

       Capture the JSON summary line. Per RESEARCH §4 production baseline, `totalEntities` MUST be ≥ 700; if not, ABORT and surface to operator. The script must exit 0 (errorCount / total ≤ 0.05 per Plan 5's error budget).

    4. Verify target is populated: `docker exec coding-services ls /coding/.data/knowledge-graph-migrated/ | wc -l` MUST be > 5 (LevelDB has multiple sst/log files).

    5. Stop the coordinator processes that hold LevelDB handles open (otherwise the rename will fail on macOS): `docker-compose stop coding-services` from the docker dir.

    6. Atomic swap (on the host, after container stop):

       `cd /Users/Q284340/Agentic/coding/.data && mv knowledge-graph knowledge-graph.pre-42-backup && mv knowledge-graph-migrated knowledge-graph`

    7. Restart container: `cd /Users/Q284340/Agentic/coding/docker && docker-compose up -d coding-services`.

    8. Smoke-check the new canonical store loads via a node -e one-liner that opens the GraphKMStore at `/coding/.data/knowledge-graph`, iterates with `for await`, counts entities, and exits 0 if `entityCount >= 700`.

    9. Create the verification script `scripts/42-07-end-to-end-verify.mjs` for use in Task 3. The script's body is the consolidated set of SC #1-5 checks; Task 3 invokes it. Structure:
       - parse `--qdrant-url` flag (default `http://localhost:6333` per the existing B compose layout)
       - parse `--collection` flag (default `coding`)
       - parse `--since` flag (the pre-run docker-logs timestamp)
       - run SC#1 check: open the km-core store, iterate first 10 entities, assert each has top-level legacyId.system==='B', layer==='evidence', ontologyClass defined
       - run SC#2 check: `store.findByOntologyClass('Detail')`; assert every entity has `embedding.length === 384`
       - run SC#3 check: shell out to docker logs since the passed-in `--since` timestamp; grep for the race-warning literal; assert count === 0
       - run SC#4 check: read `.data/workflow-progress.json`; if status === 'completed', curl `/api/status` and assert workflow.status === 'completed'
       - run SC#5 check: instantiate `new OntologyRegistry({ ontologyDir: '/coding/.data/ontologies' })`; assert hasClass for Component + Detail + SubComponent + Project
       - emit a verdict JSON line with `{ sc1: pass|fail, sc2: ..., sc3: ..., sc4: ..., sc5: ..., overall: pass|fail }` and exit 0 (all pass) or 1 (any fail)

       Use `process.stderr.write` for diagnostics; final verdict JSON to stdout.
  </action>
  <verify>
    <automated>test -d /Users/Q284340/Agentic/coding/.data/knowledge-graph.pre-42-backup &amp;&amp; test -d /Users/Q284340/Agentic/coding/.data/knowledge-graph &amp;&amp; test ! -d /Users/Q284340/Agentic/coding/.data/knowledge-graph-migrated &amp;&amp; test -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/42-07-end-to-end-verify.mjs</automated>
  </verify>
  <done>Production migration ran (≥ 700 entities migrated); atomic dir swap complete (pre-42-backup preserved); container restarted and the new canonical store reads ≥ 700 entities; verification script created.</done>
  <acceptance_criteria>
    - CLI: production migration script exits 0 with totalEntities ≥ 700 and errorCount/totalEntities ≤ 0.05.
    - CLI: `test -d /Users/Q284340/Agentic/coding/.data/knowledge-graph.pre-42-backup` exits 0 (backup preserved).
    - CLI: `test ! -d /Users/Q284340/Agentic/coding/.data/knowledge-graph-migrated` exits 0 (migration target was renamed away).
    - CLI: post-swap inline node iterate check exits 0 with entityCount ≥ 700 (the new canonical store reads cleanly).
    - CLI: `test -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/42-07-end-to-end-verify.mjs` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (Phase B): Delete dead code + remove flag + rewire content-validation-agent</name>
  <files>/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/GraphDatabaseService.js, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/KnowledgeStorageService.js, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/QdrantSyncService.js, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/graph-database-adapter.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/content-validation-agent.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§3 — full caller heat map for GraphDatabaseService; the consumer files MUST all be cleaned up before deletion)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/content-validation-agent.ts (the 4 GraphDatabaseService call sites — RESEARCH §3 lists this as the remaining consumer; locate via `grep -n "graphDB\.\|graphDatabase\.\|new GraphDatabaseService" content-validation-agent.ts`)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts (the else-branch from Plan 6 that calls persistenceAgent.persistEntities — remove it now)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/graph-database-adapter.ts (full file — confirm no Phase 42 plan added any callers; if zero callers, safe to delete)
  </read_first>
  <action>
    1. Rewire `content-validation-agent.ts` (RESEARCH §3 — 4 GraphDatabaseService hits):

       - Locate every `this.graphDB.<method>(...)` call. Map to the equivalent `kmCoreAdapter.<method>(...)`. The method names match for the hot-path subset (queryEntities, getEntity, storeEntity, storeRelationship, deleteEntity) — Plan 1 deliberately mirrored the surface.
       - If any call uses a method NOT on the adapter (e.g., `findRelated`, `queryRelations`, `queryByOntologyClass`), implement that method on the adapter NOW. RESEARCH §3 noted these have zero callers — confirm with `grep -n "this.graphDB.queryRelations" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/`; if zero in non-deleted files, the adapter's `NotImplementedError` stub stays.
       - Update the constructor injection: replace `graphDB` with `kmCoreAdapter`.

    2. In `wave-controller.ts`: locate the flag-gated branch landed in Plan 6 Task 2. Remove the legacy else-branch entirely. The km-core path becomes the unconditional path. Also remove:
       - Any `import { getPersistenceBackend } from '../config/persistence-flag.js'` lines (now dead).
       - Any `if (getPersistenceBackend() === ...)` checks (replaced by direct adapter calls).
       - The constructor's `kmCoreAdapter?: KmCoreAdapter` becomes `kmCoreAdapter: KmCoreAdapter` (required — no longer optional).
       - The Plan 1 bypass write at line 1373 also collapses: remove the flag check, leave only the km-core adapter call.

    3. Delete the six files via git rm:
       - `git rm /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/GraphDatabaseService.js`
       - `git rm /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/KnowledgeStorageService.js`
       - `git rm /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/QdrantSyncService.js`
       - `git rm /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts`
       - `git rm /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/graph-database-adapter.ts`
       - `git rm /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts`

       If `src/knowledge-management/` becomes empty after these deletions, also `rmdir` it.

    4. Cleanup grep gate (BLOCKING — if fails, do NOT proceed to build):

       `grep -rln 'GraphDatabaseService\|KnowledgeStorageService\|QdrantSyncService\|persistence-agent\|graph-database-adapter\|KM_CORE_PERSISTENCE\|getPersistenceBackend' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ --include='*.ts' --include='*.js'`

       MUST return zero matches. If any match remains, fix the import/reference before proceeding.

    5. Per RESEARCH §6 Open Question 3, also grep dist/ + docker/ + scripts/ for stale references to deleted modules. Find any and surface in SUMMARY:

       `grep -rln 'KnowledgeStorageService\|QdrantSyncService' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/dist/ /Users/Q284340/Agentic/coding/docker/ /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/ 2>/dev/null || true`

       dist/ matches are normal until the next npm run build; docker/ + scripts/ matches require remediation.

    6. Build: `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` — exit 0. This validates that all the remaining TypeScript compiles after the deletions (catches any missed reference).

    Diagnostic output via process.stderr.write.
  </action>
  <verify>
    <automated>test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/GraphDatabaseService.js &amp;&amp; test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts &amp;&amp; test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts &amp;&amp; ! grep -rln 'GraphDatabaseService\|persistence-agent\|KM_CORE_PERSISTENCE' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ --include='*.ts' --include='*.js' &amp;&amp; cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build</automated>
  </verify>
  <done>6 files deleted from src/; content-validation-agent rewired; wave-controller's else-branch removed; KM_CORE_PERSISTENCE flag gone; grep gate confirms zero residual references; npm run build green.</done>
  <acceptance_criteria>
    - CLI: `test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/GraphDatabaseService.js` exits 0.
    - CLI: `test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/KnowledgeStorageService.js` exits 0.
    - CLI: `test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/knowledge-management/QdrantSyncService.js` exits 0.
    - CLI: `test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts` exits 0.
    - CLI: `test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/storage/graph-database-adapter.ts` exits 0.
    - CLI: `test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/config/persistence-flag.ts` exits 0.
    - CLI (grep gate — MUST be empty): `grep -rln 'GraphDatabaseService\|KnowledgeStorageService\|QdrantSyncService\|persistence-agent\|graph-database-adapter\|KM_CORE_PERSISTENCE\|getPersistenceBackend' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ --include='*.ts' --include='*.js'` returns no output.
    - CLI (content-validation rewired): `grep -c 'kmCoreAdapter' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/content-validation-agent.ts` returns ≥ 1.
    - CLI (build clean): `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (Phase C automated): Submodule build + Docker rebuild + production wave-analysis run + syncQdrantFromStore + SC verification script</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md ("Rebuilding After Code Changes" section AND "UKB Workflow Control" section)
    - /Users/Q284340/.claude/projects/-Users-Q284340-Agentic-coding/memory/MEMORY.md ("UKB Workflow Execution" + "Sticky debug state bug" sections)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/42-07-end-to-end-verify.mjs (the verification script created in Task 1)
  </read_first>
  <action>
    1. Build: `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` exit 0.
    2. Container rebuild: `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` exit 0.
    3. Restart: `docker-compose up -d coding-services`.
    4. Confirm dist/ is clean of deleted modules: `docker exec coding-services bash -c "test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/knowledge-management/GraphDatabaseService.js && test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/agents/persistence-agent.js && test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/config/persistence-flag.js"` exits 0.

    5. Sticky-debug-state reset per MEMORY.md (mandatory before any production workflow):
       - `curl -s -X POST http://localhost:3033/api/ukb/mock-llm -H "Content-Type: application/json" -d '{"enabled": false}'`
       - `curl -s -X POST http://localhost:3033/api/ukb/single-step-mode -H "Content-Type: application/json" -d '{"enabled": false}'`
       - `curl -s -X POST http://localhost:3033/api/ukb/llm-mode/global -H "Content-Type: application/json" -d '{"mode": "public"}'`

    6. Record the pre-run cutoff timestamp into a shell variable PRERUN_TS by extracting the latest docker log line's timestamp. This is the `--since` argument for the SC#3 log grep later.

    7. Start the wave-analysis workflow in PRODUCTION mode via the MCP tool `mcp__semantic-analysis__execute_workflow` with `workflow_name: 'wave-analysis'`, `async_mode: true`, `parameters: { team: 'coding' }`, `debug: false`. NO singleStepMode, NO mockLLM. (Per CLAUDE.md/MEMORY.md: this is the production-mode run.)

    8. Poll `.data/workflow-progress.json` every 30s until `status === 'completed'` or `failed`. Max wait: 45 minutes (longer than Plan 2's window because this run executes the full canonical-shape pipeline for the first time on production data).

    9. If `status === 'failed'`: capture full docker logs into `42-07-VERIFY-FAIL.log` and ABORT. The failure must be diagnosed before declaring Phase 42 complete.

    10. On `status === 'completed'`: run `syncQdrantFromStore` to rebuild Qdrant from the canonical store (per RESEARCH §6 Risk 2 sequencing). Invoke via `docker exec coding-services node -e "..."` with the inline body that opens GraphKMStore at `/coding/.data/knowledge-graph`, instantiates a Qdrant client from `@qdrant/js-client-rest` against `process.env.QDRANT_URL || 'http://localhost:6333'`, calls `syncQdrantFromStore(store, { qdrantClient, collection: 'coding' })`, prints the result JSON, and exits 0. Expect `syncedCount > 100` (most Detail entities now have embeddings from the workflow run).

    11. Run the SC verification script: `docker exec coding-services node /coding/integrations/mcp-server-semantic-analysis/scripts/42-07-end-to-end-verify.mjs --since=$PRERUN_TS` and capture the verdict JSON. ALL FIVE SCs (sc1..sc5) MUST report `pass`.

    12. If ANY SC fails: write the failing checks to `42-07-VERIFY-FAIL.log` and ABORT before the human checkpoint. The operator must triage before the phase closes.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; cd /Users/Q284340/Agentic/coding/docker &amp;&amp; docker-compose build coding-services &amp;&amp; docker-compose up -d coding-services &amp;&amp; sleep 10 &amp;&amp; docker exec coding-services bash -c "test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/knowledge-management/GraphDatabaseService.js"</automated>
  </verify>
  <done>Build + rebuild green; dist/ clean of deleted modules; the workflow ran to completed; syncQdrantFromStore populated Qdrant with > 100 points; SC verification script reports all 5 SCs pass.</done>
  <acceptance_criteria>
    - CLI: `npm run build` exits 0.
    - CLI: `docker-compose build coding-services` exits 0.
    - CLI: `docker exec coding-services bash -c "test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/knowledge-management/GraphDatabaseService.js"` exits 0.
    - CLI: `.data/workflow-progress.json` shows `status: 'completed'` after the workflow run.
    - CLI: syncQdrantFromStore result JSON has `syncedCount > 100` AND `errors.length === 0`.
    - CLI: `docker exec coding-services node scripts/42-07-end-to-end-verify.mjs --since=<PRERUN_TS> | jq -e '.overall == "pass"'` exits 0.
    - Behavior (SC#2 inline): the SC#2 inline node check (open GraphKMStore, findByOntologyClass('Detail'), filter entities without 384-len embedding) returns missing=0.
    - Behavior (SC#3 inline): `docker logs coding-services --since <PRERUN_TS> 2>&1 | grep -c 'Race condition detected (0/0 steps) but no valid cache available'` returns exactly 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (Phase C human gate): Operator confirms SC#1-5 + approves backup deletion</name>
  <action>Operator runs the verification commands listed in <how-to-verify> below. The plan does NOT proceed until the operator types "approved" or describes failures for triage. This task is intentionally human-gated per autonomous: false.</action>
  <what-built>
    - Plans 42-01 through 42-06 landed: km-core adapter; race-fix in coordinator; ontology subsystem flatten; km-core embedding + syncQdrantFromStore + FastembedEmbeddingClient; LevelDB migration script; canonical-shape wave-agent emits + km-core mergeEntities adoption.
    - Plan 42-07 Phase A: production LevelDB migrated; atomic dir swap to canonical-shape store; backup preserved at `.data/knowledge-graph.pre-42-backup/`.
    - Plan 42-07 Phase B: GraphDatabaseService + KnowledgeStorageService + QdrantSyncService + persistence-agent + graph-database-adapter + persistence-flag all deleted; KM_CORE_PERSISTENCE flag removed; content-validation-agent rewired.
    - Plan 42-07 Phase C automated: production wave-analysis workflow ran to completion; syncQdrantFromStore populated Qdrant; SC verification script reports all 5 SCs passing.
  </what-built>
  <how-to-verify>
    1. **SC#1 (canonical shape):** Open the dashboard at http://localhost:3032/. Navigate to VKB graph view. Confirm entities render with their classes (Project/Component/SubComponent/Detail) and that clicking one shows the new metadata.descriptionSegments + provenance fields (no more raw `observations[]` as the primary content).

    2. **SC#2 (embeddings):** Run `docker exec coding-services node -e "import('@fwornle/km-core/store').then(async m => { const s = new m.GraphKMStore({ ontologyDir: '/coding/.data/ontologies', dataDir: '/coding/.data/knowledge-graph' }); await s.open(); const ds = await s.findByOntologyClass('Detail'); console.log('Total Detail:', ds.length, 'with embedding:', ds.filter(e => e.embedding && e.embedding.length === 384).length); await s.close(); })"`. Both numbers should match (every Detail has a 384-dim embedding).

    3. **SC#3 (race condition log):** `docker logs coding-services --since <PRERUN_TS> 2>&1 | grep -c "Race condition detected (0/0 steps)"` returns 0.

    4. **SC#4 (dashboard terminal state):** Dashboard's wave-analysis card shows `completed` (not `running`). Refresh the page to confirm it persists.

    5. **SC#5 (registry):** `docker exec coding-services node -e "import('@fwornle/km-core/ontology').then(m => { const r = new m.OntologyRegistry({ ontologyDir: '/coding/.data/ontologies' }); console.log({hasComponent: r.hasClass('Component'), hasDetail: r.hasClass('Detail'), domainCount: r.listDomains().length}); })"`. All checks should return true / ≥ 8.

    6. After confirming all 5: optionally delete the backup with `rm -rf .data/knowledge-graph.pre-42-backup` if you're confident. Keep it for at least 48h if you want a safety window.

    7. **OKB-baseline guard hygiene:** `.data/exports/coding.json` should be regenerating after the workflow run (the canonical store's git-tracked export). Verify it has been touched recently: `ls -la .data/exports/coding.json | awk '{print $6, $7, $8}'`.

    Respond "approved" if SC#1-5 all green and SUMMARY captures the migration outcome. If anything fails, describe what failed for triage.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator then dir-swap operation | Mass file move on production data; recovery depends on backup integrity |
| production workflow process then canonical store | First production run against the new pipeline; potential for unknown data-shape issues to surface |
| Qdrant rebuild | The semantic-search index is fully rebuilt; if syncQdrantFromStore fails partially, search is degraded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07-01 | Tampering | atomic dir swap | mitigate | Backup directory preserved; container stopped before rename to ensure no open file handles; restart-and-verify gate before declaring success |
| T-42-07-02 | Information Disclosure | dead-code residue in dist/ | mitigate | Docker rebuild forces fresh dist/; explicit grep checks against dist/ after rebuild |
| T-42-07-03 | Denial of Service | workflow failure on canonical pipeline | mitigate | Failure halts the plan before the human gate; failure log captured for triage; backup preserves a rollback path |
| T-42-07-04 | Repudiation | SC verification | mitigate | All 5 SCs verified by automated script + operator human-verify; verdict JSON captured in SUMMARY |
| T-42-07-05 | Tampering | Qdrant rebuild | mitigate | syncQdrantFromStore is idempotent; errors[] is captured + asserted empty in acceptance |
</threat_model>

<verification>
- 6 file deletions; grep gate confirms no residual references in src/.
- Build + Docker rebuild green; dist/ clean.
- Production wave-analysis workflow run completes successfully.
- syncQdrantFromStore populates Qdrant cleanly.
- SC#1-5 verification script reports all green.
- Human gate confirms via dashboard + inline checks.
</verification>

<success_criteria>
1. GraphDatabaseService + KnowledgeStorageService + QdrantSyncService + persistence-agent + graph-database-adapter + persistence-flag all deleted from src/ AND dist/.
2. KM_CORE_PERSISTENCE flag fully removed.
3. Production LevelDB migrated; atomic dir swap complete; backup preserved.
4. Production wave-analysis workflow ran to completion against the canonical-shape pipeline.
5. Qdrant rebuilt via syncQdrantFromStore.
6. All five SC checks (SC#1-5) report PASS via automated script.
7. Operator confirms via human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/42-offline-ukb-migration-b/42-07-SUMMARY.md` when done, recording:
- Production migration result (totalEntities, errorCount, runId)
- 6 deleted files (paths)
- LoC delta across all 7 plans (Phase 42 totals)
- Production workflow run duration
- syncQdrantFromStore result (syncedCount, errors)
- SC verification verdict (all 5 SCs must be `pass`)
- Operator approval timestamp
- Final note: Phase 42 closes; STATE.md update to Phase 43 (OKM Cross-Repo Migration C) is the next milestone work
</output>
