---
phase: 42-offline-ukb-migration-b
plan: 06
type: execute
wave: 3
depends_on:
  - 42-01
  - 42-04
  - 42-05
files_modified:
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller-canonical-emit.test.ts
autonomous: true
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "wave1/wave2/wave3 project/component/detail agents emit entities in canonical km-core Entity shape (top-level legacyId.system === 'B', ontologyClass set to the wave's class, layer === 'evidence', descriptionSegments[0] populated, embedding carried through if computed by the kg-operators embedding step)."
    - "When `KM_CORE_PERSISTENCE=km-core`, the wave-controller's `persistWaveResult` path routes entity persistence through the Plan 1 km-core adapter (`storeEntity`/`storeRelationship`) instead of through `persistence-agent.persistEntities` (the 7-layer pipeline). Per D-52b, the 7-layer pipeline is bypassed entirely when the flag is on."
    - "B's local `deduplication.ts:342 mergeEntityGroup` is DELETED. Wave-controller dedup logic that previously called it now imports `mergeEntities` from `@fwornle/km-core/maintenance` (D-50a) and invokes the shared primitive."
    - "Wave-controller's progress-write path is preserved verbatim — Plan 2 already shipped the field-preserving merge fix to coordinator.writeProgressFile; this plan adds no further changes to that surface."
    - "When `KM_CORE_PERSISTENCE=legacy` (default), no wave-controller behavior changes — the legacy path through persistence-agent runs unchanged."
    - "Embedding flows END-TO-END: a kg-operators run that populates `entity.embedding` reaches the canonical store via either (a) the bypass write at line 1373 (flag-gated to use kmCoreAdapter.mergeAttributes, landed in Plan 1) OR (b) the persistEntities-replacement path landed in this plan."
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts"
      provides: "Canonical emit + flag-gated km-core write path replacing persistence-agent.persistEntities"
      contains: "kmCoreAdapter.storeEntity"
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts"
      provides: "mergeEntityGroup deleted; calls forward to km-core mergeEntities"
      contains: "mergeEntities"
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller-canonical-emit.test.ts"
      provides: "Unit tests proving canonical shape emission + flag-gated write routing + mergeEntities adoption"
  key_links:
    - from: "deduplication.ts (after edits)"
      to: "@fwornle/km-core/maintenance mergeEntities"
      via: "import statement + invocation replacing local mergeEntityGroup"
      pattern: "from '@fwornle/km-core/maintenance'"
    - from: "wave-controller.ts persistWaveResult"
      to: "kmCoreAdapter.storeEntity + .storeRelationship (Plan 1 adapter)"
      via: "flag-gated branch routing entity persistence through km-core when KM_CORE_PERSISTENCE=km-core"
      pattern: "kmCoreAdapter\\.storeEntity|kmCoreAdapter\\.storeRelationship"
---

<objective>
Migrate the wave-controller + wave1/wave2/wave3 agents + kg-operators + deduplication to emit and persist entities in canonical km-core Entity shape (top-level legacyId, ontologyClass, descriptionSegments, layer='evidence'). This is the substantive emit-shape migration that ROADMAP SC#1 calls "a KM-Core-shaped knowledge graph (canonical entity, ontology registry, layered dedup, temporal validity) with the existing MCP interface unchanged".

Per CONTEXT D-51 Plan 3 mapping (the wave-controller + agent emit-shape migration was assigned to Plans 3+; in this 7-plan structure that lands here as Plan 6 / Wave 3), the surface to migrate is:

  - wave-controller.ts:2107-2124 (persistWaveResult → persistEntities call site)
  - wave1/wave2/wave3-*-agent.ts entity construction sites (each agent emits class-typed entities — these must now carry top-level legacyId/ontologyClass/descriptionSegments)
  - kg-operators.ts KGEntity interface (the operator-pipeline shape — must align with canonical Entity going forward)
  - deduplication.ts:342 mergeEntityGroup (DELETED per D-50a, replaced by km-core mergeEntities)
  - persistence-agent.ts storeEntityToGraph code path (when the flag is on, this is bypassed; when off, it remains)

The plan depends on Plan 4 (km-core embedding field landed — agents emit it without a TS error) and Plan 5 (migrated LevelDB exists, so the store the adapter wraps already has canonical-shape entities for queries). The cleanup of GraphDatabaseService/KnowledgeStorageService/QdrantSyncService stays in Plan 7.

Purpose: SC#1 (canonical shape after `ukb full`). Plan 7 verifies SC#1 end-to-end; this plan delivers the emit-shape change that makes SC#1 pass.

Output: ~6 modified source files + 1 new test file. ~150-250 net LoC change (the deletion of mergeEntityGroup offsets the additions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-01-PLAN.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-04-PLAN.md
@/Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- km-core mergeEntities surface (Phase 41 D-50a — replaces B's local mergeEntityGroup) -->

From /Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts (Phase 41 plan 05 — landed):

  async function mergeEntities(
    store: GraphKMStore,
    survivorId: EntityId,
    duplicateIds: EntityId[],
    opts: MergeOptions
  ): Promise<MergeResult>

  Options include: provenance: ProvenanceStamp (caller-supplied per CF-D30/D-32)

  Result: { mergedCount, edgesRewired, supersededCount, segmentFolds, errors[] }

  Behavior: atomic batch — closes validUntil on each duplicate, adds SUPERSEDED_BY edges, rewires edges to survivor, folds descriptionSegments, bumps confirmationCount. Phase 42 + 43 reuse this.

From /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts:342 (mergeEntityGroup — DELETED in this plan):

  This is the local function whose call sites now forward to km-core mergeEntities. The function body is removed; callers (search for "mergeEntityGroup(" in the file) become `await mergeEntities(store, survivorId, duplicateIds, { provenance: { provider: 'wave-controller', model: 'phase-42', runId, timestamp: <now> } })`.

  The call sites in deduplication.ts (typically inside the wave dedup loop) MUST first map B's name-keyed entity references to km-core EntityIds via `store.findByName(name)`. If a name doesn't resolve, log to stderr and skip the merge (do NOT throw — preserves wave run resilience).

From wave1-project-agent.ts / wave2-component-agent.ts / wave3-detail-agent.ts (existing emit shape):

  Currently emit entities shaped like SharedMemoryEntity (entityType, observations, significance, metadata, hierarchyLevel, parentEntityName, ...) — per persistence-agent.ts:42 + RESEARCH §4. The migration target: top-level legacyId.system === 'B', ontologyClass (matches the wave class — Project for wave1, Component for wave2, Detail for wave3 — and SubComponent for wave2's sub-emit per RESEARCH §4 production distribution), descriptionSegments[0] built via mergeDescriptionSegment building block (Phase 39 CF-D39), layer === 'evidence', metadata.subsystem === 'wave-analysis'.

  Concrete migration approach: introduce a single helper `toCanonicalEntity(rawEntity, waveClass, runId)` in `src/agents/canonical-mapper.ts` (NEW file) that wave1/wave2/wave3 agents all call before returning their entities. The helper applies the same D-54 mapping table that Plan 5's migration script implements — but on fresh emit, not on legacy data.

From wave-controller.ts:2107-2124 (the persistEntities call site):

  This is the seven-layer pipeline entry. When KM_CORE_PERSISTENCE=km-core, replace the `await persistenceAgent.persistEntities(...)` call with `await persistWithKmCore(qualityFilteredEntities, this.kmCoreAdapter, runId)` — a new private method that iterates each entity, calls `kmCoreAdapter.storeEntity(entity, { team: this.team })` for the put, and `kmCoreAdapter.storeRelationship(from, to, type, metadata)` for each edge.

  Legacy path (flag off): unchanged.

From kg-operators.ts:31-47 KGEntity interface:

  ```
  interface KGEntity {
    id: string; name: string; type: string;     // ← `type`, not `entityType`
    observations: string[]; significance: number;
    embedding?: number[]; role?: 'core' | 'non-core';
    parentId?: string; level?: number;
    // ... 13 fields total
  }
  ```

  Per the MEMORY note + RESEARCH §4 closing: this interface lacks `entityType`/`metadata`. Coordinator casts `as KGEntity` after adding those. The migration target: align the KGEntity interface with canonical Entity-compatible field names so the cast is no longer lossy. Concretely: add `entityType?: string`, `ontologyClass?: string`, `metadata?: Record<string, unknown>`, `legacyId?: { system: 'B'; id: string }` as optional fields. The `type` field stays for back-compat but is treated as deprecated (commented).

From wave-controller.ts:1351-1411 (the Plan 1 bypass write block):

  Plan 1 already flag-gated this — when km-core is on, calls `kmCoreAdapter.mergeAttributes(nodeId, enrichedAttrs)` for the operator-enriched fields (embedding, role, enrichedContext). This plan does NOT modify that block; it just confirms the upstream entity build path (wave agents) produces nodes whose IDs resolve in km-core. Since Plan 5 already migrated the LevelDB, the `${team}:${entity.name}` lookups via `store.findByName` will resolve to the migrated entities for the first run.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonical-mapper helper + wave1/wave2/wave3 agent rewire</name>
  <files>/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller-canonical-emit.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§4 — the same mapping table Plan 5 implements; emit-time and migration-time share the rules)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs (Plan 5's mapper — emit-time helper imports/borrows from the SAME mapping logic; consider factoring shared logic into a new module if duplication is significant)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts (full file — locate the entity emit point — typically at the end of the wave's main processing function)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts (same — wave2 emits Component AND SubComponent entities per RESEARCH §4)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts (same — wave3 emits Detail entities)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts (lines 31-47 — KGEntity interface; lines 148-152 + 340 — embedding-operator return shape)
    - /Users/Q284340/Agentic/km-core/src/segments/merge.ts (Phase 39 mergeDescriptionSegment building block — re-use this for descriptionSegments[0])
  </read_first>
  <behavior>
    - Test 1: `toCanonicalEntity(rawEntity, 'Project', runId)` returns an Entity with `ontologyClass: 'Project'`, `layer: 'evidence'`, `legacyId.system: 'B'`, `metadata.subsystem: 'wave-analysis'`, `metadata.provenance.createdBy.runId === runId`.
    - Test 2: `toCanonicalEntity` builds `descriptionSegments[0]` from `rawEntity.observations` via Phase 39's mergeDescriptionSegment building block; the segment's `provider` is `'wave-analysis'` (NOT `'phase-42-migration'` — emit-time differs from migration-time on the provider tag, per the canonical mapper's runtime-stamp convention).
    - Test 3: When `rawEntity.embedding` is present (e.g., from kg-operators embedding step), the canonical entity carries `embedding` verbatim (Plan 4's typed field accepts it).
    - Test 4: KGEntity interface (`kg-operators.ts:31`) gains optional fields `entityType?: string`, `ontologyClass?: string`, `metadata?: Record<string, unknown>`, `legacyId?: { system: 'B'; id: string }`. All Phase 10/Phase 11 existing tests of kg-operators still pass (added fields are optional; existing tests don't reference them).
    - Test 5: wave1-project-agent's output entities all have `ontologyClass === 'Project'`; wave2's have `ontologyClass === 'Component'` (and `'SubComponent'` for the sub-emit path); wave3's have `ontologyClass === 'Detail'`.
    - Test 6: The `runId` flowing through the wave is stable within one `ukb full` run (verified by capturing the runId on every entity emitted by all three waves in a single test run; they all match).
  </behavior>
  <action>
    1. Create `src/agents/canonical-mapper.ts` exporting:

       - `toCanonicalEntity(raw: RawWaveEntity, ontologyClass: string, runId: string, options?: { provider?: string; model?: string }): Entity`
       - `defaultProvider = 'wave-analysis'`, `defaultModel = 'b-phase-42'` — used for the provenance stamp when options aren't provided.

       The function applies the same D-54 mapping rules as Plan 5's migration script (RESEARCH §4 table). Reuse: if Plan 5 extracted the mapping into a shared module (`src/agents/canonical-mapping/mapper.js` or similar), import from there. If Plan 5's script kept the mapping inline (because it's pure-ESM scripts), reimplement here (the duplication is acceptable: emit-time has different provenance stamp values than migration-time, so the high-level structure differs by provider/model strings even if the field plumbing is identical). Surface this choice in SUMMARY.

       Internally: call `mintEntityId()` from `@fwornle/km-core` for fresh ids, build the legacyId from raw.id (B's nanoid), construct descriptionSegments[0] using Phase 39's mergeDescriptionSegment, set layer = 'evidence', set metadata.subsystem = 'wave-analysis', stamp provenance with runId.

    2. In `wave1-project-agent.ts`, `wave2-component-agent.ts`, `wave3-detail-agent.ts`: locate the entity emit point (the place where the agent assembles its return value's entities[] array). Wrap each emit with `const canonical = toCanonicalEntity(raw, '<waveClass>', this.runId)` and return the canonical entities. For wave2's SubComponent path: pass `'SubComponent'` instead of `'Component'`.

       The runId is sourced from the wave-controller, which passes it through to each wave agent. If the agent doesn't currently receive runId, add a constructor parameter `runId: string` and update the wave-controller call site to pass it.

    3. In `kg-operators.ts:31-47`, extend the KGEntity interface with the four new optional fields (entityType, ontologyClass, metadata, legacyId). Add a deprecated-doc-comment on the `type` field noting "alias of entityType; preserved for back-compat through Phase 42; remove in Phase 43".

       Do NOT modify any other kg-operators logic in this task — the embedding operator (lines 148-152 + 340) keeps populating `result[index].embedding`; the new typed field on KGEntity just makes that assignment type-clean.

    4. Write the 6 tests in `src/agents/wave-controller-canonical-emit.test.ts`. Use unit-test-level shims for the wave agents (no full controller spin-up); construct fake `RawWaveEntity` inputs, call `toCanonicalEntity`, assert the output shape.

    Diagnostic output via process.stderr.write.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; node --test dist/agents/wave-controller-canonical-emit.test.js</automated>
  </verify>
  <done>canonical-mapper.ts exists; wave1/wave2/wave3 agents emit canonical-shape entities; KGEntity interface extended (back-compat preserved); 6 tests pass.</done>
  <acceptance_criteria>
    - Source assertion: `test -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts` exits 0.
    - Source assertion (each agent uses the mapper): `for f in wave1-project-agent.ts wave2-component-agent.ts wave3-detail-agent.ts; do grep -c 'toCanonicalEntity' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/$f || exit 1; done` exits 0.
    - Source assertion (KGEntity extended): `grep -E 'ontologyClass\?:|legacyId\?:' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/kg-operators.ts | wc -l | xargs -I{} test {} -ge 2` exits 0.
    - Test: 6 unit tests pass.
    - Compile: `npm run build` exits 0 (TypeScript compiles cleanly with the extended KGEntity).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-controller persistWaveResult flag-gated km-core path; delete mergeEntityGroup; rewire dedup callers</name>
  <files>/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller-canonical-emit.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts (lines 2107-2124 — persistWaveResult → persistEntities call site; also the bypass write block at 1351-1411 already touched by Plan 1)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts (line 342 mergeEntityGroup definition + ALL its callers within the file via `grep -n "mergeEntityGroup(" deduplication.ts`)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts (lines 3540-3625 storeEntityToGraph + lines 2107-2125 persistEntities — to understand what the legacy path does, so the km-core branch matches the contract)
    - /Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-05-PLAN.md (mergeEntities signature + behavior contract)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md (D-50a — mergeEntityGroup deletion mandate)
  </read_first>
  <behavior>
    - Test 7: With `KM_CORE_PERSISTENCE=km-core`, calling wave-controller's persistWaveResult on a fixture wave result calls `kmCoreAdapter.storeEntity` for each entity AND `kmCoreAdapter.storeRelationship` for each edge — does NOT call `persistenceAgent.persistEntities`.
    - Test 8: With `KM_CORE_PERSISTENCE=legacy`, the same call routes through `persistenceAgent.persistEntities` exactly as today (no behavior change).
    - Test 9: `deduplication.ts` no longer exports `mergeEntityGroup` (grep returns 0); call sites within the file have been rewired to `mergeEntities` from `@fwornle/km-core/maintenance` with name-to-EntityId resolution via `store.findByName`.
    - Test 10: When `store.findByName(name)` returns undefined for a dedup candidate, the dedup loop logs to stderr and continues with the next group (does NOT throw). The wave still completes successfully.
    - Test 11: When the km-core path encounters an error for a single entity (e.g., adapter.storeEntity throws), wave-controller logs to stderr, increments an error counter, and continues with the next entity (matches Phase 41 resolveEntities resilience pattern — fail-soft per entity).
  </behavior>
  <action>
    1. In `wave-controller.ts` around lines 2107-2124 (the persistWaveResult → persistenceAgent.persistEntities call site):

       Add a new private method `private async persistWithKmCore(entities: Entity[], runId: string): Promise<{ stored: number; errors: number }>`:
       - For each entity in entities[]: try `await this.kmCoreAdapter.storeEntity(entity, { team: this.team })` (the Plan 1 adapter). On error: increment errors counter, write a stderr line, continue.
       - For each relationship that the wave result carries (search the wave-result shape for the edges field; typically `waveResult.relationships[]` per persistence-agent.ts:2107-2125 pattern): try `await this.kmCoreAdapter.storeRelationship(fromName, toName, type, metadata)`. On error: same fail-soft.
       - Return `{ stored, errors }` for the wave-controller's summary log.

       Then wrap the existing `await persistenceAgent.persistEntities(...)` call in a flag-gated branch:
       - If `getPersistenceBackend() === 'km-core'` AND `this.kmCoreAdapter`: call `await this.persistWithKmCore(canonicalEntities, this.runId)` instead.
       - Else: call the existing `persistenceAgent.persistEntities` path unchanged.

       NO changes to the legacy path's logic. The persistence-agent module is NOT modified in this task (Plan 7 removes it during cleanup).

    2. In `deduplication.ts`:
       - Identify the function definition at line 342: `mergeEntityGroup(...)`.
       - Identify ALL call sites within the file: `grep -n "mergeEntityGroup(" deduplication.ts`.
       - For each call site, replace with a `mergeEntities` call from `@fwornle/km-core/maintenance`. The call site receives a group of B's name-keyed entities; map to EntityIds via `store.findByName(name)`. Pick the survivor by the existing heuristic (whichever the local mergeEntityGroup currently chooses — typically highest-significance or first-emitted; preserve the heuristic). Build the provenance stamp `{ provider: 'wave-controller', model: 'phase-42-dedup', runId: this.runId, timestamp: new Date().toISOString() }`.
       - When any name in the group fails to resolve via findByName: log to stderr `[deduplication] skipping merge — entity '<name>' not in km-core store`, skip that group's merge (DON'T fail the wave).
       - Delete the `mergeEntityGroup` function definition.
       - Remove any helper functions that mergeEntityGroup uses internally if those helpers become orphans (`grep -c <helper>` returns 0 after the deletion).

    3. In `persistence-agent.ts`: NO source changes in this task. Plan 7 deletes this file along with the 7-layer pipeline.

    4. Append the 5 new tests (Tests 7-11) to the existing `src/agents/wave-controller-canonical-emit.test.ts` file. Use mocks: mock the adapter's storeEntity/storeRelationship as `vi.fn()` or `jest.fn()`. Verify call counts and call arguments.

    Diagnostic output via process.stderr.write.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; node --test dist/agents/wave-controller-canonical-emit.test.js &amp;&amp; ! grep -qE 'mergeEntityGroup\s*[(=:]' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts</automated>
  </verify>
  <done>Wave-controller's persistWaveResult is flag-gated; mergeEntityGroup deleted from deduplication.ts; 5 new tests pass (total: 11 in wave-controller-canonical-emit.test.ts).</done>
  <acceptance_criteria>
    - Source assertion: `grep -cE 'function mergeEntityGroup|mergeEntityGroup\s*=' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts` returns 0 (function deleted).
    - Source assertion: `grep -c "from '@fwornle/km-core/maintenance'" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/deduplication.ts` returns ≥ 1.
    - Source assertion: `grep -c 'persistWithKmCore' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` returns ≥ 2 (defined + called).
    - Source assertion (legacy path preserved): `grep -c 'persistenceAgent.persistEntities' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts` returns ≥ 1 (the else branch still calls it; Plan 7 removes both branches).
    - Test: 11 unit tests pass in the wave-controller-canonical-emit suite.
    - Compile: `npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Submodule build + Docker rebuild + km-core path smoke test with flag on</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md ("Rebuilding After Code Changes" section)
  </read_first>
  <action>
    1. Build: `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` — exit 0.
    2. Container rebuild: `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` — exit 0.
    3. Restart: `docker-compose up -d coding-services`.
    4. Confirm new code is in the container: `docker exec coding-services grep -c 'persistWithKmCore' /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js` returns ≥ 1.
    5. Confirm mergeEntityGroup deletion: `docker exec coding-services grep -c 'mergeEntityGroup' /coding/integrations/mcp-server-semantic-analysis/dist/agents/deduplication.js` returns 0.
    6. Confirm km-core mergeEntities is reachable inside the container: `docker exec coding-services node -e "import('@fwornle/km-core/maintenance').then(m => process.exit(m.mergeEntities ? 0 : 1))"` exits 0.

    NO `ukb full` smoke run in this task — that gate lives in Plan 7. This task only confirms build wiring.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; cd /Users/Q284340/Agentic/coding/docker &amp;&amp; docker-compose build coding-services &amp;&amp; docker-compose up -d coding-services &amp;&amp; sleep 8 &amp;&amp; docker exec coding-services grep -c 'persistWithKmCore' /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>Build + Docker rebuild green; new symbol in dist/; mergeEntityGroup removed from dist/; km-core/maintenance reachable.</done>
  <acceptance_criteria>
    - CLI: `npm run build` exits 0.
    - CLI: `docker-compose build coding-services` exits 0.
    - CLI: `docker exec coding-services grep -c 'persistWithKmCore' /coding/integrations/mcp-server-semantic-analysis/dist/agents/wave-controller.js` returns ≥ 1.
    - CLI: `docker exec coding-services grep -c 'mergeEntityGroup' /coding/integrations/mcp-server-semantic-analysis/dist/agents/deduplication.js` returns 0.
    - CLI: `docker exec coding-services node -e "import('@fwornle/km-core/maintenance').then(m => process.exit(m.mergeEntities ? 0 : 1))"` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wave-controller then km-core adapter (when flag on) | All wave-emitted entities cross this boundary; a malformed canonical mapper output would land in the canonical store |
| deduplication then km-core mergeEntities | The dedup loop now mutates the canonical store via the shared primitive; a name-resolution failure could silently drop merges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06-01 | Tampering | canonical-mapper output | mitigate | Type system enforces Entity shape on every emit; Plan 4's typed embedding field accepts the new value cleanly |
| T-42-06-02 | Repudiation | mergeEntityGroup deletion | mitigate | git rm preserves history; Plan 7's e2e gate validates no orphaned references via grep before final cleanup |
| T-42-06-03 | Denial of Service | per-entity persistence error | mitigate | Fail-soft loop: each entity error logged, counter incremented, loop continues; matches Phase 41 resolveEntities precedent (Test 11) |
| T-42-06-04 | Information Disclosure | runId leak | accept | runId is a UUIDv4 timestamp; not security-sensitive |
</threat_model>

<verification>
- canonical-mapper.ts emits Entity shape with all required fields (Tests 1-3).
- KGEntity extended back-compat (Test 4).
- 3 wave agents wire through the mapper (Test 5).
- mergeEntityGroup deleted; km-core mergeEntities adopted (Test 9-10).
- Persistence path is flag-gated (Tests 7-8).
- Fail-soft on errors (Test 11).
- Build green; container reflects changes.
</verification>

<success_criteria>
1. Wave agents emit canonical-shape entities through the new `toCanonicalEntity` helper.
2. wave-controller's persistWaveResult routes through `kmCoreAdapter` when the flag is on (legacy path preserved when off).
3. `deduplication.ts` no longer defines or references `mergeEntityGroup` — all call sites forward to `mergeEntities` from `@fwornle/km-core/maintenance`.
4. KGEntity interface extended with optional canonical fields; existing kg-operators tests still pass.
5. Build + Docker rebuild green; symbols verified inside the running container.
</success_criteria>

<output>
Create `.planning/phases/42-offline-ukb-migration-b/42-06-SUMMARY.md` when done, recording:
- canonical-mapper.ts LoC + helper count
- KGEntity field additions (must be back-compat additive — no removal of `type`)
- mergeEntityGroup deletion confirmed (grep count = 0 in both src and dist)
- Wave-controller persistWaveResult branch structure
- Test count delta
- Confirmation that this plan touches NO files Plan 1 already touched in a way that conflicts
</output>
