---
phase: 42-offline-ukb-migration-b
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/km-core/src/types/entity.ts
  - /Users/Q284340/Agentic/km-core/src/maintenance/syncQdrantFromStore.ts
  - /Users/Q284340/Agentic/km-core/src/maintenance/index.ts
  - /Users/Q284340/Agentic/km-core/src/embeddings/FastembedEmbeddingClient.ts
  - /Users/Q284340/Agentic/km-core/src/embeddings/index.ts
  - /Users/Q284340/Agentic/km-core/src/index.ts
  - /Users/Q284340/Agentic/km-core/package.json
  - /Users/Q284340/Agentic/km-core/tests/unit/types/entity-embedding.test.ts
  - /Users/Q284340/Agentic/km-core/tests/unit/maintenance/syncQdrantFromStore.test.ts
  - /Users/Q284340/Agentic/km-core/tests/unit/embeddings/FastembedEmbeddingClient.test.ts
autonomous: true
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "km-core's canonical Entity type (src/types/entity.ts) gains an optional top-level field `embedding?: number[]` — Phase 39 schema extension per D-52. Optional so A-side (which does not compute embeddings) remains a no-op."
    - "A new maintenance op `syncQdrantFromStore(store, opts)` lives under @fwornle/km-core/maintenance alongside Phase 41's resolveEntities + mergeEntities. Reads all entities with embedding.length > 0, upserts them into a caller-supplied Qdrant client. Idempotent."
    - "A new module @fwornle/km-core/embeddings exports FastembedEmbeddingClient — a default EmbeddingClient (Phase 40 interface) wrapping fastembed all-MiniLM-L6-v2 (384-dim) per D-52c. km-core stays Qdrant-agnostic at the type level (mirrors Phase 40 LLMClient pattern)."
    - "package.json exports map gains ./embeddings entry; existing ./maintenance entry stays unchanged (syncQdrantFromStore re-exports under the same sub-path)."
    - "All Phase 37-41 existing tests still pass — zero regression in km-core."
    - "The embedding field, when present, round-trips through putEntity then getEntity without modification."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/types/entity.ts"
      provides: "Entity type with embedding?: number[] field added (D-52 Phase 39 schema extension)"
      contains: "embedding?: number[]"
    - path: "/Users/Q284340/Agentic/km-core/src/maintenance/syncQdrantFromStore.ts"
      provides: "Maintenance op that reads embeddings from km-core store and upserts to Qdrant via caller-supplied client (D-52a)"
      exports: ["syncQdrantFromStore", "SyncQdrantOptions", "SyncQdrantResult", "QdrantClient"]
    - path: "/Users/Q284340/Agentic/km-core/src/embeddings/FastembedEmbeddingClient.ts"
      provides: "Default EmbeddingClient implementation wrapping fastembed all-MiniLM-L6-v2 (D-52c)"
      exports: ["FastembedEmbeddingClient"]
  key_links:
    - from: "syncQdrantFromStore"
      to: "store.iterate() + caller-supplied QdrantClient"
      via: "library function pattern; caller owns the Qdrant client (mirrors Phase 40 LLMClient pattern; km-core stays Qdrant-agnostic at the type level)"
      pattern: "qdrantClient\\.upsert"
    - from: "FastembedEmbeddingClient"
      to: "Phase 40 EmbeddingClient interface"
      via: "implements existing interface from '@fwornle/km-core/dedup'"
      pattern: "implements EmbeddingClient"
---

<objective>
Land the three km-core surfaces that Plan 5 (wave-controller migration) and Plan 7 (cleanup + e2e) depend on. This is a **CROSS-REPO PLAN** — all source changes land in `/Users/Q284340/Agentic/km-core/`, not in `coding/`. Follow the Phase 41 cross-repo commit pattern: feat/test commits land in km-core via `git -C /Users/Q284340/Agentic/km-core commit ...`; the SUMMARY.md commit lands in the coding repo.

Three additive deliverables:

  1. **embedding?: number[] on Entity** (D-52). Optional top-level field added to the canonical Entity type. Pure additive — A's existing types compile unchanged (Entity already has many optional fields; this is one more).
  2. **syncQdrantFromStore(store, opts)** (D-52a). New maintenance op under @fwornle/km-core/maintenance. Reads all entities with non-empty embeddings, upserts them into a caller-supplied Qdrant client. Idempotent (overwrites by id). Mirrors Phase 40's caller-injected-client pattern: km-core defines the minimal QdrantClient interface, caller supplies the concrete instance.
  3. **FastembedEmbeddingClient** (D-52c). Default EmbeddingClient (Phase 40 interface) implementation wrapping fastembed all-MiniLM-L6-v2. New sub-path @fwornle/km-core/embeddings (mirrors Phase 38 ./ontology + Phase 40 ./dedup precedents). B's existing fastembed wiring becomes a thin pass-through to this client in Plan 5.

These three changes are mutually orthogonal AND independent of any coding-repo file, which is why this plan sits in Wave 1 alongside Plans 1/2/3 (no file overlap, no functional dependency). Plan 5 consumes (1) and (3); Plan 7 consumes (2) at e2e gate time.

Purpose: SC#2 (Phase 10 embedding fix — requires the typed embedding field for clean codegen in Plan 5); SC#1 (preserve existing semantic-search surface — Qdrant rebuilt via syncQdrantFromStore in Plan 7).

Output: 3 new TS files + 2 modified TS files + 1 modified package.json + 3 new test files in km-core. ZERO coding-repo files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md
@/Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-05-PLAN.md

<interfaces>
<!-- km-core surfaces this plan extends. Read from the listed files. -->

From /Users/Q284340/Agentic/km-core/src/types/entity.ts:113-148 (current Entity — NO embedding field yet):

  export interface Entity {
    id: EntityId;
    name: string;
    entityType: string;
    ontologyClass?: string;
    layer: 'evidence' | 'pattern';
    description: string;
    createdAt: string;
    updatedAt: string;
    metadata: Record<string, unknown>;
    validFrom?: string;
    validUntil?: string;
    supersedes?: EntityId;
    legacyId?: { system: 'A' | 'B' | 'C'; id: string };
    // this plan adds: embedding?: number[];
  }

From /Users/Q284340/Agentic/km-core/src/maintenance/index.ts (existing sub-barrel — Phase 41):

  export { resolveEntities } from './resolveEntities.js';
  export { mergeEntities } from './mergeEntities.js';
  // this plan appends: export { syncQdrantFromStore } from './syncQdrantFromStore.js';

From /Users/Q284340/Agentic/km-core/src/dedup/types.ts (Phase 40 EmbeddingClient — the interface FastembedEmbeddingClient implements):

  export interface EmbeddingClient {
    embed(texts: string[]): Promise<number[][]>;
  }

From /Users/Q284340/Agentic/km-core/package.json (current exports map):

  "exports": {
    ".": { ... },
    "./ontology": { ... },
    "./pipeline": { ... },
    "./dedup": { ... },
    "./maintenance": { ... },
    "./store": { ... }
    // this plan adds: "./embeddings": { ... }
  }

From /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/utils/embedding_generator.py (REFERENCED but not modified here — RESEARCH §6 Open Question 4 flags it as a Python dep concern; FastembedEmbeddingClient sidesteps this by using fastembed's TS bindings, which are pure-Node).

From /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/embedding/ (B's existing embedding pipeline — referenced in RESEARCH §1 closing — B uses fastembed via JS API in `src/embedding/`). Confirms fastembed has a pure-JS path that does NOT require Python deps. FastembedEmbeddingClient uses the same JS API.

Qdrant client interface (defined inside syncQdrantFromStore.ts as a minimal structural type — km-core stays Qdrant-agnostic):

  export interface QdrantClient {
    upsert(collection: string, points: Array<{ id: string | number; vector: number[]; payload?: Record<string, unknown> }>): Promise<void>;
  }

This minimal shape matches both `@qdrant/js-client-rest` and `@qdrant/qdrant-js` 1.x (verified during Phase 40 LLMClient research — `qdrant-client.upsert(collection, { points })` is the common shape; the wrapper inside syncQdrantFromStore reshapes to the client's actual signature). Caller passes the real client; km-core's type is structural.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add embedding?: number[] to km-core Entity type + round-trip test</name>
  <files>/Users/Q284340/Agentic/km-core/src/types/entity.ts, /Users/Q284340/Agentic/km-core/tests/unit/types/entity-embedding.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (full file — to land the field in the same JSDoc-richness style as legacyId/validFrom)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (lines 200-340 — confirm putEntity's spread+merge writes arbitrary entity fields verbatim; the embedding field passes through with no special handling needed)
    - /Users/Q284340/Agentic/coding/.planning/phases/39-entity-data-model/39-CONTEXT.md (Phase 39 schema-lock decisions — embedding is a strict extension, not a re-debate)
  </read_first>
  <behavior>
    - Test 1: TypeScript compilation accepts `{ ..., embedding: [0.1, 0.2, 0.3] }` as a valid Entity literal.
    - Test 2: embedding is optional — entities without it (all Phase 37-41 fixtures, A-side entities) compile unchanged.
    - Test 3: Round-trip via putEntity then getEntity preserves embedding array element-for-element (length and values match).
    - Test 4: mergeAttributes (existing surface at GraphKMStore.ts:854) accepts an `{ embedding: [...] }` partial and updates the node attribute (existing pass-through behavior — no code change to mergeAttributes; this test just verifies the typed surface accepts the field).
  </behavior>
  <action>
    In `src/types/entity.ts`, add a new optional field to the Entity interface after `legacyId`. The new field declaration is `embedding?: number[];` and it MUST be placed AFTER `legacyId?: ...` and BEFORE the closing brace of the Entity interface.

    Document the field with JSDoc in the same prose style as the surrounding Phase 39 fields. Cover four points in the doc comment:
      - Phase 42 D-52 origin
      - Optional so A-side stays a no-op
      - Treated as an arbitrary entity attribute by Graphology — no special handling in GraphKMStore putEntity / getEntity / mergeAttributes
      - Convention: 384-dim fastembed all-MiniLM-L6-v2 (D-52c); other systems may supply different dimensions; km-core does not enforce dimension at the type level — the consumer (Qdrant sync, semantic search) is responsible

    NO changes to any other type. NO changes to Relation, SerializedGraph, or EntityProvenance.

    Add the 4 unit tests in `tests/unit/types/entity-embedding.test.ts`. Use the existing Phase 37 test patterns (describe / it from vitest) and a fresh in-memory GraphKMStore instance per test for the round-trip + mergeAttributes tests.

    No other source files modified in this task. Plan 5 consumes the new typed field; this task just lands the schema.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core &amp;&amp; npx vitest run tests/unit/types/entity-embedding.test.ts</automated>
  </verify>
  <done>Entity has embedding?: number[] field with JSDoc; 4 unit tests pass; all existing km-core tests still pass.</done>
  <acceptance_criteria>
    - Source assertion: `grep -c 'embedding?: number\[\]' /Users/Q284340/Agentic/km-core/src/types/entity.ts` returns ≥ 1.
    - Source assertion (placement — after legacyId): `awk '/legacyId\?/{p=1} p && /embedding\?: number\[\]/{print NR; exit}' /Users/Q284340/Agentic/km-core/src/types/entity.ts` returns a line number.
    - Test: `npx vitest run tests/unit/types/entity-embedding.test.ts` exits 0 with 4 tests passed.
    - Regression: `cd /Users/Q284340/Agentic/km-core && npm test` (full suite) exits 0 — zero Phase 37/38/39/40/41 test regressions.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: syncQdrantFromStore maintenance op + sub-barrel export</name>
  <files>/Users/Q284340/Agentic/km-core/src/maintenance/syncQdrantFromStore.ts, /Users/Q284340/Agentic/km-core/src/maintenance/index.ts, /Users/Q284340/Agentic/km-core/tests/unit/maintenance/syncQdrantFromStore.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/maintenance/mergeEntities.ts (Phase 41 precedent — same export shape, same options-object pattern, same process.stderr.write for diagnostics)
    - /Users/Q284340/Agentic/km-core/src/maintenance/resolveEntities.ts (Phase 41 — pattern for iterating store entities + processing in batches; reuse the same chunking idiom)
    - /Users/Q284340/Agentic/km-core/src/maintenance/index.ts (the existing barrel — add a new export line here)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md (D-52a — full signature spec)
  </read_first>
  <behavior>
    - Test 1: syncQdrantFromStore(store, { qdrantClient, collection: 'coding' }) calls qdrantClient.upsert with all entities that have embedding.length > 0.
    - Test 2: Entities WITHOUT embedding (or with empty arrays) are skipped — the upsert payload does not include them.
    - Test 3: Idempotency — calling twice with the same store state produces two upsert calls with identical point payloads (overwrite semantics; no special "already uploaded" tracking).
    - Test 4: Returns SyncQdrantResult with { syncedCount: number; skippedCount: number; errors: Array<{ entityId: string; message: string }> } — syncedCount = entities with embedding, skippedCount = entities without.
    - Test 5: When qdrantClient.upsert throws, syncQdrantFromStore captures the error in errors[] per failing batch and continues with the next batch (does NOT abort the whole sync — matches Phase 41 resolveEntities resilience).
    - Test 6: opts.batchSize (default 100) controls how many points per upsert call. Setting batchSize: 10 against 25 embedded entities produces 3 upsert calls (10 + 10 + 5).
    - Test 7: Each point's id is the entity's legacyId.id if present, else the entity's id (matches Phase 41 mapper convention — Qdrant point IDs stable across re-syncs).
    - Test 8: Each point's payload includes entityType, ontologyClass, name for downstream search-result rendering (verified by inspecting the upsert call args).
  </behavior>
  <action>
    Create `src/maintenance/syncQdrantFromStore.ts`. Structure mirrors mergeEntities.ts (Phase 41 precedent):

    1. Define the minimal QdrantClient structural interface at the top of the file (per Phase 40 LLMClient pattern — km-core stays Qdrant-agnostic). The interface has one method: `upsert(collection: string, points: Array<{ id: string | number; vector: number[]; payload?: Record<string, unknown> }>): Promise<void>`.

    2. Define SyncQdrantOptions: required `qdrantClient: QdrantClient`, required `collection: string` (the Qdrant collection name), optional `batchSize?: number` (default 100), optional `log?: (event) => void` for trace events.

    3. Define SyncQdrantResult: `syncedCount: number`, `skippedCount: number`, `errors: Array<{ entityId: string; message: string }>`.

    4. Implement `syncQdrantFromStore(store: GraphKMStore, opts: SyncQdrantOptions): Promise<SyncQdrantResult>`:
       - Iterate via `store.iterate()` (async iterator; matches Phase 41 reproject pattern).
       - For each entity, if `entity.embedding && entity.embedding.length > 0`: push `{ id: entity.legacyId?.id ?? entity.id, vector: entity.embedding, payload: { entityType: entity.entityType, ontologyClass: entity.ontologyClass, name: entity.name } }` to a current-batch array. Else: increment skippedCount.
       - When the batch reaches batchSize, call `await opts.qdrantClient.upsert(opts.collection, batch)` in a try/catch. On success: `syncedCount += batch.length`. On error: push every entity id in the batch to errors[] with the caught error message. Continue with next batch.
       - After iteration ends, flush the final partial batch.
       - Emit log events for `batch` (after each upsert call) and `error` (on each failure). Diagnostics via `process.stderr.write` for any unhandled cases.

    5. Append to `src/maintenance/index.ts`: `export { syncQdrantFromStore } from './syncQdrantFromStore.js';` and `export type { SyncQdrantOptions, SyncQdrantResult, QdrantClient } from './syncQdrantFromStore.js';`.

    6. Write the 8 tests in `tests/unit/maintenance/syncQdrantFromStore.test.ts`. Use vitest's `vi.fn()` for the mock `qdrantClient.upsert`. Construct a small in-memory GraphKMStore per test with 2-3 fixture entities (some with embedding, some without). Assert `expect(qdrantClient.upsert).toHaveBeenCalledWith(...)` for the batch shape contract; assert `result.syncedCount` / `result.skippedCount` / `result.errors` for the result contract.

    Diagnostic output uses process.stderr.write (no-console-log).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core &amp;&amp; npx vitest run tests/unit/maintenance/syncQdrantFromStore.test.ts</automated>
  </verify>
  <done>syncQdrantFromStore.ts exists; 8 unit tests pass; maintenance/index.ts exports it; full km-core test suite green.</done>
  <acceptance_criteria>
    - Source assertion: `test -f /Users/Q284340/Agentic/km-core/src/maintenance/syncQdrantFromStore.ts` exits 0.
    - Source assertion: `grep -c 'syncQdrantFromStore' /Users/Q284340/Agentic/km-core/src/maintenance/index.ts` returns ≥ 1 (sub-barrel exports it).
    - Source assertion: `grep -c 'export interface QdrantClient' /Users/Q284340/Agentic/km-core/src/maintenance/syncQdrantFromStore.ts` returns ≥ 1 (the structural type is defined locally — km-core is Qdrant-agnostic).
    - Test: `npx vitest run tests/unit/maintenance/syncQdrantFromStore.test.ts` exits 0 with 8 tests passed.
    - Regression: `cd /Users/Q284340/Agentic/km-core && npm test` (full suite) exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FastembedEmbeddingClient default + new ./embeddings sub-path + package.json wiring</name>
  <files>/Users/Q284340/Agentic/km-core/src/embeddings/FastembedEmbeddingClient.ts, /Users/Q284340/Agentic/km-core/src/embeddings/index.ts, /Users/Q284340/Agentic/km-core/src/index.ts, /Users/Q284340/Agentic/km-core/package.json, /Users/Q284340/Agentic/km-core/tests/unit/embeddings/FastembedEmbeddingClient.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/dedup/types.ts (Phase 40 — the EmbeddingClient interface FastembedEmbeddingClient implements; line range is small, read full)
    - /Users/Q284340/Agentic/km-core/package.json (current exports map structure — add new sub-path entry in the same format as ./ontology, ./pipeline, ./dedup, ./maintenance)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/embedding/ (B's existing fastembed integration — confirms the fastembed JS package name + init pattern; FastembedEmbeddingClient is a thin wrapper around the same package)
    - /Users/Q284340/Agentic/km-core/src/index.ts (root barrel — needs a Phase 42 block appended)
  </read_first>
  <behavior>
    - Test 1: new FastembedEmbeddingClient() defaults to the all-MiniLM-L6-v2 384-dim model (per D-52c memory note "fastembed with all-MiniLM-L6-v2 (384-dim) pinned").
    - Test 2: await client.embed(['hello world', 'foo bar']) returns number[][] with 2 inner arrays, each of length 384.
    - Test 3: Multiple calls to client.embed(...) reuse the same loaded model (lazy-init or singleton — measurable by mocking the FastembedEmbedding init function and asserting it's called once).
    - Test 4: FastembedEmbeddingClient implements EmbeddingClient (TypeScript structural assertion via const x: EmbeddingClient = new FastembedEmbeddingClient() in a .test.ts compile-only assertion).
    - Test 5: Importing via the root barrel `import { FastembedEmbeddingClient } from '@fwornle/km-core'` resolves cleanly (root barrel exports it).
    - Test 6: Importing via the sub-path `import { FastembedEmbeddingClient } from '@fwornle/km-core/embeddings'` resolves cleanly (package.json exports map has ./embeddings entry).
  </behavior>
  <action>
    1. Create `src/embeddings/FastembedEmbeddingClient.ts`:
       - Import the EmbeddingClient type from `'../dedup/types.js'` (Phase 40 interface).
       - Import fastembed JS package. **First** read B's actual `src/embedding/` files end-to-end and lift the EXACT package name, import shape, model enum, and init pattern from there — that is the live source of truth. Phase 28 memory notes (cacheDir absolute, Float32Array → Array.from, `_initPromise` reset on failure) are HINTS only; if B's current code diverges from them, follow B's code, not the notes. RESEARCH §1 confirms B uses fastembed via the JS API in `src/embedding/`.
       - Class FastembedEmbeddingClient implements EmbeddingClient:
         - Constructor accepts `opts?: { model?: string; cacheDir?: string }`. Defaults: model = EmbeddingModel.AllMiniLML6V2, cacheDir = absolute-resolved path (matches Phase 28 memory note "Set absolute cacheDir in FlagEmbedding.init() to prevent CWD-relative model loading").
         - Lazy-initialize the FlagEmbedding instance on first embed call. Cache in a private field.
         - `async embed(texts: string[]): Promise<number[][]>` returns texts.map(t => Array.from(await flagEmbedding.queryEmbed(t))) — matches Phase 28 memory note "queryEmbed returns Float32Array converted via Array.from()".
         - `async close()` is an optional cleanup hook (no-op if fastembed JS package doesn't expose one).
         - Reset _initPromise on failure per Phase 28 memory note "Reset _initPromise on failure for retry support".

    2. Create `src/embeddings/index.ts` exporting FastembedEmbeddingClient from './FastembedEmbeddingClient.js'.

    3. Append a Phase 42 INT-02 export block at the bottom of `src/index.ts` root barrel that re-exports FastembedEmbeddingClient + the QdrantClient/SyncQdrantOptions/SyncQdrantResult types + syncQdrantFromStore. (Dual-availability per the Phase 38/40 precedent: sub-path + root-barrel both resolve.)

    4. Update `package.json` exports map. Add a new entry between ./dedup and ./maintenance (alphabetical order, mirrors Phase 38/40 precedent). The entry maps "./embeddings" to `{ import: "./dist/embeddings/index.js", types: "./dist/embeddings/index.d.ts" }`.

    5. Write the 6 tests in `tests/unit/embeddings/FastembedEmbeddingClient.test.ts`. Tests 5 + 6 use Phase 38's external-tmpdir smoke-compile pattern: create a tmpdir, npm install km-core + typescript, write a test file importing via root and sub-path, run npx tsc --noEmit. Tests 1-4 run in-suite against the live fastembed package.

    NOTE: Tests 1-3 may be slow (fastembed downloads ~80MB on first init). Mark them it.concurrent and use a per-suite shared client init to amortize the cost.

    Diagnostic output uses process.stderr.write. Cross-check that the fastembed JS package is already a dependency in km-core's package.json — if not, add it as a regular dependency (fastembed@^1.x.x matching B's pinned version per RESEARCH §6 Open Question 4).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core &amp;&amp; npx vitest run tests/unit/embeddings/FastembedEmbeddingClient.test.ts --testTimeout 120000</automated>
  </verify>
  <done>FastembedEmbeddingClient.ts exists; sub-barrel + root barrel exports it; package.json has ./embeddings entry; 6 tests pass (some slow due to fastembed init); full km-core test suite green.</done>
  <acceptance_criteria>
    - Source assertion: `test -f /Users/Q284340/Agentic/km-core/src/embeddings/FastembedEmbeddingClient.ts && test -f /Users/Q284340/Agentic/km-core/src/embeddings/index.ts` exits 0.
    - Source assertion: `grep -c 'implements EmbeddingClient' /Users/Q284340/Agentic/km-core/src/embeddings/FastembedEmbeddingClient.ts` returns ≥ 1.
    - Source assertion: `grep -c '\"\./embeddings\"' /Users/Q284340/Agentic/km-core/package.json` returns ≥ 1 (sub-path wired).
    - Source assertion: `grep -c 'FastembedEmbeddingClient' /Users/Q284340/Agentic/km-core/src/index.ts` returns ≥ 1 (root barrel re-export).
    - Test: 6 unit tests pass (Tests 1-3 may take 30-60s due to model download; allow up to 120s testTimeout).
    - Build: `cd /Users/Q284340/Agentic/km-core && npm run build && npm test` exits 0 (full test suite green).
    - Smoke: tmpdir external compile (Test 6 internal check) passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Cross-repo commit to km-core (mirror Phase 41 pattern)</name>
  <files>(no source changes — git commit only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-01-SUMMARY.md (line ~44 — "Multi-repo execution: feat/test commits land in sibling km-core repo via `git -C /Users/Q284340/Agentic/km-core`; SUMMARY.md commit lands in coding repo")
  </read_first>
  <action>
    Use `git -C /Users/Q284340/Agentic/km-core` for every commit in this task. The coding-repo git invocations are NOT used for km-core source changes.

    Commit sequence (3 commits, one per task — preserves blast radius separation if any rollback is needed):

    1. Task 1 commit: `git -C /Users/Q284340/Agentic/km-core add src/types/entity.ts tests/unit/types/entity-embedding.test.ts && git -C /Users/Q284340/Agentic/km-core commit -m "feat(42-04): add optional embedding field to canonical Entity (D-52)"`.

    2. Task 2 commit: `git -C /Users/Q284340/Agentic/km-core add src/maintenance/syncQdrantFromStore.ts src/maintenance/index.ts tests/unit/maintenance/syncQdrantFromStore.test.ts && git -C /Users/Q284340/Agentic/km-core commit -m "feat(42-04): syncQdrantFromStore maintenance op (D-52a)"`.

    3. Task 3 commit: `git -C /Users/Q284340/Agentic/km-core add src/embeddings/ src/index.ts package.json tests/unit/embeddings/ && git -C /Users/Q284340/Agentic/km-core commit -m "feat(42-04): FastembedEmbeddingClient default + ./embeddings sub-path (D-52c)"`.

    Verify the commits landed via `git -C /Users/Q284340/Agentic/km-core log --oneline -3`. All three SHAs must be retrievable.

    DO NOT push to remote unless the user explicitly requests it (per the planner's git safety protocol). Local commits only.

    SUMMARY.md is written in the coding repo (next step of the standard execute-plan flow) and committed via the coding repo's git context.
  </action>
  <verify>
    <automated>git -C /Users/Q284340/Agentic/km-core log --oneline -3 | grep -c '42-04' | xargs -I{} test {} -eq 3</automated>
  </verify>
  <done>3 commits in km-core repo, each tagged with 42-04 in the message; SHAs verifiable via git log.</done>
  <acceptance_criteria>
    - CLI: `git -C /Users/Q284340/Agentic/km-core log --oneline -3 | grep -c '42-04'` returns 3.
    - CLI: `git -C /Users/Q284340/Agentic/km-core status --short` returns no output (working tree clean — no leftover unstaged changes from this plan's work).
    - CLI: `git -C /Users/Q284340/Agentic/km-core diff HEAD~3 HEAD --stat | grep -c 'embedding\|syncQdrant\|Fastembed'` returns ≥ 3 (the file-modification stat reflects the 3 logical changes).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller then syncQdrantFromStore then caller-supplied Qdrant client | km-core defines the structural QdrantClient type; a malicious caller could supply an upsert function that captures payloads — but the caller owns the client, so this is symmetric |
| FastembedEmbeddingClient then fastembed package | New dependency download (~80MB model); first-init is a network-bound operation |
| package.json supply chain | Adding fastembed (if not already pinned) crosses the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04-01 | Tampering | embedding field | mitigate | Type is number[] — no shape mutation possible; Phase 39 schema lock preserved (this is a pure extension) |
| T-42-04-02 | Information Disclosure | syncQdrantFromStore payload | accept | Caller controls Qdrant client; km-core only forwards entity data the caller already has access to |
| T-42-04-03 | Tampering | fastembed npm package | accept | fastembed is already a dependency of B (per RESEARCH §1; B's src/embedding/ uses it); not a new supply-chain entry — km-core just adopts the same pinned version |
| T-42-04-SC | Tampering | npm package additions | mitigate | Fastembed already in B's package.json; ASSUMED — verify via npmjs.com/package/fastembed package-legitimacy check before the npm install lands (RESEARCH did not include a Package Legitimacy Audit; if research is re-run, surface this) |
</threat_model>

<verification>
- Entity has embedding field; round-trip test passes.
- syncQdrantFromStore + tests in maintenance/.
- FastembedEmbeddingClient + ./embeddings sub-path + tests.
- Full km-core test suite green — zero Phase 37-41 regression.
- 3 commits in km-core with 42-04 prefix.
</verification>

<success_criteria>
1. km-core's Entity type has embedding?: number[] (D-52).
2. @fwornle/km-core/maintenance exports syncQdrantFromStore (D-52a).
3. @fwornle/km-core/embeddings sub-path exports FastembedEmbeddingClient (D-52c).
4. Full km-core test suite is green (zero regression).
5. 3 commits landed in km-core repo, all visible via `git -C /Users/Q284340/Agentic/km-core log`.
</success_criteria>

<output>
Create `.planning/phases/42-offline-ukb-migration-b/42-04-SUMMARY.md` (commit lands in coding repo, NOT km-core), recording:
- 3 km-core commit SHAs (verified via git -C /Users/Q284340/Agentic/km-core log)
- Total km-core test count delta (existing + 18 new = total)
- fastembed dependency status (already pinned vs newly added)
- Sanity: `npm test` exit code in km-core (must be 0)
</output>
