---
phase: 42-offline-ukb-migration-b
plan: 05
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-04
files_modified:
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/migration/migrate-leveldb-to-kmcore.test.ts
autonomous: true
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "A one-shot Node.js script at `integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` walks the existing LevelDB at `.data/knowledge-graph/` and rewrites every entity to the canonical km-core Entity shape per the D-54 mapping table in 42-RESEARCH.md §4."
    - "The script is IDEMPOTENT (D-54 hard requirement): running it twice in a row produces no further changes on the second run (verified by an integration test asserting `entitiesMigrated === N` on first run, `entitiesMigrated === 0` on second run with same input)."
    - "Migrated entities satisfy a property-based invariant: every entity has top-level `legacyId.system === 'B'`, a valid UUIDv7 `id`, `layer === 'evidence'`, `validFrom` populated, `validUntil === null` (in-force at migration), `metadata.subsystem === 'wave-analysis'`, `metadata.provenance.createdBy` stamped with the migration runId."
    - "Specialized entityTypes (Config, Port, Container, Process, File, Service, etc. — the 60 entities in RESEARCH §4 §entityType distribution that are NOT in the upper-ontology hierarchy) get `ontologyClass: <preserved entityType>` AND a fallback flag `metadata.ontologyClassUnregistered: true` when the class is not in the registry (handles RESEARCH §6 Risk 3 — ad-hoc classes that don't exist in coding-ontology.json)."
    - "Embedding field: if the source LevelDB entity has a non-empty embedding array, copy it verbatim to the migrated entity's `embedding` field. If absent (the vast majority per RESEARCH §4 — 0/727 in production data), leave undefined. Plan 7's full `ukb full` run fills these going forward."
    - "The script is fail-loud: any entity that cannot be mapped (e.g., source is malformed JSON in LevelDB) is written to a `migration-errors.log` file under `.data/`; the script exits non-zero only if `errors.length / total > 0.05` (5% error budget). Smaller errors are recorded but don't block migration."
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs"
      provides: "One-shot CLI; flags: --dry-run, --batch-size=N, --resume (skip already-migrated), --source=<leveldb path>, --target=<km-core dataDir>"
      contains: "legacyId"
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/migration/migrate-leveldb-to-kmcore.test.ts"
      provides: "Property-based integration test: seeds a fixture LevelDB with 10 known-shape entities (mix of hierarchical + specialized), runs migration, asserts canonical-shape invariants on output, asserts idempotency on second run"
  key_links:
    - from: "migrate-leveldb-to-kmcore.mjs"
      to: "@fwornle/km-core (root barrel) — Entity, GraphKMStore, mintEntityId"
      via: "import statement"
      pattern: "from '@fwornle/km-core'"
    - from: "migration script"
      to: "RESEARCH §4 schema mapping table"
      via: "every mapping rule in the table is implemented as a named helper function in the script"
      pattern: "mapToCanonical|stampProvenance|deriveOntologyClass"
---

<objective>
Land the one-shot D-54 LevelDB migration script. Per D-54b (continuity over cleanliness), this is in-place: the existing `.data/knowledge-graph/` LevelDB is rewritten entity-by-entity to canonical km-core Entity shape, preserving manual classification work and avoiding the multi-hour `ukb full` rebuild path.

The script depends on Plan 1's adapter being landed (the new km-core surface must be reachable from B's container). Plan 5 runs the script against the actual production LevelDB once — the run itself is operator-driven (not part of automated test execution), but the migration LOGIC and the property-based test fixture run in CI.

Per RESEARCH §4, the source data is fully characterized: 727 entities across 14 distinct entityType values, with the 4-level hierarchy (Project/Component/SubComponent/Detail) accounting for 92% and 60 entities using B-specific specialized classes. The mapping table in RESEARCH §4 enumerates every field translation; this plan IMPLEMENTS that table.

Risk per RESEARCH §6 Risk 2: 0/727 entities have embeddings, so post-migration the Qdrant index is empty (we don't populate it from a migration that has nothing to copy). Plan 7's e2e gate sequences: migration → `ukb full` (fills embeddings) → `syncQdrantFromStore` (rebuilds Qdrant). This plan does NOT call `syncQdrantFromStore` — that's Plan 7's territory.

Risk per RESEARCH §6 Risk 3: 60 specialized entities have classes not declared in `coding-ontology.json` (the existing upper + lower do not cover Config, Port, Container, etc.). The script applies the `metadata.ontologyClassUnregistered: true` fallback — these entities get `ontologyClass` set to their existing entityType, the registry returns false on `hasClass`, but they still load into the canonical store. Plan 7's e2e gate flags any unregistered-class count and decides whether they need follow-up (extending coding-ontology.json) or are acceptable as-is.

Purpose: ROADMAP SC#1 (canonical-shape graph after `ukb full`) — the migration converts the existing graph; SC#5 reuse path.

Output: 1 script file (~300-400 LoC) + 1 integration test file. Script execution against production data happens in Plan 7's e2e gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-01-PLAN.md
@/Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-CONTEXT.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- The km-core surface this script consumes. -->

From /Users/Q284340/Agentic/km-core (root barrel — used by the migration script):

  - GraphKMStore class — instantiate with `{ ontologyDir, dataDir, exportDir, debounceMs: 0 }`
  - mintEntityId() — branded UUIDv7 minting
  - Entity, Relation, EntityId types

From RESEARCH §4 D-54 mapping table (REQUIRED to implement verbatim — every row of the table is one helper function in the script):

  | B current attribute | km-core canonical | Migration rule (paraphrased) |
  |---|---|---|
  | `id` (13-char nanoid) | `id` (UUIDv7) AND `legacyId.id` | mintEntityId for new; preserve old in legacyId |
  | `name` | `name` | identity copy |
  | `entityType` | `ontologyClass` AND `entityType` (both — back-compat) | direct copy |
  | `observations[]` | `description` AND `metadata.descriptionSegments[0]` | join into description; first segment with migration provenance |
  | `significance` | `metadata.significance` | verbatim |
  | `source` | `metadata.source` | verbatim |
  | `metadata.created_at` | `createdAt` AND `validFrom` | promote |
  | `metadata.last_updated` | `updatedAt` | promote |
  | `problem`, `solution` | `metadata.problem`, `metadata.solution` | preserve |
  | (absent) | `legacyId` | synthesize: `{ system: 'B', id: <old> }` |
  | (absent) | `metadata.subsystem` | `'wave-analysis'` |
  | (absent) | `validUntil` | `null` |
  | (absent) | `metadata.provenance.createdBy` | migration stamp |
  | (absent) | `metadata.provenance.lastConfirmedBy` | same |
  | (absent) | `metadata.provenance.confirmationCount` | `1` |
  | `embedding` (rare) | `embedding` | verbatim if present |
  | (absent) | `layer` | `'evidence'` for all B entities |
  | `hierarchyLevel`, `parentEntityName`, ... | `metadata.hierarchyLevel`, ... | preserve in metadata if present in source |

The full prose for each row is in 42-RESEARCH.md §4 — read that section in full before implementing.

From RESEARCH §4 example migration (Example 1 — Detail entity TranscriptAdapter):

  - Before: 9-field entity with id "mc4flkglue8o7", entityType "Detail", observations[]
  - After: ~20-field canonical Entity with branded UUIDv7 id, top-level legacyId.id = "mc4flkglue8o7", layer: "evidence", joined description, descriptionSegments[0] with migration provenance

From /Users/Q284340/Agentic/coding/.data/knowledge-graph/ (the LevelDB target — entities keyed by their nanoid IDs; the script reads each key/value via the level package, applies the mapper, writes to a fresh km-core GraphKMStore instance, then replaces the original DB).

NOTE: In-place migration semantics per D-54: the script writes to a temp `.data/knowledge-graph-migrated/` dataDir first, verifies entity count match, then atomically renames the directories (`mv .data/knowledge-graph .data/knowledge-graph.pre-42-backup && mv .data/knowledge-graph-migrated .data/knowledge-graph`). Plan 7's e2e gate confirms the migrated DB serves reads correctly before deleting the backup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the migration script + property-based integration test</name>
  <files>/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/migration/migrate-leveldb-to-kmcore.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§4 — complete mapping table + 2 worked examples; §6 risks 2 + 3)
    - /Users/Q284340/Agentic/coding/.planning/phases/41-online-learning-adapter-post-hoc-resolution/41-04-PLAN.md (Phase 41 reprojectFromOnlineStore — the precedent for an idempotent legacy-id-keyed reprojection; line-by-line behavior is highly applicable)
    - /Users/Q284340/Agentic/coding/scripts/reproject-online.mjs (Phase 41's operator CLI — concrete script structure, flag parsing, exit codes)
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (Entity shape — including Plan 4's new embedding field)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (constructor signature + putEntity / batch interface — for write path)
  </read_first>
  <behavior>
    - Test 1: Fixture LevelDB seeded with 10 entities (3 Project, 3 Component, 2 SubComponent, 2 Detail) migrates to a fresh km-core store; all 10 entities have legacyId.system === 'B', layer === 'evidence', validFrom populated, validUntil === null.
    - Test 2: Idempotency: running the script twice against the same fixture produces 10 migrations on first run, 0 on second (the script detects already-migrated entities via the legacyId.system === 'B' AND valid-UUIDv7 id presence check from RESEARCH §4 idempotency contract).
    - Test 3: ontologyClass field: a fixture entity with entityType "Detail" gets ontologyClass "Detail"; another with entityType "Config" (a specialized class not in the registry) gets ontologyClass "Config" AND metadata.ontologyClassUnregistered === true.
    - Test 4: descriptionSegments: a fixture entity with 3 observations gets `description` (joined string) + `metadata.descriptionSegments[0]` (one segment with migration provenance — the runId is the same across the whole script run, all 10 segments share that runId).
    - Test 5: embedding: a fixture entity with embedding present (12-element array as test fixture, NOT actual 384-dim) gets embedding copied verbatim. A fixture entity WITHOUT embedding leaves the field undefined on the migrated output.
    - Test 6: provenance stamping: every migrated entity has metadata.provenance.createdBy.provider === 'phase-42-migration', metadata.provenance.createdBy.model === 'b-to-km-core', metadata.provenance.confirmationCount === 1.
    - Test 7: legacyId preservation: a fixture entity with B id "mc4flkglue8o7" has migrated.legacyId === { system: 'B', id: 'mc4flkglue8o7' } AND migrated.id is a branded UUIDv7 (parseable by km-core's parseEntityId).
    - Test 8: dry-run flag: `--dry-run` reports the count it WOULD migrate but writes nothing to the target dataDir.
    - Test 9: error budget: an artificially malformed entity (LevelDB value is invalid JSON) is captured in errors.log, the script continues to migrate the other 9 entities, and exits 0 because 1/10 = 10% — wait, that violates the 5% budget. Reword: seed 20 entities, 1 malformed; 1/20 = 5%, exits 0. Then seed 20 with 2 malformed; 2/20 = 10%, exits non-zero. (Two sub-cases under Test 9.)
  </behavior>
  <action>
    Create `scripts/migrate-leveldb-to-kmcore.mjs` as a Node.js ESM script. Structure:

    1. CLI flag parsing using `process.argv` (or a minimal lib already in B's deps — DO NOT add new deps). Required flags:
       - `--source=<path>` — source LevelDB dir; default `.data/knowledge-graph`
       - `--target=<path>` — destination km-core dataDir; default `.data/knowledge-graph-migrated`
       - `--dry-run` — count and validate without writing
       - `--batch-size=<N>` — default 100; controls store.batch chunk size
       - `--resume` — skip entities whose legacyId.system === 'B' already exists in target (no-op since target is fresh by default; this flag is for restart-from-crash)
       - `--run-id=<string>` — override the migration runId; defaults to deterministic hash of source path + ISO date (gives stable provenance stamps across re-runs per RESEARCH §4 idempotency contract)

    2. Open the source LevelDB read-only using the `level` package (already in B's deps). Iterate all entries via the streaming API.

    3. For each (key, value) pair:
       - `JSON.parse(value)` — capture into `errors[]` on failure with `{ entityId: key, message: err.message }`
       - If the entity already has top-level `legacyId.system === 'B'` AND a parseable UUIDv7 id, skip (idempotency — RESEARCH §4 idempotency contract).
       - Else: apply the D-54 mapping (see §4 of RESEARCH for every field rule) via a `mapToCanonical(source, runId, registry)` helper function. The helper invokes named sub-helpers per row of the mapping table:
         - `deriveCanonicalId(source)` — returns `{ canonicalId, legacyId }`
         - `deriveOntologyClass(source, registry)` — returns `{ ontologyClass, unregistered }` (unregistered true when registry.hasClass(class) returns false; per RESEARCH §6 Risk 3)
         - `deriveDescription(source)` — joins observations into one string; builds descriptionSegments[0] with provenance
         - `stampProvenance(runId, now)` — produces the provenance struct
         - `derivePromotedTimestamps(source)` — produces createdAt/updatedAt/validFrom
         - `derivePreservedMetadata(source)` — produces metadata.{problem,solution,significance,source,subsystem,legacyObservations,hierarchy*}
       - Add the canonical entity to a write-batch.

    4. Once the batch reaches `--batch-size`, flush via `store.batch([{ op: 'putEntity', entity, opts: { skipOntologyCheck: true } }, ...])`. Using batch ops with `skipOntologyCheck: true` is critical: per RESEARCH §6 Risk 3, some entities have unregistered classes; we still need them in the store. Phase 37's BatchOp.skipOntologyCheck widening covers this case (CR-01).

    5. After the iteration completes, flush any remaining batch entities. Then:
       - `await store.exportJson()` — forces a snapshot
       - `await store.close()` — releases LevelDB handle

    6. Compute error budget: `errors.length / totalEntities`. Write all errors to `<dataDir>/migration-errors.log` (newline-delimited JSON, one line per error). Exit 0 if ratio ≤ 0.05, else exit 1.

    7. Print a final summary to stderr: `{ totalEntities, migratedCount, skippedCount, errorCount, errorRatio, runId, durationMs }` as a JSON line.

    DO NOT swap source/target directories in this task (the atomic mv lives in Plan 7's e2e gate). The script ONLY writes to the target dataDir.

    Write the 9 tests (with the 2 sub-cases for Test 9 = 10 cases total) in `src/migration/migrate-leveldb-to-kmcore.test.ts`. Each test:
      - Sets up a fresh tmpdir LevelDB seeded with a known fixture
      - Invokes the script via `child_process.spawn(['node', 'scripts/migrate-leveldb-to-kmcore.mjs', ...])` (or imports the script's main as a module — pick one consistent strategy)
      - Asserts both the on-disk state in the target dataDir AND the exit code

    Diagnostic output via `process.stderr.write` only (no-console-log).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; node --test --test-timeout=60000 dist/migration/migrate-leveldb-to-kmcore.test.js</automated>
  </verify>
  <done>Script + integration test both exist; 10 test cases pass; script can be invoked standalone via `node scripts/migrate-leveldb-to-kmcore.mjs --dry-run --source=...`.</done>
  <acceptance_criteria>
    - Source assertion: `test -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` exits 0.
    - Source assertion: `grep -c "from '@fwornle/km-core'" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` returns ≥ 1.
    - Source assertion (mapping helper names exist): `grep -cE 'deriveCanonicalId|deriveOntologyClass|stampProvenance|deriveDescription' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` returns ≥ 4.
    - Source assertion (idempotency check): `grep -c "legacyId.*system.*'B'" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` returns ≥ 1 (the idempotency guard).
    - Test: 10 integration test cases pass.
    - CLI smoke: `node /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs --help` (if --help is implemented; otherwise --dry-run with missing source) exits 0 or 2 with a usable error message (no stack trace).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Submodule build + Docker rebuild + dry-run sanity against the real LevelDB</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md ("Rebuilding After Code Changes" section)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§4 — production entity count 727)
  </read_first>
  <action>
    Build dance + dry-run sanity check against the real production LevelDB:

    1. `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` — exit 0. Note: if the script is pure .mjs and not picked up by the TS build, no compilation step is needed for the script itself, but the build still validates the test file's TypeScript.
    2. `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` — exit 0.
    3. `docker-compose up -d coding-services`.
    4. Confirm the script is visible inside the container: `docker exec coding-services test -f /coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` exits 0.
    5. Dry-run sanity against the live production LevelDB INSIDE the container (read-only; no modifications):

       docker exec coding-services node /coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs \
         --dry-run \
         --source=/coding/.data/knowledge-graph \
         --target=/tmp/42-05-dryrun-target \
         --run-id=42-05-task-2-dryrun

       Capture the JSON summary line from stderr. Per RESEARCH §4, the production LevelDB has 727 entities; the dry-run summary MUST report `totalEntities` ≥ 700 (small variance tolerated for entities added between research time and migration time) and `errorCount === 0` (no malformed entities in production data; if errors > 0, surface in SUMMARY).

       NOTE: dry-run does NOT mutate the source. The exit code is 0 (no errors) OR a positive number if `errorCount / totalEntities > 0.05` (would fail the migration; investigate per RESEARCH §6 Risk 3 — likely ontology-classifier coverage gap).

    6. DO NOT run the script without --dry-run in this task. The actual migration runs as part of Plan 7's e2e gate, where the operator approves the atomic dir swap.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; cd /Users/Q284340/Agentic/coding/docker &amp;&amp; docker-compose build coding-services &amp;&amp; docker-compose up -d coding-services &amp;&amp; sleep 8 &amp;&amp; docker exec coding-services test -f /coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs</automated>
  </verify>
  <done>Build green; script visible in container; dry-run reports ≥ 700 entities in the real LevelDB with errorCount === 0 (or surfaces error details in SUMMARY).</done>
  <acceptance_criteria>
    - CLI: `npm run build` exits 0.
    - CLI: `docker-compose build coding-services` exits 0.
    - CLI: `docker exec coding-services test -f /coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` exits 0.
    - CLI: `docker exec coding-services node /coding/integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs --dry-run --source=/coding/.data/knowledge-graph --target=/tmp/42-05-dryrun-target --run-id=42-05-task-2-dryrun 2>&1 | tail -1 | jq -e '.totalEntities >= 700 and .errorCount == 0'` exits 0 (or surfaces failure JSON with context).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem then LevelDB (read) then mapping logic then LevelDB (write to target) | The script reads ALL existing entities; a single malformed key/value could halt iteration if not caught |
| migration runId then provenance stamp on all 727 entities | All migrated entities carry the same provenance stamp — provenance forensics depend on the stamp being correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Tampering | source LevelDB | mitigate | Source opened READ-ONLY via level package's read-options; target is a fresh directory; atomic dir swap deferred to Plan 7 with operator approval |
| T-42-05-02 | Information Disclosure | descriptionSegments[0] migration provenance | accept | All entries share the same provider/model stamp; this is by design (D-54 + RESEARCH §4) |
| T-42-05-03 | Denial of Service | error budget bypass | mitigate | Hard cap at 5% error ratio (RESEARCH-aligned tolerance); exit non-zero forces operator investigation before swap |
| T-42-05-04 | Repudiation | migration runId stamping | mitigate | Deterministic runId (--run-id flag with default derived from source path + date) makes the provenance reproducible and auditable |
</threat_model>

<verification>
- 10 integration test cases pass.
- Build + Docker rebuild green.
- Dry-run against production LevelDB reports ≥ 700 entities and 0 errors.
</verification>

<success_criteria>
1. `scripts/migrate-leveldb-to-kmcore.mjs` exists; implements all RESEARCH §4 mapping rules.
2. Idempotency verified by Test 2 (second run produces no further migrations).
3. Property-based invariants verified by Tests 1, 3, 4, 5, 6, 7.
4. Error budget (5%) enforced by Test 9 sub-cases.
5. Dry-run against production LevelDB inside the container succeeds with ≥ 700 entities and 0 errors.
</success_criteria>

<output>
Create `.planning/phases/42-offline-ukb-migration-b/42-05-SUMMARY.md` when done, recording:
- 10 test cases (all green)
- Production dry-run summary: totalEntities, errorCount, ontologyClassUnregistered count (the 60 specialized entities per RESEARCH §4)
- runId convention chosen
- Atomic swap deferred to Plan 7 (note this explicitly)
</output>
