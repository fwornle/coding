---
phase: 42-offline-ukb-migration-b
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/coding/.data/ontologies/upper.json
  - /Users/Q284340/Agentic/coding/.data/ontologies/agentic-ontology.json
  - /Users/Q284340/Agentic/coding/.data/ontologies/cluster-reprocessing-ontology.json
  - /Users/Q284340/Agentic/coding/.data/ontologies/code-entities-ontology.json
  - /Users/Q284340/Agentic/coding/.data/ontologies/coding-ontology.json
  - /Users/Q284340/Agentic/coding/.data/ontologies/raas-ontology.json
  - /Users/Q284340/Agentic/coding/.data/ontologies/resi-ontology.json
  - /Users/Q284340/Agentic/coding/.data/ontologies/ui-ontology.json
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/tools.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyManager.ts
  - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/registry-adoption.test.ts
autonomous: true
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "`coding/.data/ontologies/` has 8 JSON files at the root: `upper.json` (renamed from `upper/development-knowledge-ontology.json`) + 7 lower-ontology files moved up from `lower/` (per D-53a Option A — flatten)."
    - "km-core's `OntologyRegistry` (from `@fwornle/km-core/ontology`) loads all 8 files from the flat `.data/ontologies/` directory without code changes; auto-discovery (ONTO-01) + extends-chain (ONTO-02) work against B's existing component-manifest data (SC#5)."
    - "`src/ontology/OntologyManager.ts` is DELETED — the file no longer exists in the source tree."
    - "The two callers (`src/agents/ontology-classification-agent.ts` and `src/tools.ts`) instantiate `new OntologyRegistry({ ontologyDir: ... })` instead of `new OntologyManager()` and call `registry.hasClass / .getAllClassNames / .reload` instead of `manager.hasEntityClass / .getAllEntityClasses / .reload`."
    - "`src/ontology/{OntologyClassifier,OntologyValidator,OntologyQueryEngine}.ts` are PRESERVED — they survive Phase 42 per D-53."
    - "Old layout (`upper/` + `lower/` subdirs) does NOT exist after the migration; only `upper.json` + 7 lowers at root + the untouched `archive/` + `schemas/` + `suggestions/` directories (registry ignores them since it does `endsWith('.json')` at root only — verified in RESEARCH §5)."
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/.data/ontologies/upper.json"
      provides: "Flattened upper ontology (was upper/development-knowledge-ontology.json)"
      contains: "classes"
    - path: "/Users/Q284340/Agentic/coding/.data/ontologies/coding-ontology.json"
      provides: "B's coding-specific lower ontology (was lower/coding-ontology.json)"
      contains: "classes"
    - path: "/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/registry-adoption.test.ts"
      provides: "Verification that km-core OntologyRegistry loads all 8 flattened files cleanly + extends-chain resolves for B's coding-ontology"
  key_links:
    - from: "src/agents/ontology-classification-agent.ts"
      to: "@fwornle/km-core/ontology OntologyRegistry"
      via: "import + constructor call"
      pattern: "from '@fwornle/km-core/ontology'"
    - from: "src/tools.ts"
      to: "@fwornle/km-core/ontology OntologyRegistry"
      via: "same import + constructor swap"
      pattern: "OntologyRegistry"
---

<objective>
Replace B's `OntologyManager` with km-core's `OntologyRegistry` (Phase 38). Per RESEARCH §5, the two layouts are incompatible because km-core's registry only walks ONE level of `ontologyDir` (`registry.ts:71` does `readdirSync(this.ontologyDir).filter(f => f.endsWith('.json') && f !== 'upper.json')`), whereas B's existing layout has `upper/development-knowledge-ontology.json` and `lower/*.json` in subdirectories.

Per D-53a, the planner chose Option A: **flatten the layout** (single git move operation; no km-core change required). This satisfies SC#5 ("B's existing component-manifest works unchanged as a lower ontology against KM-Core's `OntologyRegistry`").

After the flatten, replace the 2 OntologyManager callers (per RESEARCH §5 — only `ontology-classification-agent.ts` and `tools.ts`), delete `OntologyManager.ts` itself, and verify no other consumers reference it. Per D-53, `OntologyClassifier`, `OntologyValidator`, `OntologyQueryEngine` STAY (they encode B-specific intelligence beyond what km-core's registry provides).

Purpose: SC#5 (B's component-manifest works against km-core's registry); structural readiness for Plan 5's wave-controller migration (which needs the registry's `findByOntologyClass` to be live).

Output: 8 JSON files moved/renamed in `.data/ontologies/`; 2 TS files updated; 1 TS file deleted; 1 new test file in B's test tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- km-core OntologyRegistry surface — read from registry.ts. -->

From /Users/Q284340/Agentic/km-core/src/ontology/registry.ts:

  class OntologyRegistry {
    constructor(opts: { ontologyDir: string; strict?: boolean })
    isValidClass(name: string): boolean
    hasClass(name: string): boolean             // alias of isValidClass (Phase 38 D-27)
    getClass(name: string): ResolvedClass | undefined
    getAllClassNames(): string[]
    listClasses(): string[]                     // alias of getAllClassNames
    getDefaultLayer(name: string): Layer | undefined
    getValidRelationships(name: string): Record<string, string[]>
    getLoadedDomains(): string[]
    listDomains(): string[]
    parentChainOf(name: string): string[]
    provenanceOf(name: string): string | undefined
    async reload(): Promise<void>
    get classCatalog(): ReadonlyMap<string, ResolvedClass>
    get domains(): ReadonlySet<string>
  }

NOTE: Single-level directory walk. Files in subdirectories are NOT loaded.

From /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyManager.ts (the surface to replace — per RESEARCH §5):

  Method                            km-core equivalent
  -----                             ------------------
  initialize()                      (constructor handles it)
  hasEntityClass(name, team?)       hasClass(name)              [team ignored; per-instance registry]
  getAllEntityClasses(team?)        getAllClassNames()
  getUpperOntology()                getClass('<upperClassName>') per-class (different shape; caller refactors)
  getLowerOntology(team)            (no 1:1 — registry exposes per-class lookups)
  reload()                          reload()
  clearCache()                      (not needed — atomic reload)
  getStatistics(team?)              (not in km-core; caller drops or builds locally)

From /Users/Q284340/Agentic/coding/.data/ontologies/ (current layout — must be flattened):

  archive/                          (untouched — historical)
  lower/agentic-ontology.json       → move to ./agentic-ontology.json
  lower/cluster-reprocessing-ontology.json → ./cluster-reprocessing-ontology.json
  lower/code-entities-ontology.json → ./code-entities-ontology.json
  lower/coding-ontology.json        → ./coding-ontology.json
  lower/raas-ontology.json          → ./raas-ontology.json
  lower/resi-ontology.json          → ./resi-ontology.json
  lower/ui-ontology.json            → ./ui-ontology.json
  schemas/ontology-schema.json      (untouched — reference, not loaded)
  suggestions/pending-classes.json  (untouched — B-specific staging, not loaded)
  upper/development-knowledge-ontology.json → rename + move to ./upper.json

After the flatten: 8 JSON files at root + 3 untouched subdirs (archive/, schemas/, suggestions/).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten the .data/ontologies/ directory layout</name>
  <files>/Users/Q284340/Agentic/coding/.data/ontologies/upper.json, /Users/Q284340/Agentic/coding/.data/ontologies/agentic-ontology.json, /Users/Q284340/Agentic/coding/.data/ontologies/cluster-reprocessing-ontology.json, /Users/Q284340/Agentic/coding/.data/ontologies/code-entities-ontology.json, /Users/Q284340/Agentic/coding/.data/ontologies/coding-ontology.json, /Users/Q284340/Agentic/coding/.data/ontologies/raas-ontology.json, /Users/Q284340/Agentic/coding/.data/ontologies/resi-ontology.json, /Users/Q284340/Agentic/coding/.data/ontologies/ui-ontology.json</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.data/ontologies/upper/development-knowledge-ontology.json (verify the JSON is valid before move; cite top-level keys in SUMMARY)
    - /Users/Q284340/Agentic/coding/.data/ontologies/lower/coding-ontology.json (verify valid before move)
    - /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (lines 63-87 — confirm the single-level walk + the `f !== 'upper.json'` filter — the flatten target shape MUST match)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§5 closing — the exact 8-file flatten table)
  </read_first>
  <action>
    Use `git mv` for every move to preserve file history (this directory is git-tracked per the project's commit hygiene):

      git mv .data/ontologies/upper/development-knowledge-ontology.json .data/ontologies/upper.json
      git mv .data/ontologies/lower/agentic-ontology.json .data/ontologies/agentic-ontology.json
      git mv .data/ontologies/lower/cluster-reprocessing-ontology.json .data/ontologies/cluster-reprocessing-ontology.json
      git mv .data/ontologies/lower/code-entities-ontology.json .data/ontologies/code-entities-ontology.json
      git mv .data/ontologies/lower/coding-ontology.json .data/ontologies/coding-ontology.json
      git mv .data/ontologies/lower/raas-ontology.json .data/ontologies/raas-ontology.json
      git mv .data/ontologies/lower/resi-ontology.json .data/ontologies/resi-ontology.json
      git mv .data/ontologies/lower/ui-ontology.json .data/ontologies/ui-ontology.json

    After moves, remove the now-empty `upper/` and `lower/` directories: `rmdir .data/ontologies/upper .data/ontologies/lower`.

    Do NOT touch `archive/`, `schemas/`, `suggestions/` — they remain in place per RESEARCH §5 (registry only loads root-level `*.json`).

    NO content modifications to any of the 8 JSON files in this task. If the upper ontology file's internal `meta.name` field is "upper" (which it should be) it loads as-is. If any file needs schema tweaks to fit km-core's `OntologyFile` shape per D-53b, surface that in the SUMMARY and (only if necessary to make Task 3's adoption test pass) add the minimal `extends` declaration. The expectation per the 8 OKM-shape ontologies tested in Phase 38 is that B's files Just Work, so default to no content edits.
  </action>
  <verify>
    <automated>test -f /Users/Q284340/Agentic/coding/.data/ontologies/upper.json &amp;&amp; test -f /Users/Q284340/Agentic/coding/.data/ontologies/coding-ontology.json &amp;&amp; test ! -d /Users/Q284340/Agentic/coding/.data/ontologies/upper &amp;&amp; test ! -d /Users/Q284340/Agentic/coding/.data/ontologies/lower &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('/Users/Q284340/Agentic/coding/.data/ontologies/upper.json','utf8'))"</automated>
  </verify>
  <done>All 8 JSON files present at root; `upper/` and `lower/` directories no longer exist; `upper.json` parses as valid JSON; archive/schemas/suggestions/ subdirs preserved.</done>
  <acceptance_criteria>
    - CLI: `ls /Users/Q284340/Agentic/coding/.data/ontologies/*.json | wc -l` returns exactly 8.
    - CLI: `test ! -d /Users/Q284340/Agentic/coding/.data/ontologies/upper && test ! -d /Users/Q284340/Agentic/coding/.data/ontologies/lower` exits 0.
    - CLI: `for f in /Users/Q284340/Agentic/coding/.data/ontologies/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || exit 1; done` exits 0.
    - CLI: `ls /Users/Q284340/Agentic/coding/.data/ontologies/archive /Users/Q284340/Agentic/coding/.data/ontologies/schemas /Users/Q284340/Agentic/coding/.data/ontologies/suggestions` all exit 0 (untouched subdirs preserved).
    - Git history preserved: `git log --follow .data/ontologies/upper.json` returns commits from before the move (proves git mv was used, not cp+rm).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire OntologyManager callers to km-core OntologyRegistry; delete OntologyManager.ts</name>
  <files>/Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/tools.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyManager.ts, /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/registry-adoption.test.ts</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts (entire file — locate every `OntologyManager` reference + every call to its methods)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/tools.ts (entire file — locate every `OntologyManager` reference)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyManager.ts (full surface to confirm what each consumer actually calls — informs the replacement mapping)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyClassifier.ts (first 80 lines — confirm it does NOT import OntologyManager-private internals per RESEARCH §5 Open Question 5; if it does, surface in SUMMARY and adjust scope)
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyValidator.ts (first 60 lines — same check)
    - /Users/Q284340/Agentic/km-core/src/ontology/registry.ts (the full OntologyRegistry API surface)
  </read_first>
  <behavior>
    - Test 1: After the swap, `grep -rn "import.*OntologyManager" src/` returns ZERO matches (no remaining imports).
    - Test 2: After the swap, `grep -rn "new OntologyManager" src/` returns ZERO matches.
    - Test 3: New km-core registry instantiation in `ontology-classification-agent.ts` uses absolute path `/coding/.data/ontologies` (Docker-internal path, matching where the container mounts `.data/`).
    - Test 4: The registry's `hasClass` method is called where `hasEntityClass` was called previously (same semantics).
    - Test 5: New unit test loads the 8 flattened ontology JSONs through `OntologyRegistry`, asserts `registry.hasClass('Component') === true` (a class declared in upper ontology), `registry.hasClass('Detail') === true` (from coding-ontology lower per RESEARCH §4 production distribution), and `registry.listDomains().length >= 8` (upper + 7 lowers).
  </behavior>
  <action>
    1. In `src/agents/ontology-classification-agent.ts`: locate every reference to `OntologyManager`. Replace with `OntologyRegistry` from km-core via the swap mapping in `<interfaces>`. Concrete substitutions:

       - Replace `import { OntologyManager } from '../ontology/OntologyManager.js'` with `import { OntologyRegistry } from '@fwornle/km-core/ontology'`.
       - Replace `new OntologyManager(...)` instantiations with `new OntologyRegistry({ ontologyDir: '/coding/.data/ontologies' })`. The Docker container bind-mounts the repo at `/coding`, so this absolute path resolves both in production and is grep-detectable.
       - Replace `manager.hasEntityClass(name)` with `registry.hasClass(name)`.
       - Replace `manager.hasEntityClass(name, team)` with `registry.hasClass(name)` (team ignored per single-tenant registry — record this semantic change in SUMMARY).
       - Replace `manager.getAllEntityClasses()` with `registry.getAllClassNames()`.
       - Replace `manager.reload()` with `await registry.reload()` (km-core's reload is async).
       - Replace `await manager.initialize()` with nothing — registry's constructor handles initial load synchronously.
       - If `manager.getLowerOntology(team)` / `manager.getUpperOntology()` / `manager.getStatistics()` are called, refactor to per-class lookups via `registry.getClass(name)`, `registry.getAllClassNames()`. If a non-trivial inline statistics calc is needed, build it locally from `registry.classCatalog` (km-core's ReadonlyMap getter).

    2. In `src/tools.ts`: same swap pattern as above. Apply identical substitutions.

    3. After both files are rewired, `grep -rn "OntologyManager" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/` MUST return matches ONLY in `src/ontology/OntologyManager.ts` itself (no other consumers left).

    3a. **Pre-deletion safety gate (per plan-checker H3):** BEFORE deleting the file, confirm OntologyClassifier / OntologyValidator / OntologyQueryEngine have ZERO references to OntologyManager:

        ```bash
        grep -rln "OntologyManager" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyClassifier.ts \
                                    /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyValidator.ts \
                                    /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyQueryEngine.ts 2>/dev/null
        ```

        Must return zero output. If ANY of the three files reference OntologyManager, STOP — the D-53 "keep classifier/validator/query-engine" assumption is invalid; report in SUMMARY and ask the user before proceeding. Do NOT run step 4 if step 3a fails.

    4. Delete `src/ontology/OntologyManager.ts`: `git rm /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyManager.ts`. (Use git rm — preserves deletion in history.)

    5. Verify OntologyClassifier / OntologyValidator / OntologyQueryEngine do NOT import OntologyManager (per D-53 they survive). If they do (RESEARCH §5 Open Question 5), update those imports to consume the registry directly via the same patterns; record any such cases in SUMMARY. If the refactor surface inside those three modules is small (1-2 lines per file), apply it; if it grows beyond a one-liner, surface in SUMMARY and propose a follow-up plan rather than expanding this task.

    6. Create the new test file `src/ontology/registry-adoption.test.ts` with the 5 behavior tests above. Use `import { OntologyRegistry } from '@fwornle/km-core/ontology'` and point `ontologyDir` at the actual `.data/ontologies/` path. Asserts the registry loads cleanly and finds known classes (`Component`, `Detail`).

    Use `process.stderr.write` for any diagnostic output (no-console-log).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; node --test dist/ontology/registry-adoption.test.js &amp;&amp; test ! -f src/ontology/OntologyManager.ts &amp;&amp; ! grep -rln 'OntologyManager' src/ --include='*.ts'</automated>
  </verify>
  <done>OntologyManager.ts deleted; 2 callers rewired to km-core OntologyRegistry; 5 new tests pass; zero residual references to OntologyManager in src/.</done>
  <acceptance_criteria>
    - Source assertion: `grep -rln 'OntologyManager' /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ --include='*.ts'` returns no output (zero matches in src/).
    - Source assertion: `test ! -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyManager.ts` exits 0.
    - Source assertion (D-53 survivors): `test -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyClassifier.ts && test -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyValidator.ts && test -f /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/ontology/OntologyQueryEngine.ts` exits 0.
    - Source assertion: `grep -c "from '@fwornle/km-core/ontology'" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts` returns ≥ 1.
    - Source assertion: `grep -c "from '@fwornle/km-core/ontology'" /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/src/tools.ts` returns ≥ 1.
    - Test: 5 unit tests in `src/ontology/registry-adoption.test.ts` all pass.
    - Compile: `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` exits 0 (no broken imports anywhere in src/).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Submodule build + Docker rebuild + container-side ontology load verification</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/CLAUDE.md ("Rebuilding After Code Changes" section)
    - /Users/Q284340/Agentic/coding/.planning/phases/42-offline-ukb-migration-b/42-RESEARCH.md (§5 — confirms the .data/ontologies path inside Docker)
  </read_first>
  <action>
    Run the mandatory build dance, then verify the registry loads inside the container:

    1. `cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis && npm run build` — exit 0.
    2. `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` — exit 0.
    3. `docker-compose up -d coding-services`.
    4. Confirm new code is in the running container: `docker exec coding-services test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/ontology/OntologyManager.js` exits 0 (the compiled output has no leftover).
    5. Confirm the flattened ontology files are visible inside the container: `docker exec coding-services ls /coding/.data/ontologies/*.json | wc -l` returns ≥ 8.
    6. Confirm km-core registry can load them inside the container:

       docker exec coding-services node -e "
         import('@fwornle/km-core/ontology').then(m => {
           const r = new m.OntologyRegistry({ ontologyDir: '/coding/.data/ontologies' });
           const c = r.getAllClassNames();
           if (c.includes('Component') && c.includes('Detail') && r.listDomains().length >= 8) process.exit(0);
           else { console.error('Missing classes or domains:', c.length, r.listDomains()); process.exit(1); }
         }).catch(e => { console.error(e); process.exit(2); });
       "

       Exit 0 = SC#5 confirmed at runtime.
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis &amp;&amp; npm run build &amp;&amp; cd /Users/Q284340/Agentic/coding/docker &amp;&amp; docker-compose build coding-services &amp;&amp; docker-compose up -d coding-services &amp;&amp; sleep 8 &amp;&amp; docker exec coding-services test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/ontology/OntologyManager.js &amp;&amp; docker exec coding-services bash -c 'ls /coding/.data/ontologies/*.json | wc -l' | xargs -I{} test {} -ge 8</automated>
  </verify>
  <done>Build green; OntologyManager.js absent from dist/; 8+ flattened JSONs visible inside the container; km-core registry inside the container loads them and finds Component + Detail.</done>
  <acceptance_criteria>
    - CLI: `npm run build` exits 0.
    - CLI: `docker-compose build coding-services` exits 0.
    - CLI: `docker exec coding-services test ! -f /coding/integrations/mcp-server-semantic-analysis/dist/ontology/OntologyManager.js` exits 0.
    - CLI: `docker exec coding-services bash -c 'ls /coding/.data/ontologies/*.json | wc -l'` returns ≥ 8.
    - Behavior (SC#5): the inline `docker exec ... node -e` block above exits 0 — km-core's OntologyRegistry loads B's flattened ontologies with `Component` + `Detail` classes resolved and ≥ 8 domains.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem ↔ git working tree | Mass file moves under `.data/ontologies/` — risk of accidentally moving non-ontology files |
| consumer modules ↔ deleted OntologyManager | If any module other than the documented 2 callers imports OntologyManager, compilation fails — risk of incomplete grep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03-01 | Tampering | flatten step | mitigate | Use `git mv` (not `cp`); file history preserved; verifiable via `git log --follow`; subdirectory removals use `rmdir` (fails on non-empty, prevents accidental data loss in archive/) |
| T-42-03-02 | Information Disclosure | content edits | mitigate | NO content modifications by default; any required `extends` tweak per D-53b is surfaced in SUMMARY |
| T-42-03-03 | Tampering | OntologyManager deletion | mitigate | Final grep gate confirms zero `OntologyManager` references in src/ before deletion (Task 2 acceptance) |
| T-42-03-04 | Denial of Service | runtime registry load failure | mitigate | Task 3's container-side smoke test fails-loud if registry can't load any of the 8 files |
</threat_model>

<verification>
- 8 JSONs at root + 0 in upper/lower/ subdirs.
- 2 callers rewired; OntologyManager.ts deleted.
- 5 new tests pass.
- Container-side load works; SC#5 confirmed at runtime.
</verification>

<success_criteria>
1. `.data/ontologies/` has exactly `upper.json` + 7 lowers at the root + the 3 untouched subdirs (archive/, schemas/, suggestions/).
2. `OntologyManager.ts` is deleted from source AND from compiled `dist/`.
3. `ontology-classification-agent.ts` + `tools.ts` import `OntologyRegistry` from `@fwornle/km-core/ontology` and use registry's API.
4. SC#5: B's coding-ontology loads cleanly through km-core's `OntologyRegistry` in the container (Task 3 smoke test exits 0).
</success_criteria>

<output>
Create `.planning/phases/42-offline-ukb-migration-b/42-03-SUMMARY.md` when done, recording:
- 8 files moved (with byte-size sanity check)
- 2 caller TS files modified (line ranges)
- OntologyManager.ts deletion confirmed
- Any D-53b content edits applied (should be zero by default; surface any if needed)
- Container-side load count + domain list
</output>
