---
phase: 04-schema-configuration-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/mcp-server-semantic-analysis/config/component-manifest.yaml
  - .data/ontologies/lower/coding-ontology.json
  - integrations/mcp-server-semantic-analysis/src/types/component-manifest.ts
autonomous: true
requirements: [SCHM-03, SCHM-04]

must_haves:
  truths:
    - "component-manifest.yaml lists all 8 L1 components (LiveLoggingSystem, LLMAbstraction, DockerizedServices, Trajectory, KnowledgeManagement, CodingPatterns, ConstraintSystem, SemanticAnalysis) with aliases, keywords, and descriptions"
    - "L2 sub-components exist under KnowledgeManagement (ManualLearning, OnlineLearning) and SemanticAnalysis (Pipeline, Ontology, Insights)"
    - "coding-ontology.json contains Component and SubComponent entity type definitions with descriptions, properties, and examples"
    - "Component and SubComponent types do NOT use extendsEntity (they are structural scaffold nodes, not code artifacts)"
    - "TypeScript types for loading the manifest are defined and exported for Phase 6 consumption"
    - "Manifest file is in the config/ directory (bind-mounted into Docker, no rebuild needed)"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/config/component-manifest.yaml"
      provides: "Authoritative component hierarchy definition"
      contains: "LiveLoggingSystem"
    - path: ".data/ontologies/lower/coding-ontology.json"
      provides: "Ontology with Component and SubComponent entity types"
      contains: "Component"
    - path: "integrations/mcp-server-semantic-analysis/src/types/component-manifest.ts"
      provides: "TypeScript interfaces for manifest loading"
      exports: ["ComponentManifestEntry", "ComponentManifest", "loadComponentManifest"]
  key_links:
    - from: "integrations/mcp-server-semantic-analysis/config/component-manifest.yaml"
      to: "integrations/mcp-server-semantic-analysis/src/types/component-manifest.ts"
      via: "YAML file parsed into ComponentManifest TypeScript type"
      pattern: "parse.*component-manifest\\.yaml"
    - from: ".data/ontologies/lower/coding-ontology.json"
      to: "integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts"
      via: "Ontology validation accepts Component/SubComponent types during entity persistence"
      pattern: "Component|SubComponent"
---

<objective>
Author the component-manifest.yaml file defining the L1/L2 component hierarchy and extend the coding ontology with Component and SubComponent entity types. Also create TypeScript interfaces for manifest loading that Phase 6 will consume.

Purpose: The manifest is the authoritative source of truth for component names, aliases, and keywords used by the hierarchy classifier (Phase 6) and migration script (Phase 5). The ontology extension ensures scaffold nodes are accepted by validation without spurious warnings.

Output: 1 new YAML config file, 1 modified JSON ontology file, 1 new TypeScript types file.
</objective>

<execution_context>
@/Users/Q284340/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Q284340/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-schema-configuration-foundation/04-CONTEXT.md
@.planning/phases/04-schema-configuration-foundation/04-RESEARCH.md

@.data/ontologies/lower/coding-ontology.json
@integrations/mcp-server-semantic-analysis/src/utils/workflow-loader.ts

<interfaces>
<!-- Existing patterns to follow -->

From coding-ontology.json -- entity type structure (follow this pattern for Component/SubComponent):
```json
{
  "entities": {
    "ExistingType": {
      "description": "...",
      "properties": { ... },
      "requiredProperties": [...],
      "examples": [...]
    }
  }
}
```

From workflow-loader.ts -- YAML loading pattern (use identical approach):
```typescript
import * as fs from 'fs';
import * as path from 'path';
import { parse } from 'yaml';

// getConfigDir() returns the config directory path
export function getConfigDir(): string { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author component-manifest.yaml with L1/L2 hierarchy</name>
  <files>
    integrations/mcp-server-semantic-analysis/config/component-manifest.yaml
  </files>
  <action>
Create a new file at `integrations/mcp-server-semantic-analysis/config/component-manifest.yaml`. This is a bind-mounted directory (confirmed at docker-compose.yml line 72), so no Docker rebuild is needed.

The manifest defines the complete component hierarchy. Use the exact content structure below. This is derived from the locked user decisions in CONTEXT.md -- the component names, L2 children, and structure are NON-NEGOTIABLE.

```yaml
# Component Manifest - Authoritative source of truth for the L1/L2 component hierarchy.
# Read by: Phase 5 migration script, Phase 6 HierarchyClassifier
# Loaded via: yaml package parse(), same pattern as workflow-loader.ts
# Location: bind-mounted read-only into Docker -- no rebuild needed to iterate

version: "1.0"

project:
  name: Coding
  level: 0
  description: "Root node of the coding project knowledge hierarchy, encompassing all development infrastructure knowledge"

components:
  - name: LiveLoggingSystem
    level: 1
    description: "Live session logging infrastructure capturing Claude Code conversations. Handles session windowing, file routing, classification layers, and transcript capture."
    aliases:
      - LSL
      - live-logging
      - session-logging
      - SpecStory
    keywords:
      - session
      - logging
      - transcript
      - LSL
      - classification
      - windowing
      - specstory
    children: []

  - name: LLMAbstraction
    level: 1
    description: "Abstraction layer over LLM providers (Anthropic, OpenAI, Groq) enabling provider-agnostic model calls, tier-based routing, and mock mode for testing."
    aliases:
      - llm-cli
      - llm-proxy
      - model-abstraction
      - DMR
    keywords:
      - LLM
      - provider
      - anthropic
      - openai
      - groq
      - tier
      - model
      - dmr
    children: []

  - name: DockerizedServices
    level: 1
    description: "Docker containerization layer for all coding services including semantic analysis MCP, constraint monitor, code-graph-rag, and supporting databases."
    aliases:
      - docker
      - containers
      - coding-services
    keywords:
      - docker
      - container
      - compose
      - service
      - deployment
      - memgraph
      - redis
    children: []

  - name: Trajectory
    level: 1
    description: "AI trajectory and planning system managing project milestones, GSD workflow, phase planning, and implementation task tracking."
    aliases:
      - GSD
      - planning
      - milestone
      - phases
    keywords:
      - trajectory
      - milestone
      - planning
      - phase
      - GSD
      - roadmap
      - spec
    children: []

  - name: KnowledgeManagement
    level: 1
    description: "Knowledge graph storage, query, and lifecycle management including the VKB server, graph database, entity persistence, and knowledge decay tracking."
    aliases:
      - knowledge-graph
      - KG
      - VKB
      - UKB
    keywords:
      - knowledge
      - graph
      - entity
      - VKB
      - UKB
      - LevelDB
      - Graphology
      - persistence
      - decay
    children:
      - name: ManualLearning
        level: 2
        description: "Knowledge created or curated by humans: manually authored entities, direct edits, and hand-crafted observations."
        aliases:
          - manual
          - human-authored
        keywords:
          - manual
          - human
          - authored
          - curated
          - hand-crafted

      - name: OnlineLearning
        level: 2
        description: "Knowledge extracted automatically by the batch analysis pipeline from git history, LSL sessions, and code analysis."
        aliases:
          - automated
          - pipeline-extracted
          - batch-learning
        keywords:
          - batch
          - pipeline
          - automated
          - extracted
          - analysis
          - online

  - name: CodingPatterns
    level: 1
    description: "General programming wisdom, design patterns, best practices, and coding conventions applicable across the project. Catch-all for entities not fitting other components."
    aliases:
      - patterns
      - best-practices
      - conventions
    keywords:
      - pattern
      - convention
      - practice
      - design
      - approach
      - idiom
      - anti-pattern
    children: []

  - name: ConstraintSystem
    level: 1
    description: "Constraint monitoring and enforcement system that validates code actions and file operations against configured rules during Claude Code sessions."
    aliases:
      - constraints
      - constraint-monitor
      - enforcement
    keywords:
      - constraint
      - rule
      - validation
      - enforcement
      - hook
      - violation
    children: []

  - name: SemanticAnalysis
    level: 1
    description: "Multi-agent semantic analysis pipeline (batch-analysis workflow) that processes git history and LSL sessions to extract and persist structured knowledge entities."
    aliases:
      - semantic-analysis
      - MCP
      - pipeline
      - batch-analysis
    keywords:
      - semantic
      - analysis
      - agent
      - pipeline
      - batch
      - ontology
      - classification
      - coordinator
    children:
      - name: Pipeline
        level: 2
        description: "The batch processing pipeline agents: coordinator, observation generation, KG operators, deduplication, and persistence."
        aliases:
          - coordinator
          - batch-pipeline
        keywords:
          - coordinator
          - batch
          - observation
          - KG
          - pipeline
          - dedup

      - name: Ontology
        level: 2
        description: "The ontology classification system: upper/lower ontology definitions, entity type resolution, and validation."
        aliases:
          - ontology-classification
          - entity-types
        keywords:
          - ontology
          - classification
          - entity-type
          - validation
          - upper
          - lower

      - name: Insights
        level: 2
        description: "Insight generation, pattern catalog extraction, and knowledge report authoring."
        aliases:
          - insight-generation
          - patterns
          - reports
        keywords:
          - insight
          - pattern
          - report
          - generation
          - catalog
          - PlantUML
```

Verify the file is valid YAML: `python3 -c "import yaml; yaml.safe_load(open('integrations/mcp-server-semantic-analysis/config/component-manifest.yaml'))"` (run from project root).
  </action>
  <verify>
    <automated>python3 -c "import yaml; m=yaml.safe_load(open('integrations/mcp-server-semantic-analysis/config/component-manifest.yaml')); print(f'Components: {len(m[\"components\"])}')"</automated>
  </verify>
  <done>
    - component-manifest.yaml exists at integrations/mcp-server-semantic-analysis/config/
    - Lists all 8 L1 components with name, level, description, aliases, keywords, children
    - KnowledgeManagement has ManualLearning and OnlineLearning children
    - SemanticAnalysis has Pipeline, Ontology, and Insights children
    - YAML parses without errors
    - PascalCase naming used for all component names
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Component/SubComponent to ontology and create manifest loader types</name>
  <files>
    .data/ontologies/lower/coding-ontology.json,
    integrations/mcp-server-semantic-analysis/src/types/component-manifest.ts
  </files>
  <action>
**Part A: Ontology Extension**

Open `.data/ontologies/lower/coding-ontology.json` and add two new entries to the `entities` object. Follow the exact same structure as existing entity types in the file. Do NOT use `extendsEntity` -- Component and SubComponent are structural scaffold nodes, not code artifacts.

Add these two entries:

```json
"Component": {
  "description": "A top-level architectural component (L1) of the Coding project hierarchy, grouping related knowledge entities under a named subsystem",
  "properties": {
    "componentName": {
      "type": "string",
      "description": "PascalCase component name matching the component manifest"
    },
    "aliases": {
      "type": "array",
      "description": "Alternative names used to match entities to this component"
    },
    "keywords": {
      "type": "array",
      "description": "Heuristic matching keywords for entity classification"
    },
    "level": {
      "type": "number",
      "description": "Hierarchy level (always 1 for Component)"
    }
  },
  "requiredProperties": ["componentName"],
  "examples": ["LiveLoggingSystem", "KnowledgeManagement", "SemanticAnalysis", "CodingPatterns"]
},
"SubComponent": {
  "description": "A named subsystem (L2) nested within a Component, grouping related Detail entities under a focused concern",
  "properties": {
    "componentName": {
      "type": "string",
      "description": "PascalCase sub-component name"
    },
    "parentComponent": {
      "type": "string",
      "description": "Name of the L1 parent component"
    },
    "level": {
      "type": "number",
      "description": "Hierarchy level (always 2 for SubComponent)"
    }
  },
  "requiredProperties": ["componentName", "parentComponent"],
  "examples": ["ManualLearning", "OnlineLearning", "Pipeline", "Ontology", "Insights"]
}
```

Verify the JSON is valid after editing: `python3 -c "import json; json.load(open('.data/ontologies/lower/coding-ontology.json'))"` (run from project root).

**Part B: Manifest Loader TypeScript Types**

Create a new file `integrations/mcp-server-semantic-analysis/src/types/component-manifest.ts` with TypeScript interfaces for loading the manifest. This provides typed access for Phase 5 migration and Phase 6 HierarchyClassifier.

Follow the YAML loading pattern from `workflow-loader.ts` (uses `yaml` package `parse()` + `fs.readFileSync`).

```typescript
/**
 * Component Manifest Types
 * 
 * TypeScript interfaces for the component-manifest.yaml file.
 * The manifest is the authoritative source of truth for the L1/L2 component hierarchy.
 * 
 * Consumed by:
 * - Phase 5: Migration script (to create scaffold nodes)
 * - Phase 6: HierarchyClassifier (to assign entities to components)
 */

import * as fs from 'fs';
import * as path from 'path';
import { parse } from 'yaml';

/** A single component or sub-component entry in the manifest */
export interface ComponentManifestEntry {
  /** PascalCase component name */
  name: string;
  /** Hierarchy level: 1 for Component, 2 for SubComponent */
  level: number;
  /** Human-readable description of the component's scope */
  description: string;
  /** Alternative names for this component */
  aliases: string[];
  /** Keywords used for heuristic entity classification */
  keywords: string[];
  /** L2 sub-components (empty array if none) */
  children?: ComponentManifestEntry[];
}

/** The project root entry */
export interface ProjectEntry {
  /** Project name (always "Coding") */
  name: string;
  /** Always 0 for project root */
  level: number;
  /** Project description */
  description: string;
}

/** Top-level manifest structure */
export interface ComponentManifest {
  /** Manifest schema version */
  version: string;
  /** Project root node */
  project: ProjectEntry;
  /** L1 component definitions */
  components: ComponentManifestEntry[];
}

/**
 * Load the component manifest from the config directory.
 * Uses the same config directory resolution as workflow-loader.ts.
 * 
 * @param configDir - Optional override for config directory path
 * @returns Parsed ComponentManifest
 * @throws Error if manifest file not found
 */
export function loadComponentManifest(configDir?: string): ComponentManifest {
  const dir = configDir || path.resolve(__dirname, '../../config');
  const manifestPath = path.join(dir, 'component-manifest.yaml');
  if (!fs.existsSync(manifestPath)) {
    throw new Error(`Component manifest not found: ${manifestPath}`);
  }
  const content = fs.readFileSync(manifestPath, 'utf-8');
  return parse(content) as ComponentManifest;
}

/**
 * Flatten all components and sub-components into a single array.
 * Useful for iterating over all hierarchy nodes.
 * 
 * @param manifest - The loaded component manifest
 * @returns Array of all entries (L1 and L2) with their hierarchy info
 */
export function flattenManifestEntries(manifest: ComponentManifest): ComponentManifestEntry[] {
  const entries: ComponentManifestEntry[] = [];
  for (const component of manifest.components) {
    entries.push(component);
    if (component.children) {
      for (const child of component.children) {
        entries.push(child);
      }
    }
  }
  return entries;
}
```

Verify TypeScript compiles: `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && python3 -c "import json; d=json.load(open('.data/ontologies/lower/coding-ontology.json')); print('Component' in d.get('entities',{}), 'SubComponent' in d.get('entities',{}))" && cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    - coding-ontology.json contains Component entity type with description, properties (componentName, aliases, keywords, level), requiredProperties, examples
    - coding-ontology.json contains SubComponent entity type with description, properties (componentName, parentComponent, level), requiredProperties, examples
    - Neither Component nor SubComponent uses extendsEntity
    - component-manifest.ts exists with exported ComponentManifestEntry, ComponentManifest, loadComponentManifest, flattenManifestEntries
    - TypeScript compiles with 0 errors in MCP server submodule
    - JSON ontology file parses without errors
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "import yaml; m=yaml.safe_load(open('integrations/mcp-server-semantic-analysis/config/component-manifest.yaml')); assert len(m['components']) == 8"` -- manifest has 8 components
2. `python3 -c "import json; d=json.load(open('.data/ontologies/lower/coding-ontology.json')); assert 'Component' in d['entities']; assert 'SubComponent' in d['entities']"` -- ontology has both types
3. `grep -c 'extendsEntity' .data/ontologies/lower/coding-ontology.json` should NOT show Component or SubComponent extending anything
4. `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit` exits with 0 errors
5. `grep 'export interface ComponentManifest' integrations/mcp-server-semantic-analysis/src/types/component-manifest.ts` returns a match
6. `grep 'export function loadComponentManifest' integrations/mcp-server-semantic-analysis/src/types/component-manifest.ts` returns a match
</verification>

<success_criteria>
- component-manifest.yaml exists at the correct bind-mounted path with all 8 L1 components and 5 L2 sub-components
- coding-ontology.json extended with Component and SubComponent types (no extendsEntity, valid JSON)
- TypeScript manifest loader types compile cleanly and export interfaces + loader function
- All content matches locked user decisions from CONTEXT.md (component names, L2 children, PascalCase naming)
</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-configuration-foundation/04-02-SUMMARY.md`
</output>
