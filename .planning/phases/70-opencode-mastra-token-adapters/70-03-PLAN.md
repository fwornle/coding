---
phase: 70-opencode-mastra-token-adapters
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md
autonomous: true
requirements: [ADAPT-04]
must_haves:
  truths:
    - "Mastra's instrumentation surface is identified and written down: per-step middleware vs observer hooks vs framework callbacks, plus the granularity tier each exposes"
    - "The D-08 fork is RESOLVED with evidence: proxy-route (endpoint is redirectable) OR framework-instrumentation fallback (not redirectable)"
    - "The chosen path's granularity tier is stated (per-llm-call if redirectable; best-available with per-session-aggregate floor if fallback)"
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md"
      provides: "the D-09 instrumentation-surface finding + the D-08 fork resolution that conditions Plan 04"
      contains: "Resolution:"
  key_links:
    - from: "70-MASTRA-SURFACE.md Resolution"
      to: "Plan 04 (Mastra implementation)"
      via: "decision record (proxy-route vs fallback)"
      pattern: "proxy-route|fallback"
---

<objective>
Resolve the D-08 fork for Mastra and deliver the D-09 instrumentation-surface finding (ADAPT-04 SC-3). Determine whether Mastra's model endpoint is redirectable to the proxy shim (proxy-route, preferred) or whether the framework-instrumentation fallback is required, and write the finding + decision into `70-MASTRA-SURFACE.md` so Plan 04 executes the resolved path without re-investigating.

Purpose: D-09 makes "identify Mastra's instrumentation surface + granularity tier" a deliverable regardless of which path is taken. This plan is the investigation/decision task; Plan 04 is the implementation conditioned on this output.

Output: `70-MASTRA-SURFACE.md` — a decision record naming the two Mastra surfaces, the redirect-capability verdict per surface, the resolved path, and the achievable granularity tier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-opencode-mastra-token-adapters/70-CONTEXT.md
@.planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disambiguate the two Mastra surfaces and probe redirect capability</name>
  <files>/Users/Q284340/Agentic/coding/.opencode/mastra.json, /Users/Q284340/Agentic/coding/config/agents/mastra.sh</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.opencode/mastra.json (the observation/reflection memory-engine config: model anthropic/claude-haiku-4-5, storagePath, token budgets)
    - /Users/Q284340/Agentic/coding/config/agents/mastra.sh (:166-191 — the mastracode proxy-reachability probe, MASTRA_MODEL wiring, the aspirational "all calls route through proxy" comment that sets NO redirect env)
    - .planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md (the ".opencode/mastra.json — SC-3 instrumentation-surface investigation" section: the two surfaces (a) memory engine vs (b) mastracode TUI, the @opencode-ai/plugin@1.3.17 type-stubs-only finding, the no-baseURL-redirect-currently-set finding, and the concrete investigation checklist)
    - .planning/phases/70-opencode-mastra-token-adapters/70-CONTEXT.md (D-08 proxy-route-if-redirectable, D-09 surface deliverable regardless of path)
  </read_first>
  <action>This task is investigation-only — it READS the source surfaces (`.opencode/mastra.json`, `config/agents/mastra.sh`, node_modules) and GATHERS evidence; Task 2 is the sole writer of `70-MASTRA-SURFACE.md`. Investigate per the PATTERNS.md checklist. (1) Disambiguate the two surfaces: surface (a) the `.opencode/mastra.json` observation/reflection memory engine (model `anthropic/claude-haiku-4-5`), and surface (b) the `mastracode` standalone TUI launched via `scripts/launch-mastra.sh` then `config/agents/mastra.sh`. (2) For surface (a): locate the package that consumes `.opencode/mastra.json` at runtime and determine whether it resolves the `anthropic/...` model through an SDK whose base URL is overridable via env (`ANTHROPIC_BASE_URL` / `OPENAI_BASE_URL` / a config `baseURL` field). Inspect the installed package(s) under node_modules (the known `@opencode-ai/plugin` is type-stubs only at 27 bytes dist/index.js, so identify the actual runtime consumer). (3) For surface (b) mastracode: check whether `MASTRA_MODEL` can be set to a shim-served id and/or whether a base-URL env (`ANTHROPIC_BASE_URL`) exported from `config/agents/mastra.sh` would route its calls through `http://localhost:12435/v1`. Use ctx7 (resolve "Mastra") if the framework instrumentation/telemetry surface needs doc confirmation. Use process.stderr.write for any probe-script logging (never console.log). Record the evidence (each surface's redirect-capability verdict + backing observation) so Task 2 can transcribe it into the decision record — this task writes NO file.</action>
  <verify>
    <automated>test -f /Users/Q284340/Agentic/coding/.opencode/mastra.json && test -f /Users/Q284340/Agentic/coding/config/agents/mastra.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the two Mastra surfaces are each named with their config/launch path and model id.
    - Behavior assertion: for each surface, the redirect-capability verdict is backed by a concrete observation (env var that exists/doesn't, SDK that honours baseURL, or a probe result) — not a guess.
    - CLI output: any redirect probe (e.g. setting ANTHROPIC_BASE_URL=http://localhost:12435/v1 and observing whether the call lands a token_usage row) is recorded with its actual result.
    - Gate note: Task 1's automated verify only confirms the two SOURCE files exist (this task investigates them, it does not produce the decision). The real enforcement gate for the investigation's conclusions is Task 2's verify, which requires `70-MASTRA-SURFACE.md` to contain a `Resolution:` line + granularity tier.
  </acceptance_criteria>
  <done>Both Mastra surfaces are disambiguated and each carries an evidence-backed redirect-capability verdict, gathered (not yet written) for Task 2 to record.</done>
</task>

<task type="auto">
  <name>Task 2: Write the D-08 resolution + D-09 surface finding into 70-MASTRA-SURFACE.md</name>
  <files>/Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md</files>
  <read_first>
    - the Task 1 evidence (the disambiguated surfaces + per-surface redirect verdicts gathered in this plan's first task)
    - .planning/phases/70-opencode-mastra-token-adapters/70-CONTEXT.md (D-08, D-09, D-10 best-available-tier with per-session-aggregate floor)
  </read_first>
  <action>Write `70-MASTRA-SURFACE.md` as a decision record with these sections: (1) Surfaces — a table of the two Mastra surfaces (config path, model, role). (2) Instrumentation surface (D-09 deliverable) — for the chosen surface, name the instrumentation type (per-step middleware vs observer hooks vs framework callbacks) and the granularity tier it exposes (per-llm-call / per-step / per-session-aggregate). (3) Resolution — a single explicit line in the exact form `Resolution: proxy-route` OR `Resolution: fallback`, with the evidence that drove it (redirectable means proxy-route reusing the Plan-01 shim with `agent='mastra'` set via the shim's X-Agent / body.agent override; not redirectable means framework-instrumentation fallback per D-10/D-11). (4) Achievable granularity tier — per-llm-call if proxy-route; if fallback, the best-available tier with per-session-aggregate as the guaranteed floor (D-10). (5) Plan 04 directive — which of Plan 04's two task tracks executes (Track A proxy-route or Track B fallback) and any surface-specific notes. For Track A (proxy-route), the directive MUST name BOTH the exact redirect seam (the env var / config field that points Mastra at `http://localhost:12435/v1`) AND the exact agent-stamp mechanism (send `X-Agent: mastra` header — or `agent: 'mastra'` in the body — to the Plan-01 shim, which reads it via the same precedence as X-Task-Id). For Track B (fallback), name the exact framework hook to instrument. Keep it factual and concise; this file is the contract Plan 04 reads. Do not use console.log in any helper.</action>
  <verify>
    <automated>grep -qE "Resolution:[[:space:]]*(proxy-route|fallback)" /Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md && grep -qi "granularity" /Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: 70-MASTRA-SURFACE.md contains exactly one `Resolution: proxy-route` OR `Resolution: fallback` line.
    - Source assertion: the file states the achievable granularity tier and the D-09 instrumentation-surface type.
    - Behavior assertion: the "Plan 04 directive" section unambiguously selects Track A (proxy-route) or Track B (fallback); if Track A, it names both the redirect seam and the `X-Agent: mastra` (or `body.agent`) stamp mechanism the Plan-01 shim consumes.
  </acceptance_criteria>
  <done>70-MASTRA-SURFACE.md records the D-09 surface finding, the D-08 Resolution line, the achievable tier, and the Plan 04 track directive (including the X-Agent stamp mechanism for Track A).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| investigator → installed packages / config | Read-only inspection of node_modules + config files; no new attack surface introduced |
| redirect probe → proxy /v1 | A probe MAY route a Mastra call through the loopback proxy (same surface as Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-07 | Information Disclosure | a redirect probe sends real Mastra prompt content through the proxy | accept | The proxy is the loopback DB owner and already logs all /api/complete traffic; a probe call is a single local telemetry row, no off-host disclosure (loopback binding confirmed in Plan 01 T-70-03). Keep probe prompts trivial/non-sensitive. |
| T-70-08 | Tampering | setting ANTHROPIC_BASE_URL globally could misroute other agents' calls | mitigate | Any redirect probe sets the env var scoped to the single probe invocation (inline `ENV=... command`), NOT exported into the shell/launchd. Record exactly where any redirect env would live before Plan 04 makes it permanent. |
| T-70-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing (read-only investigation). If investigation requires installing a Mastra package to inspect it, treat as [ASSUMED] and add a blocking-human legitimacy checkpoint first. |
</threat_model>

<verification>
- 70-MASTRA-SURFACE.md exists with a single `Resolution: proxy-route|fallback` line.
- The D-09 instrumentation surface + granularity tier are documented (deliverable regardless of path).
- The Plan 04 track directive (A proxy-route / B fallback) is unambiguous; Track A names the X-Agent stamp mechanism.
</verification>

<success_criteria>
- ADAPT-04 SC-3: Mastra's instrumentation surface is identified and its granularity tier determined. ✓ on Task 2.
- D-08 fork resolved with evidence, conditioning Plan 04's execution path.
</success_criteria>

<output>
Create `.planning/phases/70-opencode-mastra-token-adapters/70-03-SUMMARY.md` when done. Record: the two surfaces, the redirect verdicts with evidence, the Resolution, the achievable granularity tier, and which Plan 04 track is selected.
</output>
</content>
</invoke>
