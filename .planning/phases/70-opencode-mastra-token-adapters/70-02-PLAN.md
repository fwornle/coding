---
phase: 70-opencode-mastra-token-adapters
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - /Users/Q284340/.config/opencode/opencode.json
autonomous: false
requirements: [ADAPT-03]
must_haves:
  truths:
    - "OpenCode is configured with a custom OpenAI-compatible provider whose baseURL is http://localhost:12435/v1"
    - "The existing OpenCode setup (github-copilot) is not broken — the proxy provider is additive with a documented switch"
    - "A real OpenCode session driving an LLM call through the proxy lands a token_usage row with agent='opencode', granularity_tier='per-llm-call', and the active task_id"
  artifacts:
    - path: "/Users/Q284340/.config/opencode/opencode.json"
      provides: "custom OpenAI-compatible provider pointing at the proxy shim"
      contains: "12435/v1"
  key_links:
    - from: "opencode.json provider.options.baseURL"
      to: "proxy shim POST /v1/chat/completions"
      via: "@ai-sdk/openai-compatible provider config"
      pattern: "12435/v1"
    - from: "OpenCode LLM call"
      to: "token_usage row (agent='opencode')"
      via: "proxy shim logTokenCall"
      pattern: "agent.*opencode"
---

<objective>
Configure OpenCode (host-side `~/.config/opencode/opencode.json`) with a custom OpenAI-compatible provider pointing at the proxy shim (`http://localhost:12435/v1`), then prove end-to-end via a live human-verified gate that a real OpenCode call lands a correctly-stamped `token_usage` row (D-01, D-03; ADAPT-03 SC-1/SC-2 full closure).

Purpose: The shim from Plan 01 is useless until OpenCode actually routes through it. This plan flips OpenCode's provider config and runs the blocking live gate the phase's `<specifics>` requires.

Output: An additive provider entry in opencode.json + a documented switch, and a verified row in token-usage.db proving the full OpenCode→proxy→row path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-opencode-mastra-token-adapters/70-CONTEXT.md
@.planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md
@.planning/phases/70-opencode-mastra-token-adapters/70-01-SUMMARY.md

<interfaces>
<!-- Current opencode.json top-level shape (host-side, ~/.config/opencode/opencode.json): -->
  { "$schema":"https://opencode.ai/config.json",
    "model":"github-copilot/claude-opus-4.6",
    "enabled_providers":["github-copilot"],
    "agent": { "compaction": { "model":"github-copilot/gpt-5.4" } },
    "permission": {...}, "mcp": {...} }

<!-- OpenCode custom-provider schema (validate against the $schema URL via ctx7/WebFetch): -->
  "provider": {
    "<id>": { "npm":"@ai-sdk/openai-compatible", "name":"<label>",
              "options": { "baseURL":"http://localhost:12435/v1", "apiKey":"<placeholder>" },
              "models": { "<model-id>": { "name":"<label>" } } }
  }

<!-- Shim contract (from Plan 01): POST http://localhost:12435/v1/chat/completions accepts
     OpenAI { model, messages } and returns a chat.completion envelope; stamps
     agent='opencode', granularity_tier='per-llm-call', active-or-override task_id. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the custom OpenAI-compatible proxy provider to opencode.json (additive)</name>
  <files>/Users/Q284340/.config/opencode/opencode.json</files>
  <read_first>
    - /Users/Q284340/.config/opencode/opencode.json (the full current file — model, enabled_providers, agent, permission, mcp blocks)
    - .planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md (the "~/.config/opencode/opencode.json — custom OpenAI-compatible provider" section: additive edit, the provider.<id> schema shape, port discipline)
    - .planning/phases/70-opencode-mastra-token-adapters/70-CONTEXT.md (D-03 host-side localhost:12435/v1 canonical; Claude's Discretion: replace-vs-add — prefer additive + documented switch)
  </read_first>
  <action>Confirm the exact OpenCode custom-provider schema against the file's `$schema` (`https://opencode.ai/config.json`) using ctx7 (resolve "OpenCode") or WebFetch on the schema URL before editing — the shape is `provider.<id> = { npm, name, options:{ baseURL, apiKey }, models:{...} }`. Add a new `provider` block (do NOT remove the existing `github-copilot` entries) with an id like `rapid-proxy`, `npm:"@ai-sdk/openai-compatible"`, `options.baseURL:"http://localhost:12435/v1"` (D-03 canonical host-side; NEVER 3033), a placeholder `options.apiKey` (the shim does not require a real key — use a non-empty placeholder so the SDK is satisfied), and a `models` map keyed by the model id OpenCode should request (pass-through to the proxy chain, e.g. `claude-opus-4.6` or `claude-haiku-4-5`). Per Claude's Discretion + guardrail (do NOT break the existing setup): keep this ADDITIVE — leave `enabled_providers` including `github-copilot`, add the new provider id to `enabled_providers`, and document the switch (how to flip `model` to the proxy provider) in the SUMMARY rather than hard-replacing the default `model`. Preserve all existing keys (agent, permission, mcp) byte-for-byte. Validate the file is still valid JSON after editing.</action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/Users/Q284340/.config/opencode/opencode.json')); assert 'provider' in d; assert any('12435/v1' in (p.get('options',{}).get('baseURL','')) for p in d['provider'].values()); assert 'github-copilot' in d.get('enabled_providers',[]); print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: opencode.json parses as valid JSON and contains a `provider.<id>.options.baseURL` equal to `http://localhost:12435/v1`.
    - Source assertion: `enabled_providers` still contains `github-copilot` (existing setup not broken) AND now contains the new proxy provider id.
    - Source assertion: the existing `agent`, `permission`, and `mcp` blocks are unchanged (diff shows only additive provider/enabled_providers changes).
  </acceptance_criteria>
  <done>opencode.json carries an additive @ai-sdk/openai-compatible provider at localhost:12435/v1, github-copilot remains enabled, all other blocks intact, file is valid JSON.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: LIVE GATE — drive a real OpenCode call through the proxy and confirm the stamped row</name>
  <what-built>
    Plan 01 added the proxy shim POST /v1/chat/completions (agent='opencode', granularity_tier='per-llm-call', active task_id). Task 1 added a custom OpenAI-compatible OpenCode provider pointing at it. This gate proves the full OpenCode→proxy→token_usage path end-to-end with a real OpenCode session (which the harness cannot autonomously drive).
  </what-built>
  <read_first>
    - .planning/phases/70-opencode-mastra-token-adapters/70-01-SUMMARY.md (the shim contract + the observed proxy binding from T-70-03)
    - /Users/Q284340/Agentic/coding/scripts/backfill-task-id-by-timestamp.mjs (:44-64,:193-200 — the resolveDbPath / better-sqlite3 read pattern for token-usage.db)
  </read_first>
  <how-to-verify>
    1. Ensure the proxy is running and serving the shim: `curl -s http://localhost:12435/health` returns OK (if not: `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`).
    2. (Optional but recommended) Start a measurement span so the row carries a non-empty task_id, e.g. via the Phase-68 start-measurement path, OR proceed without a span (row task_id='' is acceptable — note which).
    3. Flip OpenCode to the proxy provider for one run: set `model` to the new provider's `<id>/<model>` (per the documented switch in the 70-02-SUMMARY draft), OR launch OpenCode selecting that provider/model.
    4. Run ONE real OpenCode interaction that triggers an LLM call (e.g. ask it a trivial question).
    5. Inspect the newest token_usage row:
       `sqlite3 /Users/Q284340/Agentic/coding/.data/llm-proxy/token-usage.db "SELECT agent, granularity_tier, task_id, model, total_tokens, timestamp FROM token_usage ORDER BY rowid DESC LIMIT 3;"`
    6. Confirm the latest OpenCode-driven row shows `agent=opencode`, `granularity_tier=per-llm-call`, the active `task_id` (or '' if no span was started), and non-zero/real token counts.
    7. Confirm the existing github-copilot path still works (switch `model` back or start a normal OpenCode run) — the additive change did not break the default.
  </how-to-verify>
  <acceptance_criteria>
    - Behavior assertion: the newest OpenCode-driven row in token-usage.db has `agent='opencode'` AND `granularity_tier='per-llm-call'`.
    - Behavior assertion: that row's `task_id` matches the active span (non-empty when a span was running) or '' (when none) — the chosen case is recorded.
    - Behavior assertion: a subsequent normal (github-copilot) OpenCode run still works — existing setup not broken.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the observed row (agent/granularity_tier/task_id), or describe the failure (e.g. wrong port, provider schema rejected, no row written).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenCode client → proxy /v1/chat/completions | OpenCode (host-side, trusted local agent) sends LLM requests to the loopback proxy |
| opencode.json on disk | Local config file carrying provider baseURL + placeholder apiKey |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | Information Disclosure | placeholder apiKey stored in opencode.json | accept | The shim does not validate the key; a non-secret placeholder is used. No real credential is added to the file (the proxy holds the real provider keys server-side). Verify the placeholder is not a real key before committing. |
| T-70-05 | Tampering / Spoofing | OpenCode redirected to a baseURL other than the intended loopback proxy | mitigate | The baseURL is pinned to `http://localhost:12435/v1` (loopback, the proxy this phase owns). Task 1 acceptance asserts the exact baseURL string. Port discipline (CLAUDE.md): 12435 is the LLM proxy, NOT 3033 (Health API). |
| T-70-06 | Denial of Service | flipping OpenCode's default model breaks the working setup | mitigate | Change is additive (D-03 Claude's Discretion guardrail); github-copilot stays in enabled_providers; the live gate (Task 2) re-confirms the default path still works before sign-off. |
| T-70-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan (config-only edit). `@ai-sdk/openai-compatible` is referenced by name in OpenCode's own provider config and resolved by OpenCode, not installed by this plan. If an install is introduced, add a blocking-human legitimacy checkpoint. |
</threat_model>

<verification>
- opencode.json is valid JSON, provider added additively, github-copilot still enabled.
- Live gate (Task 2) confirms a real OpenCode call lands a token_usage row with agent='opencode', granularity_tier='per-llm-call', active task_id.
- Existing OpenCode (github-copilot) path still works.
</verification>

<success_criteria>
- ADAPT-03 SC-1 (config half): OpenCode is configured to route LLM calls through the proxy. ✓ on Task 1 acceptance.
- ADAPT-03 SC-1/SC-2 (end-to-end): a per-llm-call row with the active task_id is produced by a real OpenCode call. ✓ on Task 2 live gate.
- ADAPT-03 fully discharged across Plans 01+02.
</success_criteria>

<output>
Create `.planning/phases/70-opencode-mastra-token-adapters/70-02-SUMMARY.md` when done. Record: the provider id added, the exact baseURL, the documented replace-vs-add switch, the live-gate row observed (agent/granularity_tier/task_id/model/tokens), whether a span was active, and confirmation the github-copilot path still works.
</output>
