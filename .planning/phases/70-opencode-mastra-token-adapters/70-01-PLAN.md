---
phase: 70-opencode-mastra-token-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs
autonomous: true
requirements: [ADAPT-03]
must_haves:
  truths:
    - "POSTing an OpenAI chat/completions body to the proxy at /v1/chat/completions returns a chat.completion-shaped JSON response"
    - "A call through the shim writes a token_usage row with agent='opencode' and granularity_tier='per-llm-call'"
    - "The shim stamps the active task_id (resolveLiveTaskId() result) on the row, and a client-supplied X-Task-Id / body.task_id override wins when present"
    - "Existing /api/complete callers that omit agent/task_id keep their current behaviour (backward compatible)"
  artifacts:
    - path: "/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs"
      provides: "new POST /v1/chat/completions shim branch + generic agent/granularity_tier/task_id envelope passthrough on the logTokenCall build"
      contains: "/v1/chat/completions"
  key_links:
    - from: "server.mjs /v1/chat/completions branch"
      to: "server.mjs /api/complete provider chain + logTokenCall build"
      via: "internal body normalization then fall-through to the shared pipeline"
      pattern: "v1/chat/completions"
    - from: "logTokenCall row build"
      to: "body.agent / body.granularity_tier / body.task_id"
      via: "generic envelope passthrough (same as body.process / body.subscription)"
      pattern: "body\\.agent"
---

<objective>
Add an OpenAI-compatible shim endpoint `POST /v1/chat/completions` to the rapid-llm-proxy that maps onto the existing `/api/complete` pipeline, and make the `logTokenCall` row build stamp `agent`, `granularity_tier`, and `task_id` via generic envelope passthrough (D-01, D-02, D-04, D-05, D-06).

Purpose: This is the foundational proxy change for ADAPT-03. The shim lets OpenCode (and later Mastra, if redirectable) route LLM calls through the proxy so every call is logged at `per-llm-call` granularity for free, reusing the proxy's existing provider chain and token logging. The generic passthrough means one code path serves `opencode`, `mastra`, and any future proxy-routed agent with no per-agent branches.

Output: A modified `server.mjs` with the new shim branch and the passthrough plumbing. No schema change, no second writer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-opencode-mastra-token-adapters/70-CONTEXT.md
@.planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md

<interfaces>
<!-- The shim builds an INTERNAL body for the existing pipeline. Key contracts the executor needs: -->

Proxy /api/complete request body (the internal envelope the shim builds and falls through):
  { model: string, messages: [{role, content}], process?: string, subscription?: string,
    agent?: string, granularity_tier?: string, task_id?: string }

Proxy /api/complete result (server.mjs:1701-1712 returns):
  { content: string, provider: string, model: string,
    tokens: { input, output, total }, latencyMs: number, overheadMs?: number }

resolveLiveTaskId() (imported at server.mjs:43 from ../dist/measurement-span.js):
  returns the active span's task_id, or '' when no span / on any read error. Never throws.

logTokenCall row fields already plumbed (token-usage.ts:122-127, bind at :812-817):
  agent?, task_id?, tool_call_id?, parent_call_id?, granularity_tier?, reasoning_tokens?
  — ALL coalesce to ''/0 in the bind; no schema change needed.

OpenAI chat.completion response envelope the shim must emit:
  { id, object:'chat.completion', created, model,
    choices:[{ index:0, message:{role:'assistant', content}, finish_reason:'stop' }],
    usage:{ prompt_tokens, completion_tokens, total_tokens } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generic agent/granularity_tier/task_id envelope passthrough to the logTokenCall build</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (the logTokenCall build at :1668-1698, the existing process/subscription passthrough at :1673-1676, and the task_id: resolveLiveTaskId() line at :1696)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts (:122-127 row type, :812-817 bind defaults — confirm agent/task_id/granularity_tier already plumbed; expect ZERO edits here)
    - .planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md (the "Generic envelope passthrough" shared pattern + the logTokenCall code excerpt)
  </read_first>
  <behavior>
    - When the logTokenCall build runs, `row.agent` is set from `body.agent` when it is a non-empty string, else ''.
    - `row.granularity_tier` is set from `body.granularity_tier` when non-empty string, else '' (default unchanged for existing callers).
    - `row.task_id` follows precedence: `body.task_id` (non-empty string) wins; else `resolveLiveTaskId()`. Existing /api/complete callers that omit task_id keep the current `resolveLiveTaskId()` behaviour.
  </behavior>
  <action>In the existing logTokenCall row build (server.mjs:1668-1698, inside the `if (_tokenDb)` guard shared by /api/complete), add two fields stamped the SAME way `process` and `subscription` are already threaded from `body` (server.mjs:1673-1676): `agent: typeof body.agent === 'string' && body.agent ? body.agent : ''` and `granularity_tier: typeof body.granularity_tier === 'string' && body.granularity_tier ? body.granularity_tier : ''`. Change the existing `task_id: resolveLiveTaskId()` (server.mjs:1696) to a precedence expression: use `body.task_id` when it is a non-empty string, else `resolveLiveTaskId()` (per D-04/D-05 — client override wins, otherwise the active span; backward-compatible because existing callers omit the field). Do NOT touch the schema, the insert bind order, or token-usage.ts — the fields are already bound with ''/0 defaults. Use no console.log; this region already uses the guarded best-effort path.</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && grep -v '^[[:space:]]*//' proxy-bridge/server.mjs | grep -c "body\.agent" | grep -qv '^0$' && grep -v '^[[:space:]]*//' proxy-bridge/server.mjs | grep -q "body\.task_id" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^[[:space:]]*//' proxy-bridge/server.mjs | grep -c "body\.agent"` returns >= 1 (passthrough is live code, not a comment).
    - Source assertion: the `task_id:` field in the logTokenCall build references `body.task_id` with a `resolveLiveTaskId()` fallback (precedence, not hardcoded).
    - Behavior assertion: `token-usage.ts` is unchanged (no schema edit) — `git -C /Users/Q284340/Agentic/_work/rapid-llm-proxy diff --name-only` does NOT list `src/token-usage.ts`.
  </acceptance_criteria>
  <done>The logTokenCall build stamps row.agent and row.granularity_tier from the envelope generically, and task_id uses body.task_id-then-resolveLiveTaskId() precedence; token-usage.ts untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the POST /v1/chat/completions shim branch</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs (the /api/complete branch at :1518-1528 raw-body read + JSON-parse + 400 guard; the provider-chain resolution at :1577-1626; the result shape at :1701-1712; the imports at :37,:43)
    - .planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md (the "new /v1/chat/completions shim" section — branch placement, body parse pattern, core IN/OUT mapping, D-02 non-streaming-only)
  </read_first>
  <behavior>
    - POST /v1/chat/completions with `{ model, messages:[{role,content}] }` returns HTTP 200 and a JSON body with `object:'chat.completion'`, a `choices[0].message.content` carrying the provider result content, and a `usage` block with `prompt_tokens`/`completion_tokens`/`total_tokens`.
    - The shim sets the internal body's `agent='opencode'`, `granularity_tier='per-llm-call'`, and `task_id = (X-Task-Id header || body.task_id) ?? resolveLiveTaskId()` before falling through the existing pipeline, so the logged row carries those values (via Task 1's passthrough).
    - Malformed JSON body returns HTTP 400 `{ error: 'Invalid JSON' }`, matching the /api/complete guard.
    - Non-streaming only (D-02): a single buffered JSON response, no SSE; if `body.stream === true` the shim still returns a single buffered chat.completion (stream ignored, documented as deferred).
  </behavior>
  <action>Add a new branch `if (req.method === 'POST' && req.url === '/v1/chat/completions')` beside the existing /api/complete branch (server.mjs:1518). Reuse the identical raw-body read loop + `JSON.parse` + 400-on-bad-JSON guard. Build an INTERNAL `/api/complete`-shaped body: pass `body.model` straight through (Claude's Discretion guardrail — add NO new routing logic; let the existing chain at :1577-1626 resolve it), pass `body.messages` through (role/content shape is already compatible), and set `agent='opencode'`, `granularity_tier='per-llm-call'`, and `task_id` from the override precedence: read an `X-Task-Id` request header (or `body.task_id` body field) and use it when non-empty, else leave task_id unset so Task 1's `body.task_id ?? resolveLiveTaskId()` resolves the active span. Run the SAME provider-chain resolution + completion call + logTokenCall path the /api/complete branch uses (do not duplicate the pipeline — invoke the shared resolution; if the branch must inline it, mirror :1577-1626 + the completion call + the :1668 logTokenCall build verbatim with the internal body). Map the proxy result (`{ content, provider, model, tokens:{input,output,total}, latencyMs }`) into the OpenAI envelope: `{ id: 'chatcmpl-'+<random/uuid>, object:'chat.completion', created: Math.floor(Date.now()/1000), model: result.model, choices:[{ index:0, message:{ role:'assistant', content: result.content }, finish_reason:'stop' }], usage:{ prompt_tokens: result.tokens?.input ?? 0, completion_tokens: result.tokens?.output ?? 0, total_tokens: result.tokens?.total ?? 0 } }`. Non-streaming only — return a single buffered JSON (D-02); ignore `body.stream` for now. Use process.stderr.write for any forensic logging (no console.log).</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/_work/rapid-llm-proxy && node --check proxy-bridge/server.mjs && grep -q "v1/chat/completions" proxy-bridge/server.mjs && grep -q "chat.completion" proxy-bridge/server.mjs && grep -q "per-llm-call" proxy-bridge/server.mjs && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (live): with the proxy running, `curl -s -X POST http://localhost:12435/v1/chat/completions -H 'Content-Type: application/json' -d '{"model":"claude-haiku-4-5","messages":[{"role":"user","content":"reply with the single word ok"}]}'` returns JSON containing `"object":"chat.completion"` and a non-empty `choices[0].message.content`. (Run after the launchd restart in Task 3.)
    - Behavior assertion (live): `curl -s -X POST http://localhost:12435/v1/chat/completions -H 'Content-Type: application/json' -d 'not json'` returns HTTP 400 with `Invalid JSON`.
    - Source assertion: `node --check proxy-bridge/server.mjs` exits 0; `grep -c "v1/chat/completions" proxy-bridge/server.mjs` >= 1; the shim sets `granularity_tier` to `per-llm-call` and `agent` to `opencode`.
  </acceptance_criteria>
  <done>POST /v1/chat/completions buffers a chat.completion response from the existing pipeline, stamps agent='opencode'/granularity_tier='per-llm-call'/active-or-override task_id on the logged row, and rejects malformed JSON with 400. server.mjs passes node --check.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild (if TS touched) and reload the proxy, then live-confirm the shim logs a row</name>
  <files>/Users/Q284340/Agentic/_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - .planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md (the "Proxy build pipeline (dist/ staleness trap)" + "Port discipline" shared patterns)
    - CLAUDE.md (rapid-llm-proxy endpoint/port notes: /api/complete on 12435, NOT 3033; launchd kickstart convention)
    - /Users/Q284340/Agentic/coding/scripts/backfill-task-id-by-timestamp.mjs (:44-64,:193-200 — the host-side better-sqlite3 open + resolveDbPath pattern, reused only to READ token-usage.db for the row assertion)
  </read_first>
  <action>Determine which surface changed: this plan edits `proxy-bridge/server.mjs` (plain JS — no build step). Only if Task 1/2 unexpectedly required editing `src/token-usage.ts` or `src/measurement-span.ts` (expected: NOT needed), run `npm run build` in /Users/Q284340/Agentic/_work/rapid-llm-proxy first so `dist/` is fresh. Reload the running proxy so the new `.mjs` branch is live: `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`. Wait for the proxy to come back (`curl -s http://localhost:12435/health` returns OK). Then make a real call through the shim with curl and confirm a fresh token_usage row landed with the expected stamps. The DB lives at `.data/llm-proxy/token-usage.db` (resolve via the resolveDbPath pattern). Use port 12435 (the LLM proxy) — NEVER 3033 (Health API returns `Cannot POST` HTML and masks the wrong-port bug per CLAUDE.md). Use process.stderr.write for any helper-script logging.</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && curl -s -X POST http://localhost:12435/v1/chat/completions -H 'Content-Type: application/json' -d '{"model":"claude-haiku-4-5","messages":[{"role":"user","content":"reply ok"}]}' | grep -q '"object":"chat.completion"' && sqlite3 .data/llm-proxy/token-usage.db "SELECT count(*) FROM token_usage WHERE agent='opencode' AND granularity_tier='per-llm-call';" | grep -qv '^0$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: after `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`, `curl -s http://localhost:12435/health` returns a healthy response (not connection-refused, not the 3033 `Cannot POST` HTML).
    - Behavior assertion: a curl to `:12435/v1/chat/completions` is followed by `sqlite3 .data/llm-proxy/token-usage.db "SELECT agent, granularity_tier, task_id FROM token_usage ORDER BY rowid DESC LIMIT 1;"` showing `agent=opencode` and `granularity_tier=per-llm-call` on the newest row.
    - Behavior assertion: the same newest row's `task_id` equals `resolveLiveTaskId()` (empty string is acceptable when no measurement span is active — record which case applied in the SUMMARY).
  </acceptance_criteria>
  <done>The reloaded proxy serves /v1/chat/completions; a real curl through it writes a token_usage row stamped agent='opencode', granularity_tier='per-llm-call', and the active (or empty) task_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → proxy /v1/chat/completions | Untrusted HTTP request body + headers (model, messages, optional X-Task-Id) cross into the proxy and reach the provider chain + token_usage write |
| proxy → token_usage.db | Proxy-owned write; single writer on the proxy-route path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Spoofing | client-supplied X-Task-Id / body.task_id override (D-05) injected into row.task_id | accept | task_id is an attribution label on a local-only telemetry row, not an auth principal. The override is future-proofing; the field is bound as a parameterized string (token-usage.ts insert bind, never interpolated into SQL), so injection cannot escape the column. Worst case: a mis-attributed local telemetry row, re-attributable by the timestamp-join backfill (D-07). No PII, no privilege. |
| T-70-02 | Denial of Service / abuse | unauthenticated shim invokes the full provider chain (cost/abuse) | accept (with binding precondition) | The proxy already exposes /api/complete unauthenticated on the same port; the shim adds no new exposure beyond that established surface. Precondition VERIFIED in Task 3: confirm the proxy listens on loopback only (see T-70-03). Abuse from off-host is prevented by the loopback binding, not by per-request auth. Adding shim auth is out of scope (would also break the existing /api/complete contract). |
| T-70-03 | Information Disclosure / Elevation | the proxy binding (is :12435 bound to loopback only, not 0.0.0.0?) | mitigate | Task 3 acceptance MUST confirm the listener binds to 127.0.0.1/localhost only (e.g. `lsof -nP -iTCP:12435 -sTCP:LISTEN` shows `127.0.0.1:12435`, not `*:12435`). If it binds to 0.0.0.0, this is a HIGH severity finding (unauthenticated provider-chain access from the LAN) — STOP and surface to the operator before shipping the shim. Record the observed binding in the SUMMARY. |
| T-70-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO new packages (shim reuses existing imports at server.mjs:37,43). If any install is introduced during execution, treat as [ASSUMED] and add a blocking-human legitimacy checkpoint before installing. |
</threat_model>

<verification>
- `node --check proxy-bridge/server.mjs` passes.
- Proxy reloads cleanly via launchctl kickstart and answers /health on :12435.
- A real curl through /v1/chat/completions returns a chat.completion envelope AND lands a token_usage row with agent='opencode', granularity_tier='per-llm-call'.
- token-usage.ts / measurement-span.ts unchanged (no schema edit; consume-as-is).
- T-70-03: proxy confirmed bound to loopback only (or HIGH finding surfaced).
</verification>

<success_criteria>
- ADAPT-03 SC-1 (proxy half): the proxy logs each shim call as a `per-llm-call` row. ✓ when Task 3 row assertion passes.
- ADAPT-03 SC-2 (proxy half): the active task_id is passed via the envelope and lands on the row. ✓ when the newest row's task_id matches the precedence rule.
- Backward compatibility: existing /api/complete callers (omitting agent/task_id) keep current behaviour.
</success_criteria>

<output>
Create `.planning/phases/70-opencode-mastra-token-adapters/70-01-SUMMARY.md` when done. Record: the exact line ranges edited, the observed proxy binding (T-70-03), whether a measurement span was active during the Task 3 live check (task_id value observed), and confirmation that token-usage.ts was NOT edited.
</output>
