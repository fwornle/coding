---
phase: 70-opencode-mastra-token-adapters
plan: 04
type: execute
wave: 2
depends_on: ["70-03", "70-01"]
files_modified:
  - /Users/Q284340/Agentic/coding/config/agents/mastra.sh
  - /Users/Q284340/Agentic/coding/.opencode/mastra.json
  - /Users/Q284340/Agentic/coding/scripts/sub-agent-live-opencode.mjs
  - /Users/Q284340/Agentic/coding/lib/token-adapters/mastra-token-emitter.mjs
autonomous: false
requirements: [ADAPT-04]
must_haves:
  truths:
    - "A Mastra LLM call produces a token_usage row on the shared contract with agent='mastra' at the determined granularity tier, stamped with the active task_id"
    - "If proxy-route: Mastra's model endpoint points at the proxy shim and rows are per-llm-call (no separate writer); agent='mastra' is carried via the shim's X-Agent header (Plan 01) — NO server.mjs change in this plan"
    - "If fallback: a try/catch-isolated host-side adapter writes rows with a distinct user_hash, busy_timeout set, and a passing WAL concurrency test, never crashing the LSL path"
  artifacts:
    - path: "/Users/Q284340/Agentic/coding/config/agents/mastra.sh"
      provides: "Track A: redirect env pointing Mastra at the proxy shim + X-Agent:mastra stamp seam (only if Resolution=proxy-route)"
    - path: "/Users/Q284340/Agentic/coding/lib/token-adapters/mastra-token-emitter.mjs"
      provides: "Track B: host-side better-sqlite3 token emitter for Mastra (only if Resolution=fallback)"
    - path: "/Users/Q284340/Agentic/coding/scripts/sub-agent-live-opencode.mjs"
      provides: "Track B: try/catch-isolated emission hook on the existing supervisor (only if fallback)"
  key_links:
    - from: "Mastra LLM call"
      to: "token_usage row (agent='mastra')"
      via: "proxy shim with X-Agent:mastra (Track A) OR host-side better-sqlite3 INSERT (Track B)"
      pattern: "agent.*mastra"
---

<objective>
Land Mastra LLM token spend in `token_usage` on the shared contract with `agent='mastra'` and the active `task_id` (ADAPT-04 SC-4), executing the path resolved by Plan 03's `70-MASTRA-SURFACE.md`:

- **Track A — proxy-route** (Resolution=proxy-route): point Mastra's model endpoint at the Plan-01 shim (`http://localhost:12435/v1`) AND send `X-Agent: mastra` so its calls are logged as `per-llm-call` rows with `agent='mastra'` via the shim's generic agent precedence (Plan 01 Task 2) — no separate writer, NO server.mjs change in this plan (D-08, D-06).
- **Track B — framework-instrumentation fallback** (Resolution=fallback): a host-side `better-sqlite3` emitter (Phase-69 locked write path verbatim — distinct user_hash, busy_timeout + WAL concurrency test, `getActiveMeasurement()` task_id + timestamp-join backfill) packaged as a try/catch-isolated emission hook on the existing `sub-agent-live-opencode` supervisor — no new launchd plist (D-10/D-11).

Both tracks are fully planned so execution is never blocked by which way Plan 03 landed. The executor reads `70-MASTRA-SURFACE.md` first and runs ONLY the resolved track's tasks.

Purpose: completes the all-four-agent reach (ADAPT-04).
Output: Mastra rows in token-usage.db, plus the config/adapter changes for the resolved track.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-opencode-mastra-token-adapters/70-CONTEXT.md
@.planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md
@.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md
@.planning/phases/70-opencode-mastra-token-adapters/70-01-SUMMARY.md
@.planning/phases/69-claude-copilot-token-adapters/69-CONTEXT.md

<interfaces>
<!-- Track A: the Plan-01 shim resolves `agent` by precedence — an incoming `X-Agent` header (or
     body.agent) wins when non-empty, else defaults to 'opencode'. To stamp agent='mastra' on a
     proxy-routed Mastra call, the call must reach /v1/chat/completions with `X-Agent: mastra`
     (or body.agent='mastra'). This is already implemented in Plan 01 Task 2 — Track A does NOT
     edit server.mjs. If Mastra's HTTP client cannot set a custom header, the directive in
     70-MASTRA-SURFACE.md names the alternative seam (e.g. body.agent injection at the redirect). -->

<!-- Track B host-side write path (Phase-69 D-06/D-07, reused verbatim): -->
resolveDataDir() => process.env.LLM_PROXY_DATA_DIR || '/Users/Q284340/Agentic/coding/.data'
resolveDbPath()  => path.join(resolveDataDir(), 'llm-proxy', 'token-usage.db')
open: new Database(dbPath, { readonly:false, fileMustExist:true }); db.pragma('busy_timeout = 5000')
seedLocalSeq pattern (token-usage.ts:588-595): per-user_hash id via MAX(id)+1 for the distinct adapter user_hash
INSERT columns/bind order: token-usage.ts:574-581 (columns) + :788-818 (bind values)
task_id: getActiveMeasurement() live (single reader) + timestamp-join backfill via runSweep/loadArchivedSpans
         (importable from scripts/backfill-task-id-by-timestamp.mjs — isMain guard at :281-295)

<!-- Supervisor failure-isolation discipline to mirror (sub-agent-live-opencode.mjs): -->
trackError() error budget (:170-185); atomic <file>.tmp + renameSync (:122-131);
best-effort import-with-noop-fallback (:191-211)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Read the resolution and select the track</name>
  <files>/Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md (the Resolution line + the Plan 04 directive section)
  </read_first>
  <action>Read 70-MASTRA-SURFACE.md and extract the `Resolution:` line. If `Resolution: proxy-route` execute Track A (Task A1 + A2) and SKIP Track B. If `Resolution: fallback` execute Track B (Task B1 + B2 + B3) and SKIP Track A. Record the selected track at the top of the SUMMARY. Do NOT execute both tracks.</action>
  <verify>
    <automated>grep -qE "Resolution:[[:space:]]*(proxy-route|fallback)" /Users/Q284340/Agentic/coding/.planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the executor states which track (A or B) it is running and why, sourced from the Resolution line.
  </acceptance_criteria>
  <done>The resolved track is identified; only that track's tasks will run.</done>
</task>

<task type="auto">
  <name>Task A1 (Track A — proxy-route only): Point Mastra at the proxy shim and stamp X-Agent:mastra</name>
  <files>/Users/Q284340/Agentic/coding/config/agents/mastra.sh, /Users/Q284340/Agentic/coding/.opencode/mastra.json</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/config/agents/mastra.sh (:166-191 — the proxy-reachability probe + MASTRA_MODEL wiring; the place a redirect env would be exported)
    - /Users/Q284340/Agentic/coding/.opencode/mastra.json (model anthropic/claude-haiku-4-5, token budgets)
    - .planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md (the Plan 04 directive — exact redirect seam AND the X-Agent stamp mechanism identified)
    - .planning/phases/70-opencode-mastra-token-adapters/70-01-SUMMARY.md (confirmation the shim resolves agent via X-Agent/body.agent precedence defaulting to 'opencode')
    - .planning/phases/70-opencode-mastra-token-adapters/70-PATTERNS.md (port discipline: 12435/v1 = shim, NOT 3033)
  </read_first>
  <action>Apply the exact redirect seam named in 70-MASTRA-SURFACE.md's directive (e.g. export `ANTHROPIC_BASE_URL=http://localhost:12435/v1` from config/agents/mastra.sh, and/or set the `.opencode/mastra.json` model/baseURL to the shim) so Mastra's model calls hit the Plan-01 shim. To stamp `agent='mastra'` (instead of the shim's default `'opencode'`), use the Plan-01 shim's X-Agent mechanism — NO server.mjs change is needed in this plan: configure Mastra's HTTP client to send an `X-Agent: mastra` header (or, if the client cannot set custom headers, the `body.agent='mastra'` field) on its calls to `/v1/chat/completions`, exactly as named in the 70-MASTRA-SURFACE.md directive. The shim already reads X-Agent/body.agent with precedence over the 'opencode' default (Plan 01 Task 2), so the row is stamped agent='mastra' with zero proxy edits. Keep the change scoped (do NOT export the redirect env globally in a way that misroutes other agents — scope it to the Mastra launch path). Preserve the existing reachability probe + MASTRA_MODEL logic. Use process.stderr.write for logging (no console.log). Port 12435 only — NEVER 3033. NOTE: if (and only if) 70-MASTRA-SURFACE.md's directive proves the Mastra client can set NEITHER a custom header NOR a body field, STOP and surface to the operator before adding server.mjs to this plan — the X-Agent path is the planned-for mechanism and the expected outcome.</action>
  <verify>
    <automated>grep -q "12435/v1\|ANTHROPIC_BASE_URL" /Users/Q284340/Agentic/coding/config/agents/mastra.sh && grep -qi "x-agent\|agent.*mastra" /Users/Q284340/Agentic/coding/config/agents/mastra.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the resolved redirect seam (env or config) points Mastra at `http://localhost:12435/v1` (loopback shim, not 3033).
    - Source assertion: the Mastra launch path sets `X-Agent: mastra` (or body.agent='mastra') so the shim stamps agent='mastra' via its Plan-01 precedence — and server.mjs is NOT in this plan's files_modified (no proxy edit).
    - Behavior assertion (deferred to Task A2 live gate): a Mastra call lands a row with agent='mastra'.
    - Source assertion: the redirect is scoped to the Mastra launch path (not a global export that misroutes other agents).
  </acceptance_criteria>
  <done>Mastra's model endpoint is pointed at the proxy shim via the resolved seam, scoped to the Mastra launch path, with X-Agent:mastra (or body.agent='mastra') carried to the shim so the row stamps agent='mastra' — no server.mjs change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task A2 (Track A — proxy-route only): LIVE GATE — Mastra call lands an agent='mastra' per-llm-call row</name>
  <what-built>Task A1 redirected Mastra's model endpoint at the Plan-01 proxy shim and set X-Agent:mastra so the shim stamps agent='mastra'. This gate proves a real Mastra run lands the row (the harness cannot autonomously drive a Mastra session).</what-built>
  <read_first>
    - /Users/Q284340/Agentic/coding/scripts/backfill-task-id-by-timestamp.mjs (:44-64,:193-200 — resolveDbPath / better-sqlite3 read pattern to inspect token-usage.db)
    - .planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md (which Mastra surface to exercise)
  </read_first>
  <how-to-verify>
    1. Ensure the proxy serves the shim: `curl -s http://localhost:12435/health` returns OK.
    2. (Optional) Start a measurement span so the row carries a non-empty task_id; else proceed (task_id='' acceptable — note which).
    3. Run a real Mastra interaction that triggers an LLM call (per the surface identified in 70-MASTRA-SURFACE.md — the memory engine or mastracode, whichever the directive targeted).
    4. Inspect the newest rows: `sqlite3 /Users/Q284340/Agentic/coding/.data/llm-proxy/token-usage.db "SELECT agent, granularity_tier, task_id, model, total_tokens, timestamp FROM token_usage ORDER BY rowid DESC LIMIT 3;"`
    5. Confirm the latest Mastra-driven row shows agent='mastra' (NOT 'opencode' — proves the X-Agent stamp worked), granularity_tier='per-llm-call', the active task_id (or ''), and real token counts.
  </how-to-verify>
  <acceptance_criteria>
    - Behavior assertion: the newest Mastra-driven row has agent='mastra' (not 'opencode') AND granularity_tier='per-llm-call' — confirming the X-Agent stamp seam from Task A1 reached the Plan-01 shim.
    - Behavior assertion: that row's task_id matches the active span (or '' when none), case recorded.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the observed row, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task B1 (Track B — fallback only): Build the host-side Mastra token emitter</name>
  <files>/Users/Q284340/Agentic/coding/lib/token-adapters/mastra-token-emitter.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/scripts/backfill-task-id-by-timestamp.mjs (:44-64,:131-172,:193-200,:281-295 — better-sqlite3 open, runSweep userHashFilter + parameterized UPDATE, resolveDbPath, isMain import guard)
    - /Users/Q284340/Agentic/_work/rapid-llm-proxy/src/token-usage.ts (:574-581 insert columns, :588-595 seedLocalSeq per-user MAX(id)+1, :788-818 bind order — match this INSERT shape)
    - /Users/Q284340/Agentic/coding/lib/lsl/adapters/opencode-sqlite.mjs (:168-169 — busy_timeout=5000 pragma)
    - .planning/phases/69-claude-copilot-token-adapters/69-CONTEXT.md (D-03 task_id resolution, D-06 distinct adapter user_hash, D-07 direct better-sqlite3 + WAL test, D-10 best-available tier)
    - .planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md (the instrumentation hook + achievable granularity tier to emit)
  </read_first>
  <behavior>
    - emit(usage) writes one token_usage row with agent='mastra', granularity_tier=<the tier from 70-MASTRA-SURFACE.md, per-session-aggregate floor>, the adapter's distinct user_hash, a seeded per-user id (MAX(id)+1), task_id from getActiveMeasurement() (or '' when no span), and the usage tokens.
    - The DB opens read-write with busy_timeout=5000; an emit failure throws to the caller (the supervisor hook in Task B2 isolates it) but never corrupts the DB.
    - A WAL concurrency test (N concurrent second-process INSERTs against the live DB) reports ok=N busy=0 err=0.
  </behavior>
  <action>Create `lib/token-adapters/mastra-token-emitter.mjs` reusing Phase-69's locked write path verbatim (D-11). Open token-usage.db via the resolveDataDir/resolveDbPath pattern with `{ readonly:false, fileMustExist:true }` and `db.pragma('busy_timeout = 5000')`. Bind its own distinct adapter `user_hash` namespace (NOT the proxy's LLM_PROXY_USER_HASH) per D-06; seed ids per-user via MAX(id)+1 (token-usage.ts:588-595 seedLocalSeq). Build the INSERT matching logCall's column/bind order (token-usage.ts:574-581/:788-818), stamping `agent='mastra'`, `granularity_tier=<actual tier from 70-MASTRA-SURFACE.md; per-session-aggregate as the guaranteed floor per D-10>`, and `task_id` resolved live from `getActiveMeasurement()` (the single reader). Reuse the timestamp-join backfill (runSweep/loadArchivedSpans imported from scripts/backfill-task-id-by-timestamp.mjs via its isMain guard) so out-of-span Mastra rows get re-attributed — do NOT write a second join. Parameterize all SQL (never interpolate user_hash/task_id). Export an `emit(usage)` function and a small WAL concurrency test entry. Use process.stderr.write for logging (no console.log).</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node --check lib/token-adapters/mastra-token-emitter.mjs && grep -q "busy_timeout" lib/token-adapters/mastra-token-emitter.mjs && grep -q "getActiveMeasurement" lib/token-adapters/mastra-token-emitter.mjs && grep -q "mastra" lib/token-adapters/mastra-token-emitter.mjs && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: node --check passes; the emitter opens with busy_timeout=5000, uses a distinct user_hash, and stamps agent='mastra'.
    - Behavior assertion (WAL test): a test running N concurrent second-process INSERTs against the live token-usage.db reports ok=N, busy=0, err=0 (D-07 acceptance criterion). Record the N and result.
    - Behavior assertion: a direct `emit()` call writes a row queryable via `sqlite3 .data/llm-proxy/token-usage.db "SELECT agent, granularity_tier, user_hash FROM token_usage WHERE agent='mastra' ORDER BY rowid DESC LIMIT 1;"` showing agent='mastra' and the distinct user_hash.
    - Source assertion: all SQL is parameterized (no string-interpolated user_hash/task_id).
  </acceptance_criteria>
  <done>mastra-token-emitter.mjs writes agent='mastra' rows on the shared contract with a distinct user_hash, busy_timeout, getActiveMeasurement() task_id, and a passing WAL concurrency test.</done>
</task>

<task type="auto">
  <name>Task B2 (Track B — fallback only): Wire the try/catch-isolated emission hook into the supervisor</name>
  <files>/Users/Q284340/Agentic/coding/scripts/sub-agent-live-opencode.mjs</files>
  <read_first>
    - /Users/Q284340/Agentic/coding/scripts/sub-agent-live-opencode.mjs (:122-131 atomic write, :170-185 trackError error budget, :191-211 best-effort import-with-noop-fallback — mirror these for the emitter)
    - /Users/Q284340/Agentic/coding/lib/token-adapters/mastra-token-emitter.mjs (the emit() surface from Task B1)
    - .planning/phases/69-claude-copilot-token-adapters/69-CONTEXT.md (D-08 extend existing supervisor, no new plist, failure isolation)
  </read_first>
  <action>Add a try/catch-isolated call to the Task B1 emitter into the existing `sub-agent-live-opencode.mjs` supervisor on its existing JSONL/Mastra pass (D-08/D-11: no new launchd plist). Import the emitter with the best-effort import-with-noop-fallback pattern (:191-211) so an init failure degrades to a no-op writer. Wrap each `emit()` call in try/catch that routes any error into the existing `trackError()` error budget (:170-185) — a token-emission failure must NEVER crash the LSL poll. Do not change the daemon's existing LSL behaviour, atomic state-file write, or restart semantics. Use process.stderr.write for logging (no console.log).</action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node --check scripts/sub-agent-live-opencode.mjs && grep -q "mastra-token-emitter" scripts/sub-agent-live-opencode.mjs && grep -q "try" scripts/sub-agent-live-opencode.mjs && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: node --check passes; the supervisor imports the emitter via the noop-fallback pattern and calls emit() inside a try/catch that feeds trackError().
    - Behavior assertion: a forced emit() throw does NOT crash the supervisor (the catch feeds the error budget) — demonstrate by temporarily making the emitter throw and confirming the poll continues, then revert.
    - Source assertion: the daemon's existing LSL poll / atomic write / restart logic is unchanged (diff is additive).
  </acceptance_criteria>
  <done>The supervisor calls the Mastra emitter inside try/catch with noop-fallback import; emission failures feed the error budget and never crash the LSL path; no new plist added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task B3 (Track B — fallback only): LIVE GATE — Mastra run lands an agent='mastra' row at the determined tier</name>
  <what-built>Tasks B1+B2 added a host-side better-sqlite3 emitter and wired it as a failure-isolated hook on the sub-agent-live-opencode supervisor. This gate proves a real Mastra run produces the row (the harness cannot autonomously drive a Mastra session).</what-built>
  <read_first>
    - /Users/Q284340/Agentic/coding/scripts/backfill-task-id-by-timestamp.mjs (:44-64,:193-200 — read pattern for token-usage.db)
    - .planning/phases/70-opencode-mastra-token-adapters/70-MASTRA-SURFACE.md (the granularity tier the adapter emits)
  </read_first>
  <how-to-verify>
    1. Ensure the supervisor is running (launchd `com.coding.sub-agent-live-opencode`); kickstart if needed: `launchctl kickstart -k gui/$(id -u)/com.coding.sub-agent-live-opencode`.
    2. (Optional) Start a measurement span for a non-empty task_id; else note task_id=''.
    3. Run a real Mastra interaction (the surface identified in 70-MASTRA-SURFACE.md) that produces usage the adapter can read.
    4. Inspect: `sqlite3 /Users/Q284340/Agentic/coding/.data/llm-proxy/token-usage.db "SELECT agent, granularity_tier, task_id, user_hash, total_tokens, timestamp FROM token_usage WHERE agent='mastra' ORDER BY rowid DESC LIMIT 3;"`
    5. Confirm the latest row shows agent='mastra', the determined granularity_tier (per-session-aggregate floor or better), the distinct adapter user_hash, the active task_id (or ''), and real token counts.
    6. Confirm the LSL path still works (the supervisor kept polling — observations still flowing).
  </how-to-verify>
  <acceptance_criteria>
    - Behavior assertion: the newest agent='mastra' row carries the determined granularity_tier and the distinct adapter user_hash.
    - Behavior assertion: that row's task_id matches the active span (or '' when none), case recorded.
    - Behavior assertion: the supervisor's LSL path is unaffected (observations still flowing after the Mastra run).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the observed row, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mastra client → proxy /v1 (Track A) | Mastra (trusted local agent) routed to the loopback shim, sending X-Agent:mastra |
| Mastra usage → host-side emitter → token-usage.db (Track B) | A SECOND writer to the proxy-owned DB on a distinct user_hash |
| supervisor process boundary (Track B) | Emission hook runs inside the LSL supervisor; a failure must not cross into the LSL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-09 | Tampering | Track B second-writer id collision with the proxy's in-memory id counter | mitigate | The PK is composite `(user_hash, id)`; the adapter uses a DISTINCT user_hash (D-06) and seeds ids per-user via MAX(id)+1, so it never collides with the proxy's counter. Asserted in Task B1 acceptance. |
| T-70-10 | Denial of Service | Track B concurrent writes block the proxy (DB lock) | mitigate | DB is journal_mode=wal; adapter opens with busy_timeout=5000; the WAL concurrency test (N concurrent INSERTs, ok=N/busy=0/err=0) is a Task B1 acceptance criterion (D-07). |
| T-70-11 | Denial of Service | Track B emission failure crashes the LSL supervisor | mitigate | The emit hook is try/catch-isolated, routed into the existing trackError() budget, imported with a noop-fallback (D-08/D-11). Task B2 acceptance forces a throw and confirms the poll continues. |
| T-70-12 | Spoofing | Track B task_id mis-attribution | accept | task_id is a local telemetry label resolved from the single getActiveMeasurement() reader (or '' + timestamp-join backfill). Not an auth principal; parameterized into the column. |
| T-70-13 | Information Disclosure | Track A redirect env exported too broadly, leaking Mastra traffic to a non-loopback URL | mitigate | The redirect seam is pinned to loopback `http://localhost:12435/v1` and scoped to the Mastra launch path (Task A1 acceptance); not a global export. Loopback binding confirmed in Plan 01 T-70-03. |
| T-70-14 | Spoofing | Track A X-Agent:mastra header is a client-supplied attribution label injected into row.agent | accept | The X-Agent override is the planned mechanism (D-06); it is the same threat class as the X-Task-Id/body.agent override already covered by Plan 01 T-70-01. The value is bound as a parameterized string at the proxy (token-usage.ts insert bind, never interpolated into SQL), so it cannot escape the agent column. Worst case: a mis-attributed local telemetry row on the loopback-only surface (T-70-03), re-attributable by the timestamp-join backfill (D-07). No PII, no privilege. |
| T-70-SC | Tampering | npm/pip/cargo installs | mitigate | The Track B emitter reuses the already-present `better-sqlite3` (used by backfill-task-id-by-timestamp.mjs); no new package. If any install is introduced, add a blocking-human legitimacy checkpoint first. |
</threat_model>

<verification>
- Resolved track (A or B) selected from 70-MASTRA-SURFACE.md; only that track ran.
- Track A: Mastra endpoint redirected to the loopback shim AND X-Agent:mastra set; live gate lands an agent='mastra' (not 'opencode') per-llm-call row; server.mjs NOT edited in this plan.
- Track B: emitter passes node --check + WAL concurrency test; supervisor hook is try/catch-isolated; live gate lands an agent='mastra' row at the determined tier with the distinct user_hash; LSL path unaffected.
- ADAPT-04 SC-4 discharged.
</verification>

<success_criteria>
- ADAPT-04 SC-4: a Mastra adapter emits rows on the shared contract at the determined granularity tier, stamped with the active task_id. ✓ on the resolved track's live gate.
- ADAPT-04 fully discharged across Plans 03 (surface/decision) + 04 (implementation).
</success_criteria>

<output>
Create `.planning/phases/70-opencode-mastra-token-adapters/70-04-SUMMARY.md` when done. Record: the selected track, the redirect seam + X-Agent stamp mechanism (Track A) or adapter user_hash used (Track B), the WAL concurrency test result (Track B), the live-gate row observed (agent/granularity_tier/task_id/user_hash/tokens), whether a span was active, and confirmation the LSL path is unaffected (Track B).
</output>
</content>
