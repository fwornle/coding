---
phase: 76-measurement-validity-fixes-prerequisite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/experiments-recompute-route.mjs
autonomous: true
requirements: [VALID-01]
must_haves:
  truths:
    - "experiments-recompute-route.mjs never falls back to the dominant-by-count model (byAgentModel[0]) for a Run's model"
    - "A recompute reuses the Run's already-persisted canonical_model; when absent it derives the foreground-not-dominant group's model; else null"
    - "Recomputing the archived exp-dash-start-control span yields an Opus canonical_model, NOT claude-haiku-4.5"
  artifacts:
    - path: "scripts/experiments-recompute-route.mjs"
      provides: "Route recompute CLI aligned to the canonical fg/bg model selection (no dominant fallback)"
      contains: "isForegroundGroup"
  key_links:
    - from: "scripts/experiments-recompute-route.mjs"
      to: "lib/experiments/token-aggregate.mjs isForegroundGroup"
      via: "import + filter of byAgentModel"
      pattern: "isForegroundGroup"
    - from: "scripts/experiments-recompute-route.mjs"
      to: "run.metadata.canonical_model"
      via: "prefer-persisted read before any derivation"
      pattern: "canonical_model"
---

<objective>
Close the one residual read-path (D-03) that still divergently attributes a Run's model to the dominant-by-count token group. `scripts/experiments-recompute-route.mjs:111` currently sets `model: m.model ?? dominant.model ?? null`, where `dominant = byAgentModel[0]` — the by-count winner. On the pilot span a 1.24M-token Haiku daemon out-massed the Opus foreground, so recompute re-stamps the Run as `claude-haiku-4.5` (the finding-B bug). Align this CLI with the shipped canonical fg/bg-split logic so recompute preserves / re-derives the true foreground model.

Purpose: VALID-01 acceptance requires a measured Opus session to read Opus everywhere. The stop path (`measurement-stop.mjs §327-360`) is already correct (D-01 — do NOT re-implement). This plan fixes the remaining recompute read-path so backfilling an existing Run cannot regress its model back to the dominant daemon.
Output: A corrected `experiments-recompute-route.mjs` whose model/canonical selection mirrors the stop path and `backfill-run-attribution.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-measurement-validity-fixes-prerequisite/76-CONTEXT.md
@.planning/v7.4-attribution-findings.md

<interfaces>
<!-- Canonical model-selection logic to MIRROR. Executor uses these directly. -->

From lib/experiments/token-aggregate.mjs (already exported):
  export function isForegroundGroup(group): boolean   // adapter user_hash AND non-denylisted process
  export function aggregateByTaskId(task_id): { totals, byAgentModel }

Canonical selection (scripts/measurement-stop.mjs §335-348 — the reference):
  const fgGroups = byAgentModel.filter(isForegroundGroup);
  const isSubagentGroup = (g) => g?.process === 'token-adapter-claude-subagent';
  const canonical = fgGroups.find((g) => !isSubagentGroup(g)) ?? fgGroups[0] ?? null;
  const canonicalModel = canonical?.model ?? null;   // NEVER dominant; null persists as "unmeasured"

Established backfill analog (scripts/backfill-run-attribution.mjs §22-42):
  import { aggregateByTaskId, isForegroundGroup } from '../lib/experiments/token-aggregate.mjs';
  // fills canonical_model from fg-not-subagent group, NEVER dominant; gap-fill only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the dominant.model fallback with canonical fg-not-dominant selection</name>
  <read_first>
    - scripts/experiments-recompute-route.mjs §105-140 (the file being modified — the `dominant.model` fallback at ~111 and the writeRun call at ~140)
    - scripts/measurement-stop.mjs §327-360 (canonical fg/bg-split — the REFERENCE logic to mirror; already correct, do NOT re-implement, align to it)
    - scripts/backfill-run-attribution.mjs §22-45 (the established analog that fills canonical_model from the fg-not-subagent group, never dominant)
    - lib/experiments/token-aggregate.mjs §60-80 (isForegroundGroup contract)
    - lib/experiments/run-write.mjs §110-125 (how writeRun persists canonical_model from the totals object — so the recompute must thread the corrected value in, never null/dominant it)
  </read_first>
  <action>
    Import `isForegroundGroup` from `../lib/experiments/token-aggregate.mjs` alongside the existing `aggregateByTaskId`. Remove the `const dominant = byAgentModel[0] ?? {}` by-count selector and both `?? dominant.model` / `?? dominant.agent` fallbacks in the tags object (§108-114). Install the canonical fallback chain, in this exact precedence: (1) prefer the Run's already-persisted `run.metadata.canonical_model` when non-null; (2) else derive `canonical = fgGroups.find(g => g.process !== 'token-adapter-claude-subagent') ?? fgGroups[0] ?? null` where `fgGroups = byAgentModel.filter(isForegroundGroup)`, taking `canonical?.model ?? null`; (3) else `null` — NEVER `byAgentModel[0]` / dominant-by-count. Do the same for `agent`: prefer persisted `m.agent`, else `normalizeAgent(canonical)` / declared foreground agent, never `dominant.agent`. Thread the resolved `canonical_model` into the object passed to `writeRun` (via `totals.canonical_model` per run-write.mjs §118-119) so the idempotent re-close persists the corrected value and cannot regress `canonical_model` to null or to the dominant group. Preserve the existing agent-seam trace rebuild, `--dry-run` behavior, `openExperimentStore()` try/finally, and stderr-only logging (no `console.log`). Update the inline comment block to state the canonical-not-dominant rule (mirror the measurement-stop §331-334 comment).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && grep -n "dominant" scripts/experiments-recompute-route.mjs | grep -v '^#' | grep -c "dominant.model\|dominant.agent" | grep -qx 0 && grep -q "isForegroundGroup" scripts/experiments-recompute-route.mjs && node --check scripts/experiments-recompute-route.mjs && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `scripts/experiments-recompute-route.mjs` no longer contains `dominant.model` or `dominant.agent` as a model/agent fallback (grep count 0, comments excluded).
    - Source: the file imports `isForegroundGroup` and references `canonical_model` as the preferred source.
    - Behavior: `node scripts/experiments-recompute-route.mjs <task_id> --dry-run` runs without throwing and prints an agent/steps line.
    - Behavior: the resolved model precedence is persisted-canonical → fg-not-subagent group → null; it is NEVER `byAgentModel[0]`.
  </acceptance_criteria>
  <done>The recompute CLI selects the foreground-not-dominant model exactly as the stop path does; a re-close of any Run cannot re-stamp it with the dominant daemon model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archived span JSON → recompute CLI | already-persisted, operator-local measurement data; no external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01-01 | Tampering | experiments-recompute-route.mjs model selection | mitigate | deterministic re-derivation from already-persisted token rows; canonical selection is pure (no shell, no network, no LLM) |
| T-76-01-NA | — | overall plan | accept | No new external attack surface — this is a pure deterministic re-derivation of already-persisted data (no new inputs, no exec, no network). |
</threat_model>

<verification>
Run `node scripts/experiments-recompute-route.mjs <archived-task_id> --dry-run` against the archived pilot span and confirm the printed agent line is Claude/Opus lineage, not haiku. The full before/after model assertion on real pilot data is the Wave-2 regression anchor (76-04).
</verification>

<success_criteria>
- `experiments-recompute-route.mjs` contains no `dominant.model`/`dominant.agent` fallback.
- The canonical fallback chain (persisted → fg-not-subagent → null) is installed and imports `isForegroundGroup`.
- `node --check` passes; `--dry-run` executes without error.
</success_criteria>

<output>
Create `.planning/phases/76-measurement-validity-fixes-prerequisite/76-01-SUMMARY.md` when done.
</output>
