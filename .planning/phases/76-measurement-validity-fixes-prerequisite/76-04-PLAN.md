---
phase: 76-measurement-validity-fixes-prerequisite
plan: 04
type: execute
wave: 2
depends_on: [76-01, 76-02, 76-03]
files_modified: []
autonomous: false
requirements: [VALID-01, VALID-02, VALID-03]
must_haves:
  truths:
    - "Recomputing the archived exp-dash-start-control span yields a canonical_model that is NEVER claude-haiku-4.5 (the dominant daemon) — it is an Opus id or null/unmeasured"
    - "The recomputed wallclock_per_step for the multi-hour steering-paused pilot span is within a documented sane bound (no ~28,000 s/step)"
    - "A non-GSD recompute yields non-null code_quality/test_coverage/regressions when a working-tree diff exists"
    - "A fresh measured Opus interactive session shows claude-opus-4-8 in the runs table, score drawer, AND timeline — not claude-haiku-4.5"
  artifacts:
    - path: ".planning/phases/76-measurement-validity-fixes-prerequisite/76-04-SUMMARY.md"
      provides: "Recorded before/after recompute numbers on the real pilot span + the live Opus verification result"
  key_links:
    - from: "scripts/experiments-recompute-route.mjs"
      to: "archived span exp-dash-start-control-claude-opus-4-8"
      via: "recompute run against .data/measurements/<task_id>.json"
      pattern: "exp-dash-start-control"
    - from: "dashboard runs table / score drawer / timeline"
      to: "canonical_model = claude-opus-4-8"
      via: "live fresh-session render (gsd-browser)"
      pattern: "claude-opus"
---

<objective>
Prove the three fixes hold on real data (ROADMAP success criterion 4 — neither canonical comparison is corrupted by O1/O2/O3), then confirm VALID-01 end-to-end on a live fresh session (D-04, never DB-only). First re-run `experiments-recompute-route.mjs` and `experiments-recompute-score.mjs` against the archived `exp-dash-start-control-claude-opus-4-8` span (task_id = same string) and record a concrete before/after: the model is no longer the haiku dominant, per-step time is plausible, and the non-GSD rubric dims are non-null when a diff exists. Then verify a freshly measured Opus interactive session renders Opus across the runs table, score drawer, and timeline.

Purpose: This is the prerequisite gate's acceptance. The runner phases (77–80) are not trusted until this passes.
Output: A SUMMARY recording the before/after recompute numbers and the live Opus verification outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-measurement-validity-fixes-prerequisite/76-CONTEXT.md
@.planning/v7.4-attribution-findings.md
@.data/experiments/exp-dash-start-control-claude-opus-4-8.log.md

<interfaces>
<!-- The recompute CLIs (fixed in 76-01, consumers of 76-02/76-03) + the archived span. -->
Archived pilot span:   .data/measurements/exp-dash-start-control-claude-opus-4-8.json
                       task_id = "exp-dash-start-control-claude-opus-4-8"
                       task_hash = 9eb8949af0b8fe513662a45abe5dc360025b32417d5b38ed9ea13111a9d15557
Recompute route:       node scripts/experiments-recompute-route.mjs <task_id> [--dry-run]
Recompute score:       node scripts/experiments-recompute-score.mjs <task_id> [--dry-run]
Dashboard (live check): http://localhost:3032  → Performance tab (runs table, score drawer, timeline)
                       CLAUDE.md: use the gsd-browser CLI for the screenshot/click-through (never a hand-rolled Playwright script).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression anchor — recompute the archived pilot span, record before/after</name>
  <read_first>
    - .data/experiments/exp-dash-start-control-claude-opus-4-8.log.md (the pilot log — the BEFORE numbers: model claude-haiku-4.5, ~28k s/step, the daemon token table — the baseline to beat)
    - scripts/experiments-recompute-route.mjs (as fixed by 76-01 — the CLI being run; confirm the canonical fallback landed)
    - scripts/experiments-recompute-score.mjs (the score recompute — consumes the 76-03 harness derivation + overlay)
    - lib/experiments/token-aggregate.mjs §60-135 (isForegroundGroup — to interpret whether the pilot span even has a foreground Opus group; if not, canonical is legitimately null/unmeasured, NEVER the haiku dominant)
  </read_first>
  <action>
    Run `node scripts/experiments-recompute-route.mjs exp-dash-start-control-claude-opus-4-8 --dry-run` then (after confirming) without `--dry-run`; and the same for `experiments-recompute-score.mjs`. Read the resulting Run + Score back (via `node scripts/experiments-query.mjs` or the archived span/KB) and record in the SUMMARY, as a before→after table: (1) `canonical_model` BEFORE (`claude-haiku-4.5` per the pilot log) → AFTER (an Opus id if a foreground group exists, else `null`/unmeasured — assert it is NEVER `claude-haiku-4.5`); (2) `wallclock_per_step` BEFORE (~28,000 s) → AFTER (a plausible value within the documented idle-excluded bound); (3) `code_quality`/`test_coverage`/`regressions` — assert non-null when a working-tree diff exists at recompute time (proving the non-GSD derivation fires), or record the exact null-reason (no diff AND no runnable test) if genuinely absent. Note explicitly that the DEFINITIVE Opus-everywhere proof is Task 2's live session (the old pilot span may carry no Opus foreground rows, so its corrected canonical is null not Opus — that is correct, not a failure).
  </action>
  <verify>
    <automated>cd /Users/Q284340/Agentic/coding && node scripts/experiments-recompute-route.mjs exp-dash-start-control-claude-opus-4-8 --dry-run 2>&1 | tee /tmp/recompute-route.out && node scripts/experiments-recompute-score.mjs exp-dash-start-control-claude-opus-4-8 --dry-run 2>&1 | tee /tmp/recompute-score.out && ! grep -q "claude-haiku-4.5" /tmp/recompute-route.out && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the recompute of `exp-dash-start-control-claude-opus-4-8` yields a `canonical_model` that is NEVER `claude-haiku-4.5`; it is an Opus id or null/unmeasured (recorded either way).
    - Behavior: the recomputed `wallclock_per_step` is within the documented idle-excluded bound — orders of magnitude below the ~28,000 s/step baseline.
    - Behavior: `code_quality`/`test_coverage`/`regressions` are non-null when a diff exists at recompute time; any null carries a recorded no-signal reason (no diff AND no runnable test), never a bare absence-of-GSD-files.
    - Artifact: the before→after table is recorded in the SUMMARY with the exact values.
  </acceptance_criteria>
  <done>The corrected rig, re-run on the real pilot span, provably drops the haiku dominant, produces a plausible per-step time, and populates the non-GSD dims when signal exists — recorded as concrete before/after numbers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The VALID-01 canonical-model fixes (76-01 residual read-path + the shipped stop path) so a measured Opus interactive session records claude-opus-4-8, not the haiku dominant. This session itself is a measured Opus interactive run — an honest fresh test case (76-CONTEXT §specifics).</what-built>
  <how-to-verify>
    1. Ensure a fresh Opus session was measured (this session, or `node scripts/measurement-start.mjs --task-id valid01-live-opus --goal "<goal>"` → do work → `measurement-stop`/close). The operator produces the fresh measured session; only they can.
    2. Open the dashboard at http://localhost:3032, go to the Performance tab (use the gsd-browser CLI per CLAUDE.md — `gsd-browser navigate` + `screenshot`, not a hand-rolled Playwright script).
    3. Confirm in ALL THREE surfaces the session's model reads `claude-opus-4-8` (or the actual Opus id), NOT `claude-haiku-4.5`: (a) the runs table row, (b) the score drawer for that run, (c) the timeline for that run.
    4. Expected: Opus everywhere; background daemon models (haiku/sonnet) appear ONLY in the segregated background-models column, never as the canonical model.
  </how-to-verify>
  <resume-signal>Type "approved" if all three surfaces show Opus, or describe which surface still shows haiku/wrong model (then fix only what this proves still broken, per D-04).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archived span JSON → recompute CLIs | operator-local, already-persisted measurement data; no external input crosses here |
| dashboard render → operator eyes | read-only visual verification; no write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-NA | — | recompute + live verification | accept | No new external attack surface — this plan only re-runs the (already-mitigated) recompute CLIs against already-persisted data and reads the dashboard. The test-command exec risk is owned and mitigated in 76-03 (T-76-03-EXEC). |
</threat_model>

<verification>
- Task 1 automated dry-run recompute produces output containing no `claude-haiku-4.5` for the canonical model.
- Task 2 human-verify confirms Opus across runs table, score drawer, and timeline on a live fresh session.
</verification>

<success_criteria>
- The pilot-span recompute drops the haiku dominant, shows a plausible per-step time, and non-null non-GSD dims (or a recorded no-signal reason).
- A live fresh Opus session reads Opus in all three dashboard surfaces.
- The before/after numbers and live result are recorded in the SUMMARY — the prerequisite gate is signed off.
</success_criteria>

<output>
Create `.planning/phases/76-measurement-validity-fixes-prerequisite/76-04-SUMMARY.md` when done, including the before→after recompute table and the live verification outcome.
</output>
