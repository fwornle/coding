---
phase: 73-semantic-route-judge-success-scoring
plan: 06
type: execute
wave: 3
depends_on: ["73-01", "73-02", "73-03", "73-04"]
files_modified:
  - scripts/measurement-stop.mjs
  - scripts/experiments-recompute-score.mjs
autonomous: true
requirements: [ROUTE-03, SCORE-01]
must_haves:
  truths:
    - "Closing a run computes evidence + judge + writes a Score with a scored edge, after writeRun"
    - "A judge failure or proxy-down does NOT block run-close (the close still completes)"
    - "A run's Score can be recomputed idempotently from its archived span via a CLI"
    - "The recompute CLI opens the store only via openExperimentStore (ontologyDir guaranteed)"
  artifacts:
    - path: "scripts/measurement-stop.mjs"
      provides: "step (4.5): gatherEvidence → runJudge/trivial → writeScore + scored edge inside the existing try/finally"
      contains: "writeScore"
    - path: "scripts/experiments-recompute-score.mjs"
      provides: "single-run idempotent re-judge CLI cloned from experiments-recompute-route.mjs"
      min_lines: 110
  key_links:
    - from: "scripts/measurement-stop.mjs"
      to: "lib/experiments/judge.mjs + score-write.mjs"
      via: "runJudge then writeScore after writeRun, reusing the already-built trace"
      pattern: "runJudge|writeScore"
    - from: "scripts/experiments-recompute-score.mjs"
      to: "lib/experiments/store.mjs"
      via: "openExperimentStore() factory (ontologyDir set)"
      pattern: "openExperimentStore"
---

<objective>
Wire the judge + scoring into the run-close pipeline and provide an idempotent
recompute path. In `measurement-stop.mjs`, after the existing `writeRun`, gather
on-disk evidence (73-03), filter the already-built trace to consequential events
(73-01), run the judge (73-04) — or skip to not_scored:'trivial' for a trivial run —
and `writeScore` + scored edge (73-02), all inside the existing try/finally so the
close NEVER hard-blocks on a judge failure (D-03). Then clone
`experiments-recompute-route.mjs` into `experiments-recompute-score.mjs` to re-judge
a single Run idempotently (D-03/D-06).

Purpose: ROUTE-03 + SCORE-01 reach production at run-close and become re-runnable.
Output: measurement-stop.mjs wiring + experiments-recompute-score.mjs CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-semantic-route-judge-success-scoring/73-CONTEXT.md
@.planning/phases/73-semantic-route-judge-success-scoring/73-PATTERNS.md
@lib/experiments/judge.mjs
@lib/experiments/score-write.mjs
@lib/experiments/evidence-harness.mjs
@lib/experiments/consequential-events.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire evidence → judge → writeScore into measurement-stop close path</name>
  <read_first>
    - scripts/measurement-stop.mjs (the insertion point: step (4) opens the store + writeRun inside try/finally lines 277-285; the already-built `trace` line 271; `span` with goal_sentence lines 217-242; phaseArg line 168; REPO_ROOT line 67; the "close must not hard-block" contract lines 212-215; no-console-log header lines 33-34)
    - lib/experiments/judge.mjs (runJudge — quarantines internally to pending; never throws)
    - lib/experiments/score-write.mjs (writeScore({span,judgment}))
    - lib/experiments/evidence-harness.mjs (gatherEvidence({span,phaseArg,repoRoot}))
    - lib/experiments/consequential-events.mjs (filterConsequential, isTrivialRun)
    - .planning/phases/73-semantic-route-judge-success-scoring/73-PATTERNS.md §measurement-stop.mjs (the exact 4.5 insertion block lines 247-265)
  </read_first>
  <action>
    Add the new imports (gatherEvidence, runJudge, writeScore, filterConsequential/
    isTrivialRun) to measurement-stop.mjs. Inside the EXISTING try (right after
    `await writeRun(...)`, before countPending), insert step (4.5):
      - evidence = gatherEvidence({ span, phaseArg, repoRoot: REPO_ROOT }).
      - reuse the already-computed `trace` (line 271). If trace is null OR
        isTrivialRun(trace) → judgment = { not_scored:'trivial', pending:false, ...nulls }
        (skip the proxy — D-04); else judgment = await runJudge({ span, trace, evidence })
        (runJudge internally try/catch-quarantines to pending — D-03, so it never throws).
      - await writeScore(store, { span, judgment }) (same open store, before close()).
    Do NOT let the judge/score path throw out of the try — runJudge already quarantines;
    writeScore is a local store write. The existing finally still close()s the store.
    Extend the close summary line to print the resolved goal_aligned_ratio + a
    scored/pending/trivial marker. no-console-log: process.stdout/stderr.write only.
    NO fenced code — copy the §measurement-stop 4.5 block from PATTERNS.md.
  </action>
  <verify>
    <automated>node --check scripts/measurement-stop.mjs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -q "writeScore" scripts/measurement-stop.mjs` and `grep -q "gatherEvidence" scripts/measurement-stop.mjs` and `grep -q "runJudge" scripts/measurement-stop.mjs`
    - SOURCE: the writeScore call is inside the existing try that close()s the store (insertion after writeRun, before the finally)
    - SOURCE: trivial branch present (`grep -q "isTrivialRun" scripts/measurement-stop.mjs`) so trivial runs skip runJudge
    - SOURCE: no-console-log clean (`! grep -nE "console\.(log|error|warn)" scripts/measurement-stop.mjs`)
    - BEHAVIOR: a headless close against a fixture span completes exit 0 even when the proxy is unreachable (judge quarantines to pending; close does not hard-block)
  </acceptance_criteria>
  <done>measurement-stop.mjs gathers evidence, judges (or marks trivial), and writes a Score + scored edge after writeRun, without ever hard-blocking the close.</done>
</task>

<task type="auto">
  <name>Task 2: experiments-recompute-score.mjs — idempotent single-run re-judge CLI</name>
  <read_first>
    - scripts/experiments-recompute-route.mjs (the near-verbatim clone source: arg parse + --dry-run lines 73-85, readArchivedSpan lines 52-63, findRun lines 65-71, store open + try/finally close lines 94-145, agent-seam trace rebuild lines 122-127, entry-point guard lines 147-162)
    - lib/experiments/judge.mjs (runJudge — the step swapped in for computeHeuristics)
    - lib/experiments/score-write.mjs (writeScore — the idempotent write that preserves corrected_*)
    - lib/experiments/evidence-harness.mjs (gatherEvidence)
    - lib/experiments/consequential-events.mjs (filterConsequential, isTrivialRun)
    - lib/experiments/store.mjs (openExperimentStore — the mandatory ontologyDir factory)
  </read_first>
  <action>
    Create scripts/experiments-recompute-score.mjs as a near-clone of
    experiments-recompute-route.mjs. Keep arg parse (<task_id> [--dry-run]),
    readArchivedSpan, findRun, the store open + try/finally close, the
    normalizeAgent + buildTraceSeam + buildNormalizedTrace rebuild (lines 122-127 —
    MUST copy or Claude/Copilot runs re-null), and the entry-point guard verbatim.
    SWAP the heuristics step for: evidence = gatherEvidence({span,phaseArg:<derive
    from run metadata or span>,repoRoot}); trace rebuilt as in the route CLI; judgment =
    isTrivialRun(trace) ? trivial-marker : await runJudge({span,trace,evidence});
    then (unless --dry-run) await writeScore(store,{span,judgment}). Because writeScore
    preserves corrected_* (D-06), a recompute refreshes judged fields without clobbering
    overrides. Open the store EXCLUSIVELY via openExperimentStore() — NEVER `new
    GraphKMStore` inline (CLAUDE.md ontologyDir rule). no-console-log: out/warn via
    process.stdout/stderr.write. NO fenced code — clone the route CLI.
  </action>
  <verify>
    <automated>node --check scripts/experiments-recompute-score.mjs && node scripts/experiments-recompute-score.mjs --dry-run nonexistent-task-id; test $? -ne 0 && echo "exits non-zero on missing span (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - ONTOLOGYDIR ACCEPTANCE GREP (CLAUDE.md km-core rule): `grep -q "openExperimentStore" scripts/experiments-recompute-score.mjs` AND `! grep -q "new GraphKMStore" scripts/experiments-recompute-score.mjs`
    - SOURCE: `grep -q "runJudge" scripts/experiments-recompute-score.mjs` and `grep -q "writeScore" scripts/experiments-recompute-score.mjs`
    - SOURCE: agent-seam rebuild copied — `grep -q "buildTraceSeam" scripts/experiments-recompute-score.mjs` and `grep -q "normalizeAgent" scripts/experiments-recompute-score.mjs`
    - SOURCE: entry-point guard present (`grep -q "import.meta.url" scripts/experiments-recompute-score.mjs`)
    - BEHAVIOR: `--dry-run <task_id>` performs no write; running recompute twice + after an override leaves corrected_* intact while judged fields refresh (idempotency + D-06 — verify against a real or fixture archived span and report in SUMMARY)
  </acceptance_criteria>
  <done>experiments-recompute-score.mjs re-judges a single Run idempotently via openExperimentStore, preserving corrected_* overrides; ontologyDir acceptance grep passes.</done>
</task>

</tasks>

<threat_model>
No new externally-reachable surface (CLI + close orchestrator). The only outbound
call is the judge's proxy call, whose threats are mitigated in 73-04 (parse/timeout/
quarantine/no-raw-inputs). Internal threats:
- T-73-06-BLOCK (DoS): a hung/failed judge blocking run-close — mitigated by runJudge's
  internal quarantine (never throws) + the writeScore call living inside the existing
  try/finally; the close completes (asserted Task 1 behavior).
- T-73-06-CLOBBER (Integrity): recompute clobbering human overrides — mitigated by
  writeScore's corrected_* carry-forward (D-06, proven in 73-02; re-asserted Task 2).
- T-73-06-LOCK (Availability): concurrent experiment-store owners — mitigated by the
  transient open + finally-close pattern (single-owner LevelDB; do not run two
  experiment CLIs concurrently — documented).
No package installs (T-73-SC N/A).
</threat_model>

<verification>
- `node --check scripts/measurement-stop.mjs` and `scripts/experiments-recompute-score.mjs` clean
- Close completes exit 0 with proxy unreachable (judge quarantines)
- ontologyDir acceptance grep passes on the recompute CLI
- recompute idempotent + corrected_* preserved
</verification>

<success_criteria>
Run-close emits evidence-grounded judge scores into a Score node (or trivial/pending
markers) without ever hard-blocking; the recompute CLI re-judges idempotently via
openExperimentStore while preserving overrides.
</success_criteria>

<output>
Create `.planning/phases/73-semantic-route-judge-success-scoring/73-06-SUMMARY.md` when done
</output>
