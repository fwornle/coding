---
phase: 73-semantic-route-judge-success-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/experiments/consequential-events.mjs
  - tests/experiments/consequential-events.test.mjs
  - tests/fixtures/route/consequential-mixed.json
autonomous: true
requirements: [ROUTE-03]
must_haves:
  truths:
    - "Each consequential (acting) trace event is distinguishable from navigation/read events"
    - "goal_aligned_ratio = toward/(toward+away) with neutral events excluded from the denominator"
    - "A run with zero consequential events is detectable as trivial without an LLM call"
  artifacts:
    - path: "lib/experiments/consequential-events.mjs"
      provides: "isConsequentialTool classifier, filterConsequential(trace), computeGoalAlignedRatio(labels)"
      min_lines: 60
    - path: "tests/experiments/consequential-events.test.mjs"
      provides: "fixture tests for the classifier (per-agent tool names) and the ratio math (neutral excluded, all-away→0, all-toward→1, empty→null)"
      min_lines: 50
  key_links:
    - from: "lib/experiments/consequential-events.mjs"
      to: "lib/lsl/route/route-event.mjs"
      via: "consumes RouteEvent.tool_name + RouteEvent.seq"
      pattern: "route-event"
---

<objective>
Create the pure, zero-LLM route-logic primitives that the judge and the close
orchestrator depend on: (1) a consequential-event classifier that separates
acting tool calls (Edit/Write/MultiEdit/NotebookEdit/Bash/Task) from
navigation/reads (D-02), (2) `filterConsequential(trace)` that the trivial-run
guard (D-04) and the judge input both use, and (3) `computeGoalAlignedRatio(labels)`
that computes `toward/(toward+away)` deterministically from per-event labels with
neutral excluded from the denominator (D-02).

Purpose: These are the two fixture-tested pure units called out for this phase.
Computing the ratio in code (not trusting LLM arithmetic) keeps ROUTE-03 reproducible.
Output: lib/experiments/consequential-events.mjs + its fixture test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-semantic-route-judge-success-scoring/73-CONTEXT.md
@.planning/phases/73-semantic-route-judge-success-scoring/73-PATTERNS.md

<interfaces>
<!-- The RouteEvent shape this module consumes (from lib/lsl/route/route-event.mjs) -->
RouteEvent: { seq:number, tool_call_id:string, tool_name:string, inputs_digest:string,
  target_path:string|null, started_at:string, ended_at:string|null,
  outcome:'success'|'error'|'denied'|'abandoned', agent:'claude'|'copilot'|'opencode' }

<!-- The label shape computeGoalAlignedRatio consumes (produced by the judge in 73-04) -->
EventLabel: { seq:number, label:'toward'|'neutral'|'away' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: consequential-events.mjs — classifier + filter + ratio math</name>
  <read_first>
    - lib/experiments/route-heuristics.mjs (the frozen-set classifier style: HEURISTIC_KEYS Object.freeze, isReadTool/isMutateTool at lines 58-65, ALL_NULL_HEURISTICS sentinel, module header with no-console-log discipline at lines 17-30)
    - lib/lsl/route/route-event.mjs (the RouteEvent typedef lines 22-37 + OUTCOMES/OUTCOME_VALUES frozen enums lines 46-57 — copy the frozen-enum membership-check style)
  </read_first>
  <behavior>
    - isConsequentialTool('Edit'|'Write'|'MultiEdit'|'NotebookEdit'|'Bash'|'Task') === true
    - isConsequentialTool('Read'|'Glob'|'Grep'|'WebFetch'|'WebSearch'|'TodoWrite') === false
    - filterConsequential([]) returns []
    - filterConsequential(mixed trace) returns only events whose tool_name isConsequentialTool, preserving seq order
    - isTrivialRun(trace) returns true when filterConsequential(trace).length < TRIVIAL_THRESHOLD (default 1)
    - computeGoalAlignedRatio([]) returns null (no labels → could-not-compute, not 0)
    - computeGoalAlignedRatio with only neutral labels returns null (denominator zero → null, NOT 0)
    - computeGoalAlignedRatio([toward,toward,away,neutral]) returns 2/3 (neutral excluded from denominator)
    - computeGoalAlignedRatio(all toward) returns 1; (all away) returns 0
  </behavior>
  <action>
    Create lib/experiments/consequential-events.mjs as pure ESM (no km-core, no
    node:fs imports — only node:process for the stderr-only logging guard, mirror
    route-heuristics.mjs:24-30 + 212-218). Export, per the frozen-set style of
    route-heuristics.mjs:32-40:
      - `CONSEQUENTIAL_TOOLS` Object.freeze(['Edit','Write','MultiEdit','NotebookEdit','Bash','Task'])
      - `isConsequentialTool(name)` membership check
      - `filterConsequential(trace)` — Array.isArray guard then filter by isConsequentialTool(e.tool_name); preserve order; non-array/empty → []
      - `TRIVIAL_THRESHOLD` = 1 (D-04 discretion: ≈0 consequential events) and `isTrivialRun(trace)` = filterConsequential(trace).length < TRIVIAL_THRESHOLD
      - `computeGoalAlignedRatio(labels)` — D-02 math: let toward = count(label==='toward'), away = count(label==='away'); denom = toward + away; return denom === 0 ? null : toward / denom. Neutral NEVER enters numerator or denominator. Non-array/empty labels → null (the null-not-zero contract, route-heuristics.mjs:13-16).
    Per-agent tool-name note: tool_name arrives un-normalized (route-event.mjs:30),
    so the consequential set is the union of native acting-tool names across
    claude/copilot/opencode — keep the set extensible and document each entry.
    Diagnostics via process.stderr.write only (no-console-log; do not dodge with
    another raw-write API). NO fenced implementation here — copy the structural
    pattern from route-heuristics.mjs.
  </action>
  <verify>
    <automated>node --check lib/experiments/consequential-events.mjs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -q "Object.freeze" lib/experiments/consequential-events.mjs` (frozen set, no mutable export)
    - SOURCE: `grep -q "process.stderr.write" lib/experiments/consequential-events.mjs` AND `! grep -nE "console\.(log|error|warn)" lib/experiments/consequential-events.mjs` (no-console-log)
    - SOURCE: module imports neither '@fwornle/km-core' nor 'node:fs' (`! grep -E "km-core|node:fs" lib/experiments/consequential-events.mjs`)
    - BEHAVIOR: all `<behavior>` cases pass via Task 2's suite
  </acceptance_criteria>
  <done>consequential-events.mjs exports isConsequentialTool, CONSEQUENTIAL_TOOLS, filterConsequential, isTrivialRun, TRIVIAL_THRESHOLD, computeGoalAlignedRatio; pure ESM; no-console-log clean.</done>
</task>

<task type="auto">
  <name>Task 2: fixture test suite for classifier + ratio math</name>
  <read_first>
    - tests/experiments/route-heuristics.test.mjs (the node:test + node:assert/strict convention, describe/test blocks, loadFixture helper at lines 23-30, true-negative discipline — mirror this file exactly)
    - lib/experiments/consequential-events.mjs (the unit under test, from Task 1)
    - tests/fixtures/route/abandoned.json (the RouteEvent fixture JSON shape to clone for the mixed fixture)
  </read_first>
  <action>
    Create tests/fixtures/route/consequential-mixed.json — a RouteEvent[] with a
    mix of acting (Edit/Bash/Task) and navigation (Read/Glob/Grep) events covering
    all three agents' native names, so the classifier is exercised cross-agent
    (D-08 fixture-test discipline). Create
    tests/experiments/consequential-events.test.mjs using `node:test` + `node:assert/strict`
    (NOT jest) mirroring route-heuristics.test.mjs structure: one describe block per
    exported function. Cover every case in Task 1's `<behavior>` list, each ratio
    case as an explicit assert.equal, and include the neutral-excluded case
    (toward,toward,away,neutral → 2/3) and the all-neutral → null true-negative.
    Output via process.stderr.write only.
  </action>
  <verify>
    <automated>node --test tests/experiments/consequential-events.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - TEST-COMMAND: `node --test tests/experiments/consequential-events.test.mjs` exits 0 with all tests passing
    - BEHAVIOR: assertion `computeGoalAlignedRatio` with [toward,toward,away,neutral] equals 2/3 is present and passes
    - BEHAVIOR: assertion all-neutral labels → null is present and passes (true-negative for null-not-zero)
    - SOURCE: test imports from 'node:test' (`grep -q "from 'node:test'" tests/experiments/consequential-events.test.mjs`)
  </acceptance_criteria>
  <done>Test suite green; classifier and ratio math fixture-proven including neutral-exclusion and null-not-zero true-negatives.</done>
</task>

</tasks>

<threat_model>
No new externally-reachable surface (pure in-process logic module). Trust
boundary unchanged. Relevant internal threat: T-73-01-COERCE (Tampering) — a
missing/empty label set silently coerced to ratio 0 would corrupt cross-run
averages; mitigated by returning `null` for empty/all-neutral (null-not-zero
contract, asserted in Task 2). No package installs (T-73-SC N/A).
</threat_model>

<verification>
- `node --check lib/experiments/consequential-events.mjs` clean
- `node --test tests/experiments/consequential-events.test.mjs` green
- No console.* in the new module
</verification>

<success_criteria>
isConsequentialTool, filterConsequential, isTrivialRun, and computeGoalAlignedRatio
exist, are pure, fixture-tested, and enforce the toward/(toward+away) neutral-excluded
ratio plus null-not-zero degradation.
</success_criteria>

<output>
Create `.planning/phases/73-semantic-route-judge-success-scoring/73-01-SUMMARY.md` when done
</output>
