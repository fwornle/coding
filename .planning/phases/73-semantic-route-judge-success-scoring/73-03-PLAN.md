---
phase: 73-semantic-route-judge-success-scoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/experiments/evidence-harness.mjs
  - tests/experiments/evidence-harness.test.mjs
autonomous: true
requirements: [SCORE-01]
must_haves:
  truths:
    - "The harness reads only already-on-disk artifacts and NEVER runs tests/linters/review at scoring time"
    - "A missing artifact yields null for that evidence slot (never a guess, never zero)"
    - "The harness returns a structured evidence object the judge can consume"
  artifacts:
    - path: "lib/experiments/evidence-harness.mjs"
      provides: "gatherEvidence({span,phaseArg,repoRoot}) → {verification,reviewFindings,testSummary,diffStat,planTasks}"
      exports: ["gatherEvidence"]
      min_lines: 70
    - path: "tests/experiments/evidence-harness.test.mjs"
      provides: "fail-soft → null tests + present-artifact parse tests against fixture phase dirs"
      min_lines: 50
  key_links:
    - from: "lib/experiments/evidence-harness.mjs"
      to: "active phase dir artifacts (VERIFICATION.md / REVIEW.md / PLAN.md)"
      via: "fail-soft phase-dir locator + readFileSync"
      pattern: "VERIFICATION|REVIEW|PLAN"
---

<objective>
Build the lean deterministic evidence harness (D-01): it reads cheap, already-on-disk
artifacts for the active phase and returns a structured evidence object that the
judge (73-04) feeds into the 5-dimension rubric. It reads the VERIFICATION.md verdict,
REVIEW.md findings count, the PLAN.md task list (for spec_drift), a `git diff --stat`,
and a test pass/fail summary parsed from EXISTING output — and it NEVER runs tests,
linters, or code-review at scoring time. Every slot is fail-soft to `null`.

Purpose: SCORE-01 grounding without the heavy active harness (rejected as too heavy for v0).
Output: evidence-harness.mjs + test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-semantic-route-judge-success-scoring/73-CONTEXT.md
@.planning/phases/73-semantic-route-judge-success-scoring/73-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: evidence-harness.mjs — fail-soft on-disk artifact gatherer</name>
  <read_first>
    - scripts/measurement-stop.mjs (the locatePlanMd phase-dir resolver lines 107-122 and readArchivedSpan fail-soft idiom lines 92-98 — copy both patterns; also the REPO_ROOT/no-console-log header conventions)
    - lib/experiments/goal-sentence.mjs (deriveGoalSentence fail-soft on-disk reader — the established fail-soft-returns-empty style for .planning artifacts)
    - .planning/phases/73-semantic-route-judge-success-scoring/73-PATTERNS.md §evidence-harness.mjs (the D-01 evidence list + fail-soft-to-null rule lines 96-125)
  </read_first>
  <action>
    Create lib/experiments/evidence-harness.mjs exporting
    `gatherEvidence({ span, phaseArg, repoRoot })` returning a structured object:
      - verification: parse the active phase's `*-VERIFICATION.md` for its verdict
        (e.g. PASS/FAIL/PARTIAL header or status field); null if absent/unreadable.
      - reviewFindings: count findings in `*-REVIEW.md` (e.g. number of finding
        entries / a count line); null if absent.
      - testSummary: parse a pass/fail summary from EXISTING test output if any
        on-disk artifact exists (do NOT run tests — D-01); null when no such artifact.
      - diffStat: spawn `git diff --stat` (child_process, capture stdout, fail-soft
        to null on non-zero/throw) — a cheap read, not a test run.
      - planTasks: extract the PLAN.md task list (task names) for spec_drift; null if absent.
    Reuse the locatePlanMd phase-dir locator (port it: resolve the `<NN>-<slug>` dir
    from phaseArg under .planning/phases) and glob the dir for the VERIFICATION/REVIEW/PLAN
    files. EVERY read is wrapped fail-soft → that slot becomes null (D-01: a dimension
    with no evidence is null, NOT zero/guessed). Diagnostics via process.stderr.write only.
    NO fenced code here — copy the locator + fail-soft idioms from measurement-stop.mjs.
  </action>
  <verify>
    <automated>node --check lib/experiments/evidence-harness.mjs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -q "git diff --stat" lib/experiments/evidence-harness.mjs` (reads diff stat, does not run tests)
    - SOURCE: harness does NOT spawn test/lint runners — `! grep -nE "npm (run )?test|jest|playwright test|eslint" lib/experiments/evidence-harness.mjs`
    - SOURCE: `grep -q "process.stderr.write" lib/experiments/evidence-harness.mjs` AND `! grep -nE "console\.(log|error|warn)" lib/experiments/evidence-harness.mjs`
    - BEHAVIOR: gatherEvidence returns {verification,reviewFindings,testSummary,diffStat,planTasks} with null for absent artifacts (Task 2)
  </acceptance_criteria>
  <done>evidence-harness.mjs exports gatherEvidence; reads on-disk artifacts only; fail-soft → null; never runs tests/linters/review.</done>
</task>

<task type="auto">
  <name>Task 2: evidence-harness fail-soft + parse test</name>
  <read_first>
    - tests/experiments/route-heuristics.test.mjs (node:test convention + loadFixture-from-fixtures-dir pattern)
    - lib/experiments/evidence-harness.mjs (unit under test)
    - tests/fixtures/route/sample-PLAN.md (an existing PLAN.md fixture to reuse for the planTasks parse case)
  </read_first>
  <action>
    Create tests/experiments/evidence-harness.test.mjs using node:test +
    node:assert/strict. Build a tmpdir mimicking a `.planning/phases/<NN>-x/` dir:
      - Case A (all present): write a VERIFICATION.md (with a verdict), a REVIEW.md
        (with N findings), a PLAN.md (with a task list) → assert verification verdict
        parsed, reviewFindings === N, planTasks non-empty.
      - Case B (all absent): point phaseArg at an empty phase dir → assert verification,
        reviewFindings, testSummary, planTasks are all null (fail-soft, never zero/guess).
      - Case C: diffStat is null-or-string but never throws (call against the tmpdir).
    Assert the harness NEVER throws on missing files.
  </action>
  <verify>
    <automated>node --test tests/experiments/evidence-harness.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - TEST-COMMAND: `node --test tests/experiments/evidence-harness.test.mjs` exits 0
    - BEHAVIOR: absent-artifact case asserts null (not 0, not '') for each slot
    - BEHAVIOR: present-artifact case asserts a parsed verdict + finding count + task list
  </acceptance_criteria>
  <done>Test green; fail-soft-to-null and present-artifact parsing both proven.</done>
</task>

</tasks>

<threat_model>
No new externally-reachable surface (in-process file reader). Trust boundary
unchanged. Internal threats:
- T-73-03-EXEC (Elevation): the `git diff --stat` spawn is the only external process
  — invoked with a fixed argv array (no shell string interpolation of untrusted input,
  no user-supplied flags) so command injection is not reachable; mitigate by spawning
  with an argv array, not a shell string.
- T-73-03-GUESS (Integrity): inferring evidence when absent would bias the rubric —
  mitigated by strict fail-soft → null (asserted Task 2).
No package installs (T-73-SC N/A).
</threat_model>

<verification>
- `node --check lib/experiments/evidence-harness.mjs` clean
- `node --test tests/experiments/evidence-harness.test.mjs` green
- No test/lint runner invocation in the module
</verification>

<success_criteria>
gatherEvidence returns a structured on-disk-only evidence object, fail-soft to null,
never running tests/linters/review at scoring time (D-01).
</success_criteria>

<output>
Create `.planning/phases/73-semantic-route-judge-success-scoring/73-03-SUMMARY.md` when done
</output>
