---
phase: 42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/forensics/report-42.2-00-canonical-emit.md
autonomous: true
requirements: []
tags: [forensics, canonical-emit, team-attribution, process-attribution, wave-0, blocking-wave-1]

user_setup: []

must_haves:
  truths:
    - "A single forensics report enumerates every code path that produces a canonical km-core Entity in B and identifies whether `team` is currently set on each path"
    - "Every `POST /api/complete` (or `@rapid/llm-proxy` `LLMService` call) in the wave-analysis hot path is enumerated with its current `process` argument value (likely absent or 'unknown')"
    - "A representative sample (>=10) of the 802 already-migrated entities under `.data/knowledge-graph-migrated/` is read and the presence of `team`/`metadata.team` is reported per-entity"
    - "Plan 02 has a deterministic, source-cited gap list to act on (no scope ambiguity)"
  artifacts:
    - path: ".planning/forensics/report-42.2-00-canonical-emit.md"
      provides: "Forensics report locking the exact Wave 1 Plan 02 fix scope"
      contains: "Section 1 canonical-mapper emit paths, Section 2 LLM dispatch sites, Section 3 802-entity team-field audit, Section 4 actionable gap list"
  key_links:
    - from: ".planning/forensics/report-42.2-00-canonical-emit.md"
      to: "integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts"
      via: "Source citations with file:line refs for every emit path"
      pattern: "canonical-mapper\\.ts:[0-9]+"
    - from: ".planning/forensics/report-42.2-00-canonical-emit.md"
      to: ".data/knowledge-graph-migrated/"
      via: "Reading 10 random entities from the migrated dir and reporting team-field presence per row"
      pattern: "team.*present|team.*missing"
---

<objective>
Run a focused forensics pass that locks the exact scope of the Wave 1 Plan 02 canonical-emit gap fix (per D-Emit in CONTEXT.md). Without this report, Plan 02 would speculate about which fields are missing where; with it, Plan 02 ships a surgical fix.

Purpose: D-Emit in CONTEXT.md explicitly requires a forensics-first approach so the canonical-emit gap fix in Plan 02 is narrow and source-cited rather than a wide canonical-mapper audit. The 42-07-SUMMARY recommended-next-action calls for `/gsd-forensics` over three specific paths: `canonical-mapper.ts`, wave-controller/agent LLM dispatch, and the 802 migrated entities for `team` presence.

Output: A single forensics report at `.planning/forensics/report-42.2-00-canonical-emit.md` that Plan 02 reads as its source of truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md
@.planning/phases/42-offline-ukb-migration-b/42-07-SUMMARY.md
@.planning/phases/42-offline-ukb-migration-b/42-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs to anchor the audit. Extracted from km-core + B's submodule. -->

km-core canonical Entity shape (excerpt from /Users/Q284340/Agentic/km-core/src/types/entity.ts):
```typescript
export interface Entity {
  id: EntityId;
  name: string;
  entityType: string;
  ontologyClass: string;
  layer: 'evidence' | 'pattern';
  description: string;
  createdAt: string;
  updatedAt: string;
  metadata: Record<string, unknown>;   // <-- this is where `team` MUST land if it lands anywhere
  validFrom: string;
  validUntil?: string | null;
  embedding?: number[];                 // Phase 42-04 (D-52)
  supersedes?: EntityId;
  legacyId?: { system: 'A' | 'B' | 'C'; id: string };
}
```

canonical-mapper.ts emit signature (file lines 98-103):
```typescript
export function toCanonicalEntity(
  raw: KGEntity,
  ontologyClass: string,
  runId: string,
  options: CanonicalMapperOptions = {},
): Entity;
```

km-core-adapter.ts already plumbs `team` (file lines 21-22, 77, 135, 151):
- `getEntity(name, team)`
- `deleteEntity(name, team, opts)`
- `storeEntity` interface options bag: `{ store, team }`
- Captures team for `runId: 'wave-analysis-${team}'` + `metadata.team` migration target

CLAUDE.md § "km-core LLM proxy endpoint": the local rapid-llm-proxy serves `POST /api/complete` with request shape `{ process, messages, taskType? }`. SDK consumers in B use `import { LLMService } from '@rapid/llm-proxy'` (see semantic-analyzer.ts:7, semantic-analysis-agent.ts:7, git-staleness-detector.ts:17, wave1-project-agent.ts:21, wave2-component-agent.ts:16, wave3-detail-agent.ts:14). The `process` field is wired through the LLMService surface, NOT a raw fetch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit canonical-mapper.ts and every callsite that produces a km-core Entity</name>

  <read_first>
    - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts (the file under audit)
    - integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts (already plumbs team; baseline for what canonical-mapper SHOULD propagate)
    - integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts (calls augmentWithCanonical / toCanonicalEntity at wave1 emit)
    - integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts (wave2 emit)
    - integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts (wave3 emit)
    - integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts (orchestrates wave persistence; reads team from workflow params at parameters.team)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md (D-Emit scope lock)
    - CLAUDE.md (km-core scripts ontologyDir mandate, no-console-log constraint)
  </read_first>

  <files>.planning/forensics/report-42.2-00-canonical-emit.md (section 1 of the report — append-only contribution; report is finalised in Task 4)</files>

  <action>
    Open `integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts` and read the full `toCanonicalEntity` body (lines 98-220) plus `augmentWithCanonical` (lines 234-260). For each emit path, enumerate every assignment into `metadata`, `entityType`, `ontologyClass`, `legacyId`, `layer`, `description`. Explicitly assert whether `team` is set anywhere — answer is expected to be NO (the function does not accept a team parameter; `team` is at the workflow level but never threaded into the mapper). Reference exact file:line for every claim.

    Then run the following grep gate from the repo root to enumerate ALL callers of `toCanonicalEntity` and `augmentWithCanonical`:
    ```bash
    grep -rn "toCanonicalEntity\|augmentWithCanonical" integrations/mcp-server-semantic-analysis/src/ --include="*.ts" | grep -v ".test.ts" | grep -v "// "
    ```
    For each caller, record (a) the file:line, (b) whether the caller has access to a `team` value (read the surrounding 30 lines), (c) what the caller currently passes as `options` to the mapper. This will be the source-cited basis for Plan 02's surgical fix.

    Begin drafting `.planning/forensics/report-42.2-00-canonical-emit.md` with the following sections (use Write tool, not heredoc):
    - `# Forensics report — Phase 42.2 Plan 00 — canonical emit + LLM dispatch gaps`
    - `## Section 1 — canonical-mapper emit paths`
    - subsection 1.1 `toCanonicalEntity` body audit (per-field assignment table with file:line, current value, whether `team` is set)
    - subsection 1.2 callers enumeration (table: caller file:line, has team in scope?, current options arg)
    - subsection 1.3 finding: which exact identifier/parameter should carry team from caller into the mapper (likely thread `options.team` and stamp `metadata.team` in the mapper body)

    Constraint: use `process.stderr.write` style references only when discussing existing log lines — do NOT add new logs in this plan; this is read-only forensics. Honor CLAUDE.md no-console-log constraint by referring to the literal string in prose as `console.*` if mcp-constraint-monitor flags doc lines.
  </action>

  <verify>
    <automated>test -f .planning/forensics/report-42.2-00-canonical-emit.md && grep -c "Section 1" .planning/forensics/report-42.2-00-canonical-emit.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
    <automated>grep -v '^#' .planning/forensics/report-42.2-00-canonical-emit.md | grep -c "canonical-mapper\.ts:[0-9]" | awk '$1 >= 3 {exit 0} {print "ERR: need >=3 source citations to canonical-mapper.ts lines, got " $1; exit 1}'</automated>
    <automated>grep -v '^#' .planning/forensics/report-42.2-00-canonical-emit.md | grep -c "toCanonicalEntity\|augmentWithCanonical" | awk '$1 >= 5 {exit 0} {print "ERR: need >=5 references to mapper entry points, got " $1; exit 1}'</automated>
  </verify>

  <acceptance_criteria>
    - Report file exists at `.planning/forensics/report-42.2-00-canonical-emit.md`
    - Section 1 header present
    - At least 3 source-cited file:line references to `canonical-mapper.ts` (filtered to non-header lines)
    - At least 5 mentions of `toCanonicalEntity`/`augmentWithCanonical` (covers all wave1/2/3 agents plus mapper internals)
    - Section 1.3 finding states explicitly which parameter must be threaded (e.g. `options.team` → `metadata.team` inside `toCanonicalEntity`)
    - No new code or log lines added to source files (read-only audit)
  </acceptance_criteria>

  <done>
    Report file contains a complete Section 1 with the canonical-mapper emit-path audit, every caller enumerated, and a single-sentence finding identifying the exact parameter to thread for `team` propagation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit LLM dispatch sites for `process` field attribution</name>

  <read_first>
    - integrations/mcp-server-semantic-analysis/src/agents/semantic-analyzer.ts (LLMService import at line 7)
    - integrations/mcp-server-semantic-analysis/src/agents/semantic-analysis-agent.ts (LLMService import at line 7)
    - integrations/mcp-server-semantic-analysis/src/agents/git-staleness-detector.ts (LLMService import at line 17)
    - integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts (LLMService import at line 21)
    - integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts (LLMService import at line 16)
    - integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts (LLMService import at line 14)
    - integrations/mcp-server-semantic-analysis/src/agents/wave-controller.ts:333 (LLMCallMetrics conversion)
    - CLAUDE.md § "km-core LLM proxy endpoint" (request shape `{process, messages, taskType?}`)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md (D-Emit scope)
  </read_first>

  <files>.planning/forensics/report-42.2-00-canonical-emit.md (append Section 2)</files>

  <action>
    Use grep to discover every call to the LLMService API (likely `.complete(`, `.chat(`, or similar method on the `LLMService` instance). Run from repo root:
    ```bash
    grep -rn "LLMService\|llmService\.\|\.complete(\|\.chat(\|llmClient\." integrations/mcp-server-semantic-analysis/src/ --include="*.ts" | grep -v ".test.ts" | grep -v "// "
    ```
    For each hit that is an INVOCATION (not an import), inspect the call site and record:
    - file:line
    - which agent owns the call (wave1/wave2/wave3/semantic-analyzer/semantic-analysis-agent/git-staleness-detector/etc.)
    - whether the invocation currently passes a `process` argument anywhere (in the options bag, in the LLMService constructor, in a config object)
    - what value `process` should have per CLAUDE.md guidance (suggested: `wave-analysis` for wave-controller-owned calls; `wave-analysis-wave1` / `wave-analysis-wave2` / `wave-analysis-wave3` for finer granularity per-agent if forensics finds distinct call sites)

    Inspect `@rapid/llm-proxy` SDK surface if needed: check `node_modules/@rapid/llm-proxy/` or `_work/rapid-llm-proxy/` for the `LLMService` class signature to confirm where `process` would be threaded (constructor option vs per-call option vs request body field). Reference the file path of the SDK source in the report so Plan 02's executor can read it directly.

    Append the following sections to the report:
    - `## Section 2 — LLM dispatch sites`
    - subsection 2.1 enumeration table (caller file:line, owner agent, current `process` arg, suggested value)
    - subsection 2.2 SDK surface analysis (where `process` is threadable — constructor opt, per-call opt, or wrapper layer)
    - subsection 2.3 finding: exact set of edits required (e.g. "add `process: 'wave-analysis-wave1'` to LLMService constructor at wave1-project-agent.ts:NN" — concrete identifiers, no placeholders)

    If wave-controller.ts has no direct LLM dispatch (which appears to be the case from current scan), explicitly state this in 2.3 so Plan 02 does NOT edit wave-controller.ts for the LLM gap.
  </action>

  <verify>
    <automated>grep -c "^## Section 2" .planning/forensics/report-42.2-00-canonical-emit.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
    <automated>grep -v '^#' .planning/forensics/report-42.2-00-canonical-emit.md | grep -cE "LLMService|llmService\." | awk '$1 >= 3 {exit 0} {print "ERR: need >=3 LLMService references in audit, got " $1; exit 1}'</automated>
    <automated>grep -v '^#' .planning/forensics/report-42.2-00-canonical-emit.md | grep -cE "process[:=]" | awk '$1 >= 2 {exit 0} {print "ERR: need >=2 process field discussions, got " $1; exit 1}'</automated>
  </verify>

  <acceptance_criteria>
    - Section 2 header present
    - Audit table cites at least 3 LLMService call sites with file:line refs
    - SDK surface analysis identifies where `process` is threadable (constructor / per-call / wrapper) with a concrete file path reference
    - Section 2.3 finding lists the exact edits Plan 02 will make (concrete identifiers, no "placeholder for now")
    - If wave-controller.ts is found to have NO direct LLM dispatch, the report explicitly says so
  </acceptance_criteria>

  <done>
    Report file has a complete Section 2 with LLMService dispatch audit, SDK threading analysis, and an exact-edit finding ready for Plan 02 to act on.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sample the 802 already-migrated entities for team-field presence</name>

  <read_first>
    - .data/knowledge-graph-migrated/leveldb/ (the LevelDB store from Phase 42-05 / 42-07 Phase A)
    - .data/knowledge-graph-migrated/exports/ (JSON exports if present — easier to grep)
    - integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs (the migration script that wrote these 802 entities; lets executor see whether it set `team`)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md (D-Emit — re-migration of 802 is in scope only if team is missing)
  </read_first>

  <files>.planning/forensics/report-42.2-00-canonical-emit.md (append Section 3)</files>

  <action>
    Discover the read path for the migrated entities. First try the JSON exports:
    ```bash
    ls .data/knowledge-graph-migrated/exports/ 2>&1 | head -20
    ```
    If JSON exports are present (likely `*.json` per Phase 41 pattern), read 10 random entries via:
    ```bash
    for f in $(ls .data/knowledge-graph-migrated/exports/*.json | shuf -n 5 2>/dev/null || ls .data/knowledge-graph-migrated/exports/*.json | head -5); do echo "=== $f ==="; head -200 "$f"; done
    ```
    (Use `shuf` if available; fall back to first-5+last-5 for cross-platform.) For each sampled entity, check whether the top-level entity object contains `metadata.team` (or any `team` field). Tally results.

    If JSON exports are NOT present and only `leveldb/` exists, write a short read-only Node script in a tmp file (NOT committed) that opens the leveldb dir via `level`/`classic-level` package (already in node_modules), reads the single `'graph'` key (per B's single-blob LevelDB shape — see Phase 42-05 SUMMARY surprise), and inspects the first 10 nodes. Discard the tmp script after running.

    Alternative if the live container holds a LOCK on the dir: use the snapshot pattern from Phase 42-05 (cp -R the migrated dir to /tmp inside container, remove LOCK, read, then delete the snapshot) OR just inspect the migration script source code at `integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs` to see whether it set `metadata.team` — if the source DOESN'T set team, the 802 don't carry it. Source-inspection is sufficient evidence; live entity reads are confirmation.

    Append to the report:
    - `## Section 3 — 802-entity team-field audit`
    - subsection 3.1 sampling methodology (which entities sampled, how they were read)
    - subsection 3.2 per-entity result table (entity id/name, team present? yes/no/null)
    - subsection 3.3 finding: do the 802 need re-migration (yes if any of the 10 lack team), or are they fine as-is?
    - subsection 3.4 if re-migration needed, recommend modeling Plan 02's one-shot script on `migrate-leveldb-to-kmcore.mjs` per D-Emit
  </action>

  <verify>
    <automated>grep -c "^## Section 3" .planning/forensics/report-42.2-00-canonical-emit.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
    <automated>grep -v '^#' .planning/forensics/report-42.2-00-canonical-emit.md | grep -cE "team.*present|team.*missing|team.*null|team.*set|metadata\.team" | awk '$1 >= 5 {exit 0} {print "ERR: need >=5 team-field findings, got " $1; exit 1}'</automated>
    <automated>grep -cE "re-migration|one-shot|field-augmentation|802" .planning/forensics/report-42.2-00-canonical-emit.md | awk '$1 >= 2 {exit 0} {print "ERR: need explicit re-migration recommendation, got " $1; exit 1}'</automated>
  </verify>

  <acceptance_criteria>
    - Section 3 header present
    - At least 5 specific team-presence findings recorded (e.g. "entity X: team missing", "entity Y: team='coding'")
    - Section 3.3 makes an explicit yes/no recommendation on re-migration
    - If yes, Section 3.4 references `migrate-leveldb-to-kmcore.mjs` as the pattern source for Plan 02
    - No tmp scripts committed; no source files modified
  </acceptance_criteria>

  <done>
    Report file has a complete Section 3 with the 802-entity team-field sample and a binary re-migration recommendation Plan 02 can act on.
  </done>
</task>

<task type="auto">
  <name>Task 4: Synthesize actionable gap list and commit forensics report</name>

  <read_first>
    - .planning/forensics/report-42.2-00-canonical-emit.md (the partially-drafted report from Tasks 1-3)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md (D-Emit + Wave 1 plan A scope)
  </read_first>

  <files>.planning/forensics/report-42.2-00-canonical-emit.md (finalise Section 4)</files>

  <action>
    Append the final synthesis section to the report:
    - `## Section 4 — Actionable gap list for Wave 1 Plan 02`
    - Numbered list of EXACT edits required, each citing a file:line and a one-line description. Format example: `1. integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts:189 — extend CanonicalMapperOptions to include team?: string; thread into metadata.team`
    - Expected size per D-Emit: two-edit floor (canonical-mapper team + LLMService process). If Tasks 1-3 surfaced a third gap (e.g. wave-controller persistWaveResult also drops team), include it as item 3 — do NOT defer it.
    - Subsection 4.1 explicit re-migration verdict: "RE-MIGRATION REQUIRED" or "RE-MIGRATION NOT REQUIRED" (one of these two strings, no equivocation)
    - Subsection 4.2 Plan 02 file-budget estimate: count of submodule files to edit + outer-repo files (expected 2-4 submodule, 0 outer)

    Commit the report via `gsd-sdk query commit` (outer repo only; `.planning/forensics/` is in the outer repo):
    ```bash
    gsd-sdk query commit "docs(42.2-01): forensics report for canonical-emit + LLM dispatch gaps" --files .planning/forensics/report-42.2-00-canonical-emit.md
    ```
  </action>

  <verify>
    <automated>grep -c "^## Section 4" .planning/forensics/report-42.2-00-canonical-emit.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
    <automated>grep -cE "RE-MIGRATION REQUIRED|RE-MIGRATION NOT REQUIRED" .planning/forensics/report-42.2-00-canonical-emit.md | awk '$1 == 1 {exit 0} {print "ERR: need exactly 1 binary re-migration verdict, got " $1; exit 1}'</automated>
    <automated>grep -v '^#' .planning/forensics/report-42.2-00-canonical-emit.md | grep -cE "^[0-9]+\." | awk '$1 >= 2 {exit 0} {print "ERR: need >=2 numbered gap items, got " $1; exit 1}'</automated>
    <automated>git log -1 --pretty=%s | grep -qF "docs(42.2-01): forensics report" && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - Section 4 present with numbered gap list (>=2 items per D-Emit two-edit floor)
    - Exactly one of {`RE-MIGRATION REQUIRED`, `RE-MIGRATION NOT REQUIRED`} string present
    - Plan 02 file-budget estimate present
    - Outer-repo git log shows the forensics commit
    - Report total size > 4KB (substantive content, not stub)
  </acceptance_criteria>

  <done>
    Forensics report complete, committed to outer repo, and contains a binary verdict + gap list Plan 02 can read directly and act on without re-running discovery.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-only forensics over existing source files + a LevelDB store snapshot; no network, no external input, no new code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42.2-01-01 | Information Disclosure | `.planning/forensics/report-42.2-00-canonical-emit.md` | accept | Report references public source file paths + entity names only; no secrets, no PII. Forensics dir is already git-tracked precedent (see `report-20260524-130355.md`) |
| T-42.2-01-02 | Tampering | `.data/knowledge-graph-migrated/leveldb/` | mitigate | Task 3 either uses read-only JSON exports OR snapshots LevelDB to /tmp before reading (Phase 42-05 pattern); never opens the live store while container is up |
| T-42.2-01-03 | Repudiation | Forensics commit | mitigate | Single named commit `docs(42.2-01): forensics report for canonical-emit + LLM dispatch gaps` provides audit trail; report itself cites file:line for every claim |
| T-42.2-01-SC | Tampering | npm/pip/cargo installs | accept | This plan performs ZERO package installs; no supply chain surface |
</threat_model>

<verification>
- Forensics report exists, is >4KB, and has 4 sections with all sub-sections
- Report cites at least 3 file:line refs to `canonical-mapper.ts` and at least 3 LLMService call sites
- Binary re-migration verdict present (one of the two locked strings)
- Outer-repo commit landed
- Wave 1 Plan 02 can read this report verbatim and proceed without further discovery
</verification>

<success_criteria>
- `.planning/forensics/report-42.2-00-canonical-emit.md` exists with Sections 1-4 complete
- Plan 02 (Wave 1 emit-gap fix) has a deterministic file-level edit list
- 802-entity re-migration question is answered binary (yes/no)
- Zero source files modified in this plan (read-only forensics)
</success_criteria>

<output>
Create `.planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-01-SUMMARY.md` when done, mirroring the Phase 42.1.2 SUMMARY style (compact key-decisions list + status + commits).
</output>
