---
phase: 42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le
plan: 02
type: execute
wave: 1
depends_on: ["42.2-01"]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.test.ts
autonomous: true
requirements: []
tags: [canonical-emit, team-attribution, process-attribution, wave-1, submodule-plus-pointer-bump, plan-02-action-from-plan-01-forensics]

user_setup: []

must_haves:
  truths:
    - "Every canonical km-core Entity emitted by wave1/wave2/wave3 agents carries `metadata.team` set to the workflow's team parameter (default 'coding')"
    - "Every LLMService call in the wave-analysis hot path passes `process` (default `wave-analysis` or per-agent identifier per forensics) so token-usage dashboard no longer logs `unknown`"
    - "If forensics Section 3.3 says re-migration required, the 802 already-migrated entities are augmented with `metadata.team='coding'` via a one-shot script modeled on `migrate-leveldb-to-kmcore.mjs`"
    - "Canonical-mapper unit test asserts team propagation; LLM-dispatch test asserts process is set"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts"
      provides: "CanonicalMapperOptions extended with team?: string; metadata.team stamped from options.team"
      contains: "options.team"
    - path: "integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.test.ts"
      provides: "Unit test asserting metadata.team set when team option passed; defaults to undefined when omitted"
      contains: "metadata.team"
    - path: "integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts"
      provides: "Thread team into toCanonicalEntity/augmentWithCanonical calls AND process into LLMService"
      contains: "team"
  key_links:
    - from: "canonical-mapper.ts"
      to: "metadata.team"
      via: "options.team option threaded from agent caller"
      pattern: "metadata\\.team"
    - from: "wave1-project-agent.ts / wave2-component-agent.ts / wave3-detail-agent.ts"
      to: "@rapid/llm-proxy LLMService"
      via: "process: 'wave-analysis' (or finer per-agent identifier per forensics) on LLMService invocation"
      pattern: "process.*wave-analysis"
---

<objective>
Land the canonical-emit gap fix scoped by the Plan 01 forensics report (D-Emit in CONTEXT.md). Two surgical fixes (team + process); a third folded in only if forensics surfaced one. Optional one-shot field-augmentation of the 802 already-migrated entities iff forensics Section 3.3 says RE-MIGRATION REQUIRED.

Purpose: Close the Plan 42-06 deliverable gap that operator observation surfaced after the overnight `ukb` run — `team` missing on every emitted Entity (km-core dedup/merge degraded to no-op), `process` missing on every LLM call (token-usage shows `unknown`). Without this fix Wave 3's SC#2 and SC#5 are un-verifiable per 42-07-SUMMARY § "What is DEFERRED" item 3.

Output: Submodule TS edits to canonical-mapper + at least the three wave agents + unit tests; submodule build; submodule commit + outer-repo pointer-bump commit. Optional re-migration script if forensics demands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md
@.planning/forensics/report-42.2-00-canonical-emit.md
@.planning/phases/42-offline-ukb-migration-b/42-07-SUMMARY.md
@.planning/phases/42.1-ukb-project-anchor-parity/42.1-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. The forensics report is the source of truth; this block is a refresher. -->

canonical-mapper.ts current signature (file lines 98-103):
```typescript
export function toCanonicalEntity(
  raw: KGEntity,
  ontologyClass: string,
  runId: string,
  options: CanonicalMapperOptions = {},   // <-- team?: string goes HERE
): Entity;
```

Required edit pattern (concrete identifiers, not pseudo-code):
- Extend `CanonicalMapperOptions` (declared in canonical-mapper.ts ~line 67-90) with `team?: string`
- Inside `toCanonicalEntity` body where `baseMetadata` is assembled (~lines 143-152), add: `if (typeof options.team === 'string' && options.team.length > 0) baseMetadata.team = options.team;`
- `augmentWithCanonical` (lines 234-260) passes `options` through verbatim — no change needed there if signature is preserved

Wave-agent caller pattern: each agent has a `team` value in scope (from workflow params / wave-controller injection). Thread it through:
- wave1-project-agent.ts: pass `{ team }` in the options bag at each `toCanonicalEntity`/`augmentWithCanonical` call
- wave2-component-agent.ts: same
- wave3-detail-agent.ts: same

LLMService process-field threading (per forensics Section 2):
- Pattern likely either constructor-level (`new LLMService({ process: 'wave-analysis-wave1' })`) or per-call (`.complete({ process: 'wave-analysis-wave1', ... })`); forensics report locks the exact form
- CLAUDE.md request body: `{ process, messages, taskType? }` — the SDK lifts `process` from its options into the body
- Suggested values per agent (planner discretion per D-Emit "Claude's Discretion"):
  - wave-controller's own dispatches (if any per Section 2): `process: 'wave-analysis'`
  - wave1-project-agent: `process: 'wave-analysis-wave1'`
  - wave2-component-agent: `process: 'wave-analysis-wave2'`
  - wave3-detail-agent: `process: 'wave-analysis-wave3'`
  - semantic-analyzer / quality-assurance / git-staleness-detector: per their owner-agent name

km-core-adapter precedent (already plumbs team — see km-core-adapter.ts:151 `runId: 'wave-analysis-${team}'`). canonical-mapper must align.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend canonical-mapper to accept and stamp team; add RED unit test then GREEN</name>

  <behavior>
    - Test 1 (RED first): `toCanonicalEntity(raw, ontologyClass, runId, { team: 'coding' })` produces an Entity whose `metadata.team === 'coding'`
    - Test 2: `toCanonicalEntity(raw, ontologyClass, runId)` (no options) produces an Entity whose `metadata.team` is undefined (not null, not empty string)
    - Test 3: `augmentWithCanonical(raw, ontologyClass, runId, { team: 'coding' })` propagates team into the augmented entity's metadata (regression guard for the options-passthrough invariant)
    - Test 4 (edge): empty-string team (`{ team: '' }`) MUST NOT stamp `metadata.team` (validates the `length > 0` guard)
  </behavior>

  <read_first>
    - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts (full file — 280 lines; primary edit target)
    - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.test.ts (if exists; else create — node:test convention per 42.1.2)
    - .planning/forensics/report-42.2-00-canonical-emit.md § Section 1 + Section 4 item 1 (the locked edit scope)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md (D-Emit)
    - CLAUDE.md § "Rebuilding After Code Changes" (npm run build mandate after every TS edit in this submodule)
  </read_first>

  <files>
    integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts,
    integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.test.ts
  </files>

  <action>
    RED step — Either create `integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.test.ts` (built-in node:test + `node:assert/strict`, NO third-party framework per CLAUDE.md / Phase 42 pattern) with the 4 tests listed in `<behavior>`, OR append them to an existing test file if one is present. Use `import { describe, it } from 'node:test'; import assert from 'node:assert/strict'`. Tests import from the compiled `dist/agents/canonical-mapper.js` after the build step (or from `../canonical-mapper.ts` if the file colocates with source per project convention — check what `wave-controller-canonical-emit.test.ts` does and mirror).

    Run the test once before editing source — expect Tests 1, 3, 4 to FAIL and Test 2 to PASS (undefined-team default already works). RED gate satisfied.

    GREEN step — Edit `canonical-mapper.ts`:
    - Locate `CanonicalMapperOptions` interface (currently around lines 67-90). Add `team?: string;` field with a JSDoc line: `/** Project/team identifier; stamped into metadata.team when set. Set by wave-agent callers from workflow parameters.team. */`
    - Locate `baseMetadata` assembly inside `toCanonicalEntity` (lines 143-152). After the `legacyObservations: observations,` line and before the next `if`-block (around line 154), insert: `if (typeof options.team === 'string' && options.team.length > 0) baseMetadata.team = options.team;`
    - No change to `augmentWithCanonical` body needed — it passes `options` through to `toCanonicalEntity` per line 240 (`const canonical = toCanonicalEntity(raw, ontologyClass, runId, options);`)

    Rebuild + run tests:
    ```bash
    cd integrations/mcp-server-semantic-analysis && npm run build && node --test dist/agents/canonical-mapper.test.js
    ```
    All 4 tests must pass GREEN.

    Per CLAUDE.md no-console-log: do NOT add any `console.*` calls. Use `process.stderr.write` only if absolutely necessary (none expected here).
  </action>

  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build 2>&1 | tail -3 | grep -qE "build|tsc" && echo BUILD_OK</automated>
    <automated>cd integrations/mcp-server-semantic-analysis && node --test dist/agents/canonical-mapper.test.js 2>&1 | grep -E "^# (pass|fail)" | head -5</automated>
    <automated>grep -c "options\.team" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts | awk '$1 >= 1 {exit 0} {print "ERR: options.team not threaded, got " $1; exit 1}'</automated>
    <automated>grep -c "team\?: string" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts | awk '$1 >= 1 {exit 0} {print "ERR: team field not added to CanonicalMapperOptions, got " $1; exit 1}'</automated>
    <automated>grep -c "metadata\.team" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.test.ts | awk '$1 >= 3 {exit 0} {print "ERR: need >=3 metadata.team assertions in test file, got " $1; exit 1}'</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0 inside submodule
    - `node --test dist/agents/canonical-mapper.test.js` shows 4/4 pass, 0 fail
    - Source grep: `options\.team` appears in canonical-mapper.ts (proves the stamp is wired)
    - Source grep: `team\?: string` appears in canonical-mapper.ts (proves interface extension)
    - Test file has >=3 `metadata.team` assertions (covers test 1, 3, 4)
    - Zero `console.*` calls added (CLAUDE.md no-console-log)
  </acceptance_criteria>

  <done>
    canonical-mapper.ts extended with team option; 4 RED→GREEN tests pass; submodule build clean. Ready for Task 2 (wave-agent callers thread team).
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread team + process into wave1/wave2/wave3 agents</name>

  <read_first>
    - integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts (every `toCanonicalEntity` / `augmentWithCanonical` call site + every `LLMService` invocation)
    - integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts (same)
    - integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts (same)
    - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts (the post-Task-1 surface with team option)
    - .planning/forensics/report-42.2-00-canonical-emit.md § Section 2 + Section 4 item 2 (locked LLMService threading scope)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md (D-Emit; Claude's Discretion on per-agent `process` granularity)
    - CLAUDE.md (no-console-log; submodule build mandate)
  </read_first>

  <files>
    integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts,
    integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts,
    integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts
  </files>

  <action>
    For each wave agent file (1, 2, 3):

    (a) Find every `toCanonicalEntity` / `augmentWithCanonical` call. Each agent has a `team` value in scope — either passed via the agent constructor (most likely; check the class header), via the wave-controller-injected `runOptions`, or via the workflow `parameters.team`. Identify the in-scope variable name and add `team` to the options bag passed to the mapper. Per-call example pattern (concrete identifiers to find): `toCanonicalEntity(raw, ontologyClass, runId, { team: this.team })` (the exact field name `this.team` vs `runOptions.team` vs `params.team` is determined by reading the agent class — pick the one the agent already exposes).

    (b) Find every `LLMService` construction or method invocation. Per the forensics Section 2 finding (the SDK threading point: constructor opt vs per-call opt), thread `process: 'wave-analysis-wave1'` (for wave1-project-agent.ts), `'wave-analysis-wave2'` (wave2), `'wave-analysis-wave3'` (wave3) into the appropriate slot. If the forensics report identified a different finer-granularity scheme (e.g. per-LLM-call-purpose like `wave-analysis-classify` / `wave-analysis-merge`), use that instead — forensics is the source of truth, not this plan's suggestion.

    (c) If the agent file already has a `process` argument set elsewhere (e.g. some older code path), align with the existing convention.

    Rebuild submodule:
    ```bash
    cd integrations/mcp-server-semantic-analysis && npm run build
    ```
    Build must exit 0 (TypeScript compilation clean — proves the team-threading and process-threading parameters match expected shapes).

    Re-run the canonical-mapper test from Task 1 (regression check):
    ```bash
    cd integrations/mcp-server-semantic-analysis && node --test dist/agents/canonical-mapper.test.js
    ```
    All 4 tests still pass.

    Run the existing wave-controller-canonical-emit test suite to confirm no regression:
    ```bash
    cd integrations/mcp-server-semantic-analysis && node --test dist/agents/wave-controller-canonical-emit.test.js
    ```
    All existing tests still pass (12 expected per 42-07-SUMMARY § Test counts).

    Honor CLAUDE.md no-console-log throughout.
  </action>

  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build 2>&1 | tail -3</automated>
    <automated>grep -cE "team[:=]" integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts | awk '$1 >= 1 {exit 0} {print "ERR: wave1 missing team thread, got " $1; exit 1}'</automated>
    <automated>grep -cE "team[:=]" integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts | awk '$1 >= 1 {exit 0} {print "ERR: wave2 missing team thread, got " $1; exit 1}'</automated>
    <automated>grep -cE "team[:=]" integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts | awk '$1 >= 1 {exit 0} {print "ERR: wave3 missing team thread, got " $1; exit 1}'</automated>
    <automated>grep -cE "process.*['\"]wave-analysis" integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts | wc -l | awk '$1 >= 3 {exit 0} {print "ERR: need process attr on all 3 agents, got " $1; exit 1}'</automated>
    <automated>cd integrations/mcp-server-semantic-analysis && node --test dist/agents/canonical-mapper.test.js dist/agents/wave-controller-canonical-emit.test.js 2>&1 | grep -E "^# (pass|fail|tests)" | head -5</automated>
  </verify>

  <acceptance_criteria>
    - Submodule build exits 0
    - Each of wave1/wave2/wave3 agent files contains `team[:=]` (proves threading)
    - Each of wave1/wave2/wave3 agent files contains `process.*'wave-analysis` (proves process attribution)
    - canonical-mapper.test.js still 4/4 pass
    - wave-controller-canonical-emit.test.js still 12/12 pass (no regression)
    - Zero `console.*` added
  </acceptance_criteria>

  <done>
    All three wave agents thread team into canonical-mapper calls AND process into LLMService calls; submodule build clean; existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Conditional 802-entity re-migration (if forensics Section 4.1 says RE-MIGRATION REQUIRED)</name>

  <read_first>
    - .planning/forensics/report-42.2-00-canonical-emit.md § Section 4.1 (the binary verdict)
    - integrations/mcp-server-semantic-analysis/scripts/migrate-leveldb-to-kmcore.mjs (pattern source per D-Emit)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md (D-Emit re-migration clause)
    - CLAUDE.md § "km-core scripts" (every km-core consumer MUST construct GraphKMStore with an ontologyDir option — Phase 41 lesson)
  </read_first>

  <files>
    integrations/mcp-server-semantic-analysis/scripts/augment-team-field-42.2.mjs (CREATED only if RE-MIGRATION REQUIRED; else SKIP this task)
  </files>

  <action>
    Read `.planning/forensics/report-42.2-00-canonical-emit.md` § Section 4.1.

    **If the verdict is `RE-MIGRATION NOT REQUIRED`:** Skip this task entirely. Append a one-line note to the eventual SUMMARY: "Task 3 skipped per forensics Section 4.1." Proceed to Task 4.

    **If the verdict is `RE-MIGRATION REQUIRED`:** Author a focused one-shot script at `integrations/mcp-server-semantic-analysis/scripts/augment-team-field-42.2.mjs`. Model it on `migrate-leveldb-to-kmcore.mjs` (same dir) — use the same `GraphKMStore` construction pattern WITH `ontologyDir` option (per CLAUDE.md km-core scripts mandate; missing this option throws `opts.classes omitted but store has no ontology registry`). Resolve ontologyDir via `import.meta.resolve('@fwornle/km-core')` walk + `.data/ontologies/` per Phase 41 commits `87bc2f567` / `fd35c5350`.

    Script behavior:
    - Open `.data/knowledge-graph-migrated/` (the canonical store as of Phase 42-07 Phase A)
    - Iterate all entities via `store.iterate()` (the km-core 0.1.0 surface used by km-core-adapter)
    - For each entity where `metadata.team` is undefined/missing, call `store.mergeNodeAttributes(entity.id, { metadata: { ...entity.metadata, team: 'coding' } })`
    - Print running counters: `{ scanned, augmented, skipped, errored }`
    - Fail-loud at >5% error budget (mirror migrate-leveldb-to-kmcore.mjs convention per Phase 42-05 SUMMARY)
    - Idempotent: re-running the script must be a no-op (entities with team already set are skipped)

    Test the script with `--dry-run` flag (if pattern source supports it; else add the flag — Phase 42-05 had one). Run dry-run inside the container OR against a /tmp snapshot per Phase 42-05 LOCK-file precedent. Do NOT run the real augmentation in this plan — Wave 2 Plan 05 (dir-swap) is when the migrated dir becomes canonical and any re-augmentation must happen against the post-swap dir. Document this in the script header.

    Add the script to the submodule.
  </action>

  <verify>
    <automated>VERDICT=$(grep -E "RE-MIGRATION (REQUIRED|NOT REQUIRED)" .planning/forensics/report-42.2-00-canonical-emit.md | head -1); echo "Verdict: $VERDICT"; if echo "$VERDICT" | grep -q "NOT REQUIRED"; then echo "SKIPPED per forensics"; exit 0; else test -f integrations/mcp-server-semantic-analysis/scripts/augment-team-field-42.2.mjs && echo SCRIPT_EXISTS; fi</automated>
    <automated>VERDICT=$(grep -E "RE-MIGRATION (REQUIRED|NOT REQUIRED)" .planning/forensics/report-42.2-00-canonical-emit.md | head -1); if echo "$VERDICT" | grep -q "NOT REQUIRED"; then exit 0; else grep -c "ontologyDir" integrations/mcp-server-semantic-analysis/scripts/augment-team-field-42.2.mjs | awk '$1 >= 1 {exit 0} {print "ERR: ontologyDir missing per CLAUDE.md km-core scripts mandate"; exit 1}'; fi</automated>
    <automated>VERDICT=$(grep -E "RE-MIGRATION (REQUIRED|NOT REQUIRED)" .planning/forensics/report-42.2-00-canonical-emit.md | head -1); if echo "$VERDICT" | grep -q "NOT REQUIRED"; then exit 0; else grep -cE "dry-run|dryRun|DRY_RUN" integrations/mcp-server-semantic-analysis/scripts/augment-team-field-42.2.mjs | awk '$1 >= 1 {exit 0} {print "ERR: script missing dry-run flag"; exit 1}'; fi</automated>
  </verify>

  <acceptance_criteria>
    - If forensics verdict is RE-MIGRATION NOT REQUIRED: task skipped, no script created (verified by the first automated check exiting 0 on the "SKIPPED" branch)
    - If forensics verdict is RE-MIGRATION REQUIRED: script exists at `integrations/mcp-server-semantic-analysis/scripts/augment-team-field-42.2.mjs`
    - Script constructs GraphKMStore WITH `ontologyDir` option (CLAUDE.md mandate)
    - Script supports `--dry-run`
    - Script header documents: "Run AFTER Wave 2 Plan 05 dir-swap, not before — migrated dir becomes canonical only post-swap"
    - Script not executed in this plan (only authored)
  </acceptance_criteria>

  <done>
    Re-migration script authored if forensics says required; skipped cleanly otherwise.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit submodule + outer-repo pointer-bump pair</name>

  <read_first>
    - .planning/phases/42.1-ukb-project-anchor-parity/42.1-01-SUMMARY.md (cross-repo commit dance precedent)
    - .planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-CONTEXT.md § "Established Patterns" (submodule plans require dual commits because src/agents/ is a real dir, NOT a symlink — confirmed by `ls -la` showing `drwxr-xr-x` not `lrwxr-xr-x`)
    - CLAUDE.md § "Rebuilding After Code Changes" (npm run build before commit)
  </read_first>

  <files>
    (commits only — files already edited in Tasks 1-3)
  </files>

  <action>
    Inside the submodule at `integrations/mcp-server-semantic-analysis/`:
    ```bash
    cd integrations/mcp-server-semantic-analysis
    git status   # review modified files: src/agents/canonical-mapper.ts, src/agents/canonical-mapper.test.ts, src/agents/wave1-project-agent.ts, src/agents/wave2-component-agent.ts, src/agents/wave3-detail-agent.ts, possibly scripts/augment-team-field-42.2.mjs
    git add src/agents/canonical-mapper.ts src/agents/canonical-mapper.test.ts src/agents/wave1-project-agent.ts src/agents/wave2-component-agent.ts src/agents/wave3-detail-agent.ts
    # Add re-migration script only if Task 3 created it:
    [ -f scripts/augment-team-field-42.2.mjs ] && git add scripts/augment-team-field-42.2.mjs
    git commit -m "$(cat <<'EOF'
feat(42.2-02): thread team into canonical-mapper + process into LLMService dispatches

Closes the Plan 42-06 canonical-emit gap surfaced by operator overnight ukb
observation: every emitted Entity now carries metadata.team (default 'coding'
from workflow params); every wave-agent LLMService call now sets process for
token-usage attribution per CLAUDE.md /api/complete request shape.

Per .planning/forensics/report-42.2-00-canonical-emit.md gap list.
EOF
)"
    SUBMODULE_SHA=$(git rev-parse HEAD)
    cd ../..
    git add integrations/mcp-server-semantic-analysis
    git commit -m "$(cat <<EOF
feat(42.2-02): bump semantic-analysis submodule for canonical-emit gap fix

Pointer to ${SUBMODULE_SHA} — adds team/process attribution per
.planning/forensics/report-42.2-00-canonical-emit.md.
EOF
)"
    ```

    If Task 3 produced an augmentation script, do NOT execute it here — it runs as a post-Wave-2-Plan-05 manual operator step OR as a follow-up phase per the script header. Wave 3 SC verifier will read the migrated entities and either pass or surface a regression.
  </action>

  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && git log -1 --pretty=%s | grep -qF "feat(42.2-02): thread team into canonical-mapper" && echo SUBMODULE_OK</automated>
    <automated>git log -1 --pretty=%s | grep -qF "feat(42.2-02): bump semantic-analysis submodule" && echo OUTER_OK</automated>
    <automated>git diff HEAD~1 HEAD -- integrations/mcp-server-semantic-analysis | grep -cE "^[-+]Subproject commit" | awk '$1 == 2 {exit 0} {print "ERR: pointer-bump diff malformed, got " $1; exit 1}'</automated>
  </verify>

  <acceptance_criteria>
    - Submodule HEAD commit subject matches `feat(42.2-02): thread team into canonical-mapper + process into LLMService dispatches`
    - Outer-repo HEAD commit subject matches `feat(42.2-02): bump semantic-analysis submodule for canonical-emit gap fix`
    - Outer-repo diff shows exactly one `Subproject commit` line removed + one added (pointer-bump shape)
    - If Task 3 created the augmentation script, it's committed; if not, no orphan files
  </acceptance_criteria>

  <done>
    Submodule + outer-repo pointer-bump pair landed; canonical-emit gap closed at source level. Wave 2 Plans 04/05 can begin (Plan 02 + Plan 03 are this wave's siblings; Wave 2 starts only after BOTH complete).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow params -> canonical Entity metadata | `parameters.team` (user-controlled at MCP layer) now flows into `metadata.team` on every persisted Entity |
| Wave agent -> rapid-llm-proxy | New `process` field flows into `POST /api/complete` request body for token-usage attribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42.2-02-01 | Tampering | canonical-mapper.ts options.team -> metadata.team | mitigate | Strict typecheck `typeof options.team === 'string' && options.team.length > 0` rejects undefined/null/empty/non-string inputs; no eval/no template-injection surface |
| T-42.2-02-02 | Information Disclosure | metadata.team in km-core store | accept | Team values are project identifiers ('coding', etc.) already public in repo and workflow params; no new sensitive data introduced |
| T-42.2-02-03 | Spoofing | LLMService process attribution | mitigate | `process` strings are hardcoded per agent (`'wave-analysis-wave1'` etc.), not user-controlled; token-usage tracking gains stronger attribution, not weaker |
| T-42.2-02-04 | Tampering | augment-team-field-42.2.mjs (if created) | mitigate | Idempotent + fail-loud at >5% error budget; dry-run mode default; runs only post-swap per script header; ontologyDir option per CLAUDE.md prevents the Phase 41 default-class-resolution bug |
| T-42.2-02-SC | Tampering | npm/pip/cargo installs | accept | This plan performs ZERO package installs; submodule npm run build uses existing locked deps |
</threat_model>

<verification>
- Submodule build clean (TypeScript compiles after team + process plumbing)
- canonical-mapper.test.js 4/4 pass
- wave-controller-canonical-emit.test.js 12/12 still pass (regression guard)
- Grep gates confirm team threaded in canonical-mapper + all 3 wave agents
- Grep gates confirm process attribution in all 3 wave agents
- Submodule + outer-repo commit pair landed with matching SHAs
- Wave 3 SC verifier (Plan 06) will exercise the end-to-end behavior; this plan ships the surgical fix only
</verification>

<success_criteria>
- canonical-mapper.ts exports CanonicalMapperOptions.team and stamps metadata.team
- All wave-agent emit sites thread team
- All wave-agent LLM dispatch sites thread process
- 802 re-migration script authored only if forensics demanded it
- Cross-repo commit pair landed
</success_criteria>

<output>
Create `.planning/phases/42.2-retire-deferred-42-07-work-legacy-persistence-trio-atomic-le/42.2-02-SUMMARY.md` when done.
</output>
