---
phase: 86-timeline-v2-and-declutter
plan: 04
type: execute
wave: 2
depends_on: ["86-01", "86-02"]
files_modified:
  - integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx
  - tests/e2e/dashboard/performance-compare.spec.ts
autonomous: true
requirements: [VALID-01, ATTR-02]
must_haves:
  truths:
    - "Selecting two paired runs renders a side-by-side trajectory diff aligned by run-align: the identical prefix is collapsed by default, the view starts at the first divergence, per-aligned-pair cumulative token deltas are shown, and loop badges mark repeated signatures on either side"
    - "The diff header reads canonical_model VERBATIM (null → italic unmeasured) — never recomputes per surface (ATTR-02 no-regression)"
    - "Alignment runs on ContextTurnRow[] (never TimelineRow/timestamps) and fires no network call; the isExperimentCell narrative guard is carried in"
    - "Identical runs (firstDivergence===null) render the 'these two runs are identical' empty state, not a broken diff"
  artifacts:
    - path: "integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx"
      provides: "full divergence-point trajectory diff (D-07/D-08)"
      exports: ["DifferenceViewer"]
      min_lines: 60
  key_links:
    - from: "difference-viewer.tsx"
      to: "run-align.ts"
      via: "alignRuns(aTurns, bTurns) — pure, no network"
      pattern: "alignRuns\\("
    - from: "difference-viewer.tsx"
      to: "loop-heuristic.ts"
      via: "loopFlags per side"
      pattern: "loopFlags"
    - from: "difference-viewer.tsx"
      to: "performanceSlice contextTurns"
      via: "selectContextTurnsFor(aId)/(bId) + fetchContextTurns"
      pattern: "selectContextTurnsFor"
---

<objective>
Build the full difference viewer (D-07/D-08) — the user's north-star. It reads two runs' `ContextTurnRow[]` from the store, aligns them with the pure `run-align.ts` (first-divergence + LCS tail), collapses the identical prefix, renders the divergent tail side-by-side with per-aligned-pair cumulative token deltas, and flags looped signatures via `loop-heuristic.ts`. It sits as a new surface in the Compare tab (Wave-3 wiring switches the tab / passes the selected pair). Extend the compare e2e spec.

Purpose: "see where two runs differ and how the different decisions lead to more or fewer tokens, more or fewer loops."
Output: one new component + extended e2e coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-timeline-v2-and-declutter/86-UI-SPEC.md
@.planning/phases/86-timeline-v2-and-declutter/86-PATTERNS.md
@integrations/system-health-dashboard/src/components/performance/run-compare.tsx
@CLAUDE.md

<interfaces>
<!-- FROM PLAN 86-01: alignRuns(a,b): { prefixLen, firstDivergence: number|null, pairs: {a:number|null;b:number|null}[] } from ./run-align ; loopFlags(turns): boolean[] from ./loop-heuristic -->
<!-- FROM PLAN 86-02: <ContextBand .../> from ./context-band -->

<!-- Existing slice contract this consumes (do NOT modify the slice — Plan 02 owns it): -->
<!--   selectContextTurnsFor(taskId) → ContextTurnRow[]  (performanceSlice.ts L1245) -->
<!--   fetchContextTurns(taskId) thunk (L531) -->
<!--   selectCompareA/selectCompareB (L1233-1234) — the two run ids to diff -->
<!--   Run.canonical_model / Run.loop_count (L59/L74) — read verbatim -->

<!-- run-compare.tsx analogs to copy: -->
<!--   Delta L44-60 (signed cumulative token delta render; UI-SPEC Q5 refines palette to status-* tokens) -->
<!--   two-run read L124-134 (adapt from timeline rows to context-turns) -->
<!-- Collapsible (timeline.tsx S5) for the "Show N identical turns" collapsed prefix -->
<!-- isExperimentCell guard: timeline.tsx L465 pattern (variant/base_variant present) -->
<!-- UI-SPEC §5 diff contract; empty states from Copywriting (identical / single-run) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: difference-viewer.tsx — aligned side-by-side trajectory diff (D-07)</name>
  <files>integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/run-compare.tsx (L44-60 Delta, L120-134 two-run read pattern)
    - integrations/system-health-dashboard/src/components/performance/run-align.ts (alignRuns contract — from Plan 01)
    - integrations/system-health-dashboard/src/components/performance/timeline.tsx (L205-224 Collapsible S5; L465 isExperimentCell)
    - .planning/phases/86-timeline-v2-and-declutter/86-UI-SPEC.md (§5 diff viewer contract, Q5 delta palette, Copywriting empty states)
    - .planning/phases/86-timeline-v2-and-declutter/86-PATTERNS.md (difference-viewer.tsx assignment, S2 honest-null, S4 guard, S5 collapse)
  </read_first>
  <behavior>
    - Reads aId=selectCompareA, bId=selectCompareB; dispatches fetchContextTurns for each; reads aTurns/bTurns via selectContextTurnsFor.
    - Computes { prefixLen, firstDivergence, pairs } = alignRuns(aTurns, bTurns) — pure, no network.
    - firstDivergence===null → renders "These two runs are identical — no divergence to show." (Copywriting), no diff table.
    - Only aId set (bId null) → "Select a second run to compare."
    - Prefix [0, prefixLen) collapsed by default in a Collapsible labeled "Show N identical turns".
    - Divergent tail: each pair renders A-side + B-side turns; per-pair cumulative token delta (B−A) font-mono with explicit +/−, decrease→text-status-success, increase→text-muted-foreground (Q5). One-sided pairs (a:null or b:null) render in one column, other dimmed.
    - loopFlags(aTurns)/(bTurns) mark looped turns on each side; Run.loop_count surfaced as the "hard" number beside advisory badges.
    - Header reads run.canonical_model verbatim; null → italic "unmeasured".
  </behavior>
  <action>Create `difference-viewer.tsx` exporting `DifferenceViewer`. Two-run read: adapt run-compare's L124-134 pattern to context-turns — `const aTurns = useAppSelector(selectContextTurnsFor(aId))`, same for b; `useEffect` dispatch `fetchContextTurns(aId)`/`(bId)`. Call `alignRuns(aTurns, bTurns)` (key_link — pure). Render the two empty states (identical / single-run) from Copywriting. Collapse the identical prefix `[0, prefixLen)` with the existing Radix `Collapsible` (S5), trigger "Show {prefixLen} identical turns". Render the divergent tail from `pairs`: for each `{a,b}` show the A-side and B-side turn (reuse `<ContextBand>` for the mini band; keep it lightweight — do not import the full TurnRow to avoid a Plan-03 file dependency, render a compact inline row). Compute per-pair cumulative token delta and render with a `Delta`-style signed span using `status-*` tokens (Q5): decrease → `text-status-success`, increase → `text-muted-foreground`, always explicit `+`/`−`. Flag loops per side via `loopFlags` (key_link) with the advisory outline badge + tooltip; surface `run.loop_count` beside it as the "hard" strict count (null → em-dash, S2). Header: read `run.canonical_model` VERBATIM for both runs (S2 — null → italic "unmeasured"; NEVER recompute — ATTR-02 anchor). Carry the `isExperimentCell` guard: align on ContextTurnRow only, do NOT re-run the observation join for cells (S4 / Pitfall 4). `data-testid="difference-viewer"`.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build 2>&1 | grep -iE 'error TS'; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Aligns via the pure module: `grep -c "alignRuns(" integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx` ≥ 1.
    - Aligns on context-turns not timeline/timestamps: `grep -c "selectContextTurnsFor" integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx` ≥ 2 AND `grep -Ec "selectTimelineFor|\.ts\b.*sort|align.*timestamp" integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx` returns 0.
    - Loop badges wired: `grep -c "loopFlags" integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx` ≥ 1.
    - Canonical verbatim, honest null: `grep -c "canonical_model" ...difference-viewer.tsx` ≥ 1 AND `grep -c "unmeasured" ...difference-viewer.tsx` ≥ 1.
    - Identical-runs empty state: `grep -c "identical" integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx` ≥ 1 AND `grep -c "firstDivergence" ...difference-viewer.tsx` ≥ 1.
    - Prefix collapse present: `grep -Ec "Collapsible|prefixLen" integrations/system-health-dashboard/src/components/performance/difference-viewer.tsx` ≥ 2.
    - `npm run build` no new `error TS`.
  </acceptance_criteria>
  <done>The difference viewer aligns two runs' context-turns via run-align, collapses the identical prefix, renders the divergent tail with signed cumulative deltas + loop badges, reads canonical model verbatim, and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend performance-compare.spec.ts for the difference viewer (D-07/D-08)</name>
  <files>tests/e2e/dashboard/performance-compare.spec.ts</files>
  <read_first>
    - tests/e2e/dashboard/performance-compare.spec.ts (existing structure, navigate helper, data-presence skip guard)
    - tests/e2e/dashboard/performance.spec.ts (navigateToPerformance L21-29, runRowCount, skip pattern — reuse the same idioms)
  </read_first>
  <action>Extend `performance-compare.spec.ts` with a test that (guarded by the existing data-presence skip when <2 runs) drives the difference viewer surface: assert `data-testid="difference-viewer"` renders when two runs are compared, that the collapsed identical-prefix trigger ("Show N identical turns") is present when the runs share a prefix, and that the canonical-model header renders (verbatim value or "unmeasured"). Reuse `navigateToPerformance`, `runRowCount`, and the `http://localhost:3032` baseURL — do NOT hand-roll a new browser bootstrap. Keep the assertions resilient to empty data (skip, never fail, when no paired runs exist).</action>
  <verify>
    <automated>npx playwright test tests/e2e/dashboard/performance-compare.spec.ts --list 2>&1 | grep -c difference || true</automated>
  </verify>
  <acceptance_criteria>
    - New assertions reference the diff surface: `grep -Ec "difference-viewer|Show .*identical turns|Compare selected" tests/e2e/dashboard/performance-compare.spec.ts` ≥ 1.
    - Data-presence skip preserved: `grep -c "test.skip\|runRowCount" tests/e2e/dashboard/performance-compare.spec.ts` ≥ 1.
    - `npx playwright test tests/e2e/dashboard/performance-compare.spec.ts --list` enumerates the new test without a load error.
  </acceptance_criteria>
  <done>The compare e2e spec covers the difference-viewer surface with a data-presence skip guard and lists cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two stored runs' context-turns → DOM | tool/preview strings rendered in the diff rows are untrusted text |
| compareA/compareB run ids → store read | ids come from runs-table selection; used only to key store reads (no injection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04-01 | Information disclosure | secret in a diff-row preview/tool string | mitigate | Any preview text rendered in a diff row passes through the exported `scrubSecrets` (from context-cache-explainer, Plan 02); prefer rendering tool name+size, not raw args. |
| T-86-04-02 | Tampering | recomputing canonical model per-surface (re-introducing finding-B divergence) | mitigate | Header reads `run.canonical_model` verbatim; null → "unmeasured" — grep-gated. No per-surface recompute (ATTR-02). |
| T-86-04-03 | Spoofing | experiment-cell narrative bleed in the diff | mitigate | Align on ContextTurnRow (per-cell-correct) + carry `isExperimentCell` guard; no observation time-window join for cells (Pitfall 4 / S4). |
| T-86-04-04 | Tampering (XSS) | unescaped diff-row text | mitigate | React default escaping; no `dangerouslySetInnerHTML`. |
| T-86-04-SC | Tampering | npm installs | mitigate | No packages — hand-rolled LCS lives in run-align (Plan 01), no diff library. No slopcheck surface. |
</threat_model>

<verification>
- Dashboard rebuild + tsc clean.
- Alignment is on ContextTurnRow via alignRuns, no network, canonical verbatim.
- Compare e2e lists the new diff test.
</verification>

<success_criteria>
- DifferenceViewer aligns two paired runs, collapses identical prefix, renders divergent tail with signed cumulative deltas + loop badges, reads canonical model verbatim, handles identical/single-run empty states.
- No new npm package.
</success_criteria>

<output>
Create `.planning/phases/86-timeline-v2-and-declutter/86-04-SUMMARY.md` when done.
</output>
