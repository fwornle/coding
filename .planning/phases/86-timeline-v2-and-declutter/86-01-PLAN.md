---
phase: 86-timeline-v2-and-declutter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/system-health-dashboard/src/components/performance/run-align.ts
  - integrations/system-health-dashboard/src/components/performance/loop-heuristic.ts
  - test/performance/run-align.test.js
  - test/performance/loop-heuristic.test.js
  - test/performance/fixtures/context-turns-fixtures.js
  - test/performance/context-turns-granularity.test.js
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "The A3/OQ1 granularity assumption is EMPIRICALLY confirmed against a real captured run: a context-turns.jsonl line is one-per-LLM-request carrying no per-reasoning-step/granularity_tier field, so alignRuns operates on the parent-turn-equivalent request sequence with NO pre-flatten; OQ1 marked RESOLVED in RESEARCH.md"
    - "alignRuns returns prefixLen/firstDivergence/pairs for a fixture rerun-pair; identical runs → firstDivergence null; a re-converging tail re-syncs via LCS"
    - "loopFlags flags a non-adjacent windowed repeat, ignores out-of-window matches and pure-reasoning turns"
    - "Both modules are dependency-free (import only a local ContextTurnRow-subset type) and testable by root Jest without a new package"
  artifacts:
    - path: "integrations/system-health-dashboard/src/components/performance/run-align.ts"
      provides: "turnSignature + alignRuns (common-prefix walk + LCS tail pairing)"
      exports: ["turnSignature", "alignRuns", "sizeBucket"]
      min_lines: 60
    - path: "integrations/system-health-dashboard/src/components/performance/loop-heuristic.ts"
      provides: "loopFlags windowed fuzzy repeat detector; shares turnSignature"
      exports: ["loopFlags", "LOOP_WINDOW"]
      min_lines: 20
    - path: "test/performance/context-turns-granularity.test.js"
      provides: "empirical A3/OQ1 check — real context-turns.jsonl carries no per-reasoning-step line; alignment granularity confirmed"
    - path: "test/performance/run-align.test.js"
      provides: "unit coverage for align D-07 cases"
    - path: "test/performance/loop-heuristic.test.js"
      provides: "unit coverage for loop D-09 cases"
    - path: "test/performance/fixtures/context-turns-fixtures.js"
      provides: "identical / diverge-then-reconverge / known-looping ContextTurnRow[] fixtures"
  key_links:
    - from: "loop-heuristic.ts"
      to: "run-align.ts"
      via: "import turnSignature"
      pattern: "import.*turnSignature.*run-align"
---

<objective>
Author the two greenfield pure algorithm modules the whole phase depends on — `run-align.ts` (run-pair sequence alignment + first-divergence + LCS tail pairing, D-07) and `loop-heuristic.ts` (windowed fuzzy repeat detector, D-09) — plus their fixtures and root-Jest unit tests. These are the CONTEXT-delegated research hand-offs (86-RESEARCH §1/§2/§3) and the Nyquist test seams for the phase.

Purpose: Downstream UI (difference viewer, single-run loop badges) imports these pure functions; building + testing them first means the UI plans receive a proven contract, not a scavenger hunt.
Output: Two dependency-free `.ts` modules under the dashboard + three test/fixture files at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-timeline-v2-and-declutter/86-RESEARCH.md
@.planning/phases/86-timeline-v2-and-declutter/86-VALIDATION.md

<interfaces>
<!-- The ContextTurnRow shape these modules operate on. Source: performanceSlice.ts L121-160. -->
<!-- KEEP THE MODULES DEPENDENCY-FREE: declare a local minimal subset type in each module -->
<!-- (only messages[].tool + messages[].bytes are read) so root Jest can import the .ts -->
<!-- without resolving the dashboard @/ alias or any React import. -->

ContextTurnMessage = { i: number; role: string | null; bytes: number; tool: { name: string | null; size: number } | null; preview: string }
ContextTurnRow = { ts; task_id; agent; wire: 'anthropic'|'openai'; request_id; model; usage:{input,output,cache_read,cache_write:number|null}; cache_breakpoints:number[]; categories:[]; messages: ContextTurnMessage[]; observation_ref }

<!-- Root Jest CONFIRMED can transform+import a .ts from integrations/ when the TEST file lives -->
<!-- under root test/ (probe passed 2026-07-10). No vitest, no new package. jest.config.js -->
<!-- testMatch = **/test|tests/**/*.test.js ; testPathIgnorePatterns includes /integrations/ -->
<!-- (ignores test LOCATIONS, not imported source). -->

<!-- Contrast reference (DO NOT MERGE): lib/experiments/route-heuristics.mjs:229 loopCount is -->
<!-- strict-adjacent + exact inputs_digest + persisted. loop-heuristic.ts is the OPPOSITE: -->
<!-- non-adjacent, fuzzy log2 size-bucket, advisory, never persisted. Keep separate. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Empirically resolve the alignment-granularity assumption (A3/OQ1) BEFORE locking run-align</name>
  <files>test/performance/context-turns-granularity.test.js, .planning/phases/86-timeline-v2-and-declutter/86-RESEARCH.md, .planning/phases/86-timeline-v2-and-declutter/86-VALIDATION.md</files>
  <read_first>
    - .planning/phases/86-timeline-v2-and-declutter/86-RESEARCH.md (§Assumptions A3, §Open Questions OQ1 ~L427 — the UNRESOLVED granularity question this task closes; §2 alignment operates on the parent-turn stream)
    - .data/measurements/*/context-turns.jsonl.gz (16 real Phase-84-captured runs exist; e.g. `.data/measurements/compare-fizzbuzz-v9-rmrc78fyy--claude-sonnet-straight-default--r0/context-turns.jsonl.gz`)
    - integrations/system-health-dashboard/src/components/performance/timeline.tsx (L166-224 ParentRow: `children = row.children ?? []` are the per-reasoning-step SUB-BANDS; the parent-turn count = number of top-level TimelineRow entries, NOT counting children)
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (L121-160 ContextTurnRow — has NO granularity_tier / parent_call_id / reasoning field; that structure lives only on TimelineRow)
  </read_first>
  <action>Before writing run-align, PROVE the A3/OQ1 assumption against real data so alignRuns is not silently tested only against synthetic fixtures that assume it. Read one real captured run's `context-turns.jsonl(.gz)` (gunzip the `.gz` or read a plaintext `.jsonl`), parse each line as JSON, and assert the empirical facts that make ContextTurnRow the correct alignment sequence: (a) every line is a single measured LLM request carrying the ContextTurnRow field set (`request_id`, `usage`, `messages`, `categories`) and (b) NO line carries a per-reasoning-step / `granularity_tier` / `parent_call_id` / `reasoning`/`thinking` field — i.e. reasoning steps are NOT emitted as separate context-turn lines (they are timeline `children` sub-bands only). Author `test/performance/context-turns-granularity.test.js` as a root-Jest test that loads a real `.data/measurements/*/context-turns.jsonl(.gz)` file (pick the first that exists; skip-with-reason via `test.skip` if none is present so the suite stays green on a fresh checkout), and asserts (a)+(b). If the empirical result CONTRADICTS A3 (a per-reasoning-step line IS present), do NOT proceed to Task 1 as written — instead adjust the `alignRuns` input contract to pre-flatten reasoning sub-requests into their parent request before signature computation, and record the deviation. If A3 HOLDS (expected — a spot-check on 2026-07-10 found 3-line runs with no reasoning/granularity field), record NO contract change is needed. Then update `86-RESEARCH.md`: rename `## Open Questions` to `## Open Questions (RESOLVED)` for OQ1 (or add a bold `**RESOLVED (86-01 Task 0):**` line under OQ1) stating the measured result (line-per-request, no per-reasoning-step context-turn line, alignment operates on the request sequence = parent-turn-equivalent, no pre-flatten), and tick the corresponding Wave-0 box in `86-VALIDATION.md` ("Verify one real context-turns.jsonl granularity vs timeline parent count").</action>
  <verify>
    <automated>npm test -- context-turns-granularity</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- context-turns-granularity` passes (loads a real captured `.jsonl(.gz)`, asserts line-per-request + no per-reasoning-step field; skip-with-reason only if zero captured runs exist).
    - The test reads a REAL captured file, not a fixture: `grep -Ec "\.data/measurements|context-turns\.jsonl" test/performance/context-turns-granularity.test.js` ≥ 1.
    - The test asserts the no-reasoning-line fact: `grep -Ec "granularity_tier|parent_call_id|reasoning|per-reasoning-step" test/performance/context-turns-granularity.test.js` ≥ 1.
    - OQ1 marked resolved: `grep -Ec "RESOLVED" .planning/phases/86-timeline-v2-and-declutter/86-RESEARCH.md` ≥ 1.
    - VALIDATION Wave-0 granularity box ticked: `grep -c "\[x\].*context-turns.jsonl granularity" .planning/phases/86-timeline-v2-and-declutter/86-VALIDATION.md` ≥ 1.
  </acceptance_criteria>
  <done>The A3/OQ1 granularity assumption is empirically confirmed (or the alignRuns contract adjusted) against a real captured context-turns.jsonl; OQ1 marked RESOLVED in RESEARCH.md and the Wave-0 box ticked in VALIDATION.md; run-align proceeds on the confirmed granularity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: run-align.ts + turnSignature + alignRuns (D-07)</name>
  <files>integrations/system-health-dashboard/src/components/performance/run-align.ts, test/performance/fixtures/context-turns-fixtures.js, test/performance/run-align.test.js</files>
  <read_first>
    - .planning/phases/86-timeline-v2-and-declutter/86-RESEARCH.md (§1 signature, §2 prefix-walk + LCS)
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (L121-160 ContextTurnRow shape — copy a minimal local subset type, do NOT import it)
    - lib/experiments/route-heuristics.mjs (L229 area — the strict-adjacent loopCount this is DISTINCT from; read to avoid merging)
  </read_first>
  <behavior>
    - turnSignature(t): tool-bearing turn → `T|name:bucket,...` where bucket = sizeBucket(size); no-tool turn → `R|bucket` over summed message bytes.
    - sizeBucket(bytes): bytes<=0 → 'z'; else String(Math.floor(Math.log2(bytes))). (256B and 300B collide; 256B and 4KB do not.)
    - alignRuns(a,b): Phase 1 common-prefix walk gives prefixLen p; firstDivergence = p when either side has more, else null (identical). Phase 2 LCS over sa[p..]/sb[p..] → pairs of absolute indices ({a,b}; null on the one-sided side).
    - Identical runs → { prefixLen: len, firstDivergence: null, pairs: [] }.
    - Re-converging tail (B inserts one turn, then both run the same signature) → LCS pairs the re-converged turns; the inserted B turn is { a: null, b: idx }.
  </behavior>
  <action>Using the granularity CONFIRMED in Task 0 (a context-turns line is one-per-LLM-request; alignment operates on that request sequence with no pre-flatten unless Task 0 found otherwise), create `run-align.ts` exporting `sizeBucket`, `turnSignature`, and `alignRuns` per 86-RESEARCH §1/§2. Declare a local `TurnLike` subset type (`{ messages: { tool: {name,size}|null; bytes: number }[] }`) — do NOT import ContextTurnRow from the slice (keeps the module root-Jest-importable). Implement the two-phase algorithm: `while (p<len && sa[p]===sb[p]) p++` for the prefix; a standard O(n·m) DP `lcsPairs(sa.slice(p), sb.slice(p), p)` returning absolute-index pairs for the tail. Return `AlignResult { prefixLen, firstDivergence: number|null, pairs: {a:number|null;b:number|null}[] }`. Author `context-turns-fixtures.js` with three exported ContextTurnRow[] fixtures: `identicalPair` (A===B), `divergeReconverge` (share prefix, B inserts a Bash turn at index d, both re-run the same test signature after), `loopingRun` (same Read+Bash signature repeated 3× within a 6-turn window). Author `run-align.test.js` asserting the three §2 cases from the VALIDATION map (align-module, align-identical, align-resync).</action>
  <verify>
    <automated>npm test -- run-align</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- run-align` passes (≥3 tests: fixture rerun-pair prefixLen/firstDivergence correct; identical → firstDivergence===null; re-converging tail LCS-paired).
    - `grep -c "from '@/" integrations/system-health-dashboard/src/components/performance/run-align.ts` returns 0 (dependency-free — no alias import).
    - `grep -Ec "export (function|const) (sizeBucket|turnSignature|alignRuns)" integrations/system-health-dashboard/src/components/performance/run-align.ts` returns 3.
  </acceptance_criteria>
  <done>run-align.ts exports the three functions, is dependency-free, and its unit suite is green against all three fixture shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: loop-heuristic.ts windowed fuzzy repeat detector (D-09)</name>
  <files>integrations/system-health-dashboard/src/components/performance/loop-heuristic.ts, test/performance/loop-heuristic.test.js</files>
  <read_first>
    - .planning/phases/86-timeline-v2-and-declutter/86-RESEARCH.md (§3 loop heuristic — window 6, log2 bucket, non-adjacent, skip R| turns)
    - integrations/system-health-dashboard/src/components/performance/run-align.ts (the turnSignature it imports)
  </read_first>
  <behavior>
    - loopFlags(turns): returns boolean[] same length as turns; turns[i] true iff its signature (non-R|) equals a signature within the prior LOOP_WINDOW (6) turns.
    - Pure-reasoning turns (signature starts 'R|') are NEVER flagged.
    - A tool used at turn 2 and again at turn 40 (>6 apart) is NOT flagged (out-of-window).
    - A tool repeated at turns 5,6,7 (adjacent, in-window) IS flagged on 6 and 7.
  </behavior>
  <action>Create `loop-heuristic.ts` exporting `LOOP_WINDOW = 6` and `loopFlags(turns)`. Import `turnSignature` from `./run-align` (the shared key — key_link). Map turns → signatures; for each index i, if the signature starts with `'R|'` return false, else scan `j` from `max(0, i-LOOP_WINDOW)` to `i-1` and return true on the first matching earlier signature. Keep `LOOP_WINDOW` an exported const so it is tunable (86-RESEARCH Pitfall 3). Author `loop-heuristic.test.js` asserting: the `loopingRun` fixture flags the repeated later turns; an out-of-window repeat is NOT flagged; a pure-reasoning repeat is NOT flagged.</action>
  <verify>
    <automated>npm test -- loop-heuristic</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- loop-heuristic` passes (≥3 tests covering in-window flag, out-of-window ignore, R|-turn ignore).
    - `grep -c "import.*turnSignature.*run-align" integrations/system-health-dashboard/src/components/performance/loop-heuristic.ts` returns 1 (shares the detector; no duplicate signature fn).
    - `grep -Ec "export (function|const) (loopFlags|LOOP_WINDOW)" integrations/system-health-dashboard/src/components/performance/loop-heuristic.ts` returns 2.
    - No reference to route-heuristics.mjs: `grep -c "route-heuristics" integrations/system-health-dashboard/src/components/performance/loop-heuristic.ts` returns 0 (kept DISTINCT from the backend detector).
  </acceptance_criteria>
  <done>loop-heuristic.ts flags windowed fuzzy repeats, imports the shared turnSignature, stays distinct from the backend loopCount, and its unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored context-turns → pure module input | These modules read already-persisted, already-redacted ContextTurnRow data in-memory; no untrusted network input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01-01 | Denial of Service | alignRuns LCS O(n·m) on a pathologically long run | accept | Sequences are tens of turns (86-RESEARCH §2); O(n²) DP is trivially bounded. No pagination needed this plan (Phase-84 deferral). |
| T-86-01-02 | Tampering | pure modules made client-authoritative for a persisted metric | mitigate | loopFlags/alignRuns are UI-advisory only, never persisted; the strict backend loopCount (route-heuristics.mjs) stays the source of truth — modules must NOT write to any store or endpoint. |
| T-86-01-SC | Tampering | npm installs | mitigate | This plan installs NO packages (root Jest + ts-jest already present; probe confirmed). No slopcheck surface. |
</threat_model>

<verification>
- `npm test -- run-align loop-heuristic` green (both pure modules, <5s).
- Both modules dependency-free (no `@/` import, no React import) — greppable.
- loop-heuristic imports turnSignature from run-align (single shared detector).
</verification>

<success_criteria>
- run-align.ts and loop-heuristic.ts exist, exported, dependency-free.
- Unit suites cover all D-07 (align-module/identical/resync) + D-09 (in/out-of-window, R|-skip) VALIDATION rows, green via root `npm test`.
- No new npm package introduced.
</success_criteria>

<output>
Create `.planning/phases/86-timeline-v2-and-declutter/86-01-SUMMARY.md` when done.
</output>
