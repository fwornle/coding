---
phase: 86-timeline-v2-and-declutter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx
  - integrations/system-health-dashboard/src/components/performance/context-band.tsx
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
autonomous: true
requirements: [DASH-02, VALID-01, ATTR-02]
must_haves:
  truths:
    - "scaledBand, SEGMENTS, scrubSecrets, and CACHE_WRITE_NA are exported ONCE from context-cache-explainer.tsx (no duplicated palette/regex/math anywhere)"
    - "context-band.tsx renders a CSS width-% segmented bar (mini + cumulative variants) with a hatched cache-read overlay; cache_write===null renders CACHE_WRITE_NA, never 0"
    - "performanceSlice carries a fetchReconciliation thunk (mirrors fetchContextTurns, graceful-empty) and modal/fullscreen/diff selection state — one slice, no new slice"
  artifacts:
    - path: "integrations/system-health-dashboard/src/components/performance/context-band.tsx"
      provides: "ContextBand component (variant mini|cumulative), imports scaledBand/SEGMENTS"
      exports: ["ContextBand"]
      min_lines: 40
    - path: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      provides: "fetchReconciliation thunk + reconciliationByTaskId + modalTaskId/modalTurnIndex + selectors"
      contains: "fetchReconciliation"
  key_links:
    - from: "context-band.tsx"
      to: "context-cache-explainer.tsx"
      via: "import scaledBand, SEGMENTS, CACHE_WRITE_NA"
      pattern: "import.*(scaledBand|SEGMENTS).*context-cache-explainer"
    - from: "performanceSlice.ts fetchReconciliation"
      to: "/api/experiments/runs/:taskId/reconciliation"
      via: "same-origin fetch, graceful-empty"
      pattern: "runs/.*reconciliation"
---

<objective>
Extract the band-rendering primitives (`scaledBand`, `SEGMENTS`, `scrubSecrets`, `CACHE_WRITE_NA`) as exports from `context-cache-explainer.tsx`, build the shared `context-band.tsx` component (mini + cumulative variants with honest cache overlay, D-04/D-05), and land ALL Redux surface extensions the later UI waves consume — the `fetchReconciliation` thunk (D-12) and the modal/fullscreen/diff selection state (D-01/D-02/D-07). Concentrating the slice + shared-primitive changes here means Waves 2/3 read a frozen contract and never touch the slice or the explainer's internals in parallel.

Purpose: One taxonomy across both surfaces (D-05); one slice (no new slice, per UI-SPEC Q2); one place the palette/regex/byte-math live (no copy-paste).
Output: An evolved explainer (exports only — no behavior change), a new band component, an extended slice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-timeline-v2-and-declutter/86-RESEARCH.md
@.planning/phases/86-timeline-v2-and-declutter/86-UI-SPEC.md
@.planning/phases/86-timeline-v2-and-declutter/86-PATTERNS.md
@integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx

<interfaces>
<!-- CURRENTLY FILE-LOCAL (non-exported) in context-cache-explainer.tsx — this plan EXPORTS them: -->
<!--   L70  SEGMENTS: Segment[]   (sys/tools/know/hist/tout/user palette + w + cached flag) -->
<!--   L90  scaledBand(byKey: Record<string,number>, totalBytes): { view: Segment[]; prefixPct } | null -->
<!--   L162 scrubSecrets(s: string): string   (sk-/ghp_/eyJ/Bearer/AKIA regexes) -->
<!--   L189 CACHE_WRITE_NA = 'N/A (provider reports no cache-creation)' -->
<!-- Adding `export` in front of these must NOT change the explainer's own behavior (it uses them internally). -->

<!-- ContextTurnRow.categories: { key: string; label: string; bytes: number }[]  (feed → byKey map) -->
<!-- ContextTurnRow.usage: { input; output; cache_read; cache_write: number|null }  (cache_write null on OpenAI wire) -->
<!-- ContextTurnRow.wire: 'anthropic' | 'openai'  (the honesty discriminator) -->

<!-- Slice mirror targets (performanceSlice.ts): -->
<!--   fetchContextTurns thunk L531-546 (mirror EXACTLY for fetchReconciliation) -->
<!--   contextTurnsByTaskId reducer L1027-1028 (mirror keyed-by-taskId store) -->
<!--   selectContextTurnsFor curried selector L1245 (mirror selectReconciliationFor) -->
<!--   setExplainTaskId reducer + selectExplainTaskId L928/L1295 (mirror modal open-state) -->
<!-- Reconciliation route: GET /api/experiments/runs/:taskId/reconciliation → { reconciliation: {summary:{matched,unmatched_wire,unmatched_transcript,fallback,aggregateDeltas,flaggedCount}} | null } ; ENOENT → 200 {reconciliation:null} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export band primitives + build context-band.tsx (D-04/D-05)</name>
  <files>integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx, integrations/system-health-dashboard/src/components/performance/context-band.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx (L58-99 SEGMENTS+scaledBand, L155-189 scrubSecrets+CACHE_WRITE_NA, L490-527 band usage)
    - .planning/phases/86-timeline-v2-and-declutter/86-UI-SPEC.md (§ Context-window band palette, §4 New Surface Contract, Q3 hatching)
    - .planning/phases/86-timeline-v2-and-declutter/86-PATTERNS.md (context-band.tsx assignment; S2 honest-null)
  </read_first>
  <action>Add `export` to the existing file-local `SEGMENTS`, `scaledBand`, `scrubSecrets`, and `CACHE_WRITE_NA` in `context-cache-explainer.tsx` — do NOT move or reimplement them (the explainer keeps using them internally; export is additive, zero behavior change). Create `context-band.tsx` exporting a `ContextBand` component with a `variant: 'mini' | 'cumulative'` prop. It imports `scaledBand`, `SEGMENTS`, `CACHE_WRITE_NA` from `./context-cache-explainer` (key_link — never fork). Mini variant: feed one `ContextTurnRow.categories[]` → `{key: bytes}` → `scaledBand` → a flex row of width-`{pct}%` `<div>`s, `h-2` (UI-SPEC Spacing), byte-share WITHIN the turn. Cumulative variant: accept the whole `ContextTurnRow[]`, stack per-turn category bytes cumulatively, width = cumulative/final-turn bytes, `h-3`/`h-4`. Cached overlay (D-05): render the `usage.cache_read` share of each cache-eligible segment as a hatched 45° `repeating-linear-gradient` (~4px pitch) over ~0.55 opacity fill; solid = fresh. Honesty gate: branch on `usage.cache_write === null` (the `wire` discriminator) → render the verbatim `CACHE_WRITE_NA` string, NEVER `?? 0`, never an amber write segment. No SVG, no recharts (recharts `fill` can't use `var()` — explainer L52 note).</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build 2>&1 | grep -v '^$' | grep -iE 'error TS' | grep -v CACHE_WRITE_NA; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "^export (const|function) (SEGMENTS|scaledBand|scrubSecrets)" integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx` returns 3 and `grep -c "export const CACHE_WRITE_NA" ...explainer.tsx` returns 1.
    - context-band.tsx imports from the explainer: `grep -Ec "import .*(scaledBand|SEGMENTS|CACHE_WRITE_NA).*context-cache-explainer" integrations/system-health-dashboard/src/components/performance/context-band.tsx` ≥ 1.
    - Honesty gate present (no `?? 0` on cache_write): `grep -c "cache_write === null" integrations/system-health-dashboard/src/components/performance/context-band.tsx` ≥ 1 AND `grep -Ec 'cache_write[^=]*\?\? 0' integrations/system-health-dashboard/src/components/performance/context-band.tsx` returns 0.
    - No recharts/SVG in the band: `grep -Ec "recharts|<svg" integrations/system-health-dashboard/src/components/performance/context-band.tsx` returns 0.
    - `npm run build` (tsc typecheck via vite) produces no new `error TS`.
  </acceptance_criteria>
  <done>Band primitives are exported once from the explainer; context-band.tsx renders both variants from them with an honest N/A cache path; the dashboard typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Slice extensions — fetchReconciliation + modal/fullscreen/diff state (D-01/D-02/D-07/D-12)</name>
  <files>integrations/system-health-dashboard/src/store/slices/performanceSlice.ts</files>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts (L531-546 fetchContextTurns, L894-955 reducers, L1027-1028 contextTurns reducer, L1233-1297 selectors)
    - lib/vkb-server/api-routes.js (L613-650 handleReconciliation shape)
    - .planning/phases/86-timeline-v2-and-declutter/86-PATTERNS.md (performanceSlice.ts assignment: mirror fetchContextTurns; extend, do NOT add a new slice)
  </read_first>
  <action>Extend `performanceSlice.ts` (UI-SPEC Q2 default — one slice). (a) Add a `fetchReconciliation` createAsyncThunk mirroring `fetchContextTurns` EXACTLY: same-origin `GET /api/experiments/runs/${encodeURIComponent(taskId)}/reconciliation`, graceful-empty (a non-ok or `{reconciliation:null}` resolves to null, never throws), store into a new `reconciliationByTaskId: Record<string, ReconciliationSummary | null>` reducer keyed by taskId. Define the `ReconciliationSummary` type from the api-routes shape (`{ summary: { matched; unmatched_wire; unmatched_transcript; fallback; aggregateDeltas; flaggedCount } } | null`). (b) Add `modalTaskId: string | null` + `modalTurnIndex: number | null` state with `openTurnModal({taskId,index})` / `closeTurnModal()` reducers, mirroring `setExplainTaskId`. (c) Add curried selectors `selectReconciliationFor(taskId)` (mirror `selectContextTurnsFor`) and `selectModalTurn`. Do NOT add a separate slice; do NOT re-implement `saveOverride`/`setCompareA`/`setCompareB`/`toggleRunSelected`/`selectContextTurnsFor` — they already exist and are reused as-is by later plans.</action>
  <verify>
    <automated>cd integrations/system-health-dashboard && npm run build 2>&1 | grep -iE 'error TS'; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchReconciliation" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` ≥ 3 (thunk def + reducer case + export).
    - Graceful-empty path: `grep -c "reconciliation: null\|reconciliation ?? null\|?.reconciliation" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` ≥ 1.
    - Modal state added: `grep -Ec "openTurnModal|closeTurnModal|modalTaskId" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` ≥ 3.
    - No second slice file created: `test $(ls integrations/system-health-dashboard/src/store/slices/*.ts | wc -l) -eq $(ls integrations/system-health-dashboard/src/store/slices/*.ts | wc -l)` — assert only performanceSlice.ts touched (files_modified single slice file).
    - `grep -Ec "selectReconciliationFor|selectModalTurn" integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` ≥ 2.
    - `npm run build` produces no new `error TS`.
  </acceptance_criteria>
  <done>The slice carries a graceful-empty fetchReconciliation thunk + reconciliation store + modal open-state + selectors; no new slice; dashboard typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored measured data → DOM | reconciliation summary + category bytes + cache values rendered into the band/badge; all already-persisted, already-redacted |
| client → PATCH (not this plan) | slice's existing saveOverride is server-authoritative; this plan does not alter it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-02-01 | Information disclosure | secrets in a rendered preview surfaced through the band/explainer | mitigate | scrubSecrets is exported and MUST be applied to any preview text a consumer renders (enforced in the modal plan); the band itself renders only numeric byte shares + category labels, no free text. |
| T-86-02-02 | Information disclosure | inferring cache_write=0 for OpenAI-wire runs (fabricated number) | mitigate | Band branches on `usage.cache_write === null` → CACHE_WRITE_NA verbatim (D-05/D-12 honesty; grep-gated in Task 1). |
| T-86-02-03 | Tampering | reconciliation status recomputed client-side (diverging from Phase-83 truth) | mitigate | fetchReconciliation serves the summary VERBATIM (mirror of fetchContextTurns); no client recompute of matched/flagged counts. |
| T-86-02-SC | Tampering | npm installs | mitigate | No packages installed — all deps already present (RESEARCH). No slopcheck surface. |
</threat_model>

<verification>
- Dashboard rebuild + typecheck clean (`npm run build`).
- Exported primitives greppable; band imports them (no fork).
- fetchReconciliation is graceful-empty and verbatim; modal state added; single slice.
</verification>

<success_criteria>
- SEGMENTS/scaledBand/scrubSecrets/CACHE_WRITE_NA exported once.
- context-band.tsx renders mini + cumulative with hatched cache overlay + honest N/A.
- performanceSlice has fetchReconciliation (graceful-empty) + modal/fullscreen state + selectors, one slice.
- No new npm package.
</success_criteria>

<output>
Create `.planning/phases/86-timeline-v2-and-declutter/86-02-SUMMARY.md` when done.
</output>
