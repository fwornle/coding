---
phase: 57-lower-ontology-project-tagging-foundation
plan: 03
plan_id: 57-03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts
  - integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts
autonomous: false
requirements:
  - LOWERONTO-04
must_haves:
  truths:
    - "D-04: Every km-core writer path in mcp-server-semantic-analysis stamps metadata.project before kmStore.putEntity"
    - "D-04 (pattern-mapper correction #5): metadata.project is dual-stamped at canonical-mapper.ts:160-167 AND km-core-adapter.ts:436-466 (defense-in-depth, mirrors Phase 42.2 team-stamping pattern)"
    - "D-04 (pattern-mapper correction #4): code-graph-rag has ZERO putEntity sites — verification-only no-op grep included as anti-regression guard"
    - "D-03: isProject() typeguard from km-core asserted before stamping; default 'coding' for this container per CLAUDE.md submodule mapping"
    - "D-02: Legacy `metadata.team` stamping is NOT modified or removed — new writers add `metadata.project` next to `metadata.team`, never instead of it. Existing team-read consumers keep working during the overlap; team cleanup is a follow-up phase."
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts"
      provides: "Primary stamp site — metadata.project added next to metadata.team (D-04)"
      contains: "metadata.project"
    - path: "integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts"
      provides: "Defense-in-depth dual stamp at the actual putEntity call site"
      contains: "metadata.project"
  key_links:
    - from: "wave1/2/3 agents"
      to: "canonical-mapper.ts via augmentWithCanonical"
      via: "options.project field added"
      pattern: "project:\\s*this\\.team"
    - from: "km-core-adapter.ts storeEntity"
      to: "kmStore.putEntity"
      via: "metadata.project on the literal"
      pattern: "project:\\s*projectFromOptions|sourceMetadata.*project"
tags: [writer-stamping, semantic-analysis, metadata, defense-in-depth]
---

<objective>
Stamp `metadata.project` at every km-core writer path in `integrations/mcp-server-semantic-analysis/` so every new entity emitted by `ukb full` (wave-analysis pipeline) carries the closed-set project tag (`coding` for this container per CLAUDE.md mapping). Apply the same dual-stamp defense-in-depth pattern Phase 42.2 Plan 02 Gap 4 established for `metadata.team` — primary stamp in `canonical-mapper.ts`, defense-in-depth stamp in `km-core-adapter.ts`. Use `isProject()` from km-core (Plan 01 deliverable) as the runtime typeguard.

Purpose: Realizes LOWERONTO-04 at the writer-path level. After this plan ships, every new wave-analysis-emitted entity carries `metadata.project = 'coding'`. The backfill (Plan 05) covers the existing 1262 nodes; this plan covers the going-forward writer surface.

Output: 6 modified TypeScript files in the semantic-analysis submodule + ALL required CLAUDE.md rebuild artifacts (npm run build inside submodule + Docker rebuild of coding-services).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-lower-ontology-project-tagging-foundation/57-CONTEXT.md
@.planning/phases/57-lower-ontology-project-tagging-foundation/57-PATTERNS.md
@.planning/phases/57-lower-ontology-project-tagging-foundation/57-01-PLAN.md
@integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts
@integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts
@CLAUDE.md

<interfaces>
<!-- Existing team-stamping pattern Phase 57 replicates for project. -->

From canonical-mapper.ts:160-167 (Phase 42.2 team stamp — the foundation Phase 57 extends):
```typescript
if (typeof options.team === 'string' && options.team.length > 0) {
  baseMetadata.team = options.team;
}
```

From km-core-adapter.ts:436-466 (defense-in-depth team stamp at storeEntity — Phase 42.2 Gap 4):
```typescript
const teamFromOptions =
  typeof options.team === 'string' && options.team.length > 0
    ? options.team
    : undefined;
// ...in the metadata literal:
...(typeof (sourceMetadata as { team?: unknown }).team === 'string' &&
((sourceMetadata as { team?: string }).team?.length ?? 0) > 0
  ? { team: (sourceMetadata as { team: string }).team }
  : teamFromOptions !== undefined
  ? { team: teamFromOptions }
  : {}),
```

From wave1/2/3 agents (call sites that route into canonical-mapper):
```typescript
// wave1-project-agent.ts:367-370
return augmentWithCanonical(entity, ontologyClass, this.runId, { team: this.team });
// wave2-component-agent.ts:~298 and wave3-detail-agent.ts:~275 — same shape.
```

From @fwornle/km-core (Plan 01 deliverable):
```typescript
import { isProject, type Project, PROJECTS } from '@fwornle/km-core';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stamp metadata.project in canonical-mapper.ts + wire wave1/2/3 callers</name>
  <files>
    integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts,
    integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts,
    integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts,
    integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts
  </files>
  <read_first>
    - integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts (full file — locate CanonicalMapperOptions interface ~line 69-84 and the existing team-stamping block ~160-167)
    - integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts (read around line 367-370 — the augmentWithCanonical call site)
    - integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts (read around line 298)
    - integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts (read around line 275)
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-PATTERNS.md §"canonical-mapper.ts" and §"wave1-project-agent.ts"
    - lib/km-core/src/types/project.ts (Plan 01 output — confirm isProject signature)
  </read_first>
  <action>
    In `canonical-mapper.ts`: (a) add `import { isProject, type Project } from '@fwornle/km-core';` to the existing km-core import block at the top of the file; (b) extend the `CanonicalMapperOptions` interface (around line 69-84) with an optional `project?: Project | string` field — accept `string` and narrow via `isProject()` so callers that haven't been re-typed yet (mixed transitional state) still compile; (c) immediately after the existing team-stamping block at lines 160-167, add a parallel block that stamps `baseMetadata.project` when `isProject(options.project)` returns true. Use `isProject` as the runtime guard, NOT a `length > 0` string check — D-03 locks the closed-set vocabulary. Keep the team stamp untouched (D-02 — metadata.team is NOT rewritten in Phase 57). Add an inline comment citing `Phase 57 D-04`.

    In `wave1-project-agent.ts` line ~369: change `augmentWithCanonical(entity, ontologyClass, this.runId, { team: this.team })` to also pass `project: this.team` — i.e., the options object becomes `{ team: this.team, project: this.team }`. Per PATTERNS §"wave1-project-agent.ts" recommendation: re-use `this.team` for the project value and add a `// TODO(phase-60-or-later)` comment noting that a dedicated `parameters.project` should plumb through when okm/cap teams come online. The `this.team` value defaults to `'coding'` in this container (CLAUDE.md container-side context), so `isProject(this.team)` will pass.

    Repeat the same one-line change in `wave2-component-agent.ts:~298` and `wave3-detail-agent.ts:~275` — both call `augmentWithCanonical(entity, '<class>', this.runId, { team: this.team })`. After the patch, all three sites pass `{ team: this.team, project: this.team }`.

    Do NOT touch the existing `team` stamping anywhere (D-02 invariant — legacy `metadata.team` stays untouched during this phase).
  </action>
  <verify>
    <automated>grep -c "metadata\.project\|baseMetadata\.project" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts; grep -cE "project:\s*this\.team" integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave2-component-agent.ts integrations/mcp-server-semantic-analysis/src/agents/wave3-detail-agent.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "import.*isProject.*from.*@fwornle/km-core" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts` returns ≥ 1
    - `grep -cE "baseMetadata\.project\s*=" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts` returns ≥ 1 (the stamp landed)
    - `grep -c "project\?\?:\s*Project\|project\?\s*:\s*Project" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts` returns ≥ 1 (interface extended)
    - Each wave agent file contains `project: this.team` exactly once at the augmentWithCanonical call site (`grep -cE "project:\s*this\.team" integrations/mcp-server-semantic-analysis/src/agents/wave1-project-agent.ts` returns 1; same for wave2 and wave3)
    - Existing team stamp preserved: `grep -c "baseMetadata\.team\s*=\s*options\.team" integrations/mcp-server-semantic-analysis/src/agents/canonical-mapper.ts` ≥ 1 (D-02 invariant)
    - TypeScript compiles: `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | tail -10` shows zero errors related to canonical-mapper or wave agents
  </acceptance_criteria>
  <done>Primary stamp site shipped; canonical-mapper now stamps metadata.project on every wave-emitted entity that flows through augmentWithCanonical; team stamp untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Defense-in-depth dual-stamp in km-core-adapter.ts + verify persistence-agent + code-graph-rag no-op grep</name>
  <files>
    integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts,
    integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts
  </files>
  <read_first>
    - integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts (read storeEntity method ~lines 395-487 carefully — the Phase 42.2 team dual-stamp at 436-466 is the pattern to mirror)
    - integrations/mcp-server-semantic-analysis/src/agents/persistence-agent.ts (read in full — PATTERNS says this file is "MODIFIED — likely no-op" because writes route through km-core-adapter via wave-controller; verify and document)
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-PATTERNS.md §"km-core-adapter.ts" and §"persistence-agent.ts"
  </read_first>
  <action>
    In `km-core-adapter.ts` `storeEntity` method (around lines 395-487): (a) add `import { isProject, type Project } from '@fwornle/km-core';` if not already present; (b) extend the `options` parameter type to include `project?: Project | string`; (c) immediately after the existing `teamFromOptions` derivation, add a parallel `projectFromOptions = isProject(options.project) ? options.project : undefined` derivation; (d) inside the `metadata: { ... }` literal, append a ternary block mirroring the existing team ternary: prefer `sourceMetadata.project` (canonical-mapper stamp from Task 1) when set and valid per `isProject`; fall back to `projectFromOptions` when defined; otherwise no-op (empty spread). The full block lands BELOW the team-ternary so that team and project both appear in the merged metadata when both are supplied.

    Default behavior: when neither `sourceMetadata.project` nor `options.project` is set, do NOT default to `'coding'` here — let the canonical-mapper / wave-controller boundary decide (the wave agents in Task 1 already pass `project: this.team` which defaults to `'coding'` in this container). Stamping a hardcoded `'coding'` here would silently mask upstream bugs.

    In `persistence-agent.ts`: PATTERNS classifies this as a verify-only target. Read the file and confirm no direct `putEntity` calls exist (writes flow through km-core-adapter from wave-controller). If the confirmation holds, NO source change — record the verification in the SUMMARY. If a direct `putEntity` call IS found (PATTERNS may be stale), add the same `metadata.project` stamp pattern as in km-core-adapter.ts.

    Verification-only grep guard for `code-graph-rag` (PATTERNS correction #4): execute `grep -rn "putEntity" integrations/code-graph-rag/src/ 2>/dev/null | wc -l` and confirm it returns 0. Record the grep output in the SUMMARY as an anti-regression guard — future readers should not expect a project stamp in code-graph-rag because there's no writer-call surface there.
  </action>
  <verify>
    <automated>grep -c "projectFromOptions\|isProject" integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts; echo "---"; grep -rn "putEntity" integrations/code-graph-rag/src/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "import.*isProject.*from.*@fwornle/km-core" integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts` returns ≥ 1
    - `grep -c "projectFromOptions" integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts` returns ≥ 1 (the dual-stamp derivation landed)
    - `grep -cE "\.\.\.\(isProject\(\(sourceMetadata|\.\.\.\(typeof.*project" integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts` returns ≥ 1 (the metadata-literal ternary landed)
    - Existing team dual-stamp preserved: `grep -c "teamFromOptions" integrations/mcp-server-semantic-analysis/src/storage/km-core-adapter.ts` ≥ 1
    - `grep -rn "putEntity" integrations/code-graph-rag/src/ 2>/dev/null | wc -l` returns 0 (PATTERNS correction #4 anti-regression guard)
    - persistence-agent.ts either unchanged OR contains a new project stamp; SUMMARY records which (verify-only outcome documented)
    - TypeScript compiles: `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | tail -10` shows zero errors
  </acceptance_criteria>
  <done>Dual-stamp defense-in-depth complete; the actual putEntity call site at km-core-adapter.ts also stamps metadata.project independent of canonical-mapper's stamp; persistence-agent verified no-op; code-graph-rag verified no putEntity sites.</done>
</task>

<task type="auto">
  <name>Task 3: Build submodule + rebuild Docker coding-services container (CLAUDE.md mandatory)</name>
  <files>integrations/mcp-server-semantic-analysis/dist/, docker/docker-compose.yml (no source change — rebuild only)</files>
  <read_first>
    - CLAUDE.md §"Rebuilding After Code Changes" (the two-step submodule build dance — npm run build + docker-compose build coding-services && docker-compose up -d coding-services)
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-PATTERNS.md §"Submodule Build Pipeline"
  </read_first>
  <action>
    Step 1 (mandatory per CLAUDE.md): `cd integrations/mcp-server-semantic-analysis && npm run build`. This compiles the TypeScript changes from Tasks 1 + 2 into `dist/`. Confirm the build exits 0; if there are TypeScript errors, return to Task 1 or Task 2 and fix the underlying issue (per CLAUDE.md "API design: Never modify working APIs for TypeScript compliance; fix types instead" — DO NOT cast around type errors).

    Step 2 (mandatory per CLAUDE.md): `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services` (use `--timeout 600` if needed per Docker build memory). Then `docker-compose up -d coding-services` to restart the container with the new image.

    Verify post-rebuild that the dist file picks up the new stamps:
    ```
    grep -c "projectFromOptions\|baseMetadata.project" integrations/mcp-server-semantic-analysis/dist/agents/canonical-mapper.js integrations/mcp-server-semantic-analysis/dist/storage/km-core-adapter.js 2>/dev/null
    ```
    The combined count must be ≥ 2 (one in each dist file). If 0, the source-side patch didn't survive `tsc`; investigate.

    Run a thin live smoke test if feasible: `docker exec coding-services node -e "const km = require('@fwornle/km-core'); console.log('isProject coding:', km.isProject('coding'))"` should print `isProject coding: true`. If the container can't access km-core at that path (different resolution), skip and rely on the dist-file grep instead.
  </action>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (output tail shows no "error TS" lines)
    - `integrations/mcp-server-semantic-analysis/dist/agents/canonical-mapper.js` exists and contains `baseMetadata.project` (grep: `grep -c "baseMetadata.project" integrations/mcp-server-semantic-analysis/dist/agents/canonical-mapper.js` returns ≥ 1)
    - `integrations/mcp-server-semantic-analysis/dist/storage/km-core-adapter.js` exists and contains `projectFromOptions` (grep: `grep -c "projectFromOptions" integrations/mcp-server-semantic-analysis/dist/storage/km-core-adapter.js` returns ≥ 1)
    - Docker rebuild completes: `docker images coding-services --format '{{.CreatedSince}}' | head -1` shows a recent timestamp (≤ 10 minutes since rebuild)
    - Container is running: `docker ps --filter name=coding-services --format '{{.Status}}'` shows "Up" status
    - km-core local patch verified intact (CLAUDE.md): `grep -c "JSON has more nodes" node_modules/@fwornle/km-core/dist/store/persistence.js 2>/dev/null` returns ≥ 1 (or operator-applied note in SUMMARY)
  </acceptance_criteria>
  <done>Submodule built, Docker container rebuilt and restarted; the new writer-side stamps are live in the running coding-services container.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN-UAT — confirm new ukb-emitted entities carry metadata.project</name>
  <what-built>
    Wave-analysis writer-path stamping is live. Every new entity emitted by the next `ukb full` run carries `metadata.project = 'coding'` via the canonical-mapper primary stamp + km-core-adapter defense-in-depth stamp. The existing `metadata.team` field is untouched (legacy data preserved per D-02).
  </what-built>
  <how-to-verify>
    1. Trigger a small wave-analysis run via the MCP tool (operator decision — run `ukb full debug` for a single observation if a full run isn't desired, or wait for the next scheduled run). Per MEMORY.md UKB workflow control rules.
    2. After the run completes, inspect the latest entities in the live km-core export:
       ```bash
       jq '[.nodes[] | select(.attributes.createdAt > "2026-06-14T00:00:00Z")] | map(.attributes.metadata.project) | group_by(.) | map({project: .[0], count: length})' .data/knowledge-graph/exports/general.json
       ```
       Expected: `coding` dominates the new entities; no `null`/`undefined` for newly-written entities.
    3. Inspect the dashboard at http://localhost:3032 to confirm the wave-analysis run reported success and no errors related to project tagging.
    4. Verify the dist files match expectations: `grep -c "baseMetadata.project\|projectFromOptions" integrations/mcp-server-semantic-analysis/dist/agents/canonical-mapper.js integrations/mcp-server-semantic-analysis/dist/storage/km-core-adapter.js` returns ≥ 2.
    5. Spot-check that legacy `metadata.team` is preserved on the new entities (D-02 invariant):
       ```bash
       jq '[.nodes[] | select(.attributes.createdAt > "2026-06-14T00:00:00Z") | .attributes.metadata.team] | unique' .data/knowledge-graph/exports/general.json
       ```
       Expected: at minimum `["coding"]` (team stamp still flows through).
  </how-to-verify>
  <resume-signal>Type "approved" if new entities carry metadata.project='coding' AND metadata.team='coding'. Describe issues if either is missing or if the wave run errored.</resume-signal>
</task>

</tasks>

<verification>
Writer-side coverage assertion (post-next-ukb-run):
```bash
# All entities created after Plan 03 ships should carry metadata.project.
jq '[.nodes[] | select(.attributes.createdAt > "<plan-ship-iso-timestamp>") | select(.attributes.metadata.project == null)] | length' .data/knowledge-graph/exports/general.json
```
Expect: `0` (zero new entities lack metadata.project after the cutover).

Dual-stamp lock:
```bash
grep -c "baseMetadata.project" integrations/mcp-server-semantic-analysis/dist/agents/canonical-mapper.js
grep -c "projectFromOptions" integrations/mcp-server-semantic-analysis/dist/storage/km-core-adapter.js
# Both must be ≥ 1.
```
</verification>

<success_criteria>
- canonical-mapper.ts stamps metadata.project (primary site)
- km-core-adapter.ts stamps metadata.project (defense-in-depth dual-stamp)
- wave1/2/3 agents pass `project: this.team` to augmentWithCanonical
- persistence-agent.ts verified no-op OR patched (recorded in SUMMARY)
- code-graph-rag/src/ has ZERO putEntity sites (anti-regression grep)
- npm run build succeeds; Docker rebuild succeeds; container running
- New ukb-run entities carry metadata.project='coding' (human-verify gate)
- Legacy metadata.team preserved (D-02 invariant)
- km-core local patch intact post-rebuild
</success_criteria>

<output>
Create `.planning/phases/57-lower-ontology-project-tagging-foundation/57-03-SUMMARY.md` when done. Record: files modified (with line counts), persistence-agent verification outcome (no-op vs patched), code-graph-rag grep result (must be 0), build + Docker rebuild durations, km-core local-patch verification, and the operator's UAT signoff text.
</output>
