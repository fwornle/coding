---
phase: 57-lower-ontology-project-tagging-foundation
plan: 04
plan_id: 57-04
type: execute
wave: 3
depends_on: ["57-02", "57-03"]
files_modified:
  - integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts
  - integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.test.ts
  - scripts/check-l2-emission-rate.mjs
autonomous: false
requirements:
  - LOWERONTO-01
must_haves:
  truths:
    - "D-10: ontology-classification-agent.ts loads the 10 L2 classes from coding.lower.json via OntologyRegistry at startup and injects them into the classification prompt"
    - "D-10: Classifier defaults to L1 parent when LLM declines all L2 options (no forced classification — Unclassified path preserved)"
    - "Success criterion #3: ≥18 of 20 recent online-learned entities carry an L2 ontology class (runtime gate, codified as scripts/check-l2-emission-rate.mjs)"
  artifacts:
    - path: "integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts"
      provides: "Updated classifier loading 10 L2 classes from coding.lower.json + prompt refinement step"
      contains: "coding.lower\\|L2\\|LiveLoggingSystem"
    - path: "scripts/check-l2-emission-rate.mjs"
      provides: "Sample-of-20 smoke test for SC#3 acceptance"
  key_links:
    - from: "ontology-classification-agent.ts"
      to: "coding.lower.json via OntologyRegistry"
      via: "registry.getResolvedClasses() or equivalent Phase 38 accessor"
      pattern: "L2|coding\\.lower|getResolvedClasses"
tags: [classifier, ontology, prompt-engineering, l2-emission]
---

<objective>
Update `ontology-classification-agent.ts` to load the 10 L2 classes shipped in Plan 02's `coding.lower.json` via the existing `OntologyRegistry` (Phase 38 surface) and inject the L2 vocabulary with one-line descriptions into the classification prompt as a refinement step after the L1 classification. When the LLM declines all L2 options, default to the L1 parent — no forced L2 classification, no synthetic emissions.

Ship a runtime gate (`scripts/check-l2-emission-rate.mjs`) that exercises Phase 57 success criterion #3: sample the last 20 online-learned entities and assert ≥18 carry an L2 ontology class (one of the 10 names in coding.lower.json). The smoke script is the acceptance signal for both this plan and end-of-phase verification.

Purpose: Realizes LOWERONTO-01 at the runtime emission level (Plan 02 covered the static data shape). Without this plan, the lower-ontology file ships but no classifier consumes it — every new entity continues to emit L1 only.

Output: 1 modified TS file in the submodule + 1 new test file in the submodule + 1 new emission-rate smoke script under `scripts/` + ALL CLAUDE.md rebuild artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-lower-ontology-project-tagging-foundation/57-CONTEXT.md
@.planning/phases/57-lower-ontology-project-tagging-foundation/57-PATTERNS.md
@.planning/phases/57-lower-ontology-project-tagging-foundation/57-02-PLAN.md
@integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts
@CLAUDE.md

<interfaces>
<!-- The classifier integration point. -->

From ontology-classification-agent.ts (current shape, per PATTERNS):
- Line 32: existing `import { OntologyRegistry } from '@fwornle/km-core/ontology';` (or root barrel; verify at read time)
- Lines 500-557: `classifySingleObservation()` method — calls `this.classifier.classify(...)` (line 512) to get the L1 classification, defaults to `'Unclassified'` (line 523) when classifier returns null
- The agent already constructs OntologyRegistry — Phase 57 is additive (loads more classes via the same surface)

From Plan 02 (coding.lower.json):
- 10 L2 classes: LiveLoggingSystem, ConstraintMonitor, OnlineObservation, OnlineDigest, OnlineInsight, KnowledgeManagement, BatchSemanticAnalysis, RapidLlmProxy, DockerizedServices, EtmDaemon
- Each carries a one-line description (PATTERNS §"L1 parent mapping table" + the descriptions locked in Plan 02 Task 1)

Phase 38 D-29 registry accessors (verify at read time):
- `getResolvedClasses(): ReadonlyMap<string, ResolvedClass>` — returns all classes (upper + all lowers + chain resolution applied)
- `getClass(name): ResolvedClass | undefined` — per-class lookup
- `parentChainOf(name): string[]` — walk extends chain
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classifier loads L2 classes + injects prompt refinement step</name>
  <files>
    integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts,
    integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.test.ts
  </files>
  <read_first>
    - integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts (FULL FILE — locate the OntologyRegistry import, the constructor, `classifySingleObservation` method, and the prompt-building logic)
    - lib/km-core/src/ontology/registry.ts (confirm the exact accessor name — `getResolvedClasses` or `classCatalog` per Phase 38 D-29)
    - .data/ontologies/coding.lower.json (Plan 02 output — the 10 L2 classes + their descriptions; the classifier will read these via registry at runtime)
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-PATTERNS.md §"ontology-classification-agent.ts"
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-CONTEXT.md §D-10
  </read_first>
  <behavior>
    - Test: When the classifier is constructed against a registry containing coding.lower.json's 10 classes, `getL2ClassesForPrompt()` (or equivalent prompt-building helper) returns exactly those 10 class names + their descriptions
    - Test: Given a synthetic observation whose content clearly matches `LiveLoggingSystem` (e.g., mentions "ETM" + ".specstory/history/"), the classifier emits `ontologyClass === 'LiveLoggingSystem'`, NOT `'Component'`
    - Test: Given a synthetic observation that does NOT match any of the 10 L2 classes (e.g., generic "fixed a typo"), the classifier falls back to the L1 parent OR `'Unclassified'` — never invents a new L2
    - Test: When `coding.lower.json` is missing from the ontology directory (registry returns 0 L2 classes), the classifier degrades gracefully to L1-only emission (no crash, no exception)
  </behavior>
  <action>
    Step 1 — Load L2 classes from registry:
    - In `ontology-classification-agent.ts` constructor (or wherever OntologyRegistry is initialized — read to confirm), after registry construction, call the appropriate accessor (`registry.getResolvedClasses()` or `registry.classCatalog` per Phase 38 D-29 — verify by reading `lib/km-core/src/ontology/registry.ts` first) and filter to classes whose `extends` is one of `Component`, `SubComponent`, `Detail`. Store the filtered list on `this.l2Classes: ResolvedClass[]` as a class field. Expected size: 10 (matches coding.lower.json). Log the count to stderr on construction so operators can spot regression (`process.stderr.write` per CLAUDE.md constraint rules — NOT console.log).

    Step 2 — Inject into the prompt:
    - Locate the prompt-building call site that feeds `this.classifier.classify(...)` (around line 512 per PATTERNS). Either modify the prompt string directly OR add a helper method `buildL2RefinementPrompt(l1Class: string): string` that returns a refinement instruction. Wording (planner-locked, per D-10 + Claude's discretion bullet 2):

      "REFINEMENT STEP — if the L1 class is one of [Component, SubComponent, Detail], try to refine to a more specific L2 class from this list, otherwise return the L1 class unchanged. Decline (return L1) if none of these L2 classes fit the observation:
      - LiveLoggingSystem: Live Session Logging subsystem (.specstory/history/, ETM daemon, LSL resolver)
      - ConstraintMonitor: Constraint monitor service on port 3030 (no-console-log, doc-filename rules)
      - OnlineObservation: Leaf observation written by the online learning pipeline
      - OnlineDigest: Daily roll-up of online observations
      - OnlineInsight: Weekly synthesized insight from online digests
      - KnowledgeManagement: km-core / observations DB / JSON exports / Qdrant sidecar
      - BatchSemanticAnalysis: Manual UKB wave-analysis pipeline (ukb full)
      - RapidLlmProxy: LLM routing surface on port 12435
      - DockerizedServices: docker-compose coding-services container
      - EtmDaemon: Enhanced Transcript Monitor launchd daemon under LiveLoggingSystem"

      The descriptions should be the same one-liners locked in Plan 02 — read them from `this.l2Classes[i].description` at prompt-build time so they stay in sync with the JSON file. Inject the rendered list as a refinement step appended to the existing L1 prompt.

    Step 3 — Fallback behavior:
    - When the LLM returns an L2 name NOT in the registered list (hallucination), reject and fall back to the L1 parent — do NOT stamp the hallucinated class. Existing fallback to `'Unclassified'` (line 523) is preserved for cases where the LLM declines L1 too.
    - When `this.l2Classes.length === 0` (registry didn't load coding.lower.json — file missing or malformed), skip the refinement step entirely and emit L1 as before — log a stderr warning `[ontology-classification-agent] coding.lower.json not loaded — L2 refinement disabled`.

    Step 4 — Test file:
    - Create `ontology-classification-agent.test.ts` (vitest or whatever the submodule uses — read existing test files in `integrations/mcp-server-semantic-analysis/src/` to confirm; likely `vitest` or `node:test`). Mock the LLM client and OntologyRegistry; feed synthetic observations covering the 4 behavior cases above. Use `mockLLM: true` style — do not hit a real proxy.

    DO NOT change anything outside the classifier's L2-refinement scope. The L1 classification path stays untouched. The `metadata.project` stamping is Plan 03's responsibility — do not duplicate it here (PATTERNS §"ontology-classification-agent.ts" → recommended: skip the classifier project stamp).
  </action>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | tail -10; cd integrations/mcp-server-semantic-analysis && npm test -- ontology-classification-agent 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l2Classes\|L2 class\|L2_REFINEMENT\|REFINEMENT STEP" integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts` returns ≥ 2 (load site + prompt injection)
    - `grep -c "LiveLoggingSystem\|coding.lower" integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts` returns ≥ 1 (one of the L2 names or the lower-ontology file reference must appear, confirming the file is wired)
    - Test file exists with ≥4 it() blocks: `grep -c "  it(\|  test(" integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.test.ts` returns ≥ 4
    - Tests pass: `cd integrations/mcp-server-semantic-analysis && npm test -- ontology-classification-agent 2>&1 | tail -5` exits 0
    - TypeScript compiles: `cd integrations/mcp-server-semantic-analysis && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0
    - Existing `'Unclassified'` fallback preserved: `grep -c "Unclassified" integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts` returns ≥ 1 (D-10 invariant — no forced classification)
    - No console.log: `grep -c "console\.log\|console\.error" integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts` returns 0 (CLAUDE.md no-console-log rule; use process.stderr.write)
  </acceptance_criteria>
  <done>Classifier loads 10 L2 classes from coding.lower.json via OntologyRegistry; refinement prompt injected; graceful fallback when file missing; unit tests pass with mocked LLM.</done>
</task>

<task type="auto">
  <name>Task 2: Submodule build + Docker rebuild + L2-emission smoke script (SC#3 gate)</name>
  <files>
    integrations/mcp-server-semantic-analysis/dist/,
    scripts/check-l2-emission-rate.mjs
  </files>
  <read_first>
    - CLAUDE.md §"Rebuilding After Code Changes" (mandatory two-step rebuild)
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-CONTEXT.md §"specifics" — "Success criterion #3 sampling: last 20 entities written by the online pipeline whose metadata.source is online"
    - .planning/phases/57-lower-ontology-project-tagging-foundation/57-PATTERNS.md §"Submodule Build Pipeline" + §"Verification One-Liners"
    - .data/knowledge-graph/exports/general.json (current production export — confirms the shape the smoke script reads)
  </read_first>
  <action>
    Step 1 (mandatory per CLAUDE.md): `cd integrations/mcp-server-semantic-analysis && npm run build`. Confirm exit 0. If TS errors, fix Task 1's source, do not cast around them.

    Step 2 (mandatory per CLAUDE.md): `cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services`. Use --timeout 600 if needed.

    Step 3 — Create `scripts/check-l2-emission-rate.mjs` — the SC#3 acceptance gate. Standalone Node.js script (no submodule build needed for the script itself; lives at outer-repo `scripts/`). Behavior:
    - Read `.data/knowledge-graph/exports/general.json`
    - Filter to nodes that are recent online entities: `attributes.createdAt > <cutoff>` AND `attributes.metadata.source === 'online'` (verify the field name by inspecting an actual entity — adjust if `source` lives elsewhere in metadata)
    - Sort by `createdAt` descending and take the top 20 (the sample-of-20 from CONTEXT §"specifics")
    - Parse CLI flags: `--sample N` (default 20), `--min M` (default 18) — both integers
    - Count how many of the 20 carry `attributes.ontologyClass` that is one of the 10 L2 names from coding.lower.json (read the L2 names dynamically from `.data/ontologies/coding.lower.json` rather than hardcoding — fail-loud if the file is missing)
    - Exit 0 if `l2Count >= --min`, exit 1 otherwise — print a summary line to stderr: `[l2-emission-rate] sample=N, l2_emitted=K, threshold=M, status=<PASS|FAIL>`
    - Print the per-class breakdown for the operator to spot-check: which L2 classes were emitted, which weren't

    Step 4 — Run the smoke script as a dry-run sanity check (the script may emit FAIL until the next wave-analysis run lands new L2-classified entities — that's expected for a fresh deploy). Record the dry-run output in the SUMMARY so operators know the baseline.

    Step 5 — Verify the km-core local patch (CLAUDE.md): `grep -c "JSON has more nodes" node_modules/@fwornle/km-core/dist/store/persistence.js` returns ≥ 1, or re-apply per CLAUDE.md and document in SUMMARY.
  </action>
  <verify>
    <automated>cd integrations/mcp-server-semantic-analysis && npm run build 2>&1 | tail -5; ls scripts/check-l2-emission-rate.mjs; node scripts/check-l2-emission-rate.mjs --sample 20 --min 0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `integrations/mcp-server-semantic-analysis/dist/agents/ontology-classification-agent.js` exists and contains references to the L2 vocabulary (grep: `grep -c "l2Classes\|REFINEMENT\|LiveLoggingSystem" integrations/mcp-server-semantic-analysis/dist/agents/ontology-classification-agent.js` returns ≥ 1)
    - Docker container rebuilt and running: `docker ps --filter name=coding-services --format '{{.Status}}'` shows "Up"
    - File `scripts/check-l2-emission-rate.mjs` exists and is executable; `node scripts/check-l2-emission-rate.mjs --sample 20 --min 0` exits 0 (with threshold 0, it always passes — confirms the script itself runs end-to-end without error)
    - The smoke script reads L2 names from `.data/ontologies/coding.lower.json` (grep: `grep -c "coding.lower" scripts/check-l2-emission-rate.mjs` returns ≥ 1) — not hardcoded
    - The smoke script honors `--sample` and `--min` flags (grep: `grep -c "sample\|min" scripts/check-l2-emission-rate.mjs` returns ≥ 2)
    - km-core local patch verified intact post-rebuild
  </acceptance_criteria>
  <done>Classifier change is live in the container; smoke script ships and is runnable; baseline emission rate recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-UAT — trigger a small ukb run and verify L2 emission rate ≥ 18/20</name>
  <what-built>
    The classifier is updated to load 10 L2 classes from coding.lower.json and inject them as a refinement step in the classification prompt. After a wave-analysis or online-learning run, ≥18 of the last 20 online-learned entities should carry an L2 ontology class (one of the 10 names) — codified in `scripts/check-l2-emission-rate.mjs`.
  </what-built>
  <how-to-verify>
    1. Trigger an online-learning run via the normal pipeline (or run `ukb full` if the operator prefers exercising the full wave). Per MEMORY.md UKB workflow control — `ukb full` = production mode, NOT debug.
    2. After the run completes, execute the smoke gate:
       ```bash
       node scripts/check-l2-emission-rate.mjs --sample 20 --min 18
       ```
       Expected: exit code 0; stderr shows `[l2-emission-rate] sample=20, l2_emitted=>=18, threshold=18, status=PASS`. The script also prints which L2 classes were emitted.
    3. Spot-check a few classified entities directly:
       ```bash
       jq '[.nodes[] | select(.attributes.metadata.source == "online") | select(.attributes.createdAt > "<cutoff>")] | sort_by(.attributes.createdAt) | reverse | .[0:5] | map({name: .attributes.name, ontologyClass: .attributes.ontologyClass})' .data/knowledge-graph/exports/general.json
       ```
       Expected: at least 4 of the 5 most-recent online entities show `ontologyClass` in the 10 L2 names.
    4. If the rate is < 18/20, investigate: read the latest semantic-analysis logs in the container (`docker logs coding-services --tail 200 | grep -i classif`), check whether the L2 refinement prompt is actually being injected, and whether the LLM is returning L2 names from the registered list (not hallucinating).
  </how-to-verify>
  <resume-signal>Type "approved" if the smoke script exits 0 with l2_emitted >= 18. Describe the failure pattern if the rate is below threshold (which L2 classes are missing, what the LLM emitted instead).</resume-signal>
</task>

</tasks>

<verification>
SC#3 gate command:
```
node scripts/check-l2-emission-rate.mjs --sample 20 --min 18
```
Exit 0 = pass.

Source-side reading from coding.lower.json:
```
grep -c "coding.lower\|getResolvedClasses\|l2Classes" integrations/mcp-server-semantic-analysis/dist/agents/ontology-classification-agent.js
```
Expected ≥ 1.
</verification>

<success_criteria>
- Classifier loads 10 L2 classes from coding.lower.json via OntologyRegistry at startup
- L2 refinement step injected into classification prompt with one-line descriptions
- Graceful fallback to L1 when LLM declines L2 options (no forced classification)
- Graceful no-op when coding.lower.json absent (degraded behavior, stderr warning)
- scripts/check-l2-emission-rate.mjs ships and codifies SC#3 (≥18/20 online entities carry L2)
- Submodule built + Docker rebuilt + container running
- Operator verifies smoke gate exits 0 with ≥18/20 emission rate
- km-core local patch intact
</success_criteria>

<output>
Create `.planning/phases/57-lower-ontology-project-tagging-foundation/57-04-SUMMARY.md` when done. Record: classifier line-count delta, exact OntologyRegistry accessor used (e.g., `getResolvedClasses` vs `classCatalog`), the rendered L2-refinement prompt text, baseline + post-rebuild smoke-gate output (l2_emitted=K out of 20), and the operator's HUMAN-UAT signoff.
</output>
