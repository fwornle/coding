---
phase: 38-ontology-registry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/upper.json"
  - "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/kpifw.json"
  - "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/business.json"
  - "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/raas.json"
  - "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json"
autonomous: true
requirements:
  - ONTO-01
  - ONTO-02
tags: [km-core, ontology, test-fixtures, sc3-fixture]

must_haves:
  truths:
    - "Four OKM ontology JSON files (upper/kpifw/business/raas) are present in km-core tests/fixtures/ontology/ byte-identical to the OKM originals (per D-26 + PATTERNS.md \"copy verbatim\")."
    - "The synthetic coding-ontology.json fixture exists in the same directory, has `meta.extends: \"upper\"`, contains the L1 classes from B's component-manifest.yaml each with `extends: \"Component\"`, and L2 classes each with `extends: \"<L1 parent>\"`."
    - "All 5 fixture files parse as valid JSON."
    - "The synthetic fixture's `meta.description` explicitly notes it is a temporary B-shape proxy that Phase 42 owns the real YAML→JSON conversion for (per D-26 + PATTERNS.md landmine)."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/upper.json"
      provides: "OKM upper ontology (13 classes) — verbatim copy"
      contains: "\"name\": \"upper\""
    - path: "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/kpifw.json"
      provides: "OKM kpifw lower ontology, exercises `meta.extends: upper` + per-class `KPIPipeline extends Pipeline`"
      contains: "\"extends\": \"upper\""
    - path: "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/business.json"
      provides: "OKM business lower ontology — verbatim"
      contains: "\"name\": \"business\""
    - path: "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/raas.json"
      provides: "OKM raas lower ontology, includes per-class extends across upper (RPU extends Component, ArgoWorkflow extends Pipeline, S3DataPath extends DataAsset)"
      contains: "\"name\": \"raas\""
    - path: "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json"
      provides: "Synthetic B-shape fixture for SC#3 verification; L1+L2 classes derived from coding/integrations/mcp-server-semantic-analysis/config/component-manifest.yaml"
      contains: "\"extends\": \"upper\""
  key_links:
    - from: "tests/fixtures/ontology/kpifw.json"
      to: "tests/fixtures/ontology/upper.json"
      via: "meta.extends + per-class extends"
      pattern: "\"extends\":\\s*\"(upper|Pipeline)\""
    - from: "tests/fixtures/ontology/coding-ontology.json"
      to: "tests/fixtures/ontology/upper.json"
      via: "meta.extends + per-class extends Component"
      pattern: "\"extends\":\\s*\"(upper|Component)\""
---

<objective>
Land the five test fixture JSON files. Wave-1 plan with zero source dependencies — can run in parallel with Plan 01. These fixtures back every test the registry (Plan 03) and the GraphKMStore integration (Plan 06) assert against.

Purpose: D-26 mandates the synthetic coding-ontology.json (B-shape proxy) for SC#3 verification — Phase 38 ships JSON-only; Phase 42 owns YAML→JSON conversion. The 4 OKM fixtures (verbatim copies) prove SC#1+SC#2 against real production-shape data.

Output: Five JSON files under /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/. Four are byte-for-byte copies of OKM source; one is a synthetic composite derived from B's component-manifest.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md

<interfaces>
<!-- Source-of-truth files the executor must mirror (Task 1) or derive from (Task 2). -->

OKM ontology directory (verbatim source for Task 1):
- /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/upper.json (7809 bytes, 13 classes)
- /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/kpifw.json (2899 bytes, 5 classes, meta.extends: "upper")
- /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/business.json (3608 bytes, 5 classes)
- /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/raas.json (2889 bytes, 6 classes, includes per-class extends across upper)

B manifest source for the synthetic fixture (Task 2):
- /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/config/component-manifest.yaml — actual content has 7 L1 components (LiveLoggingSystem, LLMAbstraction, DockerizedServices, KnowledgeManagement, CodingPatterns, ConstraintSystem, SemanticAnalysis) and 5 L2 components (ManualLearning, OnlineLearning under KnowledgeManagement; Pipeline, Ontology, Insights under SemanticAnalysis). NOTE: D-26 and PATTERNS.md mention "8 L1 + 5 L2" — that figure drifts from the on-disk count of 7 L1. Use the on-disk source of truth (7 L1 + 5 L2 = 12 classes) and call out the count discrepancy in the SUMMARY as a documentation-source drift, not a fixture defect.

PATTERNS.md fixture skeleton for the synthetic file (lines 916-955 of 38-PATTERNS.md):
```json
{
  "meta": {
    "name": "coding",
    "version": "1.0.0",
    "extends": "upper",
    "description": "Synthetic coding-domain lower ontology — proxy for B's component-manifest.yaml (7 L1 + 5 L2; see 38-02-SUMMARY for source-count drift note). Phase 42 owns the real YAML→JSON conversion; this fixture exists purely so Phase 38 SC#3 can verify load + extends-merge works against B-shape data without forcing the YAML adapter."
  },
  "classes": {
    "<L1-name>": { "extends": "Component", "defaultLayer": "evidence", "description": "...", "relationships": {} },
    "<L2-name>": { "extends": "<L1-parent>", "defaultLayer": "evidence", "description": "...", "relationships": {} }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy 4 OKM ontology JSONs verbatim into km-core fixtures/ontology/</name>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/upper.json (full file — to copy)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/kpifw.json (full file — to copy)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/business.json (full file — to copy)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/raas.json (full file — to copy)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"tests/fixtures/ontology/{upper,kpifw,business,raas}.json" (landmines: byte-identical copy, no .gitkeep, no reformat)
  </read_first>
  <files>
    /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/upper.json,
    /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/kpifw.json,
    /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/business.json,
    /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/raas.json
  </files>
  <action>
    The km-core fixture directory /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/ does not exist yet (per PATTERNS.md "Verified on disk"). Create it via `mkdir -p`, then copy the four OKM ontology JSON files using `cp` (verbatim — bytes preserved; OKM's 2-space indentation, trailing newlines, ordering must be exactly preserved).
    Do NOT read+parse+rewrite via JSON.parse + JSON.stringify (would reformat the file and break the byte-equal contract PATTERNS.md flags as a landmine). Use shell `cp` directly via Bash.
    Verify byte equality with `cmp` after each copy. Do NOT add a `.gitkeep` or sibling file (PATTERNS.md landmine: would clutter the canonical fixture set; the readdirSync filter would skip non-.json files but the convention is to keep the dir clean).
    no-console-log: not applicable (no JS/TS files created).
  </action>
  <acceptance_criteria>
    - `mkdir -p /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/` completes (idempotent).
    - All four files exist at the target paths.
    - `cmp /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/upper.json /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/upper.json` exits 0 (byte-identical).
    - Same `cmp` test passes for kpifw.json, business.json, raas.json.
    - `cat /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/kpifw.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['meta']['extends']=='upper'; print('OK')"` prints OK (validates meta.extends round-trips).
    - File count in dir is exactly 4 after this task (Task 2 brings it to 5): `test "$(ls -1 /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/*.json | wc -l)" -eq 4`.
  </acceptance_criteria>
  <verify>
    <automated>for f in upper kpifw business raas; do cmp "/Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/${f}.json" "/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/${f}.json" || exit 1; done && echo OK</automated>
  </verify>
  <done>All four OKM fixtures are present in km-core, byte-identical to OKM originals, validated by `cmp` for each. The directory contains only these 4 .json files (Task 2 adds the 5th).</done>
</task>

<task type="auto">
  <name>Task 2: Author the synthetic coding-ontology.json (B-shape proxy for SC#3)</name>
  <read_first>
    - /Users/Q284340/Agentic/coding/integrations/mcp-server-semantic-analysis/config/component-manifest.yaml (source-of-truth for L1+L2 names — head 60 lines is enough to enumerate component names)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/kpifw.json (JSON-shape skeleton to mirror — meta + classes layout)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/ontology/upper.json (confirm `Component` class name is defined in upper — the synthetic fixture's L1 entries depend on it)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"tests/fixtures/ontology/coding-ontology.json" (DELTAS — aliases/keywords dropped, names verbatim, empty relationships acceptable)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md §Specific Ideas (acknowledged 8 L1 vs actual 7 L1 — proceed with on-disk truth, surface in SUMMARY)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json</files>
  <action>
    Write the synthetic fixture as a JSON file (2-space indent to match OKM style; LF line endings; no trailing whitespace).
    Top-level shape:
    - `meta.name`: "coding"
    - `meta.version`: "1.0.0"
    - `meta.extends`: "upper" (exercises ontology-level inheritance — ONTO-02)
    - `meta.description`: include explicit text noting (a) "Synthetic coding-domain lower ontology", (b) "proxy for B's component-manifest.yaml", (c) "Phase 42 owns the real YAML→JSON conversion". This makes the file self-documenting per PATTERNS.md landmine.
    - `classes`: an object containing entries for the 7 L1 components from B's manifest AND the 5 L2 children — total 12 classes.
    Per-class shape:
    - Each L1 class: `extends: "Component"` (exercises cross-ontology per-class extends — ONTO-02), `defaultLayer: "evidence"`, `description: "L1 component from B's manifest (synthetic proxy)."`, `relationships: {}`.
    - Each L2 class: `extends: "<L1-parent-from-manifest>"` (exercises in-lower per-class extends), `defaultLayer: "evidence"`, `description: "L2 sub-component of <parent> (synthetic proxy)."`, `relationships: {}`.
    Exact names to use (from on-disk component-manifest.yaml — DO NOT invent names; copy verbatim from the YAML):
    - L1 (7): LiveLoggingSystem, LLMAbstraction, DockerizedServices, KnowledgeManagement, CodingPatterns, ConstraintSystem, SemanticAnalysis
    - L2 under KnowledgeManagement (2): ManualLearning, OnlineLearning
    - L2 under SemanticAnalysis (3): Pipeline, Ontology, Insights
    Drop B's `aliases` and `keywords` (PATTERNS.md DELTAS bullet 1 — no JSON home; Phase 42 decides whether they survive the real conversion).
    Empty `relationships: {}` is valid per the type contract (Record&lt;string, string[]&gt; — empty object satisfies); PATTERNS.md landmine confirms this is the simpler path.
    Validate the file parses as JSON (`python3 -c "import json; json.load(open('/Users/.../coding-ontology.json'))"`).
    no-console-log: not applicable.
  </action>
  <acceptance_criteria>
    - File exists at /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json.
    - Parses as valid JSON: `python3 -c "import json; d=json.load(open('/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json')); print('OK')"` prints OK.
    - `meta.extends == "upper"`: `python3 -c "import json; d=json.load(open('/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json')); assert d['meta']['extends']=='upper'; print('OK')"` prints OK.
    - Total class count is 12 (7 L1 + 5 L2): `python3 -c "import json; d=json.load(open('/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json')); assert len(d['classes'])==12, len(d['classes']); print('OK')"` prints OK.
    - Every L1 class has `extends == "Component"`: `python3 -c "import json; d=json.load(open('/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json'))['classes']; l1=['LiveLoggingSystem','LLMAbstraction','DockerizedServices','KnowledgeManagement','CodingPatterns','ConstraintSystem','SemanticAnalysis']; assert all(d[n]['extends']=='Component' for n in l1); print('OK')"` prints OK.
    - Every L2 class extends a known L1: `python3 -c "import json; d=json.load(open('/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json'))['classes']; assert d['ManualLearning']['extends']=='KnowledgeManagement' and d['OnlineLearning']['extends']=='KnowledgeManagement' and d['Pipeline']['extends']=='SemanticAnalysis' and d['Ontology']['extends']=='SemanticAnalysis' and d['Insights']['extends']=='SemanticAnalysis'; print('OK')"` prints OK.
    - `grep -c "aliases\\|keywords" /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json` returns 0 (B's aliases/keywords dropped per PATTERNS DELTAS).
    - Total fixture file count in dir is 5: `test "$(ls -1 /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/*.json | wc -l)" -eq 5`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/Users/Q284340/Agentic/km-core/tests/fixtures/ontology/coding-ontology.json')); assert d['meta']['extends']=='upper' and len(d['classes'])==12; print('OK')"</automated>
  </verify>
  <done>The synthetic fixture is valid JSON, declares meta.extends="upper", contains 12 classes (7 L1 + 5 L2) matching B's component-manifest.yaml verbatim, every L1 extends "Component", every L2 extends its L1 parent, and aliases/keywords from B are dropped. Source-count drift between CONTEXT.md "8 L1" and actual "7 L1" is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OKM-repo -> km-core fixtures | OKM JSON files copied into km-core via local filesystem; both are operator-curated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Tampering | OKM fixture copy not byte-equal to source | mitigate | `cmp` byte-equality assertion on each of the 4 copies (acceptance criterion). Verbatim-copy invariant is the PATTERNS.md landmine; if it fails, the executor must redo the copy with `cp` (not JSON.stringify roundtrip). |
| T-38-02-02 | Information Disclosure | synthetic fixture leaks proprietary B-internal class names | accept | B's component-manifest.yaml is in the public coding repo; these names are already public-facing per the project's open-source posture. |
| T-38-02-03 | Tampering | synthetic fixture meta.description hides the synthetic nature | mitigate | Acceptance criterion requires the description string to contain "Synthetic", "proxy", and "Phase 42 owns the real YAML→JSON conversion" — operator inspecting the file sees the lineage immediately. |
</threat_model>

<verification>
- All 5 fixtures parse as valid JSON.
- The 4 OKM fixtures are byte-identical to OKM source (cmp clean).
- The synthetic fixture's class count matches the on-disk B manifest count (7 L1 + 5 L2 = 12), not the 8+5 figure mentioned in CONTEXT.md / PATTERNS.md (which is a doc-source drift, not a fixture defect).
- All per-class `extends` references point to either `"upper"` (meta.extends) or a class name defined in another fixture (`Component` from upper.json; the 7 L1 names from this file itself).
</verification>

<success_criteria>
- The fixture directory contains exactly 5 .json files; nothing else (no .gitkeep, no README).
- The 4 OKM fixtures load via `loadOntologyFile` (Plan 01) without throwing (validated indirectly in Plan 06 integration tests).
- The synthetic fixture exercises both kinds of `extends`: ontology-level (`meta.extends: "upper"`) and per-class (`L2 extends L1`, `L1 extends Component`).
- Path is /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/ (matches PATTERNS.md and the integration tests in Plans 05/06).
</success_criteria>

<output>
Create `.planning/phases/38-ontology-registry/38-02-SUMMARY.md` on completion. Surface the 8 L1 vs 7 L1 source-count drift in the SUMMARY.
</output>
