---
phase: 38-ontology-registry
plan: 04
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - "/Users/Q284340/Agentic/km-core/src/validation/ontology.ts"
  - "/Users/Q284340/Agentic/km-core/src/index.ts"
autonomous: true
requirements:
  - ONTO-01
  - ONTO-02
tags: [km-core, ontology, validator, factory, barrel]

must_haves:
  truths:
    - "src/validation/ontology.ts exports a new factory function `registryBackedValidator(registry: OntologyRegistry): OntologyValidator` that, when called, returns an OntologyValidator whose `validate(entityType)` throws `Error(\\`Unknown ontology class: ${entityType}\\`)` iff `registry.isValidClass(entityType)` is false."
    - "The thrown error message text is the EXACT substring 'Unknown ontology class:' — Phase 37's existing tests in tests/unit/graph-store.test.ts:198 grep-assert on this regex `/Unknown ontology class/`. Changing the message breaks Phase 37 tests."
    - "The existing `OntologyValidator` interface and `noopOntologyValidator` constant remain exported and unchanged (additive only)."
    - "The root barrel src/index.ts re-exports `registryBackedValidator` alongside the existing OntologyValidator / noopOntologyValidator exports."
    - "No circular import: src/validation/ontology.ts imports `OntologyRegistry` from src/ontology/registry.js (one-way); src/ontology/registry.ts does NOT import from src/validation/ (verified by grep)."
    - "Zero `console.*` in the new factory implementation."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/validation/ontology.ts"
      provides: "OntologyValidator interface + noopOntologyValidator (existing) + registryBackedValidator factory (NEW)"
      contains: "export function registryBackedValidator"
    - path: "/Users/Q284340/Agentic/km-core/src/index.ts"
      provides: "Root barrel updated to export registryBackedValidator"
      contains: "registryBackedValidator"
  key_links:
    - from: "src/validation/ontology.ts"
      to: "src/ontology/registry.ts"
      via: "type-only import of OntologyRegistry"
      pattern: "import type \\{ OntologyRegistry \\} from '\\.\\./ontology/registry\\.js'"
    - from: "src/index.ts"
      to: "src/validation/ontology.ts"
      via: "registryBackedValidator export"
      pattern: "registryBackedValidator.*from.*validation/ontology\\.js"
---

<objective>
Wire the OntologyRegistry into the existing pluggable validator interface. Wave-2 plan: depends on Plan 01 (types) only — does NOT directly depend on Plan 03 (registry class) because the factory uses a TYPE-ONLY import of `OntologyRegistry`, which TypeScript erases at compile time. The factory body invokes `registry.isValidClass(entityType)` — a method that Plan 03's class exposes; the type-only dependency means Plan 03 and Plan 04 can ship in either order within wave 2, but Plan 05 (which consumes both) requires both.

Purpose: Bridges the Phase 37 plug-in validator surface (D-19) to the Phase 38 registry. Plan 05 will auto-wire this factory inside the GraphKMStore constructor when `ontologyDir` is set. The factory is also a standalone-usable export for consumers who manage the registry lifecycle externally.

Output: One modified file (src/validation/ontology.ts — pure additive) + one modified file (root barrel adds the factory export).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md
@/Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-04-SUMMARY.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- Existing validator interface and noop default from Phase 37. -->

From /Users/Q284340/Agentic/km-core/src/validation/ontology.ts (current 27-line file — KEEP unchanged, APPEND only):
```typescript
export interface OntologyValidator {
  /** Throws an Error if `entityType` is not a registered ontology class. */
  validate(entityType: string): void;
}

export const noopOntologyValidator: OntologyValidator = {
  validate(_entityType: string): void { /* no-op */ },
};
```

OntologyRegistry surface used by the factory (from Plan 03 — TYPE-ONLY import):
```typescript
import type { OntologyRegistry } from '../ontology/registry.js';

// Method signature on the registry (from PATTERNS.md §"src/ontology/registry.ts"):
//   isValidClass(className: string): boolean
// The factory only calls this method.
```

PATTERNS.md §"src/validation/ontology.ts" landmines:
1. Error message text MUST be the substring "Unknown ontology class:" — Phase 37's tests/unit/graph-store.test.ts line 198 asserts `/Unknown ontology class/`. The factory's thrown error must match.
2. Circular-import risk: validation/ontology.ts → ontology/registry.ts via type-only import is safe; the registry must NOT import OntologyValidator from validation/ontology.ts. Verify with grep AFTER Plan 03 + Plan 04 both land.
3. Do NOT make `OntologyRegistry` implement `OntologyValidator` directly — that creates a runtime circular reference. The factory pattern (validator wraps registry) is the contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append registryBackedValidator factory to src/validation/ontology.ts</name>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/validation/ontology.ts (current 27 lines — additive edit, preserve every existing line)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"src/validation/ontology.ts" (DELTAS, landmines, error-message contract — lines 569-628 of PATTERNS.md)
    - /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts (Phase 37 — lines 184-216 to confirm the existing `Unknown ontology class` regex contract; do NOT modify this test file in Plan 04)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/src/validation/ontology.ts</files>
  <action>
    Use Edit (not Write) to APPEND to the existing file. Do not modify any existing line; pure additive edit at the end of the file.

    Append (use NodeNext .js extension on the import path per CF-D06):
    - A type-only import at an appropriate location (after the existing comment block and BEFORE the new function — TypeScript style is to group all imports at the top of the file, so insert this import at the top alongside any existing imports; if the file currently has no imports, add a new import block at the top):
      `import type { OntologyRegistry } from '../ontology/registry.js';`
    - A new exported function:
      - Signature: `export function registryBackedValidator(registry: OntologyRegistry): OntologyValidator`
      - Body: returns an object literal implementing OntologyValidator. The `validate` method calls `registry.isValidClass(entityType)`; if it returns false, throw `new Error(\`Unknown ontology class: ${entityType}\`)`. If true, return without throwing.
    - JSDoc block ABOVE the function (no fenced code blocks; prose only): naming Phase 38 / ONTO-01/02, the error-message contract (Phase 37 test compatibility), and a usage hint pointing at Plan 05's auto-wire path (GraphKMStoreOptions.ontologyDir).

    Critical constraints:
    - Error message MUST be exactly `\`Unknown ontology class: ${entityType}\`` — the substring "Unknown ontology class" must appear. Phase 37 tests grep this regex.
    - DO NOT alter the existing `OntologyValidator` interface declaration or the `noopOntologyValidator` constant — pure additive edit.
    - The import is type-only (`import type`) so it erases at compile time — no runtime dependency cycle.
    - no-console-log: the factory's only output is the thrown Error; no `console.*` allowed.
  </action>
  <acceptance_criteria>
    - File still contains the original interface + noop default: `grep -F "export interface OntologyValidator" /Users/Q284340/Agentic/km-core/src/validation/ontology.ts` matches; `grep -F "export const noopOntologyValidator" /Users/Q284340/Agentic/km-core/src/validation/ontology.ts` matches.
    - New function declared: `grep -E "^export function registryBackedValidator\\(registry: OntologyRegistry\\): OntologyValidator" /Users/Q284340/Agentic/km-core/src/validation/ontology.ts` matches exactly once.
    - Type-only import: `grep -F "import type { OntologyRegistry } from '../ontology/registry.js'" /Users/Q284340/Agentic/km-core/src/validation/ontology.ts` matches.
    - Error message text contract: `grep -F "Unknown ontology class:" /Users/Q284340/Agentic/km-core/src/validation/ontology.ts` matches.
    - `grep -F "isValidClass" /Users/Q284340/Agentic/km-core/src/validation/ontology.ts` matches (factory body invokes the registry method).
    - No console.*: `grep -v '^\s*//\|^\s*\*' /Users/Q284340/Agentic/km-core/src/validation/ontology.ts | grep -cE "console\\.(log|warn|error|info|debug)"` returns 0.
    - No circular import (Plan 03's registry.ts MUST NOT import from src/validation/ontology.ts — verified after both Plan 03 + 04 land):
      `grep -F "from '../validation/ontology" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts | wc -l` returns 0. (Run this only if registry.ts exists; if it does not yet exist when Plan 04 runs, skip this check — it must be re-verified at Plan 05 / Plan 06 time.)
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0 (this requires Plan 03's registry.ts to be present — if Plan 04 runs before Plan 03 lands the registry file, the type-only import resolves to a missing module and tsc fails. In that case, the executor of Plan 04 must wait for Plan 03 to land. Per the wave structure, both are wave 2 and depend on Plan 01; the orchestrator may run them sequentially. The acceptance is: if Plan 03's file does not yet exist, this acceptance is deferred until both land — the orchestrator will retry).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && grep -qF "Unknown ontology class:" src/validation/ontology.ts && grep -qF "export const noopOntologyValidator" src/validation/ontology.ts && grep -qE "^export function registryBackedValidator" src/validation/ontology.ts && test "$(grep -v '^\\s*//\\|^\\s*\\*' src/validation/ontology.ts | grep -cE 'console\\.(log|warn|error|info|debug)')" -eq 0 && echo OK</automated>
  </verify>
  <done>The factory is appended to the file without altering existing exports; error message text matches Phase 37 regex; type-only import isolates the registry dependency at compile time; no console.* in the file. If Plan 03 has not yet landed registry.ts when this task verifies, the executor surfaces the dependency and waits — does NOT remove the type-only import to make tsc pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update root barrel src/index.ts to re-export registryBackedValidator</name>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/index.ts (current root barrel — may already have been modified by Plan 03; this task adds ONE more line in the same export block)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"src/index.ts" (the existing-pattern excerpt; Plan 03 added the OntologyRegistry lines, Plan 04 adds the registryBackedValidator line)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/src/index.ts</files>
  <action>
    Use Edit to add a single export line to the root barrel:
    - `export { registryBackedValidator } from './validation/ontology.js';`
    - Place this line near the existing `export { noopOntologyValidator } from './validation/ontology.js';` (lines 44-46 of the original 47-line file; may have shifted if Plan 03 already inserted its block). Group together with the other validation/ontology.js exports.
    - If Plan 03 has NOT yet run (the file does not contain `OntologyRegistry`), still add this Plan 04 line — Plan 04 is independent of Plan 03 for this barrel edit. Order does not matter at the barrel layer (each line is independent).
    - Do not collapse the existing `noopOntologyValidator` export into a multi-name `export {a, b} from ...` — PATTERNS.md landmine: preserve the existing one-per-line style if that's what the file currently uses; otherwise mirror whatever style is already in the file.
    - DO NOT remove or reorder any existing exports — same PATTERNS.md landmine as Plan 03.

    no-console-log: not applicable.
  </action>
  <acceptance_criteria>
    - `grep -F "export { registryBackedValidator } from './validation/ontology.js';" /Users/Q284340/Agentic/km-core/src/index.ts` matches exactly once.
    - Existing exports intact: `grep -F "export { noopOntologyValidator } from './validation/ontology.js';" /Users/Q284340/Agentic/km-core/src/index.ts` still matches.
    - `grep -F "export { GraphKMStore }" /Users/Q284340/Agentic/km-core/src/index.ts` still matches.
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0.
    - `cd /Users/Q284340/Agentic/km-core && npm run build` exits 0; `dist/index.js` exists and exports `registryBackedValidator`: `grep -F "registryBackedValidator" /Users/Q284340/Agentic/km-core/dist/index.js` matches.
    - External-consumer smoke (mirroring Phase 37 Plan 04 pattern): a tmp consumer doing `import { registryBackedValidator } from '@fwornle/km-core'` compiles `tsc --noEmit` clean. (The orchestrator may choose to skip this if Plan 06 covers the same surface via the integration test.)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && npm run build && grep -qF "registryBackedValidator" dist/index.js && grep -qF "export { GraphKMStore }" src/index.ts && grep -qF "export { noopOntologyValidator }" src/index.ts && echo OK</automated>
  </verify>
  <done>registryBackedValidator is exported from the root barrel and reachable in the built dist/index.js. Phase 37 exports unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> validate() | entityType strings cross from consumer code into the validator's strict path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Spoofing | caller passes an entityType that is NOT in the registry expecting validation to pass | mitigate | The factory's validate() invokes `registry.isValidClass(entityType)` and throws `Error(\`Unknown ontology class: ${entityType}\`)` on miss. The Phase 37 BC-2 `skipOntologyCheck: true` opt-out (in GraphKMStore — preserved by Plan 05) is the only bypass; non-bulk paths are strict by D-19. |
| T-38-04-02 | Tampering | error message tampering breaks Phase 37 test regex contract | mitigate | Acceptance criterion grep-asserts the exact substring "Unknown ontology class:" — matches Phase 37 graph-store.test.ts line 198 regex `/Unknown ontology class/`. |
| T-38-04-03 | Information Disclosure | error message includes the rejected entityType string verbatim | accept | Operator/developer diagnostic value > minor info-leak (entityType is consumer-supplied, not user-PII). Same posture as Phase 37 (the existing test stub at line 187 throws the same shape). |
| T-38-04-04 | Tampering | circular import between validation/ontology.ts and ontology/registry.ts causes module-load failure | mitigate | Type-only import (`import type`) in validation/ontology.ts; the registry MUST NOT import from validation/ontology.ts (verified by grep). The factory pattern (validator wraps registry) is the one-way dependency contract. |
</threat_model>

<verification>
- `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0 after both tasks (requires Plan 03's registry.ts to exist).
- `cd /Users/Q284340/Agentic/km-core && npm run build` exits 0; `dist/validation/ontology.js` contains the new factory; `dist/index.js` re-exports it.
- Phase 37's full test suite (existing 33 tests) still passes: `cd /Users/Q284340/Agentic/km-core && npx vitest run` exits 0. Plan 04 should not regress any test.
- No circular import: `grep -F "from '../validation/ontology" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` returns no matches (Plan 03's registry.ts has zero references to validation/ontology.ts).
- No console.*: `grep -v '^\s*//\|^\s*\*' /Users/Q284340/Agentic/km-core/src/validation/ontology.ts | grep -cE "console\\.(log|warn|error|info|debug)"` returns 0.
</verification>

<success_criteria>
- `registryBackedValidator(registry)` is a standalone-usable factory exported from `@fwornle/km-core` root barrel.
- The factory's thrown error message preserves the Phase 37 test contract (`/Unknown ontology class/` regex).
- The factory implementation is the bridge that Plan 05 will auto-wire inside the GraphKMStore constructor when `ontologyDir` is set.
- Type-only import (`import type { OntologyRegistry }`) keeps the validator module's runtime footprint zero — Plan 03's class loads only when a consumer actually constructs the factory.
- ONTO-01 + ONTO-02: this plan does not directly close the requirements (Plans 03 + 05 do) but is the load-bearing glue that makes the strict-validation path actually consult the registry.
</success_criteria>

<output>
Create `.planning/phases/38-ontology-registry/38-04-SUMMARY.md` on completion.
</output>
