---
phase: 38-ontology-registry
plan: 05
type: execute
wave: 3
depends_on:
  - 38-03
  - 38-04
files_modified:
  - "/Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts"
autonomous: true
requirements:
  - ONTO-01
  - ONTO-02
tags: [km-core, ontology, graph-store, integration, options-extension, getter]

must_haves:
  truths:
    - "GraphKMStoreOptions gains `ontologyDir?: string` and `ontologyStrict?: boolean` optional fields."
    - "When `ontologyDir` is supplied, the constructor instantiates `new OntologyRegistry({ ontologyDir, strict: ontologyStrict ?? false })` and stores it in a private `registry` field."
    - "Validator resolution order (most-specific wins): (1) explicit `opts.ontologyValidator` (test-stub injection — Phase 37 behavior preserved); (2) auto-wired `registryBackedValidator(registry)` when `ontologyDir` is set; (3) `noopOntologyValidator` (legacy/unconfigured default — Phase 37 behavior preserved)."
    - "Public getter `store.ontology: OntologyRegistry | undefined` exposes the registry (returns undefined when `ontologyDir` was not supplied)."
    - "The existing `putEntity` strict-validation path (line 240-242 `if (!trusted) this.validator.validate(e.entityType)`) is UNCHANGED — the registryBackedValidator conforms to OntologyValidator, so the call site works without modification."
    - "The trusted-path bypass (skipOntologyCheck: true → bypasses BOTH parseEntityId AND validator — Phase 37 D-19 + BC-2 + CF-D19) is preserved verbatim."
    - "`mergeAttributes` ontology-validation behavior is UNCHANGED (T-37-04-06 accepted disposition preserved — Phase 38 does NOT revisit per PATTERNS.md landmine 'Behavior surprises §6')."
    - "PersistenceManager and Exporter instantiation order is UNCHANGED in the constructor (PATTERNS.md landmine: 'Plan 03 set that order deliberately')."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts"
      provides: "Phase 37 GraphKMStore extended with ontologyDir option, internal registry, and `ontology` getter"
      contains: "ontologyDir"
  key_links:
    - from: "src/store/GraphKMStore.ts"
      to: "src/ontology/registry.ts"
      via: "OntologyRegistry runtime import"
      pattern: "from '\\.\\./ontology/registry\\.js'"
    - from: "src/store/GraphKMStore.ts"
      to: "src/validation/ontology.ts"
      via: "registryBackedValidator import"
      pattern: "registryBackedValidator.*from '\\.\\./validation/ontology\\.js'"
---

<objective>
Wire the OntologyRegistry into the existing GraphKMStore constructor. Wave-3 plan: depends on Plan 03 (registry class) and Plan 04 (validator factory). Pure additive extension of the Phase 37 constructor — no method signatures change, no existing behavior changes.

Purpose: Closes the Phase 38 integration story — consumers writing `new GraphKMStore({ ontologyDir: '/path/to/ontology' })` get a registry-backed strict validator for free. The `store.ontology` getter exposes the registry for downstream consumers (Phase 41/42/43) to call `reload()`, enumerate classes, walk parent chains.

Output: One modified file (GraphKMStore.ts). PATTERNS.md §"src/store/GraphKMStore.ts" specifies the exact edit set (lines 427-566 of PATTERNS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md
@/Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-04-SUMMARY.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- Phase 37 GraphKMStore.ts current shape (the file Plan 05 modifies). -->

From /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (455 lines as of Phase 37 Plan 04):
- Lines 63-80: import block (existing imports — Plan 05 appends two new imports here).
- Lines 87-103: `GraphKMStoreOptions` interface — Plan 05 adds two fields.
- Lines 117-122: class header + private field declarations (graph, persistence, exporter, validator, initialized) — Plan 05 adds `private readonly registry: OntologyRegistry | undefined`.
- Lines 124-136: constructor body — Plan 05 inserts (a) registry instantiation BEFORE the validator assignment, (b) validator resolution chain that prefers explicit > auto-wired > noop.
- Line 240-242: `putEntity` strict-validation call `if (!trusted) this.validator.validate(e.entityType);` — UNCHANGED.
- New getter to append (location: anywhere in the public-method section; PATTERNS.md suggests right after the constructor):
  ```typescript
  get ontology(): OntologyRegistry | undefined { return this.registry; }
  ```

Phase 38 surfaces being imported:
- `import { OntologyRegistry } from '../ontology/registry.js'` — runtime import (Plan 03's class)
- `import { registryBackedValidator } from '../validation/ontology.js'` — runtime import (Plan 04's factory)

PATTERNS.md §"src/store/GraphKMStore.ts" landmines (CRITICAL — re-read before editing):
1. PersistenceManager + Exporter creation order: do NOT reorder (Plan 03 of Phase 37 set this deliberately).
2. The `validator` field stays `private` — only the `registry` is exposed via the getter; the validator is internal plumbing.
3. `mergeAttributes` does NOT re-run ontology validation (T-37-04-06 accepted disposition). PATTERNS.md explicit: Phase 38 ships *without* revisiting this — do not silently change.
4. `skipOntologyCheck: true` bypasses BOTH parseEntityId AND the validator (Phase 37 BC-2 + CF-D19). The trusted-path branch `if (!trusted)` already implements this — preserve verbatim.
5. The default ontologyDir is NOT `<cwd>/ontology/` inside km-core (D-28 final paragraph: no env-var/cwd pickup buried in helper code). Consumers wire defaults at the call site. The JSDoc on the new option must call this out.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GraphKMStore — add ontologyDir option, registry field, getter, validator resolution chain</name>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (full file — 455 lines; the patch is surgical but you must understand the surrounding context, especially the constructor and putEntity strict-path)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"src/store/GraphKMStore.ts" (lines 427-566 — the full edit list including imports, options extension, registry instantiation, validator resolution chain, getter, and the 3 things explicitly NOT to change)
    - /Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-04-SUMMARY.md §"Behavioral Differences for Consumers" item 6 (mergeAttributes ontology-validation skip — accepted disposition Phase 38 does NOT revisit)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts</files>
  <action>
    Apply six surgical edits to the file. Use Edit (not Write) per edit — this is a 455-line file and the patch is small.

    **Edit 1: Imports** (insert AFTER the existing import block, near lines 72-80):
    - `import { OntologyRegistry } from '../ontology/registry.js';`
    - `import { registryBackedValidator } from '../validation/ontology.js';`
    Both are runtime imports (Plan 03 + Plan 04 export the values, not just types).

    **Edit 2: GraphKMStoreOptions interface** (extend lines 87-103, adding two optional fields):
    Add to the interface body:
    - `ontologyDir?: string;` — with a JSDoc block explaining: "Directory containing upper.json + lower ontology JSON files (Phase 38, D-28). When set, GraphKMStore instantiates an OntologyRegistry internally and auto-wires it as the validator (unless `ontologyValidator` is ALSO set, which takes precedence — allows tests to inject stubs). Default: undefined (no registry; falls back to `ontologyValidator` or `noopOntologyValidator`). km-core does NOT default to `<cwd>/ontology/` — D-28 forbids env-var/cwd pickup. Consumers wire defaults at the call site."
    - `ontologyStrict?: boolean;` — with JSDoc: "When true, treats malformed lower-ontology files as fatal (re-throws). Default false (skip+warn per D-29 atomic-build semantics). Forwarded to `OntologyRegistry({ strict })`."

    **Edit 3: Class private field** (line 121 area):
    Add to the class private field declarations (alongside graph, persistence, exporter, validator, initialized):
    - `private readonly registry: OntologyRegistry | undefined;`

    **Edit 4: Constructor body** (lines 124-136):
    The current constructor is:
    ```
    constructor(opts: GraphKMStoreOptions) {
      super();
      this.graph = new MultiDirectedGraph<Entity, Relation>();
      this.persistence = new PersistenceManager(opts.dbPath, opts.exportDir, { domains: opts.domains });
      this.exporter = new Exporter({ exportDir: opts.exportDir, domains: opts.domains, debounceMs: opts.debounceMs });
      this.validator = opts.ontologyValidator ?? noopOntologyValidator;
    }
    ```
    Apply this transformation (DO NOT reorder PersistenceManager + Exporter creation per PATTERNS landmine):
    - AFTER the `this.exporter = new Exporter(...)` line, INSERT:
      `// Phase 38: Ontology registry (D-28 — constructor-injected, no env pickup).`
      `// Build the registry FIRST so the auto-wired validator below can reference it.`
      `if (opts.ontologyDir !== undefined) {`
      `  this.registry = new OntologyRegistry({ ontologyDir: opts.ontologyDir, strict: opts.ontologyStrict ?? false });`
      `} else {`
      `  this.registry = undefined;`
      `}`
    - REPLACE the existing `this.validator = opts.ontologyValidator ?? noopOntologyValidator;` line with the 3-way resolution:
      `// Validator resolution order (most-specific wins):`
      `//   1. Explicit opts.ontologyValidator (test stubs)`
      `//   2. Auto-wired registry-backed validator (when ontologyDir is set)`
      `//   3. noopOntologyValidator (legacy / unconfigured)`
      `this.validator =`
      `  opts.ontologyValidator`
      `  ?? (this.registry ? registryBackedValidator(this.registry) : noopOntologyValidator);`

    **Edit 5: Public getter for the registry** (add anywhere in the public-method section; recommended location: right after the constructor body, BEFORE `open()`):
    - Add the getter:
      `get ontology(): OntologyRegistry | undefined { return this.registry; }`
    - JSDoc block above the getter (prose; no fenced code):
      "Read-only access to the OntologyRegistry instance. Undefined when ontologyDir was not supplied at construction time. Use cases (Phase 39+): `await store.ontology?.reload()` to pick up new ontology files (D-29); `store.ontology?.getAllClassNames()` to enumerate valid classes for UI; `store.ontology?.parentChainOf(class)` for extension provenance."

    **Edit 6: NO CHANGE to putEntity, mergeAttributes, addRelation, or any other method.** Specifically:
    - DO NOT touch line 240-242 (the `if (!trusted) this.validator.validate(e.entityType);` call site). The registry-backed validator implements the same `OntologyValidator` interface, so the call site works unchanged.
    - DO NOT add ontology validation to mergeAttributes — T-37-04-06 accepted, Phase 38 does not revisit.
    - DO NOT add ontology validation to addRelation — Phase 37 D-14 path-of-least-resistance (hasNode safety check only).
    - DO NOT modify the `skipOntologyCheck` / `trusted` flag semantics.

    no-console-log: this edit does not add any diagnostic output to GraphKMStore.ts (the registry emits its own stderr warnings — they propagate up unchanged).
  </action>
  <acceptance_criteria>
    - All Edit 1-5 landed (verified via grep):
      - `grep -F "import { OntologyRegistry } from '../ontology/registry.js';" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
      - `grep -F "import { registryBackedValidator } from '../validation/ontology.js';" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
      - `grep -F "ontologyDir?: string" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
      - `grep -F "ontologyStrict?: boolean" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
      - `grep -F "private readonly registry: OntologyRegistry | undefined" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
      - `grep -E "get ontology\\(\\): OntologyRegistry \\| undefined" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
      - `grep -F "new OntologyRegistry({ ontologyDir: opts.ontologyDir," /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
      - `grep -F "registryBackedValidator(this.registry)" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` matches.
    - Edit 6 (NO-CHANGE) preserved — these existing patterns are still present unchanged:
      - `grep -F "if (!trusted)" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` still matches (line ~240).
      - `grep -F "this.validator.validate(e.entityType)" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` still matches.
      - `grep -F "this.persistence = new PersistenceManager" /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts` still matches AND appears BEFORE `this.exporter = new Exporter` in the file (preserve ordering): `awk '/this\\.persistence = new PersistenceManager/{p=NR} /this\\.exporter = new Exporter/{e=NR} END{exit !(p && e && p<e)}' /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts`.
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0.
    - `cd /Users/Q284340/Agentic/km-core && npm run build` exits 0; `dist/store/GraphKMStore.js` contains references to `OntologyRegistry`: `grep -F "OntologyRegistry" /Users/Q284340/Agentic/km-core/dist/store/GraphKMStore.js` matches.
    - Phase 37 test regression suite still GREEN: `cd /Users/Q284340/Agentic/km-core && npx vitest run` exits 0; all 33 pre-existing tests still pass. Plan 05 must NOT regress any of them.
    - no-console-log preserved: `grep -v '^\s*//\|^\s*\*' /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts | grep -cE "console\\.(log|warn|error|info|debug)"` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && npm run build && npx vitest run && grep -qF "get ontology()" src/store/GraphKMStore.ts && grep -qF "if (!trusted)" src/store/GraphKMStore.ts && awk '/this\.persistence = new PersistenceManager/{p=NR} /this\.exporter = new Exporter/{e=NR} END{exit !(p && e && p<e)}' src/store/GraphKMStore.ts && echo OK</automated>
  </verify>
  <done>The 5 additive edits land; the 1 NO-CHANGE constraint holds (line 240-242 and PersistenceManager/Exporter ordering unchanged); Phase 37 test suite remains green; the new options + getter compile and build clean; no console.* introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer code -> GraphKMStore constructor | new GraphKMStore({ ontologyDir }) crosses configuration data into the store |
| GraphKMStore -> OntologyRegistry | constructor instantiates the registry which reads filesystem (see Plan 03's threats T-38-03-*) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05-01 | Tampering | consumer supplies `ontologyDir` pointing at attacker-controlled directory | accept | Library code; consumer controls the directory. Same posture as `dbPath`/`exportDir` in Phase 37 (no path validation; consumer responsibility). |
| T-38-05-02 | Elevation of Privilege | auto-wired registry-backed validator gets bypassed by skipOntologyCheck=true on an attacker-controlled call | mitigate | Same as Phase 37 BC-2 (accepted): `skipOntologyCheck: true` is the documented trusted-bulk-import flag. Phase 38 preserves the semantics verbatim — operator audit responsibility, not library responsibility. |
| T-38-05-03 | Spoofing | explicit `opts.ontologyValidator` provided alongside `opts.ontologyDir` — could be used to bypass the registry | accept | Documented behavior (validator resolution order #1 wins). Required for test-stub injection. Consumer who wires both gets exactly what they asked for. |
| T-38-05-04 | Tampering | constructor error inside `new OntologyRegistry(...)` propagates out of `new GraphKMStore(...)` | accept | Standard JS behavior; not surprising. Consumer can wrap in try/catch. Phase 37 already has this posture for PersistenceManager/Exporter construction failures. |
</threat_model>

<verification>
- All 33 Phase 37 tests remain green (`npx vitest run` clean).
- The new option fields are wired through to a real registry instantiation (verified via grep).
- The `ontology` getter is reachable on a `GraphKMStore` instance — verified indirectly via the integration test added in Plan 06.
- No-console-log preserved across the entire file.
- The Phase 37 `mergeAttributes` no-ontology-validation behavior is preserved (T-37-04-06 disposition stands; PATTERNS.md landmine honored).
</verification>

<success_criteria>
- `new GraphKMStore({ ontologyDir: <path> })` works end-to-end — the constructor builds the registry, auto-wires the validator, and exposes the registry via `store.ontology`.
- Validator resolution chain matches PATTERNS.md spec: explicit > auto-wired registry-backed > noop.
- Phase 37 contracts UNCHANGED: skipOntologyCheck trusted-path, mergeAttributes ontology-skip, PersistenceManager+Exporter ordering, validator/registry visibility (validator private, registry exposed via getter).
- ONTO-01 + ONTO-02 close at the integration layer for downstream Phase 41/42/43 consumers — they can drop a new ontology JSON into the configured directory + call `await store.ontology?.reload()` to pick it up without modifying km-core code.
</success_criteria>

<output>
Create `.planning/phases/38-ontology-registry/38-05-SUMMARY.md` on completion.
</output>
