---
phase: 38-ontology-registry
plan: 06
type: execute
wave: 3
depends_on:
  - 38-02
  - 38-03
  - 38-05
files_modified:
  - "/Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts"
  - "/Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts"
autonomous: true
requirements:
  - ONTO-01
  - ONTO-02
tags: [km-core, ontology, tests, vitest, sc1-sc2-sc3-sc4]

must_haves:
  truths:
    - "A new file tests/unit/ontology-registry.test.ts exists with describe-blocks covering auto-discovery (ONTO-01), extends + property merging (ONTO-02), public API (D-28 accessors), collision handling (D-27 incl. stderr spy), reload semantics (D-29 incl. atomic-rebuild), and SC#3 coding-ontology fixture verification."
    - "Test cases include AT LEAST: 'loads upper.json + all sibling .json files dynamically', 'alphabetical load order is deterministic', 'throws if upper.json is missing', 'skip+warn on malformed lower file (default non-strict)', 'throw on malformed lower file (strict: true)', 'child class inherits parent relationships', 'child properties override parent on conflict', 'per-class extends chain across upper→lower', 'last-loaded wins on duplicate class name', 'emits stderr warning on collision', 'reload() picks up newly-added ontology files', 'reload() forgets removed classes', 'loads coding-ontology and resolves 12 classes against upper'."
    - "The collision test uses `vi.spyOn(process.stderr, 'write').mockImplementation(() => true)` and asserts the warning text contains both 'redefined' and 'last-loaded wins' substrings."
    - "tests/unit/graph-store.test.ts gains TWO new test cases (append-only — existing 11 test names per Phase 37 Plan 04 SUMMARY are unmodified): (a) 'ontologyDir option auto-wires registry-backed validator' verifies the validator resolution chain via real fixture-directory; (b) 'skipOntologyCheck bypasses registry validator (CF-D19 / BC-2)' verifies the Phase 37 BC-2 invariant is preserved."
    - "All NEW tests are vitest; tmpdir+cleanup pattern is `beforeEach`/`afterEach` mirroring Phase 37's tests/unit/graph-store.test.ts."
    - "Final full-suite run: `npx vitest run` exits 0 with all pre-existing 33 tests still green plus the new ones (estimate ~15+ new tests; the exact count is whatever the executor implements covering the listed must-have cases)."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts"
      provides: "Unit tests for the OntologyRegistry class — every concern from PATTERNS.md test-shape section"
      min_lines: 200
      contains: "describe('OntologyRegistry'"
    - path: "/Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts"
      provides: "Phase 37 graph-store tests EXTENDED with 2 new tests (ontologyDir auto-wiring + skipOntologyCheck preservation)"
      contains: "ontologyDir option auto-wires registry-backed validator"
  key_links:
    - from: "tests/unit/ontology-registry.test.ts"
      to: "tests/fixtures/ontology/"
      via: "FIXTURE_DIR constant + import.meta.dirname"
      pattern: "fixtures/ontology"
    - from: "tests/unit/graph-store.test.ts"
      to: "tests/fixtures/ontology/"
      via: "ontologyDir option passed to makeStore() factory"
      pattern: "ontologyDir.*fixtures/ontology"
---

<objective>
Ship the test layer that verifies ALL four Phase 38 success criteria (SC#1 auto-discovery, SC#2 extends + property merging, SC#3 B-shape fixture loads cleanly, SC#4 stable programmatic API). Wave-3 plan: depends on Plan 02 (fixtures), Plan 03 (registry class), Plan 05 (GraphKMStore integration). Plan 04 is transitively required but not declared because Plan 05 hard-depends on Plan 04 — the orchestrator's wave graph enforces correct ordering.

Purpose: This is the verification spine for the phase. The registry-registry tests prove the class works in isolation; the graph-store extension test proves the integration is wired correctly without regressing Phase 37 behavior.

Output: One new test file + one extended test file. Both run under `npx vitest run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md
@/Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-04-SUMMARY.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- Test scaffolding patterns from km-core and OKM. -->

Composite analog (read both — they teach different halves):
- /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/unit/ontology-loader.test.ts (237 lines — describe-block shape per concern; mirrors OKM's structure)
- /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts (217 lines — vitest-with-tmpdir pattern; `makeStore()` factory; `beforeEach`/`afterEach`)

PATTERNS.md §"tests/unit/ontology-registry.test.ts" (lines 631-746 of PATTERNS.md) supplies the full describe-block template; lines 746-816 supply the two `tests/unit/graph-store.test.ts` extensions.

Fixture path constant (matches OKM idiom + km-core Phase 37 style):
```typescript
const FIXTURE_DIR = path.join(import.meta.dirname, '../fixtures/ontology');
```
`import.meta.dirname` is Node 22+ only; km-core's CI matrix is `['22.x']` per Phase 37 — safe to use (PATTERNS.md landmine confirms).

Stderr spy pattern (D-27 collision-warning test):
```typescript
const spy = vi.spyOn(process.stderr, 'write').mockImplementation(() => true);
// ... load registry with colliding fixtures ...
expect(spy).toHaveBeenCalledWith(expect.stringContaining('redefined'));
expect(spy).toHaveBeenCalledWith(expect.stringContaining('last-loaded wins'));
spy.mockRestore();
```
Note: `process.stderr.write` returns `boolean`; the mock impl MUST return `true` to satisfy Node's writable-stream contract (PATTERNS.md landmine).

OntologyRegistry signature (from Plan 03):
```typescript
new OntologyRegistry({ ontologyDir: string, strict?: boolean })
isValidClass(name: string): boolean
getClass(name: string): ResolvedClass | undefined
getAllClassNames(): string[]
parentChainOf(name: string): ResolvedClass[]
provenanceOf(name: string): string | undefined
get classCatalog(): ReadonlyMap<string, ResolvedClass>
get domains(): ReadonlySet<string>
async reload(): Promise<void>
```

GraphKMStore extension (from Plan 05):
```typescript
new GraphKMStore({ ontologyDir?: string, ontologyStrict?: boolean, ... })
get ontology(): OntologyRegistry | undefined
```

Existing tests/unit/graph-store.test.ts test names (Phase 37 Plan 04 SUMMARY: "all 11 verbatim test names from Plan 01 Task 3" — DO NOT modify any of these names; pure APPEND only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/unit/ontology-registry.test.ts with all 6 describe-blocks from PATTERNS.md</name>
  <read_first>
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"tests/unit/ontology-registry.test.ts" (lines 631-746 — the full describe-block template + landmines: mock returns true, fixtures don't reformat, mkdtempSync cleanup, import.meta.dirname Node 22+)
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/tests/unit/ontology-loader.test.ts (full 237 lines — OKM analog for shape; the assertion-text patterns translate; the constructor pattern does NOT — OKM uses `new OntologyRegistry()` + `.load(dir)`, km-core uses constructor injection `new OntologyRegistry({ ontologyDir })`)
    - /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts (full 217 lines — vitest-with-tmpdir + beforeEach/afterEach + makeStore() factory pattern)
    - /Users/Q284340/Agentic/km-core/tests/fixtures/ontology/*.json (the 5 fixtures from Plan 02 — for asserting class counts and parent chains)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts</files>
  <action>
    Create the new test file. Use vitest (matches Phase 37 — `npx vitest run`). NodeNext .js extensions on every internal import.

    **Imports** (at top of file):
    - `import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';`
    - `import * as fs from 'node:fs';`
    - `import * as path from 'node:path';`
    - `import * as os from 'node:os';`
    - `import { OntologyRegistry } from '../../src/ontology/registry.js';`
    - `import { loadOntologyFile } from '../../src/ontology/loader.js';`

    **Constants**:
    - `const FIXTURE_DIR = path.join(import.meta.dirname, '../fixtures/ontology');`

    **Top-level describe block: `describe('OntologyRegistry', () => { ... })`** containing the following nested describes (PATTERNS.md spec):

    1. `describe('auto-discovery (ONTO-01)', ...)` — at least 5 tests:
       - `it('loads upper.json + all sibling .json files dynamically', ...)` — construct `new OntologyRegistry({ ontologyDir: FIXTURE_DIR })`; assert `registry.domains.has('upper')`, `.has('kpifw')`, `.has('business')`, `.has('raas')`, `.has('coding')` — domains.size should be 5 (all 5 fixtures discovered).
       - `it('alphabetical load order is deterministic', ...)` — use tmpdir + write fixtures with names like `a-ontology.json`, `z-ontology.json` that both declare the same class name; assert `provenanceOf(class) === 'z'` (later alphabetical wins per D-27).
       - `it('throws if upper.json is missing', ...)` — tmpdir without upper.json; assert constructor throws (loader's responsibility, registry does not swallow per PATTERNS landmine).
       - `it('skip+warn on malformed lower file (default non-strict)', ...)` — tmpdir + upper.json + a broken lower file (e.g., truncated JSON); spy on stderr; construct registry; assert no throw + stderr.write called with substring `'skipping malformed ontology file'`.
       - `it('throw on malformed lower file (strict: true)', ...)` — same tmpdir setup; construct `new OntologyRegistry({ ontologyDir, strict: true })`; assert the constructor throws.

    2. `describe('extends + property merging (ONTO-02)', ...)` — at least 3 tests covering OKM's ONTO-02 behavior:
       - `it('child class inherits parent relationships', ...)` — load FIXTURE_DIR; get `kpifw`'s `KPIPipeline` class via `registry.getClass('KPIPipeline')`; assert `merged.relationships` includes BOTH upper-`Pipeline`'s relationship keys AND KPIPipeline's own. (Translate OKM's analog test lines 86-100 verbatim.)
       - `it('child properties override parent on conflict', ...)` — if no natural conflict in fixtures, construct synthetic fixtures in tmpdir that exercise the override. Assert child key value, not parent's.
       - `it('per-class extends chain across upper→lower', ...)` — assert `registry.parentChainOf('KPIPipeline')[0].name === 'Pipeline'` (the parent from upper.json).

    3. `describe('public API (D-28 + canonical refs)', ...)` — at least 5 tests:
       - `it('isValidClass / getClass / getAllClassNames work', ...)` — basic round-trips.
       - `it('parentChainOf returns closest-first chain', ...)` — verify ordering.
       - `it('provenanceOf returns source domain', ...)` — assert `provenanceOf('KPIPipeline') === 'kpifw'`.
       - `it('domains getter exposes loaded ontology names', ...)` — assert `registry.domains.has('upper')`, etc.
       - `it('classCatalog is a ReadonlyMap', ...)` — TypeScript-level check (just inspect the returned shape); at runtime, asserts `registry.classCatalog.size > 0` and that `.set` is either undefined or throws on attempted mutation.

    4. `describe('collision handling (D-27)', ...)` — at least 2 tests:
       - `it('last-loaded wins on duplicate class name', ...)` — tmpdir + 2 fixtures with the SAME class name (e.g., both declare `Component`); load registry; assert `provenanceOf('Component')` is the later (alphabetical) source.
       - `it('emits stderr warning on collision', ...)` — `const spy = vi.spyOn(process.stderr, 'write').mockImplementation(() => true);` after registry construction with colliding fixtures, assert: `expect(spy).toHaveBeenCalledWith(expect.stringContaining('redefined'))` AND `expect(spy).toHaveBeenCalledWith(expect.stringContaining('last-loaded wins'))`. Then `spy.mockRestore()`.

    5. `describe('reload (D-29)', ...)` — at least 3 tests:
       - `it('reload() picks up newly-added ontology files', ...)` — tmpdir + copy only `upper.json`; construct registry; assert `isValidClass('RPU') === false` (RPU is from raas.json); copy `raas.json` into tmpdir; `await registry.reload()`; assert `isValidClass('RPU') === true`.
       - `it('reload() forgets removed classes (D-29 last paragraph)', ...)` — tmpdir with upper + raas; construct registry; assert `isValidClass('RPU') === true`; delete raas.json; `await registry.reload()`; assert `isValidClass('RPU') === false`.
       - `it('reload() is atomic — concurrent lookup never sees half-built state', ...)` — simple version: synchronously call `isValidClass` immediately after `await reload()` completes; verify the state is fully new (not mixed). True concurrency is hard to test deterministically; this version asserts the contract.

    6. `describe('coding-ontology fixture (SC#3 — B-shape proxy)', ...)` — at least 2 tests:
       - `it('loads upper + coding-ontology and resolves L1 + L2 classes', ...)` — IMPORTANT: use a separate tmpdir to avoid kpifw/business/raas cross-contamination per PATTERNS landmine. Copy ONLY `upper.json` + `coding-ontology.json` into the tmpdir; construct registry; assert each of the 7 L1 class names from Plan 02's coding-ontology.json is `isValidClass(name) === true`; assert each of the 5 L2 names is `isValidClass(name) === true` AND `parentChainOf(name)[0].name === <its L1 parent>`. Source-of-truth class count: 7 L1 + 5 L2 = 12 classes (matches on-disk B manifest per Plan 02 SUMMARY drift note).
       - `it('L1 classes inherit Component relationships via extends "Component"', ...)` — for each L1, assert `parentChainOf(L1)[0].name === 'Component'`.

    **tmpdir + cleanup pattern** (Pattern S5 from PATTERNS.md):
    - Use a top-level `let tmpdir: string;` paired with `beforeEach(() => { tmpdir = fs.mkdtempSync(path.join(os.tmpdir(), 'km-core-ontology-test-')); })` and `afterEach(() => { fs.rmSync(tmpdir, { recursive: true, force: true }); })` — but only inside describe-blocks that need tmpdir mutation. Static-fixture tests (FIXTURE_DIR) don't need tmpdir cleanup.

    **PATTERNS.md landmines re-stated**:
    - Mock `process.stderr.write` MUST return `true` (Node writable-stream contract).
    - Do NOT modify fixture files inside the asserts; mutation tests use tmpdir copies.
    - `import.meta.dirname` is Node 22+ only (km-core's CI matrix is 22.x).
    - Tests in `beforeAll` blocks vs `beforeEach`: PATTERNS.md recommends `beforeAll` for static-fixture-only tests (saves construction cost), `beforeEach` for mutation tests.

    no-console-log: tests are allowed to use stderr-spying but MUST NOT introduce `console.*` calls.
  </action>
  <acceptance_criteria>
    - File exists at /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts.
    - File contains at least 6 nested describes: `grep -cE "^\\s+describe\\(" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` returns at least 6.
    - All 6 top-level concern names appear as describe strings: `grep -c "describe('auto-discovery (ONTO-01)'\\|describe('extends + property merging (ONTO-02)'\\|describe('public API\\|describe('collision handling (D-27)'\\|describe('reload (D-29)'\\|describe('coding-ontology fixture" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` returns at least 6 lines combined.
    - Stderr spy pattern present: `grep -F "vi.spyOn(process.stderr, 'write')" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches; `grep -F "stringContaining('redefined')" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches; `grep -F "stringContaining('last-loaded wins')" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches.
    - reload-test present: `grep -F "await reg.reload()" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches OR `grep -F "await registry.reload()" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches.
    - tmpdir cleanup pattern present: `grep -F "fs.mkdtempSync" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches; `grep -F "fs.rmSync" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches.
    - SC#3 assertion present: `grep -F "coding-ontology" /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts` matches; the test references at least one L1 name from Plan 02 (e.g., LiveLoggingSystem, KnowledgeManagement) AND at least one L2 name (e.g., ManualLearning, Pipeline).
    - no-console-log preserved: `grep -v '^\s*//\|^\s*\*' /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts | grep -cE "console\\.(log|warn|error|info|debug)"` returns 0.
    - The test file compiles: `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0.
    - The new test file PASSES when run in isolation: `cd /Users/Q284340/Agentic/km-core && npx vitest run tests/unit/ontology-registry.test.ts` exits 0 with all assertions green.
    - File size sanity: at least 200 lines (the 6 describes + tests + tmpdir scaffolding): `wc -l < /Users/Q284340/Agentic/km-core/tests/unit/ontology-registry.test.ts | xargs test 200 -le`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && npx vitest run tests/unit/ontology-registry.test.ts && grep -qF "stringContaining('last-loaded wins')" tests/unit/ontology-registry.test.ts && grep -qF "await registry.reload()" tests/unit/ontology-registry.test.ts || grep -qF "await reg.reload()" tests/unit/ontology-registry.test.ts && echo OK</automated>
  </verify>
  <done>The new test file covers all 6 PATTERNS-required concerns; collision warning text is grep-asserted via stderr spy; reload semantics (add + remove) verified; SC#3 fixture loads against upper without collisions; all assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Append 2 new tests to tests/unit/graph-store.test.ts (do not modify existing 11)</name>
  <read_first>
    - /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts (current 217 lines — pure APPEND only; existing 11 test names are protected per Phase 37 Plan 04 SUMMARY)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"tests/unit/graph-store.test.ts" (lines 765-846 — the two new tests' shape + landmines)
    - /Users/Q284340/Agentic/km-core/src/store/GraphKMStore.ts (modified by Plan 05 — confirms the `ontology` getter + new option fields exist)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts</files>
  <action>
    Use Edit to APPEND two new test cases to the existing file. Do NOT modify any existing test name (per Phase 37 Plan 04 SUMMARY: "all 11 verbatim test names from Plan 01 Task 3" — grep-verified elsewhere).

    Identify the location: AFTER the existing `test('skipOntologyCheck flag bypasses validation', ...)` test (line ~216 in the current file) — append the new tests at the end of the same describe-block (or top-level if there is no enclosing describe).

    **New Test 1: `test('ontologyDir option auto-wires registry-backed validator', async () => { ... })`**

    Use the same `makeStore()` factory pattern but pass `ontologyDir`. PATTERNS.md template (lines 793-816) gives the body shape. Translate to:
    - Close & cleanup the default ctx from `beforeEach`.
    - `ctx = makeStore({ ontologyDir: path.join(import.meta.dirname, '../fixtures/ontology') })`.
    - `await ctx.store.open()`.
    - Assert the getter: `expect(ctx.store.ontology).toBeDefined()`.
    - Assert classes from multiple loaded fixtures: `expect(ctx.store.ontology!.isValidClass('Component')).toBe(true)` (upper.json), `expect(ctx.store.ontology!.isValidClass('RPU')).toBe(true)` (raas.json), `expect(ctx.store.ontology!.isValidClass('Bogus')).toBe(false)`.
    - Assert validator behavior via putEntity:
      - Valid class succeeds: `await ctx.store.putEntity({ name: 'Valid', entityType: 'Component' })` resolves.
      - Invalid class throws with the Phase 37 regex: `await expect(ctx.store.putEntity({ name: 'Bogus', entityType: 'Bogus' })).rejects.toThrow(/Unknown ontology class/)`.

    **New Test 2: `test('skipOntologyCheck bypasses registry validator (CF-D19 / BC-2)', async () => { ... })`**

    Verifies the Phase 37 BC-2 invariant is preserved by Plan 05.
    - Close & cleanup default ctx.
    - `ctx = makeStore({ ontologyDir: path.join(import.meta.dirname, '../fixtures/ontology') })`.
    - `await ctx.store.open()`.
    - Call putEntity with skipOntologyCheck and a non-v7 id + a class not in the registry:
      - `const id = await ctx.store.putEntity({ id: 'not-a-uuid' as ..., name: 'TrustedBulk', entityType: 'NotInRegistry' }, { skipOntologyCheck: true });`
      - `expect(id).toBe('not-a-uuid');` — both parseEntityId AND validator are bypassed.

    PATTERNS.md landmines:
    - `import.meta.dirname` is Node 22+ (matches CI matrix per Phase 37). If the existing file uses `fileURLToPath(new URL('.', import.meta.url))` instead, mirror that idiom.
    - The `path` module may not be imported at the top of the existing file yet — verify and add `import * as path from 'node:path';` if missing.
    - For the `id: 'not-a-uuid' as ...` cast: the existing test file already uses `as unknown as ReturnType<typeof mintEntityId>` (PATTERNS.md template line 821); mirror that cast verbatim so TypeScript is happy.
    - DO NOT add a third test, even if the file pattern invites it. Plan 06 only adds these 2 tests; further coverage lives in the standalone ontology-registry.test.ts.
    - DO NOT touch existing test names (the 11 protected names per Phase 37 Plan 04 SUMMARY). Verify post-edit:
      `grep -cF "test('" /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts` should return exactly 13 (11 original + 2 new). If the actual count differs, investigate before claiming done.

    no-console-log: tests are exempt from console.* in the strict sense (vitest output is its own pipe) but maintain the convention — do not introduce `console.*`.
  </action>
  <acceptance_criteria>
    - Both new tests are present:
      - `grep -F "ontologyDir option auto-wires registry-backed validator" /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts` matches.
      - `grep -F "skipOntologyCheck bypasses registry validator (CF-D19 / BC-2)" /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts` matches.
    - Total test count is exactly 13 (11 existing + 2 new): `grep -cE "^\\s*test\\('" /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts` returns 13. If the file uses `it(` instead of `test(`, the gate must be adjusted; current Phase 37 file uses `test(`.
    - No existing test name was deleted or renamed: each of the 11 original test names from Phase 37 Plan 04 still appears verbatim. Run: `grep -F "test('strict ontology validation rejects unknown class'" /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts` matches; `grep -F "test('skipOntologyCheck flag bypasses validation'" /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts` matches.
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0.
    - `cd /Users/Q284340/Agentic/km-core && npx vitest run tests/unit/graph-store.test.ts` exits 0 with all 13 tests passing.
    - FULL suite run is GREEN (Phase 37 33 tests + Plan 06's new tests):
      `cd /Users/Q284340/Agentic/km-core && npx vitest run` exits 0; total test count goes from 33 (Phase 37 baseline) to 33 + new-ontology-registry-tests + 2 graph-store-tests. Exact count depends on Task 1 implementation but must be >= 48.
    - no-console-log: `grep -v '^\s*//\|^\s*\*' /Users/Q284340/Agentic/km-core/tests/unit/graph-store.test.ts | grep -cE "console\\.(log|warn|error|info|debug)"` returns 0 (preserved from Phase 37).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && npx vitest run && grep -qF "ontologyDir option auto-wires registry-backed validator" tests/unit/graph-store.test.ts && grep -qF "skipOntologyCheck bypasses registry validator (CF-D19 / BC-2)" tests/unit/graph-store.test.ts && test "$(grep -cE '^\\s*test\\(' tests/unit/graph-store.test.ts)" -eq 13 && echo OK</automated>
  </verify>
  <done>The graph-store test file has exactly 13 tests (11 Phase 37 protected + 2 new Phase 38); both new tests pass; the full suite is green; no existing test was renamed; no console.* introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> registry constructor | the 5 fixture files cross from disk into the registry under test |
| test code -> process.stderr | vi.spyOn mocks the stderr stream during the collision test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06-01 | Tampering | a vitest assertion accidentally passes due to a flaky test (e.g., race between fs operations + reload) | mitigate | Tests use `await` on every async op; tmpdir cleanup happens in `afterEach`; the atomic-reload test asserts the contract synchronously after `await reload()` returns. No fs.watch-based timing assertions. |
| T-38-06-02 | Tampering | one of the 11 protected Phase 37 test names is accidentally renamed or removed | mitigate | Acceptance criterion requires grep-asserts on at least 2 of the 11 protected names AND a numeric assertion `test count = 13`. Failures here block the plan from being declared done. |
| T-38-06-03 | Tampering | mock `process.stderr.write` not restored after test, leaking into subsequent tests | mitigate | The test always calls `spy.mockRestore()` after assertions. vitest also resets mocks between tests by default (Phase 37 baseline). |
| T-38-06-04 | Information Disclosure | tests log fixture paths to test output | accept | Test diagnostic value; no PII. |
</threat_model>

<verification>
- `cd /Users/Q284340/Agentic/km-core && npx vitest run` exits 0 with full suite green.
- New test file `tests/unit/ontology-registry.test.ts` exists with all 6 concern-describes.
- Existing test file `tests/unit/graph-store.test.ts` has exactly 13 tests (11 + 2).
- Phase 37's 11 protected test names are all still present (grep-verified).
- All Phase 38 success criteria are exercised by tests:
  - SC#1 (drop-in new ontology JSON): covered by the `reload() picks up newly-added ontology files` test (D-29 path).
  - SC#2 (extends chains + property merging): covered by the entire `describe('extends + property merging (ONTO-02)')` block.
  - SC#3 (B-shape fixture loads cleanly): covered by `describe('coding-ontology fixture (SC#3 — B-shape proxy)')`.
  - SC#4 (registry surface): covered by `describe('public API (D-28 + canonical refs)')` plus the GraphKMStore `ontologyDir option auto-wires...` test.
- D-27 collision warning text contract verified by stderr spy.
- D-29 atomicity contract verified by reload tests.
- Phase 37 BC-2 invariant verified by the `skipOntologyCheck bypasses registry validator` test.
</verification>

<success_criteria>
- All 4 ROADMAP Phase 38 success criteria are exercised by at least one test case.
- The collision warning text is asserted character-by-character to match D-27 spec.
- reload() add-case and remove-case both have dedicated tests.
- The synthetic coding-ontology.json fixture loads against upper.json without collisions and the 7 L1 + 5 L2 classes are all `isValidClass` true.
- Phase 37's 11 graph-store test names are preserved verbatim (grep-verified).
- Full library test suite is green at the close of Plan 06; the project is ready for Plan summary + Phase 38 verification handoff.
</success_criteria>

<output>
Create `.planning/phases/38-ontology-registry/38-06-SUMMARY.md` on completion. The SUMMARY should list the total final test count and the specific assertions for each Phase 38 success criterion.
</output>
