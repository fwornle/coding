---
phase: 38-ontology-registry
plan: 03
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - "/Users/Q284340/Agentic/km-core/src/ontology/registry.ts"
  - "/Users/Q284340/Agentic/km-core/src/ontology/index.ts"
  - "/Users/Q284340/Agentic/km-core/src/index.ts"
autonomous: true
requirements:
  - ONTO-01
  - ONTO-02
tags: [km-core, ontology, registry, reload, collision-warning, barrel]

must_haves:
  truths:
    - "OntologyRegistry class auto-discovers upper.json + all sibling .json lower-ontology files in `ontologyDir` at construction time."
    - "Lower ontology load order is alphabetical by filename (deterministic per D-27 contract)."
    - "Per-class extends chains are resolved at load-time with `{...parent, ...child}` property/relationship merging (child wins on key conflicts) — ONTO-02."
    - "When a class name appears in two ontologies, the second-loaded wins AND `process.stderr.write(...)` emits the collision warning text matching the D-27 spec: includes 'redefined', previous-source, new-source, and 'last-loaded wins'."
    - "Malformed lower-ontology files: default (non-strict) mode warns to stderr via `process.stderr.write(...)` and skips the file; strict mode (constructor option `strict: true`) re-throws."
    - "Missing upper.json throws (loader's responsibility); the registry does not swallow that error (PATTERNS.md landmine)."
    - "`reload()` is async, atomic (builds new maps fully, then swaps refs) — concurrent lookups never see half-built state per D-29."
    - "Registry exposes: isValidClass, getClass, getAllClassNames, getDefaultLayer, getValidRelationships, getLoadedDomains, parentChainOf, provenanceOf, `classCatalog: ReadonlyMap`, `domains: ReadonlySet`."
    - "Sub-barrel src/ontology/index.ts re-exports OntologyRegistry, OntologyRegistryOptions, loadOntologyFile, and the public types from src/types/ontology.ts."
    - "Root barrel src/index.ts re-exports the registry surface following the existing comment-block-per-category style (matching the noopOntologyValidator/OntologyValidator export pattern)."
    - "Zero `console.*` in any new file — repo-wide no-console-log constraint preserved (CLAUDE.md mandate carried from Phase 37)."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/ontology/registry.ts"
      provides: "OntologyRegistry class + OntologyRegistryOptions interface"
      min_lines: 100
      contains: "export class OntologyRegistry"
    - path: "/Users/Q284340/Agentic/km-core/src/ontology/index.ts"
      provides: "Sub-barrel for the ontology module — single import point for OntologyRegistry + types"
      contains: "export { OntologyRegistry }"
    - path: "/Users/Q284340/Agentic/km-core/src/index.ts"
      provides: "Root barrel updated with the ontology re-exports"
      contains: "export { OntologyRegistry }"
  key_links:
    - from: "src/ontology/registry.ts"
      to: "src/ontology/loader.ts"
      via: "loadOntologyFile import"
      pattern: "from './loader\\.js'"
    - from: "src/ontology/registry.ts"
      to: "src/types/ontology.ts"
      via: "OntologyFile + ResolvedClass type imports"
      pattern: "from '\\.\\./types/ontology\\.js'"
    - from: "src/index.ts"
      to: "src/ontology/registry.ts"
      via: "OntologyRegistry class re-export"
      pattern: "OntologyRegistry.*from.*ontology/registry\\.js"
---

<objective>
Land the `OntologyRegistry` class and wire the public surface. Wave-2 plan: depends on Plan 01 (types + loader). Adopts OKM's `OntologyRegistry` (86 lines) as the base and applies the 5 deltas from PATTERNS.md (constructor injection, async reload, stderr warnings, collision warning, provenance/parent-chain accessors).

Purpose: ONTO-01 (auto-discovery) and ONTO-02 (extends + property merging) close at the registry layer. The validator factory (Plan 04) and the GraphKMStore integration (Plan 05) consume the surface this plan exports.

Output: One new class file + one sub-barrel + a 7-line append to the root barrel. No tests in this plan — Plan 06 owns the registry unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md
@/Users/Q284340/Agentic/coding/.planning/phases/37-km-core-foundation/37-04-SUMMARY.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- OKM analog (the base implementation to lift) — read these first. -->

From /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/registry.ts (full 86-line file — read for the algorithm):
```typescript
export class OntologyRegistry {
  private classes = new Map<string, ResolvedClass>();
  private loadedDomains = new Set<string>();

  load(ontologyDir: string): void {
    // 1. Mandatory upper.json (throws if missing — preserved)
    // 2. readdirSync + filter .json + filter !== 'upper.json' + .sort() (alphabetical)
    // 3. registerClasses(file, source) — merges {parent, ...child} for relationships + properties
    // 4. silent try/catch on lower files (REPLACED by stderr warn + strict throw)
  }

  isValidClass(name: string): boolean { return this.classes.has(name); }
  getClass(name: string): ResolvedClass | undefined { return this.classes.get(name); }
  getAllClassNames(): string[] { return Array.from(this.classes.keys()); }
  getDefaultLayer(name: string): 'evidence' | 'pattern' | undefined { /* delegate to class.defaultLayer */ }
  getValidRelationships(name: string): Record<string, string[]> | undefined { /* delegate */ }
  getLoadedDomains(): string[] { return Array.from(this.loadedDomains); }
}
```

PATTERNS.md §"src/ontology/registry.ts" specifies five deltas (read the full section for exact wording — the file is loaded into context above):
1. Constructor-injected `ontologyDir` (D-28) — replace `.load(dir)` with options-object constructor
2. Add `async reload(): Promise<void>` — atomic rebuild (new maps built fully, then swapped)
3. Replace silent try/catch{} with `process.stderr.write(...)` (D-27 + no-console-log); honor `strict: true` opt-in to re-throw
4. Emit collision warning when `this.classes.set(name, ...)` would overwrite a different-source entry (D-27)
5. Expose provenance + parent-chain: `parentChainOf(name)`, `provenanceOf(name)`, `classCatalog: ReadonlyMap`, `domains: ReadonlySet`

Collision warning text spec (PATTERNS.md §Specific Ideas + S1 — VERBATIM, the test in Plan 06 grep-asserts on this):
`[km-core/ontology-registry] class '${className}' redefined: ${prev.source} → ${source} (last-loaded wins; see D-27 in 38-CONTEXT.md)\n`

Malformed-file warning text suggestion (PATTERNS.md DELTA #3):
`[km-core/ontology-registry] skipping malformed ontology file '${file}': ${msg}\n`

Existing root barrel at /Users/Q284340/Agentic/km-core/src/index.ts (current state, 47 lines):
- Line 7: `export const KM_CORE_VERSION = '0.1.0';`
- Lines 11-12: GraphKMStore + GraphKMStoreOptions export
- Lines 44-46: OntologyValidator + noopOntologyValidator export
- Plan 03 appends AFTER line 46.

Sub-barrel pattern (the only existing sub-barrel) at /Users/Q284340/Agentic/km-core/src/types/index.ts (21 lines) — mirror its shape for src/ontology/index.ts. PATTERNS.md "Cross-cutting reminders" item 1 documents that ids/ and validation/ have NO sub-barrel; CONTEXT.md explicitly names src/ontology/index.ts, so Option A (add sub-barrel) is chosen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ontology/registry.ts (OntologyRegistry class with 5 deltas vs OKM)</name>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/registry.ts (full 86 lines — adopt as base)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"src/ontology/registry.ts" (the five DELTAs + landmines, lines 94-285 of PATTERNS.md)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md §Decisions D-26..D-29 (locked behavior contracts)
    - /Users/Q284340/Agentic/km-core/src/types/ontology.ts (created in Plan 01 — for the import shape)
    - /Users/Q284340/Agentic/km-core/src/ontology/loader.ts (created in Plan 01 — for `loadOntologyFile` import)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/src/ontology/registry.ts</files>
  <action>
    Create the file. Adopt OKM's `OntologyRegistry` (86 lines) as the structural base with these 5 deltas applied (DO NOT skip any — PATTERNS.md is the spec):

    1. **Options-object constructor (D-28)**:
       - Define `export interface OntologyRegistryOptions { ontologyDir: string; strict?: boolean; }`.
       - Constructor signature: `constructor(opts: OntologyRegistryOptions)`.
       - Store `this.ontologyDir` and `this.strict = opts.strict ?? false` as private readonly fields.
       - Constructor calls a private `loadFromDisk()` synchronously. No env-var pickup, no cwd resolution (D-28 final paragraph).

    2. **Async atomic `reload()` (D-29)**:
       - Signature: `async reload(): Promise<void>`.
       - Body: build NEW `classes` Map and `loadedDomains` Set in local vars (DO NOT mutate `this.classes` / `this.loadedDomains` until the load completes). Same algorithm as `loadFromDisk()`. After all files are processed, assign: `this.classes = newClasses; this.loadedDomains = newDomains;` in two adjacent statements. This is the "atomic-before-swap" contract per D-29.
       - The async signature exists to match the Phase 37 store-API idiom (S4 pattern in PATTERNS.md); the underlying ops remain sync via readFileSync. Do not introduce false `await`s; the function can simply `return Promise.resolve()` or `return;` after the swap.

    3. **stderr warn + strict-mode rethrow on malformed lower files (D-27 + no-console-log)**:
       - Replace OKM's silent `catch {}` with: catch the error; if `this.strict` is true, rethrow; otherwise emit `process.stderr.write(\`[km-core/ontology-registry] skipping malformed ontology file '${file}': ${msg}\\n\`)` where `msg = err instanceof Error ? err.message : String(err)`.
       - Missing `upper.json` is NOT caught by this branch — `loadOntologyFile` throws inside the upper-load call, and the constructor lets that propagate (PATTERNS.md landmine: upper.json is mandatory; lower files are optional).
       - Use `process.stderr.write` ONLY — no `console.warn`, no `console.error` (no-console-log constraint, CLAUDE.md mandate, Phase 37 pattern S1).

    4. **Collision warning on overwriting (D-27)**:
       - In `registerClasses(file, source)`, BEFORE the final `this.classes.set(name, ...)` call, fetch `const prev = this.classes.get(name)` and check `if (prev && prev.source !== source)`. When true, emit:
         `process.stderr.write(\`[km-core/ontology-registry] class '${name}' redefined: ${prev.source} → ${source} (last-loaded wins; see D-27 in 38-CONTEXT.md)\\n\`)`
       - The text is VERBATIM the suggested form from CONTEXT.md §Specific Ideas and PATTERNS.md §Pattern S1. Plan 06 grep-asserts on substrings "redefined", "last-loaded wins". DO NOT paraphrase.

    5. **Provenance + parent-chain accessors + readonly views**:
       - `parentChainOf(className: string): ResolvedClass[]` — walks `cur.extends` upward, pushing each parent into a `chain` array (closest first), stops on missing parent or no extends.
       - `provenanceOf(className: string): string | undefined` — returns `this.classes.get(className)?.source`.
       - `get classCatalog(): ReadonlyMap<string, ResolvedClass>` — getter returning the internal map cast as ReadonlyMap (no defensive copy needed — TypeScript type widening only).
       - `get domains(): ReadonlySet<string>` — same pattern.

    **Preserve verbatim from OKM**:
    - `private registerClasses(file: OntologyFile, source: string): void` body for the `extends` chain resolution (lines 35-52 of analog) — `merged = { ...classDef, relationships: { ...parent.relationships, ...classDef.relationships }, properties: { ...parent.properties, ...classDef.properties } }`. This is the ONTO-02 implementation; do not refactor.
    - `isValidClass`, `getClass`, `getAllClassNames`, `getDefaultLayer`, `getValidRelationships`, `getLoadedDomains` — all 6 keep their OKM signatures and bodies.
    - `getClassesForPrompt()` if present in OKM analog (PATTERNS.md notes "keep verbatim; LLM-context formatter used by Phase 40/42") — adopt as-is.

    **Imports** (NodeNext .js extensions per CF-D06):
    - `import { join } from 'node:path';`
    - `import { readdirSync } from 'node:fs';`
    - `import { loadOntologyFile } from './loader.js';`
    - `import type { OntologyFile, ResolvedClass } from '../types/ontology.js';`

    **Comment block at top of file**: short paragraph naming Phase 38 ONTO-01/02, D-26..D-29 lineage, and the OKM analog path. No fenced code blocks in the file beyond the actual implementation.
  </action>
  <acceptance_criteria>
    - File exists at /Users/Q284340/Agentic/km-core/src/ontology/registry.ts.
    - `grep -c "^export class OntologyRegistry" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` returns 1.
    - `grep -c "^export interface OntologyRegistryOptions" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` returns 1.
    - `grep -E "async reload\\(\\): Promise<void>" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` matches.
    - `grep -E "process\\.stderr\\.write\\(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts | wc -l` returns at least 2 (one for malformed-file, one for collision).
    - Collision warning text contract: `grep -F "last-loaded wins; see D-27 in 38-CONTEXT.md" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` matches at least once.
    - Collision warning includes "redefined": `grep -F "redefined" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` matches.
    - Malformed-file warning prefix present: `grep -F "skipping malformed ontology file" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` matches.
    - No-console-log preserved (count non-comment lines only): `grep -v '^\s*//\|^\s*\*' /Users/Q284340/Agentic/km-core/src/ontology/registry.ts | grep -E "console\\.(log|warn|error|info|debug)" | wc -l` returns 0.
    - Required public methods present (each `grep -F` match must be at least 1):
      - `grep -F "isValidClass(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts`
      - `grep -F "getClass(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts`
      - `grep -F "getAllClassNames(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts`
      - `grep -F "parentChainOf(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts`
      - `grep -F "provenanceOf(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts`
      - `grep -F "get classCatalog(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts`
      - `grep -F "get domains(" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts`
    - Alphabetical sort preserved: `grep -E "files\\.sort\\(\\)|\\.sort\\(\\)" /Users/Q284340/Agentic/km-core/src/ontology/registry.ts` matches (D-27 contract).
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0.
    - File size sanity: at least 100 lines (OKM analog is 86; deltas add ~30): `wc -l < /Users/Q284340/Agentic/km-core/src/ontology/registry.ts | xargs test 100 -le`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && grep -qF "last-loaded wins; see D-27 in 38-CONTEXT.md" src/ontology/registry.ts && grep -qE "async reload\\(\\): Promise<void>" src/ontology/registry.ts && test "$(grep -v '^\\s*//\\|^\\s*\\*' src/ontology/registry.ts | grep -cE 'console\\.(log|warn|error|info|debug)')" -eq 0 && echo OK</automated>
  </verify>
  <done>OntologyRegistry compiles clean; all 5 PATTERNS deltas applied; collision warning text matches CONTEXT.md §Specific Ideas verbatim; no console.* in source; OKM's 6 public lookup methods preserved verbatim; reload() is atomic (assignment-after-load contract).</done>
</task>

<task type="auto">
  <name>Task 2: Create src/ontology/index.ts sub-barrel and update src/index.ts root barrel</name>
  <read_first>
    - /Users/Q284340/Agentic/km-core/src/types/index.ts (existing sub-barrel — only precedent on disk; mirror its shape)
    - /Users/Q284340/Agentic/km-core/src/index.ts (existing root barrel — 47 lines; append AFTER line 46)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"src/ontology/index.ts" and §"src/index.ts" (the exact patterns to mirror; Option A for sub-barrel)
  </read_first>
  <files>
    /Users/Q284340/Agentic/km-core/src/ontology/index.ts,
    /Users/Q284340/Agentic/km-core/src/index.ts
  </files>
  <action>
    **Create src/ontology/index.ts** (the sub-barrel — Option A from PATTERNS.md):
    - Header comment: short paragraph noting this is the Phase 38 ontology sub-barrel; mention that consumers needing the full registry surface can import from `@fwornle/km-core/ontology`.
    - Exports (use `export {...}` for runtime values, `export type {...}` for types — mirrors the existing /src/types/index.ts split):
      - `export { OntologyRegistry } from './registry.js';`
      - `export type { OntologyRegistryOptions } from './registry.js';`
      - `export { loadOntologyFile } from './loader.js';`
      - `export type { OntologyFile, OntologyClass, OntologyProperty, ResolvedClass } from '../types/ontology.js';`

    **Modify src/index.ts** — append AFTER line 46 (the existing `noopOntologyValidator` export). Match the existing comment-block-per-category style (PATTERNS.md landmine: "do NOT collapse into one giant block").
    - Add a comment header naming Phase 38 + ONTO-01/02 + a brief one-liner about auto-discovery + extends-merging.
    - Add the runtime export: `export { OntologyRegistry } from './ontology/registry.js';`
    - Add the type-only export: `export type { OntologyRegistryOptions } from './ontology/registry.js';`
    - Add: `export { loadOntologyFile } from './ontology/loader.js';`
    - Add the public types from src/types/ontology.ts as type-only exports: `OntologyFile`, `OntologyClass`, `OntologyProperty`, `ResolvedClass`.
    - DO NOT add `registryBackedValidator` here — Plan 04 owns that export (it lives in src/validation/ontology.ts, not in src/ontology/).
    - DO NOT remove or reorder any existing exports — PATTERNS.md landmine: the root barrel is the public package surface; existing consumers must not break.

    NodeNext .js extensions on every internal import path (CF-D06).
    no-console-log: not applicable (barrel files have no diagnostic output).
  </action>
  <acceptance_criteria>
    - /Users/Q284340/Agentic/km-core/src/ontology/index.ts exists.
    - `grep -c "OntologyRegistry" /Users/Q284340/Agentic/km-core/src/ontology/index.ts` returns at least 2 (runtime + type re-exports).
    - `grep -c "loadOntologyFile" /Users/Q284340/Agentic/km-core/src/ontology/index.ts` returns at least 1.
    - /Users/Q284340/Agentic/km-core/src/index.ts now contains: `grep -c "OntologyRegistry" /Users/Q284340/Agentic/km-core/src/index.ts` >= 2 (runtime + type).
    - The existing line `export { noopOntologyValidator }` in src/index.ts is still present: `grep -F "export { noopOntologyValidator }" /Users/Q284340/Agentic/km-core/src/index.ts` matches (no accidental deletion).
    - The existing `export { GraphKMStore }` still present: `grep -F "export { GraphKMStore }" /Users/Q284340/Agentic/km-core/src/index.ts` matches.
    - The new types are exported from the root: `grep -F "OntologyFile" /Users/Q284340/Agentic/km-core/src/index.ts` matches; same for `OntologyClass`, `OntologyProperty`, `ResolvedClass`.
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0.
    - `cd /Users/Q284340/Agentic/km-core && npm run build` exits 0; `dist/ontology/registry.js` exists; `dist/ontology/index.js` exists.
    - External-consumer smoke compile (mirror Phase 37 Plan 04 SUMMARY pattern): a tmp consumer doing `import { OntologyRegistry, type OntologyRegistryOptions } from '@fwornle/km-core'` plus `import { OntologyRegistry } from '@fwornle/km-core/ontology'` (sub-path resolution if exports map allows it; otherwise only the root import is asserted) compiles `tsc --noEmit` clean. If the package.json `exports` field does not include a sub-path for `./ontology`, the sub-path import is allowed to fail and the executor must EITHER (a) extend the exports map in a separate commit before declaring done, OR (b) document the limitation in SUMMARY and rely on the root import only. Decision rule: prefer (a) when adding a single entry; defer to (b) only if the exports map structure forces a wider change beyond Plan 03's scope.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && npm run build && test -f dist/ontology/registry.js && test -f dist/ontology/index.js && grep -qF "export { OntologyRegistry }" src/index.ts && grep -qF "export { GraphKMStore }" src/index.ts && echo OK</automated>
  </verify>
  <done>Both barrels compile and build clean; root barrel re-exports OntologyRegistry, OntologyRegistryOptions, loadOntologyFile, and the 4 public ontology types; the existing GraphKMStore + noopOntologyValidator exports are intact; sub-barrel src/ontology/index.ts mirrors the src/types/index.ts shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> registry | upper.json + lower ontology JSON files cross into the registry via loader.ts |
| consumer code -> registry | external callers invoke isValidClass / getClass / reload() (no auth boundary, but type-safety boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Tampering | malformed lower-ontology JSON file in ontologyDir | mitigate | Default: catch the loader's error + emit stderr warn + skip — D-29 atomic-build (one bad file doesn't block the rest). Opt-in strict mode (`new OntologyRegistry({ ontologyDir, strict: true })`) re-throws for production deployments where partial loads are unacceptable. |
| T-38-03-02 | Tampering | colliding class names across two lower ontologies — second silently overwrites the first | mitigate | D-27: emit stderr warning naming both source domains and the redefined class name; "last-loaded wins" is the documented contract. Operators inspecting stderr logs see the redefinition immediately. |
| T-38-03-03 | Tampering | concurrent reload() during a findByOntologyClass() call sees half-built state | mitigate | D-29: reload() builds new Map + Set in local vars, then assigns to `this.classes` and `this.loadedDomains` in two adjacent statements. JavaScript single-threaded execution model + adjacent-assignment idiom prevents any observable intermediate state. |
| T-38-03-04 | Information Disclosure | stderr warning leaks filesystem paths | accept | Operator-diagnostic value > info-leak risk. Same reasoning as T-38-01-03 (Plan 01); km-core is library code. |
| T-38-03-05 | Denial of Service | very large ontologyDir (1000+ .json files) blocks the constructor for seconds | accept | Operator-curated; in practice 3-5 files. Library does not impose a count limit in v0.1. |
| T-38-03-06 | Tampering | reload() called concurrently from two paths racing on each other's swap | accept | v0.1 is single-instance per consumer process; the two-statement swap is itself the synchronization point. Multi-process synchronization is out of scope (REQUIREMENTS Out-of-Scope bullet on multi-instance). |
</threat_model>

<verification>
- `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0 after both tasks.
- `cd /Users/Q284340/Agentic/km-core && npm run build` exits 0; `dist/ontology/{registry,loader,index}.js` all present.
- `cd /Users/Q284340/Agentic/km-core && npx vitest run` exits 0 (the existing 33 tests from Phase 37 still pass; Plan 03 should not introduce regressions in graph-store / round-trip / exporter / persistence tests). Plan 06 owns the NEW ontology-registry tests.
- No-console-log preserved across all 3 files modified.
</verification>

<success_criteria>
- The OntologyRegistry class is fully landed and exported from the root barrel.
- The 5 PATTERNS.md deltas vs OKM are all applied (constructor injection, async atomic reload, stderr warn + strict mode, collision warning text-verbatim, provenance/parent-chain accessors + readonly views).
- The package is importable from external consumers via `import { OntologyRegistry } from '@fwornle/km-core'`.
- The sub-barrel `src/ontology/index.ts` exists for consumers who want the focused surface (`import { OntologyRegistry } from '@fwornle/km-core/ontology'`).
- ONTO-01 (auto-discovery) closes at this layer: dropping a new .json into ontologyDir + calling reload() makes its classes available without code change.
- ONTO-02 (extends + property merging) closes at this layer: the `registerClasses` body's `{...parent.relationships, ...child.relationships}` + `{...parent.properties, ...child.properties}` merging is preserved from OKM verbatim.
</success_criteria>

<output>
Create `.planning/phases/38-ontology-registry/38-03-SUMMARY.md` on completion.
</output>
