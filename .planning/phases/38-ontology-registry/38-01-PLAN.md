---
phase: 38-ontology-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "/Users/Q284340/Agentic/km-core/src/types/ontology.ts"
  - "/Users/Q284340/Agentic/km-core/src/ontology/loader.ts"
autonomous: true
requirements:
  - ONTO-01
  - ONTO-02
tags: [km-core, ontology, types, loader, foundations]

must_haves:
  truths:
    - "src/types/ontology.ts declares OntologyFile, OntologyClass, OntologyProperty, ResolvedClass with the exact shape from OKM analog (verbatim)."
    - "loadOntologyFile(path) reads a JSON file, validates meta+classes presence, and throws on malformed input."
    - "Layer literal in OntologyClass.defaultLayer is sourced from src/types/entity.ts (single source of truth for 'evidence' | 'pattern')."
  artifacts:
    - path: "/Users/Q284340/Agentic/km-core/src/types/ontology.ts"
      provides: "OntologyFile, OntologyClass, OntologyProperty, ResolvedClass type declarations"
      contains: "export interface OntologyFile"
    - path: "/Users/Q284340/Agentic/km-core/src/ontology/loader.ts"
      provides: "loadOntologyFile(path) sync JSON reader with shape validation"
      contains: "export function loadOntologyFile"
  key_links:
    - from: "src/ontology/loader.ts"
      to: "src/types/ontology.ts"
      via: "import type { OntologyFile } from '../types/ontology.js'"
      pattern: "from '\\.\\./types/ontology\\.js'"
---

<objective>
Land the type-only foundation for Phase 38: the OntologyFile/OntologyClass/OntologyProperty/ResolvedClass shape (mirrors OKM's analog verbatim) and the sync JSON file reader. Wave-1 plan: zero dependencies, lays the contracts that the registry (Plan 03) and validator factory (Plan 04) will import.

Purpose: Lifts the type surface OKM already proved (29 lines, 3 interfaces + 1 derived ResolvedClass) into km-core unchanged. No registry logic, no auto-discovery, no extends-merging — just shapes + file I/O.

Output: Two new files under src/types/ and src/ontology/. No barrel changes (Plan 03 owns barrel update so the surface lands atomically with the registry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Q284340/Agentic/coding/.planning/PROJECT.md
@/Users/Q284340/Agentic/coding/.planning/ROADMAP.md
@/Users/Q284340/Agentic/coding/.planning/STATE.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-CONTEXT.md
@/Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md
@/Users/Q284340/Agentic/coding/CLAUDE.md

<interfaces>
<!-- Existing km-core type that OntologyClass.defaultLayer must align with -->

From /Users/Q284340/Agentic/km-core/src/types/entity.ts (Phase 37 Plan 02):
```typescript
export type Layer = 'evidence' | 'pattern';
```

OKM analog to lift verbatim (only path/extension differs):
- /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/types/ontology.ts (29 lines)
- /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/loader.ts (13 lines)

Note on `defaultLayer`: PATTERNS.md §"src/types/ontology.ts" landmines recommends importing Layer rather than inlining the literal — preserves single source of truth. Use:
`defaultLayer?: Layer;` with `import type { Layer } from './entity.js';`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/types/ontology.ts (verbatim OKM shape + Layer import)</name>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/types/ontology.ts (29 lines — adopt verbatim)
    - /Users/Q284340/Agentic/km-core/src/types/entity.ts (existing — confirm `Layer` export name & path)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"src/types/ontology.ts" (DELTAS = none; landmines about defaultLayer)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/src/types/ontology.ts</files>
  <action>
    Create the file with these four exported interfaces (adopt OKM analog verbatim, with one delta: import `Layer` from './entity.js' instead of inlining the literal union):
    - `OntologyProperty` (fields: type, required?, enum?, format?)
    - `OntologyClass` (fields: extends?, description, relationships: Record&lt;string, string[]&gt;, properties?, defaultLayer?: Layer)
    - `OntologyFile` (fields: meta: { name, version, extends?, description }, classes: Record&lt;string, OntologyClass&gt;)
    - `ResolvedClass extends OntologyClass` (adds: name: string, source: string)
    Add a top-of-file comment block referencing OKM analog + Phase 38 D-26/D-27 lineage (one short paragraph; no fenced code blocks).
    Two `extends?: string` fields exist deliberately (one on OntologyFile.meta = ontology-level inheritance e.g. `kpifw extends upper`; one on OntologyClass = per-class chain e.g. `KPIPipeline extends Pipeline`). Preserve both per PATTERNS.md landmines.
    NodeNext extension on import: `from './entity.js'` (CF-D06; verified by Phase 37).
    no-console-log applies repo-wide — there is no diagnostic output in this file.
  </action>
  <acceptance_criteria>
    - File exists at /Users/Q284340/Agentic/km-core/src/types/ontology.ts.
    - `grep -c "^export interface " /Users/Q284340/Agentic/km-core/src/types/ontology.ts` returns 4.
    - `grep -E "import type \{ Layer \} from '\\./entity\\.js'" /Users/Q284340/Agentic/km-core/src/types/ontology.ts` matches at least once.
    - `grep -v "^//" /Users/Q284340/Agentic/km-core/src/types/ontology.ts | grep -c "defaultLayer\\?:"` returns 1 (single declaration of optional defaultLayer in OntologyClass).
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0 (the new type file integrates without breaking anything).
    - `grep -E "console\\.(log|warn|error|info|debug)" /Users/Q284340/Agentic/km-core/src/types/ontology.ts` returns no matches (no-console-log constraint).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && test "$(grep -c '^export interface ' src/types/ontology.ts)" = "4" && echo OK</automated>
  </verify>
  <done>The four interfaces compile clean, Layer is imported from entity.js, and there is no `console.*` in the file. PATTERNS.md §"src/types/ontology.ts" "DELTAS: NONE" honored.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/ontology/loader.ts (sync JSON reader, throws on malformed input)</name>
  <read_first>
    - /Users/Q284340/Agentic/_work/rapid-automations/integrations/operational-knowledge-management/src/ontology/loader.ts (13 lines — adopt verbatim with .js extension on import path)
    - /Users/Q284340/Agentic/coding/.planning/phases/38-ontology-registry/38-PATTERNS.md §"src/ontology/loader.ts" (landmines: sync read intentional; throw — don't swallow)
    - /Users/Q284340/Agentic/km-core/src/types/ontology.ts (created in Task 1 — Task 2 imports from it)
  </read_first>
  <files>/Users/Q284340/Agentic/km-core/src/ontology/loader.ts</files>
  <action>
    Create the file. The directory `src/ontology/` does not yet exist on disk (verified by PATTERNS.md §"Verified on disk"); create the directory by writing the file (the Write tool creates parent dirs).
    Export one function: `loadOntologyFile(path: string): OntologyFile`.
    Implementation: `readFileSync(path, 'utf8')` -> `JSON.parse` -> validate `parsed.meta && parsed.meta.name && parsed.classes` -> throw `Error(\`Invalid ontology file at ${path}: missing meta or classes\`)` on validation failure -> return the parsed value cast to `OntologyFile`.
    Import shape (NodeNext + ESM with .js extension per CF-D06):
    - `import { readFileSync } from 'node:fs';`
    - `import type { OntologyFile } from '../types/ontology.js';`
    Loader is intentionally synchronous (PATTERNS.md §Pattern S4 "Sync registry + async store API" — the calling `store.open()` is async; only the file reads are sync). Do NOT add `async`/`await`.
    Loader THROWS on shape error; the registry (Plan 03) catches and warns or rethrows depending on strict-mode (D-29). Loader has no knowledge of strict-mode.
    no-console-log: this file does not emit diagnostics.
  </action>
  <acceptance_criteria>
    - File exists at /Users/Q284340/Agentic/km-core/src/ontology/loader.ts.
    - Directory /Users/Q284340/Agentic/km-core/src/ontology/ now exists (created by the file write).
    - `grep -E "^export function loadOntologyFile\\(path: string\\): OntologyFile" /Users/Q284340/Agentic/km-core/src/ontology/loader.ts` matches exactly once.
    - `grep -E "readFileSync\\(path, 'utf8'\\)" /Users/Q284340/Agentic/km-core/src/ontology/loader.ts` matches.
    - `grep -E "throw new Error\\(`Invalid ontology file at " /Users/Q284340/Agentic/km-core/src/ontology/loader.ts` matches.
    - `grep -E "from '\\.\\./types/ontology\\.js'" /Users/Q284340/Agentic/km-core/src/ontology/loader.ts` matches (NodeNext .js extension).
    - `grep -E "console\\.(log|warn|error|info|debug)" /Users/Q284340/Agentic/km-core/src/ontology/loader.ts` returns no matches.
    - `cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Q284340/Agentic/km-core && npx tsc --noEmit && test -f src/ontology/loader.ts && grep -qE "from '\\.\\./types/ontology\\.js'" src/ontology/loader.ts && echo OK</automated>
  </verify>
  <done>loader.ts exists, compiles, throws on malformed input, imports OntologyFile via the NodeNext .js path, and uses synchronous readFileSync (per PATTERNS.md landmine about intentional sync read).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> loader | untrusted JSON content crosses here via `readFileSync` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | loader.ts JSON parse | mitigate | `JSON.parse` throws SyntaxError on malformed input; shape-validation throws on missing meta/classes — registry layer (Plan 03) catches and either rethrows (strict mode) or warns and skips (default). No silent acceptance. |
| T-38-01-02 | Denial of Service | loader.ts readFileSync of arbitrarily large file | accept | Ontology files are operator-curated, not user-uploaded. OKM has run this pattern in production with files up to ~10KB. v0.1 does not impose a size limit. |
| T-38-01-03 | Information Disclosure | loader.ts error message includes filesystem path | accept | Path is in error message by design (operator diagnostic). km-core is library code; consumers control where errors surface. |
</threat_model>

<verification>
- Both files compile (`npx tsc --noEmit` clean) inside km-core repo.
- Zero `console.*` anywhere in /Users/Q284340/Agentic/km-core/src/types/ontology.ts and /Users/Q284340/Agentic/km-core/src/ontology/loader.ts (no-console-log enforced).
- `loadOntologyFile` is a named export; `OntologyFile`, `OntologyClass`, `OntologyProperty`, `ResolvedClass` are all named exports from the types file.
- No root-barrel changes (Plan 03 owns the atomic barrel update so the surface lands together with the registry).
</verification>

<success_criteria>
- src/types/ontology.ts exists with exactly 4 exported interfaces (OntologyFile, OntologyClass, OntologyProperty, ResolvedClass).
- src/ontology/loader.ts exists, exports `loadOntologyFile`, is synchronous, throws on malformed input.
- Layer literal is imported from src/types/entity.js (not inlined) — verifies the "single source of truth" landmine from PATTERNS.md.
- Both contribute to ONTO-01 (ResolvedClass type used by registry for class catalog) and ONTO-02 (OntologyClass.extends? field is the parent-chain wire).
</success_criteria>

<output>
Create `.planning/phases/38-ontology-registry/38-01-SUMMARY.md` on completion.
</output>
