---
phase: 51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as
plan: 15
type: execute
wave: 8
depends_on: ["51-12", "51-13", "51-14"]
files_modified: []
autonomous: true
requirements: []
gap_closure: true
gaps_addressed:
  - "AC #2 (CONTEXT.md) — fresh wave-execution produces sub-agent LSL files under .specstory/history/{YYYY}/{MM}/ matching D-LSL-Filename"
tags: [phase-51, gap-closure, ac-2, lsl-production-backfill]

must_haves:
  truths:
    - "Production `.specstory/history/` contains > 0 files matching the S-pattern after this plan's writer-CLI run completes"
    - "The 2026-05-23 parent sessions referenced in CONTEXT.md / VERIFICATION.md (UUIDs 5d22e2d5 AND 31274d29) are BOTH represented by at least one D-LSL-Filename file each under `.specstory/history/2026/05/` (verification uses AND-coupled greps, not OR alternation)"
    - "The writer CLI invocation is logged in 51-15-SUMMARY.md with the exact command + run timestamp so the production backfill is auditable"
  artifacts:
    - path: .specstory/history/2026/05/
      provides: "Production LSL files for the 2026-05-23 sub-agents (the actual files this plan produces)"
      contains: "_S"
  key_links:
    - from: "scripts/write-sub-agent-lsl.mjs (after invocation)"
      to: ".specstory/history/{YYYY}/{MM}/*_S{slot}-{idx}-{hash}.md"
      via: "lib/lsl/sub-agent-lsl-writer.mjs::writeSubAgentLSL + DEFAULT_OUTPUT_ROOT = '.specstory/history'"
      pattern: "\\.specstory/history/2026/05/.*_S[0-9]+-[0-9]+-[a-f0-9]+\\.md"
---

<objective>
Close AC #2 (the CONTEXT.md acceptance criterion that says "fresh wave-execution produces sub-agent LSL files under `.specstory/history/{YYYY}/{MM}/` matching D-LSL-Filename") by EXECUTING the writer CLI against the production output path with the production agent + project.

Per VERIFICATION.md gaps block (line 11-17), the Plan 51-06 SUMMARY's "624 LSL files written" claim refers to a sandbox smoke test under `/private/var/folders/.../tmp.JxUhsiW7Dc/history/` — NOT the production target. The writer code is correct AND the smoke test proves it works, but no production backfill or live-tier capture has populated the production LSL directory. `find .specstory/history -name '*_S*-*-*.md'` returns 0 files.

This is **NOT a code change** — it is a **production data backfill execution**. This plan is a single-task, pure-execution plan. The single load-bearing action is:

```bash
node scripts/write-sub-agent-lsl.mjs --agent claude --project coding --historical --limit 1000
```

The writer's `DEFAULT_OUTPUT_ROOT` (line 63 of write-sub-agent-lsl.mjs) is already `.specstory/history` — so no `--output-root` flag is needed and the default IS the production path.

**Scope note (revised 2026-05-27):** WR-05 (the `--historical` flag dead-code issue) is EXPLICITLY OUT OF SCOPE for Phase 51 gap closure per the locked scope statement: "Out of scope (intentional): WR-01..WR-09 warnings (9 items)". Both branches of the `--historical` ternary in `scripts/write-sub-agent-lsl.mjs:187` currently return `null` (`historical ? null : null`) — the flag is a no-op, so the production command works identically whether `--historical` is passed or not. The dead-code cleanup will be addressed in a future deferred-warnings cleanup phase, NOT here.

This plan depends on 51-12 (CR-04 fix) and 51-13 + 51-14 only for the OBSERVATION side of AC #3 in Plan 51-16. The LSL backfill in this plan does NOT require launchd to be working — the writer CLI runs synchronously in the operator's shell and writes files directly. The depends_on is conservative: we want the 4 critical fixes landed first so the production backfill happens in a clean codebase, not against half-fixed adapters.

Purpose: Convert AC #2 from FAIL (0 files) to PASS (≥1 file per discovered sub-agent, including the 2026-05-23 incident parents).
Output: Production LSL files under `.specstory/history/2026/05/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-CONTEXT.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-VERIFICATION.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-REVIEW.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-06-SUMMARY.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-12-PLAN.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-13-PLAN.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-14-PLAN.md
@scripts/write-sub-agent-lsl.mjs
@lib/lsl/sub-agent-lsl-writer.mjs
@lib/lsl/sub-agent-slot-allocator.mjs
@lib/lsl/adapters/claude-jsonl-tree.mjs
@CLAUDE.md

<interfaces>
<!-- Writer CLI default contracts (scripts/write-sub-agent-lsl.mjs:60-66) -->

```js
const DEFAULT_LIMIT = 100;
const DEFAULT_OUTPUT_ROOT = path.join('.specstory', 'history');   // ← production default
// DEFAULT_STATE_PATH comes from lib/lsl/sub-agent-slot-allocator.mjs
```

<!-- WR-05 dead-code state (scripts/write-sub-agent-lsl.mjs:155, 187) — OUT OF SCOPE for this plan -->

```js
const historical = hasFlag(argv, '--historical');
// ...
const sinceArg = historical ? null : null;   // ← both branches return null; the flag is a no-op
```

The `--historical` flag is preserved in the production command for operator clarity (signals intent to scan all historical data), even though the flag's branches currently return identical `null` values. This is a documented WR-05 deferred issue.

<!-- The 2026-05-23 parent UUIDs from CONTEXT.md / VERIFICATION.md -->

UUID 1: 5d22e2d5 (truncated form — sub_hash; full UUID lives in claude-jsonl-tree adapter discovery)
UUID 2: 31274d29 (truncated form — sub_hash)

BOTH UUIDs must be represented in the output (per VERIFICATION.md `missing:` line 17 and CONTEXT.md AC #1 stipulation that both UUIDs are confirmed Phase 42 wave parents). Verification uses AND-coupled greps — both must succeed.

<!-- Discovery path layout (already proven by Plan 51-06 sandbox smoke test which produced 624 files) -->

The writer iterates AGENTS (claude/opencode/copilot/mastra), but per planning-context locked decision the production run targets `--agent claude --project coding --limit 1000`. Other agents are out of scope for THIS plan (deferred per CONTEXT.md / locked-scope-2026-05-27 line in planning context).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute production LSL backfill against .specstory/history (resolves AC #2)</name>
  <files>scripts/write-sub-agent-lsl.mjs (read-only; the CLI is invoked, not modified)</files>
  <read_first>
    - scripts/write-sub-agent-lsl.mjs (confirm DEFAULT_OUTPUT_ROOT = '.specstory/history' at line 63; confirm --historical flag is accepted by parseArgs)
    - .planning/phases/51-…/51-VERIFICATION.md frontmatter `gaps:` block AC #2 entry (lines 7-17)
    - .planning/phases/51-…/51-CONTEXT.md § acceptance criteria (AC #2 specifically)
    - .planning/phases/51-…/51-06-SUMMARY.md (Plan 51-06 — the writer's sandbox smoke test that produced 624 files; confirms the writer works against fixtures)
  </read_first>
  <action>
    This task is a **CLI invocation against the production output path**, NOT a code change. The single command:

    ```bash
    node scripts/write-sub-agent-lsl.mjs --agent claude --project coding --historical --limit 1000
    ```

    Execution steps the executor MUST perform:

    1. **Baseline measurement** — capture the current state:
       ```bash
       find .specstory/history -name '*_S[0-9]*-*-*.md' | wc -l
       ```
       Record the result (expected: 0 per VERIFICATION.md).

    2. **Run the writer**:
       ```bash
       cd /Users/Q284340/Agentic/coding
       node scripts/write-sub-agent-lsl.mjs --agent claude --project coding --historical --limit 1000 2> /tmp/51-15-writer.log
       ```
       Capture stderr to `/tmp/51-15-writer.log` for the audit trail.

    3. **Post-run verification** — count produced files + verify BOTH 2026-05-23 parent UUIDs are represented (AND, not OR):
       ```bash
       find .specstory/history -name '*_S[0-9]*-*-*.md' | wc -l
       # Expected: > 0
       find .specstory/history/2026/05 -name '*_S*-*-*.md' | head -20
       # Expected: at least a handful of files

       # Verify BOTH 2026-05-23 parent sessions are represented (UUIDs 5d22e2d5 AND 31274d29) — AND, not OR:
       COUNT_5D22=$(grep -l '5d22e2d5' .specstory/history/2026/05/*_S*-*.md 2>/dev/null | wc -l)
       COUNT_3127=$(grep -l '31274d29' .specstory/history/2026/05/*_S*-*.md 2>/dev/null | wc -l)
       echo "5d22e2d5: $COUNT_5D22  31274d29: $COUNT_3127"
       # Expected: BOTH counts >= 1 (both parents must be represented; AND condition)
       ```

       Note: the parent UUID may appear either in the filename's hash component (sub_hash truncation of parent OR sub's own UUID — verify against Plan 51-06 SUMMARY's naming convention) OR in the frontmatter's `parent_session_id` field. Use `grep -l` against file CONTENT, not just filenames.

    4. **Inspect one produced file** to verify shape matches D-LSL-Filename + has full frontmatter:
       ```bash
       head -30 "$(find .specstory/history/2026/05 -name '*_S*-*-*.md' | head -1)"
       ```
       Expected frontmatter fields: agent, parent_session_id, sub_index, sub_hash, project, sub_session_id, lsl_incomplete, captured_via, captured_at (per Plan 51-06 writer contract).

    5. **Commit the produced files** to git:
       ```bash
       git add .specstory/history/2026/05/
       git commit -m "data(51-15): production LSL backfill — $(date -u +%Y-%m-%dT%H:%MZ) (closes AC #2)"
       ```
       The commit should include ALL produced LSL files. NOTE: `.specstory/history/` may be tracked as a submodule or be in `.gitignore` — check `cat .gitignore | grep specstory` and `git submodule status | grep specstory` BEFORE committing. If `.specstory/history` is a submodule, do the commit inside the submodule + add a pointer-bump commit on the outer repo. If it's gitignored, document the produced file count + paths in SUMMARY but DO NOT bypass .gitignore — the LSL files exist on disk regardless.

    Failure modes the executor should report (and document in SUMMARY):
    - **No claude transcripts discoverable**: writer exits 2 with `[lsl-writer] agent=claude discover error: ...`. Verify `~/.claude/projects/-Users-Q284340-Agentic-coding/` has JSONLs (check for the 2026-05-23 incident files specifically). If empty, the operator's claude transcript directory has been pruned and AC #2 cannot be retroactively satisfied for the 2026-05-23 parents — escalate.
    - **Writer produces 0 files**: log + diagnose. Likely cause is no sub-agent sidechains in the discovered transcripts (parent sessions but no spawned agents). Per Plan 51-02 SUMMARY, the claude adapter filters `isSidechain` records — verify the discovered transcripts contain `isSidechain: true` records.
    - **Slot allocator collision**: writer might fail mid-run if `.data/sub-agent-slot-state.json` is corrupted. `cat .data/sub-agent-slot-state.json` to inspect; reset to `{}` if corrupted (document in SUMMARY).
    - **Only ONE of the two parent UUIDs is represented** (the AND check fails): the writer ran successfully but is missing one parent. Likely cause: the missing parent's transcript JSONL is no longer on disk OR was filtered out by the isSidechain check. Investigate which of 5d22e2d5 / 31274d29 is missing and check `~/.claude/projects/-Users-Q284340-Agentic-coding/` for the corresponding parent UUID. Document in SUMMARY and escalate if unrecoverable.

    Per CLAUDE.md submodule rules: `scripts/write-sub-agent-lsl.mjs` runs HOST-SIDE — no Docker rebuild needed.

    Threat model:
    - T-51-15-PROD (production-data backfill): mitigate by capturing exact CLI invocation + log file in SUMMARY for auditability.
    - T-51-15-FS (file-system write — writer creates many files under .specstory/history): accept — Plan 51-06 SUMMARY confirms the writer's per-file atomic-write semantics + slot-allocator collision avoidance.
    - T-51-15-IDEM (idempotency — re-running the writer should not create duplicates): mitigate via slot-allocator state file (`.data/sub-agent-slot-state.json`) which Plan 51-06 contract guarantees prevents slot reuse.
  </action>
  <verify>
    <automated>find .specstory/history -name '*_S[0-9]*-*-*.md' | wc -l | awk '{ if ($1 > 0) exit 0; else exit 1 }' &amp;&amp; [ $(grep -l '5d22e2d5' .specstory/history/2026/05/*_S*-*.md 2>/dev/null | wc -l) -ge 1 ] &amp;&amp; [ $(grep -l '31274d29' .specstory/history/2026/05/*_S*-*.md 2>/dev/null | wc -l) -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `find .specstory/history -name '*_S[0-9]*-*-*.md' | wc -l` returns a value > 0 (was 0 per VERIFICATION.md line 13)
    - `[ $(grep -l '5d22e2d5' .specstory/history/2026/05/*_S*-*.md 2>/dev/null | wc -l) -ge 1 ]` is TRUE (UUID 5d22e2d5 represented)
    - `[ $(grep -l '31274d29' .specstory/history/2026/05/*_S*-*.md 2>/dev/null | wc -l) -ge 1 ]` is TRUE (UUID 31274d29 represented)
    - BOTH greps above succeed (AND condition — not OR alternation)
    - At least one produced file's frontmatter contains all 9 D-LSL-Filename fields (agent, parent_session_id, sub_index, sub_hash, project, sub_session_id, lsl_incomplete, captured_via, captured_at)
    - The writer's stderr log is captured to /tmp/51-15-writer.log and the SUMMARY references the exact CLI command + the run-completion timestamp
    - Commit: `data(51-15): production LSL backfill ...`
    - If `.specstory/history` is gitignored or submoduled, the SUMMARY documents this clearly + the count of produced files
  </acceptance_criteria>
  <done>
    Production LSL files exist under `.specstory/history/2026/05/`; BOTH 2026-05-23 parent UUIDs (5d22e2d5 AND 31274d29) are represented per AND-coupled grep verification; SUMMARY captures the exact CLI invocation + run timestamp + produced-file count.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → writer CLI | shell invokes `node scripts/write-sub-agent-lsl.mjs` with operator's uid. |
| Writer → file system | atomic .tmp + rename per LSL file (Plan 51-06 contract). |
| Writer → claude transcript discovery | adapter reads `~/.claude/projects/-Users-Q284340-Agentic-coding/` (uid-checked per Plan 51-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-15-PROD | Repudiation | one-shot production data backfill — operator needs an audit trail | mitigate | SUMMARY captures exact CLI + log |
| T-51-15-FS | Information Disclosure | writer creates many files under tracked .specstory/history | accept | Plan 51-06's atomic write + slot-allocator semantics |
| T-51-15-IDEM | Tampering | re-running writer could create duplicate slot allocations | mitigate | slot-allocator state file (.data/sub-agent-slot-state.json) enforces single-slot-per-sub-agent semantics per Plan 51-06 contract |
| T-51-15-SC | Tampering | npm dependencies | accept | zero new packages |
</threat_model>

<verification>
- `find .specstory/history -name '*_S[0-9]*-*-*.md' | wc -l` returns > 0
- BOTH 2026-05-23 parent UUIDs (5d22e2d5 AND 31274d29) represented (AND-coupled greps, not alternation)
- SUMMARY has the auditable command + timestamp
</verification>

<success_criteria>
- Single task commit lands
- AC #2 closed (production LSL parity restored)
- Both parent UUIDs (5d22e2d5 + 31274d29) confirmed present
- D-Reuse cumulative gate clean
- No new npm packages
- WR-05 dead-code remains as a known deferred issue (out of scope per locked scope statement)
</success_criteria>

<output>
Create `.planning/phases/51-…/51-15-SUMMARY.md` documenting:
- The EXACT CLI invocation that produced the production backfill + run-completion timestamp
- Count of produced LSL files + a sample file's frontmatter excerpt
- The two 2026-05-23 parent UUID confirmations (grep results for BOTH 5d22e2d5 AND 31274d29)
- Whether .specstory/history is tracked / submoduled / gitignored (relevant for the commit shape)
- Phase 51 D-Reuse cumulative gate status
- Confirmation that WR-05 remains deferred (out of scope per locked scope statement)
- Any Rule 1/2/3 deviations
</output>
</content>
