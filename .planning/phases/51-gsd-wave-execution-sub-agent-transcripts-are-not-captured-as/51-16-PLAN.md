---
phase: 51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as
plan: 16
type: execute
wave: 9
depends_on: ["51-12", "51-13", "51-14", "51-15"]
files_modified:
  - .planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/verification/51-16-dashboard-baseline.png
  - .planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/verification/51-16-dashboard-during-wave.png
  - .planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-HUMAN-UAT.md
autonomous: false
requirements: []
gap_closure: true
gaps_addressed:
  - "AC #3 (CONTEXT.md) — observation parity within ≤15 min for live tier"
  - "HUMAN-UAT Test 1 — Plan 51-11 Task 3 operator launchd install + smoke-verify"
  - "HUMAN-UAT Test 2 — Plan 51-11 Task 4 six final CONTEXT.md acceptance criteria"
  - "HUMAN-UAT Test 3 — Plan 51-10 Task 3 live tmux statusline verification"
  - "Health-coordinator restart so /health/state surfaces sub_agent_capture (VERIFICATION.md human_verification entry #3)"
tags: [phase-51, gap-closure, ac-3, human-uat, checkpoint, browser-verification]

must_haves:
  truths:
    - "All 3 stale nohup'd live daemons are killed BEFORE launchd takes over (per HUMAN-UAT Test 1 step 1) so launchd can acquire required locks"
    - "After Plan 51-12's CR-04 fix, `launchctl kickstart -k gui/$UID/com.coding.sub-agent-live-{claude,copilot,opencode}` boots each daemon with a real PID (exit code is NOT 78); /opt/homebrew/bin/node resolves correctly"
    - "Health-coordinator process (currently PID 1806 — pre Plan 51-11 merge) is restarted so /health/state returns the sub_agent_capture block (currently 404)"
    - "A fresh `/gsd-execute-phase` against a SMALL DOCS-ONLY TEST PHASE (NOT Phase 51 — that caused the nested-execute incident on 2026-05-27 per HUMAN-UAT.md line 61) produces at least one fresh observation with `source='sub-agent'` (live tier, NOT -backfill) within 15 minutes"
    - "All 6 CONTEXT.md acceptance criteria (AC1 backfill, AC2 LSL parity, AC3 observation parity ≤15min, AC4 agent-agnostic, AC5 idempotency, AC6 dashboard truth) are verified via browser screenshots captured to .planning/phases/51-.../verification/"
    - ".planning/phases/51-…/51-HUMAN-UAT.md is updated from `result: pending` → `result: passed` for Tests 1, 2, and 3 (with screenshot paths recorded)"
  artifacts:
    - path: .planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/verification/51-16-dashboard-baseline.png
      provides: "Pre-wave dashboard screenshot for AC #6 baseline (knowledge-pipeline badge GREEN)"
    - path: .planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/verification/51-16-dashboard-during-wave.png
      provides: "Mid-wave dashboard screenshot for AC #6 + Plan 51-10 Task 3 closure (badge stays GREEN under sub-agent load)"
    - path: .planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-HUMAN-UAT.md
      provides: "Updated UAT log with PASSED results + screenshot references + DB queries that verified each AC"
  key_links:
    - from: "launchd live daemons (post CR-04 fix)"
      to: ".observations/observations.db (live-tier rows)"
      via: "daemon → ObservationWriter.processMessages → SQLite INSERT with metadata.source='sub-agent'"
      pattern: "source='sub-agent'(?!-backfill)"
    - from: "scripts/health-coordinator.js (post-restart)"
      to: "/health/state.sub_agent_capture"
      via: "pollSubAgentCapture → loadAllHeartbeats → currentState.sub_agent_capture"
      pattern: "sub_agent_capture"
---

<objective>
Final closure plan for Phase 51 gap-closure. This is a `checkpoint:human-verify` plan (`autonomous: false`) that the operator drives in a live shell. It re-attempts ALL three HUMAN-UAT tests from 51-HUMAN-UAT.md AND verifies AC #3 (observation parity ≤15 min via the live tier) end-to-end.

This plan can ONLY run after Plans 51-12 (CR-04 fix), 51-13 (CR-01+CR-02 OpenCode fixes), 51-14 (CR-03 Claude registry fix), and 51-15 (production LSL backfill) have landed. Without 51-12, the launchctl kickstart will still exit 78. Without 51-15, AC #2 still reads 0 files. Without 51-13/51-14, the daemons can boot but emit broken signals.

The plan has three sequential operator-driven phases, each with explicit shell commands and expected outputs. The single human-verify task wraps all three phases into one resume-signal gate. After approval, the operator updates 51-HUMAN-UAT.md and a final SUMMARY closes Phase 51.

Per HUMAN-UAT.md line 61 (lesson from the 2026-05-27 incident): the operator MUST NOT re-run Phase 51 itself as the test phase — pick a SMALL DOCS-ONLY TEST PHASE (suggestion in 51-HUMAN-UAT.md: Phase 50 or another small docs-only phase). Re-running Phase 51 as the test phase caused a nested `/gsd-execute-phase` incident.

Per memory/feedback_e2e_verify.md: visual browser verification is REQUIRED for AC #6 (dashboard truth). Don't claim "it works" from DB queries alone. Use `/playwright-cli` skill (see `.claude/skills/playwright-cli/SKILL.md` if available, OR `/playwright-cli` command from CLAUDE.md skills section) for the dashboard screenshots.

Per memory/feedback_test_statusline_in_tmux.md (CRITICAL): for Test 3 (live tmux statusline under sub-agent load), the verification must happen INSIDE A LIVE tmux session — offline output doesn't count.

Purpose: Convert all 6 ACs from PARTIAL/FAIL → PASS by re-running the operator-side smoke under a fixed codebase. Update 51-HUMAN-UAT.md from `pending` → `passed`. Phase 51 closes after this checkpoint approves.
Output: Browser screenshots + updated HUMAN-UAT log + checkpoint resume-signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-CONTEXT.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-VERIFICATION.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-HUMAN-UAT.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-11-SUMMARY.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-12-PLAN.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-13-PLAN.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-14-PLAN.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-15-PLAN.md
@scripts/install-sub-agent-launchd.sh
@scripts/health-coordinator.js
@launchd/com.coding.sub-agent-live-claude.plist
@launchd/com.coding.sub-agent-live-opencode.plist
@launchd/com.coding.sub-agent-live-copilot.plist
@launchd/com.coding.sub-agent-sweep.plist
@CLAUDE.md

<interfaces>
<!-- /health/state response shape after coordinator restart (per Plan 51-11 SUMMARY § Decisions) -->

```json
{
  "sub_agent_capture": {
    "status": "healthy",
    "live_registrations": {
      "claude":   { "running": N, "last_heartbeat_age_ms": M },
      "opencode": { "running": N, "last_heartbeat_age_ms": M },
      "copilot":  { "running": N, "last_heartbeat_age_ms": M },
      "mastra":   { "running": 0, "available": false, "reason": "Path A not viable per RESEARCH" }
    },
    "last_sweep_at": "<ISO>",
    "registry_size": N
  }
}
```

<!-- DB query for AC #3 (live-tier observations within 15 min) -->

```sql
SELECT COUNT(*) FROM observations
WHERE json_extract(metadata, '$.source') = 'sub-agent'
  AND created_at > strftime('%s', 'now', '-15 minutes');
```

<!-- D-LSL-Filename pattern for AC #2 sub-agent LSL files -->

```
{YYYY-MM-DD}_{HHHH-HHHH}_S{slot}-{idx}-{hash}.md
e.g. 2026-05-28_1430-1545_S03-001-abc1234.md
```

<!-- HUMAN-UAT.md Tests 1-3 current state (51-HUMAN-UAT.md lines 14-65) — all three records say `result: pending`. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-attempt all three HUMAN-UAT tests + verify AC #3 end-to-end (operator-driven, sequential)</name>
  <what-built>
    Plans 51-12 through 51-15 closed all 4 critical bugs and ran the production LSL backfill:
    - 51-12: CR-04 fix — launchd plists now resolve node correctly on Apple Silicon
    - 51-13: CR-01 + CR-02 — OpenCode adapter --limit honored + heartbeat emits registry_rows
    - 51-14: CR-03 — claude-fs-watch observations_written increment fixed
    - 51-15: production .specstory/history backfill — AC #2 from 0 files → >0 files

    Now the operator must drive the launchd re-install + 6-AC verification in a live shell + browser. This is a SINGLE checkpoint with three sequential phases.
  </what-built>
  <how-to-verify>
    Operator runs the following steps in order from `/Users/Q284340/Agentic/coding`. Each phase has its own verification gate; report failures inline before proceeding to the next phase.

    **PHASE A — Kill stale daemons + restart launchd jobs (closes HUMAN-UAT Test 1)**

    1. Kill stale nohup'd daemons so launchd can acquire required locks (per HUMAN-UAT.md line 22):
       ```bash
       pkill -f 'node scripts/sub-agent-live-(claude|opencode|copilot)\.mjs' || true
       ps aux | grep -E "node scripts/sub-agent-live" | grep -v grep    # should be empty
       ```

    2. Re-run the installer (post 51-12 CR-04 fix):
       ```bash
       bash /Users/Q284340/Agentic/coding/scripts/install-sub-agent-launchd.sh
       ```
       Expected: `[install-sub-agent] OK: com.coding.sub-agent-{sweep,live-claude,live-copilot,live-opencode} is loaded` for all 4. NO exit-78 messages.

    3. Kickstart the live daemons (skip opencode if HUMAN-UAT Test 1.A schema mismatch is still in effect — non-blocking per HUMAN-UAT.md):
       ```bash
       launchctl kickstart -k gui/$(id -u)/com.coding.sub-agent-live-claude
       launchctl kickstart -k gui/$(id -u)/com.coding.sub-agent-live-copilot
       launchctl kickstart -k gui/$(id -u)/com.coding.sub-agent-live-opencode  # may fail with schema=null per Test 1.A — log + continue
       ```

    4. Verify PID column is NOT '-' for the live daemons:
       ```bash
       launchctl list | grep com.coding.sub-agent
       ```
       Expected for live-claude + live-copilot: a numeric PID + exit code 0 (or no exit code). For sub-agent-sweep: PID '-' is fine (sweep is one-shot per interval). For live-opencode: see HUMAN-UAT 1.A.

    5. Tail the daemon logs for 60s — expect at least one heartbeat each:
       ```bash
       tail -F .data/live-claude.log .data/live-copilot.log
       # Wait 30s. Expect to see lines like:
       #   [live-claude] heartbeat — watched=N tailing=M registered=K
       ```

    6. Update `.planning/phases/51-…/51-HUMAN-UAT.md` Test 1: `result: pending` → `result: passed` (or `partial` if opencode 1.A still failing). Add a `passed_at: <ISO>` field.

    **PHASE B — Restart health-coordinator + verify /health/state.sub_agent_capture (closes VERIFICATION human_verification #3)**

    1. Identify the running coordinator process:
       ```bash
       ps aux | grep 'scripts/health-coordinator.js' | grep -v grep
       ```
       Note the PID (per VERIFICATION.md line 67, this is currently PID 1806 — pre-merge from Tue 07AM).

    2. Restart via launchctl (the coordinator runs as a launchd job):
       ```bash
       launchctl kickstart -k gui/$(id -u)/com.coding.health-coordinator
       ```
       (or the correct launchd label — check `launchctl list | grep -i health` first)

    3. Wait 10s for the coordinator to come up, then probe:
       ```bash
       sleep 10
       curl -s http://localhost:3033/health/state | jq -e .sub_agent_capture
       ```
       Expected: jq exits 0 (the field exists; previously returned 404 per VERIFICATION.md line 68). Inspect the per-agent block:
       ```bash
       curl -s http://localhost:3033/health/state | jq .sub_agent_capture
       ```
       Expected shape per the interfaces block above.

    **PHASE C — Run a fresh /gsd-execute-phase against a SMALL DOCS-ONLY test phase + verify all 6 ACs**

    1. **Capture baseline observation count** (for AC #3 delta):
       ```bash
       BASELINE_TS=$(date +%s)
       sqlite3 .observations/observations.db "SELECT COUNT(*) FROM observations WHERE json_extract(metadata,'\$.source')='sub-agent'"
       # Record the value (likely 2 per VERIFICATION.md line 61).
       echo "BASELINE_TS=$BASELINE_TS BASELINE_LIVE_COUNT=<value>"
       ```

    2. **Capture pre-wave dashboard screenshot** for AC #6:
       Use `/playwright-cli` (or `.claude/skills/playwright-cli/SKILL.md` if available). Open `http://localhost:3032`, screenshot to:
       ```
       .planning/phases/51-…/verification/51-16-dashboard-baseline.png
       ```
       Confirm the knowledge-pipeline badge is GREEN and per-project bubble shows current state.

    3. **Pick a small docs-only test phase** (NOT Phase 51!). Per HUMAN-UAT.md line 61 recommendation, candidates: Phase 50 (already executed) re-run on a synthetic plan, OR a small docs-only phase from the backlog. The executor's discretion — the only hard constraint is "NOT Phase 51, NOT a heavyweight migration".

    4. **Run the wave**:
       ```
       /gsd-execute-phase <small-test-phase>
       ```
       Watch the live-claude log for sub-agent registrations:
       ```bash
       tail -F .data/live-claude.log
       # Expected within seconds of each sub-agent spawn:
       #   [live-claude] new sub-agent detected: agent-... parent_session=...
       ```

    5. **Verify AC #1 — Backfill** (re-verify the existing 2026-05-23 backfill is still healthy):
       ```bash
       sqlite3 .observations/observations.db "SELECT COUNT(*) FROM observations WHERE json_extract(metadata,'\$.source')='sub-agent-backfill'"
       # Expected: >= 117 (per VERIFICATION.md line 6 / table row #6)
       ```

    6. **Verify AC #2 — LSL parity** (post Plan 51-15):
       ```bash
       find .specstory/history -name '*_S[0-9]*-*-*.md' | wc -l
       # Expected: > 0 (post Plan 51-15 backfill)
       find .specstory/history -name '*_S[0-9]*-*-*.md' -newer /tmp/51-16-wave-start | head -5
       # Expected: at least one file with mtime AFTER the wave started (live-tier produced LSL files for the new sub-agents)
       ```

    7. **Verify AC #3 — Observation parity ≤15 min** (the critical gap):
       ```bash
       sqlite3 .observations/observations.db "SELECT COUNT(*) FROM observations WHERE json_extract(metadata,'\$.source')='sub-agent' AND created_at > $BASELINE_TS"
       # Expected: > 0 (new live-tier observations from the wave, NOT from the 2 pre-existing manual rows)

       sqlite3 .observations/observations.db "SELECT json_extract(metadata,'\$.sub_hash'), datetime(created_at, 'unixepoch') FROM observations WHERE json_extract(metadata,'\$.source')='sub-agent' AND created_at > $BASELINE_TS ORDER BY created_at DESC LIMIT 10"
       # Expected: rows from the last few minutes with sub_hash matching live-detected agents
       ```

    8. **Verify AC #4 — Agent-agnostic** (per VERIFICATION.md AC #4 was PARTIAL):
       For minimum closure: confirm the claude daemon produced rows (post-CR-03 fix means observations_written>0 mid-tail).
       For full closure: re-run a small test in opencode CLI (skip if HUMAN-UAT 1.A is still failing) and copilot. Capture sub-agent observations from each with `metadata.agent='opencode'` and `metadata.agent='copilot'`. THIS IS BEST-EFFORT — the locked planning context defers AC #4 expansion to a future plan if opencode 1.A is still blocking.

    9. **Verify AC #5 — Idempotency** (re-running sweep is a no-op):
       ```bash
       launchctl kickstart -k gui/$(id -u)/com.coding.sub-agent-sweep
       sleep 30
       # Compare observation count before & after sweep — should NOT have any new duplicates
       sqlite3 .observations/observations.db "SELECT COUNT(*) FROM observations WHERE json_extract(metadata,'\$.source')='sub-agent-backfill'"
       # Expected: same count as step 5 (ObservationWriter content_hash dedup + registry's idempotency key)
       ```

    10. **Verify AC #6 — Dashboard truth** (DURING the wave, not after):
        Capture a screenshot during step 4's wave-execution to:
        ```
        .planning/phases/51-…/verification/51-16-dashboard-during-wave.png
        ```
        Confirm knowledge-pipeline badge stays GREEN. Per-project bubble `C🟢` stays green (resolves Plan 51-10 Task 3 partial-verify — HUMAN-UAT Test 3).

    11. **Update `.planning/phases/51-…/51-HUMAN-UAT.md`** — change Test 2 (Plan 51-11 Task 4) and Test 3 (Plan 51-10 Task 3) from `result: pending` → `result: passed`. Add `passed_at: <ISO>` + `evidence: see .planning/phases/51-…/verification/` for each.

    **FAILURE MODES the operator should report (do NOT mark Phase 51 closed if any of these fire):**

    - If `launchctl kickstart` still returns exit code 78: 51-12 fix is incomplete; escalate to a 51-12 revision.
    - If /health/state.sub_agent_capture remains 404 after coordinator restart: the running coordinator is NOT the launchd-managed one (could be a stale shell-spawned process). `pkill -f health-coordinator` + restart via launchctl.
    - If AC #3 fails (zero new sub-agent observations within 15 min of wave start): CR-03 fix may be incomplete OR Plan 51-11's wired pollSubAgentCapture path has a bug; escalate.
    - If LSL files exist but Phase 50 window.mjs can't parse them: Plan 51-06 D-LSL-Filename regression; escalate.
  </how-to-verify>
  <resume-signal>
    Type "phase 51 closed" to confirm:
    (a) All 4 launchd jobs boot cleanly (Phase A)
    (b) /health/state.sub_agent_capture returns the expected JSON (Phase B)
    (c) All 6 ACs verified with screenshots captured (Phase C)
    (d) 51-HUMAN-UAT.md updated to passed for Tests 1, 2, 3.

    Type "AC#N failure: <description>" to report a specific AC regression — Phase 51 remains open and a revision plan-phase is the next step.

    Type "blocked: <description>" if a prerequisite (e.g. Phase 50 not running, .observations/observations.db corrupted) prevents verification — escalate for separate fix before retrying this checkpoint.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → launchctl | install + kickstart in gui/$UID/; no sudo. |
| Operator → /gsd-execute-phase | spawns sub-agents via Agent tool; ETM + live daemons must capture both LSL + observations. |
| Operator → dashboard | browser screenshot via /playwright-cli skill against http://localhost:3032. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-16-CR04 | Denial of Service | Plan 51-12 fix doesn't actually work on the operator's host | mitigate | Phase A explicit check; failure-mode escalation path documented |
| T-51-16-NESTED | Tampering | nested /gsd-execute-phase incident from 2026-05-27 (HUMAN-UAT line 61) | mitigate | explicit "do NOT use Phase 51 as the test phase" directive |
| T-51-16-AC3 | Repudiation | AC #3 cannot be verified without live-tier observations within 15 min | mitigate | sub_hash + timestamp DB query proves the live tier wrote |
| T-51-16-DB | Information Disclosure | screenshots may capture session-identifying data | accept | screenshots go into the project's .planning/ tree which is git-tracked but uid-private |
| T-51-16-AS | Tampering | AC #4 partial closure (opencode 1.A unresolved) | accept | locked planning context defers opencode schema migration=null to a future plan |
</threat_model>

<verification>
The Task 1 checkpoint IS the verification. All ACs are operator-driven shell + browser checks. No automatable verification — by definition, this is the human-verify final gate.

Post-checkpoint, the SUMMARY captures:
- launchctl list output (4 jobs loaded, no exit-78)
- curl /health/state output (sub_agent_capture block present)
- DB queries with results for AC #1, AC #3, AC #5
- Screenshot paths for AC #6
- LSL file counts pre/post wave for AC #2
- 51-HUMAN-UAT.md diff
</verification>

<success_criteria>
- Task 1 checkpoint approves with resume-signal "phase 51 closed"
- 51-HUMAN-UAT.md Tests 1, 2, 3 all show `result: passed`
- All 6 ACs verified (AC #4 may be `partial` if opencode 1.A is still blocking — accepted per locked planning context)
- 2 screenshots in `.planning/phases/51-…/verification/`
- STATE.md re-points to Phase 43 (per Plan 51-11 SUMMARY + locked planning context)
- ROADMAP.md Phase 51 entry: all 11 + 5 (= 16) plans marked [x] complete
- D-Reuse cumulative gate clean across all 16 plans
- No new npm packages cumulative across all 16 plans
</success_criteria>

<output>
Create `.planning/phases/51-…/51-16-SUMMARY.md` documenting:
- The exact operator command transcript from Phases A, B, C
- DB query results for AC #1, #3, #5
- Screenshot paths for AC #6 (baseline + during-wave)
- LSL file deltas for AC #2 (pre / during-wave / post counts)
- 51-HUMAN-UAT.md diff (Tests 1, 2, 3 — pending → passed)
- The test phase used (NOT Phase 51 per HUMAN-UAT line 61 directive)
- Any AC that landed `partial` instead of `passed` with rationale + follow-up tracking
- Phase 51 cumulative closure metrics: total tests (sum across all 16 plans), total commits, total files created/modified, cumulative D-Reuse gate status
- Confirmation that STATE.md was re-pointed to Phase 43 (or note if a separate state-update commit is needed)

ALSO update .planning/STATE.md:
- progress.completed_phases += 1 (Phase 51 closed)
- progress.completed_plans += 5 (51-12 through 51-16 — five new gap-closure plans)
- Current Position: Phase 43 (v7.1 OKM Cross-Repo Migration)
- stopped_at: "Phase 51 closed via gap-closure plans 51-12..51-16 — agent-agnostic sub-agent capture operational; CR-01..CR-04 closed; production LSL backfill done; all 6 ACs verified"
- last_activity: <today>
- Append Phase 51 closure entry to Roadmap Evolution log noting the gap-closure cycle

ALSO update .planning/ROADMAP.md Phase 51 entry: mark plans 51-12 through 51-16 as [x] complete.
</output>
</content>
