---
phase: 51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as
plan: 10
type: execute
wave: 5
depends_on: ["51-07", "51-08", "51-09"]
files_modified:
  - scripts/combined-status-line.js
  - scripts/status-line-fast.cjs
  - lib/lsl/registry-reader.mjs
  - tests/live-logging/registry-reader.test.js
  - tests/live-logging/statusline-registry-sourced.test.js
autonomous: false   # contains checkpoint:human-verify for the live tmux bubble check
requirements: []
tags: [phase-51, statusline, cleanup, d-statusline, tmux, mitigation-removal]

must_haves:
  truths:
    - "lib/lsl/registry-reader.mjs reads the heartbeat state files written by Plans 51-07, 51-08, 51-09 daemons and aggregates a per-project sub-agent freshness signal"
    - "combined-status-line.js _freshestProjectActivityAgeMs() sources `subMt` from the registry-reader (NOT from a re-walk of <parent>/subagents/) — D-Statusline cleanup target #1"
    - "combined-status-line.js transcriptAgeMs() (the helper near line 974) sources from the registry-reader — D-Statusline cleanup target #2"
    - "scripts/combined-status-line-projects.json mapping write only stamps `subMt` when the registry surfaces a current sub-agent for that project — D-Statusline cleanup target #3"
    - "Per-project bubble (C🟢 / KC⚫) AND the [📚] badge BOTH go green during an active sub-agent run — verified live in tmux per checkpoint:human-verify"
    - "No regression on the 2026-05-12 codepoint-widths fix (reference_tmux_emoji_width_fix.md). Verified by visual inspection in actual tmux per feedback_test_statusline_in_tmux.md"
    - "The original 2026-05-24 mitigation hooks are REMOVED from combined-status-line.js — replaced by registry-sourced reads. Cleanup target verified via grep gate."
    - "status-line-fast.cjs's existing `subMt` consumption is unchanged — it already reads the field from the mapping JSON; only the WRITER of that field changes"
  artifacts:
    - path: lib/lsl/registry-reader.mjs
      provides: "Reads heartbeat state files; aggregates per-project sub-agent freshness; exports getFreshSubAgents(project)"
      exports: ["getFreshSubAgents", "getProjectSubMt", "loadAllHeartbeats"]
      min_lines: 100
    - path: tests/live-logging/registry-reader.test.js
      provides: "Unit tests: heartbeat file parsing, per-project aggregation, stale-heartbeat handling"
      min_lines: 80
    - path: tests/live-logging/statusline-registry-sourced.test.js
      provides: "Integration tests for combined-status-line.js's registry-sourced subMt + verifies absence of <parent>/subagents/ re-walk"
      min_lines: 60
---

<objective>
Wave 5 — Replace the 2026-05-24 statusline-bubble mitigation per CONTEXT.md D-Statusline. The interim hooks (`_freshestProjectActivityAgeMs()`, `transcriptAgeMs()`, and the projects-mapping `subMt` write — all in scripts/combined-status-line.js) re-walk `<parent>/subagents/` on every tick. After Plans 51-07/08/09 write per-agent heartbeat files, the registry IS the source of truth and these mitigations become redundant.

Per CONTEXT.md verbatim:
> Phase 51 includes a dedicated cleanup plan (one of the final plans in the phase) that:
> - Sources subMt for each project from the new registry instead of re-walking <parent>/subagents/ on every tick
> - Removes the three mitigation hooks (_freshestProjectActivityAgeMs treats sub-agent mtimes the same as parent mtimes natively; transcriptAgeMs returns whichever is freshest from the registry; mapping writes subMt only when registry surfaces a current sub-agent)
> - Verification: the per-project bubble (C🟢 / KC⚫) and the [📚] badge BOTH go green during an active sub-agent run (no mtime fade), AND the dashboard does not regress on the 2026-05-12 tmux emoji-width fix

This plan has THREE tasks:
1. Build lib/lsl/registry-reader.mjs — reads heartbeat files Plans 51-07/08/09 wrote, aggregates per-project freshness.
2. Refactor combined-status-line.js — replace the three mitigation hooks with registry-reader calls. Remove inline `<parent>/subagents/` walks.
3. CHECKPOINT — live tmux visual verification per feedback_test_statusline_in_tmux.md.

Per D-Reuse: Phase 50 primitives unchanged. lib/lsl/registry.mjs (the in-memory registry from Plan 51-01) is NOT what this plan reads — that registry is per-daemon-process and not visible to the statusline script. The READER consumes the per-daemon HEARTBEAT FILES (.data/sub-agent-live-state-{claude,opencode,copilot}.json) which DO have persistent state.

Per project rules from memory:
- reference_tmux_emoji_width_fix.md — do NOT modify visibleCellWidth() or related width-computation code. The fix lives in ~/.tmux.conf (codepoint-widths). This plan does NOT touch tmux.conf and MUST NOT modify rendering-width logic.
- feedback_test_statusline_in_tmux.md — NEVER git add a statusline change without live tmux render check. The Task 3 checkpoint:human-verify enforces this.
- CLAUDE.md: statusline rules — project-level .claude/settings.local.json only, never global.

Purpose: Close CONTEXT.md AC #6 (Dashboard truth — the knowledge-pipeline badge no longer goes stale during an actively-running wave that's producing sub-agent work) and D-Statusline (replace 2026-05-24 mitigation).

Output: One new registry-reader module, modified combined-status-line.js, two test files, one checkpoint for live tmux verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-CONTEXT.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-07-SUMMARY.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-08-SUMMARY.md
@.planning/phases/51-gsd-wave-execution-sub-agent-transcripts-are-not-captured-as/51-09-SUMMARY.md
@scripts/combined-status-line.js
@scripts/status-line-fast.cjs
@scripts/combined-status-line-projects.json
@CLAUDE.md
@~/.claude/projects/-Users-Q284340-Agentic-coding/MEMORY.md
@~/.claude/projects/-Users-Q284340-Agentic-coding/memory/reference_tmux_emoji_width_fix.md
@~/.claude/projects/-Users-Q284340-Agentic-coding/memory/feedback_test_statusline_in_tmux.md

<interfaces>
<!-- Registry reader API (NEW) -->
```javascript
// lib/lsl/registry-reader.mjs
export function loadAllHeartbeats({ stateDir = '.data' } = {}) {
  // Reads .data/sub-agent-live-state-{claude,opencode,copilot}.json (the heartbeat files
  // Plans 51-07/08/09 daemons write every 30s).
  // Returns: { claude: {...heartbeat}, opencode: {...}, copilot: {...} }
  // Missing files → empty objects (daemon not running OR no heartbeats yet).
  // Files older than 90s (3x heartbeat interval) → treated as stale + flagged.
}

export function getFreshSubAgents(project, { stateDir = '.data', maxAgeMs = 90_000 } = {}) {
  // Aggregates across all three agent heartbeats.
  // Returns Array<{ agent, sub_hash, parent_session_id, status, heartbeat_age_ms }>
  // filtered to ONLY sub-agents currently 'running' for the given project.
  // Empty array when nothing live.
}

export function getProjectSubMt(project, { stateDir = '.data' } = {}) {
  // Returns the most-recent heartbeat mtime (ms since epoch) ACROSS all three agents
  // for the given project. Used by combined-status-line.js to source the subMt field.
  // Returns 0 when no fresh sub-agents.
}
```

<!-- combined-status-line.js modifications (REPLACE the 2026-05-24 mitigation hooks) -->

Current locations to modify (from grep results):
- Line 498: `async _freshestProjectActivityAgeMs() { ... }` — sub-agent freshness folded in
- Line 974: `const transcriptAgeMs = (projectName) => { ... }` — sub-agent walk inside
- Line 1136-1156: subMt-write logic inside the projects-mapping refresh — computes subMt by walking subagents/
- Line 2015: caller of `_freshestProjectActivityAgeMs()`

After refactor:
- _freshestProjectActivityAgeMs(): treats sub-agent mtimes the same as parent mtimes natively. The sub-agent mtime now comes from getProjectSubMt(project) instead of walking subagents/.
- transcriptAgeMs(): returns whichever is freshest — parent transcript mtime or registry-sourced sub-agent mtime.
- mapping write: only stamps subMt when getProjectSubMt(project) > 0; otherwise omits the field.

<!-- status-line-fast.cjs (NO MODIFICATIONS) -->
status-line-fast.cjs already reads `subMt` from the mapping JSON (lines 103-156 per the grep). The writer of that field changes (combined-status-line.js); the reader is untouched. status-line-fast.cjs is the tmux-rendered shim — modifying it risks the codepoint-widths fix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build lib/lsl/registry-reader.mjs (TDD)</name>
  <files>lib/lsl/registry-reader.mjs, tests/live-logging/registry-reader.test.js</files>
  <read_first>
    - scripts/sub-agent-live-claude.mjs (Plan 51-07 — for heartbeat file shape)
    - scripts/sub-agent-live-opencode.mjs (Plan 51-08 — same shape)
    - scripts/sub-agent-live-copilot.mjs (Plan 51-09 — same shape + lsl_incomplete_marker_present field)
    - .planning/phases/51-…/51-07-SUMMARY.md + 51-08-SUMMARY.md + 51-09-SUMMARY.md — heartbeat schema confirmation
    - lib/lsl/registry.mjs (NOT consumed by this reader — important context only)
  </read_first>
  <behavior>
    Test 1 — loadAllHeartbeats({stateDir:<empty-tmpdir>}) returns {claude:{}, opencode:{}, copilot:{}} (no errors on missing files).
    Test 2 — loadAllHeartbeats with one fresh heartbeat file present (e.g. claude's) returns {claude:{last_heartbeat_at, registered, ...}, opencode:{}, copilot:{}}.
    Test 3 — loadAllHeartbeats with a STALE heartbeat (mtime > 90s old) returns the parsed object PLUS a `stale: true` flag stamped at the per-agent root.
    Test 4 — getFreshSubAgents('coding', stateDir) returns ALL three agents' running sub-agents merged into one array. Filter excludes completed/failed/stale.
    Test 5 — getFreshSubAgents filters by project: a heartbeat with registry_rows for project='other-project' is excluded.
    Test 6 — getProjectSubMt('coding') returns max heartbeat-file mtime across the three agents (NOT the heartbeat content's last_heartbeat_at — the FILE's mtime, because the statusline cares about freshness of the SIGNAL not freshness of the data inside).
    Test 7 — getProjectSubMt returns 0 when all three heartbeat files are stale (> 90s).
    Test 8 — getProjectSubMt returns 0 when no heartbeat files exist (daemons not running).
    Test 9 — Malformed JSON in a heartbeat file: loadAllHeartbeats logs stderr 'failed to parse heartbeat <path>' and treats as empty (defensive — does not crash the statusline).
    Test 10 — uid-check on heartbeat files: a heartbeat file owned by another uid is skipped + stderr 'skipping non-owned heartbeat'.
  </behavior>
  <action>
    Create lib/lsl/registry-reader.mjs implementing:

    1. HEARTBEAT_FILES = `Object.freeze({ claude: 'sub-agent-live-state-claude.json', opencode: 'sub-agent-live-state-opencode.json', copilot: 'sub-agent-live-state-copilot.json' })`.

    2. `loadAllHeartbeats({stateDir = '.data'} = {})`:
       - For each agent in HEARTBEAT_FILES:
         a. filePath = path.join(stateDir, filename).
         b. If !fs.existsSync(filePath): result[agent] = {}; continue.
         c. fs.statSync; uid-check; skip non-owned with stderr.
         d. age = Date.now() - stat.mtimeMs. If age > 90_000: result[agent] = {...parsed, stale: true, age_ms: age}.
         e. Else: result[agent] = {...parsed, stale: false, mtime_ms: stat.mtimeMs}.
         f. Try/catch JSON.parse → stderr log + result[agent] = {}.
       - Return result.

    3. `getFreshSubAgents(project, {stateDir, maxAgeMs = 90_000} = {})`:
       - heartbeats = loadAllHeartbeats({stateDir}).
       - out = [].
       - For each agent, if heartbeats[agent].stale: continue. Else: for each row in heartbeats[agent].registry_rows ?? []: if row.status === 'running' AND (row.project ?? project) === project: out.push({agent, sub_hash: row.sub_hash, parent_session_id: row.parent_session_id, status: row.status, heartbeat_age_ms: Date.now() - heartbeats[agent].mtime_ms}).
       - Return out.

    4. `getProjectSubMt(project, {stateDir} = {})`:
       - heartbeats = loadAllHeartbeats({stateDir}).
       - For each agent's heartbeat, if not stale AND any registry_rows match the project AND any are status='running': return max(mtime_ms). Else return 0.

    Write tests/live-logging/registry-reader.test.js with 10 tests per Behavior block. Use tmpdir-isolated state files for each test.

    Threat-model:
    - T-51-10-FI (statusline reading state files): uid-check; non-owned skipped.
    - T-51-10-CR (heartbeat file corruption): try/catch + stderr log + empty fallback. Defensive — never crash the statusline.
    - T-51-10-RC (stale heartbeats during daemon restart): 90s threshold (3x heartbeat interval) accommodates one missed heartbeat. Operator can override via maxAgeMs param.
  </action>
  <verify>
    <automated>NODE_OPTIONS='--experimental-vm-modules --no-warnings' npx jest tests/live-logging/registry-reader.test.js --no-coverage 2>&amp;1 | grep -E "Tests:\s+10 passed"</automated>
  </verify>
  <acceptance_criteria>
    - 10 tests pass
    - `grep -F "loadAllHeartbeats" lib/lsl/registry-reader.mjs` returns >= 1 line
    - `grep -F "getProjectSubMt" lib/lsl/registry-reader.mjs` returns >= 1 line
    - `grep -F "stale" lib/lsl/registry-reader.mjs` returns >= 1 line — stale-heartbeat handling
    - `grep -F "fs.statSync" lib/lsl/registry-reader.mjs` returns >= 1 line — uid-check
    - `grep -c "console\\." lib/lsl/registry-reader.mjs` = 0
    - 2 commits: test(51-10) RED + feat(51-10) GREEN
  </acceptance_criteria>
  <done>
    Registry reader exists; consumes per-agent heartbeat files; exposes per-project subMt + fresh-sub-agent enumeration; defensive against missing/corrupt/stale files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace 2026-05-24 mitigation in combined-status-line.js with registry-reader calls (TDD with grep gates)</name>
  <files>scripts/combined-status-line.js, tests/live-logging/statusline-registry-sourced.test.js</files>
  <read_first>
    - scripts/combined-status-line.js — entire file (~2100 lines based on the grep references). Pay special attention to lines 498, 974, 1136-1156, 2015 (the mitigation hooks)
    - lib/lsl/registry-reader.mjs (just created by Task 1)
    - scripts/status-line-fast.cjs lines 100-160 — to confirm the `subMt` consumption is unchanged
    - memory/reference_tmux_emoji_width_fix.md — DO NOT modify visibleCellWidth() or related width logic
    - memory/feedback_test_statusline_in_tmux.md — Task 3 will verify in live tmux; this task pre-stages the file
    - CLAUDE.md § statusline rules
  </read_first>
  <behavior>
    Test 1 — combined-status-line.js no longer walks `<parent>/subagents/`: grep gate `grep -cE "fs\\.readdirSync.*subagents" scripts/combined-status-line.js` returns 0.
    Test 2 — combined-status-line.js imports lib/lsl/registry-reader.mjs (via require for CJS): grep `grep -cE "require\\(.*registry-reader" scripts/combined-status-line.js` returns 1.
    Test 3 — `_freshestProjectActivityAgeMs()` invokes `getProjectSubMt(projectName)` for each project's freshness calc.
    Test 4 — `transcriptAgeMs(projectName)` returns `Math.min(parentMt, getProjectSubMt(projectName))` (smaller age = fresher).
    Test 5 — Projects-mapping write logic at lines 1136-1156 only sets `subMt` when `getProjectSubMt(projectName) > 0`. When the registry-reader returns 0 (no fresh sub-agents), the field is OMITTED from the JSON (or explicitly set to 0; planner picks — status-line-fast.cjs handles both per existing grep `subMt: v?.subMt || 0`).
    Test 6 — visibleCellWidth() and codepoint-handling logic is UNCHANGED (grep for unchanged lines is a textual diff: `git show HEAD:scripts/combined-status-line.js | grep -c "visibleCellWidth"` matches the post-modification count).
    Test 7 — Integration: an in-process test creates fake heartbeat files in a tmpdir, invokes combined-status-line.js's projects-mapping refresh (via spawn or via importing the relevant function), and asserts the resulting projects-mapping.json contains `subMt` field set to the heartbeat mtime.
    Test 8 — Empty state: no heartbeat files present → projects-mapping has NO `subMt` field (or `subMt: 0`).
    Test 9 — Stale daemon: a 5-min-old heartbeat file → registry-reader returns 0 → mapping has no fresh subMt.

    NOTE: combined-status-line.js is CJS, so use `require()` for the registry-reader. But the reader is ESM. The integration is: combined-status-line.js does `const { getProjectSubMt } = await import('../lib/lsl/registry-reader.mjs')` (dynamic import — works in CJS). Cache the import after first call to avoid repeated dynamic-import overhead on each tick.
  </behavior>
  <action>
    Refactor scripts/combined-status-line.js:

    1. At module init (near top, after existing imports):
       ```javascript
       // Lazy-loaded ESM registry reader (Phase 51 Plan 10).
       let _registryReader = null;
       async function getRegistryReader() {
         if (!_registryReader) {
           _registryReader = await import('../lib/lsl/registry-reader.mjs');
         }
         return _registryReader;
       }
       ```

    2. Refactor `_freshestProjectActivityAgeMs()` (line ~498):
       - Remove the in-line `<parent>/subagents/` walk (the 2026-05-24 mitigation).
       - For each project, fetch registry-sourced subMt:
         ```javascript
         const reader = await getRegistryReader();
         const subMt = reader.getProjectSubMt(projectName, {stateDir: path.join(this.projectRoot, '.data')});
         ```
       - Treat subMt the same as parent transcript mtime: `effectiveAgeMs = Math.min(parentAgeMs, subAgeMs)`.

    3. Refactor `transcriptAgeMs(projectName)` (line ~974):
       - Same pattern: parent mtime via existing stat-walk + registry subMt → return min.
       - Remove the existing inline subagents/ walk.

    4. Refactor projects-mapping write (lines ~1136-1156):
       - Replace the inline subMt computation with `getProjectSubMt(projectName)`.
       - Only stamp `subMt` field when the value > 0. (Or stamp 0 explicitly — status-line-fast.cjs at line 112 already handles `subMt: v?.subMt || 0` so either works.)
       - Atomic .tmp + rename write (existing pattern — DO NOT change).

    5. ZERO modifications to visibleCellWidth() or any rendering-width logic (memory/reference_tmux_emoji_width_fix.md gate).

    Write tests/live-logging/statusline-registry-sourced.test.js with 9 tests per Behavior block. Use:
    - Grep-gate tests for the source modifications (Tests 1, 2, 6).
    - Integration tests for actual behavior (Tests 3, 4, 5, 7, 8, 9) — spawn the script with a controlled env (`HOME=<tmpdir>`, fake heartbeat files in `<tmpdir>/.data/`) and inspect the resulting projects-mapping.json.

    Threat-model:
    - T-51-10-EM (emoji-width regression): Test 6 grep gate confirms visibleCellWidth() and width-related code is byte-identical. Task 3 (checkpoint) confirms live-tmux rendering.
    - T-51-10-AT (atomic mapping write): pre-existing atomic write pattern preserved. No new corruption surface.
    - T-51-10-PI (statusline reads operator-controlled .data/): registry-reader's uid-check + try/catch defenses propagate; statusline cannot crash from a tampered heartbeat file.
  </action>
  <verify>
    <automated>NODE_OPTIONS='--experimental-vm-modules --no-warnings' npx jest tests/live-logging/statusline-registry-sourced.test.js --no-coverage 2>&amp;1 | grep -E "Tests:\s+9 passed"</automated>
  </verify>
  <acceptance_criteria>
    - 9 statusline-integration tests pass
    - `grep -cE "fs\\.readdirSync.*subagents" scripts/combined-status-line.js` returns 0 (mitigation removed)
    - `grep -F "registry-reader" scripts/combined-status-line.js` returns >= 1 line (reader imported)
    - `grep -F "visibleCellWidth" scripts/combined-status-line.js` count BEFORE === count AFTER (verifiable via `git show HEAD:scripts/combined-status-line.js | grep -c visibleCellWidth` vs `grep -c visibleCellWidth scripts/combined-status-line.js` — same number)
    - `grep -c "console\\." scripts/combined-status-line.js` did NOT increase (no new console.log calls)
    - `git diff scripts/status-line-fast.cjs` is empty (0 lines changed — D-Statusline NOT-TOUCHED clause)
    - `git diff --stat lib/lsl/window.mjs lib/lsl/scan-and-convert.mjs` returns 0 files changed
    - 2 commits: test(51-10) RED + refactor(51-10) GREEN (use `refactor:` prefix for the mitigation removal)
  </acceptance_criteria>
  <done>
    combined-status-line.js no longer walks <parent>/subagents/; sources subMt from registry-reader; visibleCellWidth() untouched; status-line-fast.cjs untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — live tmux visual verification per feedback_test_statusline_in_tmux.md</name>
  <what-built>
    Tasks 1 + 2 refactored the statusline to source sub-agent freshness from the registry-reader (heartbeat files) instead of re-walking the filesystem. All unit + integration tests pass. NOW the live tmux rendering must be verified — per memory/feedback_test_statusline_in_tmux.md, NEVER ship a statusline change without a live tmux render check. Offline output looking right ≠ tmux rendering it.
  </what-built>
  <how-to-verify>
    PRE-FLIGHT (operator):
    1. Confirm all three Phase 51 daemons are running (or skip this gate if daemons are wired in Plan 51-11):
       ```bash
       ps aux | grep -E "sub-agent-live-(claude|opencode|copilot)" | grep -v grep
       ```
       Expected: 0-3 processes. If 0, run them manually:
       ```bash
       node scripts/sub-agent-live-claude.mjs &
       node scripts/sub-agent-live-opencode.mjs &
       node scripts/sub-agent-live-copilot.mjs &
       ```

    LIVE VERIFICATION:
    1. Open a tmux session in an actual terminal (NOT VS Code's integrated terminal — Plan 51 fixes the same VirtioFS/xterm.js issue family the original mitigation tried to work around; verify in real tmux).
    2. Check the per-project bubble shows current state: `C🟢` if coding has a current claude transcript, `C⚫` otherwise. Capture before-state via screenshot or copy-paste.
    3. Run a GSD wave-execute that spawns sub-agents:
       ```bash
       # In one tmux pane:
       claude --dangerously-skip-permissions
       # In claude:
       /gsd-execute-phase <some-test-phase>
       ```
    4. While sub-agents are running, watch the statusline. EXPECTED:
       - The `C🟢` (coding) bubble stays GREEN (does not fade to ⚫) even while the parent transcript's mtime is frozen — because sub-agents are appending to their transcripts and Plan 51-07's daemon is writing fresh heartbeats every 30s.
       - The `[📚]` knowledge-pipeline badge stays GREEN.
    5. After the wave completes, the bubble may go ⚫ within 90s (heartbeat staleness threshold).
    6. Check tmux statusline width-rendering (codepoint-widths fix from 2026-05-12):
       - The `⚠` (U+26A0) and other emoji glyphs render in 2 cells, NOT 1.
       - No trailing-char residue when the statusline updates (the original VS Code/xterm.js bug).
    7. Repeat step 3-5 with `coding --opencode` and `coding --copilot` to verify opencode and copilot live tiers also produce green bubbles.

    SCREENSHOTS:
    Capture 3 screenshots (before/during/after a wave) and attach to the SUMMARY:
    - `.planning/phases/51-.../verification/51-10-statusline-before.png`
    - `.planning/phases/51-.../verification/51-10-statusline-during-wave.png`
    - `.planning/phases/51-.../verification/51-10-statusline-after.png`

    FAILURE MODES to report:
    - If the bubble FADES TO ⚫ during a sub-agent run despite Plan 51-07's daemon running: registry-reader's getProjectSubMt is returning 0 or stale. Debug the heartbeat file contents.
    - If the statusline shows trailing-char residue or wrong width: the codepoint-widths fix regressed. STOP — do NOT commit. Bisect Task 2's changes.
    - If tmux doesn't update the statusline at all: status-line-fast.cjs may have failed to read the modified projects-mapping. Inspect `~/.claude/projects-mapping-debug.json` (if logged) or rerun the projects-mapping refresh manually.
  </how-to-verify>
  <resume-signal>Type "verified" to confirm green bubbles + no width regression. Type "failure: <description>" to report a regression. Plan 51-11 cannot proceed without this gate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Statusline → `.data/sub-agent-live-state-*.json` | Reads (never writes) heartbeat files. uid-check + try/catch + stale-mtime gate. |
| combined-status-line.js → lib/lsl/registry-reader.mjs | CJS dynamic-import of ESM module. Cached after first call. |
| Registry-reader → heartbeat files | Defensive: missing/corrupt/stale files NEVER crash the statusline. |
| Tmux emoji-width fix | NOT TOUCHED. Lives in ~/.tmux.conf (codepoint-widths U+26A0=2). visibleCellWidth() unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-10-FI | Information Disclosure | statusline reads .data/*.json | mitigate | uid-check + try/catch + stale-mtime gate. Defensive — never crash. |
| T-51-10-EM | Tampering | codepoint-widths emoji-width fix regression | mitigate | Test 6 grep gate confirms visibleCellWidth() byte-identical. Task 3 checkpoint verifies live tmux render. |
| T-51-10-CR | Tampering | corrupt heartbeat file | mitigate | try/catch in loadAllHeartbeats; stderr log + empty fallback. |
| T-51-10-PI | Tampering | maliciously-crafted heartbeat file affecting statusline | mitigate | uid-check rejects non-owned files. Only operator-written files are trusted. |
| T-51-10-AT | Tampering | concurrent projects-mapping writes | accept | Pre-existing atomic .tmp + rename pattern preserved. No new corruption surface. |
| T-51-10-AD | Repudiation | mitigation-removal audit trail | mitigate | Git commit `refactor(51-10):` references this plan + the D-Statusline decision. The removed lines are recoverable via `git log` for forensics. |

</threat_model>

<verification>
End-to-end (operator-verified):
1. All unit + integration tests green: 10 reader tests + 9 statusline tests + Phase 50 47-test regression suite
2. Grep gates: zero `subagents/` walks in combined-status-line.js; status-line-fast.cjs untouched; visibleCellWidth() byte-identical
3. Live tmux rendering: green bubbles during a wave, no width regression — verified by Task 3 checkpoint
4. Pre-2026-05-24-mitigation behavior restored (the bubble used to fade ⚫ during waves — now it stays 🟢 via the proper registry-sourced signal)
</verification>

<success_criteria>
- 19 tests pass (10 reader + 9 statusline)
- Phase 50 47-test regression suite still green
- `grep -cE "fs\\.readdirSync.*subagents" scripts/combined-status-line.js` returns 0
- `git diff scripts/status-line-fast.cjs` is empty
- `grep -c visibleCellWidth scripts/combined-status-line.js` unchanged from baseline
- Task 3 checkpoint approved with screenshots in `.planning/phases/51-.../verification/`
- 4 commits: test(51-10) + feat(51-10) for Task 1; test(51-10) + refactor(51-10) for Task 2
- `git diff --stat lib/lsl/window.mjs lib/lsl/scan-and-convert.mjs` returns 0 files changed (D-Reuse cumulative)
</success_criteria>

<output>
Create `.planning/phases/51-…/51-10-SUMMARY.md` documenting:
- D-Statusline three-target cleanup status (all three mitigation hooks replaced)
- The CJS-to-ESM dynamic import pattern (cached for performance)
- Codepoint-widths fix protection (Test 6 grep gate + Task 3 visual check)
- Live tmux verification screenshots (3 captures: before/during/after a wave)
- Any Rule 1/2/3 deviations
- Phase 50 D-Reuse cumulative gate status
</output>
