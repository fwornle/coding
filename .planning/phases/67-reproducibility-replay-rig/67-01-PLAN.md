---
phase: 67-reproducibility-replay-rig
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/repro/fixtures/match-key.mjs
  - lib/repro/fixtures/llm-record.mjs
  - lib/repro/fixtures/llm-replay.mjs
  - tests/repro/_fixtures/llm-complete.req.json
  - tests/repro/_fixtures/llm-complete.resp.json
  - tests/repro/_fixtures/transcript-fragment.jsonl
  - tests/repro/match-key.test.mjs
  - tests/repro/llm-record-replay.test.mjs
autonomous: true
requirements: [REPRO-02]

must_haves:
  truths:
    - "A normalized request produces a stable sha256 match key; identical repeated calls get distinct per-key ordinals (D-07)"
    - "A recorded LLM response can be replayed byte-identically by match key"
    - "A replay lookup miss surfaces a hard-fail signal (never a live fallthrough) (D-06)"
  artifacts:
    - path: "lib/repro/fixtures/match-key.mjs"
      provides: "normalizeReq + matchKey (sha256 + per-key ordinal) — the single D-07 implementation shared by record, replay, and the proxy tap"
      exports: ["normalizeReq", "matchKey", "resetOrdinals"]
    - path: "lib/repro/fixtures/llm-record.mjs"
      provides: "recordFixture(dir, key, resp) best-effort append of {content,provider,model,tokens,latencyMs}"
      exports: ["recordFixture"]
    - path: "lib/repro/fixtures/llm-replay.mjs"
      provides: "replayLookup(dir, key) → recorded response or null (caller hard-fails on null)"
      exports: ["replayLookup"]
    - path: "tests/repro/llm-record-replay.test.mjs"
      provides: "record→replay round-trip: byte-identical served response + miss→null hard-fail"
  key_links:
    - from: "lib/repro/fixtures/llm-replay.mjs"
      to: "lib/repro/fixtures/match-key.mjs"
      via: "import matchKey/normalizeReq (one hash implementation)"
      pattern: "matchKey|normalizeReq"
    - from: "lib/repro/fixtures/llm-record.mjs"
      to: "fixtures/llm/ directory"
      via: "append fixture keyed by matchKey"
      pattern: "recordFixture"
---

<objective>
Build the LLM channel's pure record/replay core as importable, unit-tested functions: the D-07 normalized-hash + per-key-ordinal match key, the best-effort fixture recorder, and the fixture replay lookup. These are the single source of truth for request matching, consumed later by the proxy tap (Plan 06) and the integration flow (Plan 07).

Purpose: Give the proxy exactly one hash/record/replay implementation so recording and replay never disagree, and make the round-trip testable with no live proxy daemon (RESEARCH Wave 0 Gaps).
Output: `lib/repro/fixtures/{match-key,llm-record,llm-replay}.mjs`, shared `tests/repro/_fixtures/`, and two node:test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md
@.planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md
@.planning/phases/67-reproducibility-replay-rig/67-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 scaffold — shared fixtures + failing match-key/round-trip tests</name>
  <files>tests/repro/_fixtures/llm-complete.req.json, tests/repro/_fixtures/llm-complete.resp.json, tests/repro/_fixtures/transcript-fragment.jsonl, tests/repro/match-key.test.mjs, tests/repro/llm-record-replay.test.mjs</files>
  <read_first>
    - tests/experiments/consequential-events.test.mjs (node:test + node:assert/strict convention header, lines 11-17 — copy verbatim; WebSearch/WebFetch tool-name constants at :43)
    - tests/experiments/run-write.test.mjs (isolated-store + env-gated live pattern, :16-18)
    - tests/experiments/_fixtures/ (fixture layout to mirror)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Phase Requirements → Test Map"; §"Validation Architecture"; response contract §"Code Examples")
  </read_first>
  <behavior>
    - match-key.test.mjs: two requests differing only in a volatile field (task_id, subscription, request id) produce the SAME sha256 key; requests differing in model or messages produce DIFFERENT keys; canonical vs raw model name (e.g. an alias) hash to the SAME key.
    - match-key.test.mjs: calling matchKey twice on the same normalized request yields ordinal 0 then 1 (per-key call counter, D-07); resetOrdinals() zeroes the counters.
    - llm-record-replay.test.mjs: recordFixture then replayLookup with the same request returns the recorded `{content,provider,model,tokens,latencyMs}` byte-identical (deep-equal + JSON.stringify equal); a lookup for an un-recorded key returns null (the D-06 hard-fail signal); two identical recorded calls replay in recorded order via ordinal.
  </behavior>
  <action>
    Create tests/repro/_fixtures/: `llm-complete.req.json` = a synthetic `/api/complete` request body of shape `{ process, messages:[{role,content}], model, subscription, temperature, max_tokens }`; `llm-complete.resp.json` = the response contract `{ content, provider, model, tokens:{input,output,total}, latencyMs }`; `transcript-fragment.jsonl` = 2-3 redacted transcript lines containing a `tool_use`/`tool_result` pair for a WebSearch tool (used by Plan 02 — provide now as shared fixture). Redact any token/key-shaped values. Write both test files against the not-yet-existing exports from match-key.mjs (normalizeReq, matchKey, resetOrdinals), llm-record.mjs (recordFixture), llm-replay.mjs (replayLookup), using node:test + node:assert/strict per the verbatim convention header. Each test writes fixtures into a `mkdtempSync` temp dir and removes it in cleanup — NEVER a real .data path. Tests MUST currently fail (RED) because the lib modules do not exist yet.
  </action>
  <verify>
    <automated>node --test tests/repro/match-key.test.mjs tests/repro/llm-record-replay.test.mjs 2>&1 | grep -Eq 'Cannot find module|not found|fail' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/repro/_fixtures/llm-complete.resp.json` parses and contains keys `content`, `provider`, `model`, `tokens`, `latencyMs`.
    - `grep -Eqv 'sk-|gho_|Bearer ' tests/repro/_fixtures/transcript-fragment.jsonl` (no secret-shaped strings in the fixture).
    - Both test files import from `node:test` and `node:assert/strict` (grep confirms), NOT from jest.
    - Running the two suites fails at RED (modules under test do not yet exist).
  </acceptance_criteria>
  <done>Shared fixtures exist; both test suites are authored and fail only because the implementation modules are absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: match-key.mjs — normalize + sha256 + per-key ordinal (D-07)</name>
  <files>lib/repro/fixtures/match-key.mjs</files>
  <read_first>
    - scripts/measurement-stop.mjs (crypto sha256 idiom, :51 import, :364-365 hash pattern)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"match-key.mjs" — normalizeReq drops task_id/subscription/request id, canonicalizes model, in-memory Map<hash,count> ordinal)
    - tests/repro/match-key.test.mjs (the contract written in Task 1)
  </read_first>
  <action>
    Implement `normalizeReq(body)`: return a stable object with volatile fields dropped (task_id, subscription, request/trace id, provider-selection hints) and the model name lower-cased/trimmed to its canonical form (mirror the proxy's `canonicalizeModelName` intent — normalize aliases so record and replay agree), keeping `{ model, messages, temperature, max_tokens }` and any other content-affecting params. Implement `matchKey(normalized)`: `crypto.createHash('sha256').update(JSON.stringify(normalized)).digest('hex')` combined with a per-hash call ordinal from a module-level `Map<hash, number>` — return a composite key string `"<sha256>#<ordinal>"` and increment the counter for that hash. Implement `resetOrdinals()` to clear the Map (used at span open and in tests). `import crypto from 'node:crypto'`. No fenced code in the module beyond implementation; export normalizeReq, matchKey, resetOrdinals.
  </action>
  <verify>
    <automated>node --test tests/repro/match-key.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/match-key.test.mjs` passes (GREEN).
    - Requests differing only in a volatile field hash to the same sha256 base; the composite key differs only by ordinal for identical repeats.
    - `resetOrdinals()` restores ordinal 0 for a previously-seen request.
  </acceptance_criteria>
  <done>match-key.mjs implements D-07 and its suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: llm-record.mjs + llm-replay.mjs — append + lookup with hard-fail-on-miss (D-06)</name>
  <files>lib/repro/fixtures/llm-record.mjs, lib/repro/fixtures/llm-replay.mjs</files>
  <read_first>
    - _work/rapid-llm-proxy/proxy-bridge/server.mjs (best-effort append idiom `if (_tokenDb) { logTokenCall(...) }` ~:1861-1863; response contract ~:1957-1969)
    - lib/repro/fixtures/match-key.mjs (Task 2 — the shared key producer)
    - tests/repro/llm-record-replay.test.mjs (the round-trip + miss contract from Task 1)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"llm-record.mjs", §"llm-replay.mjs")
  </read_first>
  <action>
    `llm-record.mjs`: export `recordFixture(fixturesDir, key, resp)` — append/write a fixture file under `<fixturesDir>/llm/` keyed by `key`, storing exactly `{ content, provider, model, tokens, latencyMs }` (plus optional `overheadMs`). Wrap all I/O in try/catch and return silently on failure (best-effort — a fixture write must never break the caller, mirroring the `_tokenDb` best-effort contract). Use `node:fs` with `{ recursive: true }` mkdir. `llm-replay.mjs`: export `replayLookup(fixturesDir, key)` — read the fixture for `key` from `<fixturesDir>/llm/`; return the parsed response object on hit or `null` on miss (the caller — proxy tap in Plan 06 — turns null into the 409 REPLAY_MISS hard-fail per D-06; this module NEVER falls back to live). Both modules import the key producer from match-key.mjs so there is one hash implementation.
  </action>
  <verify>
    <automated>node --test tests/repro/llm-record-replay.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/llm-record-replay.test.mjs` passes: recorded response replays byte-identical (JSON.stringify equal) and an un-recorded key returns null.
    - A forced write failure (e.g. read-only dir) does not throw from `recordFixture` (best-effort proven in test or by inspection).
    - `replayLookup` never returns a live/synthesized response on miss — only null.
  </acceptance_criteria>
  <done>Record→replay round-trip is green including the miss→null hard-fail signal; both modules reuse match-key.mjs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recorded LLM traffic → fixture files on disk | Request messages + model responses (may contain prompt content / user data) are serialized to `fixtures/llm/` |
| Replay lookup → caller | A miss must not silently become a live response (comparability guarantee) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01-01 | Information Disclosure | fixture files contain prompt/response content | mitigate | Fixtures written only under the span/snapshot dir which Plan 03 adds to `.gitignore` (`.data/run-snapshots/`); this plan writes no fixtures to tracked paths and its tests use mkdtemp temp dirs only |
| T-67-01-02 | Repudiation / false comparability | `replayLookup` on miss | mitigate | `replayLookup` returns null on miss (never a live/synthesized response); the D-06 hard-fail (409) is enforced by the proxy caller in Plan 06; round-trip test asserts miss→null |
| T-67-01-03 | Tampering | match-key hash of normalized request | accept | sha256 via node:crypto (no key mgmt, ASVS V6 partial); collision risk negligible for run-local fixtures |
| T-67-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan or phase (RESEARCH §"Package Legitimacy Audit": all Node built-ins / existing repo modules) |
</threat_model>

<verification>
- `node --test tests/repro/match-key.test.mjs tests/repro/llm-record-replay.test.mjs` all green.
- `grep -rn "from 'node:test'" tests/repro/` confirms node:test convention (no jest).
- The three lib modules export exactly the documented functions; llm-replay/llm-record both import from match-key.mjs (single hash implementation).
</verification>

<success_criteria>
- D-07 match key is stable, volatile-field-robust, canonical-model-agnostic, and ordinal-ordered.
- Record→replay round-trip is byte-identical; a miss returns null (feeds D-06 hard-fail downstream).
- No live proxy daemon required for any test.
</success_criteria>

<output>
Create `.planning/phases/67-reproducibility-replay-rig/67-01-SUMMARY.md` when done
</output>
