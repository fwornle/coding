---
phase: 67-reproducibility-replay-rig
plan: 06
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - _work/rapid-llm-proxy/proxy-bridge/server.mjs
autonomous: true
requirements: [REPRO-02]

must_haves:
  truths:
    - "When the active span has meta.replay_from, the proxy serves the recorded LLM response by match key (D-07) and hard-fails 409 REPLAY_MISS on a miss — never falling through to a live provider (D-06)"
    - "When the active span has meta.record, the proxy appends each LLM response to the span's fixtures dir, best-effort (never breaking the real call)"
    - "The proxy uses the single getActiveMeasurement reader and the single match-key implementation from Plan 01 (no second span parser, no duplicate hash)"
  artifacts:
    - path: "_work/rapid-llm-proxy/proxy-bridge/server.mjs"
      provides: "replay tap after body-parse (~1698) + record tap where result is ready (~1853) in POST /api/complete"
      contains: "replay_from"
  key_links:
    - from: "_work/rapid-llm-proxy/proxy-bridge/server.mjs"
      to: "getActiveMeasurement (../dist/measurement-span.js)"
      via: "read span.meta.replay_from / span.meta.record off the active span"
      pattern: "getActiveMeasurement|replay_from"
    - from: "_work/rapid-llm-proxy/proxy-bridge/server.mjs"
      to: "lib/repro/fixtures/{match-key,llm-replay,llm-record}.mjs"
      via: "env-resolved dynamic import (single hash + record/replay implementation)"
      pattern: "match-key|llm-replay|llm-record"
---

<objective>
Wire the LLM record/replay taps into the live proxy bridge. Inside the existing `POST /api/complete` handler, add a replay tap (right after body parse, before provider resolution) that serves recorded responses by D-07 match key or hard-fails 409 on a miss (D-06), and a record tap (where `result` is ready) that appends the response to the span's fixtures dir best-effort. Reuse the single `getActiveMeasurement` reader and the single Plan 01 match/record/replay implementation via an env-resolved dynamic import.

Purpose: REPRO-02's LLM channel — the only channel with a real in-repo chokepoint. This is the point where a replay run reads fixtures instead of hitting live providers.
Output: two edits to `_work/rapid-llm-proxy/proxy-bridge/server.mjs` + daemon restart + health verification. NOTE: proxy is a separate repo; main is protected (push via PR branch per project rules — do not push in this plan, just edit the runtime file the daemon loads).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md
@.planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md
@.planning/phases/67-reproducibility-replay-rig/67-CONTEXT.md

<interfaces>
From lib/repro/fixtures/match-key.mjs (Plan 01): normalizeReq(body), matchKey(normalized) → "<sha256>#<ordinal>", resetOrdinals()
From lib/repro/fixtures/llm-replay.mjs (Plan 01): replayLookup(fixturesDir, key) → recorded {content,provider,model,tokens,latencyMs} | null
From lib/repro/fixtures/llm-record.mjs (Plan 01): recordFixture(fixturesDir, key, resp)
From _work/rapid-llm-proxy/src/measurement-span.ts: getActiveMeasurement() (:112) — null-safe single reader; already imported alias resolveLiveTaskId at server.mjs:44
Response contract (server.mjs:1957-1969): { content, provider, model, tokens:{input,output,total}, latencyMs, overheadMs? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert replay + record taps into POST /api/complete</name>
  <files>_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - _work/rapid-llm-proxy/proxy-bridge/server.mjs (:44 existing `import { resolveLiveTaskId } from '../dist/measurement-span.js'`; :1683 handler entry; :1684-1698 body parse; :1754 provider-chain resolution; :1813-1816 404 hard-fail return style; :1853 where `result` is ready; :1861-1863 best-effort logTokenCall append; :1957-1969 response contract)
    - lib/repro/fixtures/match-key.mjs, lib/repro/fixtures/llm-replay.mjs, lib/repro/fixtures/llm-record.mjs (Plan 01 — the functions to call)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"llm-replay.mjs + the proxy tap" — verified anchors + hard-fail return style + replay/record tap snippets)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Pattern 1" line anchors; §"Common Pitfalls" Pitfall 2 — edit the runtime proxy-bridge/server.mjs NOT src/dist; restart command)
    - memory/reference_llm_proxy_bridge_flap.md (runtime is proxy-bridge/server.mjs)
    - CLAUDE.md (proxy /api/complete on port 12435; llm-cli-proxy launchd label)
  </read_first>
  <action>
    Extend the existing `../dist/measurement-span.js` import to also bring in `getActiveMeasurement` (the full-span reader — do NOT add a second parser of active-measurement.json). Resolve the coding-repo repro modules once via an env-resolved absolute path (`process.env.LLM_PROXY_REPRO_DIR` default `/Users/Q284340/Agentic/coding/lib/repro`, mirroring the existing `../dist` local-import discipline and the CODING_REPO allowlist) and dynamic-import `fixtures/match-key.mjs`, `fixtures/llm-replay.mjs`, `fixtures/llm-record.mjs` — a single hash/record/replay implementation shared with the tests. REPLAY TAP: immediately after body parse (~1698) and before provider-chain resolution (1754), read `const span = getActiveMeasurement();` and if `span?.meta?.replay_from`: compute `key = matchKey(normalizeReq(body))`, `hit = replayLookup(<fixturesDir from span.meta.replay_from>, key)`; on hit `res.writeHead(200,{'Content-Type':'application/json'}); return res.end(JSON.stringify(hit));` on miss `res.writeHead(409,{'Content-Type':'application/json'}); return res.end(JSON.stringify({ error:'replay miss: '+key, type:'REPLAY_MISS' }));` (mirror the 404 return style — NEVER fall through to a provider; D-06). RECORD TAP: where `result` is ready (~1853), guarded by `span?.meta?.record`, call `recordFixture(<fixturesDir>, matchKey(normalizeReq(body)), { content:result.content, provider:providerName, model:result.model, tokens:result.tokens, latencyMs })` wrapped in try/catch (best-effort — mirror the `if (_tokenDb) logTokenCall` contract; a fixture write must never fail the LLM call). Resolve the fixtures dir from `span.meta.replay_from` (replay) / the active span's snapshot fixtures dir (record). Compute the match key ONCE per request (the ordinal counter must advance consistently). Keep all edits inside the `/api/complete` block.
  </action>
  <verify>
    <automated>node --check _work/rapid-llm-proxy/proxy-bridge/server.mjs && grep -c "replay_from" _work/rapid-llm-proxy/proxy-bridge/server.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check _work/rapid-llm-proxy/proxy-bridge/server.mjs` passes (valid syntax).
    - `grep -c "getActiveMeasurement" _work/rapid-llm-proxy/proxy-bridge/server.mjs` >= 1 and there is NO new hand-rolled `JSON.parse` of active-measurement.json (single reader preserved).
    - `grep -c "REPLAY_MISS" server.mjs` >= 1 and the miss path uses `res.writeHead(409` and returns — no fall-through to provider resolution (source assertion: the replay `return` precedes line 1754's provider chain).
    - The record tap is guarded by `span?.meta?.record` and wrapped in try/catch (best-effort).
    - match-key/llm-replay/llm-record are imported via the `LLM_PROXY_REPRO_DIR`-resolved path (single implementation; grep confirms the three imports).
  </acceptance_criteria>
  <done>Replay + record taps are wired into the live handler using the single span reader and the single Plan 01 fixture implementation; hard-fails 409 on miss.</done>
</task>

<task type="auto">
  <name>Task 2: Restart the proxy daemon and verify normal serving is unaffected</name>
  <files>_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - CLAUDE.md (llm-cli-proxy launchd label `com.coding.llm-cli-proxy`; proxy /api/complete on port 12435; Health API is port 3033 — do NOT confuse)
    - memory/reference_llm_proxy_bridge_flap.md (proxy log locations `.data/llm-proxy/logs/`; runtime is proxy-bridge/server.mjs)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Common Pitfalls" Pitfall 2 restart step)
  </read_first>
  <action>
    Restart the runtime so the edited file loads: `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`. Then confirm the daemon is healthy and normal (non-replay) traffic is unchanged: poll the proxy health endpoint on port 12435 and issue one ordinary `POST /api/complete` (with NO active replay span) and assert a normal `{content,provider,model,tokens,latencyMs}` response — i.e. when no span meta is armed, the taps are inert and behavior is byte-for-byte the prior contract. Check `.data/llm-proxy/logs/` for startup errors. Do NOT push the proxy repo (main is protected; edits to the runtime file suffice for the daemon; a PR is out of scope for this plan).
  </action>
  <verify>
    <automated>curl -s -m 8 -X POST http://localhost:12435/api/complete -H 'Content-Type: application/json' -d '{"process":"repro-smoke","messages":[{"role":"user","content":"ping"}]}' | grep -Eq '"content"|"provider"' && echo PROXY-OK</automated>
  </verify>
  <acceptance_criteria>
    - After `launchctl kickstart`, an ordinary `POST /api/complete` (no armed replay span) returns a normal response containing `content`/`provider` (taps inert when unarmed — no regression).
    - No startup error in `.data/llm-proxy/logs/` after restart (grep for the last-run error markers shows none from this restart).
    - `node --check` on the runtime file still passes.
  </acceptance_criteria>
  <done>The edited proxy runtime is loaded, healthy, and serves normal traffic unchanged when replay/record are not armed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → proxy /api/complete | untrusted request body crosses into the record/replay taps |
| replay fixtures on disk → served response | recorded responses substituted for live provider output |
| active span meta → tap behavior | span file drives whether record/replay is armed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06-01 | Repudiation / false comparability | replay miss silently hitting a live provider | mitigate | On miss the replay tap `res.writeHead(409)` + `return` BEFORE provider resolution (1754) — never falls through (D-06); asserted via source ordering |
| T-67-06-02 | Information Disclosure | recorded fixtures contain prompt + response content | mitigate | Fixtures live under the gitignored span/snapshot dir (Plan 03/04); record is best-effort and writes only there |
| T-67-06-03 | Tampering | match key computed inconsistently between record and replay | mitigate | Single match-key implementation imported from Plan 01 via LLM_PROXY_REPRO_DIR (no duplicate hash); ordinal advances once per request |
| T-67-06-04 | Denial of Service | a fixture write/read error breaking the live LLM call | mitigate | Record tap try/catch best-effort (mirrors `if (_tokenDb) logTokenCall`); replay only active when span.meta.replay_from set — inert otherwise (no-regression smoke) |
| T-67-06-05 | Tampering | editing the wrong proxy file (src/dist) | mitigate | Edit the runtime `proxy-bridge/server.mjs` only (Pitfall 2) + `launchctl kickstart` restart + health smoke |
| T-67-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (RESEARCH Package Legitimacy Audit: N/A); no new proxy deps |
</threat_model>

<verification>
- `node --check _work/rapid-llm-proxy/proxy-bridge/server.mjs` passes.
- Unarmed `POST /api/complete` returns a normal response post-restart (taps inert).
- Replay miss path returns 409 REPLAY_MISS before provider resolution (source ordering assertion); record tap best-effort guarded.
</verification>

<success_criteria>
- REPRO-02 LLM channel: recorded responses served by match key; misses hard-fail 409; record best-effort — all via the single span reader + single Plan 01 implementation.
- No regression to normal proxy serving when record/replay is unarmed.
</success_criteria>

<output>
Create `.planning/phases/67-reproducibility-replay-rig/67-06-SUMMARY.md` when done
</output>
