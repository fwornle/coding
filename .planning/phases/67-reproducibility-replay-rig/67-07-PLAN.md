---
phase: 67-reproducibility-replay-rig
plan: 07
type: execute
wave: 4
depends_on: [67-01, 67-02, 67-04, 67-05, 67-06]
files_modified:
  - scripts/measurement-start.mjs
  - scripts/measurement-stop.mjs
  - lib/experiments/run-write.mjs
  - tests/repro/run-link.test.mjs
autonomous: false
requirements: [REPRO-01, REPRO-02]

must_haves:
  truths:
    - "Opening a measurement span auto-captures a RunSnapshot and (if --replay <snapshot>) arms replay via span.meta, reusing active-measurement.json (no schema change, no second reader) (D-09)"
    - "At span open, unsupported replay channels (WebSearch/WebFetch/MCP) are surfaced to the operator (honest gating)"
    - "At span close, fixtures are archived into the snapshot dir and the Run entity carries snapshot_id (run-write.mjs:108 populated)"
    - "A full recorded run can be replayed so LLM responses come from fixtures (live end-to-end verified)"
  artifacts:
    - path: "scripts/measurement-start.mjs"
      provides: "captureSnapshot call + --replay arming via startMeasurement meta + clock_base + unsupported-channel notice"
      contains: "captureSnapshot"
    - path: "scripts/measurement-stop.mjs"
      provides: "fixture archival into snapshot dir + snapshot_id threaded into the writeRun tags"
      contains: "snapshot_id"
    - path: "lib/experiments/run-write.mjs"
      provides: "snapshot_id populated from tags (was hardcoded null at :108)"
      contains: "snapshot_id"
    - path: "tests/repro/run-link.test.mjs"
      provides: "isolated-store test asserting writeRun persists snapshot_id on the Run"
  key_links:
    - from: "scripts/measurement-start.mjs"
      to: "startMeasurement({ meta:{ record, replay_from } })"
      via: "meta passthrough (measurement-span.ts:173-191) — no schema change"
      pattern: "meta.*record|replay_from"
    - from: "scripts/measurement-stop.mjs"
      to: "lib/experiments/run-write.mjs snapshot_id"
      via: "tags.snapshot_id threaded through writeRun (measurement-stop.mjs:367-377)"
      pattern: "snapshot_id"
---

<objective>
Integrate the rig into the Phase 68 measurement span (D-09): `measurement-start.mjs` auto-captures a RunSnapshot and arms record (and, with `--replay <snapshot>`, replay) via the existing `startMeasurement` `meta` passthrough — reusing `active-measurement.json`, no schema change, no second reader — plus a `clock_base` and an at-open notice that WebSearch/WebFetch/MCP replay is unsupported. `measurement-stop.mjs` archives fixtures into the snapshot dir and threads `snapshot_id` into `writeRun`, and `run-write.mjs:108` stops hardcoding `snapshot_id: null`. A final live end-to-end checkpoint proves a recorded run replays from fixtures.

Purpose: REPRO-01 + REPRO-02 — one workflow: capture at open, record during, archive + Run-link at close, replay via a flag. The seam that makes N=1 runs comparable.
Output: three edits + one node:test + a live human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md
@.planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md
@.planning/phases/67-reproducibility-replay-rig/67-CONTEXT.md

<interfaces>
From lib/repro/capture-snapshot.mjs (Plan 04): captureSnapshot(task_id, { repoRoot, dataDir, prompt }) → { snapshot_id, dir, clock_base }
From lib/repro/fixtures/harness-record.mjs (Plan 02): recordHarnessFixtures(opts); replayHarnessChannel(name) throws REPLAY_UNSUPPORTED_CHANNEL
From lib/repro/fixtures/clock.mjs (Plan 02): installClock(clockBase)
From _work/rapid-llm-proxy/src/measurement-span.ts: startMeasurement({ task_id, goal_sentence?, meta? }) (meta persisted+read back unchanged, :173-191); getActiveMeasurement (:112); sanitizeTaskId (:83)
From lib/experiments/run-write.mjs: writeRun(store, { span, taskClass, pending, tags, totals, heuristics }); snapshot_id is null at :108 (this plan populates it — pattern of the other 8 tags at :102-109 = `t.<field> ?? null`)
From scripts/measurement-stop.mjs: the tags object built at :367-377; writeRun call at :394-398; openExperimentStore + close-in-finally
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: measurement-start.mjs — capture at open + --replay arming + clock_base + unsupported-channel notice</name>
  <files>scripts/measurement-start.mjs</files>
  <read_first>
    - scripts/measurement-start.mjs (:40-44 parseStrArg; :57-96 arg/main flow; :82-87 dist-import + startMeasurement call — extend, do not replace)
    - lib/repro/capture-snapshot.mjs (Plan 04 — captureSnapshot signature + clock_base return)
    - lib/repro/fixtures/harness-record.mjs (Plan 02 — the three unsupported channel names to surface)
    - _work/rapid-llm-proxy/src/measurement-span.ts (:173-191 startMeasurement meta passthrough; :83 sanitizeTaskId)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"measurement-start.mjs (EDIT)" — arming pattern + clock_base + sanitizeTaskId)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Open Questions" #2 meta arming; §"Harness Channels" surface-at-arm directive)
  </read_first>
  <action>
    In measurement-start.mjs, after the existing `startMeasurement()` call (~:82-87): parse `--replay <snapshot>` with the existing `parseStrArg` helper; build the meta object `meta: { record: true, ...(replayFrom ? { replay_from: <path to .data/run-snapshots/<replayFrom>/fixtures> } : {}) }` and pass it into `startMeasurement({ task_id, ...(goal ? { goal_sentence } : {}), meta })` (the meta is persisted into active-measurement.json and read back unchanged by the proxy — no schema change). Call `captureSnapshot(taskId, { repoRoot, dataDir, prompt })` to write the RunSnapshot at span open (pre-mutation baseline, Pattern 3) and fold its `clock_base` into the snapshot manifest (already handled by captureSnapshot). When `--replay` is set, print a clear operator notice that WebSearch/WebFetch/MCP replay is UNSUPPORTED for this run (call/reference `replayHarnessChannel` semantics or list the channel names) and that only the LLM channel + clock replay faithfully — honesty at arm time (D-08/SC-4). Use `sanitizeTaskId` for the snapshot dir. Log via process.stderr.write. Keep capture best-effort so a capture failure does not abort the measurement.
  </action>
  <verify>
    <automated>node --check scripts/measurement-start.mjs && grep -Eq "captureSnapshot" scripts/measurement-start.mjs && grep -Eq "replay_from|--replay" scripts/measurement-start.mjs && echo START-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/measurement-start.mjs` passes.
    - `grep -c "captureSnapshot" scripts/measurement-start.mjs` >= 1 and the call occurs after `startMeasurement`.
    - `startMeasurement` is invoked with a `meta` object carrying `record` (and `replay_from` when `--replay` is passed); no new second parser of active-measurement.json is added (single reader preserved).
    - When `--replay` is passed the script emits an UNSUPPORTED-channel notice naming WebSearch/WebFetch/MCP.
    - Snapshot dir uses `sanitizeTaskId`.
  </acceptance_criteria>
  <done>Span open captures a snapshot, arms record/replay via meta, records clock_base, and honestly surfaces unsupported replay channels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: measurement-stop.mjs fixture archive + snapshot_id linkage (run-write.mjs:108)</name>
  <files>scripts/measurement-stop.mjs, lib/experiments/run-write.mjs, tests/repro/run-link.test.mjs</files>
  <read_first>
    - scripts/measurement-stop.mjs (:367-377 tags object; :394-398 writeRun call + openExperimentStore/close-in-finally; :364-365 hash idiom)
    - lib/experiments/run-write.mjs (:100-113 the 8-tags block; :108 `snapshot_id: null // deferred → Phase 67` — the field to populate; pattern `t.<field> ?? null`)
    - lib/repro/capture-snapshot.mjs (Plan 04 — snapshot dir path + fixtures location to archive)
    - tests/experiments/run-write.test.mjs (:16-18 env-gated; :38-58 isolated mkdtemp store helper — copy REAL .data/ontologies-experiment/ in, openExperimentStore({ repoRoot: tmp }), close+rm in cleanup)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"measurement-stop.mjs (EDIT)" — thread snapshot_id; run-write.mjs:108 change)
  </read_first>
  <behavior>
    - run-write.mjs: `snapshot_id` reads `t.snapshot_id ?? null` (like the other 8 tags), no longer hardcoded null.
    - run-link.test.mjs (isolated store): writeRun with a tags object containing `snapshot_id: 'snap-abc'` persists a Run whose snapshot_id === 'snap-abc'; writeRun with no snapshot_id yields null (backward compatible).
    - measurement-stop.mjs: at close, when a snapshot dir exists for the span's task_id, archive the span's `fixtures/` (llm + harness) into that snapshot dir and set `snapshot_id` in the tags object it passes to writeRun.
  </behavior>
  <action>
    Edit lib/experiments/run-write.mjs:108 to `snapshot_id: t.snapshot_id ?? null` (mirror the sibling tags at :102-109). In measurement-stop.mjs: after `stopMeasurement()` archives the span, archive the recorded `fixtures/` dir into `.data/run-snapshots/<task_id>/fixtures/` (best-effort copy/move — LLM fixtures from the record tap + harness fixtures via `recordHarnessFixtures` for the span window), then add `snapshot_id: <the .data/run-snapshots/<task_id> id>` to the tags object built at :367-377 so `writeRun` persists it. Keep all additions best-effort (try/catch, stderr log) so a fixture-archive failure never breaks span close. Write tests/repro/run-link.test.mjs using the isolated-store helper from run-write.test.mjs (mkdtemp repoRoot, copy real `.data/ontologies-experiment/`, `openExperimentStore({ repoRoot: tmp })`, close+rm in cleanup — NEVER the real store; gate any live-only bits on an ENV VAR not `--live` argv) asserting snapshot_id round-trips through writeRun.
  </action>
  <verify>
    <automated>node --test tests/repro/run-link.test.mjs && node --check scripts/measurement-stop.mjs && grep -q "t.snapshot_id" lib/experiments/run-write.mjs && echo LINK-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/run-link.test.mjs` passes: a Run written with `tags.snapshot_id='snap-abc'` reads back snapshot_id === 'snap-abc'; without it, null.
    - `grep -q "snapshot_id: t.snapshot_id ?? null" lib/experiments/run-write.mjs` (no longer hardcoded null at :108).
    - `grep -c "snapshot_id" scripts/measurement-stop.mjs` >= 1 (threaded into the tags object).
    - `node --check scripts/measurement-stop.mjs` passes; fixture archival is best-effort (try/catch present).
  </acceptance_criteria>
  <done>Fixtures are archived into the snapshot dir at close and the Run entity carries snapshot_id; the reserved field is populated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full record→replay loop wired through the measurement span: `measurement-start.mjs` captures a snapshot + arms record; the proxy tap (Plan 06) records LLM responses to fixtures; `measurement-stop.mjs` archives fixtures + links snapshot_id; and `measurement-start.mjs --replay <snapshot>` serves recorded LLM responses (miss → 409). Automated node:test suites for all pure functions and the round-trip already pass; this checkpoint verifies the LIVE end-to-end against the running proxy daemon.
  </what-built>
  <how-to-verify>
    1. Ensure the proxy daemon is running the Plan-06 edit (`launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`).
    2. RECORD: run `node scripts/measurement-start.mjs --task-id repro-e2e --goal "smoke"`; confirm `.data/run-snapshots/repro-e2e/` was created with git/kb/env/manifest artifacts. Make 1-2 real LLM calls through the proxy (e.g. `curl -s -X POST http://localhost:12435/api/complete -H 'Content-Type: application/json' -d '{"process":"repro-e2e","messages":[{"role":"user","content":"say hello"}]}'`). Run `node scripts/measurement-stop.mjs --task-id repro-e2e`; confirm `fixtures/llm/` was archived into the snapshot dir and the Run has a snapshot_id.
    3. REPLAY: run `node scripts/measurement-start.mjs --task-id repro-e2e-replay --replay repro-e2e`; confirm the UNSUPPORTED-channel notice prints for WebSearch/WebFetch/MCP. Re-issue the SAME curl call; confirm the response is byte-identical to the recorded one (served from fixture, not a fresh live call — check no new provider latency / identical content).
    4. MISS: issue a curl with a DIFFERENT prompt while replay is armed; confirm HTTP 409 `REPLAY_MISS` (hard-fail, not a live response).
    5. SAFETY: run `node scripts/repro-restore.mjs --snapshot repro-e2e`; confirm it creates an isolated worktree + sandbox data dir and the live checkout HEAD is unchanged (`git rev-parse HEAD` identical before/after).
    6. Stop the replay span (`node scripts/measurement-stop.mjs --task-id repro-e2e-replay`) and confirm normal proxy traffic works again.
  </how-to-verify>
  <resume-signal>Type "approved" if record→replay serves recorded responses + miss hard-fails 409 + default restore never touches live; else describe the divergence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (--replay, --task-id) → span meta | untrusted operator input drives arming + snapshot dir |
| span meta (active-measurement.json) → proxy behavior | arming state read by the proxy tap |
| fixtures + snapshot dir → Run entity | archived artifacts linked into the KB via snapshot_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-07-01 | Tampering / path traversal | --task-id / --replay → snapshot dir path | mitigate | `sanitizeTaskId` on all snapshot paths; snapshot dir resolved under `.data/run-snapshots/` only |
| T-67-07-02 | Repudiation / false comparability | replay arming without honest channel gating | mitigate | At-open UNSUPPORTED notice for WebSearch/WebFetch/MCP; LLM miss → 409 (Plan 06); no silent live mixing (D-06/SC-4) |
| T-67-07-03 | Information Disclosure | archived fixtures (prompt/response/tool content) | mitigate | Archived only into the gitignored `.data/run-snapshots/<task_id>/` (Plan 03); best-effort, no tracked-path writes |
| T-67-07-04 | Tampering | second reader of active-measurement.json | mitigate | Reuse startMeasurement meta passthrough + getActiveMeasurement single reader; no new JSON parser (grep) |
| T-67-07-05 | Denial of Service | capture/archive failure aborting measurement | mitigate | Capture + fixture-archive are best-effort try/catch; span start/stop proceed regardless |
| T-67-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node --test tests/repro/run-link.test.mjs` green; `node --check` on both scripts passes.
- run-write.mjs:108 populated from tags; snapshot_id round-trips through writeRun.
- Live checkpoint: record→replay serves recorded LLM responses; miss → 409; default restore leaves live HEAD unchanged.
- Full suite gate: `node --test tests/repro/` green and `npm test` (jest src suite) unaffected.
</verification>

<success_criteria>
- D-09: one workflow — capture at span open, record during, archive + snapshot_id at close, replay via `--replay`.
- REPRO-01 + REPRO-02 fully wired end-to-end and live-verified; N=1 runs are comparable modulo provider non-determinism.
</success_criteria>

<output>
Create `.planning/phases/67-reproducibility-replay-rig/67-07-SUMMARY.md` when done
</output>
