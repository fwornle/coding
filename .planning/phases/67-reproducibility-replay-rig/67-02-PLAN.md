---
phase: 67-reproducibility-replay-rig
plan: 02
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - lib/repro/fixtures/clock.mjs
  - lib/repro/fixtures/harness-record.mjs
  - tests/repro/clock.test.mjs
  - tests/repro/harness-stub.test.mjs
autonomous: true
requirements: [REPRO-02]

must_haves:
  truths:
    - "On replay, the clock returns a deterministic monotonic value derived from the frozen snapshot base (D-08 clock)"
    - "WebSearch/WebFetch/MCP tool results within the span window are recorded from the Claude transcript JSONL"
    - "Replay of a harness channel with no viable tap hard-fails REPLAY_UNSUPPORTED_CHANNEL (never silent degradation) (D-06/D-08/SC-4)"
  artifacts:
    - path: "lib/repro/fixtures/clock.mjs"
      provides: "freeze-base + monotonic-offset Date shim for the replay run's own node entrypoints only"
      exports: ["installClock", "clockBase"]
    - path: "lib/repro/fixtures/harness-record.mjs"
      provides: "record() transcript scraper (real) + replay() honest hard-fail stub"
      exports: ["recordHarnessFixtures", "replayHarnessChannel"]
    - path: "tests/repro/harness-stub.test.mjs"
      provides: "asserts replay throws REPLAY_UNSUPPORTED_CHANNEL and record extracts tool_result within window"
  key_links:
    - from: "lib/repro/fixtures/harness-record.mjs"
      to: "~/.claude/projects/*.jsonl transcript"
      via: "env-override LSL_CLAUDE_PROJECTS_DIR → home default, ISO-8601 window filter"
      pattern: "LSL_CLAUDE_PROJECTS_DIR|tool_result|toolUseResult"
---

<objective>
Ship the two remaining REPRO-02 channels that have no proxy chokepoint: the deterministic clock shim (Claude's-discretion mechanism = freeze-at-snapshot base + monotonic offset) and the harness channel (WebSearch/WebFetch/MCP) as honest record-present / replay-hard-fails per RESEARCH's verified feasibility verdict.

Purpose: Honor D-08's "all five channels ship" while being honest about D-06/SC-4 — record what is recordable (transcript tool results), and hard-fail replay of channels the harness cannot be fed fixtures for, rather than silently hitting live services.
Output: `lib/repro/fixtures/{clock,harness-record}.mjs` + two node:test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md
@.planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md
@.planning/phases/67-reproducibility-replay-rig/67-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: clock.mjs — deterministic freeze-base + monotonic-offset Date shim</name>
  <files>lib/repro/fixtures/clock.mjs, tests/repro/clock.test.mjs</files>
  <read_first>
    - _work/rapid-llm-proxy/src/measurement-span.ts (:112-161 — small null-safe module that reads Date; shim shape analog)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"clock.mjs" — freeze-at-snapshot base + monotonic offset; NOT injected into daemons)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Clock Channel (D-08 + Claude's discretion)")
    - tests/experiments/consequential-events.test.mjs (node:test header)
  </read_first>
  <behavior>
    - installClock(clockBase) overrides Date.now / new Date() to return `clockBase + (performance.now() - replayStart)`; successive Date.now() calls are strictly non-decreasing (monotonic).
    - The first Date.now() after install is >= clockBase and within a small epsilon of clockBase.
    - installClock returns an uninstall handle that restores the native Date.now / Date constructor.
    - Determinism: given a fixed clockBase and a stubbed performance.now sequence, Date.now() output is exactly reproducible.
  </behavior>
  <action>
    Implement `installClock(clockBase)`: capture `replayStart = performance.now()`, save the native `Date.now` and `Date` constructor, then override `Date.now` to return `clockBase + Math.round(performance.now() - replayStart)` and make `new Date()` (no-arg) use the shimmed now while leaving parametrized `new Date(x)` native. Return `{ uninstall() }` restoring natives. Export `installClock` and `clockBase` (the last-installed base, for inspection). This shim is loaded ONLY by the replay run's own node entrypoints — document in a header comment that it MUST NOT be imported by long-running daemons (obs-api, proxy) per RESEARCH. Write clock.test.mjs with an injectable/stubbed performance.now to prove determinism + monotonicity without real sleeps.
  </action>
  <verify>
    <automated>node --test tests/repro/clock.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/clock.test.mjs` passes.
    - With a stubbed performance.now, Date.now() outputs are reproducible and strictly non-decreasing.
    - uninstall() restores the native Date.now (a post-uninstall Date.now differs from the frozen base).
    - Header comment states the shim is not for daemons.
  </acceptance_criteria>
  <done>Deterministic monotonic clock shim implemented and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: harness-record.mjs — transcript record() + honest replay() hard-fail</name>
  <files>lib/repro/fixtures/harness-record.mjs, tests/repro/harness-stub.test.mjs</files>
  <read_first>
    - lib/lsl/route/build-trace.mjs (:22-28 env-override→home default; :30-35 inclusive lexical ISO-8601 window compare; :41-51 JSONL line read)
    - tests/repro/_fixtures/transcript-fragment.jsonl (shared fixture created in Plan 01 Task 1)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"harness-record.mjs")
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Harness Channels" feasibility verdict; Assumption A1, A5)
  </read_first>
  <behavior>
    - recordHarnessFixtures({ transcriptDir, startedAt, endedAt, outDir }) scans transcript JSONL for tool_use/tool_result pairs whose tool name is WebSearch/WebFetch or an MCP tool, keeps only those whose timestamp is within [startedAt, endedAt] (inclusive lexical ISO-8601 compare), and writes them to `<outDir>/harness/`.
    - Against the shared transcript-fragment.jsonl fixture with a window bracketing its entries, at least one WebSearch tool_result fixture is written; with a window excluding them, zero are written.
    - replayHarnessChannel(name) throws an Error whose message contains `REPLAY_UNSUPPORTED_CHANNEL: <name>` for name in {WebSearch, WebFetch, MCP} — it NEVER returns a fabricated result.
  </behavior>
  <action>
    Implement `recordHarnessFixtures(opts)` mirroring build-trace.mjs's transcript reader contract: resolve the transcript dir from `LSL_CLAUDE_PROJECTS_DIR` with a `~/.claude/projects/-Users-Q284340-Agentic-coding` home default; read `*.jsonl` line-by-line; select `tool_use`/`tool_result` (a.k.a. `toolUseResult`) blocks for WebSearch/WebFetch/MCP tools within the span `[started_at, ended_at]` window via inclusive lexical ISO-8601 compare; write the matched pairs to `<outDir>/harness/`. Wrap in try/catch, best-effort (never throw on the hot path). Implement `replayHarnessChannel(name)` to throw `REPLAY_UNSUPPORTED_CHANNEL: ${name}` — the honest hard-fail per D-06/D-08/SC-4 (harness cannot be fed fixtures in-repo, RESEARCH Assumption A1). Add a header comment documenting: record is real (post-hoc transcript scrape), replay is unsupported by design, surfaced at span-open by the integration layer (Plan 07).
  </action>
  <verify>
    <automated>node --test tests/repro/harness-stub.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/harness-stub.test.mjs` passes.
    - Recording against the shared fixture with a bracketing window writes >=1 harness fixture; an excluding window writes 0.
    - `replayHarnessChannel('WebSearch')` throws with message matching `/REPLAY_UNSUPPORTED_CHANNEL: WebSearch/`; same for WebFetch and MCP.
    - `replayHarnessChannel` never returns a value (only throws) for the three channels.
  </acceptance_criteria>
  <done>Harness record works from the transcript; replay hard-fails honestly for all three harness channels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude transcript JSONL → harness fixture files | tool_result content may contain fetched web content, prompt text, or PII |
| Replay call for a harness channel → caller | must hard-fail, never fabricate/hit-live (comparability) |
| Date shim → process globals | overriding Date must be scoped to the replay run, never leak into daemons |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-02-01 | Information Disclosure | harness fixtures (tool_result content / PII) | mitigate | Written only under the span/snapshot dir (`.data/run-snapshots/`, gitignored by Plan 03); record best-effort; no fixtures to tracked paths; tests use mkdtemp only |
| T-67-02-02 | Repudiation / false comparability | harness replay | mitigate | `replayHarnessChannel` throws REPLAY_UNSUPPORTED_CHANNEL — never a fabricated or live result; asserted in test |
| T-67-02-03 | Tampering | global Date override | mitigate | Clock shim installed only in the replay run's own entrypoints, header-documented as daemon-forbidden; uninstall handle restores natives (test-verified) |
| T-67-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (RESEARCH Package Legitimacy Audit: N/A — Node built-ins + existing repo modules only) |
</threat_model>

<verification>
- `node --test tests/repro/clock.test.mjs tests/repro/harness-stub.test.mjs` green.
- `grep -n "REPLAY_UNSUPPORTED_CHANNEL" lib/repro/fixtures/harness-record.mjs` present.
- `grep -n "daemon" lib/repro/fixtures/clock.mjs` (header warning present).
</verification>

<success_criteria>
- Clock replay is deterministic + monotonic from a frozen base, scoped to replay entrypoints.
- Harness channels record from the transcript and hard-fail replay honestly (D-08 five-channels honesty requirement satisfied).
</success_criteria>

<output>
Create `.planning/phases/67-reproducibility-replay-rig/67-02-SUMMARY.md` when done
</output>
