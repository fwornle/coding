---
phase: 67-reproducibility-replay-rig
plan: 04
type: execute
wave: 2
depends_on: [67-03]
files_modified:
  - lib/repro/kb-capture.mjs
  - lib/repro/capture-snapshot.mjs
  - tests/repro/kb-capture.test.mjs
  - tests/repro/capture-snapshot.test.mjs
autonomous: true
requirements: [REPRO-01]

must_haves:
  truths:
    - "The live KB is captured as a byte-exact directory artifact (leveldb tar/copy) AND the atomic JSON export, without opening a second store on the live DB (D-02)"
    - "captureSnapshot(task_id) assembles all SC-1 items into .data/run-snapshots/<task_id>/ with a manifest and a clock_base"
    - "The snapshot dir path is task_id-sanitized (no path traversal)"
  artifacts:
    - path: "lib/repro/kb-capture.mjs"
      provides: "captureKb(dataDir, destDir) → leveldb artifact + exports/general.json copy; hydrateSandbox(exportPath, sandboxDir) helper"
      exports: ["captureKb", "hydrateSandbox"]
    - path: "lib/repro/capture-snapshot.mjs"
      provides: "captureSnapshot(task_id, opts) → writes the full RunSnapshot dir + manifest.json; returns snapshot_id + clock_base"
      exports: ["captureSnapshot"]
    - path: "tests/repro/capture-snapshot.test.mjs"
      provides: "asserts all SC-1 items land in the snapshot dir (git/env/config/mcp/version/kb/planning/prompt/clock_base)"
  key_links:
    - from: "lib/repro/capture-snapshot.mjs"
      to: "lib/repro/{git-state,env-allowlist,mcp-inventory,kb-capture}.mjs"
      via: "compose the Plan 03 primitives + kb-capture into one snapshot dir"
      pattern: "captureGitState|captureEnvAllowlist|captureMcpInventory|captureKb"
    - from: "lib/repro/capture-snapshot.mjs"
      to: "sanitizeTaskId (measurement-span)"
      via: ".data/run-snapshots/<sanitized task_id>/"
      pattern: "sanitizeTaskId"
---

<objective>
Capture the KB (D-02: byte-exact leveldb artifact + atomic JSON export) by filesystem copy only — never a second store handle on the live single-owner DB — and assemble the full RunSnapshot: compose Plan 03's git/env/MCP primitives with KB capture, processOverrides config, `.planning/` tar, prompt text, agent+node version, and a `clock_base`, all under a `sanitizeTaskId`'d `.data/run-snapshots/<task_id>/` with a manifest.

Purpose: REPRO-01 / SC-1 — a single restorable RunSnapshot directory. Honors the km-core single-owner invariant (RESEARCH Pitfall 5) and the byte-exact caveat (Pitfall 1) honestly.
Output: `lib/repro/{kb-capture,capture-snapshot}.mjs` + two node:test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md
@.planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md
@.planning/phases/67-reproducibility-replay-rig/67-CONTEXT.md

<interfaces>
From lib/repro/git-state.mjs (Plan 03): captureGitState(repoRoot) → { sha, dirtyPatch, untracked[], submodules[] }
From lib/repro/env-allowlist.mjs (Plan 03): captureEnvAllowlist(env) → { ...allowlisted }
From lib/repro/mcp-inventory.mjs (Plan 03): captureMcpInventory() → { servers[], source }
From _work/rapid-llm-proxy/src/measurement-span.ts: sanitizeTaskId(id) (:83) — allows [A-Za-z0-9._-], path.basename'd; resolveMeasurementPaths() (:57) — LLM_PROXY_DATA_DIR → cwd/.data
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kb-capture.mjs — filesystem KB copy (D-02) + sandbox hydrate helper</name>
  <files>lib/repro/kb-capture.mjs, tests/repro/kb-capture.test.mjs</files>
  <read_first>
    - lib/experiments/store.mjs (:40-53 openExperimentStore — km-core store construction MUST pass ontologyDir; :43-51 dbPath/exportDir shape)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"kb-capture.mjs" — filesystem copy ONLY, never a second store; capture live leveldb/ subdir + exports/general.json; sandbox hydrate prefers JSON export)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Common Pitfalls" Pitfall 1 + Pitfall 5; §"Restore Safety" KB step; State-of-the-Art stale-dir warning — target `.data/knowledge-graph/leveldb/` NOT `level.db`/`leveldb.before-*`)
    - CLAUDE.md (km-core node_modules patch — hydrate prefers JSON export; ontologyDir requirement)
  </read_first>
  <behavior>
    - captureKb(dataDir, destDir): copies the LIVE `<dataDir>/knowledge-graph/leveldb/` dir to `<destDir>/kb/leveldb/` (byte-exact artifact) AND copies `<dataDir>/knowledge-graph/exports/general.json` to `<destDir>/kb/exports/general.json`; returns `{ levelDbCaptured:bool, exportCaptured:bool, caveat }` where caveat states the copy is consistent-at-span-open only.
    - captureKb NEVER opens a GraphKMStore on the live dir (grep: no `new GraphKMStore` / `openExperimentStore` against the live path in capture).
    - Given a temp dataDir with a fake leveldb/ dir + a general.json, both are copied into destDir with identical bytes (fs comparison).
    - hydrateSandbox(exportPath, sandboxDataDir): opens a fresh GraphKMStore pointed at the SANDBOX dir (dbPath/exportDir under sandboxDataDir) WITH ontologyDir set, restores from the JSON export, and closes it in a finally.
  </behavior>
  <action>
    Implement `captureKb(dataDir, destDir)`: use `fs.cpSync(src, dst, { recursive: true })` (or system tar) to copy the live `knowledge-graph/leveldb/` subdir → `<destDir>/kb/leveldb/` and copy `exports/general.json` → `<destDir>/kb/exports/general.json`. Best-effort per artifact; return status flags + a human-readable `caveat` string documenting Pitfall 1 (byte-exact only for this point-in-time artifact; consistent-at-span-open). Do NOT open any store on the live dir. Implement `hydrateSandbox(exportPath, sandboxDataDir)`: construct `new GraphKMStore({ dbPath: <sandbox>/…/leveldb, exportDir: <sandbox>/…/exports, ontologyDir: <repo>/.data/ontologies, ontologyStrict:false, debounceMs:0 })` (ontologyDir MANDATORY per CLAUDE.md else resolveEntities throws), open, restore from the JSON export, `await store.close()` in finally (single-owner). Write kb-capture.test.mjs using a synthetic temp dataDir (no real KB) asserting the copy fidelity + that no store is opened during capture.
  </action>
  <verify>
    <automated>node --test tests/repro/kb-capture.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/kb-capture.test.mjs` passes.
    - `grep -v '^\s*//' lib/repro/kb-capture.mjs | grep -A3 -i 'captureKb' | grep -qi 'GraphKMStore'` returns nothing (capture opens no store on the live dir).
    - Copied leveldb dir + general.json are byte-identical to the temp source (fs read comparison in test).
    - `hydrateSandbox` constructs GraphKMStore WITH an `ontologyDir` option (`grep -c 'ontologyDir' lib/repro/kb-capture.mjs` >= 1) and closes in a finally.
  </acceptance_criteria>
  <done>KB captured byte-exact by filesystem copy + JSON export; sandbox hydrate uses the JSON export with ontologyDir; single-owner invariant respected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture-snapshot.mjs — assemble the full RunSnapshot dir + manifest (SC-1)</name>
  <files>lib/repro/capture-snapshot.mjs, tests/repro/capture-snapshot.test.mjs</files>
  <read_first>
    - lib/repro/git-state.mjs, lib/repro/env-allowlist.mjs, lib/repro/mcp-inventory.mjs, lib/repro/kb-capture.mjs (Plan 03 + Task 1 — the primitives to compose)
    - _work/rapid-llm-proxy/src/measurement-span.ts (:83 sanitizeTaskId; :57 resolveMeasurementPaths / LLM_PROXY_DATA_DIR → cwd/.data)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Internal-State Capture" full item table — every SC-1 item + its mechanism; llm-settings.json path server.mjs:171-172; agent version via `claude --version`; .planning tar; prompt.txt)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"Shared Patterns" task_id→filename safety; Pattern 3 capture-at-open)
  </read_first>
  <behavior>
    - captureSnapshot(task_id, { repoRoot, dataDir, prompt }) creates `.data/run-snapshots/<sanitizeTaskId(task_id)>/` containing: git-sha.txt, dirty.patch, untracked/ (+ list), submodules.json, kb/leveldb/ + kb/exports/general.json, llm-settings.json (processOverrides copy), mcp-inventory.json, env-allowlist.json, agent-version.txt (claude --version + process.version), planning.tar (or planning/ copy of .planning/), prompt.txt, and manifest.json.
    - manifest.json records `{ snapshot_id, task_id, created_at, clock_base, git_sha, kb_caveat, channels: { llm:'record', WebSearch:'record-only', WebFetch:'record-only', MCP:'record-only', clock:'virtualized' } }`.
    - captureSnapshot returns `{ snapshot_id, dir, clock_base }`; snapshot_id equals the sanitized task_id (D-01 per-run dir).
    - A task_id containing path-traversal chars (`../`) is sanitized so the dir stays under `.data/run-snapshots/`.
    - Before copying llm-settings.json, verify it holds no secret-shaped fields (deny-regex) — if a key-shaped field is present, omit it and note in manifest.
  </behavior>
  <action>
    Implement `captureSnapshot(task_id, opts)`: resolve the snapshot dir as `<repoRoot>/.data/run-snapshots/<sanitizeTaskId(task_id)>/` (import sanitizeTaskId from the proxy dist per the single-reader dist-import pattern, or basename-guard defensively). Compose: `captureGitState(repoRoot)` → git-sha.txt/dirty.patch/untracked//submodules.json; `captureEnvAllowlist(process.env)` → env-allowlist.json; `captureMcpInventory()` → mcp-inventory.json; `captureKb(dataDir, dir)` → kb/; copy `<dataDir>/llm-proxy/llm-settings.json` → llm-settings.json (processOverrides — first check no secret fields per T-67-03-04); `claude --version` + `process.version` → agent-version.txt; tar/copy `<repoRoot>/.planning/` → planning.tar (or planning/); write `opts.prompt` → prompt.txt; record `clock_base = Date.now()` (span-open baseline, Pattern 3) into manifest.json along with the per-channel capability map. Each step best-effort (try/catch, stderr log via process.stderr.write, never throw the whole capture on one item). Return `{ snapshot_id, dir, clock_base }`. Write capture-snapshot.test.mjs against a temp repoRoot + temp dataDir (synthetic git repo + fake KB + fake llm-settings.json) asserting every SC-1 artifact exists and the manifest carries clock_base + channel map + the traversal-safe dir.
  </action>
  <verify>
    <automated>node --test tests/repro/capture-snapshot.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/capture-snapshot.test.mjs` passes.
    - After a test capture, the snapshot dir contains ALL of: git-sha.txt, dirty.patch, submodules.json, kb/exports/general.json, llm-settings.json, mcp-inventory.json, env-allowlist.json, agent-version.txt, prompt.txt, manifest.json, and a planning artifact (test asserts each).
    - manifest.json parses and has `clock_base` (number), `git_sha`, and a `channels` map with `llm:'record'` and `WebSearch:'record-only'`.
    - A `captureSnapshot('../evil', …)` call keeps the created dir under `.data/run-snapshots/` (traversal neutralized; `grep -c sanitizeTaskId lib/repro/capture-snapshot.mjs` >= 1 or a basename guard is present).
    - env-allowlist.json contains no key/token/secret-shaped values (spot-checked in test).
  </acceptance_criteria>
  <done>captureSnapshot assembles a complete, traversal-safe, secret-safe RunSnapshot dir with manifest + clock_base + honest per-channel capability map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live single-owner LevelDB → capture | must not be opened by a second reader |
| task_id → snapshot dir path | untrusted; path-traversal risk |
| llm-settings.json / .planning / prompt → snapshot dir | may carry routing config, prompts, planning text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-04-01 | Tampering / path traversal | `.data/run-snapshots/<task_id>/` | mitigate | `sanitizeTaskId` (allows [A-Za-z0-9._-], basename'd) on every snapshot path (same threat class as T-68-02-01); test with `../` input |
| T-67-04-02 | Denial of Service / corruption | opening a 2nd store on live LevelDB | mitigate | Capture by filesystem copy only; assertion that captureKb opens no GraphKMStore on the live dir (RESEARCH Pitfall 5) |
| T-67-04-03 | Information Disclosure | processOverrides/config + prompt + .planning captured | mitigate | Snapshot dir gitignored (Plan 03); llm-settings.json copied only after secret-field deny-regex check; env is allowlist-only |
| T-67-04-04 | Repudiation | over-promising byte-exact KB | mitigate | manifest `kb_caveat` states consistent-at-span-open only (RESEARCH Pitfall 1); JSON export is the canonical restore source |
| T-67-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node --test tests/repro/kb-capture.test.mjs tests/repro/capture-snapshot.test.mjs` green.
- capture opens no store on the live KB dir; snapshot dir is traversal-safe; manifest carries clock_base + channel map + kb_caveat.
</verification>

<success_criteria>
- SC-1: a single `.data/run-snapshots/<task_id>/` RunSnapshot holds every internal-state item.
- D-02 KB capture is filesystem-copy-only with an honest byte-exact caveat.
- Path and secret safety enforced.
</success_criteria>

<output>
Create `.planning/phases/67-reproducibility-replay-rig/67-04-SUMMARY.md` when done
</output>
