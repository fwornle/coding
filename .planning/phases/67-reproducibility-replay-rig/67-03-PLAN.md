---
phase: 67-reproducibility-replay-rig
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/repro/git-state.mjs
  - lib/repro/env-allowlist.mjs
  - lib/repro/mcp-inventory.mjs
  - tests/repro/git-state.test.mjs
  - tests/repro/env-allowlist.test.mjs
  - .gitignore
autonomous: true
requirements: [REPRO-01]

must_haves:
  truths:
    - "git SHA, a re-applyable binary dirty patch, the untracked-file list, and per-submodule dirty state are captured for the workspace (D-03)"
    - "Only allowlisted agent-affecting env vars are captured; secret-shaped vars are excluded (Claude's discretion + Security V9)"
    - "MCP server inventory + versions are enumerated at snapshot time (live claude mcp list with config fallback)"
    - "The snapshot output dir (.data/run-snapshots/) is gitignored so captured secrets/prompts never get committed"
  artifacts:
    - path: "lib/repro/git-state.mjs"
      provides: "captureGitState(repoRoot) → { sha, dirtyPatch, untracked[], submodules[] } via fixed-argv spawnSync git"
      exports: ["captureGitState"]
    - path: "lib/repro/env-allowlist.mjs"
      provides: "captureEnvAllowlist(env) → allowlisted vars only, deny-regex secrets"
      exports: ["captureEnvAllowlist", "ENV_ALLOWLIST", "SECRET_DENY_RE"]
    - path: "lib/repro/mcp-inventory.mjs"
      provides: "captureMcpInventory() → { servers:[{name,version}], source } (live | config-fallback)"
      exports: ["captureMcpInventory"]
  key_links:
    - from: "lib/repro/git-state.mjs"
      to: "git CLI (fixed argv)"
      via: "spawnSync('git', [...]) — never a shell string (injection-safe)"
      pattern: "spawnSync\\('git'"
    - from: "lib/repro/env-allowlist.mjs"
      to: "SECRET_DENY_RE"
      via: "deny KEY/TOKEN/SECRET/PASSWORD before allowlisting"
      pattern: "KEY|TOKEN|SECRET|PASSWORD"
---

<objective>
Build the three internal-state capture primitives that Plan 04's snapshot assembler composes: git workspace state (D-03), the agent-affecting env-var allowlist (secret-safe), and the MCP inventory. Also add the `.data/run-snapshots/` gitignore entry so snapshots (which contain prompts, KB, and env) are never committed.

Purpose: REPRO-01's internal-state capture, built as independently testable units with the injection-safe fixed-argv git idiom and a strict secrets deny-regex.
Output: `lib/repro/{git-state,env-allowlist,mcp-inventory}.mjs`, two node:test suites, and a `.gitignore` entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md
@.planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md
@.planning/phases/67-reproducibility-replay-rig/67-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: git-state.mjs — SHA + binary dirty patch + untracked + submodule state (D-03)</name>
  <files>lib/repro/git-state.mjs, tests/repro/git-state.test.mjs</files>
  <read_first>
    - lib/experiments/evidence-harness.mjs (:23 spawnSync import; :150-166 readDiffStat — the ONLY fixed-argv git idiom; copy this shape exactly)
    - .planning/phases/67-reproducibility-replay-rig/67-PATTERNS.md (§"git-state.mjs" — full command list for D-03)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Internal-State Capture" git rows; §"Restore Safety" submodule caveat; Assumption A2)
    - tests/experiments/consequential-events.test.mjs (node:test header)
  </read_first>
  <behavior>
    - captureGitState(repoRoot) returns `{ sha, dirtyPatch, untracked, submodules }` where sha = `git rev-parse HEAD`, dirtyPatch = `git diff HEAD --binary` output (string, re-applyable, covers staged+unstaged, binary-safe), untracked = array from `git ls-files --others --exclude-standard`, submodules = array of `{ path, sha, dirtyPatch }` from `git submodule status` + per-submodule `git -C <path> diff --binary`.
    - Against a temp git repo (mkdtemp + git init + a committed file + an uncommitted edit + an untracked file), captureGitState returns the correct sha (40 hex), a non-empty dirtyPatch containing the edit, and the untracked file in `untracked`.
    - Every git call uses a fixed argv array (no shell string) and tolerates a repo with zero submodules (empty array, no throw).
  </behavior>
  <action>
    Implement `captureGitState(repoRoot)` replicating evidence-harness.mjs:150-166's `spawnSync('git', [...], { cwd, encoding:'utf8', timeout })` shape for each command: `['rev-parse','HEAD']`, `['diff','HEAD','--binary']`, `['ls-files','--others','--exclude-standard']`, `['submodule','status']`; for each submodule path run `git -C <path> diff --binary` (via `['-C', path, 'diff','--binary']`). Never build a shell string. Return `{ sha, dirtyPatch, untracked:[], submodules:[{path,sha,dirtyPatch}] }`. Guard each call (res.error/status/typeof stdout) and degrade to null/empty rather than throwing — capture must be best-effort. Write git-state.test.mjs that builds a throwaway git repo under mkdtempSync (git init, commit, dirty edit, untracked file), asserts the returned shape, and cleans up.
  </action>
  <verify>
    <automated>node --test tests/repro/git-state.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/git-state.test.mjs` passes.
    - `grep -c "spawnSync('git'" lib/repro/git-state.mjs` >= 4 (fixed-argv per command); `grep -v '^#' lib/repro/git-state.mjs | grep -qi 'exec(' ` returns nothing (no shell exec).
    - Returned `sha` matches `/^[0-9a-f]{40}$/`; `dirtyPatch` contains the temp-repo edit; `untracked` contains the temp-repo untracked file.
    - A repo with no submodules yields `submodules: []` without throwing.
  </acceptance_criteria>
  <done>git-state.mjs captures full D-03 workspace state via injection-safe fixed-argv git; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: env-allowlist.mjs + mcp-inventory.mjs + .gitignore entry</name>
  <files>lib/repro/env-allowlist.mjs, lib/repro/mcp-inventory.mjs, tests/repro/env-allowlist.test.mjs, .gitignore</files>
  <read_first>
    - scripts/backfill-raw-observations.mjs (:38-42 env resolution idiom)
    - lib/experiments/evidence-harness.mjs (:150-166 spawnSync CLI-capture shape — reused for `claude mcp list` / `claude --version`)
    - .planning/phases/67-reproducibility-replay-rig/67-RESEARCH.md (§"Internal-State Capture" env-allowlist row — the recommended allowlist; MCP inventory row; Assumption A3; §"Security Domain" V9 deny-regex)
    - .gitignore (current .data/ patterns around lines 186-192 — add the new entry adjacent to `.data/measurements/`)
  </read_first>
  <behavior>
    - captureEnvAllowlist(env) returns an object containing ONLY keys in ENV_ALLOWLIST that are present in env, and NEVER any key matching SECRET_DENY_RE (`/KEY|TOKEN|SECRET|PASSWORD/i`) even if such a key were mistakenly added to the allowlist.
    - Given an env with LLM_PROXY_DATA_DIR set and an AWS_SECRET_ACCESS_KEY set, output includes LLM_PROXY_DATA_DIR and excludes the secret.
    - captureMcpInventory() returns `{ servers, source }` where source is 'live' (parsed from `claude mcp list`) or 'config' (fallback) and never throws (returns `{ servers:[], source:'unavailable' }` if both fail).
  </behavior>
  <action>
    `env-allowlist.mjs`: define `ENV_ALLOWLIST` per RESEARCH (LLM_PROXY_DATA_DIR, LLM_PROXY_DIST_DIR, LLM_PROXY_SETTINGS_PATH, LLM_PROXY_TOKEN_DB_PATH, LLM_PROXY_PORT, LLM_CLI_PROXY_URL, RAPID_LLM_PROXY_URL, LLM_PROXY_URL, HTTPS_PROXY, HTTP_PROXY, NO_PROXY, CODING_REPO, CI, LSL_CLAUDE_PROJECTS_DIR, NODE_OPTIONS); define `SECRET_DENY_RE = /KEY|TOKEN|SECRET|PASSWORD/i`; implement `captureEnvAllowlist(env = process.env)` returning only allowlisted, non-secret-matching keys (deny-regex applied as belt-and-suspenders even to allowlisted names). `mcp-inventory.mjs`: implement `captureMcpInventory()` running `claude mcp list` via fixed-argv spawnSync (parse name + version lines), falling back to reading the MCP config file if the CLI fails, returning `{ servers:[{name,version}], source }`; best-effort, never throw. Also capture agent version helper if convenient (or leave to Plan 04). `.gitignore`: add `.data/run-snapshots/` adjacent to the existing `.data/measurements/` block (security V9 — snapshots hold prompts/KB/env and MUST NOT be committed). Write env-allowlist.test.mjs asserting the allowlist + deny behavior.
  </action>
  <verify>
    <automated>node --test tests/repro/env-allowlist.test.mjs && git check-ignore .data/run-snapshots/x && echo GITIGNORE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/repro/env-allowlist.test.mjs` passes.
    - `captureEnvAllowlist({ LLM_PROXY_DATA_DIR:'/x', AWS_SECRET_ACCESS_KEY:'s', OPENAI_API_KEY:'k' })` returns `{ LLM_PROXY_DATA_DIR:'/x' }` (no secret keys).
    - `git check-ignore .data/run-snapshots/x` exits 0 (path is ignored).
    - `captureMcpInventory()` returns an object with `servers` array and a `source` string and does not throw when `claude` is absent.
  </acceptance_criteria>
  <done>Env capture is secret-safe, MCP inventory enumerates with fallback, and the snapshot dir is gitignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → snapshot | env may contain OAuth tokens, API keys, gho_* GitHub tokens |
| task-supplied paths / git output → capture | untrusted repo state; git invoked with untrusted cwd |
| snapshot dir → git working tree | captured secrets/prompts must never enter version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03-01 | Information Disclosure | env capture | mitigate | Allowlist-only capture + `SECRET_DENY_RE=/KEY|TOKEN|SECRET|PASSWORD/i` applied even to allowlisted names; never `process.env` wholesale (RESEARCH Pitfall 4, ASVS V9); test asserts secret exclusion |
| T-67-03-02 | Information Disclosure | snapshot dir committed to git | mitigate | Add `.data/run-snapshots/` to `.gitignore`; verified by `git check-ignore` |
| T-67-03-03 | Tampering / injection | git invocation | mitigate | Fixed-argv `spawnSync('git', [...])` only (evidence-harness idiom); no shell string; timeout-bounded |
| T-67-03-04 | Information Disclosure | processOverrides config (llm-settings.json, captured in Plan 04) | mitigate | Confirm llm-settings.json holds routing overrides only (no key fields) before copy — flag surfaced here; Plan 04 enforces the copy-time check |
| T-67-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node --test tests/repro/git-state.test.mjs tests/repro/env-allowlist.test.mjs` green.
- `git check-ignore .data/run-snapshots/x` exits 0.
- `grep -c "spawnSync('git'" lib/repro/git-state.mjs` >= 4; no `exec(`/shell-string git calls.
</verification>

<success_criteria>
- D-03 workspace state (SHA + binary patch + untracked + submodules) captured injection-safely.
- Env capture is allowlist-only and secret-safe; MCP inventory enumerates with a fallback.
- Snapshot output dir is gitignored before any snapshot is written (Plan 04).
</success_criteria>

<output>
Create `.planning/phases/67-reproducibility-replay-rig/67-03-SUMMARY.md` when done
</output>
