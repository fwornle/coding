---
phase: 84-per-turn-context-revelation
plan: 08
type: execute
wave: 3
depends_on: ["84-07"]
files_modified:
  - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts
  - integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "A fetchContextTurns(taskId) thunk + selectContextTurnsFor selector mirror the existing fetchTimeline pattern (D-11)."
    - "The explainer feeds real wire values from context-turns for sent/cached/fresh per turn, replacing the ~bytes/4 estimate copy where real token counts now exist."
    - "OpenAI-wire turns (copilot/opencode) render cache-write as 'N/A (provider reports no cache-creation)', never 0 or an inferred value (D-12)."
    - "The explainer includes 'how prompt caching actually works' copy explaining the Anthropic-wire (has cache-write counter) vs OpenAI-wire (none) asymmetry."
    - "No new components are added (D-11) — the existing context-cache-explainer.tsx is extended in place."
  artifacts:
    - path: "integrations/system-health-dashboard/src/store/slices/performanceSlice.ts"
      provides: "fetchContextTurns thunk + contextTurnsByTaskId state + reducers + selectContextTurnsFor"
      contains: "fetchContextTurns"
    - path: "integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx"
      provides: "honest per-turn sent/cached/fresh + N/A for OpenAI-wire + caching explainer copy"
      contains: "N/A (provider reports no cache-creation)"
  key_links:
    - from: "context-cache-explainer.tsx"
      to: "selectContextTurnsFor(taskId)"
      via: "useAppSelector + dispatch(fetchContextTurns(taskId))"
      pattern: "fetchContextTurns"
    - from: "fetchContextTurns thunk"
      to: "/api/experiments/runs/:taskId/context-turns"
      via: "fetch"
      pattern: "context-turns"
---

<objective>
Wire the context-turns data into the EXISTING cache explainer (D-11/D-12): add the Redux `fetchContextTurns` thunk + selector mirroring `fetchTimeline`, feed honest per-turn sent/cached/fresh from real wire values, render cache-write as "N/A (provider reports no cache-creation)" for OpenAI-wire agents (never 0 or an inferred value), and add the "how prompt caching actually works" copy. No new components — richer per-turn UI is Phase 86.

Purpose: Honest measurement over inference — the explainer stops estimating what the wire now reports, and stops implying OpenAI-wire caching failed.
Output: The thunk/selector seam + the explainer's three edited seams, visually verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchContextTurns thunk + state + reducers + selector in performanceSlice.ts</name>
  <files>integrations/system-health-dashboard/src/store/slices/performanceSlice.ts</files>
  <read_first>
    - integrations/system-health-dashboard/src/store/slices/performanceSlice.ts `fetchTimeline` thunk (~345-360), state field `timelineByTaskId` (type ~198, init ~257), extraReducers (~708-720), `selectTimelineFor` (~881) — the exact seams to mirror.
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"performanceSlice.ts — fetchContextTurns thunk + selector + reducers + state" (mirror each seam; thunk fetches `/api/experiments/runs/${taskId}/context-turns`, returns {taskId, contextTurns}).
    - The pinned line shape from 84-04 (each turn carries `wire`, `usage.{input,output,cache_read,cache_write}`, `categories`, `messages`) — define a `ContextTurnRow` TS type matching it.
  </read_first>
  <behavior>
    - fetchContextTurns(taskId) fetches /api/experiments/runs/<taskId>/context-turns, returns { taskId, contextTurns: ContextTurnRow[] } on success, rejectWithValue on error.
    - fetchContextTurns.fulfilled stores contextTurnsByTaskId[taskId] = payload.contextTurns.
    - selectContextTurnsFor(taskId)(state) returns the stored array or [] when absent/null.
  </behavior>
  <action>Mirror the timeline seams: add a `ContextTurnRow` type (ts, task_id, agent, wire:'anthropic'|'openai', request_id, model, usage:{input,output,cache_read,cache_write:number|null}, cache_breakpoints:number[], categories, messages), a `fetchContextTurns` createAsyncThunk fetching `/api/experiments/runs/${encodeURIComponent(taskId)}/context-turns` and returning `{ taskId, contextTurns: data?.contextTurns ?? [] }`, a `contextTurnsByTaskId: Record<string, ContextTurnRow[]>` state field (typed + initialized `{}`), an extraReducer on `.fulfilled` storing the array, and a `selectContextTurnsFor(taskId)` selector returning `state.performance.contextTurnsByTaskId[taskId] ?? []`. Keep types strict (no `any`).</action>
  <acceptance_criteria>
    - `grep -c 'fetchContextTurns' integrations/system-health-dashboard/src/store/slices/performanceSlice.ts` returns >=3 (thunk def + reducer + export) and `grep -c 'selectContextTurnsFor\|contextTurnsByTaskId' performanceSlice.ts` (in that file) returns >=2.
    - `cd integrations/system-health-dashboard && npx tsc --noEmit` reports no NEW type errors introduced by these edits (strict types; no `any`).
  </acceptance_criteria>
  <verify><automated>bash -lc 'cd integrations/system-health-dashboard && test $(grep -c "fetchContextTurns" src/store/slices/performanceSlice.ts) -ge 3 && ! (npx tsc --noEmit 2>&1 | grep -i "performanceSlice" | grep -q "error TS")'</automated></verify>
  <done>fetchContextTurns thunk + contextTurnsByTaskId state + reducer + selectContextTurnsFor exist, strictly typed, mirroring the timeline seams; slice type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Honest per-turn render + N/A for OpenAI-wire + caching explainer copy</name>
  <files>integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx: the timeline dispatch effect (~302-304), `summarize()` flattening cache_read_tokens/cache_write_tokens (~117-134), and the "~bytes/4 estimate" honesty copy (~520-531).
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"context-cache-explainer.tsx — wire data + honest copy" (the three seams: dispatch, honest per-turn numbers branching on `wire`, corrected estimate copy) + D-12 exact N/A string.
  </read_first>
  <action>Three edits, no new components. (1) Dispatch: alongside the existing `fetchTimeline` effect, add `dispatch(fetchContextTurns(taskId))` and `const contextTurns = useAppSelector(selectContextTurnsFor(taskId))`. (2) Honest per-turn numbers: feed the per-turn `usage.{input,cache_read,cache_write,output}` from `contextTurns` (real wire values) into the existing chart/summary; branch cache-write rendering on the turn's `wire` — for `wire==='openai'` (copilot/opencode) render the literal string `N/A (provider reports no cache-creation)` instead of a number; for `wire==='anthropic'` render the real `cache_write`. Never infer or estimate a cache-write value. (3) Copy: replace the current "token figures are a ~bytes/4 estimate" note with real wire values where context-turns now supplies exact counts, and ADD a short "how prompt caching actually works" paragraph explaining that Anthropic-wire reports a cache-creation (write) counter while OpenAI-wire reports only cache reads (hence N/A for write). Deploy: `npm run build` in the submodule then `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend` (bind-mounted; no docker-compose build).</action>
  <acceptance_criteria>
    - `grep -c 'fetchContextTurns\|selectContextTurnsFor' integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx` returns >=2.
    - `grep -c 'N/A (provider reports no cache-creation)' integrations/system-health-dashboard/src/components/performance/context-cache-explainer.tsx` returns >=1.
    - `grep -Ec 'how prompt caching|cache-creation counter|cache reads' context-cache-explainer.tsx` returns >=1 (explainer copy present).
    - `cd integrations/system-health-dashboard && npm run build` completes exit 0 (frontend bundle builds).
  </acceptance_criteria>
  <verify><automated>cd integrations/system-health-dashboard && npm run build</automated></verify>
  <done>The explainer dispatches fetchContextTurns, renders honest per-turn sent/cached/fresh, renders the exact N/A string for OpenAI-wire cache-write, adds the caching-explainer copy, no new components; frontend builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of the honest explainer at :3032</name>
  <what-built>The cache explainer now pulls real per-turn context-turns data, renders honest sent/cached/fresh, shows "N/A (provider reports no cache-creation)" for OpenAI-wire (copilot/opencode) cache-write, and carries the "how prompt caching actually works" copy.</what-built>
  <action>Pause for human visual verification of the rebuilt context-cache-explainer at http://localhost:3032 (Performance tab). Rebuild the dashboard frontend and restart `web-services:health-dashboard-frontend` first, then capture a `gsd-browser` screenshot of the explainer for a task that has context-turns data and confirm the four honest-rendering conditions in <how-to-verify> before resuming.</action>
  <how-to-verify>
    1. Ensure the frontend was rebuilt + `web-services:health-dashboard-frontend` restarted.
    2. Capture a screenshot with the gsd-browser CLI (project rule — never a hand-rolled node playwright script): `gsd-browser navigate` to the Performance tab at http://localhost:3032 that renders the context-cache-explainer for a task that has context-turns data, then `gsd-browser screenshot --output <path>`.
    3. Confirm visually: (a) per-turn cache split shows real numbers, (b) an OpenAI-wire (copilot/opencode) turn shows "N/A (provider reports no cache-creation)" for cache-write (NOT 0), (c) the "how prompt caching actually works" copy is present, (d) an Anthropic-wire (claude) turn shows a real cache-write number.
  </how-to-verify>
  <resume-signal>Type "approved" if the explainer renders honest values + N/A + copy, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| context-turns API response → UI render | A missing/partial response must degrade gracefully, not crash the tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-08-01 | Information Disclosure (integrity of measurement) | OpenAI-wire cache-write rendered as 0 | mitigate | Branch on the per-line `wire` discriminator; render the exact N/A string; never infer (D-12) — honest-measurement is a hard project value |
| T-84-08-02 | Denial of Service | empty/failed context-turns fetch | mitigate | selectContextTurnsFor returns [] on absence; thunk rejectWithValue; the tab degrades to the existing timeline path |
| T-84-08-SC | Tampering | npm installs | accept | No new packages; existing dashboard deps only |
</threat_model>

<verification>
- `npm run build` (submodule) exit 0.
- gsd-browser screenshot at :3032 confirms honest values + N/A string + caching copy (Task 3 human-verify).
</verification>

<success_criteria>
The existing explainer shows honest per-turn sent/cached/fresh from real wire values, renders N/A for OpenAI-wire cache-write, adds the caching explainer copy, adds no new components, and is visually verified at :3032.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-08-SUMMARY.md` when done.
</output>
