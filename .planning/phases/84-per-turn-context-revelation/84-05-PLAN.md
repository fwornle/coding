---
phase: 84-per-turn-context-revelation
plan: 05
type: execute
wave: 1
depends_on: ["84-01"]
files_modified:
  - scripts/measurement-stop.mjs
  - tests/context-turns/close-gzip.test.mjs
  - tests/context-turns/correlate.test.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "At span close, context-turns.jsonl is gzipped to context-turns.jsonl.gz and the plaintext is removed (D-03)."
    - "raw-bodies.jsonl is gzipped to raw-bodies.jsonl.gz at close when present."
    - "Each turn is enriched with observation_ref: nearest observation by createdAt within the span window (started_at..ended_at), matching agent — best-effort (D-07); null when none correlates."
    - "A crashed/never-closed span leaves the readable plaintext context-turns.jsonl for the age sweeper to reclaim."
    - "The whole close step is best-effort never-throw: a failure writes to stderr and never aborts span close."
  artifacts:
    - path: "scripts/measurement-stop.mjs"
      provides: "gzip-at-close + observation-enrichment step co-located with reconciliation.json write"
      contains: "context-turns.jsonl"
  key_links:
    - from: "scripts/measurement-stop.mjs (reconciliation write block ~471)"
      to: ".data/measurements/<task_id>/context-turns.jsonl.gz"
      via: "zlib.gzipSync + fs.unlink plaintext"
      pattern: "gzip"
    - from: "scripts/measurement-stop.mjs"
      to: "obs-api :12436 / observation-export/*.json"
      via: "time-window+agent correlation (fetchRunNarrative pattern)"
      pattern: "observation"
---

<objective>
Complete the D-03 write-timing lifecycle at span close (`scripts/measurement-stop.mjs`, beside the reconciliation.json write): gzip the plaintext `context-turns.jsonl` → `.gz` and remove the plaintext, gzip `raw-bodies.jsonl` → `.gz` when present, and enrich each turn's `observation_ref` via the best-effort time-window+agent correlation (D-07 semantic layer). Correlation runs HERE (not in the proxy hot path — Pitfall 1: observations have no task_id and don't exist at request time).

Purpose: Crash-durable capture (plaintext survives an unclosed span), compressed at the one-time close, semantically annotated where an observation correlates.
Output: The gzip + correlation step slotted into the existing best-effort reconciliation block.

## Shared file contract (must match Plan 04)
- Dir: `.data/measurements/<sanitizeTaskId(span.task_id)>/`
- Input plaintext: `context-turns.jsonl` (one line per request; each has `ts`, `agent`, `observation_ref:null`).
- Output: `context-turns.jsonl.gz` (plaintext removed after gzip); `raw-bodies.jsonl.gz` if `raw-bodies.jsonl` present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-PATTERNS.md
@scripts/measurement-stop.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: gzip-at-close + plaintext removal, co-located with the reconciliation write</name>
  <files>scripts/measurement-stop.mjs</files>
  <read_first>
    - scripts/measurement-stop.mjs ~441-483: the reconciliation.json write block — `sanitizeTaskId(span.task_id)`, `path.join(REPO_ROOT,'.data','measurements',id)`, `fs.mkdirSync(...,{recursive:true})`, and the best-effort catch at ~478-481 (`process.stderr.write('[measurement-stop] ... (non-fatal): '+err.message)`) — the exact co-location anchor + never-throw wrap to clone.
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"measurement-stop.mjs — gzip-at-close" (reuse the SAME dir build; gzip via node:zlib zlib.gzipSync, same lib lsl-file-manager.js imports; scope to span.task_id — resolveLiveTaskId gone at close).
    - The shared file contract in this plan's <objective>.
  </read_first>
  <action>In its OWN best-effort try/catch beside the reconciliation write (reusing the same `reconcileDir`/`reconcileDirId` = `sanitizeTaskId(span.task_id)` + `.data/measurements` build), add: read `context-turns.jsonl` if it exists → `zlib.gzipSync(buf)` → write `context-turns.jsonl.gz` → `fs.unlinkSync` the plaintext (D-03). Do the same for `raw-bodies.jsonl` → `raw-bodies.jsonl.gz` when present. Guard each file independently (a missing context-turns file is normal — the span may have had no measured requests; never throw). Emit any failure to `process.stderr.write('[measurement-stop] context-turns gzip failed (non-fatal): ...')`. Do the observation enrichment (Task 2) BEFORE the gzip so the enriched lines are what get compressed.</action>
  <acceptance_criteria>
    - `node --check scripts/measurement-stop.mjs` exits 0.
    - `grep -c 'context-turns.jsonl' scripts/measurement-stop.mjs` returns >=1 and `grep -c 'raw-bodies.jsonl' scripts/measurement-stop.mjs` returns >=1.
    - `grep -Ec 'gzipSync|createGzip' scripts/measurement-stop.mjs` returns >=1 (uses node:zlib, no new compression code).
  </acceptance_criteria>
  <verify><automated>bash -lc 'node --check scripts/measurement-stop.mjs && grep -q "context-turns.jsonl" scripts/measurement-stop.mjs && grep -Eq "gzipSync|createGzip" scripts/measurement-stop.mjs'</automated></verify>
  <done>Span close gzips context-turns.jsonl (and raw-bodies.jsonl when present) to .gz and removes the plaintext, best-effort never-throw, co-located with the reconciliation write.</done>
</task>

<task type="auto">
  <name>Task 2: Observation-ref enrichment (time-window + agent correlation) + fill close-gzip/correlate tests</name>
  <files>scripts/measurement-stop.mjs, tests/context-turns/close-gzip.test.mjs, tests/context-turns/correlate.test.mjs</files>
  <read_first>
    - .planning/phases/84-per-turn-context-revelation/84-RESEARCH.md §"Research Hand-off #1" (observations have NO task_id; join by [from,to] window + agent; nearest-by-createdAt; obs-api :12436 OR exported .data/observation-export/observations.json fallback when obs-api mid-restart; many turns → ONE observation; null when none).
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"Observation correlation (D-07)" (the fetchRunNarrative time-window+agent pattern; proxy always wrote observation_ref:null + preview; enrich later).
    - scripts/measurement-stop.mjs — where `span.started_at`/`span.ended_at`/`span.agent` are available at close.
    - tests/context-turns/{close-gzip,correlate}.test.mjs stubs + tests/fixtures/context-turns/observations-slice.json (from 84-01).
  </read_first>
  <action>Add a best-effort `enrichObservationRefs(lines, span)` that computes the window `from=span.started_at`, `to=span.ended_at`, and for each turn line finds the nearest observation by `createdAt` within `[from,to]` whose `agent` matches `span.agent` (project too when available), setting `line.observation_ref = { id, theme|intentSnippet }` or leaving it `null` when none. Source observations via obs-api `GET http://host.docker.internal:12436/api/observations?from=&to=&agent=&limit=200` with a try/catch fallback to reading `.data/observation-export/observations.json`; on any failure leave every observation_ref null (the ≤120-char preview from Plan 04 already stands — always something rather than nothing). Call this on the parsed context-turns lines BEFORE gzip. Then un-skip the two tests: close-gzip asserts that given a temp measurements dir with a `context-turns.jsonl`, the close routine produces a `.gz` whose gunzip round-trips the lines AND the plaintext is removed, and that a raw-bodies.jsonl is likewise gzipped; correlate asserts that against the observations fixture, a turn whose ts falls inside the window+agent gets a non-null observation_ref pointing at the nearest observation, and a turn outside the window (or wrong agent) stays null. Drive the correlation with an injected observations array (no live obs-api) so the test is offline/deterministic.</action>
  <acceptance_criteria>
    - `node --test tests/context-turns/close-gzip.test.mjs tests/context-turns/correlate.test.mjs` exits 0 (un-skipped, 0 failures).
    - close-gzip test: after close, `context-turns.jsonl.gz` exists, plaintext `context-turns.jsonl` is gone, and `zlib.gunzipSync` of the .gz round-trips the original line count.
    - correlate test: in-window+matching-agent turn → `observation_ref.id` set to the nearest observation; out-of-window turn → `observation_ref === null`; an empty/failed observation source leaves all refs null without throwing.
    - `grep -c 'enrichObservationRefs\|observation_ref' scripts/measurement-stop.mjs` returns >=1.
  </acceptance_criteria>
  <verify><automated>node --test tests/context-turns/close-gzip.test.mjs tests/context-turns/correlate.test.mjs</automated></verify>
  <done>Turns are enriched with nearest-by-createdAt observation_ref within span window+agent (null when none, offline-fallback safe); close-gzip + correlate tests pass proving round-trip gzip, plaintext removal, and correct correlation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| obs-api :12436 reachability at close | obs-api may be mid-restart (SIGTERM); correlation must degrade gracefully |
| span.task_id → measurements path | Path stays under .data/measurements/<sanitized> |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-05-01 | Denial of Service | obs-api unreachable / span with no context-turns file | mitigate | Best-effort try/catch → stderr; fall back to exported observations JSON; missing file is normal; never aborts span close |
| T-84-05-02 | Tampering | sanitizeTaskId(span.task_id) path build | mitigate | Reuse the existing reconciliation sanitizeTaskId path build; scope to span.task_id (resolveLiveTaskId gone at close) |
| T-84-05-03 | Information Disclosure | mis-attributed observation_ref | accept | Correlation is a coarse best-effort annotation (many turns → one obs), not an authoritative join; null when uncertain; documented as reference not proof |
| T-84-05-SC | Tampering | npm installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node --check scripts/measurement-stop.mjs` clean.
- `node --test tests/context-turns/close-gzip.test.mjs tests/context-turns/correlate.test.mjs` green.
</verification>

<success_criteria>
Span close enriches observation_ref best-effort, gzips context-turns.jsonl (and raw-bodies.jsonl) to .gz, removes the plaintext, and never aborts on failure; a crashed span leaves a readable plaintext for the sweeper.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-05-SUMMARY.md` when done.
</output>
