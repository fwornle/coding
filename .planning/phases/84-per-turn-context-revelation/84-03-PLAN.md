---
phase: 84-per-turn-context-revelation
plan: 03
type: execute
wave: 1
depends_on: ["84-01"]
files_modified:
  - launchd/com.coding.context-turns-sweeper.plist
  - scripts/context-turns-sweeper-job.sh
  - scripts/install-context-turns-sweeper-launchd.sh
  - .planning/config.json
  - tests/context-turns/sweeper.test.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "A dedicated launchd job com.coding.context-turns-sweeper is registered and appears in `launchctl list`."
    - "The sweeper deletes context-turns.jsonl(.gz) and raw-bodies.jsonl(.gz) older than the retention window and keeps newer files."
    - "Retention window is configurable via CONTEXT_TURNS_RETENTION_DAYS (default 14) and documented in .planning/config.json."
    - "The sweep is best-effort: a bad/missing dir or unreadable file never aborts the run (exit 0)."
    - "Retention runs decoupled from span close, so an abandoned/never-closed span is still reclaimed by age."
  artifacts:
    - path: "launchd/com.coding.context-turns-sweeper.plist"
      provides: "launchd job: RunAtLoad + StartInterval 3600, sweep job as ProgramArguments"
      contains: "com.coding.context-turns-sweeper"
    - path: "scripts/context-turns-sweeper-job.sh"
      provides: "Portable file_mtime age-gate sweep over .data/measurements/*/"
      contains: "CONTEXT_TURNS_RETENTION_DAYS"
    - path: "scripts/install-context-turns-sweeper-launchd.sh"
      provides: "plutil-lint → copy → bootout → bootstrap → verify installer"
  key_links:
    - from: "launchd/com.coding.context-turns-sweeper.plist"
      to: "scripts/context-turns-sweeper-job.sh"
      via: "ProgramArguments"
      pattern: "context-turns-sweeper-job\\.sh"
    - from: "scripts/context-turns-sweeper-job.sh"
      to: ".data/measurements"
      via: "walk + age-gate delete"
      pattern: "measurements"
---

<objective>
Ship the age-based retention sweeper for context-turns files (D-01/D-02): a dedicated launchd job decoupled from span close that reclaims `context-turns.jsonl(.gz)` and `raw-bodies.jsonl(.gz)` older than a configurable window (default 14 days). Clone the proven `com.coding.lsl-lock-sweeper` trio (plist + portable job.sh + installer), change cadence to hourly and retarget the sweep at `.data/measurements/*/`. Include an explicit install-script RUN task — a plist alone does not register the job (RESEARCH Pitfall 4).

Purpose: Abandoned/never-closed spans still get cleaned by age; retention is honest and bounded.
Output: The launchd trio, a documented config key, a registered job, and a driven sweep test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-PATTERNS.md
@launchd/com.coding.lsl-lock-sweeper.plist
@scripts/lsl-lock-sweeper-job.sh
@scripts/install-lsl-lock-sweeper-launchd.sh
@.planning/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the sweeper trio (plist + job.sh + installer), retargeted to .data/measurements</name>
  <files>launchd/com.coding.context-turns-sweeper.plist, scripts/context-turns-sweeper-job.sh, scripts/install-context-turns-sweeper-launchd.sh, .planning/config.json</files>
  <read_first>
    - launchd/com.coding.lsl-lock-sweeper.plist (Label, ProgramArguments, RunAtLoad, StartInterval, StandardErr/OutPath, EnvironmentVariables PATH — the exact plist shape to clone).
    - scripts/lsl-lock-sweeper-job.sh (portable `file_mtime` BSD/GNU dual-form helper, `set -uo pipefail`, age gate, `exit 0` never-throw — clone verbatim, change the walk target + age source).
    - scripts/install-lsl-lock-sweeper-launchd.sh (plutil-lint → copy-with-backup → bootout → bootstrap → `launchctl list | grep -qF` verify sequence — clone verbatim, swap Label + paths).
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"launchd sweeper trio" (cadence 3600, drop ThrottleInterval, walk context-turns.jsonl(.gz) + raw-bodies.jsonl(.gz), per-file mtime).
    - .planning/config.json (add the documented retention key alongside existing keys).
  </read_first>
  <action>Create the three files under absolute paths matching the lsl-lock-sweeper analog. (1) `launchd/com.coding.context-turns-sweeper.plist`: Label `com.coding.context-turns-sweeper`, ProgramArguments `[/bin/bash, /Users/Q284340/Agentic/coding/scripts/context-turns-sweeper-job.sh]`, `RunAtLoad true`, `StartInterval 3600`, StandardErr/OutPath `/Users/Q284340/Agentic/coding/.data/context-turns-sweeper.log`, EnvironmentVariables PATH `/opt/homebrew/bin:/usr/local/bin:/usr/bin:/usr/sbin:/bin:/sbin` (drop the 30s ThrottleInterval — age-based, no lock-race). (2) `scripts/context-turns-sweeper-job.sh`: clone the portable `file_mtime` helper and age-gate loop; read `CONTEXT_TURNS_RETENTION_DAYS` (default 14) → STALE_SECS = days*86400; walk `${CODING_REPO:-/Users/Q284340/Agentic/coding}/.data/measurements/*/` and for each of `context-turns.jsonl`, `context-turns.jsonl.gz`, `raw-bodies.jsonl`, `raw-bodies.jsonl.gz`, delete when `NOW - file_mtime >= STALE_SECS` (per-file mtime, independent so a raw-body file can drop while a digest survives, D-05 intent); `set -uo pipefail`, best-effort per file, `exit 0` always. Make it executable (chmod +x). (3) `scripts/install-context-turns-sweeper-launchd.sh`: clone the installer verbatim with LABEL `com.coding.context-turns-sweeper`, SRC_PLIST the new plist, DEST_PLIST `~/Library/LaunchAgents/com.coding.context-turns-sweeper.plist`; chmod +x. (4) Add a documented key to `.planning/config.json` recording the retention default, e.g. a `context_turns` object with `retention_days: 14` (D-02 says config.json OR env; the bash sweeper reads the env var, config.json documents the shipped default).</action>
  <acceptance_criteria>
    - `/usr/bin/plutil -lint launchd/com.coding.context-turns-sweeper.plist` prints `OK`.
    - `grep -c 'com.coding.context-turns-sweeper' launchd/com.coding.context-turns-sweeper.plist` returns >=1 and `grep -c 'StartInterval' launchd/com.coding.context-turns-sweeper.plist` returns 1 with value 3600.
    - `bash -n scripts/context-turns-sweeper-job.sh` and `bash -n scripts/install-context-turns-sweeper-launchd.sh` both exit 0 (syntax valid); both files are executable (`test -x`).
    - `grep -c 'CONTEXT_TURNS_RETENTION_DAYS' scripts/context-turns-sweeper-job.sh` returns >=1; `grep -Ec 'context-turns\.jsonl|raw-bodies\.jsonl' scripts/context-turns-sweeper-job.sh` returns >=1.
    - `node -e "const c=require('./.planning/config.json'); if(!JSON.stringify(c).includes('retention')) throw new Error('retention default not documented in config.json')"` exits 0.
  </acceptance_criteria>
  <verify><automated>bash -n scripts/context-turns-sweeper-job.sh && /usr/bin/plutil -lint launchd/com.coding.context-turns-sweeper.plist</automated></verify>
  <done>Three trio files exist, plist lints OK with StartInterval 3600, job.sh reads CONTEXT_TURNS_RETENTION_DAYS and walks .data/measurements/*/, config.json documents retention_days 14.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the sweeper via a temp measurements dir + register the job</name>
  <files>tests/context-turns/sweeper.test.mjs</files>
  <read_first>
    - tests/context-turns/sweeper.test.mjs (Wave-0 skipped stub) + tests/context-turns/_helpers.mjs.
    - scripts/context-turns-sweeper-job.sh (Task 1 — respects CODING_REPO override + CONTEXT_TURNS_RETENTION_DAYS).
  </read_first>
  <action>Un-skip the sweeper stub: create a temp repo root with `.data/measurements/<taskA>/context-turns.jsonl` given an OLD mtime (e.g. `fs.utimesSync` to now-30d) and `.data/measurements/<taskB>/context-turns.jsonl.gz` with a FRESH mtime; run `scripts/context-turns-sweeper-job.sh` with env `CODING_REPO=<tmp>` and `CONTEXT_TURNS_RETENTION_DAYS=14`; assert the old file is deleted and the fresh file survives; then run it once against a non-existent measurements dir and assert exit code 0 (never-throw). After the test passes, RUN `bash scripts/install-context-turns-sweeper-launchd.sh` to register the real job and verify with `launchctl list`.</action>
  <acceptance_criteria>
    - `node --test tests/context-turns/sweeper.test.mjs` exits 0 (not skipped): old file gone, fresh file present, missing-dir run returns exit 0.
    - `bash scripts/install-context-turns-sweeper-launchd.sh` completes and `launchctl list | grep com.coding.context-turns-sweeper` returns a row (Pitfall 4: plist alone does not register — the installer MUST be run).
  </acceptance_criteria>
  <verify><automated>node --test tests/context-turns/sweeper.test.mjs</automated></verify>
  <done>sweeper.test.mjs proves age-gated delete + never-throw; the installer has been run and `launchctl list | grep com.coding.context-turns-sweeper` returns a row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem walk over .data/measurements | A malformed dir/file must not abort the sweep or leave the daemon crashed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03-01 | Denial of Service | sweeper walking bad/missing dirs | mitigate | `set -uo pipefail` + per-file best-effort + `exit 0`; test drives a non-existent dir and asserts exit 0 |
| T-84-03-02 | Information Disclosure | stale raw-bodies files lingering | mitigate | Age-based deletion of raw-bodies.jsonl(.gz) independent of digests so secrets-bearing files are reclaimed on schedule |
| T-84-03-SC | Tampering | npm installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node --test tests/context-turns/sweeper.test.mjs` green.
- `/usr/bin/plutil -lint launchd/com.coding.context-turns-sweeper.plist` OK.
- `launchctl list | grep com.coding.context-turns-sweeper` returns a row (job registered).
</verification>

<success_criteria>
A dedicated hourly launchd sweeper is registered and reclaims context-turns/raw-bodies files by age (default 14 days, env-configurable), decoupled from span close, best-effort never-throw.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-03-SUMMARY.md` when done.
</output>
