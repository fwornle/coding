---
phase: 84-per-turn-context-revelation
plan: 07
type: execute
wave: 2
depends_on: ["84-04", "84-05"]
files_modified:
  - lib/vkb-server/api-routes.js
  - integrations/system-health-dashboard/server.js
  - tests/vkb/context-turns-route.test.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /api/experiments/runs/:taskId/context-turns returns the per-turn array by gunzipping the local .gz (or reading plaintext .jsonl if the span is not yet closed)."
    - "A missing file returns 200 graceful-empty ({contextTurns: []}), NOT 500 (D-10)."
    - "The route rejects a traversal :taskId via the existing _validTaskId guard (path stays under .data/measurements/)."
    - "The dashboard backend proxies /api/context-turns to the proxy same-origin surface, mirroring /api/context-breakdown."
  artifacts:
    - path: "lib/vkb-server/api-routes.js"
      provides: "handleContextTurns + route registration parallel to handleReconciliation"
      contains: "handleContextTurns"
    - path: "integrations/system-health-dashboard/server.js"
      provides: "proxy mirror line for context-turns"
      contains: "context-turns"
  key_links:
    - from: "GET /api/experiments/runs/:taskId/context-turns"
      to: ".data/measurements/<taskId>/context-turns.jsonl.gz"
      via: "_validTaskId + zlib gunzip + JSON.parse per line"
      pattern: "context-turns"
    - from: "dashboard server.js"
      to: "proxy :12435 / vkb :8080"
      via: "reverse-proxy mirror line"
      pattern: "context-turns"
---

<objective>
Expose the per-span context-turns file through the read surfaces (D-10), mirroring the Phase-83 D-13 pattern: a thin vkb-server pass-through cloned verbatim from `handleReconciliation` (gunzip the local `.gz`, graceful-empty on ENOENT, traversal-guarded), plus the dashboard backend proxy mirror line so the UI (Plan 08) can fetch it. No re-shaping — the file is served verbatim.

Purpose: Give the cache explainer (Plan 08) a data source and provide a debuggable read API.
Output: `handleContextTurns` + registration in vkb + a proxy mirror line in the dashboard backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-PATTERNS.md
@lib/vkb-server/api-routes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: handleContextTurns in vkb-server (clone handleReconciliation, gunzip the .gz)</name>
  <files>lib/vkb-server/api-routes.js, tests/vkb/context-turns-route.test.mjs</files>
  <read_first>
    - lib/vkb-server/api-routes.js `handleReconciliation` (~605-633): the try/catch → 200 verbatim / ENOENT→200 graceful-empty / 500-only-on-unexpected shape; the `_validTaskId` traversal guard (~608, `[A-Za-z0-9._-]`, ≤80); `_dataDir()` (~771); and the route registration at ~90 (`app.get('/api/experiments/runs/:taskId/reconciliation', ...)`).
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"handleContextTurns + registration" (read measurements/<taskId>/context-turns.jsonl.gz, zlib.gunzip → split lines → JSON.parse each → res.status(200).json({contextTurns:[...]}); ENOENT → {contextTurns: []}).
    - tests/vkb/context-turns-route.test.mjs (Wave-0 stub).
  </read_first>
  <action>Add `handleContextTurns(req, res)` cloned from `handleReconciliation`: validate `req.params.taskId` via `_validTaskId` (400 on invalid), resolve `path.join(this._dataDir(),'measurements',taskId,'context-turns.jsonl.gz')`, read + `zlib.gunzip` the buffer, split on newlines, `JSON.parse` each non-empty line, and `res.status(200).json({ contextTurns: arr })`. When the `.gz` is ENOENT, fall back to reading the plaintext `context-turns.jsonl` (span not yet closed); when BOTH are ENOENT → `res.status(200).json({ contextTurns: [] })` (graceful, NOT 500). 500 only on an unexpected error, logged via the existing logger. Register `app.get('/api/experiments/runs/:taskId/context-turns', (req,res)=>this.handleContextTurns(req,res))` parallel to the reconciliation registration. Then un-skip the vkb route test: assert verbatim gunzip round-trip for a seeded `.gz`, `{contextTurns: []}` for a missing file (200 not 500), and a 400/blocked response for a traversal taskId like `../../etc`.</action>
  <acceptance_criteria>
    - `node --check lib/vkb-server/api-routes.js` exits 0.
    - `grep -c 'handleContextTurns' lib/vkb-server/api-routes.js` returns >=2 (definition + registration) and `grep -c "runs/:taskId/context-turns" lib/vkb-server/api-routes.js` returns >=1.
    - `node --test tests/vkb/context-turns-route.test.mjs` exits 0: seeded .gz → array round-trips; missing file → HTTP 200 with `{contextTurns: []}`; traversal taskId → rejected (not a filesystem read outside measurements).
  </acceptance_criteria>
  <verify><automated>node --test tests/vkb/context-turns-route.test.mjs</automated></verify>
  <done>handleContextTurns serves the per-turn array verbatim (gunzip .gz or plaintext fallback), graceful-empty on miss (200 not 500), traversal-guarded; registered parallel to reconciliation; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard backend proxy mirror line for context-turns</name>
  <files>integrations/system-health-dashboard/server.js</files>
  <read_first>
    - integrations/system-health-dashboard/server.js the `/api/context-breakdown` same-origin proxy (~294-304) → `http://host.docker.internal:12435/api/context-breakdown` — the exact mirror to clone; and the experiments reverse-proxy (~320) that already forwards `/api/experiments/*` to vkb :8080.
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"server.js — proxy mirror line" + Pitfall 5 (VirtioFS: server.js edits need a full `docker-compose restart coding-services`, not a supervisor-only restart).
  </read_first>
  <action>Add one mirror proxy line for `/api/context-turns` → `http://host.docker.internal:12435/api/context-turns` (the proxy same-origin surface, same shape as the context-breakdown proxy: forward query string, mirror status, 502 on unreachable). The vkb `/api/experiments/runs/:taskId/context-turns` route rides the EXISTING `/api/experiments/*` reverse-proxy — verify it does (no new line needed there), and only add the `/api/context-turns` mirror. After editing, restart per the VirtioFS caveat: `cd docker && docker-compose restart coding-services` (a supervisor-only restart re-reads the stale cached file).</action>
  <acceptance_criteria>
    - `node --check integrations/system-health-dashboard/server.js` exits 0.
    - `grep -c "'/api/context-turns'" integrations/system-health-dashboard/server.js` returns >=1 and it targets `:12435`.
    - After `docker-compose restart coding-services`, `curl -s http://localhost:3032/api/context-turns?task_id=nonexistent` returns HTTP 200 with a JSON body (graceful-empty, proving the mirror + vkb/proxy graceful path are wired end to end).
  </acceptance_criteria>
  <verify><automated>bash -lc 'node --check integrations/system-health-dashboard/server.js && grep -q "/api/context-turns" integrations/system-health-dashboard/server.js && grep -q "12435" integrations/system-health-dashboard/server.js'</automated></verify>
  <done>Dashboard backend mirrors /api/context-turns to the proxy surface; container restarted per VirtioFS caveat; a nonexistent task_id returns 200 graceful-empty through the dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| :taskId path param → filesystem read | Untrusted path segment must not traverse outside .data/measurements/ |
| dashboard → proxy/vkb reverse proxy | Local same-origin; unreachable upstream must not 500-storm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-07-01 | Tampering | :taskId path param | mitigate | Reuse `_validTaskId` (`[A-Za-z0-9._-]`, ≤80); path.join under `_dataDir()/measurements`; traversal test asserts rejection |
| T-84-07-02 | Information Disclosure | verbatim file serve | accept | The file is context-turns digests already scoped per-task; raw bodies are a separate gated file; read APIs are local same-origin (V4 light) |
| T-84-07-03 | Denial of Service | proxy/vkb unreachable | mitigate | 502 on unreachable, ENOENT→200 graceful-empty; never 500 for a missing file |
| T-84-07-SC | Tampering | npm installs | accept | No package installs |
</threat_model>

<verification>
- `node --test tests/vkb/context-turns-route.test.mjs` green.
- `curl -s http://localhost:3032/api/context-turns?task_id=nonexistent` → 200 JSON after container restart.
</verification>

<success_criteria>
The context-turns file is readable via a traversal-guarded vkb pass-through (verbatim gunzip, graceful-empty on miss) and the dashboard backend proxies the same-origin surface, ready for the UI.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-07-SUMMARY.md` when done.
</output>
