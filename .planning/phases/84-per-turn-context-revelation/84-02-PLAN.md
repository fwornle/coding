---
phase: 84-per-turn-context-revelation
plan: 02
type: execute
wave: 1
depends_on: ["84-01"]
files_modified:
  - scripts/enhanced-redaction-system.js
  - tests/redaction/config-load.test.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "The redaction applier loads all 27 patterns from .specstory/config/redaction-patterns.json (not the 4 hardcoded PII regexes)."
    - "A raw body containing sk-/Bearer/JWT/env-var secrets is masked before any write."
    - "The redact() return shape {content, redactionCount, securityLevel} and the fail-closed catch are preserved so the LSL caller does not break."
    - "A bad regex in the config is skipped with a stderr note, never crashing the process."
  artifacts:
    - path: "scripts/enhanced-redaction-system.js"
      provides: "Config-loading redaction applier + exported loadRedactionPatterns(configPath)"
      contains: "loadRedactionPatterns"
  key_links:
    - from: "scripts/enhanced-redaction-system.js"
      to: ".specstory/config/redaction-patterns.json"
      via: "loadRedactionPatterns(configPath) at construction"
      pattern: "redaction-patterns\\.json"
    - from: "scripts/lsl-file-manager.js"
      to: "scripts/enhanced-redaction-system.js"
      via: ".redact() consuming result.content/redactionCount/securityLevel"
      pattern: "\\.redact\\("
---

<objective>
Rewire the redaction applier so raw-body capture (D-05/D-06, Plan 06) redacts via the project's configured 27-pattern set at `.specstory/config/redaction-patterns.json`, closing the RESEARCH-verified gap where the applier module currently applies only 4 hardcoded PII regexes and never reads the config. Export a shared `loadRedactionPatterns(configPath)` so the proxy-side raw-body writer consumes the identical compiled pattern list from the identical config file (one source of truth, D-06).

Purpose: Fail-closed credential+PII masking is the primary security control of this phase (V6 Cryptography/Secrets).
Output: A config-loading applier that preserves the exact caller contract and adds an exported loader helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-PATTERNS.md
@scripts/enhanced-redaction-system.js
@.specstory/config/redaction-patterns.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add loadRedactionPatterns + rewire redact() to apply the 27-pattern config</name>
  <files>scripts/enhanced-redaction-system.js</files>
  <read_first>
    - scripts/enhanced-redaction-system.js FULL (current `redact(content, options) -> {content, redactionCount, securityLevel}` at ~21-64; the 4 hardcoded regexes; the fail-closed catch returning `[REDACTION_ERROR_CONTENT_BLOCKED]` / securityLevel `MAXIMUM`; the `require.main === module` CLI-test block that uses console.* — DO NOT touch those lines).
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"config-loader rewire" (the loadRedactionPatterns core + return-shape contract to preserve + the established-name caveat: the applier module's exported class name is an existing API name, do not rename it).
    - .specstory/config/redaction-patterns.json (pattern object fields: id, name, pattern [string regex], flags, replacementType, replacement [supports $1 refs], enabled, severity; top-level version/description/enabled/patterns[]).
    - scripts/lsl-file-manager.js ~125-155 (the caller consuming result.content/redactionCount/securityLevel — the contract that must not break).
  </read_first>
  <behavior>
    - loadRedactionPatterns('.specstory/config/redaction-patterns.json') returns a compiled array of {id, re, replacement} for each enabled pattern; returns [] when top-level cfg.enabled === false.
    - A pattern with an invalid regex string is skipped (filtered out) and a stderr line is emitted; the loader still returns the other patterns.
    - redact(content) applies every compiled pattern in order via content.replace(re, replacement), keeps the appended 4 hardcoded PII regexes as an additive safety net, and returns {content, redactionCount, securityLevel} with securityLevel 'HIGH' when redactionCount>0 else 'CLEAN'.
    - On an unexpected throw inside redact(), the fail-closed branch still returns {content:'[REDACTION_ERROR_CONTENT_BLOCKED]', redactionCount:1, securityLevel:'MAXIMUM'} (content blocked, never re-thrown).
  </behavior>
  <action>Add an exported module-level function `loadRedactionPatterns(configPath)` that JSON.parses the config, returns `[]` when `cfg.enabled === false`, and maps `cfg.patterns.filter(p=>p.enabled)` to `{ id, re: new RegExp(p.pattern, p.flags), replacement: p.replacement }`, wrapping each `new RegExp` in try/catch so a bad pattern is skipped with a `process.stderr.write('[redaction] bad pattern <id>: <msg>\n')` note (never throw). In the applier's constructor, compile the config patterns once via `loadRedactionPatterns` (default path resolved to the repo's `.specstory/config/redaction-patterns.json`; accept an override path option for testability). In `redact()`, apply the compiled config patterns in order BEFORE (or in addition to) the existing 4 hardcoded PII regexes — strictly additive so LSL redaction cannot regress — and preserve the exact `{content, redactionCount, securityLevel}` return shape plus the fail-closed catch verbatim. Export both the applier class (keep its existing exported name — do NOT rename it) and `loadRedactionPatterns` via module.exports so Plan 06's proxy-side applier can import the identical loader. Do NOT modify the CLI-test block's console.* lines. Use `process.stderr.write`, never `console.*`, for any new logging.</action>
  <acceptance_criteria>
    - `node -e "const {loadRedactionPatterns}=require('./scripts/enhanced-redaction-system.js'); const p=loadRedactionPatterns('.specstory/config/redaction-patterns.json'); if(p.length<20) throw new Error('expected 27-pattern config, got '+p.length)"` exits 0 (loads the configured set, not 4).
    - `node -e "const M=require('./scripts/enhanced-redaction-system.js'); const C=M.EnhancedRedactionSystem||M; const R=new C(); const r=R.redact('key sk-ant-api03-AAAA Bearer abc.def.ghi header'); if(/sk-ant-api03-AAAA/.test(r.content)) throw new Error('sk- not masked'); if(!('content'in r&&'redactionCount'in r&&'securityLevel'in r)) throw new Error('return shape changed')"` exits 0.
    - `grep -nE 'console\.(log|error|warn)' scripts/enhanced-redaction-system.js | grep -v 'require.main'` shows no NEW console usage outside the pre-existing CLI-test block (constraint: no-console-log).
    - Applier name preserved: `grep -c 'RedactionSystem' scripts/enhanced-redaction-system.js` returns >=1 (module not renamed).
  </acceptance_criteria>
  <verify><automated>node -e "const {loadRedactionPatterns}=require('./scripts/enhanced-redaction-system.js'); if(loadRedactionPatterns('.specstory/config/redaction-patterns.json').length<20) process.exit(1)"</automated></verify>
  <done>loadRedactionPatterns loads the 27-pattern config; redact() masks sk-/Bearer/JWT while preserving {content,redactionCount,securityLevel} and the fail-closed catch; bad regex skipped with stderr; applier module not renamed; no new console.*.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the redaction config-load test (un-skip the Wave-0 stub)</name>
  <files>tests/redaction/config-load.test.mjs</files>
  <read_first>
    - tests/redaction/config-load.test.mjs (the Wave-0 skipped stub from 84-01).
    - scripts/enhanced-redaction-system.js (the rewired applier + exported loadRedactionPatterns from Task 1).
  </read_first>
  <action>Un-skip the stub and assert: (a) loadRedactionPatterns loads >=20 compiled patterns from the real config; (b) redact() masks a sample containing an Anthropic key (`sk-ant-...`), a `Bearer <token>`, a JWT (`eyJ...`), and an env-var secret (`SOME_SECRET=value`) — none of the raw secret substrings survive in `result.content`; (c) the return object has exactly `content`, `redactionCount`, `securityLevel`; (d) a config with one deliberately malformed pattern (via a temp copy) still loads the rest and does not throw. Require the module via CommonJS interop from the `.mjs` test (`createRequire`).</action>
  <acceptance_criteria>
    - `node --test tests/redaction/config-load.test.mjs` exits 0 with >=4 assertions passing and 0 skipped.
    - The test asserts `result.content` contains none of: `sk-ant`, `Bearer `, `eyJ`, the env-var secret value.
  </acceptance_criteria>
  <verify><automated>node --test tests/redaction/config-load.test.mjs</automated></verify>
  <done>config-load.test.mjs passes (not skipped); proves 27-pattern load, secret masking, return-shape preservation, and bad-pattern resilience.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw request/response body → redaction applier | Untrusted body may contain live credentials that must never reach disk |
| redaction-patterns.json → RegExp compile | A malformed config pattern must not crash the applier or its host daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02-01 | Information Disclosure | redact() output written to raw-bodies file | mitigate | Load and apply the 27-pattern configured set (sk-/XAI/Groq/AWS/Bearer/JWT/Authorization/env-var/PII) before any write; keep the fail-closed catch that blocks content on error |
| T-84-02-02 | Denial of Service | `new RegExp(p.pattern, p.flags)` on config load | mitigate | Compile-guard each pattern in try/catch; skip + `process.stderr.write`; never throw out of loadRedactionPatterns |
| T-84-02-03 | Tampering | LSL caller contract (lsl-file-manager.js) | mitigate | Change is strictly additive; return shape {content,redactionCount,securityLevel} preserved; verified by acceptance grep |
| T-84-02-SC | Tampering | npm installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node --test tests/redaction/config-load.test.mjs` green.
- `grep -c 'RedactionSystem' scripts/enhanced-redaction-system.js` >= 1 (applier module not renamed).
- No new `console.*` outside the pre-existing CLI block.
</verification>

<success_criteria>
The redaction applier loads the 27-pattern config, masks credentials/PII fail-closed, preserves the LSL caller contract exactly, and exports a shared loadRedactionPatterns for the proxy-side raw-body writer.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-02-SUMMARY.md` when done.
</output>
