---
phase: 84-per-turn-context-revelation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/context-turns/anthropic-messages-body.json
  - tests/fixtures/context-turns/openai-complete-body.json
  - tests/fixtures/context-turns/observations-slice.json
  - tests/context-turns/_helpers.mjs
  - tests/context-turns/write-line.test.mjs
  - tests/context-turns/cache-split.test.mjs
  - tests/context-turns/openai-wire.test.mjs
  - tests/context-turns/digest.test.mjs
  - tests/context-turns/close-gzip.test.mjs
  - tests/context-turns/sweeper.test.mjs
  - tests/context-turns/correlate.test.mjs
  - tests/vkb/context-turns-route.test.mjs
  - tests/redaction/config-load.test.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "The Wave-0 test scaffold exists so every downstream task has an <automated> command to satisfy (Nyquist)."
    - "Recorded Anthropic and OpenAI request-body fixtures exist for offline write-line + taxonomy tests."
    - "A small observations fixture exists for the correlation test."
  artifacts:
    - path: "tests/context-turns/_helpers.mjs"
      provides: "Shared test harness (tmp measurements dir, fixture loaders, jsonl reader)"
    - path: "tests/fixtures/context-turns/anthropic-messages-body.json"
      provides: "Recorded /v1/messages request body with cache_control blocks + tool_use"
    - path: "tests/fixtures/context-turns/openai-complete-body.json"
      provides: "Recorded /api/complete internalBody with messages/tools"
    - path: "tests/fixtures/context-turns/observations-slice.json"
      provides: "Observation records (id, summary, agent, project, createdAt) with no task_id"
  key_links:
    - from: "downstream plan tests"
      to: "tests/context-turns/_helpers.mjs"
      via: "import"
      pattern: "from '.*_helpers.mjs'"
---

<objective>
Create the Wave-0 test scaffold and fixtures that every downstream Phase-84 task will satisfy. This closes the RESEARCH "Wave 0 Gaps": no `tests/context-turns/` suite exists yet, and there are no recorded request-body / observation fixtures for offline testing.

Purpose: Guarantee each downstream task has a concrete `<automated>` command (Nyquist rule) instead of `MISSING`.
Output: Fixture JSON files + a shared harness + one failing/skipped stub test per behavior in the RESEARCH test map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-per-turn-context-revelation/84-CONTEXT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record request-body + observation fixtures</name>
  <files>tests/fixtures/context-turns/anthropic-messages-body.json, tests/fixtures/context-turns/openai-complete-body.json, tests/fixtures/context-turns/observations-slice.json</files>
  <read_first>
    - .planning/phases/84-per-turn-context-revelation/84-RESEARCH.md §"Wave 0 Gaps" + §"Research Hand-off #1" (observation record keys VERIFIED: id, summary, agent, project, quality, createdAt, digestedAt, llm, modifiedFiles — NO task_id).
    - ../_work/rapid-llm-proxy/proxy-bridge/server.mjs analyzer at ~1659 `analyzeAnthropicRequest` (to know which request-body shape the taxonomy consumes).
    - .data/observation-export/observations.json (real record shape to mirror in the fixture slice).
  </read_first>
  <action>Author three JSON fixtures under tests/fixtures/context-turns/. (1) `anthropic-messages-body.json`: a realistic `/v1/messages` request body with a top-level `system`, a `tools` array (>=1 tool), and a `messages` array where at least two message blocks carry `cache_control: {type:'ephemeral'}` (so cache-breakpoint indices are non-empty) and at least one message contains a `tool_use` and one a `tool_result` (so tool name+size can be extracted). (2) `openai-complete-body.json`: an `/api/complete` internalBody with `messages`, `tools`, `agent:'copilot'`, and `task_id`. (3) `observations-slice.json`: an array of >=3 observation records mirroring the real export keys (id, summary, agent, project, quality, createdAt ISO, modifiedFiles) with NO task_id, whose `createdAt` timestamps straddle a chosen span window for the correlation test. Keep fixtures small and secret-free.</action>
  <acceptance_criteria>
    - `node -e "const b=require('./tests/fixtures/context-turns/anthropic-messages-body.json'); const n=JSON.stringify(b).match(/cache_control/g)?.length||0; if(n<2) throw new Error('need >=2 cache_control blocks'); if(!/tool_use/.test(JSON.stringify(b))||!/tool_result/.test(JSON.stringify(b))) throw new Error('need tool_use+tool_result')"` exits 0.
    - `node -e "const o=require('./tests/fixtures/context-turns/observations-slice.json'); if(!Array.isArray(o)||o.length<3) throw 0; if(o.some(r=>'task_id' in r)) throw new Error('observations must NOT carry task_id')"` exits 0.
    - `grep -RniE 'sk-|bearer |eyJ' tests/fixtures/context-turns/ | grep -v '^#'` returns no matches (fixtures secret-free).
  </acceptance_criteria>
  <verify><automated>node -e "require('./tests/fixtures/context-turns/anthropic-messages-body.json');require('./tests/fixtures/context-turns/openai-complete-body.json');require('./tests/fixtures/context-turns/observations-slice.json')"</automated></verify>
  <done>Three valid fixture files exist; anthropic body has >=2 cache_control blocks + tool_use + tool_result; observations slice has >=3 records and zero task_id fields; no secret substrings.</done>
</task>

<task type="auto">
  <name>Task 2: Shared harness + failing stub tests for every RESEARCH test-map behavior</name>
  <files>tests/context-turns/_helpers.mjs, tests/context-turns/write-line.test.mjs, tests/context-turns/cache-split.test.mjs, tests/context-turns/openai-wire.test.mjs, tests/context-turns/digest.test.mjs, tests/context-turns/close-gzip.test.mjs, tests/context-turns/sweeper.test.mjs, tests/context-turns/correlate.test.mjs, tests/vkb/context-turns-route.test.mjs, tests/redaction/config-load.test.mjs</files>
  <read_first>
    - .planning/phases/84-per-turn-context-revelation/84-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map" (the 11 behaviors + their exact `node --test` commands).
    - tests/live-logging/ or tests/lsl/ (any existing `*.test.mjs` in this repo) for the local `node --test` house style (import { test } from 'node:test', assert from 'node:assert/strict').
  </read_first>
  <action>Create `tests/context-turns/_helpers.mjs` exporting: `mkTmpMeasurementsDir()` (mkdtemp under os.tmpdir returning a dir + a `withTaskId(id)` path builder), `loadFixture(name)` (reads tests/fixtures/context-turns/<name>), and `readJsonl(path)` (split lines → JSON.parse). Then create one stub test file per RESEARCH test-map row, each with a `test(...)` that is marked `{ skip: 'Wave 0 stub — implemented in 84-0X' }` referencing the plan that will fill it (write-line/cache-split/openai-wire/digest → 84-04; close-gzip/correlate → 84-05; sweeper → 84-03; context-turns-route → 84-07; config-load → 84-02). Each stub imports `_helpers.mjs` and a fixture so the harness is proven wired. Do NOT implement production assertions here — downstream plans un-skip and fill them.</action>
  <acceptance_criteria>
    - `node --test tests/context-turns/ tests/vkb/context-turns-route.test.mjs tests/redaction/config-load.test.mjs` exits 0 with every test reported as skipped (0 failures).
    - `grep -l "_helpers.mjs" tests/context-turns/*.test.mjs | wc -l` returns >= 6 (harness is imported by the stubs).
    - Each stub's skip reason names the downstream plan number: `grep -c "84-0" tests/context-turns/*.test.mjs tests/vkb/context-turns-route.test.mjs tests/redaction/config-load.test.mjs` shows a match in every file.
  </acceptance_criteria>
  <verify><automated>node --test tests/context-turns/ tests/vkb/context-turns-route.test.mjs tests/redaction/config-load.test.mjs</automated></verify>
  <done>Harness `_helpers.mjs` exports the three helpers; ten stub test files exist, all skipped (0 failures), each naming the plan that will implement it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture authoring | Recorded bodies could accidentally embed a real secret from a live request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01-01 | Information Disclosure | recorded request fixtures | mitigate | Task-1 acceptance greps for `sk-`/`bearer`/`eyJ` and fails if present; fixtures are hand-authored, not copied from live traffic |
| T-84-01-SC | Tampering | npm installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit: phase installs nothing) |
</threat_model>

<verification>
- `node --test tests/context-turns/ tests/vkb/context-turns-route.test.mjs tests/redaction/config-load.test.mjs` exits 0 (all skipped).
- Fixtures load and are secret-free.
</verification>

<success_criteria>
Wave-0 scaffold complete: fixtures + harness + one skipped stub per RESEARCH test-map behavior, each pointing at the downstream plan that implements it.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-01-SUMMARY.md` when done.
</output>
