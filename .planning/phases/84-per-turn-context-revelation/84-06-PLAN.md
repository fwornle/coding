---
phase: 84-per-turn-context-revelation
plan: 06
type: execute
wave: 2
depends_on: ["84-02", "84-04"]
files_modified:
  - ../_work/rapid-llm-proxy/proxy-bridge/server.mjs
  - tests/redaction/proxy-raw-body.test.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Full raw request/response bodies are captured only when the per-span flag span.meta.capture_raw_bodies is true (default OFF, D-05)."
    - "When on, raw bodies are written to a SEPARATE raw-bodies.jsonl sibling file so retention can drop them independently (D-05)."
    - "Every raw body is redacted via the 27-pattern configured set BEFORE write, using the shared loadRedactionPatterns loader from Plan 02 (D-06)."
    - "A redaction error blocks that body's content (fail-closed on content) but never crashes the proxy daemon."
    - "raw-bodies.jsonl contains no sk-/Bearer/JWT substrings after capture."
  artifacts:
    - path: "../_work/rapid-llm-proxy/proxy-bridge/server.mjs"
      provides: "Proxy-side redaction applier (shared config) + flag-gated raw-body write at both sites"
      contains: "capture_raw_bodies"
  key_links:
    - from: "proxy raw-body applier"
      to: "<CODING_ROOT>/.specstory/config/redaction-patterns.json"
      via: "loadRedactionPatterns(configPath) at proxy startup"
      pattern: "redaction-patterns\\.json"
    - from: "both write sites"
      to: ".data/measurements/<task_id>/raw-bodies.jsonl"
      via: "flag-gated redacted append"
      pattern: "raw-bodies"
---

<objective>
Add flag-gated full raw request/response body capture at the proxy (D-05), redacted via the project's 27-pattern configured set before write (D-06). Raw bodies go to a SEPARATE `raw-bodies.jsonl` sibling under the same measurements dir so retention drops them independently of the always-on context-turns digest. Default OFF; on only when `span.meta.capture_raw_bodies` is true. This is the phase's primary security surface — redaction is fail-closed on content, never on the daemon.

Purpose: Deep debugging of a measured run when explicitly opted in, without ever leaking credentials to disk.
Output: A proxy-side redaction applier consuming the shared loader + a flag-gated redacted-append at both write sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-PATTERNS.md
@scripts/enhanced-redaction-system.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proxy-side redaction applier consuming the shared 27-pattern loader</name>
  <files>../_work/rapid-llm-proxy/proxy-bridge/server.mjs</files>
  <read_first>
    - scripts/enhanced-redaction-system.js (Plan 02 — the exported `loadRedactionPatterns(configPath)` and the `{content, redactionCount, securityLevel}` + `[REDACTION_ERROR_CONTENT_BLOCKED]` fail-closed contract to replicate).
    - .planning/phases/84-per-turn-context-revelation/84-RESEARCH.md §"Research Hand-off #2" (the proxy is a DIFFERENT repo; the cleanest split is a small self-contained proxy applier that reads `<CODING_ROOT>/.specstory/config/redaction-patterns.json` at startup — the proxy already resolves CODING_ROOT via CTX_BREAKDOWN_DIR at server.mjs:1591; the config JSON is the single source of truth, the compile logic is shared).
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"Redaction (D-06) — one config, shared compile helper".
    - ../_work/rapid-llm-proxy/proxy-bridge/server.mjs ~1591-1594 (CODING_ROOT resolution).
  </read_first>
  <action>At proxy startup, resolve `<CODING_ROOT>/.specstory/config/redaction-patterns.json` (reuse the same CODING_ROOT the proxy already computes for CTX_BREAKDOWN_DIR) and compile the 27-pattern list ONCE using the identical loader logic as Plan 02 — import `loadRedactionPatterns` from the coding module if a cross-repo require resolves, otherwise inline a byte-identical `loadRedactionPatterns` (same field mapping: `{id, re:new RegExp(p.pattern,p.flags), replacement:p.replacement}`, compile-guarded, skip-on-bad-pattern + stderr). Add a proxy `redactRawBody(str)` that applies the compiled patterns in order and returns the redacted string, wrapped in try/catch returning `'[REDACTION_ERROR_CONTENT_BLOCKED]'` on any error (fail-closed on content, never throw). Do NOT reinvent a second pattern set — the config JSON is the single source of truth.</action>
  <acceptance_criteria>
    - `node --check ../_work/rapid-llm-proxy/proxy-bridge/server.mjs` exits 0.
    - `grep -c 'redaction-patterns.json' ../_work/rapid-llm-proxy/proxy-bridge/server.mjs` returns >=1 and `grep -c 'redactRawBody\|loadRedactionPatterns' ../_work/rapid-llm-proxy/proxy-bridge/server.mjs` returns >=1.
    - A focused node check: extracting the proxy `redactRawBody` (or driving it via the test in Task 2) masks `sk-ant-api03-XXXX` and returns `[REDACTION_ERROR_CONTENT_BLOCKED]` when handed a value that forces an error.
  </acceptance_criteria>
  <verify><automated>bash -lc 'node --check ../_work/rapid-llm-proxy/proxy-bridge/server.mjs && grep -q "redaction-patterns.json" ../_work/rapid-llm-proxy/proxy-bridge/server.mjs && grep -Eq "redactRawBody|loadRedactionPatterns" ../_work/rapid-llm-proxy/proxy-bridge/server.mjs'</automated></verify>
  <done>Proxy compiles the 27-pattern config from CODING_ROOT at startup; redactRawBody masks credentials and fail-closes to [REDACTION_ERROR_CONTENT_BLOCKED] on error; one config, no second pattern set.</done>
</task>

<task type="auto">
  <name>Task 2: Flag-gated redacted raw-body append at both write sites + test</name>
  <files>../_work/rapid-llm-proxy/proxy-bridge/server.mjs, tests/redaction/proxy-raw-body.test.mjs</files>
  <read_first>
    - ../_work/rapid-llm-proxy/proxy-bridge/server.mjs the two logContextTurn call sites added in Plan 04 (~2223 /v1/messages, ~2766 /api/complete) — the raw-body write rides alongside them in the same best-effort blocks.
    - .planning/phases/84-per-turn-context-revelation/84-RESEARCH.md §"Runtime State Inventory" (the flag rides span.meta.capture_raw_bodies, read via getActiveMeasurement(), precedent meta.record/meta.replay_from) + §"Assumptions Log" A1.
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"Shared file contract" (raw bodies to raw-bodies.jsonl sibling, NOT into context-turns.jsonl).
  </read_first>
  <action>At both write sites, read the per-span flag from the active measurement (`getActiveMeasurement()?.meta?.capture_raw_bodies === true`; default OFF when absent). When true, build a line `{ ts, task_id, request_id, wire, request: redactRawBody(JSON.stringify(requestBody)), response: redactRawBody(JSON.stringify(responseBodyOrResult)) }` and append it to `path.join(contextTurnsDir(taskId), 'raw-bodies.jsonl')` via the same never-throw `fs.appendFile` wrap as logContextTurn — redaction ALWAYS applied before write. Skip entirely when the flag is off or task_id is empty. Then create `tests/redaction/proxy-raw-body.test.mjs`: given a body containing `sk-ant-api03-SECRET`, a `Bearer tok`, and a JWT, assert the produced raw-bodies line (via the extracted redactRawBody + append helper, or by parsing an appended temp file) contains none of those substrings, and that with the flag OFF no raw-bodies.jsonl is written.</action>
  <acceptance_criteria>
    - `node --check ../_work/rapid-llm-proxy/proxy-bridge/server.mjs` exits 0.
    - `grep -c 'capture_raw_bodies' ../_work/rapid-llm-proxy/proxy-bridge/server.mjs` returns >=1 and `grep -c 'raw-bodies.jsonl' ../_work/rapid-llm-proxy/proxy-bridge/server.mjs` returns >=1.
    - `node --test tests/redaction/proxy-raw-body.test.mjs` exits 0: flag-on line contains no `sk-ant`, `Bearer `, or `eyJ` substring; flag-off writes no raw-bodies.jsonl.
    - Fail-closed: a body that forces a redaction error yields `[REDACTION_ERROR_CONTENT_BLOCKED]` in place of content, and the daemon does not crash (test asserts no throw).
  </acceptance_criteria>
  <verify><automated>node --test tests/redaction/proxy-raw-body.test.mjs</automated></verify>
  <done>Raw bodies are captured only when span.meta.capture_raw_bodies is true, redacted before write to the raw-bodies.jsonl sibling, fail-closed on redaction error, never crashing the daemon; test proves no secret substrings survive and flag-off writes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full raw request/response body → disk | The single highest-value secret-leak surface in the phase |
| span.meta.capture_raw_bodies flag | Must default OFF so a non-consenting span never persists raw bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-06-01 | Information Disclosure | secret leakage into raw-bodies.jsonl | mitigate | 27-pattern redaction (sk-/XAI/Groq/AWS/Bearer/JWT/Authorization/env-var/PII) applied to request AND response before write; acceptance greps assert no secret substrings survive |
| T-84-06-02 | Information Disclosure | raw body persisted for a non-consenting span | mitigate | Per-span flag span.meta.capture_raw_bodies default OFF; separate sibling file so retention drops raw bodies independently (Plan 03 sweeper) |
| T-84-06-03 | Denial of Service | redaction error crashing the tap | mitigate | redactRawBody fail-closed → returns [REDACTION_ERROR_CONTENT_BLOCKED]; append in best-effort try/catch → logErr; never blocks forwarding |
| T-84-06-04 | Tampering | second divergent redaction pattern set | mitigate | Single source of truth: config JSON at <CODING_ROOT>/.specstory/config/redaction-patterns.json; shared compile logic with Plan 02, no independent regex set |
| T-84-06-SC | Tampering | npm installs | accept | No package installs; server.mjs runtime JS, no build |
</threat_model>

<verification>
- `node --test tests/redaction/proxy-raw-body.test.mjs` green.
- `node --check ../_work/rapid-llm-proxy/proxy-bridge/server.mjs` clean.
- Deploy (execution): kickstart proxy after confirming coordinator :3034 location=open; NO npm run build (server.mjs).
</verification>

<success_criteria>
Raw bodies are captured only under the default-OFF per-span flag, redacted via the shared 27-pattern config before write to a separate sibling file, fail-closed on redaction error, never crashing the daemon; no secret substrings survive.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-06-SUMMARY.md` when done.
</output>
