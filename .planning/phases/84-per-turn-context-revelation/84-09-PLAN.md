---
phase: 84-per-turn-context-revelation
plan: 09
type: execute
wave: 4
depends_on: ["84-03", "84-06", "84-07", "84-08"]
files_modified:
  - .planning/phases/84-per-turn-context-revelation/84-LIVE-GATE.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "The proxy is redeployed with the new write hooks (kickstart, no build) after confirming coordinator :3034 location=open."
    - "One LIVE measured span produces a real context-turns.jsonl.gz under .data/measurements/<task_id>/."
    - "GET /api/context-turns (proxy) AND /api/experiments/runs/:taskId/context-turns (vkb via dashboard) return the per-turn array for that live span."
    - "With capture_raw_bodies on for the run, raw-bodies.jsonl.gz exists and contains no sk-/Bearer/JWT substrings after gunzip (redaction verified on live traffic)."
    - "The honest cache explainer renders the live span's per-turn data with N/A for OpenAI-wire (verified visually at :3032)."
  artifacts:
    - path: ".planning/phases/84-per-turn-context-revelation/84-LIVE-GATE.md"
      provides: "Recorded evidence of the live end-to-end run (task_id, file paths, curl output, redaction grep result, screenshot path)"
  key_links:
    - from: "live measured span"
      to: ".data/measurements/<task_id>/context-turns.jsonl.gz"
      via: "proxy write hook → span close gzip"
      pattern: "context-turns.jsonl.gz"
    - from: "curl /api/context-turns"
      to: "the live span's turns"
      via: "read API"
      pattern: "contextTurns"
---

<objective>
Prove the whole pipeline honestly end-to-end (project feedback: never claim "works" from DB/file inspection alone). Redeploy the proxy, run ONE live measured span (with raw-body capture enabled), and verify: a real `context-turns.jsonl.gz` is produced, both read APIs return its turns, `raw-bodies.jsonl.gz` contains no secret substrings after gunzip, and the explainer renders the live data with N/A for OpenAI-wire. Record the evidence.

Purpose: The phase gate — one live span, real files, honest API + redaction + UI evidence.
Output: `84-LIVE-GATE.md` capturing the run evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-per-turn-context-revelation/84-RESEARCH.md
@.planning/phases/84-per-turn-context-revelation/84-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redeploy the proxy + run one live measured span with raw-body capture</name>
  <files>.planning/phases/84-per-turn-context-revelation/84-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/84-per-turn-context-revelation/84-RESEARCH.md §"Runtime State Inventory" + Pitfall 6 (redeploy: confirm coordinator :3034 `location=open` BEFORE `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`; NO npm run build for server.mjs).
    - .planning/phases/83-token-reconciliation-layer/83-CONTEXT.md and the STATE decision [83-07] / [82-06] (the two-cell live-run house style used to produce a measured span with a bound task_id).
    - lib/experiments/experiment-runner.mjs (how a measured cell is launched with a task_id + span.meta).
  </read_first>
  <action>Confirm the coordinator location is `open` (curl :3034 or the documented check) then redeploy the proxy via `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy` (server.mjs is runtime JS — do NOT run npm run build). Launch ONE live measured span (a small 1-cell run in the Phase-82/83 house style) with `capture_raw_bodies: true` set on the run's span.meta so both context-turns AND raw-bodies are exercised. Let the span close (so gzip-at-close fires). Record the resolved `task_id`, the produced file paths under `.data/measurements/<task_id>/`, and the run command in 84-LIVE-GATE.md.</action>
  <acceptance_criteria>
    - `ls .data/measurements/<task_id>/context-turns.jsonl.gz` exists after span close (real .gz, not plaintext — span closed).
    - `ls .data/measurements/<task_id>/raw-bodies.jsonl.gz` exists (capture flag honored).
    - The proxy redeploy was preceded by a coordinator :3034 location=open confirmation (recorded in 84-LIVE-GATE.md).
  </acceptance_criteria>
  <verify><automated>bash -lc 'ls .data/measurements/*/context-turns.jsonl.gz >/dev/null 2>&1'</automated></verify>
  <done>Proxy redeployed safely; one live measured span closed, producing a real context-turns.jsonl.gz + raw-bodies.jsonl.gz; task_id + paths recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Verify both read APIs + redaction on the live artifacts</name>
  <files>.planning/phases/84-per-turn-context-revelation/84-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/84-per-turn-context-revelation/84-PATTERNS.md §"GET /api/context-turns" + §"handleContextTurns" (the two read surfaces).
    - 84-LIVE-GATE.md Task-1 record (the live task_id + paths).
  </read_first>
  <action>Against the live task_id: (1) `curl -s "http://localhost:12435/api/context-turns?task_id=<task_id>"` and confirm the JSON carries a non-empty `contextTurns` array whose lines have the separate `usage.{input,output,cache_read,cache_write}` fields + `wire` discriminator + `messages[].preview`. (2) `curl -s "http://localhost:3032/api/experiments/runs/<task_id>/context-turns"` (vkb via dashboard) and confirm the same turns. (3) Gunzip `raw-bodies.jsonl.gz` and grep for secret substrings: `gunzip -c .data/measurements/<task_id>/raw-bodies.jsonl.gz | grep -Ei 'sk-|bearer |eyJ' | grep -v '<SECRET_REDACTED>'` MUST return no unredacted matches (redaction proven on live traffic). Record all three outputs in 84-LIVE-GATE.md.</action>
  <acceptance_criteria>
    - `curl -s "http://localhost:12435/api/context-turns?task_id=<task_id>"` returns a non-empty `contextTurns` array (proxy read surface).
    - `curl -s "http://localhost:3032/api/experiments/runs/<task_id>/context-turns"` returns the same turns (vkb-via-dashboard surface).
    - `gunzip -c .data/measurements/<task_id>/raw-bodies.jsonl.gz | grep -Ei 'sk-[a-z0-9]|bearer [a-z0-9]|eyJ'` returns no unredacted secret substrings (only `<...REDACTED...>` placeholders permitted).
  </acceptance_criteria>
  <verify><automated>bash -lc 'test -f .planning/phases/84-per-turn-context-revelation/84-LIVE-GATE.md && { n=0; for f in .data/measurements/*/raw-bodies.jsonl.gz; do [ -e "$f" ] || continue; c=$(gunzip -c "$f" | grep -Eic "sk-[a-z0-9]{6,}|bearer [a-z0-9]{6,}|eyJ[a-z0-9]"); n=$((n+c)); done; test "$n" -eq 0; }'</automated></verify>
  <done>Both read APIs return the live span's turns with the honest cache split; the live raw-bodies.jsonl.gz contains no unredacted secrets; evidence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-gate visual confirmation of the honest explainer on live data</name>
  <what-built>The full pipeline ran live: a measured span produced a real context-turns.jsonl.gz + redacted raw-bodies.jsonl.gz, both read APIs serve the turns, and the cache explainer renders the live span's per-turn data honestly.</what-built>
  <action>Pause for the phase-gate human confirmation that the live measured span renders honestly end-to-end. Verify against the ACTUAL live `<task_id>` recorded in 84-LIVE-GATE.md: the real `.gz` artifacts on disk, both read APIs returning the turns, the redaction grep showing no unredacted secrets, and the gsd-browser screenshot of the explainer — before resuming.</action>
  <how-to-verify>
    1. Open the Performance tab at http://localhost:3032 for the live `<task_id>` (from 84-LIVE-GATE.md).
    2. Capture a screenshot via the gsd-browser CLI (project rule — never a hand-rolled node playwright script): `gsd-browser navigate` then `gsd-browser screenshot --output <path>`; record the path in 84-LIVE-GATE.md.
    3. Confirm the explainer shows the live per-turn sent/cached/fresh (real numbers), "N/A (provider reports no cache-creation)" for any OpenAI-wire turn, and the "how prompt caching actually works" copy — for the ACTUAL live span, not a fixture.
    4. Sanity-check the redaction grep output and the two curl outputs recorded in 84-LIVE-GATE.md.
  </how-to-verify>
  <resume-signal>Type "approved" if the live span renders honestly end-to-end (files + APIs + redaction + explainer), or describe the gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live LLM traffic → raw-bodies.jsonl.gz | Real credentials in live requests must be redacted before disk |
| proxy redeploy | A kickstart can mis-detect corporate routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-09-01 | Information Disclosure | live secrets in raw-bodies | mitigate | Task-2 gunzips the live .gz and greps for unredacted sk-/Bearer/JWT; gate fails if any survive |
| T-84-09-02 | Tampering | proxy kickstart corporate mis-detect | mitigate | Confirm coordinator :3034 location=open BEFORE kickstart (Pitfall 6), recorded as evidence |
| T-84-09-SC | Tampering | npm installs | accept | No package installs; proxy is runtime JS (no build) |
</threat_model>

<verification>
- Real `.gz` files exist under `.data/measurements/<task_id>/`.
- Both read APIs return the turns; live raw-bodies has no unredacted secrets.
- gsd-browser screenshot confirms honest explainer on live data (Task 3 human-verify).
</verification>

<success_criteria>
One live measured span proves the pipeline end-to-end: real context-turns.jsonl.gz, both read APIs serving the turns, redaction verified on live raw bodies, and the honest explainer rendering the live data at :3032.
</success_criteria>

<output>
Create `.planning/phases/84-per-turn-context-revelation/84-09-SUMMARY.md` when done.
</output>
