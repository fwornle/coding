# CodingPatterns

**Type:** Component

[LLM] The most concrete evidence of a project-wide pattern convention is the suffix-based naming scheme visible across component names like GraphDatabaseAdapter, TranscriptAdapter, SpecstoryAdapter, ConnectionManager, and LLMServiceProvider. This suggests a Java/C#-style layered convention has been adopted in what is otherwise a Node.js/TypeScript codebase (per docs/architecture/agent-abstraction-api.md and docs/architecture/adding-new-agent.md), where *Adapter classes wrap external systems (graph databases, transcript sources, Specstory format) behind a uniform interface, *Manager classes own lifecycle/state (e.g., connection pooling), and *Service/*Provider classes expose higher-level operations (e.g., LLM routing per docs/architecture/llm-routing.md). Any new component added to the system is expected to conform to one of these suffixes so that its role is discoverable from its name alone, which is a lightweight but effective form of self-documentation given the project's scale (1982 docs, 17280 code references).

# CodingPatterns: Technical Insight Document

## What It Is

CodingPatterns is not a component in the conventional sense — it has no dedicated source directory, module, or file (there is no `CodingPatterns.ts` or `patterns/` directory to find). Instead, it is a conceptual grouping label applied during architecture analysis of the Coding project to capture naming and structural conventions that recur across many subsystems but are not owned by any single one. As the root Coding project's documentation states, this component exists to catch cross-cutting idioms — visible in class names like `GraphDatabaseAdapter`, `TranscriptAdapter`, `SpecstoryAdapter`, `ConnectionManager`, and `LLMServiceProvider` — that would otherwise fall through the cracks between subsystem-specific docs like `docs/architecture/memory-systems.md` or `docs/architecture/llm-routing.md`.

Because CodingPatterns has no browsable codebase location, onboarding should redirect toward `CLAUDE.md` (the "Coding Project Guidelines") and the `docs/architecture/` directory rather than expecting to find implementation files. The actual pattern instances live scattered inside real subsystems — wave agents, adapters, managers — and are only retroactively categorized as CodingPatterns during synthesis.

![CodingPatterns — Architecture](images/coding-patterns-architecture.png)

## Architecture and Design

The clearest architectural signal is a suffix-based naming convention layered onto an otherwise Node.js/TypeScript codebase, evoking Java/C#-style typing-by-role: `*Adapter` classes (e.g., `TranscriptAdapter`, `SpecstoryAdapter`, `GraphDatabaseAdapter`) wrap external systems behind uniform interfaces; `*Manager` classes (e.g., `ConnectionManager`) own lifecycle and state; and `*Service`/`*Provider` classes (e.g., `LLMServiceProvider`) expose higher-level operations, consistent with `docs/architecture/llm-routing.md`. This lightweight, name-driven self-documentation strategy scales reasonably well given the project's size (1982 docs, 17280 code references), since a developer can infer a class's architectural role without reading its implementation.

This naming-driven classification is formalized in the child component NamingConventions, which explicitly classifies by suffix pattern rather than directory location — meaning classification is name-driven, not structure-driven. The other two children, AdapterPattern and AgentLifecyclePattern, represent concrete instantiations of this broader convention: AdapterPattern captures the `TranscriptAdapter`/`SpecstoryAdapter` external-format-to-internal-shape conversion idiom, while AgentLifecyclePattern captures a "potentially used" construct/lazy-init/execute shape inferred from wave-agent naming similarity rather than a confirmed shared interface or base class.

A second, distinct pattern category also surfaces: process-control/workflow-gating conventions, hinted at by environment identifiers like `RUNG_OFFLOADED`, `GATES`, `QWEN_LAPTOP_API_BASE_URL`, and `LLM_PROXY_URL`. This suggests a runtime decision-pattern for routing/offloading computation between local and remote LLM proxies, likely implemented as conditional dispatch within the LLM routing subsystem documented in `docs/architecture/llm-routing.md`.

![CodingPatterns — Relationship](images/coding-patterns-relationship.png)

## Implementation Details

Since no source files were found for CodingPatterns itself, all implementation claims are inferences from naming and documentation-index entries rather than confirmed class or function bodies. The suffix taxonomy (Adapter/Manager/Service/Provider) is the strongest evidence, cross-referenced against `docs/architecture/agent-abstraction-api.md` and `docs/architecture/adding-new-agent.md`. Environment-configuration naming also follows a suffix convention — `*_PROXY_URL` and `*_API_KEY` (e.g., `LLM_CLI_PROXY_URL`, `RAPID_LLM_PROXY_URL`, `QWEN_LOCAL_API_KEY`) — mirroring the class-naming discipline at the configuration layer, likely documented in `CLAUDE.md` alongside error-handling idioms like `ERR_MODULE_NOT_FOUND`.

The top-level `docs/architecture-report.md` and `docs/architecture/README.md` function as aggregation/index documents rather than deep-dives, implying any "pattern catalog" for CodingPatterns is more likely a short "Conventions" or "Design Principles" subsection within `architecture-report.md` than a standalone page. Developers should grep these files for "pattern," "convention," or "idiom" rather than expecting a `docs/patterns/` directory.

## Integration Points

CodingPatterns sits under the root Coding component alongside six siblings — LiveLoggingSystem, LLMAbstraction, DockerizedServices, KnowledgeManagement, ConstraintSystem, and SemanticAnalysis — each of which likely exhibits instances of the naming and lifecycle conventions this component catalogs (e.g., DockerizedServices' `ProcessStateManager`, KnowledgeManagement's VKB server, ConstraintSystem's `UnifiedHookManager` all follow manager/service-style naming). Its gating/offload hypothesis connects most directly to LLMAbstraction's documented mode-resolution hierarchy (`getLLMMode()` in `llm-mock-service.ts`), suggesting a shared vocabulary between per-agent override logic and the `RUNG_OFFLOADED`/`GATES` routing concepts.

Its three children — NamingConventions, AgentLifecyclePattern, and AdapterPattern — are the concrete, decomposed facets of this catch-all grouping, each meant to be validated independently against real source files (e.g., an actual `WaveAgent.ts`) rather than trusted as confirmed fact.

## Usage Guidelines

Treat CodingPatterns' documentation as a hypothesis, not a verified specification — it was synthesized with zero representative source files, so any claims about lifecycle patterns or adapter conventions must be validated against `CLAUDE.md` and real implementation files before being used in code review or refactoring decisions. When adding new components, conform to the established suffix taxonomy (`*Adapter`, `*Manager`, `*Service`/`*Provider`) so the component's role remains discoverable from its name alone. When searching for pattern documentation, start with `CLAUDE.md` and `docs/architecture/README.md`/`architecture-report.md` rather than expecting a dedicated patterns directory. Finally, cross-reference any offload/gating logic discoveries against `docs/architecture/llm-routing.md` and LLMAbstraction's mode-resolution hierarchy, since these two areas are the most likely eventual home for concrete source evidence.


## Hierarchy Context

### Parent
- [Coding](./Coding.md) -- Root node of the coding project knowledge hierarchy, encompassing all development infrastructure knowledge. The project consists of 7 major components: LiveLoggingSystem: [LLM] The LiveLoggingSystem is structured as a pipeline with distinct, separable stages: configuration validation, session windowing, file routing, cl; LLMAbstraction: [LLM] The LLMAbstraction component implements a mode-resolution hierarchy that is critical for understanding how any given agent call is actually disp; DockerizedServices: DockerizedServices provides the containerization and process-management layer that wraps Coding's various services (semantic analysis MCP, constraint ; KnowledgeManagement: [LLM] The KnowledgeManagement component centers on a graph-based knowledge storage architecture (documented in docs/architecture/memory-systems.md) th; CodingPatterns: [LLM] CodingPatterns has no dedicated source directory or module of its own within the Coding project; it exists purely as a conceptual grouping label; ConstraintSystem: The ConstraintSystem provides rule-based validation and enforcement for tool calls and file operations during Claude Code sessions, plus a hook infras; SemanticAnalysis: [LLM] The batch-analysis workflow is structured as a multi-stage pipeline where semantic-analysis-agent.ts is responsible for extracting raw entities .

### Children
- [NamingConventions](./NamingConventions.md) -- The convention classifies classes by suffix pattern (e.g., *Manager, *Service, *Adapter, *Provider) rather than by directory location, meaning classification is name-driven not structure-driven
- [AgentLifecyclePattern](./AgentLifecyclePattern.md) -- The pattern is described as 'potentially used' by wave/agent components, indicating it was inferred from repeated structural similarity rather than a shared interface or abstract class
- [AdapterPattern](./AdapterPattern.md) -- TranscriptAdapter and SpecstoryAdapter are cited as concrete examples of this pattern, both converting external format-specific data into a common internal shape

### Siblings
- [LiveLoggingSystem](./LiveLoggingSystem.md) -- [LLM] The LiveLoggingSystem is structured as a pipeline with distinct, separable stages: configuration validation, session windowing, file routing, classification, and transcript capture. This separation is evident in the presence of scripts/validate-lsl-config.js as a standalone pre-flight check that runs independently of the main logging.ts pipeline, meaning developers can validate configuration changes (session window durations, file routing rules) without triggering the full logging pipeline. This design supports fail-fast behavior: catching misconfiguration before any live session data is captured, rather than discovering issues mid-capture when data loss or malformed logs could occur.
- [LLMAbstraction](./LLMAbstraction.md) -- [LLM] The LLMAbstraction component implements a mode-resolution hierarchy that is critical for understanding how any given agent call is actually dispatched. getLLMMode() in llm-mock-service.ts checks, in strict order: a per-agent override (allowing individual agents to be pinned to mock/local/public independently of global state), then a global mode setting, then a legacy mockLLM boolean flag (retained for backward compatibility with older config schemas), and finally falls back to 'public' as the safe default. This layered precedence means a developer debugging unexpected LLM behavior for a specific agent must check all four levels rather than assuming the global setting applies uniformly—per-agent overrides silently win even if the global mode says otherwise, which is a common source of confusion during multi-agent experiments.
- [DockerizedServices](./DockerizedServices.md) -- DockerizedServices provides the containerization and process-management layer that wraps Coding's various services (semantic analysis MCP, constraint monitor API/dashboard, graphify, LLM services) so they can run reliably both inside Docker containers and as standalone Node processes managed by a Global Service Coordinator. The layer combines Docker artifacts (docker-compose.yml, Dockerfile.coding-services, supervisord.conf, entrypoint.sh) with a set of Node.js wrapper scripts (api-service.js, dashboard-service.js) that spawn actual backend processes, forward signals, and register/unregister with a ProcessStateManager (PSM) for lifecycle tracking.

A core architectural pattern is robust startup with retry/backoff and health verification, implemented in lib/service-starter.js's startServiceWithRetry(), which wraps a start function and a health-check function with timeouts (via withDeadline) and exponential backoff, distinguishing required vs optional services for graceful degradation. Complementing this, lib/utils/service-probe.js implements liveness probes (probeHttpHealth, probeTcpPort) used by scripts/health-coordinator.js to poll services every 5 seconds per config/health-verification-rules.json, strictly avoiding false-positive 'healthy' states per its SPEC R6 invariant.

Service wrappers such as api-service.js and dashboard-service.js follow a consistent pattern: resolve CODING_REPO-relative paths, verify target files/directories exist, spawn the real process with stdio inherited, forward SIGTERM/SIGINT, and asynchronously register/unregister with ProcessStateManager for centralized process tracking across the dockerized/global service fleet. Mock-mode support (llm-mock-service.ts) allows service behavior (LLM calls) to be swapped for deterministic mocks driven by a shared workflow-progress.json state file, aiding testing inside containers where CODING_ROOT may differ from host paths.
- [KnowledgeManagement](./KnowledgeManagement.md) -- [LLM] The KnowledgeManagement component centers on a graph-based knowledge storage architecture (documented in docs/architecture/memory-systems.md) that replaced or augments a prior LevelDB-based store. The VKB (Virtual Knowledge Base) server acts as the primary runtime interface for querying and mutating the graph, exposing entity persistence operations that downstream agents (like code-graph-agent.ts) rely on rather than talking to the storage layer directly. This separation of concerns means the VKB server is the single choke point for consistency guarantees, caching, and decay tracking logic, so any schema change to entities must be coordinated through it rather than through ad-hoc script access.
- [ConstraintSystem](./ConstraintSystem.md) -- The ConstraintSystem provides rule-based validation and enforcement for tool calls and file operations during Claude Code sessions, plus a hook infrastructure that lets agent-agnostic events (pre-tool, post-tool, startup, shutdown, etc.) trigger scripts, commands, or modules. It is composed of a hook configuration layer (lib/agent-api/hooks/hook-config.js) that loads and merges user-level (~/.coding-tools/hooks.json) and project-level (.coding/hooks.json) configs, and a central UnifiedHookManager (lib/agent-api/hooks/hook-manager.js) that registers, prioritizes, and dispatches handlers per event across all supported agents. Violations detected during live sessions are captured and persisted via the ViolationCaptureService (scripts/violation-capture-service.js), which writes JSONL logs and maintains rolling statistics for dashboard consumption.

Architecturally, the system favors a layered configuration-merge pattern (defaults → user → project) with validation warnings rather than hard failures, and a priority-sorted handler registry keyed by event name. Constraint violations flow from live session monitoring into a persistent JSON store capped at 1000 entries, with statistics (severity breakdown, most common violation, per-session averages) computed on write. The system also integrates with content/documentation validation (ContentValidationAgent in integrations/semantic-analysis) which, while primarily about entity staleness, shares the same validation-issue/severity taxonomy pattern used elsewhere in constraint reporting, and with the System Health Dashboard's workflow visualization hooks (React-side, distinct from the agent-api hook system) for rendering constraint/violation data.
- [SemanticAnalysis](./SemanticAnalysis.md) -- [LLM] The batch-analysis workflow is structured as a multi-stage pipeline where semantic-analysis-agent.ts is responsible for extracting raw entities and relationships from git history and LSL (likely 'Language/Session Log') session data, while ontology-classification-agent.ts takes those extracted entities and maps them into a hierarchical ontology structure. This separation of concerns means the extraction logic (identifying what something is—a function, a decision, a bug fix) is decoupled from the classification logic (determining where that entity fits in a broader knowledge hierarchy), allowing each agent to be independently tuned, tested, and potentially swapped out without affecting the other stage's implementation.


---

*Generated from 6 observations*
