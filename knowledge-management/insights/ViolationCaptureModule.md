# ViolationCaptureModule

**Type:** SubComponent

ViolationCaptureModule may utilize a data processing mechanism, such as a data processor module, to process and transform captured constraint violations for dashboard display.

## What It Is  

The **ViolationCaptureModule** is a sub‑component of the **ConstraintSystem** that is responsible for intercepting, persisting, and optionally notifying stakeholders about constraint‑violation events generated by the agent.  The module lives inside the same repository as the rest of the constraint‑monitoring stack and is wired to the central **UnifiedHookManager** located at `lib/agent-api/hooks/hook-manager.js`.  When the `UnifiedHookManager` dispatches a *violation* event, the ViolationCaptureModule’s handler—registered through the manager’s `registerHandler` API—is invoked.  From there the module records the violation (e.g., into a database or file), runs any required transformation for dashboard consumption, emits notifications, and writes diagnostic entries to the shared logger.  Configuration for the module is supplied via a dedicated YAML file, typically named `violation-capture.yaml`, which allows operators to tune capture thresholds, storage back‑ends, and notification channels without changing code.

---

## Architecture and Design  

The overall architecture of the **ConstraintSystem** is deliberately modular.  The **UnifiedHookManager** acts as a *central hub* for hook registration, using a `Map` keyed by event name to store handler functions.  This design follows an **observer‑style** pattern: producers (the constraint engine) emit events, while consumers such as **ViolationCaptureModule**, **ConstraintMonitor**, and **HookConfigurationManager** subscribe through the manager.  The ViolationCaptureModule therefore does not need direct knowledge of the constraint engine’s internals; it only needs to know the event name and the signature of the handler payload.

The module’s internal responsibilities are split into three loosely coupled concerns:

1. **Capture & Persistence** – a storage abstraction (e.g., a database client or file writer) that writes each violation record.  Because the observation only says “likely implements a data storage mechanism,” the concrete class is not named, but the design anticipates a pluggable repository interface.
2. **Processing & Transformation** – a data‑processor component that reshapes raw violation data into a format suitable for the UI dashboard.  This keeps the capture path lightweight while delegating heavy‑weight enrichment to a separate module.
3. **Notification** – an optional notifier that forwards critical violations to users or administrators, possibly via email, Slack, or another alerting channel.

All three concerns share the **logger module** for audit trails, and they are driven by settings read from `violation-capture.yaml`.  By keeping configuration external, the module can be re‑configured at runtime (or on restart) without recompilation, reinforcing the system’s **customizability**.

Because the sibling **ConstraintMonitor** also registers a handler for the same event via the same `UnifiedHookManager`, both modules coexist without stepping on each other: the manager’s `Map` permits multiple handlers per event, and the order of invocation is deterministic (typically registration order).  This shared hook infrastructure is the primary integration point that unifies the parent, sibling, and child components.

---

## Implementation Details  

*Hook Registration* – The ViolationCaptureModule imports the manager from `lib/agent-api/hooks/hook-manager.js` and calls `registerHandler('constraintViolation', this.handleViolation.bind(this))`.  The `registerHandler` function, as described in the parent component’s documentation, stores the handler in a `Map` keyed by `'constraintViolation'`.  This enables fast O(1) lookup when the manager later dispatches the event.

*Handler Logic* – `handleViolation(eventPayload)` is the entry point for every captured violation.  Inside this function the module typically performs the following steps:

1. **Logging** – Calls the shared logger (`logger.info('Violation captured', eventPayload)`) to create an immutable audit record.
2. **Persistence** – Delegates to a storage adapter (`this.storage.save(eventPayload)`).  The adapter abstracts the concrete back‑end (SQL, NoSQL, or flat file) and can be swapped by changing the configuration in `violation-capture.yaml`.
3. **Processing** – Passes the raw payload to a processor (`this.processor.transform(eventPayload)`) that enriches the data (e.g., adds timestamps, severity levels, or correlates with asset metadata) before it is persisted for dashboard consumption.
4. **Notification** – If the configuration flags the violation as “critical,” the handler invokes the notifier (`this.notifier.send(alert)`) which may push alerts to external systems.

*Configuration* – The module reads `violation-capture.yaml` at startup (or on reload) using a lightweight YAML parser.  The file defines keys such as `storage.type`, `processor.enabled`, `notifier.channels`, and `logging.level`.  Because the configuration is external, developers can add new storage back‑ends or notification channels simply by extending the YAML schema and providing the corresponding adapter implementation.

*Logging Integration* – The logger module is shared across the entire ConstraintSystem.  By adhering to the same logging interface, ViolationCaptureModule’s log entries are automatically aggregated with those from **ConstraintMonitor** and **HookConfigurationManager**, simplifying operational troubleshooting.

---

## Integration Points  

1. **UnifiedHookManager (`lib/agent-api/hooks/hook-manager.js`)** – The sole entry point for event subscription.  ViolationCaptureModule registers its handler here and receives the same event payload that **ConstraintMonitor** and any other hook consumer receive.
2. **ConstraintSystem (parent)** – Provides the overall lifecycle (initialization, shutdown) and ensures that the hook manager is instantiated before any sub‑components, including ViolationCaptureModule, attempt registration.
3. **Storage Adapter** – An internal dependency that may be a database client (e.g., `pg` for PostgreSQL) or a file writer.  The exact class name is not listed in the observations, but the adapter must implement a `save` method compatible with the payload.
4. **Data Processor Module** – A separate module that the ViolationCaptureModule calls to transform raw violations.  Its interface is expected to expose a `transform` function.
5. **Notification Module** – Optional; invoked when configuration flags a violation as needing user or admin attention.  The notifier must expose a `send` method.
6. **Logger Module** – Shared logging facility used for audit trails.  The module writes to the same log streams as its siblings, ensuring consistent observability.
7. **Configuration File (`violation-capture.yaml`)** – External YAML file that drives the module’s runtime behavior.  It is read by the module at startup and may be reloaded on configuration change events.

These integration points keep the ViolationCaptureModule loosely coupled: it only depends on well‑defined interfaces (hook manager, storage, processor, notifier, logger) and never directly contacts the constraint evaluation engine.

---

## Usage Guidelines  

*Register Early* – Ensure that the ViolationCaptureModule’s registration call (`registerHandler`) occurs **before** the constraint engine begins emitting violation events.  Typically this is handled automatically during the ConstraintSystem bootstrap, but custom deployments should respect the initialization order.

*Configuration First* – Populate `violation-capture.yaml` with the desired storage type, processing flags, and notification channels **prior** to starting the service.  Missing or malformed configuration may cause the module to fall back to a no‑op storage adapter, leading to lost violation data.

*Idempotent Handlers* – The handler should be written to tolerate duplicate invocations of the same violation (e.g., if the hook manager retries).  Persisted records should include a unique identifier (such as a hash of the violation payload) to avoid duplicate entries.

*Logging Levels* – Align the module’s logging level with the global logger configuration to avoid excessive log noise.  Use `logger.debug` for payload inspection and `logger.info` for successful captures; reserve `logger.error` for storage or notification failures.

*Extending Storage or Notification* – When adding a new back‑end (e.g., a cloud object store) or a new notification channel (e.g., PagerDuty), implement the corresponding adapter adhering to the existing `save` or `send` signatures and reference it in `violation-capture.yaml`.  No changes to the core ViolationCaptureModule code are required, thanks to the configuration‑driven design.

---

### Architectural Patterns Identified  

1. **Observer / Publish‑Subscribe** – via `UnifiedHookManager`’s handler registration and event dispatch.  
2. **Strategy (Pluggable Adapters)** – storage, processor, and notifier are interchangeable implementations selected through configuration.  
3. **Configuration‑Driven Design** – behavior controlled by `violation-capture.yaml`.  

### Design Decisions and Trade‑offs  

*Decision*: Centralize hook management in a single `UnifiedHookManager` rather than scattering event listeners throughout the codebase.  
*Trade‑off*: Introduces a single point of failure; however, the manager’s simple `Map`‑based storage is lightweight and well‑tested.

*Decision*: Externalize persistence, processing, and notification behind adapters.  
*Trade‑off*: Adds indirection and requires disciplined interface contracts, but dramatically improves extensibility and testability.

*Decision*: Use a YAML file for configuration.  
*Trade‑off*: Human‑readable and flexible, but parsing errors must be handled gracefully at startup.

### System Structure Insights  

- **ConstraintSystem** (parent) → **UnifiedHookManager** (shared hub) → **ViolationCaptureModule**, **ConstraintMonitor**, **HookConfigurationManager** (siblings).  
- ViolationCaptureModule’s internal pipeline: **Handler → Logger → Processor → Storage → Notifier** (optional).  
- All sub‑components share the same logger, reinforcing a unified observability surface.

### Scalability Considerations  

- The `Map` used by `UnifiedHookManager` provides O(1) handler lookup, supporting a large number of registered listeners without performance degradation.  
- Storage adapters can be swapped for horizontally scalable back‑ends (e.g., distributed databases) without code changes, allowing the capture pipeline to handle increased violation volume.  
- Processing can be off‑loaded to asynchronous workers if the transformation step becomes CPU‑intensive, though this would require extending the processor interface.

### Maintainability Assessment  

The module’s **modular** and **configuration‑driven** nature yields high maintainability:

- **Low Coupling** – Interaction is limited to well‑defined interfaces (hook manager, storage, processor, notifier).  
- **High Cohesion** – All logic related to violation capture resides within a single sub‑component, making the codebase easier to reason about.  
- **Extensibility** – Adding new storage back‑ends or notification channels requires only new adapter implementations and configuration updates.  
- **Observability** – Consistent logging across the parent and sibling components aids debugging and reduces the learning curve for new developers.

Overall, the ViolationCaptureModule exemplifies a clean, extensible design that aligns with the broader modular philosophy of the ConstraintSystem.


## Hierarchy Context

### Parent
- [ConstraintSystem](./ConstraintSystem.md) -- The ConstraintSystem component's architecture is designed with flexibility and customizability in mind, utilizing a modular design that allows for easy extension and modification. This is evident in the use of the UnifiedHookManager (lib/agent-api/hooks/hook-manager.js), which provides a central hub for hook management, handling hook event dispatch, handler registration, and configuration loading. The UnifiedHookManager uses a Map to store handlers for each event, allowing for efficient registration and retrieval of handlers. For example, the registerHandler function in hook-manager.js takes in an event name and a handler function, and stores them in the handlers Map for later retrieval.

### Siblings
- [ConstraintMonitor](./ConstraintMonitor.md) -- ConstraintMonitor uses the UnifiedHookManager (lib/agent-api/hooks/hook-manager.js) to register handlers for constraint violation events.
- [HookConfigurationManager](./HookConfigurationManager.md) -- HookConfigurationManager uses the UnifiedHookManager (lib/agent-api/hooks/hook-manager.js) to load and merge hook configurations.
- [UnifiedHookManager](./UnifiedHookManager.md) -- UnifiedHookManager uses a Map to store handlers for each event, allowing for efficient registration and retrieval of handlers.


---

*Generated from 6 observations*
